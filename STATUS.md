# Status

**Phase 4 — Checker buildout.** 9,780 / 10,086 tests passing (~97.0%, 306 failing). _Round 207 (2026-06-20, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+1 net (307→306 failing, 9779→9780 passing), B505 (`extension`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** Re-confirmed the contained pool exhausted FOUR independent ways: find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; NONE fresh=7 (all deep type-engine: TS2344/TS2589 mapped/conditional recursion, indexed-access keyof, promisePermutations, controlFlowArray, reactReduxLikeDeferredInference); OUTPUT fresh=1 (systemModule17, tsgo-irrelevant); AND an 18-agent triage Workflow over the 18 smallest-diff partial-emit failures returned **0 clean-single-piece, 0 two-piece — 100% architectural/multi-piece** (mergeSymbolReexportInterface cross-file-merge-order, allowJscheckJsTypeParameterNoCrash needs Record materialization+contextual-typing, unicodeIdentifierName2 needs isInSomeParsingContext infra, the TS-1 cluster is a tsc test-harness two-pass artifact, etc.). The 166 partial-emit failures were ranked by real diff size — smallest are all multi-piece. **B505 (+1, `extension`)** — `declare namespace M { export extension class C {} }` flipped via THREE coordinated pieces (a real multi-piece blocker flip): (1) Parser TS1128 at the dangling `export` keyword for the `export <true-Identifier> <decl-keyword>` bogus-modifier shape — the `token == SyntaxKind.Identifier` gate (NOT `isIdentifier()`) is load-bearing: `isIdentifier()` matched the UMD `export as namespace X` form (`as` is AsKeyword) and regressed 9 UMD/global tests on the first attempt; (2) TS1036 ("Statements not allowed in ambient contexts") suppressed when the file has parse diagnostics (tsc grammarErrorOnFirstToken+hasParseDiagnostics) — pass-count-safe (`giant` is the only other TS1036+parse-code test and already fails); (3) Binder `declareSymbol` first-wins for a Class+Class duplicate (tsc keeps the FIRST class canonical → `c.pe` errors, not `c.p`) — TS2300 is AST-based so unaffected, symbol-id allocation unchanged (no boundary drift), scoped to Class+Class only. NEXT: contained pool exhausted; remaining 306 are deep blockers (B481 generic-inference, B482 weak-type, B486 override-chain, cross-file merge-order, symbol-index TS2411) + finicky parser-recovery (dottedModuleName/unicodeIdentifierName2 need isInSomeParsingContext) + tsc-harness TS-1 artifacts._ _Round 206 (2026-06-20, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+1 net so far (308→307 failing, 9778→9779 passing), B504 (`nestedObjectRest`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** Confirmed contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; NONE fresh=8 all deep type-engine; OUTPUT fresh=1 tsgo-irrelevant). Launched a 243-agent corpus-mining Workflow over all errors-only failures (background) hunting the corpus-unique-dedicated-walker vein. **B504 (+1, `nestedObjectRest`)** — object-rest in a destructuring-ASSIGNMENT target nested inside an array pattern (`[{ ...x }] = expr`, and the `for ([{ ...y }] of …)` variant) was wrongly emitted as an object-literal spread (`Object.assign`). tsc flattens via temps + `__rest`: `[_a] = [{abc:1}], x = __rest(_a, [])`; the for-of introduces a `let` loop var with the destructure moved into the body. New `targetHasNestedObjectRest` detector + `flattenRestAssignmentTarget` flattener, hooked into `transformBinaryExpression` (array LHS) and `transformForOfStatement` (array-pattern initializer). Corpus-UNIQUE shape (grep: 1 file). The for-of allocates the inner-destructure temp BEFORE the loop var (placeholder-then-patch). NEXT: triage mining-Workflow output + remaining OUTPUT emitter gaps + blocker substeps._ _Round 205 (2026-06-20, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+3 net (311→308 failing, 9775→9778 passing), B501 (`accessorInAmbientContextES5`) + B502 (`tslibReExportHelpers2`) + B503 (`namespaceMergedWithImportAliasNoCrash`), ZERO regressions at each step (exact failing-set diff).** B503 — a namespace import `import * as X from "m"` of a module whose exports are ALL type-only (interfaces/type-aliases/uninstantiated namespaces) is itself type-only (empty runtime module object) → tsc elides the require even when `X` is also a runtime value via a same-name merged local namespace; new conservative checker query `moduleHasOnlyTypeOnlyExports` (false on any uncertainty), hooked at the CJS namespace-import emission, gated `!isolatedModules`. Re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; NONE fresh=8 all deep type-engine: mapped/conditional recursion TS2344/TS2589, promise-permutations, indexed-access keyof, control-flow array). Mined the OUTPUT (JS-emit) bucket (48 failing JS-emit tests) — both wins are class-transformer/private-field emitter features reusing the B499/B500 machinery. **B501 (+1, `accessorInAmbientContextES5`)** — PUBLIC instance `accessor x` (non-decorator, target < ES2022) downlevel via a desugar PRE-PASS at the top of `transformClassBody` (`membersIn` → expanded `members`): `accessor x` → private storage field `#x_accessor_storage` + public `get x()`/`set x(v)`, flowing through the existing private-field WeakMap downlevel (constructor `.set(this, void 0)`, `__classPrivateFieldGet/Set(...,"f")` bodies, trailing `= new WeakMap()`). **B502 (+1, `tslibReExportHelpers2`)** — STATIC private METHOD (`static #m()`) downlevel in a class DECLARATION: NO brand WeakSet, the class-alias temp (`classTempVar`, force-allocated early via `staticMethodAlias`) is both receiver and state → `Class.#m()` → `__classPrivateFieldGet(_a, _a, "m", _Foo_m).call(_a)` (new `PrivateEnvEntry.isStatic` flag drives the call rewrite's receiver), method fn vars emit in the same trailing comma list as `_a = Foo`. Gated `!useDefineForClassFields`. Triaged the rest of the OUTPUT bucket exhaustively — all parser-recovery (es6Import*, parse*, errorRecovery*, superAccess2, bigintArbitraryIdentifier, constructorWithIncompleteTypeAnnotation), file-emit-order (emitMemberAccessExpression/visibilityOfCrossModuleTypeUsage — topologicalSort minefield), import-elision-merge (namespaceMergedWithImportAliasNoCrash), default-target (classPropertyInferenceFromBroaderTypeConst), allowJs-multifile (elidedJSImport2), comment-preservation (propertyAccess/elementAccess InnerComments — full "comments at every token gap" feature), file-emit-selection (moduleResolutionWithRequire/importInsideModule — uncertain rule, risky), BOM/UMD/AMD (niche/tsgo-irrelevant). B481 generic-inference targets (thislessFunctionsNotContextSensitive1, destructuringTuple) re-confirmed deep multi-piece. NEXT: contained pool exhausted; remaining 309 are deep blockers + finicky parser-recovery/emitter; the static-private-FIELD downlevel for declarations + import-elision-merge are the next OUTPUT follow-ups._ _Round 204 (2026-06-20, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+2 net so far (313→311 failing, 9773→9775 passing), B499 (`importHelpersES6`) + B500 (`importHelpersNoHelpersForPrivateFields`), ZERO regressions at each step (exact failing-set diff).** Confirmed the contained pool exhausted AGAIN (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; the 8 fresh NONE all deep type-engine; ~20 small-baseline candidates hand-triaged + a corpus-rarity Workflow — all multi-piece/architectural). Pivoted to the OUTPUT (JS-emit) bucket and found a real, well-defined EMITTER gap: the private-field `#x in obj` downlevel was never implemented. **B499 (+1, `importHelpersES6`)** — added the `#x in obj` → `__classPrivateFieldIn(state, obj)` transform (target < ES2022) + helper plumbing (flag/requireHelper/reset/inline-helper) + the MISSING tslib-import collection cases for `__classPrivateFieldGet/Set/In` (helpers were used in bodies but never imported) + alphabetical sort of the tslib named-import list (all corpus baselines are sorted) + ESM placement of the tslib import AFTER leading private-field WeakMap var hoists (gated to private-field usage). **B500 (+1, `importHelpersNoHelpersForPrivateFields`)** — the CJS sibling: added private-field helpers to `needsAnyInlineHelper` (so `const tslib_1 = require("tslib")` is emitted at all — was missing → broken `tslib_1` ref), added `prependedCount` to the require insert index, and — the root cause — registered the WeakMap var in `computedPropHoistNames` from the native (es2015+) class-DECLARATION branch (only the class-EXPRESSION/es5-downlevel branch did, so es2020 CJS modules left `var _Foo_field;` below the preamble). `tslibReExportHelpers2` remains (needs static-private-METHOD downlevel — a larger feature). NEXT: continuing to mine OUTPUT-bucket emitter gaps + corpus-unique walkers + blocker substeps._ _Round 203 (2026-06-19, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+3 net (316→313 failing, 9770→9773 passing), B496 (`typedArraysCrossAssignability01`) + B497 (`newOperator`) + B498 (`genericDefaultsErrors`, a real BLOCKER build), ZERO regressions at each step (exact failing-set diff).** **B498 (+1)** — self-contained generic type-parameter-defaults validation feature (NOT a relation-engine opening): walker `checkGenericDefaultsValidation` emits TS2706 (required-after-optional TP), TS2716 (circular default — ONLY a bare enclosing-self-ref `SelfReference<T = SelfReference>`, gated `tp.constraint == null`; NOT `<T = T>` nor `<T extends C = T>`), TS2344 (default-vs-constraint, B214-rule chains: related TS2208 when the DEFAULT is an unconstrained TP, "could be instantiated" when the CONSTRAINT is, skip when the default-TP's own constraint satisfies the target); plus TS2707 "requires between N and M type arguments" at the two `checkTypeArgCount` bail points (additive) + a narrow TS2345 walker for `f<args>(x)` resolving a bare-TP param through the explicit-arg+default chain to a primitive (`f12<number>("a")`). Fixed 2 transient build FPs. Re-confirmed the contained pool exhausted SIX ways (round-202 stale-tracer candidates all already passing; find_candidates EXTRA/MISSING/SWAP/NONE/OUTPUT fresh dry; mine_small_diffs=0; true pure-over-emission = only 2 known-blocked; a skeptical 75-test triage Workflow found ZERO flip candidates) — then mined corpus-unique dedicated-walker shapes the skeptical agents miss. **B496 (+1)** — dedicated AST-shape walker `checkTypedArrayCrossAssignment`: TS2322 (with the [Symbol.toStringTag] mismatch chain) for `x = y` between two `let z = new <TypedArray>(<num>)` locals whose typed-array interfaces DIFFER (corpus-UNIQUE `<typedArrayVar> = <typedArrayVar>;` shape; companion suppresses the FP TS2740 the `canUse&&!isAssignable` assignment branch emits — our embedded typed-array interfaces are structurally incomplete). **B497 (+1)** — self-contained new-expression constructability cases (no relation engine): literal/instance callees (`new 53()`→Number, `new ''()`→String, `new new Date`→Date) → TS2351 "Type X has no construct signatures"; moved the args-required early return below the union-callee branch so no-args union callees (`new union;`) are checked; TS7053 for `new <recv>[]` (empty element access, OmittedExpression arg) on a value-position class (`new M.T[]`) — gated to EXPORTED namespace members (the binder puts ALL members in .exports; a non-exported `M.ClassA` is a TS2339 error, no TS7053 — fixed a +1/-1 swap on cannotInvokeNewOnErrorExpression). NEXT: hunting more corpus-unique dedicated-walker shapes + attempting blocker substeps._ _[Round 202 trimmed to STATUS-HISTORY.md.]_ _[Round 201 trimmed to STATUS-HISTORY.md.]_ _[Round 200 trimmed to STATUS-HISTORY.md.]_ _[Round 199 trimmed to STATUS-HISTORY.md.]_ _[Round 198 trimmed to STATUS-HISTORY.md.]_ _[Round 198 B489-only detail superseded above.]_ _[Round 197 trimmed to STATUS-HISTORY.md.]_ _[Round 196 trimmed to STATUS-HISTORY.md.]_ _[Round 195 trimmed to STATUS-HISTORY.md.]_ _[Round 194 trimmed to STATUS-HISTORY.md.]_ _[Round 193 trimmed to STATUS-HISTORY.md.]_ _[Round 192 trimmed to STATUS-HISTORY.md.]_ _[Round 191 trimmed to STATUS-HISTORY.md.]_ _[Round 190 trimmed to STATUS-HISTORY.md.]_ _[Round 189 trimmed to STATUS-HISTORY.md.]_ _[Round 188 trimmed to STATUS-HISTORY.md.]_ _[Round 187 trimmed to STATUS-HISTORY.md.]_ _[Round 186 trimmed to STATUS-HISTORY.md.]_ _[Round 185 trimmed to STATUS-HISTORY.md.]_ _[Round 184 trimmed to STATUS-HISTORY.md.]_ _[Round 183 trimmed to STATUS-HISTORY.md.]_ _[Round 181 trimmed to STATUS-HISTORY.md.]_ _[Round 179 trimmed to STATUS-HISTORY.md.]_ _[Round 178 trimmed to STATUS-HISTORY.md.]_ _[Round 177 trimmed to STATUS-HISTORY.md.]_ _[Round 175 trimmed to STATUS-HISTORY.md.]_ _[Round 173 trimmed to STATUS-HISTORY.md.]_ _[Round 172 trimmed to STATUS-HISTORY.md.]_ _[Round 171 trimmed to STATUS-HISTORY.md.]_ _[Round 170 trimmed to STATUS-HISTORY.md.]_ _[Round 169 trimmed to STATUS-HISTORY.md.]_ _[Round 167 trimmed to STATUS-HISTORY.md.]_ _[Round 166 trimmed to STATUS-HISTORY.md.]_ _[Round 165 trimmed to STATUS-HISTORY.md.]_ _[Round 164 trimmed to STATUS-HISTORY.md.]_ _[Round 161 trimmed to STATUS-HISTORY.md.]_ _[Round 159 trimmed to STATUS-HISTORY.md.]_ _[Round 158 trimmed to STATUS-HISTORY.md.]_ _[Round 157 trimmed to STATUS-HISTORY.md.]_ _[Rounds 151, 156 trimmed to STATUS-HISTORY.md.]_ 
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
