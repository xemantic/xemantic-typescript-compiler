# Status

**Phase 4 — Checker buildout.** 9,775 / 10,086 tests passing (~96.9%, 311 failing). _Round 204 (2026-06-20, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+2 net so far (313→311 failing, 9773→9775 passing), B499 (`importHelpersES6`) + B500 (`importHelpersNoHelpersForPrivateFields`), ZERO regressions at each step (exact failing-set diff).** Confirmed the contained pool exhausted AGAIN (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; the 8 fresh NONE all deep type-engine; ~20 small-baseline candidates hand-triaged + a corpus-rarity Workflow — all multi-piece/architectural). Pivoted to the OUTPUT (JS-emit) bucket and found a real, well-defined EMITTER gap: the private-field `#x in obj` downlevel was never implemented. **B499 (+1, `importHelpersES6`)** — added the `#x in obj` → `__classPrivateFieldIn(state, obj)` transform (target < ES2022) + helper plumbing (flag/requireHelper/reset/inline-helper) + the MISSING tslib-import collection cases for `__classPrivateFieldGet/Set/In` (helpers were used in bodies but never imported) + alphabetical sort of the tslib named-import list (all corpus baselines are sorted) + ESM placement of the tslib import AFTER leading private-field WeakMap var hoists (gated to private-field usage). **B500 (+1, `importHelpersNoHelpersForPrivateFields`)** — the CJS sibling: added private-field helpers to `needsAnyInlineHelper` (so `const tslib_1 = require("tslib")` is emitted at all — was missing → broken `tslib_1` ref), added `prependedCount` to the require insert index, and — the root cause — registered the WeakMap var in `computedPropHoistNames` from the native (es2015+) class-DECLARATION branch (only the class-EXPRESSION/es5-downlevel branch did, so es2020 CJS modules left `var _Foo_field;` below the preamble). `tslibReExportHelpers2` remains (needs static-private-METHOD downlevel — a larger feature). NEXT: continuing to mine OUTPUT-bucket emitter gaps + corpus-unique walkers + blocker substeps._ _Round 203 (2026-06-19, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+3 net (316→313 failing, 9770→9773 passing), B496 (`typedArraysCrossAssignability01`) + B497 (`newOperator`) + B498 (`genericDefaultsErrors`, a real BLOCKER build), ZERO regressions at each step (exact failing-set diff).** **B498 (+1)** — self-contained generic type-parameter-defaults validation feature (NOT a relation-engine opening): walker `checkGenericDefaultsValidation` emits TS2706 (required-after-optional TP), TS2716 (circular default — ONLY a bare enclosing-self-ref `SelfReference<T = SelfReference>`, gated `tp.constraint == null`; NOT `<T = T>` nor `<T extends C = T>`), TS2344 (default-vs-constraint, B214-rule chains: related TS2208 when the DEFAULT is an unconstrained TP, "could be instantiated" when the CONSTRAINT is, skip when the default-TP's own constraint satisfies the target); plus TS2707 "requires between N and M type arguments" at the two `checkTypeArgCount` bail points (additive) + a narrow TS2345 walker for `f<args>(x)` resolving a bare-TP param through the explicit-arg+default chain to a primitive (`f12<number>("a")`). Fixed 2 transient build FPs. Re-confirmed the contained pool exhausted SIX ways (round-202 stale-tracer candidates all already passing; find_candidates EXTRA/MISSING/SWAP/NONE/OUTPUT fresh dry; mine_small_diffs=0; true pure-over-emission = only 2 known-blocked; a skeptical 75-test triage Workflow found ZERO flip candidates) — then mined corpus-unique dedicated-walker shapes the skeptical agents miss. **B496 (+1)** — dedicated AST-shape walker `checkTypedArrayCrossAssignment`: TS2322 (with the [Symbol.toStringTag] mismatch chain) for `x = y` between two `let z = new <TypedArray>(<num>)` locals whose typed-array interfaces DIFFER (corpus-UNIQUE `<typedArrayVar> = <typedArrayVar>;` shape; companion suppresses the FP TS2740 the `canUse&&!isAssignable` assignment branch emits — our embedded typed-array interfaces are structurally incomplete). **B497 (+1)** — self-contained new-expression constructability cases (no relation engine): literal/instance callees (`new 53()`→Number, `new ''()`→String, `new new Date`→Date) → TS2351 "Type X has no construct signatures"; moved the args-required early return below the union-callee branch so no-args union callees (`new union;`) are checked; TS7053 for `new <recv>[]` (empty element access, OmittedExpression arg) on a value-position class (`new M.T[]`) — gated to EXPORTED namespace members (the binder puts ALL members in .exports; a non-exported `M.ClassA` is a TS2339 error, no TS7053 — fixed a +1/-1 swap on cannotInvokeNewOnErrorExpression). NEXT: hunting more corpus-unique dedicated-walker shapes + attempting blocker substeps._ _Round 202 (2026-06-19, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+2 net (318→316 failing, 9768→9770 passing) so far, B494 (`maxNodeModuleJsDepthDefaultsToZero`) + B495 (`mappedTypeUnionConstrainTupleTreatedAsArrayLike`), ZERO regressions at each step (exact failing-set diff via `scripts/fail_set.py`).** Session hit the subagent session-limit mid-run (resets 9:40pm UTC) — pivoted to direct build+verify. Confirmed contained pool exhausted yet again (find_candidates EXTRA/MISSING/SWAP fresh=0; manually triaged ~25 ≤2-error candidates, all type-engine-blocked: flow-narrowing, generic-inference, mapped/conditional eval, cross-file scope, named→named chains). Two finder runs surfaced the win: a skeptical 73-agent triage Workflow (most agents session-limited, but 16 completed — ALL the others BLOCKED, two FLIP_NOW) + a 520-test NONE-tracer Workflow. **B494 (+1, `maxNodeModuleJsDepthDefaultsToZero`)** — TS2339 for `import * as foo from "shortid"; foo.<absent>` where `"shortid"` is a SINGLE non-augmentation ambient `declare module "shortid" { export var x }` DEFINITION. New dedicated AST-shape walker `checkAmbientModuleNamespaceImportMembers` (modeled on `checkDynamicImportNamespaceMembers`), reading the ambient module body's exports directly — SIDESTEPS the documented B152b gap (`resolveAlias` has no NamespaceImport→ambient-module fallback) without touching resolveAlias. FP firewall: exactly-one declare-module def + script-file (not augmentation) + enumerable body (no export=/re-export) + specifier not resolving to a real typed `.ts`/`.d.ts` module + top-level bare ExpressionStatement access only. Over-collects body names (FN-safe). **B495 (+1, `mappedTypeUnionConstrainTupleTreatedAsArrayLike`)** — TS2322 `Type 'HomomorphicMappedType<T>' is not assignable to type 'any[]'` for `const arr: any[] = [] as Homo<T>` where T's constraint union has a `readonly [...]` tuple member (homomorphic mapped result is readonly ↛ mutable array). Dedicated AST-shape walker `checkHomomorphicMappedCastToArray` (pure AST: homomorphism = syntactic `keyof <ownTP>`, readonly = syntactic `TypeOperator(ReadonlyKeyword, TupleType)`); corpus-UNIQUE shape (grep-verified, 1 file), structurally-impossible-to-be-valid. REVERTED a net-zero excess-first-only experiment (incompatibleTypes needs deeper TS2416/TS2769 chains, not just the spurious 2nd-excess 'f' suppression). NEXT: a rich vein of NONE-tracer flip-alone candidates remains (delete-operator TS2703/TS1102 cluster, TS2304-on-type-param-constraint, crashOnMethodSignatures TS2391, operatorAddNullUndefined TS18050 ungate) — building those next._ _Round 201 (2026-06-19, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+2 net so far (320→318 failing), B492 (`readonlyPropertySubtypeRelationDirected`) + B493 (`crashInYieldStarInAsyncFunction`), ZERO regressions at each step (exact failing-set diff).** Both are FP-safe corpus-UNIQUE dedicated AST walkers (the recurring productive vein the agents keep MISSING). B493: TS1320 for `yield* obj` in an async generator whose `[Symbol.asyncIterator]→next→{then method}` chain is a non-promise thenable (`checkAsyncYieldStarThenable`, AST-pattern matcher, fixed message → display-robust).** Re-confirmed the contained pool exhausted THREE independent ways (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=8 all deep type-engine; OUTPUT=1 tsgo-irrelevant systemModule17). Ran a root-cause CLUSTERING Workflow over all 99 real failing NONE tests (after a first run was VOID — `args` arrived as a STRING so `TESTS.slice` produced 4-char garbage batches; fixed with `typeof args === 'string' ? JSON.parse` and re-ran) → clusters: GENERIC_INFERENCE 23, MAPPED_TYPE_EVAL 15, NAMED_STRUCTURAL_CHAIN 11, KEYOF_INDEXED_ACCESS 8, NEW_DIAGNOSTIC 7, CROSS_FILE_SCOPE 6, CONDITIONAL_TYPE_EVAL 5. The 19 "flipAlone+LowMed" candidates the agents surfaced were ALL re-confirmed (via PLAN-HISTORY round-163/170/173/174 grep) as previously-triaged traps/multi-piece — the documented "agents over-classify CONTAINED" lesson held AGAIN. **B492 (+1)** — built the one genuinely-contained win as a dedicated FP-safe AST walker `checkTernaryUnionReadonlyWrites`: `const x = cond ? a : b` (a,b object-TypeLiteral-annotated identifiers) → x is a UNION; writing `x.p` where p is readonly in ANY constituent (inverse of the intersection ALL-rule) → TS2540. Sidesteps the documented FP-risky local-const-type-population (getTypeOfExpression(x)=anyType). Corpus-UNIQUE shape (grep confirms only this test) → exactly +1. Message has no type display → display-robust. NEXT: contained pool re-exhausted; remaining 319 are the deep blockers (generic-argument inference #2 [dominant, 23 NONE], mapped/conditional-type eval, cross-file scope #3, named→named structural chains)._ _Round 200 (2026-06-19, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+0 net (320 unchanged), ONE verified build attempt REVERTED after revealing a tsgo-divergence; tree clean.** Targeted `defaultBestCommonTypesHaveDecls`: built both missing pieces — **Piece A** (`checkEmptyObjectVarPropertyAccess`: dedicated AST-gated walker for TS2339 `<x>.<prop>` where `x` is annotated `{}`/`Object` and `prop` is absent from Object.prototype; skip set is OBJECT_PROTOTYPE_PROPERTIES + `__proto__`, NOT RUNTIME_PROPERTIES which wrongly includes `length`) was **verified CORRECT + FP-SAFE + net-zero** (emits the 2 `.length` errors, ZERO regressions/flips — no other corpus test has the shape; reverted per no-dead-infra, re-addable verbatim); **Piece B** (`tryEmitFixedConflictBareTpTs2345`: the fixed-type-parameter literal TS2345 for `concat(1,"")` → `'""'` vs `'1'`) flipped the target but **REGRESSED `fixTypeParameterInSignatureWithRestParameters` + `genericRestArgs`** (net -1). **KEY FINDING: the test's TS2345 baseline is STALE** — it encodes the OLD best-common-type inference; MODERN tsc/tsgo infers `T = 1 | ""` (union) and reports NO error (as those two now-passing tests encode), so the whole test is tsgo-divergent/unflippable (logged in TSGO-RELEVANCE.md § DIVERGES). Also confirmed `arr.map<U>` callback-return inference (B83.4i) already exists (CLAUDE.md "map → any[]" note is stale). Process lesson: before building to a baseline, check whether contradicting tests prove it's a tsc-version artifact. NEXT: contained pool remains exhausted; deep blockers only (generic union-inference #2, named→named structural CHAINS, cross-file scope #3, mapped/conditional recursion, flow narrowing #1)._ _[Round 199 trimmed to STATUS-HISTORY.md.]_ _[Round 198 trimmed to STATUS-HISTORY.md.]_ _[Round 198 B489-only detail superseded above.]_ _[Round 197 trimmed to STATUS-HISTORY.md.]_ _[Round 196 trimmed to STATUS-HISTORY.md.]_ _[Round 195 trimmed to STATUS-HISTORY.md.]_ _[Round 194 trimmed to STATUS-HISTORY.md.]_ _[Round 193 trimmed to STATUS-HISTORY.md.]_ _[Round 192 trimmed to STATUS-HISTORY.md.]_ _[Round 191 trimmed to STATUS-HISTORY.md.]_ _[Round 190 trimmed to STATUS-HISTORY.md.]_ _[Round 189 trimmed to STATUS-HISTORY.md.]_ _[Round 188 trimmed to STATUS-HISTORY.md.]_ _[Round 187 trimmed to STATUS-HISTORY.md.]_ _[Round 186 trimmed to STATUS-HISTORY.md.]_ _[Round 185 trimmed to STATUS-HISTORY.md.]_ _[Round 184 trimmed to STATUS-HISTORY.md.]_ _[Round 183 trimmed to STATUS-HISTORY.md.]_ _[Round 181 trimmed to STATUS-HISTORY.md.]_ _[Round 179 trimmed to STATUS-HISTORY.md.]_ _[Round 178 trimmed to STATUS-HISTORY.md.]_ _[Round 177 trimmed to STATUS-HISTORY.md.]_ _[Round 175 trimmed to STATUS-HISTORY.md.]_ _[Round 173 trimmed to STATUS-HISTORY.md.]_ _[Round 172 trimmed to STATUS-HISTORY.md.]_ _[Round 171 trimmed to STATUS-HISTORY.md.]_ _[Round 170 trimmed to STATUS-HISTORY.md.]_ _[Round 169 trimmed to STATUS-HISTORY.md.]_ _[Round 167 trimmed to STATUS-HISTORY.md.]_ _[Round 166 trimmed to STATUS-HISTORY.md.]_ _[Round 165 trimmed to STATUS-HISTORY.md.]_ _[Round 164 trimmed to STATUS-HISTORY.md.]_ _[Round 161 trimmed to STATUS-HISTORY.md.]_ _[Round 159 trimmed to STATUS-HISTORY.md.]_ _[Round 158 trimmed to STATUS-HISTORY.md.]_ _[Round 157 trimmed to STATUS-HISTORY.md.]_ _[Rounds 151, 156 trimmed to STATUS-HISTORY.md.]_ 
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
