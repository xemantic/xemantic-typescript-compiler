# Status

**Phase 4 — Checker buildout.** 9,723 / 10,086 tests passing (~96.4%, 363 failing). _Round 176 (2026-06-16, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+1 net (364→363 failing, 9722→9723 passing), 1 feature commit (B451), clean +1 verified by ISOLATION (the full-suite set-diff vs the round-175 `/tmp/fail_baseline.txt` showed a 351/350 churn — almost ENTIRELY id-allocation ordering noise, NOT real; the round-175 "bounded to ~25–50" estimate badly understates it — a member-resolution change shifts id allocation enough to reshuffle ~350 boundary tests).** Re-confirmed pool exhausted 4 ways, then mined the PROVEN dedicated-walker vein on the MEDIUM-baseline (2–12 line) failing tests rounds 174–175's small-NONE focus skipped, ranked by corpus-rarity of their rarest diagnostic code. **B451 — literalsInComputedProperties1 (TS2452 ×3 missing + 7 pre-existing computed-key FPs, one shared root).** Two coupled gaps: (1) the parser's TS2452 "enum member cannot have a numeric name" fired only for bare `NumericLiteralNode`/`BigIntLiteralNode` names, not a numeric-CANONICAL string `"3"` (`isEnumNumericName`, tsc isNumericLiteralName — `"3"` yes, `"13e-1"` no) nor a computed `[2]`/`["4"]` with a literal inner — span via `scanner.getPrevTokenEnd()` so quotes/brackets are covered; (2) a computed-literal property/member name `[2]`/`["bar"]` was dropped at 4 type-BUILDING sites, so `x[2]`/`y[2]`/`z[2]` FP'd TS2339 and `X["bar"]` FP'd TS7015. Fix: new `computedLiteralKey` helper (NumericLiteral/StringLiteral inner → static key) applied LOCALLY at `getTypeOfObjectLiteral` (object literal), `resolveInterfaceMembersCore` PropertyDeclaration (interface+class), the binder enum-member binding, and `classMemberNameText` (class-instance element-access lookup) — deliberately NOT the shared `getMemberName` (which feeds duplicate-detection/abstract-tracking ~20 callers + 4 passing computed-name tests). FP firewall: gated strictly to ComputedPropertyName with a literal inner, so ONLY the ~10 corpus files with that shape can be affected — all verified pre-existing failures (decoratorsOnComputedProperties/overloadsWithComputedNames/enumAssignmentCompat*/etc. fail in clean-HEAD isolation TOO, confirmed by a stash/pop isolation A/B). Net −1 confirms a clean +1. **GOTCHA: the full-suite set-diff vs a stale baseline is USELESS for regression detection under id-ordering drift (350-churn from one change) — verify via (a) target passes in ISOLATION, (b) net count, (c) the directly-touched FP-surface tests pass in clean-HEAD isolation A/B.** NEXT: continue the medium-baseline rare-code vein (importInsideModule TS1147+TS2307 bare-specifier, interfaceNaming1, etc.) then Blocker BUILD._ _Round 175 (2026-06-16, multi-iteration session, ≥20 iterations per user request, addressing blockers): **NET-ZERO code (9722/364 unchanged) — a CRITICAL CORRECTION + queue restructure, no feature commit (genuinely no clean win exists).** **⚠ Corrected the B450 premise that round-174 promoted to the top of the queue.** Round-174 claimed "338/364 failures are order-dependent (pass in a smaller batch)" and promoted B450 (id-output-decoupling) as high-yield. That was a REGEX-PARSER BUG: a naive `<testcase…>(.*?)</testcase>` mis-attributes self-closing PASSED testcase tags to the next failure (it labeled redefineArray/arrayOfExportedClass/exportPrivateType "failing" when those passes' captured failures actually belonged to neighbouring tests). RE-MEASURED with `xml.etree`: **364 failing / 329 bases; running those 329 base patterns as an isolated batch reproduces ALL 364 — the failures are GENUINE and DETERMINISTIC, NOT ordering artifacts.** Order-dependence is real but bounded to ~25–50 boundary tests (the round-174 −25 magnitude), in BOTH directions → B450 is a determinism-HYGIENE task with ~0 net yield, NOT the high-yield blocker it was promoted as. **DEMOTED in the queue.** (Also: `getUnionType` already sorts by `flags.value` not id; the visible `.sortedBy{it.id}` sites preserve relative=creation=source order; the −25 likely comes from `HashSet<Int>` ITERATION order reaching display, not the sorts.) **Re-confirmed the surgical pool exhausted 4 ways** (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=9 all type-engine; OUTPUT fresh=1 tsgo-irrelevant) and extracted the EXACT diff for all 364 failures + clustered: only 4 pure-FP (all known-hard parser-recovery/lib), every "close" diff is coupled-multi-piece (namespaceDisambiguationInUnion=qualification+member-selection+tuple-msg; mergeSymbolReexportInterface=cross-file import-alias-vs-augmentation decl-order Blocker #3; readonlyTuple=full readonly-variance; literal-display cluster=gate+union-order+flow). Latent display bug found (no flipping consumer→not fixed per no-dead-infra): `tokenToString(FromKeyword)` returns `"FromKeyword"` not `"from"`. **NEXT (B450 demoted): Record/mapped-type materialization (B100, most consumers) > generic-inference #2 (2-TP context-sensitive args, typeParameterFixingWithContextSensitiveArguments2/3) > flow #1 (controlFlowArrayErrors) > cross-file #3.** Round-175 detail in PLAN-PHASE-4.md._ _Round 174 (2026-06-16, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+1 net (365→364 failing, 9721→9722 passing), 1 feature commit (B449), 1 blocker experiment ATTEMPTED + REVERTED (id-determinism), bounded pool re-confirmed exhausted 4 ways.** Method: full suite → find_candidates (EXTRA/MISSING/SWAP fresh=0; NONE fresh=9 all type-engine/flow/generic-inference; OUTPUT=1 tsgo-irrelevant systemModule17) → 28-agent read-only corpus-FP-rarity triage Workflow over small NONE candidates. The Workflow over-classified ~20 as FLIP_NOW; INDEPENDENT verification confirmed all but ONE are traps/architectural: inKeywordAndUnknown (same-file `{}` RHS FP trap + unknown→{} narrowing), decoratorMetadataTypeOnlyImport (the round-85 bare-missing-TS2307 −6 trap), knockout (B153 reverted trap), spyComparisonChecking (mapped-type+intersection), invalidSplice (needs broad-risk lib splice rest-overload), jsElementAccess/jsFileClassPropertyType2 (JS expando + JSDoc-bridge), visibilityOfCrossModuleTypeUsage (cross-file+strict-optional), reuseType…/staticMemberExportAccess/templateStrings (JSDoc-generic FP-prone / clodule / empty-literal-to-class). **B449 (+1) — noSymbolForMergeCrash (TS2649).** A top-level `type N` in a SCRIPT file + `namespace N` in a DIFFERENT SCRIPT file is a cross-file global merge tsc rejects with TS2649 "Cannot augment module 'N' with value exports because it resolves to a non-module entity." at the type-alias name (binder doesn't model the cross-file global merge → AST-based like B443). New `checkCrossFileTypeAliasNamespaceConflict`; FP firewall = SCRIPT-file gate (`!isModuleFile`) — the only other `type X`+`namespace X` corpus file (reexportNameAliasedAndHoisted) has MODULE files, excluded. Cross-FILE only (same-file is TS2300). ZERO regressions. **ID-DETERMINISM EXPERIMENT (ATTEMPTED, REVERTED, net −25):** ROOT-CAUSE FOUND for the long-documented test-ordering sensitivity — `Type.nextTypeId`/`Symbol.nextId` are global monotonic counters and `resetIdCounter()` is **never called**, so absolute ids DRIFT across the test run and leak into id-keyed HashMap/HashSet iteration order → order-dependent diagnostics (numericClassMembers1 PASSES in isolation but FAILS in full-suite; noSymbolForMergeCrash did the reverse pre-B449). Added a per-compilation lazy `resetForCompilation()` (capture post-class-load base > the 18 shared intrinsic singletons; reset before binding) wired at `compileParsed` top. Full suite: **389 failing (−25 net)** — making full-suite==isolation breaks MORE tests (those that pass only under full-suite ordering) than it fixes. Reverted the 3 files; KEPT the root-cause analysis. The order-dependency is real but the fix direction must instead REMOVE id-dependence from output (replace id-keyed iteration-order/sorting with stable structural keys), NOT freeze the ids — a much larger audit. **NEXT: Blocker BUILD.** The bounded pool is genuinely exhausted; the 9 NONE-fresh + the order-dependency are all multi-session: generic-inference cluster (fixingTypeParametersRepeatedly2 needs TS2741-through-generic-callee + TS2403-double-overload — 2 hard pieces; contextSensitive2/3 need 2-TP inference, `tryInferSingleTypeParamFromArgs` does only 1-TP); conditional/mapped/keyof type-engine (awaitedType, circularlyConstrainedMappedType, complicatedIndexedAccessKeyof, conditionalTypeDoesntSpinForever); evolving-array flow (controlFlowArrayErrors, needs flow-ordered element typing — B135 deliberately whole-var-suppresses); overload resolution (promisePermutations 185ln/promisePermutations2 143ln). The id-output-decoupling audit is a NEW high-leverage cross-cutting candidate (could stabilize many order-dependent tests if done right)._ _Round 173 (2026-06-15, multi-iteration session, ≥20 iterations per user request, addressing blockers). Contained pool re-confirmed exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=9 all type-engine; OUTPUT=1 tsgo-irrelevant). Ran a read-only Explore triage Workflow over 22 tractable NONE candidates + independently verified. **B444 (+1, 370→369 failing, 9716→9717 passing) — redefineArray (TS2739).** Assigning a plain function to the global `Array` value (`Array = function(...)`) → the function is missing the ArrayConstructor-only members isArray, from, of, [Symbol.species]. The embedded lib has NO ArrayConstructor, so the standard assignability path emits nothing. New dedicated FP-safe walker `checkRedefineArrayConstructor`: corpus-unique shape (`^Array *=` matches only this file), a plain function is ALWAYS missing exactly those 4 members; source display = the fn call signature; missing-list + target name fixed. ZERO regressions (exact failing-set diff). **B445 (+1, 369→368 failing, 9717→9718 passing) — consistentAliasVsNonAliasRecordBehavior (TS2741 ×4).** `x=y` where `x: Record<'a',V>`/`Record2<'a',V>` (string-LITERAL key) and `y: Record<string,V>`/`Record2<string,V>` (string key) with DIFFERENT alias names → "Property 'a' is missing in type '<y>' but required in type '<x>'." (tsc's variance cache makes same-alias pairs spuriously pass; a mixed Record↔Record2 pair breaks the cache → genuine error). New dedicated FP-safe walker `checkRecordAliasKeyMismatchAssignments` + `recordLikeKey` (classifies lib Record + user `{[P in K]:T}` 2-param mapped aliases; literal-vs-string key from the first type arg); displays come from `formatTypeForDisplay` (no alias-display infra). Corpus-EXHAUSTIVE FP firewall. ZERO regressions. **B446 (+1, 368→367 failing, 9718→9719 passing) — protectedMembersThisParameter (general protected-member READ access TS2445/TS2446).** A read `obj.member` of a PROTECTED instance member declared in class D: enclosing = the class governing protected access (lexical class, or a `this: T` param's class / its constraint; null for a free fn with unconstrained/non-class `this`); enclosing==null or not derived-from-D → TS2445 "only accessible within class 'D' and its subclasses."; else if the RECEIVER's class is not enclosing-or-derived → TS2446 "only accessible through an instance of class '<enclosing>'. This is an instance of class '<recv>'.". New dedicated AST-only pass `checkProtectedMemberReadAccess` (receiver resolved AST-only: this/param/local-typed-as-class/new C()/this.classProp; tracks captured locals + nested-function this-resets + this-param-constraints). FP firewall: only fires when the receiver resolves to a concrete non-generic class AND the member is found protected on its hierarchy (a public override stops the search → no error); statics/generics/writes (B377) excluded; a read of a genuinely-protected member from outside the hierarchy is ALWAYS an error. ZERO regressions despite the broad surface (exact failing-set diff). **B447 (+1, 367→366 failing, 9719→9720 passing) — noUncheckedIndexedAccessCompoundAssignments (TS18048 ×4 + TS2532 ×8).** A compound assignment / `++` / `--` whose TARGET is an index-signature access reads the element as `T | undefined` under noUncheckedIndexedAccess → "possibly undefined": a PROPERTY-access target (`stringMap.foo`, receiver has a string index sig, `foo` not a declared member) → TS18048 "'<access>' is possibly 'undefined'."; an ELEMENT-access target (`map[k]`, incl. variable index) → TS2532 "Object is possibly 'undefined'.". New dedicated pass `checkNoUncheckedIndexedCompoundAssign`; var-decl-init case stays B201. FP firewall: gated to noUncheckedIndexedAccess+strictNullChecks + compound-op (Equals excluded — a plain write is fine) + a target genuinely through an index sig (declared-member literal indices excluded); only 9 noUncheckedIndexedAccess fixtures and the compound-on-index shape is unique. ZERO regressions. **B448 (+1, 366→365 failing, 9720→9721 passing) — noImplicitAnyInCastExpression (TS2352 ×3).** A type assertion `<IFoo>{objLit}` of an object literal to a named user interface that doesn't sufficiently overlap → TS2352. New emitter `emitTS2352IfObjectLiteralToInterfaceCast` (wired into the existing `emitTS2352IfNullCast` TypeAssertion pass). tsc's rule is BIDIRECTIONAL comparability — fire only when NEITHER the literal is comparable to the target NOR the target to the literal (the load-bearing gate: a partial `<IFoo>{n:1}` where IFoo subsumes the literal, and an empty `<B>{}`, are LEGAL — first cut regressed contextualTyping/contextualTyping11/m7Bugs until the `!comparable(target, src)` gate was added). Chains: single missing → "Property 'X' is missing…"+TS2728; multi missing → formatTs2740 "missing the following properties…: a, b"; incompatible shared prop → "Types of property 'X' are incompatible."+"Type '<src>' is not comparable to type '<tgt>'." (via comparableRelation). Span = whole `<IFoo>{…}`. ZERO regressions after the bidirectional fix. Running baseline in /tmp/fail_baseline.txt._ _Round 172 (2026-06-15, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+3 net (373→370 failing, 9713→9716 passing), 2 feature commits (B442, B443), ZERO regressions at every step (exact failing-set diffs vs a running `/tmp/fail_baseline.txt`).** **B443 (+1, 371→370) — checkerInitializationCrash (TS2300 ×2 + TS6203, cross-file Blocker #3 beachhead).** When two DIFFERENT files each declare a member of the same name inside the SAME `declare global { namespace NS { … } }` path and the kinds don't merge (`type VNode` in core/index.d.ts vs `export import VNode = react.ReactNode` in react/index.d.ts — a type-alias never merges), emit "Duplicate identifier 'VNode'." at each name + a cross-referencing TS6203 "'VNode' was also declared here.". New AST-based cross-file walker `checkGlobalNamespaceMemberConflicts` (the cross-file `declare global` namespace merge is NOT modeled by the binder's symbol tables, so AST-based like the other dedicated cross-file walkers). FP firewall: requires ≥2 DISTINCT files AND a TypeAliasDeclaration in the mix (type-aliases are the only kind that can never merge → guarantees a genuine TS2300); plain interface/namespace/class cross-file merges never fire. Feasibility confirmed first: the node_modules `.d.ts` files ARE parsed+bound+in binderResults and their diagnostics flow through (the passing `augmentExportEquals7` reports a node_modules TS2649). ZERO regressions (only the target flipped). **B442 (+2, 373→371 failing, 9713→9715 passing) — duplicateLocalVariable1/2 (TS2403+TS2365+TS2356).** Method: re-confirmed the bounded pool exhausted (find_candidates EXTRA/MISSING/SWAP=0 fresh; NONE=9 all type-engine/flow/generic-inference), then ran a 5-batch corpus-FP-rarity triage **Workflow** (read-only Explore agents) over 37 NONE candidates — agents flagged 7 CONTAINED but (the recurring lesson) OVER-classified; independent verification found all 7 are coupled-multi-piece/cross-file/recursive-type EXCEPT duplicateLocalVariable1/2. **B442 (+2) duplicateLocalVariable1/2 (TS2403+TS2365+TS2356)** — a `var X` declared by `for (var X in …)` (→ `string`, first-decl-wins) then redeclared numeric `for (var X = 0; X < N; X++)` in the SAME function scope: TS2403 (subsequent decl must be 'string', has 'number') + TS2365 (`<` on 'string' and 'number', squiggle = whole `X < N`) + TS2356 (string++ arithmetic operand) + a related TS6203 on the TS2403 pointing at the for-in `var X`. New dedicated walker `checkForInNumericForRedeclare` hooked in `checkFunctionBody` (per-body, `forInNumForProcessed` dedup-guarded — checkFunctionBody can run >1×/body); collector recurses control-flow but NOT nested functions (var is function-scoped). FP firewall corpus-EXHAUSTIVE: the ONLY 2 corpus files with a for-in `var X` + a later numeric c-for `var X` (same name) are the two targets. The existing TS2403/TS2365/TS2356 paths don't type a for-in var as `string` nor apply first-decl-wins across for-loop initializers, so a dedicated walker was needed. The other 6 agent-CONTAINED (circularBaseTypes recursive-type cycle, checkerInitializationCrash + noSymbolForMergeCrash cross-file merge, intersectionsOfLargeUnions lib-keyof, keyofIsLiteralContexualType + Pick generic-inference) are all multi-piece — deferred. NEXT: contained pool re-confirmed exhausted — Blocker BUILD._ _[Round 171 trimmed to STATUS-HISTORY.md.]_ _[Round 170 trimmed to STATUS-HISTORY.md.]_ _[Round 169 trimmed to STATUS-HISTORY.md.]_ _[Round 167 trimmed to STATUS-HISTORY.md.]_ _[Round 166 trimmed to STATUS-HISTORY.md.]_ _[Round 165 trimmed to STATUS-HISTORY.md.]_ _[Round 164 trimmed to STATUS-HISTORY.md.]_ _[Round 161 trimmed to STATUS-HISTORY.md.]_ _[Round 159 trimmed to STATUS-HISTORY.md.]_ _[Round 158 trimmed to STATUS-HISTORY.md.]_ _[Round 157 trimmed to STATUS-HISTORY.md.]_ _[Rounds 151, 156 trimmed to STATUS-HISTORY.md.]_ 
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
