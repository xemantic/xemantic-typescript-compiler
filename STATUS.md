# Status

**Phase 4 — Checker buildout.** 9,738 / 10,086 tests passing (~96.5%, 348 failing). _Round 181 (2026-06-16, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+2 net (350→348 failing, 9736→9738 passing), 2 feature commits (B461, B462), ZERO regressions at every committed step (exact failing-set diffs via xml.etree vs a running `/tmp/fail_baseline.txt`).** Method: re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=9 all type-engine; OUTPUT=1 tsgo-irrelevant), built a smallest-diff ranker + a rarest-expected-code ranker over all 348, and triaged ~15 candidates directly (all multi-piece: compose-style generic composition inferFromGenericFunctionReturnTypes1, weak-type cluster needing 3+ pieces, objectLiteralExcessProperties union/generic/intersection divergence, inheritance1 elaboration-display picks-wrong-class, computedPropertiesInDestructuring1 5-code, mapped-type materialization, JS-expando). Both wins EXTEND the B460 generic-inference beachhead (Blocker #2). **B461 (+1) — fixingTypeParametersRepeatedly2 (single-TP generic-arrow call TS2741 + overload-reinference TS2403).** Relaxed the B460 `checkGenericCallContextualArrowReturns` bare-TP anchor gate from ≥2 to ≥1 (single-TP `foo<T>(x:T, cb:(p:T)=>T)`; FP firewall is the unchanged bail-on-any-non-bare-TP/non-FunctionType-param + every-TP-inferable gate); added a CallExpression arrow body case (`param.method()` → method's call-sig return type) via `genericArrowReturnType`; gated TS2741 to SINGLE-decl callees (the anchor is fixed first → arrow-return mismatch observable; an OVERLOADED callee re-infers the callback's return TP as the supertype of {anchor, arrow-return} → no TS2741, result widens — "fixing type parameters repeatedly"); `processGenericArrowCall` now returns the call result type and the walker tracks top-level `var x = <genericArrowCall>` result types per statement list, emitting TS2403 + TS6203 on a redeclaration with a structurally-different type (foo→Derived vs overloaded bar→Base). **B462 (+1) — chainedCallsWithTypeParameterConstrainedToOtherTypeParameter (fluent generic-method-chain constraint TS2741/TS2739).** New `checkGenericThenChainConstraints`: for `new Chain(arg).then(a=>new B).then(b=>new C)...` where Chain is a generic class with a single self-fluent `then<S extends T>(cb:(x:T)=>S):Chain<S>` method, walk left-to-right tracking the current type arg T; each `.then` infers S from the arrow body (`new X` instance type) and checks `S extends T` via collectMissingProperties → TS2741 (one missing, related TS2728+TS6502) / TS2739 (≥2, own-before-inherited order via reversed missing-list, related TS6502) at the arrow body; on failure the result type arg CLAMPS to the constraint T (next link checks against T, not the unsatisfied S — tsc's exact chain semantics). FP firewall `thenChainMethod`: single-type-param class with exactly one `then<S extends T>(cb:(x:T)=>S):ClassName<S>` method + `new X` arrow bodies + ≥2 links (corpus-rare). NOTE: a duplicated-2-lines paste bug in the B462 insert left an unbalanced `{` (caught via `git diff` brace-net count — the compiler's misleading "Unresolved reference" cascade + OOM thrash masked it; ALSO a low-RAM env detour: a lingering 2.4GB java daemon + concurrent compiles caused GC-overhead OOM — kill all java + single compile fixed it). NEXT: B461/B462 extend the beachhead but the harder generic-inference variants (chainedCalls2 method-body type-param-vs-type-param TS2322, compose-style composition) and the big type-engine Blockers (Record/mapped-type materialization, flow evolving-array, cross-file scope) remain._ _Round 180 (2026-06-16, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+4 net (354→350 failing, 9732→9736 passing), 3 feature commits (B459, B459b, B460), ZERO regressions at every committed step (exact failing-set diffs via xml.etree vs a running `/tmp/fail_baseline.txt`).** Method: re-confirmed the contained pool scraped 6 ways (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=9 all type-engine; OUTPUT=1 tsgo-irrelevant; structural-only-missing filter = 57 all skip-logged/parser-recovery; rarest-missing-code; pure-missing=7; pure-extra=4 all known-hard) PLUS a 10-agent read-only triage Workflow (agents OVER-classified CONTAINED as always — widenedTypes/elidedJSImport1 verified multi-piece). Landed 3 dedicated-walker wins, two in previously-skip-logged blocker-adjacent territory, one a genuine Blocker #2 build. **B459 (+1) — overloadsWithComputedNames (TS2389/TS2391/TS2393 for computed METHOD names).** Computed method names that LATE-BIND (string/numeric literal, or unique-symbol/`Symbol()` identifier) now participate in overload impl-name/missing/duplicate checking; non-late-bindable (`[sym]` plain symbol, `[strUnion]` union) stay skipped (matches tsc). New `methodOverloadNameAndDisplay`/`collectUniqueSymbolConstNames`; the 3 emit fns gained a `ComputedPropertyName` span branch (`matchClosingBracket`). Classes only (interfaces untouched). **B459b (+1) — functionsWithModifiersInBlocks1 (block-level fn modifiers TS1184/TS1029 + dup-impl TS2393).** A fn in a PLAIN block can't carry export/declare/default → ONE TS1184 at the LEFTMOST modifier (broadened from export-only), and the modifier-ordering TS1029/TS1030 grammar check is SUPPRESSED there (new `atTopLevel` flag threaded through `checkDupModInStatement` — tsc reports TS1184 and returns); block-NESTED bodied functions sharing a name → TS2393 on each (binder doesn't bind block-scoped fns, B83.5; `checkMissingImplInStatements` gated `atBlockLevel`). **B460 (+2, both contextSensitive2/3) — typeParameterFixingWithContextSensitiveArguments2/3 (Blocker #2 generic-inference narrow build).** New `checkGenericCallContextualArrowReturns`: for the shape `f<T,U>(a:T, b:U, …, cb:(x:T)=>U)` (≥2 BARE-TP value-param anchors + fn-typed-of-TP params + arrow args), infers TPs from the anchors, substitutes into each fn-typed param, types the arrow body (simple ident / `param.prop`), checks `collectMissingProperties(arrowRet, substitutedRet)` → TS2741 + related TS2728/TS6502. FP firewall EXCLUDES the documented round-79l regressors (genericCombinators2 / contextualTypingOfGenericFunctionTypedArguments1, which use generic-REFERENCE params + method calls): bail the whole call on any non-bare-TP/non-FunctionType value param, require ≥2 bare-TP anchors + all TPs inferable. ZERO regressions — the tight gate is what made the long-net-zero-prone generic-inference area land clean. NEXT: the contained pool is exhausted; B460's narrow walker could extend to adjacent generic-inference shapes (different anchor counts, NewExpression arrow bodies) — and the remaining failures are the big type-engine Blockers (Record/mapped-type materialization, the broad generic-inference engine, flow evolving-array, cross-file scope)._ _Round 179 (2026-06-16, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+2 net (356→354 failing, 9730→9732 passing), 2 feature commits (B457 flow, B458 const-enum cluster), ZERO regressions at every step (exact failing-set diffs via xml.etree).** Method: re-confirmed find_candidates exhausted (EXTRA/MISSING/SWAP fresh=0; NONE fresh=9 type-engine; OUTPUT=1 tsgo-irrelevant), regenerated the round-177/178 purely-structural-missing-codes filter (30 candidates, all `[TRIAGED]`), ran a 12-agent read-only triage Workflow over the small-diff candidates (ALL returned ARCHITECTURAL — corroborating the contained pool is scraped), and INDEPENDENTLY re-verified the most tractable. Two wins: one CONTAINED the prior triage missed (B457), one genuine blocker BUILD (B458) per the user's "address blockers" directive + the protocol's one-cycle revert budget. **B457 (+1) — checkSuperCallBeforeThisAccess (flow-based super-before-`this`, TS17009/TS17011).** Replaced the naive "first `this` before first `super()`, then stop" heuristic with a direct port of tsc's `isPostSuperFlowNode`: `collectThisSuperRefs` gathers every `this` Identifier + every `super.<prop>` receiver in the ctor body (NOT descending into nested function-likes — lazy, tsc `getThisContainer(includeArrowFunctions)` — nor into `super()` argument lists, owned by the disjoint TS2336/TS2351/B51.5 emitters); each ref's flow node is walked for a `super()` `FlowCall` (branch labels post-super only if EVERY antecedent is; memoized). The flow builder already models if/else, switch-fallthrough, ternary, and object-literal super(), so `{w: cond ? super() : 0}` (not-post-super) vs both-branches (post-super) join exactly. Fixed all 12 missing TS17009/TS17011 across if/else/switch/ternary, zero regressions incl. the 9-test `checkSuperCallBeforeThisAccessing*` family. **B458 (+1) — const-enum diagnostic cluster (constEnumErrors), 6 checks in one new `checkConstEnumDiagnostics` walker, all AST/constant-eval (no relation engine).** TS2474 (member init not a constant expr — references a non-existent member via `E.X`/`E["X"]`, distinct from a TS2651 forward-ref to an existing-but-later member); TS2477/2478 (init evaluated to non-finite/NaN — reuses the existing `evaluateEnumInitializer`/`enumValues`, which already produce Double Infinity/NaN); TS2476 (const-enum element access via non-string-literal index — squiggle the index arg, with a manual template-literal end-scan since `expressionTrueEnd` overshoots TemplateExpression); TS2475 (const-enum NAME in value position — a recursive value-expr walker that treats prop/elem-access bases, typeof operands, and import/export-= RHS as the only legal positions); TS2567 (const enum + INSTANTIATED same-name namespace, via `isNamespaceInstantiated`, fires on both names). FP firewall = const-enum-only gating (rare shape) — zero regressions despite const enums appearing across the corpus. NEXT: the contained pool is genuinely scraped (12-agent workflow + 3 find_candidates buckets all dry/architectural); remaining wins are multi-piece type-engine Blockers (TS2411 index-sig-value cluster propertiesAndIndexers ×36 + inheritance/merge; evolving-array controlFlowArrayErrors; Record/mapped-type materialization; generic-inference #2)._ _Round 178 (2026-06-16, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+3 net (359→356 failing, 9727→9730 passing), 2 feature commits (B455 +1, B456 +2), ZERO regressions at every step (exact failing-set diffs via xml.etree baseline).** Method: regenerated the round-177 "purely-structural missing codes" filter over all 359 failing tests (60 candidates), independently triaged ~30 (most multi-piece: type-engine index-sig/intersections, parser-recovery cascades, cross-file), ran a skeptical read-only Explore triage Workflow over 31 (agents OVER-classified CONTAINED again — decoratorsOnComputedProperties=broad parser FP cascade, es6Import=grammar-suppression coupling, readonlyMembers=3 sub-features w/ IIFE scope rule — all verified independently). **B455 — letAndVarRedeclaration (function-scope var-hoist TS2451).** The round-177 deferred var-hoisting item. A `var`/for-init-`var` hoisting OUT of a nested block INTO a function body whose TOP declares a same-named let/const is a block-scoped redeclaration → TS2451 on BOTH. The binder hoists nested-block vars into NAMESPACE/FILE scopes (duplicate pipeline → TS2451) but NOT function bodies, and the TS2481 walker wrongly fired "cannot initialize outer scoped variable" for the no-init function case. New dedicated `checkVarHoistRedeclaration` (function bodies only); `checkOuterScopeVarShadowing` now suppresses TS2481 when a var matches the nearest function/module body-TOP let/const. TS2481 still fires for INTERMEDIATE-block lets (the 6 legit TS2481 baselines unaffected). **B456 (+2, both es5+es2015 variants) — blockScopedVariablesUseBeforeDef (IIFE-body TDZ TS2448/TS2450).** An IIFE (immediately-invoked arrow/fn-expr, possibly paren-wrapped) in a let/const initializer runs EAGERLY, so block-scoped names referenced inside its body — the var being initialized (self) or one declared in a LATER statement — are read in their temporal dead zone. The two UBD walkers (checkUBDForwardInExpr forward-refs, checkUBDInInitializer self-refs) both skipped arrow/fn bodies as "lazy"; now they detect an IIFE callee and walk its eagerly-evaluated body expressions. Gating (matches tsc): generator IIFE bodies don't run on call → skipped; async IIFEs run their synchronous prefix → forward refs fire but self refs do NOT (gated non-async); body-declared names shadow the outer TDZ set (FP-safety); the two mechanisms are positionally disjoint (no double-emit). New helpers ubdIifeCallee/collectUbdBodyBlockNames/forEachUbdEvaluatedExpr. NEXT: structural pool now genuinely scraped (B455+B456 were the last clean dedicated-walker wins; remaining = parser-recovery Blocker #7 + grammar-suppression coupling + type-engine Blockers) → type-engine Blocker BUILD (Record/mapped-type materialization, generic-inference #2, flow evolving-array #1)._ _Round 177 (2026-06-16, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+4 net (363→359 failing, 9723→9727 passing), 4 feature commits (B452-B454, B453=+2), ZERO regressions at every step (exact failing-set diffs).** Method: the `--none`/rare-code bucket was exhausted, but a NEW filter — failing tests whose ALL MISSING codes are PURELY STRUCTURAL (no type-engine code) — surfaced the remaining dedicated-walker wins (a diagnostic depending only on AST/modifiers/names/scope is the contained-win signature). The skeptical read-only triage Workflow again over-classified CONTAINED; independent verification (source+baseline+our-diff) was load-bearing. **B452 erasableSyntaxOnly (TS1294):** extended the narrow TypeAssertion-only walker to DECLARATION syntax (parameter properties, enums, INSTANTIATED namespaces via moduleInstanceStates, import/export-equals) in non-ambient context; gated on the rare option. **B453 abstractPropertyNegative (+2):** new `checkClassDeclAbstractAndAccessors` mirroring the class-EXPRESSION TS2653 walker — TS2654(≥2)/TS2515(1) unimplemented inherited abstract members (param-properties count; rule count==1→2515 else 2654), TS2676 get/set-pair abstractness disagreement, TS1005 concrete-accessor-no-body. ZERO regressions despite broad class-decl surface. **B454 letDeclarations-scopes-duplicates (TS2451):** B83.5 block-scoped blocker — new additive `checkBlockScopedRedeclarations` for same-block let/const dups in Block/switch/try/catch (binder covers only file/module top-level; function bodies already covered → process those only for nested blocks to avoid the var12 double-emit) + a top-level pipeline fix (FIRST-decl-kind picks TS2451 vs TS2300; a `function` in the let's scope forces TS2300). DEFERRED: letAndVarRedeclaration (var-hoisting into the let's function scope), exportAsNamespace_augment. NEXT: structural pool scraped (flow/parser-recovery/var-hoisting) → type-engine Blocker BUILD or the var-hoisting TS2451 extension._ _[Round 175 trimmed to STATUS-HISTORY.md.]_ _[Round 173 trimmed to STATUS-HISTORY.md.]_ _[Round 172 trimmed to STATUS-HISTORY.md.]_ _[Round 171 trimmed to STATUS-HISTORY.md.]_ _[Round 170 trimmed to STATUS-HISTORY.md.]_ _[Round 169 trimmed to STATUS-HISTORY.md.]_ _[Round 167 trimmed to STATUS-HISTORY.md.]_ _[Round 166 trimmed to STATUS-HISTORY.md.]_ _[Round 165 trimmed to STATUS-HISTORY.md.]_ _[Round 164 trimmed to STATUS-HISTORY.md.]_ _[Round 161 trimmed to STATUS-HISTORY.md.]_ _[Round 159 trimmed to STATUS-HISTORY.md.]_ _[Round 158 trimmed to STATUS-HISTORY.md.]_ _[Round 157 trimmed to STATUS-HISTORY.md.]_ _[Rounds 151, 156 trimmed to STATUS-HISTORY.md.]_ 
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
