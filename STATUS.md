# Status

**Phase 4 — Checker buildout.** 9,676 / 10,086 tests passing (~95.9%, 410 failing). _Round 159 (2026-06-14, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+2 net so far (412→410 failing, 9674→9676 passing), 2 feature commits (B413, B414), ZERO regressions (exact failing-set diffs).** After exhaustively RE-confirming the contained pool architectural (fresh EXTRA/MISSING/SWAP=0; the bounded NONE pool is JS-expando / namespace-import-ambient-resolveAlias / mapped-conditional type-engine / huge-overload; the small-diff fresh tests are all 2-piece — `indexSignatureInOtherFile` TS2314-value-extends-FP + TS2411 cross-file symbol-index; `importWithTrailingSlash` `.`/`..` resolution + default-import member modeling), found a productive ADDITIVE ELABORATION-CHAIN vein (FP-safe by construction: each new branch fires only on a type-pair shape that previously produced no chain). **B413 (+1, 9675/411) — errorMessageOnIntersectionsWithDiscriminants01**: discriminated intersection→intersection TS2322 elaboration. For `A = Common & {foo:1}` vs `B = Common & {bar:1}` (Common a discriminated union) tsc distributes the source intersection-of-union into constituents, reports the first failing constituent against the target alias, drills into the discriminant-matched target constituent, and names the missing required property's sub-object. New `getDiscriminatedIntersectionElaboration` + `distributeIntersectionConstituents` + `intersectionDiscriminants`; dispatched from `getPropertyElaborationChain` (Intersection→Intersection) and wired into the assignment-expression chain path (which only built a chain for Object→Object / Union sources). **B414 (+1, 9676/410) — unionTypeErrorMessageTypeRefs01**: generic-ref/alias → union elaboration. `A<Foo>` vs `A<Bar> | B<Baz> | C<Kwah>` (and the `type X<T>` alias half) drills into the same-generic constituent and reports the type-ARG mismatch. New assignment-path branch + `genericIdentityAndArgs` (keys a Reference on its target interface, an alias-displayed Object on its alias NAME); gated to a same-generic best constituent with a non-empty deeper chain. _Round 158 (2026-06-14, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+3 net (415→412 failing, 9671→9674 passing, ~95.9%), 2 feature commits (B411, B412), ZERO regressions at every committed step (exact failing-set diffs).** Both are Blocker BUILDs (the user directive). After exhaustive recon RE-confirmed the contained pool architectural (every code cluster swept via XML actual-vs-expected extraction — parser-recovery=Blocker #7; crash tests=cross-file global-merge/instantiation-expr-types; type-engine=mapped/conditional/generic-inference; flow=switch-true/TS18048/typeof-in-type-position; JS-emit=file-inclusion/ordering; TS5107=AMD/UMD tsgo-irrelevant), committed to two blockers: **B412 (+2, 9674/412, Blocker #3 cross-file scope) — mergeSymbol cluster**: a name declared in a `declare module "X"` AUGMENTATION that is ALSO re-exported into X via `export {N} from './other'` → duplicate (TS2300 for a type-alias aug decl, TS2451 for a block-scoped const/let), reported at BOTH the augmentation decl and the re-export specifier with cross-referencing TS6203 "was also declared here". New `checkModuleAugmentationReexportDuplicates` walker + `resolveAugmentationTargetFile` (resolves the `.`/`./` package-index specifier — which `resolveModuleSpecifier` doesn't — to the sibling `index.{ts,tsx,d.ts}` LOCALLY, avoiding a risky global resolver change) + `emitAugReexportDup`. The whole `.d.ts` duplicate-identifier pipeline is skipped and the B92d augmentation walker only covers the target's OWN exports with EXPORTED aug decls, so this shape was uncovered. FP-safe (a re-exported name + a non-mergeable aug decl is always a genuine duplicate). Flips mergeSymbolRexportFunction (TS2451) + mergeSymbolReexportedTypeAliasInstantiation (TS2300). The 3rd cluster member, mergeSymbolReexportInterface, now gets its 2 TS2300 but still fails on a SEPARATE TS2741 (we resolve Row2 to common's `{b}` not the augmentation's first-declared `{a}` — cross-file merge ORDER, deeper Blocker #3, deferred). **B411 (+1, 9672/414, Blocker #1 control-flow narrowing) — narrowByClauseExpressionInSwitchTrue7**: Per the anti-loop rule, fresh EXTRA/MISSING/SWAP=0 and the NONE/OUTPUT pools were re-confirmed exhaustively architectural (a full code-cluster sweep: parser-recovery=Blocker #7 HIGH-risk bespoke; mergeSymbol cluster=Blocker #3 blocked on `.`-package-index resolution; crash tests=cross-file global-namespace-merge / instantiation-expression-types+intersection-comparability; TS5107=AMD/UMD tsgo-irrelevant; objectFreeze/mapped/conditional/generic-inference=type-engine; switch-true/typeof-in-type-position/evolving-arrays/TS18048=flow). Committed to the highest-yield blocker. **B411 (+1, 9672/414) — narrowByClauseExpressionInSwitchTrue7**: `switch (true) { case <cond>: ... }` narrowing. New `narrowBySwitchOnTrue` in `narrowBySwitchClause` (gated on the switch expression being the literal `true` Identifier): the matched clause narrows the scrutinee positively by its case expression via the existing `applyConditionNarrowing` (handles `===`/`!==` discriminants, `instanceof`, `in`, truthiness, `&&`/`||`); every CASE clause appearing before the matched clause narrows negatively (those cases tested false to reach here); a `default` clause narrows negatively by EVERY case clause. Fallthrough between clause bodies is already modeled by the FlowGraphBuilder's BranchLabel merges, so only the per-clause direct-match narrowing was needed. Suppresses the FP TS2339/TS2322 we previously emitted on the un-narrowed union in all three switch-true tests; flips test7 cleanly (its residual is exactly the one TS2322 at the exhaustiveness `const never: never = someDerived` where someDerived narrows to Derived2 — the var-decl assignability path already consults flow narrowing). test3 (case-expression flow sees fallthrough-after-`return` → `never`, + TS2367 must consult narrowing) and test6 (single-member discriminant narrowing→never + union→alias `MyType` display) remain — multi-piece, deferred. ZERO regressions (exact failing-set diff: +1/−0). Method: after exhaustive recon confirmed the contained pool dry, committed to a Blocker BUILD per the user directive._ _Round 157 (2026-06-14, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+2 net (417→415 failing, 9669→9671 passing, ~95.9%), 2 feature commits (B408, B409), ZERO regressions** (exact failing-set diffs). The contained surgical pool was RE-confirmed exhaustively dry this session via a 39-test deep triage across 13 parallel agents + direct analysis (every fresh/skip-logged NONE candidate classified ARCHITECTURAL — per-candidate verdicts in PLAN round-157 note). The win is a Blocker #3-adjacent BUILD per the user's directive to address blockers. **B408 (+1, 9670/416) — errorForUsingPropertyOfTypeAsType01**: a namespace-LOCAL TYPE (class/interface/type-alias) used as a NAMESPACE qualifier in type position (`namespace N { interface Foo{}; var x: Foo.bar }`) → TS2702/TS2713. The file-scoped `checkTypeNameResolved` walker only emits TS2702 for TOP-LEVEL leftmost types and never distinguishes TS2713, and can't see a namespace-local type (its leftmost lookup consults only globals/file-locals). New dedicated FP-safe walker `checkTypeUsedAsNamespaceRefs` resolves each qualified segment through the enclosing-namespace chain (via `nodeToSymbol`) and applies tsc's rule: LEFTMOST segment is the type → TS2713 if the member is a property of its declared type (`getPropertyOfType`, union=common-prop), else TS2702; a NON-leftmost segment is the type (`N.Foo.bar`) → always TS2713. Leftmost emission gated to namespace-local leftmosts (`fromNsChain`) so it never double-emits with the existing top-level TS2702 path. Companion parser fix: `parseQualifiedName` now emits TS1003 'Identifier expected.' (len 0) for a missing right side after `.` (`import lol = N.Foo.` → TS1003 + TS2713). FP-safe by construction (a `Type.member` ref in type position is always a tsc error; `isPureTypeSymbol` excludes Module/NamespaceModule/ValueModule/Enum so clodules+enums stay legal qualifiers). ZERO regressions (exact diff). **B409 (+1, 9671/415) — errorForUsingPropertyOfTypeAsType03**: completes the sibling test by extending the B408 walker with two more narrow FP-safe enum-in-type-position shapes — `Enum.Member.X` (3+ seg QualifiedName) → TS2749 "'Enum.Member.X' refers to a value, but is being used as a type here." (the enum member is a VALUE; a further `.X` makes the whole thing value-as-type), and `Enum["Member"]` / `AliasToEnum["Member"]` (IndexedAccessType) → TS2339 "Property 'Member' does not exist on type 'Enum'." (a member NAME is never a property of the enum VALUE type — members live on `typeof Enum`). Companion: the leftmost TS2713/TS2702 member-presence check now special-cases `type C = typeof Enum` (the typeof resolver returns anyType for a namespace-local enum, so it'd wrongly miss the members → TS2702; the special case consults `enum.exports` directly → TS2713). `resolveTypeRefToEnum` follows one level of `type X = Enum` alias but NOT `type X = typeof Enum` (TypeQuery body → keeps members). ZERO regressions (exact diff). [Round 152 trimmed to STATUS-HISTORY.md.]_ _Round 156 (2026-06-14, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+2 net (419→417 failing, 9667→9669 passing, ~95.9%), 2 feature commits (B406, B407), ZERO regressions** (exact failing-set diffs). Contained pool RE-CONFIRMED exhausted across every vein this session (NONE all-sizes, DIFF EXTRA/MISSING/SWAP, grammar/parser, enum-compat, tuple, indexed-access, utility-types, TS18048 flow, JS-expando, JSDoc, discriminated-union, OUTPUT) via 16 triage-agent classifications + ~25 deep-reads; every remaining candidate is a multi-session architectural blocker (per-candidate triage in PLAN round-156 note + skip-log). The 2 wins are blocker-adjacent FEATURE builds: **B406 (+1, 9668/418) — assignmentToAnyArrayRestParameters**: TS2339/TS2536 for an invalid string-literal index in an indexed-access TYPE (`string[]["0.0"]` → TS2339 at the literal; `T["0.0"]` with `T extends string[]` → TS2536 at the whole access). New checkBadArrayStringLiteralIndex + faithful isNumericLiteralName (canonical iff String(Number(s))===s); NameScope now threads type-param CONSTRAINTS (typeParamConstraints map + typeParamConstraintOf). FP-safe: a numeric-looking-but-non-canonical string is never a valid array member/index; gated to array-shaped object types. **B407 (+1, 9669/417) — parameterListAsTupleType**: three companion pieces — (1) Parameters/ConstructorParameters<F> materialize to a TUPLE of the sig params, ReturnType<F> to the return type (materializeSignatureUtility, symbol==null dispatch, additive/null-on-unmodelable; buildTupleFromTypes extracted); (2) array-literal init vs TUPLE target → per-element TS2322 (checkArrayLiteralElementsAgainstTuple, closes the "Array→tuple needs contextual typing" gap for array-LITERALS); (3) TS2344 for Parameters<typeof Class> / ReturnType<typeof Class> via checkUtilitySignatureConstraint (FP firewall: whole ref resolved errorType + arg is a constructor type). NEXT: surgical pool exhausted; the only lever is a multi-session Blocker BUILD — Blocker #1 flow (TS18048 family ×9), #2 generic inference, #3 cross-file scope, the indexed-access/mapped/conditional type engine, or the enum-assignability model (5 tests, 9-32 errors). [Round 151 trimmed to STATUS-HISTORY.md.]_ _Round 155 (2026-06-14, multi-iteration session, ≥20 iterations per user request, addressing blockers): +6 net (425→419 failing), 6 feature commits (B400–B405), ZERO regressions at every step (exact failing-set diffs). All 6 are FP-safe narrow detectors harvested from the skip-logged/contained pool the broad triage under-counted. **B405 (+1, 9667/419) — assignLambdaToNominalSubtypeOfFunction**: TS2345 for a lambda (arrow/function-expression) argument passed where the parameter is an interface that `extends Function` AND declares its own required property. `interface I extends Function { x: number }; function fn(cb: I){}; fn((a,b)=>true)` → the lambda is a Function but lacks `x` → "Argument of type '(a: any, b: any) => boolean' is not assignable to parameter of type 'I'." New checkLambdaToFunctionSubtypeArgs + lambdaSigDisplay (infers the lambda return: boolean/number/string-literal → widened name). tsc squiggles the WHOLE arrow but only the `function` keyword (8 chars) for a function-expression arg. FP-safe (a bare lambda can never carry the interface's extra required member). ZERO regressions.** **B404 (+1, 9666/420) — contextualTypingArrayDestructuringWithDefaults**: TS2322 for an object-destructuring default value incompatible with the destructured property's type from a TUPLE annotation. `let {length = {a:1}}: [number]` → `length` has type `1` (the tuple's literal length, modeled at Checker.kt:101400), the default `{a:1}` widens to `{ a: number; }`, not assignable to `1` → squiggle on `length`. New walker checkTupleDestructuringDefaultTypes (top-level + fn/method/block/module traversal). FP-safe: object-binding + tuple annotation + object-literal default not assignable to the identifier-named property's type. ZERO regressions.** **B403 (+1, 9665/421) — assignmentStricterConstraints**: TS2322 for `g = f` assigning a generic function value with a STRICTER type-parameter constraint to one with a looser (unconstrained) parameter. `var f=function<T,S extends T>(x:T,y:S){}; var g=function<T,S>(x:T,y:S){}; g=f` → "Type '<T, S extends T>(x: T, y: S) => void' is not assignable to type '<T, S>(x: T, y: S) => void'." + parameter-incompatibility / could-be-instantiated chain + related TS2208 ("This type parameter might need an `extends T` constraint.") on g's S. New standalone walker checkGenericFunctionValueAssignmentConstraints + genericFunctionExprSig helper (mirrors B401's method-override check at the value site). FP-safe (top-level `g=f` of two generic-fn-expr vars, equal tp/param counts, some tp index with f constrained to a sibling tp and g unconstrained, names align, a param typed by that tp). ZERO regressions (exact diff).** **B402 (+1, 9664/422) — genericWithNoConstraintComparableWithCurlyCurly**: TS2352 for `expr as T` where expr has the empty-object type `{}` and T is a type parameter whose constraint cannot overlap `{}` (constraint entirely null/undefined). `function f<T extends null|undefined>(){ let x={}; x as T }` → "Conversion of type '{}' to type 'T' may be a mistake…" + "'T' could be instantiated with an arbitrary type unrelated to '{}'.". New emitTS2352IfEmptyObjectCastToTypeParam, gated behind a new inTypeParamCastPass flag in the AsExpression branch of walkTypeAssertionsInExpr (AsExpression isn't forwarded to the cast callback — CLAUDE.md gotcha). Source identified via empty-object-local tracking (emptyObjectCastLocals, populated in walkStmtsForTypeParamCasts) because the cast pass doesn't populate local var types (getTypeOfExpression returns any). GOTCHA HIT: emptyObjectCastLocals (a HashSet mutated from a pass called during Checker init) MUST be declared before init {} (Kotlin init-order) — first cut NPE-crashed. FP-safe (source must be bare `{}`; constraint a non-empty null/undefined-only union). ZERO regressions (exact diff).** **B401 (+1, 9663/423) — genericImplements**: TS2416 generic-method-override constraint mismatch. A base method `f<Tb extends A>(): Tb` overridden by `f<Td extends B>(): Td` where the base constraint A is NOT assignable to the derived constraint B → the override demands a narrower type parameter than the base provides ("'T' could be instantiated with an arbitrary type unrelated to 'B'"). New narrow detector in checkClassPropertyOverrides: both methods single-type-param, both return their own bare TP, derived constrained, base-constraint (or unknown) not assignable to derived-constraint → TS2416 with the `<T extends B>() => T` sig display + the two-line could-be-instantiated chain. Fires regardless of the structural assignable result (our generic sigs pass trivially). New genericMethodSig helper. FP-safe (only fires when genuinely unsafe; the same-constraint Y and unconstrained Z cases don't). ZERO regressions (exact diff).** **B400 (+1, 9662/424) — nonExportedElementsOfMergedModules**: TS2339 for a NON-EXPORTED nested namespace member referenced across separate blocks of a merged outer namespace (Blocker #3 cross-block scope). A non-exported `namespace B` inside `namespace One {…}` does NOT merge with the `namespace B` inside a SECOND `namespace One {…}` block — each outer block keeps its own private copy — so `B.x` in block 2 (where x lives only in block-1's B) is TS2339 while `B.y` is fine. Our binder flat-merges all of One's members (Blocker #3), wrongly exposing x; new FP-safe AST walker `checkNonExportedNestedNamespaceCrossBlockRefs` (B396-pattern standalone pass) restores per-block scoping for exactly this shape. Firewall: script files only; outer ns ≥2 blocks; recv is a block-local non-exported nested ns, not exported in ANY block, no global binding; member absent from the referencing block's copy yet present in a sibling block's copy (genuine cross-block leak); only DIRECT expression-statement refs scanned. ZERO regressions (exact failing-set diff)._
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
