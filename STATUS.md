# Status

**Phase 4 — Checker buildout.** 9,630 / 10,086 tests passing (~95.5%, 456 failing). _Round 149 (2026-06-13, multi-iteration session, ≥20 iterations per user request): in progress. B363 (+1, 9624/462): `uniqueSymbolAllowsIndexInObjectWithIndexSignature` — TS2418 "Type of computed property's value is 'X', which is not assignable to type 'Y'." for an object-literal computed property `[SYM]` (SYM a unique-symbol const) assigned to an interface that declares a member keyed by the SAME unique symbol, value mismatches. `getMemberName` only keys well-known `[Symbol.X]` members (unique-symbol consts return null) so the member is invisible to the structural table and the relation engine sees only the passing string index signature — NEW AST-level walker `checkComputedSymbolKeyValues` resolves the key→const (via `varDeclIsSymbol`) and matches the interface member declaration directly; squiggle at `[SYM]`, related TS6500 "comes from property 'unique symbol'… on type 'I'"; called from the var-decl init path._ B364 (+1, 9625/461): `errorsForCallAndAssignmentAreSimilar` — TS2820/TS2322 discriminant-property mismatch for an OBJECT-LITERAL ELEMENT of an array literal assigned to `T[]` where `T` is a literal-discriminated union alias (`const ds: Disc[] = [{kind:"hdpvd"}]`, `foo([{kind:"hdpvd"}])`). The relation engine passes such element shapes trivially, so the new walker is the sole emitter; a close string-literal spelling upgrades TS2322→TS2820 "Did you mean 'X'?" (via `getSpellingSuggestionFromNames`). Uses `literalTypeOfExpression` (NO global widening). TWO blockers solved: function-LOCAL type alias `Disc` is unbound (Binder block-scope) → `arrayElementUnionAlias` falls back to `findLocalTypeAlias` (DFS the file for `type X = A|B`, resolve the UnionType body directly — constituent TypeLiterals resolve without a name lookup); function-LOCAL callee `foo` is unbound → NEW `checkLocalCallDiscriminantArgs` resolves a callee to a SIBLING function decl in the same statement list (top-level calls skipped — the integrated arg path owns them)._ B365 (+1, 9626/460): `genericConstructorFunction1` — TS2348 "Value of type 'new (arg: T) => Date' is not callable. Did you mean to include 'new'?" for a plain CALL of a value typed as a construct-signature-ONLY type (`var v2 = v1['k']` where `v1: { [i:string]: new(arg:T)=>Date }`; `v2(args)`). NEW init-walker `checkConstructSigOnlyCalls`: detects `var V2 = V1[...]` where V1's annotated TypeLiteral index-sig value is a `ConstructorType` or a construct-sig-only interface (`isConstructSigOnlyNode` + `findInterfaceDecl`), then flags plain calls (not `new`). The display preserves the generic `T` via a `(){}[]<>`-balanced SOURCE slice (`indexSigValueTypeText`) — the type engine would resolve the function-local `T` to errorType→'any'._ B366 (+1, 9627/459): `switchCaseCircularRefeference` — TS2678 "Type '{ a: \"A\"; b: any; } | { a: \"C\"; e: any; }' is not comparable to type '\"A\" | \"C\"'." (+ a messageChain sub-line naming the trailing union member) for `switch (x.disc) { case x: }` where `x` is a param annotated with a union of object literals sharing a literal discriminant `disc`. The switch subject `x.disc` is a literal union while `case x` is the whole object union — never comparable; FP-safe by shape. NEW dedicated AST walker `checkSwitchParamDiscriminantComparable` threads the param's union annotation (the existing syntactic switch walker has no param-type context)._ B367 (+2, 9629/457): `recursiveTypeComparison2` + `extendArray` — a `declare namespace` body historically SKIPPED all TS2304 checks (ambient types expected). Now the body runs the normal unresolved-name check, but a POST-FILTER keeps only the bare-NAME unresolved family (TS2304 + spelling-variant TS2552) and drops the namespace family (TS2503/TS2833/TS2694) and body-position arg-count (TS2314) — those qualified/cross-namespace resolutions are still incomplete in ambient scope (Blocker #3) and FP-prone (a blunt full-check probe regressed 6 import/ambient/parse-error tests, all non-2304/2552). So a bare undeclared type name like `StateValue<U,V>` inside a declare-namespace interface method now correctly fires TS2304. Probe-then-narrow: full check = net −3 (+3/−6); post-filter = +2/0._ B368 (+1, 9630/456): `narrowingTruthyObject` — TS18047 "'x' is possibly 'null'." for `if (typeof x === 'object') { x.member }` where `x: unknown` (typeof null === 'object', so narrowing unknown by `typeof==='object'` yields `object | null`). FP-safe by EXACT shape: the whole if-condition must be the bare `typeof x === 'object'` binary — the `&& x` / `x &&` truthiness variants have an `&&` root operator so don't match (and they correctly DO narrow null away). Reassignment of x in the then-block suppresses; strictNullChecks-gated; .js skipped. NEW walker `checkUnknownTypeofObjectPossiblyNull`. (A first contained Blocker-#1 flow substep.)_ _Round 148 (2026-06-11, multi-iteration session, ≥20 iterations per user request): +14 net (477→463, 9609→9623, ~95.4%), 14 feature commits (B353–B362 incl. B358b), zero regressions at every committed step per exact failing-set diffs. Themes: stale-skip-log mining (the EXTRA/MISSING/SWAP buckets had stale entries that fell to narrow walkers) + the round-147 parked list + the NEW regex-grammar validator foundation (a faithful scoped port of tsc scanRegularExpressionWorker that fires on every regex corpus-wide with zero FPs). B353 (+1, 9610/476): destructuringUnspreadableIntoRest — object-REST destructuring from a class instance excludes class-declared methods/accessors and private/protected members from the rest type (tsc getRestType + isSpreadableProperty); NEW walker checkObjectRestUnspreadableAccess emits TS2339 for accesses of excluded members on the rest var, with the generic display 'Omit<this|T, "k1" | "k2">' (destructured keys first, then public accessor/method names in declaration order — private/protected names drop silently via keyof semantics) or the literal remaining-props display '{ p: string; }'/'{}' for as-cast/class-typed sources; FP firewall: heritage-free type-param-free index-sig-free non-merged top-level class, accessed name must be class-declared-excluded, reassignment suppresses._ _B354 (+1, 9611/475): potentiallyUncalledDecorators — tsc isPotentiallyUncalledDecorator + decorator return-type rules: TS1329 ('accepts too few arguments… @X()?', span @+name) when EVERY overload of a bare-identifier decorator has minArgs 0, no rest, and fewer params than the position supplies (class=1, property=2, method = sig.params<=2 ? 2 : 3 per tsc getDecoratorArgumentCount); otherwise single all-any-param sigs returning a call-signatures-only in-file interface (own call sigs or heritage to lib ClassDecorator/MethodDecorator/PropertyDecorator — fails the weak-type rule vs TypedPropertyDescriptor) emit TS1270 'void | typeof C' (class), TS1271 (property), TS1270 'void | TypedPropertyDescriptor<() => void>' (method) at the expression span._ _B355 (+3, 9614/472): regExpWithOpenBracketInCharClass ×3 (es5/es2015/esnext) — TS1501 'This regular expression flag is only available when targeting <v> or later.' per-flag minimum targets (tsc regExpFlagToFirstAvailableLanguageVersion: u/y→es6, s→es2018, d→es2022, v→es2024; one per flag char at the flag position) + TS1005 "']' expected." ZERO-width at the terminating '/' for a v-flag regex whose char class never closes (v-mode '[' inside a class opens a NESTED class — the first-pass terminator scan is deliberately nesting-blind per tsc reScanSlashToken). Both ride the existing per-regex emitter block (TS1538/TS1503/TS1532/TS1499/TS1517)._ _B356 (+1, 9615/471): emitClassExpressionInDeclarationFile2 — TS4094 for mixin FUNCTIONS: an exported annotation-less 'function WithTags<T extends Constructor<X>>(Base: T) { return class extends Base {…} }' infers an anonymous-class return inheriting X's private/protected members → TS4094 per member at the fn NAME, and again at the name of an exported class extending a CALL of the fn (B317's mixinCallMembers extended with the mixinFns map; Constructor = in-file ConstructorType alias)._ _B357 (+1, 9616/470): dynamicNamesErrors — computed property names referencing top-level 'const k = <literal>' resolve to literal KEYS (numeric 1 ≡ string "1"): TS2717 + related TS6203 for a later interface property re-declaring an earlier member's key at a different type (computed-involved pairs only), and TS2322 with the 2-level 'Types of property [c0] are incompatible' messageChain for assignments between vars annotated with ALL-computed-literal-key interfaces (named→named is relation-engine-blocked, so the walker is the sole emitter; chains must use Diagnostic.messageChain, NOT embedded newlines — the formatter prefixes each chain line)._ _B358 (+1, 9617/469): emitCapturingThisInTupleDestructuring1 — TS7017 'Element implicitly has an any type because type typeof globalThis has no index signature.' at the property NAME of 'this.<unknown>' when the containing arrow captures GLOBAL this (the TS7041 condition in checkThisInExpr's PropertyAccess branch); gated noImplicitAny-family (explicit @strict:false suppresses — noImplicitThisFunctions) and name must be neither a KNOWN_GLOBAL nor in the merged globals map (topLevelLambda3's this.window stays silent)._ _B358b (+1, 9618/468): implicitAnyDeclareVariablesWithoutTypeAndInit — the B337 captured-reads walker now accepts VAR declarations (not just let) and NEVER-assigned variables ('var y; function f() { y }' — the type cannot be determined at the capture either way → TS7034 at the decl + TS7005 at each captured read; the ≥1-same-scope-assignment requirement was an over-gate)._ _B359 (+1, 9619/467): varianceAnnotationValidation — TS2636 'Type Controller<sub-T> is not assignable to type Controller<super-T> as implied by variance annotation.' at the 'out T' span when T appears as a DIRECT parameter type of a function-typed property (a bare-T param always breaks covariance), with the 4-line sub-T/super-T elaboration chain naming the FIRST offending property; oruRenderType gained void/never/undefined/symbol/bigint/object (the missing 'void' silently killed the first run — renderers bail null and the walker continues)._ _B360 (+1, 9620/466): umdGlobalAugmentationNoCrash — the namespace-Module branch of checkMemberAccessMissing now BAILS for UMD globals (identName in umdGlobalNames): 'export as namespace X' is misparsed into a bogus 'namespace X' whose exports don't reflect the real module's, so member access through the UMD global is unknowable (we FP'd TS2339 'foo does not exist on typeof React'; the expected TS2451 pair was already emitted by 73j)._ _B361 (+1, 9621/465): regularExpressionAnnexB — scoped FAITHFUL port of tsc scanRegularExpressionWorker (annexB=true; scanner.ts 2613–3609 via git cat-file) covering the term/quantifier grammar + atom/class escapes: TS1005('}'), TS1125, TS1505, TS1506, TS1507, TS1508, TS1510, TS1512, TS1516, TS1531, TS1535 with tsc's exact recovery quirks (the '}'-expected pos-- fall-through; the same-start dedup that kills the \\i TS1535 after \\u's TS1125; AnnexB's empty-min '{' early-break). Out-of-subset constructs BAIL the whole regex silently (groups, v flag, braced \\p bodies, octal escapes, unterminated classes) — zero corpus-wide regressions despite firing on every regex. regularExpressionScanning ×3 (386 errors: capture groups + backreference validation + Unicode property tables + suggestions + v-mode class sets) parked as the full-worker dedicated session._ _B362 (+2, 9623/463): regularExpressionUnicodePropertyValueExpressionSuggestions ×2 — \\p{...} property validation in the B361 walker (u-mode): TS1524 'Unknown Unicode property name.' / TS1526 'Unknown Unicode property value.' / TS1529 'Unknown Unicode property name or value.' each with the position-less '!!! related TS1369: Did you mean X?' suggestion (the formatter already renders fileName-less related info location-free); tables ported from tsc scanner.ts 4073–4090 (nonBinary aliases gc/sc/scx, General_Category/Script value sets, binary props; Script_Extensions values = Script values), suggestion via the tsc core.ts getSpellingSuggestion/levenshteinWithMax port; binary-props-of-strings and empty names/values bail (v-mode strings rules out of subset). Init-order trap re-hit: tables must live at FILE level, not as class vals after init._ _Round 147 (2026-06-11, multi-iteration session, ≥20 iterations per user request): +8 net (485→477, 9601→9609), 8 feature commits (B345–B352), zero regressions at every committed step per exact failing-set diffs (one transient regression caught pre-commit at B348 — chain-temp placeholders leaked into class METHOD bodies because methods bump functionScopeDepth, not withFreshTempVarCounter; destructuringUnspreadableIntoRest caught it). The round landed the ES-DECORATORS (TC39) downlevel family end-to-end: B345 CLASS decorators, static-block form ('let C = (() => { let _classDecorators…; var C = class { static { _classThis = this; } static { _metadata + __esDecorate(kind:class) + reassign + defineProperty(Symbol.metadata) + __runInitializers } }; return C = _classThis; })();' + export { C }; new __esDecorate/__runInitializers helper texts through inline/tslib/CJS-preserve paths) (+1 modulePreserveImportHelpers). B346 instance FIELD decorators, target<ES2022 comma form ('return _a = class C {…}, (() => {…})(), _a;' — NEW BinaryExpression.multiLineComma flag for tsc's one-operand-per-line geometry; decorated field initializers rewrite to __runInitializers and ride the es2015 field-to-ctor downlevel; CJS keeps the IIFE-initialized export-let local via esDecoratedClassNames) (+1 parameterDecoratorsEmitCrash). B347 decorated anonymous class EXPRESSIONS (fields + static fields + auto-accessors: __setFunctionName block, decorator EVAL in member order vs APPLY order static-nonfield/instance-nonfield/static-field/instance-field, '#<name>_accessor_storage' (+_1 dedup) get/set pairs, per-side extras-fold chains) (+1 esDecoratorsClassFieldsCrash). B348 objectRestSpread: tsc-faithful destructuring-DECLARATION flattening (pending mini-pattern groups; computed keys split with key temps feeding 'typeof _g === "symbol" ? _g : _g + ""' exclusions; pattern targets capture the default-checked value before recursing) + chain-temp PRINT-ORDER naming (top-level chain temps allocate as placeholders renamed after the file transform, base = final tempVarCounter — file-hoisted 'var _a.._d' grab the first names; fn-scope temps name immediately) + assignment-form computed keys + needsValue rest-assignments + async-GENERATOR downlevel at es2017 (ES2018 feature; gate split from plain async at 3 sites) (+1). B349 for-await-of downlevel (__asyncValues protocol: try/for(_d=true, c_1=__asyncValues(c); c_1_1 = yield c_1.next(), _a = c_1_1.done, !_a; _d=true)/catch(e_1_1){e_1={error:e_1_1}}/finally{try{if(!_d && !_a && (_b=c_1.return)) yield _b.call(c_1)}finally{if(e_1) throw e_1.error}}; uniqueName per-file counters for e_1/c_1/c_1_1; __asyncValues sorts AFTER __awaiter; synthetic catch blocks stay single-line) (+1 operationsAvailableOnPromisedType es2015 JS). B350 async param defaults containing AWAIT move into the __awaiter generator (outer fn takes '_a' placeholders for fn.length; '__awaiter(this, arguments, void 0, function* ({ foo = yield … }))'; CJS dynamic-import rewrite now reaches PARAM defaults via rewriteCjsDynParams) (+1 expressionsForbiddenInParameterInitializers). B351 legacy class-decorator STATIC self-reference alias ('var C1_1;' + 'static { C1_1 = this; }' first member + static-ref rewrite + 'C1 = C1_1 = __decorate(…)') (+1 staticInitializersAndLegacyClassDecorators). B352 classFieldSuperAccessibleJs1: collectClassInstanceFields now SKIPS static PropertyDeclarations (statics ARE reachable via the constructor prototype chain — the TS2855 walker FP'd on 'super.<staticField>' in static blocks) + NEW TS2565 'Property X is used before being assigned.' for 'super.<expandoStatic>' reads in derived STATIC BLOCKS where X is a top-level 'C.X = …' expando of the base (strict-gated; class-body members of the same name win) (+1). Triaged-not-built: potentiallyUncalledDecorators (TS1329/TS1270/TS1271 decorator-signature family, 19 errors), decoratorMetadataTypeOnlyImport (bare-missing TS2307 — the round-85 reverted trap), operationsAvailableOnPromisedType errors halves (TS2773 'did you forget await' related-info family), jsFileCompilationTypeArgumentSyntaxOfCall (.jsx generic-call rules + JSX recovery), propertyAccessExpressionInnerComments (optional-chain comment duplication quirks), bundledDtsLateExportRenaming (outFile decl bundling), es6ExportEqualsInterop (ambient export= interop breadth)._ _Round 146 (2026-06-11, multi-iteration session, ≥20 iterations per user request): +8 net (493→485, 9593→9601), 9 feature commits (B338–B344), zero regressions at every committed step per exact failing-set diffs (one transient regression caught pre-commit at B343 — an UNREFERENCED namespace import under isolatedModules must still elide, isolatedModules_resolveJsonModule caught the blanket keep). The round built out the private-class-elements + class-state emit family: B338 class-EXPRESSION computed-name captures (the no-statics transformClassExpression path dropped the body transform's leading/trailing capture statements — now hoists temps + wraps in '(_b = class …, _a = expr, _b)') (+1). B339/B339b private-field ACCESS downlevel (__classPrivateFieldGet/Set for reads + plain '=' writes via the new privateFieldEnvStack; destructuring-target setter-object form '(_a = this, { key: ({ set value(_b) {…} }).value } = rhs)'; hoisted temps insert AFTER prologue directives; ctor propInits after synthetic hoisted vars; multi-class WeakMap vars merge into ONE var statement with detached-comment transfer) (+2). B340 class-EXPRESSION private state (state vars into hoistedVarScopes — ONE combined var with temps; private METHODS '_m = function _m(){…}' + WeakSet brand + ctor '.add(this)'; static-private '{ value: init }' descriptor vars; bare '_<field>' names for no-statics anonymous classes vs '_<assignedName>_<field>' on the __setFunctionName path) (+1). B341 class-DECLARATION private methods + class-alias substitution ('_a = Foo, _Foo_instances = new WeakSet(), _Foo_x = function …' as ONE comma statement; method calls → '__classPrivateFieldGet(this, _brand, \"m\", _m).call(this)'; static blocks referencing this force the alias; the IIFE rewrites this/className→alias and is always multi-line; static blocks interleave with static-prop assignments in MEMBER order) (+1). B342 parameter-default-to-body conversion (a class-capture temp in a param default converts ALL the fn's defaults to synthetic 'if (p === void 0) { p = init; }' single-line body blocks; killed the flattenRestParameters DOUBLE-transform of params; __setFunctionName from the PARAM name; statics-path computed captures hoist + splice BEFORE __setFunctionName) (+1). B343 isolatedModules namespace-import retention under CJS (kept iff referenced anywhere in the ORIGINAL source — type positions erase pre-CJS) + tsc helper PRIORITY order (__decorate between __setModuleDefault and __importStar, __metadata after) (+1). B344 super-in-static-block ('extends (_c = Base)' heritage capture + 'Reflect.get(_c, \"X\", alias)' via new replaceSuperReadsIn* walkers) (+1). Parked with analysis: staticFieldWithInterfaceContext (tsc assigns generated temp NAMES at PRINT time per scope — fn-local temps avoid ENCLOSING scopes' names (_r after file _a.._q) while sibling fns reuse _a; our transform-time per-function reset can't reproduce both), ES-decorators downlevel ×3 (__esDecorate/__runInitializers class IIFE), for-await-of (__asyncValues protocol), async-param-defaults-into-generator, objectRestSpread declaration downlevel, propertyAccessExpressionInnerComments, es6ExportEqualsInterop, classFieldSuperAccessibleJs1 (TS2855-in-JS gating + expando definite-assignment TS2565); systemModule17 + dynamicImportWithNestedThis (@module system/umd) added to TSGO-RELEVANCE curated list._ _Round 144 (2026-06-11, multi-iteration session, ≥20 iterations per user request): +15 net (520→505, 9566→9581), 9 feature commits (B321–B329), zero regressions at every committed step per exact failing-set diffs (two transient regressions caught and fixed pre-commit at B324 — 'constructor (export a' needs the modifier-kind exception per tsc isStartOfParameter, and 'class C extends A ¬ {' needs the heritage-list Unknown-token skip — plus one at B325: '=>'-followed missing bodies must stay NULL or dottedModuleName's erased 'function f(x)=>…;' re-appears in the emit): B321 CJS deep ++/--/exported-local rewrite (tsc module.ts visitPreOrPostfixUnaryExpression — prefix exports.x = ++x; postfix discarded exports.x = (x++, x); postfix value-used (exports.x = (_a = x++, x), _a) with file-hoisted temps; comma-left operands are value-discarded; iterative left-spine) (+1). B322 CJS destructuring-assignment flattening (tsc flattenDestructuringAssignment via the module transform's export-wrap callback — multi-element patterns hoist _a = value, single-element read value.prop directly; clause aliases of direct-exported vars join assignment chains; clause sync assignments only for INITIALIZED declarations per appendExportsOfVariableStatement; pass runs BEFORE the global direct-export identifier rewrite) (+1). B323 static computed-name captures (tsc classProperties pendingExpressions — moved fields capture their computed name in MEMBER order; pending captures HOST in the next kept member's brackets 'static [(_a = A.p1, A.p2)]()'; leftovers flush after the class; moved statics assign A[_a] = init) (+1). B324 reservedWords3 ×2 — param-list reserved-word TS1390 abort (modifier kinds export/default/const/in excepted), missing-'(' returns an EMPTY param list with nothing consumed, aborted signatures get MISSING zero-width Block bodies (TS7010 treats them bodyless with '(Missing)' displays; emit prints 'function f1() { }'), missing fn names synthesize zero-width Identifiers (empty-name enum+function TS2567 merge), enum/class parsers STOP on failed '{', heritage tail skips Unknown tokens with TS1127 (+2). B325 overloadConsecutiveness ×2 — general TS1144 "'{' or ';' expected." + missing-block bodies for fn decls/methods (EXCEPT '=>' follows — tsc erases those from emit), class-member ',' skip with TS1068, statement-level ',' TS1128 (never the comma-expression path), checker nodeIsPresent semantics (missing blocks are signatures for TS2393/TS2389 but the FINAL missing-impl rule keys on body-node EXISTENCE), NEW pair-gap TS2391 per tsc checkFunctionOrConstructorSymbolWorker (previousDeclaration.end !== node.pos — onlyTriviaBetween over a missing-block/closeBrace anchor) (+2). B326 arrowFunctionsMissingTokens ×2 — '{' after a BARE identifier/comma paren-group commits to an arrow ('=>' expected; interior gate load-bearing — parserUnparsedTokenCrash2 caught the broad version) + arrow bodies starting with statement-only keywords get "'{' expected." + an IgnoreMissingOpenBrace block ('() => var k = 10;}' → '() => { var k = 10; }') (+2). B327 staticsInAFunction ×2 + bonus overloadingStaticFunctionsInFunctions ×2 — statement-level 'static'+same-line-ident consumes the modifier with TS1128 (tsc MissingDeclaration), arg-list ','-skip-re-enter extended to ':', non-bare-identifier expression statements followed by same-line '{' get ';' expected at the '{' (bare identifiers stay silent — TS1434 owns them, interfaceDeclaration4 caught the broad version) (+4). B328 TS1108 joins the grammar-suppression set (tsc grammarErrorOnFirstToken/hasParseDiagnostics; both driver filters) (+1). B329 'export default async' is a FunctionDeclaration only when same-line 'function' follows — 'export default async(...)' is a CALL of the identifier (+1, JS half; the errors half needs the NEW TS1262 'await is a reserved word at the top-level of a module' family + import-clause await binding). Parked with analysis in the round-144 plan note: jsxRuntimePragma ×3 (automatic-runtime feature), objectRestSpread (destructuring-declaration downlevel), propertyAccessExpressionInnerComments (comment fidelity), reservedWords2 (~10 mechanisms), commonMissingSemicolons (TS1435 pool), constructorWithParameterPropertiesAndPrivateFields_es2015 (private-field downlevel), bundledDtsLateExportRenaming (outFile decl bundling)._



---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
