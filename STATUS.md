# Status

**Phase 4 — Checker buildout.** 9,107 / 10,078 tests passing (~90.37%). _Round 86 (2026-05-31, `/goal` multi-iteration session, +9 net 9098 → 9107, zero regressions): re-triaged the EXTRA/MISSING + bounded-NONE surgical tail that round 85 declared "exhausted" and found 9 more flips. **B98.r27** suppress FP TS2430 for index-sig narrowing vs the empty-object `{}` base (the B98.r25 multi-base check also ran for single-base `interface B extends A` with an own index sig; `{}` is a universal assignment target so a `{}`-valued base index can never lose a conflict) — flips `hidingIndexSignatures`. **B98.r28** per-tslib-install TS2343 dedup + `__rest` for object-rest params — FunctionDeclaration params weren't walked for object-rest binding patterns, and the `__awaiter`/`__generator` dedup was program-wide (should be per-resolved-tslib-install: shared install dedups program-wide, distinct per-package installs each report) — flips `tslibMultipleMissingHelper`. **B98.r29** TS2304/TS2552 for unresolved local `export {...}`/`export type {...}` specifiers (the global-`in globals` test was fooled by the over-merged map binding the export specifier's own name; now requires a real non-ExportSpecifier decl) — flips `duplicateErrorNameNotFound`. **B98.r30** TS2610/TS2611 accessor↔property override mismatch (extended `classMemberShapeMismatchDiagnostic`, handled the base-accessor-pair multi-declaration path, gated extends-only + non-abstract base) — flips `inheritanceMemberPropertyOverridingAccessor` + `inheritanceMemberAccessorOverridingProperty` (+2). **B98.r31** TS2675 cannot extend a class with a private constructor (reuses `findEffectiveConstructorVisibility`, resolves bare + qualified bases) — flips `extendPrivateConstructorClass`. **B98.r32** TS2347 for `this.foo<T>()` where `foo` is an any-typed class member (new `isImplicitAnyThisMember` via `callWalkerClassStack`) — flips `invokingNonGenericMethodWithTypeArguments2`. **B98.r33** TS2653 class expression must implement inherited abstract members (new `checkClassExprAbstractImpl` walking the base chain) — flips `classExpressionExtendingAbstractClass`. **B98.r34** TS2724 named-import spelling suggestion + scoped `.js`→`.ts` resolution + related TS2728 — flips `missingMemberErrorHasShortPath`. Deferred (logged): `tslibNotFoundDifferentModules` (TS2354 per-package found/not-found vs TS2343 interaction is order-dependent + risks destabilizing r28), `constantEnumAssert` (TS1355 needs enum-member-literal-value semantics), `declarationEmitExpandoPropertyPrivateName` (TS4032 decl-emit privacy), `newExpressionWithCast` (parser misparse of `new <any>X()`). _Round 85 (2026-05-31, `/goal` multi-iteration session): **B98.r26** TS2343 `__awaiter` for plain `async function` under `importHelpers` + tslib-missing-helper — the existing `checkMissingTslibHelpers` handled async-GENERATORS (`__asyncGenerator`/`__await`) but not plain async (`__awaiter`); added a non-generator branch emitting at the function name, with PER-PROGRAM dedup (`reportedMissingTslibHelpers`) mirroring TypeScript's `requestedExternalEmitHelpers` bitmask (the error fires once total, not per-occurrence). Flips `tslibMissingHelper` (+1, 9097 → 9098), zero regressions. **B98.r25** TS2430 for multi-base inherited index-signature conflicts — `interface E extends A, D {}` where A has `[s:string]: number` and D has `[s:string]: string` → E inherits A's `number` (first-base-wins) which is not assignable to D's `string` → "Interface 'E' incorrectly extends interface 'D'." + "'string' index signatures are incompatible." chain. New `checkMultiBaseIndexSigConflicts` runs before the own-member early-return; conservative (only fires on non-error/non-any value types failing the structural relation). Flips `inheritedStringIndexersFromDifferentBaseTypes` (+1, 9096 → 9097), zero regressions. **B98.r24** TS2540 for intersection-property writes — `x: A & B; x.p = v` is read-only only when `p` is readonly in EVERY constituent that declares it (a single writable constituent — plain field or setter — makes it writable); getter-only + `readonly`-modifier members count as readonly. Confined to a new `Type.Intersection` branch in `isReadonlyPropertyAccess` (no global broadening). Flips `intersectionsAndReadonlyProperties` (+1, 9095 → 9096), zero regressions. (One reverted attempt this round: bare-specifier genuinely-missing TS2307 — -6 FP on symlinked/untyped/absolute-path resolution, architectural, logged.) **B98.r21b** TS2307-suppression refinement — a relative import resolving to an untyped `.js`/`.jsx` sibling is suppressed (no TS2307) even when `noImplicitAny` is OFF (TS resolves it as an implicit-`any` module silently); only under noImplicitAny does it become TS7016. Fixes the B98.r1 residual EXTRA TS2307 on `moduleResolutionWithExtensions_notSupported` (the existing `checkJsxImportResolutions` already emitted its TS6142s; only the stray `./js` TS2307 blocked it). +1 (9094 → 9095), zero regressions. **B98.r21** TS7016 (implicit-any JS module) SWAP — a relative import resolving to an untyped `.js`/`.jsx` sibling that is in the raw input set but parsed-not-bound (no `allowJs`) is NOT a missing module; under `noImplicitAny` TypeScript reports TS7016 ("Could not find a declaration file for module 'X'. '/path.js' implicitly has an 'any' type.") rather than TS2307. Threaded a new `allInputFileNames` set into the Checker (so it can see the skipped `.js` files); new `resolveRelativeJsSibling` resolver; pure SWAP gated on noImplicitAny + a known JS sibling (every other case keeps TS2307). Flips `moduleResolution_relativeImportJsFile_noImplicitAny` (+1, 9093 → 9094), zero regressions. **B98.r20** TS2413 'number' index type not assignable to 'string' index type — for a class/interface with BOTH index sigs (own or inherited from `extends` bases), the number-index value must be assignable to the string-index value; emit at the type name. Gated to ANONYMOUS object-literal value types on both sides (named/Reference values skipped — incomplete-comparison FP surface). Effective number sig is own-first then inherited (mirrors the existing string lookup). Flips `inheritedStringIndexersFromDifferentBaseTypes2` (+1, 9092 → 9093), zero regressions. `indexerConstraints` (the other TS2413 test) deferred — needs declaration-merged-interface handling + index-sig-position squiggle + named-type comparison._ _Round 84 (2026-05-31, bounded NONE-PRODUCED parser/config tail, +6 net 9076 → 9082): **B98.r6** TS1194 for `export {x} from "mod"` in an identifier-named namespace (real-string-vs-recovery-literal disambig via raw source char at `spec.pos`) — flips `exportDeclarationsInAmbientNamespaces2`. **B98.r7** TS1206 for parameter decorators under standard (non-experimental) decorators — flips `parameterDecoratorsEmitCrash`. **B98.r8** TS6053 for missing `/// <reference path>` targets (+3: `selfReferencingFile2`/`declarationEmitInvalidReference2`/`invalidTripleSlashReference`); regex MUST be local (mid-file `private val` → null during init pipeline → NPE → +1158 cascade), skip `/.`-prefixed harness-lib paths. **B98.r9** TS5069 for `declarationDir` without `declaration`/`composite` (new `declarationDir` option + check) — flips `declarationEmitToDeclarationDirWithoutCompositeAndDeclarationOptions`. Zero regressions across all six._ _Round 83 (2026-05-31, B98 TS2307 cluster, +3 net): **B98.r4 relative `require("./x")`-in-JS TS2307 — +1 (9075 → 9076), flips `noCrashOnParameterNamedRequire_ts`, zero regressions.** Threaded an `includeRequire` flag through the B98.r3 collector; for JS-like files (`.js/.jsx/.cjs/.mjs`) it also collects `require("…")` call args (TS treats them as CommonJS module refs even when `require` is a local param). Same relative-only + index-aware-resolver + config-guard validation. **B98.r3 relative dynamic-import TS2307 — +1 (9074 → 9075), flips `dynamicImportInDefaultExportExpression_ts`, zero regressions.** New `collectDynamicImportSpecifiers` walker (mirrors the Emitter's `expressionContainsDynamicImport` traversal incl. iterative BinaryExpression right-spine + ObjectLiteral/ExportAssignment recursion) accumulates `import("...")` string-literal args across all statements; relative specifiers that the index-aware resolver can't find emit TS2307. Bare specifiers deliberately excluded (B98.r2 dead-end); gated to no path/baseUrl/rootDirs + `!noResolve`; applies to single-file too. **B98.r1 relative-missing TS2307 — +1 (9073 → 9074), flips `shorthandPropertyAssignmentInES6Module_ts`, zero regressions.** Added a node/commonjs relative-import TS2307 gate (gated to explicit `@module: commonjs` + default resolution + no path/root config) backed by a NEW comprehensive index-aware resolver `resolveRelativeIncludingIndex` (checks every module extension `.ts/.tsx/.d.ts/.js/.jsx/.json/.mts/.cts/.mjs/.cjs` + `./`-prefixed and unprefixed fileResults keys + `<spec>/index.<ext>`) plus a narrow `hasTsErrorSuppressionAbove` line-scan that suppresses the emit when the import line or the line above carries `@ts-expect-error`/`@ts-ignore`. Both guards were load-bearing — the naive gate FP'd on `.js`-target imports (`commonJsExportTypeDeclarationError`), `./`-prefixed @filename keys (`enumNoInitializer…`), and a `@ts-expect-error`'d missing import (`isolatedModulesExportDeclarationType`). One residual EXTRA TS2307 remains on the already-failing `moduleResolutionWithExtensions_notSupported` (`./js` → `/js.js` untyped module, not in fileResults without allowJs) — noise on a test that needs unimplemented TS6142, not a regression._ _Round 82 (2026-05-30, ULTRACODE multi-agent session): **+9 net (9064 → 9073)** via two parallel diagnosis workflows over the bounded NONE-PRODUCED tail. Landed B95b/c/d/e/g/h (functionCall18, errorForwardReference, parenthesizedArrowExpressionASI, exactSpellingSuggestion, declarationEmitComputedPropertyNameEnum2, contextualTyping39/41), **B98a** (unmetTypeConstraintInImportCall — TS2344+TS2208 for `import('./m').Foo<T>` type-args), **B98b** (thisPredicateInObjectLiteral — TS2526 for a `this` type in an object-literal method predicate `x is this`; new FP-safe walker, BinaryExpression iterative to survive `binderBinaryExpressionStress`). Zero regressions across all. **Crossed 90%.** Landed B95b (functionCall18), B95c (errorForwardReference — inherited-ctor TS2554), B95d (parenthesizedArrowExpressionASI — TS2352 `as`-cast), B95e (exactSpellingSuggestion — enum TS2551), B95g (declarationEmitComputedPropertyNameEnum2 — TS2304 in emitDeclarationOnly), **B95h** (contextualTyping39/41 — TS2352 for a function-expr cast whose call-sig return is a different primitive: `<{():number}> function(){return "err"}` → `() => string` vs `() => number` + "Type 'string' is not comparable to type 'number'."). Zero regressions across all. **Crossed 90%.** Landed: **B95b** overload-arity TS2554 w/ explicit type args (`functionCall18`); **B95c** inherited-ctor TS2554 (`errorForwardReferenceForwadingConstructor`) — class w/ `extends` base + no own ctor inherits base ctor arity via a fixpoint; squiggle = whole `new X()`, TS6210 anchored at Parameter.pos (pre-modifier); **B95d** TS2352 `undefined as T` (`parenthesizedArrowExpressionASI`) — shared core + AsExpression caller; **B95e** enum-receiver TS2551 `E.foo` spelling (`exactSpellingSuggestion`) — gated to property-access/non-numeric so `E[0]` reverse-mapping doesn't FP. Diagnosis workflow classified TS2540/TS2367/TS2488/TS2403/TS2694/decl-emit clusters as architectural; `global` (spellingSuggestionGlobal*) found architectural (binder binds `declare global{}` as a NamespaceModule named "global" → `global.x` resolves). _Round 82 earlier: **B95b — +1 (9061 → 9062), flips `functionCall18_ts`.** `foo<string>("hello")` against `foo<T>(a,b)` + `foo(a)`: explicit type args filter the overload set to GENERIC signatures that can accept that many type args (the non-generic `foo(a)` is inapplicable), then arity-check against that subset → TS2554 "Expected 2 arguments, but got 1." + TS6210 for param 'b'. `FuncParamInfo` gained an `overloadSigs: List<OverloadSig>` field (per-overload-signature arity + type-param shape, excl. impl); `checkArgCountInExprCore` added a `isOverloaded && typeArguments non-empty` branch. TS6210 emitted only when the applicable subset is a single signature.  _Round 81 (2026-05-30): **TP-constraint substitution in fn-param mismatch elaboration — +1 (9060 → 9061), flips `typeParameterConstrainedToOuterTypeParameter_ts`, zero regressions.** `getFunctionMismatchElaboration` now substitutes each call-signature TypeParam by its CONSTRAINT for display in the bare-TP-target vs `Array<TP>`-source param shape, instantiating the inner `<U extends T>` constraint through the enclosing `Type.Reference`'s type-arg mapper (`A<string>` → `T=string`) so the chain reads `Type 'U' is not assignable to type 'string[]'.` / `Type 'string' is not assignable to type 'string[]'.` instead of the raw `T[]`/`T`. Display-only (runs after incompatibility already decided), tightly gated → FP-safe.  _Round 80e (2026-05-29): **B97 TS2320 generic-vs-non-generic inherited-method conflict — +2 (9058 → 9060), flips `genericAndNonGenericInheritedSignature1/2_ts`, zero regressions.** `checkInterfaceMultiBaseConflicts` computed the conflicting method's return type via `getTypeFromTypeNode` WITHOUT the method's type-param scope, so a generic base method `f<T>(x:T):T` resolved its return to `errorType` and the conflict was suppressed. Now tracks `hasTypeParams` per base method and treats a generic-vs-non-generic same-name method pair from two bases as a structural non-identity → TS2320 + 'Named property X ... are not identical.' chain.  _Round 80d (2026-05-29): **B96 TS2300 'prototype' for class+namespace clodule merge — +2 (9056 → 9058), flips `augmentedClassWithPrototypePropertyOnModule_ts` + `mergedClassWithNamespacePrototype_ts`, zero regressions.** A class + same-named namespace merge into a clodule; the class's implicit static `prototype` conflicts with any `prototype`-named member in the namespace body → TS2300. New `checkClassNamespacePrototypeConflict` walker emits at the namespace member's name; the class is found via the merged `globals` symbol (Class+Module canMerge), covering same-file and cross-file (class in `.d.ts`, namespace in `.ts`) shapes.  _Round 80c (2026-05-29): **B95 overload-arity TS2554 for direct overloaded-function calls — +3 (9053 → 9056), flips `functionOverloads29/34/37_ts`, zero regressions.** `checkArgumentCounts` collapsed every overloaded function to `(0, MAX, rest, overloaded)` and the call-site check skipped `isOverloaded`, so `foo()` against overloads all requiring 1 arg never fired TS2554. `collectFuncDecls` now accumulates the real arity RANGE across the overload SIGNATURES (excl. impl) + preserves the first sig's params (for TS6210); the call-site emits TS2554 ONLY when arg count is outside [min,max] for ALL overloads (within range → an overload may match → TS2769). Gated to calls without explicit type args.  _Round 80b (2026-05-29): **namespace-aware heritage base resolution for TS2416 — +1 (9052 → 9053), flips `interfaceDeclaration3_ts`, zero regressions.** `checkClassPropertyOverrides` resolved `implements`/`extends` base names via `globals[baseName]` only, so a class inside `namespace M { interface I1 {...} }` whose base `I1` is also a global bound to the GLOBAL `I1` (reversed-direction FP TS2416 on non-shadowing classes, missed the real mismatch on the shadowing one; qualified bases `M2.M3.I1` took only the last segment). Fix: thread an `enclosingNs` stack through `checkPropertyOverride*` (pushed per `ModuleDeclaration`); bare names consult the enclosing-namespace chain innermost-first before globals, qualified names use `resolveQualifiedValueSymbol`; falls back to `globals` when not shadowed.

---

Older round notes (Round ≤80b and the full B-series history) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — when it grows past that, move the oldest into `STATUS-HISTORY.md` (do NOT let it balloon; it ballooned to 351 KB / unreadable-in-one-Read before the 2026-05-30 trim).
