# Status

**Phase 4 — Checker buildout.** 9,693 / 10,086 tests passing (~96.1%, 393 failing). _Round 166 (2026-06-15, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+2 net so far (395→393 failing, 9691→9693 passing), 2 feature commits (B427, B428), ZERO regressions (exact failing-set diff: only jsFileCompilationBindDeepExportsAssignment flipped).** Method: re-confirmed the contained DIFF/SWAP/MISSING pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; mine_small_diffs empty — every error-line-diff test plan-mentioned). Ran a 14-candidate deep code-path-tracing workflow (read-only agents, no gradle) — agents again systematically over-classified architectural (e.g. they tagged jsFileCompilationBindDeepExportsAssignment "architectural/high-confidence" yet a direct corpus-FP analysis found a corpus-EXHAUSTIVE FP-safe gate). Direct-investigated optionalParamTypeComparison (fn-vs-fn optional-param `| undefined` widening): implemented + REVERTED — line 4 fixes cleanly but TS's `getBestMatchingType`/`findMostOverlappyType` produces an intractable asymmetry on line 5 (inner "to" keeps `| undefined` for `number→boolean|undefined` but drops it to bare `number` for `boolean→number|undefined`); not cleanly replicable, no test flips → reverted to keep commit clean. **B427 (+1, 9692/394) — jsFileCompilationBindDeepExportsAssignment (TS2339, CommonJS deep-exports-read, Blocker #5 substep)**: in a checkJs `.js` CJS module a DIRECT `exports.X = …` DECLARES export X, but `exports.X.<deeper> = …` (using `exports.X` as a receiver) is a READ of `exports.X`; when X was never directly declared it doesn't exist → TS2339 "Property 'X' does not exist on type 'typeof import(\"<base>\")'." New `checkJsModuleExportsDeepReads` (modeled on B424's dedicated-walker pattern): collects created exports (`exports.X=`/`module.exports.X=`/`module.exports={…}`), bails on opaque shape (`module.exports=<non-obj>`/`exports=…`), shadowed `exports`, or ESM files; then fires on the DEEP-READ shape (`exports.X` as receiver of a further access) for absent X. **FP firewall (corpus-EXHAUSTIVE)**: across the whole corpus the ONLY checkJs file with a deep read of an uncreated export member is the single target — every other `exports.X` read is of a directly-declared member. Reusable: `checkJsModuleExportsDeepReads` + the deep-read-shape gate (extend for bare `exports.X` reads / `module.exports.X` reads when a consumer appears). **B428 (+1, 9693/393) — jsCheckObjectDefineThisNoCrash (TS2339)**: reading `this.X` in a checkJs class constructor that uses `Object.defineProperty(this, "X", …)` — tsc does NOT treat that call as a property declaration, so `this.X` (e.g. as the first arg of a further `Object.defineProperty(this.X, …)`) does not exist on the class → TS2339 "Property 'X' does not exist on type '<Class>'." New `checkJsObjectDefinePropertyThisReads` (B419/B424/B427-style dedicated walker, `.js` checkPropertyAccess skip intact). FP firewall corpus-EXHAUSTIVE: gated to a class ctor containing `Object.defineProperty(this, …)` (the single corpus file with that shape); declared set = class members + `this.X=` writes (defineProperty names deliberately excluded, matching tsc). NEXT: contained pool exhausted; remaining is multi-session Blocker BUILDs (JS-expando/CommonJS #5, flow #1, generic-inference #2, cross-file #3, utility-type materialization)._ _Round 165 (2026-06-15, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+3 net (398→395 failing, 9688→9691 passing), 3 feature commits (B424, B425, B426), ZERO regressions (exact failing-set diffs at every step).** Method: independently re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; the fresh NONE pool all JS-modeling/mapped-type/generic-inference; an inline diff-size ranker over all 396 failing XMLs surfaced the closest DIFF tests) via a 5-agent read-only triage fleet (40 candidates) + ~15 manual deep-reads — ALL multi-piece/architectural except the 2 wins (agents again over-classify CONTAINED). **B424 (+1, 9689/397) — inexistentPropertyInsideToStringType (TS2339)**: reading an undeclared `this.<prop>` inside a top-level constructor-style `function NAME(){…}` in a checkJs JS file (`function toString(){ this.yadda; this.someValue="" }` → TS2339 "Property 'yadda' does not exist on type 'toString'"). New dedicated walker `checkJsConstructorThisReads` (NOT an un-gating of the load-bearing `.js` checkPropertyAccess skip — B152/B153): fires only when the fn has ≥1 `this.X=` write AND its body is EXCLUSIVELY `this.<id>` bare-read + `this.<id>=…` write expr-statements (bails on any other shape); reuses `collectConstructorThisAssignments`. Corpus-exhaustive FP-safety (4 checkJs files have a top-level fn+`this`; the other 3 bail or have only writes). **B425 (+1, 9690/396) — enumAssignmentCompat3 (enum-to-enum nominal TS2322)**: assigning one enum-typed var to an incompatible enum-typed var. Derived TypeScript's nominal enum relation from the baseline (same SIMPLE name + every source member present in target with EQUAL value; const→self only; first-failing-member chain "Property X is missing" / "Each declaration of E.X differs in its value"; relative-name qualification ONLY when src/tgt share a simple name). New `checkEnumToEnumAssignments` (additive, nominal — NO relation engine; reuses evalEnumConstExpr/collectEnumsForAsg). String/un-evaluable enums → conservatively assignable (FN, no FP). **B426 (+1, 9691/395) — spreadUnionPropOverride (object-literal spread support + TS2783/TS2785)**: a genuine blocker BUILD (the user asked to address blockers). (1) `getTypeOfObjectLiteral` is now spread-aware (was `// spread not yet supported`) — a spread's GUARANTEED props (required, non-nullish; union → required in EVERY non-nullish constituent, since spreading nullish is a no-op) merge into the literal type, fixing the FALSE TS2345 "label missing" at `fun({id, ...find()??{label}})`. (2) New full-tree walker `checkSpreadPropertyOverrides` emits TS2783 "'X' is specified more than once…" + TS2785 related for an explicit prop guaranteed by a LATER spread (`{x:1, ...({x,y}|{x,z})}`); FP firewall = the spread must guarantee the prop (`{y}|{y,x}` / optional `{a,b?}` do NOT fire). The remaining enum tests need broad multi-piece features (compat/compat2=typeof-enum-object value typing; compat6=string-enums+function-params+nested-scopes+`import("f")` cross-file display; compat7=enum-relation wired into TS2416/TS2394 + namespace-qualified-enum-member TS2339) — deferred. Also confirmed-architectural this round (skip-logged): allowJscheckJsTypeParameterNoCrash (generic-fn-alias display), emitBOM (@emitBOM output feature), esModuleInteropPrettyErrorRelatedInformation (export=+TS7038), duplicateLocalVariable1/2 (for-in-var string typing + cross-loop redeclaration), interfaceAssignmentCompat (fn-arg structural + enum-static), protectedMembers (TS2415/2445/2446), noImplicitAnyLoopCrash (spread-arg TS2556/2488). NEXT: contained pool genuinely exhausted; remaining is multi-session Blocker BUILDs (JS-modeling/JSDoc #5, flow #1, generic-inference #2, cross-file #3, utility-type materialization, enum typeof-modeling)._ _Round 164 (2026-06-15, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+2 net so far (400→398 failing, 9686→9688 passing), 1 feature commit (B423), ZERO regressions (exact failing-set diff: pre=400, post=398, only the 2 targets flipped).** Method: independently RE-confirmed the contained DIFF/SWAP/MISSING pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; mine_small_diffs shows every error-line-diff test already plan-mentioned) and the NONE/closest-DIFF pool architectural by directly deep-reading ~30 candidates (all multi-piece: utility-type materialization `Record`/`ReturnType`; deep conditional/indexed-access/Parameters resolution; instantiation-expression types + the `comparable` relation; JS-modeling/JSDoc Blocker #5; cross-file Blocker #3; flow Blocker #1; `Object.freeze` literal-preservation). Two read-only agent fleets RE-confirmed the recurring lesson (agents systematically over-classify CONTAINED by focusing on emission MECHANICS and missing that the diagnostic CONTENT — message/positions/chain — needs deep type resolution; e.g. `inKeywordAndUnknown` flagged "HIGH-confidence 1-piece" actually needs flow narrowing wired into the `in`-check (the exact `getTypeOfIdentifier`-broadcast CLAUDE.md forbids) + an FP surface in f3–f7; the `aliasInstantiationExpression…NoCrash` TS2352 pair needs instantiation-expression types + the comparable-relation chain; `constructorOverloads4` is a 3-piece ambient class+function merge). **B423 (+2, 9688/398) — jsdocResolveNameFailureInTypedef (TS2304) + jsdocPropertyTagInvalid (TS2552)**: an UNRESOLVABLE single-identifier type name inside a JSDoc `@typedef {Name}` / `@property {Name}` tag in a checkJs `.js` file now emits TS2304 (`@typedef {CantResolveThis}`) or TS2552 ("Did you mean 'string'?" for `@property {sting}`). New `checkJsDocTypeNameResolution` raw-scans (regex `@(?:typedef|property)\s*\{\s*(IDENT)\s*\}` — single bare identifier ONLY, auto-skips `{number|string}`/`{import(...)}`/`{{…}}`/`{Array<T>}`) and resolves IDENT against `JSDOC_TYPE_PRIMITIVE_NAMES` ∪ KNOWN_GLOBALS ∪ file binder-locals ∪ sibling `@typedef`/`@template` names. **FP firewall (corpus-EXHAUSTIVE)**: across the whole corpus the ONLY unresolvable single-identifier `@typedef`/`@property` types are the 2 intended targets — every other (`Object`,`number`,`string`,`boolean`,`object`,`function`) is in the known set; checkJs-gated. Companion fix (this is what made it a clean +2, NOT a SWAP): these tests were ALREADY emitting FP TS2304 on `@param {Ty}`/`@param {MyType}` because the binder does NOT bind `@typedef` names so the EXISTING JSDoc-param type-name resolver (`checkUnresolvedInType`) wrongly flagged a valid typedef — fixed by registering each file's `@typedef NAME` via `fileScope.addType()` in `checkUnresolvedNames`' `.js`/`.jsx` branch. Reusable infra: `checkJsDocTypeNameResolution` + `JSDOC_TYPE_PRIMITIVE_NAMES` + the @typedef-name scope registration (the first place `@typedef` names are made resolvable as types — extend for `@param`/`@returns`/`@type` name resolution or constrained `@template` when a consumer appears)._ _Round 163 (2026-06-14, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+2 net so far (402→400 failing, 9684→9686 passing), 2 feature commits (B421, B422), ZERO regressions at every step (exact failing-set diffs).** Method: re-confirmed the contained DIFF/SWAP/MISSING pool exhausted (EXTRA/MISSING/SWAP fresh=0; mine_small_diffs.py shows every error-line-diff test already plan-mentioned; the fresh NONE pool — controlFlowArrayErrors/awaitedType/fixingTypeParametersRepeatedly2 — all confirmed multi-piece by direct read; the TS5107 ×14 cluster is AMD/UMD/System JS-emit tsgo-irrelevant, the deprecation diagnostic itself works). Traced ~20 candidates (incl. the 5 TS18048-only tests — all multi-piece closure-const/Record/union-tuple/contextual-generic flow) + ran a 14-candidate adversarial-triage workflow (stopped early after extracting concrete sketches). **B421 (+1, 9685/401) — overloadOnConstInheritance2**: TS2430 for a derived interface that re-declares an OVERLOADED base method with a single incompatible signature. `interface Base { f(x:string):any; f(x:'foo'):string }` + `interface Deriver extends Base { f(x:'bar'):string }` → the derived `(x:'bar')=>string` is not assignable to the base overload set `{ (x: string): any; (x: "foo"): string; }` (a single derived sig must satisfy EVERY base overload; `'bar'` passes `string` bivariantly but fails `'foo'`). The existing `checkInterfaceExtendsClauses` method branch fired only for single-base-decl arity mismatches; new `tryEmitTS2430OverloadSetMismatch` + `methodSigDisplayColon` handle the ≥2-base-decl overload-set case with the 4-line param-mismatch chain. **FP firewall (load-bearing): base method has ≥2 decls AND derived has EXACTLY ONE decl of the name** — a derived that re-declares the FULL overload set (overloadOnConstInheritance1, derived count 2) is compatible and is skipped; single-base-decl (inheritance3) and class-implements (inheritance4) take other paths. Generic (type-param) methods skipped; same-arity only; bivariant param check so only genuinely-unrelated params fire. ZERO regressions (exact diff: only overloadOnConstInheritance2 flipped). **B422 (+1, 9686/400) — strictOptionalProperties3 (TS2375, JS @typedef exactOptionalPropertyTypes)**: `/** @type {A} */ const a = { value: undefined }` in a `.js` file where the JSDoc `@typedef` declares `value` OPTIONAL with a non-undefined type → under exactOptionalPropertyTypes, assigning `undefined` to an optional-but-non-undefined property is TS2375 "...is not assignable to type 'A' with 'exactOptionalPropertyTypes: true'. Consider adding 'undefined'...". The `@typedef` is NOT bound (raw-source only), so new `resolveJsTypedefOptionalProps` scans the JSDoc comments for both forms (`@typedef {object} A` + `@property {number} [value]`, and inline `@typedef {{ value?: number }} B`); `tryEmitJsTypedefExactOptional` checks each object-literal `prop: undefined` against the typedef's optional non-undefined prop and emits TS2375 + the 2-line chain at the var name. Added as a `run {}` block in `checkVarDeclAssignability` after B286. FP firewall (nil surface — no other corpus test combines exactOptional+JS+@type+object-literal): exactOptional+strictNull + JS file + `decl.typeFromJSDoc` + UNBOUND `@type {Name}` TypeReference + object-literal init + a `prop: undefined` value matching an optional non-undefined typedef prop. ZERO regressions (exact diff: only strictOptionalProperties3 flipped)._ _Round 162 (2026-06-14, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+1 net so far (403→402 failing, 9683→9684 passing), 1 feature commit (B420), ZERO regressions (exact failing-set diffs).** Method: two read-only research-agent fleets (14 candidates) + direct deep-reads RE-confirmed the contained pool genuinely thin — every closest-miss DIFF/NONE candidate is 2-piece, flow-blocked, shared-display-path-risky, or tsgo-irrelevant (keyofDoesntContainSymbols = @keyofStringsOnly; the agents systematically over-classify CONTAINED + miss tsgo-relevance + miss the CLAUDE.md `getTypeOfIdentifier`-no-narrowing gotcha). **B420 (+1, 9684/402) — returnInConstructor1**: a class constructor returning an OBJECT LITERAL or a NEW expression whose type is not assignable to the instance type now emits TS2322 + TS2409 (primitive/type-param/null returns were already covered). Object-literal: per-property TS2322 at the property KEY + TS6500 'expected type comes from property X', plus one TS2409 at `return`. New-expression: whole-type TS2322 + a LOCALLY-built 'Types of property X are incompatible' chain + TS2409. **Two load-bearing lessons**: (1) the local chain builder renders a no-return-body method as `() => void` (the global `getTypeOfSymbol` defaults such methods to `() => any`; broadening THAT — even though CLAUDE.md called it net-zero — now regresses 5 accessor/override tests, so the void inference is DISPLAY-ONLY); (2) resolving every class's declared type eagerly in the walker poisoned type caches by first-touch ordering (regressed getAndSetNotIdenticalType2/3 + inheritanceMemberAccessorOverriding*) → resolve the class type ONLY when a ctor actually returns an obj-lit/new-expr._ _[Round 161 trimmed to STATUS-HISTORY.md.]_ _[Round 159 trimmed to STATUS-HISTORY.md.]_ _[Round 158 trimmed to STATUS-HISTORY.md.]_ _[Round 157 trimmed to STATUS-HISTORY.md.]_ _[Rounds 151, 156 trimmed to STATUS-HISTORY.md.]_ 
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
