# Status

**Phase 4 — Checker buildout.** 9,669 / 10,086 tests passing (~95.9%, 417 failing). _Round 156 (2026-06-14, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+2 net (419→417 failing, 9667→9669 passing, ~95.9%), 2 feature commits (B406, B407), ZERO regressions** (exact failing-set diffs). Contained pool RE-CONFIRMED exhausted across every vein this session (NONE all-sizes, DIFF EXTRA/MISSING/SWAP, grammar/parser, enum-compat, tuple, indexed-access, utility-types, TS18048 flow, JS-expando, JSDoc, discriminated-union, OUTPUT) via 16 triage-agent classifications + ~25 deep-reads; every remaining candidate is a multi-session architectural blocker (per-candidate triage in PLAN round-156 note + skip-log). The 2 wins are blocker-adjacent FEATURE builds: **B406 (+1, 9668/418) — assignmentToAnyArrayRestParameters**: TS2339/TS2536 for an invalid string-literal index in an indexed-access TYPE (`string[]["0.0"]` → TS2339 at the literal; `T["0.0"]` with `T extends string[]` → TS2536 at the whole access). New checkBadArrayStringLiteralIndex + faithful isNumericLiteralName (canonical iff String(Number(s))===s); NameScope now threads type-param CONSTRAINTS (typeParamConstraints map + typeParamConstraintOf). FP-safe: a numeric-looking-but-non-canonical string is never a valid array member/index; gated to array-shaped object types. **B407 (+1, 9669/417) — parameterListAsTupleType**: three companion pieces — (1) Parameters/ConstructorParameters<F> materialize to a TUPLE of the sig params, ReturnType<F> to the return type (materializeSignatureUtility, symbol==null dispatch, additive/null-on-unmodelable; buildTupleFromTypes extracted); (2) array-literal init vs TUPLE target → per-element TS2322 (checkArrayLiteralElementsAgainstTuple, closes the "Array→tuple needs contextual typing" gap for array-LITERALS); (3) TS2344 for Parameters<typeof Class> / ReturnType<typeof Class> via checkUtilitySignatureConstraint (FP firewall: whole ref resolved errorType + arg is a constructor type). NEXT: surgical pool exhausted; the only lever is a multi-session Blocker BUILD — Blocker #1 flow (TS18048 family ×9), #2 generic inference, #3 cross-file scope, the indexed-access/mapped/conditional type engine, or the enum-assignability model (5 tests, 9-32 errors). [Round 151 trimmed to STATUS-HISTORY.md.]_ _Round 155 (2026-06-14, multi-iteration session, ≥20 iterations per user request, addressing blockers): +6 net (425→419 failing), 6 feature commits (B400–B405), ZERO regressions at every step (exact failing-set diffs). All 6 are FP-safe narrow detectors harvested from the skip-logged/contained pool the broad triage under-counted. **B405 (+1, 9667/419) — assignLambdaToNominalSubtypeOfFunction**: TS2345 for a lambda (arrow/function-expression) argument passed where the parameter is an interface that `extends Function` AND declares its own required property. `interface I extends Function { x: number }; function fn(cb: I){}; fn((a,b)=>true)` → the lambda is a Function but lacks `x` → "Argument of type '(a: any, b: any) => boolean' is not assignable to parameter of type 'I'." New checkLambdaToFunctionSubtypeArgs + lambdaSigDisplay (infers the lambda return: boolean/number/string-literal → widened name). tsc squiggles the WHOLE arrow but only the `function` keyword (8 chars) for a function-expression arg. FP-safe (a bare lambda can never carry the interface's extra required member). ZERO regressions.** **B404 (+1, 9666/420) — contextualTypingArrayDestructuringWithDefaults**: TS2322 for an object-destructuring default value incompatible with the destructured property's type from a TUPLE annotation. `let {length = {a:1}}: [number]` → `length` has type `1` (the tuple's literal length, modeled at Checker.kt:101400), the default `{a:1}` widens to `{ a: number; }`, not assignable to `1` → squiggle on `length`. New walker checkTupleDestructuringDefaultTypes (top-level + fn/method/block/module traversal). FP-safe: object-binding + tuple annotation + object-literal default not assignable to the identifier-named property's type. ZERO regressions.** **B403 (+1, 9665/421) — assignmentStricterConstraints**: TS2322 for `g = f` assigning a generic function value with a STRICTER type-parameter constraint to one with a looser (unconstrained) parameter. `var f=function<T,S extends T>(x:T,y:S){}; var g=function<T,S>(x:T,y:S){}; g=f` → "Type '<T, S extends T>(x: T, y: S) => void' is not assignable to type '<T, S>(x: T, y: S) => void'." + parameter-incompatibility / could-be-instantiated chain + related TS2208 ("This type parameter might need an `extends T` constraint.") on g's S. New standalone walker checkGenericFunctionValueAssignmentConstraints + genericFunctionExprSig helper (mirrors B401's method-override check at the value site). FP-safe (top-level `g=f` of two generic-fn-expr vars, equal tp/param counts, some tp index with f constrained to a sibling tp and g unconstrained, names align, a param typed by that tp). ZERO regressions (exact diff).** **B402 (+1, 9664/422) — genericWithNoConstraintComparableWithCurlyCurly**: TS2352 for `expr as T` where expr has the empty-object type `{}` and T is a type parameter whose constraint cannot overlap `{}` (constraint entirely null/undefined). `function f<T extends null|undefined>(){ let x={}; x as T }` → "Conversion of type '{}' to type 'T' may be a mistake…" + "'T' could be instantiated with an arbitrary type unrelated to '{}'.". New emitTS2352IfEmptyObjectCastToTypeParam, gated behind a new inTypeParamCastPass flag in the AsExpression branch of walkTypeAssertionsInExpr (AsExpression isn't forwarded to the cast callback — CLAUDE.md gotcha). Source identified via empty-object-local tracking (emptyObjectCastLocals, populated in walkStmtsForTypeParamCasts) because the cast pass doesn't populate local var types (getTypeOfExpression returns any). GOTCHA HIT: emptyObjectCastLocals (a HashSet mutated from a pass called during Checker init) MUST be declared before init {} (Kotlin init-order) — first cut NPE-crashed. FP-safe (source must be bare `{}`; constraint a non-empty null/undefined-only union). ZERO regressions (exact diff).** **B401 (+1, 9663/423) — genericImplements**: TS2416 generic-method-override constraint mismatch. A base method `f<Tb extends A>(): Tb` overridden by `f<Td extends B>(): Td` where the base constraint A is NOT assignable to the derived constraint B → the override demands a narrower type parameter than the base provides ("'T' could be instantiated with an arbitrary type unrelated to 'B'"). New narrow detector in checkClassPropertyOverrides: both methods single-type-param, both return their own bare TP, derived constrained, base-constraint (or unknown) not assignable to derived-constraint → TS2416 with the `<T extends B>() => T` sig display + the two-line could-be-instantiated chain. Fires regardless of the structural assignable result (our generic sigs pass trivially). New genericMethodSig helper. FP-safe (only fires when genuinely unsafe; the same-constraint Y and unconstrained Z cases don't). ZERO regressions (exact diff).** **B400 (+1, 9662/424) — nonExportedElementsOfMergedModules**: TS2339 for a NON-EXPORTED nested namespace member referenced across separate blocks of a merged outer namespace (Blocker #3 cross-block scope). A non-exported `namespace B` inside `namespace One {…}` does NOT merge with the `namespace B` inside a SECOND `namespace One {…}` block — each outer block keeps its own private copy — so `B.x` in block 2 (where x lives only in block-1's B) is TS2339 while `B.y` is fine. Our binder flat-merges all of One's members (Blocker #3), wrongly exposing x; new FP-safe AST walker `checkNonExportedNestedNamespaceCrossBlockRefs` (B396-pattern standalone pass) restores per-block scoping for exactly this shape. Firewall: script files only; outer ns ≥2 blocks; recv is a block-local non-exported nested ns, not exported in ANY block, no global binding; member absent from the referencing block's copy yet present in a sibling block's copy (genuine cross-block leak); only DIRECT expression-statement refs scanned. ZERO regressions (exact failing-set diff)._ _Round 154 (2026-06-14, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+4 net (429→425 failing, 9657→9661 passing, ~95.8%), 4 feature commits (B395-B397, B399), ZERO regressions** (exact failing-set diffs). 6-agent triage fleet + ~20 deep-reads CONFIRMED the bounded surgical pool is GENUINELY exhausted (the finders single-code labels mask multi-piece/JSDoc/generic deps); every remaining fresh candidate needs a multi-session Blocker BUILD (see PLAN round-154 note for the per-candidate triage so the next session skips it). REVERTED (no-dead-infra): knockout (Blocker #2 — `o.name`→`any`), jsDeclarationsInheritedTypes/B398 (JSDoc Blocker #5 — spurious-TS2304 @typedef cascade + leadingComments not on JS fields). **B395 (+1, 9658/428) — deeplyNestedAssignabilityIssue**: nested object-literal value missing a required property of its target now emits TS2741/2739/2740 at the property KEY (e.g. `thing: {}` vs `thing: A` → "Property 'a' is missing in type '{}' but required in type 'A'." squiggling `thing`), with TS2728 ("'a' is declared here") + TS6500 ("the expected type comes from property 'thing'…"). Three edits in checkNestedObjLitPropTypes/emitNestedMissingRequiredProps: (a) the arrayPropsOnly gate now skips only SIMPLE values (object-literal values descend for the nested missing-prop check); (b) emitNestedMissingRequiredProps gained optional key-position (squiggle the KEY, not the value — tsc reports the missing-prop at the property name in a var-decl context) + enclosing-property params (the TS6500 related info); (c) the coarse `canUse && !isAssignable` whole-init TS2322 is suppressed via a new `nestedMissingEmitted` flag (the nested check poisons the relation cache making isAssignable false, so the coarse chain would double-report). ZERO regressions (429→428, exact). Method: 6-agent parallel triage fleet over ~70 closest-miss tests → CONTAINED vs ARCHITECTURAL with fix sketches; knockout was triaged CONTAINED but REVERTED (its `o.name` receiver resolves to `any`, not `Observable<string>` — object-literal-member + generic-return inference is Blocker #2). **B396 (+1, 9659/427) — exportSpecifierReferencingOuterDeclaration4**: TS2694 "Namespace 'X' has no exported member 'bar'" for `X.bar` in a MODULE file where X is a same-file top-level `declare namespace X` lacking `bar` (bar is only in another file's `declare namespace X`). A module-file top-level `declare namespace` is module-LOCAL (does NOT merge cross-file), but our checker merges all locals into globals (Blocker #3), so the symbol wrongly carries the sibling file's members. NEW walker checkModuleFileLocalNamespaceMemberRefs collects X's members from THIS file's AST only (B146 sidestep, for top-level Identifier-named declare-namespaces instead of `declare module "..."` blocks). FP firewall: module files only (script-file top-level ns IS global, merge legit); declare-namespace roots; shadow-skip when X is also a non-namespace top-level binding or its body has re-export/import-equals (un-trackable members). ZERO regressions (428→427, exact). **B397 (+1, 9660/426) — importTypeAssertionDeprecation**: TS2880 "Import assertions have been replaced by import attributes. Use 'with' instead of 'assert'." for a deprecated `assert` clause in import-CALL attributes `import(spec, { assert: {...} })`. Our parser did NOT parse the second argument (import options/attributes) in EITHER position — both `parseImportType` (type) and the dynamic-import call (value) errored on it. Now both parse the optional 2nd arg (the comma is unambiguously inside the import parens, so this is additive — `Foo<import("x"), Bar>` type-args are unaffected). tsc reports TS2880 at DIFFERENT spans per position (replicated): VALUE-position squiggles the `assert` keyword (len 6); TYPE-position squiggles the assert clause's VALUE (inner `{`, len 1). Emitted from the parser; 2880 added to GRAMMAR_CLASS_CODES so it does not trigger the real-parse-diagnostic suppression. ZERO regressions (427→426, exact). **B399 (+1, 9661/425) — largeControlFlowGraph**: TS2563 "The containing function or module body is too large for control flow analysis." A genuine Blocker #1 flow-infra substep: tsc disables flow analysis when a containers flow walk exceeds its budget. We approximate per-FILE via the flow-node count (`BinderResult.flowGraph.nodeToFlow.size`); an instrumented full-suite probe found the ONLY file > 2000 nodes is the 10000-deep top-level assignment chain (~20000 nodes), the next-largest valid file being ~1372 — so a 2000 threshold is FP-safe by a wide margin. Reported at the first top-level statements first token (`const`, len 5). ZERO regressions (426→425, exact). [Round 149 trimmed to STATUS-HISTORY.md.]_ _Round 153 (2026-06-14, multi-iteration session, ≥20 iterations per user request): **+3 net (432→429 failing, 9654→9657 passing, ~95.7%), 3 feature commits (B391, B392, B393), ZERO regressions** (exact failing-set diffs). **B393 (+1, 9657/429) — moduleVisibilityTest3** (a genuine Blocker #3-adjacent BUILD, the user asked to address blockers): namespace-alias visibility through `import X = <local-namespace>`. The file-scoped TS2709 walker and the shared qualified-name resolver never reach a namespace-LOCAL import-equals alias, so a NEW dedicated FP-safe walker `checkNamespaceAliasTypeRefs` owns the shape — bare `X` as a type → TS2709 "Cannot use namespace 'X' as a type.", and `X.member` where member EXISTS in the target namespace but is NOT exported → TS2724 (via emitTS2694 + spelling, candidate pool = exported members only). Gates (load-bearing): target resolved via globals/file-locals + `resolveAlias`, must be namespace-only (`Module|NamespaceModule|ValueModule`, NOT Class/Interface/TypeAlias/Enum — a clodule stays a valid bare type); scan only identifier-named NON-`declare` namespaces (ambient `declare module "X"` routes alias-as-type TS2709 through the existing path — scanning them double-emits, as in privacyImportParseErrors where the module name and local alias name collide). Method this round: 4 parallel Explore-agent triage fleets (~35 candidates) + a sequential implement/verify loop; the B393 build came AFTER confirming the surgical pool dry. 4 parallel Explore-agent triage fleets (~35 candidates) feeding a sequential implement/verify loop. The 2 wins were round-152's "remaining batch-2" candidates. REVERTED (one-cycle, no-dead-infra): invalidSplice+indexSignatureOfTypeUnknown batch — neither flipped (empty `[]` types as `any[]` not `never[]`; the `{[x:string]:T}` T=unknown param isn't produced — generic-inference gap) AND the indexSig Array-nominal change regressed arraySlice. The other ~33 candidates triaged ALL architectural (rootDir dual-root, namespace-alias scope, cross-file/package identity, mapped/recursive/tuple modeling, Object.freeze literal-preservation, lib member-counts, JS-expando-this) — see PLAN-PHASE-4.md round-153 note so the next session skips re-triage. Surgical pool exhaustively confirmed dry; next lever is a multi-session Blocker BUILD. **B392 (+1, 9656/430) — grammarAmbiguities1**: relational comparison (`<`/`>`) of a non-comparable object/function/typeof-class operand IS a TS2365 in tsc — the `f(g < A, B > 7)` grammar-ambiguity parse makes the call args two comparisons. The arithmetic/comparison checker bailed early for any non-wrapper-Object operand; now it only bails for non-comparison ops, and the comparison branch displays object operands via typeToString (no literal-text override → `7` widens to `number`) with a `typeof X` render for a bare class-value identifier. ZERO regressions. **B391 (+1, 9655/431) — switchStatementsWithMultipleDefaults**: tsc's grammar-error suppression rule — a `default` (escaped-keyword) default clause emits TS1260 "Keywords cannot contain escape characters." as a REAL parse diagnostic, which then SUPPRESSES every TS1113 "duplicate default clause" grammar diagnostic in the file (grammarErrorOnNode/hasParseDiagnostics). Three edits: Scanner tracks `tokenHadUnicodeEscape` (set in scanIdentifierWithEscapes, reset in scan()); parseCaseOrDefaultClause emits TS1260 at the keyword span when the flag is set (both case+default branches, FP-safe — only escaped keywords trigger it); TS1113 added to the driver grammar-suppression post-filter (single-file gate + multi-file filesWithRealParseDiagnostics). ZERO regressions (exact failing-set diff). [Round 152 note retained below.]_ _Round 152 (2026-06-14, ultracode multi-iteration session, ≥20 iterations per user request): **+13 net (445→432 failing, 9641→9654 passing, ~95.7%), 12 feature commits (B379–B390)**, ZERO regressions at every COMMITTED step (exact failing-set diffs). After B387 a full re-read of the (slow) batch-2 triage workflow result revealed 10 CONTAINED candidates (I'd checked it too early at 6 architectural-only results): B388 typeAliasDeclareKeywordNewlines (TS1142 line-break-after-`type`-keyword in the parser + a null-vs-alias FP firewall in the string fallback), B389 declarationEmitExpressionInExtends3 (walk heritage base EXPRESSIONS for call-arg checking → TS2345 through the explicit-type-args machinery, same shape as B379), B390 didYouMeanStringLiteral (new tryEmitLiteralUnionDidYouMean: TS2820 'Did you mean' spelling for a string literal vs a literal-union annotation, alias-name display for union aliases / reduced-union display for intersection aliases). REVERTED (one-cycle budget): jsdocRestParameter — the Parser `{...number}` rest-bridge worked but the checker rest-element TS2345 never fired because `.js` call-type checking is skipped (load-bearing gotcha); needs a dedicated JS-aware rest-param walker, not checkArgumentsAgainstSignature. REMAINING batch-2 CONTAINED for NEXT session (fix sketches in PLAN-PHASE-4.md): switchStatementsWithMultipleDefaults (scanner unicode-escape-in-keyword tracking + parser, MEDIUM-HIGH), grammarAmbiguities1 / interfaceNaming1 / arrayOfSubtypeIsAssignableToReadonlyArray / exhaustiveSwitchCheckCircularity (all fullFlip=FALSE — partial). +10 net (445→435 failing, 9641→9651 passing, ~95.7%), 9 feature commits (B379–B387), ZERO regressions at every COMMITTED step (exact failing-set diffs). Three parallel-agent TRIAGE workflows (41+32+29 agents over ~100 closest-miss tests) fed a sequential implement/verify loop. After B385, a NONE-bucket triage surfaced 2 more LOW-risk full-flip wins: B386 (+2, exportSpecifierAndLocalMemberDeclaration + bigintIndex) — cross-block pre-pass in checkAmbientModuleLocalNamespaceMemberRefs emits TS2503 for a "poisoned" namespace (non-exported local `namespace X` value-shadowed by `export { Y as X }`, never exported) referenced in a sibling `declare module` block lacking its own local X; the callback now passes the root Identifier node. B387 (+1, externalModuleRefernceResolutionOrderInImportDeclaration) — a BARE ambient `declare module "X"` coinciding with a sibling SOURCE basename is a STANDALONE ambient module, not an augmentation; gate the collectModuleAugmentations target-locals merge to skip bare-specifier→plain-.ts/.tsx targets so the real source's exports stay intact → TS2339 fires (relative `declare module "./foo"` augmentations unaffected). A subagent-surfaced literal-target FP firewall (typeEngineSettledLiteralTarget, suppress the widened string fallback when the type engine confidently passes a literal-union target) verified NET-ZERO (0 flips/0 regressions) → DISCARDED per no-dead-infra. CAVEAT: workflow triage agents share the working tree (not isolated worktrees) — one left an unverified edit (saved, verified separately) and single-test gradle runs from agents wiped full-suite XMLs mid-verification (worked around by stopping the workflow + clean re-runs). NEXT-SESSION: surgical pool exhausted across all sweeps; remaining ~435 are the type-engine/flow/cross-file/JSDoc/lib BLOCKERS — commit to a Blocker BUILD substep. (Original +7 B379–B385 detail below.) +7 net (445→438 failing, 9641→9648 passing, ~95.7%), 7 feature commits (B379–B385), ZERO regressions at every COMMITTED step (exact failing-set diffs). Method: a 41-agent parallel triage workflow classified the ~40 closest-miss DIFF-bucket tests as CONTAINED vs ARCHITECTURAL with concrete fix sketches; the 7 LOW/MEDIUM-risk full-flip CONTAINED ones were implemented + verified sequentially. B379 (+1, recursiveClassReferenceTest): dotted-namespace name (`namespace A.B.C.D` parses as one ModuleDeclaration with a left-nested PropertyAccessExpression name) wasn't resolved by the call-types walker, so its body's `new State(self)` callee was anyType → no constructor-arg TS2345; new resolveModuleDeclNamespaceSymbol flattens segments (mirroring bindModuleDeclaration), walks .exports, pushes the innermost onto inferenceNamespaceStack. B380 (+1, missingFunctionImplementation): TS2393 for function impls spanning 2+ merged `namespace N {}` blocks — checkMergedNamespaceDuplicates now collects per-block FunctionDeclarations and, when impl bodies exist in 2+ distinct blocks, fires TS2393 on every decl of that name (distinct-block gate keeps it disjoint from the single-block overload path). B381 (+1, jsExtendsImplicitAny): JS `@augments`/`@extends <Base>` JSDoc tag supplies heritage type args — new scanner checkJsDocExtendsTags emits TS2314 at the tag (balanced-angle-bracket arg count + span) and hasGoverningExtendsTag suppresses the TS8026 on the extends clause. B382 (+1, declarationFileNoCrashOnExtraExportModifier): (a) `export { x }` (no specifier) in an AMBIENT (`declare`) namespace is a legal re-export → thread inAmbient flag to suppress TS1194; (b) new walker checkExportEqualsCloduleReExport — `export = X` clodule + `export { Name }` re-export of a top-level exported Name → TS2300 at both positions + TS6203. B383 (+1, importAliasInModuleAugmentation): (a) collectGlobalAugmentationNames registers ImportEqualsDeclaration names (suppress TS2304 on `export import x = A.y`); (b) `import f = require("fs")` in `declare global` → TS2667 at `import` kw + emitTS2307 routes `fs` to TS2591 node hint. B384 (+1, typeMatch1): (a) getTypeFromTypeLiteral named-method branch APPENDS overload sigs instead of overwriting (fixes FP TS2322 for `{f(n):string;f(s):number}` vs multi-call-sig target); (b) new tryEmitClassValueNoOverlap — `C == D` of two distinct class values each with a private member → TS2367 'typeof C'/'typeof D'. B385 (+1, namespaceMergedWithImportAliasNoCrash): 3 guards — (1) USER type-only namespace value-use owned by TS2708, suppress dup TS2339 (gated non-lib, after the TS2550 lib check); (2) `import * as Lib` + later non-declare local `namespace Lib` → TS2440; (3) symbolToQualifiedName renders a NON-EXPORTED local namespace unqualified ('Lib' not '"file2".Lib'), exported keeps the prefix. The B385 first cut regressed internalAlias*2 + doYouNeed* via over-broad gates — fixed by the export/lib gates (the only regression in the session, caught + fixed pre-... wait, post-commit diff, before commit). REVERTED (one-cycle budget): unicodeIdentifierName2 (TS1127 parser recovery — naive consume-through-terminator drops `"hello"` from JS emit; tsc keeps statement-starters via isInSomeParsingContext abort + suppresses trailing ';', too finicky for +1 risk; blocker #7). SKIPPED: assignmentCompat1 (documented 39+-regression relation-engine hot path — dedicated-session task, not a tail attempt). NEXT: contained DIFF pool harvested (batch-2 triage of diff-13..19 tests ≈ all architectural); remaining is the type-engine/flow/cross-file blockers + the NONE-bucket bounded tail (under triage)._
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
