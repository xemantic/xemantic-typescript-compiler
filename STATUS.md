# Status

**Phase 4 — Checker buildout: COMPLETE. Phase 17 — self-compile the TypeScript compiler itself (re-scoped 2026-07-03; "any TypeScript project" is post-v1) — is the LIVE mission (see PLAN-PHASE-5.md; v1 exit = all 8 tsc-source profiles at zero FPs — **the FP leg was MET at round 481 (2026-07-12): all eight profiles at zero real FPs, only env-legit offline artifacts remain**; emit/crash legs verified same round: all 8 profiles emit every program file, exit 0, no crashes — the OFFLINE-VERIFIABLE v1 DEFINITION OF DONE IS MET; next: M5 performance; dashboard: bench/*.tsv, self-compile diagnostics 13,245 → 936 (measured at round 430 — rounds 395–412 burned bounded non-M3 histogram-tail buckets + M3.4 flow-narrowing slices the round-394 "pool picked over" triage missed, all surfaced by bucketing the full `--listAll` output by normalized message shape: TS2499 multi-base-generic misparse, TS2440 type-only-barrel-merge, TS2344 constraint-chain + cross-file `typeParamInternCache` pos-collision, TS2693/TS2709 clodule + destructuring-shadow, TS2314 enum-member-vs-generic, TS2588 nested-shadow, TS2551 Object.setPrototypeOf, SetIterator/MapIterator lib gap, TS2774 local-shadows-function, TS1100 interface-property-name, TS7023 callback-arg self-ref, TS2365 arithmetic local-shadows-function, TS2362 branded-number, TS7053 enum-reverse-mapping, round 408's TS2349 callee flow-narrowing family, and round 409's `export *`-barrel / ESM-`.js` imported-guard flow narrowing (M3.4, TS2339 838 → 672), and round 410's TS2862 constraint-index-sig gate / assign-RHS type-guard narrowing / void-overload-return (2,443 → 2,433), and round 411's M3.4 enum-member discriminant narrowing (barrel-import-resolved, three families UpToDateStatus/TypeMapper/PrivateIdentifierInfo) + type-guard-narrows-a-member-DOWN (2,433 → 2,374, TS2339 672 → 614) + round 412's M3.4 single-type type-guard narrow-DOWN + TS18048 receiver-narrowing (2,374 → 2,373), and round 413's `export *` LEAF-EXPORT gate (barrel-imported `Debug.assert` & every barrel guard now resolves — the TRUE builder.ts blocker, NOT the round-412 depth red herring) + tsc-faithful linear flow-walk iteration + return-path narrowing (2,373 → 1,965, TS2339 614 → 237)), then rounds 414-417 burned more bounded/M3.4 buckets (TS2366 CFA fall-through, NonNull/&&-narrowing arithmetic, closure-loop narrowing, namespace-local `extends` bases: 1,965 → 1,902), and round 418 resolved NESTED type-guard functions (M1.12, 1,902 → 1,854, TS2339 237 → 189: tsc guards `isTupleType`/... are nested in `createTypeChecker` so the binder skips them and `resolveFlowCalleeDecl` missed them - the guard never narrowed, the single biggest TS2339 sub-family), and round 419 resolved properties on INTERSECTION union-members (M1.12, 1,854 → 1,808, TS2339 189 → 143: `getPropertyOfType`/`typeHasOwnProperty` bail on a `Type.Intersection` member so `PropertyAccessExpression | (ElementAccessExpression & …)` FP'd TS2339 on `.parent`; fold the constituents in property resolution + discriminant-narrowing annotation; 0 new FPs, self-compile time −17%), and round 420 resolved TYPE-ALIAS enum-member discriminants in narrowing (M1.12, 1,808 → 1,799, TS2339 143 → 134: a `.kind: <alias>` discriminant member survived a `switch (x.kind) { case }` because `enumMemberKeysOfTypeNode` handled only a direct `Enum.Member`, not a bare-Identifier alias; 0 new FPs), and round 422 burned FIVE bounded M3.4/overload families (1,799 → 1,756: TS2769 60 → 45 — overload arg-check helpers now flow-narrow reference args (mirroring B469) + a synthetic true|false union for guard-narrowed boolean args vs literal overload params; TS2339 134 → 117 — mixed enum + string-literal discriminant unions narrow on disjoint lit:s: keys (PrivateIdentifierInfo); TS18048 10 → 7 — an optional-chain discriminant access x?.kind === <non-nullish> proves the receiver non-nullish; TS2366 12 → 4 — union-.kind exhaustive switches terminate via requiredUnionDiscriminantKeys), and round 423 (a) drove TS2366 to ZERO (1,756 → 1,752: the switch RECEIVER is guard-narrowed + typed before the exhaustive-switch union walk — flow graph via the pass-dedicated implicitReturnFlowGraph, local-const-from-call typing, optional-discriminant @undefined keys, LiteralToken["kind"] indexed-access) and (b) fixed union-TARGET type guards end-to-end (1,752 → 1,708: `x is A | B` PARSED truncated so isCallOrNewExpression/isPropertyNameLiteral/isOptionalChain never narrowed; + aliased conditions `const isFrag = guard(x); if (!isFrag)` via a memoized flow back-walk; + kind-key disjointness beating the lenient relation; TS2339 117 → 104, TS2322 784 → 756) and (c) narrowed truthy optional-chain CALL conditions (1,708 → 1,707: `if (x.y?.size())` proves `x.y` non-nullish), and round 424 burned SEVEN more M3.4 flow-narrowing slices (1,707 → 1,662, TS2339 104 → 68, TS18048 5 → 1, TS2322 756 → 751: loop-entry union suppression with a STRUCTURAL wash gate, call-RHS return-annotation narrowing, closure/join/call-crossing aliased conditions, prefix-path receiver guards, asserts-with-inferred-TP via test-arg predicate inference, assignment-overwrite reset to the declaration/call-RHS resolved type, and the DebugTypeMapper this-narrowing slice (explicit-type-arg asserts re-type `this`; an overload-cluster name collision resolves to the unique TypePredicate-bearing decl) — every step by-site strictly removals), and round 425 found the TS2339 never-cluster ROOT CAUSES (1,662 → 1,600, TS2339 68 → 6: the round-411 enum-discriminant key space SPLIT across resolution paths — canonicalEnumSymbol at all four key-builder sites — plus per-candidate narrow-DOWN distribution in union-target guards/instanceof, aliased ===/switch discriminants, switch-default negative narrowing, typeof-object filtering, boolean-literal discriminant truthiness, destructuring-read narrowing, the loop-entry retry for the single-type suppression, and the positive-empty intersection fallback whose round-423 dead-end verdict was an ARTIFACT of the key split) — driving to 0, and round 426 replaced the B399 TS2563 per-file proxy with the faithful flow-walk depth trip + the asserts-callee call gate (1,600 → 1,593: TS2563 27 → 0; TS2454×20 pre-existing definite-assignment walker FPs unmasked), and round 427 burned that bucket (1,593 → 1,577: TS2454 20 → 4 via tsc's definiteness rules — logical assigns ??=/||=/&&= are DEFINITE so captured reads assume initialized, a !-asserted read assumes initialized, and the anywhere-scan's left-spine walk now applies the target rule per spine node), and round 428 took the first real M3.1 slice of the TS2322/TS2345 cores (1,577 → 1,186, −25%: nullable-union generic param inference + overloaded generic callees for tsc's `x = append(x, item)` idiom (TS2322 751 → 559), the TS2345 histogram top — explicit this-param binding, guarded optional-member args, numeric-enum→number (394 → 280) — the array-literal-vs-union-member string-layer rule (TS2322 → 501), and the body-local-shadows-function anyType registration (TS2345 → 261, TS2769 → 36)), and round 429 drove the TS2345 core 261 → 86 (1,186 → 1,000, −15.7%: call-types lexical shadowing — body-locals vs enclosing params, DESTRUCTURED params via the currentParamBindingNames side set in getTypeOfIdentifier (the round-428 "mini-repro does not reproduce" residue), arrow own-params; embedded String replace/replaceAll/search/split accept RegExp; optional-param union args + NonNull-asserted args; guard-narrowed interface/unknown Identifier args — the ~110-site dominant mechanism, never-param excluded for assertType<never> exhaustive-switch defaults; string-enum→string in isSimpleTypeRelatedTo resolving the round-410 Extension[][] deferral; rest-arg flow narrowing), and round 430 unlocked the `append`/`addRange` inference (936: the `T extends {}` constraint failed `string → {}` and killed every mapper — empty-object-target relation rule with TP-source excluded per genericPrototypeProperty3's pin; readonly-array anchors; TP-from-PREDICATE binding for the getFirstJSDocTag family); self-compile time ~110–150 s single-run band), and round 431 took the FIRST M3.2 slice — the TS7006 core 301 → 11 via contextual typing (nested-fn/lexical callee resolvability, assignment-RHS context from the LHS declared type under the B476 single-applicable-sig rule, ||/??-both/&&-comma-right propagation, intersection/Reference/extends receiver members) — plus engine return-checking in switch/try bodies behind the foreign-TP source gate (un-inferred generic call results bail instead of FP'ing; ~130 T[]/U|undefined/SearchResult<T> return sites cleared) and the TS2367 anchored-disjointness gate (936 → 551, −41%; TS2322 435 → 345, TS7019 4 → 0; by-code strictly shrinking at every landed step) — then extended the foreign-TP gate to the var-decl/assignment/property-write/conditional-return paths (551 → 482, −69 strictly removals; TS2322 → 276; a generic FUNCTION-VALUE source's sig-own TPs stay checkable per 5 corpus pins the suite gate caught), and round 435 took SEVEN more M3.1/M3.2 slices at post-perf-arc iteration speed (482 → 373, −22.6%: generator TReturn returns, fresh-objlit literal props via the freshObjLitRange relation retry, TP-literal-constraint args, the union-decomposition-transparent relation re-entry gate resolving the NodeArray-covariance family ×23, bare-`new` contextual instantiation, the foreign-TP gate on assignment TARGETS (visitor family ×15+), nullish alias-union returns; TS2322 276 → 184, TS7006 11 → 1, TS2345 86 → 79; full-dashboard baseline: compiler 482→373 / tsc-cli 484 / jsTyping 480 / deprecatedCompat 481 / typingsInstallerCore 489 / services 1,603 / server 1,894 / harness 2,193 at 29–41 s wall each), and round 436 took SEVEN more M3.1/M3.2/M3.4 slices (373 → 294, −21.2%: TP-carrying callback-return param skip (forEachEntry family ×14), destructured-LOCAL shadowing (semver/checker + 12 transformer sites), literal-return syntactic union membership (the string-fallback re-widening trap — `return false` vs `Tag | false` ×7, incl. the completely UNPINNED `return "a"` vs `"a" | "b"` shape), constraint-filtered explicit-type-arg overload selection ('true' vs 'false' ×6), overload-helper optional-param union-arg + foreign-TP rules (union-receiver TS2769 ×13), bare-string switch-case narrowing (semver ×3), guard-gated ternary return arms with the tri-state all-leaves-verified early return (×19); TS2322 184 → 158, TS2345 79 → 47, TS2769 30 → 9; round-436 dashboard baseline: compiler 373 / tsc-cli 375 / jsTyping 371 / deprecatedCompat 372 / typingsInstallerCore 380 / services 1,476 / server 1,769 / harness 2,062 at 27–41 s wall), then round 438 (2026-07-07) took FIVE more M3.1/M3.4 narrowing/relation slices (294 → 244, −17%: an empty-object `{}` union-member return-path early return; UNION-target extension of the assignment-RHS AND return-path type-guard narrowing gates (`currentSourceFile = node` / `return node` vs `SourceFile | undefined`); PROPERTY-ACCESS args in the call-arg guard-narrow-DOWN branch (`getExports(node.left)`); and nullish-strip-gated narrowing of object-literal property VALUES (`{ moduleSpecifiers: specs }` where `specs = append(specs, x)`); TS2322 158 → 116, TS2345 47 → 39), then round 439 (2026-07-07) took THREE predicate-overload / arg-narrow-DOWN slices (244 → 228, −16 strictly removals: findAncestor-style type-guard-callback overload return inference + a companion NonNull concrete-union strip, the overload arg-check non-union narrow-DOWN, and its `unknown`-via-typeof extension; TS2769 9 → 1).** Corpus suite green as a permanent regression gate: 11,347 tests / 0 failing / 3 skipped (generated tsgo-relevant corpus + local corner-case tests; count as of round 587 (−12 net: both giants' audit scaffolding retired with their passes) — incl. 78 giant-migration pins; every per-statement emission of the legacy assignability dispatchers, class property initializers, AND the checkFunctionBody body-level walkers are spine-anchored) — **the full tsgo-relevant TypeScript compiler test suite passes.** _Round 443 (2026-07-08, Phase 17 — module-augmentation family + the module-file-local TYPE-alias leak (Blocker #3)): FOUR bounded fixes, all suppression-only. Compiler profile UNCHANGED (190) but the fixes GENERALIZE hugely: services 591 → 498 (−93), server 887 → 733 (−154), harness 1,118 → 989 (−129); suite 9,504 → 9,512 (+8 local across 3 test files, 0 regressions); 4 fix commits; services diffed via --listAll as strictly by-position removals. (1) TS2664 10 → 0: `resolveModuleSpecifierRelativeJsAware` — a `declare module "../compiler/types.js"` augmentation resolves `.js` → the `.ts` sibling (the base resolver deliberately avoids `.js` per the TS2459 gotcha, so the augmentation target never resolved); purely additive → only SUPPRESSES a false 'cannot be found'. (2) TS2564 17 → 0: a class property whose declared type INCLUDES `undefined` needs no definite assignment (tsc's strictPropertyInitialization `getFalsyFlags(type) & TypeFlags.Undefined`); `checkClassPropertyInit` missed the `| undefined` case, so `SourceFileObject`'s `nameTable: Map<...> | undefined` + siblings FP'd; reuses the existing `typeIncludesUndefined`. (3) TS2304 24 → 2: `buildNamespaceScope` gained a StringLiteralNode branch — a `declare module "X"` augmentation body sees the AUGMENTED module's exports by bare name (Node/NodeArray/SymbolFlags/TypeChecker/__String — compiler/types.ts exports NOT imported into services/types.ts); the 2 remaining are NodeJS `global` (env-legit, offline). INVESTIGATED & REVERTED (dashboard no-op): TS7006 array-element contextual typing — a correct M3.2 enabler (3 passing tests) but the real inferFromUsage.ts `Priority[]` case is B83.5-blocked (`interface Priority` nested in a function → unbound → `Priority[]` resolves to any); land it with nested-type resolution. (4) TS2339 44 → 0 (SourceFileLike, Blocker #3): a module-file-local `type X = A | B` alias leaks into globals and shadows the global `interface X` in OTHER files (TYPE-space analog of round 442's variable leak). importTracker.ts's non-exported `type SourceFileLike = SourceFile | AmbientModuleDeclaration` won the last-wins merge over compiler/types.ts's `interface SourceFileLike`, so a `sourceFile: SourceFileLike` param resolved to the bogus union (AmbientModuleDeclaration has no `.text`); pinned via an instrumented probe (whole-program-only — minimal repros don't reproduce). Built `conflatedTypeAliasFiles`; checkMemberAccessMissing bails a union-receiver TS2339 when the receiver displays as a conflated name in a NON-alias file (own-file still fires). NEXT (services @ 498, all deep): TS2322×235 / TS2339×85 (residual conflations — Type×20/Info×12) / TS2345×54 M3 relation, TS2416×11, TS2353×10, TS2740×9/TS2739×8._ 

_Round 570 (2026-07-17) — round 569's enabling fix LANDED: inference-aware contextual param typing. The property-access pass's contextual param registration (ArrowFunction + FunctionExpression twins) skips a param whose contextual type carries an UN-INFERRED callee TP (typeContainsUnresolvedTypeParam) — the bare-TP registration was the round-568 fourslashImpl drift substrate (the TS2339 verdict flipped on WHEN the TP's constraint materialized). Un-registered, the param stays any (suppression-only) and the verdict is order-FREE. Pinned by UninferredTpCallbackParamTest (+3). Gates: corpus 11,310/0; listAll ×8 vs HEAD error-line IDENTICAL. Round 570b: (cta-m3c) RE-LANDED — the fn-body sandwich is ALWAYS-ON; the standing acceptance probe passed (harness at the 94-error no-FP floor, zero fourslashImpl TS2339, listAll ×8 identical; corpus 11,310/0). Round 570c: (cta-m3d) LANDED — fn-body-DIRECT statement emissions of eligible FunctionDeclaration chains (anchor-SIMPLE bodies: {Var/Expr/Return/FnDecl} only — legacy nested-scope dispatches record into the shared currentLocalTypes with no spine reproduction yet) dispatch from the spine anchor; three drifts caught by the corpus gate and fixed (typeParams threading, sandwich ns pushes, the recording leak → the SIMPLE rule). Corpus 11,316/0; listAll ×8 identical. Round 571: (cta-m3e) LANDED — anchor-SIMPLE lifted: non-anchored VariableStatements reproduce the nested legacy arm's recordings spine-side (recordOnly mode, diagnostics truncated; narrowing-DISCARD regions skipped via extractNullNarrowing verdicts at IfStatement enters; TS2563 trip reporting suppressed during recordOnly — a persisted flowDisabledRanges registration would silently kill the legacy-time report). Corpus 11,318/0; listAll ×8 identical. Round 571b: (cta-m3f) LANDED — class METHOD bodies anchor-eligible (ctaM3FnHop; method frames carry the ClassDeclaration-arm quirks: class-TPs-only typeParams override, accumulated TP decls, classForThis threading); m3e recordOnly cost interleave-A/B'd at −0.7% = noise. Corpus 11,320/0; listAll ×8 identical. Round 572: (cta-m3g) LANDED — ctor/accessor bodies complete the class-member tier (localTypes seeding for ctor/setter, B63.5 bidirectional accessor bridging, class-TPs-only override). Corpus 11,323/0; listAll ×8 identical. Round 572b: (cta-m3h0) — the legacy marks consult the spine's RECORDED anchor set (gate identity by construction; pure refactor, listAll ×8 identical). Round 573: (cta-m3h1) LANDED — nested-position emissions anchored (LIST vs BARE legacy surfaces reproduced; narrowed regions legacy-owned, pinned by the sharp narrowed-display test). Corpus 11,325/0; listAll ×8 identical. Round 573b: (cta-m3i) LANDED — narrowing FRAMES anchor narrowed regions (the copy IS the discard semantics; discard machinery deleted); a second round-570 order-dependence channel closed (foreign-TP var-decl recordings gated, probe-root-caused from the single 8-profile drift line). Corpus 11,326/0; listAll ×8 identical. Round 574: (cta-m3j) LANDED — the B417 if-condition TS2367 anchored; the legacy assignability dispatchers' per-statement emission surface is fully spine-anchored. Corpus 11,326/0; listAll ×8 identical. Round 574b: (cta-m3k) LANDED — class property initializers anchored (member-loop ambient installed locally over the enclosing frame). Corpus 11,327/0; listAll ×8 identical. Round 575: (cta-m3l) LANDED — the checkFunctionBody BODY-LEVEL walkers (B442 redeclare / B205 FlatArray) anchored at eligible body-Block enters (legacy pair gated by the recorded set on the Block's nodeId) + a latent reach over-shoot closed: ctaM3FnHop requires a statement-LIST landing (a bare-position `if (c) function f(){}` body is unreached by the legacy giant — gate-invisible since m3d, now pinned). Corpus 11,332/0; listAll ×8 identical. Round 576: (cta-m3m) LANDED — the checkFunctionBody param-loop emission (16.4ei destructuring-from-nullable-union) anchors in the ctaFnBodyFrame sandwich at its exact legacy position (before param typing, flow graph installed — a null graph would over-emit); eligibility unified into ctaM3BodyWalkersEligible shared by all dispatch + legacy gates. The cta giant's EMISSION surface is complete across reproduced scopes; remaining legacy-owned: the emit-twice map-evolution runs only. Corpus 11,335/0; listAll ×8 identical. Round 577: (cpa-m1) LANDED — the g2 (checkPropertyAccess) migration opens: legacy-side audit instrumentation (per-statement context fingerprints keyed by nodeId; types by DISPLAY never Type.id — resolution-order-sensitive). Corpus 11,339/0; listAll ×8 identical (test-only scaffolding). Round 578: (cpa-m2-prep) LANDED — the cta→cpa residue channel CLOSED (per-file currentLocalTypes reset in the cpa driver + the element-access own-recording; the dependence was exactly ONE corpus shape, noImplicitAnyForIn, as round 542 predicted; listAll ×8 byte-identical — the residue fed nothing on real tsc sources). Unblocks (cpa-m2) and (cta-retire). Round 579: (cpa-m2a) LANDED — the spine-side statement-TIER frame skeleton, audit-verified on widened fixtures (per-member-kind map-copy asymmetries, ns frames, loop-var overrides, per-decl-leave recordings; quirk: try/catch Blocks + catch vars are list-reached, never statement-dispatched — the immediate-position gate). Round 580: (cpa-m2b) LANDED — tier 2 completes the cpa frame skeleton, 7/7 audit-green on the FIRST run (the design scout paid off): unified per-edge reach walker, arrow/fn-expr frames with the asymmetric legacy orders, ClassExpression members, cpaCtxAt/cpaEctAt pull-derivations over the shared cpaComputeArgCtxTypes; the audit asserts FULL bidirectional key equality. Round 581: (cpa-m3a) LANDED — the FIRST cpa emissions run from the spine (frames always-on): Var/Expr/Return direct expression walks anchored with emit-twice-suppress-legacy; anchor at the statement's LEAVE (cpa emitters probe the diagnostics list for subtree spine emissions — the round-537 lesson recurs); inStaticClassMethod + lexical tables join the emission ambient. Corpus 11,348/0; listAll ×8 identical. Round 582: (cpa-m3b) LANDED first-try — condition/subject/incrementor/case-expr walks anchor at the EXPRESSION node's leave (probe-safe + state-correct); Throw/Export/TypeAlias/Enum at the statement's leave. Corpus 11,351/0; listAll ×8 identical. Round 583: (cpa-m3c) LANDED — heritage + property-initializer walks anchored; the cpa statement-dispatch emission surface is FULLY anchored (emit-twice persists only for map evolution). Corpus 11,353/0; listAll ×8 identical. Round 584: (ccet-prep) LANDED — the cpa→ccet residue channel closed (per-file reset in the ccet driver; zero couplings — corpus + listAll ×8 byte-identical, no own-recording needed). Round 585: (cpa-retire) LANDED — THE checkPropertyAccess LEGACY PASS IS RETIRED (the first giant off emit-twice; every cpa emission runs once, from the spine; audit scaffolding deleted, −7 tests). Corpus 11,346/0; listAll ×8 byte-identical. Round 586: (cta-retire) LANDED — the checkTypeAssignability legacy pass is RETIRED too (both migrated giants off emit-twice in one day; scaffolding deleted, −5 tests). Corpus 11,341/0; listAll ×8 byte-identical. Round 587: dead-marks sweep (52 truncation lines deleted, listAll-gated) + the measured retirement payoff: compiler profile 35.67s → 30.79s wall (−14%) from the cpa retirement alone. Round 588: the g3 (ccet) decomposition queued from the in-code scout (fn/class TP-scope frames, dotted-aware ns pushes, the If-arm scoped narrowing override, the M1.11 shadowing ecology; audit-or-pins decision recorded). Round 588b: (ccet-m1) scout COMPLETE — ccet's emissions are PER-CALL-NODE (the m3 anchor is per-Call/New-node at its own leave; no contextualType channel; no containing-walk ownership complication); decision: pins-first, no fingerprint audit. Round 588c: the ccet-m2 FULL SPEC recorded from an every-arm in-code read (frames, the B246 contextual channel, the If-arm narrowing override, and the reach quirks that differ from BOTH prior giants: for-INIT + param-defaults walked, declare-module bodies skipped, Do body-before-condition, dotted namespaces recursed). Round 589: (ccet-m2) LANDED — the g3 frame skeleton always-on and inert, identity gates green (corpus 11,341/0; listAll ×8 byte-identical); one quirk extracted: decl recordings deferred to the m3 anchors (frame-time resolution side effects flipped the 17.21 static skip-gate — one-probe bisect). Round 590: (ccet-m3) BUILT on wip/ccet-m3-anchors (anchors+classifier+marks+8 pins, corpus green) but BLOCKED on ONE interleave FP: the cta return anchor at services.ts:1327 sees ccet-warmed caches → a TS2322 (NodeObject<TKind> member display) the pass-after-pass order never produced; probe plan recorded. Round 591: (ccet-m3) MERGED — the last giant's emissions run from the spine; the blocker resolved exactly per the probe plan (the gap-signature rule: a nested-position RAW uninstantiated generic interface = the un-inferred-generic signature → order-free bail). Corpus 11,347/0; listAll ×8 identical. Round 592: (ccet-retire) LANDED — THE LAST GIANT IS RETIRED; ALL THREE INV.4(e) GIANTS ARE OFF EMIT-TWICE (cta 586, cpa 585, ccet 592 — the 38%-of-checker-init trio now emits once, from the spine). Corpus 11,347/0; listAll ×8 identical. Round 593: the (f) RE-MEASURE — checkSpine is now 18.4s of 25.7s checker-init (72%; 620k typeOfExpr calls + 111k narrowing walks inside the ONE walk; the 441 remaining passes are all sub-200ms). The ≤10s target = shrinking checkSpine via (f1) the epoch-tagged per-node expression-type memo (~×2.8 recompute) and (f2) folding flow narrowing into reference typing. Round 594: the (f1) probe — 93% of typeOfExpr repeats return the IDENTICAL instance (~60% of all 627k calls are pure recompute); invalidation design recorded (recommended: the MutationCountedMap wrapper for the localTypes family + a (nodeId, epoch) memo). NEXT: (f1b) implement + interleaved A/B._

_Round 566 (2026-07-17) — (cta-m3a): THE FIRST cta EMISSIONS RUN FROM THE SPINE. Top-level + namespace-body VariableStatement emissions dispatch from a spine anchor under the frame context (emit-twice-suppress-legacy: the legacy arm re-runs the leaf for its interleaved state and truncates duplicates; frames always-on; the TS2322 post-filter window opens at spine entry). The probe-driven root-cause chain validated the whole migration methodology: the qualify verdict flip was attributed via twin ambient-dump markers to the NAMESPACE-LEAKED currentLocalTypes channel (the first-decl-wins bail is load-bearing) and to checkSpine never setting currentCheckFileName — both now pinned as gotchas. Earlier same-arc: rounds 559-565 built the pin batches (43), the audit-verified frame skeleton (5 quirk-extraction cycles), the param-loop split, and the sandwich-reuse machinery. Gates: corpus 11,307/0; listAll ×8 error-line identical. NEXT: (cta-m3b) ExpressionStatement/ReturnStatement arms via the same template, then scope-by-scope descent._

_Round 556 (2026-07-17) — ALL THREE INV.4(e) GIANTS SLOT-MOVED to the spine block. With round 555's fixed probe + producer hoists, g2 (checkPropertyAccess) and g3 (checkCallExpressionTypes) probed clean on the FIRST try: all three giants now run right after `pass("checkSpine")` in legacy relative order. Corpus 11,259/0 + listAll ×8 error-line identical at every step. The order-coupling barrier that closed INV.4(e) at rounds 542/543 is fully gone — remaining work is the per-giant spine migration itself (reach classifiers + ambient-state discipline per the INV.4(d) playbook), now free of slot-order risk._

_Round 555 (2026-07-17) — INV.4(e) g1 UNBLOCKED + SLOT-MOVED: checkTypeAssignability (the biggest giant) now runs at the spine slot. The round-542/543 "giants blocked on first-touch order-sensitivity" thesis mostly dissolved into a PROBE BUG (the slot-move scripts matched a COMMENT containing `pass("checkSpine")`, inserting the giant ~100 passes early — invalidating two rounds of (c2)/(c3) audits) plus exactly TWO producer-order dependencies, both fixed by the established hoist pattern: checkTemplateUnionIntersectionComplexity (the B572 relationComplexityHandled side-set) and checkTypeParameterDefaults (found by a 10-cycle automated pass-order bisection — checkTpListDefaults materializes TypeParam .constraint/.default, which the `Test<any>` no-args annotation display consumes). VERIFIED: with both producers hoisted, corpus 11,259/0 AND listAll ×8 error-line identical BOTH at the legacy slot and with the giant AT the spine slot. The (c3) audits are CLOSED. Next: the actual g1 spine MIGRATION per the INV.4(d) playbook + re-probe g2/g3 with the fixed probe._

_Round 553 (2026-07-17) — INV.5(e) first half: canUseTypeEngine's GENERIC GATE IS DELETED. The historical `hasUnresolvedTypeParams(source/target) → return false` skip — the documented "#1 named→named blocker" (generic instantiations skipped because instantiation was incomplete) — is gone: bare-TP-arg References now flow through the structural relation. Probe-first landing: corpus 11,259/0 AND listAll ×8 error-line identical vs 548a with the skip lifted — the accumulated M3.1 work + round-552's instantiation budget made the engine's verdicts match the bail everywhere it mattered on real tsc source. A/B-verified capability gain: `Box<T>` assigned to `Box<string>` inside a generic body was a FALSE NEGATIVE at HEAD (emitted nothing) and now fires tsc's exact TS2322 with the right display (Inv5GenericGateTest, 3 pins). Next: (e) second half — the superseded-pin-walker deletion sweep (suite-gated per deletion), then back to the INV.4(e) giants._

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `docs/history/STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `docs/history/STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
