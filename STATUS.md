# Status

**Phase 4 — Checker buildout.** 9,731 / 10,086 tests passing (~96.5%, 355 failing). _Round 179 (2026-06-16, multi-iteration session, ≥20 iterations per user request, addressing blockers): **IN PROGRESS — +1 so far (356→355 failing, 9730→9731 passing), 1 feature commit (B457), ZERO regressions (exact failing-set diff via xml.etree).** Method: confirmed the find_candidates buckets exhausted (EXTRA/MISSING/SWAP fresh=0; NONE fresh=9 all type-engine; OUTPUT=1 tsgo-irrelevant), regenerated the round-177/178 "purely-structural missing codes" filter over all 356 failures (30 candidates, all triaged), and independently re-verified the most tractable — finding `checkSuperCallBeforeThisAccess` was a CONTAINED win the prior triage missed (we already emitted the TS2855 + the first bare-`this` TS17009, only the flow-dependent cases were missing). **B457 (+1) — checkSuperCallBeforeThisAccess (flow-based super-before-`this`, TS17009/TS17011).** Replaced the naive "first `this` before first `super()`, then stop" heuristic with a direct port of tsc's `isPostSuperFlowNode`: `collectThisSuperRefs` gathers every `this` Identifier + every `super.<prop>` receiver in the ctor body (NOT descending into nested function-likes — lazy, tsc `getThisContainer(includeArrowFunctions)` — nor into `super()` argument lists, owned by the disjoint TS2336/TS2351/B51.5 emitters), and each ref's flow node is walked for a `super()` `FlowCall` (branch labels post-super only if EVERY antecedent is; memoized). The existing flow builder already models if/else, switch-fallthrough, ternary, and object-literal super() calls, so conditional cases (`{w: cond ? super() : 0}` → not-post-super; both-branches → post-super) join exactly. Fixed all 12 missing TS17009/TS17011 across if/else/switch/ternary with zero regressions in the 9-test `checkSuperCallBeforeThisAccessing*` family. NEXT: continue the structural vein (constEnumErrors 7-check cluster, bluebirdStaticThis TS2339→TS2694 swap) then type-engine Blocker BUILD._ _Round 178 (2026-06-16, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+3 net (359→356 failing, 9727→9730 passing), 2 feature commits (B455 +1, B456 +2), ZERO regressions at every step (exact failing-set diffs via xml.etree baseline).** Method: regenerated the round-177 "purely-structural missing codes" filter over all 359 failing tests (60 candidates), independently triaged ~30 (most multi-piece: type-engine index-sig/intersections, parser-recovery cascades, cross-file), ran a skeptical read-only Explore triage Workflow over 31 (agents OVER-classified CONTAINED again — decoratorsOnComputedProperties=broad parser FP cascade, es6Import=grammar-suppression coupling, readonlyMembers=3 sub-features w/ IIFE scope rule — all verified independently). **B455 — letAndVarRedeclaration (function-scope var-hoist TS2451).** The round-177 deferred var-hoisting item. A `var`/for-init-`var` hoisting OUT of a nested block INTO a function body whose TOP declares a same-named let/const is a block-scoped redeclaration → TS2451 on BOTH. The binder hoists nested-block vars into NAMESPACE/FILE scopes (duplicate pipeline → TS2451) but NOT function bodies, and the TS2481 walker wrongly fired "cannot initialize outer scoped variable" for the no-init function case. New dedicated `checkVarHoistRedeclaration` (function bodies only); `checkOuterScopeVarShadowing` now suppresses TS2481 when a var matches the nearest function/module body-TOP let/const. TS2481 still fires for INTERMEDIATE-block lets (the 6 legit TS2481 baselines unaffected). **B456 (+2, both es5+es2015 variants) — blockScopedVariablesUseBeforeDef (IIFE-body TDZ TS2448/TS2450).** An IIFE (immediately-invoked arrow/fn-expr, possibly paren-wrapped) in a let/const initializer runs EAGERLY, so block-scoped names referenced inside its body — the var being initialized (self) or one declared in a LATER statement — are read in their temporal dead zone. The two UBD walkers (checkUBDForwardInExpr forward-refs, checkUBDInInitializer self-refs) both skipped arrow/fn bodies as "lazy"; now they detect an IIFE callee and walk its eagerly-evaluated body expressions. Gating (matches tsc): generator IIFE bodies don't run on call → skipped; async IIFEs run their synchronous prefix → forward refs fire but self refs do NOT (gated non-async); body-declared names shadow the outer TDZ set (FP-safety); the two mechanisms are positionally disjoint (no double-emit). New helpers ubdIifeCallee/collectUbdBodyBlockNames/forEachUbdEvaluatedExpr. NEXT: structural pool now genuinely scraped (B455+B456 were the last clean dedicated-walker wins; remaining = parser-recovery Blocker #7 + grammar-suppression coupling + type-engine Blockers) → type-engine Blocker BUILD (Record/mapped-type materialization, generic-inference #2, flow evolving-array #1)._ _Round 177 (2026-06-16, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+4 net (363→359 failing, 9723→9727 passing), 4 feature commits (B452-B454, B453=+2), ZERO regressions at every step (exact failing-set diffs).** Method: the `--none`/rare-code bucket was exhausted, but a NEW filter — failing tests whose ALL MISSING codes are PURELY STRUCTURAL (no type-engine code) — surfaced the remaining dedicated-walker wins (a diagnostic depending only on AST/modifiers/names/scope is the contained-win signature). The skeptical read-only triage Workflow again over-classified CONTAINED; independent verification (source+baseline+our-diff) was load-bearing. **B452 erasableSyntaxOnly (TS1294):** extended the narrow TypeAssertion-only walker to DECLARATION syntax (parameter properties, enums, INSTANTIATED namespaces via moduleInstanceStates, import/export-equals) in non-ambient context; gated on the rare option. **B453 abstractPropertyNegative (+2):** new `checkClassDeclAbstractAndAccessors` mirroring the class-EXPRESSION TS2653 walker — TS2654(≥2)/TS2515(1) unimplemented inherited abstract members (param-properties count; rule count==1→2515 else 2654), TS2676 get/set-pair abstractness disagreement, TS1005 concrete-accessor-no-body. ZERO regressions despite broad class-decl surface. **B454 letDeclarations-scopes-duplicates (TS2451):** B83.5 block-scoped blocker — new additive `checkBlockScopedRedeclarations` for same-block let/const dups in Block/switch/try/catch (binder covers only file/module top-level; function bodies already covered → process those only for nested blocks to avoid the var12 double-emit) + a top-level pipeline fix (FIRST-decl-kind picks TS2451 vs TS2300; a `function` in the let's scope forces TS2300). DEFERRED: letAndVarRedeclaration (var-hoisting into the let's function scope), exportAsNamespace_augment. NEXT: structural pool scraped (flow/parser-recovery/var-hoisting) → type-engine Blocker BUILD or the var-hoisting TS2451 extension._ _Round 176 (2026-06-16, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+1 net (364→363 failing, 9722→9723 passing), 1 feature commit (B451), clean +1 verified by ISOLATION (the full-suite set-diff vs the round-175 `/tmp/fail_baseline.txt` showed a 351/350 churn — almost ENTIRELY id-allocation ordering noise, NOT real; the round-175 "bounded to ~25–50" estimate badly understates it — a member-resolution change shifts id allocation enough to reshuffle ~350 boundary tests).** Re-confirmed pool exhausted 4 ways, then mined the PROVEN dedicated-walker vein on the MEDIUM-baseline (2–12 line) failing tests rounds 174–175's small-NONE focus skipped, ranked by corpus-rarity of their rarest diagnostic code. **B451 — literalsInComputedProperties1 (TS2452 ×3 missing + 7 pre-existing computed-key FPs, one shared root).** Two coupled gaps: (1) the parser's TS2452 "enum member cannot have a numeric name" fired only for bare `NumericLiteralNode`/`BigIntLiteralNode` names, not a numeric-CANONICAL string `"3"` (`isEnumNumericName`, tsc isNumericLiteralName — `"3"` yes, `"13e-1"` no) nor a computed `[2]`/`["4"]` with a literal inner — span via `scanner.getPrevTokenEnd()` so quotes/brackets are covered; (2) a computed-literal property/member name `[2]`/`["bar"]` was dropped at 4 type-BUILDING sites, so `x[2]`/`y[2]`/`z[2]` FP'd TS2339 and `X["bar"]` FP'd TS7015. Fix: new `computedLiteralKey` helper (NumericLiteral/StringLiteral inner → static key) applied LOCALLY at `getTypeOfObjectLiteral` (object literal), `resolveInterfaceMembersCore` PropertyDeclaration (interface+class), the binder enum-member binding, and `classMemberNameText` (class-instance element-access lookup) — deliberately NOT the shared `getMemberName` (which feeds duplicate-detection/abstract-tracking ~20 callers + 4 passing computed-name tests). FP firewall: gated strictly to ComputedPropertyName with a literal inner, so ONLY the ~10 corpus files with that shape can be affected — all verified pre-existing failures (decoratorsOnComputedProperties/overloadsWithComputedNames/enumAssignmentCompat*/etc. fail in clean-HEAD isolation TOO, confirmed by a stash/pop isolation A/B). Net −1 confirms a clean +1. **GOTCHA: the full-suite set-diff vs a stale baseline is USELESS for regression detection under id-ordering drift (350-churn from one change) — verify via (a) target passes in ISOLATION, (b) net count, (c) the directly-touched FP-surface tests pass in clean-HEAD isolation A/B.** NEXT: continue the medium-baseline rare-code vein (importInsideModule TS1147+TS2307 bare-specifier, interfaceNaming1, etc.) then Blocker BUILD._ _Round 175 (2026-06-16, multi-iteration session, ≥20 iterations per user request, addressing blockers): **NET-ZERO code (9722/364 unchanged) — a CRITICAL CORRECTION + queue restructure, no feature commit (genuinely no clean win exists).** **⚠ Corrected the B450 premise that round-174 promoted to the top of the queue.** Round-174 claimed "338/364 failures are order-dependent (pass in a smaller batch)" and promoted B450 (id-output-decoupling) as high-yield. That was a REGEX-PARSER BUG: a naive `<testcase…>(.*?)</testcase>` mis-attributes self-closing PASSED testcase tags to the next failure (it labeled redefineArray/arrayOfExportedClass/exportPrivateType "failing" when those passes' captured failures actually belonged to neighbouring tests). RE-MEASURED with `xml.etree`: **364 failing / 329 bases; running those 329 base patterns as an isolated batch reproduces ALL 364 — the failures are GENUINE and DETERMINISTIC, NOT ordering artifacts.** Order-dependence is real but bounded to ~25–50 boundary tests (the round-174 −25 magnitude), in BOTH directions → B450 is a determinism-HYGIENE task with ~0 net yield, NOT the high-yield blocker it was promoted as. **DEMOTED in the queue.** (Also: `getUnionType` already sorts by `flags.value` not id; the visible `.sortedBy{it.id}` sites preserve relative=creation=source order; the −25 likely comes from `HashSet<Int>` ITERATION order reaching display, not the sorts.) **Re-confirmed the surgical pool exhausted 4 ways** (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=9 all type-engine; OUTPUT fresh=1 tsgo-irrelevant) and extracted the EXACT diff for all 364 failures + clustered: only 4 pure-FP (all known-hard parser-recovery/lib), every "close" diff is coupled-multi-piece (namespaceDisambiguationInUnion=qualification+member-selection+tuple-msg; mergeSymbolReexportInterface=cross-file import-alias-vs-augmentation decl-order Blocker #3; readonlyTuple=full readonly-variance; literal-display cluster=gate+union-order+flow). Latent display bug found (no flipping consumer→not fixed per no-dead-infra): `tokenToString(FromKeyword)` returns `"FromKeyword"` not `"from"`. **NEXT (B450 demoted): Record/mapped-type materialization (B100, most consumers) > generic-inference #2 (2-TP context-sensitive args, typeParameterFixingWithContextSensitiveArguments2/3) > flow #1 (controlFlowArrayErrors) > cross-file #3.** Round-175 detail in PLAN-PHASE-4.md._ _[Round 173 trimmed to STATUS-HISTORY.md.]_ _[Round 172 trimmed to STATUS-HISTORY.md.]_ _[Round 171 trimmed to STATUS-HISTORY.md.]_ _[Round 170 trimmed to STATUS-HISTORY.md.]_ _[Round 169 trimmed to STATUS-HISTORY.md.]_ _[Round 167 trimmed to STATUS-HISTORY.md.]_ _[Round 166 trimmed to STATUS-HISTORY.md.]_ _[Round 165 trimmed to STATUS-HISTORY.md.]_ _[Round 164 trimmed to STATUS-HISTORY.md.]_ _[Round 161 trimmed to STATUS-HISTORY.md.]_ _[Round 159 trimmed to STATUS-HISTORY.md.]_ _[Round 158 trimmed to STATUS-HISTORY.md.]_ _[Round 157 trimmed to STATUS-HISTORY.md.]_ _[Rounds 151, 156 trimmed to STATUS-HISTORY.md.]_ 
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
