# Status

**Phase 4 — Checker buildout.** 9,766 / 10,086 tests passing (~96.8%, 320 failing). _Round 199 (2026-06-19, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+1 net (321→320 failing), B491 (`deepElaborationsIntoArrowExpressions`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** Re-confirmed the contained pool exhausted via THREE independent finders (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=8 all type-engine; a new "same-multiset-of-top-level-error-CODES" finder = 7 display-mismatch candidates, all blocked: `inheritance1` hierarchy-name relation display, `arrayAssignmentTest1` deliberate `[]`→any[] B87.6, `mergeSymbolReexportInterface` cross-file merge order) + a 40-agent deep-tracing triage Workflow (killed mid-run after 11 verdicts confirmed ALL closest-40 are ARCHITECTURAL/CROSS-FILE/PARSER-RECOVERY/fragile-display). One reverted attempt: the `allowJscheckJsTypeParameterNoCrash` alias-display fix (register `aliasDisplayMap` for a DIRECT function-type alias body so `WatchHandler<any>` keeps its name vs unfolding to `(val: any) => void`) compiled + ran net-ZERO — the alias id can't survive the `ComponentOptions → Record → indexed-access → cross-file contextual` chain (the documented B58.x id-stability fragility); reverted per no-dead-infra. **B491 (+1, `deepElaborationsIntoArrowExpressions`)** — three coordinated elaboration drills reporting the LEAF mismatch at the inner position instead of the coarse whole-value TS2322: (case 1) a var-decl object-literal arrow-valued property vs a method target (`{ y: () => "b" }` vs `{ y(): "a" }`) → TS2322 at the arrow return `"b"` + TS6502, new branch in `emitPerPropertyMismatchesForObjectLiteral` (gated to single-return arrow/fn value + function-typed target member with simple/literal return); (cases 2/3) a return statement drill (`tryDrillReturnArrowOrArray`/`singleReturnExprOf`, wired into `checkReturnAssignability` before the general relation path) — `return () => ({a:''})` vs `() => Foo` drills the returned object literal via `checkNestedObjLitPropTypes`, `return [{a:''}]` vs `Foo[]` reuses `checkArrayLiteralElementExcessProps`, both → TS2322 at the inner key + TS6500 (mirroring the var-decl array/object drill that already made `var y: Foo[] = [{a:''}]` pass). NEXT: contained pool re-exhausted; remaining 320 need the big blockers (generic-argument inference #2, named→named deep-structural CHAINS, cross-file scope #3, mapped/conditional recursion TS2589, flow narrowing) — each a dedicated multi-piece BUILD._ _Round 198 (2026-06-19, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+2 net (323→321 failing), B489 (`namespaceDisambiguationInUnion`) + B490/B490b (`genericAssignmentCompatWithInterfaces1`), ZERO regressions at each step (exact failing-set diff via `scripts/fail_set.py`).** After the most rigorous exhaustion scan any session has run (find_candidates all buckets fresh; a 24-result fresh-eyes triage Workflow over the 160 error-baseline failures = 0 BUILDABLE; cross-test code histograms; manual code-trace of ~8 candidate builds), used a NEW finder — "same-multiset-of-top-level-codes" (right errors, wrong positions/text) — to surface the two DISPLAY/elaboration flips the per-test triage missed. **B490 + B490b (+1, `genericAssignmentCompatWithInterfaces1`)**: a fresh object literal's property mismatch is reported ELEMENTWISE at the property key (value-type-vs-property-type chain) even for OBJECT property types — broadened `emitPerPropertyMismatchesForObjectLiteral` past its `isSimpleCheckableType` (primitive-only) gate to object props via the existing `getPropertyElaborationChain` (gated to a non-empty chain), using `getPropertyTypeForRelation` to INSTANTIATE the generic target's prop (`I<string>.x` = `Comparable<string>`, not the un-instantiated `Comparable<T>` that raw `getTypeOfSymbol` returns → relation trivially passes → no emit). Companion B490b: `getReturnTypeOfCallExpression` returns an IIFE's EXPLICIT return annotation type (`function(): T {…}()`) instead of anyType (gated to `callee.type != null`), so an annotated-IIFE initializer reaches the assignability check. **B489 (+1, `namespaceDisambiguationInUnion`)**: pure display — tsc qualifies a union/tuple member with its namespace ONLY on a simple-name collision (`Foo.Yep | Bar.Yep`), shows the bare `Yep` in a single-member chain; four collision-gated pieces in `formatTypeForDisplay` (Union+Tuple), the Object→Union chain (pick LAST failing colliding constituent, render unqualified), and the var-decl array→tuple message (per-length-bound "requires N…fewer" for a non-empty tuple). Both FP-safe. NEXT: contained pool re-exhausted; remaining 321 need the big blockers (generic-argument inference #2, named→named deep-structural CHAINS [the gate is OPEN — wrong chains not wrong pass/fail], cross-file scope #3, mapped/conditional recursion, flow narrowing) — each a dedicated multi-piece BUILD._ _[Round 198 B489-only detail superseded above.]_ _Round 197 (2026-06-19, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+3 net (326→323 failing, 9760→9763 passing), B487 + B487b + B487c (3 commits, +1 each), ZERO regressions at each step (exact failing-set diff via `scripts/fail_set.py`).** Re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=8 all type-engine) AND ran TWO triage Workflows (~26 agents over the closest+mid-rank failures) — **0 FLIP_NOW, all BLOCKED/MULTI_PIECE** (consistent with rounds 173–196). Then found a NEW dedicated-walker family the find_candidates ≤3-code window + the agents both missed: **"structurally-impossible assignment, AST-shape-gated, corpus-FP-safe"** — landed 3 of them as branches in the existing `pdduCheckExpr` CallExpression traversal. **B487 (+1, invalidSplice)** — `[].splice(start, deleteCount, ...items)`: a fresh empty `[]` is `never[]`, so the first value item is checked against `never` → TS2345 (AST-shape gated to an empty-`[]` receiver, bypassing our B87.6 `[]`→any[] typing). **B487b (+1, arrayConcatMap)** — `[].concat([{a:1}],…).map(b=>b.a)`: TS2769 (2-overload `ConcatArray<never>[]` chain, `{ a: number; }` not assignable to `never`) at the first concat element + TS2339 `b.a` on `never` in the chained map callback. **B487c (+1, indexSignatureOfTypeUnknownStillRequiresIndexSignature)** — an ARRAY arg can never satisfy a `{ [x: string]: T }` param (arrays have a numeric index sig, not a string one) → TS2345 "Index signature for type 'string' is missing"; bare-Identifier-callee branch gated to a generic fn with a single `{[x:string]:<bare TP defaulting to unknown>}` param + array arg. All 3 corpus-FP-safe by construction (the only other `[].<method>` calls are `[].push.apply(...)` (callee 'apply', not matched) and these exact shapes are corpus-unique). NEXT: the dedicated-walker surface is now exhausted; remaining 323 need the big blockers (generic-argument inference #2, cross-file scope #3, named→named structural, mapped-type/conditional recursion, NoInfer/Partial materialization, flow narrowing) — all multi-piece/net-zero-prone, a dedicated BUILD session each._ _Round 196 (2026-06-18, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+2 net (328→326 failing, 9758→9760 passing), B482-WEAK-TYPE extension (flips `nestedExcessPropertyChecking`) + B482ext (flips `intersectionPropertyCheck`), ZERO regressions at each step (exact failing-set diff via `scripts/fail_set.py`).** **B482ext (+1, intersectionPropertyCheck)** — its error 2 (nested-weak) already fired via the round-196 weak-type infra; landed the remaining 3 elaboration pieces: (a) intersection-SOURCE assignment chain (broaden the assignment-path chain branch to accept a `Type.Intersection` source vs Object target → `getIntersectionPropertyElaborationChain`, mirroring the var-decl path: `x=y` where `x:{a?:string}`,`y:T&{a:boolean}` → "Types of property 'a' are incompatible. Type 'boolean' is not assignable to type 'string'."); (b) TS6500 on a DEPTH-1 nested var-decl excess via a new `topLevelRelatedOnly` flag in `checkExcessProperties` (attaches the "expected type comes from property" related info to the immediate child of the top-level literal but NOT deeper — matches intersectionPropertyCheck depth-1 TS2353→TS6500 vs nestedFreshLiteral depth-2 TS2561→related-free); (c) sync return-spread per-property — relax `checkNestedObjLitPropTypes`' leaf gate to a SIMPLE source vs ANY target (a primitive value can't have deeper structure → a flat per-property line is always tsc-correct) + call it for SYNC object-literal returns (`return {...v, hi:true}` vs `{hi?:string[]}` → TS2322 'boolean'/'string[]' at the `hi` key + TS6500). **B482-WEAK-TYPE extension (+1, nestedExcessPropertyChecking)** — completed the round-195 hand-off (5/6 → 6/6) by building the three missing weak-type pieces + the optional-`any` display fix. (1) **Top-level weak var-decl check** `tryEmitTopLevelWeakVarDecl` — TS2559/TS2560 for `let x: W = src` (W weak, src no-common), with the empty-source guard added to `tryEmitWeakTypeAssignment` (so `let x: AllOptional = {}` does NOT fire). Source classification `topLevelWeakSource`: `expr as T` (cast-alias display `C2`), enum-member `E.A` (`enumMemberWeakSource` — `getTypeOfExpression(E.A)` is anyType, so use the enum's string/number base for source-name enumeration + the enum name for display), primitive literals (`"A"` — wrapper apparent props for names, literal for display). (2) **Object-literal-leaf weak descent** `tryEmitObjectLiteralWeakLeaves`/`descendObjectLiteralWeak` — descends a fresh object-literal initializer against its annotation TYPE-NODE (`findWeakPropAnnotation` walks intersection/type-literal/alias/interface), so a `Partial<…>`-bearing intersection annotation that RESOLVES TO errorType does NOT block the lookup of the present constituent; at a non-object-literal leaf whose target prop type is weak + value no-common → TS2559 at the property KEY + related TS6500 (prop decl name + `formatTypeForDisplay(parentAnn)` enclosing display — renders the Partial syntactically). (3) **Combined intersection display** `collectPropertyTypesAcross` for the nested-var-decl inner chain (`A1.x & B1.x` → `{ a?: string | undefined; } & { b?: string | undefined; }`). (4) **`formatTypeForDisplay` optional-`any`/`unknown` fix** — an optional `any` prop renders `nope?: any` not `nope?: any | undefined` (any absorbs undefined). Remaining weak-type tests (`intersectionPropertyCheck`, `noInferCommonPropertyCheck1`) are multi-piece/non-weak (TS2353 nested-excess + intersection-source assignment mismatch + return-spread; NoInfer/Partial materialization). NEXT: a Blocker #2 (generic-inference)/#3 (cross-file scope) or B486 (override-chain) BUILD session._ _Round 195 (2026-06-18, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+1 net (329→328 failing, 9757→9758 passing), B482-WEAK-TYPE (flips `weakType`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** Confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=8 all type-engine; OUTPUT=1 systemModule17 tsgo-irrelevant) and triaged ~15 CLOSE/parser candidates (unicodeIdentifierName2, dottedModuleName, es6ImportNamedImportIdentifiersParsing, contextualTupleTypeParameterReadonly, classPropertyErrorOnNameOnly, thislessFunctionsNotContextSensitive1, destructuringTuple, intersectionPropertyCheck …) — all multi-piece — then BUILT the promoted weak-type blocker (the user's "address blockers" directive). **B482 (+1) — the weak-type rule (TS2559/TS2560).** A WEAK target (object/intersection with ≥1 property, ALL optional, no call/construct/index sigs) is VACUOUSLY assignable from any source (the relation passes), so tsc additionally requires ≥1 common property; with none it reports TS2559 (non-callable source) / TS2560 + related TS6212(call)/TS6213(new) (callable source). Built as DEDICATED walkers (NOT the relation engine — the FP firewall): (1) a call-arg check in `checkArgumentsAgainstSignature` placed EARLY (before the intervening fn-vs-fn/excess blocks `continue` past it) using `weakTargetProperties` (intersection-aware) + `weakSourcePropertyNames` (CONSERVATIVE — bails on union/intersection/typeparam/Reference/any/unknown/nullish sources so a missed property never FPs; primitives use wrapper apparent props, callables add FUNCTION_TYPE_PROPERTIES); (2) a var-decl NESTED-weak chain `tryEmitNestedWeakVarDecl` (`let weak: Weak & Spoiler = propertiesWrong` → TS2322 + "Types of property 'properties' are incompatible." → "Type '{wrong}' has no properties in common with type '{b?}'."); (3) the array `push` case folded into the B473 push walker (function-body locals/params aren't bound there, so a threaded local-annotation map resolves `changes`/`error`). Display fixes: literal-preserving source (`12` not `number`), localized named-function object-return render `() => { timeout: number; }` (our `inferReturnTypeFromBody` lacks an object-literal branch — done for display only via a Signature copy, NOT the broad return-inference change), AsExpression span clip, receiver-annotation element-alias target display (`ChangeOptions`). The other 3 weak-type tests (`nestedExcessPropertyChecking`, `intersectionPropertyCheck`, `noInferCommonPropertyCheck1`) need additional pieces (intersection SOURCE, NoInfer/Partial materialization, FP-suppression of existing TS2322) — left for follow-up (B482 queue item). NEXT: extend the weak-type walkers to the other 3 tests, or a Blocker #2/#3 BUILD session._ _[Round 194 trimmed to STATUS-HISTORY.md.]_ _[Round 193 trimmed to STATUS-HISTORY.md.]_ _[Round 192 trimmed to STATUS-HISTORY.md.]_ _[Round 191 trimmed to STATUS-HISTORY.md.]_ _[Round 190 trimmed to STATUS-HISTORY.md.]_ _[Round 189 trimmed to STATUS-HISTORY.md.]_ _[Round 188 trimmed to STATUS-HISTORY.md.]_ _[Round 187 trimmed to STATUS-HISTORY.md.]_ _[Round 186 trimmed to STATUS-HISTORY.md.]_ _[Round 185 trimmed to STATUS-HISTORY.md.]_ _[Round 184 trimmed to STATUS-HISTORY.md.]_ _[Round 183 trimmed to STATUS-HISTORY.md.]_ _[Round 181 trimmed to STATUS-HISTORY.md.]_ _[Round 179 trimmed to STATUS-HISTORY.md.]_ _[Round 178 trimmed to STATUS-HISTORY.md.]_ _[Round 177 trimmed to STATUS-HISTORY.md.]_ _[Round 175 trimmed to STATUS-HISTORY.md.]_ _[Round 173 trimmed to STATUS-HISTORY.md.]_ _[Round 172 trimmed to STATUS-HISTORY.md.]_ _[Round 171 trimmed to STATUS-HISTORY.md.]_ _[Round 170 trimmed to STATUS-HISTORY.md.]_ _[Round 169 trimmed to STATUS-HISTORY.md.]_ _[Round 167 trimmed to STATUS-HISTORY.md.]_ _[Round 166 trimmed to STATUS-HISTORY.md.]_ _[Round 165 trimmed to STATUS-HISTORY.md.]_ _[Round 164 trimmed to STATUS-HISTORY.md.]_ _[Round 161 trimmed to STATUS-HISTORY.md.]_ _[Round 159 trimmed to STATUS-HISTORY.md.]_ _[Round 158 trimmed to STATUS-HISTORY.md.]_ _[Round 157 trimmed to STATUS-HISTORY.md.]_ _[Rounds 151, 156 trimmed to STATUS-HISTORY.md.]_ 
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
