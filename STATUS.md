# Status

**Phase 4 — Checker buildout: COMPLETE. Phase 17 — self-compile the TypeScript compiler itself (re-scoped 2026-07-03; "any TypeScript project" is post-v1) — is the LIVE mission (see PLAN-PHASE-5.md; v1 exit = all 8 tsc-source profiles at zero FPs — **the FP leg was MET at round 481 (2026-07-12): all eight profiles at zero real FPs, only env-legit offline artifacts remain**; emit/crash legs verified same round: all 8 profiles emit every program file, exit 0, no crashes — the OFFLINE-VERIFIABLE v1 DEFINITION OF DONE IS MET; next: M5 performance; dashboard: bench/*.tsv, self-compile diagnostics 13,245 → 936 (measured at round 430 — rounds 395–412 burned bounded non-M3 histogram-tail buckets + M3.4 flow-narrowing slices the round-394 "pool picked over" triage missed, all surfaced by bucketing the full `--listAll` output by normalized message shape: TS2499 multi-base-generic misparse, TS2440 type-only-barrel-merge, TS2344 constraint-chain + cross-file `typeParamInternCache` pos-collision, TS2693/TS2709 clodule + destructuring-shadow, TS2314 enum-member-vs-generic, TS2588 nested-shadow, TS2551 Object.setPrototypeOf, SetIterator/MapIterator lib gap, TS2774 local-shadows-function, TS1100 interface-property-name, TS7023 callback-arg self-ref, TS2365 arithmetic local-shadows-function, TS2362 branded-number, TS7053 enum-reverse-mapping, round 408's TS2349 callee flow-narrowing family, and round 409's `export *`-barrel / ESM-`.js` imported-guard flow narrowing (M3.4, TS2339 838 → 672), and round 410's TS2862 constraint-index-sig gate / assign-RHS type-guard narrowing / void-overload-return (2,443 → 2,433), and round 411's M3.4 enum-member discriminant narrowing (barrel-import-resolved, three families UpToDateStatus/TypeMapper/PrivateIdentifierInfo) + type-guard-narrows-a-member-DOWN (2,433 → 2,374, TS2339 672 → 614) + round 412's M3.4 single-type type-guard narrow-DOWN + TS18048 receiver-narrowing (2,374 → 2,373), and round 413's `export *` LEAF-EXPORT gate (barrel-imported `Debug.assert` & every barrel guard now resolves — the TRUE builder.ts blocker, NOT the round-412 depth red herring) + tsc-faithful linear flow-walk iteration + return-path narrowing (2,373 → 1,965, TS2339 614 → 237)), then rounds 414-417 burned more bounded/M3.4 buckets (TS2366 CFA fall-through, NonNull/&&-narrowing arithmetic, closure-loop narrowing, namespace-local `extends` bases: 1,965 → 1,902), and round 418 resolved NESTED type-guard functions (M1.12, 1,902 → 1,854, TS2339 237 → 189: tsc guards `isTupleType`/... are nested in `createTypeChecker` so the binder skips them and `resolveFlowCalleeDecl` missed them - the guard never narrowed, the single biggest TS2339 sub-family), and round 419 resolved properties on INTERSECTION union-members (M1.12, 1,854 → 1,808, TS2339 189 → 143: `getPropertyOfType`/`typeHasOwnProperty` bail on a `Type.Intersection` member so `PropertyAccessExpression | (ElementAccessExpression & …)` FP'd TS2339 on `.parent`; fold the constituents in property resolution + discriminant-narrowing annotation; 0 new FPs, self-compile time −17%), and round 420 resolved TYPE-ALIAS enum-member discriminants in narrowing (M1.12, 1,808 → 1,799, TS2339 143 → 134: a `.kind: <alias>` discriminant member survived a `switch (x.kind) { case }` because `enumMemberKeysOfTypeNode` handled only a direct `Enum.Member`, not a bare-Identifier alias; 0 new FPs), and round 422 burned FIVE bounded M3.4/overload families (1,799 → 1,756: TS2769 60 → 45 — overload arg-check helpers now flow-narrow reference args (mirroring B469) + a synthetic true|false union for guard-narrowed boolean args vs literal overload params; TS2339 134 → 117 — mixed enum + string-literal discriminant unions narrow on disjoint lit:s: keys (PrivateIdentifierInfo); TS18048 10 → 7 — an optional-chain discriminant access x?.kind === <non-nullish> proves the receiver non-nullish; TS2366 12 → 4 — union-.kind exhaustive switches terminate via requiredUnionDiscriminantKeys), and round 423 (a) drove TS2366 to ZERO (1,756 → 1,752: the switch RECEIVER is guard-narrowed + typed before the exhaustive-switch union walk — flow graph via the pass-dedicated implicitReturnFlowGraph, local-const-from-call typing, optional-discriminant @undefined keys, LiteralToken["kind"] indexed-access) and (b) fixed union-TARGET type guards end-to-end (1,752 → 1,708: `x is A | B` PARSED truncated so isCallOrNewExpression/isPropertyNameLiteral/isOptionalChain never narrowed; + aliased conditions `const isFrag = guard(x); if (!isFrag)` via a memoized flow back-walk; + kind-key disjointness beating the lenient relation; TS2339 117 → 104, TS2322 784 → 756) and (c) narrowed truthy optional-chain CALL conditions (1,708 → 1,707: `if (x.y?.size())` proves `x.y` non-nullish), and round 424 burned SEVEN more M3.4 flow-narrowing slices (1,707 → 1,662, TS2339 104 → 68, TS18048 5 → 1, TS2322 756 → 751: loop-entry union suppression with a STRUCTURAL wash gate, call-RHS return-annotation narrowing, closure/join/call-crossing aliased conditions, prefix-path receiver guards, asserts-with-inferred-TP via test-arg predicate inference, assignment-overwrite reset to the declaration/call-RHS resolved type, and the DebugTypeMapper this-narrowing slice (explicit-type-arg asserts re-type `this`; an overload-cluster name collision resolves to the unique TypePredicate-bearing decl) — every step by-site strictly removals), and round 425 found the TS2339 never-cluster ROOT CAUSES (1,662 → 1,600, TS2339 68 → 6: the round-411 enum-discriminant key space SPLIT across resolution paths — canonicalEnumSymbol at all four key-builder sites — plus per-candidate narrow-DOWN distribution in union-target guards/instanceof, aliased ===/switch discriminants, switch-default negative narrowing, typeof-object filtering, boolean-literal discriminant truthiness, destructuring-read narrowing, the loop-entry retry for the single-type suppression, and the positive-empty intersection fallback whose round-423 dead-end verdict was an ARTIFACT of the key split) — driving to 0, and round 426 replaced the B399 TS2563 per-file proxy with the faithful flow-walk depth trip + the asserts-callee call gate (1,600 → 1,593: TS2563 27 → 0; TS2454×20 pre-existing definite-assignment walker FPs unmasked), and round 427 burned that bucket (1,593 → 1,577: TS2454 20 → 4 via tsc's definiteness rules — logical assigns ??=/||=/&&= are DEFINITE so captured reads assume initialized, a !-asserted read assumes initialized, and the anywhere-scan's left-spine walk now applies the target rule per spine node), and round 428 took the first real M3.1 slice of the TS2322/TS2345 cores (1,577 → 1,186, −25%: nullable-union generic param inference + overloaded generic callees for tsc's `x = append(x, item)` idiom (TS2322 751 → 559), the TS2345 histogram top — explicit this-param binding, guarded optional-member args, numeric-enum→number (394 → 280) — the array-literal-vs-union-member string-layer rule (TS2322 → 501), and the body-local-shadows-function anyType registration (TS2345 → 261, TS2769 → 36)), and round 429 drove the TS2345 core 261 → 86 (1,186 → 1,000, −15.7%: call-types lexical shadowing — body-locals vs enclosing params, DESTRUCTURED params via the currentParamBindingNames side set in getTypeOfIdentifier (the round-428 "mini-repro does not reproduce" residue), arrow own-params; embedded String replace/replaceAll/search/split accept RegExp; optional-param union args + NonNull-asserted args; guard-narrowed interface/unknown Identifier args — the ~110-site dominant mechanism, never-param excluded for assertType<never> exhaustive-switch defaults; string-enum→string in isSimpleTypeRelatedTo resolving the round-410 Extension[][] deferral; rest-arg flow narrowing), and round 430 unlocked the `append`/`addRange` inference (936: the `T extends {}` constraint failed `string → {}` and killed every mapper — empty-object-target relation rule with TP-source excluded per genericPrototypeProperty3's pin; readonly-array anchors; TP-from-PREDICATE binding for the getFirstJSDocTag family); self-compile time ~110–150 s single-run band), and round 431 took the FIRST M3.2 slice — the TS7006 core 301 → 11 via contextual typing (nested-fn/lexical callee resolvability, assignment-RHS context from the LHS declared type under the B476 single-applicable-sig rule, ||/??-both/&&-comma-right propagation, intersection/Reference/extends receiver members) — plus engine return-checking in switch/try bodies behind the foreign-TP source gate (un-inferred generic call results bail instead of FP'ing; ~130 T[]/U|undefined/SearchResult<T> return sites cleared) and the TS2367 anchored-disjointness gate (936 → 551, −41%; TS2322 435 → 345, TS7019 4 → 0; by-code strictly shrinking at every landed step) — then extended the foreign-TP gate to the var-decl/assignment/property-write/conditional-return paths (551 → 482, −69 strictly removals; TS2322 → 276; a generic FUNCTION-VALUE source's sig-own TPs stay checkable per 5 corpus pins the suite gate caught), and round 435 took SEVEN more M3.1/M3.2 slices at post-perf-arc iteration speed (482 → 373, −22.6%: generator TReturn returns, fresh-objlit literal props via the freshObjLitRange relation retry, TP-literal-constraint args, the union-decomposition-transparent relation re-entry gate resolving the NodeArray-covariance family ×23, bare-`new` contextual instantiation, the foreign-TP gate on assignment TARGETS (visitor family ×15+), nullish alias-union returns; TS2322 276 → 184, TS7006 11 → 1, TS2345 86 → 79; full-dashboard baseline: compiler 482→373 / tsc-cli 484 / jsTyping 480 / deprecatedCompat 481 / typingsInstallerCore 489 / services 1,603 / server 1,894 / harness 2,193 at 29–41 s wall each), and round 436 took SEVEN more M3.1/M3.2/M3.4 slices (373 → 294, −21.2%: TP-carrying callback-return param skip (forEachEntry family ×14), destructured-LOCAL shadowing (semver/checker + 12 transformer sites), literal-return syntactic union membership (the string-fallback re-widening trap — `return false` vs `Tag | false` ×7, incl. the completely UNPINNED `return "a"` vs `"a" | "b"` shape), constraint-filtered explicit-type-arg overload selection ('true' vs 'false' ×6), overload-helper optional-param union-arg + foreign-TP rules (union-receiver TS2769 ×13), bare-string switch-case narrowing (semver ×3), guard-gated ternary return arms with the tri-state all-leaves-verified early return (×19); TS2322 184 → 158, TS2345 79 → 47, TS2769 30 → 9; round-436 dashboard baseline: compiler 373 / tsc-cli 375 / jsTyping 371 / deprecatedCompat 372 / typingsInstallerCore 380 / services 1,476 / server 1,769 / harness 2,062 at 27–41 s wall), then round 438 (2026-07-07) took FIVE more M3.1/M3.4 narrowing/relation slices (294 → 244, −17%: an empty-object `{}` union-member return-path early return; UNION-target extension of the assignment-RHS AND return-path type-guard narrowing gates (`currentSourceFile = node` / `return node` vs `SourceFile | undefined`); PROPERTY-ACCESS args in the call-arg guard-narrow-DOWN branch (`getExports(node.left)`); and nullish-strip-gated narrowing of object-literal property VALUES (`{ moduleSpecifiers: specs }` where `specs = append(specs, x)`); TS2322 158 → 116, TS2345 47 → 39), then round 439 (2026-07-07) took THREE predicate-overload / arg-narrow-DOWN slices (244 → 228, −16 strictly removals: findAncestor-style type-guard-callback overload return inference + a companion NonNull concrete-union strip, the overload arg-check non-union narrow-DOWN, and its `unknown`-via-typeof extension; TS2769 9 → 1).** Corpus suite green as a permanent regression gate: 13,092 tests / 0 failing / 3 skipped (generated tsgo-relevant corpus + local corner-case tests; count as of round 756 = 13,089 owner-verified at `2f728c1e` plus 3 new `CtaSectionProbeTest` pins for (FN.1)'s expression-bodied-arrow descent; round 755 = 13,053 owner-verified at `38b79404` plus 18 new `NarrowSectionProbeTest` pins for (CALL.4)'s intra-`applyConditionNarrowing` harness and 4 added to `CtaSectionProbeTest` for (ENGINE.1)'s level-C partition; rounds 751-754 added 5 / 9 / 6 / 9, taking 13,024 → 13,053; round 750 added 6 pins for (REL.1)(c) step 5a — the single mint of the enum discriminant key space; rounds 746-749 added 18 / 8 / 4 / 6 for (c) steps 2 / 3 / 4 / 4b; round 745 added 11 pins for (REL.1)(c) step 1's value-aware enum rule; round 744 which added 15 pins for (REL.1)(b) and its two enum-free unblockers and turned the five (REL.1) `@Ignore`s ON — which is why the skipped count fell 8 → 3; round 743 added 7 for the overload picker's two bugs; round 742 added 9 pins for (REL.1)(b0) — the enum-member ACCESS EXPRESSION; round 741 added 5 for (REL.1)(a)'s enum-member type — 4 stating the base-primitive legs, 1 the `typeof === "object"` knock-on — plus one `@Ignore`d divergence pin; round 734 added 5 for the (CALL.1)(a) one; round 733 added 5 for the (SPINE.1)(a) intra-handler attribution harness; round 732 added 6 for the (DISPATCH.1)(a) derivation harness; historical accretion follows — round 658 was at 12,534 and added no pins (its 33 M04TypeParamTypedOpsSpineMigrationTest pins landed at the round-657 tail); round 657: +23 M04ArrayToClassCastSpineMigrationTest = 12,474, +33 M04TypeParamTypedOpsSpineMigrationTest at the session tail = 12,507; round 656 added no pins — its 30 M04ArgumentsInFieldSpineMigrationTest pins landed at the round-655 tail); round 655: +37 M04ImplicitAnyNewSpineMigrationTest = 12,421, +30 M04ArgumentsInFieldSpineMigrationTest at the session tail = 12,451; round 654: +40 M04IncDecTypeParamSpineMigrationTest and +9 ConflictMarkerScanTest = 12,384; round 653: +44 M04AbstractAccessSpineMigrationTest = 12,335; round 652: +36 M04ImplicitAnyYieldSpineMigrationTest = 12,291; round 651: +30 M04AbstractContextSpineMigrationTest = 12,255; round 650: +33 M04CtorParamInitSpineMigrationTest = 12,225; round 649: +46 M04DeleteOpSpineMigrationTest = 12,192; round 648: +30 M04TypeParamCastSpineMigrationTest = 12,146; round 647: +41 M04ObjLitSuperSpineMigrationTest = 12,116; round 646: +42 M04ConstLitSpineMigrationTest = 12,075; round 645: +52 M04StrictModeSpineMigrationTest = 12,033; round 644: +33 M04ExpandoSpineMigrationTest = 11,981; round 643: +56 M04TpDefaultsSpineMigrationTest = 11,948; round 642: +35 M04InvalidAssignSpineMigrationTest = 11,892; round 641: +35 M04SuperRebindSpineMigrationTest = 11,857; round 640: +32 M04UndefinedNameSpineMigrationTest = 11,822; round 639: +33 M04EvolvingArraySpineMigrationTest = 11,790; round 638: +25 M04ArgsCollisionSpineMigrationTest = 11,757; round 637: +31 M04GenericIndexWriteSpineMigrationTest = 11,732; round 636: +33 M04PropertyInitSpineMigrationTest = 11,701; round 635: +39 M04ProtectedReadSpineMigrationTest = 11,668; round 633: +15 M04NullCastSpineMigrationTest; round 632: +25 M04ConstEnumSpineMigrationTest; round 631: +31 M04BindingPatternSpineMigrationTest; round 630: +26 M04CastOverlapSpineMigrationTest; round 629: +17 M04DaFlowSpineMigrationTest; round 628: +25 M04Sym2StrSpineMigrationTest; round 627: +24 M04AbstractClassSpineMigrationTest; round 626: +17 M04FnTypedParamSpineMigrationTest; round 625: +18 M04ImplicitThisSpineMigrationTest; round 624: +14 M04SpreadSpineMigrationTest +9 M04ArrayPushSpineMigrationTest; round 623: +8 M03StructureBundleTest; round 622: +10 IntKeyMapTest; round 621: +6 NodeKindIdTest, +5 LongKeyMapTest); earlier baseline 11,347 as of round 587 (−12 net: both giants' audit scaffolding retired with their passes) — incl. 78 giant-migration pins; every per-statement emission of the legacy assignability dispatchers, class property initializers, AND the checkFunctionBody body-level walkers are spine-anchored) — **the full tsgo-relevant TypeScript compiler test suite passes.** _Round 443 (2026-07-08, Phase 17 — module-augmentation family + the module-file-local TYPE-alias leak (Blocker #3)): FOUR bounded fixes, all suppression-only. Compiler profile UNCHANGED (190) but the fixes GENERALIZE hugely: services 591 → 498 (−93), server 887 → 733 (−154), harness 1,118 → 989 (−129); suite 9,504 → 9,512 (+8 local across 3 test files, 0 regressions); 4 fix commits; services diffed via --listAll as strictly by-position removals. (1) TS2664 10 → 0: `resolveModuleSpecifierRelativeJsAware` — a `declare module "../compiler/types.js"` augmentation resolves `.js` → the `.ts` sibling (the base resolver deliberately avoids `.js` per the TS2459 gotcha, so the augmentation target never resolved); purely additive → only SUPPRESSES a false 'cannot be found'. (2) TS2564 17 → 0: a class property whose declared type INCLUDES `undefined` needs no definite assignment (tsc's strictPropertyInitialization `getFalsyFlags(type) & TypeFlags.Undefined`); `checkClassPropertyInit` missed the `| undefined` case, so `SourceFileObject`'s `nameTable: Map<...> | undefined` + siblings FP'd; reuses the existing `typeIncludesUndefined`. (3) TS2304 24 → 2: `buildNamespaceScope` gained a StringLiteralNode branch — a `declare module "X"` augmentation body sees the AUGMENTED module's exports by bare name (Node/NodeArray/SymbolFlags/TypeChecker/__String — compiler/types.ts exports NOT imported into services/types.ts); the 2 remaining are NodeJS `global` (env-legit, offline). INVESTIGATED & REVERTED (dashboard no-op): TS7006 array-element contextual typing — a correct M3.2 enabler (3 passing tests) but the real inferFromUsage.ts `Priority[]` case is B83.5-blocked (`interface Priority` nested in a function → unbound → `Priority[]` resolves to any); land it with nested-type resolution. (4) TS2339 44 → 0 (SourceFileLike, Blocker #3): a module-file-local `type X = A | B` alias leaks into globals and shadows the global `interface X` in OTHER files (TYPE-space analog of round 442's variable leak). importTracker.ts's non-exported `type SourceFileLike = SourceFile | AmbientModuleDeclaration` won the last-wins merge over compiler/types.ts's `interface SourceFileLike`, so a `sourceFile: SourceFileLike` param resolved to the bogus union (AmbientModuleDeclaration has no `.text`); pinned via an instrumented probe (whole-program-only — minimal repros don't reproduce). Built `conflatedTypeAliasFiles`; checkMemberAccessMissing bails a union-receiver TS2339 when the receiver displays as a conflated name in a NON-alias file (own-file still fires). NEXT (services @ 498, all deep): TS2322×235 / TS2339×85 (residual conflations — Type×20/Info×12) / TS2345×54 M3 relation, TS2416×11, TS2353×10, TS2740×9/TS2739×8._ 

_Round 764 (2026-07-29) — **TWO PARTS OF (REL.2), AND EACH ONE'S RESULT IS A MEASUREMENT RATHER THAN A FEATURE.** **PART 1: round 763's +4.78% flow-walk bill is returned in full — `narrow.walks` 74,729 -> 71,326, three walks above the pre-763 71,323.** The call-argument narrowability gate's enum arm is now a SECOND CHANCE: it pays for the walk only on the REJECTING path, because if the DECLARED enum already satisfies the parameter no narrowing can change any verdict. **99.9% of round 763's added walks were on the ACCEPTING path** — in tsc's own sources an enum-typed argument overwhelmingly goes to a parameter of that same enum. The idiom is round 743's (a second chance can turn a rejection into an acceptance, never the reverse) and it is scoped to the enum sub-case alone; the Interface / `unknown` / `string` / `number` arms beside it are corpus-pinned and keep walking unconditionally. It tightens automatically as the relation does: enum -> MEMBER is vacuously true TODAY, so a `K.A` parameter takes the skip path now and will take the walk path once the global rule lands. **PART 2: the NEGATIVE direction landed — `k !== K.A`, the else-branch of `k === K.A` and a type guard's FALSE branch on a bare enum now SUBTRACT from the enum's member domain** (`K.B | K.C | K.D` where the answer was `K`), via `enumMemberTypesOf` (the domain, in DECLARATION order) and `enumMinusMembers` (the subtraction, gated by the round-746 owner rule so a foreign enum's member subtracts nothing). **The interesting result is that it FIRES AND CHANGES NOTHING: 14 firings on the compiler profile and 31 on services — 5 of them building a 395-of-396-member `SyntaxKind` union — with the grid byte-identical and all 20 deterministic cost counters BIT-identical.** That firing count was measured with a temporary counter (added, measured, removed) precisely so the byte-identity would not be the round-753 non-evidence; what it establishes is that every firing is VERDICT-NEUTRAL on these profiles, and what it does NOT establish — recorded as a limit — is whether the consumers agree or discard, which the counters cannot separate. **No corpus baseline moved, so no `LogicalParityDivergence` was needed and none was added.** Arm-A grid **46 / 46 / 46 / 46 / 46 / 46 / 46 / 94** with composition unchanged and every profile's error lines byte-identical to round 763; corpus subset E/C/N/S/T (`enum*`/`controlFlow*`/`narrowing*`/`switch*`/`type*`) **3,363 / 0**; filtered narrowing/enum/guard/switch/flow batch **954 / 0**; build warning-clean; cost counters rebaselined in Part 1's own commit (`narrow.walks` -4.55%), unchanged by Part 2. **Part 2's 5 targets all FAIL on unmodified `4c0f1150` and its 5 controls all pass; Part 1 has NO target pin and cannot have one** — its skipped branch is the one where the raw type was already accepted, so it has no observable behaviour by construction, and that is stated as the limit on its evidence rather than dressed up. +19 pins (`EnumArgumentSecondChanceTest` 9, `EnumNegativeNarrowingTest` 10). **Predictions 5 of 6** — the miss is Part 2's, predicting a >5% wall-time rise from `SyntaxKind` decomposition, which is the POPULATION-vs-FREQUENCY error in its cheapest form: how often the source writes a shape is not how often a narrow for it is requested. The full suite and the cost gate are the owner's to run._

_Round 763 (2026-07-29) — **(REL.2)(B)+(C) LANDED, AND BOTH LABELS ARE AGAIN SHARPER THAN THEY READ: (C) IS NOT A DECOMPOSITION AT ALL, AND THE TWO `completions.ts` LINES ROUND 762 FILED UNDER (B) ARE NOT (B).** The scaffolded global-rule price falls **compiler 46 -> 52 becomes 46 -> 47, services 46 -> 57 becomes 46 -> 52** — **5 of the 11 unmasked worklist lines closed** (`parser.ts:2629`, `3762`x2, `8444`, `8728`), and the compiler profile is now ONE line away from the rule being free. Arm-A grid **46 / 46 / 46 / 46 / 46 / 46 / 46 / 94**, every profile's error lines **byte-identical to round 762**; corpus all 25 generated classes **8,837 / 0**; filtered narrowing/enum/guard/switch batch **918 / 0**; build warning-clean. **The global rule was NOT landed** — it was scaffolded behind a temporary flag purely to re-price, and removed. **(C) IS NOT ABOUT DECOMPOSITION.** An enum's own type is a member-LESS `Type.Object`: it is neither a `Type.Union` nor a `Type.Interface`, so it fell through the call-argument site's NARROWABILITY GATE and the flow read was never attempted. `narrowByCallPredicate`'s single-type arm answers `K.A | K.B` correctly the moment it is reached — the fix is one clause in one gate, not a member union. That is why an `if` BLOCK narrowed an enum correctly all along (its body types from `currentLocalTypes`) while a ternary, an `&&`, an early-return guard, an assert call and a `switch` all failed: those are exactly the positions that need the flow read. **(B) IS INDEPENDENT OF (C), which round 762 said it was not**: `k === K.A` fails even when the reference is ALREADY declared `K.A | K.B | K.C`, so it never needed a union to filter — `literalTypeOfExpression` simply has no answer for an enum-member reference, and `narrowUnionByLiteral`'s two comparators had no enum arm. **THE INSTRUMENT WAS THE ROUND.** Five probe generations narrowed it to "flow fails for a non-union enum, succeeds for a union", and every structural reading of the guard code said that was impossible — a one-line trace then showed `narrowByCallPredicate` was **never called** with the enum type at all. The lesson is the one this arc keeps paying for: when the code says the answer should be right and the output says it is wrong, the missing step is upstream, and a `println` settles in 90 seconds what reading cannot. **`completions.ts:2234`/`2239` did NOT close and are not (B)** — 2234 is a property read on a union of generic instantiations (`Modifier.kind` answers `SyntaxKind`, the constraint of `Token<TKind>`), 2239 is generic inference through `tryCast` losing `KeywordSyntaxKind`; neither is narrowing. So the remaining worklist is **4 x cause (D) + 2 property-read/inference lines**, and (D) is still the arc it was sized as. Every pin run against unmodified `d47ffdc0`: **9 TARGETS FAIL, 5 NEGATIVE CONTROLS PASS**; +14 pins (`EnumFlowNarrowingRel2Test`), all reading the narrowed type out of a PRIMITIVE probe target so silence can never be mistaken for narrowing. **Predictions 4 of 6.** The full suite and the cost gate are the owner's to run._

_Round 762 (2026-07-29) — **THE SIX (REL.2) GAPS ENUMERATED: THEY ARE FOUR CAUSES, ROUND 760's LABEL IS WRONG FOR THREE OF THE SIX SITES, AND THE ONE CAUSE THAT LANDED IS THE ONE THAT OWNED `completions.ts:2237` — THE LAST NON-ENV-LEGIT LINE ON THE ENTIRE 8x2 GRID.** Arm A is now **46 / 46 / 46 / 46 / 46 / 46 / 46 / 94** (services 47 -> 46, server 47 -> 46, harness 95 -> 94; the other five unchanged), and **all eight profiles carry an IDENTICAL composition** (`TS2591x43 / TS2304x2 / TS2584x1`, harness proportionally) — every remaining line is env-legit. **The enumeration is the deliverable and it collapsed six symptoms to four causes:** (A) a guard target's enum member lost through GENERIC INSTANTIATION — `completions.ts:2237`, and never one of the six; (B) `===`/`switch` does not narrow against an enum MEMBER at all — `parser.ts:8444`/`8728` plus `completions.ts:2234`/`2239`; (C) an enum type is never decomposed into its member union — `parser.ts:2629`/`3762`x2; (D) `const` initialisers do not preserve LITERAL types — `scanner.ts:905` plus `importFixes.ts:1127`/`1162` and `formattingScanner.ts:113`. **Both of round 760's labels are incidental:** the `||` does nothing (a plain single `if (kk === K.A)` fails too, while `1|2|3` on `=== 1` narrows), and the ternary does nothing (the same guard narrows in an `if`, and narrows in a ternary when the reference is declared as a member union rather than as the enum). **Cause D is not about enums and is an ARC, not a round** — `const x = "a"; const y: "a" = x;` already reports *Type 'string' is not assignable to type '"a"'*, so this is tsc's fresh/widening literal-type machinery, and it owns **4 of the 11** unmasked lines, the largest share. **THE INSTRUMENT CAME FIRST AND THE FIRST ONE WAS WRONG:** a branded-object probe target reported silence for six of eight sites, because a member-less enum `Type.Object` relates to it VACUOUSLY; a PRIMITIVE probe target separates them in one run and names the narrowed type — the same class of error as round 760's read-into-`string`, caught before it was spent. **CAUSE A LANDED (`88386a1d`) AND ROUND 760's FOUR-INGREDIENT REDUCTION WAS WRONG ON THREE OF FOUR:** the early return is irrelevant, the enum discriminant is irrelevant, the second guard is irrelevant — what matters is that **the PRECEDING guard's target is a union of GENERIC INSTANTIATIONS**, which tsc's `Modifier` (a union of `ModifierToken<SyntaxKind.X>`, with `kind` declared once on the generic base `Token<TKind>`) is and no plain-interface reduction is. Root cause is one read: `enumMemberDomainProvesNotSubtype` asked `getTypeOfSymbol`, which answers with the DECLARING interface's member type — the bare parameter `TKind`, carrying no `EnumLiteral` flag — so the veto declined and the collapse to `never` survived; `propertyTypeOnCarrier` substitutes through the carrier's type arguments. **EIGHT REDUCTIONS STAYED CLEAN BEFORE THE REAL SOURCE WAS PROBED** (extensionless-vs-`.js` specifiers, barrel depth 1/2/3, named-vs-namespace imports, augmentation of base and derived) — the answer came from compiling a probe file INTO the materialised services profile against the genuine types, and a guard onto a SINGLE generic instantiation already answered correctly, which is exactly why only a UNION reproduces. Unrelated find, unqueued and costing the dashboard nothing: an imported guard behind an EXTENSIONLESS specifier never narrows, while `.js` does. **THE GLOBAL RULE WAS RE-MEASURED, NOT ASSUMED, AND NOT LANDED: the price is UNCHANGED at compiler 46 -> 52 / services 46 -> 57**, so cause A removed a CONSUMER of the leniency, not any of the gaps; `completions.ts:2237` does not return under the rule. Every pin run against unmodified `254b4151`: **3 TARGETS FAIL, 3 NEGATIVE CONTROLS PASS**. Corpus: all 25 generated classes, **0 failures**; narrowing/guard/enum/relation filtered batch **816 / 0**; +6 pins (`GenericGuardTargetEnumMemberTest`); build warning-clean. **Predictions 3 of 6** — the misses are that the gaps would be enum-specific and that closing them would make the global rule free. The full suite and the cost gate are the owner's to run._

_Round 761 (2026-07-29) — **(REL.3) SIZED, FIXED, AND ITS HEADLINE CLAIM FALSIFIED IN THE SAME ROUND.** Round 760 reported *"every keyword node's `kind` in tsc's own source is `any`"* from an 8-line synthetic reduction. Measured against the REAL `src/compiler/types.ts` this round: **`AbstractKeyword.kind` is `SyntaxKind.AbstractKeyword`, correct, and always was** — tsc writes `export type AbstractKeyword = ModifierToken<SyntaxKind.AbstractKeyword>`, a type ALIAS, and an alias to a generic reference takes the WORKING `resolveGenericPropertyType` path; the `interface X extends Intermediate<Arg>` form the reduction used does not occur in the source. **So (REL.3) is NOT the cause of `completions.ts:2237` and does not block (REL.2)** — that claim is withdrawn, and the line's visible cause is that inside `if (isIdentifier(node))` the reference stays `Node`, i.e. the (REL.2) narrowing family. **The defect itself was real and is now fixed.** It is not `any` — it is the **unsubstituted type PARAMETER**; round 760's read-into-`string` probe cannot tell the two apart (an unconstrained TP relates leniently that way), while a WRITE probe separates them in one run and renaming each declaration's parameter names WHICH hop failed. Root cause is one function: `findInheritedBaseRef` enqueued a `Type.Reference`'s target's `baseTypes` RAW, but those are written in the TARGET's type-parameter scope (`interface Fwd<TFWD> extends Box<TFWD>` stores `Box<TFWD>`), so the walk answered `Box<TFWD>`; it now applies that reference's mapper first, exactly as `resolveGenericPropertyTypeWorker` already does for the same descent. **Shape census: 13 shapes, 10 broken, 9 fixed** (forwarded / wrapping / extra / reordered TPs, class chains, method signatures, nested args, 3 hops); a type-ALIAS intermediate still degrades to `any` at a different site and is recorded, not fixed. **Blast radius zero on every axis** — corpus all 25 generated classes **8,837 / 0**, and the 8x2 arm-A grid **46 / 46 / 46 / 46 / 46 / 47 / 47 / 95, identical count AND composition to round 760**. **That zero is exactly the round-753 trap, so it was instrumented**: a shadow A/B counter ran the OLD walk beside the new one and measured **0 firings on the compiler profile and 0 on services** — the changed code never executes on tsc's source, so the green corpus and unmoved grid prove nothing about them, and this is recorded as a limit on the evidence rather than banked. What does fire is the synthetic tsc-shape repro (1 firing). Every pin run against unmodified `bf9abfb3`: **5 TARGETS FAIL, 3 NEGATIVE CONTROLS PASS**, and the controls are load-bearing (a SPECIALIZED intermediate must keep `string`; a REORDERED one must not be matched positionally). +8 pins (`HeritageTypeArgumentSecondHopTest`); build warning-clean. **Predictions 2 of 6** — the two misses are that the lost type was `any` and that the member table was the site. The full suite and the cost gate are the owner's to run._

_Round 760 (2026-07-29) — **THE 8x2 DASHBOARD RE-MEASURED END TO END FOR THE FIRST TIME SINCE ROUND 730, AND IT BARELY MOVED — five profiles are unchanged CODE FOR CODE across ~50 commits.** Arm A (embedded libs, `types: []`): compiler / tsc-cli / jsTyping / deprecatedCompat / typingsInstallerCore **46** (`TS2591x43 / TS2304x2 / TS2584x1`, composition identical to round 730), services **47**, server **47**, harness **95**. **The only movement anywhere on the grid is ONE line, not three**: services / server / harness are exactly the three profiles containing `src/services`, and each carries the byte-identical `completions.ts:2237` TS2345 — UP, so a regression (tsc reports zero on its own source). Every other line on the grid, both arms, is env-legit. **Arm C is recorded with its caveat rather than compared**: round 730 measured it against the real `@types/node` v22.20.1, `bench-compile-tsc.sh` `rm -rf`s that directory on any run without `--node-stub`, and there is none on this box — this round's arm C uses the all-`any` STUB, which can only suppress, so 13 -> 1 is a FLOOR and is NOT claimed as an improvement. **The reduction is 12 lines and the fix is not the reduction.** Four ingredients, each shown necessary by ablation: an ENUM discriminant, TWO guards on one reference, an EARLY RETURN in the first, the guards declared in ANOTHER file. Measured with a `const s: string = node` probe (a washed reference is SILENT, because `never` is assignable to `string`): after `!isMod(node)` the type was `never` and is now `Node`; inside the next guard it was `never` and is now `Ident`. **Round 753's stated premise is falsified, and the comment saying so was written the same round** — `getDeclaredTypeOfEnumMember` mints member types with NO members, so `Node <: Mod` relates VACUOUSLY; round 472 vetoed the resulting `never`-collapse, round 753 deleted the veto claiming (REL.1)(a)/(b) had taught the relation to decide for itself. It had, for member-vs-member; `checkTypeRelatedToCore` already recorded that the enum-vs-MEMBER direction was left open. `enumMemberDomainProvesNotSubtype` restores that one verdict at that one site. **The global rule was WRITTEN, COMPILED, MEASURED AND REVERTED — a sized refusal with the size**: closing enum -> MEMBER properly costs **compiler 46 -> 52, services 47 -> 57**, because the vacuous `true` masks that many flow-narrowing gaps (a guard used as a TERNARY CONDITION; `===` narrowing across `||`), so the unit of work is the NARROWING FEATURES, not the relation rule — queued as (REL.2) with all six sites. **The real root cause is a SUBSTITUTION defect and it is not enum-specific — queued as (REL.3)**: `interface FwdA extends Fwd<K.A>` where `Fwd<T> extends Box<T>` resolves `.v` to **`any`**, while `BoxA extends Box<K.A>` resolves it to `K.A` — one heritage hop substitutes, a second hop through an intermediate FORWARDING its own type parameter does not, which is tsc's `AbstractKeyword -> KeywordToken<TKind> -> Token<TKind>` exactly, so every keyword node's `kind` is `any`. That is why the landed veto leaves the profile line in place and the 8 profiles are byte-identical — stated as a LIMIT ON THE EVIDENCE, not a credential. **The emit path, un-exercised for 22 rounds, is confirmed working**: the compiler profile with emit ON writes **78 `.js` files**, exactly the `emitted=78` of every pre-738 TSV row, with the same 46 diagnostics; tree sha256 recorded in the artifact. Every pin run against unmodified `aef21e76`: **three TARGETS FAIL, three NEGATIVE CONTROLS PASS**. Filtered batches **858 + 1052** hand-written and **the ENTIRE generated corpus, all 25 classes**, 0 failures; the two changed files compile warning-clean. **Predictions 3 of 6** — the two misses are the ones that moved the map. The full suite and the cost gate are the owner's to run. Artifact: `docs/perf/dashboard-round760.md`._






Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `docs/history/STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `docs/history/STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
