# Status

**Phase 4 — Checker buildout.** 9,744 / 10,086 tests passing (~96.6%, 342 failing). _Round 186 (2026-06-16, multi-iteration session, addressing blockers): **+2 net so far (344→342 failing, 9742→9744 passing), B467 + B468 (2 commits), ZERO regressions at each step (exact failing-set diff via `scripts/fail_set.py`).** Re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE=8 all type-engine; OUTPUT=1 tsgo-irrelevant systemModule17) + ~30 manual triages all multi-piece. Both wins extend warm flow/generic-inference Blocker beachheads. **B468 (+1) — narrowingPastLastAssignment (Blocker #1 flow, extends B464 `isPastLastAssignment`).** Two coupled fixes: (1) `checkUninitializedLetCapturedReads` (TS7034/7005 for uninitialized `let x;` captured in a closure) now DROPS captured reads PAST the last UNCONDITIONAL same-scope assignment (those have the known last-assigned type) — but CONDITIONAL assignments (inside if/while/for via a new `UlState.conditionalDepth`) do NOT count as definite, so f9/f10's `if(cond){x=…}`-then-closure reads still fire (the regression the first cut caused — fixed by the conditionalDepth gate); (2) B464 closure narrowing now EXCLUDES captured `var`s — tsc gives a captured `var` (function-scoped/hoisted) its declared type in a closure, not the narrowed type, so f13's `var foo:string|undefined; foo=''; ()=>foo.x` keeps `string|undefined` → TS18048 fires. Pieces: `FlowStart.enclosingVarDecls` (Map name→VariableDeclaration, collected in `bindFunctionLikeBody` from the enclosing function body, not descending into nested fns); `outerFlowForCapturedName` returns null for a captured var; the TS18048 emitter recovers the captured var's declared annotation type via `enclosingVarDecls[root].type` (getTypeOfExpression returns `any` for a function-body var across the closure boundary, unlike a param). **B467 (+1) — chainedCallsWithTypeParameterConstrainedToOtherTypeParameter2, the documented harder variant of B462 (Blocker #2 generic-inference).** New dedicated `checkChainedTypeParamClimbing` walker, gated to the exact `thenChainMethod` class shape (`Chain<T [extends I]>` with `then<S extends T>(cb:(x:T)=>S):C<S>`, reusing B462's `thenChainMethod`), walking INSIDE the `then` method body (which B462's `walkThenChains` never recurses into → disjoint; B462's `processThenChain` also bails on Identifier arrow bodies). For each fluent `.then` chain whose arrow bodies RETURN bare type-param LOCALS (`var t!:T; var s!:S; var i!:I`), it tracks the current chain type-arg as a NAME and uses an AST constraint graph {S→T→I} (NO relation engine for the climb): (1) climbing TS2322 "Type 'R' is not assignable to type 'curArg'." + "could be instantiated" chain + TS2208 (at the returned TP's decl, suggesting `extends <curArg>`) + TS6502 (at the cb FunctionType) when an arrow returns a local of type-param R that is NOT a subtype of the chain's current type arg; (2) final-write TS2322 (string-vs-number) when a non-climbing chain ends in `.value.<prop> = rhs` and rhs is not assignable to the property type resolved via the relation engine from the type-arg's ultimate constraint interface. Result type-arg clamps to the inferred R even on error. NEXT: contained pool genuinely exhausted; remaining are multi-piece type-engine Blockers (Record/mapped-type materialization, broad generic-inference engine, flow-aware-typeof-in-type-position, cross-file scope) + parser-recovery._ _Round 185 (2026-06-16, multi-iteration session, addressing blockers): **+1 net so far (345→344 failing, 9741→9742 passing), B466 (1 commit), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** Re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=8 all type-engine; OUTPUT=1 tsgo-irrelevant), then ranked ALL 345 failures by raw diff size (mine_small_diffs with the mention filter removed) and triaged the bounded `errors` candidates directly. **B466 (+1) — allowImportClausesToMergeWithTypes (default-import-of-value used as a type → TS2749 + type/value default-export merge → TS2528 FP suppression).** Two coupled pieces: (1) `isValueOnlyTypeRef` previously suppressed TS2749 for ALL aliases; now a DEFAULT import whose target module's default export is unambiguously a VALUE (new AST-only `defaultExportClassification` + `defaultImportIsStrictlyValue`) fires TS2749 — LOAD-BEARING: scan the CURRENT file's own import statements, not the merge-polluted symbol `declarations` (picking the wrong same-name import FP'd `zzz`-from-./a); (2) broadened the B274 interface-default merge exception so an `export default interface` (type-space) is excluded from TS2528 participants when ANY value-providing default coexists (class/function DECL, EXPR, REF_VALUE, or a local `export { value as default }` re-export) — `exportDefaultAlias_excludesEverything` (interface + interface-REF_TYPE, both type-space) still fires. NEXT: continuing to triage the bounded diff-ranked `errors` tail; remaining are mostly type-engine Blockers (mapped-type/Record materialization, generic inference, flow-aware-typeof-in-type-position, cross-file module-resolution modeling) + parser-recovery._ _Round 184 (2026-06-16, multi-iteration session, addressing blockers): **+1 net (346→345 failing, 9740→9741 passing), B464 flow-into-closures (Blocker #1) LANDED in 1 commit, ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** Flipped `implicitConstParameters`. Two coupled pieces, both committed together: **(1) closure-narrowing infra** — `FlowStart` gained `outerFlow`/`reassignedAfterNames`/`localNames`; `bindFunctionLikeBody` sets them for ArrowFunction/FunctionExpression containers (outerFlow = the enclosing flow at the closure's definition point; reassignedAfterNames = a source-text scan of assignments in `[closure.pos, enclosingFn.end)` — tsc's `isPastLastAssignment`, the f4-vs-f5 distinction; localNames = the closure's own param+body-decl names for shadowing). The checker's `narrowTypeFromFlow` (+ the FollowLoopEntry mirror) `FlowStart` case continues into `outerFlow` via `outerFlowForCapturedName` iff the captured root is not this/super/arguments, not a closure local, and not reassigned-at/after the closure. This alone is **exactly net-zero** (verified — removes `fn`'s FP TS2339 @ 7:27 by flowing `number` into the closure, no other change). **(2) general closure-captured TS18048 emitter** `emitTs18048ForClosureCapturedUndefinedReceiver` — for `x.prop` where `x` is a captured (not closure-local) identifier inside a closure (found via `FlowGraph.closureStarts` range-containment) whose flow-narrowed type is still a union including `undefined`, under strictNullChecks, not optional-chained → TS18048 at the receiver. The **closure restriction is the FP firewall** (no general possibly-undefined emitter exists; our narrowing has gaps outside closures). Together: f1–f4/fn/f6 flow narrowing in (no undefined → no emit), f5 (reassigned-after) keeps `string|undefined` → TS18048 @ 44:27. The round-183 "reassigned-anywhere" approximation would have FP'd f4 once the emitter was added; the `[closure.pos, enclosingFn.end)` RANGE scan is what gets f4 right (assignment before the closure still flows in). NEXT: the closure-narrowing infra + TS18048 emitter are reusable; the broad type-engine Blockers (Record/mapped-type materialization, generic-inference engine, cross-file scope) remain the bulk._ _Round 183 (2026-06-16, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+1 net (347→346 failing, 9739→9740 passing), 1 feature commit (B419b), ZERO regressions (exact failing-set diff via xml.etree vs `/tmp/fail_baseline.txt`).** Method: re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=8 all type-engine; OUTPUT=1 tsgo-irrelevant systemModule17), then built a `scripts/fail_set.py` failing-set differ + a header-diff-size ranker over the 236 "close" (non-NONE) failures and triaged ~25 candidates directly (all multi-piece: finicky multi-file emit-ordering moduleResolutionWithRequire/importInsideModule; generic-composition inferFromGenericFunctionReturnTypes1; flow-aware-typeof-in-type-position controlFlowForIndexSignatures; circular-type-cycle circularBaseTypes; Partial+exactOptional multipleBaseInterfaes…2; bare-specifier TS2307 decoratorMetadataTypeOnlyImport = FP-risky per CLAUDE.md; lib-dependent templateStringsArray/noParameterReassignmentJSIIFE; JSON-module-typing requireOfJsonFileInJsFile). **B419b (+1) — jsFileClassPropertyType2 (JSDoc `@type` on a JS class expando `this.X = …`).** The documented B419 gap: `/** @type {number[]} */ this.p = []` in a checkJs class constructor was inferring `any[]` (from the empty-array RHS) instead of the JSDoc-declared `number[]`, so a cross-file `(new C()).p.push("string")` missed TS2345. Exposed a public `Parser.parseJsDocTypeNodeFromComments` (wraps the existing `parsePropertyTypeFromJSDoc` sub-parser) and extended `collectConstructorThisAssignments` with an optional `jsdocTypes` capture of each `this.X=`-statement's leading `@type {T}` (first-wins); the B419 synthesis now prefers the JSDoc type (resolved to a concrete non-error Type) over the RHS-inferred type, falling back to RHS inference. FP firewall: only OVERRIDES when the parsed `@type` resolves to a concrete non-error Type — else identical to before; the only failing consumer was the target. NEXT: the contained pool is genuinely exhausted (10+ rounds + this round's ~25 triages); remaining wins are multi-piece type-engine Blockers (Record/mapped-type materialization, generic-inference engine, flow into-closures/typeof-in-type-position, circular-type-cycle detection, cross-file scope) + parser-recovery (Blocker #7)._ _Round 182 (2026-06-16, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+1 net (348→347 failing, 9738→9739 passing), 1 feature commit (B463), ZERO regressions (exact failing-set diff via xml.etree vs `/tmp/fail_baseline.txt`).** Method: re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=8 all type-engine; OUTPUT=1 tsgo-irrelevant), then ranked ALL 347 by diff size and triaged ~50 candidates directly — all multi-piece (relation-engine elaboration display inheritance1/optionalParamTypeComparison; generic-composition inferFromGenericFunctionReturnTypes1; flow-into-closures implicitConstParameters; flow-aware-typeof-in-type-position controlFlowForIndexSignatures; unknownEmptyObjectType narrowing inKeywordAndUnknown; circular-base/constraint TS2310/2313/2456; clodule staticMemberExportAccess; cross-file/Record/mapped-type). **B463 (+1) — enumAssignmentCompat7 (nominal enum mismatches in class-override / overload / member-access).** New `checkEnumNominalClassMismatches` walker (AST + B425 `EnumAsgInfo`, no relation engine), gated to genuinely-incompatible NAMED enums (different simple name OR different member value) which never relate in tsc — so it never double-emits with the general relation paths (which treat numeric enums as `number` and stay silent here): (A) TS2339 for a QUALIFIED `<ns>.E.X` value-position member access where X is not a member of enum E (bare-enum access is owned by the standard checkPropertyAccess TS2551 path → gate to a PropertyAccess receiver; enum merged with a same-name namespace excluded via `encmCollectNsKeys` — the mergedDeclarations3 FP firewall); (B) TS2416 for a class method override whose single param is a different incompatible enum than the base method's → contravariant param-mismatch chain; (C) TS2394 for an overload signature whose return enum differs from the impl signature's + related TS2750. The two regressions the first cut caused (exactSpellingSuggestion = bare `U8.bit_2` already owned by the TS2551 path; mergedDeclarations3 = `M.Color.Blue` enum+namespace merge) both fixed by the qualified-receiver gate + the namespace-merge exclusion. NEXT: the contained pool is genuinely exhausted (50+ candidate triages this round + 9 prior rounds); remaining wins are multi-piece type-engine Blockers (Record/mapped-type materialization, the broad generic-inference engine, flow evolving-array/into-closures, cross-file scope) and parser-recovery (Blocker #7)._ _[Round 181 trimmed to STATUS-HISTORY.md.]_ _[Round 179 trimmed to STATUS-HISTORY.md.]_ _[Round 178 trimmed to STATUS-HISTORY.md.]_ _[Round 177 trimmed to STATUS-HISTORY.md.]_ _[Round 175 trimmed to STATUS-HISTORY.md.]_ _[Round 173 trimmed to STATUS-HISTORY.md.]_ _[Round 172 trimmed to STATUS-HISTORY.md.]_ _[Round 171 trimmed to STATUS-HISTORY.md.]_ _[Round 170 trimmed to STATUS-HISTORY.md.]_ _[Round 169 trimmed to STATUS-HISTORY.md.]_ _[Round 167 trimmed to STATUS-HISTORY.md.]_ _[Round 166 trimmed to STATUS-HISTORY.md.]_ _[Round 165 trimmed to STATUS-HISTORY.md.]_ _[Round 164 trimmed to STATUS-HISTORY.md.]_ _[Round 161 trimmed to STATUS-HISTORY.md.]_ _[Round 159 trimmed to STATUS-HISTORY.md.]_ _[Round 158 trimmed to STATUS-HISTORY.md.]_ _[Round 157 trimmed to STATUS-HISTORY.md.]_ _[Rounds 151, 156 trimmed to STATUS-HISTORY.md.]_ 
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
