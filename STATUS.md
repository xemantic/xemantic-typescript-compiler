# Status

**Phase 4 — Checker buildout.** 9,686 / 10,086 tests passing (~96.1%, 400 failing). _Round 163 (2026-06-14, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+2 net so far (402→400 failing, 9684→9686 passing), 2 feature commits (B421, B422), ZERO regressions at every step (exact failing-set diffs).** Method: re-confirmed the contained DIFF/SWAP/MISSING pool exhausted (EXTRA/MISSING/SWAP fresh=0; mine_small_diffs.py shows every error-line-diff test already plan-mentioned; the fresh NONE pool — controlFlowArrayErrors/awaitedType/fixingTypeParametersRepeatedly2 — all confirmed multi-piece by direct read; the TS5107 ×14 cluster is AMD/UMD/System JS-emit tsgo-irrelevant, the deprecation diagnostic itself works). Traced ~20 candidates (incl. the 5 TS18048-only tests — all multi-piece closure-const/Record/union-tuple/contextual-generic flow) + ran a 14-candidate adversarial-triage workflow (stopped early after extracting concrete sketches). **B421 (+1, 9685/401) — overloadOnConstInheritance2**: TS2430 for a derived interface that re-declares an OVERLOADED base method with a single incompatible signature. `interface Base { f(x:string):any; f(x:'foo'):string }` + `interface Deriver extends Base { f(x:'bar'):string }` → the derived `(x:'bar')=>string` is not assignable to the base overload set `{ (x: string): any; (x: "foo"): string; }` (a single derived sig must satisfy EVERY base overload; `'bar'` passes `string` bivariantly but fails `'foo'`). The existing `checkInterfaceExtendsClauses` method branch fired only for single-base-decl arity mismatches; new `tryEmitTS2430OverloadSetMismatch` + `methodSigDisplayColon` handle the ≥2-base-decl overload-set case with the 4-line param-mismatch chain. **FP firewall (load-bearing): base method has ≥2 decls AND derived has EXACTLY ONE decl of the name** — a derived that re-declares the FULL overload set (overloadOnConstInheritance1, derived count 2) is compatible and is skipped; single-base-decl (inheritance3) and class-implements (inheritance4) take other paths. Generic (type-param) methods skipped; same-arity only; bivariant param check so only genuinely-unrelated params fire. ZERO regressions (exact diff: only overloadOnConstInheritance2 flipped). **B422 (+1, 9686/400) — strictOptionalProperties3 (TS2375, JS @typedef exactOptionalPropertyTypes)**: `/** @type {A} */ const a = { value: undefined }` in a `.js` file where the JSDoc `@typedef` declares `value` OPTIONAL with a non-undefined type → under exactOptionalPropertyTypes, assigning `undefined` to an optional-but-non-undefined property is TS2375 "...is not assignable to type 'A' with 'exactOptionalPropertyTypes: true'. Consider adding 'undefined'...". The `@typedef` is NOT bound (raw-source only), so new `resolveJsTypedefOptionalProps` scans the JSDoc comments for both forms (`@typedef {object} A` + `@property {number} [value]`, and inline `@typedef {{ value?: number }} B`); `tryEmitJsTypedefExactOptional` checks each object-literal `prop: undefined` against the typedef's optional non-undefined prop and emits TS2375 + the 2-line chain at the var name. Added as a `run {}` block in `checkVarDeclAssignability` after B286. FP firewall (nil surface — no other corpus test combines exactOptional+JS+@type+object-literal): exactOptional+strictNull + JS file + `decl.typeFromJSDoc` + UNBOUND `@type {Name}` TypeReference + object-literal init + a `prop: undefined` value matching an optional non-undefined typedef prop. ZERO regressions (exact diff: only strictOptionalProperties3 flipped)._ _Round 162 (2026-06-14, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+1 net so far (403→402 failing, 9683→9684 passing), 1 feature commit (B420), ZERO regressions (exact failing-set diffs).** Method: two read-only research-agent fleets (14 candidates) + direct deep-reads RE-confirmed the contained pool genuinely thin — every closest-miss DIFF/NONE candidate is 2-piece, flow-blocked, shared-display-path-risky, or tsgo-irrelevant (keyofDoesntContainSymbols = @keyofStringsOnly; the agents systematically over-classify CONTAINED + miss tsgo-relevance + miss the CLAUDE.md `getTypeOfIdentifier`-no-narrowing gotcha). **B420 (+1, 9684/402) — returnInConstructor1**: a class constructor returning an OBJECT LITERAL or a NEW expression whose type is not assignable to the instance type now emits TS2322 + TS2409 (primitive/type-param/null returns were already covered). Object-literal: per-property TS2322 at the property KEY + TS6500 'expected type comes from property X', plus one TS2409 at `return`. New-expression: whole-type TS2322 + a LOCALLY-built 'Types of property X are incompatible' chain + TS2409. **Two load-bearing lessons**: (1) the local chain builder renders a no-return-body method as `() => void` (the global `getTypeOfSymbol` defaults such methods to `() => any`; broadening THAT — even though CLAUDE.md called it net-zero — now regresses 5 accessor/override tests, so the void inference is DISPLAY-ONLY); (2) resolving every class's declared type eagerly in the walker poisoned type caches by first-touch ordering (regressed getAndSetNotIdenticalType2/3 + inheritanceMemberAccessorOverriding*) → resolve the class type ONLY when a ctor actually returns an obj-lit/new-expr._ _Round 161 (2026-06-14, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+4 net (407→403 failing, 9679→9683 passing), 3 feature commits (B418a, B418b, B419), ZERO regressions at every step (exact failing-set diffs).** Method: line-level closeness analyzer over all 407 failing XMLs (counts every differing diff line, not just `error TS` summary lines) → ranked the genuinely-closest DIFF tests; parallel READ-ONLY research agents traced candidates (lesson re-learned: research agents must NOT run `gradle --tests` — it wipes the full-suite XMLs and collides with the background run). Confirmed the contained pool otherwise architectural (JS-modeling/JSDoc Blocker #5, parser Blocker #7, cross-file merge-order Blocker #3, generic-inference Blocker #2, flow Blocker #1). **B418a (+1, 407→406) — heterogeneousArrayAndOverloads**: tsc's best-matching-overload collapse. When ALL overloads of a call fail AND ≥1 fails on multiple array-literal elements, tsc reports ONLY the overload with the FEWEST per-element diagnostics and anchors at its failing element (`chooseOverload`'s `max>1 ? allDiagnostics[minIndex] : flatten`); we reported every overload + anchored at the method name. New `countFailingArgDiagnostics` + `reported` collapse in `checkArgumentsAgainstOverloads`. FP firewall: collapse only when an array-literal arg fails ≥2 elements; corpus-verified sole consumer. **B418b (+1, 406→405) — assignmentCompat1**: two index-signature assignability fixes — (1) a source INDEX SIGNATURE no longer satisfies a target's required NAMED property (removed a spurious string-index shortcut in `propertiesRelatedTo`; `{[k:string]:any}`→`{one:number}` now TS2741 like the number-index case); (2) a primitive `string` IS assignable to a target whose only index sig is NUMERIC (`{[n:number]:any}` — "string has numeric indexer", via String's apparent-type numeric index sig) but NOT a string-index target (new tightly-gated branch in `structuredTypeRelatedTo` requiring the source apparent type to supply each index sig the target needs). **B419 (+2, 405→403) — jsFileClassPropertyType ×2**: JS class expando instance properties. A `.js`/checkJs class declares instance props via `this.X = expr` in its constructor; `resolveInterfaceMembersCore` now synthesizes those as members (type inferred+widened from the RHS, nullish dropped when a non-nullish assignment exists) for JS-file classes, so external `(new C()).X` from a .ts file sees the property (TS2322 on bad write instead of TS2339). Companion guard `classHasConstructorThisAssignment` in the NewExpression-receiver TS2339 branch (which uses AST-based `lookupInstanceMemberInResolvableChain`, not the resolved Type). JSDoc `@type` on the assignment is NOT parsed (the `@type {number[]}` sibling stays failing). NEXT: contained pool exhausted; remaining is multi-session Blocker BUILDs (JS-modeling/JSDoc #5, flow #1, generic-inference #2, cross-file #3)._ _Round 160 (2026-06-14, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+3 net (410→407 failing, 9676→9679 passing), 3 feature commits (B415 additive excess; B416 + B417 — a Blocker #1 control-flow-narrowing BUILD that FULLY healed the switch-true cluster), ZERO regressions at every step (exact failing-set diffs).** **B417 (+1, 9679/407) — narrowByClauseExpressionInSwitchTrue3** completes the switch-true family (test7=B411, test6=B416, test3=B417): test3 narrows the scrutinee PAST a `switch (true) { case shape.kind==="circle": return … }` — reaching past the switch means circle didn't match → shape is `{square}` → `if (shape.kind==="circle")` is a no-overlap comparison. Two pieces: (1) **binder post-switch exhaustiveness narrowing** — when a switch has no default, the no-match path that reaches postSwitch did so with EVERY case condition false, so narrow the no-match flow by each case-expr-false (chained FlowConditions over preSwitch) instead of joining bare preSwitch (NET-ZERO standalone; already made test3's TS2339 'radius on never' fire); (2) **`checkFlowNoOverlapCondition`** (hooked into BOTH if-statement assignability dispatchers for param+flow context) — flow-aware TS2367 for `recv.prop === literal` in an if/else-if condition where narrowing reduced recv so recv.prop can no longer be the literal. FP firewall: fires only when the DECLARED prop overlapped but the NARROWED doesn't (flow-INDUCED no-overlap; statically-impossible comparisons stay owned by the existing TS2367 sites), narrowed prop all-literals. Re-confirmed the contained pool architectural via fresh candidate finder (EXTRA/MISSING/SWAP=0; NONE pool all JS-expando / mapped-conditional type-engine / lib-content / full-flow) and ~30 candidate deep-reads. **B416 (+1, 9678/408) — narrowByClauseExpressionInSwitchTrue6** (Blocker #1): ground-truth-traced the switch-true siblings B411 left (we emitted a ~13-error CASCADE of wrong never/over-narrow diagnostics) and found test6's baseline needs EXACTLY 2 TS2339s — flipped it with THREE coupled flow pieces, each individually net-zero/zero-regression. (1) **Binder** (`bindSwitchStatement`): bind each case EXPRESSION at `preSwitch`, not at the prior clause-body fallthrough flow (which after a return/break body is unreachable `never` → FP TS2339 'kind on never' on the case exprs — case expressions all evaluate at the switch head before any body runs). (2) **`narrowByDiscriminantProperty`**: narrow a SINGLE discriminated member (non-Union) to `never` when its literal discriminant contradicts the comparison (FP-safely gated to a member genuinely carrying the discriminant) — lets sequential negative narrowing reach the exhaustive `never` once a union collapses to one member, fixing the default `const never:never=x` AND the fallthrough cascade (case "a" was wrongly A|C because the default fell through as C, not never). (3) **`unionAliasStructural`**: a reconstructed full union (switch fallthrough rebuilds `MyType=A|B|C` via `getUnionType`, which does NOT intern → fresh id → the id-keyed `aliasDisplayMap` misses) displays as the alias name via a member-id-set map; a strict subset (`A|B`, line 70) has a different member set → unfolds (matches tsc). FP firewall: only non-generic union aliases whose members are ALL named object types register (primitive unions share singleton ids → over-match). test3 still needs case-expr prior-cases-FALSE narrowing + TS2367-consults-flow, deferred. **B415 (+1, 9677/409) — nestedRecursiveArraysOrObjectsError01**: array-literal excess (TS2353 'jj' "does not exist in type 'Style'") through a recursive `Style = StyleBase | StyleArray` / `interface StyleArray extends Array<Style>` target. `checkArrayLiteralElementExcessProps` previously bailed unless the target was a direct `Type.Reference` to Array; extended additively with `arrayElementTypeForExcess` (Reference-to-Array | interface-extends-Array recovering the arg from the heritage AST via the element SYMBOL — `baseTypes` drops the instantiation and the node cache holds the self-recursive-alias errorType sentinel | union-with-one-array-member), `objectMemberForExcess` (the single non-array object member), and a self-contained Union branch that recurses array elements + excess-checks leaf object literals against the object member, threading the annotation alias name 'Style'. FP-safe (excess is conservative; union descent requires exactly one object + one array member); existing Reference-to-Array/primitive paths untouched. ZERO regressions._ _Round 159 (2026-06-14, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+2 net so far (412→410 failing, 9674→9676 passing), 2 feature commits (B413, B414), ZERO regressions (exact failing-set diffs).** After exhaustively RE-confirming the contained pool architectural (fresh EXTRA/MISSING/SWAP=0; the bounded NONE pool is JS-expando / namespace-import-ambient-resolveAlias / mapped-conditional type-engine / huge-overload; the small-diff fresh tests are all 2-piece — `indexSignatureInOtherFile` TS2314-value-extends-FP + TS2411 cross-file symbol-index; `importWithTrailingSlash` `.`/`..` resolution + default-import member modeling), found a productive ADDITIVE ELABORATION-CHAIN vein (FP-safe by construction: each new branch fires only on a type-pair shape that previously produced no chain). **B413 (+1, 9675/411) — errorMessageOnIntersectionsWithDiscriminants01**: discriminated intersection→intersection TS2322 elaboration. For `A = Common & {foo:1}` vs `B = Common & {bar:1}` (Common a discriminated union) tsc distributes the source intersection-of-union into constituents, reports the first failing constituent against the target alias, drills into the discriminant-matched target constituent, and names the missing required property's sub-object. New `getDiscriminatedIntersectionElaboration` + `distributeIntersectionConstituents` + `intersectionDiscriminants`; dispatched from `getPropertyElaborationChain` (Intersection→Intersection) and wired into the assignment-expression chain path (which only built a chain for Object→Object / Union sources). **B414 (+1, 9676/410) — unionTypeErrorMessageTypeRefs01**: generic-ref/alias → union elaboration. `A<Foo>` vs `A<Bar> | B<Baz> | C<Kwah>` (and the `type X<T>` alias half) drills into the same-generic constituent and reports the type-ARG mismatch. New assignment-path branch + `genericIdentityAndArgs` (keys a Reference on its target interface, an alias-displayed Object on its alias NAME); gated to a same-generic best constituent with a non-empty deeper chain. _[Round 158 trimmed to STATUS-HISTORY.md.]_ _[Round 157 trimmed to STATUS-HISTORY.md.]_ _[Rounds 151, 156 trimmed to STATUS-HISTORY.md.]_ 
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
