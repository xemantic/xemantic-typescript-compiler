# Status

**Phase 4 — Checker buildout.** 9,711 / 10,086 tests passing (~96.3%, 375 failing). _Round 171 (2026-06-15, multi-iteration session, ≥20 iterations per user request, addressing blockers): in progress. **B439 (+1, 376→375 failing) — jsDeclarationsInheritedTypes (TS2564 ×3 in emitDeclarationOnly).** Root cause: `@emitDeclarationOnly:true` takes the `declarationOnly` checker path, and `checkPropertyInitialization()` (the TS2564 strict-property-init walker) lived ONLY in the `if (!declarationOnly)` block — so it never ran for emitDeclarationOnly files, even though tsc DOES emit TS2564 there. The 3 class properties `value;` carry JSDoc `@type {A}` annotations (parser bridges these to `PropertyDeclaration.type`, `typeFromJSDoc=true`), so they're non-any typed, no initializer, no ctor assignment → TS2564. Fix: also call `checkPropertyInitialization()` (behind the same `!strictExplicitlyFalse && !strictPropertyInitializationExplicitlyFalse` gate) inside the `declarationOnly` branch; the walker is well-guarded (skips any/optional/declare/static/abstract/import-type/unresolved-generic/typeof-class-member) so it's FP-safe there. Full-suite: exactly the one flip, zero regressions. The triage Workflow's agent had the DIRECTION backwards (proposed SUPPRESSING TS2564) — independently verified before implementing (recurring lesson: verify every agent verdict).** _Round 170 (2026-06-15, multi-iteration session, ≥20 iterations per user request, addressing blockers): **+6 net (382→376 failing, 9704→9710 passing), 5 feature commits (B438, B438b, B438c+d, B438e), ZERO regressions at every committed step (exact failing-set diffs).** Method: confirmed the fresh bounded pool tiny (9 type-engine NONE tests, all conditional/mapped-type/generic-inference architectural), then ran a corpus-FP-rarity triage **Workflow** over 24 skip-logged "architectural" candidates — surfacing 5 contained wins (the recurring lesson: agents over-classify; the corpus-FP-rarity grep finds the dedicated-walker win). **B438 (+1) uniqueSymbolJs2 (TS2367)** — comparing two distinct JSDoc `@type {unique symbol}` consts in a checkJs file; our engine collapses every `unique symbol` to the shared `esSymbolType` singleton so the standard no-overlap path (which also skips JS) can't see distinct identities. New AST-only `checkJsUniqueSymbolNoOverlap` reads the parser-bridged `@type` annotations (`TypeOperator(UniqueKeyword)`/`TypeQuery`) and fires when two compared idents have distinct unique-symbol origins. **B438b (+1) thisInFunctionCallJs (TS2683)** — the whole `this`-implicit-any pipeline already handles the file; it only failed because `checkImplicitThis` blanket-skipped ALL JS files. Refined the gate to `isJsLikeFileName && !options.noImplicitThis` (let EXPLICIT `@noImplicitThis:true` JS files through; the 2 corpus non-targets use object-literal-method `this`, still suppressed). **B438c (+1) uniqueSymbolJs (TS1337+TS1005)** — malformed index signature inside a JSDoc inline-object `@typedef {{ [key: foo] boolean }}` (illegal index-param type + missing `:`); raw-scan walker `checkJsDocTypedefIndexSignature` (B229-style). **B438d (+1) pushTypeGetTypeOfAlias (TS2303+TS2339)** — a checkJs CJS callable-exports module aliasing one export member to an undeclared other (`module.exports=fn; exports.blah=exports.someProp`); new `checkJsCjsExpandoAliasReads` (TS2303 circular-alias on the LHS + TS2339 on the RHS read against the `{ (): void; blah: any; }` expando display). **B438e (+1) iteratorExtraParameters + asyncIteratorExtraParameters (TS2488/TS2504)** — an object whose `[Symbol.iterator]`/`[Symbol.asyncIterator]` method REQUIRES a parameter is not a valid iterable; every iteration position (`for(-await)-of`/`yield*`/`[...x]`/`f(...x)`) → TS2488/TS2504 + a related TS2322 sig-arity chain. New `checkIteratorMethodExtraParameters` builds the expando display (`{ [Symbol.iterator](_: number): Generator<number, void, unknown>; }`) + generator-yield inference; **also a provably-additive BaselineFormatter fix to render `related.messageChain` continuation lines** (the standard `!!! related` renderer dropped them; verified zero existing related-chain usage + zero regressions). **GOTCHA (cost an extra full-suite cycle): a triage-Workflow agent had EDIT access and left a stray `code=29999` DBG marker in Checker.kt — it FP'd on 8 unrelated tests (any class with a `value` property). Future triage MUST use read-only `Explore` agents (agentType:'Explore').** All 5 walkers FP-safe by corpus-unique/narrow gates. Remaining bounded pool re-confirmed exhausted of trivial wins; ~15 batch-2 candidates still to re-triage (read-only) + multi-session Blockers (#1 flow controlFlowArrayErrors, #2 generic-inference fixingTypeParametersRepeatedly2, conditional/mapped type-engine, #3 cross-file). NEXT: re-triage batch-2 read-only, then a Blocker BUILD._ _Round 169 (2026-06-15, multi-iteration session, ≥20 iterations per user request, addressing blockers): **B437 JSDoc-type-bridge (Blocker #5) — +4 net (386→382 failing, 9700→9704 passing), 4 feature commits (B437a–d), ZERO regressions at every step (HEAD stash-baseline established + exact per-step failing-set diffs).** The top queue item (B437, promoted round 168) is the JSDoc-type bridge — the most-consumed missing piece across the small checkJs failures. Landed all 4 tractable consumers as narrow FP-safe slices: **B437a (+1) jsdocRestParameter** — `@param {...number} a` now parses to a typed rest param (`...a: number[]`), removing the FP TS7006 + TS2554; new dedicated walker `checkJsRestParamArgTypes` emits TS2345 for args not matching the rest element type (the `.js` checkCallExpressionTypes skip is load-bearing, B152b). Parser: `parseJSDocParamPrimitiveTypeMap` returns `JsDocParamTypeMap` (types + restNames); `applyJSDocParamPrimitiveTypes` sets `dotDotDotToken`. **B437b (+1) jsdocCallbackAndType** — a standalone `@callback NAME` (no @param/@returns) is a `() => any` type; a var `@type {NAME<...>}` initialized with `{}` → TS2322 + "provides no match for the signature '(): any'", and calling it with args → TS2554 (reuses `emitTS2554TooMany(0,0,…)`); new `checkJsCallbackTypeAssignments` + `collectSimpleJsCallbacks` raw-scan. **B437c (+1) arrowExpressionBodyJSDoc** — a JS arrow with `@template T` + `@returns {T}` whose concise body is a JSDoc cast to a CONCRETE type C → TS2322 "Type 'C' is not assignable to type 'T'." + "'T' could be instantiated…" at the cast paren (new `checkJsDocArrowReturnConstraint`, reads `ParenthesizedExpression.jsdocCastType`); companion: TS7006 suppressed for a JS arrow/fn-expr whose JSDoc @param tags document EVERY param (tsc never flags a documented param), all-params-documented gate keeps partial cases reporting. **B437d (+1) jsdocFunctionTypeFalsePositive** — `@param {<} x` (empty type-param list) → TS1098 + TS1139 via raw-scan walker `checkJsDocEmptyTypeParamList` (mirrors B259/B286). All four FP firewalls corpus-verified. Method: deep code-path tracing + per-step exact failing-set diffs (no agent over-reliance). The remaining 2 JSDoc failures (jsdocBracelessTypeTag1 = braceless `@type`-on-function return-checking + intersection/union @type assignability; unmetTypeConstraintInJSDocImportCall = cross-file `@typedef {import().Foo<T>}` constraint) are each multi-piece — deferred. Post-B437 the fresh pool (EXTRA/MISSING/SWAP=0; NONE=9 all type-engine TS2344/TS2322/TS2769/TS2403/TS2589/TS7034; OUTPUT=1 tsgo-irrelevant systemModule17) is genuinely exhausted of bounded surgical wins; remaining is multi-session Blocker BUILDs (generic-inference #2 fixingTypeParametersRepeatedly2, flow #1 controlFlowArrayErrors, mapped/conditional type-engine, cross-file #3, utility-type materialization). NEXT: B157 (generic-callee-return) or another Blocker #2/#1 build._ _Round 168 (2026-06-15, multi-iteration session, ≥20 iterations per user request, addressing blockers): **B435 (+2, 9698→9700 passing, 388→386 failing) — objectFreeze + objectFreezeLiteralsDontWiden (bounded type-engine blocker, PROMOTED round 167)**: model `Object.freeze(x)`'s return type. New `tryResolveObjectFreezeCall` in `getReturnTypeOfCallExpression` (next to `tryResolveArrayFromCall`): `Object.freeze(objLit)` → a Type.Object with members marked readonly (`mappedReadonlyMemberIds`) and LITERAL member types preserved as FRESH non-widening literals (`literalTypeOfExpression` + `nonWideningLiteralTypeIds`; boolean literals skipped — `trueType`/`falseType` are singletons whose id-registration would globally break `let b=true; b=false`); `Object.freeze([prim,…])` → `ReadonlyArray<elem>` (primitive-element arrays only — array-of-arrays bails to anyType, preserving objectFromEntries' `readonly [string,number][]` annotation). Function/class/other args → null (anyType; those produce no error). Three coupled pieces: (1) the freeze object SURVIVES `widenType` via a new `frozenObjectTypeIds` set (widenType returns it as-is) — without this, the `getTypeOfSymbol`→`inferTypeFromInitializer`→`widenType` path that a plain object-literal var goes through REBUILT the object with fresh UNMARKED symbols + widened-away literals, so the readonly `o.b=` TS2540 silently didn't fire (the LITERALS test passed via `currentLocalTypes`, which skips widenType for objects, masking the gap); (2) `checkPropertyAccessAssignment` skips the assignability TS2322 when the target prop is in `mappedReadonlyMemberIds` (TS2540 owns readonly writes — tsc types the write as `any`, never TS2322); (3) new `tryEmitReadonlyArrayElementWrite` for `a[i]=v` on a ReadonlyArray receiver → TS2542 "Index signature in type 'readonly number[]' only permits reading." + element-type TS2322 (`a[2].toString()` string vs number) — both squiggle `a[0]`, ordered by the formatter's code-sort (2322<2542). FP firewall: freeze gated to lib `Object` (no user shadow); the TS2322-skip gated to `mappedReadonlyMemberIds` (declared `readonly x` members keep prior behavior); the element-write gated tightly to a ReadonlyArray Type.Reference. **ZERO regressions** (stash-diff: pre=388, post=386, ONLY the 2 targets flipped). Method: deep code-path trace (no over-reliance on agents); the round-167 note's MED-HIGH risk + "under-counted coupling" warning was accurate — the widenType-rebuild interaction was the hidden third piece. NEXT: B436 (cross-file arity extend, no consumer — do not build speculatively); remaining is multi-session Blocker BUILDs (JS-modeling #5, flow #1, generic-inference #2, cross-file #3, utility-type materialization, enum typeof-modeling)._ _Round 167 (2026-06-15, multi-iteration session, ≥20 iterations per user request, addressing blockers): **B434 (+1, 9697→9698 passing, 389→388 failing) — jsFileFunctionParametersAsOptional2 (TS2554+TS6210), Blocker #3 substep (cross-file function-call arity)**: in SCRIPT-mode multi-file programs a top-level `function f` declared in file A is globally visible in file B, but `checkArgumentCounts` arity-checked PER-FILE only. New cross-file overlay (built once from all NON-module non-.d.ts files' top-level FunctionDeclarations; a NON-module file consults it for callees not declared locally). Companion: JS-param optionality is now JSDoc-`@param`-aware (`jsDocRequiredParamNames` + `paramInfo` 3rd arg) — a JS param is implicitly optional UNLESS a non-bracket `@param` tag documents it (so `function f(a,b,c)` with `@param a/b/c` → 3 required; without JSDoc → optional, keeping the sibling jsFileFunctionParametersAsOptional passing). TS6210 "argument for 'x' not provided" related-info resolves against the DECLARING file's source/name (new FuncParamInfo.declFileName/declSource + emitTS2554TooFew relSource/relFileName). FP firewall: cross-file overlay only for NON-module files, only for names not in the file's own funcParams NOR its binder locals (local-shadow guard), cross-file dup names (overloads across files) skipped. ZERO regressions across the full suite (exact diff: only the target flipped). **B433 (+1, 9696→9697 passing, 390→389 failing) — checkingObjectDefinePropertyOnFunctionNonexistentPropertyNoCrash1 (TS2339)**: in a checkJs JS file, a LOCAL function-valued binding `const composed = function(...){}` augmented by `Object.defineProperty(composed,'name',{value:…})` has synthesized expando type `{ (...args: any[]): void; readonly name: string; }`; reading `composed.<undeclared>` (`composed.fn`) → TS2339 against that synthesized display. New dedicated walker `checkJsObjectDefinePropertyLocalFnReads` (+ `buildExpandoFnTypeDisplay`/`blockHasValueReturn` helpers building the type-display string from the fn-expr signature + defineProperty'd readonly members). FP firewall corpus-EXHAUSTIVE: the only checkJs file calling Object.defineProperty on a LOCAL function var. ZERO regression risk by construction (fires only on the unique shape). Method (round-166 lesson): the workflow triage rated this FLIP_NOW fp=0 corpus-unique; the only risk was reproducing the synthesized type-display string, which a dedicated builder matched exactly. **B432 (+1, 9695→9696 passing, 391→390 failing) — jsFunctionWithPrototypeNoErrorTruncationNoCrash (TS2339)**: in a checkJs JS file, a constructor function + `Color.prototype = { …methods… }` defines instance type `Color` (members = ctor `this.X=` writes + prototype obj-literal keys); a prototype method reading `this.<undeclared>` (`toJSON(){ return this.rgb(); }`, `rgb` never declared) → TS2339 "Property 'rgb' does not exist on type 'Color'.". New dedicated walker `checkJsPrototypeMethodThisReads` (B424-style; .js checkPropertyAccess skip intact). FP firewall corpus-EXHAUSTIVE: the ONLY checkJs corpus file assigning an obj literal to `<Fn>.prototype` is the target (the other 3 `.prototype =` files are .ts-only or read `instance.prototype`); `this.X=` write targets inside methods EXCLUDED (they declare expando members); read-walk doesn't cross a nested `function(){}` (rebinds this). MISSING-bucket (we already emit the 8 TS7006). ZERO regressions (exact diff: only the target flipped). **B431 (+1, 9694→9695 passing, 392→391 failing) — expandoFunctionNestedAssigments (TS2339)**: an expando-function property write inside a NESTED function (`Foo.inNestedFunction = 1` in `function bar(p = (Foo.inNestedFunction = 1))`) does NOT declare the property (tsc declares expando props only when the write's container is the function's container = file scope), so the access references a property absent from `typeof Foo` → TS2339. New dedicated walker `checkExpandoFunctionNestedReads` (2-pass: collect file-scope `Foo.X=` declarations NOT descending into function-likes; then emit TS2339 for accesses at function-nesting depth ≥1 of an undeclared prop). FP firewall: plain top-level `function Foo` with NO same-name merge (namespace/interface/class/enum/type-alias/import-equals/var) and NO overload, receiver not shadowed, prop ∉ RUNTIME_PROPERTIES; top-level undeclared READS deliberately not checked (broader FP surface). Verified against the ~18 corpus expando tests (the Declared sibling + topLevelBlockExpando + blockShadowing all have only file-scope/depth-0 accesses → untouched). ZERO regressions (exact count 392→391). Method: workflow triage + direct corpus-FP-rarity analysis (the round-166 lesson — agents over-classify these as architectural)._ _[Round 166 trimmed to STATUS-HISTORY.md.]_ _[Round 165 trimmed to STATUS-HISTORY.md.]_ _[Round 164 trimmed to STATUS-HISTORY.md.]_ _[Round 161 trimmed to STATUS-HISTORY.md.]_ _[Round 159 trimmed to STATUS-HISTORY.md.]_ _[Round 158 trimmed to STATUS-HISTORY.md.]_ _[Round 157 trimmed to STATUS-HISTORY.md.]_ _[Rounds 151, 156 trimmed to STATUS-HISTORY.md.]_ 
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
