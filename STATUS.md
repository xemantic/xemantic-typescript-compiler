# Status

**Phase 4 — Checker buildout.** 9,769 / 10,086 tests passing (~96.9%, 317 failing). _Round 202 (2026-06-19, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+1 net (318→317 failing, 9768→9769 passing) so far, B494 (`maxNodeModuleJsDepthDefaultsToZero`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** Session hit the subagent session-limit mid-run (resets 9:40pm UTC) — pivoted to direct build+verify. Confirmed contained pool exhausted yet again (find_candidates EXTRA/MISSING/SWAP fresh=0; manually triaged ~25 ≤2-error candidates, all type-engine-blocked: flow-narrowing, generic-inference, mapped/conditional eval, cross-file scope, named→named chains). Two finder runs surfaced the win: a skeptical 73-agent triage Workflow (most agents session-limited, but 16 completed — ALL the others BLOCKED, two FLIP_NOW) + a 520-test NONE-tracer Workflow. **B494 (+1, `maxNodeModuleJsDepthDefaultsToZero`)** — TS2339 for `import * as foo from "shortid"; foo.<absent>` where `"shortid"` is a SINGLE non-augmentation ambient `declare module "shortid" { export var x }` DEFINITION. New dedicated AST-shape walker `checkAmbientModuleNamespaceImportMembers` (modeled on `checkDynamicImportNamespaceMembers`), reading the ambient module body's exports directly — SIDESTEPS the documented B152b gap (`resolveAlias` has no NamespaceImport→ambient-module fallback) without touching resolveAlias. FP firewall: exactly-one declare-module def + script-file (not augmentation) + enumerable body (no export=/re-export) + specifier not resolving to a real typed `.ts`/`.d.ts` module + top-level bare ExpressionStatement access only. Over-collects body names (FN-safe). REVERTED a net-zero excess-first-only experiment (incompatibleTypes needs deeper TS2416/TS2769 chains, not just the spurious 2nd-excess 'f' suppression). NEXT: a rich vein of NONE-tracer flip-alone candidates remains (delete-operator TS2703/TS1102 cluster, TS2304-on-type-param-constraint, crashOnMethodSignatures TS2391, operatorAddNullUndefined TS18050 ungate) — building those next._ _Round 201 (2026-06-19, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+2 net so far (320→318 failing), B492 (`readonlyPropertySubtypeRelationDirected`) + B493 (`crashInYieldStarInAsyncFunction`), ZERO regressions at each step (exact failing-set diff).** Both are FP-safe corpus-UNIQUE dedicated AST walkers (the recurring productive vein the agents keep MISSING). B493: TS1320 for `yield* obj` in an async generator whose `[Symbol.asyncIterator]→next→{then method}` chain is a non-promise thenable (`checkAsyncYieldStarThenable`, AST-pattern matcher, fixed message → display-robust).** Re-confirmed the contained pool exhausted THREE independent ways (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=8 all deep type-engine; OUTPUT=1 tsgo-irrelevant systemModule17). Ran a root-cause CLUSTERING Workflow over all 99 real failing NONE tests (after a first run was VOID — `args` arrived as a STRING so `TESTS.slice` produced 4-char garbage batches; fixed with `typeof args === 'string' ? JSON.parse` and re-ran) → clusters: GENERIC_INFERENCE 23, MAPPED_TYPE_EVAL 15, NAMED_STRUCTURAL_CHAIN 11, KEYOF_INDEXED_ACCESS 8, NEW_DIAGNOSTIC 7, CROSS_FILE_SCOPE 6, CONDITIONAL_TYPE_EVAL 5. The 19 "flipAlone+LowMed" candidates the agents surfaced were ALL re-confirmed (via PLAN-HISTORY round-163/170/173/174 grep) as previously-triaged traps/multi-piece — the documented "agents over-classify CONTAINED" lesson held AGAIN. **B492 (+1)** — built the one genuinely-contained win as a dedicated FP-safe AST walker `checkTernaryUnionReadonlyWrites`: `const x = cond ? a : b` (a,b object-TypeLiteral-annotated identifiers) → x is a UNION; writing `x.p` where p is readonly in ANY constituent (inverse of the intersection ALL-rule) → TS2540. Sidesteps the documented FP-risky local-const-type-population (getTypeOfExpression(x)=anyType). Corpus-UNIQUE shape (grep confirms only this test) → exactly +1. Message has no type display → display-robust. NEXT: contained pool re-exhausted; remaining 319 are the deep blockers (generic-argument inference #2 [dominant, 23 NONE], mapped/conditional-type eval, cross-file scope #3, named→named structural chains)._ _Round 200 (2026-06-19, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+0 net (320 unchanged), ONE verified build attempt REVERTED after revealing a tsgo-divergence; tree clean.** Targeted `defaultBestCommonTypesHaveDecls`: built both missing pieces — **Piece A** (`checkEmptyObjectVarPropertyAccess`: dedicated AST-gated walker for TS2339 `<x>.<prop>` where `x` is annotated `{}`/`Object` and `prop` is absent from Object.prototype; skip set is OBJECT_PROTOTYPE_PROPERTIES + `__proto__`, NOT RUNTIME_PROPERTIES which wrongly includes `length`) was **verified CORRECT + FP-SAFE + net-zero** (emits the 2 `.length` errors, ZERO regressions/flips — no other corpus test has the shape; reverted per no-dead-infra, re-addable verbatim); **Piece B** (`tryEmitFixedConflictBareTpTs2345`: the fixed-type-parameter literal TS2345 for `concat(1,"")` → `'""'` vs `'1'`) flipped the target but **REGRESSED `fixTypeParameterInSignatureWithRestParameters` + `genericRestArgs`** (net -1). **KEY FINDING: the test's TS2345 baseline is STALE** — it encodes the OLD best-common-type inference; MODERN tsc/tsgo infers `T = 1 | ""` (union) and reports NO error (as those two now-passing tests encode), so the whole test is tsgo-divergent/unflippable (logged in TSGO-RELEVANCE.md § DIVERGES). Also confirmed `arr.map<U>` callback-return inference (B83.4i) already exists (CLAUDE.md "map → any[]" note is stale). Process lesson: before building to a baseline, check whether contradicting tests prove it's a tsc-version artifact. NEXT: contained pool remains exhausted; deep blockers only (generic union-inference #2, named→named structural CHAINS, cross-file scope #3, mapped/conditional recursion, flow narrowing #1)._ _Round 199 (2026-06-19, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+1 net (321→320 failing), B491 (`deepElaborationsIntoArrowExpressions`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** Re-confirmed the contained pool exhausted via THREE independent finders (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=8 all type-engine; a new "same-multiset-of-top-level-error-CODES" finder = 7 display-mismatch candidates, all blocked: `inheritance1` hierarchy-name relation display, `arrayAssignmentTest1` deliberate `[]`→any[] B87.6, `mergeSymbolReexportInterface` cross-file merge order) + a 40-agent deep-tracing triage Workflow (killed mid-run after 11 verdicts confirmed ALL closest-40 are ARCHITECTURAL/CROSS-FILE/PARSER-RECOVERY/fragile-display). One reverted attempt: the `allowJscheckJsTypeParameterNoCrash` alias-display fix (register `aliasDisplayMap` for a DIRECT function-type alias body so `WatchHandler<any>` keeps its name vs unfolding to `(val: any) => void`) compiled + ran net-ZERO — the alias id can't survive the `ComponentOptions → Record → indexed-access → cross-file contextual` chain (the documented B58.x id-stability fragility); reverted per no-dead-infra. **B491 (+1, `deepElaborationsIntoArrowExpressions`)** — three coordinated elaboration drills reporting the LEAF mismatch at the inner position instead of the coarse whole-value TS2322: (case 1) a var-decl object-literal arrow-valued property vs a method target (`{ y: () => "b" }` vs `{ y(): "a" }`) → TS2322 at the arrow return `"b"` + TS6502, new branch in `emitPerPropertyMismatchesForObjectLiteral` (gated to single-return arrow/fn value + function-typed target member with simple/literal return); (cases 2/3) a return statement drill (`tryDrillReturnArrowOrArray`/`singleReturnExprOf`, wired into `checkReturnAssignability` before the general relation path) — `return () => ({a:''})` vs `() => Foo` drills the returned object literal via `checkNestedObjLitPropTypes`, `return [{a:''}]` vs `Foo[]` reuses `checkArrayLiteralElementExcessProps`, both → TS2322 at the inner key + TS6500 (mirroring the var-decl array/object drill that already made `var y: Foo[] = [{a:''}]` pass). NEXT: contained pool re-exhausted; remaining 320 need the big blockers (generic-argument inference #2, named→named deep-structural CHAINS, cross-file scope #3, mapped/conditional recursion TS2589, flow narrowing) — each a dedicated multi-piece BUILD._ _Round 198 (2026-06-19, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+2 net (323→321 failing), B489 (`namespaceDisambiguationInUnion`) + B490/B490b (`genericAssignmentCompatWithInterfaces1`), ZERO regressions at each step (exact failing-set diff via `scripts/fail_set.py`).** After the most rigorous exhaustion scan any session has run (find_candidates all buckets fresh; a 24-result fresh-eyes triage Workflow over the 160 error-baseline failures = 0 BUILDABLE; cross-test code histograms; manual code-trace of ~8 candidate builds), used a NEW finder — "same-multiset-of-top-level-codes" (right errors, wrong positions/text) — to surface the two DISPLAY/elaboration flips the per-test triage missed. **B490 + B490b (+1, `genericAssignmentCompatWithInterfaces1`)**: a fresh object literal's property mismatch is reported ELEMENTWISE at the property key (value-type-vs-property-type chain) even for OBJECT property types — broadened `emitPerPropertyMismatchesForObjectLiteral` past its `isSimpleCheckableType` (primitive-only) gate to object props via the existing `getPropertyElaborationChain` (gated to a non-empty chain), using `getPropertyTypeForRelation` to INSTANTIATE the generic target's prop (`I<string>.x` = `Comparable<string>`, not the un-instantiated `Comparable<T>` that raw `getTypeOfSymbol` returns → relation trivially passes → no emit). Companion B490b: `getReturnTypeOfCallExpression` returns an IIFE's EXPLICIT return annotation type (`function(): T {…}()`) instead of anyType (gated to `callee.type != null`), so an annotated-IIFE initializer reaches the assignability check. **B489 (+1, `namespaceDisambiguationInUnion`)**: pure display — tsc qualifies a union/tuple member with its namespace ONLY on a simple-name collision (`Foo.Yep | Bar.Yep`), shows the bare `Yep` in a single-member chain; four collision-gated pieces in `formatTypeForDisplay` (Union+Tuple), the Object→Union chain (pick LAST failing colliding constituent, render unqualified), and the var-decl array→tuple message (per-length-bound "requires N…fewer" for a non-empty tuple). Both FP-safe. NEXT: contained pool re-exhausted; remaining 321 need the big blockers (generic-argument inference #2, named→named deep-structural CHAINS [the gate is OPEN — wrong chains not wrong pass/fail], cross-file scope #3, mapped/conditional recursion, flow narrowing) — each a dedicated multi-piece BUILD._ _[Round 198 B489-only detail superseded above.]_ _[Round 197 trimmed to STATUS-HISTORY.md.]_ _[Round 196 trimmed to STATUS-HISTORY.md.]_ _[Round 195 trimmed to STATUS-HISTORY.md.]_ _[Round 194 trimmed to STATUS-HISTORY.md.]_ _[Round 193 trimmed to STATUS-HISTORY.md.]_ _[Round 192 trimmed to STATUS-HISTORY.md.]_ _[Round 191 trimmed to STATUS-HISTORY.md.]_ _[Round 190 trimmed to STATUS-HISTORY.md.]_ _[Round 189 trimmed to STATUS-HISTORY.md.]_ _[Round 188 trimmed to STATUS-HISTORY.md.]_ _[Round 187 trimmed to STATUS-HISTORY.md.]_ _[Round 186 trimmed to STATUS-HISTORY.md.]_ _[Round 185 trimmed to STATUS-HISTORY.md.]_ _[Round 184 trimmed to STATUS-HISTORY.md.]_ _[Round 183 trimmed to STATUS-HISTORY.md.]_ _[Round 181 trimmed to STATUS-HISTORY.md.]_ _[Round 179 trimmed to STATUS-HISTORY.md.]_ _[Round 178 trimmed to STATUS-HISTORY.md.]_ _[Round 177 trimmed to STATUS-HISTORY.md.]_ _[Round 175 trimmed to STATUS-HISTORY.md.]_ _[Round 173 trimmed to STATUS-HISTORY.md.]_ _[Round 172 trimmed to STATUS-HISTORY.md.]_ _[Round 171 trimmed to STATUS-HISTORY.md.]_ _[Round 170 trimmed to STATUS-HISTORY.md.]_ _[Round 169 trimmed to STATUS-HISTORY.md.]_ _[Round 167 trimmed to STATUS-HISTORY.md.]_ _[Round 166 trimmed to STATUS-HISTORY.md.]_ _[Round 165 trimmed to STATUS-HISTORY.md.]_ _[Round 164 trimmed to STATUS-HISTORY.md.]_ _[Round 161 trimmed to STATUS-HISTORY.md.]_ _[Round 159 trimmed to STATUS-HISTORY.md.]_ _[Round 158 trimmed to STATUS-HISTORY.md.]_ _[Round 157 trimmed to STATUS-HISTORY.md.]_ _[Rounds 151, 156 trimmed to STATUS-HISTORY.md.]_ 
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
