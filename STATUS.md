# Status

**Phase 4 — Checker buildout: COMPLETE. Phase 17 — self-compile the TypeScript compiler itself (re-scoped 2026-07-03; "any TypeScript project" is post-v1) — is the LIVE mission (see PLAN-PHASE-5.md; v1 exit = all 8 tsc-source profiles at zero FPs; dashboard: bench/*.tsv, self-compile diagnostics 13,245 → 2,700 (measured at round 397 — three bounded non-M3 parser/checker bugs the round-394 "pool picked over" triage missed: multi-base-generic heritage misparse TS2499 16 → 0, type-only-barrel-import-merges-with-value-local TS2440 10 → 0, and a type-param call arg whose bound satisfies the constraint TS2344 8 → 6) — driving to 0; self-compile time ~74 s, −60% vs the round-386 start).** Corpus suite green as a permanent regression gate: 8,999 tests / 0 failing / 3 skipped (generated tsgo-relevant corpus + local corner-case tests) — **the full tsgo-relevant TypeScript compiler test suite passes.** _Round 397 (2026-07-04, Phase 17 — self-compile burn-down, third bounded bug this session): **a type-param call arg satisfies a generic call's constraint when its OWN bound does — self-compile 2,702 → 2,700 (TS2344 8 → 6), zero corpus regressions.** `createNodeArray<T>()` calling `createNodeArray<U extends Node>` with `T extends Node` FP-emitted TS2344 — T's constraint `Node` ≤ `Node` so T satisfies it, but the relation engine's TypeParam-source path under-resolves. `checkCallTypeArgConstraints` now mirrors the skip the TypeReference path (`checkConstraintsForTypeArgs`) already had: a failed relation is suppressed when the arg is a `Type.TypeParam` with a constraint that IS assignable to the target constraint (gated `constraint != null` so an unconstrained T doesn't over-skip). The remaining 6 TS2344 are other sub-shapes queued under M1.12 (enum-subset `TKind extends JSDocSyntaxKind` vs `SyntaxKind`, an unconstrained-param-vs-`{}` implicit-constraint artifact, a union arg `TIn | undefined`). 4 local tests (GenericCallArgConstraintTest) with unrelated-constraint + unconstrained-param negative controls. Full suite 8,999 / 0 / 3 (+4 local). **This session (rounds 395–397) took the compiler profile 2,726 → 2,700 (−26) on THREE bounded buckets the by-shape `--listAll` histogram surfaced — the round-394 "M3-gated / pool picked over" triage was about the code-path analysis, not the actual FP distribution.**_ _Round 396 (2026-07-04, Phase 17 — self-compile burn-down, bounded checker bug): **a type-only import declaration-merges with a value-only local — self-compile 2,712 → 2,702 (TS2440 10 → 0), zero corpus regressions.** tsc's own `src/compiler` does `import { Node, Identifier, Signature, Symbol, Type, Token, SourceMapSource, NodeLinks, SymbolLinks } from "./_namespaces/ts.js"` (the barrel re-exports the interfaces from types.ts) PLUS local `function Node`/`const SymbolLinks = class` AST/object-allocator helpers — the import binds the TYPE, the local binds the VALUE (disjoint spaces), so tsc reports no error; we FP-emitted TS2440. Fix: `checkImportConflictsWithLocal`'s named-specifier loop skips the conflict when `importedNameIsTypeOnlyThroughBarrel(sourceName)` (a NEW conservative `export *`-following resolver — the existing `isExportedNameTypeOnly` inspects only DIRECT exports, missing barrel-re-exported names; the `.js` specifier needs `resolveBarrelStarTarget` because `resolveModuleSpecifier` won't strip `.js`) AND `localAlias in valueOnlyLocalNames` (function + value-var, MINUS class/enum/interface/typealias/namespace which have a type side that DOES conflict). LOAD-BEARING gates: (1) `!isolatedModules && !verbatimModuleSyntax` — those modes DO error on the merge (TS2865 / TS1484 / TS2440 via the paths below the guard), so a blanket suppression first regressed isolatedModulesSketchyAliasLocalMerge / isolatedModulesExportDeclarationType (the suite gate caught it, fixed); (2) the barrel closure is the WHOLE program (a barrel `export *`s everything), so the per-(barrel,name) result is memoized (`barrelTypeOnlyMemo`, declared before init) and the recursion shares ONE visited set — an un-memoized/fresh-visited walk hangs. **Debugging note now armored in memory + a CLAUDE.md gotcha: the fix appeared to hang for two rounds — it was MEMORY PRESSURE (a manual `-Xmx4g` self-compile atop the Gradle + Kotlin daemons exceeds the 7.7 GB box → swap), NOT the barrel walk; a stubbed-to-no-op helper STILL timed out, which is the tell. Free memory first (`./gradlew --stop && pkill -9 -f KotlinCompileDaemon`, want ≥5 GB free).** FN-safe: any uncertainty (unresolvable star, `export { } from`, `export =`, value found anywhere) keeps firing. 5 local tests (ImportTypeOnlyBarrelMergeTest) with a value-import negative control + a local-class type-side-conflict negative control. Full suite 8,995 / 0 / 3 (+5 local). Combined with round 395, this session took the compiler profile 2,726 → 2,702 (−24) on two bounded buckets the by-shape FP histogram surfaced but the code-path triage missed._ _Round 395 (2026-07-04, Phase 17 — self-compile burn-down, bounded parser bug): **fixed the multi-base-generic heritage misparse — self-compile 2,726 → 2,712 (−14), TS2499 16 → 0, zero corpus regressions.** `interface NodeArray<T> extends ReadonlyArray<T>, TextRange` used to misparse the non-last generic base `ReadonlyArray<T>` as a value-position instantiation expression (synthetic `ParenthesizedExpression`) that DROPPED its `<T>` type arguments — so `resolveBaseSym` returned null AND the checker FP-emitted TS2499. The last base always worked (`{`/`implements` aren't in `canFollowTypeArgumentsInExpression()`); only the `,`-followed non-last base misparsed. Fix: a heritage-scoped `parsingHeritageBase` flag (set around the base spine in `parseExpressionWithTypeArguments`, reset inside `parseArgumentList`) makes the postfix `<` branch bail so `tryScan` restores and the type args are re-read as heritage type arguments — matching tsc's `parseLeftHandSideExpressionOrHigher`, which yields an ExpressionWithTypeArguments verbatim. A genuine heritage call `extends mixin<T>()` (the `(` produces a CallExpression above the guard) and a real non-entity-name base (`extends foo()` / `extends (typeof A)`) are unaffected. The net −14 trades 16 pure-parser TS2499 FPs + 24 other removed FPs for +20 TS2322 `NodeArray<T>` (the M3.1 generic-inference gap, previously MASKED because the unresolved base made `hasUnresolvedTypeParams` bail the comparison — now honestly attributed) + 4 TS2339 union-narrowing (M3.4); base-member inheritance for the non-last generic base is also restored. This is the round-317-324 META-LESSON: the round-394 note called the burn-down pool "picked over / M3-gated", but the by-shape FP histogram surfaced two bounded non-M3 buckets (TS2499×16, TS2440×10) the code-path triage missed — always bucket the actual `--listAll` output by normalized message shape. 6 local tests (MultiBaseGenericHeritageTest) with the sharp member-inheritance signal + a `extends foo()` negative control. Full suite 8,990 / 0 / 3 (+6 local)._ _Round 394 (2026-07-04, Phase 17 M2.2 — burn-down #4: two clean wins, real-lib A/B recount 29 → 27, zero corpus regressions): **(1) `delete x.<Object.prototype member>` now fires TS2790 under real libs (keywordExpressionInternalComments).** `getApparentType` does NOT fold Object.prototype's members (toString/valueOf/hasOwnProperty/…) into an object type's apparent members; under the embedded lib `Array` (value) resolves to the `Array<any>` INSTANCE (declares its own `toString`) so the member was found and TS2790 fired, but under real libs `Array` → `ArrayConstructor` (no own `toString` — inherited from `Object.prototype`) so it was missed and no error fired. Fix: an Object.prototype fallback in the delete check — receiver object-like (`Type.Object`) + name ∈ `OBJECT_PROTOTYPE_PROPERTIES` + no own decl (`propSym == null`) → emit TS2790 (those members are non-optional, present on every object → always TS2790 under strictNullChecks). FP-safe: a user type declaring the name optionally still routes through the own-member branch; scoped to the 7 Object.prototype names. 6 local tests (DeleteObjectPrototypeTs2790Test). **(2) The B553 CJS-string-import spelling-suggestion TS2728 now attributes lib-first (jsExportMemberMergedWithModuleAugmentation2).** `emitCjsStringImportMethodAccess` built its "declared here" via the position-based `resolveDeclarationSourceFile`, so under multi-file real libs `fixed` (real String interface, a DEPRECATED HTML helper) false-matched the large `/index.ts` (`/index.ts:8:18528`) instead of `lib.es2015.core.d.ts:--:--` — the SAME lib-file-attribution bug round 392 fixed at three other TS2728 builders, just an unwired 4th path. Fix: consult `libFileOfDecl(decl)` (node-first) before the position path; empty under the embedded lib → byte-identical there. 1 local test (RealLibsTs2728FileTest). Both are the round-317-324 META-LESSON: a read-only "ENGINE / M3" triage verdict is about the GENERAL fix — a corpus-unique, FP-safe, narrow fallback still flips a test the triage rated engine-gated. No self-compile dashboard delta (corpus-A/B fixes; default stays off). Full suite 8,984 / 0 / 3 (+7 local)._ _Round 393 (2026-07-04, Phase 17 M2.2 — burn-down #3): **the lib-declared utility-alias modifier cluster + the redefineArray construct-sig double-emit — real-lib A/B recount 34 → 29 corpus failures (omitTypeHelperModifiers01, omitTypeTestErrors01, intersectionsAndOptionalProperties, parameterListAsTupleType, redefineArray), zero corpus regressions.** Two "fix by convergence" changes (make the real-lib path behave like the embedded path that already passes the corpus): (1) under `useRealLibs` the lib's `Pick`/`Omit`/`Readonly`/`Parameters`/`ConstructorParameters`/`ReturnType` resolve to a real `TypeAlias` symbol, so the generic alias-substitution path expands their definitions (`Omit<T,K> = Pick<T, Exclude<keyof T,K>>` → the non-homomorphic mapped `{ [P in K]: T[P] }`) and DROPS optional/readonly modifiers — the embedded lib doesn't declare these names so it hits the modifier-preserving `materialize*` dispatch (symbol==null). New `isBuiltinUtilityAlias(name, symbol)` (name ∈ the six utilities + `symbol.declarations.all { it in builtinLibDecls }`) routes a lib-only utility symbol through the same materializers so both paths agree (the materializers reuse the source property Symbols → `readonly`/`?` survive); a user shadow keeps a non-lib decl → gate false → user def wins. (2) redefineArray `Array = fn` double-emitted B444's TS2739 (missing isArray/from/of/[Symbol.species]) AND a construct-/call-sig TS2322 — tsc reports a structural failure ONCE. Guarded the assignment path's 17.111 branch + the general `canUse && !isAssignable` block with `!targetHasRequiredPropAbsentFromSource(source, tt)` (a null-tolerant helper — `collectMissingProperties`/`getMissingRequiredPropertySymbol` BAIL on a null-members function source); fires ONLY when props are genuinely missing (a construct-only interface with no named props still reports the coarse TS2322). 6 local tests (RealLibsUtilityModifiersTest ×4 with a user-shadow negative control; RealLibsCtorAssignTest ×2 with a construct-only positive control). No self-compile dashboard delta (corpus-A/B fixes; default stays off). Remaining 29 are M3 engine (mapped/conditional/indexed-access/variance/intrinsic-string-mapping/Iterator-inheritance), DOM-dependent (`@lib: dom` → post-v1), M2.3 pin-unwind (typed arrays, TemplateStringsArray), or the getApparentType-Object.prototype gap (keywordExpressionInternalComments). Full suite 8,977 / 0 / 3 (+6 local)._ –383 and older trimmed to STATUS-HISTORY.md.]_
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
