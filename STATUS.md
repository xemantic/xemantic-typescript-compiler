# Status

**Phase 4 — Checker buildout: COMPLETE. Phase 17 — self-compile the TypeScript compiler itself (re-scoped 2026-07-03; "any TypeScript project" is post-v1) — is the LIVE mission (see PLAN-PHASE-5.md; v1 exit = all 8 tsc-source profiles at zero FPs — **the FP leg was MET at round 481 (2026-07-12): all eight profiles at zero real FPs, only env-legit offline artifacts remain**; emit/crash legs verified same round: all 8 profiles emit every program file, exit 0, no crashes — the OFFLINE-VERIFIABLE v1 DEFINITION OF DONE IS MET; next: M5 performance; dashboard: bench/*.tsv, self-compile diagnostics 13,245 → 936 (measured at round 430 — rounds 395–412 burned bounded non-M3 histogram-tail buckets + M3.4 flow-narrowing slices the round-394 "pool picked over" triage missed, all surfaced by bucketing the full `--listAll` output by normalized message shape: TS2499 multi-base-generic misparse, TS2440 type-only-barrel-merge, TS2344 constraint-chain + cross-file `typeParamInternCache` pos-collision, TS2693/TS2709 clodule + destructuring-shadow, TS2314 enum-member-vs-generic, TS2588 nested-shadow, TS2551 Object.setPrototypeOf, SetIterator/MapIterator lib gap, TS2774 local-shadows-function, TS1100 interface-property-name, TS7023 callback-arg self-ref, TS2365 arithmetic local-shadows-function, TS2362 branded-number, TS7053 enum-reverse-mapping, round 408's TS2349 callee flow-narrowing family, and round 409's `export *`-barrel / ESM-`.js` imported-guard flow narrowing (M3.4, TS2339 838 → 672), and round 410's TS2862 constraint-index-sig gate / assign-RHS type-guard narrowing / void-overload-return (2,443 → 2,433), and round 411's M3.4 enum-member discriminant narrowing (barrel-import-resolved, three families UpToDateStatus/TypeMapper/PrivateIdentifierInfo) + type-guard-narrows-a-member-DOWN (2,433 → 2,374, TS2339 672 → 614) + round 412's M3.4 single-type type-guard narrow-DOWN + TS18048 receiver-narrowing (2,374 → 2,373), and round 413's `export *` LEAF-EXPORT gate (barrel-imported `Debug.assert` & every barrel guard now resolves — the TRUE builder.ts blocker, NOT the round-412 depth red herring) + tsc-faithful linear flow-walk iteration + return-path narrowing (2,373 → 1,965, TS2339 614 → 237)), then rounds 414-417 burned more bounded/M3.4 buckets (TS2366 CFA fall-through, NonNull/&&-narrowing arithmetic, closure-loop narrowing, namespace-local `extends` bases: 1,965 → 1,902), and round 418 resolved NESTED type-guard functions (M1.12, 1,902 → 1,854, TS2339 237 → 189: tsc guards `isTupleType`/... are nested in `createTypeChecker` so the binder skips them and `resolveFlowCalleeDecl` missed them - the guard never narrowed, the single biggest TS2339 sub-family), and round 419 resolved properties on INTERSECTION union-members (M1.12, 1,854 → 1,808, TS2339 189 → 143: `getPropertyOfType`/`typeHasOwnProperty` bail on a `Type.Intersection` member so `PropertyAccessExpression | (ElementAccessExpression & …)` FP'd TS2339 on `.parent`; fold the constituents in property resolution + discriminant-narrowing annotation; 0 new FPs, self-compile time −17%), and round 420 resolved TYPE-ALIAS enum-member discriminants in narrowing (M1.12, 1,808 → 1,799, TS2339 143 → 134: a `.kind: <alias>` discriminant member survived a `switch (x.kind) { case }` because `enumMemberKeysOfTypeNode` handled only a direct `Enum.Member`, not a bare-Identifier alias; 0 new FPs), and round 422 burned FIVE bounded M3.4/overload families (1,799 → 1,756: TS2769 60 → 45 — overload arg-check helpers now flow-narrow reference args (mirroring B469) + a synthetic true|false union for guard-narrowed boolean args vs literal overload params; TS2339 134 → 117 — mixed enum + string-literal discriminant unions narrow on disjoint lit:s: keys (PrivateIdentifierInfo); TS18048 10 → 7 — an optional-chain discriminant access x?.kind === <non-nullish> proves the receiver non-nullish; TS2366 12 → 4 — union-.kind exhaustive switches terminate via requiredUnionDiscriminantKeys), and round 423 (a) drove TS2366 to ZERO (1,756 → 1,752: the switch RECEIVER is guard-narrowed + typed before the exhaustive-switch union walk — flow graph via the pass-dedicated implicitReturnFlowGraph, local-const-from-call typing, optional-discriminant @undefined keys, LiteralToken["kind"] indexed-access) and (b) fixed union-TARGET type guards end-to-end (1,752 → 1,708: `x is A | B` PARSED truncated so isCallOrNewExpression/isPropertyNameLiteral/isOptionalChain never narrowed; + aliased conditions `const isFrag = guard(x); if (!isFrag)` via a memoized flow back-walk; + kind-key disjointness beating the lenient relation; TS2339 117 → 104, TS2322 784 → 756) and (c) narrowed truthy optional-chain CALL conditions (1,708 → 1,707: `if (x.y?.size())` proves `x.y` non-nullish), and round 424 burned SEVEN more M3.4 flow-narrowing slices (1,707 → 1,662, TS2339 104 → 68, TS18048 5 → 1, TS2322 756 → 751: loop-entry union suppression with a STRUCTURAL wash gate, call-RHS return-annotation narrowing, closure/join/call-crossing aliased conditions, prefix-path receiver guards, asserts-with-inferred-TP via test-arg predicate inference, assignment-overwrite reset to the declaration/call-RHS resolved type, and the DebugTypeMapper this-narrowing slice (explicit-type-arg asserts re-type `this`; an overload-cluster name collision resolves to the unique TypePredicate-bearing decl) — every step by-site strictly removals), and round 425 found the TS2339 never-cluster ROOT CAUSES (1,662 → 1,600, TS2339 68 → 6: the round-411 enum-discriminant key space SPLIT across resolution paths — canonicalEnumSymbol at all four key-builder sites — plus per-candidate narrow-DOWN distribution in union-target guards/instanceof, aliased ===/switch discriminants, switch-default negative narrowing, typeof-object filtering, boolean-literal discriminant truthiness, destructuring-read narrowing, the loop-entry retry for the single-type suppression, and the positive-empty intersection fallback whose round-423 dead-end verdict was an ARTIFACT of the key split) — driving to 0, and round 426 replaced the B399 TS2563 per-file proxy with the faithful flow-walk depth trip + the asserts-callee call gate (1,600 → 1,593: TS2563 27 → 0; TS2454×20 pre-existing definite-assignment walker FPs unmasked), and round 427 burned that bucket (1,593 → 1,577: TS2454 20 → 4 via tsc's definiteness rules — logical assigns ??=/||=/&&= are DEFINITE so captured reads assume initialized, a !-asserted read assumes initialized, and the anywhere-scan's left-spine walk now applies the target rule per spine node), and round 428 took the first real M3.1 slice of the TS2322/TS2345 cores (1,577 → 1,186, −25%: nullable-union generic param inference + overloaded generic callees for tsc's `x = append(x, item)` idiom (TS2322 751 → 559), the TS2345 histogram top — explicit this-param binding, guarded optional-member args, numeric-enum→number (394 → 280) — the array-literal-vs-union-member string-layer rule (TS2322 → 501), and the body-local-shadows-function anyType registration (TS2345 → 261, TS2769 → 36)), and round 429 drove the TS2345 core 261 → 86 (1,186 → 1,000, −15.7%: call-types lexical shadowing — body-locals vs enclosing params, DESTRUCTURED params via the currentParamBindingNames side set in getTypeOfIdentifier (the round-428 "mini-repro does not reproduce" residue), arrow own-params; embedded String replace/replaceAll/search/split accept RegExp; optional-param union args + NonNull-asserted args; guard-narrowed interface/unknown Identifier args — the ~110-site dominant mechanism, never-param excluded for assertType<never> exhaustive-switch defaults; string-enum→string in isSimpleTypeRelatedTo resolving the round-410 Extension[][] deferral; rest-arg flow narrowing), and round 430 unlocked the `append`/`addRange` inference (936: the `T extends {}` constraint failed `string → {}` and killed every mapper — empty-object-target relation rule with TP-source excluded per genericPrototypeProperty3's pin; readonly-array anchors; TP-from-PREDICATE binding for the getFirstJSDocTag family); self-compile time ~110–150 s single-run band), and round 431 took the FIRST M3.2 slice — the TS7006 core 301 → 11 via contextual typing (nested-fn/lexical callee resolvability, assignment-RHS context from the LHS declared type under the B476 single-applicable-sig rule, ||/??-both/&&-comma-right propagation, intersection/Reference/extends receiver members) — plus engine return-checking in switch/try bodies behind the foreign-TP source gate (un-inferred generic call results bail instead of FP'ing; ~130 T[]/U|undefined/SearchResult<T> return sites cleared) and the TS2367 anchored-disjointness gate (936 → 551, −41%; TS2322 435 → 345, TS7019 4 → 0; by-code strictly shrinking at every landed step) — then extended the foreign-TP gate to the var-decl/assignment/property-write/conditional-return paths (551 → 482, −69 strictly removals; TS2322 → 276; a generic FUNCTION-VALUE source's sig-own TPs stay checkable per 5 corpus pins the suite gate caught), and round 435 took SEVEN more M3.1/M3.2 slices at post-perf-arc iteration speed (482 → 373, −22.6%: generator TReturn returns, fresh-objlit literal props via the freshObjLitRange relation retry, TP-literal-constraint args, the union-decomposition-transparent relation re-entry gate resolving the NodeArray-covariance family ×23, bare-`new` contextual instantiation, the foreign-TP gate on assignment TARGETS (visitor family ×15+), nullish alias-union returns; TS2322 276 → 184, TS7006 11 → 1, TS2345 86 → 79; full-dashboard baseline: compiler 482→373 / tsc-cli 484 / jsTyping 480 / deprecatedCompat 481 / typingsInstallerCore 489 / services 1,603 / server 1,894 / harness 2,193 at 29–41 s wall each), and round 436 took SEVEN more M3.1/M3.2/M3.4 slices (373 → 294, −21.2%: TP-carrying callback-return param skip (forEachEntry family ×14), destructured-LOCAL shadowing (semver/checker + 12 transformer sites), literal-return syntactic union membership (the string-fallback re-widening trap — `return false` vs `Tag | false` ×7, incl. the completely UNPINNED `return "a"` vs `"a" | "b"` shape), constraint-filtered explicit-type-arg overload selection ('true' vs 'false' ×6), overload-helper optional-param union-arg + foreign-TP rules (union-receiver TS2769 ×13), bare-string switch-case narrowing (semver ×3), guard-gated ternary return arms with the tri-state all-leaves-verified early return (×19); TS2322 184 → 158, TS2345 79 → 47, TS2769 30 → 9; round-436 dashboard baseline: compiler 373 / tsc-cli 375 / jsTyping 371 / deprecatedCompat 372 / typingsInstallerCore 380 / services 1,476 / server 1,769 / harness 2,062 at 27–41 s wall), then round 438 (2026-07-07) took FIVE more M3.1/M3.4 narrowing/relation slices (294 → 244, −17%: an empty-object `{}` union-member return-path early return; UNION-target extension of the assignment-RHS AND return-path type-guard narrowing gates (`currentSourceFile = node` / `return node` vs `SourceFile | undefined`); PROPERTY-ACCESS args in the call-arg guard-narrow-DOWN branch (`getExports(node.left)`); and nullish-strip-gated narrowing of object-literal property VALUES (`{ moduleSpecifiers: specs }` where `specs = append(specs, x)`); TS2322 158 → 116, TS2345 47 → 39), then round 439 (2026-07-07) took THREE predicate-overload / arg-narrow-DOWN slices (244 → 228, −16 strictly removals: findAncestor-style type-guard-callback overload return inference + a companion NonNull concrete-union strip, the overload arg-check non-union narrow-DOWN, and its `unknown`-via-typeof extension; TS2769 9 → 1).** Corpus suite green as a permanent regression gate: 10,628 tests / 0 failing / 3 skipped (generated tsgo-relevant corpus + local corner-case tests; count as of round 520) — **the full tsgo-relevant TypeScript compiler test suite passes.** _Round 443 (2026-07-08, Phase 17 — module-augmentation family + the module-file-local TYPE-alias leak (Blocker #3)): FOUR bounded fixes, all suppression-only. Compiler profile UNCHANGED (190) but the fixes GENERALIZE hugely: services 591 → 498 (−93), server 887 → 733 (−154), harness 1,118 → 989 (−129); suite 9,504 → 9,512 (+8 local across 3 test files, 0 regressions); 4 fix commits; services diffed via --listAll as strictly by-position removals. (1) TS2664 10 → 0: `resolveModuleSpecifierRelativeJsAware` — a `declare module "../compiler/types.js"` augmentation resolves `.js` → the `.ts` sibling (the base resolver deliberately avoids `.js` per the TS2459 gotcha, so the augmentation target never resolved); purely additive → only SUPPRESSES a false 'cannot be found'. (2) TS2564 17 → 0: a class property whose declared type INCLUDES `undefined` needs no definite assignment (tsc's strictPropertyInitialization `getFalsyFlags(type) & TypeFlags.Undefined`); `checkClassPropertyInit` missed the `| undefined` case, so `SourceFileObject`'s `nameTable: Map<...> | undefined` + siblings FP'd; reuses the existing `typeIncludesUndefined`. (3) TS2304 24 → 2: `buildNamespaceScope` gained a StringLiteralNode branch — a `declare module "X"` augmentation body sees the AUGMENTED module's exports by bare name (Node/NodeArray/SymbolFlags/TypeChecker/__String — compiler/types.ts exports NOT imported into services/types.ts); the 2 remaining are NodeJS `global` (env-legit, offline). INVESTIGATED & REVERTED (dashboard no-op): TS7006 array-element contextual typing — a correct M3.2 enabler (3 passing tests) but the real inferFromUsage.ts `Priority[]` case is B83.5-blocked (`interface Priority` nested in a function → unbound → `Priority[]` resolves to any); land it with nested-type resolution. (4) TS2339 44 → 0 (SourceFileLike, Blocker #3): a module-file-local `type X = A | B` alias leaks into globals and shadows the global `interface X` in OTHER files (TYPE-space analog of round 442's variable leak). importTracker.ts's non-exported `type SourceFileLike = SourceFile | AmbientModuleDeclaration` won the last-wins merge over compiler/types.ts's `interface SourceFileLike`, so a `sourceFile: SourceFileLike` param resolved to the bogus union (AmbientModuleDeclaration has no `.text`); pinned via an instrumented probe (whole-program-only — minimal repros don't reproduce). Built `conflatedTypeAliasFiles`; checkMemberAccessMissing bails a union-receiver TS2339 when the receiver displays as a conflated name in a NON-alias file (own-file still fires). NEXT (services @ 498, all deep): TS2322×235 / TS2339×85 (residual conflations — Type×20/Info×12) / TS2345×54 M3 relation, TS2416×11, TS2353×10, TS2740×9/TS2739×8._ 

_Round 520 (2026-07-14 — INV.4(b) batch 13: duplicate object-literal members + reserved-word identifiers migrated onto the check spine; 6 walker funs ~370 lines deleted, 2 init dispatches removed): `checkDuplicateObjectLiteralProperties` (TS1117/TS1118/TS2300) — [checkObjectLiteralDuplicates] retained as the per-literal core dispatched from the ObjectLiteralExpression enter; the destructuring-assignment-LHS skip (duplicate TARGETS in `({a: x, a: y} = obj)` are legal JS) became the came-from-child parent walk `spineObjLitInDestructuringLhs`: climb through pattern-position parents (object/array literals, a PropertyAssignment when the child is its INITIALIZER, spread positions) and skip iff a `=` BinaryExpression is reached with the climbed child as its LEFT — mirroring tsc's inDestructuringPattern/isAssignmentTarget; a ShorthandPropertyAssignment default VALUE terminates the climb, so `({q = {a,a}} = o)` is now checked (tsc-faithful widening, alongside ternary CONDITIONS, parameter defaults, and object-literal METHOD bodies — all under-visits of the old hand-walk). `checkReservedWordIdentifiers` (TS1359) — checkAwaitParams retained as the per-signature core, dispatched from every ASYNC function-like's enter (fn decl / method / arrow / fn-expr); the enum `void`/`await`/`yield` name rule became an EnumDeclaration-enter handler; widenings: class property-initializer arrows, new-expression var initializers, and arrows nested in a var-init arrow's expression body (the old statement walk checked a var-init arrow's params but never descended its body). Also deleted: the already-dead `reservedWords` val (never referenced — the Kotlin compiler doesn't warn on unused private members). VERIFIED: 23 pins written FIRST and pre-run against the OLD walkers (16 green + 7 widening fail pre-migration); suite 10,580 → 10,603 (0 regressions); listAll error lines IDENTICAL on ALL 8 profiles (520a vs 519c); bench 25,552 ms self (+0.2% = noise), 46 errors unchanged (all env-legit). BATCH 14 same session — checkStrictModeReservedWords (TS1212/TS1213/TS1214/TS2480/TS18006, the most stateful zero-typing walker yet): the threaded isStrict/isExpressionStrict/inClass/realStrict flags became ONE shared ancestor-chain context (`spineStrictReservedCtx` — collect the parent chain, walk it DOWN applying the old descent arms: Block/If/ForIn/ForOf/ModuleBlock/ModuleDeclaration transparent; a FunctionDeclaration entered ONLY under the strictness at ITS position, its "use strict" prologue upgrading realStrict for the subtree; a ClassDeclaration entered only through METHOD/CONSTRUCTOR members with inClass + both strictness flags forced; any other ancestor kind → null = the old no-visit) feeding ten per-statement-kind handlers; per-file flags computed in checkSpine's loop (binding strictness by effectiveTarget, EXPRESSION strictness by RAW target — the two deliberately differ; the explicitNonStrict suppression); the strictReserved* instance flags moved to the pre-init spine block, assigned per position. Reach deliberately NOT widened (corpus-tuned family): while/accessor/arrow bodies + class-expression members stay unvisited, pinned negative; the load-bearing quirk — fn bodies UNVISITED in non-strict files (no TS2480 for `let let` there) — reproduced and pinned. 3 walker funs (~250 lines) deleted, 1 init dispatch removed. Suite 10,603 → 10,628 (+25 Inv4SpineBatch14Test, ALL pre-verified against the OLD walker — a pure reach-preserving migration); listAll IDENTICAL on ALL 8 profiles (520b vs 520a). Session total: TWO batches, 9 walker funs (~620 lines) deleted, 3 init dispatches removed, suite +48. --passTiming RE-MEASURE closed the session: checker-init 20.0 s (21.6 s pre-batch-8), spine 718 ms @ ~34 passes, 459 passes recorded (~55 dispatches gone since INV.0's 514); the next-biggest non-giant passes are EXACTLY the INV.4(c) pair (checkUnresolvedNames 744 ms + checkTypeUsedAsValue 739 ms), the remaining zero-typing tail mostly sub-100 ms. NEXT: INV.4(c) — the name-resolution pair onto spine-maintained lexical state (the INV.2(c) lexicalScopes mass consumption); residual (b): checkAwaitContext (93 ms, stateful — low yield), checkConflictMarkers (text scan, may not need the spine)._

_Round 519 (2026-07-14 — INV.4(b) batches 10+11: TS2373 param-init forward refs + the break/continue jump-target family migrated onto the check spine; 6 walker funs ~376 lines deleted, 2 init dispatches removed): Batch 10 — checkParamInitForwardRef/walkForParamInitForwardRef deleted; checkForwardRefsInParams (+ findForwardParamRefs/findForwardParamRefsInBlock/collectHoistedVarNamesFromStmts) retained as the per-function core dispatched from spineCheckParamForwardRefs at every BODIED function-like's enter; arrows/fn-exprs/objlit-methods/class-EXPRESSION members are faithful widenings (position-independent per-signature tsc grammar); bodyless signatures keep the old no-check (TS2371 territory), GetAccessor params stay unchecked (TS1054 territory); the ES5 hoisted-body-var TS2454 companion unchanged. Batch 11 — the checkJumpTargets family (TS1104/TS1105/TS1107/TS1115/TS1116 + TS1344): the threaded inIteration/inSwitch/labelNames/crossedFunctionBoundary flags became ONE parent-chain walk (spineCheckJumpTarget) mirroring tsc checkGrammarBreakOrContinueStatement — first function-like ancestor → TS1107 (class static blocks now count, a faithful widening); a matching LabeledStatement resolves the jump with tsc's isIterationStatement(lookInLabeledStatements=true) nested-label unwrap for labeled continue — a faithfulness FIX (`L1: L2: for(;;){continue L1}` no longer false-fires TS1115); iteration ancestor legalizes unlabeled jumps, SwitchStatement legalizes unlabeled break, ModuleBlock ancestor suppresses unlabeled break (the old inSwitch=true namespace rule); TS1344 as a LabeledStatement-enter handler (widened to arrow-in-condition positions). VERIFIED: 32 pins written FIRST and pre-run against the OLD walkers (batch 10: 10 green + 4 widening fail pre-migration; batch 11: 14 green + 3 widening + 1 faithfulness-fix fail pre-migration); suite 10,538 → 10,552 → 10,570 (0 regressions); listAll error lines IDENTICAL on ALL 8 profiles after EACH batch (519a vs 518b, 519b vs 519a); warning-clean; bench 25,491 ms self, 46 errors unchanged (single-run −7.9% = the box-drift band). Batch 12 same session: checkObjectLiteralModifiers (TS1042/TS1184) — the near-full-tree explicit-stack expression walk became a pure ObjectLiteralExpression-enter handler (spineCheckObjLitModifiers; OBJLIT_ACCESS_MODIFIERS companion-hosted per the init-order gotcha — the spine runs during init, an instance field declared after init would read null); nested literals get their own enters; parameter-default + spread-operand positions are faithful widenings. 3 walker funs (~206 lines) deleted, 1 init dispatch removed. Suite 10,570 → 10,580 (+10 Inv4SpineBatch12Test — 8 pre-verified against the OLD walker, 2 widening pins fail pre-migration as expected); listAll error lines IDENTICAL on ALL 8 profiles (519c vs 519b). NEXT: INV.4(b) batch 13 — remaining zero-typing tail (checkDuplicateObjectLiteralProperties — its destructuring-assignment-LHS skip needs a came-from-child parent walk / checkReservedWordIdentifiers / checkStrictModeReservedWords / checkAwaitContext)._

_Round 518 (2026-07-14 — INV.4(b) batch 8: the parameter-initializer family migrated onto the check spine; 24 walker functions ~902 lines deleted, 6 init dispatches removed): SIX passes as three Parameter-enter handlers + one SetAccessor-enter handler. `checkOptionalParamWithInitializer` (TS1015) — parent-kind dispatch preserves the corpus-tuned requireType gate (declarations need a type annotation or param-property modifier; arrow/fn-expr params fire bare; tsc fires unconditionally — lifting is signal-driven) and the old exclusions (interface/type-literal signatures, fn TYPES, objlit/class-expr getters); widened faithfully to class property initializers / parameter defaults / dotted-namespace bodies. `checkOptionalBindingPatternParams` (TS2463) — per-parent owner-has-body gate; overload/ambient signatures exempt. `checkParamInitializerForbidden` (TS2523/TS2524/TS2372/TS2502/TS18048) — walkParamInitForbidden + binding-name walk + collectParamSelfRefs retained as the per-parameter core; the per-file code@pos dedup set became `spineParamForbiddenEmitted`; the walkParamForbiddenExprForFns nested-fn descent dissolves into per-Parameter enters; findParamSelfRef deleted as already-dead. `checkParameterInitializerInNonImpl` (TS2371) — verified against tsc checker.ts:45130 and widened to EVERY FunctionType/ConstructorType position (old reach: var annotations/aliases/casts only — a fn-type in a parameter annotation was silently unchecked); accessors stay excluded. `checkSetAccessorInitializer`/`checkSetAccessorRestParameter` (TS1052/TS1053) — parent gate widened from class declarations to class expressions + object literals per tsc checkGrammarAccessor; interface/type-literal setters excluded. Session opened with the queued --passTiming re-measure: checker-init 21.6 s; spine 529 ms carrying 24 passes; top-3 unchanged (3.73/2.43/2.27 s); this batch's six passes summed ~292 ms of old pass time. VERIFIED: 29 pins written FIRST against the OLD walkers (23 green, 6 widening pins fail pre-migration as expected); suite 10,491 → 10,520 (+29 Inv4SpineBatch8Test, 0 regressions); listAll error lines IDENTICAL on ALL 8 profiles (518a vs 517b); warning-clean; bench 26,357 ms self, 46 errors unchanged. BATCH 9 same session (16 walker funs ~606 lines deleted, 4 init slots removed): checkForInLhsTypeAnnotation (TS2404 — ForInStatement-enter, widened to arrow/fn-expr bodies) + checkEmptyTypeArguments (TS1099 on calls/new — Call/New-enter; the type-position TS1099 emitter untouched; reportEmptyTypeArgs deleted as orphaned) + checkSetterReturns (TS2408 — SetAccessor-enter, checkSetterBodyReturns retained; widened to await operands) + checkWithStatements (TS1101/TS1300/TS2410 — the threaded isInWith/isInAsync pair became ONE parent-chain walk: WithStatement ancestor before any fn boundary suppresses TS1300/TS2410; nearest fn boundary's Async modifier decides TS1300, arrows still reset async=false — old behavior, pinned negative as a signal-driven candidate; TS1101 gated spineWithStrictActive; TS2410 paren-scan preserved). VERIFIED: 18 pins pre-run against OLD walkers (4 widening pins fail pre-migration as expected); suite 10,520 → 10,538 (+18, 0 regressions); listAll IDENTICAL on ALL 8 profiles (518b vs 518a). Session total: TEN passes, 40 walker funs (~1,508 lines) deleted, 10 init dispatches removed, suite +47. NEXT: INV.4(b) batch 10 — checkParamInitForwardRef (TS2373); remaining zero-typing tail (checkJumpTargets / checkReservedWordIdentifiers / checkStrictModeReservedWords / checkDuplicateObjectLiteralProperties / checkObjectLiteralModifiers / checkAwaitContext)._

_Round 517 (2026-07-14 — INV.4(b) batch 6: duplicate-modifier grammar + ambient initializers + switch/case comparability migrated onto the check spine; 9 walker functions ~453 lines deleted, 3 init slots removed): `checkDuplicateModifiers` (TS1030/TS1029/TS1044) — statement-kind handlers over the exact 10 node kinds the old walk checked; its TWO threaded flags with DIFFERENT reset rules (inAmbientContext reset by fn/member bodies + set by declare-module descent; atTopLevel false in Blocks, reset TRUE in ModuleBlocks, preserved through if/else) became ONE parent-chain walk (`spineDupModContext`) where the innermost flag-deciding ancestor wins per flag and any non-descended ancestor kind (loops/switch/try/labeled/arrow/fn-expr/class-expr) returns null = the old no-visit; reach deliberately NOT widened (checkModifiers is an FP-firewalled TEXT heuristic — B69.6/B459b/B61.5g preserved, negative pins hold the line). `checkAmbientInitializers` (TS1039/TS1254/TS1066/TS1031) — Enum/VariableStatement/ClassDeclaration enter handlers over `spineAmbientInitContext` (full old container set passes through; declare-module sets ambient, fn bodies reset, SourceFile terminal returns spineIsDts = the .d.ts-top-level-ambient rule — this pass has NO dts skip); class-member/arrow bodies stay unreached (pinned negative — a signal-driven widening candidate); B162 same-enum sibling scan via `spineSiblingStatements`; the class-EXPRESSION member path (TS1031 regardless of ambient) retained via unchanged visitExprForClassExpression. `checkSwitchCaseComparable` (TS2678) — the per-statement-LIST const/annotated binding maps (fresh per nested block, order-accumulated, annotated-wins) reproduced as a preceding-sibling scan AT the switch node (`spineSwitchSubjectBinding` breaks at the switch, declared-after bindings invisible — pinned); the old broad expression descent subsumed by the spine. VERIFIED: 27 pins written FIRST and run green against the OLD walkers, then the migration; suite 10,443 → 10,470 (+27 Inv4SpineBatch6Test, 0 regressions); listAll error lines IDENTICAL on ALL 8 profiles (517a vs 516b); bench in band (27,212 ms self, +1.3% = noise; 46 errors unchanged). BATCH 7 same session (15 walker funs ~551 lines deleted, 4 init slots removed): checkRestElementPropertyNames (TS2566 — pure-syntax ObjectBindingPattern-enter handler, nested-pattern recursion dissolves into per-node enters, widened faithfully to catch-clause patterns) + checkRestBindingPatternElements (TS1186/TS2493/TS2322 — checkRestBindingParam retained as the Parameter-dispatch core; widened to objlit-method/class-expr params) + checkAmbientImplementation (TS1183 — the most intricate reach walk yet, spineAmbientImplContext: ambient fn/class-DECL member bodies were never descended (own-declare → null + the passedDeclBody declare-module-above rule), while arrow/fn-expr/class-EXPR-member/objlit-method bodies RESET ambient unconditionally (passedDeclBody CLEARED — the old expr walk descended them with false even under ambient); statement containers position-checked, expressions pass generically; the interface arm is de-facto dormant — the parse drops interface method bodies, the TS1246 situation) + checkAmbientRelativeModuleNames (TS2436 — SourceFile-parent + !spineFileIsModule gate). VERIFIED: 21 pins pre-run against OLD walkers (2 widening pins fail pre-migration as expected); suite 10,470 → 10,491 (+21, 0 regressions); listAll IDENTICAL on ALL 8 profiles (517b vs 517a); bench NEUTRAL by interleaved 3-pair A/B (mean −285 ms) after a box-drift episode (single-run +11.7%/+16.1% on the SAME binary at 864 MB free; daemons stopped → both ~26 s — the round-493 rule vindicated; the two inflated TSV rows are artifacts). Session total: SEVEN passes, 24 walker funs (~1,004 lines) deleted, 7 init slots, suite +48. NEXT: INV.4(b) batch 8 — re-measure --passTiming; checkMixinClassConstructor stays (d)._

_Round 516 (2026-07-14 — INV.4(b) batch 4: the accessor-grammar family + TS1014/TS1113/TS1246 migrated onto the check spine; 17 walker functions ~733 lines deleted, 4 init slots removed): `checkSetterParameterCount` (the largest hand-walk family so far — 6 functions, FOUR codes) split into three handlers: `spineCheckGetAccessorGrammar` (TS1054 — widened faithfully to class EXPRESSIONS + interface/type-literal getters), `spineCheckSetAccessorGrammar` (TS1049 across all old contexts + interface/type-literal setters; TS1095 widened from class declarations to exactly class EXPRESSIONS — the objlit accessor parse never PARSES a setter return annotation and the interface parse drops it, a first-cut objlit pin caught this), and `spineCheckAccessorPairVisibility` (TS2808 — deliberately KEPT at the ClassDeclaration gate, pinned negative for class expressions). `checkRestParameterLast` (TS1014) became a second Parameter-enter handler beside batch 2's TS2370 sibling — widened to FunctionType/ConstructorType/type-literal methods (tsc checkGrammarParameterList runs per signature declaration); GetAccessor parents stay excluded (old behavior). `checkMultipleDefaults` (TS1113) — trivial SwitchStatement-enter handler, one-per-switch latch preserved, widened to parameter-default initializers. `checkInterfacePropertyInitializers` (TS1246) — InterfaceDeclaration-enter handler; the PARSER owns the common `= init` shape (consumes without storing), so the handler covers only initializer-carrying members (faithful migration, kept cheap). VERIFIED: suite 10,405 → 10,427 (+22 Inv4SpineBatch4Test, 0 regressions); listAll error lines IDENTICAL on ALL 8 profiles vs the 515b baselines; bench in band (26,738 ms self, +1.1% = noise). BATCH 5 same session: checkConstWithoutInitializer (TS1155) + checkDestructuringWithoutInitializer (TS1182/TS7031) as VariableDeclaration-enter handlers — shared owner gate (VariableStatement non-declare/non-ambient via spineInDeclareModuleChain, the parent-walk equivalent of the old isAmbient threading which reset at every non-module descent; or a for(;;) initializer; for-in/for-of iteration vars excluded), emitTs1182IfMissingInit retained, for-of/for-in BODIES a faithful widening (the old walks had no ForOf/ForIn case) — plus checkComputedPropertyNameLiteral (TS1166/TS1169 by PropertyDeclaration parent kind; TypeLiteral stays unchecked) + spineCheckClassExprComputedProps (the TS1206 legacy-decorator short-circuit, position-GATED to the old expression-statement-only reach, pinned negative per the round-42 TS1166 direction-of-emission history). 7 more walker funs (~318 lines) deleted, 3 init slots removed. VERIFIED: suite 10,427 → 10,443 (+16 Inv4SpineBatch5Test, 0 regressions); listAll IDENTICAL on ALL 8 profiles; bench in band (+0.5%). Session total: SEVEN passes, 24 walker funs (~1,050 lines) deleted, 7 init slots removed, suite +38. NEXT: INV.4(b) batch 6 — checkDuplicateModifiers / checkAmbientInitializers / checkSwitchCaseComparable; checkMixinClassConstructor stays (d)._

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `docs/history/STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `docs/history/STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
