# Status

**Phase 4 — Checker buildout: COMPLETE. Phase 17 — self-compile the TypeScript compiler itself (re-scoped 2026-07-03; "any TypeScript project" is post-v1) — is the LIVE mission (see PLAN-PHASE-5.md; v1 exit = all 8 tsc-source profiles at zero FPs; dashboard: bench/*.tsv, self-compile diagnostics 13,245 → 1,577 (measured at round 427 — rounds 395–412 burned bounded non-M3 histogram-tail buckets + M3.4 flow-narrowing slices the round-394 "pool picked over" triage missed, all surfaced by bucketing the full `--listAll` output by normalized message shape: TS2499 multi-base-generic misparse, TS2440 type-only-barrel-merge, TS2344 constraint-chain + cross-file `typeParamInternCache` pos-collision, TS2693/TS2709 clodule + destructuring-shadow, TS2314 enum-member-vs-generic, TS2588 nested-shadow, TS2551 Object.setPrototypeOf, SetIterator/MapIterator lib gap, TS2774 local-shadows-function, TS1100 interface-property-name, TS7023 callback-arg self-ref, TS2365 arithmetic local-shadows-function, TS2362 branded-number, TS7053 enum-reverse-mapping, round 408's TS2349 callee flow-narrowing family, and round 409's `export *`-barrel / ESM-`.js` imported-guard flow narrowing (M3.4, TS2339 838 → 672), and round 410's TS2862 constraint-index-sig gate / assign-RHS type-guard narrowing / void-overload-return (2,443 → 2,433), and round 411's M3.4 enum-member discriminant narrowing (barrel-import-resolved, three families UpToDateStatus/TypeMapper/PrivateIdentifierInfo) + type-guard-narrows-a-member-DOWN (2,433 → 2,374, TS2339 672 → 614) + round 412's M3.4 single-type type-guard narrow-DOWN + TS18048 receiver-narrowing (2,374 → 2,373), and round 413's `export *` LEAF-EXPORT gate (barrel-imported `Debug.assert` & every barrel guard now resolves — the TRUE builder.ts blocker, NOT the round-412 depth red herring) + tsc-faithful linear flow-walk iteration + return-path narrowing (2,373 → 1,965, TS2339 614 → 237)), then rounds 414-417 burned more bounded/M3.4 buckets (TS2366 CFA fall-through, NonNull/&&-narrowing arithmetic, closure-loop narrowing, namespace-local `extends` bases: 1,965 → 1,902), and round 418 resolved NESTED type-guard functions (M1.12, 1,902 → 1,854, TS2339 237 → 189: tsc guards `isTupleType`/... are nested in `createTypeChecker` so the binder skips them and `resolveFlowCalleeDecl` missed them - the guard never narrowed, the single biggest TS2339 sub-family), and round 419 resolved properties on INTERSECTION union-members (M1.12, 1,854 → 1,808, TS2339 189 → 143: `getPropertyOfType`/`typeHasOwnProperty` bail on a `Type.Intersection` member so `PropertyAccessExpression | (ElementAccessExpression & …)` FP'd TS2339 on `.parent`; fold the constituents in property resolution + discriminant-narrowing annotation; 0 new FPs, self-compile time −17%), and round 420 resolved TYPE-ALIAS enum-member discriminants in narrowing (M1.12, 1,808 → 1,799, TS2339 143 → 134: a `.kind: <alias>` discriminant member survived a `switch (x.kind) { case }` because `enumMemberKeysOfTypeNode` handled only a direct `Enum.Member`, not a bare-Identifier alias; 0 new FPs), and round 422 burned FIVE bounded M3.4/overload families (1,799 → 1,756: TS2769 60 → 45 — overload arg-check helpers now flow-narrow reference args (mirroring B469) + a synthetic true|false union for guard-narrowed boolean args vs literal overload params; TS2339 134 → 117 — mixed enum + string-literal discriminant unions narrow on disjoint lit:s: keys (PrivateIdentifierInfo); TS18048 10 → 7 — an optional-chain discriminant access x?.kind === <non-nullish> proves the receiver non-nullish; TS2366 12 → 4 — union-.kind exhaustive switches terminate via requiredUnionDiscriminantKeys), and round 423 (a) drove TS2366 to ZERO (1,756 → 1,752: the switch RECEIVER is guard-narrowed + typed before the exhaustive-switch union walk — flow graph via the pass-dedicated implicitReturnFlowGraph, local-const-from-call typing, optional-discriminant @undefined keys, LiteralToken["kind"] indexed-access) and (b) fixed union-TARGET type guards end-to-end (1,752 → 1,708: `x is A | B` PARSED truncated so isCallOrNewExpression/isPropertyNameLiteral/isOptionalChain never narrowed; + aliased conditions `const isFrag = guard(x); if (!isFrag)` via a memoized flow back-walk; + kind-key disjointness beating the lenient relation; TS2339 117 → 104, TS2322 784 → 756) and (c) narrowed truthy optional-chain CALL conditions (1,708 → 1,707: `if (x.y?.size())` proves `x.y` non-nullish), and round 424 burned SEVEN more M3.4 flow-narrowing slices (1,707 → 1,662, TS2339 104 → 68, TS18048 5 → 1, TS2322 756 → 751: loop-entry union suppression with a STRUCTURAL wash gate, call-RHS return-annotation narrowing, closure/join/call-crossing aliased conditions, prefix-path receiver guards, asserts-with-inferred-TP via test-arg predicate inference, assignment-overwrite reset to the declaration/call-RHS resolved type, and the DebugTypeMapper this-narrowing slice (explicit-type-arg asserts re-type `this`; an overload-cluster name collision resolves to the unique TypePredicate-bearing decl) — every step by-site strictly removals), and round 425 found the TS2339 never-cluster ROOT CAUSES (1,662 → 1,600, TS2339 68 → 6: the round-411 enum-discriminant key space SPLIT across resolution paths — canonicalEnumSymbol at all four key-builder sites — plus per-candidate narrow-DOWN distribution in union-target guards/instanceof, aliased ===/switch discriminants, switch-default negative narrowing, typeof-object filtering, boolean-literal discriminant truthiness, destructuring-read narrowing, the loop-entry retry for the single-type suppression, and the positive-empty intersection fallback whose round-423 dead-end verdict was an ARTIFACT of the key split) — driving to 0, and round 426 replaced the B399 TS2563 per-file proxy with the faithful flow-walk depth trip + the asserts-callee call gate (1,600 → 1,593: TS2563 27 → 0; TS2454×20 pre-existing definite-assignment walker FPs unmasked), and round 427 burned that bucket (1,593 → 1,577: TS2454 20 → 4 via tsc's definiteness rules — logical assigns ??=/||=/&&= are DEFINITE so captured reads assume initialized, a !-asserted read assumes initialized, and the anywhere-scan's left-spine walk now applies the target rule per spine node); self-compile time ~110–150 s single-run band).** Corpus suite green as a permanent regression gate: 9,291 tests / 0 failing / 3 skipped (generated tsgo-relevant corpus + local corner-case tests) — **the full tsgo-relevant TypeScript compiler test suite passes.** _Round 427 (2026-07-06, Phase 17 — M3.4: the TS2454 bucket round 426 unmasked — three tsc-faithful assumeInitialized/definiteness rules. Self-compile (compiler profile) 1,593 → 1,577 (−16: TS2454 20 → 4, all else byte-identical); suite 9,282 → 9,291 (+9 local, 0 regressions); 1 fix commit. (1) Logical assignments ??=/||=/&&= classify AssignmentKind.Definite (tsc getAssignmentTargetKind), so isNeverInitialized is false and a CAPTURED (cross-closure) read of an outer let assumes initialized when any definite assignment exists anywhere, nested closures included — our B78.2 anywhere-scan recognized only plain `=`, so tsc's own (sourceStack ??= []).push(source) closures FP'd ×13; compound |=/+=/++ stay NON-definite (unusedLocalsInMethod4's enabledSubstitutions |= … baseline pins the negative). (2) A read whose DIRECT parent is a non-null assertion assumes initialized (the literal node.parent.kind === NonNullExpression disjunct; tsc core.ts `return lastResult!`) — applied in BOTH read walkers; x! and x!.prop exempt, (obj.foo)! still walks obj. (3) The anywhere-scan's iterative left-spine walk applied the assignment-target rule only to the OUTERMOST BinaryExpression — a COMMA-nested assignment ((!memberName ? (memberName = X, true) : …), checker.ts getSignaturesOfType) needed BOTH the per-spine-node target rule AND a captured-read exemption in the flow-walker's expression-bodied-arrow branch (a nested-if condition is walked flagged; names definitely assigned inside the arrow body are masked out) — both caught by the bench by-site diff. Residual TS2454×4 triaged: while(true)-body assignment evidence (scanner.ts ×2, isAssignedAtFlow's loop-entry-only bound), same-container flow precision (checker.ts:14106), for-of shadow of an outer let (generators.ts:1681, block-unaware name tracking). Ts2454AssumeInitializedTest ×9 with negative controls._ _Round 426 (2026-07-06, Phase 17 — M3.4 (absorbs M1.2): faithful TS2563 — flow-walk DEPTH-TRIP semantics with per-container disable, replacing the B399 per-file node-count proxy. Self-compile (compiler profile) 1,600 → 1,594 → 1,593 (−7: TS2563 27 → 1 → 0, TS2454 0 → 20 — pre-existing definite-assignment walker FPs the proxy's blanket per-file filter had masked, now honestly visible; all else byte-identical by-code); suite 9,276 → 9,282 (+6 local, 0 regressions); 2 fix commits. tsc fires TS2563 ONLY when a flow walk recurses 2000 deep (getTypeAtFlowNode flowDepth === 2000 → per-container flowAnalysisDisabled + one report at the containing function-or-module block + errorType thereafter — TS2563 OR TS2454, never both, per CONTAINER): all three flow walkers set flowDepthTripped at the trip (isAssignedAtFlow rewritten iterative — linear pass-through antecedents consume no depth, only branch-join/condition/loop-entry recursion does); every depth-0 entry (11 sites) routes through flowWalkWithTripCheck (pre-checks flowDisabledRanges → conservative default without walking; one-shot report via the flow graph's new containerStarts — innermost containing function-like body block, else the file); the end-of-init TS2454 filter is per-container-range (cfaTooLargeFiles deleted); the dedicated evolvingArrayWalkTrips init walk pins largeControlFlowGraph (whose generated corpus test is JS-emit-only and asserts no diagnostic). The 27th TS2563 was OURS (fix 426b): flowCallMightNarrow lacked tsc's getEffectsSignature check, so diagnosticInformationMap.generated.ts's ~2,100 non-assert diag(…, DiagnosticCategory.Error, …) statements each consumed a depth level on any DiagnosticCategory-reference walk → false trip; flowCalleeMayHaveAssertEffects is EXACT for Identifier callees (map-lookup resolution, provably matches narrowByAssertCall's own bail) and conservative-true for PropertyAccess callees (resolving those types the RECEIVER — the round-385 services-hang hazard). TS2454×20 triage (next bounded bucket): cross-closure reads of an outer let ×~16 ((sourceStack ??= []).push in a nested fn — tsc's used-before-assigned check is same-container-only; captured reads assume initialized), a `return lastResult!` non-null-asserted read (tsc never reports TS2454 through `!`), and while(true)-body assignment evidence invisible to isAssignedAtFlow's loop-entry-only bound (scanner.ts resultingToken ×2). CfaTooLargeBailTest 2 → 8 tests. (Landed across two sessions — the implementing session was OOM-killed mid-verification with the work complete-but-uncommitted; this session verified, measured, landed it, and fixed the residual.)_ _Round 425 (2026-07-06, Phase 17 — M3.4/M1.12: the TS2339 never-cluster root causes + eight narrowing slices. Self-compile (compiler profile) 1,662 → 1,634 → 1,628 → 1,608 → 1,607 → 1,603 → 1,600 (−62; TS2339 68 → 6, never×21 → 2); every step's by-site diff STRICTLY removals; suite 9,251 → 9,276 (+25 local, 0 regressions); 7 fix commits. (1) THE BIG ONE: the round-411 enum-discriminant key space ("symId#member") SPLIT — the same enum reaches the key builders as DIFFERENT Symbol instances (the program-global merged instance vs the declaring file's local instance via the barrel resolver), so ALL SyntaxKind keys looked pairwise disjoint and typeGuardMemberDisjoint dropped every guard-narrowed member → the never×21 cluster; canonicalEnumSymbol (identity-shared EnumDeclaration + enumValues gates, memoized) at all four key-builder sites; also cleared the round-423 "dead-end" Identifier | ComputedPropertyName ×8 family — the disjointness verdicts, not the relation, were the blocker; diagnosed only by stderr-instrumenting the REAL corpus after two repro-enrichment rounds found nothing. (2) Union-target guards + instanceof distribute the narrow-DOWN direction PER CANDIDATE (tsc getNarrowedType mapType(candidate,…)); the whole-union test never holds. (3) Aliased SWITCH + === discriminants (const kind1 = m1.kind; switch (kind1) / const optType = opt.type; if (optType === "listOrElement")) via the round-423 back-walk (compareTypeMappers ×6, convertJsonOption). (4) Switch-DEFAULT negative narrowing (executeCommandLine option.type.forEach / deprecatedKeys). (5) typeof-object three-way filtering (callables and ENUM members are NOT "object"), boolean-LITERAL discriminant truthiness (isStatic ? … : …), destructuring reads consult flow narrowing, discriminant filters for union-of-literals + object-typed props — LANDMINE: both gate on a definite VALUE literal (optionality is a symbol attribute; x.body === undefined proves nothing — the ungated cut cost +3 nevers). (6) Loop-entry retry for the round-418 single-type suppression (pre-loop guard, read inside the loop). (7) The positive-empty INTERSECTION fallback landed strictly-removals — REVERSING the round-423 dead-end verdict, which was an artifact of the key split; re-measure dead-ends when an upstream root cause falls. Residual TS2339×6 triaged (try/finally flow antecedent ×2, B425 enum-literal-union ×1, M3 ×3); next: the M3 cores (TS2322×751, TS2345×394, TS7006×301) and TS2769×45 (M3.1)._ _Round 424 (2026-07-06, Phase 17 — M3.4/M1.12: seven flow-narrowing burn-down fixes from the round-423 residual triage. Self-compile (compiler profile) 1,707 → 1,691 → 1,687 → 1,683 → 1,680 → 1,672 → 1,662 (−45; TS2339 104 → 68, TS18048 5 → 1, TS2322 756 → 751); every step's by-site diff STRICTLY removals; suite 9,223 → 9,251 (+28 local, 0 regressions, 2 DELIBERATE pin flips — both toward tsc semantics, one pre-planned by its own note); 7 fix commits. (1) Union-receiver TS2339 suppression survives loop boundaries (−16, tsc parseResponseFile): the union branch of checkMemberAccessMissing retries a WASHED plain walk with the loop-entry-following variant, suppression-only — the wash gate must be STRUCTURAL (member-id sets), since any &&/|| on the path mints a fresh Type.Union at the branch join (getUnionType does not intern) and identity === misses the wash. (2) narrowByAssignmentRhs accepts a CALL RHS whose callee return annotation classifies non-nullish (syntactic typeNodeDefinitelyNonNullish); no compiler delta — the motivating checker.ts:21170 instantiateType is an overload cluster → ambiguous → M3 (overload selection). (3) The aliased-condition back-walk follows closure FlowStarts (B464 gates on BOTH alias + walked root), branch JOINS (all reachable antecedents must agree), and calls (value-preserving), with a per-call memo (||-chain diamond fan-out) — −4: builder.ts canCopyEmitSignatures + 2 bonus JsxCallLike TS2339; the invisible blocker was FlowAssignment.node being the whole BinaryExpression for assignment EXPRESSIONS (flowAssignmentRootName reads its LHS). (4) Prefix-path receiver guards (−4, moduleNameResolver getAutomaticTypeDirectiveNames): a guard 'options is CompilerOptions & {types: string[]}' narrows the walked path options.types — drop-nullish only, tail must be REQUIRED (optionality is a symbol attribute, not in the property type; intersection = required iff ANY constituent requires). (5) asserts node is U with U an inferred callee TP (−3, Debug.assertNode): resolveNamespaceMemberFnDecl prefers a TypePredicate-bearing decl (the overload cluster's valueDeclaration is the annotation-less impl), U resolves from the TEST argument's own predicate target (drop-nullish alone TRADES the TS18048 for a TS2339 on surviving union members), constraint-chain fallback. (6) An overwrite of the walked reference with an unclassifiable initializer/RHS resets to the PRECISE overwritten type — a shadowing `const resolved = g()` after an outer falsy guard collapsed to never (moduleNameResolver 1924/1931/1950 ×6 + 2 bonus TS2322); a DECLARATION resets to its own annotation/initializer-call return, a plain `=` to its call-RHS return; NEVER to the reader's flat-map declared type (block-unaware/first-decl-wins — a blanket declaredType reset was MEASURED to inject the outer shadowed binding's type: 3 new FPs, reverted to the precise form). (7) The DebugTypeMapper slice ×10 (debug.ts 832-850): `type<TypeMapper>(this)` — an `asserts value is <TP>` binds the TP from EXPLICIT call type args, an assertion on an any/unknown reference RE-TYPES it (the relation gate trivially passes and would keep the useless any), checkMemberAccessMissing consults flow narrowing for `this` receivers (getTypeOfExpression(this) is deliberately anyType per B101, so the round-418 suppression never applied), the exhaustive-switch receiver typing recovers an anyType receiver through the assert re-type, and buildNestedFunctionMap resolves a name collision to the UNIQUE TypePredicate-bearing decl (Debug's `type` is an overload pair — the plain ≥2→ambiguous rule made the guard invisible). Residual TS18048×1 (checker.ts:21170, M3 overload selection); next: TS2339 never×21 remaining (per-site M3-relation diagnosis), DebugTypeMapper×10 (needs explicit-type-arg asserts + this-path narrowing in the TS2339 this-branch + this.kind switch narrowing — partially unblocked by this round's machinery)._ _Round 423 (2026-07-06, Phase 17 — M3.4: exhaustive-switch receiver narrowing (TS2366 → 0) + union-target type guards + aliased conditions. Self-compile (compiler profile) 1,756 → 1,752 → 1,708 → 1,707 (−49); suite 9,202 → 9,223 (+21 local, 0 regressions); 3 fix commits. Fix 1 (50297e6a): the four round-422 residual TS2366 sites, per the next-agent note — the discriminant receiver is guard-narrowed via the pass-dedicated implicitReturnFlowGraph (lifted into currentFlowGraph only around the walk), a body-local `const target = getAssignmentTarget(node)` types from the callee return annotation, an OPTIONAL enum discriminant contributes a required @undefined key (case undefined completes the cover), and `LiteralToken["kind"]` resolves via a new IndexedAccessType branch. Fix 2: `x is A | B` was PARSE-TRUNCATED (the union-continuation wrapped the PREDICATE — `(x is A) | B` — so the return annotation was never a TypePredicate and every union-target guard silently never narrowed); plus ALIASED CONDITIONS (tsc inlineLevel: `const isFrag = isJsxOpeningFragment(node); if (!isFrag)`) via a MEMOIZED value-preserving flow back-walk (the uncached cut ran 4×+ slower — killed), and kind-key DISJOINTNESS beats the lenient relation in the predicate union filters (enum-member kinds resolve to any, so `!isJsxOpeningFragment` collapsed JsxCallLike to never). By-site −68/+24; the +24 are the same M3-relation-gap family newly EXPOSED (union-target guards now narrow at all), catalogued in the session note. Measured dead-ends (2 A/Bs, reverted): key-SUBSET verdict (→1,720; brand-intersection targets like CallChain share kinds without matching), plain-gated subset + tsc-faithful positive-empty fallback (→1,710), same-SYMBOL union membership (exact no-op). Fix 3: a truthy optional-chain CALL condition (`if (state.referencedMap?.size())`, builder.ts) excludes nullish from the `?.`-guarded receiver — positive branch only. 21 local tests across 2 files._ _[Rounds 313–419 and older trimmed to docs/history/STATUS-HISTORY.md.]_
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `docs/history/STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `docs/history/STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
