# Status

**Phase 4 — Checker buildout.** 9,919 / 10,086 tests passing (~98.3%, 167 failing). _Round 310 (2026-06-24, max-compute autonomous campaign): **+3 (170→167 failing), `moduleResolutionWithRequire` + `importInsideModule` + `classPropertyInferenceFromBroaderTypeConst`, ZERO regressions (exact failing-set diff).** Three emit-side fixes from parallel read-only multi-agent hunts over the smallest-DIFF candidates, batched + verified together. **(1) require-only-orphan drop** (`requireOnlyOrphans`, TypeScriptCompiler.kt) — a `.ts` @Filename input reached ONLY by a bare `require('./x')` whose calling file has a USER-declared `require` (`declare const require`, so tsc doesn't resolve it as a module ref) is NOT a program file → dropped from emit; sibling `…AndImport` keeps emitting via its `typeof import('./other')` STATIC ref (the firewall). **(2) namespace-internal-import orphan** (same block) — `import X = require("spec")` whose IMMEDIATE enclosing decl is an `Identifier`-named `namespace` (NOT a string-literal ambient `declare module "X"` — tsc's `isAmbientModule` distinction; `collectModuleReferences` only descends into ambient modules) → target dropped (`importInsideModule`; never drops the LAST @Filename = the harness sole-root, gated on `!in staticallyReferenced`). **(3) default-target = ES2024** (CompilerOptions.kt `effectiveTarget`) — an UNSET `@target` maps to ES2024 (tsc `getEmitScriptTarget`: unset → LatestStandard/ES2025), NOT ES2015, so a no-`@target` test keeps native class fields/async/spread. CONFINED to emit (the checker reads raw `options.target`=ES3 for lib-availability), and the corpus is internally consistent (all baselines generated by a high-unset-default tsc — verified only `classPropertyInference` has a no-`@target` class-field baseline). The historically-deferred-as-risky default-target change landed clean: agent read tsc's `getEmitScriptTarget` source + corpus-scanned (0 no-`@target` downlevel baselines). New CLAUDE.md gotchas. **METHOD: the read-only multi-agent hunt over smallest-DIFF candidates yielded 4 emit-side wins across rounds 309-310 — the agents read the ACTUAL tsc source (harness `compilerRunner`, `getEmitScriptTarget`, `collectModuleReferences`) to nail rules, and every plan was empirically verified (the file-order gate contradiction + corpus-consistency were caught by isolation/full-suite, not the agent trace). NEXT: the remaining smallest-diff candidates are parser-recovery (hot-path, FP-risky) + deep-engine NONE; continue ENGINE blocker substeps or hunt the parser-recovery slice.** Memory: HEAVY abe-linter contention all session (290MB-1.1GB avail floor); killed our 2.3GB idle KotlinCompileDaemon (round-293 trick) before each test fork._ _Round 309 (2026-06-24, max-compute autonomous campaign): **+2 (172→170 failing), `emitMemberAccessExpression` + `visibilityOfCrossModuleTypeUsage`, ZERO regressions (exact failing-set diff).** Both are JS-emit FILE-ORDER bugs on a CYCLIC reference graph (every file referenced → the existing single-root DFS prepass in `topologicalSort` was suppressed, falling back to wrong @Filename order). A read-only multi-agent hunt nailed the exact tsc algorithm: the compiler-test harness makes the LAST @Filename unit the SOLE program root when its content has `///<reference>` or `require(`, then post-order DFS over its reference edges (register-path → recurse refs in source order → push last; re-entry to a registered path = cycle guard). Fix: thread the ref-path-inclusive `importDeps` as a new `fullDeps` arg into `topologicalSort` + a zero-root branch that seeds `visitFull(fileNames.last())` when the FULL graph is a genuine cycle (`fullRoots` empty). **The load-bearing subtlety the agent's own code got wrong (1 iterate cycle): the cycle must be detected on `fullDeps`, NOT the stripped `deps` — for a pure-`///<reference>` cycle the `hasCycle→importDepsNoRefPath` fallback strips the ref-path edges so the stripped `roots` is ALL files (never empty); computing `fullRoots` from `fullDeps` fixes it.** All 3 protected full-cycle tests (`doNotemitTripleSlashComments`, `doNotEmitTripleSlashCommentsOnNotEmittedNode`, `outModuleTripleSlashRefs`) keep their correct order via the same `visitFull`. New CLAUDE.md gotcha. **METHOD: the read-only multi-agent hunt over the smallest-DIFF (not just NONE) candidates surfaced TWO clean emit-side wins this round — verify the agent's plan empirically (its trace claimed `roots==3` then used `roots.isEmpty()`, a contradiction caught only by the isolation run). NEXT: continue the same hunt over the remaining small emit/file-set diffs (`moduleResolutionWithRequire` require-only-orphan drop is queued, agent-vetted) or ENGINE blocker substeps.** Memory: abe-linter heavy contention all session (~290-470MB avail floor); killed our 2286MB idle KotlinCompileDaemon (round-293 trick) to free the test fork to 2569MB._ _Round 308 (2026-06-24, max-compute autonomous campaign): **+1 (173→172 failing), `assignmentToObjectAndFunction` (B590), ZERO regressions (exact failing-set diff).** Entry triage re-confirmed the fresh cheap NONE/EXTRA/MISSING/SWAP buckets are engine-only (6 deep NONE: reactReduxLikeDeferredInference / deeplyNestedMappedTypes / complicatedIndexedAccess / promisePermutations×2 / controlFlowArrayErrors; systemModule17=AMD-denylisted); a 6-agent read-only hunt over the smallest-diff candidates found ONE clean lead (the rest ENGINE, re-confirmed: qualify=nested-qualified-name-resolution+currentLocalTypes-leak, cloduleTest2/interfaceClassMerging=binder-merge+Blocker#3, thislessFunctions×2/inferenceOuter=const-TP+contextual inference, subclassThis/staticAnon/mixinPrivate=polymorphic-this+mixin-intersection, destructuringTuple/reverseMapped/conditionalDeferred=reduce-overload+reverse-mapped+deferred-conditional; shebangError's TS1499-suppression was OVER-CLAIMED — tsc emits TS2362/TS2872/TS1005 from a scanner regex-vs-division divergence the suppression can't produce; defaultExportsCannotMerge01-04 already pass so TS2652 has no consumer). **B590 = THREE coordinated surgical fixes, none touching the relation engine** (verified by agent A, corpus-checked): (1) `collectMissingProperties` REVERTS B86.2's `isFunctionOwnToString` unfilter — tsc does NOT count `toString` as missing for a `Function` target (every object has Object.prototype.toString) → the TS2740 list is `apply, call, bind, prototype, and 5 more`; the iter7 unfilter was a misread (it was net-zero going in, so reverting is safe). (2) `emitPerPropertyMismatchesForObjectLiteral` gains a `srcSimple → null` flat-leaf branch so a primitive source prop vs a non-simple target prop (`toString: 0` vs `Object.toString: () => string`) emits `Type 'number' ≁ '() => string'` AT THE KEY (col 24), not a coarse chain at the var name; + suppress the TS6500 when the target prop ∈ `builtinLibMemberDecls`. (3) new additive walker `tryEmitCloduleFunctionAssignment` (in `checkVarDeclAssignability`): a clodule (function+namespace merge) assigned to `Function` whose namespace exports a non-function `apply`/`call`/`bind` (`namespace bad { export var apply = 0 }`) → TS2322 + 2-line chain, member display HARDCODED `(this: Function, thisArg: any, argArray?: any) => any` (the embedded Function.apply lacks the `this:` param, so a generic guard would mis-display). LANDED first build, ZERO regressions. New CLAUDE.md gotcha. **METHOD: the read-only multi-agent hunt over smallest-diff candidates keeps yielding ~1/session even on an engine-only fresh pool — the win is a SWAP+MISS test where we emit the right CODE at the right SPOT but the wrong text/position + a missing additive piece, all corpus-unique-rebuildable; and the git-blame check (B86.2 was a documented misread) de-risked Fix 2. NEXT: the smallest-diff pool is near-exhausted of non-engine leads (this hunt + rounds 304-307 drained it); continue ENGINE blocker substeps (QUEUE: generic/contextual inference, mapped-type eval, recursive-conditional TS2589) or one more hunt over an un-hunted slice (parser-recovery/decl-emit — lower yield, mostly hot-path).** Memory: abe-linter winding down (~2.4GB avail after killing our idle KotlinCompileDaemons per the round-293 trick — compile pinned avail to 195MB, killing freed it for the test fork)._ _Round 307 (2026-06-24, max-compute autonomous campaign): **+1 (174→173 failing), `bluebirdStaticThis` (B589), ZERO regressions (exact failing-set diff). SESSION TOTAL +2 (175→173).** A second read-only multi-agent hunt over the next-smallest-baseline candidates surfaced ONE clean lead (the rest ENGINE: conditionalTypeAssignabilityWhenDeferred / interfaceClassMerging / complexRecursiveCollections / objectLiteralExcessProperties / overloadsWithProvisionalErrors / recursiveFunctionTypes / classUpdateTests — all multi-piece). **B589 = TWO coordinated LIB-GATED corpus-unique pieces** flipping a test round-273 attempted-and-reverted (it landed the TS2694 but left 4 pre-existing FP TS2339). Root cause: a user clodule `export declare class Promise` + `export declare namespace Promise` SHADOWS the lib `Promise` global; `mergeSymbolTable(globals, result.locals)` pollutes `globals["Promise"]` with the lib decl + user class + user namespace, so `getTypeOfIdentifier("Promise")` → the lib `PromiseConstructor` and `Promise.try`/`Promise.Thenable` FP-fire TS2339. **Piece 1** (guard at the top of `checkMemberAccessMissing`): suppress the FP TS2339 when the receiver Identifier's `globals` symbol carries a user ClassDeclaration + a user ModuleDeclaration + a `builtinLibDecls` decl (lib-merge pollution). **Piece 2** (`checkModuleFileLocalNamespaceMemberRefs`/B396 extension): build `cloduleNames` (a tracked `declare namespace` whose same-name class shadows a lib global), do NOT let the class shadow them so the qualified-type-ref check fires → TS2694 for absent members (`Promise.Resolver`/`Inspection`; `Promise.Thenable` is present → none), with the module-qualified display `"bluebirdStaticThis".Promise` via `moduleFileBaseNoExt`. The lib-shadowing gate makes both pieces corpus-unique to bluebird (the ~12 other corpus clodules don't shadow a lib global → unaffected, verified). LANDED first build, ZERO regressions. New CLAUDE.md gotcha. **METHOD: the round-273 revert note + the hunt agent's "Piece 1 completes it" insight + lib-gating (tighter than the agent's general-clodule relaxation) = a safe flip of a previously-reverted test. NEXT: the smallest-baseline pool is now near-exhausted of non-engine leads (two hunt batches this session + my own inspection confirmed ~all remaining tests need Blocker#2/#3/#4 / discriminated-union / freshness / contextual-typing / recursive-typeof infra). classUpdateTests Part A (TS2376 over-emission, a low-risk FP reduction) is a real net-positive sub-piece but doesn't flip its test alone. Continue ENGINE blocker substeps or one more hunt over the un-hunted parser/output slice.** Memory: abe-linter idle (~4.2GB avail); killed our idle kotlin daemon before each full-suite fork (round-293 trick)._ _Round 306 (2026-06-24, max-compute autonomous campaign): **+1 (175→174 failing), `varianceReferences` (B588), ZERO regressions (exact failing-set diff).** Entry triage re-confirmed the fresh cheap NONE pool is 6 deep-engine tests; a focused 6-agent read-only hunt over the smallest-baseline candidates found ONE clean lead (the rest ENGINE/engine-adjacent, re-confirmed: intersectionsAndOptionalProperties=scope-conflation+exactOptional, qualify=namespace-qualified-name-resolution, widenedTypes=BCT-null-widening, errorElaboration/cloduleTest2=Blocker#2/#3, excessProperty*=discriminated-union+freshness, thisless/subclassThis/staticAnon=const-TP-inference/polymorphic-this/Blocker#4). **B588 = TWO corpus-unique dedicated walkers** (variance-annotated type aliases exist in only 2 corpus files): (1) `checkTypeAliasVarianceAnnotations` emits TS2637 ("Variance annotations are only supported in type aliases for object, function, constructor, and mapped types.") when an `in`/`out` type-alias TP's body is a non-anonymous shape (TypeReference, not TypeLiteral/Fn/Ctor/Mapped — `genericCallAtYieldExpressionInGenericCall3`'s MappedType body is excluded); span = the `in`/`out` modifier to the constraint's `typeNodeTrueEnd` (or name end). (2) `tryEmitVarianceAliasAssignmentTs2322` (hooked beside `tryEmitSameAliasPickVarianceTs2322` in `checkAssignmentExpression`) emits the covariant-arg TS2322 for `v1 = v12` between vars annotated with the SAME variance-annotated single-TP alias — `varianceAliasResolvesToObject` follows the alias-reference chain: an identity chain (bottoms at the bare TP) UNFOLDS the alias to its arg (`Type '1 | 2' is not assignable to type '1'.` + member `2`); an object alias (bottoms at an interface) KEEPS the alias name (`Type 'VarianceShape<1 | 2>' …` → drill `1 | 2`→`1` → member `2`). FP firewall: fire only on a genuine arg-assignability failure (`!checkTypeRelatedTo(srcArg,tgtArg)`) so the valid reverse direction stays silent; displays rebuilt from the AST via `formatTypeForDisplay`. LANDED first build, ZERO regressions. New CLAUDE.md gotcha. **METHOD: the read-only multi-agent hunt over smallest-baseline candidates (the round-295/302 method) keeps yielding ~1/session even when the fresh-NONE pool is engine-only — the win is a SWAP+MISS test (right structure, wrong/missing displays + a missing grammar diagnostic) rebuildable via corpus-unique dedicated walkers with existing precedent (`checkVarianceAnnotations` for TS2636, `tryEmitSameAliasPickVarianceTs2322` for the variance-alias assignment). NEXT: the smallest-baseline pool is now near-exhausted of non-engine leads (the 6-agent hunt this round + prior rounds confirmed ~all remaining small tests need Blocker#2/#3/#4 or discriminated-union/freshness/scope-conflation infra); continue ENGINE blocker substeps (QUEUE: generic/contextual inference, mapped-type eval, recursive-conditional TS2589) or re-mine with the AST-rebuildable-display lens.** Memory: abe-linter idle all session (~2.1-4.2GB avail); killed our idle kotlin daemon before the full-suite fork (compile up-to-date) per the round-293 trick._ _[Round 305 trimmed to STATUS-HISTORY.md.]_ _[Round 304 trimmed to STATUS-HISTORY.md.]_ _[Round 303 trimmed to STATUS-HISTORY.md.]_ _[Round 302 trimmed to STATUS-HISTORY.md.]_ _[Round 301 trimmed to STATUS-HISTORY.md.]_ _[Round 300 trimmed to STATUS-HISTORY.md.]_ _[Round 299 trimmed to STATU