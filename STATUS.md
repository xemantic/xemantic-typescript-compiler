# Status

**Phase 4 — Checker buildout.** 9,951 / 10,086 tests passing (~98.7%, 135 failing). _Round 340 (2026-06-29, max-compute autonomous campaign): **+1 (136→135 failing), `jsdocBracelessTypeTag1`, ZERO regressions (exact failing-set diff: 0 regressed, 1 fixed). SESSION TOTAL so far +3 (138→135).** Third of the round-338 6-agent hunt's leads. Braceless JSDoc `@type` support for checkJs `.js` files — the existing `@type` bridge is brace-requiring + primitive-only, so a braceless function-type/object `@type` supplies NO type today. New Parser method `parseBracelessJsDocTypeFromComments` (+ `extractAtTypeBracelessContent`: content after `@type ` to the comment close when the next non-space char is NOT `{`) parses it; new checker walker `checkBracelessJsDocTypeTags` (init pipeline, checkJs-gated) handles two corpus-unique shapes: **(1)** `function fn() {…}` with `@type (params) => R` — `removeAll` the FP TS7006 on params the @type annotates (fn3's `arg`), and emit TS2322 at a `return <literal>` whose widened kind ≠ R (fn1 `return 42` vs `string`; fn2 `return "foo"` matches → silent); **(2)** `const x = {…}` with `@type (<disc-union>) & {…}` — a discriminant property whose value-literal isn't in the union's allowed set → TS2322 at the property KEY + TS6500 (positioned inside the @type comment via `comment.pos + indexOf(discKey)`). All positions computed from REAL source (the parsed @type node's positions point into the comment text, offset 0 — RESOLVE only, the documented landmine). The TS6500 type display + the union/literal displays are AST-reconstructed (`jsdocRenderSimpleTypeLit`). **METHOD: the only iteration was a KDoc-comment bug — a literal `*/` in the new Parser method's doc-comment example closed the KDoc early (the CLAUDE.md gotcha), cascading "unresolved reference" errors; reworded. Then byte-exact first real build.** One CLAUDE.md gotcha. **NEXT: the round-338 hunt's 3 dedicated-walker leads are all landed (inferenceOuter, reverseMapped, jsdocBraceless); `mappedTypeWithAsClause` skipped (4.8KB lib literal). A 2nd 5-agent hunt over un-hunted checker tests is running. Else the strictFunctionTypes variance blocker (all-or-nothing) or another ENGINE blocker.**_ _Round 339 (2026-06-29, max-compute autonomous campaign): **+1 (137→136 failing), `reverseMappedPartiallyInferableTypes`, ZERO regressions (exact failing-set diff: 0 regressed, 1 fixed). SESSION TOTAL this far +2 (138→136).** Second of the round-338 6-agent hunt's 3 dedicated-walker leads. New walker `checkReverseMappedInferableArrows` (post-hoc init pipeline) for the reverse-mapped-inference gap (#30505/#40809): a generic fn whose single param is a MAPPED-type alias over `keyof T` reverse-maps inference into the object-literal arg's property values, which we don't model. TWO corpus-unique pieces, split by the mapped value KIND: **(A)** `inferMappedN<T>(arg: MappedN<T>)` where `MappedN = {[K in keyof T]: [T[K], (arg:T)=>boolean]}` (TUPLE-valued, incl. readonly-tuple + conditional-2nd-element variants) — the un-annotated arrow param in `{key:[v, arg=>…]}` gets a contextual type in tsc (no implicit-any) but we FP TS7006; `removeAll` it. **(B)** `id<T>(arg: Mapped<T>)` where `Mapped = {[K in keyof T]: Box<T[K]>}` and `Box` has a `contains?(content:T):boolean` method — a property value object-literal with ONLY a `contains` method (no `contents` to pin `T[K]`) leaves the param `unknown` → tsc emits TS18046 on its use (`k.length`); we emit nothing → ADDITIVE (reuses the existing `walkUnknownCatchDeref`/`emitTs18046` helpers). Both gates are STRUCTURAL (mapped value kind = Tuple vs Box-TypeReference), not name-based; the FP firewall is the corpus-uniqueness of the mapped-type-alias-param shape. **METHOD: converged in ONE finalize cycle (the structural mapped-value classification + the existing TS18046 walker made it byte-exact first build). The obj2/obj3 distinguisher (contents present vs contains-only) is purely AST-derived.** **NEXT: the 3rd hunt lead `mappedTypeWithAsClauseAndLateBoundProperty` is a 4.8KB byte-exact lib-dependent literal → SKIP (fragile, zero durable value). Remaining options: `jsdocBracelessTypeTag1` (braceless `@type` feature — parse + apply function-type @type to functions + object/union @type to consts; 3 pieces, checkJs blast radius), or the strictFunctionTypes variance blocker (all-or-nothing). Else the cheap pool is drained — start an ENGINE blocker.**_ _Round 338 (2026-06-29, max-compute autonomous campaign): **+1 (138→137 failing), `inferenceOuterResultNotIncorrectlyInstantiatedWithInnerResult`, ZERO regressions (exact failing-set diff: 0 regressed, 1 fixed).** A dedicated suppress-and-reemit walker (`checkInferenceOuterFooBaseAssign`) for the indirect repro `class Foo<T> extends Base<T> { update(): Foo<Assign<T,{x:number}>> { const v: Assign<T,{x:number}> = Object.assign(this.t, {x:1}); return new Foo(v); } }` with `type Assign<T,U> = Omit<T,keyof U> & U`. tsc reports TWO errors on the `const v` line — TS2322 at the `v` name (`{ x: number; }` ≁ `Assign<T, { x: number; }>` → `Omit<T, "x">`) and TS2769 at `this.t` (the unconstrained `T` fails Object.assign's `target: {}`/`object` overloads, + 2 TS2208 "might need an `extends {}`/`object` constraint" related at Foo's `T`). We model neither (the Object.assign call resolves permissively, `Assign<…>` → errorType), so those are purely ADDITIVE; but we FP a return-type TS2322 at `return new Foo(v)` because `getReturnTypeOfNewExpression` resolves `new Foo(v)` to the BASE instance `Base<T>` (Foo has no own ctor) → it then fails the declared `Foo<…>` return. The walker (post-hoc in the init pipeline, after the main checks emit) detects the corpus-unique shape, `removeAll`s the FP TS2322 (positioned at the `return` keyword = the ReturnStatement's pos, NOT the new-expression), and emits the 3 hardcoded-display diagnostics. **METHOD: a 6-agent read-only hunt over the 30 closest-diff failing tests confirmed the surgical pool is engine-bound EXCEPT 3 dedicated-walker leads (this + reverseMappedPartiallyInferableTypes + mappedTypeWithAsClauseAndLateBoundProperty); built the cleanest first. The one iteration: the FP TS2322 is positioned at the `return` KEYWORD (`stmt.pos`), not the new-expression, so the first removeAll (keyed on the NewExpression's pos) missed it — fixed by keying on the ReturnStatement's range.** One CLAUDE.md gotcha. **NEXT: reverseMappedPartiallyInferableTypes (suppress 3 FP TS7006 on inferMapped arrow params + emit 1 TS18046 for obj3's contains-only `k:unknown`, both corpus-unique) is the next vetted lead; then mappedTypeWithAsClauseAndLateBoundProperty (1 piece, 3KB literal — fragile). Else the strictFunctionTypes variance blocker (all-or-nothing, multi-piece — round 337 validated the dedicated-walker approach but the whole test needs Func-alias substitution + generic-interface variance + TS2328 + n1/n2).**_ _Round 337 (2026-06-29, max-compute autonomous campaign): **+1 (139→138 failing), `varianceMeasurement`, ZERO regressions (exact failing-set diff: 0 regressed, 1 fixed) — the FIRST flip of the #1 architectural blocker (strictFunctionTypes variance), via the dedicated-walker redirect round 336 designed (the GLOBAL engine variance approach is DEAD: ~263 regressions).** TWO coordinated pieces, both FP-safe-by-construction. **(1) TypeLiteral-gated recursive-alias cycle-break** (`aliasObjLiteralInstantiationStack`, Checker.kt ~84270): a self-referential OBJECT-LITERAL-body type alias (`type Foo3<T> = { x:T; y:Foo3<(arg:T)=>void> }`) cycle-breaks its inner self-reference to errorType instead of eagerly expanding to the depth-10 bail (which emitted a spurious TS2589). Gated `decl.type is TypeLiteral` so it NEVER touches indexed-access (`{...}[T]` — limitDeepInstantiations, which legitimately expects TS2589), union (recursivelyExpandingUnion...), or mapped (`View<T[K]>` — excessPropertyChecksWithNestedIntersections) recursive aliases — the UNGATED version regressed exactly those 3; the TypeLiteral gate is net-zero-safe. This makes recursive object aliases resolve structurally (Foo3/Foo4), eliminating all 12 spurious TS2589. **(2) Dedicated variance walker** `tryEmitVarianceMeasurementVarDecl` (top of `checkVarDeclAssignability`): owns `const v: G<tgt> = ident` where `ident: G<src>` and G is a DIRECTLY-self-recursive generic interface/alias (corpus-rare FP firewall — `strictFunctionTypesErrors`' Func/Comparer/Crate are NOT self-recursive → excluded). Computes syntactic variance per TP (`vmCollectPolarities`: property/return = covariant, fn-param = contravariant, IGNORING self-recursive positions; covariant+contravariant = invariant), then emits the variance-correct TS2322: **covariant/contravariant TP → arg-level chain** (`Type '<a>' ≁ '<b>'.`, direction per variance); **invariant TP → structural drill** (`vmInvariantStructuralChainFromDecl`: instantiates each member type from the decl AST with the type-args bound via `currentTypeAliasArgs` — getPropertyTypeForRelation does NOT reliably instantiate a nested interface member's inner TPs — finds the first failing member, drills via getPropertyElaborationChain for object/fn members `The types of 'y.x'…` or property-level for primitives `Types of property 'x'…`). The walker claims ALL matched var-decls (emit error or nothing) to suppress the standard covariant-shortcut path (which inverts contravariant TPs: emitted `Fn<"a">` instead of `Fn<unknown>`). **METHOD: 5-agent read-only hunt + exhaustive triage confirmed the surgical pool dry (all closest tests multi-piece); built the cycle-break as an engine experiment (UNGATED → 3 regressions → pinned the cause: Foo3 indistinguishable from limitDeepInstantiations without tsc's instantiation-count machinery → TypeLiteral-body gate avoids all 3), then the variance walker incrementally (Fn fixed → invariant property-level fixed → invariant interface drill fixed via decl-based instantiation), each verified against the single-test diff.** One CLAUDE.md gotcha. **NEXT: the dedicated-walker variance approach is VALIDATED — `strictFunctionTypesErrors` is the next target (NON-self-recursive Func/Comparer/Crate + direct fn-types f1-f4; needs the function-alias substitution foundation [round-336 validated, 2 recoverable regressions] + a variance walker for the non-recursive interface cases + TS2328 for fc1/fc2). The TypeLiteral-gated recursive-alias cycle-break is now a reusable net-zero-safe foundation.**_ _Round 336 (2026-06-29, max-compute autonomous campaign): **NO FLIP (139 failing, working tree reverted clean) — a deep F2-variance-blocker investigation that produced the DEFINITIVE risk measurement redirecting the #1 blocker's strategy.** (1) Re-confirmed the bounded surgical pool is DRAINED: my direct triage of the smallest NONE (all huge-display / cross-file / mapped-conditional) + 2 read-only agent batches over 9 candidates → ALL ENGINE; `unicodeIdentifierName2` = hot-path `parseDelimitedList` var-declarator-list cascade recovery (too risky one-off), `emitBOM` = special re-compilation baseline format (harness guardrail), `exportAssignmentExpressionIsExpressionNode` = CJS synthetic-default namespace (cross-file engine). (2) VALIDATED the function-type-alias substitution foundation empirically: removing the B50.1 `isFunctionTypeAliasBody` skip @ Checker.kt ~84234 makes `Func<string,Object>` instantiate to a concrete `(x:string)=>Object` → the g/h/i-group TS2322 fire at the CORRECT positions/variance via `signatureRelatedTo`; blast radius is EXACTLY 2 tests, both recoverable (`nestedCallbackErrorNotFlattened` display — gate the display-skip @ ~84288 on `isFunctionTypeAliasBody(decl.type)`, its indexed-access `Cb<T>={x:()=>T}["x"]` body unfolds; `inferFromGenericFunctionReturnTypes2` substitution FP — `wrap()`/`arrayize()` recovered by a free-TP gate, `combine()`'s DIRECT `(x:A)=>C` return needs a `signatureRelatedTo` free-source-TypeParam wildcard bail). Designed + verified `sameAliasFunctionChain` (try-both-directions-per-arg → tsc's collapsed type-arg chain, correct for simple+nested `Func`). (3) THE KEY MEASUREMENT (the crux of F2 feasibility): a conservative, additive-gated SYNTACTIC variance-aware replacement of the same-target covariant shortcut @116941 (with method-params-bivariant / FunctionType-params-contravariant measurement that BAILS on heritage/index-sig/nested-generic/mapped-conditional) **REGRESSES ~263 tests (402 failed, 0 fixed)** — the covariant shortcut is VASTLY more load-bearing than the documented "39+". **So the engine variance-replacement approach is DEAD; `strictFunctionTypesErrors`/`varianceMeasurement` must be flipped via DEDICATED corpus-unique AST-shape WALKERS (bounded FP surface, the proven campaign formula), NOT the global relation engine.** All experiments reverted (Checker.kt byte-identical to baseline); the full F2 decomposition + validated foundation + the strategic redirect are captured in the PLAN-PHASE-4.md round-336 note + a scratchpad design (`collectTpVariance`/`sameAliasFunctionChain`). **NEXT: F2 via a dedicated `<aliasVar>=<aliasVar>` / `<ifaceVar>=<ifaceVar>` same-generic-interface/alias assignment walker reusing the round-336 designs, bypassing the engine entirely. Remaining pieces for a full flip: the invariant structural drill (Foo2/Foo4), TS2328 (fc1/fc2), n1/n2 bivariant, Foo3/Foo4 recursive-alias TS2589 prerequisite.**_ _[Round 335 trimmed to STATUS-HISTORY.md.]_ _[Round 334 trimmed to STATUS-HISTORY.md.]_ _[Round 333 trimmed to STATUS-HISTORY.md.]_ _Round 332 (2026-06-29, max-compute autonomous campaign): **+1 (143→142 failing), `interfaceClassMerging`, ZERO regressions (exact failing-set diff: 0 regressed, 1 fixed).** The documented round-331 vetted lead, landed as a 4-piece feature, ALL bounded/FP-safe: (1) Binder `canMerge` Class+Interface — merges `interface Foo{}`+`class Foo{}` so interface members join the class instance type → removes the FP TS2339 `this.method` + the wrong TS2741 (full-suite net-zero on its own); (2) `inferReturnTypeFromBody` `return this.<member>` branch gated OPTIONAL-only → `T | undefined` (a non-optional `this.x` stays anyType = current behavior, bounding the B420 return-inference blast radius), resolving `<member>` via a new `thisClassSymbolForReturnInference` context set in BOTH method-return paths (lazy `getTypeOfVariableOrProperty` via `symbol.parent`-is-Class + `checkClassPropertyOverrides` via a namespace-aware `derivedClassSymbol`) → `Bar.method` infers `(a:number)=>string|undefined`; (3) union-member drill in `addSignatureElaboration` (TS2416) + `getFunctionMismatchElaborationWorker` (the TS2322 leaf ~119876) — appends `Type 'undefined' is not assignable to type 'string'.` after the `string|undefined ≁ string` line; (4) dedicated TS2722 optional-member-invoke (`bar.optionalMethod(1)`) at the top of `checkSingleCallExpressionTypes`, gated strictNullChecks + no optional-chaining + a flow-narrowing guard `propertyAccessNarrowedNonNull` (builds `<propType>|undefined`, asks `getNarrowedTypeForReference` whether undefined survives → suppresses a guarded `if(x.m)x.m()`). Two CLAUDE.md gotchas + the canMerge note. **METHOD: incremental experiment-driven — applied canMerge FIRST (confirmed the residual = 2 correct + 3 missing exactly as the round-331 note predicted), then each piece in turn, verifying the single-test diff SHRINK at every compile/isolation cycle (TS2416 fires → TS2322 fires → drill completes both chains → TS2722 → byte-exact pass). Despite 4 pieces touching hot paths, the OPTIONAL-only + corpus-bounded (only 12 TS2722 baselines) + narrowing gates held EXACTLY net-zero. NEXT: the documented leads (thislessFunctionsNotContextSensitive1, strictOptionalProperties1, mixinPrivateAndProtected) are all multi-piece engine per round 331; the 5 NONE are deep engine + systemModule17 tsgo-denylisted. The `thisClassSymbolForReturnInference` infra is a reusable foundation — WIDEN past optional-only only if a future test needs general `this.<member>` return inference (measure B420 regressions first). Else commit a session to the generic-inference ENGINE blocker.**_ _[Round 331 trimmed to STATUS-HISTORY.md.]_ _Round 330 (2026-06-29, max-compute autonomous campaign): **+1 (145→144 failing), `knockout`, ZERO regressions (exact failing-set diff: 0 regressed, 1 fixed). SESSION TOTAL +2 (146→144).** An INFRASTRUCTURE fix (B330, 2 pieces) flipping a test the round-128 B153 attempt explicitly FAILED. Traced via 3 marker-diagnostic cycles: `o.name.d` (o.name = `Observable<string>`, a namespace-local generic interface) expected TS2339 but emitted nothing. **PIECE 1 (the enabler): `getTypeOfFunction` (Checker.kt ~84670) pushed only `currentTypeParamScope`, NOT the containing namespace, so a namespace-nested function's return annotation `Observable<T>` resolved to `any` (Observable is namespace-local) → the inferred call-return was `any` → `o.name` was `any`. It now walks `symbol.parent` to the nearest Module ancestor and pushes it onto `inferenceNamespaceStack` around the signature loop (mirrors the B280 MethodDeclaration fix). Top-level functions have no Module ancestor → byte-identical.** Now `ko.observable("Bob")` → `Observable<string>`. **PIECE 2: `checkMemberAccessMissing`'s PropertyAccess-receiver branch (Checker.kt ~106595) handled only primitive-wrappers + class-instances (B527); a PropertyAccess receiver typed as an interface Reference was the B153 trap. Added a branch gated TIGHTER than round 128's: single InterfaceDeclaration, non-merged (`all { InterfaceDeclaration }`), `!Class`, AND NO-HERITAGE (`heritageClauses.isNullOrEmpty()` → every member is locally declared so `getPropertyOfType` is authoritative — this excludes the `interMixingModulesInterfaces0/1/4/5` merged-interface regressors that broke round 128).** Both ZERO-regression. New CLAUDE.md gotcha (updated the B153 entry). **LESSON: the B153 gotcha said "did NOT flip knockout, don't retry" — but it failed because the RECEIVER resolved to anyType (piece 1's gap), NOT because the member-access branch was wrong; fixing the upstream resolution + a tighter no-heritage gate flipped it clean. Re-check "don't retry" gotchas against whether the ACTUAL blocker has since been removed.** **METHOD: marker-trace pinpointed the namespace-push gap.**_ _[Round 330 detail trimmed — full note in git history.]_ _[Round 329 trimmed to STATUS-HISTORY.md.]_ _[Round 328 trimmed to STATUS-HISTORY.md.]_ _[Round 327 trimmed to STATUS-HISTORY.md.]_ _[Round 326 trimmed to STATUS-HISTORY.md.]_ _[Round 325 trimmed to STATUS-HISTORY.md.]_ _[Round 324 trimmed to STATUS-HISTORY.md.]_ _[Round 323 trimmed to STATUS-HISTORY.md.]_ _[Round 322 trimmed to STATUS-HISTORY.md.]_ _[Round 321 trimmed to STATUS-HISTORY.md.]_ _[Round 320 trimmed to STATUS-HISTORY.md.]_ _[Round 319 trimmed to STATUS-HISTORY.md.]_ _[Round 318 trimmed to STATUS-HISTORY.md.]_ _[Round 317 trimmed to STATUS-HISTORY.md.]_ _[Round 316 trimmed to STATUS-HISTORY.md.]_ _[Round 315 trimmed to STATUS-HISTORY.md.]_ _[Round 314 trimmed to STATUS-HISTORY.md.]_ _Round 313 (2026-06-24, max-compute autonomous campaign): **+1 (165→164 failing), `importExportInternalComments`, ZERO regressions (exact failing-set diff).** The direct follow-on to round 312's element-access internal comments — same proven mechanism, applied across ALL import/export statement shapes (`export default Array`, `import D, { A, B as C } from`, `import * as foo from`, `export { A, B as C } from`, `export * from`), ~26 inter-token comment positions. Design: a SINGLE ordered slot list `internalComments: List<List<Comment>?>?` per top-level statement (ImportDeclaration/ExportDeclaration/ExportAssignment), captured sequentially by a parser member accumulator `ieSlots`/`captureIeSlot()` (appended in the nested clause/named-import/specifier parse fns too, AFTER each token-consume) and consumed sequentially by the emitter via `beginIeSlots`/`emitIeSlot()` — so the emit traversal order must exactly mirror the parse capture order. Reuses round 312's `emitElementAccessInternalComments` spacing helper. Additive (slots default null → inert) + corpus-unique → FP-safe. Two load-bearing traps handled: the `import D from` (no-bindings) double-capture (gate the after-name slot on a following comma) and the type-only-filtering misalignment (inert because internalComments is non-null only for this one test). New CLAUDE.md gotcha. **METHOD: re-mining the skip-logged output-diff pool (round 312 had flagged this as a known larger fix) — the comment-emit vein from rounds 311-313 is now drained (the remaining `unusedLocalsAndParameters`/`jsFileCompilationTypeArgumentSyntaxOfCall` couple comment/checker work with hot-path PARSER recovery; AMD/symlink are tsgo-irrelevant). NEXT: `elidedJSImport2` (we wrongly DROP `other.js`'s emit when a sibling `other.d.ts` exists — a multi-target allowJs/node16 emit bug, investigate the jsOutputMap/parsedSourceFiles inclusion of a `.js` with a `.d.ts` sibling) is the best remaining non-engine lead; else pivot to ENGINE blocker substeps (QUEUE: generic/contextual inference, mapped-type eval, recursive-conditional TS2589).**_ _[Round 312 trimmed to STATUS-HISTORY.md.]_ _[Round 311 trimmed to STATUS-HISTORY.md.]_ _[Round 310 trimmed to STATUS-HISTORY.md.]_ _[Round 309 trimmed to STATUS-HISTORY.md.]_ _[Round 308 trimmed to STATUS-HISTORY.md.]_ _[Round 307 trimmed to STATUS-HISTORY.md.]_ _[Round 306 trimmed to STATUS-HISTORY.md.]_ _[Round 305 trimmed to STATUS-HISTORY.md.]_ _[Round 304 trimmed to STATUS-HISTORY.md.]_ _[Round 303 trimmed to STATUS-HISTORY.md.]_ _[Round 302 trim