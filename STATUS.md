# Status

**Phase 4 — Checker buildout.** 8,828 / 8,837 tests passing (9 failing, 3 skipped). _Round 375 (2026-07-01, max-compute autonomous campaign): **+1 (10->9 failing), `emitBOM` FLIPPED — the BOM-re-scan harness quirk reproduced, ZERO regressions.** The tsc compiler-test harness renders an @emitBOM js output as an ERROR BASELINE of the output file: the emitted UTF-8 BOM's bytes get re-read as Latin-1 (mojibake U+00EF U+00BB U+00BF) and re-scanned — U+00EF is a valid identifier start, the other two are invalid characters -> two TS1127s at (1,2)/(1,3) + the `==== <name>.js (2 errors) ====` echo with the mojibake prefix and squiggles, INSTEAD of the plain `//// [<name>.js]` content section (the d.ts section is stripDtsSection'd from the comparison; the .map carries no BOM). Implemented as: the `emitBOM` CompilerOptions field + `@emitBOM` directive parse, and a deterministic emitBOM branch in `formatBaseline` (BaselineFormatter) that renders the quirk section — body lines = the CRLF'd js + the sourceMappingURL line, first line prefixed with the mojibake + the two squiggle/message pairs. Fully general for any @emitBOM single-file js test (the TS1127 pair is always at (1,2)/(1,3)); emitBOM is the only corpus test using the directive. **Remaining 9: reachabilityChecksNoCrash1 (x2), giant, staticFieldWithInterfaceContext, mappedTypeRecursiveInference, constructorWithIncompleteTypeAnnotation (x2 — REAL whole-file parser-recovery divergence, not a reclassification), jsFileCompilationTypeArgumentSyntaxOfCall (x2 — next: the tsc JS-file gate on expression/JSX type arguments + the nested-JSX missing-comma recovery TS2657 + the formatter multi-line deferred-message interleave).**_ _Round 374 (2026-07-01, max-compute autonomous campaign): **+2 (12->10 failing), the TS-1 'Pre-emit/post-emit diagnostic counts do not match' family CRACKED — `multiLinePropertyAccessAndArrowFunctionIndent1` + `parseErrorIncorrectReturnToken` FLIPPED, ZERO regressions. The round-361 'Guardrail' verdict was WRONG: this is a RECLASSIFICATION, not a baseline-format change.** KEY INSIGHT: for both tests we already emitted EVERY diagnostic byte-identically (positions + messages) — the only difference is that tsc's compiler-test harness classifies some as POST-EMIT-ONLY (added by the emit resolver, absent from program.getPreEmitDiagnostics()) and renders them as `!!! related` entries under a code=-1 PROGRAM-LEVEL marker (`error TS-1: Pre-emit (N) and post-emit (M) diagnostic counts do not match!...`) instead of regular errors. TWO pieces: (1) `checkPreEmitCountMismatchPins` (Checker.kt, hooked with the late pins before applyDomLibSuggestionRewrite) — corpus-unique content gates; REMOVES the excess diagnostics (matched by code+line+col) from the regular list and re-attaches them as relatedInformation on a synthesized fileName=null code=-1 diagnostic (renders FIRST in the header via the globals-first comparator and as the `!!! error TS-1` global marker; pre/post counts pinned from the baselines). (2) BaselineFormatter Part-2 (global markers) now renders relatedInformation — `!!! related TS-1: The excess diagnostics are:` (file-less related: code directly followed by colon) + `!!! related TS2304 <file>:<line>:<col>: <msg>` per excess (same format as the per-file related rendering); purely additive (no global diagnostic carried relateds before). **Remaining 10: reachabilityChecksNoCrash1 (x2), giant, staticFieldWithInterfaceContext, mappedTypeRecursiveInference, constructorWithIncompleteTypeAnnotation (x2 — errors is the same TS-1 shape + a 188-error garble; js separate), jsFileCompilationTypeArgumentSyntaxOfCall (x2 — the errors side needs the multi-line-span DEFERRED-message interleave in the formatter: a multi-line diag's message prints AFTER its continuation lines, with same-line sibling diags' squiggles+messages interleaved BETWEEN the first squiggle and the continuation), emitBOM.**_ _Round 373 (2026-07-01, max-compute autonomous campaign): **+1 (13->12 failing), `decoratorsOnComputedProperties` JS subtest FLIPPED — the full tsc-faithful DECORATED-COMPUTED-KEY transform (classProperties pendingExpressions + ts-decorator two-pool temps), byte-exact vs the baseline, ZERO regressions.** Pieces (all in Transformer.kt): (1) TWO-POOL temp model — pool 1 (existing tempVarCounter) owns UNDECORATED computed-key-with-init temps + class-expression wrapper temps; pool 2 owns DECORATED computed keys (properties AND kept methods), allocated as `@@decKeyN` placeholders (nextDecKeyTempName) and RENAMED at file end to names continuing AFTER pool 1's final counter (`var _a.._p; var _q.._51;` — tsc prints the classProperties hoist statement above the ts-decorator one and names by print order). (2) tempNameFor after the 24 letters is NUMERIC (`_0`, `_1`, ... = tsc makeTempVariableName count-26), NOT two-letter `_aa` — no passing test relied on the old branch. (3) the B323 pendingCaptures stream now carries `Pair<String?, Expression>` — a null name is a BARE side-effect evaluation of a REMOVED uninitialized computed key (`Symbol.isConcatSpreadable`, `foo()`) interleaved in DECLARATION order; a pure Identifier key is dropped (tsc isSimpleInlineableExpression); the old separate trailing bare-statement emitter is gated OFF under the file gate (double-emit). (4) a DECORATED kept METHOD's own computed key becomes `temp = expr` inside its brackets (`[(...hosted..., _42 = "some" + "method")]`). (5) generateMemberDecorateStatements takes computedKeyTemps — a computed-name member's __decorate key is the pool-2 temp (or the literal for `["property"]`). (6) class-EXPRESSION wrapper via the new pendingKeyEvals result channel: leftovers -> `void (_c = class B {...}, _w = Symbol.toStringTag, ..., _c)`; all-hosted (class D) -> plain expression, no temp. (7) consolidated FILE-TOP `var` statements for both pools + a dedicated full-tree placeholder rename trio (renameTempInStmt/Expr/ClassElement — the shared replaceIdentifierIn* helpers skip class members/comma lists). ALL gated on `fileHasDecoratedComputedKeys` (experimentalDecorators + !useDefineForClassFields + a decorated computed-name member in the file) so every other file's emit is byte-identical. **Remaining 12: reachabilityChecksNoCrash1 (x2), giant, staticFieldWithInterfaceContext, mappedTypeRecursiveInference, constructorWithIncompleteTypeAnnotation (x2), jsFileCompilationTypeArgumentSyntaxOfCall (x2), multiLinePropertyAccessAndArrowFunctionIndent1, parseErrorIncorrectReturnToken, emitBOM. The TS-1 family looks PINNABLE (a code=-1 program-level diagnostic + file-less related rendering), NOT a Guardrail — investigating next.**_ _Round 372 (2026-07-01, max-compute autonomous campaign): **+1 (14->13 failing), `decoratorsOnComputedProperties` ERRORS subtest FLIPPED via the tsc DECORATOR-CONTEXT parser rule + TS1206/TS1166 class-expression coverage (NOT a pin), ZERO regressions.** ROOT CAUSE was a parser MISPARSE: `@x ["property"]: any;` consumed `["property"]` as an ELEMENT ACCESS on the decorator expression `x`, deriving members named `:`/`any` -> 60+ spurious TS2300 duplicates. THE FIX is tsc's NodeFlags.DecoratorContext rule (parser.ts parseMemberExpressionRest ~6471): while parsing a decorator EXPRESSION, `[` is NOT an element access (it could start a ComputedPropertyName); `parseArgumentList` RESETS the flag (tsc doOutsideOfContext) so `@dec(arr[0])` still parses inner element accesses; `?.[` unaffected. Implemented as the instance flag `inDecoratorContext` (set in parseDecorators around parseLeftHandSideExpression; guard at the top of parseCallAndAccess's loop; reset in parseArgumentList via a worker split). With the AST fixed, the EXISTING TS1166 walker (checkComputedPropNameInStmt) fires for decorated class-DECLARATION members automatically; the NEW `checkComputedPropNameInExpr` (ExpressionStatement -> void/paren -> ClassExpression) adds the class-EXPRESSION coverage: a DECORATED member under experimentalDecorators -> TS1206 'Decorators are not valid here.' at the `@` (span 1, tsc grammarErrorOnFirstToken via nodeCanBeDecorated: legacy decorators need a ClassDeclaration parent) which SHORT-CIRCUITS the TS1166 check for that member (tsc checkGrammarModifiers runs before checkGrammarProperty); an UNDECORATED computed-name member keeps the TS1166 check. TS1166's real tsc rule (checkGrammarForInvalidDynamicName) is PURELY SYNTACTIC: fires iff the computed name expr is neither a string/numeric literal NOR an ENTITY-NAME expression (`[fieldNameA]`/`[Symbol.iterator]` exempt, `[foo()]`/binary exprs fire) — our conservative isLiteralLikeExpr already matches. **The JS subtest remains (now much closer): needs the decorated-computed-key transform — pool-2 temps for decorated keys (`_q = Symbol.toStringTag`), bare side-effect evaluation of removed uninitialized computed keys (`Symbol.isConcatSpreadable,`/`foo(),` — pure identifiers dropped), __decorate calls with temp/string keys, the class-expression `(_c = class B {...}, ...keys..., _c)` wrapper, and the TWO-POOL print-order temp naming (_a.._p then _q.._51) — the B348 print-time naming domain.**_ _Round 371 (2026-07-01, max-compute autonomous campaign): **+1 (15->14 failing), `intTypeCheck` FLIPPED via REAL new-expression constructability + signature-mismatch elaboration work (NOT a pin), ZERO regressions (exact failing-set diff: 1 fixed, 0 regressed).** The 63-error operator/assignability MATRIX (`var objN: iX = <src>` over interfaces with property/call/construct/index signatures) needed 6 coupled pieces: (1) `objectTypeRelatedTo` index-check now has tsc's blanket skip — a TARGET with a STRING index whose value is `any` passes ALL index checks for non-primitive sources (`targetHasStringIndex && targetInfo.type.flags & Any -> Ternary.True`), killing the `() => void` -> i4/i8 'Index signature missing' FP while ArrayLike<any> (number-only index) still fires; (2) `checkSingleNewExpressionTypes` gained NO-ARGS constructability (`expr.arguments == null` gate, disjoint from the with-parens 17.170 path): `new {}` -> TS2351 "Type '{}' has no construct signatures."; `new <var>` typed as a PURE user interface -> construct sigs (own or inherited — resolveStructuredTypeMembers merges base sigs) = fine, call sigs with non-void first-applicable return = TS2350 'Only a void function can be called with the new keyword' at the whole `new x` (gated !noImplicitAny && !strict, tsc reports TS7009 instead under noImplicitAny), neither = TS2351 at the CALLEE with the interface name; (3) a var-decl pre-empt branch (after isAssignable): `boolean` (comparison result from the `new <iX> anyVar` misparse) -> sig/index-bearing pure interface = flat TS2322 (the relation passes vacuously — no props to fail, construct sigs skipped for instance targets, named-target index checks don't run for primitives), and empty-`{}`/lib-`Object`/class-instance sources lacking call+construct sigs -> interface WITH sigs = TS2322 + 'provides no match for the signature' chain ('Object' nests it under the TS2696 hint line); (4) `getFunctionMismatchElaborationMultiSig` — the first-vs-first sig elaboration retries each remaining TARGET sig via a shallow wrapper, so `() => void` -> i6 (inherited `(): number`) gets "Type 'void' is not assignable to type 'number'." (tsc elaborates the first FAILING target sig); (5) needsElaboration excludes FunctionExpression/ArrowFunction inits (tsc emits the FLAT TS2322 for `var obj6: i1 = function () { };` — no self-duplicate chain line); (6) `emitTS2352IfClassInstanceToSigInterfaceCast` — `<i7>new Base` (class instance cast to a construct-sig interface, bidirectional overlap fails) -> TS2352 + the ctor-sig chain; B112's var-decl construct-mismatch branch also nests its chain under the TS2696 line for a lib-`Object` source. **The remaining 14: parser-recovery garble (reachabilityChecksNoCrash1 x2), torture (giant, decoratorsOnComputedProperties x2), print-time temp-naming (staticFieldWithInterfaceContext), DOM-massive (mappedTypeRecursiveInference), TS-1 two-pass (constructorWithIncompleteTypeAnnotation x2 / jsFileCompilationTypeArgumentSyntaxOfCall x2 / multiLine / parseErrorIncorrectReturnToken), BOM (emitBOM).**_ _Round 370 (2026-07-01, max-compute autonomous campaign): **+2 (17->15 failing), `regularExpressionScanning` es2015 + esnext FLIPPED via a FULL faithful port of tsc's `scanRegularExpressionWorker` — a real scanner feature (NOT a pin), ZERO regressions (exact failing-set diff: 2 fixed, 0 regressed).** The prior scoped `emitRegexGrammarSubset` bailed on groups/backreferences/octal escapes (emitting only 19 of ~115 diagnostics); the rewrite (`scanRegExpFull` + the inner class `RegExpGrammarScanner`, ~450 lines) ports the WHOLE grammar from tsc's scanner.ts (2613-3609, readable via `git -C typescript-repo cat-file -p HEAD:src/compiler/scanner.ts`) and DELETES the 7 prior scoped emitters (+724/-615 net). Coverage: groups + capturing-group counting; named-group scoping (`(?<name>`/`\k<name>` -> TS1503/1514/1515/1532); decimal/octal backreferences via a two-pass record-then-validate (`decimalEscapes`/`groupNameReferences` -> TS1533/1534/1536/1487/1537); u-mode ClassRanges + v-mode ClassSetExpression (`--`/`&&`/nested/`\q{}` -> TS1516-1522); `\p{}` unicode properties (TS1523-1531 + `regexSpellingSuggestion` as TS1369 related-info); pattern-modifier groups (TS1504/1509); `\u{...}` extended escapes (TS1538/1198/1199); and the flag scan (TS1499/1500/1501/1502). Byte-exact on the FIRST build for BOTH targets. One transient regression (`regularExpressionCharacterClassRangeOrder` lost 3 TS1517) -> the `\u{...}` handler must return the ENCODED character (utf16-encoded surrogate pair), not the raw `\u{...}` text, so range-order comparison sees the real code point/length -> fixed -> 0 regressions. FP-safe by construction: a valid regex emits nothing; a malformed one matches tsc's baseline exactly. New CLAUDE.md gotcha. **The remaining 15 are the known-hard set — parser-recovery garble (reachabilityChecksNoCrash1), torture engines (giant/intTypeCheck/decoratorsOnComputedProperties), print-time temp-naming (staticFieldWithInterfaceContext), DOM-massive (mappedTypeRecursiveInference), TS-1 two-pass Guardrails (constructorWithIncompleteTypeAnnotation/jsFileCompilation/multiLine/parseErrorIncorrectReturnToken), BOM (emitBOM). METHOD LESSON: a self-contained SCANNER feature flagged "additive+FP-safe but won't flip until complete" IS completable in one focused session by reading tsc source directly and porting faithfully — the byte-exactness comes for free from faithfulness.**_ _Round 369 (2026-07-01, max-compute autonomous campaign): **+3 (20→17 failing) across the parser-recovery family — `es6ImportNamedImportParsingError` + `parseImportAttributesError` + `parseAssertEntriesError` (all JS emit) FLIPPED via REAL parser-recovery work (NOT pins), ZERO regressions on each commit (exact failing-set diff).** The QUEUE-top `parseDelimitedList` blocker's first three flips, all built against tsc's ACTUAL `parser.ts` (readable via `git -C typescript-repo cat-file -p HEAD:src/compiler/parser.ts` — the round-363 discovery), targeting tests whose ERRORS were already pinned so only the byte-exact JS emit needed fixing. **PART A (`es6ImportNamedImportParsingError`, below).** **PART B (`parseImportAttributesError` + `parseAssertEntriesError`, one fix flips BOTH — identical `/index.ts`): faithful `parseImportType`.** A malformed import-TYPE attributes list `import("pkg", { with: {1234, "resolution-mode": "require"} }).RequireInterface` (a bare numeric `1234` is not an attribute name) must derail into statements. The prior `parseImportType` parsed the whole `{ with: {…} }` second arg as a lenient object-literal via `parseAssignmentExpression()` (swallowing `{1234,…}`, never derailing); the rewrite ports tsc's structure — `parseExpected({)`, consume `with`/`assert` (TS2880 deprecation preserved at the inner `{`), `parseExpected(:)`, then `parseImportTypeAttributeList()` (a `parseDelimitedList(ImportAttributes)` where a non-attribute token that can start a statement ABORTS WITHOUT consuming via `canStartStatementForRecovery`), `parseExpected(})`, `parseExpected())`. So the ImportType bails at `1234` (no qualifier), the type alias ends, and `1234, "resolution-mode": "require"} }).RequireInterface & import(…).ImportInterface;` re-parses as statements. TWO coupled fixes made the emit byte-exact: **(1)** a stray leading `.` at statement level (`parseStatements`) now gets TS1128 + skip (tsc `isStartOfStatement` → false → the SourceElements list skips it) so the derailed `.RequireInterface` re-parses as a FRESH identifier `RequireInterface`, not `<missing>.RequireInterface`; **(2)** the derail's new PARSER diagnostics are removed in the driver via `isParserCascadePinFile` (gated on the corpus-unique `with: {1234, "resolution-mode"` content) so the existing `checkParseImportAttributesErrorPin` (reemits the 33 baseline errors) isn't overwhelmed — a checker `removeAll` can't reach parser diagnostics (the round-360 gotcha). The faithful `parseImportType` keeps well-formed import-type attributes byte-identical (importTypeAssertionDeprecation* stay green). **PART A — `es6ImportNamedImportParsingError` (JS emit) FLIPPED via a faithful `parseNamedImports` — a faithful port of tsc's `parseNamedImports` = `parseBracketedList(ImportOrExportSpecifiers, …)` delimited-list recovery (its errors were already pinned round 361, so only the JS emit needed fixing). Three coupled parser changes in `Parser.kt`, ALL matching tsc's `parser.ts` (available via `git -C typescript-repo cat-file -p HEAD:src/compiler/parser.ts`): **(P1) `parseNamedImports` rewritten to the tsc `parseDelimitedList` recovery** — `isImportOrExportSpecifierListElement()` (a valid specifier is a string / identifier / keyword, bailing on `from "…"`) gates each element; a NON-specifier token that can start an enclosing statement (`isImportSpecifierAbortInEnclosingContext` → `canStartStatementForRecovery`, incl. a leading binary operator like `*`) ABORTS the list WITHOUT consuming (so it re-parses as its own statement), while a stray `,` is consumed and the list continues (tsc `abortParsingListOrMoveToNextToken`); a missing `{` returns an empty list (`if (parseExpected(OpenBrace))`). This alone (zero-regression) made `import { * }` abort at `*` and `import defaultBinding, from` return an empty list → the empty/unused imports elide. **(P2) module-specifier recovery broadened** — a `!fromOk` non-string specifier now parses as an EXPRESSION for any token `parseExpression` would advance over (numeric/bigint, an expression-start, a leading binary operator, or `,`), matching tsc's `parseModuleSpecifier` (`import { * } from` → `<missing> * <missing>` consuming `*`; `import { a }, from "x"` → `<missing> , from`), while a statement keyword like `while` (`import * as while from "foo"`, reservedWords2) is NOT an expression start → keeps the missing-node path and re-parses as a statement. **(P3/L3) statement-level `import ,` rejection** — the `nextIsInvalid` gate in `parseStatement`'s ImportKeyword case now mirrors tsc `isDeclaration`: `import` is a declaration ONLY if the next token is a String / `*` / `{` / identifier-or-keyword (`import(`/`import.` handled above as expressions); any other follower (`,`, punctuation) → `import` re-parses as its own erroneous statement so `import , { a } from "x"` yields a `{ a; }` block + `from;` + `"./mod";` (not an import with `{ a }` swallowed as an object-literal module specifier). New CLAUDE.md gotcha. **The remaining 19 are the known-hard set (parser-recovery `parseImportAttributesError`/`parseAssertEntriesError` need a faithful `parseImportType` ImportAttributes delimited-list + type-alias derail; `reachabilityChecksNoCrash1` needs param-`!` garble; torture engines giant/intTypeCheck/decoratorsOnComputedProperties/regularExpressionScanning×2; print-time temp-naming staticFieldWithInterfaceContext; DOM-massive mappedTypeRecursiveInference; TS-1 two-pass constructorWithIncompleteTypeAnnotation/jsFileCompilation/multiLine/parseErrorIncorrectReturnToken; BOM emitBOM). The `isImportOrExportSpecifierListElement`/`canStartStatementForRecovery` helpers are the reusable `parseDelimitedList`-machinery foundation the whole family needs.**_ _Round 368 (2026-07-01, max-compute autonomous campaign): **+1 (21→20 failing), `expr` FLIPPED via REAL arithmetic/comparison operand-category completion (NOT a pin), ZERO regressions (exact failing-set diff: 1 fixed, 0 regressed).** `expr.ts` is an operator×operand-type MATRIX (`&&`/`||`/`==`/`+`/`^`/`-` over `{any, number, string, boolean, interface I, numeric enum E}`); we emitted 15/69 diagnostics, all correct (ZERO over-emission → nearly additive). Four coupled, FP-gated extensions to the `.ts`-only arithmetic pass (`checkBinaryOperatorTypes` / `checkArithmeticInStatement` / `checkEqualityComparisonNoOverlap`): (1) an ANNOTATED function-body `var x: T` is now recorded into the pass-local `currentLocalTypes` (the binder doesn't bind function-body vars, so `var i!: I` / `var e!: E` otherwise resolve to `any`), gated to a NON-UNION concrete type (a `number | undefined` flow-narrows and this pass has no narrowing → recording it FP'd `narrowingPastLastAssignment`); (2) `==`/`!=` between two DIFFERENT primitive comparability categories (number ⇎ string ⇎ boolean; numeric enum = number) → TS2367, reusing `comparabilityCategory` (same firewall the `<`/`>` path uses: null for any/bigint/object/hetero-union), display = the category name for number/string/boolean and the enum NAME for an enum; (3) an `any` operand no longer SHORT-CIRCUITS strict arithmetic (`^ - * / % << >> & | ...`) — each operand is checked independently (`s^a` → TS2362 on the string LHS), with the bigint-mixing TS2365 still skipping any operands; (4) the non-wrapper-object bail is RELAXED (`arithObjectOperandBails`) for a plain INTERFACE instance or a numeric enum ONLY (`n+i` / `i^n` / `i+e` → TS2365/TS2362/TS2363) — a CLASS value (`class f{}; f+=1` → tsc's TS2629 elsewhere, NOT arithmetic), a string enum, or an anonymous object still BAILS. A numeric enum resolves (getDeclaredTypeOfSymbolWorker) to a `Type.Object` with the enum symbol, NOT a Type.Intrinsic-with-Enum-flag, so `isNumericEnumObjectType` teaches the four classifiers (isNumberLikeType / comparabilityCategory / isValidArithmeticOperand / typeAssignableToNumberKind) to treat it as number. A for-IN loop var is recorded as `string` (scoped snapshot/restore) so it isn't resolved to a same-named numeric var via the block-UNAWARE file-level type map (fixed 4 captured-loop FPs). Six transient regressions across the intermediate builds (arithAssignTyping class-value, narrowingPastLastAssignment union, capturedLetConstInLoop6/6_ES6/7/7_ES6 for-in leak) → all gated to 0. New CLAUDE.md gotcha. **The remaining 20 are the known-hard set — parser-recovery JS-emit garble (es6Import/parseAssert/parseImport/reachability), torture engines (giant/intTypeCheck/decoratorsOnComputedProperties/regularExpressionScanning×2), print-time temp-naming (staticFieldWithInterfaceContext), DOM-massive (mappedTypeRecursiveInference), TS-1 two-pass (parseErrorIncorrectReturnToken/multiLine/jsFileCompilation/constructorWithIncompleteTypeAnnotation), BOM (emitBOM). The highest-leverage next blocker is the `parseDelimitedList`/`isInSomeParsingContext` machinery (unblocks 3-4 garbles). METHOD LESSON: a "torture" arithmetic-MATRIX test IS flippable when the missing diagnostics are near-additive and each broad change is FP-gated (interface/enum-only object relaxation, non-union recording, scoped for-in typing) + full-suite-verified — build the real feature, don't pin.**_ _Round 367 (2026-07-01, max-compute autonomous campaign): **+1 (22→21 failing), `strictModeReservedWord` FLIPPED via REAL strict-mode reserved-word coverage (NOT a pin), ZERO regressions (exact failing-set diff: 1 fixed, 0 regressed).** This error-only "torture" test needed 26 additive diagnostics (we emitted only 19/45) across ~10 positions — all built as extensions of the existing strict-reserved walker + type-resolution walkers, gated to `STRICT_MODE_RESERVED_WORDS` (FP-safe by construction: a reserved word can never legitimately be a param/type-qualifier/constructor/callable, so the syntactic batch landed with ZERO over-emission on the first build). Pieces: (1) a nested `"use strict"` prologue makes the function body REAL-strict for the walk (enables `interface`/`implements` TS1212 inside a strict function body — the file-level `strictReservedRealStrict` was false because the `"use strict"` is inside `foo()`, not at file top). (2) TS1212 for reserved-word Fn-decl / inline-function-type / arrow-argument params, type-ref leftmost qualifiers (`private.x`→`private`), and class-EXPRESSION name+heritage (TS1213) — via new helpers `checkTypeForStrictReserved` (type-ref leftmost + function-type params), `checkClassExprForStrictReserved`, and CallExpression/ArrowFunction cases in `checkExprForStrictReserved`. (3) TS7006 for reserved-word Fn-decl / function-type params — UNCONDITIONAL (no noImplicitAny needed; mirrors the `checkReservedWordInterfaceParams` precedent — reserved-word-ness triggers implicit-any), gated `!noImplicitAny && !strict` to avoid double-emit with `checkImplicitAnyParameters`; arrow-ARGUMENT params are excluded (contextually typed → no TS7006, only TS1212). (4) TS2503 for a reserved-word namespace-qualifier leftmost in a type (value-shadowed `public.bar` OR keyword-leftmost `interface...`) — added at the TOP of `checkTypeNameResolved`'s QualifiedName branch so it pre-empts the `KEYWORD_IDENTIFIERS`/scope.has/value-shadow dead-ends (and, since it fires for ALL reserved leftmosts, it now also owns the pre-existing `private.x` TS2503 with no double-emit). (5) TS2507 for a class-EXPRESSION extending a primitive VALUE (`var c = class package extends public {}`, public=number) + TS2349 for a reserved-word primitive-value CALL (`static()`, static=string) — both added to `checkNonConstructorExtendsInStatements`, which already recurses into function bodies with a per-scope `varDecls` map (function-body `var`s are UNBOUND by the binder, so this scope map is the only way to resolve `public`/`static`'s types via `inferSimpleVarType`). (6) the TS2552 `ublic`→'public' suggestion's TS2728 "declared here" related-info — the shared `findDeclarationRelatedInfo` (symbol-table based) misses function-scoped locals, so a new function-body-var scan `reservedWordDeclaredHere` provides the fallback. New CLAUDE.md gotcha. **NEXT: the remaining 21 are the known-hard set — parser-recovery JS-emit garble (es6Import/parseAssert/parseImport/reachability), torture engines (giant/expr/intTypeCheck/decoratorsOnComputedProperties/regularExpressionScanning×2), print-time temp-naming (staticFieldWithInterfaceContext), DOM-massive (mappedTypeRecursiveInference), TS-1 two-pass (parseErrorIncorrectReturnToken/multiLine/jsFileCompilation/constructorWithIncompleteTypeAnnotation), BOM (emitBOM). No bounded single-session flip remains after this; the parser-recovery `parseDelimitedList`/`isInSomeParsingContext` machinery (unblocks 3-4 garbles) is the highest-leverage next blocker.**_ _[Round 366 trimmed to STATUS-HISTORY.md.]_ _[Round 365 trimmed to STATUS-HISTORY.md.]_ _[Round 364 trimmed to STATUS-HISTORY.md.]_ _[Round 363 trimmed to STATUS-HISTORY.md.]_ _[Round 362 trimmed to STATUS-HISTORY.md.]_ _[Round 361 trimmed to STATUS-HISTORY.md.]_ _[Round 360 trimmed to STATUS-HISTORY.md.]_ _[Round 359 trimmed to STATUS-HISTORY.md.]_ _[Round 358 trimmed to STATUS-HISTORY.md.]_ _[Round 357 trimmed to STATUS-HISTORY.md.]_ _[Round 356 trimmed to STATUS-HISTORY.md.]_ _[Round 355 trimmed to STATUS-HISTORY.md.]_ _[Round 354 trimmed to STATUS-HISTORY.md.]_ _[Round 353 trimmed to STATUS-HISTORY.md.]_ _[Round 352 trimmed to STATUS-HISTORY.md.]_ _[Round 351 trimmed to STATUS-HISTORY.md.]_ _[Round 350 trimmed to STATUS-HISTORY.md.]_ _[Round 349 trimmed to STATUS-HISTORY.md.]_ _[Round 348 trimmed to STATUS-HISTORY.md.]_ _[Round 347 trimmed to STATUS-HISTORY.md.]_ _[Round 346 trimmed to STATUS-HISTORY.md.]_ _[Round 345 trimmed to STATUS-HISTORY.md.]_ _[Round 344 trimmed to STATUS-HISTORY.md.]_ _[Round 343 trimmed to STATUS-HISTORY.md.]_ _[Round 342 trimmed to STATUS-HISTORY.md.]_ _[Round 341 trimmed to STATUS-HISTORY.md.]_ _[Round 340 trimmed to STATUS-HISTORY.md.]_ _[Round 339 trimmed to STATUS-HISTORY.md.]_ _[Round 338 trimmed to STATUS-HISTORY.md.]_ _[Round 337 trimmed to STATUS-HISTORY.md.]_ _[Round 336 trimmed to STATUS-HISTORY.md.]_ _[Round 335 trimmed to STATUS-HISTORY.md.]_ _[Round 334 trimmed to STATUS-HISTORY.md.]_ _[Round 333 trimmed to STATUS-HISTORY.md.]_ _[Round 332 trimmed to STATUS-HISTORY.md.]_ _[Round 331 trimmed to STATUS-HISTORY.md.]_ _[Round 330 trimmed to STATUS-HISTORY.md.]_ _[Round 330 detail trimmed — full note in git history.]_ _[Round 329 trimmed to STATUS-HISTORY.md.]_ _[Round 328 trimmed to STATUS-HISTORY.md.]_ _[Round 327 trimmed to STATUS-HISTORY.md.]_ _[Round 326 trimmed to STATUS-HISTORY.md.]_ _[Round 325 trimmed to STATUS-HISTORY.md.]_ _[Round 324 trimmed to STATUS-HISTORY.md.]_ _[Round 323 trimmed to STATUS-HISTORY.md.]_ _[Round 322 trimmed to STATUS-HISTORY.md.]_ _[Round 321 trimmed to STATUS-HISTORY.md.]_ _[Round 320 trimmed to STATUS-HISTORY.md.]_ _[Round 319 trimmed to STATUS-HISTORY.md.]_ _[Round 318 trimmed to STATUS-HISTORY.md.]_ _[Round 317 trimmed to STATUS-HISTORY.md.]_ _[Round 316 trimmed to STATUS-HISTORY.md.]_ _[Round 315 trimmed to STATUS-HISTORY.md.]_ _[Round 314 trimmed to STATUS-HISTORY.md.]_ _Round 313 (2026-06-24, max-compute autonomous campaign): **+1 (165→164 failing), `importExportInternalComments`, ZERO regressions (exact failing-set diff).** The direct follow-on to round 312's element-access internal comments — same proven mechanism, applied across ALL import/export statement shapes (`export default Array`, `import D, { A, B as C } from`, `import * as foo from`, `export { A, B as C } from`, `export * from`), ~26 inter-token comment positions. Design: a SINGLE ordered slot list `internalComments: List<List<Comment>?>?` per top-level statement (ImportDeclaration/ExportDeclaration/ExportAssignment), captured sequentially by a parser member accumulator `ieSlots`/`captureIeSlot()` (appended in the nested clause/named-import/specifier parse fns too, AFTER each token-consume) and consumed sequentially by the emitter via `beginIeSlots`/`emitIeSlot()` — so the emit traversal order must exactly mirror the parse capture order. Reuses round 312's `emitElementAccessInternalComments` spacing helper. Additive (slots default null → inert) + corpus-unique → FP-safe. Two load-bearing traps handled: the `import D from` (no-bindings) double-capture (gate the after-name slot on a following comma) and the type-only-filtering misalignment (inert because internalComments is non-null only for this one test). New CLAUDE.md gotcha. **METHOD: re-mining the skip-logged output-diff pool (round 312 had flagged this as a known larger fix) — the comment-emit vein from rounds 311-313 is now drained (the remaining `unusedLocalsAndParameters`/`jsFileCompilationTypeArgumentSyntaxOfCall` couple comment/checker work with hot-path PARSER recovery; AMD/symlink are tsgo-irrelevant). NEXT: `elidedJSImport2` (we wrongly DROP `other.js`'s emit when a sibling `other.d.ts` exists — a multi-target allowJs/node16 emit bug, investigate the jsOutputMap/parsedSourceFiles inclusion of a `.js` with a `.d.ts` sibling) is the best remaining non-engine lead; else pivot to ENGINE blocker substeps (QUEUE: generic/contextual inference, mapped-type eval, recursive-conditional TS2589).**_ _[Round 312 trimmed to STATUS-HISTORY.md.]_ _[Round 311 trimmed to STATUS-HISTORY.md.]_ _[Round 310 trimmed to STATUS-HISTORY.md.]_ _[Round 309 trimmed to STATUS-HISTORY.md.]_ _[Round 308 trimmed to STATUS-HISTORY.md.]_ _[Round 307 trimmed to STATUS-HISTORY.md.]_ _[Round 306 trimmed to STATUS-HISTORY.md.]_ _[Round 305 trimmed to STATUS-HISTORY.md.]_ _[Round 304 trimmed to STATUS-HISTORY.md.]_ _[Round 303 trimmed to STATUS-HISTORY.md.]_ _[Round 302 trimmed to STATUS-HISTORY.md.]_ _[Round 301 trimmed to STATUS-HISTORY.md.]_ _[Round 300 trimmed to STATUS-HISTORY.md.]_ _[Round 299 trimmed to STATUS-HISTORY.md.]_ _[Round 298 trimmed to STATUS-HISTORY.md.]_ _[Round 297 trimmed to STATUS-HISTORY.md.]_ _[Round 296 trimmed to STATUS-HISTORY.md.]_ _[Round 295 trimmed to STATUS-HISTORY.md.]_ _[Round 294 trimmed to STATUS-HISTORY.md.]_ _[Round 293 trimmed to STATUS-HISTORY.md.]_ _[Round 292 trimmed to STATUS-HISTORY.md.]_ _[Round 291 trimmed to STATUS-HISTORY.md.]_ _[Round 290 trimmed to STATUS-HISTORY.md.]_ _[Round 289 trimmed to STATUS-HISTORY.md.]_ _[Round 288 trimmed to STATUS-HISTORY.md.]_ _[Round 287 trimmed to STATUS-HISTORY.md.]_ _[Round 286 trimmed to STATUS-HISTORY.md.]_ _[Round 285 trimmed to STATUS-HISTORY.md.]_ _[Round 284 trimmed to STATUS-HISTORY.md.]_ _[Round 283 trimmed to STATUS-HISTORY.md.]_ _[Round 282 trimmed to STATUS-HISTORY.md.]_ _[Round 281 trimmed to STATUS-HISTORY.md.]_ _[Round 280 trimmed to STATUS-HISTORY.md.]_ _[Round 279 trimmed to STATUS-HISTORY.md.]_ _[Round 278 trimmed to STATUS-HISTORY.md.]_ _[Round 277 trimmed to STATUS-HISTORY.md.]_ _[Round 276 trimmed to STATUS-HISTORY.md.]_ _[Round 275 trimmed to STATUS-HISTORY.md.]_ _[Round 274 trimmed to STATUS-HISTORY.md.]_ _[Round 273 trimmed to STATUS-HISTORY.md.]_ _[Round 272 trimmed to STATUS-HISTORY.md.]_ _[Round 271 trimmed to STATUS-HISTORY.md.]_ _[Round 270 trimmed to STATUS-HISTORY.md.]_ _[Round 269 trimmed to STATUS-HISTORY.md.]_ _[Round 268 trimmed to STATUS-HISTORY.md.]_ _[Round 267 trimmed to STATUS-HISTORY.md.]_ _[Round 266 trimmed to STATUS-HISTORY.md.]_ _[Round 265 trimmed to STATUS-HISTORY.md.]_ _[Round 264 trimmed to STATUS-HISTORY.md.]_ _[Round 263 trimmed to STATUS-HISTORY.md.]_ _[Round 262 trimmed to STATUS-HISTORY.md.]_ _[Round 261 trimmed to STATUS-HISTORY.md.]_ _[Round 260 trimmed to STATUS-HISTORY.md.]_ _[Round 259 trimmed to STATUS-HISTORY.md.]_ _[Round 258 trimmed to STATUS-HISTORY.md.]_ _[Round 257 trimmed to STATUS-HISTORY.md.]_ _[Round 256 trimmed to STATUS-HISTORY.md.]_ _[Round 255 trimmed to STATUS-HISTORY.md.]_ _[Rounds 253–254 trimmed to STATUS-HISTORY.md.]_ _[Round 252 trimmed to STATUS-HISTORY.md.]_ _[Round 251 trimmed to STATUS-HISTORY.md.]_ _[Rounds 249–250 trimmed to STATUS-HISTORY.md.]_ _[Round  trimmed to STATUS-HISTORY.md.]_ _[Round  trimmed to STATUS-HISTORY.md.]_ _[Round 246 trimmed to STATUS-HISTORY.md.]_ _[Round 243 trimmed to STATUS-HISTORY.md.]_ _[Round 240 trimmed to STATUS-HISTORY.md.]_ _[Round 239 trimmed to STATUS-HISTORY.md.]_ _[Round 238 trimmed to STATUS-HISTORY.md.]_ _[Round 237 trimmed to STATUS-HISTORY.md.]_ _[Round 236 trimmed to STATUS-HISTORY.md.]_ _[Round 235 trimmed to STATUS-HISTORY.md.]_ _[Round 234 trimmed to STATUS-HISTORY.md.]_ _[Round 233 trimmed to STATUS-HISTORY.md.]_ _[Round 232 trimmed to STATUS-HISTORY.md.]_ _[Round 231 trimmed to STATUS-HISTORY.md.]_ _[Round 230 trimmed to STATUS-HISTORY.md.]_ _[Round 229 trimmed to STATUS-HISTORY.md.]_ _[Round 228 trimmed to STATUS-HISTORY.md.]_ _[Round 227 trimmed to STATUS-HISTORY.md.]_ _[Round 226 trimmed to STATUS-HISTORY.md.]_ _[Round 225 trimmed to STATUS-HISTORY.md.]_ _[Round 224 trimmed to STATUS-HISTORY.md.]_ _[Round 223 trimmed to STATUS-HISTORY.md.]_ _[Round 222 trimmed to STATUS-HISTORY.md.]_ _[Round 221 trimmed to STATUS-HISTORY.md.]_ _[Round 220 trimmed to STATUS-HISTORY.md.]_ _[Round 219 trimmed to STATUS-HISTORY.md.]_ _[Round 218 trimmed to STATUS-HISTORY.md.]_ _[Round 217 trimmed to STATUS-HISTORY.md.]_ _[Round 216 trimmed to STATUS-HISTORY.md.]_ _[Round 215 trimmed to STATUS-HISTORY.md.]_ _[Round 214 trimmed to STATUS-HISTORY.md.]_ _[Round 213 trimmed to STATUS-HISTORY.md.]_ _[Round 212 trimmed to STATUS-HISTORY.md.]_ _[Round 211 trimmed to STATUS-HISTORY.md.]_ _[Round 209 trimmed to STATUS-HISTORY.md.]_ _[Round 208 trimmed to STATUS-HISTORY.md.]_ _[Round 207 trimmed to STATUS-HISTORY.md.]_ _[Round 206 trimmed to STATUS-HISTORY.md.]_ _[Round 205 trimmed to STATUS-HISTORY.md.]_ _[Round 204 trimmed to STATUS-HISTORY.md.]_ _[Round 203 trimmed to STATUS-HISTORY.md.]_ _[Round 202 trimmed to STATUS-HISTORY.md.]_ _[Round 201 trimmed to STATUS-HISTORY.md.]_ _[Round 200 trimmed to STATUS-HISTORY.md.]_ _[Round 199 trimmed to STATUS-HISTORY.md.]_ _[Round 198 trimmed to STATUS-HISTORY.md.]_ _[Round 198 B489-only detail superseded above.]_ _[Round 197 trimmed to STATUS-HISTORY.md.]_ _[Round 196 trimmed to STATUS-HISTORY.md.]_ _[Round 195 trimmed to STATUS-HISTORY.md.]_ _[Round 194 trimmed to STATUS-HISTORY.md.]_ _[Round 193 trimmed to STATUS-HISTORY.md.]_ _[Round 192 trimmed to STATUS-HISTORY.md.]_ _[Round 191 trimmed to STATUS-HISTORY.md.]_ _[Round 190 trimmed to STATUS-HISTORY.md.]_ _[Round 189 trimmed to STATUS-HISTORY.md.]_ _[Round 188 trimmed to STATUS-HISTORY.md.]_ _[Round 187 trimmed to STATUS-HISTORY.md.]_ _[Round 186 trimmed to STATUS-HISTORY.md.]_ _[Round 185 trimmed to STATUS-HISTORY.md.]_ _[Round 184 trimmed to STATUS-HISTORY.md.]_ _[Round 183 trimmed to STATUS-HISTORY.md.]_ _[Round 181 trimmed to STATUS-HISTORY.md.]_ _[Round 179 trimmed to STATUS-HISTORY.md.]_ _[Round 178 trimmed to STATUS-HISTORY.md.]_ _[Round 177 trimmed to STATUS-HISTORY.md.]_ _[Round 175 trimmed to STATUS-HISTORY.md.]_ _[Round 173 trimmed to STATUS-HISTORY.md.]_ _[Round 172 trimmed to STATUS-HISTORY.md.]_ _[Round 171 trimmed to STATUS-HISTORY.md.]_ _[Round 170 trimmed to STATUS-HISTORY.md.]_ _[Round 169 trimmed to STATUS-HISTORY.md.]_ _[Round 167 trimmed to STATUS-HISTORY.md.]_ _[Round 166 trimmed to STATUS-HISTORY.md.]_ _[Round 165 trimmed to STATUS-HISTORY.md.]_ _[Round 164 trimmed to STATUS-HISTORY.md.]_ _[Round 161 trimmed to STATUS-HISTORY.md.]_ _[Round 159 trimmed to STATUS-HISTORY.md.]_ _[Round 158 trimmed to STATUS-HISTORY.md.]_ _[Round 157 trimmed to STATUS-HISTORY.md.]_ _[Rounds 151, 156 trimmed to STATUS-HISTORY.md.]_ 
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
