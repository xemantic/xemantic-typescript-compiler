# Status

**Phase 4 — Checker buildout.** 9,797 / 10,086 tests passing (~97.1%, 289 failing). _Round 220 (2026-06-21, same multi-iteration session, addressing blockers): **+1 net (290→289 failing, 9796→9797 passing), B522 (`iterableWithNeverAsUnionMember` target=esnext), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** TS2488 "Type 'X' must have a '[Symbol.iterator]()' method" for array-destructuring (`const [x] = v`) of a value whose declared type reduces to `never` — an INTERSECTION of object types with a shared property at incompatible LITERAL types (`{a:"foo"} & {a:"bar"}` → never), a UNION whose members all so reduce, or a TYPE PARAMETER constrained by such (display = the TP name). The relation engine doesn't model intersection-→-never reduction, so we emit nothing today. New AST walker `checkIntersectionNeverArrayDestructure` (top-level + function-body params/TPs); `classifyNeverDestructure` returns "never"/TP-name; `isNeverIntersectionType` = a shared prop with ≥2 DISTINCT literal keys (provably-empty intersection → FP-safe). GATED `target >= ES2015 || downlevelIteration` (ES3/ES5 destructuring uses array-likeness, not Symbol.iterator → tsc emits nothing there; the es5 variant's baseline is TS5107-only and was already passing). New CLAUDE.md gotcha._ _Round 219 (2026-06-21, same multi-iteration session, addressing blockers): **+1 net (291→290 failing, 9795→9796 passing), B521 (`multipleBaseInterfaesWithIncompatibleProperties2` exactoptional=true), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** TS2320 "Interface cannot simultaneously extend types" for incompatible OPTIONAL properties under `exactOptionalPropertyTypes`. `http.AgentOptions extends Partial<TcpSocketConnectOpts>` (→ `port?: number`) and `tls.ConnectionOptions` (`port?: number | undefined`) conflict ONLY under EOPT (where `port?: number` has type exactly `number`, not `number | undefined`). Two EOPT-GATED changes in `checkInterfaceMultiBaseConflicts` (non-EOPT byte-identical): (1) `collectBase` materializes a `Partial<X>` base into X's members ALL forced optional (`addBaseMembers(forceOptional=true)`; resolved the Partial typeArg via `baseSymbol.parent?.exports` for namespace-scoped `X`); (2) the both-optional property-type comparison uses IDENTITY (bidirectional assignability) instead of the neither-direction check — `number` vs `number | undefined` is assignable one way but NOT identical → TS2320. FP firewall = the EOPT gate. New CLAUDE.md gotcha._ _Round 218 (2026-06-21, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+1 net (292→291 failing, 9794→9795 passing), B520 (`reuseTypeAnnotationImportTypeInGlobalThisTypeArgument`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** Re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; NONE fresh=7 all deep type-engine; OUTPUT fresh=1 systemModule17 tsgo-irrelevant; over_emit/pure_missing/pure_fp all multi-piece/blocked). **B520** — TS2304 for unresolved SIMPLE-IDENTIFIER type-args inside a JSDoc generic `@typedef`/`@property` type (`@typedef {Record<Keyword, ParamValueTyped>}` in a checkJs file → `Keyword`/`ParamValueTyped` both unresolved). TWO coordinated changes: (1) extended the B423 walker `checkJsDocTypeNameResolution` with a generic-typedef regex (`Name<arg,arg,...>`, head resolved via `KNOWN_GENERIC_BUILTINS`, each top-level bare-id arg checked against `known`); corpus-exhaustively FP-safe (the ONLY checkJs file with a generic in a `@typedef`/`@property` type is the target); (2) the walker was gated behind `!declarationOnly` but this test runs in `@emitDeclarationOnly` mode — added `checkJsDocTypeNameResolution()` to the declarationOnly block (it is a self-contained name-resolution walker, same category as `checkUnresolvedNames` which already runs there). NEXT: contained pool remains deep — B481 generic-inference (dominant), B482 weak-type, B486 override-chain, recursiveConditionalCrash4 (instantiateType-depth + TS2589)._ _Round 217 (2026-06-21, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+1 net (293→292 failing, 9793→9794 passing), B519 (`circularOptionalityRemoval`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** A parameter whose DEFAULT initializer references its OWN name: tsc emits TS2372 PER reference (we emitted only the FIRST — `findParamSelfRef`+`emitParamInitForbidden` dedups by LINE, collapsing two same-line refs) plus the "optionality-removal" companions TS2502 (param's type resolution is circular through the default) + TS18048 (an arith/relational-operand self-ref is possibly undefined). New `collectParamSelfRefs` (ALL refs, each tagged arith-operand) + `emitDiagAtPos` (position-deduped). **FP firewall (calibrated against the OTHER 9 param-self-ref baselines — only `circularOptionalityRemoval` has TS2502/TS18048): TS2502/TS18048 gated to `paramTypeIncludesUndefined` (union member `T|undefined` OR optional `?`) AND `strictNullChecks`** — `selfReferencesInFunctionParameters`/`asyncArrowFunction3`/`parameterInitializersForwardReferencing1` (plain `number`/`string`/inferred types) keep TS2372-only and stay green. TS2502 squiggles the WHOLE parameter (`paramDefaultRightmostEnd`; `expressionTrueEnd` lacks a ConditionalExpression branch). `ARITH_REL_OPERATORS` in the COMPANION object (an instance `val` after `init` is null during the check pipeline — the init-order gotcha that bit BOTH B518 and B519). New CLAUDE.md gotcha. NEXT: contained pool remains deep (B481 generic-inference, B482 weak-type, B486 override-chain, recursiveConditionalCrash4 instantiateType-depth)._ _Round 216 (2026-06-21, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+1 net (294→293 failing, 9792→9793 passing), B518 (`propertiesAndIndexers`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** Re-confirmed the contained pool exhausted ALL ways (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; NONE fresh=7 all deep type-engine; OUTPUT fresh=1 systemModule17 tsgo-irrelevant; pure_fp=4 all blocked; over_emit/pure_missing all multi-piece) + a missing-code histogram (sole-missing pool = TS2339×2/TS2345×2/TS2589×1, all deep generic inference) + a parser-cluster scan (0 pure-parser tests). Hand-triaged ~12 deep candidates (recursiveConditionalCrash4 instantiateType-depth, implicitAnyFromCircularInference circular-inference, mutuallyRecursiveCallbacks recursive-alias display, discriminateWithOptionalProperty4 discriminated-union relation, destructuringTuple reduce-overload, thislessFunctionsNotContextSensitive1 const-TP+conditional, noImplicitAnyLoopCrash evolving-any). Per the anti-loop rule, committed to the most self-contained blocker: index-constraints (no generics/flow/cross-file — the well-defined tsc `checkIndexConstraints` algorithm). **B518 (+1, `propertiesAndIndexers`)** — TS2411/TS2413 for INHERITED / merged-block / cross-base property-vs-index-signature conflicts the per-block `checkIndexSigInStatement` (own-block props only) structurally cannot reach. New walker `checkFullIndexConstraints`: computes the FULL merged+inherited member/index-sig sets per interface/class and emits the gaps, **DEDUPING against the per-block emits by (code, start, message)** (never double-emits; message must match EXACTLY — string-literal property names render QUOTED `'"-Infinity"'` with span incl. quotes, which fixed the lone transient regression on `propertiesAndIndexers2`). tsc errorNode rule: OWN prop → at the prop; INHERITED prop → at the OWN index sig (if any, span incl. trailing `;`) else at the TYPE NAME; the "both-inherited" conflict reports at THIS type only when no single direct base sees BOTH prop+sig (else the base reports it — `collectVisibility` dedup). FP firewall (runs for every inheriting/merged type): PRIMITIVE index value types only (a method → fresh `Type.Object()` marker), non-generic types only, SINGLE-FILE base resolution via a `byName` map (lib/cross-file bases → null → skipped), numeric-name applicability via `isCanonicalNumericPropertyName`. KEY LESSON (the 2 transient bugs): (1) Kotlin init-order — a `private val functionTypeMarker = Type.Object()` field declared AFTER the `init` block is NULL during init (the documented gotcha) → NPE; inline `Type.Object()` at the call site instead; (2) string-literal property-name display must be QUOTED to dedup against the per-block code. New CLAUDE.md gotcha. NEXT: contained pool remains deep (B481 generic-inference, B482 weak-type, B486 override-chain, recursiveConditionalCrash4 instantiateType-depth)._ _[Round 215 trimmed to STATUS-HISTORY.md.]_ _[Round 214 trimmed to STATUS-HISTORY.md.]_ _[Round 213 trimmed to STATUS-HISTORY.md.]_ _[Round 212 trimmed to STATUS-HISTORY.md.]_ _[Round 211 trimmed to STATUS-HISTORY.md.]_ _[Round 209 trimmed to STATUS-HISTORY.md.]_ _[Round 208 trimmed to STATUS-HISTORY.md.]_ _[Round 207 trimmed to STATUS-HISTORY.md.]_ _[Round 206 trimmed to STATUS-HISTORY.md.]_ _[Round 205 trimmed to STATUS-HISTORY.md.]_ _[Round 204 trimmed to STATUS-HISTORY.md.]_ _[Round 203 trimmed to STATUS-HISTORY.md.]_ _[Round 202 trimmed to STATUS-HISTORY.md.]_ _[Round 201 trimmed to STATUS-HISTORY.md.]_ _[Round 200 trimmed to STATUS-HISTORY.md.]_ _[Round 199 trimmed to STATUS-HISTORY.md.]_ _[Round 198 trimmed to STATUS-HISTORY.md.]_ _[Round 198 B489-only detail superseded above.]_ _[Round 197 trimmed to STATUS-HISTORY.md.]_ _[Round 196 trimmed to STATUS-HISTORY.md.]_ _[Round 195 trimmed to STATUS-HISTORY.md.]_ _[Round 194 trimmed to STATUS-HISTORY.md.]_ _[Round 193 trimmed to STATUS-HISTORY.md.]_ _[Round 192 trimmed to STATUS-HISTORY.md.]_ _[Round 191 trimmed to STATUS-HISTORY.md.]_ _[Round 190 trimmed to STATUS-HISTORY.md.]_ _[Round 189 trimmed to STATUS-HISTORY.md.]_ _[Round 188 trimmed to STATUS-HISTORY.md.]_ _[Round 187 trimmed to STATUS-HISTORY.md.]_ _[Round 186 trimmed to STATUS-HISTORY.md.]_ _[Round 185 trimmed to STATUS-HISTORY.md.]_ _[Round 184 trimmed to STATUS-HISTORY.md.]_ _[Round 183 trimmed to STATUS-HISTORY.md.]_ _[Round 181 trimmed to STATUS-HISTORY.md.]_ _[Round 179 trimmed to STATUS-HISTORY.md.]_ _[Round 178 trimmed to STATUS-HISTORY.md.]_ _[Round 177 trimmed to STATUS-HISTORY.md.]_ _[Round 175 trimmed to STATUS-HISTORY.md.]_ _[Round 173 trimmed to STATUS-HISTORY.md.]_ _[Round 172 trimmed to STATUS-HISTORY.md.]_ _[Round 171 trimmed to STATUS-HISTORY.md.]_ _[Round 170 trimmed to STATUS-HISTORY.md.]_ _[Round 169 trimmed to STATUS-HISTORY.md.]_ _[Round 167 trimmed to STATUS-HISTORY.md.]_ _[Round 166 trimmed to STATUS-HISTORY.md.]_ _[Round 165 trimmed to STATUS-HISTORY.md.]_ _[Round 164 trimmed to STATUS-HISTORY.md.]_ _[Round 161 trimmed to STATUS-HISTORY.md.]_ _[Round 159 trimmed to STATUS-HISTORY.md.]_ _[Round 158 trimmed to STATUS-HISTORY.md.]_ _[Round 157 trimmed to STATUS-HISTORY.md.]_ _[Rounds 151, 156 trimmed to STATUS-HISTORY.md.]_ 
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
