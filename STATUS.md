# Status

**Phase 4 — Checker buildout: COMPLETE. Phase 17 — self-compile the TypeScript compiler itself (re-scoped 2026-07-03; "any TypeScript project" is post-v1) — is the LIVE mission (see PLAN-PHASE-5.md; v1 exit = all 8 tsc-source profiles at zero FPs; dashboard: bench/*.tsv, self-compile diagnostics 13,245 → 936 (measured at round 430 — rounds 395–412 burned bounded non-M3 histogram-tail buckets + M3.4 flow-narrowing slices the round-394 "pool picked over" triage missed, all surfaced by bucketing the full `--listAll` output by normalized message shape: TS2499 multi-base-generic misparse, TS2440 type-only-barrel-merge, TS2344 constraint-chain + cross-file `typeParamInternCache` pos-collision, TS2693/TS2709 clodule + destructuring-shadow, TS2314 enum-member-vs-generic, TS2588 nested-shadow, TS2551 Object.setPrototypeOf, SetIterator/MapIterator lib gap, TS2774 local-shadows-function, TS1100 interface-property-name, TS7023 callback-arg self-ref, TS2365 arithmetic local-shadows-function, TS2362 branded-number, TS7053 enum-reverse-mapping, round 408's TS2349 callee flow-narrowing family, and round 409's `export *`-barrel / ESM-`.js` imported-guard flow narrowing (M3.4, TS2339 838 → 672), and round 410's TS2862 constraint-index-sig gate / assign-RHS type-guard narrowing / void-overload-return (2,443 → 2,433), and round 411's M3.4 enum-member discriminant narrowing (barrel-import-resolved, three families UpToDateStatus/TypeMapper/PrivateIdentifierInfo) + type-guard-narrows-a-member-DOWN (2,433 → 2,374, TS2339 672 → 614) + round 412's M3.4 single-type type-guard narrow-DOWN + TS18048 receiver-narrowing (2,374 → 2,373), and round 413's `export *` LEAF-EXPORT gate (barrel-imported `Debug.assert` & every barrel guard now resolves — the TRUE builder.ts blocker, NOT the round-412 depth red herring) + tsc-faithful linear flow-walk iteration + return-path narrowing (2,373 → 1,965, TS2339 614 → 237)), then rounds 414-417 burned more bounded/M3.4 buckets (TS2366 CFA fall-through, NonNull/&&-narrowing arithmetic, closure-loop narrowing, namespace-local `extends` bases: 1,965 → 1,902), and round 418 resolved NESTED type-guard functions (M1.12, 1,902 → 1,854, TS2339 237 → 189: tsc guards `isTupleType`/... are nested in `createTypeChecker` so the binder skips them and `resolveFlowCalleeDecl` missed them - the guard never narrowed, the single biggest TS2339 sub-family), and round 419 resolved properties on INTERSECTION union-members (M1.12, 1,854 → 1,808, TS2339 189 → 143: `getPropertyOfType`/`typeHasOwnProperty` bail on a `Type.Intersection` member so `PropertyAccessExpression | (ElementAccessExpression & …)` FP'd TS2339 on `.parent`; fold the constituents in property resolution + discriminant-narrowing annotation; 0 new FPs, self-compile time −17%), and round 420 resolved TYPE-ALIAS enum-member discriminants in narrowing (M1.12, 1,808 → 1,799, TS2339 143 → 134: a `.kind: <alias>` discriminant member survived a `switch (x.kind) { case }` because `enumMemberKeysOfTypeNode` handled only a direct `Enum.Member`, not a bare-Identifier alias; 0 new FPs), and round 422 burned FIVE bounded M3.4/overload families (1,799 → 1,756: TS2769 60 → 45 — overload arg-check helpers now flow-narrow reference args (mirroring B469) + a synthetic true|false union for guard-narrowed boolean args vs literal overload params; TS2339 134 → 117 — mixed enum + string-literal discriminant unions narrow on disjoint lit:s: keys (PrivateIdentifierInfo); TS18048 10 → 7 — an optional-chain discriminant access x?.kind === <non-nullish> proves the receiver non-nullish; TS2366 12 → 4 — union-.kind exhaustive switches terminate via requiredUnionDiscriminantKeys), and round 423 (a) drove TS2366 to ZERO (1,756 → 1,752: the switch RECEIVER is guard-narrowed + typed before the exhaustive-switch union walk — flow graph via the pass-dedicated implicitReturnFlowGraph, local-const-from-call typing, optional-discriminant @undefined keys, LiteralToken["kind"] indexed-access) and (b) fixed union-TARGET type guards end-to-end (1,752 → 1,708: `x is A | B` PARSED truncated so isCallOrNewExpression/isPropertyNameLiteral/isOptionalChain never narrowed; + aliased conditions `const isFrag = guard(x); if (!isFrag)` via a memoized flow back-walk; + kind-key disjointness beating the lenient relation; TS2339 117 → 104, TS2322 784 → 756) and (c) narrowed truthy optional-chain CALL conditions (1,708 → 1,707: `if (x.y?.size())` proves `x.y` non-nullish), and round 424 burned SEVEN more M3.4 flow-narrowing slices (1,707 → 1,662, TS2339 104 → 68, TS18048 5 → 1, TS2322 756 → 751: loop-entry union suppression with a STRUCTURAL wash gate, call-RHS return-annotation narrowing, closure/join/call-crossing aliased conditions, prefix-path receiver guards, asserts-with-inferred-TP via test-arg predicate inference, assignment-overwrite reset to the declaration/call-RHS resolved type, and the DebugTypeMapper this-narrowing slice (explicit-type-arg asserts re-type `this`; an overload-cluster name collision resolves to the unique TypePredicate-bearing decl) — every step by-site strictly removals), and round 425 found the TS2339 never-cluster ROOT CAUSES (1,662 → 1,600, TS2339 68 → 6: the round-411 enum-discriminant key space SPLIT across resolution paths — canonicalEnumSymbol at all four key-builder sites — plus per-candidate narrow-DOWN distribution in union-target guards/instanceof, aliased ===/switch discriminants, switch-default negative narrowing, typeof-object filtering, boolean-literal discriminant truthiness, destructuring-read narrowing, the loop-entry retry for the single-type suppression, and the positive-empty intersection fallback whose round-423 dead-end verdict was an ARTIFACT of the key split) — driving to 0, and round 426 replaced the B399 TS2563 per-file proxy with the faithful flow-walk depth trip + the asserts-callee call gate (1,600 → 1,593: TS2563 27 → 0; TS2454×20 pre-existing definite-assignment walker FPs unmasked), and round 427 burned that bucket (1,593 → 1,577: TS2454 20 → 4 via tsc's definiteness rules — logical assigns ??=/||=/&&= are DEFINITE so captured reads assume initialized, a !-asserted read assumes initialized, and the anywhere-scan's left-spine walk now applies the target rule per spine node), and round 428 took the first real M3.1 slice of the TS2322/TS2345 cores (1,577 → 1,186, −25%: nullable-union generic param inference + overloaded generic callees for tsc's `x = append(x, item)` idiom (TS2322 751 → 559), the TS2345 histogram top — explicit this-param binding, guarded optional-member args, numeric-enum→number (394 → 280) — the array-literal-vs-union-member string-layer rule (TS2322 → 501), and the body-local-shadows-function anyType registration (TS2345 → 261, TS2769 → 36)), and round 429 drove the TS2345 core 261 → 86 (1,186 → 1,000, −15.7%: call-types lexical shadowing — body-locals vs enclosing params, DESTRUCTURED params via the currentParamBindingNames side set in getTypeOfIdentifier (the round-428 "mini-repro does not reproduce" residue), arrow own-params; embedded String replace/replaceAll/search/split accept RegExp; optional-param union args + NonNull-asserted args; guard-narrowed interface/unknown Identifier args — the ~110-site dominant mechanism, never-param excluded for assertType<never> exhaustive-switch defaults; string-enum→string in isSimpleTypeRelatedTo resolving the round-410 Extension[][] deferral; rest-arg flow narrowing), and round 430 unlocked the `append`/`addRange` inference (936: the `T extends {}` constraint failed `string → {}` and killed every mapper — empty-object-target relation rule with TP-source excluded per genericPrototypeProperty3's pin; readonly-array anchors; TP-from-PREDICATE binding for the getFirstJSDocTag family); self-compile time ~110–150 s single-run band), and round 431 took the FIRST M3.2 slice — the TS7006 core 301 → 11 via contextual typing (nested-fn/lexical callee resolvability, assignment-RHS context from the LHS declared type under the B476 single-applicable-sig rule, ||/??-both/&&-comma-right propagation, intersection/Reference/extends receiver members) — plus engine return-checking in switch/try bodies behind the foreign-TP source gate (un-inferred generic call results bail instead of FP'ing; ~130 T[]/U|undefined/SearchResult<T> return sites cleared) and the TS2367 anchored-disjointness gate (936 → 551, −41%; TS2322 435 → 345, TS7019 4 → 0; by-code strictly shrinking at every landed step) — then extended the foreign-TP gate to the var-decl/assignment/property-write/conditional-return paths (551 → 482, −69 strictly removals; TS2322 → 276; a generic FUNCTION-VALUE source's sig-own TPs stay checkable per 5 corpus pins the suite gate caught), and round 435 took SEVEN more M3.1/M3.2 slices at post-perf-arc iteration speed (482 → 373, −22.6%: generator TReturn returns, fresh-objlit literal props via the freshObjLitRange relation retry, TP-literal-constraint args, the union-decomposition-transparent relation re-entry gate resolving the NodeArray-covariance family ×23, bare-`new` contextual instantiation, the foreign-TP gate on assignment TARGETS (visitor family ×15+), nullish alias-union returns; TS2322 276 → 184, TS7006 11 → 1, TS2345 86 → 79; full-dashboard baseline: compiler 482→373 / tsc-cli 484 / jsTyping 480 / deprecatedCompat 481 / typingsInstallerCore 489 / services 1,603 / server 1,894 / harness 2,193 at 29–41 s wall each), and round 436 took SEVEN more M3.1/M3.2/M3.4 slices (373 → 294, −21.2%: TP-carrying callback-return param skip (forEachEntry family ×14), destructured-LOCAL shadowing (semver/checker + 12 transformer sites), literal-return syntactic union membership (the string-fallback re-widening trap — `return false` vs `Tag | false` ×7, incl. the completely UNPINNED `return "a"` vs `"a" | "b"` shape), constraint-filtered explicit-type-arg overload selection ('true' vs 'false' ×6), overload-helper optional-param union-arg + foreign-TP rules (union-receiver TS2769 ×13), bare-string switch-case narrowing (semver ×3), guard-gated ternary return arms with the tri-state all-leaves-verified early return (×19); TS2322 184 → 158, TS2345 79 → 47, TS2769 30 → 9; round-436 dashboard baseline: compiler 373 / tsc-cli 375 / jsTyping 371 / deprecatedCompat 372 / typingsInstallerCore 380 / services 1,476 / server 1,769 / harness 2,062 at 27–41 s wall), then round 438 (2026-07-07) took FIVE more M3.1/M3.4 narrowing/relation slices (294 → 244, −17%: an empty-object `{}` union-member return-path early return; UNION-target extension of the assignment-RHS AND return-path type-guard narrowing gates (`currentSourceFile = node` / `return node` vs `SourceFile | undefined`); PROPERTY-ACCESS args in the call-arg guard-narrow-DOWN branch (`getExports(node.left)`); and nullish-strip-gated narrowing of object-literal property VALUES (`{ moduleSpecifiers: specs }` where `specs = append(specs, x)`); TS2322 158 → 116, TS2345 47 → 39), then round 439 (2026-07-07) took THREE predicate-overload / arg-narrow-DOWN slices (244 → 228, −16 strictly removals: findAncestor-style type-guard-callback overload return inference + a companion NonNull concrete-union strip, the overload arg-check non-union narrow-DOWN, and its `unknown`-via-typeof extension; TS2769 9 → 1).** Corpus suite green as a permanent regression gate: 9,510 tests / 0 failing / 3 skipped (generated tsgo-relevant corpus + local corner-case tests) — **the full tsgo-relevant TypeScript compiler test suite passes.** _Round 443 (2026-07-08, Phase 17 — module-augmentation family + the module-file-local TYPE-alias leak (Blocker #3)): FOUR bounded fixes, all suppression-only. Compiler profile UNCHANGED (190) but the fixes GENERALIZE hugely: services 591 → 498 (−93), server 887 → 733 (−154), harness 1,118 → 989 (−129); suite 9,504 → 9,512 (+8 local across 3 test files, 0 regressions); 4 fix commits; services diffed via --listAll as strictly by-position removals. (1) TS2664 10 → 0: `resolveModuleSpecifierRelativeJsAware` — a `declare module "../compiler/types.js"` augmentation resolves `.js` → the `.ts` sibling (the base resolver deliberately avoids `.js` per the TS2459 gotcha, so the augmentation target never resolved); purely additive → only SUPPRESSES a false 'cannot be found'. (2) TS2564 17 → 0: a class property whose declared type INCLUDES `undefined` needs no definite assignment (tsc's strictPropertyInitialization `getFalsyFlags(type) & TypeFlags.Undefined`); `checkClassPropertyInit` missed the `| undefined` case, so `SourceFileObject`'s `nameTable: Map<...> | undefined` + siblings FP'd; reuses the existing `typeIncludesUndefined`. (3) TS2304 24 → 2: `buildNamespaceScope` gained a StringLiteralNode branch — a `declare module "X"` augmentation body sees the AUGMENTED module's exports by bare name (Node/NodeArray/SymbolFlags/TypeChecker/__String — compiler/types.ts exports NOT imported into services/types.ts); the 2 remaining are NodeJS `global` (env-legit, offline). INVESTIGATED & REVERTED (dashboard no-op): TS7006 array-element contextual typing — a correct M3.2 enabler (3 passing tests) but the real inferFromUsage.ts `Priority[]` case is B83.5-blocked (`interface Priority` nested in a function → unbound → `Priority[]` resolves to any); land it with nested-type resolution. (4) TS2339 44 → 0 (SourceFileLike, Blocker #3): a module-file-local `type X = A | B` alias leaks into globals and shadows the global `interface X` in OTHER files (TYPE-space analog of round 442's variable leak). importTracker.ts's non-exported `type SourceFileLike = SourceFile | AmbientModuleDeclaration` won the last-wins merge over compiler/types.ts's `interface SourceFileLike`, so a `sourceFile: SourceFileLike` param resolved to the bogus union (AmbientModuleDeclaration has no `.text`); pinned via an instrumented probe (whole-program-only — minimal repros don't reproduce). Built `conflatedTypeAliasFiles`; checkMemberAccessMissing bails a union-receiver TS2339 when the receiver displays as a conflated name in a NON-alias file (own-file still fires). NEXT (services @ 498, all deep): TS2322×235 / TS2339×85 (residual conflations — Type×20/Info×12) / TS2345×54 M3 relation, TS2416×11, TS2353×10, TS2740×9/TS2739×8._ 

_Round 456 (2026-07-09, Phase 17 — Array/ReadonlyArray find type-guard overload + NonNull-identifier undefined-strip + branded-intersection flow-narrowing + a build-warning cleanup; ONE broad relation rule investigated & reverted. Dashboard: compiler 132 → 125 (−7, TS2322 56 → 49); services 230 → 221 (−9, all generalize cleanly, no new FP); suite 9,666 → 9,678 (+12 local, 0 regressions); 4 commits (860046fc find / 042e2551 nonnull / 3ad1ae4d branded-intersection / 54e644db warning). (1) FIND TYPE-GUARD OVERLOAD (860046fc, compiler/services −1): the embedded `Array<T>`/`ReadonlyArray<T>` `find`/`findLast` had only the `(predicate: … => unknown): T | undefined` signature, so `arr.find(isFoo)` returned the base element type; added the type-predicate overload `find<S extends T>(predicate: … => value is S, thisArg?): S | undefined` (mirrors round 455's filter/every), so `tryInferPredicateOverloadReturn` (round 439) refines the result — cleared utilities.ts:8276 `getClassLikeDeclarationOfSymbol` (`symbol.declarations?.find(isClassLike)`). Adding an OVERLOAD to an EXISTING method (not a new member name) → no "and N more" count shift. (2) NONNULL-IDENTIFIER STRIP (042e2551, compiler −4/services −6): `getTypeOfExpression(NonNullExpression)` kept the union for a bare-identifier `x!` (only `undefined!`→never and `<call>()!`-all-concrete stripped, round 439), so `writer = _writer!` (emitter.ts `EmitTextWriter | undefined`) and `currentFlow = preSwitchCaseFlow!` (binder.ts `FlowNode | undefined`) FP-fired `X | undefined ⊄ X`. Extended the strip to `expr.expression is Identifier` with the SAME all-concrete gate (`types.none { unresolved TP }`); property-access `.x!` (object-literal member-precision gap) and TP-carrying operands stay deferred per the round-407 note (a bare Identifier can't expose the `.x!` gap). (3) BRANDED-INTERSECTION NARROWING (3ad1ae4d, compiler −2/services −2): the assignment-RHS (round 410/438) and return-path (round 413/438) flow-narrowing gates covered Interface/Reference/Object/Union targets but excluded `Type.Intersection`, so a reference narrowed non-nullish (`if (!x) return` / `if (x === undefined) { x = … }`) assigned/returned against a BRANDED intersection (`Path = string & {__pathBrand}`, `IncrementalBuildInfoFileId = number & {__…Brand}`) kept its `X | undefined` type → FP; added `Type.Intersection` to both gates (same relation-passes FP-safety); cleared resolutionCache.ts:1518 + builder.ts:1394. (4) WARNING-CLEAN (54e644db): removed a redundant `as TypeOfExpression` cast (round 451 typeof-switch, Checker.kt:79547) Kotlin flagged "No cast needed". (5) INVESTIGATED & REVERTED — the broad relation-engine `source is Type.TypeParam` → relate-its-constraint rule (cycle-guarded for `T extends T`): MEASURED net-zero on BOTH compiler and services — it fixed exactly one assignment FP (utilities.ts:12338 `getSynthesizedDeepCloneWithReplacements<T extends Node>`) but INTRODUCED one via OVERLOAD SELECTION (a bare un-inferred `T extends string` from `getPathFromPathComponents` now matched `ensureTrailingDirectorySeparator`'s `(path: string)` overload instead of `(path: Path)` → FP `string ⊄ string & {__pathBrand}` at moduleNameResolver.ts:2933). The rule is SOUND but overload arg-matching shares checkTypeRelatedTo, so it perturbs signature selection wherever inference under-resolves a TypeParam arg; CLAUDE.md's standing warning against this broad rule confirmed. NEXT (compiler @ 125): TransformerFactory<T> generic-type-alias-of-fn relation (M3.3, whole-program); the TS2353 IterableIterator-heritage excess check (sourcemap.ts MappingsDecoder); the exhaustive-switch `assertNever(reason)` enum-union residual (programDiagnostics.ts, round 441 deep); the `OptionalChain.questionDotToken` Exclude<>-narrowing (es2020.ts); `string → __String` branding (whole-program)._

_Round 455 (2026-07-09, Phase 17 — global-shadow anyType + widenType tuple shape + ReadonlyArray filter guard + generic-identity-fn assignability: FOUR fixes, all GENERAL correctness that reproduce minimally. Dashboard: compiler 153 → 132 (−21, TS2322 77 → 56); the fixes are all in the shared checker/lib so they generalize to every profile; suite 9,654 → 9,666 (+12 local across 4 new test files, 0 regressions; lib + relation-engine changes → full blast-radius gate clean); 4 fix commits (4e94a4e1 shadow / b90d79f1 widen-tuple / 7d82a0aa filter / aada9e31 generic-fn). (1) BODY-LOCAL SHADOWS A GLOBAL FUNCTION (4e94a4e1, TS2322 −7): `applyBodyLocalShadowing` recorded an un-annotated top-level local shadowing an outer binding by REMOVING it from currentLocalTypes, so a value-position use (`return clone`) fell through getTypeOfIdentifier to the global — when that global is an exported fn merged into globals (core.ts `clone<T>(object: T): T`/`identity`) the reference resolved to the generic fn itself → FP `<T>(object: T) => T`. Now an un-annotated `inGlobals && !inLocal` shadow registers anyType (a concrete inferred local still wins, checked first; a file-level VAR shadow keeps round 351's remove-and-re-infer), AND applyBodyLocalShadowing descends into nested blocks (`applyNestedGlobalShadow`, anyType-only, GLOBAL collisions only) — cleared nodeFactory.ts cloneIdentifier/clonePrivateIdentifier, checker.ts:15679, expressionToTypeNode.ts:535 (round-454's unmasked gap), plus the builder.ts `create`/`map` and checker.ts `length` object-literal-factory shadows. (2) widenType PRESERVES TUPLE SHAPE (b90d79f1, TS2322 −8): widenType's Type.Object branch dropped `tupleElementTypes` and widened a tuple's `length` LITERAL (`2 → number`), so a widened `[SF, FR]` became a `{0,1,length:number}` object that failed to relate to a tuple target (`length: number ⊄ 2`) — tsc's `map(arr, x => [a, b])` idiom flowing through `concatenate(...)` into a `[A, B][]` variable (declarations.ts/builder.ts/destructuring.ts ×8). widenType now widens a tuple's ELEMENT types only + rebuilds via buildTupleFromTypes (length literal + tupleElementTypes + optionality preserved). (3) ReadonlyArray.filter/every TYPE-PREDICATE OVERLOAD (7d82a0aa, TS2322 −3): the embedded ReadonlyArray lacked the `filter<S extends T>(…): S[]` overload Array already had, so `roArr.filter(isFoo)` returned the base `T[]` (utilitiesPublic.ts `.filter(isJSDocParameterTag)` ×3); added it (and `every`), so tryInferPredicateOverloadReturn (round 439) infers S. (4) GENERIC-IDENTITY-FN → CONCRETE-FN ASSIGNABILITY (TS2322 −3): `signatureRelatedTo` pinned source type params from the target's params (17.10d, `identity<T>(x: T)` → T:=string) but did NOT substitute those pins into the source RETURN type, so `identity`'s return `T` FP-rejected against a concrete `(fileName: string) => string` target — core.ts:2378 `createGetCanonicalFileName`, sourcemap.ts:820 `identitySourceMapConsumer`, plus the construct-sig variant parser.ts:1737 (`new <TKind extends SyntaxKind>(…): Token<TKind>` → `new (…): Node`). The return type is now instantiated with the pins before the covariant compare (name fallback for the per-position TypeParam interning; correctness preserved — `identity` vs `(x: string) => number` still fails). NEXT (compiler @ 132): TransformerFactory<T> generic-type-alias-of-fn relation ×3 (M3.3); `string → __String` branding (whole-program); the exhaustive-switch `assertNever(reason)` enum-union residual (programDiagnostics.ts, round 441); TS2345 Expression→Identifier narrowing (whole-program-only, confirmed via minimal repro)._

_Round 454 (2026-07-09, Phase 17 — nested-function-shadow generalization + flow-narrowing + method-param bivariance: FIVE fixes. Dashboard: compiler 170 → 153 (−17); services 275 → 251 (−24, the fixes generalize); suite 9,642 → 9,654 (+12 local across 5 new test files, 0 regressions); 5 fix commits (0df21c9b nested-arrow / a0c0f3d8 cast / d4e93ead TS2722 / 3eb6c99b nested-checkFunctionBody / a505ce0a method-bivariance). (1) NESTED-FN SHADOW IN ARROW/FN-EXPR BODIES (0df21c9b): `shadowNestedFunctionNames` (anyType-bails a body-nested `function NAME` colliding with an outer/global binding, B83.5) ran only for FunctionDeclaration bodies in the call-types pass; now also for ArrowFunction/FunctionExpression bodies — tsc's program.ts nests `createDiagnosticForNodeArray(nodes, message)` inside the `runWithCancellationToken(() => {…})` arrow body, shadowing utilities.ts's exported 3-param version; a sibling-nested `walkArray`'s call FP'd TS2345 ×3. (2) CAST NARROWING (a0c0f3d8): `x = expr as T` narrows `x` by the cast target T — `rhsIsDefinitelyNonNullish` unwrapped the cast to its inner and classified that, so `Object.create(...) as NodeArray<Node>` (debug.ts) inside `if (!proto)` kept `proto` narrowed to `undefined` → TS2345; now a cast to a concrete non-nullish target (castTargetIsNonNullish) re-narrows to declared-minus-nullish. (3) TS2722 LOOP-ENTRY (d4e93ead): the optional-member-invoke suppression used the plain narrower which washes at a loop's FlowLoopLabel — a pre-loop guard `if (host.directoryExists && …) { for (…) { host.directoryExists(root) } }` (moduleNameResolver.ts) FP'd inside; propertyAccessNarrowedNonNull now retries with the loop-entry-following variant. (4) NESTED-FN SHADOW IN checkFunctionBody (3eb6c99b): the RETURN/arg-assignability pass now applies shadowNestedFunctionNames too — tsc's builderState.ts nests `create(forward, reverse, deleted): ManyToManyPathMap` shadowing the exported `create(newProgram: Program, …): BuilderState`, so `return create(…)` FP'd TS2740+TS2345; the pervasive `{ clear, create, … }` object-literal-factory pattern (shorthand → nested fn) was poisoned the same way (TS2322 89 → 78, ~13 cases across moduleNameResolver/sys/resolutionCache/emitter/nodeFactory). The more-correct fix-4 resolution UNMASKED two pre-existing latent gaps; (5) FIXED one of them: object-literal METHOD members now relate to an interface target BIVARIANTLY (a505ce0a — tsc's strictFunctionTypes never applies to methods; `propertiesRelatedTo` retries a failed member via methodSignaturesBivariantlyRelated when BOTH members are methods, suppression-only, a function-typed PROPERTY stays contravariant) — cleared checker.ts:6310 (`canReuseTypeNodeAnnotation(…, symbol: Symbol)` vs interface `symbol: Symbol | undefined`), a general correctness win. The remaining unmasked gap is expressionToTypeNode.ts:535 (an un-annotated block-scoped `const clone` shadowing global `clone<T>(object: T): T` resolves to the global in the return check — applyBodyLocalShadowing does not descend into if-blocks). NEXT (compiler @ 153): TS2322×77 deep-M3 relation (generic-fn-identity `<T>(object: T) => T`, `.map`-returns-tuple-array M3.2, whole-program conflation), the expressionToTypeNode.ts:535 unmasked gap (applyBodyLocalShadowing if-block descent), the TS2353 sourcemap getter/method excess + utilities.ts mapped-type-key eval (M3.3), TS2349 `(x || (x = [])).push()` inline-property-receiver, factory/utilities.ts:713 tsc-5.5 inferred type predicates, the moduleNameResolver.ts:2823 `.value` union-return, checker.ts:21170 restrictiveInstantiation overload-selection TS18048._

_Round 453 (2026-07-09, Phase 17 — bounded FP burn-down: THREE fixes, all GENERAL checker correctness that reproduce minimally. Dashboard: compiler 176 → 170 (−6), tsc-cli 179 → 172, jsTyping 176 → 169, deprecatedCompat 179 → 172, typingsInstallerCore 176 → 169, services 280 → 275 (−5), server 485 → 479 (−6), harness 701 → 695 (−6); suite 9,628 → 9,642 (+14 local across 3 new test files, 0 regressions); 3 fix commits (9f53366d arith / 1246458a optional-undefined / df18ce2f param-shadow). (1) ARITHMETIC REASSIGNMENT/GUARD NARROWING (9f53366d): a `number | undefined` reference proven non-nullish by a cross-statement reassignment (`length = end - start; length - 5` — parser.ts) or an enclosing guard (`if (m !== undefined) indent += m`) was still rejected as an arithmetic operand. `arithOperandType` now runs a SCOPED flow-narrowing walk (currentFlowGraph set ONLY around the walk — pass-wide regressed 78 tests) for an Identifier / PropertyAccess operand, using the narrowed type only when it proves non-nullish; `rhsIsDefinitelyNonNullish` classifies arithmetic/bitwise/relational/`+` BinaryExpressions and enum-member value accesses (incl. imported enums via resolveAlias) as non-nullish. Barrel-imported enums (checker.ts:6639 `NodeBuilderFlags` from `_namespaces/ts.js`) stay — resolveAlias can't follow the `.js` barrel. (2) OPTIONAL-TARGET = undefined (1246458a): `<ident> = undefined` where the target's DECLARED type includes undefined — an optional param `x?: T` (effective `T | undefined`) or a `T | undefined` local — FP'd TS2322 because the string `varTypes` drops the `?`; the type-engine `currentLocalTypes` / its pre-narrowing `narrowedDeclaredTypes` carries the correct union. Bail when the RHS is literally `undefined`, `!eOPT`, and the declared engine type includes undefined (mirrors round 448's `this.optionalProp = undefined`). Cleared generators.ts / checker.ts:19908 / nodeFactory.ts:1331. (3) UN-ANNOTATED PARAM SHADOWS LEAKED MODULE VAR (df18ce2f): program.ts's `const indent = "    "` (module const) shadowed by `flattenDiagnosticMessageText(…, indent = 0)`'s param, but `populateParameterLocalTypes` left an un-annotated non-function-default param unregistered, so the recursive arg `indent` resolved to the leaked const's `string` → FP TS2345. Now such a param is added to `currentParamBindingNames` (→ anyType) and overrides an inherited `currentLocalTypes` entry; un-annotated-only (an annotated shadowing param is still checked). INVESTIGATED & REVERTED: the varTypes-side (return-path) shadow override — didn't reach the return-check's resolution path; the return-path shadowed-param TS2322 is not a confirmed dashboard FP. NEXT (compiler @ 170): TS2322×93 deep-M3 relation (mostly whole-program contextual inference), the TS2353 getter/method object-literal excess (sourcemap.ts) + mapped-type-key eval (utilities.ts, M3.3), TS2349 `(x || (x = [])).push()` inline-property-receiver, the CFA `indexInfos` cluster (TS2454)._

_Round 452 (2026-07-09, Phase 17 — bounded FP burn-down: TWO fixes, both GENERAL parser/checker correctness that reproduce minimally. Dashboard: compiler 177 → 176 (−1), services 282 → 280 (−2), server 488 → 485 (−3), harness 704 → 701 (−3); suite 9,619 → 9,628 (+9 local across 2 new test files, 0 regressions); 2 fix commits (4c47c918 string-index / 0db79740 tuple). (1) STRING-INDEX (4c47c918): a numeric-literal element access on a STRING-typed receiver — `(arr[i])[0]` where the inner access is typed `string` (jsTyping.ts `pathComponents[…][0] === "@"`) — FP'd "Property '0' does not exist on type 'string'." The B292 bail suppressed only NON-numeric string keys; a numeric key `[0]` (and numeric-looking `["0"]`) fell through to the missing-member check (a plain `str[0]` resolved elsewhere, but an element-access-typed receiver reached it). Element access on a string primitive is never TS2339 in tsc (numeric index sig; the TS2339 path is property-access-only) → broadened the bail to any literal key. (2) OPTIONAL-TUPLE (0db79740): `return emptyArray as []` against an all-optional tuple `readonly [kind?: T, …]` (moduleSpecifiers.ts) FP'd TS2739 "missing 0, 1, 2". The parser DISCARDS a tuple element's `?` (and label), so the resolved tuple marked every member REQUIRED and its `length` a fixed literal. The parser now records per-element optionality on the new `TupleType.elementOptional` (named `?` in the labeled branch; unnamed `[T?]` bridged via a flag since parseType's trailing-`?` recovery eats it); getTupleType marks optional members (`optionalTupleMemberIds`, consulted by isOptionalProperty) and makes an optional-containing tuple's `length` the union `minLength..maxLength`. FP-safe: a required-leading `[a, b?]` and an all-required tuple still error. INVESTIGATED & REVERTED (dashboard no-op): `(x || (x = [])).push()` empty-array/union-target contextual typing — the IDENTIFIER inline case now types correctly, but the real FPs (binder.ts:1375 property-target, core.ts:2135 `??=` IIFE-call return) resolve the receiver in the call-type pass via a path the fix doesn't reach → 0 dashboard FPs cleared. NEXT (services @ 280): deep-M3 TS2322×154 fragments; the whole-program cross-file `declare module` augmentation method-guard family (Blocker #3); the `length = end - start; length - 5` cross-statement reassignment arithmetic-narrowing residual (M3.4); the evolving-let `indexInfos` CFA cluster._


Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `docs/history/STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `docs/history/STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
