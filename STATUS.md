# Status

**Phase 4 — Checker buildout: COMPLETE. Phase 17 — self-compile the TypeScript compiler itself (re-scoped 2026-07-03; "any TypeScript project" is post-v1) — is the LIVE mission (see PLAN-PHASE-5.md; v1 exit = all 8 tsc-source profiles at zero FPs — **the FP leg was MET at round 481 (2026-07-12): all eight profiles at zero real FPs, only env-legit offline artifacts remain**; emit/crash legs verified same round: all 8 profiles emit every program file, exit 0, no crashes — the OFFLINE-VERIFIABLE v1 DEFINITION OF DONE IS MET; next: M5 performance; dashboard: bench/*.tsv, self-compile diagnostics 13,245 → 936 (measured at round 430 — rounds 395–412 burned bounded non-M3 histogram-tail buckets + M3.4 flow-narrowing slices the round-394 "pool picked over" triage missed, all surfaced by bucketing the full `--listAll` output by normalized message shape: TS2499 multi-base-generic misparse, TS2440 type-only-barrel-merge, TS2344 constraint-chain + cross-file `typeParamInternCache` pos-collision, TS2693/TS2709 clodule + destructuring-shadow, TS2314 enum-member-vs-generic, TS2588 nested-shadow, TS2551 Object.setPrototypeOf, SetIterator/MapIterator lib gap, TS2774 local-shadows-function, TS1100 interface-property-name, TS7023 callback-arg self-ref, TS2365 arithmetic local-shadows-function, TS2362 branded-number, TS7053 enum-reverse-mapping, round 408's TS2349 callee flow-narrowing family, and round 409's `export *`-barrel / ESM-`.js` imported-guard flow narrowing (M3.4, TS2339 838 → 672), and round 410's TS2862 constraint-index-sig gate / assign-RHS type-guard narrowing / void-overload-return (2,443 → 2,433), and round 411's M3.4 enum-member discriminant narrowing (barrel-import-resolved, three families UpToDateStatus/TypeMapper/PrivateIdentifierInfo) + type-guard-narrows-a-member-DOWN (2,433 → 2,374, TS2339 672 → 614) + round 412's M3.4 single-type type-guard narrow-DOWN + TS18048 receiver-narrowing (2,374 → 2,373), and round 413's `export *` LEAF-EXPORT gate (barrel-imported `Debug.assert` & every barrel guard now resolves — the TRUE builder.ts blocker, NOT the round-412 depth red herring) + tsc-faithful linear flow-walk iteration + return-path narrowing (2,373 → 1,965, TS2339 614 → 237)), then rounds 414-417 burned more bounded/M3.4 buckets (TS2366 CFA fall-through, NonNull/&&-narrowing arithmetic, closure-loop narrowing, namespace-local `extends` bases: 1,965 → 1,902), and round 418 resolved NESTED type-guard functions (M1.12, 1,902 → 1,854, TS2339 237 → 189: tsc guards `isTupleType`/... are nested in `createTypeChecker` so the binder skips them and `resolveFlowCalleeDecl` missed them - the guard never narrowed, the single biggest TS2339 sub-family), and round 419 resolved properties on INTERSECTION union-members (M1.12, 1,854 → 1,808, TS2339 189 → 143: `getPropertyOfType`/`typeHasOwnProperty` bail on a `Type.Intersection` member so `PropertyAccessExpression | (ElementAccessExpression & …)` FP'd TS2339 on `.parent`; fold the constituents in property resolution + discriminant-narrowing annotation; 0 new FPs, self-compile time −17%), and round 420 resolved TYPE-ALIAS enum-member discriminants in narrowing (M1.12, 1,808 → 1,799, TS2339 143 → 134: a `.kind: <alias>` discriminant member survived a `switch (x.kind) { case }` because `enumMemberKeysOfTypeNode` handled only a direct `Enum.Member`, not a bare-Identifier alias; 0 new FPs), and round 422 burned FIVE bounded M3.4/overload families (1,799 → 1,756: TS2769 60 → 45 — overload arg-check helpers now flow-narrow reference args (mirroring B469) + a synthetic true|false union for guard-narrowed boolean args vs literal overload params; TS2339 134 → 117 — mixed enum + string-literal discriminant unions narrow on disjoint lit:s: keys (PrivateIdentifierInfo); TS18048 10 → 7 — an optional-chain discriminant access x?.kind === <non-nullish> proves the receiver non-nullish; TS2366 12 → 4 — union-.kind exhaustive switches terminate via requiredUnionDiscriminantKeys), and round 423 (a) drove TS2366 to ZERO (1,756 → 1,752: the switch RECEIVER is guard-narrowed + typed before the exhaustive-switch union walk — flow graph via the pass-dedicated implicitReturnFlowGraph, local-const-from-call typing, optional-discriminant @undefined keys, LiteralToken["kind"] indexed-access) and (b) fixed union-TARGET type guards end-to-end (1,752 → 1,708: `x is A | B` PARSED truncated so isCallOrNewExpression/isPropertyNameLiteral/isOptionalChain never narrowed; + aliased conditions `const isFrag = guard(x); if (!isFrag)` via a memoized flow back-walk; + kind-key disjointness beating the lenient relation; TS2339 117 → 104, TS2322 784 → 756) and (c) narrowed truthy optional-chain CALL conditions (1,708 → 1,707: `if (x.y?.size())` proves `x.y` non-nullish), and round 424 burned SEVEN more M3.4 flow-narrowing slices (1,707 → 1,662, TS2339 104 → 68, TS18048 5 → 1, TS2322 756 → 751: loop-entry union suppression with a STRUCTURAL wash gate, call-RHS return-annotation narrowing, closure/join/call-crossing aliased conditions, prefix-path receiver guards, asserts-with-inferred-TP via test-arg predicate inference, assignment-overwrite reset to the declaration/call-RHS resolved type, and the DebugTypeMapper this-narrowing slice (explicit-type-arg asserts re-type `this`; an overload-cluster name collision resolves to the unique TypePredicate-bearing decl) — every step by-site strictly removals), and round 425 found the TS2339 never-cluster ROOT CAUSES (1,662 → 1,600, TS2339 68 → 6: the round-411 enum-discriminant key space SPLIT across resolution paths — canonicalEnumSymbol at all four key-builder sites — plus per-candidate narrow-DOWN distribution in union-target guards/instanceof, aliased ===/switch discriminants, switch-default negative narrowing, typeof-object filtering, boolean-literal discriminant truthiness, destructuring-read narrowing, the loop-entry retry for the single-type suppression, and the positive-empty intersection fallback whose round-423 dead-end verdict was an ARTIFACT of the key split) — driving to 0, and round 426 replaced the B399 TS2563 per-file proxy with the faithful flow-walk depth trip + the asserts-callee call gate (1,600 → 1,593: TS2563 27 → 0; TS2454×20 pre-existing definite-assignment walker FPs unmasked), and round 427 burned that bucket (1,593 → 1,577: TS2454 20 → 4 via tsc's definiteness rules — logical assigns ??=/||=/&&= are DEFINITE so captured reads assume initialized, a !-asserted read assumes initialized, and the anywhere-scan's left-spine walk now applies the target rule per spine node), and round 428 took the first real M3.1 slice of the TS2322/TS2345 cores (1,577 → 1,186, −25%: nullable-union generic param inference + overloaded generic callees for tsc's `x = append(x, item)` idiom (TS2322 751 → 559), the TS2345 histogram top — explicit this-param binding, guarded optional-member args, numeric-enum→number (394 → 280) — the array-literal-vs-union-member string-layer rule (TS2322 → 501), and the body-local-shadows-function anyType registration (TS2345 → 261, TS2769 → 36)), and round 429 drove the TS2345 core 261 → 86 (1,186 → 1,000, −15.7%: call-types lexical shadowing — body-locals vs enclosing params, DESTRUCTURED params via the currentParamBindingNames side set in getTypeOfIdentifier (the round-428 "mini-repro does not reproduce" residue), arrow own-params; embedded String replace/replaceAll/search/split accept RegExp; optional-param union args + NonNull-asserted args; guard-narrowed interface/unknown Identifier args — the ~110-site dominant mechanism, never-param excluded for assertType<never> exhaustive-switch defaults; string-enum→string in isSimpleTypeRelatedTo resolving the round-410 Extension[][] deferral; rest-arg flow narrowing), and round 430 unlocked the `append`/`addRange` inference (936: the `T extends {}` constraint failed `string → {}` and killed every mapper — empty-object-target relation rule with TP-source excluded per genericPrototypeProperty3's pin; readonly-array anchors; TP-from-PREDICATE binding for the getFirstJSDocTag family); self-compile time ~110–150 s single-run band), and round 431 took the FIRST M3.2 slice — the TS7006 core 301 → 11 via contextual typing (nested-fn/lexical callee resolvability, assignment-RHS context from the LHS declared type under the B476 single-applicable-sig rule, ||/??-both/&&-comma-right propagation, intersection/Reference/extends receiver members) — plus engine return-checking in switch/try bodies behind the foreign-TP source gate (un-inferred generic call results bail instead of FP'ing; ~130 T[]/U|undefined/SearchResult<T> return sites cleared) and the TS2367 anchored-disjointness gate (936 → 551, −41%; TS2322 435 → 345, TS7019 4 → 0; by-code strictly shrinking at every landed step) — then extended the foreign-TP gate to the var-decl/assignment/property-write/conditional-return paths (551 → 482, −69 strictly removals; TS2322 → 276; a generic FUNCTION-VALUE source's sig-own TPs stay checkable per 5 corpus pins the suite gate caught), and round 435 took SEVEN more M3.1/M3.2 slices at post-perf-arc iteration speed (482 → 373, −22.6%: generator TReturn returns, fresh-objlit literal props via the freshObjLitRange relation retry, TP-literal-constraint args, the union-decomposition-transparent relation re-entry gate resolving the NodeArray-covariance family ×23, bare-`new` contextual instantiation, the foreign-TP gate on assignment TARGETS (visitor family ×15+), nullish alias-union returns; TS2322 276 → 184, TS7006 11 → 1, TS2345 86 → 79; full-dashboard baseline: compiler 482→373 / tsc-cli 484 / jsTyping 480 / deprecatedCompat 481 / typingsInstallerCore 489 / services 1,603 / server 1,894 / harness 2,193 at 29–41 s wall each), and round 436 took SEVEN more M3.1/M3.2/M3.4 slices (373 → 294, −21.2%: TP-carrying callback-return param skip (forEachEntry family ×14), destructured-LOCAL shadowing (semver/checker + 12 transformer sites), literal-return syntactic union membership (the string-fallback re-widening trap — `return false` vs `Tag | false` ×7, incl. the completely UNPINNED `return "a"` vs `"a" | "b"` shape), constraint-filtered explicit-type-arg overload selection ('true' vs 'false' ×6), overload-helper optional-param union-arg + foreign-TP rules (union-receiver TS2769 ×13), bare-string switch-case narrowing (semver ×3), guard-gated ternary return arms with the tri-state all-leaves-verified early return (×19); TS2322 184 → 158, TS2345 79 → 47, TS2769 30 → 9; round-436 dashboard baseline: compiler 373 / tsc-cli 375 / jsTyping 371 / deprecatedCompat 372 / typingsInstallerCore 380 / services 1,476 / server 1,769 / harness 2,062 at 27–41 s wall), then round 438 (2026-07-07) took FIVE more M3.1/M3.4 narrowing/relation slices (294 → 244, −17%: an empty-object `{}` union-member return-path early return; UNION-target extension of the assignment-RHS AND return-path type-guard narrowing gates (`currentSourceFile = node` / `return node` vs `SourceFile | undefined`); PROPERTY-ACCESS args in the call-arg guard-narrow-DOWN branch (`getExports(node.left)`); and nullish-strip-gated narrowing of object-literal property VALUES (`{ moduleSpecifiers: specs }` where `specs = append(specs, x)`); TS2322 158 → 116, TS2345 47 → 39), then round 439 (2026-07-07) took THREE predicate-overload / arg-narrow-DOWN slices (244 → 228, −16 strictly removals: findAncestor-style type-guard-callback overload return inference + a companion NonNull concrete-union strip, the overload arg-check non-union narrow-DOWN, and its `unknown`-via-typeof extension; TS2769 9 → 1).** Corpus suite green as a permanent regression gate: 11,170 tests / 0 failing / 3 skipped (generated tsgo-relevant corpus + local corner-case tests; count as of round 536) — **the full tsgo-relevant TypeScript compiler test suite passes.** _Round 443 (2026-07-08, Phase 17 — module-augmentation family + the module-file-local TYPE-alias leak (Blocker #3)): FOUR bounded fixes, all suppression-only. Compiler profile UNCHANGED (190) but the fixes GENERALIZE hugely: services 591 → 498 (−93), server 887 → 733 (−154), harness 1,118 → 989 (−129); suite 9,504 → 9,512 (+8 local across 3 test files, 0 regressions); 4 fix commits; services diffed via --listAll as strictly by-position removals. (1) TS2664 10 → 0: `resolveModuleSpecifierRelativeJsAware` — a `declare module "../compiler/types.js"` augmentation resolves `.js` → the `.ts` sibling (the base resolver deliberately avoids `.js` per the TS2459 gotcha, so the augmentation target never resolved); purely additive → only SUPPRESSES a false 'cannot be found'. (2) TS2564 17 → 0: a class property whose declared type INCLUDES `undefined` needs no definite assignment (tsc's strictPropertyInitialization `getFalsyFlags(type) & TypeFlags.Undefined`); `checkClassPropertyInit` missed the `| undefined` case, so `SourceFileObject`'s `nameTable: Map<...> | undefined` + siblings FP'd; reuses the existing `typeIncludesUndefined`. (3) TS2304 24 → 2: `buildNamespaceScope` gained a StringLiteralNode branch — a `declare module "X"` augmentation body sees the AUGMENTED module's exports by bare name (Node/NodeArray/SymbolFlags/TypeChecker/__String — compiler/types.ts exports NOT imported into services/types.ts); the 2 remaining are NodeJS `global` (env-legit, offline). INVESTIGATED & REVERTED (dashboard no-op): TS7006 array-element contextual typing — a correct M3.2 enabler (3 passing tests) but the real inferFromUsage.ts `Priority[]` case is B83.5-blocked (`interface Priority` nested in a function → unbound → `Priority[]` resolves to any); land it with nested-type resolution. (4) TS2339 44 → 0 (SourceFileLike, Blocker #3): a module-file-local `type X = A | B` alias leaks into globals and shadows the global `interface X` in OTHER files (TYPE-space analog of round 442's variable leak). importTracker.ts's non-exported `type SourceFileLike = SourceFile | AmbientModuleDeclaration` won the last-wins merge over compiler/types.ts's `interface SourceFileLike`, so a `sourceFile: SourceFileLike` param resolved to the bogus union (AmbientModuleDeclaration has no `.text`); pinned via an instrumented probe (whole-program-only — minimal repros don't reproduce). Built `conflatedTypeAliasFiles`; checkMemberAccessMissing bails a union-receiver TS2339 when the receiver displays as a conflated name in a NON-alias file (own-file still fires). NEXT (services @ 498, all deep): TS2322×235 / TS2339×85 (residual conflations — Type×20/Info×12) / TS2345×54 M3 relation, TS2416×11, TS2353×10, TS2740×9/TS2739×8._ 

_Round 536 (2026-07-17) — INV.4(d) walker 7: checkUseBeforeDeclaration (the TS2448/TS2449/TS2450 TDZ pass + the TS2454 co-emit and static-init TS2729, 205 ms in the round-529 cost table) migrated onto the spine — the per-file driver, the per-list activation (checkUBDInStatements), and the nested-scope recursion walkers (checkUBDInStatement / checkUBDInExprForNested) are DELETED (~195 lines). The lightest stateful shape since the dupId walker: reach is a 5-state memoized ancestor classifier (`spineUbdStatus` over `spineUbdEdge` — STMT/EXPR/LIST/MEMBER/NONE, the deleted arms verbatim), the per-LIST blockScopedDecls map (a pure WHOLE-list function: let/const/class/ enum/namespace decls minus hoisted fn/var names) is memoized per list owner (`spineUbdListDecls`), the retained BOUNDED per-statement walk `checkUBDForwardRefs` (which recurses if/labeled descent itself — nested statements never re-anchor) runs at DIRECT statements of ACTIVATED lists, and the For/ForIn/ForOf loop-header SELF-ref checks re-host at the loop statements' enters (any UBD_STMT-reached loop, since the deleted recursion ran them at every statement position, not just list members). ORDER COUPLINGS, both resolved by slot placement and empirically gated: (1) the producer sibling populateAmbientCyclicBaseClasses (the TS2449 ambient-cyclic suppression set consulted by emitTS2449) moved BEFORE the spine; (2) emitTS2448 CO-EMITS TS2454, and checkDefiniteAssignmentViaFlowGraph's position-dedup scan — which runs right after the spine since round 534 — now SEES those co-emissions where the legacy order hid them (UBD ran ~500 slots later); the slot-move pre-gate (intact pass between the spine and the DA-flowgraph siblings) measured error-line-identical listAll ×8 AND corpus-green, so the interplay is inert. The cross-file leg (checkCrossFileUseBeforeDeclaration) stays a separate pass at the spine slot, preserving per-file-first order. Reach quirks pinned (33 pins, Inv4SpineBatch27Test, ALL pre-verified on the OLD walker first run): the ForwardRefs walk descends if/labeled ONLY — an UNBRACED if-body statement IS forward-checked while a BRACED one is a fresh activation and is NOT; while bodies / do-statements (condition AND body) / for headers / for-in/of iterated expressions / try bodies / switch CLAUSE statements are never forward-checked against the OUTER list's later declarations; async IIFE self-refs exempt while plain IIFE self- and forward-refs fire (with body shadowing suppression); const enums / declare-decls have no TDZ; a hoisted var/function alongside the let disables the check; instance property initializers are deferred while static ones fire TS2729+TS2449. VERIFIED: suite 11,137 → 11,170 (+33, 0 regressions, XML-verified); listAll error lines IDENTICAL on ALL 8 profiles (536a vs 535a); bench row recorded. NEXT: INV.4(d) walker 8 — checkImplicitReturns (199 ms) / checkConstAssignment (170 ms)._

_Round 535 (2026-07-17) — INV.4(d) walker 6: checkArgumentCounts (the TS2554/TS2555/TS2575 function-call arity pass, 230 ms in the round-529 cost table) migrated onto the spine — the per-file driver and the recursion walkers (checkArgCountInStatements / checkArgCountInStatement / checkArgCountInExpr(Core)) are DELETED, plus the argCountDepth / argCountFnDepth / argCountSuperCtor threading fields. Two firsts in the (d) series: (1) reach is a memoized DEPTH classifier, not a boolean/state one — the legacy `argCountDepth` recursion counter (with its load-bearing ≤200 cap: `++argCountDepth > 200` silently prunes a subtree, the anti-SOE guard for left-nested binary chains) is reproduced per node as an Int over `spineArgEdge` (ShortArray memo, 0/1/depth+2 encoding); an expression-position edge costs one frame (+1), a statement/list/member edge carries the ambient value, and a BinaryExpression's RIGHT operand that is itself a binary is ABSORBED (the legacy right-spine loop — no frame, so right-leaning ``/`=` chains never consume depth while left-nested chains prune at 200; both pinned). (2) the downward context is MAP-valued and rebuilt PULL-based — the funcParams/classCtorParams/fnDepth/superCtor threading is a pure function of the ancestor chain (every list overlay reads its WHOLE statement list, never a position-ordered prefix), so `spineArgCtxAt` ascends to the nearest statement-LIST owner (SourceFile/Block/ModuleBlock/switch clause), takes its per-owner memoized `spineArgListCtx` (the checkArgCountInStatements activation: M1.11 ModuleBlock namespace collection + 17.126 nested-fn overlay + fnDepth-gated var-shadow removal), and re-applies the transforming edges top-down (fn-boundary minusParamShadowedNames + fnDepth++, class-DECL-constructor superCtor rebind, for-of loop-name shadow). TRAP HIT AND PINNED IN THE DESIGN COMMENT: `spineArgCtxAt` RE-ENTERS itself through `spineArgListCtx` (each owner's incoming ctx ascends to the next owner up), so the shared ascent buffer must be MARK-based, not clear()-based — the first cut silently dropped the for-of loop-shadow edge (the one pin that caught it; 45/46 on first migrated run). ORDER COUPLING: the producer sibling checkSpreadNonIterableIntoFixedArity moved BEFORE the spine (its spreadNonIterableHandledCalls set is consulted by the TS2554 too-many arm); checkCloduleTest2 (which REMOVES/EDITS TS2554 diagnostics at NewExpression positions) sits at a later slot than both orders, so it needed no move. Slot-move pre-gate: intact pass at the spine slot + SNI before = error-line identical listAll ×8 (the pass is self-contained — own maps, reads only AST + the SNI set — so no arith-style residue coupling). Bug-compat asymmetries pinned (46 pins, Inv4SpineBatch26Test, ALL pre-verified on the OLD walker): class-EXPRESSION member bodies get NEITHER the param shadow NOR the fnDepth increment (params don't shadow; top-level body-locals don't shadow) and a class-expression constructor never rebinds superCtor (a nested `super(1)` vs a 2-param base is silent); `new` CALLEE expressions, switch CASE expressions, param defaults, heritage arguments, enum member initializers, objlit accessor bodies, shorthand destructuring defaults, and computed names are unreached; the fnDepth-gated var-shadow removal is nested-lists-only (a top-level `var f` redeclaration keeps the check). VERIFIED: suite 11,091 → 11,137 (+46 Inv4SpineBatch26Test, 0 regressions, XML-verified); listAll error lines IDENTICAL on ALL 8 profiles (535a vs 535base); bench row recorded. NEXT: INV.4(d) walker 7 — checkUseBeforeDeclaration (205 ms) / checkImplicitReturns (199 ms)._

_Round 534 (2026-07-16 — INV.4(d) walker 5: checkDefiniteAssignment (the SET-based TS2454 pass) onto the spine — the per-file driver + the two recursion walkers (checkDefiniteAssignmentInNestedScopes/-InExprContext) are DELETED, ~370 lines): the first per-statement-LIST ordered walker with a DOWNWARD leak context. Each legacy checkDefiniteAssignmentInStatements activation becomes a CORE FRAME (statement-ordered uninitialized set + round-469 closure pre-scan + per-statement B78.2 nested-leak snapshot) pushed at its owner's enter (SourceFile leak-disabled+fileLocals / fn-body Blocks params+incoming-leak / Block-statements+ModuleBlocks fresh); the legacy loop body (collect → closure-removal → checkUses → mark → nestedLeak, all leaf utilities retained verbatim) runs at each direct-child statement's enter (parent === top.owner — no classifier for the step), and the recursion's reach is a memoized 10-state ancestor classifier (spineDaStatus/spineDaEdge: STMT/EXPR/MEMBER × leak-flavor + CORE/PASS/NONE/ROOT; try/switch-clause lists are PASS — descend leak-dropped with NO frame step). The downward leak SET is never threaded — a LEAK-flavored status reads the TOP frame's per-statement currentLeak at core-spawn time (sound: leak-preserving paths never cross a core spawn). The flow-graph siblings (ViaFlowGraph dedups one-directionally against this pass's emitted positions; the B223 try/catch walker) moved to right AFTER the spine dispatch, preserving set-pass-first order — slot-move pre-gate error-line-identical ×8. Reach quirks pinned (39 pins, Inv4SpineBatch25Test, ALL pre-verified on the OLD walker): if-CONDITION arrows / throw-expression closures / class-DECLARATION prop-init arrows unreached (class-EXPRESSION ones ARE); statement-order-late lets absent from earlier leaks; arrow EXPRESSION bodies pass the leak through. VERIFIED: suite 11,052 → 11,091 (0 regressions, XML-verified); listAll error lines IDENTICAL on ALL 8 profiles (534a vs 533a); bench row recorded. NEXT: INV.4(d) walker 6 — checkArgumentCounts (230 ms) / checkUseBeforeDeclaration (205 ms)._

_Round 533 (2026-07-16 — INV.4(d) walker 4: checkDuplicateIdentifiers (TS2300 family) onto the spine — the recursive checkDuplicatesInStatement(s)/InExpr/InClassElement walkers + the per-file pass driver are DELETED, ~215 lines): the lightest (d) shape — STATELESS (no threaded walk state; the checkDuplicateDeclarations isAmbientContext/inNamespaceBody flags derive at the anchor) and ZERO-TYPING, so the migration is a pure boolean reach classifier (`spineDupIdReached` over `spineDupIdEdge`, the deleted arms verbatim) + anchor dispatch at node enters running the RETAINED bounded leaf utilities (checkDuplicateTypeParams/Params, checkDuplicatesInType(Member), member-list checks, checkDuplicateDeclarations) minus the recursion the spine edges own; class/objlit MEMBER emissions dispatch uniformly at the member's own enter (a reached MethodDeclaration is class-or-objlit — both emitted TPs+params; a reached SetAccessor/Constructor is class-only, objlit edges never admit accessors). Per-file top-level scans ride checkSpine's loop in legacy within-file order (TS2502 self-typeof + circular-inference at file enter; declaration-group + merged-namespace/lib-shadow/cross-interface/prototype/clodule scans post-walk) — each wrapped in a currentFileLocals=null install (the legacy pass ran with it null; checkClassNamespacePrototypeConflict's `currentFileLocals ?: globals` consult makes it load-bearing). SLOT-MOVE PRE-GATE: intact pass at the spine slot = error-line-identical listAll ×8 (statelessness empirically confirmed, no residue coupling). Reach quirks pinned (48 pins, Inv4SpineBatch24Test, ALL pre-verified green on the OLD walker first run): call args / binary operands / conditions / case exprs / for headers / param defaults / class property initializers / objlit accessor bodies unreached; duplicate-declaration GROUPS fire only at file level / FunctionDeclaration bodies / ModuleBlocks (arrow/method bodies legacy-silent). Also cleared the 6 pre-existing round-532 warnings. VERIFIED: suite 11,004 → 11,052 (0 regressions, XML-verified); listAll error lines IDENTICAL on ALL 8 profiles (533a vs 532a); bench row recorded. NEXT: INV.4(d) walker 5 — checkDefiniteAssignment (241 ms) / checkArgumentCounts (230 ms), decompose when reached._

_Round 532 (2026-07-16 — INV.4(d) walker 3: checkImplicitAnyParameters (TS7005/TS7006/TS7008/TS7013/TS7019/TS7031/TS7032/TS7051) onto the spine — the recursive checkImplicitAnyInStatements/-InClassElement(Core)/-InExpr walkers + the per-file pass driver are DELETED, ~770 lines): the first DOWNWARD-CONTEXT-THREADING migration. The expression recursion's FIVE explicit context parameters (contextuallyTyped/contextualType/viaUnionWithPrimitive/ctxAnnotation/ctxViaAssignment) became ONE push-maintained SpineIanyCtx value with frames defined at EXACTLY the edges the legacy recursion passed arguments over — the load-bearing rule: every REACHED expression-position edge must DEFINE the ctx (even to null), or the parent ctx silently leaks where the legacy call passed defaults. Kinded carriers hold the one-level special states (call-args ctxProp computed once per call; objlit enrichment with the fresh viaUnion verdict + lazily-shared computed-key mapped-value arity; destructure-default typed keys); the binary left-spine LOOP dissolved into per-edge operator rules with no chain-root machinery (assignment-family right operands run the assign-target resolution + implicitAnyCtxUnknowable side channel at the right child's enter; `||`/`??` inherit on both operands, `&&`/comma right-only; viaUnion is inert with a null type so non-inherit edges reset both together); returnCtxAnnotation (nearest fn-like through Block/If/For only; Ctor/SetAccessor → null) and inAmbientContext (declare on the unbroken module chain; plain Blocks reset) pull-derive from parent chains; the three implicit-any scope stacks stay the SAME checker fields (fn-BODY-edge pushes, declarator-ENTER recordings — `var f = function(){ f(); }` sees itself); the eager shape-B annotation resolution is mirrored at the declarator enter. NO ambient install wrapper: the SLOT-MOVE pre-gate (intact pass moved past the 4 sibling TS7xxx passes to the spine slot) measured listAll error-identical ×8 AND corpus-green — no giants-residue coupling (the pass ran BEFORE the giants at its old slot 2180). VERIFIED: 56 pins (Inv4SpineBatch23Test) ALL pre-verified on the OLD walker — incl. the reach quirks (while/do/switch/try/for-in/for-of bodies, call CALLEES, conditional CONDITIONS, as-cast operands, objlit accessors, class static blocks all UNREACHED; class-expression setters fire TS7032 even with a sibling getter; ambient-class private members unchecked); suite 10,948 → 11,004 (0 regressions, XML-verified); listAll error-line SETS IDENTICAL on ALL 8 profiles (532a vs 531b; 7/8 raw-identical, harness raw print-order shift only — 3 TS7006 emissions moved with the slot, the round-527 precedent); bench row recorded. NEXT: INV.4(d) walker 4 — checkDuplicateIdentifiers (260 ms, zero-typing), decompose when reached._

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `docs/history/STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `docs/history/STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
