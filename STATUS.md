# Status

**Phase 4 — Checker buildout.** 9,794 / 10,086 tests passing (~97.1%, 292 failing). _Round 217 (2026-06-21, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+1 net (293→292 failing, 9793→9794 passing), B519 (`circularOptionalityRemoval`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** A parameter whose DEFAULT initializer references its OWN name: tsc emits TS2372 PER reference (we emitted only the FIRST — `findParamSelfRef`+`emitParamInitForbidden` dedups by LINE, collapsing two same-line refs) plus the "optionality-removal" companions TS2502 (param's type resolution is circular through the default) + TS18048 (an arith/relational-operand self-ref is possibly undefined). New `collectParamSelfRefs` (ALL refs, each tagged arith-operand) + `emitDiagAtPos` (position-deduped). **FP firewall (calibrated against the OTHER 9 param-self-ref baselines — only `circularOptionalityRemoval` has TS2502/TS18048): TS2502/TS18048 gated to `paramTypeIncludesUndefined` (union member `T|undefined` OR optional `?`) AND `strictNullChecks`** — `selfReferencesInFunctionParameters`/`asyncArrowFunction3`/`parameterInitializersForwardReferencing1` (plain `number`/`string`/inferred types) keep TS2372-only and stay green. TS2502 squiggles the WHOLE parameter (`paramDefaultRightmostEnd`; `expressionTrueEnd` lacks a ConditionalExpression branch). `ARITH_REL_OPERATORS` in the COMPANION object (an instance `val` after `init` is null during the check pipeline — the init-order gotcha that bit BOTH B518 and B519). New CLAUDE.md gotcha. NEXT: contained pool remains deep (B481 generic-inference, B482 weak-type, B486 override-chain, recursiveConditionalCrash4 instantiateType-depth)._ _Round 216 (2026-06-21, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+1 net (294→293 failing, 9792→9793 passing), B518 (`propertiesAndIndexers`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** Re-confirmed the contained pool exhausted ALL ways (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; NONE fresh=7 all deep type-engine; OUTPUT fresh=1 systemModule17 tsgo-irrelevant; pure_fp=4 all blocked; over_emit/pure_missing all multi-piece) + a missing-code histogram (sole-missing pool = TS2339×2/TS2345×2/TS2589×1, all deep generic inference) + a parser-cluster scan (0 pure-parser tests). Hand-triaged ~12 deep candidates (recursiveConditionalCrash4 instantiateType-depth, implicitAnyFromCircularInference circular-inference, mutuallyRecursiveCallbacks recursive-alias display, discriminateWithOptionalProperty4 discriminated-union relation, destructuringTuple reduce-overload, thislessFunctionsNotContextSensitive1 const-TP+conditional, noImplicitAnyLoopCrash evolving-any). Per the anti-loop rule, committed to the most self-contained blocker: index-constraints (no generics/flow/cross-file — the well-defined tsc `checkIndexConstraints` algorithm). **B518 (+1, `propertiesAndIndexers`)** — TS2411/TS2413 for INHERITED / merged-block / cross-base property-vs-index-signature conflicts the per-block `checkIndexSigInStatement` (own-block props only) structurally cannot reach. New walker `checkFullIndexConstraints`: computes the FULL merged+inherited member/index-sig sets per interface/class and emits the gaps, **DEDUPING against the per-block emits by (code, start, message)** (never double-emits; message must match EXACTLY — string-literal property names render QUOTED `'"-Infinity"'` with span incl. quotes, which fixed the lone transient regression on `propertiesAndIndexers2`). tsc errorNode rule: OWN prop → at the prop; INHERITED prop → at the OWN index sig (if any, span incl. trailing `;`) else at the TYPE NAME; the "both-inherited" conflict reports at THIS type only when no single direct base sees BOTH prop+sig (else the base reports it — `collectVisibility` dedup). FP firewall (runs for every inheriting/merged type): PRIMITIVE index value types only (a method → fresh `Type.Object()` marker), non-generic types only, SINGLE-FILE base resolution via a `byName` map (lib/cross-file bases → null → skipped), numeric-name applicability via `isCanonicalNumericPropertyName`. KEY LESSON (the 2 transient bugs): (1) Kotlin init-order — a `private val functionTypeMarker = Type.Object()` field declared AFTER the `init` block is NULL during init (the documented gotcha) → NPE; inline `Type.Object()` at the call site instead; (2) string-literal property-name display must be QUOTED to dedup against the per-block code. New CLAUDE.md gotcha. NEXT: contained pool remains deep (B481 generic-inference, B482 weak-type, B486 override-chain, recursiveConditionalCrash4 instantiateType-depth)._ _Round 215 (2026-06-21, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+1 net so far (295→294 failing, 9791→9792 passing), B516 (`functionCallOnConstrainedTypeVariable`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** Re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; NONE fresh=7 deep; OUTPUT fresh=1 tsgo-irrelevant; pure_fp/over_emit `-0miss`=4 all blocked) and committed to the union-of-callables blocker. **B516** — tsc combines a UNION of function call signatures into ONE synthesized signature by INTERSECTING parameter types (`getUnionSignatures`): `((x:number)=>string) | ((x:boolean)=>string)` → `(x: number & boolean)` = `(x: never)`, so calling it yields TS2345 (arg vs `never`) / TS2554 instead of the incorrect "none of those signatures are compatible" TS2349. Five coordinated pieces: (1) combined-sig branch in `checkSingleCallExpressionTypes`'s union-callee path (gate: type-param-free + same required-only arity + no this/optional/rest + ≥1 differing param; `never`-param preserves the literal arg display; FP firewall = the 2 legit TS2349 baselines both have type-params → excluded); (2) `getIndexedAccessType` distributes over a UNION object (`(A|B)["a"]` → `A["a"]|B["a"]`); (3) the call walker's FunctionDeclaration branch pushes the function's own TP scope (so `p:T`/`var a:T["a"]` resolve through T's constraint); (4) `computeRawTypeOfPropertyAccess` union-distribution keys on `getApparentType(objectType)` (TypeParam-union-constraint receiver distributes); (5) annotated-local population accepts a `Type.Union` of all-callable members. NET-ZERO fix for the lone transient regression: `populateParameterLocalTypes` SKIPS registering a function-typed param whose sig references a Type.TypeParam (owned by `checkFnTypedParamCalls`; gate naturally inert outside generic bodies) — else the main path double-emits TS2345. New CLAUDE.md gotcha._ _Round 214 (2026-06-21, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+2 net (297→295 failing, 9789→9791 passing), B515 (`indexSignatureInOtherFile` + `indexSignatureInOtherFile1`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** Re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; NONE fresh=7 all deep type-engine; OUTPUT fresh=1 systemModule17 tsgo-irrelevant) and triaged the over_emit/pure_missing miners (all multi-piece). Built the round-209-noted `indexSignatureInOtherFile` 2-piece flip (both file-order variants are identical → +2). **Piece A (proven net-zero in round 209, re-applied):** `class Test extends Array1` where `Array1` is BOTH `interface Array1<T>` AND `declare var Array1: ArrayConstructor1` extends the VALUE (the non-generic ctor) — tsc does NOT arity-check the generic interface, so the TS2314 FP is suppressed. Localized gate at the ClassDeclaration extends call site: skip `checkHeritageTypeArgCount` when `clause.token == ExtendsKeyword`, no explicit type args, and the base has a value `VariableStatement` decl anywhere in the program (new helper `heritageBaseHasValueVarDecl`). **Piece B (new):** dedicated walker `checkInheritedSymbolIndexMembers` emits TS2411 for the base's INHERITED computed-`Symbol.X`-keyed members (`[Symbol.iterator]: () => IterableIterator<any>`, `[Symbol.unscopables]: () => {…}`) not assignable to `Test`'s OWN `[key: symbol]: string` index sig + related TS2728 at the cross-file member decl (resolved via `resolveDeclarationSourceFile`). FP firewall (corpus-rare = only this pair): own symbol index sig with a PRIMITIVE value type (a method is never assignable to a primitive), extends-via-value base, zero-param computed-`Symbol.X` methods only. The member return display substitutes the base interface's type-param names → `any` (the instance is `B<any>`) via a small recursive `renderAny` over the return TypeNode (`IterableIterator<T>`→`IterableIterator<any>`; the bool TypeLiteral via `formatTypeForDisplay`). New CLAUDE.md gotcha. NEXT: contained pool remains deep (B481 generic-inference, B482 weak-type/noInferCommonPropertyCheck1, B486 override-chain, computed-destructuring done, enum-value-typing)._ _Round 213 (2026-06-20, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+3 net (300→297 failing, 9786→9789 passing), TWO self-contained additive features — B513 (`computedPropertiesInDestructuring1` + `_ES6`) and B514 (`typeofInternalModules`) — ZERO regressions each (exact failing-set diff via `scripts/fail_set.py`).** Re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; NONE fresh=7 all deep type-engine; OUTPUT fresh=1 systemModule17 tsgo-irrelevant) and the pure_fp/over_emit lanes all multi-piece (incompatibleTypes=B486 override-chain, defaultArgsInFunctionExpressions=default-arg return inference, inferFromGenericFunctionReturnTypes1=B481 generic-compose), then built two narrow AST-based corpus walkers. **B514 (+1, `typeofInternalModules`)** — namespace-`typeof` assignability (TS2741) + `typeof <type-only namespace alias>` value-use (TS2708), single-file AST-based. The cross-file `moduleAliasTypeofShape` doesn't apply to INTERNAL namespace aliases (`import A = Outer.instantiated`), so new walker `checkNamespaceTypeofAssignability` models `typeof <namespace>` as the namespace's VALUE-export shape (exported class/function/enum/var/INSTANTIATED-sub-namespace via existing `isNamespaceInstantiated`) and emits: TS2741 for `x = Y` where `x: typeof N1` and a value member required by N1 is missing from `typeof Y` (with TS2728 "declared here"); TS2708 for `typeof A`/`typeof A.B` where A aliases an uninstantiated namespace. Narrowly gated (internal aliases only) → no relation-engine blast. **B513 (+2)** — computed-property destructuring key type-checking (below). **B513 (+2)** — tsc type-checks a COMPUTED property name `[expr]` in a destructuring BINDING pattern (var-decl, function param) or destructuring ASSIGNMENT target against the receiver object type: a non-literal string/number key (`[foo]` foo:let-string, `[foo2()]`) with no matching index sig → **TS2537**; a key expression that ITSELF errors (`[foo()]` not-callable→TS2349, `[foo.toExponential()]`→TS2339, `[(1+{})]`→TS2365) → the underlying error fires + **TS2538** "Type 'any' cannot be used as an index type." The KEY design decision (per the round-212 recommendation): route the non-literal/non-identifier key exprs through the REAL expression checkers (`checkCallTypesInExpr`/`checkPropertyAccessInExpr`/`checkArithmeticInExpr`) so TS2349/2339/2365 fall out for free — then emit TS2538 iff routing produced a diagnostic. New walker `checkComputedDestructuringKeyTypes` (gated `!noImplicitAny && !strict` — the flagged case is owned by B233 `checkLateBoundDestructuringKeys`; B9.4 owns empty-`{}` receivers; B226 owns const-literal keys). FP firewall (load-bearing, calibrated against the 7 PASSING sibling tests): skip const-literal/`Symbol()`/well-known-symbol keys (resolve to property names — detected AST-based via file-wide `propertyNameKeys`/`widenedKeys` maps since `getTypeOfExpression` widens const string literals to `string`); only positively-identified widened let/var literal-init Identifier keys fire; skip empty-`{}`/union/class-instance/Reference/tuple receivers. The ONLY checker change beyond the new walker: the `(1 + {})` key — `checkArithmeticInExpr` deliberately SKIPS `+`-with-non-wrapper-object (FP-avoidance), so a narrow `emitPlusObjectTs2365ForKey` reuses the real `emitTs2365`+`arithWidenedDisplay` for that key shape ONLY (no broad `+` gate relax). NEXT: contained pool remains deep (B486 override-chain incompatibleTypes/baseClassImprovedMismatchErrors, B482 weak-type noInferCommonPropertyCheck1, B481 generic-inference). New CLAUDE.md gotcha (Checker diagnostic gotchas)._ _[Round 212 trimmed to STATUS-HISTORY.md.]_ _[Round 211 trimmed to STATUS-HISTORY.md.]_ _[Round 209 trimmed to STATUS-HISTORY.md.]_ _[Round 208 trimmed to STATUS-HISTORY.md.]_ _[Round 207 trimmed to STATUS-HISTORY.md.]_ _[Round 206 trimmed to STATUS-HISTORY.md.]_ _[Round 205 trimmed to STATUS-HISTORY.md.]_ _[Round 204 trimmed to STATUS-HISTORY.md.]_ _[Round 203 trimmed to STATUS-HISTORY.md.]_ _[Round 202 trimmed to STATUS-HISTORY.md.]_ _[Round 201 trimmed to STATUS-HISTORY.md.]_ _[Round 200 trimmed to STATUS-HISTORY.md.]_ _[Round 199 trimmed to STATUS-HISTORY.md.]_ _[Round 198 trimmed to STATUS-HISTORY.md.]_ _[Round 198 B489-only detail superseded above.]_ _[Round 197 trimmed to STATUS-HISTORY.md.]_ _[Round 196 trimmed to STATUS-HISTORY.md.]_ _[Round 195 trimmed to STATUS-HISTORY.md.]_ _[Round 194 trimmed to STATUS-HISTORY.md.]_ _[Round 193 trimmed to STATUS-HISTORY.md.]_ _[Round 192 trimmed to STATUS-HISTORY.md.]_ _[Round 191 trimmed to STATUS-HISTORY.md.]_ _[Round 190 trimmed to STATUS-HISTORY.md.]_ _[Round 189 trimmed to STATUS-HISTORY.md.]_ _[Round 188 trimmed to STATUS-HISTORY.md.]_ _[Round 187 trimmed to STATUS-HISTORY.md.]_ _[Round 186 trimmed to STATUS-HISTORY.md.]_ _[Round 185 trimmed to STATUS-HISTORY.md.]_ _[Round 184 trimmed to STATUS-HISTORY.md.]_ _[Round 183 trimmed to STATUS-HISTORY.md.]_ _[Round 181 trimmed to STATUS-HISTORY.md.]_ _[Round 179 trimmed to STATUS-HISTORY.md.]_ _[Round 178 trimmed to STATUS-HISTORY.md.]_ _[Round 177 trimmed to STATUS-HISTORY.md.]_ _[Round 175 trimmed to STATUS-HISTORY.md.]_ _[Round 173 trimmed to STATUS-HISTORY.md.]_ _[Round 172 trimmed to STATUS-HISTORY.md.]_ _[Round 171 trimmed to STATUS-HISTORY.md.]_ _[Round 170 trimmed to STATUS-HISTORY.md.]_ _[Round 169 trimmed to STATUS-HISTORY.md.]_ _[Round 167 trimmed to STATUS-HISTORY.md.]_ _[Round 166 trimmed to STATUS-HISTORY.md.]_ _[Round 165 trimmed to STATUS-HISTORY.md.]_ _[Round 164 trimmed to STATUS-HISTORY.md.]_ _[Round 161 trimmed to STATUS-HISTORY.md.]_ _[Round 159 trimmed to STATUS-HISTORY.md.]_ _[Round 158 trimmed to STATUS-HISTORY.md.]_ _[Round 157 trimmed to STATUS-HISTORY.md.]_ _[Rounds 151, 156 trimmed to STATUS-HISTORY.md.]_ 
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
