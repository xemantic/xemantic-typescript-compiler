# Status

**Phase 4 — Checker buildout: COMPLETE. Phase 17 — self-compile the TypeScript compiler itself (re-scoped 2026-07-03; "any TypeScript project" is post-v1) — is the LIVE mission (see PLAN-PHASE-5.md; v1 exit = all 8 tsc-source profiles at zero FPs; dashboard: bench/*.tsv, self-compile diagnostics 13,245 → 2,443 (measured at round 409 — rounds 395–409 burned bounded non-M3 histogram-tail buckets the round-394 "pool picked over" triage missed, all surfaced by bucketing the full `--listAll` output by normalized message shape: TS2499 multi-base-generic misparse, TS2440 type-only-barrel-merge, TS2344 constraint-chain + cross-file `typeParamInternCache` pos-collision, TS2693/TS2709 clodule + destructuring-shadow, TS2314 enum-member-vs-generic, TS2588 nested-shadow, TS2551 Object.setPrototypeOf, SetIterator/MapIterator lib gap, TS2774 local-shadows-function, TS1100 interface-property-name, TS7023 callback-arg self-ref, TS2365 arithmetic local-shadows-function, TS2362 branded-number, TS7053 enum-reverse-mapping, round 408's TS2349 callee flow-narrowing family, and round 409's `export *`-barrel / ESM-`.js` imported-guard flow narrowing (M3.4, TS2339 838 → 672)) — driving to 0; self-compile time ~71 s).** Corpus suite green as a permanent regression gate: 9,074 tests / 0 failing / 3 skipped (generated tsgo-relevant corpus + local corner-case tests) — **the full tsgo-relevant TypeScript compiler test suite passes.** _Round 409 (2026-07-05, Phase 17 — M3.4 decomposed slice: user type-guards/asserts imported through `export *` barrels + ESM `.js` specifiers now NARROW, the round-408-flagged next-step). **Self-compile (compiler profile) 2,618 → 2,443 (−175, TS2339 838 → 672); suite 9,066 → 9,074 (+8 local, 0 regressions); 1 commit.** tsc's own sources import via `import { some, isDefined, Debug, … } from "./_namespaces/ts.js"` where the barrel is `export * from "../core.js"; …`, so an imported guard/assert never narrowed — the pervasive root cause behind a large TS18048/TS2339/TS2722/TS2349 slice. TWO independent gaps (round 408's naive "wire resolveAlias into resolveFlowCalleeDecl" was inert because of the FIRST, undiscovered until now): (1) `resolveModuleSpecifier` deliberately won't strip the ESM `.js` extension (TS2459 FP-avoidance) → `resolveAlias` couldn't resolve ANY `.js` import (tsc uses `.js` everywhere), so even a DIRECT imported guard failed; (2) even resolved, `targetFile.locals[name]` misses through an `export *` barrel. **CRITICAL SCOPING LESSON (MEASURED): the fix is FLOW-ONLY. A first cut added the `.js`+`export *`-star fallback to the GENERAL `resolveAlias` (the clean general fix) — corpus stayed green (0 corpus tests use `.js`/`export *` imports) BUT the self-compile REGRESSED 2,618 → 2,915 with a TS2315×466 flood (resolving barrel-imported TYPES exposed our generic-arity/type-resolution M3 gaps). Reverted `resolveAlias` to byte-identical; scoped resolution to the flow walkers via a dedicated `resolveImportedFunctionLikeDecl` (memoized process-wide in `importedGuardDeclCache`, the services-hang firewall) + the new `resolveExportedSymbolThroughStars` (the SYMBOL-resolving companion to M1.1's name-only `getModuleExportsFollowingStars`).** FP-safe: flow narrowing only REMOVES union constituents. ALSO fixed the generic `isDefined<T>(x: T | undefined): x is T` (tsc's own, pervasive): `collectPredicateTpBindings`' UnionType branch now binds a SINGLE naked type-param member to the actual constituents no concrete member covers (tsc's naked-type-parameter union inference). 8 local tests (BarrelImportedGuardNarrowingTest: guard then/else, generic isDefined, asserts x is T, multi-hop `export *` chain, renamed re-export, + 2 negative controls — a non-guard call and a pre-guard call both still fire TS2345). Perf: compiler ~61 → ~71 s (+16%, the additional narrowing work — the memo doesn't change it; services FLAT despite being bigger → the OPPOSITE of an O(n²) blowup); services hang-check CLEAN (no hang/crash, 252 files emitted, lowest FP count ever, 4,301). META (re-confirms 407/408): a read-only "M3-gated" verdict is about the GENERAL fix; a tightly-scoped FP-safe slice flips a whole cross-file family even while resolveAlias/M3 stays as-is — and "the clean general fix" can be a dashboard REGRESSION only the self-compile (not the corpus) reveals._ _Round 408 (2026-07-05, Phase 17 — a decomposed M3.4 slice: the TS2349 "not callable" family, the biggest bounded bucket a fresh full `--listAll` bucketing surfaced within the "M3-gated" tail). **Self-compile (compiler profile) 2,639 → 2,618 (−21, TS2349 25 → 5); suite 9,053 → 9,066 (+13 local, 0 regressions); 3 commits.** THREE FP-safe fixes, all "narrowing/typing removes constituents → can only suppress a false positive": **(1) callee flow-narrowing (−13):** `getCalleeType` resolves an Identifier callee via `currentLocalTypes` (DECLARED type) without consulting flow narrowing, so `if (fn) fn()`, `fn?.()`, and `typeof v === "string" ? v : v()` FP-fired the union "not all constituents callable" verdict. Before the callability check, re-narrow an Identifier/PropertyAccess callee via `getNarrowedTypeForReference` + drop nullish members under an optional call; gated to a callee that originally HAD a non-callable member (case-(c) untouched). **(2) `typeof x === "function"` narrowing (−2, incl. a bonus TS2722):** `narrowByTypeOfGuard` returned the union unchanged for the "function" tag ("can't identify by flags") — now filters by call/construct-signature callability (any/unknown kept both branches, never narrows to never). **(3) empty-array contextual assignment (−6):** `(x || (x = [])).push(v)` / `(x ??= [])` typed the receiver `T[] | any[]` because `x = []` types the bare `[]` as `any[]` (B87.6); `.push` on that union resolved to a UNION of two differing sigs → TS2349. `contextualAssignmentRhsType` types an empty `[]` RHS as the Array/ReadonlyArray target — tsc's contextual typing, FP-safe. 13 local tests (CalleeNarrowingNotCallableTest ×8, EmptyArrayAssignmentTypeTest ×5) with negative controls. **Remaining 5 TS2349 deferred (3 sub-problems): the `Debug.assertIsDefined(machine.onLeft)` generic-class param-property assert cases (×3 — six minimal probes could NOT reproduce it; a deeper real-code interaction), a `??=`-with-call-RHS (core.ts, assignment-narrowing gap), and a `FlowNode[] | undefined` union-LHS default-init (binder.ts, cross-interface-merge member typing). META: the round-407 "bounded pool is M3-gated" verdict was about the log tail — re-bucketing the FULL 2,639-line listAll surfaced TS2349×25 as a bounded M3.4 slice, exactly the CLAUDE.md META-LESSON that an "ENGINE/M3" verdict is about the GENERAL fix._ _Round 407 (2026-07-05, Phase 17 — M1.12, same session as 406): **the arithmetic pass + noImplicitAny element-access yield THREE more bounded buckets (self-compile 2,659 → 2,639, −20). Suite 9,043 → 9,053 (+10 local, 0 regressions).** (1) TS2365 21→7 — a local `const length = arr.length` (number) SHADOWING tsc's own imported `function length(): number` resolved to the FUNCTION → `i < length` FP'd `number < (…)=>number`; the pass now records an un-annotated const whose name shadows an outer FUNCTION (concrete primitive else `anyType`). The SHADOW gate is load-bearing — recording every primitive const unmasks narrowing FPs on the OTHER operand (`const numStatements = source.length` exposes `statementOffset < numStatements`; `const max = length(sig.tp)` exposes `min < max`) — so a first cut was a +5/−6 messy swap, the shadow gate makes it a clean −14. (2) TS2362 19→15 — a branded number `type IncrementalBuildInfoFileId = number & {__brand}` is number-like (intersection ⊆ number member) → `fileId - 1` valid; added `Type.Intersection` (ANY member) to the operand classifiers. (3) TS7053 3→1 — an enum reverse-mapping `NumericEnum[key]` (tsc's `ModuleResolutionKind[moduleResolution]`) is valid; excluded the enum-object receiver from the empty-object element-access branch. 10 local tests with negative controls. **A NonNull nullish-strip (`(T|undefined)! → T`) measured net −17 but UNMASKED M3 object-literal-vs-interface + generic-inference gaps → reverted, deferred to M3.** Residual arithmetic/index FPs are M3.4/M3 (enum-`| undefined` un-narrowed via `&&`/`!`; min/max overload; `number | undefined`; literal-union index key). Session total (406+407): 2,663 → 2,639 (−24) on SEVEN clean bounded buckets._ _Round 406 (2026-07-05, Phase 17 — M1.12 continued): **TWO more bounded self-compile FPs killed by bucketing the fresh full `--listAll` (self-compile 2,663 → 2,659, −4). Suite 9,034 → 9,043 (+9 local, 0 regressions).** (1) TS1100×2 — the `InterfaceDeclaration` branch of `checkStrictModeInStatement` checked a property NAME (`interface CallExpression { readonly arguments: NodeArray<Expression>; }`) via `checkStrictModeName`; tsc's `checkStrictModeEvalOrArguments` fires ONLY for binding names (variable/parameter/function names + assignment LHS), never a property/method name — removed the `PropertyDeclaration` name-check arm. (2) TS7023×2 — `getMutableArrayOrTupleType`/`unwrapAwaitedType` are `return cond ? mapType(t, self) : concrete;`; the self-reference is ONLY a callback ARG, so it receives a contextual param type and never forces self's return-type resolution (tsc emits nothing). New `selfRefsOnlyAsCallbackArgs` gate: safe iff every self-occurrence is a direct `CallExpression` argument (`[self][0]()` / `{next: self}` stay stuck → still fire). Both FP-safe by construction; diff = exactly the 4 lines removed, nothing added. 9 local tests (StrictModeInterfacePropertyTest ×5, CircularReturnCallbackArgTest ×4) with negative controls. META: round 405's "pool exhausted" was about the 30-line LOG TAIL; bucketing the FULL 2,663-line listAll surfaced two more — the residual bounded pool is now genuinely M3-gated (TS2349×25 typeof/`??=` callee narrowing = M3.4; arithmetic + brand-property + enum-subset TS2344 all M3)._ _Round 405 (2026-07-04, Phase 17 — M1.12 continued): **TS2774×1 fixed (self-compile 2,664 → 2,663) + TS7019×4 investigated and reclassified M3.2-gated.** The tsc-source FP `if (shouldElaborateErrors)` where `let shouldElaborateErrors = reportErrors` — `reportErrors` is a boolean PARAM but the uncalled-function check's syntactic pass has no local param scope, so `getTypeOfExpression(reportErrors)` found the sibling `function reportErrors` (callable) → mis-typed the local → FP TS2774. Fix: `collectUncalledTypedLocalsFromBody` types a bare-identifier initializer from the uncalled check's OWN scope knowledge (`shadowed`/`into`, or the enclosing-scope stack via `isUncalledShadowed`/`lookupUncalledTypedLocal`) instead of the unreliable global resolution — a boolean param → boolean (no TS2774), a same-scope local FUNCTION → still callable (genuine `let f = localFn; if (f)` keeps firing). 3 local tests. TS7019×4 (arrow rest params contextually typed by an assignment-LHS member/callback param) was a NO-OP fix attempt (reverted): the implicit-any pass has no enclosing param scope either, so the LHS type resolves to `any` — M3.2, not bounded. META: both remaining "bounded" candidates share ONE gap — the specialized syntactic passes lack the enclosing function's param scope; TS2774 was fixable only because the uncalled check already tracks a scope stack. Suite 9,031 → 9,034 (+3 local, 0 regressions)._ _[Rounds 313–404 and older trimmed to STATUS-HISTORY.md.]_
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
