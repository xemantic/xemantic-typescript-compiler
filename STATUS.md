# Status

**Phase 4 — Checker buildout: COMPLETE. Phase 17 — self-compile the TypeScript compiler itself (re-scoped 2026-07-03; "any TypeScript project" is post-v1) — is the LIVE mission (see PLAN-PHASE-5.md; v1 exit = all 8 tsc-source profiles at zero FPs; dashboard: bench/*.tsv, self-compile diagnostics 13,245 → 2,726 after rounds 384–389 — driving to 0; self-compile time ~74 s, −60% vs the round-386 start).** Corpus suite green as a permanent regression gate: 8,983 tests / 0 failing / 3 skipped (generated tsgo-relevant corpus + local corner-case tests) — **the full tsgo-relevant TypeScript compiler test suite passes.** _Round 394 (2026-07-04, Phase 17 M2.2 — burn-down #4): **`delete x.<Object.prototype member>` now fires TS2790 under real libs — real-lib A/B recount 29 → 28 (keywordExpressionInternalComments fixed), zero corpus regressions.** `getApparentType` does NOT fold Object.prototype's members (toString/valueOf/hasOwnProperty/…) into an object type's apparent members; under the embedded lib `Array` (value) resolves to the `Array<any>` INSTANCE (declares its own `toString`) so the member was found and TS2790 fired, but under real libs `Array` → `ArrayConstructor` (no own `toString` — inherited from `Object.prototype`) so it was missed and no error fired. Fix: an Object.prototype fallback in the delete check — when the receiver is object-like (`Type.Object`), the member name ∈ `OBJECT_PROTOTYPE_PROPERTIES`, and the type has no own declaration of it (`propSym == null`), emit TS2790 (those members are non-optional and present on every object, so `delete x.<member>` is always TS2790 under strictNullChecks — matches tsc). FP-safe: a user type declaring the name optionally still routes through the own-member branch; the fallback is scoped to the 7 Object.prototype names. 6 local tests (DeleteObjectPrototypeTs2790Test) incl. embedded regression control + strictNullChecks-off + index-sig-non-prototype negatives. No self-compile dashboard delta (corpus-A/B fix; default stays off). Full suite 8,983 / 0 / 3 (+6 local)._ _Round 393 (2026-07-04, Phase 17 M2.2 — burn-down #3): **the lib-declared utility-alias modifier cluster + the redefineArray construct-sig double-emit — real-lib A/B recount 34 → 29 corpus failures (omitTypeHelperModifiers01, omitTypeTestErrors01, intersectionsAndOptionalProperties, parameterListAsTupleType, redefineArray), zero corpus regressions.** Two "fix by convergence" changes (make the real-lib path behave like the embedded path that already passes the corpus): (1) under `useRealLibs` the lib's `Pick`/`Omit`/`Readonly`/`Parameters`/`ConstructorParameters`/`ReturnType` resolve to a real `TypeAlias` symbol, so the generic alias-substitution path expands their definitions (`Omit<T,K> = Pick<T, Exclude<keyof T,K>>` → the non-homomorphic mapped `{ [P in K]: T[P] }`) and DROPS optional/readonly modifiers — the embedded lib doesn't declare these names so it hits the modifier-preserving `materialize*` dispatch (symbol==null). New `isBuiltinUtilityAlias(name, symbol)` (name ∈ the six utilities + `symbol.declarations.all { it in builtinLibDecls }`) routes a lib-only utility symbol through the same materializers so both paths agree (the materializers reuse the source property Symbols → `readonly`/`?` survive); a user shadow keeps a non-lib decl → gate false → user def wins. (2) redefineArray `Array = fn` double-emitted B444's TS2739 (missing isArray/from/of/[Symbol.species]) AND a construct-/call-sig TS2322 — tsc reports a structural failure ONCE. Guarded the assignment path's 17.111 branch + the general `canUse && !isAssignable` block with `!targetHasRequiredPropAbsentFromSource(source, tt)` (a null-tolerant helper — `collectMissingProperties`/`getMissingRequiredPropertySymbol` BAIL on a null-members function source); fires ONLY when props are genuinely missing (a construct-only interface with no named props still reports the coarse TS2322). 6 local tests (RealLibsUtilityModifiersTest ×4 with a user-shadow negative control; RealLibsCtorAssignTest ×2 with a construct-only positive control). No self-compile dashboard delta (corpus-A/B fixes; default stays off). Remaining 29 are M3 engine (mapped/conditional/indexed-access/variance/intrinsic-string-mapping/Iterator-inheritance), DOM-dependent (`@lib: dom` → post-v1), M2.3 pin-unwind (typed arrays, TemplateStringsArray), or the getApparentType-Object.prototype gap (keywordExpressionInternalComments). Full suite 8,977 / 0 / 3 (+6 local)._ _Round 392 (2026-07-04, Phase 17 M2.2 — burn-down #2): **the TS2728 lib-file-attribution cluster — real-lib A/B recount 38 → 34 corpus failures (libMembers + externModule + errorMessageOnObjectLiteralType fixed), zero corpus regressions.** Under `useRealLibs` the default library is SPLIT across many files whose node positions OVERLAP (each parsed independently from 0), so the TS2728 "declared here" builders — which resolved the declaring file by POSITION (`resolveDeclarationSourceFile`) — could not disambiguate and false-matched a large USER file (a lib `sub` decl's position landed on `subby` in libMembers.ts → `libMembers.ts:15:19748` instead of `lib.es2015.core.d.ts:--:--`). Fix: NODE-first attribution via `realLibDeclFile: Map<Node, String>` (populated in `bindRealLibs`: every lib statement / interface-class member / inner var-decl node → its DIST fileName), consulted by the three TS2728 builders (`findDeclarationRelatedInfo`, the property-suggestion site, `createPropertyDeclaredHereRelatedInfo`) BEFORE `resolveDeclarationSourceFile`. Real-libs-scoped by construction (map EMPTY under the embedded single-file lib → embedded byte-identical); the `DEPRECATED_STRING_HTML_HELPERS` override still fires on top. 3 local tests (RealLibsTs2728FileTest): non-es5 member → its real lib file (not es5), es5 member → `lib.es5.d.ts`, USER member control → still the user file with a real position (map is lib-only). No self-compile dashboard delta (corpus-A/B fix; default stays off). Full suite 8,971 / 0 / 3 (+3 local)._ _Round 391 (2026-07-04, Phase 17 M2.2 — first burn-down): **value-position spelling suggestions no longer propose type-only lib/cross-file names — the round-390 real-lib A/B failing set drops 40 → 38 (arguments + unaryOperatorsInStrictMode fixed), zero corpus regressions.** Under `useRealLibs` the real lib's `interface IArguments` (type-only — no `declare var` companion) leaked into the VALUE-position spelling-suggestion candidate pool, so an unresolved value-position `arguments` drew "Did you mean 'IArguments'?" (TS2552) where tsc emits a plain TS2304; the embedded lib lacked IArguments entirely, which is why ONLY the real-lib A/B exposed it. Fix: `getSpellingSuggestion` classifies type-only symbols (Type flag, no Value/Module) from `perFileScope[fileName]` (lib globals + cross-file script locals — the SAME source the value-position pool draws from) into `typeOnlyNames`, not just the current file's binder locals; the type-position branch never consults `typeOnlyNames` so it is structurally unaffected (it does carry a symmetric latent FN — it won't suggest a type-only LIB global for a mistyped TYPE — but no corpus test exercises that). 3 local tests (SpellingSuggestionTypeOnlyTest) with a value-carrying positive control (`Object` still suggested) + a type-position control (a user interface still suggested). No self-compile dashboard delta (a corpus-A/B fix, useRealLibs still default off). Full suite 8,968 / 0 / 3 (+3 local)._ _Round 390 (2026-07-04, Phase 17 M2.1a): **the real TypeScript lib sources ship as generated Kotlin — `generateRealLibSources` (build.gradle.kts) extracts the non-DOM ES set (100 files, 565,732 bytes) from the typescript-repo object DB (`git ls-tree`+`git show` at the pin — offline; the sparse working tree never materializes src/lib) into `RealLibFiles.kt` (commonMain srcDir, all Kotlin compiles depend on it), keyed by bare lib name (`es5`, `es2015.core`), byte-faithful incl. CRLF.** The 64 KB class-file string-constant cap is dodged by chunking each file into `sb.append` literals of ≤ 60,000 modified-UTF-8 value bytes split at line boundaries (es5 = 4 chunks) reassembled at runtime — never fold chunks into one literal/`const val` concat. 3 local tests (RealLibFilesTest) pin multi-chunk reassembly + the reference directives M2.1(b) consumes. **Same round, M2.1(b): `RealLibResolver` (RealLibs.kt) — tsc's `libMap` verbatim (110 entries, `es6`/`es7` aliases + `esnext.bigint`-style fallbacks), `targetToLibMap` target defaults (`.full` variants; ES2015 → lib.es6.d.ts, ES5/ES3 → lib.d.ts), the `/// <reference lib>` closure, and tsc's FINAL priority order (`getDefaultLibFilePriority` = libEntries index, NOT DFS — es5 references decorators, which still sorts last); unknown names + unshipped DOM refs surface via `Resolution` side channels. 6 local tests (RealLibResolverTest) against the real headers.** **And M2.1(c): `RealLibSnapshots` — lib files parsed ONCE process-wide (immutable shared ASTs, dist file names `lib.es5.d.ts`/`lib.d.ts`), BINDING fresh per consumer (mergeSymbolTable MUTATES merged-in symbols — a shared bound table would cross-pollute programs; bind-sharing deferred to M5.4/M5.5), `CompilerOptions.useRealLibs` flag (default off); the real es5.d.ts (218 KB) parses + binds cleanly first try. 4 local tests (RealLibSnapshotTest).** **And M2.1(d): checker wiring + corpus A/B — `bindRealLibs()` behind `useRealLibs` (+ `// @useRealLibs` directive); cross-lib-file interface merging proven end-to-end (`[1,2,3].includes(2)` under `@lib: es2016` — es2016.array.include's Array merges onto es5's); the A/B (default temporarily flipped) measured 40 / 8,961 failures, ALL error-baseline subtests, ZERO js-emit regressions, +70% wall time — the M2.2 burn-down list is seeded in the queue; 4 smoke tests (RealLibsInCheckerTest). M2.1 COMPLETE.** No dashboard delta (checker doesn't read RealLibFiles yet). Debugging saga → new CLAUDE.md gotcha variant: a path GLOB in a build-script KDoc opened a NESTED Kotlin block comment that a LATER KDoc's close re-balanced, so build.gradle.kts COMPILED with a silently-DEAD region (tasks "not found", probes never ran, an appended syntax error "built successfully"). Full suite 8,965 / 0 / 3 (after (d))._

_[Rounds 313–383 and older trimmed to STATUS-HISTORY.md.]_
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
