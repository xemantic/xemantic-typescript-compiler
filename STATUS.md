# Status

**Phase 4 — Checker buildout: COMPLETE. Phase 17 — self-compile the TypeScript compiler itself (re-scoped 2026-07-03; "any TypeScript project" is post-v1) — is the LIVE mission (see PLAN-PHASE-5.md; v1 exit = all 8 tsc-source profiles at zero FPs — **the FP leg was MET at round 481 (2026-07-12): all eight profiles at zero real FPs, only env-legit offline artifacts remain**; emit/crash legs verified same round: all 8 profiles emit every program file, exit 0, no crashes — the OFFLINE-VERIFIABLE v1 DEFINITION OF DONE IS MET; next: M5 performance; dashboard: bench/*.tsv, self-compile diagnostics 13,245 → 936 (measured at round 430 — rounds 395–412 burned bounded non-M3 histogram-tail buckets + M3.4 flow-narrowing slices the round-394 "pool picked over" triage missed, all surfaced by bucketing the full `--listAll` output by normalized message shape: TS2499 multi-base-generic misparse, TS2440 type-only-barrel-merge, TS2344 constraint-chain + cross-file `typeParamInternCache` pos-collision, TS2693/TS2709 clodule + destructuring-shadow, TS2314 enum-member-vs-generic, TS2588 nested-shadow, TS2551 Object.setPrototypeOf, SetIterator/MapIterator lib gap, TS2774 local-shadows-function, TS1100 interface-property-name, TS7023 callback-arg self-ref, TS2365 arithmetic local-shadows-function, TS2362 branded-number, TS7053 enum-reverse-mapping, round 408's TS2349 callee flow-narrowing family, and round 409's `export *`-barrel / ESM-`.js` imported-guard flow narrowing (M3.4, TS2339 838 → 672), and round 410's TS2862 constraint-index-sig gate / assign-RHS type-guard narrowing / void-overload-return (2,443 → 2,433), and round 411's M3.4 enum-member discriminant narrowing (barrel-import-resolved, three families UpToDateStatus/TypeMapper/PrivateIdentifierInfo) + type-guard-narrows-a-member-DOWN (2,433 → 2,374, TS2339 672 → 614) + round 412's M3.4 single-type type-guard narrow-DOWN + TS18048 receiver-narrowing (2,374 → 2,373), and round 413's `export *` LEAF-EXPORT gate (barrel-imported `Debug.assert` & every barrel guard now resolves — the TRUE builder.ts blocker, NOT the round-412 depth red herring) + tsc-faithful linear flow-walk iteration + return-path narrowing (2,373 → 1,965, TS2339 614 → 237)), then rounds 414-417 burned more bounded/M3.4 buckets (TS2366 CFA fall-through, NonNull/&&-narrowing arithmetic, closure-loop narrowing, namespace-local `extends` bases: 1,965 → 1,902), and round 418 resolved NESTED type-guard functions (M1.12, 1,902 → 1,854, TS2339 237 → 189: tsc guards `isTupleType`/... are nested in `createTypeChecker` so the binder skips them and `resolveFlowCalleeDecl` missed them - the guard never narrowed, the single biggest TS2339 sub-family), and round 419 resolved properties on INTERSECTION union-members (M1.12, 1,854 → 1,808, TS2339 189 → 143: `getPropertyOfType`/`typeHasOwnProperty` bail on a `Type.Intersection` member so `PropertyAccessExpression | (ElementAccessExpression & …)` FP'd TS2339 on `.parent`; fold the constituents in property resolution + discriminant-narrowing annotation; 0 new FPs, self-compile time −17%), and round 420 resolved TYPE-ALIAS enum-member discriminants in narrowing (M1.12, 1,808 → 1,799, TS2339 143 → 134: a `.kind: <alias>` discriminant member survived a `switch (x.kind) { case }` because `enumMemberKeysOfTypeNode` handled only a direct `Enum.Member`, not a bare-Identifier alias; 0 new FPs), and round 422 burned FIVE bounded M3.4/overload families (1,799 → 1,756: TS2769 60 → 45 — overload arg-check helpers now flow-narrow reference args (mirroring B469) + a synthetic true|false union for guard-narrowed boolean args vs literal overload params; TS2339 134 → 117 — mixed enum + string-literal discriminant unions narrow on disjoint lit:s: keys (PrivateIdentifierInfo); TS18048 10 → 7 — an optional-chain discriminant access x?.kind === <non-nullish> proves the receiver non-nullish; TS2366 12 → 4 — union-.kind exhaustive switches terminate via requiredUnionDiscriminantKeys), and round 423 (a) drove TS2366 to ZERO (1,756 → 1,752: the switch RECEIVER is guard-narrowed + typed before the exhaustive-switch union walk — flow graph via the pass-dedicated implicitReturnFlowGraph, local-const-from-call typing, optional-discriminant @undefined keys, LiteralToken["kind"] indexed-access) and (b) fixed union-TARGET type guards end-to-end (1,752 → 1,708: `x is A | B` PARSED truncated so isCallOrNewExpression/isPropertyNameLiteral/isOptionalChain never narrowed; + aliased conditions `const isFrag = guard(x); if (!isFrag)` via a memoized flow back-walk; + kind-key disjointness beating the lenient relation; TS2339 117 → 104, TS2322 784 → 756) and (c) narrowed truthy optional-chain CALL conditions (1,708 → 1,707: `if (x.y?.size())` proves `x.y` non-nullish), and round 424 burned SEVEN more M3.4 flow-narrowing slices (1,707 → 1,662, TS2339 104 → 68, TS18048 5 → 1, TS2322 756 → 751: loop-entry union suppression with a STRUCTURAL wash gate, call-RHS return-annotation narrowing, closure/join/call-crossing aliased conditions, prefix-path receiver guards, asserts-with-inferred-TP via test-arg predicate inference, assignment-overwrite reset to the declaration/call-RHS resolved type, and the DebugTypeMapper this-narrowing slice (explicit-type-arg asserts re-type `this`; an overload-cluster name collision resolves to the unique TypePredicate-bearing decl) — every step by-site strictly removals), and round 425 found the TS2339 never-cluster ROOT CAUSES (1,662 → 1,600, TS2339 68 → 6: the round-411 enum-discriminant key space SPLIT across resolution paths — canonicalEnumSymbol at all four key-builder sites — plus per-candidate narrow-DOWN distribution in union-target guards/instanceof, aliased ===/switch discriminants, switch-default negative narrowing, typeof-object filtering, boolean-literal discriminant truthiness, destructuring-read narrowing, the loop-entry retry for the single-type suppression, and the positive-empty intersection fallback whose round-423 dead-end verdict was an ARTIFACT of the key split) — driving to 0, and round 426 replaced the B399 TS2563 per-file proxy with the faithful flow-walk depth trip + the asserts-callee call gate (1,600 → 1,593: TS2563 27 → 0; TS2454×20 pre-existing definite-assignment walker FPs unmasked), and round 427 burned that bucket (1,593 → 1,577: TS2454 20 → 4 via tsc's definiteness rules — logical assigns ??=/||=/&&= are DEFINITE so captured reads assume initialized, a !-asserted read assumes initialized, and the anywhere-scan's left-spine walk now applies the target rule per spine node), and round 428 took the first real M3.1 slice of the TS2322/TS2345 cores (1,577 → 1,186, −25%: nullable-union generic param inference + overloaded generic callees for tsc's `x = append(x, item)` idiom (TS2322 751 → 559), the TS2345 histogram top — explicit this-param binding, guarded optional-member args, numeric-enum→number (394 → 280) — the array-literal-vs-union-member string-layer rule (TS2322 → 501), and the body-local-shadows-function anyType registration (TS2345 → 261, TS2769 → 36)), and round 429 drove the TS2345 core 261 → 86 (1,186 → 1,000, −15.7%: call-types lexical shadowing — body-locals vs enclosing params, DESTRUCTURED params via the currentParamBindingNames side set in getTypeOfIdentifier (the round-428 "mini-repro does not reproduce" residue), arrow own-params; embedded String replace/replaceAll/search/split accept RegExp; optional-param union args + NonNull-asserted args; guard-narrowed interface/unknown Identifier args — the ~110-site dominant mechanism, never-param excluded for assertType<never> exhaustive-switch defaults; string-enum→string in isSimpleTypeRelatedTo resolving the round-410 Extension[][] deferral; rest-arg flow narrowing), and round 430 unlocked the `append`/`addRange` inference (936: the `T extends {}` constraint failed `string → {}` and killed every mapper — empty-object-target relation rule with TP-source excluded per genericPrototypeProperty3's pin; readonly-array anchors; TP-from-PREDICATE binding for the getFirstJSDocTag family); self-compile time ~110–150 s single-run band), and round 431 took the FIRST M3.2 slice — the TS7006 core 301 → 11 via contextual typing (nested-fn/lexical callee resolvability, assignment-RHS context from the LHS declared type under the B476 single-applicable-sig rule, ||/??-both/&&-comma-right propagation, intersection/Reference/extends receiver members) — plus engine return-checking in switch/try bodies behind the foreign-TP source gate (un-inferred generic call results bail instead of FP'ing; ~130 T[]/U|undefined/SearchResult<T> return sites cleared) and the TS2367 anchored-disjointness gate (936 → 551, −41%; TS2322 435 → 345, TS7019 4 → 0; by-code strictly shrinking at every landed step) — then extended the foreign-TP gate to the var-decl/assignment/property-write/conditional-return paths (551 → 482, −69 strictly removals; TS2322 → 276; a generic FUNCTION-VALUE source's sig-own TPs stay checkable per 5 corpus pins the suite gate caught), and round 435 took SEVEN more M3.1/M3.2 slices at post-perf-arc iteration speed (482 → 373, −22.6%: generator TReturn returns, fresh-objlit literal props via the freshObjLitRange relation retry, TP-literal-constraint args, the union-decomposition-transparent relation re-entry gate resolving the NodeArray-covariance family ×23, bare-`new` contextual instantiation, the foreign-TP gate on assignment TARGETS (visitor family ×15+), nullish alias-union returns; TS2322 276 → 184, TS7006 11 → 1, TS2345 86 → 79; full-dashboard baseline: compiler 482→373 / tsc-cli 484 / jsTyping 480 / deprecatedCompat 481 / typingsInstallerCore 489 / services 1,603 / server 1,894 / harness 2,193 at 29–41 s wall each), and round 436 took SEVEN more M3.1/M3.2/M3.4 slices (373 → 294, −21.2%: TP-carrying callback-return param skip (forEachEntry family ×14), destructured-LOCAL shadowing (semver/checker + 12 transformer sites), literal-return syntactic union membership (the string-fallback re-widening trap — `return false` vs `Tag | false` ×7, incl. the completely UNPINNED `return "a"` vs `"a" | "b"` shape), constraint-filtered explicit-type-arg overload selection ('true' vs 'false' ×6), overload-helper optional-param union-arg + foreign-TP rules (union-receiver TS2769 ×13), bare-string switch-case narrowing (semver ×3), guard-gated ternary return arms with the tri-state all-leaves-verified early return (×19); TS2322 184 → 158, TS2345 79 → 47, TS2769 30 → 9; round-436 dashboard baseline: compiler 373 / tsc-cli 375 / jsTyping 371 / deprecatedCompat 372 / typingsInstallerCore 380 / services 1,476 / server 1,769 / harness 2,062 at 27–41 s wall), then round 438 (2026-07-07) took FIVE more M3.1/M3.4 narrowing/relation slices (294 → 244, −17%: an empty-object `{}` union-member return-path early return; UNION-target extension of the assignment-RHS AND return-path type-guard narrowing gates (`currentSourceFile = node` / `return node` vs `SourceFile | undefined`); PROPERTY-ACCESS args in the call-arg guard-narrow-DOWN branch (`getExports(node.left)`); and nullish-strip-gated narrowing of object-literal property VALUES (`{ moduleSpecifiers: specs }` where `specs = append(specs, x)`); TS2322 158 → 116, TS2345 47 → 39), then round 439 (2026-07-07) took THREE predicate-overload / arg-narrow-DOWN slices (244 → 228, −16 strictly removals: findAncestor-style type-guard-callback overload return inference + a companion NonNull concrete-union strip, the overload arg-check non-union narrow-DOWN, and its `unknown`-via-typeof extension; TS2769 9 → 1).** Corpus suite green as a permanent regression gate: 12,507 tests / 0 failing / 3 skipped (generated tsgo-relevant corpus + local corner-case tests; count as of round 658 — unchanged, that round added no pins (its 33 M04TypeParamTypedOpsSpineMigrationTest pins landed at the round-657 tail); round 657: +23 M04ArrayToClassCastSpineMigrationTest = 12,474, +33 M04TypeParamTypedOpsSpineMigrationTest at the session tail = 12,507; round 656 added no pins — its 30 M04ArgumentsInFieldSpineMigrationTest pins landed at the round-655 tail); round 655: +37 M04ImplicitAnyNewSpineMigrationTest = 12,421, +30 M04ArgumentsInFieldSpineMigrationTest at the session tail = 12,451; round 654: +40 M04IncDecTypeParamSpineMigrationTest and +9 ConflictMarkerScanTest = 12,384; round 653: +44 M04AbstractAccessSpineMigrationTest = 12,335; round 652: +36 M04ImplicitAnyYieldSpineMigrationTest = 12,291; round 651: +30 M04AbstractContextSpineMigrationTest = 12,255; round 650: +33 M04CtorParamInitSpineMigrationTest = 12,225; round 649: +46 M04DeleteOpSpineMigrationTest = 12,192; round 648: +30 M04TypeParamCastSpineMigrationTest = 12,146; round 647: +41 M04ObjLitSuperSpineMigrationTest = 12,116; round 646: +42 M04ConstLitSpineMigrationTest = 12,075; round 645: +52 M04StrictModeSpineMigrationTest = 12,033; round 644: +33 M04ExpandoSpineMigrationTest = 11,981; round 643: +56 M04TpDefaultsSpineMigrationTest = 11,948; round 642: +35 M04InvalidAssignSpineMigrationTest = 11,892; round 641: +35 M04SuperRebindSpineMigrationTest = 11,857; round 640: +32 M04UndefinedNameSpineMigrationTest = 11,822; round 639: +33 M04EvolvingArraySpineMigrationTest = 11,790; round 638: +25 M04ArgsCollisionSpineMigrationTest = 11,757; round 637: +31 M04GenericIndexWriteSpineMigrationTest = 11,732; round 636: +33 M04PropertyInitSpineMigrationTest = 11,701; round 635: +39 M04ProtectedReadSpineMigrationTest = 11,668; round 633: +15 M04NullCastSpineMigrationTest; round 632: +25 M04ConstEnumSpineMigrationTest; round 631: +31 M04BindingPatternSpineMigrationTest; round 630: +26 M04CastOverlapSpineMigrationTest; round 629: +17 M04DaFlowSpineMigrationTest; round 628: +25 M04Sym2StrSpineMigrationTest; round 627: +24 M04AbstractClassSpineMigrationTest; round 626: +17 M04FnTypedParamSpineMigrationTest; round 625: +18 M04ImplicitThisSpineMigrationTest; round 624: +14 M04SpreadSpineMigrationTest +9 M04ArrayPushSpineMigrationTest; round 623: +8 M03StructureBundleTest; round 622: +10 IntKeyMapTest; round 621: +6 NodeKindIdTest, +5 LongKeyMapTest); earlier baseline 11,347 as of round 587 (−12 net: both giants' audit scaffolding retired with their passes) — incl. 78 giant-migration pins; every per-statement emission of the legacy assignability dispatchers, class property initializers, AND the checkFunctionBody body-level walkers are spine-anchored) — **the full tsgo-relevant TypeScript compiler test suite passes.** _Round 443 (2026-07-08, Phase 17 — module-augmentation family + the module-file-local TYPE-alias leak (Blocker #3)): FOUR bounded fixes, all suppression-only. Compiler profile UNCHANGED (190) but the fixes GENERALIZE hugely: services 591 → 498 (−93), server 887 → 733 (−154), harness 1,118 → 989 (−129); suite 9,504 → 9,512 (+8 local across 3 test files, 0 regressions); 4 fix commits; services diffed via --listAll as strictly by-position removals. (1) TS2664 10 → 0: `resolveModuleSpecifierRelativeJsAware` — a `declare module "../compiler/types.js"` augmentation resolves `.js` → the `.ts` sibling (the base resolver deliberately avoids `.js` per the TS2459 gotcha, so the augmentation target never resolved); purely additive → only SUPPRESSES a false 'cannot be found'. (2) TS2564 17 → 0: a class property whose declared type INCLUDES `undefined` needs no definite assignment (tsc's strictPropertyInitialization `getFalsyFlags(type) & TypeFlags.Undefined`); `checkClassPropertyInit` missed the `| undefined` case, so `SourceFileObject`'s `nameTable: Map<...> | undefined` + siblings FP'd; reuses the existing `typeIncludesUndefined`. (3) TS2304 24 → 2: `buildNamespaceScope` gained a StringLiteralNode branch — a `declare module "X"` augmentation body sees the AUGMENTED module's exports by bare name (Node/NodeArray/SymbolFlags/TypeChecker/__String — compiler/types.ts exports NOT imported into services/types.ts); the 2 remaining are NodeJS `global` (env-legit, offline). INVESTIGATED & REVERTED (dashboard no-op): TS7006 array-element contextual typing — a correct M3.2 enabler (3 passing tests) but the real inferFromUsage.ts `Priority[]` case is B83.5-blocked (`interface Priority` nested in a function → unbound → `Priority[]` resolves to any); land it with nested-type resolution. (4) TS2339 44 → 0 (SourceFileLike, Blocker #3): a module-file-local `type X = A | B` alias leaks into globals and shadows the global `interface X` in OTHER files (TYPE-space analog of round 442's variable leak). importTracker.ts's non-exported `type SourceFileLike = SourceFile | AmbientModuleDeclaration` won the last-wins merge over compiler/types.ts's `interface SourceFileLike`, so a `sourceFile: SourceFileLike` param resolved to the bogus union (AmbientModuleDeclaration has no `.text`); pinned via an instrumented probe (whole-program-only — minimal repros don't reproduce). Built `conflatedTypeAliasFiles`; checkMemberAccessMissing bails a union-receiver TS2339 when the receiver displays as a conflated name in a NON-alias file (own-file still fires). NEXT (services @ 498, all deep): TS2322×235 / TS2339×85 (residual conflations — Type×20/Info×12) / TS2345×54 M3 relation, TS2416×11, TS2353×10, TS2740×9/TS2739×8._ 

_Round 659 (2026-07-25) — **(M0.4-AB) THE ARC MEASUREMENT, PAID: the M0.4 migration arc is NOT a wall-clock lever, the mechanism is measured, and M0.4 is CLOSED at 35 passes — (M1) identity stability is next.** No compiler change this round; it paid a debt the arc had carried since round 624 ("A/B the ARC once several passes land") and turned it into a decision. Method: pre-arc binary 4b0dfcc7 (round-623 HEAD) vs HEAD e9d8279d, both class dirs snapshotted, NO recompile between measurements, alternating within-pair order, medians. compiler profile, 6 interleaved pairs: pre 28,945 ms → post 29,015 = **+0.24%**, post wins 3/6, per-pair deltas −667…+1,190 ms; harness, 2 pairs: 40,256 → 39,605 = **−1.6%**, post wins 2/2. The per-pair spread (~4% of total) is an order of magnitude above the effect, so the honest statement is a SMALL gain in 0–2% — inside the ±2% band the PERF ground rules refuse to land on. **THE MECHANISM, measured (the transferable finding): 75% of a migrated pass's cost REAPPEARS INSIDE checkSpine** — same-run --passTiming both sides, the 35 deleted rows summed 3,146 ms while checkSpine grew 18,896 → 21,253 = +2,358 ms. So the arc's premise (the tail is mostly REDUNDANT TRAVERSAL one walk eliminates) is wrong for these passes: their cost is per-node work a single walk still does, and folding them in converts "N walks over the tree" into "N when(kindId) dispatches + memoized ancestor-climb reach classifiers on every node of every file" — same order of multiplication, relocated. That is also why every single-pass round measured neutral in isolation: each was genuinely ~25% of its row ≈ 15 ms. **The rate arithmetic that closes the arc:** the remaining ~90 rows >20 ms ≈ 4.3 s would buy ~25% × 4.3 s ≈ 1.1 s ≈ 4% of wall for ~90 single-pass rounds, while (M1) targets ≤15–20 s from ~29 s = 30–45%. Stopping is not a retreat — the 35 migrations are behaviour-preserving, deleted ~8 k lines of walker recursion and made the spine the single place per-node checks live (the architectural value INV wanted); what is retired is the CLAIM that continuing buys speed. Rule written into the queue: do NOT migrate another tail pass FOR PERFORMANCE — only when it is on the path of another change — and keep M0.4's migration-pattern zoo (20+ documented shapes, round 624 → round 658's ambient-region capture) as the reference for HOW. INSTRUMENT LESSON reconfirmed: the first single un-interleaved --passTiming sample this session showed the post 0.5 s SLOWER, exactly the artefact the interleaving rule kills. Both medians went to the local bench/self-compile-tsc.tsv, but `bench/` is GITIGNORED — the durable record of this A/B is the round-659 session note, which carries every per-pair number; suite untouched at 12,507/0/3 (no code change). NEXT: (M1) — and its real first step is (b) canonicalize narrowing outputs, because the round-596/599 memo dead-ends were both revived-as-blocked pending exactly that._

_Round 658 (2026-07-25) — (M0.4) thirty-fifth tail-pass migration: checkTypeParamTypedOps (B60.12 — TS2339/TS2349/TS2351 for property access, call and `new` on a value typed as an EFFECTIVELY UNCONSTRAINED type parameter, whose apparent type `{}` has no call/construct signatures and no own properties; 71.0 ms — the #36 row of the round-656 table) is ON THE SPINE; the driver + the whole walkStmtsForTypeParamOps / walkStmtForTypeParamOps / walkFnLikeBodyForTypeParamOps / emitTypeParamTypedOps recursion DELETED, the three emissions anchor-called at PropertyAccess/Call/New enters. Shape: the round-635 PUSH-BASED ORDER-DEPENDENT variant — and the first migration whose downward context includes a **TYPE-SYSTEM AMBIENT** rather than only plain data, which is the transferable lesson. The data half is familiar (`tpVars` is MUTATED in statement order, LEAKS through block/if/loop/try/namespace descents, and REBUILDS at every function-like body from its own parameters → a LIFO of frames at exactly the legacy new-map/new-scope boundaries, skipped for a bodyless fn-like so no interning happens there either). The ambient half is new: the legacy walk held `withInternedTpScope(tps, withAst = true)` OPEN as a REGION across the subtree, and a spine migration cannot hold an inline region open across nodes — **so the boundary CAPTURES the region's result: run withInternedTpScope for its interning and constraint-materialization side effects, read currentTypeParamScope/currentTypeParamAstForOps from INSIDE the block, carry that pair on the frame, install it around each dispatch only** (first-touch behaviour identical; the round-538 never-walk-wide rule obeyed). File-level base = the spine-entry RESTING pair, since the legacy driver installed only currentFileLocals and inherited its slot's ambient (the round-655 reproduce-the-driver rule in its inherit-nothing form). Second detail: the legacy VariableStatement arm's TWO loops (record every declaration, THEN emit on the initializers) reproduce EXACTLY as a recording dispatch at that statement's own ENTER, before any child is walked — so `var x: T = x.foo` still sees `x` recorded, no special casing. Reach is unusually NARROW and every gap is deliberate: `for` HEAD initializers, `switch`, object and array literals, templates, all four cast forms, await/yield, typeof/void/delete operands, spreads and comma chains have NO arm — and the big one, ARROW and function-EXPRESSION bodies are never walked (a TypeParam op there is UNREACHED, not merely unrecorded); on the class side only method/ctor/accessor BODIES are reached, never property initializers or static blocks. No sibling move needed — the TS2349-retracting checkUnresolvableSelfReferencingAwaitedUnion sits after both the old slot and checkSpine. Gates: 33 pins (M04TypeParamTypedOpsSpineMigrationTest, written green against the LEGACY walkers last session) 33/33 on the spine on the FIRST run — the FOURTH consecutive single-attempt migration, so pre-writing pins against the legacy pass is now the established discipline for this arc; suite 12,507/0 (3 skipped, unchanged); `--listAll` ×8 byte-identical vs the round-657 capture (46×7/94, only `time:` differs); `--partitionCheck 2` EQUIVALENT ×8; pass table 405 → 404 (zero checkTypeParamTypedOps rows; checkSpine 20.9 s — in-band); warning-clean. M0.4 total: top THIRTY-FIVE tail passes migrated. NEXT by cost: checkVarHoistRedeclaration 68.9 ms, checkCallTypeArgCount 66.2, checkIllegalSuperCallsInNestedFunctions 62.7, checkTypeArgumentConstraints 62.7, checkSpreadPropertyOverrides 62.5 (checkCrossFileModuleAugmentationDuplicates 109.7 stays SKIP — cross-file). Per-pass wall value is now clearly below the noise floor (~65 ms on a ~25 s checker-init), so the honest next milestone is the ARC-level interleaved A/B once a few more land — or a decision to stop the arc and take (M1) identity stability, which carries the ≤15–20 s path._

_Round 657 (2026-07-25) — (M0.4) thirty-fourth tail-pass migration: checkArrayToClassCastOverlap (TS2352 for a `<C<X>>arr` angle-bracket cast from an ARRAY-typed source to a different GENERIC CLASS target missing a required non-prototype property; 72.5 ms — the #35 row of the round-656 table) is ON THE SPINE, and it is **THE CHEAPEST MIGRATION CLASS THERE IS: the pass OWNED NO WALK.** The tell is one grep — a tail pass whose driver only DRIVES A SHARED WALKER with its emitter as the callback has no reach of its own, and if a sibling driving the SAME walker is already on the spine, the migration is a FOLD-IN: add the leaf call to that sibling's arm, delete the driver, done. Here the sibling is round 630's checkSameTargetReferenceCastOverlap (joined by round 632's checkNullTypeAssertionOverlap and round 633's flag-arm lift), whose spineCoStatus/spineCoEdge already reproduce walkTypeAssertionsInStmt/-InExpr exactly — so CO_REACHED IS this pass's reach BY CONSTRUCTION: no new classifier, no edge set to diff, no frames, no ctx rebuild, no status channel, no setup/teardown, no memo; the whole diff is one call plus the driver's deletion. Two placement details carry the correctness content: the leaf goes LAST in the TypeAssertionExpression arm because its legacy slot (14''c) ran AFTER the two round-630/632 passes (keeping the insertion order of two TS2352s at one shared position byte-exact — the stable diagnostic sort hides insertion order except on exact start/length/code/message ties), and the legacy per-file ambient needs NO new install since checkSpine's own per-file loop already sets currentFileLocals to the file's binder locals and the shared arm installs currentCheckFileName while nulling currentFlowGraph. The shared walker SURVIVES (round-630 sync rule) — its last owner is the option-gated checkErasableSyntaxOnly, never in the compiler profile's table. PIN CALIBRATION worth remembering: on a SHARED anchor a "this leaf stays silent" negative control must discriminate by the emitter's own message CHAIN, not by the code count — `<C<string>>s` for `s: string` DOES draw a TS2352, from a sibling emitter. Gates: 23 pins (M04ArrayToClassCastSpineMigrationTest, written green against the LEGACY pass in a preceding commit — the third consecutive round using that discipline) unchanged and green on the fold-in; suite 12,451 → 12,474/0 (3 skipped); `--listAll` ×8 byte-identical vs the round-656 capture from this same tree (46×7/94, only the `time:` line differs — consecutive rounds touching only the migrating pass can reuse the prior capture as the pre-migration baseline at zero cost); `--partitionCheck 2` EQUIVALENT ×8; pass table 406 → 405 (zero checkArrayToClassCastOverlap rows; checkSpine 20.9 s — in-band); warning-clean. M0.4 total: top THIRTY-FOUR tail passes migrated. NEXT by cost: checkTypeParamTypedOps 71.0 ms, checkVarHoistRedeclaration 68.9, checkCallTypeArgCount 66.2, checkIllegalSuperCallsInNestedFunctions 62.7, checkTypeArgumentConstraints 62.7 — and before picking one, grep its driver for a shared-walker call. SESSION TAIL: the #36 pass (checkTypeParamTypedOps, 71.0 ms) is PREPARED and is NOT a fold-in — no slot-move pre-gate needed (already post-spine at slot 14''g) and its 33 migration pins are green against the LEGACY walkers on the FIRST run (suite 12,474 → 12,507/0). Shape diagnosed for the next session: a DOWNWARD-MAP migration of the round-635 ORDER-DEPENDENT flavour — tpVars is MUTATED in statement order, LEAKS through if/loop/try/namespace descents, and REBUILDS at every function-like body from its own parameters (so nothing leaks inward), with withInternedTpScope re-pushing the TP scope there as the pass's one ambient; reach is unusually NARROW (class DECLARATION method/ctor/accessor bodies walked; arrow and fn-EXPRESSION bodies, class property initializers, `for` heads and `switch` have NO arm), all pinned both directions._

_Round 656 (2026-07-25) — (M0.4) thirty-third tail-pass migration: checkArgumentsInClassFieldInitializers (TS2815 — an `arguments` reference in a class PROPERTY INITIALIZER or a class STATIC BLOCK; 82.9 ms — the #34 row of the round-655 table) is ON THE SPINE; the driver and the whole findClassesForTS2815InStatements/-InExpr (ROUTING) + checkClassMembersForTS2815 (MEMBER dispatch) + checkExprForTS2815Arguments / checkStatement(s)ForTS2815Arguments (EMISSION) recursion DELETED, emitTS2815 anchor-called at `arguments` Identifier enters. The shape was pre-diagnosed last session (the round-640 TWO-INTERLEAVED-WALKS variant with the round-653 re-entry boundary) and held, with ONE template refinement worth naming: **when two interleaved walks share MOST of their arms — here ~30 of ~45, every shared arm having an identical child set whose child simply KEEPS the parent's status — key the fold on the node KIND and branch on `pStatus` only inside the differing arms, so the walk identity rides along a pass-through arm (`-> pStatus`) for free; round 640's outer `when (pStatus)` form is right only when the two walks' node sets are near-DISJOINT, otherwise it duplicates 30 arms verbatim, which is exactly how an arm diff silently drifts.** Three statuses: AF_ROUTE (class-finding), AF_EMIT (inside a property initializer / static block, where the anchor fires), AF_MEMBER (the class/objlit member conduit whose member KIND picks the resuming walk — property initializer/static block → AF_EMIT, method/ctor/accessor body → AF_ROUTE, since those bind their own `arguments`); the re-entries that make the walks inseparable are ordinary edges in that scheme. All five reach asymmetries pinned both directions: if/loop HEADS + switch SUBJECT + case EXPRESSIONS are EMISSION-only (routing descends statement BODIES only); objlit METHOD/ACCESSOR bodies EMISSION-only; an arrow's parameter DEFAULTS EMISSION-only (a Parameter is reached only through an EMISSION-walked arrow, and only its initializer — so a parameter *named* `arguments` draws nothing); a ClassExpression is `declare`-gated in ROUTING and UNGATED in EMISSION (frozen legacy asymmetry); namespaces and `export =` are ROUTING-only. Multiplicity 1 everywhere (no INT-valued round-636 classifier), fully syntactic — no type resolution, no flow, NO ambient install at all (the first since round 650 needing no sandwich); the anchor pre-gates on `text == "arguments"` before the reach climb since Identifier is the most frequent node kind. GATE NOTE: TS2815 fires 0 times across all 8 profiles and its corpus baselines are `.errors.txt` subtests, so listAll ×8 pins PURE NON-PERTURBATION and the 30 pins carry the equivalence burden: 30/30 on the spine on the FIRST run — the second consecutive round where pre-writing the pins against the legacy pass made the migration a single-attempt change. Suite 12,451/0 (3 skipped, unchanged — the pins landed at the round-655 tail); `--listAll` ×8 byte-identical vs a purpose-built LEGACY capture from this same tree (46×7/94, only the `time:` line differs); `--partitionCheck 2` EQUIVALENT ×8; pass table 407 → 406 (zero checkArgumentsInClassFieldInitializers rows; checkSpine 21.1 s — in-band); warning-clean. M0.4 total: top THIRTY-THREE tail passes migrated. NEXT (fresh round-656 table): checkArrayToClassCastOverlap 72.5 ms, checkTypeParamTypedOps 71.0, checkVarHoistRedeclaration 68.9, checkCallTypeArgCount 66.2, checkIllegalSuperCallsInNestedFunctions 62.7, checkTypeArgumentConstraints 62.7 (checkCrossFileModuleAugmentationDuplicates, 109.7 ms, stays SKIP — cross-file); the tail is now VERY flat — no per-file row above 73 ms, ~90 passes >20 ms carrying the residual ~4.3 s, so the arc-level interleaved A/B is the only honest wall evidence left._

_Round 655 (2026-07-25) — (M0.4) thirty-second tail-pass migration: checkImplicitAnyNewExpressions (TS7009 — `new F()` whose target is a plain FUNCTION symbol, so it carries no construct signature and the expression implicitly has type `any`; 66.9 ms — the #33 row of the round-651 table) is ON THE SPINE; the driver + the whole walkNewImplicitAnyStmts/-Stmt/-ClassMember/-InExpr recursion (~8.8 KB) DELETED, the emission leaf checkNewExprImplicitAny anchor-called at NewExpression enters. THE SIMPLEST MIGRATION CLASS, named for the next migrator: **a pass whose walk threads NO DOWNWARD VALUE AT ALL — only the invariant source/fileName pair — needs no ctx rebuild, no frames, no leave hook and no status channel; the whole migration is a memoized BINARY reach classifier plus an anchor (the round-649 spineDelStatus shape with a different edge set).** The tell is mechanical: grep the deleted walker's signature — if every recursive call passes the arguments it received, none of the downward machinery accumulated since round 624 is needed. Two per-migrator notes. (1) The ambient install is the FILE's OWN binder locals, not the spine-entry resting value: this pass's legacy DRIVER installed `result.locals` itself (unlike rounds 624/625/631/649, whose legacy slots ran outside the spine's per-file install), so the faithful reproduction is also the one immune to any other handler's ambient. (2) The arm diff against the same-shaped round-649 `del` classifier is REAL and is exactly what a blind transcription would get wrong — objlit method/accessor bodies, `for`-head DECL-LIST initializers and switch case EXPRESSIONS are walked here and not there. Frozen quirks pinned both directions: class EXPRESSIONS, `<T>expr` type assertions, `satisfies` casts, tagged templates, parameter DEFAULTS and class STATIC BLOCKS are NOT walked, while class-DECLARATION property initializers, objlit method/accessor bodies and the typeof/void/delete/await/yield operands ARE. Fully syntactic apart from one flags-only symbol consult; binderResults driver → the partition view, `--partitionCheck 2` EQUIVALENT ×8. GATE NOTE: TS7009 fires 0 times across all 8 profiles (real code never `new`s a plain function) and its corpus baselines are `.errors.txt` subtests, so listAll ×8 pins PURE NON-PERTURBATION and the 37 pins (written green against the LEGACY walker at its slot-move slot) carry the equivalence burden: 37/37 on the spine on the FIRST run, no calibration. Suite 12,384 → 12,421/0 (3 skipped); `--listAll` ×8 byte-identical vs the pre-migration capture (46×7/94); pass table 408 → 407 (zero checkImplicitAnyNewExpressions rows; checkSpine 20.8 s — in-band); warning-clean. M0.4 total: top THIRTY-TWO tail passes migrated. NEXT (fresh round-655 table): checkArgumentsInClassFieldInitializers 82.9 ms, checkTypeParamTypedOps 72.0, checkArrayToClassCastOverlap 69.4, checkIllegalSuperCallsInNestedFunctions 62.2 (checkCrossFileModuleAugmentationDuplicates 106.9 stays SKIP — cross-file); the tail stays FLAT — no per-file row above 83 ms. SESSION TAIL: the #34 pass (checkArgumentsInClassFieldInitializers, TS2815, 82.9 ms) is PREPARED — slot-move pre-gate LANDED (intact to the post-spine slot; suite 12,421/0; listAll ×8 byte-identical pre-vs-post; fully syntactic, no ambient install, no TS2815 list consumers) and its 30 migration pins are green against the LEGACY walkers on the first run (suite 12,451/0). Shape already diagnosed for the next session: the round-640 TWO-INTERLEAVED-WALKS variant with the round-653 re-entry split (a ROUTING walk finding non-`declare` classes + an EMISSION walk inside property initializers / static blocks, each re-entering the other — fn expressions/declarations/objlit methods bind their own `arguments` and hand back to routing, a ClassExpression hands to the member dispatch, arrows stay in emission including parameter defaults); the risk surface is that the two walks reach DIFFERENT positions (routing descends only statement BODIES; emission descends if conditions, loop heads, the switch subject and case expressions too), pinned both directions._

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `docs/history/STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `docs/history/STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
