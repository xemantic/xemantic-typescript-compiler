# Status

**Phase 4 — Checker buildout: COMPLETE. Phase 17 — self-compile the TypeScript compiler itself (re-scoped 2026-07-03; "any TypeScript project" is post-v1) — is the LIVE mission (see PLAN-PHASE-5.md; v1 exit = all 8 tsc-source profiles at zero FPs; dashboard: bench/*.tsv, self-compile diagnostics 13,245 → 1,799 (measured at round 420 — rounds 395–412 burned bounded non-M3 histogram-tail buckets + M3.4 flow-narrowing slices the round-394 "pool picked over" triage missed, all surfaced by bucketing the full `--listAll` output by normalized message shape: TS2499 multi-base-generic misparse, TS2440 type-only-barrel-merge, TS2344 constraint-chain + cross-file `typeParamInternCache` pos-collision, TS2693/TS2709 clodule + destructuring-shadow, TS2314 enum-member-vs-generic, TS2588 nested-shadow, TS2551 Object.setPrototypeOf, SetIterator/MapIterator lib gap, TS2774 local-shadows-function, TS1100 interface-property-name, TS7023 callback-arg self-ref, TS2365 arithmetic local-shadows-function, TS2362 branded-number, TS7053 enum-reverse-mapping, round 408's TS2349 callee flow-narrowing family, and round 409's `export *`-barrel / ESM-`.js` imported-guard flow narrowing (M3.4, TS2339 838 → 672), and round 410's TS2862 constraint-index-sig gate / assign-RHS type-guard narrowing / void-overload-return (2,443 → 2,433), and round 411's M3.4 enum-member discriminant narrowing (barrel-import-resolved, three families UpToDateStatus/TypeMapper/PrivateIdentifierInfo) + type-guard-narrows-a-member-DOWN (2,433 → 2,374, TS2339 672 → 614) + round 412's M3.4 single-type type-guard narrow-DOWN + TS18048 receiver-narrowing (2,374 → 2,373), and round 413's `export *` LEAF-EXPORT gate (barrel-imported `Debug.assert` & every barrel guard now resolves — the TRUE builder.ts blocker, NOT the round-412 depth red herring) + tsc-faithful linear flow-walk iteration + return-path narrowing (2,373 → 1,965, TS2339 614 → 237)), then rounds 414-417 burned more bounded/M3.4 buckets (TS2366 CFA fall-through, NonNull/&&-narrowing arithmetic, closure-loop narrowing, namespace-local `extends` bases: 1,965 → 1,902), and round 418 resolved NESTED type-guard functions (M1.12, 1,902 → 1,854, TS2339 237 → 189: tsc guards `isTupleType`/... are nested in `createTypeChecker` so the binder skips them and `resolveFlowCalleeDecl` missed them - the guard never narrowed, the single biggest TS2339 sub-family), and round 419 resolved properties on INTERSECTION union-members (M1.12, 1,854 → 1,808, TS2339 189 → 143: `getPropertyOfType`/`typeHasOwnProperty` bail on a `Type.Intersection` member so `PropertyAccessExpression | (ElementAccessExpression & …)` FP'd TS2339 on `.parent`; fold the constituents in property resolution + discriminant-narrowing annotation; 0 new FPs, self-compile time −17%), and round 420 resolved TYPE-ALIAS enum-member discriminants in narrowing (M1.12, 1,808 → 1,799, TS2339 143 → 134: a `.kind: <alias>` discriminant member survived a `switch (x.kind) { case }` because `enumMemberKeysOfTypeNode` handled only a direct `Enum.Member`, not a bare-Identifier alias; 0 new FPs)) — driving to 0; self-compile time ~100 s).** Corpus suite green as a permanent regression gate: 9,178 tests / 0 failing / 3 skipped (generated tsgo-relevant corpus + local corner-case tests) — **the full tsgo-relevant TypeScript compiler test suite passes.** _Round 421 (2026-07-06, Phase 17 — maintenance, owner-requested: CLAUDE.md trim + root-folder history reorg. No code changes; suite re-verified green. (1) CLAUDE.md had silently REGROWN to 594 KB / ~147k tokens (3.5× its own 170 KB cap from the 2026-06-10 audit), loaded into every session. Applied the Phase 17 residency criterion — corpus-unique pin/walker docs, per-test parser-recovery cascades, emitter/baseline byte minutiae, and checkJs one-offs are FROZEN behavior protected by the 2-minute corpus gate, not agent memory — and archived 250 of 650 entries (316 KB) to docs/history/CLAUDE-GOTCHAS-ARCHIVE.md (greppable by walker name/B-number/test/TS-code; new rule: grep it BEFORE touching a dedicated walker or frozen subsystem). CLAUDE.md 594 → 280 KB (−53%); kept process traps, live-subsystem architecture, and measured negative knowledge + a distilled 'Measured dead-ends' block. (2) Root .md files 19 → 7: moved STATUS-HISTORY, PLAN-PHASE-4-HISTORY (4.1 MB), PLAN-PHASE-5-HISTORY, PLAN-PHASE-3(-done), PLAN.md, NEXT-SESSION.md, FAILURES.md, both DESIGN docs, ANALYSIS-A0, TYPESCRIPT-TEST-HARNESS.md to docs/history/; updated the path couplings (scripts/find_candidates.py + scripts/mine_small_diffs.py smoke-tested green, CLAUDE.md/STATUS.md/PLAN-PHASE-4.md pointers). PLAN-PHASE-4.md stays at root (live M3-blockers reference)._ _Round 420 (2026-07-06, Phase 17 — M1.12: resolve TYPE-ALIAS enum-member discriminants in narrowing. Self-compile (compiler profile) 1,808 → 1,799 (−9, TS2339 143 → 134); suite 9,177 → 9,178 (+1 local, 0 regressions); 1 fix commit (47c655c8). A discriminated-union member whose `.kind` is a TYPE ALIAS to a union of enum members (`ProjectReferenceFile { kind: ProjectReferenceFileKind }`, `ProjectReferenceFileKind = FileIncludeKind.Source | FileIncludeKind.Output`) SURVIVED a `switch (x.kind) { case … }` narrowing — `enumMemberKeysOfTypeNode`'s TypeReference branch handled only a DIRECT `Enum.Member` (QualifiedName), not a bare-Identifier alias, so the member's `.kind` read as unknown → conservatively kept → the over-wide narrowed union FP'd TS2339 on a case-body property (watch.ts `reason.typeReference`, ×9). Resolve + recurse the alias body (mirroring round-415's `enumSwitchKeysFromTypeNode`), depth-guarded (≤8). Continues the round-419 lesson — the intersection/alias discriminant-reading gap must be closed at EVERY narrowing site._ _Round 419 (2026-07-06, Phase 17 — M1.12: resolve properties on INTERSECTION union-members. Self-compile (compiler profile) 1,854 → 1,808 (−46, TS2339 189 → 143); suite 9,173 → 9,177 (+4 local, 0 regressions); 1 fix commit (39f22170). A UNION whose member is itself an INTERSECTION (`PropertyAccessExpression | (ElementAccessExpression & Declaration & {…})`, tsc's `BindableStaticAccessExpression`) FP'd TS2339 on a property INHERITED by the intersection arm — `getPropertyOfType` has NO Intersection branch (deliberately; "modifying it is broad") and `typeHasOwnProperty` bails on a `Type.Intersection` member (`type !is Type.Object`), so ~28 sites in binder.ts/utilities.ts FP'd `.parent`/`.expression`/`.moduleSpecifier`. TWO coupled pieces (the 2nd because the 1st EXPOSED a switch-narrowing gap): (1) `resolveMemberPropertyType` folds an intersection member's constituents (a property exists on `A & B` iff ANY constituent has it — the round-352 rule); used in the B83.4e union-member property fold (`computeRawTypeOfPropertyAccess`) AND in `checkMemberAccessMissing`'s `memberHasIt` (the emission). (2) THE 1ST PIECE ALONE INTRODUCED 3 NEW FPs at utilities.ts — a `switch (node.kind) { case Import: case Export: return node.moduleSpecifier }` left an intersection member (`BindingPattern & {…}`, a NON-matching `.kind`) in the narrowed union because `discriminantPropAnnotation` bailed on the intersection (`as? Type.Object`) → the member's `.kind` read as unknown → it SURVIVED the switch. Folding the intersection constituents in `discriminantPropAnnotation` filters it correctly → 0 new FPs. PERF: self-compile TIME −17% (122 → 101 s) — the more-accurate narrowing + fewer FP elaborations RECLAIM round 418's +17%, so the two rounds net roughly FLAT on time while −94 on FPs; both dashboard metrics improved. +4 local tests (IntersectionMemberPropertyTest). META: the union-member intersection fold is the documented `getPropertyOfType`-has-no-Intersection-branch gap — fixed narrowly in the union-member path (NOT `getPropertyOfType` itself, which stays broad-risk), and the 1st cut's 3-FP regression showed the fold must be applied to BOTH property resolution AND discriminant-narrowing reading._ _Round 418 (2026-07-06, Phase 17 — M1.12: resolve NESTED type-guard functions so their narrowing fires. Self-compile (compiler profile) 1,902 → 1,854 (−48, TS2339 237 → 189); suite 9,168 → 9,173 (+5 local, 0 regressions); 1 fix commit (7a806360). tsc's giant closures (`createTypeChecker`, ...) declare their type guards (`isTupleType`, `isGenericTupleType`, `isArrayOrTupleType`, ...) as NESTED functions, which the binder does NOT bind (B83.5), so `resolveFlowCalleeDecl` missed the callee and the guard never narrowed — the single biggest TS2339 sub-family (`x.target` on `Type` after `isTupleType(x)`, ×46 on checker.ts). THREE coupled pieces (the resolver exposes the other two): (1) `resolveFlowCalleeDecl` falls back to a program-wide UNIQUE-name FunctionDeclaration map (`uniqueFunctionDeclByName`/`buildNestedFunctionMap`, a full-tree worklist walk); a colliding name → null → no narrowing (conservative). (2) `checkMemberAccessMissing` gains a single-type narrow-DOWN suppression — the receiver-narrowing consumers (`computeRawTypeOfPropertyAccess`, the narrowed-to-never branch) are ALL gated on `Type.Union`, so a NON-union narrow-DOWN (`Type` → `TupleTypeReference`) never reaches the property access; suppress when the narrowed strict subtype (`narrowed <: raw`, non-union, non-never) HAS the property. (3) `narrowByCallPredicate` falls back to the antecedent union when a POSITIVE guard against an INTERSECTION target `X & {p}` drops every constituent (declarations.ts's `shouldPrintWithInitializer(node): node is CanHaveLiteralInitializer & { initializer: Expression }` — would collapse to `never`), narrowly gated `targetType is Type.Intersection` + empty result. THE REGRESSION THAT SHAPED THE FINAL FORM: the instanceofWithStructurallyIdenticalTypes corpus test EXPECTS TS2339-on-`never` at `x.item` — a GENUINE never (union C1|C2|C3 exhausted by `!isC1 && !isC2` on structurally-identical types, via the NEGATIVE branch). A blanket never-fix (any positive-empty → union) AND a raw-has-property suppression BOTH over-suppressed it; the distinction is positive-collapse (bug) vs negative-exhaustion (correct) = the `Type.Intersection`-gated positive-branch fallback + the `narrowed !== never` guard-DOWN gate. 0 NEW self-compile FPs (a clean burn-down, no swaps). Perf: self-compile +17% (the nested guards now resolve → more narrowing walks; correctness-first, M5). +5 local tests (NestedTypeGuardNarrowingTest: nested if/&&/union narrow + FP-safety missing-prop + ambiguous-name-no-narrow)._ _Round 417 (2026-07-06, Phase 17 — M1.12: resolve NAMESPACE-LOCAL interface/class `extends` bases. Self-compile (compiler profile) 1,904 → 1,902 (−2); suite 9,161 → 9,168 (+7 local, 0 regressions); 1 commit (2a05b3ea). `getTypeFromBaseTypeExpression` resolved a bare-Identifier base via `globals` only, so a namespace-local base (`namespace M { interface Base {}; interface Derived extends Base {} }`) was never inherited — FP'ing TS2353 (an inherited member in an object literal looked "excess") on tsc's builderState.ts (`ManyToManyPathMap extends ReadonlyManyToManyPathMap`). The fix is COORDINATED across the two base-resolution sites, because the first cut REGRESSED genericRecursiveImplicitConstructorErrors3: (1) `getTypeFromBaseTypeExpression` resolves the base through the enclosing namespace (`lookupTypeSymbolInInferenceNamespace`, pushed by resolveInterfaceMembers) before `globals` — a strict superset; (2) `lookupInstanceMemberInResolvableChain` gains a threaded `enclosingNs` so the `this.X` TS2339 check resolves a namespace-local base chain. The second site is LOAD-BEARING: once base resolution populates `type.baseTypes` for a namespace-local base, the "class has base types" branch runs, and a globals-only base lookup returns `null` (uncertain → bail), swallowing the genuinely-missing-member TS2339 (`PullTypeSymbol extends PullSymbol`, both in `namespace TypeScript`; `this.isArray()` is declared nowhere → tsc emits TS2339). FP-safe BY CONSTRUCTION: the chain returns `false` (→ emit) ONLY when it is fully resolvable and the member is absent everywhere; any uncertainty propagates `null`. Cleared builderState:126 (TS2353 getKeys) + builderState:339 (downstream TS2322); builderState:168 morphed TS2739→TS2740 (a pre-existing interface+namespace-merge FP — `const map: ManyToManyPathMap` mis-resolves as `BuilderState`; orthogonal M3). +7 local tests (NamespaceLocalBaseInheritanceTest: excess/nested/module-level + the this-member TS2339 pair, both edges as negative controls). META (hard-won): the FIRST cut looked like B451 id-drift (genericClasses0 "passed in isolation, failed in full suite") but a naive JUnit-XML regex had MISATTRIBUTED the failure — the real failing test was genericRecursiveImplicitConstructorErrors3, and the mechanism was a REAL semantic interaction (the this-member "has base types" heuristic), not id-drift. Use an XML parser, not a self-closing-tag-fragile regex._ _[Rounds 313–404 and older trimmed to docs/history/STATUS-HISTORY.md.]_
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `docs/history/STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `docs/history/STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
