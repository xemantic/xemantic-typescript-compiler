# Status

**Phase 4 — Checker buildout.** 9,752 / 10,086 tests passing (~96.7%, 334 failing). _Round 190 (2026-06-17, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+3 net so far (337→334 failing, 9749→9752 passing), B475 + B476 + B478 (3 commits), ZERO regressions at each step (exact failing-set diff via `scripts/fail_set.py`).** Re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=8 all type-engine; OUTPUT=1 tsgo-irrelevant), then diff-ranked ALL 337 failures and found two CLOSE candidates (>3-line diffs, so find_candidates' ≤3-code window missed them) in the contextual-typing area. **B475 (+1) — objectLitIndexerContextualType (index-signature contextual typing of object-literal arrow params in the arithmetic walker).** `x = { s: t => t*t }` where `x: I`, `I` has `[s:string]: (s:string)=>number`: the arithmetic walker (`checkArithmeticInExpr`) now sets the LHS type as `contextualType` for an ObjectLiteralExpression assignment RHS, propagates each property's contextual VALUE type (named prop OR index sig, via the existing `lookupPropertyTypeForCtx` — `Type.Interface` is a `Type.Object` so its index sigs resolve) into the property initializer, and the new `applyContextualParamTypesForArrow` fills `currentLocalTypes` for unannotated arrow params from the contextual single-call-sig fn type → `t` resolves to `string` → `t*t` fires TS2362/TS2363. Numeric keys route to the number index sig (string fallback); string keys to string only (no number-index match → TS2353 owns it). Body walked with `contextualType` cleared so nested object literals don't inherit the fn type. **B476 (+1) — contextualTypingWithGenericAndNonGenericSignature (overloaded contextual fn-type provides NO single signature).** An arrow assigned to an OVERLOADED fn-type (`f2: { (x:string,y:number):string; <T,U>(x:T,y:U):T }`) gets no single contextual signature in non-strict mode (tsc only intersects under strictFunctionTypes, which we don't model). Two coupled fixes: (1) `applyContextualParameterTypes` now picks the single ARITY-APPLICABLE call signature, or bails (non-strict) when 0 or ≥2 are applicable — so the arrow params stay `any` instead of being typed from `ctxSigs[0]`, removing the FP TS2322 `(x: string, y: number) => any not assignable to {overloaded}` (the any-param arrow IS assignable); (2) `maybeEmit7006ForOverloadedAssignTarget` emits TS7006 for the RHS arrow's unannotated params when the assignment target's fn-type has ≠1 arity-applicable call sig. Both gated to non-strict mode; single-signature contextual types keep the legacy path. **B478 (+1) — uncalledFunctionChecksInConditional2 (extends B476).** An arrow assigned to an UNTYPED target (`let mark; mark = (tag) => …` — LHS resolves to `any`) gets no contextual signature → its unannotated params are implicitly any → TS7006; `maybeEmit7006ForOverloadedAssignTarget` now emits for an `any` LHS in addition to the overloaded-fn-type case (non-fn OBJECT targets stay conservative). The 3 TS2774 for this test were already correct; the 4 TS7006 (mark/measure arrows) were the only gap. **B477 (ATTEMPTED+REVERTED) — allowJscheckJsTypeParameterNoCrash** (generic-instantiation fn-alias display `WatchHandler<any>`): the aliasDisplayMap fix was correct (register fn-typed results for generic instantiations whose decl.type is a direct FunctionType, not the `{x:()=>T}["x"]` trick) but didn't flip the target — `this.data2` resolves to the method's own inferred `(val: any) => void`, NOT contextually from `Record<string, WatchHandler<any>>`'s index sig (Record isn't materialized, Blocker B100). Reverted per no-dead-infra. NEXT: contextual-typing contained wins exhausted; remaining are multi-piece type-engine/cross-file/parser Blockers._ _Round 189 (2026-06-17, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+1 net so far (338→337 failing, 9748→9749 passing), B473 (1 commit), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** Re-confirmed the contained pool EXHAUSTIVELY empty: find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=8 all type-engine. Ran a 28-agent triage Workflow over the smallest-baseline candidates; it over-called FLIP_NOW ~10x (every one I independently verified — knockout/spyComparisonChecking = B153-reverted property-access-receiver; inKeywordAndUnknown gate FPs on a declared `{}`; enumAssignmentCompat6 = 12-error multi-chain-variant + cross-file; signatureCombiningRestParameters* = mapped+conditional+intersected-call-sigs; unionSubtypeReductionErrors = subtype-reduction complexity infra we lack; maxNodeModuleJsDepth = B152b namespace-import-member 2-piece; decoratorMetadataTypeOnlyImport = commonjs bare-specifier resolver gaps [ATTEMPTED+REVERTED: B473-commonjs broadening regressed extendsUntypedModule + moduleResolutionWithSymlinks ×2 via symlink/untyped node_modules]; circularResolvedSignature/inferFromGenericFunctionReturnTypes1 = generic-inference Blocker #2; mergeSymbolReexportInterface = Blocker #3 cross-file merge-order). **B473 (+1) — partialDiscriminatedUnionMemberHasGoodError (Blocker #1/contextual, dedicated FP-safe walker).** `foo.push({ types: [{ type: "A" }] })` where `foo: Wrapper[]`, `Wrapper.types: ValidType[]` (ValidType = TypeA|TypeB): the array element `{ type:"A" }` selects TypeA by its `type` discriminant but is missing TypeA’s required `param` → TS2322 "Type ‘{ type: "A"; }’ is not assignable to type ‘ValidType’." + chain "Property ‘param’ is missing… required in type ‘TypeA’." + related TS2728 at the element. NEW dedicated walker `checkArrayPushDiscriminatedUnionElements` — NOT the per-property arg-check loop (that is `!isRestParam`-gated, so a `push`/rest-position object-literal arg never reaches it). Reuses the FP-safe `collectMissingProperties` + `arrayElementUnionAlias`; discriminant member-selection picks EXACTLY ONE union member by a shared literal discriminant. Source display preserves the contextual literal (`{ type: "A"; }` not widened `string`) via a local literal-preserving builder. FP firewall: `<recv:T[]>.push(<objLit>)` + array-literal property + 2+-member object union with shared literal discriminant + exactly-one match + genuinely-missing required props — corpus-rare (grep: only this test has the shape), zero double-emit (those positions emit nothing today). NEXT: pool exhaustively confirmed empty; remaining are multi-piece type-engine/cross-file/parser Blockers._ _Round 188 (2026-06-17, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+2 net so far (340→338 failing, 9746→9748 passing), B471 + B472 (2 commits), ZERO regressions at each step (exact failing-set diff via `scripts/fail_set.py`).** **B472 (+1) — importWithTrailingSlash (Blocker #3: `.`/`..` dir-index default-import resolution + cross-file member-access TS2339).** A self-contained, corpus-FP-safe (grep: only this test imports from exactly `.`/`..`) fix: (1) the relative resolvers now treat the bare `.`/`..` package-index specifiers as directory-index imports (`normalizeDotDirSpecifier`), with `resolveRelativeIncludingIndex` resolving `.`/`..` to `<dir>/index` ONLY — NOT a sibling FILE whose stem equals the dir (`.` from /a/x.ts → /a/index.ts, never the file /a.ts); (2) `checkUnresolvedModules` suppresses TS2307 for resolvable `.`/`..`; (3) a NEW dedicated walker `checkDotDirIndexDefaultImports` emits TS2339 for a missing member of a `.`/`..` default import bound to an `export default {objLit}` — NOT an un-gating of the FP-risky shared import-receiver member-access path (CLAUDE.md B152/B153), but a narrow corpus-rarity-gated walker (B419-style) that reads the resolved default object literal's keys directly. ZERO regressions. **The earlier B472 attempt (reverted) routed through the shared default-import-alias type path, which doesn't fire member-access TS2339 for alias receivers — the dedicated walker sidesteps that.** B471 (+1):_ _[B471 detail retained below.] Round 188 B471: **+1 net (340→339 failing), B471 (1 commit), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).** Re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE=8 all type-engine; OUTPUT=1 tsgo-irrelevant), then diff-ranked ALL 340 failures and triaged ~30 candidates directly (all confirmed multi-piece: indexedAccessWithVariableElement = variadic-tuple indexed access; exhaustiveSwitchCheckCircularity = loop flow-narrowing; circularOptionalityRemoval = self-ref param defaults; importWithTrailingSlash = `.`/`..` dir-index resolution; reverseMappedPartiallyInferableTypes = reverse-mapped inference; the pathMapping/symlink pair = project-config TS5055/5011/6059; etc.). **B471 (+1) — typeName1 (anonymous-object-type DISPLAY ordering + inline-TypeLiteral TS2411).** Three coupled pieces flipped one test: (1) **`formatTypeForDisplay` TypeLiteral member reordering** — tsc renders an anonymous object type's members in a CANONICAL order (call signatures → construct signatures → index signatures → properties+named-methods) regardless of source order; we preserved source order. Also fixed construct-sig spacing (`new ()` not `new(`) and empty object (`{}` not `{ ; }`). FP-safe: a passing test with a non-canonical-order TypeLiteral annotation would already be failing, so reordering can only help/neutral. (2) **`typeof C` source display** — a bare class identifier in value position is its CONSTRUCTOR (`typeof C`), but getTypeOfExpression resolved it to the instance type (display `C`); override displaySource to `typeof C` when the init is a pure-class identifier whose display collapsed to the class name. (3) **inline-TypeLiteral TS2411** (`checkInlineTypeLiteralIndexProperties`) — a named data property whose declared type isn't assignable to the applicable index-sig value type, INSIDE a var-decl's inline object type (recursing through array/union/intersection/nested-property types); the existing TS2411 machinery only covered class/interface declaration bodies. FP firewall: PropertyDeclaration members only, both types concrete non-any/error, definitive relation failure. ZERO regressions. NEXT: contained pool exhausted; the display-reorder + inline-TS2411 infra is reusable; remaining failures are multi-piece type-engine/parser/config Blockers._ _Round 187 (2026-06-17, multi-iteration session, ≥30 iterations per user request, addressing blockers): **+2 net so far (342→340 failing, 9744→9746 passing), B469 + B470 (2 commits), ZERO regressions at each step (exact failing-set diff via `scripts/fail_set.py`).** Both extend the warm Blocker #1 flow-narrowing beachhead. **B470 (+1) — controlFlowForIndexSignatures (CFA of `typeof v` in an index-signature value position).** Dedicated local walker `tryEmitTypeofIndexSigNarrowing`: for `const x: { [k:string]: typeof v } = {...}` declared inside a block narrowing `v` (`if (typeof v === 'string')`, via a block-scoped type alias `C`), narrow `v` at the alias declaration's RECORDED flow (Flow.kt already records flow at `TypeAliasDeclaration` nodes precisely for typeof-CFA), build the narrowed index value type, and re-check the literal's primitive property values → TS2322 at the property NAME + related TS6501 at the index signature. THREE sub-blockers solved together (each surfaced by a reverted attempt): (a) the binder doesn't bind flow inside type positions → use the alias-DECL flow node, not the typeof node; (b) the non-generic alias's type is cached/eagerly-resolved without flow → narrow LOCALLY in the walker (no getTypeFromTypeQuery, no cache); (c) the `type C` is BLOCK-SCOPED → not bound (B83.5) → resolve via recursive AST scan (`findTypeAliasDeclInStatements`). FP firewall: corpus-rare shape; narrowing must change `v` to a primitive; only property values that PASS the un-narrowed union but FAIL the narrowed type fire. ZERO regressions. **B469 (+1) — controlFlowNullTypeAndLiteral (flow narrowing into the TS2345 call-arg path + const-null narrowing).** Re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE=8 all type-engine; OUTPUT=1 tsgo-irrelevant), then directly triaged the smallest-baseline failures (`invalidSplice`, `unionOfArraysFilterCall`, `partialDiscriminatedUnion…`, `genericFunctionInference1`, the enum/flow families) — all multi-piece except `controlFlowNullTypeAndLiteral`. **B469 (+1) — controlFlowNullTypeAndLiteral (Blocker #1 flow narrowing wired into the TS2345 call-arg path).** Two coupled pieces: (1) **const-nullish equality narrowing** — `narrowByEquality` now resolves a bare-Identifier `other` side whose declared annotation is exactly the `null`/`undefined` keyword type (a `const myNull: null`) to nullType/undefinedType (new AST-only `constNullishAnnotationType`, FP-safe — only the two nullish-keyword annotations qualify), so `x === myNull` narrows like `x === null`; (2) **flow narrowing during the call-type walk** — `checkCallExpressionTypes` now sets `currentFlowGraph` (was null there, so a `T|null` property-access arg FP-fired TS2345 in the `!== null` narrowed-to-T branch too), and the arg-check site narrows reference args (`getNarrowedTypeForReference` for Identifier/PropertyAccess Union args — Identifiers don't narrow via getTypeOfExpression). Together: the var-decl path (`const s: string = x` past `x===myNull`) already narrowed via the existing line-72491 path; the call args (`addOne(obj.val)` past `obj.val===null`/`===myNull`) now narrow to `null`→TS2345 and the `!==` branches narrow to `number`→no error. The broad flow-in-call-walk change had ZERO regressions (narrowing only refines types → fixes FPs). NEXT: continuing to address blockers; the flow-in-call-walk infra is reusable for other guard-narrowed-arg tests._ _Round 186 (2026-06-16, multi-iteration session, addressing blockers): **+2 net so far (344→342 failing, 9742→9744 passing), B467 + B468 (2 commits), ZERO regressions at each step (exact failing-set diff via `scripts/fail_set.py`).** Re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE=8 all type-engine; OUTPUT=1 tsgo-irrelevant systemModule17) + ~30 manual triages all multi-piece. Both wins extend warm flow/generic-inference Blocker beachheads. **B468 (+1) — narrowingPastLastAssignment (Blocker #1 flow, extends B464 `isPastLastAssignment`).** Two coupled fixes: (1) `checkUninitializedLetCapturedReads` (TS7034/7005 for uninitialized `let x;` captured in a closure) now DROPS captured reads PAST the last UNCONDITIONAL same-scope assignment (those have the known last-assigned type) — but CONDITIONAL assignments (inside if/while/for via a new `UlState.conditionalDepth`) do NOT count as definite, so f9/f10's `if(cond){x=…}`-then-closure reads still fire (the regression the first cut caused — fixed by the conditionalDepth gate); (2) B464 closure narrowing now EXCLUDES captured `var`s — tsc gives a captured `var` (function-scoped/hoisted) its declared type in a closure, not the narrowed type, so f13's `var foo:string|undefined; foo=''; ()=>foo.x` keeps `string|undefined` → TS18048 fires. Pieces: `FlowStart.enclosingVarDecls` (Map name→VariableDeclaration, collected in `bindFunctionLikeBody` from the enclosing function body, not descending into nested fns); `outerFlowForCapturedName` returns null for a captured var; the TS18048 emitter recovers the captured var's declared annotation type via `enclosingVarDecls[root].type` (getTypeOfExpression returns `any` for a function-body var across the closure boundary, unlike a param). **B467 (+1) — chainedCallsWithTypeParameterConstrainedToOtherTypeParameter2, the documented harder variant of B462 (Blocker #2 generic-inference).** New dedicated `checkChainedTypeParamClimbing` walker, gated to the exact `thenChainMethod` class shape (`Chain<T [extends I]>` with `then<S extends T>(cb:(x:T)=>S):C<S>`, reusing B462's `thenChainMethod`), walking INSIDE the `then` method body (which B462's `walkThenChains` never recurses into → disjoint; B462's `processThenChain` also bails on Identifier arrow bodies). For each fluent `.then` chain whose arrow bodies RETURN bare type-param LOCALS (`var t!:T; var s!:S; var i!:I`), it tracks the current chain type-arg as a NAME and uses an AST constraint graph {S→T→I} (NO relation engine for the climb): (1) climbing TS2322 "Type 'R' is not assignable to type 'curArg'." + "could be instantiated" chain + TS2208 (at the returned TP's decl, suggesting `extends <curArg>`) + TS6502 (at the cb FunctionType) when an arrow returns a local of type-param R that is NOT a subtype of the chain's current type arg; (2) final-write TS2322 (string-vs-number) when a non-climbing chain ends in `.value.<prop> = rhs` and rhs is not assignable to the property type resolved via the relation engine from the type-arg's ultimate constraint interface. Result type-arg clamps to the inferred R even on error. NEXT: contained pool genuinely exhausted; remaining are multi-piece type-engine Blockers (Record/mapped-type materialization, broad generic-inference engine, flow-aware-typeof-in-type-position, cross-file scope) + parser-recovery._ _[Round 185 trimmed to STATUS-HISTORY.md.]_ _[Round 184 trimmed to STATUS-HISTORY.md.]_ _[Round 183 trimmed to STATUS-HISTORY.md.]_ _[Round 181 trimmed to STATUS-HISTORY.md.]_ _[Round 179 trimmed to STATUS-HISTORY.md.]_ _[Round 178 trimmed to STATUS-HISTORY.md.]_ _[Round 177 trimmed to STATUS-HISTORY.md.]_ _[Round 175 trimmed to STATUS-HISTORY.md.]_ _[Round 173 trimmed to STATUS-HISTORY.md.]_ _[Round 172 trimmed to STATUS-HISTORY.md.]_ _[Round 171 trimmed to STATUS-HISTORY.md.]_ _[Round 170 trimmed to STATUS-HISTORY.md.]_ _[Round 169 trimmed to STATUS-HISTORY.md.]_ _[Round 167 trimmed to STATUS-HISTORY.md.]_ _[Round 166 trimmed to STATUS-HISTORY.md.]_ _[Round 165 trimmed to STATUS-HISTORY.md.]_ _[Round 164 trimmed to STATUS-HISTORY.md.]_ _[Round 161 trimmed to STATUS-HISTORY.md.]_ _[Round 159 trimmed to STATUS-HISTORY.md.]_ _[Round 158 trimmed to STATUS-HISTORY.md.]_ _[Round 157 trimmed to STATUS-HISTORY.md.]_ _[Rounds 151, 156 trimmed to STATUS-HISTORY.md.]_ 
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
