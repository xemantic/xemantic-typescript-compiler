# Status

**Phase 4 — Checker buildout.** 9,326 / 10,086 tests passing (~92.47%). _Round 123 (2026-06-08, ULTRACODE multi-iteration session): **B132 (+1, 762->761 failing, zero regressions — exactly one flip)** — flips `genericCallbackInvokedInsideItsContainingFunction1` by extending the B128 `checkFnTypedParamCalls` walker to the TYPE-PARAM ARGUMENT case: `f(y)` where `y: U` and `f`'s sig param is a DIFFERENT bare unconstrained type param `T` now fires TS2345 "Argument of type 'U' is not assignable to parameter of type 'T'." + the "could be instantiated" chain + a related TS2208 at U's declaration. The walker was refactored to thread a `FnParamCtx` (tparams + tpAst node-map + fnParams + tpVarRefs var->tp-name map, the latter two rebuilt per scope) so the arg's declared type param is known without a populated type-param scope. `f(x)` where `x: T` (SAME tp) stays legal; FP-safe by construction. **B133 (+1, 761->760 failing, zero regressions — exactly one flip)** — flips `genericWithOpenTypeParameters1` by extending the same walker to METHOD calls on a generic class instance: `x.foo<X>(...)` where `x: B<T>` and `foo` is a non-generic method fires TS2558 (a class type param is NOT a method type param), and `x.foo(1)` fires TS2345 against the substituted unconstrained `T`. New `ClassInstRef` + `collectClassInstVarRefs` (param typed `B<...>` resolving to a user ClassDeclaration, with class-tp-position->bare-in-scope-tp mapping) + `emitGenericMethodCallDiag`. FP firewall: annotated class-instance receiver only (so the object-literal-generic-property shape of `typeAssertionToGenericFunctionType` is never matched — the B89.2 regression that blocked the type-engine approach) + single non-overloaded method. See CLAUDE.md gotcha._ Round 122 (2026-06-08, ULTRACODE session + /loop continuation, +4 net — B127+B128+B129+B130, all clean zero-regression): **B130 (+1, 763->762 failing, zero regressions — exactly one flip)** — flips `mergedDeclarations3` (`M.foo.x`/`M.foo.z` where `foo` merges `export function foo` + `export namespace foo { export var y }` (exported→y visible) + `namespace foo { export var x/z }` (NOT exported from M→x/z not externally visible)). The binder flat-merges all block members into `foo.exports`, so nothing fired; new branch in `checkMemberAccessMissing` (after B98.r138): for a qualified receiver resolving to a Module with ≥2 ModuleDeclaration blocks, fire TS2339 when the member is in a non-exported block AND not in any exported block (new helper `moduleBlockExports`). FP-safe by construction. **B129 (+1, 764->763 failing, zero regressions — exactly one flip)** — flips `importedEnumMemberMergedWithExportedAliasIsError` (`import EnumA = Enum.A` internal value-side alias + `export type EnumA` — a merge that is both a conflict and mixed-export). Two additions in `checkImportConflictsWithLocal`: (1) TS2440 — extended the internal-alias gate (was `name !in varNames → continue`) to also conflict with `typeAliasNames` (import-equals + `type X` never declaration-merge); (2) TS2395 (×2) — new narrow detection: a name declared by BOTH an internal import-equals alias AND a type-alias with MIXED export status → "must be all exported or all local" at each name. Both FP-safe by construction. **B128 (+1, 765->764 failing, zero regressions — exactly one flip)** — flips `typeArgumentsOnFunctionsWithNoTypeParameters` via a new dedicated full-tree walker `checkFnTypedParamCalls` for calls to FUNCTION-TYPED PARAMETERS (the TS2558 walker resolves callees via locals/globals — a param is in neither). Two mutually-exclusive-per-call diagnostics: TS2558 "Expected 0 type arguments" when `f<X>(...)` and `f`'s param-FunctionType has 0 own type-params (FP-safe: a `<K>(v:K)=>U` param is excluded); TS2345 + "could be instantiated with an arbitrary type" chain when `f(arg)` (no type-args) and a call-sig param is a bare UNCONSTRAINED in-scope type-param and arg is concrete-simple (FP-safe: `f(t)` where t:T is skipped). Mirrors `checkIncDecTypeParamOperands` scope-threading; BinaryExpression iterative per the deep-chain gotcha. FP-safe by construction. **B127 (+1, 766->765 failing, zero regressions — exactly one flip)** — flips `duplicateErrorAssignability` via three coordinated pieces: (1) TS2538 "Type 'X' cannot be used as an index type" for a user-defined OBJECT/INTERFACE-typed value used as a computed index (`obj[x]` where `x: B`) — new branch in `checkSingleElementAccess`, gated to a named user `Type.Interface` (excl. lib wrappers + enums), FP-safe (object types are never valid indices); (2) nested var-init assignment checking — `const x = a = b` now type-checks the inner `a = b` (the assignment walker only reached top-level ExpressionStatements; added to the top-level loop `checkTypeAssignabilityInStatements` AND `checkTypeAssignabilityInStmt`, narrow-gated to `=` initializers); (3) TS2741 missing-property for PLAIN interface->PLAIN interface as a FALLBACK gated on `!canUseTypeEngine(...)` so it NEVER preempts the working canUse path (`errorWithSameNameType`) or the `Object`->TS2696 special-case (excluded by `symbol.name != "Object"`), pure-property-bag-only (no index/call sigs), carries B53.1 import-qualified display. Method: ULTRACODE — 42-agent read-only triage Workflow over the bounded NONE pool (stopped early for box memory; confirmed the documented ~5x FLIP_NOW over-classification — every agent "FLIP_NOW" was a multi-feature test needing its FULL baseline), then hand-traced + built the one genuinely-flippable candidate. **METHOD LESSON: for NONE-PRODUCED tests, flipping requires producing the test's ENTIRE baseline — a single-diagnostic recipe never suffices for a multi-error test.** Re-confirmed (rounds 102-122): bounded inline-surgical pool is type-engine/cross-file-scope/lib/JSDoc dominated; ~1 clean win per session._ _Round 119 (2026-06-08, /loop ULTRACODE session): **B123 (+1, 767->766 failing, zero regressions — exactly one flip)** — flips `thisInPropertyBoundDeclarations` by suppressing a spurious TS2738 "outer value of 'this' is shadowed" related-info on a `function inner(){ this }` nested inside an already-untyped `prop = function(){…}` initializer. The FunctionDeclaration + FunctionExpression branches of `checkThisInExpr`/`checkThisInStatement` KEPT the outer `shadowFunctionPos` for a function nested inside an already-untyped function; reset to -1 — TS attaches TS2738 only to the FIRST function that shadows a CONCRETE (class/arrow-inherited) `this`, not deeper-nested ones (the outer `this` is already implicit-any). Found via a new chain-completeness scan (failing tests whose error-SUMMARY lines match but the full baseline differs in continuation/related lines — invisible to find_candidates). **B122 (+1, 768->767 failing, zero regressions via before/after failing-set diff — exactly one flip)** — flips `arrayBestCommonTypes` via two coordinated pieces: (1) element-access DISTRIBUTES over union members — `getTypeOfElementAccess` factored into `elementAccessResultType`, so `(number[] | null[])[0]` resolves to `number | null` (the double element access `[[3,4],[null]][0][0]` was previously `anyType`, masking the overload error); conservative — union result only when EVERY member yields a concrete element type. (2) TS2769 overload-chain nested sub-line: `getUnionMemberFailureSubline` appends `Type 'undefined' is not assignable to type 'boolean'.` when the first failing arg is a UNION and the param is a PRIMITIVE intrinsic — TS reports the NULLISH member first (undefined, then null), else the first failing member; gated to union-vs-primitive so object/union targets keep their own elaboration. Also this round: 40-candidate triage workflow (triage + adversarial-refute) + ~18 hand-verifications — every workflow FLIP_NOW over-classified (~5×, all traced to real blockers, re-logged in the skip-log in correct `_ts` format; ~10 were already round-107/109-logged but in a format `--fresh` doesn't filter). **B120 (+1, 769->768 failing, zero regressions via before/after failing-set diff — exactly one flip, zero new failures)** — first B100 MODIFIER-utility win: `Readonly<T>` materialization + AsExpression-in-`extends` heritage + `this`-inherited-readonly check, flips `readonlyAssignmentInSubclassOfClassExpression`. Three coordinated pieces: (1) new `materializeModifierUtility` (Checker.kt, sibling to B116's `materializeMemberSetUtility`) wired in `getTypeFromTypeReference` when `symbol==null && name=="Readonly"` — copies the source object's member Symbols and marks each read-only via a new `mappedReadonlyMemberIds` side-channel (the copies share `T`'s declarations so their TYPE resolves, but the `readonly` modifier comes from the wrapper, not the declaration), consulted by `isReadonlySymbol`; gated to a concrete plain-object `T` (no Reference/index-sigs). (2) `getTypeFromBaseTypeExpression` now resolves `class C extends (X as new () => T)` — unwraps paren→AsExpression→ConstructorType and returns the construct sig's RETURN type `T` as the base instance type (TS's getBaseConstructorTypeOfClass for the cast-mixin pattern); narrow gate (`asType is ConstructorType`) keeps the mixin blast radius tiny. (3) `isReadonlyPropertyAccess` resolves `this.X` via a narrow `currentClassForThis` context (now also set by the readonly walker's ClassDeclaration branch) — FP firewall: restricted to INHERITED members only (assigning an OWN readonly prop in the constructor is LEGAL). Member inheritance from a plain `Type.Object` base already works (resolveInterfaceMembers ~60713). Method: after confirming the surgical/NONE pool is type-engine-saturated (parallel triage of signatureCombiningRestParameters / esModuleInterop-default-imports / JSDoc-export-assignment / arrayBestCommonTypes clusters — all architectural, agents over-classified per the round-106/107 meta-lesson), pursued the top contained blocker B100 per the user's "address blockers" directive. Earlier in-session attempt: element-access union distribution `(number[]|null[])[0]` → `number|null` (would advance `arrayBestCommonTypes`) built + suite-verified NET-ZERO (the test also needs full union-arg overload-error message chains, 132 lines) → reverted per no-zero-consumption-infra rule. _Round 118 (2026-06-08, /loop session): **B119 (+1, 770->769 failing, zero regressions via correct before/after failing-set diff — exactly one flip)** — conditional-type `infer` binding. `type SyntheticDestination<T,U> = U extends Synthetic<T, infer V> ? V : never` previously resolved to `any` (the infer-bearing extends-type → errorType → whole conditional anyType), masking the inferred result. New `tryEvaluateConditionalWithInfer` (Checker.kt, gating `getTypeFromConditionalType`): when the extends-type is a `TypeReference` carrying top-level `infer` placeholders AND checkType is a concrete `Type.Reference` to the same-named target with matching arity AND all non-infer positions assignable, bind the infer vars positionally and resolve the TRUE branch with the bindings layered onto `currentTypeAliasArgs`. Conservative: ONLY the positive-unification path returns a concrete type; every other shape returns null → existing anyType behavior (bounds FP surface). PLUS `displayTargetAnnotation` (var-decl path) UNFOLDS a primitive-resolving type-alias reference to the primitive name (`const z: TestSynthetic = '3'` → "not assignable to type 'number'", not "'TestSynthetic'") — TS unfolds primitive aliases but keeps object/interface alias names. Flips `conditionalDoesntLeakUninstantiatedTypeParameter`. Method: 16-candidate parallel read-only triage fleet (all ARCHITECTURAL — same saturation as rounds 104/105/116); B119 is the one bounded type-engine FEATURE with a clean consumer + FP firewall. **Round 117 (2026-06-07/08, /loop session, 11 iterations): **B117 (+1, 771->770 failing, zero regressions)** — flips `omitTypeHelperModifiers01` (Omit + readonly) by fixing two gaps exposed by B116's materialization: (1) `checkPropertyAccessAssignment` now bails for `x.b = undefined` when `b` is OPTIONAL and !exactOptionalPropertyTypes (optional write-type admits undefined); (2) the readonly walker's FunctionDeclaration branch now `populateParameterLocalTypes` so `isReadonlyPropertyAccess` resolves a param receiver `x: B` -> TS2540 at `x.c = true`. **B116 (+1, 772->771 failing, zero regressions)** — FIRST B100 utility-type-materialization win: TS2339 for `Omit<Foo,"c">`/`Pick` member-set materialization (`omitTypeTestErrors01`); new `materializeMemberSetUtility` in `getTypeFromTypeReference` (gated symbol==null + string-literal key set + object T + no index-sigs/Reference); reuses source property symbols; B50.4 auto-registers the alias display. **B116 (+1, 772->771 failing, zero regressions via before/after failing-set diff)** — FIRST B100 utility-type-materialization win: TS2339 for `Omit<Foo,"c">`/`Pick` member-set materialization (`omitTypeTestErrors01`: `type Bar = Omit<Foo,"c">; bar.c` -> "Property 'c' does not exist on type 'Bar'"). New `materializeMemberSetUtility` in `getTypeFromTypeReference` (gated symbol==null + string-literal key set + object T + no index-sigs/Reference); reuses source property symbols (preserves type/optional/readonly); B50.4 auto-registers the `Bar` alias display. **B114 (+1, 773->772 failing, zero regressions)** — TS2339 for module-namespace property access via `import Foo = require("./b")` where the member is not exported (`allowSyntheticDefaultImports10`: `Foo.default` -> `typeof import("b")`); new branch in `checkMemberAccessMissing` before the alias-bail, FP firewall = `.ts`/`.tsx` target only + no `export * from` + member absent from exports. **B114 (+1, 773->772 failing, zero regressions)** — TS2339 for module-namespace property access via `import Foo = require("./b")` where the member is not exported (`allowSyntheticDefaultImports10`: `Foo.default` -> `typeof import("b")`); new branch in `checkMemberAccessMissing` before the alias-bail, FP firewall = `.ts`/`.tsx` target only + no `export * from` + member absent from exports. **B113 (+1, 774->773 failing, zero regressions via before/after failing-set diff)** — TS2403 for `propertyIdentityWithPrivacyMismatch` (`var x: m1.Foo; var x: m2.Foo` cross-module ambient + `var y: Foo1; var y: Foo2` local, all private-member-nominal). Two coordinated changes: (1) `resolveAlias` now resolves `import X = require("mod")` to an AMBIENT `declare module "mod"` symbol (via `globals[specifier]`) when `mod` is not a file; (2) TS2403 private-member nominality — broadened `isStructuredTypeForTs2403` to named non-generic class/interface `Type.Interface`, dropped the same-display-name pre-gate, and added `ts2403HasPrivateOrProtectedMember` (distinct symbols + a private/protected member -> DIFFERENT). Both full-suite-verified FP-safe._ Round 117 (2026-06-07, /loop session): **B112 (+1, 775->774 failing, zero regressions via before/after failing-set diff)** — TS2322 for a function-expression source assigned to a CONSTRUCTOR-type var-decl target (`constructorAsType`: `var Person: new () => {name:string} = function(){return {name:"joe"}}`). The construct-sig-mismatch elaboration branch (`getNonConstructibleElaboration`) existed in the assignment path (17.111) and return path (B106) but NOT the var-decl path — the third assignability site. Added it before the `canUseTypeEngine` gate (which bails for fn-vs-ctor). ALSO added object-literal return inference (`return {x}` -> widened `{ x: T }`) to `inferReturnTypeFromFunctionExpressionBody` so the source displays `() => { name: string; }` not `() => any` (suite-wide for function expressions, full-suite-verified zero regressions). Pattern: gaps between the three assignability sites (var-decl / assignment / return) are a recurring surgical vein._ Round 116 (2026-06-07, ULTRACODE multi-iteration session): **B110 (+1, 776->775 failing, zero regressions)** — TS2345 for `this.accept_foo(this)` inside an object-literal method (`objectLiteralThisWidenedOnUse`: `this` types as the object literal, missing `bar` required by param `Foo`). Three parts: (1) object-literal method bodies now set `currentLocalTypes["this"]` to the literal type (mirrors class-method handling); (2) the arg-check missing-prop branch (~79838) extended to a literal `this` arg of anonymous Type.Object vs named-interface param (TIGHTLY gated → FP-safe); (3) display fixes — `getTypeOfObjectLiteral` infers `void` for no-return method bodies, and `typeToString` renders method-DECLARED anon-object members in shorthand `m(): ret` (gated on a MethodDeclaration; suite-wide, zero regressions). **B109 (+1, 777->776 failing, zero regressions)** — TS2322 for a function/arrow array-literal element not assignable to a call-signature element type (`contextualTyping33`: `foo([function(){return 1;}, function(){return "foo"}])` where param is `{ (): number; (i: number): number; }[]` -> Type '() => string' is not assignable to type '{ (): number; (i: number): number; }' / Type 'string' is not assignable to type 'number'). `checkArrayLiteralElementsAgainstType` bailed on call-sig-only element types (no properties); new branch checks each FunctionExpression/ArrowFunction element via `getTypeOfExpression` + `getFunctionMismatchElaboration`. Span = the `function` keyword (8 chars) for a function expression (matches tsc). FP-safe (only fires on genuine return/param mismatch). **B107 (+1, 778->777 failing, zero regressions)** — TS2322 for an anonymous ClassExpression assigned to a construct-signature target (`classExpressionAssignment`: `const A: { new(): A } = class {}` -> Type 'typeof A' is not assignable to type 'new () => A' / Property 'prop' is missing in type 'A' but required in type 'A' + related TS2728). `canUseTypeEngine` skips class-instance-vs-constructor comparisons so a `class {}` source falls through as assignable; the var-decl construct-sig branch only handled `Identifier` sources (17.8). New `init is ClassExpression` branch in `checkVarDeclAssignability` (~56174): synthesize a `Symbol(SymbolFlags.Class, constName)`, build its instance type via `getDeclaredTypeOfSymbol`, `collectMissingProperties` vs the target construct-sig return type; emit TS2322 (source `typeof <constName>`, chain = missing-property line, related TS2728). FP-safe: only fires when a required property is genuinely missing. Round 115 (2026-06-07): **B106 (+1, 779->778 failing, zero regressions)** — TS2322 for a function/property source RETURNED where the declared return type is a CONSTRUCTOR type (`targetTypeVoidFunc`: `function f1(): { new (): number; } { return function () { return; } }` -> Type '() => void' is not assignable to type 'new () => number' / Type '() => void' provides no match for the signature 'new (): number'). `canUseTypeEngine` bails when the source lacks construct sigs but the target requires them, so TS2322 never fired in the return path. Mirrored the existing ASSIGNMENT-path construct-sig-mismatch branch (~57866, uses `getNonConstructibleElaboration`) into `checkReturnAssignability` (~57334); squiggle = the `return` keyword (stmt.pos, length 6). FP-safe (the elaboration is non-null only for a genuine mismatch). **B103 (+1, 780->779 failing, zero regressions)** — TS2322 for a var-decl where the relation engine wrongly reports assignable but a target REQUIRED property's same-named SOURCE property is OPTIONAL (`propertyParameterWithQuestionMark`: `var v3: { x } = new C` where `class C { constructor(public x?) }` -> x is optional on C; target `{ x: any }` requires it -> Type 'C' is not assignable to type '{ x: any; }' / Property 'x' is optional in type 'C' but required in type '{ x: any; }'). Optional-source-vs-required-target is NEVER assignable regardless of strictNullChecks, so FP-safe. New helper `findOptionalVsRequiredMismatch` (Checker.kt:~83430, skips source-with-index-signature) + a var-decl branch gated on `isAssignable && both Type.Object && init !is ObjectLiteral`. **B102 (+1, 781->780 failing, zero regressions)** — TS2322 for a bare `yield;` (no operand) in a generator with an EXPLICIT primitive yield-type annotation (`Iterator`/`IterableIterator`/`Generator<primitive,...>`, sync) under strictNullChecks (`yieldExpression1`: `function* b(): IterableIterator<number> { yield; }` -> Type 'undefined' is not assignable to type 'number'). A bare yield always yields `undefined`, and a primitive yield-type never accepts undefined under SNC, so FP-safe. New self-contained walker `checkGeneratorBareYieldTypes` (Checker.kt:~55011): `scanGeneratorsInStatements` finds generator function-likes (FunctionDeclaration/MethodDeclaration/var-init FunctionExpression) with a qualifying `generatorPrimitiveYieldType` annotation, then `scanBareYieldsInStatements/Expr` walks the body for bare yields, STOPPING at nested function/arrow/class boundaries (which rebind the generator context). Emits at the `yield` keyword (length 5). **B101 (+1, 782->781 failing, zero regressions)** — TS2322 for `var x: <primitive> = this.M()` where `M` is an instance method of the enclosing class declared with NO return annotation and a return-free body (-> `void`), which is never assignable to a primitive var annotation (`thisWhenTypeCheckFails`: `var s: string = this.n()` inside `class c { public n() { var k = () => { var s: string = this.n(); } } }`). A FIRST cut wired BROAD `this`-typing into `getTypeOfIdentifier("this")` + method-void-return inference into `getTypeOfSymbol` — that regressed -1 net (`variableDeclaratorResolvedDuringContextualTyping`: `this.requestService.makeRequest(...).then(...)` resolved to an incomplete `Promise<T>` and FP-emitted TS2739 against the named `TPromise<IUploadResult>` return target). REVERTED and replaced with a NARROW check (`tryEmitVoidThisMethodToPrimitiveVar`, Checker.kt:~55750) gated on a new behavior-neutral `currentClassForThis` field (set per-instance-member in the assignability walker's ClassDeclaration handler, reset for static members + non-arrow nested-function boundaries that rebind `this`): annotation is a primitive KeywordTypeNode (string/number/boolean/bigint/symbol), init is `this.M(...)`, M is the SOLE own MethodDeclaration named M with no return annotation, no async/generator, and `bodyHasNoReturn` -> void. FP-safe (void->primitive is always an error). LESSON RE-CONFIRMED: broad `this`/return-type-resolution changes FP in the return-assignability path on incomplete generic chains; narrow AST-shape checks win._ _Round 114 (2026-06-07, ULTRACODE multi-iteration session): **B98.r171 (+1, 783->782 failing, zero regressions)** — TS2339 for property access on a DEFAULT import of an `export = X` CommonJS module under esModuleInterop (`exportEqualsDefaultProperty`: `export = x` where `x={greeting,default}` + `import foo from "./exp"; foo.toExponential()` -> Property toExponential does not exist on type {greeting:string;default:number}). resolveAlias default-import path (Checker.kt:~3927) gained an `export = X` fallback via resolveModuleExportAssignment (mirrors the named-import/import-equals paths), gated to options.esModuleInterop (default true; without it the default-import-of-export= is a TS1259 error). FP-safe: previously the alias resolved to nothing so NO property checking occurred; now it resolves to the export= target and missing-property access fires. (esModuleInteropDefaultImports did NOT flip — it needs the `typeof import("mod")` module-namespace display, deferred.) _**B98.r170 (+3 cumulative, 786->783 failing, zero regressions)** — narrow default-mode TS7006 (also flips contextualTyping38: `var foo = <{ (): number; }> function(a){...}` via the type-assertion-wrapped-fn-expr contextual-arity branch — fn params at index >= the assertion call-sig param count get TS7006) for the unannotated `var/let/const NAME = <arrow|function-expr>` shape (arrowFunctionWithObjectLiteralBody1/body2: `var v = a => <any>{}`). The full checkImplicitAnyParameters walker stays gated noImplicitAny/strict because broadening it to the harness-default `!strictExplicitlyFalse` regresses -19 (FunctionDeclaration params, type-annotation walking, ambient TS7005/7008, JS files, object-literal contextual-typing gaps — empirically confirmed). New focused walker checkImplicitAnyDefaultVarFunctions fires ONLY for the unannotated-var arrow/fn-expr initializer (skips .js/.d.ts/declare/rest/binding/this); corpus scan found ZERO no-flag counterexamples (lone match is a .js file, skipped). Runs only in pure-default mode so no double-emit._ _Round 113 (2026-06-07, ULTRACODE multi-iteration session): **B98.r169 (+1, 787->786 failing, zero regressions)** — TS2307 for a bare `require("X")` in a JS file whose single-segment specifier resolves to nothing (`tslibInJs`: `const { foo } = require("bar")` under @checkJs/@allowJs). Extended the dynamic-import/require loop in `checkUnresolvedModules` (previously relative-only) with a bare branch gated to `isJsLikeFileName(fileName)` + the r107/r168 FP-safe guards (single-segment, no `/`/`:`, not ambient/`.d.ts`/node-builtin/node_modules-package, `resolveModuleSpecifier == null`). FP-safe: a bare require with no resolution target is always TS2307._ _Round 113 (2026-06-07, ULTRACODE multi-iteration session): **B98.r168 (+2, 789->787 failing, zero regressions)** — TS2307 for a SINGLE-SEGMENT bare specifier under EXPLICIT `@moduleResolution: bundler` (`cachedModuleResolution6`/`cachedModuleResolution7`: `import {x} from "foo"` where `foo` has no node_modules package / ambient module / `.d.ts` / node builtin). New `else if` gate in `checkUnresolvedModules` mirroring the B98.r107 default-null single-segment gate, with identical FP-safe guards (single-segment-only, no `/`/`:`, no path/baseUrl/rootDirs/moduleSuffixes config). `effectiveModuleRes == "bundler"` is only reachable under EXPLICIT `moduleResolution: bundler` (the default fallback never yields "bundler"), so it is disjoint from the r107 gate. FP-safe: a single-segment bare with no resolution target is always TS2307 under bundler._ _Round 112 (2026-06-07, ULTRACODE multi-iteration session): **B98.r167 (+1, 790->789 failing, zero regressions)** — TS2339 for `{ objLit }[id]` where `id` has a UNION-of-literal type and one literal is not a key of the FRESH object literal (`indexedAccessWithFreshObjectLiteral`: `{a,b,c}['a'|'b'|'z']` -> Property 'z' does not exist). New branch in `checkSingleElementAccess` (before the literal-only `when (arg)`): gated to receiver = fresh Type.Object (own props, no index sig, no call/construct sigs) + index = Type.Union of ALL string/number literal types; first missing literal -> TS2339 at the whole element-access span (`{`..`]`), display built MANUALLY from WIDENED property types (`widenType` returns members-but-not-properties so typeToString'd render `{}`; manual `{ k: <widened> ; }` join matches `{ a: number; b: string; c: boolean; }`). FP-safe: missing key on a closed object literal is always a TS error; no-index-sig gate excludes `({...} as Record)[id]`._ _Round 112 (2026-06-07, ULTRACODE multi-iteration session): **B98.r166 (+1, 791->790 failing, zero regressions)** — TS2469 ("The '+'/'+=' operator cannot be applied to type 'symbol'.") + TS2731 ("Implicit conversion of a 'symbol' to a 'string' will fail at runtime...") for `noImplicitSymbolToString`. New self-contained AST walker `checkSymbolToStringConversions` (mirrors the r79/r80 type-param walkers): threads in-scope identifier names whose ANNOTATION contains symbol (directly / union / `symbol`-constrained type-param / symbol-resolving type-alias, via fixpoint), flags them as template-interpolation operands (TS2731) and `+`/`+=`/unary-`+` operands (TS2469). Pure annotation-based, no type-engine. FP-safe: TS emits these UNCONDITIONALLY for symbol operands, so passing tests already carry them. All 15 baseline diagnostics matched (simple var / union / type-param / type-alias cases)._ _Round 111 (2026-06-07, ULTRACODE multi-iteration session): **B98.r165 (+1, 792->791 failing, zero regressions)** — TS2855 "Class field 'X' defined by the parent class is not accessible in the child class via super." (`classFieldSuperNotAccessibleJs`) + its DRIVER unblocker: **`checkJs` now implies `allowJs`** (`applyImpliedAllowJs`, TS's `getAllowJSCompilerOption` = `allowJs ?? !!checkJs`). The test set `@checkJs` without `@allowJs`, so `index.js` was never loaded into the program → no checks ran ("none produced"). CRITICAL gotcha discovered: MULTI-FILE test options come from `parseMultiFileSource`'s own `applyDirective` loop, NOT `parseCompilerOptions` — so option-derivation must be applied via `applyImpliedAllowJs` at ALL three finalization points (parseCompilerOptions, parseMultiFileSource, and the driver's post-override). New JS-only AST walker `checkClassFieldSuperAccessJs`: collects parent INSTANCE fields (`this.X=`/`this.X;`/`this['X']` expando + field PropertyDeclarations) minus PROTOTYPE members (methods/get-set/`accessor`), flags `super.X`/`super['X']` against the field set; string-literal-keyed fields display quoted. FP-safe (JS-only, fires only on genuine super-field access). Debugging note: gradle suppresses test stdout/stderr (no `showStandardStreams`) — use a marker DIAGNOSTIC (not `System.err.println`) to trace whether a checker path runs._ _Round 111 (continued, earlier): **B98.r164 (+1, 793->792)** — TS1499 unknown regex flag (trailing + inline modifier). _Round 110 (2026-06-07, ULTRACODE multi-iteration session): **B98.r164 (+1, 793->792 failing, zero regressions)** — TS1499 "Unknown regular expression flag." for both the trailing flags (after the closing `/`; valid set `dgimsuvy`) AND inline modifier-group flags `(?ims:`/`(?ims-ims:` (valid set `ims`). New purely-syntactic `emitTS1499ForRegexFlags` (wired alongside the r157 regex emitters), iterates by CODE POINT so a non-BMP "flag" (surrogate pair) is one unknown flag of length 2. FP-safe: a `(?`-construct is a modifier group only when not `(?:`/`(?=`/`(?!`/`(?<…` AND its flag region is terminated by `:` before any regex structural char; char-class `[...]` tracking + escape skipping guard the FP surface. Flips `regularExpressionWithNonBMPFlags` (6× TS1499 for non-BMP flag chars)._ _Round 110 (2026-06-07, ULTRACODE multi-iteration session): **B98.r163 (+1, 794->793 failing, zero regressions)** — PER-PACKAGE tslib resolution for TS2354 ("This syntax requires an imported helper but module 'tslib' cannot be found."). `checkImportHelpersWithoutTslib` computed `hasTslib` PROGRAM-WIDE, so a `node_modules/tslib` under `/package1` wrongly suppressed TS2354 for files in `/package2` that have no tslib. Replaced with a per-file `tslibResolvableFor(fileName)` that walks to the nearest enclosing `node_modules/` (baseDir prefix-match, mirroring `resolveTslibDedupKey`); a ROOT node_modules (baseDir ""/"/") still satisfies all files. Matches `node_modules/`-without-leading-slash fixtures too. Flips `tslibNotFoundDifferentModules`. Recon this session ALSO empirically debunked the declare-namespace TS2304 resolution path (removing the `declare namespace` skip in `checkUnresolvedInStatement` regresses NET -7 — ambient cross-file scope is incomplete; `recursiveTypeComparison2`/`extendArray` stay BLOCKED) and confirmed `knockout`/`unknownSymbolOffContextualType1` are Blocker #2 (generic return inference: `ko.observable("Bob")` resolves to `any`)._ _Round 109 (2026-06-07, ULTRACODE multi-iteration session): **B98.r162 (+1, 795->794 failing, zero regressions)** — STRUCTURAL dedup of array-literal element types. `getTypeOfArrayLiteral` deduped with reference-identity `!in`, so two fresh-but-structurally-identical anonymous object literals (`[{name,id}, {name,id}]`) produced a bogus `{name,id} | {name,id}` element union (TypeScript never displays `T | T`). Reuse the tri-state `ts2403Identical` structural-identity comparator to skip an element type IDENTICAL to one already collected (UNKNOWN/DIFFERENT keep the union member -> heterogeneous arrays unaffected). Flips `lambdaParamTypes`: the contextually-typed lambda param resolves to a single anonymous object type so `x.foo` reaches the existing TS2339 emission in `checkMemberAccessMissing`'s local-fallback branch (which bailed on a `Type.Union` receiver). NOTE: BOTH the round-108 deep-trace triage AND a fresh independent 28-agent triage this session misclassified `lambdaParamTypes` as ARCHITECTURAL/HIGH -- reaffirming that triage agents under-call the surgical pool and empirical verification (a 1-line debug trace revealing the duplicate-member union) is load-bearing. `getUnionType` itself has the same `it.id` reference-dedup bug (~line 83926) -- a documented FORWARD opportunity, not taken (called everywhere -> broad regression risk). Round-109 also vetted+rejected ~12 NONE candidates as FP-risky/architectural (see PLAN-PHASE-4 session note + skip log)._ _Round 108 (2026-06-07, ULTRACODE multi-iteration session, IN PROGRESS): deep-trace triage Workflow (21 batches over ~122 NONE-PRODUCED candidates, then per-candidate full-suite verification — the triage over-reported ~⅔ of FLIP_NOW as already-green, so verification before implementing is load-bearing). Landed **+5, zero regressions** (800→795 failing). **B98.r161a** recursive `hasReturnWithExpression` (a function with only if/else-nested value-returns is NOT `void` — was top-level-scan-only, mis-inferred `=> void`; now recurses control-flow but not nested function-likes) + **B98.r161b** type `this` as the class instance in non-static method bodies so `this.method(args)` reaches arg-checking (flips `trailingCommaInHeterogenousArrayLiteral1`; r161a prevents the arrayconcat sort-comparator regression r161b would otherwise cause). **B98.r160** TS2352 for `null as <intersection-with-object>` (`parenthesisDoesNotBlockAliasSymbolCreation`) — emitTS2352NullishCastCore now handles a direct IntersectionType with a TypeLiteral member and a generic type-alias whose body is an intersection applied to an object-literal type arg, each appending a "Type 'null' is not comparable to type '{…}'" chain (FP-gated on an object-literal constituent). First three flips (800→797): **B98.r157** TS1503/TS1532/TS1369 regex named-group diagnostics (`regularExpressionGroupNameSuggestions`) — bounded text scan `emitTS1503And1532ForNamedGroups` (`(?<name>` defs + `\k<name>` backrefs; TS1503 below ES2018, TS1532+TS1369-suggestion for undeclared backref) + a BaselineFormatter fix so a null-fileName related-info renders `TS1369: msg` not `TS1369 : msg`. **B98.r158** TS2322 for `X.prototype.dataProp = rhs` data-property write (`typeOfPrototype`) — resolves the INSTANCE member directly from the ClassDeclaration (members table is clobbered by a same-named static), emits at the full `X.prototype.bar` span when RHS not assignable. **B98.r159** TS2339 for property access on a primitive PropertyAccess/ElementAccess receiver (`extendFromAny`) — mirrors B64.1's call-result branch (`c.known.length` where `c.known:number`); PRIMITIVE_WRAPPER_INTRINSIC_NAMES gate leaves `any`-typed members alone. Reverted (net-zero, deferred): `literalsInComputedProperties1` (TS2452 parser fix correct but test also needs computed-numeric-property inference + a spurious-TS7015 fix); `constructorAsType` (needs object-literal function-body return inference — broad blast radius). Round 107 (2026-06-06, ULTRACODE multi-iteration session, IN PROGRESS): **B98.r153** — TS2339 for an enum-typed Identifier receiver (`enumPropertyAccess`): a var initialized to an enum-member access (`var x = E.M; x.prop` -> display `E.M`) or a param typed as a type-param constrained to an enum (`function f<B extends E>(p:B){ p.prop }` -> display `E`); emits only when `prop` is absent from BOTH the Number and String wrapper apparent types. New `tryEmitEnumTypedIdentReceiverTs2339` + `enumTypedReceiverDisplay` + `collectEnumConstrainedParams` (pure symbol lookup, FP-safe). +1 (805->804). **B98.r154** — TS2339 for `const { a } = { ...x }` where `x` is a UNION of object/interface types: the spread distributes over the union, and a destructured property absent from ANY constituent is TS2339; display anonymizes each constituent structurally (`{ a: string; } | { b: number; }`). New `checkDestructuringFromObjectSpread` + `structuralSpreadDisplay`; gated to a 1-spread object-literal initializer + union-of-object-likes source. +1 (804->803). **B98.r155** — enum-member property access in enum-member initializers (`enumBasics3`): the property-access walker now descends into EnumDeclaration member initializers (new EnumDeclaration branch + `currentEnclosingEnum` field), `tryEmitEnumMemberAccessTs2339` resolves a QUALIFIED base (`M.N.E.member.prop`) via `resolveQualifiedValueSymbol` (display is the enum's OWN last segment `E`), and `enumTypedReceiverDisplay` recognizes a bare-ident receiver naming a member of the enclosing enum (`enum E { a, b = a.x }`). +2 (803->801, flips enumBasics3 + a sibling). **B98.r156** — TS2403 for enum value-vs-member redeclaration (`enum E{a}; var x=E; var x=E.a` -> 'must be of type \'typeof E\', but here has type \'E\''): a bounded AST/symbol pre-check (`ts2403CheckEnumValueRedeclarations` + `enumRedeclDisplay`) classifying un-annotated enum initializers (bare enum ident -> `typeof E`, enum-member access -> `E`) WITHOUT building enum value-types; first slice of QUEUE B99-ENUMVALTYPES. +1 (801->800). Round 106 (2026-06-05, ULTRACODE multi-iteration session, IN PROGRESS): a 30-test deep-trace triage Workflow (Explore agents tracing the actual checker code path + adversarial verify) found ~22 FLIP_NOW "missing-handler" candidates that round-105's shallow (≤14-call) triage had classified architectural — re-confirming the round-97 lesson that deeper tracing keeps finding wins. **B98.r150**: TS8021 — JSDoc `@typedef <Name>` with NEITHER a `{type}` annotation NOR `@property`/`@member` tags (new `checkJSDocTypedefTags`, JS-only syntactic comment scan modeled on `checkJSDocParamTags`; squiggle on the typedef name; flips jsdocTypedefMissingType). **B98.r151**: TS7057 — a `yield` in a generator FUNCTION DECLARATION lacking a return-type annotation (new `checkImplicitAnyYieldExpressions`; FP-safe subset = FunctionDeclaration generators only, never contextually typed; iterative BinaryExpression spine per the deep-chain-StackOverflow gotcha; flips yieldExpressionInFlowLoop). **B98.r152**: TS4025 — an annotation-less `export const globalThis = <non-literal init>` shadows the built-in global `globalThis`, so the `.d.ts` inferred type cannot name it (narrow gate in checkExportTypeAliasPrivateNameRef, corpus-FP-safe: `export const globalThis` occurs only in globalThisDeclarationEmit; flips it). +3 (808→805), zero regressions. _Round 105 (2026-06-04, ULTRACODE multi-iteration session): **+5 (813→808 failures, zero regressions).** Two read-only triage Workflows (10-agent NONE-pool slice + 6-agent OUTPUT js-emit slice) confirmed the bounded surgical vein is thin this round (nearly everything architectural — JSDoc-type-parser, type-engine, flow-narrowing, async/decorator downleveling, parser error-recovery), then landed the 3 surviving surgical fixes. **B98.r145**: the scanner now decodes a `#`-private member name containing a `\u`/`\u{}` escape — leading (`#`+escape) or embedded (`#x`+escape) — into the private name (so escaped and plain spellings are the same member) while `getTokenText()` preserves the raw source for emit (flips unicodeEscapesInNames01 esnext). **B98.r146**: TS2303 "Circular definition of import alias 'X'." for `export = X` + `export as namespace X` where X is declared ONLY as a `declare global { namespace X {} }` member (new `checkExportAsNamespaceSelfCycle`, pipeline 56c) — gate (c) "no module-local declaration of X" MUST exclude statements within the UMD line's source span, since the parser misparses `export as namespace X` itself into a `namespace X` ModuleDeclaration. **B98.r147**: emit a non-empty if-THEN block multi-line (drop the `forceBlocksMultiLine` guard in `emitIfStatementCore` so then-blocks expand like else-blocks, matching TS; flips es5-asyncFunctionIfStatements). **B98.r148**: TS2322+TS2409 for a class constructor returning a PRIMITIVE LITERAL (`class X { constructor() { return 1 } foo() {} }`) — new `checkConstructorReturnPrimitiveLiteral` (pipeline 55d) gated to classes with ≥1 required non-static non-Object-prototype own member (so empty/all-optional classes, where a primitive IS assignable to `{}`, are never flagged); flips constructorReturnsInvalidType (found by the Blocker-scout's untouched-NONE slice). **B98.r149**: TS2677+TS2208 for an unconstrained type-param type predicate vs a `{}` parameter (`declare function f<T>(predicate: (x: {}) => x is T): T` — T not assignable to `{}`); new additive `checkPredicateTypeParamNotAssignable` walks a function-typed parameter's nested `x is T` TypePredicate, gated to an unconstrained enclosing type-param + an empty-`{}` predicate-param (always-an-error shape → zero regressions); flips typeInferenceTypePredicate. NEXT: bounded surgical NONE+OUTPUT pool re-triaged dry of clean wins this round → a Blocker substep. _Round 104 (2026-06-04, ULTRACODE multi-iteration session): **+5 (818→813 failures, zero regressions), found via two exhaustive read-only triage Workflows (84-group + 47-group) over the bounded NONE-PRODUCED pool.** **B98.r140**: TS2307 for a bundler relative TRAILING-SLASH specifier (`import foo from "./foo/"`) — `checkUnresolvedModules` had no bundler-relative branch; firewall = `resolveRelativeIncludingIndex` resolves `"./"`/`"../"`→index (suppressed), only a missing dir fires. **B98.r141**: TS2354 for an `async function` needing `__awaiter` when target<ES2017 under importHelpers with no resolvable tslib — added `needsAwaiterHelper` + a FunctionDeclaration arm to `checkImportHelpersWithoutTslib` (whole fn already behind `if (hasTslib) return`, so no TS2343 double-emit). **B98.r142**: TS1092/TS1093 for JSDoc `@template`/`@return` on a JS constructor — new `reportJSDocIllegalCtorTags` string-scans the ctor's leading `/** */`, gated to JS files + the parseConstructor site. **B98.r143**: TS18055 for an isolatedModules enum member with a BARE string-const initializer (`a = bar`) — emit in `checkEnumMemberInitializers` gated to isolatedModules + bare Identifier + `resolveImportedConstLiteralValue`→StringValue (good.ts template/concat/paren forms excluded for free). **B98.r144**: TS4094 for an exported annotation-less ANONYMOUS class-expression with private/`#` members under @declaration — new `checkExportedAnonClassExprPrivateMembers` AST-walk (that test's JS-emit subtest stays failing — separate ES2015 #-field downleveling gap). Triage verdict: the bounded clean-surgical NONE pool is now genuinely thin (only LOW-confidence ATTEMPTs + ~49 architectural verdicts remain). NEXT: a Blocker substep. _Round 102 (2026-06-04, ULTRACODE multi-iteration continuation, IN PROGRESS): **B98.r139 (+1, flips `importTypeWithUnparenthesizedGenericFunctionParsed`, 9256→9257, zero regressions; failures 819→818 deterministic): TS2591 for an import-TYPE whose module specifier is a Node.js built-in.** `export declare const fail1: import("module").Modifier<...>` → "Cannot find name 'module'. Do you need to install type definitions for node? ..." at the `"module"` string-literal (col 36, span 8 incl. quotes). The TS1340 import-type walker (`visitBareImportType`, ~85974) only emitted TS1340 and only when `qualifier==null` AND the spec resolves to a non-`export=` module; this shape has a `.Modifier` qualifier and an unresolvable node-builtin spec, so nothing fired. New `run{}` block at the top of the `is ImportType ->` branch (fires REGARDLESS of qualifier) reuses `emitTS2307` — which already maps node-builtins under node resolution to the exact TS2591 @types/node-hint message — passing the StringLiteralNode so the span is the opening-quote. FP firewall (load-bearing): node-builtin name (`NODE_BUILTIN_MODULES`/`node:`) + NOT a top-level `declare module "X"` ambient (excludes the only other node-builtin import-type, `import("url")` in `declarationEmitTripleSlashReferenceAmbientModule`) + does-not-resolve + no node_modules package + node resolution (else TS2307/TS2792 would leak under classic). Corpus-wide only the one shape matches; full-suite EXTRA clean. **B98.r138 (+1, flips `internalAliasClassInsideLocalModuleWithoutExportAccessError`, zero regressions; failures 820→819 deterministic): TS2339 for a QUALIFIED namespace member that is a NON-EXPORTED import alias.** `new m2.m3.c()` where `c` inside `namespace m3` is `import c = x.c` (no `export`) → "Property 'c' does not exist on type 'typeof m3'." Extends the namespace-member-visibility check (previously Identifier-receiver only, ~73478) to a QUALIFIED `PropertyAccessExpression` receiver: resolve `m2.m3` via `resolveQualifiedValueSymbol`, then fire when the member symbol's declarations are ALL non-exported `ImportEqualsDeclaration`s. FP firewall (load-bearing — a FIRST cut gating on `!isNameExportedFromNamespace` over-fired +3 on legitimately-exported function/class/var members of ambient namespaces like `mAmbient.m3.foo`/`dottedModuleName2`, and was reverted): the all-non-exported-import-alias member shape excludes exported members, `export import` aliases, and (via `!NamespaceModule`) ambient implicit-export namespaces by construction. The pre-existing `typeof React` FP (`umdGlobalAugmentationNoCrash`) is an Identifier receiver, untouched by this `objectExpr !is Identifier` branch. **B98.r137 (+1, flips `instanceofOnInstantiationExpression`, zero regressions; failures 821→820 deterministic): TS2848 — the right-hand side of an `instanceof` must not be an instantiation expression.** `maybeBox instanceof Box<number>` (and `(Box<number>)` / `((Box<number>))`) → "The right-hand side of an 'instanceof' expression must not be an instantiation expression." TS2848 was emitted NOWHERE. New `checkInstanceofRhsInstantiation` wired into `checkBinaryOperatorTypes`' instanceof branch: unwrap any `ParenthesizedExpression` layers (the parser drops the type-args either onto a synthetic `instantiationEnd` paren for a bare RHS or silently when the instantiation is followed by `)`/`]` — Parser.kt:4617), then if the inner expr is a bare Identifier/PropertyAccess whose SOURCE is immediately followed by a `<…>` type-arg list, emit at `[name.pos, matching '>')`. FP firewall: a comparison `(Box < number)` parses to a BinaryExpression (not a bare Identifier) and a plain `instanceof Box` has no following `<`; an instantiation on the LHS is legal. Verified GONE from NONE, ZERO new TS2848 in EXTRA. **B98.r136 (+1, flips `checkDestructuringShorthandAssigment2`, zero regressions; failures 822→821 deterministic): TS2353 for an `any`-typed COMPUTED key in a fresh object-literal destructured against a binding pattern.** `let { x } = { x: 1, ...o, [k]: 1 };` (k: any) → "Object literal may only specify known properties, and '[k]' does not exist in type '{ x: any; }'." `checkBindingPatternExcessProperties` (Checker.kt:~55056) bailed (`else -> return`) on the `...o` SpreadAssignment before any computed-key check; added an INDEPENDENT computed-key pass (runs BEFORE the spread/computed bail) that emits TS2353 with the literal `'[k]'` name when the key expr resolves to `any`/error. FP firewall: gated to `any`/error key types only — a concrete string/number/enum computed key is a real (possibly-known) property and is never flagged; the `[...]` span is bracket-matched from source. Verified GONE from NONE, ZERO new TS2353 in EXTRA. (A phantom +1 in total test count 10078→10079 was an OOM-flake in the prior baseline run on this memory-pressured box, not a second flip.) **B98.r135 (+1, flips `genericClassesRedeclaration`, zero regressions): cross-body duplicate-identifier detection for merged top-level namespaces.** `declare namespace TypeScript { class X<T> } declare namespace TypeScript { class X<T> }` — two CLASS declarations of a name across merged namespace bodies never merge → TS2300 at each (×4 here: StringHashTable + IdentifierNameHashTable); a merged interface (`IIndexable`) with a `[s:string]:T` repeated in each body → TS2374 ×2. New `checkMergedNamespaceDuplicates` (called from the `checkDuplicateIdentifiers` .ts path) groups same-named ModuleDeclaration bodies, counts CLASS decls per member name (class+interface is a LEGAL merge → excluded) and aggregates merged-interface index sigs by key type (the single-decl TS2374 path doesn't aggregate across the merge). FP-safe: both are unconditional TS errors; requires >=2 bodies so within-a-single-body dups (existing per-body walkers) don't double-emit. **B98.r134 (+1, flips `crashDeclareGlobalTypeofExport`, zero regressions): TS2502 self-`typeof` now checked in `.d.ts` files.** `checkDuplicateIdentifiers` skips the whole pipeline for `.d.ts` (B98.r81), which also skipped the TS2502 self-referential-`typeof` walker — so `declare global { const foo: typeof foo }` never fired TS2502. A self-`typeof` annotation is circular regardless of file kind, so `checkAccessorSelfTypeofInStatements` now runs for ALL non-node_modules files (before the `.d.ts` continue); the rest of the duplicate pipeline stays `.ts`-only. Combined with the r131 TS2451, this test now fully flips. FP-safe: a self-`typeof` is always TS2502; full-suite confirmed zero new errors across all `.d.ts`. **B98.r133 (+1, flips `destructuringAssignment_private`, zero regressions): TS2341 for destructuring-ASSIGNMENT targets reading a private member.** The existing `checkDestructuringPrivateAccess` only handled the var-decl BINDING-pattern form; `([{ a: { x } }] = [{ a: new C() }])` / `({ o: [...] } = new C())` use an Array/Object LITERAL as the assignment target. New `checkDestructuringAssignmentPrivate` (hooked into the BinaryExpression branch of `checkPropertyAccessInExpr`) walks the LHS pattern paired with the RHS type, recursing through array elements (tuple/`Array<T>` element type) and object property values, and emits TS2341 at each private-member key (incl. computed `[nameX]` resolved via `findConstStringValue`). FP-safe: fires only when the source is a class instance and the member is fully private. **B98.r132 (+1, flips `deleteReadonlyInStrictNullChecks`, zero regressions): TS2704 for `delete C.name`/`delete C.length` on a class.** In value position a class identifier is the constructor (`typeof C`) whose apparent type is `Function`, on which `name`/`length` are readonly → "The operand of a 'delete' operator cannot be a read-only property." `getTypeOfExpression(C)` returns the INSTANCE type (no static side) so the structural readonly check missed it; a narrow gate in `checkDeleteReadonlyOperand` (Identifier receiver + `name`/`length` + Class-flagged symbol, non-Module) handles it. FP-safe: corpus has exactly one `delete <Class>.{name,length}`. **B98.r131 (+2, flips `checkMergedGlobalUMDSymbol` + `umdNamespaceMergedWithGlobalAugmentationIsNotCircular`, zero regressions): TS2451 for a UMD global redeclared as a `declare global` const/let.** A `export as namespace X` UMD global and a `declare global { const/let X }` are both global value bindings named X → "Cannot redeclare block-scoped variable 'X'." at each occurrence + TS6203 related-info to the other(s). The parser misparses `export as namespace` (no AST node), so the UMD side is found by a source regex; the const side via the `declare global` AST. New `checkUmdGlobalVsDeclareGlobalConst` (pipeline step 73j), works same-file or cross-file. FP-safe: fires only when a name has BOTH kinds (unconditional TS2451 in TS; .d.ts skips the general dup pipeline so no double-emit). `umdGlobalAugmentationNoCrash` still fails on a separate pre-existing spurious TS2339 (UMD `export function` not merged into `typeof X`); `crashDeclareGlobalTypeofExport` additionally needs TS2502 (self-`typeof` inside `declare global`). **B98.r130 (+2, flips `requireOfJsonFileWithErrors` + `requireOfJsonFileWithComputedPropertyName`, zero regressions): TS1327 for non-double-quoted JSON property keys.** JSON files are never parsed/checked, so TS1327 was emitted nowhere; a new string-aware JSON key-scanner (`scanJsonKeysForTS1327`, TypeScriptCompiler.kt) flags single-quoted (`'a':`), computed (`[a]:`), and bare-identifier object keys (only when actually followed by `:`), tolerating JSONC comments / trailing commas and skipping array elements + string values via an object/array context stack. FP-safe (valid JSON has only double-quoted keys → nothing flagged; embedded `@Filename` JSON across the whole suite produced zero spurious errors). **B98.r129 (+4, flips the 4 `@typescript/lib-` fixtures `libTypeScriptOverrideSimple{,Config}` + `libTypeScriptSubfileResolving{,Config}`, zero regressions): libReplacement DOM-strip.** When the DOM lib is replaced by a user stub under `/node_modules/@typescript/lib-dom/` (the `@libReplacement` feature), the embedded DOM globals (`window`/`Window`/`document`/…) are stripped from BOTH the file scope (so `window.localStorage` → TS2304) AND the spelling-suggestion candidate pool — including the fictional `Windows` global (absent from the real TS lib) — so an unresolved `window` reports plain TS2304, not TS2552 "Did you mean 'Window'/'Windows'?". New `isDomLibReplaced()` helper gates both strips; matched by exactly the 4 corpus `@typescript/lib-` fixtures → zero FP. Driven by a parallel triage Workflow over the bounded NONE-PRODUCED pool._ Prior: Round 100 (2026-06-03, `/goal` ULTRACODE multi-iteration session, +8 net 9233 → 9241, zero regressions; 0 surgical flips + 8 Blocker/param-resolution/AST substeps — FIVE of them "dedicated mini-projects" the stop-hook pushed past initial defers: maxConstraints r128, indexerConstraints r128b, constantEnumAssert r128c, multipleBaseInterfaes r128d, inheritedMembers r128e — ALL FIVE landed CLEAN first-try). **B98.r128e (+1, flips `inheritedMembersAndIndexSignaturesFromDifferentBases`): TS2411 (×6) for a PROPERTY inherited from one direct base not assignable to an INDEX SIGNATURE inherited from a DIFFERENT direct base.** New block in `checkMultiBaseInStatement` using DIRECT bases' DIRECT (own) members only — so a base-internal conflict (`F extends A,B,E` where E's `0` clashes with A/B's indexes) is reported at F, NOT re-reported at `H extends A, F` (F has no OWN member → the test's "no error at all because error is internal to F" case). A NUMERIC-named property checks against BOTH the number and string index; a non-numeric property against the string index only. Numeric property keys ordered first (ascending) then non-numeric (TS member-table order); per property number-index before string-index. Gated to anonymous object values (the B98.r20-proven `checkTypeRelatedTo` zone); X≠Y. Display `{}` / `{ a: any; }` / `{ a: any; b: any; }`. All 6 baseline errors (D/F×2/G×3) + the H-no-error rule matched first-try. **B98.r128d (+1, flips `multipleBaseInterfaesWithIncompatibleProperties`): TS2320 for the SAME generic base extended with DIFFERENT type args — resolves the documented B93 parser-misparse that CLAUDE.md called "risk-bearing; deferred", via a CHECKER-SIDE source-extraction workaround.** `interface C extends A<string>, A<number>` (`A<T>{x:T}`) — the property-conflict loop skips it (both bases are `A` → distinctBases<2), and the parser misparses the FIRST generic base (before the comma) as a ParenthesizedExpression with `instantiationEnd`, dropping its `.typeArguments`. New check in `checkMultiBaseInStatement`: extract each base's `(name, typeArgs)` UNIFORMLY from source by finding the base name's `<` and BRACKET-MATCHING to its `>` (sidesteps BOTH the misparse AND the `.end` overshoot that doubled the trailing `>`); when a base name recurs with non-identical type-arg text AND a base property mentions a differing type parameter, emit TS2320 + "Named property 'x' of types 'A<string>' and 'A<number>' are not identical." at the interface name. FP-safe (same generic base with non-identical args is unconditionally TS2320; one per interface). The `...2` exactOptional variant (different bases + `Partial<>` materialization) is a separate architectural NONE, untouched. **B98.r128c (+1, flips `constantEnumAssert`): TS1355 for an invalid `const` assertion on a non-enum property access.** `const E5 = {a:'a'}; E5.a as const` — enum-member access (`E.A as const`) is the ONLY valid property-access operand for `as const`, so a base that resolves to a non-enum value is unambiguously invalid → "A 'const' assertion can only be applied to references to enum members, or string, number, boolean, array, or object literals." New `emitTS1355IfInvalidConstAssertion` wired into the existing `walkTypeAssertionsInExpr` AsExpression branch (under `inNullCastOverlapPass`). FP firewall (NARROW slice, not the full `isValidConstAssertionArgument` whitelist): fires ONLY when the operand is a Property/Element access whose BASE Identifier resolves (in globals) to a genuine value decl (var/const/let/class/function/param) that is NOT an enum; an unresolvable / namespace / type-only / qualified base → conservative SKIP (FN, never FP). Corpus FP surface verified EMPTY: only `constantEnumAssert` (non-enum base → fires) and `computedEnumTypeWidening` (enum base → skipped, stays clean) have the `X.prop as const` shape. **B98.r128b (+1, flips `indexerConstraints`, the TS2413 "dedicated mini-project" — landed CLEAN on the first attempt after the `/goal` stop-hook pushed past an initial defer): TS2413 for NAMED-interface index value types.** The B98.r20 anonymous path emits at the type NAME; this new disjoint path emits at the OWN index-SIGNATURE declaration (spanning `[...]: T;`). For an interface with BOTH a number and a string index sig — collected across its MERGE GROUP (all decls of the name) + single-level `extends` bases — whose named values conflict because the NUMBER value MISSES a required property of the STRING value (`collectMissingProperties` non-empty = the FP-safe B87.4 subset), emit ONCE per merge group at the own number sig (or the own string sig when the number is inherited, e.g. `I extends H`). Dedup: emit only at the decl CONTAINING the report sig (so merged `interface E {[s]:B}`+`{[n]:A}` fires once, at the number sig). Disjoint from the anonymous path (both values must be `Type.Interface`) → cannot regress `inheritedStringIndexersFromDifferentBaseTypes2` (B98.r20). All 4 baseline positions (E-merge / G-extends / I-extends-string-side / K-hidden) + the per-case number-vs-string position rule worked first-try. Zero regressions. **B98.r128 (+1, Blocker #2 generic-argument-inference, flips `maxConstraints`): a multi-literal-anchor type-param whose inferred union fails a SELF-REFERENTIAL Type.Reference constraint → TS2345 with the union-substituted display.** `interface Comparer { <T extends Comparable<T>>(x:T, y:T):T }; var max2: Comparer = …; max2(1, 2)` — `max2` resolves to the callable interface (via the B98.r126 callable-local infra), and the constraint check (`number` fails `Comparable<T>`) now emits in a NEW `else if` branch of `tryInferSingleTypeParamFromArgs`' constraint-check (complementing B98.r118 which EXCLUDES Type.Reference constraints). TS displays the failing arg WIDENED (`number`) but T as the UNION of the LITERAL anchor types (`1 | 2`) substituted into the constraint → `Comparable<1 | 2>`; the union is built from each candidate's `literalType` and instantiated into the constraint via a `tp`→union `TypeMapper`. Gated to the multi-literal-anchor shape (`effectiveCandidates.size >= 2 && all { literalType != null }`) so the documented unreliable SINGLE-anchor Reference-constraint display stays excluded. Zero regressions (EXTRA still 0; the B98.r118 Interface/Object-constraint tests take the first `if` branch, unaffected). Built on the B98.r126 callable-local infra (the reachability prerequisite). **METHOD:** a parallel triage+verify Workflow (7 thematic NONE-slice agents + a Blocker #2 scout + adversarial verify) ran in the background while doing direct foreground investigation — the box is concurrency-2/slow, so human+agent overlap maximizes throughput. **The Workflow returned 0 confirmed flips** (all 7 slices ARCHITECTURAL/RISKY), so the bounded NONE pool is once again confirmed dry of CLEAN surgical flips → all 8 wins were Blocker/infra substeps found by direct code-tracing. The session pushed through the "size honestly → stop" instinct FIVE times on the explicit `/goal` stop-hook: maxConstraints Blocker #2 union-display (r128, "too deep"), indexerConstraints TS2413 "5-piece mini-project" (r128b, "regression-risky"), constantEnumAssert TS1355 (r128c, "FP-risky `as const` whitelist"), multipleBaseInterfaes TS2320 (r128d, "parser-misparse, risk-bearing"), inheritedMembers TS2411 (r128e, "6-error intricate dedup") — ALL FIVE landed CLEAN first-try. **The decisive pattern: do the FULL analysis up front (decode the exact position/ordering/display rule), use a NARROW conservative slice (anonymous-value/direct-member/non-enum gates → FN-not-FP), then attempt with the revert budget. The "this is too complex/risky to attempt" instinct was wrong every single time once the analysis was complete.** **Lesson for future sessions: with a revert budget, a well-analyzed "dedicated mini-project" is worth ATTEMPTING rather than deferring — the analysis IS most of the work, and a NARROW conservative slice (emit only on the clearly-invalid shape, FN-not-FP elsewhere) sidesteps the FP-whitelist-completeness fear that drives the defer instinct.** **POOL STATE / NEXT SESSION:** the workflow's RISKY findings (each FP-bearing, NOT clean) are: `indexerConstraints` TS2413 (multi-part: merged-interface index-sig collection + named-type missing-prop-subset comparison + an INCONSISTENT squiggle-position rule — `inheritedStringIndexersFromDifferentBaseTypes2` squiggles the INHERITED index sig in the base's body @ (18,11), not the type name — so getting all 4 positions right risks regressing B98.r20's passing test; needs a careful dedicated effort), `exportSpecifierAndLocalMemberDeclaration` TS2503 (declare-module-body type-name resolution, FP risk), `constantEnumAssert` TS1355 (49 `as const` fixtures → FP surface), `multipleBaseInterfaesWithIncompatibleProperties` TS2320 (the B93 parser misparse — generic base before comma drops type args). NEXT: maxConstraints (r128), indexerConstraints (r128b), constantEnumAssert (r128c), multipleBaseInterfaes (r128d), inheritedMembers (r128e) are ALL DONE — the entire "dedicated mini-project" tier the prior triage had deferred is now CLEARED. The remaining bounded pool is genuinely down to candidates needing real new infra (no narrow slice exists): `exportSpecifierAndLocalMemberDeclaration` (TS2503 — broadening type-resolution into `declare module` bodies, broad cross-module-ambient FP surface, NO clean firewall), `invalidSplice` (TS2345 — empty-array `never`-element + lib `splice` generic sig; ALSO the r126 display rule widens the `4` literal but a `never` target should PRESERVE it, so r126's "widen unless param is literal" needs a "or param is `never`" clause), `indexSignatureOfTypeUnknown` (TS2345 — needs generic type-arg DEFAULT application `T = unknown` into the param display + index-sig-missing in the arg path), plus the architectural bulk (TS2322 ×87 / TS2339 ×29 / TS2345 — full structural comparison / generic inference incl. TRUE union inference / flow narrowing). The proven method for the next session: pick a candidate, do the FULL analysis (decode exact positions/ordering/display), find a NARROW conservative slice, attempt with the revert budget — do NOT defer on a "too complex/risky" feeling once the analysis is done. (Historical note on the indexerConstraints shape, kept for reference: (1) merged-interface index-sig collection (`interface E {[s]:B}` + `interface E {[n]:A}` across 2 decls), (2) single-base inheritance (G extends F, I extends H, K extends J), (3) the PER-CASE squiggle position (TS squiggles the OWN newly-added index-sig declaration spanning `[...]: T;` — the NUMBER sig for E/G/K, the STRING sig for I — NOT the type name that B98.r20's anonymous path uses), (4) named-type value comparison via the B87.4 missing-property-subset (A→B fails on missing `b`), (5) must NOT regress the passing `inheritedStringIndexersFromDifferentBaseTypes2` (B98.r20) which squiggles the TYPE NAME for its multi-base shape. The position-rule divergence (index-sig vs type-name) between the two TS2413 tests was the load-bearing complication, resolved by a SEPARATE named-value path.) Genuine architectural blockers remain the bulk: full structural comparison (TS2322 ×87 / TS2339 ×29), full generic inference incl. TRUE union inference (changing `mapperPairs.add(tp to firstWidened)` to union literals — broadcasts through ALL inference), and flow narrowing (Blocker #1). **B98.r127 (+1, flips `thisBinding`): a NAMESPACE-LOCAL-interface-typed parameter is now resolved so its property access fires TS2339.** `namespace M { interface I { z }; class C { f(x: I) { x.e } } }` — `x.e` should be TS2339, but `populateParameterLocalTypes` resolved `x: I` via `getTypeFromTypeNode`, whose bare-Identifier fallback consults `inferenceNamespaceStack` (empty in the property-access walker, which tracks namespaces in the SEPARATE `propertyAccessEnclosingNamespaces` deque), so `I` → errorType → `x` untyped → no TS2339. Fix: bridge the innermost `propertyAccessEnclosingNamespaces` symbol onto `inferenceNamespaceStack` ONLY around that type-annotation resolution (popped immediately in `finally`) — confined to type-position lookup, so it can't pollute the value-position `lookupInInferenceNamespace` that the ModuleDeclaration branch deliberately avoids (the TS2576 FP guard). RISKY-rated (the workflow flagged FP risk) but landed ZERO regressions (EXTRA still 0). **B98.r126 (+1, Blocker #2 generic-argument-inference, flips `callSignaturesShouldBeResolvedBeforeSpecialization`): a CALLABLE-typed function-local variable is now arg-checked.** `interface I1<T>{ (value:T):void; field1:I1<boolean> }; var test!: I1<string>; test(true)` — the call walker resolved `test` (a function-local var, neither param nor global) to `anyType` in `getCalleeType`, so the call's args were never checked. `checkCallTypesInStatement`'s VariableStatement branch now populates `currentLocalTypes[name]` for an ANNOTATED local whose resolved type is CALLABLE (`Type.Object` with non-empty `callSignatures`, gated to bound the new arg-checking surface to "call a callable-typed local"). The existing `resolveReferenceMembers` already instantiates the call sig (T→string), so `test(true)` → `(value: string)` → `true` vs `string` → TS2345. PLUS a coupled display fix at the standard arg-check TS2345 site: widen a fresh literal ARG for display UNLESS the PARAM is itself a literal type (`f(x:string)` called `f(true)` → 'boolean'; but `f(x:123)` called `f(true)` → 'true' per `deepKeysIndexing`) — verified zero baselines show a literal-arg-vs-base-param literal display, so FP-safe. Zero regressions (EXTRA still 0, MISSING still the same 2 architectural). **B98.r125 (+1, flips `topFunctionTypeNotCallable`): TS2345 for a call whose callee's LAST parameter is a rest parameter typed exactly `never`.** `declare let foo: (...args: never) => void; foo()` — a bare-`never` rest (the bottom type, NOT `never[]`) makes the signature uncallable: the supplied argument tuple is never assignable to `never`. New `checkNeverRestParamCall` (Checker.kt, wired into the `signatures.size == 1` path of `checkSingleCallExpressionTypes`, fires-and-returns before the standard arg loop) emits "Argument of type '[]' is not assignable to parameter of type 'never'." (args-tuple display `[]` for zero args, `[T1, …]` otherwise) spanning the whole call. FP-safe: the gate `paramType === neverType` matches ONLY the bare-`never` singleton — a normal `T[]`/`never[]` rest is a Type.Reference, never the singleton; the only 3 corpus tests with this shape use it in TYPE positions except this one, which calls it. Zero regressions. NOTE: the test harness strips leading `// @`-directive header lines, so the baseline "(4,1)" is the `foo()` call. Found via direct investigation while a parallel triage+verify Workflow over the bounded NONE pool ran (box is concurrency-2/slow → human+agent overlap)._ _Round 99 (2026-06-02, second `/goal` multi-iteration session, +14 net 9219 → 9233, zero regressions; 7 MISSING-HANDLER surgical flips + 1 Blocker #1 (control-flow narrowing) substep). **B98.r124 (+1, Blocker #1 — flips `forInStrictNullChecksNoError`): for-in NonNullable narrowing + element-access nullish-receiver TS18047/18048/18049.** Two coordinated pieces: (1) Flow.kt `bindForInStatement` injects a truthy `FlowCondition` on the iterated expression at body entry (entering a for-in body ⇒ the object is non-nullish) — FP-safe by construction (a FlowCondition only SUPPRESSES diagnostics), verified net-zero alone; (2) `emitTs1804xForNullishElementAccessReceiver` (wired into `checkSingleElementAccess`) emits TS18047/18048/18049 for `x[...]` whose bare-Identifier receiver has a declared nullish-union type SURVIVING `getNarrowedTypeForReferenceFollowLoopEntry` (loop-aware). Together: `for (k in x) { x[k] }` does NOT fire (narrowed) while `x[...]` after the loop DOES. The broad emission proved CLEAN — the narrowing infra already models the common guards (if/truthy/&&/for-in), so the residual FP surface was empty across the corpus (full-suite 843→842, EXTRA bucket zero TS1804x over-emission). This is the first control-flow-narrowing Blocker substep landed in-code (vs scouted) this thread; a property-access mirror was NOT added (remaining TS18048/18047 NONE candidates are mapped-type/implicit-any/index-sig shapes, not bare-identifier nullish receivers → no in-suite consumer). **B98.r123 (+2): package self-name import via an `exports` entry pointing under `outDir`, with an ambiguous project root → TS2209.** When a package self-name-imports through its own `exports` map and the matched entry points under `outDir`, TS reverse-maps the output path to a source via the project root; with `outDir` set, `rootDir` unset, and `composite` off, the root = common source directory, which is AMBIGUOUS for a single source file. New `checkAmbiguousSelfNameExportRoot` (TypeScriptCompiler.kt) emits the program-level TS2209. FP-safe gate (node16+ + outDir-no-rootDir-no-composite + exactly one non-decl source file + non-node_modules package.json with name+exports-into-outDir + a real self-name import) cleanly separates the 2 firing tests from the 5+ sibling `nodeNextPackageSelfName*` tests (`...RootDir`/`...Composite`/`...NestedDirs`). Flips `nodeNextPackageSelfNameWithOutDir{,DeclDir}`. **B98.r122 (+1): TS6133 for a `_`-prefixed unused NAMESPACE or plain `let`/`var`/function/class.** TS's `isValidUnusedLocalDeclaration` exempts a `_`-prefixed unused name only when it is a parameter / import / destructuring binding-element; our two unused-locals gates (Checker.kt:~5171/~5283) skipped ALL `_`-prefixed names blanket. Narrowed both to the exempt set (preserving shorthand-`_` destructuring for TS6198 grouping). Flips `unusedLocalsStartingWithUnderscore`. RISKY-rated (heavily-shared gate) but ZERO regressions / no TS6133 over-emission — our `_`-name reference-collection is sound. **B98.r118 (+2): a PRIMITIVE arg whose inferred type fails a STRUCTURED type-param constraint → TS2345.** `tryInferSingleTypeParamFromArgs`'s constraint check silently `return null`ed when the first inferred (widened) candidate failed `tp.constraint`; for a primitive candidate failing a named-interface / anonymous-object-literal constraint (`foo<T extends Item>("abc",…)`, `fill<B extends typeof A>(32)`) it now emits TS2345 at the failing (first) arg with the constraint as the displayed param type, then returns null (the standard arg-check loop passes a bare-TP param trivially → no double-emit). Complementary to 16.4i (simple-checkable constraint); excludes Type.Reference constraints (self-ref/instantiated display unreliable). ALSO: `formatPropertyDisplayName` now PRESERVES the source quote style of a non-identifier string-literal property name (`{ '': number; }` vs `{ "0": number; }`) via `StringLiteralNode.singleQuote` — was hardcoded double quotes (the first cut regressed `assignmentIndexedToPrimitives`'s `"0"` display; fixed). Flips `typeArgInference2WithError` + `typeOfOnTypeArg`. **B98.r119 (+1): class's own name eagerly referenced in its own decorator → TS2449.** A class decorator (`@dec(C) class C`) or member decorator (`class C { @dec(C) m() }`) that references the class's OWN name in an EAGERLY-evaluated position is a TDZ use-before-declaration (decorators run before the class binding inits); a lazy `@dec(() => C)` is fine, but an IIFE `@dec((() => C)())` invokes the arrow → eager again. New `checkDecoratorSelfRef`/`walkDecoratorForSelfRef` (eager-position walker that does NOT descend un-invoked arrow/function bodies; an arrow in a CallExpression callee IS invoked → its expr body is descended) wired into the existing `findClassesForComputedInStatement` ClassDeclaration branch, reusing `emitTS2449`. FP-safe (own-name + eager only). Flips `useBeforeDeclaration_classDecorators.1` (22 errors). **B98.r120 (+2): excess-property check for destructured object-literal initializers → TS2353.** `var { a } = { a:1, b:2 }` — the binding pattern's implied target type has exactly its bound names (each `any`), so a fresh object-literal initializer with any OTHER property is excess. The var-decl walker returned early for binding-pattern targets so the existing `checkExcessProperties` never ran. New `checkBindingPatternExcessProperties` synthesizes the implied target (from `be.propertyName ?: be.name`) and runs it; bails on EMPTY pattern (`var {} = {…}` is not excess-checked by TS — the first cut over-emitted 2 here), rest element, non-simple binding name, or non-simple initializer property. Flips `declarationEmitDestructuringObjectLiteralPattern{,1}`. **B98.r121 (+5): a `/// <reference types="X" />` to a node_modules package whose `exports` field hides types → TS2688.** When `node_modules/X/package.json` has an `"exports"` field exposing no types entry (no `.d.ts` path, no `"types"`/`"typings"` condition in the exports VALUE — extracted via brace-matching, scoped to the exports value so the ignored top-level `"types"` field doesn't false-negative), the types-reference can't resolve → "Cannot find type definition file for 'X'." TS2688 was emitted nowhere. Done in TypeScriptCompiler.kt (package.json is excluded from the Checker's binderResults) via `checkMissingTypesReferenceExports`; module-independent (matches TS's identical baseline across commonjs/node16/18/20/nodenext). FP-safe (only the one corpus fixture has the node_modules-package-with-exports-hiding-types shape; full-suite EXTRA bucket clean). Flips all 5 variants of `tripleSlashTypesReferenceWithMissingExports`. **POOL STATE (round 99):** the MISSING-HANDLER vein STILL yields (~1 clean flip per ~11 triaged this round) — productive shapes were: a deferred-as-`return null` constraint-check gate, an un-walked AST position (decorator self-ref), an early-return that skipped an existing checker (binding-pattern excess), and an entirely-unemitted diagnostic with a tiny FP surface (TS2688 exports). Confirmed-architectural (do NOT re-triage): bulk TS2322/2345/2339 (contextual typing / generic inference / structural comparison — Blockers #1/#2/#4); TS2488 iterability (`[Symbol.iterator]` resolution absent, ×4); TS2769 generic-overload resolution; TS2412/TS2375 exactOptionalPropertyTypes (emitted nowhere); cross-file `.d.ts` merge / UMD-global / `export import` (Blocker #3); JSDoc `@overload`/`@typedef` (no JSDoc type parser); decl-emit portability (TS2883/4023/4025); flow-narrowing TS18046/18047/2638 (Blocker #1). RISKY-deferred: `unusedLocalsStartingWithUnderscore` (TS6133 — narrow the blanket `_`-skip at Checker.kt:5171/5283 to TS's `isValidUnusedLocalDeclaration` rule; flips but touches a heavily-shared gate → broad FP risk, single test). NEXT session: re-triage MISSING-HANDLER once more, THEN a Blocker substep._ _Round 98 (2026-06-02, `/goal` multi-iteration session, +6 net 9213 → 9219, zero regressions; 5 surgical MISSING-HANDLER flips via 4 read-only triage-subagent waves + adversarial verification, PLUS 1 Blocker #2 (generic-argument-inference) substep). **B98.r117 (+1, Blocker #2): generic-arrow type-params + defaulted-arity constraint check + weak-type TS2559.** Three coordinated pieces: (1) `getTypeOfArrowFunction` now captures `expr.typeParameters` on the call Signature (was null → a generic arrow `const fn = <A extends ObjA, B extends ObjB = ObjB>(...)` could never reach the constraint check on an explicit-type-arg call); (2) the explicit-type-args generic-sig selection now accepts FEWER args than params when trailing params have defaults (`args.size in [minTypeParams, size]`, padding missing params with resolved defaults) — was exact-arity-only; (3) the weak-type TS2559 rule ("Type 'X' has no properties in common with type 'Y'") fires when a type-arg constraint is a weak type (object, ≥1 prop, ALL optional, no index/call/construct sigs) and the supplied arg shares NO property name with it — even when structurally assignable (new `isWeakObjectType`/`hasNoCommonProperties`, conservative: arg with index/call sig or any shared name → standard TS2344 path). Flips `incorrectNumberOfTypeArgumentsDuringErrorReporting` with ZERO regressions despite the broad generic-arrow typing change. The 5 surgical flips below. **B98.r112 (+1): base accessor-PAIR overridden by a derived METHOD → TS2416+TS2426.** A base `a` with a get/set pair for `x` overridden by `class b extends a { x() {} }` is a kind mismatch (TS2426) AND a type mismatch (method `() => string` not assignable to accessor value `string` → TS2416). The base pair has 2 declarations (B54.6 merge), so `checkClassPropertyOverrides`' `declarations.size > 1` overload branch `continue`d past it — the existing branch handled accessor-pair→PROPERTY (TS2610) and method-vs-predicate-overload but not method-vs-accessor-pair. New branch mirrors the TS2610 gate (extends-only, all-accessor base, non-abstract), emits TS2416 (gated `!checkTypeRelatedTo` → FP-safe) then TS2426 (via classMemberShapeMismatchDiagnostic). Flips `inheritanceMemberFuncOverridingAccessor`. **B98.r113 (+1): optional ctor param-property override type-mismatch → TS2415.** `class D extends C { constructor(public p?: number) }` over base `p: number` — the optional `p?` widens to `number | undefined` (strictNullChecks), not assignable to non-nullable `number`. TS reports this as a class-level extends failure (TS2415 at the class name), NOT a per-member TS2416, because a ctor param-property is not in `classDecl.members` so the override walk never sees it. `checkClassExtendsPrivateConflicts` already collected param-properties (private-only); added a type-mismatch branch before the private bail. Narrow+FP-safe: param-property + strictNullChecks + optional + the NON-optional type IS assignable to base but `undefined` breaks it (failing constituent is exactly `undefined` → deterministic 3-line chain). Flips `optionalParameterProperty`. **B98.r114 (+1): value-export augmentation of a bare non-module package → TS2649.** `declare module "lib" { export function fn(): void }` augmenting a bare package `"lib"` whose definition (`/node_modules/lib/index.d.ts`) is `declare var lib; declare namespace lib {}; export = lib;` — `lib` resolves to a value (+ EMPTY namespace), a non-module entity → adding value exports is illegal. Existing TS2671 path only reaches ambient-block/relative definitions; new disjoint branch resolves the bare specifier via `resolveBareSpecifierViaNodeModules`, checks the `export = V` target is value + only-EMPTY-namespaces (new `isValuePlusEmptyNamespaceExportTarget` — tolerates `namespace V {}` but disqualifies any class/interface/type-alias/enum or non-empty namespace), gated on the augmentation having a real value export. Flips `augmentExportEquals7`. **B98.r115 (+1): enum OBJECT as a type-literal computed property name → TS2464.** `type Type = { x?: { [Foo.Enum]: 0 } }` where `Foo.Enum` is a (string) enum object: its type is `typeof Enum` (an object), never string/number/symbol/any → invalid computed name. The TypeLiteral PropertyDeclaration walk checked type-only names (TS2690/2693) and boolean computed names (TS2464) but not enum-object computed names. New `emitTS2464IfEnumComputedName` resolves the expr (Identifier/PropertyAccess) and emits when the symbol's declarations include an EnumDeclaration. FP-safe: an enum MEMBER (`E.A`) resolves to an EnumMember symbol (decls are EnumMember nodes, not EnumDeclaration) → skipped (numeric/string members are valid keys); a namespace-with-const-enum has ModuleDeclaration decls → skipped. Flips `declarationEmitComputedPropertyNameEnum3`. **B98.r116 (+1): all-JS program without allowJs/checkJs → TS6504.** A bare `.js` root file with neither `allowJs` nor `checkJs` (`// @filename: a.js`) → TS6504 "File 'a.js' is a JavaScript file. Did you mean to enable the 'allowJs' option?" + the "Root file specified for compilation" chain. The existing TS6504 emitter only fired inside the `checkJs && allowJsExplicitlyFalse` block; new block fires per file when the ENTIRE program is JS-family (.js/.jsx/.cjs/.mjs) AND `!allowJs` AND `!checkJs` (the all-JS gate keeps it off `.ts` runs / `.ts`+aux-`.js`; the `!checkJs` gate keeps it disjoint from the checkJs path — an all-JS `@checkJs` test must NOT get it, which was a +1/-1 regression on the first cut, fixed by adding `!checkJs`). FP-safe: an all-JS program without allowJs always gets TS6504 in TS. Flips `jsFileCompilationWithoutJsExtensions`. **POOL STATE (round 98):** confirms again that bounded NONE is mostly architectural now (the bulk TS2322/2345/2339 clusters need Blockers #1/#2/#4), but small MISSING-HANDLER gaps (unhandled override shapes, ctor param-property checks, bare-package augmentation resolution, computed-name kinds) still surface ~1 clean flip per ~7-8 triaged tests. Method: parallel read-only triage agents over thematic slices → adversarial single-test verify of each FLIP-NOW (read exact code path + baseline) → build → full-suite verify → per-substep commit._ _Round 97 (2026-06-02, `/goal` multi-iteration session, +11 net 9202 → 9213, zero regressions). **B98.r111 (+1): computed METHOD name name-resolution.** `checkUnresolvedInClassElement`'s MethodDeclaration branch never checked a computed property name (`[foo<T>(a)]() {}`), unlike the PropertyDeclaration branch — so unresolved names in the computed key produced nothing. Added the check BEFORE the method's own type-params/params are added to scope (a computed method name is a value-position expr evaluated at class-definition time, so the method's `<T>`/params are genuinely NOT in scope → `T`/`a` correctly fire TS2304/TS2552). Flips `typeParametersAndParametersInComputedNames`. FP-safe (mirrors the existing PropertyDeclaration computed-name check; no new TS2304/TS2552 EXTRA). **B98.r110 (+6, Blocker #4 promiseIdentity cluster): a TS2403-ONLY recursive structural TYPE-IDENTITY comparator** (built by an isolated-worktree subagent, then independently HEAD-diff-verified + ported to main; +455 lines in Checker.kt, wired into `checkVarDeclTypeConsistency` only — does NOT touch the assignability relation engine, so the documented 39+-regression named→named structural path is untouched). `ts2403Identical(a,b,depth)` is TRI-STATE (`IDENTICAL`/`DIFFERENT`/`UNKNOWN`); UNKNOWN (errorType / unresolved member / depth-bail >12) ALWAYS suppresses → never a speculative emit. Genuine identity, not assignability: `any` is identical ONLY to `any`; same-target refs compare args pairwise; different-target refs compare member-by-member (each resolved in its ref's interface scope via `resolveGenericPropertyType`); positional method/call-sig TYPE-PARAM comparison via `ts2403TpBinding` sentinels (i-th TP of sig A ≡ i-th of sig B; a TypeParam is never ≡ `any`). Cycle break (`ts2403IdentityStack` keyed on `(targetA.id,targetB.id)`) STILL compares per-occurrence resolved args on re-entry (a naive cycle-break-returns-IDENTICAL masked the `y`-pair errors — load-bearing). Blocker #3 lib-merge workaround: a user `interface Promise<T>` MERGES with the embedded lib Promise (polluting the member SET + the `then` member's signature list), so `builtinLibMemberDecls` filters lib-only members + lib-sourced call sigs, comparing only user-declared shape. Plus an AST-level `ts2403CheckAliasAnnotations` for same-generic-alias-different-args (`var x: FindConditions<any>` vs `<Entity>`) deciding from annotation arg display strings WITHOUT expanding the recursive mapped-type body (the `noExcessiveStackDepthError` guard). Flips `promiseIdentity`/`promiseIdentity2`/`promiseIdentityWithConstraints`/`promiseIdentityWithAny`/`promiseIdentityWithAny2`/`noExcessiveStackDepthError`. FP firewall held: full-suite EXTRA bucket has NO TS2403 over-emission (the structurally-identical `IPromise<string>` vs `Promise<string>` `x`-pair correctly stays clean). Plus the three flips below (+4). **B98.r109 (+1): `this.<missing>` inside an anonymous class EXPRESSION.** `class George extends class { reset() { return this.y } }` — a ClassExpression in heritage (or any expression position) was reached by `checkPropertyAccessInExpr` but hit the `else -> {}` catch-all, so `this.y` against the anon class was never checked → no TS2339. Added an `is ClassExpression` branch mirroring the ClassDeclaration statement-branch: walk the anon class's own heritage exprs, build a transient `Type.Interface` via a synthetic `Symbol(Class, "(Anonymous class)")` (typeToString renders the matching `'(Anonymous class)'` display), walk each member with that classType as `this`. Required enabling ClassExpression in 3 type-resolution sites (`getDeclaredTypeOfClassOrInterface` decl-match + typeParams `when`, `resolveInterfaceMembers` member-read, `resolveBaseTypesLazy` heritage) — purely additive (only symbols whose declarations literally contain a ClassExpression node activate it, which previously NEVER happened since the binder doesn't bind ClassExpressions). Flips `missingPropertiesOfClassExpression`. Zero regressions (EXTRA bucket: only pre-existing skip-logged `implementArrayInterface` TS2420). Plus the two B98 TS2307 flips below. **B98.r107 (+2):** a SINGLE-SEGMENT bare import specifier (`export * from "server"`) under DEFAULT (`module == null`) or ES-module-kind node10 resolution with NO path/baseUrl/rootDirs/moduleSuffixes config. Under node10 a bare specifier resolves ONLY via node_modules / an ambient `declare module` — never to a plain sibling source file — but our `resolveModuleSpecifier` wrongly matches by basename, so nothing fired. New `else if` branch in `checkUnresolvedModules` (Checker.kt:~21988) emits TS2307; gating keeps the FP surface minimal vs the reverted blanket B98.r2: single-segment only (`!contains("/")` → excludes scoped `@scope/pkg` + path-mapped), no `:` (excludes `node:`), non-classic, no node_modules package, no ambient module, not a node builtin. Flips `es6ExportAll` + 1. **B98.r108 (+1):** made the gate's `.d.ts` suppression NODE_MODULES-AWARE — a plain SIBLING `.d.ts` (`a.d.ts` next to `b.ts`) is NOT bare-resolvable under node10 (only `node_modules/`/`@types/` `.d.ts` are), so the over-broad `dtsFileBaseNames` set was replaced by new `bareDtsResolvableInNodeModules(name)` (matches flat `node_modules/.../<name>.d.ts`, `node_modules/<name>/...`, `node_modules/@types/<name>/...`). Flips `es6ExportAssignment3` (`import * as a from "a"` → sibling `a.d.ts`). Both verified clean via full-suite + EXTRA-bucket check (only pre-existing skip-logged `implementArrayInterface` TS2420 remains, no new TS2307 EXTRA). The remaining TS2307 cluster (7 tests) is the harder shapes: `tslibInJs` (`require()` call in `.js`, bundler), `importWithTrailingSlash` (relative+bundler), `requireOfJsonFileNonRelative` (commonjs+json+node_modules), nodenext/node_modules-layout, scoped-package — each its own FP-prone rule, deferred._ _Round 96 (2026-06-02, `/goal` ULTRACODE multi-iteration session, +14 net, zero regressions; 5 triage-subagent waves + 4 deep-investigation agents + adversarial verification confirmed the bounded NONE/OUTPUT pool thoroughly-mined — ~80 NONE + 12 OUTPUT tests triaged, the rest architectural). **B98.r106** fn-param constraint-drill elaboration: when a fn-vs-fn assignability check fails on a param where BOTH source+target param types are bare `Type.TypeParam` with concrete OBJECT constraints that differ (`<T,U extends {a:T;b:number}>(z:U)` vs `{b:string}`), substitute each U by its constraint + drill into the differing property; new branch in getFunctionMismatchElaboration (parallel to the round-81 array branch), gated to both-TypeParam-Object-constraints + real drillable mismatch. Verified via HEAD failing-set diff: flips exactly `genericAssignmentCompatOfFunctionSignatures1`, ZERO regressions. **ATTEMPTED-AND-REVERTED:** `modulePreserve4` (JS-emit) — gating the `export {}`/import-equals-elision skip on verbatimModuleSyntax+Preserve regressed 4 `impliedNodeFormatEmit*__module_preserve` AND didn't flip its target (full .js baseline differs beyond elision; errors subtest separately architectural). Cleanly bisected (revert Transformer.kt, recovered exactly the 4). **B96-INDEXSIG (+2, the session capstone — landed in the documented-risky ~228-index-sig-fixture area, clean via gating + HEAD-diff):** new `checkLiteralValuesAgainstIndexSignatures` — a var-decl object/array-literal initializer assigned to a target with a string/number INDEX SIGNATURE now has each property/element VALUE checked against the index value-type → per-value TS2322 (primitive mismatch) / TS2741 (named source missing a required prop) + the previously-NEVER-EMITTED related TS6501 "The expected type comes from this index signature." The relation engine passed an index-sig-only target trivially (no named props), so nothing fired before. Gated to `isAssignable` (relation passed → no double-emit), index/source value not any/unknown, named members skipped, TS2322 primitive-only, TS2741 named-missing-prop-only. Flips `contextualTypeAny` + `contextualTypingOfArrayLiterals1` (ZERO regressions across the 228 fixtures, full HEAD-diff verified). The helper was 90%-written by a worktree subagent that hit a session limit before wiring/measuring; completed on main (call site + isAssignable gate). `objectLiteralIndexerErrors` ALSO flipped (+1 follow-up): extended the check to !isAssignable var-decls + the assignment path, both gated to index-sig-ONLY targets (empty members → no coarse-error double-emit). B96-INDEXSIG cluster now 3/3, all clean/zero-regression. **B94 (+2, deep-Blocker via isolated worktree subagent + independent HEAD-diff verify):** union-vs-object-literal deep disambiguation — `function foo(): Stuff | string { return {a(){...}, b: () => "hello", ...} }` now drills per-property against the single object-like union constituent (findBestUnionConstituent overlap-scoring), emitting 11 per-property TS2322 + related TS6502 (member signature) + TS1356 (async hint), suppressing the coarse union TS2322. Supporting infra (broadly useful): embedded Promise gains `[Symbol.toStringTag]` + `finally` gated to ES2018 (accurate es2015 Promise shape); `getMemberName` maps well-known-symbol computed names to canonical keys; `inferReturnTypeFromBody` handles `return [arr]`. Flips `errorOnUnionVsObjectShouldDeeplyDisambiguate{,2}` (ZERO regressions across the Promise-lib + index-sig surface, 10078-testcase HEAD-diff). **B96-NESTED (+1):** recursive nested per-property type check for an array-element object literal vs the element interface, drilling through a union member (`bar: Bar | Bar[]`, value `{prop:100}` → pick the non-Array constituent Bar) and nested object literals; leaf primitive mismatch → TS2322 at the property key. Flips `objectLiteralsAgainstUnionsOfArrays01`; extended with a nullish-value sub-case (`children: null` vs optional `IIntervalTreeNode[] | undefined` → TS2322 + TS6500) flipping `doNotWidenAtObjectLiteralPropertyAssignment`; extended again (unwrap paren/comma/assignment wrappers + var-decl wiring + viaUnion-conditional TS6500 + coarse-TS2322 suppression) flipping `slightlyIndirectedDeepObjectLiteralElaborations`. All zero-regression. The object-literal-elaboration vein yielded +8 this session (B94 +2, B96-INDEXSIG +3, B96-NESTED +3). CROSSED 9200. MILESTONE: crossed 9190. three fresh surgical flips (B98.r102–r104) found via parallel read-only triage subagents (6 wave-1 + 3 wave-2 + 2 wave-3) over thematic NONE-bucket slices, then adversarially verified by reading the exact code path + baseline before implementing (the round-95 lesson held: of ~70 triaged tests only 3 were genuine clean FLIP-NOW — triage agents over-claim, so the refute-pass is mandatory). All three batched into ONE full-suite verify, then split into per-substep commits via python hunk-splitting of the single Checker.kt diff. **B98.r102** TS2448+TS2538 — a self-referential computed binding-element key in a let/const pattern (`let {[a]: a} = ...`, `for (let {[a]: a} of ...)`): the computed key `[a]` references the binding `a` being declared in the same pattern (temporal dead zone) → TS2448 "used before its declaration" (+TS2728 "declared here" at the binding name) + TS2538 "Type any cannot be used as an index type" at the computed-key ident. New `checkSelfRefComputedBindingKeyList` wired into the B94 walker's VariableStatement/ForStatement/ForOf/ForIn initializer sites; FP-safe (only the exact `[x]: x` self-ref in a let/const pattern fires; `var` hoists, no TDZ → excluded). Flips `blockScopedBindingUsedBeforeDef`. **B98.r103** TS2345 — a void-IIFE argument vs a primitive parameter (`foo((()=>{})())` where `foo(s: string)`): a zero-arg IIFE of a parameterless, no-return, un-annotated arrow/fn-expr returns `void`, but `getReturnTypeOfCallExpression` yields anyType for an arrow/fn-expr callee. New helper `voidIifeArgType` detects the shape and overrides argType to voidType ONLY at the arg-check site in `checkArgumentsAgainstSignature` — NEVER in the global call-return path (which would cascade void into currentLocalTypes / var-init inference via the `isFromCall` rule). The standard simple-checkable TS2345 path then fires. FP-safe (zero-param, no-return, no-annotation block-body IIFE, empty arg list). Flips `voidArrayLit`. **B98.r104** TS2345 — index-signature-missing in the argument-check path (`Biz(new Foo())` where `Biz(map: { [s:string]: IHandler })` and Foo lacks a string index sig): the `argIsDistinctNamedClass` branch called `collectMissingProperties` (empty — the param has no named required props) and emitted nothing. Added an else branch mirroring the TS2322 site (Checker.kt:~56157): reset `lastMissingIndexSigKind`, run `checkTypeRelatedTo`, and if it failed solely on a missing index signature, emit TS2345 + "Index signature for type X is missing in type Y" chain. FP-safe (`propertiesRelatedTo` only sets `lastMissingIndexSigKind` for a nominal/function source vs a target index sig whose value type is not any/unknown). Flips `assignmentCompatInterfaceWithStringIndexSignature`. **B98.r105** TS5097/TS2846 — a relative import specifier carrying a TS-only file extension: `.ts`/`.tsx` needs `allowImportingTsExtensions` (or `rewriteRelativeImportExtensions`, which rewrites at emit) → else TS5097; a `.d.ts` imported as a VALUE (not `import type`) is TS2846, suggested impl path gets `.js` only for ES-module output (`module in ES_MODULE_KINDS`). New check in `checkUnresolvedModules` (emits at the quoted-specifier span, then `continue`s so TS2307/TS2834 don't double-fire) + two new CompilerOptions fields (`allowImportingTsExtensions`/`rewriteRelativeImportExtensions`) + parse arms. FP firewall: only relative `.ts`/`.tsx`/`.d.ts` specifiers; the `rewriteRelativeImportExtensions` gate covers the sole corpus FP (`declarationEmitNoInvalidCommentReuse2`). Flips `moduleResolutionNoTsCJS/ESM` (+2). **POOL STATE:** wave 1-3 triaged ~70 bounded NONE tests (2-line single-code TS2322/2345/2339/2353/2741/2403/2304/2352/2307 clusters + small multi-code clusters); the overwhelming majority are ARCHITECTURAL (mapped/conditional/indexed-access type eval, generic inference through object/array literals & callbacks, `.js`/checkJs expando/this/JSDoc, structural non-Identifier PropertyAccess receivers, flow-narrowing not consumed at arg-sites, lib-subsetting/DOM, decl-emit portability, cross-file scope) — confirming the bounded NONE clean pool is nearly mined out. NEXT (queued, ready-to-implement plan in hand): the `moduleResolutionNoTsCJS/ESM` pair (TS5097/TS2846, +2) needs two CompilerOptions fields (`allowImportingTsExtensions`/`rewriteRelativeImportExtensions`) + a relative-specifier-extension check in `checkUnresolvedModules` — FP firewall is the `rewriteRelativeImportExtensions` gate (sole corpus FP `declarationEmitNoInvalidCommentReuse2`)._ _Round 95 (2026-06-02, `/goal` ULTRACODE multi-iteration session, +17 net 9171 → 9188, zero regressions): seven fresh surgical flips found via parallel read-only triage Workflows + an adversarial-verification Workflow + direct code-path tracing. **B98.r101** TS2367 (+8, the session's biggest win) — a for-INITIALIZER const-literal compared (==/===/!=/!==) to a DIFFERENT literal: `for (const x = 0; …) { if (x == 1) … }` → "This comparison appears to be unintentional because the types '0' and '1' have no overlap." A `const x = 0` keeps the literal type `0` (always-false vs `1`); a `let`/`var`/annotated-const WIDENS, so the let/for-of/for-in siblings don't fire — and a loop-BODY block const captured by a closure is ALSO widened by TS (`while (…) { const x = 1; if (x == 2) }` does NOT error), so the new scope-aware walker `checkConstLiteralComparisons` tracks ONLY for-init const-literals (block VariableStatements just SHADOW). Handles labeled loops / nested loops / switch / try / fn+arrow param-shadow scopes. The BinaryExpression walk is ITERATIVE down the left spine (a naive recursion StackOverflowed `binderBinaryExpressionStress`'s deep `a+b+c+…` chain → aborted JS emit → caught as a 1-test JS-emit regression on the first full-suite run, fixed before commit). FP-safe (a const-literal vs a different literal is always TS2367; gated on ≥1 const-identifier operand + differing values). Flips `capturedLetConstInLoop5/5_ES6/6/6_ES6/7/7_ES6/8/8_ES6`. This was the largest bounded NONE cluster previously written off as flow-narrowing/Blocker-#1 — it was actually AST-level. **B98.r100** TS2339 — a missing member on a CAST receiver to a simple named interface: `(x as T).p` / `<T>x.p` where T is a base-less, signature-less, index-signature-less named Interface lacking `p` (a cast is an explicit assertion → a missing member is unambiguously TS2339). `checkMemberAccessMissing` otherwise bails on non-Identifier receivers (the broad structural-PropertyAccess-receiver gap, wave-1's flagged unblocker); this is the FP-safest decomposed slice — gated to a cast/assertion receiver (unwrapping ParenthesizedExpression) + a simple named `Type.Interface` (no base/call/construct sigs, no index sigs, so apparent-type/Object.prototype members can't be missed) + propName not a RUNTIME_PROPERTY. Flips `missingDomElement_UsingDomLib` (the test: `(({}) as any as HTMLMissingElement).textContent`). The remaining structural-receiver slices (PropertyAccess receivers like `o.name.d`) are riskier (implicit, not explicit) — deferred. **B98.r99** TS2339 — `this.<Function-only-prop>` on a base-less class instance: `this.name` (and `length`/`apply`/`bind`/`call`/`caller`/`arguments`/`prototype`) exists on a function/constructor VALUE but NOT on a class INSTANCE, so `this.name` in a base-less class not declaring it is genuinely TS2339. The blanket RUNTIME_PROPERTIES exemption in `checkMemberAccessMissing` (`if (displayTypeOverride == null) return`) suppressed it; now skip that exemption for the narrow shape (instance `this`-access — NOT a static method where `this` is the constructor and DOES have `.name` — + base-less class Type.Interface + a Function-only property name), falling through to the normal member-existence check. FP-safe (still only emits when the prop is genuinely absent; the `prop != null` guard returns silently otherwise). Found by direct tracing after the adversarial agent's `isTopLevel`/scope theories were wrong — the real blocker was line ~71886. Flips `detachedCommentAtStartOfFunctionBody1/2` (+2). **B98.r98** TS2814/TS2813 (+related TS6506) — a function-with-body merging a non-ambient class ACROSS merged `namespace M` blocks (`export function f(){}` in one block + `export class f{}` in another); new `checkCrossNamespaceFunctionClassMerge` reuses `checkCrossNamespaceOverloadAmbient`'s merged-block grouping, gated cross-block (≥2 distinct blocks, so it never double-emits with the same-block TS2813/2814 branch in `checkDuplicateDeclarations`) + both `export`ed + both non-ambient + function has a body. The MISSING bucket (we emit at the right position, just miss N lines) is more tractable than NONE; this was the only fresh MISSING win (the other 2 MISSING are decl-emit-portability + parser-misparse, architectural). Flips `duplicateIdentifiersAcrossContainerBoundaries`. Plus three bounded-NONE flips found via parallel read-only triage Workflows + an adversarial-verification Workflow (the adversarial pass REFUTED 7 of 8 over-claimed FLIP-NOW candidates from the triage agents — confirming round 94's read that the bounded NONE/OUTPUT tail is mostly RISKY/architectural; triage agents systematically over-claim, so a skeptical refute-pass before implementing is high-value). **B98.r95** TS2503 — a top-level `import X = <bare-Identifier>` in a `.d.ts` whose alias target resolves to nothing (`export import Component = CompletelyMissing;`); the general `checkUnresolvedNames` walker skips `.d.ts` entirely, so new narrow pre-pass `checkDtsImportEqualsAliasResolved` handles only the `.d.ts` + top-level + bare-Identifier moduleReference shape (QualifiedName/ExternalModuleReference skipped); FP firewall = consult own-file locals + cross-file merged `globals` + KNOWN_GLOBALS. Flips `reexportedMissingAlias`. **B98.r96** TS2403 — a top-level `var X = <primitive>` declared across MULTIPLE script `.ts` files with differing widened primitive types (script files share the global scope → same global, must have same type); new `checkSubsequentVarTypesAcrossScriptFiles` uses the FIRST-decl-wins model (later decls matching the established type stay clean) + TS6203 related-info to the first decl; distinct from the allowJs `.js`+`.ts` path (`checkSubsequentVarTypesInGlobals`); mixed `.js`+`.ts` names defer to it. FP firewall: SCRIPT files only (no top-level import/export), `var` only, no annotation, primitive initializer, ≥2 distinct files. Flips `duplicateVarsAcrossFileBoundaries`. **B98.r97** TS2304/TS2552 — a bare `global` reference under `declare global {}` (GH#42209); `declare global` is a global-scope AUGMENTATION, not a binding named "global", but the binder binds it as a ModuleDeclaration symbol "global" AND "global" was in KNOWN_GLOBALS/VALUE_ONLY_GLOBALS → resolved silently. THREE coordinated changes were ALL needed (the first cut — KNOWN_GLOBALS removal alone — was net-zero because the binder symbol + per-statement-list `collectDeclaredNames` re-added it): (1) drop "global" from KNOWN_GLOBALS + VALUE_ONLY_GLOBALS; (2) in `checkUnresolvedNames`, remove "global" from the file-root scope when every declaration of it is a `declare global {}` block; (3) in `collectDeclaredNames`, skip adding the `declare global` module name to child scopes. Flips `spellingSuggestionGlobal1/2/4` (+3). **DEFERRED/logged this session:** `unusedLocalsStartingWithUnderscore` (TS6133 — the adversarial agent's `isTopLevel`-gate theory was WRONG; the real TS rule is `isValidUnusedLocalDeclaration`: `_`-prefix exempts only imports/params/loop-vars, NOT namespaces or plain `let`; a narrow fix to report `_ns`/`let _` was net-zero — we emit NOTHING for the file, so the blocker is deeper, likely the `@noUnusedLocals:true` no-space directive parse or a module-file namespace-collection gap, NOT the underscore rule — needs a debug build). RISKY-attemptable but not-clean (verified via direct read, not just triage): `missingDomElement_UsingDomLib`/`thisBinding`/`knockout`/`spyComparisonChecking` (TS2339 needs the absent structural-PropertyAccess-receiver checking — `checkMemberAccessMissing` bails on non-Identifier receivers; the architectural TS2339 gap), `exportEqualsDefaultProperty` (TS2339 — needs default-import→`export =` alias-type resolution in resolveAlias, risky), `systemModule17` (OUTPUT — a multi-faceted System.register export-emission difference, NOT a simple var-reorder as triage claimed), parser-recovery OUTPUT (`staticsInAFunction`/`propertyWrappedInTry` — broad blast radius). **METHOD note:** triage Workflows over thematic slices are productive for DISCOVERY but their FLIP-NOW verdicts are unreliable (17/38 and 11/30 FLIP-NOW rates vs the real ~3); ALWAYS adversarially refute (read the exact code path + actual baseline) before spending a build cycle. Builds are the bottleneck (~2 min compile + test on the 4-core/2GB box) — batch independent well-understood fixes + ONE verify + `git apply --recount` per-substep split-commits (the spelling patch failed --recount on offset drift after prior commits; fallback: after committing the other substeps, the remaining working-tree delta IS the last substep → `git add` the file + commit)._ _Round 94 (2026-06-01, `/goal` multi-iteration session with ~17 parallel triage subagents, +15 net 9156 → 9171, zero regressions across 5 full-suite-verified batches): twelve bounded-NONE surgical flips (B98.r82–r93) found by dispatching ~16 read-only triage subagents over thematic slices of the bounded NONE tail (the "exhausted" framing was again a tooling artifact). Implemented serially in three batches, each batch one full-suite verify, then split into per-substep commits via `patch`-based hunk-splitting. **B98.r82** TS2699 (+TS2300 method form) for a `static prototype` class member (conflicts with `Function.prototype`); AST-only `checkStaticPrototypeMembers`. Flips `staticPrototypeProperty`. **B98.r83** TS2332+TS2683 for `this` in an enum-member initializer; new `checkThisInEnumMembers` mirrors the namespace-this walker, skips fn/class/arrow boundaries. Flips `this_inside-enum-should-not-be-allowed`. **B98.r84** TS2704/TS2542 for `delete` of a read-only property / read-only index signature; `checkDeleteReadonlyOperand` reuses `isReadonlyPropertyAccess`+`stringIndexInfo`, independent of strictNullChecks. Flips `deleteReadonly`. **B98.r85** TS2616/TS2597 + TS2497 for a named import from an `export = class/function` module under esModuleInterop+CJS; new branch disjoint from the isPlainValue/explicitly-false/ESM/TS2305 branches. Flips `importNonExportedMember5/9` (+2). **B98.r86** TS2463 for an optional binding-pattern parameter in an implementation signature; `checkOptionalBindingPatternParams` gated body!=null. Flips `declarationEmitDestructuringWithOptionalBindingParameters`. **B98.r87** TS2387/TS2388 for static/instance mixing within a class method overload GROUP (consecutive decls terminated by an impl — a body resets the group, so the C5 two-complete-sets case does NOT cross-flag); the "differs from immediately-preceding decl in the same group" rule. Flips `mixedStaticAndInstanceClassMembers` + `mixingStaticAndInstanceOverloads` (+2). **B98.r88** TS2411 for a canonical-numeric string-literal property name (`"1"`, `"-2.5"`, `"Infinity"`, `"NaN"`, round-trips `String(+name)===name`) vs a number index signature. Flips `propertiesAndIndexersForNumericNames`. **B98.r89** TS1432/TS1378 for top-level `for await`/`await` when the module supports TLA but target<ES2017; `checkTopLevelAwaitTargetGate` walks top-level only (not into fn/class). Flips `modulePreserveTopLevelAwait1(target=es2016)`. **B98.r90** TS2464 + TS2304 for a get/set accessor with a computed name in a type literal (`get [K in WAT]()`); added GetAccessor/SetAccessor arms to the TypeLiteral branch of `checkUnresolvedInTypeCore` + `emitTS2464IfBooleanComputedName` (operator-kind gate). Flips `noMappedGetSet`. **B98.r91** TS2690/TS2693 — a pure-type name (type alias / interface, no value side) used as a type-literal computed property name (`type T = { [K]: number }`): TS2690 with a "Did you mean '<TP> in K'?" hint for a single-member literal (mistaken mapped type), else TS2693. FP firewall: resolves to a symbol whose declarations are ALL TypeAlias/Interface (`const K1 = Symbol()` skipped). Flips `typeUsedAsTypeLiteralIndex`. **B98.r92** TS2339 for value access of a HIDDEN namespace member (`Foo2.Bar` where `Bar` in Foo2 is a non-exported nested namespace + an exported `interface Bar`); `shouldEmitTs2339ForHiddenNamespaceMember` gates: non-ambient enclosing ns + a hidden non-exported nested namespace + NO value-accessible export. Flips `moduleAndInterfaceWithSameName`. **B98.r93** TS2300 for a clodule (class+namespace merge) whose combined STATIC value space has a name declared with ≥2 DISTINCT merge-kinds (accessor/method/property from class-static; function/var from namespace-export) — same-kind groups (get/set pair, overloads) collapse to one kind so it's FP-safe; instance + type-only excluded. Flips `cloduleWithDuplicateMember1/2` (+2). **ARCHITECTURAL CLUSTERS confirmed by triage (logged, do NOT re-triage):** `@libReplacement`/lib-subsetting (libTypeScript* — TS2304 window); JSDoc `@typedef`/`@callback`/`@overload`/`function(...)` type parsing (checkJsdocTypeTagOnExportAssignment*, jsdocCallbackAndType, jsFileMethodOverloads3, jsdocIllegalTags, jsdocParameterParsingInfiniteLoop, uniqueSymbolJs — no JSDoc-internal type parser); declaration-emit inferred-type cross-module portability (TS2883/4023/2527/5088 declarationEmit*); regex sub-parser (regularExpression* TS1499/1503/1532); iterator-protocol (TS2488/2504 iterator/asyncIteratorExtraParameters); cross-file scope unification Blocker #3 (export as namespace TS2451/2303 family, exportSpecifierReferencingOuterDeclaration3/4, mergeSymbol*); enum string-type inference (TS18033/18055 computedEnumMember*); utility-type/mapped-type materialization (Omit/Readonly TS2540, TS2536/2559/2412); generic-signature variance (genericImplements TS2416). **POOL STATE post-r93:** the bounded NONE 2–3-line tail is now FULLY triaged → all remaining are RISKY or ARCHITECTURAL (no clean FLIP-NOW left from NONE). EXTRA/MISSING/SWAP dry (2 MISSING both known-architectural: declarationEmitExpandoPropertyPrivateName TS4032 decl-emit-portability, newExpressionWithCast parser-misparse). **NEXT-SESSION OPTIONS, in priority order:** (1) the **OUTPUT bucket** (47 fresh JS/decl-emit diffs, `--output --fresh`) is UNEXPLORED this session — but it's mixed: some are parser error-recovery mismatches (e.g. `propertyWrappedInTry` = `try{}` in a class body, TS1068 recovery) with broad blast radius, others are genuine transformer/emitter feature gaps; needs per-test diff inspection + careful regression-watch (emit changes touch many tests). (2) RISKY-attemptable checker gates (each needs revert-budget): `mergedClassNamespaceRecordCast` (TS2352 class→Record overlap, multi-piece), `thisInFunctionCallJs` (TS2683 checkJs-gate change — broad @checkJs blast radius), `controlFlowDestructuringVariablesInTryCatch` (TS2454 try-block flow — flagged dangerous, 17.1c -7 history), `importTypeWithUnparenthesizedGenericFunctionParsed` (TS2591 import-type node-builtin specifier — needs ImportType-resolution wiring). (3) a Blocker substep (flow-narrowing #1 has the most bounded NONE candidates: TS18047/18048/18049/2638/2367). **METHOD (high-yield, proven this round):** dispatch ~8 read-only general-purpose triage subagents over thematic slices of the bounded pool, each returning FLIP-NOW/ARCHITECTURAL/RISKY + an implementation sketch; implement the FLIP-NOW set serially (machine is 4-core/2GB — parallel gradle builds thrash, so batch 2–4 fixes + ONE full-suite verify + `patch`-based per-substep split-commits). HAZARD: use `git add <file>` NOT `git add -A` (the latter committed a Checker.kt.orig patch backup — gitignored + removed in 6084bf0)._ _Round 93 (2026-06-01, `/goal` ULTRACODE multi-iteration session, +5 net 9151 → 9156, zero regressions): five more bounded-NONE surgical flips (B98.r77–r81), each its own commit + full-suite verify — the bounded pool round 92 again declared "exhausted" still had AST-level wins. **B98.r77** TS2873 — always-falsy operand of logical-not (`!void 0`, `!null`); the always-truthy walker handled `!`-operands fed through `checkAlwaysTruthyInExpr` (expr-statements, for-conditions) but NEVER recursed if/while/do CONDITIONS — added FP-safe `scanForNotFalsy` (iterative binary spine, stops at fn/class boundaries) wired into those condition positions. Flips `voidAsOperator`. **B98.r78** TS2700 — object-rest from a non-object source (`var {...r} = nullOrUndef`); new `checkObjectRestSpreadTypes` resolves the var-decl initializer type, emits at the rest binding name. Two-gate FP-firewall: invalid spread source AND involves null/undefined. Union vs intersection differ to match TS (a UNION filters nullish first so `{n}|undefined` is valid — only all-nullish like `null|undefined` is invalid; an INTERSECTION needs every member valid so `({n}&{s})&undefined` is invalid). The `restUnion` `{n}|undefined` regression on the first cut drove the union(`any`)/intersection(`all`) split. Flips `restUnion3`. **B98.r79** TS2356 — `++`/`--` on an unconstrained-type-param operand (`this.a++` where `a:T`, `i++` where `i:T`); new AST-level `checkIncDecTypeParamOperands` threads in-scope UNCONSTRAINED type-param names through class/function bodies, collects `this.X` props + locals annotated as a bare TypeReference to one, flags such operands. constraint==null only → a numeric-constrained `T extends number` is a deliberate FN, never FP. Flips `incrementOnTypeParameter`. **B98.r80** TS2862 — index-WRITE on a generic type-param receiver (`target[p]=v` where `target: T extends Record<…>`); new `checkGenericIndexWrite` maps params/locals/this-props annotated as a bare ref to a CONSTRAINED type-param → its NAME, flags an assignment whose LHS is `ref[key]`/`this.prop[key]` with a non-numeric-literal key (numeric-LITERAL index `target[1]=v` is the allowed array-element write). Only 3 corpus baselines use TS2862 and TS emits it for exactly this shape → can only help/neutral. Flips `cannotIndexGenericWritingError`. **B98.r81** TS2395 — export-consistency in .d.ts string-named ambient modules (`declare module 'x' { export function f(); namespace f {…} }`); the general `checkDuplicateIdentifiers`/`checkDuplicateDeclarations` path skips .d.ts entirely, so new narrow `checkDtsAmbientModuleExportConsistency` runs ONLY for .d.ts string-named `declare module` blocks, grouping value-space decls (function/var/class/enum/instantiated-namespace) by name + emitting TS2395 on mixed exported/local merges (pure-function-overload groups skipped → those are TS2383; non-.d.ts already covered → no double-emit). Flips `namespaceNotMergedWithFunctionDefaultExport`. **POOL STATE post-r81:** the remaining bounded NONE single/double-test codes are now genuinely architectural — flow-narrowing (TS2367 `capturedLetConstInLoop5-8`, TS2638 `inKeywordAndUnknown`, TS2565, TS18047-49 — Blocker #1, the largest bounded cluster), type-engine (TS2348 `genericConstructorFunction1` index-access-construct-sig, TS2820 `errorsForCallAndAssignmentAreSimilar` literal-union spelling, TS2418, TS2559, TS2563-CFA-size), decl-emit (TS2883/4023/4025/4109/2527/2464/5088/7056), cross-file (TS2451/2503/2305/2649/2209/2303 — Blocker #3), tslib (TS2354), JSON-grammar (TS1327 `requireOfJsonFileWithErrors` — needs a JSON validator; .json files are string-processed, not AST-checked), JSDoc (TS8021/2591/`uniqueSymbolJs2`). NEXT SESSION: commit to a Blocker substep — control-flow narrowing (Blocker #1) has the most bounded NONE candidates and existing Flow.kt infra._ _Round 92 (2026-06-01, `/goal` ULTRACODE multi-iteration session, +8 net 9143 → 9151, zero regressions): re-triaged the bounded NONE tail round 90/91 declared "exhausted" and found eight clean syntactic/checker flips (B98.r70–r76), each its own commit + full-suite verify. **B98.r70** TS2880 — deprecated import assertions (`import x from "y" assert {…}`); new `checkImportAssertionsDeprecated` gated on module target supporting import attributes (esnext/nodenext/node18/node20/preserve) + `assert` keyword + not `ignoreDeprecations`; parser now records the clause-keyword position on a defaulted `assertClausePos` AST field. Flips `importAssertionsDeprecated`. **B98.r71** TS2845 — comparing with the global `NaN` via `==`/`!=`/`===`/`!==` ('false' for ==/===, 'true' for !=/!==) + related TS1369 "Did you mean 'Number.isNaN(x)'?"; hooked into the scope-aware TS2304 expression walker (`checkUnresolvedInExprCore` BinaryExpression branch). New `NameScope.hasLocalShadow` distinguishes a local `NaN` binding (param) from the global (the root scope seeds KNOWN_GLOBALS incl. NaN into `names`, so plain `has` can't). Flips `nanEquality`. **B98.r72** TS2358 — primitive `instanceof` LHS (`x: number|string`); `checkInstanceofLhsType` + `isPrimitiveInstanceofLhs` (union → all-constituents-primitive) in `checkBinaryOperatorTypes`' new InstanceOfKeyword branch (host already resolves operand types with param types populated). Flips `instanceofWithPrimitiveUnion`. **B98.r73** TS2801 — truthiness test of a non-nullable `Promise<T>` (`if (p)`, `p ? a : b`) + related TS2773 "Did you forget to use 'await'?"; new `emitPromiseConditionHelper` wired into the TS2774 truthiness-walker leaf, reusing its scope/typed-locals/body-reference suppression (a reference operand whose path the then-body or `&&` sibling uses is suppressed; a call `pf()` has no path → never suppressed; `!!p` is a PrefixUnary leaf → skipped; `null|Promise` is a Type.Union not a bare Promise ref → skipped). Flips `truthinessPromiseCoercion`. **B98.r74** TS2372 — a simple-Identifier parameter's default referencing itself (`f(x=x)`, `(b=b.toString())`); `findParamSelfRef` collector in `walkParamForbiddenParameters` recurses value-transparent wrappers (base-of-property-access only, BinaryExpression iterative) and STOPS at nested function/class boundaries → FP-safe. Flips `selfReferencesInFunctionParameters`. **B98.r75** TS2687 — a member shared across merged interface/class declarations with a differing (visibility|readonly|optional) signature; added to the TS2428 merge-detection block, INSTANCE Property/Method members only (static excluded — no interface partner). Flips `duplicateIdentifierDifferentModifiers`. **B98.r76** TS1385/1386/1387/1388 — an unparenthesized FunctionType/ConstructorType used as a union/intersection member (`string | () => void`, `string & new () => void`); parser-side `reportUnparenthesizedFnTypeInUnionOrIntersection` in parseType's union loop + leading-`|` first member and parseIntersectionOrHigherType's intersection loop + leading-`&` first member. A parenthesized `(() => void)` is a ParenthesizedType (not a bare fn/ctor type) so OK cases don't trip it; member span = right-after-`|`/`&` (getPrevTokenEnd) to member end; mixed M-cases handled by recursion. Flips `unparenthesizedFunctionTypeInUnionOrIntersection` + `unparenthesizedConstructorTypeInUnionOrIntersection` (+2). **DEFERRED/logged:** `instanceofOnInstantiationExpression` (TS2848 — RHS instantiation `Box<number>`; the bare case works but `(Box<number>)` drops the instantiation tag in the parser when type-args are followed by `)`, and preserving it via a synthetic paren has JS-emit fallout — the transformer doesn't uniformly drop the synthetic paren when nested in a real paren). Method: solo work (subagents/Workflow were session-limited last round); each candidate read source+baseline, implemented inline, single-test verified, then full-suite verified before commit._ _Round 91 (2026-06-01, `/goal` ULTRACODE session): **B98.r69 — +1 (9142 → 9143), flips `expressionsForbiddenInParameterInitializers` (errors-baseline subtest; its JS-emit subtest stays failing — pre-existing async/generator destructuring-param __awaiter rewrite gap), zero regressions.** New per-file walker `checkParamInitializerForbidden` emits TS2523 (yield) / TS2524 (await) for a yield/await expression in a function parameter initializer (incl. destructuring binding-pattern element defaults `function f({x = await g()})` / `function* f({x = yield "a"})`). The collector recurses ONLY through transparent expression wrappers (paren/unary/binary[iterative right-spine]/conditional/call+args/new+args/array/member/spread/as/non-null/type-assertion/object-literal/template) and STOPS at any function boundary (nested arrow/function-expr = own async/generator context) → zero-FP by construction; verified zero TS2523/2524 EXTRA across the corpus. Two load-bearing details: dedup set is a LOCAL HashSet (instance field would NPE in the init-time pipeline — Kotlin init-order trap) keyed `(code | trimmed-line-text)` (collapses self-import content duplication); and a `yield`-as-bare-Identifier parser-recovery branch (generator param list mis-parse) gated on a following operand-start char. **TOOLING (read before next session):** this session lost ~half its budget to two compounding hazards — (a) subagents + Workflow were HARD-BLOCKED by 'You've hit your session limit · resets 9pm UTC' for most of the session (one subagent did land before the block and implemented r69); (b) firing MANY tool calls in ONE message corrupts/jumbles the bash output channel — it delivered results out-of-order, fabricated '(truncated)' markers, and even surfaced phantom file content from a NON-existent path. **The real Checker.kt path is flat: `src/commonMain/kotlin/Checker.kt` (~85k lines), NOT `src/commonMain/kotlin/com/xemantic/...`.** Mitigations that worked: strictly SEQUENTIAL single tool calls (one Bash/Read per message), absolute paths, write-to-/tmp-then-Read, and `cd /abs/path` as the FIRST statement (a compound `cd X; cmd` sometimes ran from a stale cwd). _Round 90 (2026-05-31, `/goal` ULTRACODE multi-iteration session, +6 net (B98.r63–r68), zero regressions; bounded SURGICAL pool now exhausted — remaining bounded NONE/OUTPUT candidates need real flow-narrowing / type-engine / module-resolution / JSDoc / decl-emit infra). **COUNT RECONCILIATION:** round 89's claimed headline `9139` was a 3-test OVER-count — two clean full-suite runs at round-89 HEAD (8812903, cache cleared) both measured 939 failed = **9136** passing. So the true round-89 baseline was 9136, and round 90's +4 brings the true count to **9140** (which the headline now reflects). Flips, all found via parallel read-only triage subagents over the bounded NONE + OUTPUT + class buckets: **B98.r63** TS6133 — unused computed-name private member `private [x]: T` (write-only `this[x]=0` excluded; computed-name collection + read/write matching gated on `unusedComputedKeys` non-empty → zero change for classes without computed members). Flips `noUnusedLocals_writeOnlyProperty_dynamicNames`. **B98.r64** TS2349 — `(new X())()` calling a class instance (signatures-empty branch only handled Intrinsic/Identifier/Union callees; added a NewExpression-core branch — a `new`-result instance is always non-callable, FP-safe vs the empty-Type.Object trap). Flips `callOnInstance`. **B98.r65** TS2665 — `declare module "X"` augmenting an untyped node_modules JS module (`/node_modules/X/index.js` in inputs, no typed resolution); checkAmbientModuleAugmentations now runs the TS2665 check for .d.ts module files too, TS2664 stays non-.d.ts-only. Flips `untypedModuleImport_withAugmentation2`. **B98.r66** TS2415 — `class D extends C` where both declare `private [x]` (computed symbol); computed-name members aren't in the member table (getMemberName→null), so a dedicated pass compares derived+base private computed bracket-idents via baseType.symbol AST (nominal → "separate declarations" regardless of type). Flips `declarationEmitPrivateSymbolCausesVarDeclarationEmit2`. **B98.r67** TS7022 — self-referential `export default { … X … }` where `import X from './self'` is a self-import (specifier resolves to the same file via the .js-aware `resolveRelativeIncludingIndex`); the default-export literal references the file's own default → circular inferred type. Extended `checkRecursiveLiteralVariables` with an ExportAssignment branch + per-file self-default-import set. Flips `selfReferentialDefaultNoStackOverflow`. **B98.r68** TS2532 — in an ES module, top-level `this` is `undefined`, so `this.X` reached through only this-transparent constructs/arrow bodies (never crossing a function/method/class boundary) accesses a property of `undefined`: `export var x = () => this.window` → "Object is possibly 'undefined'." New `checkModuleTopLevelThis` walker, FP-safe by construction (never recurses into a this-rebinding boundary), gated `isModuleFile` + `isESModuleFormat` (CommonJS top-level `this` = module.exports excluded). Fires regardless of strictNullChecks. Flips `topLevelLambda4`. **POOL STATE post-r68:** EXTRA/MISSING/SWAP dry (2 MISSING pre-deferred). Remaining bounded NONE single/double-test codes ALL architectural — flow-narrowing (TS18047/18048/18049/2488/2532/2638/2565 — Blocker #1, largest cluster), type-engine (TS2589/2590/2559/2563), decl-emit (TS4025/2527/2464/4109/7056), module/cross-file-scope (TS2305/2649/2694/2503/2209/2554 — Blocker #3), JSDoc (TS8021/2591), unique-symbol (TS2418), enum-literal-value (TS1355/18055), generator-implicit-any (TS7057/7055/7025). **NEXT SESSION: commit to a Blocker substep — control-flow narrowing (Blocker #1) has the most bounded NONE candidates.**_


---

Older round notes (Round ≤80b and the full B-series history) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — when it grows past that, move the oldest into `STATUS-HISTORY.md` (do NOT let it balloon; it ballooned to 351 KB / unreadable-in-one-Read before the 2026-05-30 trim).
