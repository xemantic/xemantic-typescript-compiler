# Status

**Phase 4 — Checker buildout.** 9,219 / 10,078 tests passing (~91.48%). _Round 98 (2026-06-02, `/goal` multi-iteration session, +6 net 9213 → 9219, zero regressions; 5 surgical MISSING-HANDLER flips via 4 read-only triage-subagent waves + adversarial verification, PLUS 1 Blocker #2 (generic-argument-inference) substep). **B98.r117 (+1, Blocker #2): generic-arrow type-params + defaulted-arity constraint check + weak-type TS2559.** Three coordinated pieces: (1) `getTypeOfArrowFunction` now captures `expr.typeParameters` on the call Signature (was null → a generic arrow `const fn = <A extends ObjA, B extends ObjB = ObjB>(...)` could never reach the constraint check on an explicit-type-arg call); (2) the explicit-type-args generic-sig selection now accepts FEWER args than params when trailing params have defaults (`args.size in [minTypeParams, size]`, padding missing params with resolved defaults) — was exact-arity-only; (3) the weak-type TS2559 rule ("Type 'X' has no properties in common with type 'Y'") fires when a type-arg constraint is a weak type (object, ≥1 prop, ALL optional, no index/call/construct sigs) and the supplied arg shares NO property name with it — even when structurally assignable (new `isWeakObjectType`/`hasNoCommonProperties`, conservative: arg with index/call sig or any shared name → standard TS2344 path). Flips `incorrectNumberOfTypeArgumentsDuringErrorReporting` with ZERO regressions despite the broad generic-arrow typing change. The 5 surgical flips below. **B98.r112 (+1): base accessor-PAIR overridden by a derived METHOD → TS2416+TS2426.** A base `a` with a get/set pair for `x` overridden by `class b extends a { x() {} }` is a kind mismatch (TS2426) AND a type mismatch (method `() => string` not assignable to accessor value `string` → TS2416). The base pair has 2 declarations (B54.6 merge), so `checkClassPropertyOverrides`' `declarations.size > 1` overload branch `continue`d past it — the existing branch handled accessor-pair→PROPERTY (TS2610) and method-vs-predicate-overload but not method-vs-accessor-pair. New branch mirrors the TS2610 gate (extends-only, all-accessor base, non-abstract), emits TS2416 (gated `!checkTypeRelatedTo` → FP-safe) then TS2426 (via classMemberShapeMismatchDiagnostic). Flips `inheritanceMemberFuncOverridingAccessor`. **B98.r113 (+1): optional ctor param-property override type-mismatch → TS2415.** `class D extends C { constructor(public p?: number) }` over base `p: number` — the optional `p?` widens to `number | undefined` (strictNullChecks), not assignable to non-nullable `number`. TS reports this as a class-level extends failure (TS2415 at the class name), NOT a per-member TS2416, because a ctor param-property is not in `classDecl.members` so the override walk never sees it. `checkClassExtendsPrivateConflicts` already collected param-properties (private-only); added a type-mismatch branch before the private bail. Narrow+FP-safe: param-property + strictNullChecks + optional + the NON-optional type IS assignable to base but `undefined` breaks it (failing constituent is exactly `undefined` → deterministic 3-line chain). Flips `optionalParameterProperty`. **B98.r114 (+1): value-export augmentation of a bare non-module package → TS2649.** `declare module "lib" { export function fn(): void }` augmenting a bare package `"lib"` whose definition (`/node_modules/lib/index.d.ts`) is `declare var lib; declare namespace lib {}; export = lib;` — `lib` resolves to a value (+ EMPTY namespace), a non-module entity → adding value exports is illegal. Existing TS2671 path only reaches ambient-block/relative definitions; new disjoint branch resolves the bare specifier via `resolveBareSpecifierViaNodeModules`, checks the `export = V` target is value + only-EMPTY-namespaces (new `isValuePlusEmptyNamespaceExportTarget` — tolerates `namespace V {}` but disqualifies any class/interface/type-alias/enum or non-empty namespace), gated on the augmentation having a real value export. Flips `augmentExportEquals7`. **B98.r115 (+1): enum OBJECT as a type-literal computed property name → TS2464.** `type Type = { x?: { [Foo.Enum]: 0 } }` where `Foo.Enum` is a (string) enum object: its type is `typeof Enum` (an object), never string/number/symbol/any → invalid computed name. The TypeLiteral PropertyDeclaration walk checked type-only names (TS2690/2693) and boolean computed names (TS2464) but not enum-object computed names. New `emitTS2464IfEnumComputedName` resolves the expr (Identifier/PropertyAccess) and emits when the symbol's declarations include an EnumDeclaration. FP-safe: an enum MEMBER (`E.A`) resolves to an EnumMember symbol (decls are EnumMember nodes, not EnumDeclaration) → skipped (numeric/string members are valid keys); a namespace-with-const-enum has ModuleDeclaration decls → skipped. Flips `declarationEmitComputedPropertyNameEnum3`. **B98.r116 (+1): all-JS program without allowJs/checkJs → TS6504.** A bare `.js` root file with neither `allowJs` nor `checkJs` (`// @filename: a.js`) → TS6504 "File 'a.js' is a JavaScript file. Did you mean to enable the 'allowJs' option?" + the "Root file specified for compilation" chain. The existing TS6504 emitter only fired inside the `checkJs && allowJsExplicitlyFalse` block; new block fires per file when the ENTIRE program is JS-family (.js/.jsx/.cjs/.mjs) AND `!allowJs` AND `!checkJs` (the all-JS gate keeps it off `.ts` runs / `.ts`+aux-`.js`; the `!checkJs` gate keeps it disjoint from the checkJs path — an all-JS `@checkJs` test must NOT get it, which was a +1/-1 regression on the first cut, fixed by adding `!checkJs`). FP-safe: an all-JS program without allowJs always gets TS6504 in TS. Flips `jsFileCompilationWithoutJsExtensions`. **POOL STATE (round 98):** confirms again that bounded NONE is mostly architectural now (the bulk TS2322/2345/2339 clusters need Blockers #1/#2/#4), but small MISSING-HANDLER gaps (unhandled override shapes, ctor param-property checks, bare-package augmentation resolution, computed-name kinds) still surface ~1 clean flip per ~7-8 triaged tests. Method: parallel read-only triage agents over thematic slices → adversarial single-test verify of each FLIP-NOW (read exact code path + baseline) → build → full-suite verify → per-substep commit._ _Round 97 (2026-06-02, `/goal` multi-iteration session, +11 net 9202 → 9213, zero regressions). **B98.r111 (+1): computed METHOD name name-resolution.** `checkUnresolvedInClassElement`'s MethodDeclaration branch never checked a computed property name (`[foo<T>(a)]() {}`), unlike the PropertyDeclaration branch — so unresolved names in the computed key produced nothing. Added the check BEFORE the method's own type-params/params are added to scope (a computed method name is a value-position expr evaluated at class-definition time, so the method's `<T>`/params are genuinely NOT in scope → `T`/`a` correctly fire TS2304/TS2552). Flips `typeParametersAndParametersInComputedNames`. FP-safe (mirrors the existing PropertyDeclaration computed-name check; no new TS2304/TS2552 EXTRA). **B98.r110 (+6, Blocker #4 promiseIdentity cluster): a TS2403-ONLY recursive structural TYPE-IDENTITY comparator** (built by an isolated-worktree subagent, then independently HEAD-diff-verified + ported to main; +455 lines in Checker.kt, wired into `checkVarDeclTypeConsistency` only — does NOT touch the assignability relation engine, so the documented 39+-regression named→named structural path is untouched). `ts2403Identical(a,b,depth)` is TRI-STATE (`IDENTICAL`/`DIFFERENT`/`UNKNOWN`); UNKNOWN (errorType / unresolved member / depth-bail >12) ALWAYS suppresses → never a speculative emit. Genuine identity, not assignability: `any` is identical ONLY to `any`; same-target refs compare args pairwise; different-target refs compare member-by-member (each resolved in its ref's interface scope via `resolveGenericPropertyType`); positional method/call-sig TYPE-PARAM comparison via `ts2403TpBinding` sentinels (i-th TP of sig A ≡ i-th of sig B; a TypeParam is never ≡ `any`). Cycle break (`ts2403IdentityStack` keyed on `(targetA.id,targetB.id)`) STILL compares per-occurrence resolved args on re-entry (a naive cycle-break-returns-IDENTICAL masked the `y`-pair errors — load-bearing). Blocker #3 lib-merge workaround: a user `interface Promise<T>` MERGES with the embedded lib Promise (polluting the member SET + the `then` member's signature list), so `builtinLibMemberDecls` filters lib-only members + lib-sourced call sigs, comparing only user-declared shape. Plus an AST-level `ts2403CheckAliasAnnotations` for same-generic-alias-different-args (`var x: FindConditions<any>` vs `<Entity>`) deciding from annotation arg display strings WITHOUT expanding the recursive mapped-type body (the `noExcessiveStackDepthError` guard). Flips `promiseIdentity`/`promiseIdentity2`/`promiseIdentityWithConstraints`/`promiseIdentityWithAny`/`promiseIdentityWithAny2`/`noExcessiveStackDepthError`. FP firewall held: full-suite EXTRA bucket has NO TS2403 over-emission (the structurally-identical `IPromise<string>` vs `Promise<string>` `x`-pair correctly stays clean). Plus the three flips below (+4). **B98.r109 (+1): `this.<missing>` inside an anonymous class EXPRESSION.** `class George extends class { reset() { return this.y } }` — a ClassExpression in heritage (or any expression position) was reached by `checkPropertyAccessInExpr` but hit the `else -> {}` catch-all, so `this.y` against the anon class was never checked → no TS2339. Added an `is ClassExpression` branch mirroring the ClassDeclaration statement-branch: walk the anon class's own heritage exprs, build a transient `Type.Interface` via a synthetic `Symbol(Class, "(Anonymous class)")` (typeToString renders the matching `'(Anonymous class)'` display), walk each member with that classType as `this`. Required enabling ClassExpression in 3 type-resolution sites (`getDeclaredTypeOfClassOrInterface` decl-match + typeParams `when`, `resolveInterfaceMembers` member-read, `resolveBaseTypesLazy` heritage) — purely additive (only symbols whose declarations literally contain a ClassExpression node activate it, which previously NEVER happened since the binder doesn't bind ClassExpressions). Flips `missingPropertiesOfClassExpression`. Zero regressions (EXTRA bucket: only pre-existing skip-logged `implementArrayInterface` TS2420). Plus the two B98 TS2307 flips below. **B98.r107 (+2):** a SINGLE-SEGMENT bare import specifier (`export * from "server"`) under DEFAULT (`module == null`) or ES-module-kind node10 resolution with NO path/baseUrl/rootDirs/moduleSuffixes config. Under node10 a bare specifier resolves ONLY via node_modules / an ambient `declare module` — never to a plain sibling source file — but our `resolveModuleSpecifier` wrongly matches by basename, so nothing fired. New `else if` branch in `checkUnresolvedModules` (Checker.kt:~21988) emits TS2307; gating keeps the FP surface minimal vs the reverted blanket B98.r2: single-segment only (`!contains("/")` → excludes scoped `@scope/pkg` + path-mapped), no `:` (excludes `node:`), non-classic, no node_modules package, no ambient module, not a node builtin. Flips `es6ExportAll` + 1. **B98.r108 (+1):** made the gate's `.d.ts` suppression NODE_MODULES-AWARE — a plain SIBLING `.d.ts` (`a.d.ts` next to `b.ts`) is NOT bare-resolvable under node10 (only `node_modules/`/`@types/` `.d.ts` are), so the over-broad `dtsFileBaseNames` set was replaced by new `bareDtsResolvableInNodeModules(name)` (matches flat `node_modules/.../<name>.d.ts`, `node_modules/<name>/...`, `node_modules/@types/<name>/...`). Flips `es6ExportAssignment3` (`import * as a from "a"` → sibling `a.d.ts`). Both verified clean via full-suite + EXTRA-bucket check (only pre-existing skip-logged `implementArrayInterface` TS2420 remains, no new TS2307 EXTRA). The remaining TS2307 cluster (7 tests) is the harder shapes: `tslibInJs` (`require()` call in `.js`, bundler), `importWithTrailingSlash` (relative+bundler), `requireOfJsonFileNonRelative` (commonjs+json+node_modules), nodenext/node_modules-layout, scoped-package — each its own FP-prone rule, deferred._ _Round 96 (2026-06-02, `/goal` ULTRACODE multi-iteration session, +14 net, zero regressions; 5 triage-subagent waves + 4 deep-investigation agents + adversarial verification confirmed the bounded NONE/OUTPUT pool thoroughly-mined — ~80 NONE + 12 OUTPUT tests triaged, the rest architectural). **B98.r106** fn-param constraint-drill elaboration: when a fn-vs-fn assignability check fails on a param where BOTH source+target param types are bare `Type.TypeParam` with concrete OBJECT constraints that differ (`<T,U extends {a:T;b:number}>(z:U)` vs `{b:string}`), substitute each U by its constraint + drill into the differing property; new branch in getFunctionMismatchElaboration (parallel to the round-81 array branch), gated to both-TypeParam-Object-constraints + real drillable mismatch. Verified via HEAD failing-set diff: flips exactly `genericAssignmentCompatOfFunctionSignatures1`, ZERO regressions. **ATTEMPTED-AND-REVERTED:** `modulePreserve4` (JS-emit) — gating the `export {}`/import-equals-elision skip on verbatimModuleSyntax+Preserve regressed 4 `impliedNodeFormatEmit*__module_preserve` AND didn't flip its target (full .js baseline differs beyond elision; errors subtest separately architectural). Cleanly bisected (revert Transformer.kt, recovered exactly the 4). **B96-INDEXSIG (+2, the session capstone — landed in the documented-risky ~228-index-sig-fixture area, clean via gating + HEAD-diff):** new `checkLiteralValuesAgainstIndexSignatures` — a var-decl object/array-literal initializer assigned to a target with a string/number INDEX SIGNATURE now has each property/element VALUE checked against the index value-type → per-value TS2322 (primitive mismatch) / TS2741 (named source missing a required prop) + the previously-NEVER-EMITTED related TS6501 "The expected type comes from this index signature." The relation engine passed an index-sig-only target trivially (no named props), so nothing fired before. Gated to `isAssignable` (relation passed → no double-emit), index/source value not any/unknown, named members skipped, TS2322 primitive-only, TS2741 named-missing-prop-only. Flips `contextualTypeAny` + `contextualTypingOfArrayLiterals1` (ZERO regressions across the 228 fixtures, full HEAD-diff verified). The helper was 90%-written by a worktree subagent that hit a session limit before wiring/measuring; completed on main (call site + isAssignable gate). `objectLiteralIndexerErrors` ALSO flipped (+1 follow-up): extended the check to !isAssignable var-decls + the assignment path, both gated to index-sig-ONLY targets (empty members → no coarse-error double-emit). B96-INDEXSIG cluster now 3/3, all clean/zero-regression. **B94 (+2, deep-Blocker via isolated worktree subagent + independent HEAD-diff verify):** union-vs-object-literal deep disambiguation — `function foo(): Stuff | string { return {a(){...}, b: () => "hello", ...} }` now drills per-property against the single object-like union constituent (findBestUnionConstituent overlap-scoring), emitting 11 per-property TS2322 + related TS6502 (member signature) + TS1356 (async hint), suppressing the coarse union TS2322. Supporting infra (broadly useful): embedded Promise gains `[Symbol.toStringTag]` + `finally` gated to ES2018 (accurate es2015 Promise shape); `getMemberName` maps well-known-symbol computed names to canonical keys; `inferReturnTypeFromBody` handles `return [arr]`. Flips `errorOnUnionVsObjectShouldDeeplyDisambiguate{,2}` (ZERO regressions across the Promise-lib + index-sig surface, 10078-testcase HEAD-diff). **B96-NESTED (+1):** recursive nested per-property type check for an array-element object literal vs the element interface, drilling through a union member (`bar: Bar | Bar[]`, value `{prop:100}` → pick the non-Array constituent Bar) and nested object literals; leaf primitive mismatch → TS2322 at the property key. Flips `objectLiteralsAgainstUnionsOfArrays01`; extended with a nullish-value sub-case (`children: null` vs optional `IIntervalTreeNode[] | undefined` → TS2322 + TS6500) flipping `doNotWidenAtObjectLiteralPropertyAssignment`; extended again (unwrap paren/comma/assignment wrappers + var-decl wiring + viaUnion-conditional TS6500 + coarse-TS2322 suppression) flipping `slightlyIndirectedDeepObjectLiteralElaborations`. All zero-regression. The object-literal-elaboration vein yielded +8 this session (B94 +2, B96-INDEXSIG +3, B96-NESTED +3). CROSSED 9200. MILESTONE: crossed 9190. three fresh surgical flips (B98.r102–r104) found via parallel read-only triage subagents (6 wave-1 + 3 wave-2 + 2 wave-3) over thematic NONE-bucket slices, then adversarially verified by reading the exact code path + baseline before implementing (the round-95 lesson held: of ~70 triaged tests only 3 were genuine clean FLIP-NOW — triage agents over-claim, so the refute-pass is mandatory). All three batched into ONE full-suite verify, then split into per-substep commits via python hunk-splitting of the single Checker.kt diff. **B98.r102** TS2448+TS2538 — a self-referential computed binding-element key in a let/const pattern (`let {[a]: a} = ...`, `for (let {[a]: a} of ...)`): the computed key `[a]` references the binding `a` being declared in the same pattern (temporal dead zone) → TS2448 "used before its declaration" (+TS2728 "declared here" at the binding name) + TS2538 "Type any cannot be used as an index type" at the computed-key ident. New `checkSelfRefComputedBindingKeyList` wired into the B94 walker's VariableStatement/ForStatement/ForOf/ForIn initializer sites; FP-safe (only the exact `[x]: x` self-ref in a let/const pattern fires; `var` hoists, no TDZ → excluded). Flips `blockScopedBindingUsedBeforeDef`. **B98.r103** TS2345 — a void-IIFE argument vs a primitive parameter (`foo((()=>{})())` where `foo(s: string)`): a zero-arg IIFE of a parameterless, no-return, un-annotated arrow/fn-expr returns `void`, but `getReturnTypeOfCallExpression` yields anyType for an arrow/fn-expr callee. New helper `voidIifeArgType` detects the shape and overrides argType to voidType ONLY at the arg-check site in `checkArgumentsAgainstSignature` — NEVER in the global call-return path (which would cascade void into currentLocalTypes / var-init inference via the `isFromCall` rule). The standard simple-checkable TS2345 path then fires. FP-safe (zero-param, no-return, no-annotation block-body IIFE, empty arg list). Flips `voidArrayLit`. **B98.r104** TS2345 — index-signature-missing in the argument-check path (`Biz(new Foo())` where `Biz(map: { [s:string]: IHandler })` and Foo lacks a string index sig): the `argIsDistinctNamedClass` branch called `collectMissingProperties` (empty — the param has no named required props) and emitted nothing. Added an else branch mirroring the TS2322 site (Checker.kt:~56157): reset `lastMissingIndexSigKind`, run `checkTypeRelatedTo`, and if it failed solely on a missing index signature, emit TS2345 + "Index signature for type X is missing in type Y" chain. FP-safe (`propertiesRelatedTo` only sets `lastMissingIndexSigKind` for a nominal/function source vs a target index sig whose value type is not any/unknown). Flips `assignmentCompatInterfaceWithStringIndexSignature`. **B98.r105** TS5097/TS2846 — a relative import specifier carrying a TS-only file extension: `.ts`/`.tsx` needs `allowImportingTsExtensions` (or `rewriteRelativeImportExtensions`, which rewrites at emit) → else TS5097; a `.d.ts` imported as a VALUE (not `import type`) is TS2846, suggested impl path gets `.js` only for ES-module output (`module in ES_MODULE_KINDS`). New check in `checkUnresolvedModules` (emits at the quoted-specifier span, then `continue`s so TS2307/TS2834 don't double-fire) + two new CompilerOptions fields (`allowImportingTsExtensions`/`rewriteRelativeImportExtensions`) + parse arms. FP firewall: only relative `.ts`/`.tsx`/`.d.ts` specifiers; the `rewriteRelativeImportExtensions` gate covers the sole corpus FP (`declarationEmitNoInvalidCommentReuse2`). Flips `moduleResolutionNoTsCJS/ESM` (+2). **POOL STATE:** wave 1-3 triaged ~70 bounded NONE tests (2-line single-code TS2322/2345/2339/2353/2741/2403/2304/2352/2307 clusters + small multi-code clusters); the overwhelming majority are ARCHITECTURAL (mapped/conditional/indexed-access type eval, generic inference through object/array literals & callbacks, `.js`/checkJs expando/this/JSDoc, structural non-Identifier PropertyAccess receivers, flow-narrowing not consumed at arg-sites, lib-subsetting/DOM, decl-emit portability, cross-file scope) — confirming the bounded NONE clean pool is nearly mined out. NEXT (queued, ready-to-implement plan in hand): the `moduleResolutionNoTsCJS/ESM` pair (TS5097/TS2846, +2) needs two CompilerOptions fields (`allowImportingTsExtensions`/`rewriteRelativeImportExtensions`) + a relative-specifier-extension check in `checkUnresolvedModules` — FP firewall is the `rewriteRelativeImportExtensions` gate (sole corpus FP `declarationEmitNoInvalidCommentReuse2`)._ _Round 95 (2026-06-02, `/goal` ULTRACODE multi-iteration session, +17 net 9171 → 9188, zero regressions): seven fresh surgical flips found via parallel read-only triage Workflows + an adversarial-verification Workflow + direct code-path tracing. **B98.r101** TS2367 (+8, the session's biggest win) — a for-INITIALIZER const-literal compared (==/===/!=/!==) to a DIFFERENT literal: `for (const x = 0; …) { if (x == 1) … }` → "This comparison appears to be unintentional because the types '0' and '1' have no overlap." A `const x = 0` keeps the literal type `0` (always-false vs `1`); a `let`/`var`/annotated-const WIDENS, so the let/for-of/for-in siblings don't fire — and a loop-BODY block const captured by a closure is ALSO widened by TS (`while (…) { const x = 1; if (x == 2) }` does NOT error), so the new scope-aware walker `checkConstLiteralComparisons` tracks ONLY for-init const-literals (block VariableStatements just SHADOW). Handles labeled loops / nested loops / switch / try / fn+arrow param-shadow scopes. The BinaryExpression walk is ITERATIVE down the left spine (a naive recursion StackOverflowed `binderBinaryExpressionStress`'s deep `a+b+c+…` chain → aborted JS emit → caught as a 1-test JS-emit regression on the first full-suite run, fixed before commit). FP-safe (a const-literal vs a different literal is always TS2367; gated on ≥1 const-identifier operand + differing values). Flips `capturedLetConstInLoop5/5_ES6/6/6_ES6/7/7_ES6/8/8_ES6`. This was the largest bounded NONE cluster previously written off as flow-narrowing/Blocker-#1 — it was actually AST-level. **B98.r100** TS2339 — a missing member on a CAST receiver to a simple named interface: `(x as T).p` / `<T>x.p` where T is a base-less, signature-less, index-signature-less named Interface lacking `p` (a cast is an explicit assertion → a missing member is unambiguously TS2339). `checkMemberAccessMissing` otherwise bails on non-Identifier receivers (the broad structural-PropertyAccess-receiver gap, wave-1's flagged unblocker); this is the FP-safest decomposed slice — gated to a cast/assertion receiver (unwrapping ParenthesizedExpression) + a simple named `Type.Interface` (no base/call/construct sigs, no index sigs, so apparent-type/Object.prototype members can't be missed) + propName not a RUNTIME_PROPERTY. Flips `missingDomElement_UsingDomLib` (the test: `(({}) as any as HTMLMissingElement).textContent`). The remaining structural-receiver slices (PropertyAccess receivers like `o.name.d`) are riskier (implicit, not explicit) — deferred. **B98.r99** TS2339 — `this.<Function-only-prop>` on a base-less class instance: `this.name` (and `length`/`apply`/`bind`/`call`/`caller`/`arguments`/`prototype`) exists on a function/constructor VALUE but NOT on a class INSTANCE, so `this.name` in a base-less class not declaring it is genuinely TS2339. The blanket RUNTIME_PROPERTIES exemption in `checkMemberAccessMissing` (`if (displayTypeOverride == null) return`) suppressed it; now skip that exemption for the narrow shape (instance `this`-access — NOT a static method where `this` is the constructor and DOES have `.name` — + base-less class Type.Interface + a Function-only property name), falling through to the normal member-existence check. FP-safe (still only emits when the prop is genuinely absent; the `prop != null` guard returns silently otherwise). Found by direct tracing after the adversarial agent's `isTopLevel`/scope theories were wrong — the real blocker was line ~71886. Flips `detachedCommentAtStartOfFunctionBody1/2` (+2). **B98.r98** TS2814/TS2813 (+related TS6506) — a function-with-body merging a non-ambient class ACROSS merged `namespace M` blocks (`export function f(){}` in one block + `export class f{}` in another); new `checkCrossNamespaceFunctionClassMerge` reuses `checkCrossNamespaceOverloadAmbient`'s merged-block grouping, gated cross-block (≥2 distinct blocks, so it never double-emits with the same-block TS2813/2814 branch in `checkDuplicateDeclarations`) + both `export`ed + both non-ambient + function has a body. The MISSING bucket (we emit at the right position, just miss N lines) is more tractable than NONE; this was the only fresh MISSING win (the other 2 MISSING are decl-emit-portability + parser-misparse, architectural). Flips `duplicateIdentifiersAcrossContainerBoundaries`. Plus three bounded-NONE flips found via parallel read-only triage Workflows + an adversarial-verification Workflow (the adversarial pass REFUTED 7 of 8 over-claimed FLIP-NOW candidates from the triage agents — confirming round 94's read that the bounded NONE/OUTPUT tail is mostly RISKY/architectural; triage agents systematically over-claim, so a skeptical refute-pass before implementing is high-value). **B98.r95** TS2503 — a top-level `import X = <bare-Identifier>` in a `.d.ts` whose alias target resolves to nothing (`export import Component = CompletelyMissing;`); the general `checkUnresolvedNames` walker skips `.d.ts` entirely, so new narrow pre-pass `checkDtsImportEqualsAliasResolved` handles only the `.d.ts` + top-level + bare-Identifier moduleReference shape (QualifiedName/ExternalModuleReference skipped); FP firewall = consult own-file locals + cross-file merged `globals` + KNOWN_GLOBALS. Flips `reexportedMissingAlias`. **B98.r96** TS2403 — a top-level `var X = <primitive>` declared across MULTIPLE script `.ts` files with differing widened primitive types (script files share the global scope → same global, must have same type); new `checkSubsequentVarTypesAcrossScriptFiles` uses the FIRST-decl-wins model (later decls matching the established type stay clean) + TS6203 related-info to the first decl; distinct from the allowJs `.js`+`.ts` path (`checkSubsequentVarTypesInGlobals`); mixed `.js`+`.ts` names defer to it. FP firewall: SCRIPT files only (no top-level import/export), `var` only, no annotation, primitive initializer, ≥2 distinct files. Flips `duplicateVarsAcrossFileBoundaries`. **B98.r97** TS2304/TS2552 — a bare `global` reference under `declare global {}` (GH#42209); `declare global` is a global-scope AUGMENTATION, not a binding named "global", but the binder binds it as a ModuleDeclaration symbol "global" AND "global" was in KNOWN_GLOBALS/VALUE_ONLY_GLOBALS → resolved silently. THREE coordinated changes were ALL needed (the first cut — KNOWN_GLOBALS removal alone — was net-zero because the binder symbol + per-statement-list `collectDeclaredNames` re-added it): (1) drop "global" from KNOWN_GLOBALS + VALUE_ONLY_GLOBALS; (2) in `checkUnresolvedNames`, remove "global" from the file-root scope when every declaration of it is a `declare global {}` block; (3) in `collectDeclaredNames`, skip adding the `declare global` module name to child scopes. Flips `spellingSuggestionGlobal1/2/4` (+3). **DEFERRED/logged this session:** `unusedLocalsStartingWithUnderscore` (TS6133 — the adversarial agent's `isTopLevel`-gate theory was WRONG; the real TS rule is `isValidUnusedLocalDeclaration`: `_`-prefix exempts only imports/params/loop-vars, NOT namespaces or plain `let`; a narrow fix to report `_ns`/`let _` was net-zero — we emit NOTHING for the file, so the blocker is deeper, likely the `@noUnusedLocals:true` no-space directive parse or a module-file namespace-collection gap, NOT the underscore rule — needs a debug build). RISKY-attemptable but not-clean (verified via direct read, not just triage): `missingDomElement_UsingDomLib`/`thisBinding`/`knockout`/`spyComparisonChecking` (TS2339 needs the absent structural-PropertyAccess-receiver checking — `checkMemberAccessMissing` bails on non-Identifier receivers; the architectural TS2339 gap), `exportEqualsDefaultProperty` (TS2339 — needs default-import→`export =` alias-type resolution in resolveAlias, risky), `systemModule17` (OUTPUT — a multi-faceted System.register export-emission difference, NOT a simple var-reorder as triage claimed), parser-recovery OUTPUT (`staticsInAFunction`/`propertyWrappedInTry` — broad blast radius). **METHOD note:** triage Workflows over thematic slices are productive for DISCOVERY but their FLIP-NOW verdicts are unreliable (17/38 and 11/30 FLIP-NOW rates vs the real ~3); ALWAYS adversarially refute (read the exact code path + actual baseline) before spending a build cycle. Builds are the bottleneck (~2 min compile + test on the 4-core/2GB box) — batch independent well-understood fixes + ONE verify + `git apply --recount` per-substep split-commits (the spelling patch failed --recount on offset drift after prior commits; fallback: after committing the other substeps, the remaining working-tree delta IS the last substep → `git add` the file + commit)._ _Round 94 (2026-06-01, `/goal` multi-iteration session with ~17 parallel triage subagents, +15 net 9156 → 9171, zero regressions across 5 full-suite-verified batches): twelve bounded-NONE surgical flips (B98.r82–r93) found by dispatching ~16 read-only triage subagents over thematic slices of the bounded NONE tail (the "exhausted" framing was again a tooling artifact). Implemented serially in three batches, each batch one full-suite verify, then split into per-substep commits via `patch`-based hunk-splitting. **B98.r82** TS2699 (+TS2300 method form) for a `static prototype` class member (conflicts with `Function.prototype`); AST-only `checkStaticPrototypeMembers`. Flips `staticPrototypeProperty`. **B98.r83** TS2332+TS2683 for `this` in an enum-member initializer; new `checkThisInEnumMembers` mirrors the namespace-this walker, skips fn/class/arrow boundaries. Flips `this_inside-enum-should-not-be-allowed`. **B98.r84** TS2704/TS2542 for `delete` of a read-only property / read-only index signature; `checkDeleteReadonlyOperand` reuses `isReadonlyPropertyAccess`+`stringIndexInfo`, independent of strictNullChecks. Flips `deleteReadonly`. **B98.r85** TS2616/TS2597 + TS2497 for a named import from an `export = class/function` module under esModuleInterop+CJS; new branch disjoint from the isPlainValue/explicitly-false/ESM/TS2305 branches. Flips `importNonExportedMember5/9` (+2). **B98.r86** TS2463 for an optional binding-pattern parameter in an implementation signature; `checkOptionalBindingPatternParams` gated body!=null. Flips `declarationEmitDestructuringWithOptionalBindingParameters`. **B98.r87** TS2387/TS2388 for static/instance mixing within a class method overload GROUP (consecutive decls terminated by an impl — a body resets the group, so the C5 two-complete-sets case does NOT cross-flag); the "differs from immediately-preceding decl in the same group" rule. Flips `mixedStaticAndInstanceClassMembers` + `mixingStaticAndInstanceOverloads` (+2). **B98.r88** TS2411 for a canonical-numeric string-literal property name (`"1"`, `"-2.5"`, `"Infinity"`, `"NaN"`, round-trips `String(+name)===name`) vs a number index signature. Flips `propertiesAndIndexersForNumericNames`. **B98.r89** TS1432/TS1378 for top-level `for await`/`await` when the module supports TLA but target<ES2017; `checkTopLevelAwaitTargetGate` walks top-level only (not into fn/class). Flips `modulePreserveTopLevelAwait1(target=es2016)`. **B98.r90** TS2464 + TS2304 for a get/set accessor with a computed name in a type literal (`get [K in WAT]()`); added GetAccessor/SetAccessor arms to the TypeLiteral branch of `checkUnresolvedInTypeCore` + `emitTS2464IfBooleanComputedName` (operator-kind gate). Flips `noMappedGetSet`. **B98.r91** TS2690/TS2693 — a pure-type name (type alias / interface, no value side) used as a type-literal computed property name (`type T = { [K]: number }`): TS2690 with a "Did you mean '<TP> in K'?" hint for a single-member literal (mistaken mapped type), else TS2693. FP firewall: resolves to a symbol whose declarations are ALL TypeAlias/Interface (`const K1 = Symbol()` skipped). Flips `typeUsedAsTypeLiteralIndex`. **B98.r92** TS2339 for value access of a HIDDEN namespace member (`Foo2.Bar` where `Bar` in Foo2 is a non-exported nested namespace + an exported `interface Bar`); `shouldEmitTs2339ForHiddenNamespaceMember` gates: non-ambient enclosing ns + a hidden non-exported nested namespace + NO value-accessible export. Flips `moduleAndInterfaceWithSameName`. **B98.r93** TS2300 for a clodule (class+namespace merge) whose combined STATIC value space has a name declared with ≥2 DISTINCT merge-kinds (accessor/method/property from class-static; function/var from namespace-export) — same-kind groups (get/set pair, overloads) collapse to one kind so it's FP-safe; instance + type-only excluded. Flips `cloduleWithDuplicateMember1/2` (+2). **ARCHITECTURAL CLUSTERS confirmed by triage (logged, do NOT re-triage):** `@libReplacement`/lib-subsetting (libTypeScript* — TS2304 window); JSDoc `@typedef`/`@callback`/`@overload`/`function(...)` type parsing (checkJsdocTypeTagOnExportAssignment*, jsdocCallbackAndType, jsFileMethodOverloads3, jsdocIllegalTags, jsdocParameterParsingInfiniteLoop, uniqueSymbolJs — no JSDoc-internal type parser); declaration-emit inferred-type cross-module portability (TS2883/4023/2527/5088 declarationEmit*); regex sub-parser (regularExpression* TS1499/1503/1532); iterator-protocol (TS2488/2504 iterator/asyncIteratorExtraParameters); cross-file scope unification Blocker #3 (export as namespace TS2451/2303 family, exportSpecifierReferencingOuterDeclaration3/4, mergeSymbol*); enum string-type inference (TS18033/18055 computedEnumMember*); utility-type/mapped-type materialization (Omit/Readonly TS2540, TS2536/2559/2412); generic-signature variance (genericImplements TS2416). **POOL STATE post-r93:** the bounded NONE 2–3-line tail is now FULLY triaged → all remaining are RISKY or ARCHITECTURAL (no clean FLIP-NOW left from NONE). EXTRA/MISSING/SWAP dry (2 MISSING both known-architectural: declarationEmitExpandoPropertyPrivateName TS4032 decl-emit-portability, newExpressionWithCast parser-misparse). **NEXT-SESSION OPTIONS, in priority order:** (1) the **OUTPUT bucket** (47 fresh JS/decl-emit diffs, `--output --fresh`) is UNEXPLORED this session — but it's mixed: some are parser error-recovery mismatches (e.g. `propertyWrappedInTry` = `try{}` in a class body, TS1068 recovery) with broad blast radius, others are genuine transformer/emitter feature gaps; needs per-test diff inspection + careful regression-watch (emit changes touch many tests). (2) RISKY-attemptable checker gates (each needs revert-budget): `mergedClassNamespaceRecordCast` (TS2352 class→Record overlap, multi-piece), `thisInFunctionCallJs` (TS2683 checkJs-gate change — broad @checkJs blast radius), `controlFlowDestructuringVariablesInTryCatch` (TS2454 try-block flow — flagged dangerous, 17.1c -7 history), `importTypeWithUnparenthesizedGenericFunctionParsed` (TS2591 import-type node-builtin specifier — needs ImportType-resolution wiring). (3) a Blocker substep (flow-narrowing #1 has the most bounded NONE candidates: TS18047/18048/18049/2638/2367). **METHOD (high-yield, proven this round):** dispatch ~8 read-only general-purpose triage subagents over thematic slices of the bounded pool, each returning FLIP-NOW/ARCHITECTURAL/RISKY + an implementation sketch; implement the FLIP-NOW set serially (machine is 4-core/2GB — parallel gradle builds thrash, so batch 2–4 fixes + ONE full-suite verify + `patch`-based per-substep split-commits). HAZARD: use `git add <file>` NOT `git add -A` (the latter committed a Checker.kt.orig patch backup — gitignored + removed in 6084bf0)._ _Round 93 (2026-06-01, `/goal` ULTRACODE multi-iteration session, +5 net 9151 → 9156, zero regressions): five more bounded-NONE surgical flips (B98.r77–r81), each its own commit + full-suite verify — the bounded pool round 92 again declared "exhausted" still had AST-level wins. **B98.r77** TS2873 — always-falsy operand of logical-not (`!void 0`, `!null`); the always-truthy walker handled `!`-operands fed through `checkAlwaysTruthyInExpr` (expr-statements, for-conditions) but NEVER recursed if/while/do CONDITIONS — added FP-safe `scanForNotFalsy` (iterative binary spine, stops at fn/class boundaries) wired into those condition positions. Flips `voidAsOperator`. **B98.r78** TS2700 — object-rest from a non-object source (`var {...r} = nullOrUndef`); new `checkObjectRestSpreadTypes` resolves the var-decl initializer type, emits at the rest binding name. Two-gate FP-firewall: invalid spread source AND involves null/undefined. Union vs intersection differ to match TS (a UNION filters nullish first so `{n}|undefined` is valid — only all-nullish like `null|undefined` is invalid; an INTERSECTION needs every member valid so `({n}&{s})&undefined` is invalid). The `restUnion` `{n}|undefined` regression on the first cut drove the union(`any`)/intersection(`all`) split. Flips `restUnion3`. **B98.r79** TS2356 — `++`/`--` on an unconstrained-type-param operand (`this.a++` where `a:T`, `i++` where `i:T`); new AST-level `checkIncDecTypeParamOperands` threads in-scope UNCONSTRAINED type-param names through class/function bodies, collects `this.X` props + locals annotated as a bare TypeReference to one, flags such operands. constraint==null only → a numeric-constrained `T extends number` is a deliberate FN, never FP. Flips `incrementOnTypeParameter`. **B98.r80** TS2862 — index-WRITE on a generic type-param receiver (`target[p]=v` where `target: T extends Record<…>`); new `checkGenericIndexWrite` maps params/locals/this-props annotated as a bare ref to a CONSTRAINED type-param → its NAME, flags an assignment whose LHS is `ref[key]`/`this.prop[key]` with a non-numeric-literal key (numeric-LITERAL index `target[1]=v` is the allowed array-element write). Only 3 corpus baselines use TS2862 and TS emits it for exactly this shape → can only help/neutral. Flips `cannotIndexGenericWritingError`. **B98.r81** TS2395 — export-consistency in .d.ts string-named ambient modules (`declare module 'x' { export function f(); namespace f {…} }`); the general `checkDuplicateIdentifiers`/`checkDuplicateDeclarations` path skips .d.ts entirely, so new narrow `checkDtsAmbientModuleExportConsistency` runs ONLY for .d.ts string-named `declare module` blocks, grouping value-space decls (function/var/class/enum/instantiated-namespace) by name + emitting TS2395 on mixed exported/local merges (pure-function-overload groups skipped → those are TS2383; non-.d.ts already covered → no double-emit). Flips `namespaceNotMergedWithFunctionDefaultExport`. **POOL STATE post-r81:** the remaining bounded NONE single/double-test codes are now genuinely architectural — flow-narrowing (TS2367 `capturedLetConstInLoop5-8`, TS2638 `inKeywordAndUnknown`, TS2565, TS18047-49 — Blocker #1, the largest bounded cluster), type-engine (TS2348 `genericConstructorFunction1` index-access-construct-sig, TS2820 `errorsForCallAndAssignmentAreSimilar` literal-union spelling, TS2418, TS2559, TS2563-CFA-size), decl-emit (TS2883/4023/4025/4109/2527/2464/5088/7056), cross-file (TS2451/2503/2305/2649/2209/2303 — Blocker #3), tslib (TS2354), JSON-grammar (TS1327 `requireOfJsonFileWithErrors` — needs a JSON validator; .json files are string-processed, not AST-checked), JSDoc (TS8021/2591/`uniqueSymbolJs2`). NEXT SESSION: commit to a Blocker substep — control-flow narrowing (Blocker #1) has the most bounded NONE candidates and existing Flow.kt infra._ _Round 92 (2026-06-01, `/goal` ULTRACODE multi-iteration session, +8 net 9143 → 9151, zero regressions): re-triaged the bounded NONE tail round 90/91 declared "exhausted" and found eight clean syntactic/checker flips (B98.r70–r76), each its own commit + full-suite verify. **B98.r70** TS2880 — deprecated import assertions (`import x from "y" assert {…}`); new `checkImportAssertionsDeprecated` gated on module target supporting import attributes (esnext/nodenext/node18/node20/preserve) + `assert` keyword + not `ignoreDeprecations`; parser now records the clause-keyword position on a defaulted `assertClausePos` AST field. Flips `importAssertionsDeprecated`. **B98.r71** TS2845 — comparing with the global `NaN` via `==`/`!=`/`===`/`!==` ('false' for ==/===, 'true' for !=/!==) + related TS1369 "Did you mean 'Number.isNaN(x)'?"; hooked into the scope-aware TS2304 expression walker (`checkUnresolvedInExprCore` BinaryExpression branch). New `NameScope.hasLocalShadow` distinguishes a local `NaN` binding (param) from the global (the root scope seeds KNOWN_GLOBALS incl. NaN into `names`, so plain `has` can't). Flips `nanEquality`. **B98.r72** TS2358 — primitive `instanceof` LHS (`x: number|string`); `checkInstanceofLhsType` + `isPrimitiveInstanceofLhs` (union → all-constituents-primitive) in `checkBinaryOperatorTypes`' new InstanceOfKeyword branch (host already resolves operand types with param types populated). Flips `instanceofWithPrimitiveUnion`. **B98.r73** TS2801 — truthiness test of a non-nullable `Promise<T>` (`if (p)`, `p ? a : b`) + related TS2773 "Did you forget to use 'await'?"; new `emitPromiseConditionHelper` wired into the TS2774 truthiness-walker leaf, reusing its scope/typed-locals/body-reference suppression (a reference operand whose path the then-body or `&&` sibling uses is suppressed; a call `pf()` has no path → never suppressed; `!!p` is a PrefixUnary leaf → skipped; `null|Promise` is a Type.Union not a bare Promise ref → skipped). Flips `truthinessPromiseCoercion`. **B98.r74** TS2372 — a simple-Identifier parameter's default referencing itself (`f(x=x)`, `(b=b.toString())`); `findParamSelfRef` collector in `walkParamForbiddenParameters` recurses value-transparent wrappers (base-of-property-access only, BinaryExpression iterative) and STOPS at nested function/class boundaries → FP-safe. Flips `selfReferencesInFunctionParameters`. **B98.r75** TS2687 — a member shared across merged interface/class declarations with a differing (visibility|readonly|optional) signature; added to the TS2428 merge-detection block, INSTANCE Property/Method members only (static excluded — no interface partner). Flips `duplicateIdentifierDifferentModifiers`. **B98.r76** TS1385/1386/1387/1388 — an unparenthesized FunctionType/ConstructorType used as a union/intersection member (`string | () => void`, `string & new () => void`); parser-side `reportUnparenthesizedFnTypeInUnionOrIntersection` in parseType's union loop + leading-`|` first member and parseIntersectionOrHigherType's intersection loop + leading-`&` first member. A parenthesized `(() => void)` is a ParenthesizedType (not a bare fn/ctor type) so OK cases don't trip it; member span = right-after-`|`/`&` (getPrevTokenEnd) to member end; mixed M-cases handled by recursion. Flips `unparenthesizedFunctionTypeInUnionOrIntersection` + `unparenthesizedConstructorTypeInUnionOrIntersection` (+2). **DEFERRED/logged:** `instanceofOnInstantiationExpression` (TS2848 — RHS instantiation `Box<number>`; the bare case works but `(Box<number>)` drops the instantiation tag in the parser when type-args are followed by `)`, and preserving it via a synthetic paren has JS-emit fallout — the transformer doesn't uniformly drop the synthetic paren when nested in a real paren). Method: solo work (subagents/Workflow were session-limited last round); each candidate read source+baseline, implemented inline, single-test verified, then full-suite verified before commit._ _Round 91 (2026-06-01, `/goal` ULTRACODE session): **B98.r69 — +1 (9142 → 9143), flips `expressionsForbiddenInParameterInitializers` (errors-baseline subtest; its JS-emit subtest stays failing — pre-existing async/generator destructuring-param __awaiter rewrite gap), zero regressions.** New per-file walker `checkParamInitializerForbidden` emits TS2523 (yield) / TS2524 (await) for a yield/await expression in a function parameter initializer (incl. destructuring binding-pattern element defaults `function f({x = await g()})` / `function* f({x = yield "a"})`). The collector recurses ONLY through transparent expression wrappers (paren/unary/binary[iterative right-spine]/conditional/call+args/new+args/array/member/spread/as/non-null/type-assertion/object-literal/template) and STOPS at any function boundary (nested arrow/function-expr = own async/generator context) → zero-FP by construction; verified zero TS2523/2524 EXTRA across the corpus. Two load-bearing details: dedup set is a LOCAL HashSet (instance field would NPE in the init-time pipeline — Kotlin init-order trap) keyed `(code | trimmed-line-text)` (collapses self-import content duplication); and a `yield`-as-bare-Identifier parser-recovery branch (generator param list mis-parse) gated on a following operand-start char. **TOOLING (read before next session):** this session lost ~half its budget to two compounding hazards — (a) subagents + Workflow were HARD-BLOCKED by 'You've hit your session limit · resets 9pm UTC' for most of the session (one subagent did land before the block and implemented r69); (b) firing MANY tool calls in ONE message corrupts/jumbles the bash output channel — it delivered results out-of-order, fabricated '(truncated)' markers, and even surfaced phantom file content from a NON-existent path. **The real Checker.kt path is flat: `src/commonMain/kotlin/Checker.kt` (~85k lines), NOT `src/commonMain/kotlin/com/xemantic/...`.** Mitigations that worked: strictly SEQUENTIAL single tool calls (one Bash/Read per message), absolute paths, write-to-/tmp-then-Read, and `cd /abs/path` as the FIRST statement (a compound `cd X; cmd` sometimes ran from a stale cwd). _Round 90 (2026-05-31, `/goal` ULTRACODE multi-iteration session, +6 net (B98.r63–r68), zero regressions; bounded SURGICAL pool now exhausted — remaining bounded NONE/OUTPUT candidates need real flow-narrowing / type-engine / module-resolution / JSDoc / decl-emit infra). **COUNT RECONCILIATION:** round 89's claimed headline `9139` was a 3-test OVER-count — two clean full-suite runs at round-89 HEAD (8812903, cache cleared) both measured 939 failed = **9136** passing. So the true round-89 baseline was 9136, and round 90's +4 brings the true count to **9140** (which the headline now reflects). Flips, all found via parallel read-only triage subagents over the bounded NONE + OUTPUT + class buckets: **B98.r63** TS6133 — unused computed-name private member `private [x]: T` (write-only `this[x]=0` excluded; computed-name collection + read/write matching gated on `unusedComputedKeys` non-empty → zero change for classes without computed members). Flips `noUnusedLocals_writeOnlyProperty_dynamicNames`. **B98.r64** TS2349 — `(new X())()` calling a class instance (signatures-empty branch only handled Intrinsic/Identifier/Union callees; added a NewExpression-core branch — a `new`-result instance is always non-callable, FP-safe vs the empty-Type.Object trap). Flips `callOnInstance`. **B98.r65** TS2665 — `declare module "X"` augmenting an untyped node_modules JS module (`/node_modules/X/index.js` in inputs, no typed resolution); checkAmbientModuleAugmentations now runs the TS2665 check for .d.ts module files too, TS2664 stays non-.d.ts-only. Flips `untypedModuleImport_withAugmentation2`. **B98.r66** TS2415 — `class D extends C` where both declare `private [x]` (computed symbol); computed-name members aren't in the member table (getMemberName→null), so a dedicated pass compares derived+base private computed bracket-idents via baseType.symbol AST (nominal → "separate declarations" regardless of type). Flips `declarationEmitPrivateSymbolCausesVarDeclarationEmit2`. **B98.r67** TS7022 — self-referential `export default { … X … }` where `import X from './self'` is a self-import (specifier resolves to the same file via the .js-aware `resolveRelativeIncludingIndex`); the default-export literal references the file's own default → circular inferred type. Extended `checkRecursiveLiteralVariables` with an ExportAssignment branch + per-file self-default-import set. Flips `selfReferentialDefaultNoStackOverflow`. **B98.r68** TS2532 — in an ES module, top-level `this` is `undefined`, so `this.X` reached through only this-transparent constructs/arrow bodies (never crossing a function/method/class boundary) accesses a property of `undefined`: `export var x = () => this.window` → "Object is possibly 'undefined'." New `checkModuleTopLevelThis` walker, FP-safe by construction (never recurses into a this-rebinding boundary), gated `isModuleFile` + `isESModuleFormat` (CommonJS top-level `this` = module.exports excluded). Fires regardless of strictNullChecks. Flips `topLevelLambda4`. **POOL STATE post-r68:** EXTRA/MISSING/SWAP dry (2 MISSING pre-deferred). Remaining bounded NONE single/double-test codes ALL architectural — flow-narrowing (TS18047/18048/18049/2488/2532/2638/2565 — Blocker #1, largest cluster), type-engine (TS2589/2590/2559/2563), decl-emit (TS4025/2527/2464/4109/7056), module/cross-file-scope (TS2305/2649/2694/2503/2209/2554 — Blocker #3), JSDoc (TS8021/2591), unique-symbol (TS2418), enum-literal-value (TS1355/18055), generator-implicit-any (TS7057/7055/7025). **NEXT SESSION: commit to a Blocker substep — control-flow narrowing (Blocker #1) has the most bounded NONE candidates.**_


---

Older round notes (Round ≤80b and the full B-series history) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — when it grows past that, move the oldest into `STATUS-HISTORY.md` (do NOT let it balloon; it ballooned to 351 KB / unreadable-in-one-Read before the 2026-05-30 trim).
