# Status

**Phase 4 — Checker buildout: COMPLETE. Phase 17 — self-compile the TypeScript compiler itself (re-scoped 2026-07-03; "any TypeScript project" is post-v1) — is the LIVE mission (see PLAN-PHASE-5.md; v1 exit = all 8 tsc-source profiles at zero FPs — **the FP leg was MET at round 481 (2026-07-12): all eight profiles at zero real FPs, only env-legit offline artifacts remain**; emit/crash legs verified same round: all 8 profiles emit every program file, exit 0, no crashes — the OFFLINE-VERIFIABLE v1 DEFINITION OF DONE IS MET; next: M5 performance; dashboard: bench/*.tsv, self-compile diagnostics 13,245 → 936 (measured at round 430 — rounds 395–412 burned bounded non-M3 histogram-tail buckets + M3.4 flow-narrowing slices the round-394 "pool picked over" triage missed, all surfaced by bucketing the full `--listAll` output by normalized message shape: TS2499 multi-base-generic misparse, TS2440 type-only-barrel-merge, TS2344 constraint-chain + cross-file `typeParamInternCache` pos-collision, TS2693/TS2709 clodule + destructuring-shadow, TS2314 enum-member-vs-generic, TS2588 nested-shadow, TS2551 Object.setPrototypeOf, SetIterator/MapIterator lib gap, TS2774 local-shadows-function, TS1100 interface-property-name, TS7023 callback-arg self-ref, TS2365 arithmetic local-shadows-function, TS2362 branded-number, TS7053 enum-reverse-mapping, round 408's TS2349 callee flow-narrowing family, and round 409's `export *`-barrel / ESM-`.js` imported-guard flow narrowing (M3.4, TS2339 838 → 672), and round 410's TS2862 constraint-index-sig gate / assign-RHS type-guard narrowing / void-overload-return (2,443 → 2,433), and round 411's M3.4 enum-member discriminant narrowing (barrel-import-resolved, three families UpToDateStatus/TypeMapper/PrivateIdentifierInfo) + type-guard-narrows-a-member-DOWN (2,433 → 2,374, TS2339 672 → 614) + round 412's M3.4 single-type type-guard narrow-DOWN + TS18048 receiver-narrowing (2,374 → 2,373), and round 413's `export *` LEAF-EXPORT gate (barrel-imported `Debug.assert` & every barrel guard now resolves — the TRUE builder.ts blocker, NOT the round-412 depth red herring) + tsc-faithful linear flow-walk iteration + return-path narrowing (2,373 → 1,965, TS2339 614 → 237)), then rounds 414-417 burned more bounded/M3.4 buckets (TS2366 CFA fall-through, NonNull/&&-narrowing arithmetic, closure-loop narrowing, namespace-local `extends` bases: 1,965 → 1,902), and round 418 resolved NESTED type-guard functions (M1.12, 1,902 → 1,854, TS2339 237 → 189: tsc guards `isTupleType`/... are nested in `createTypeChecker` so the binder skips them and `resolveFlowCalleeDecl` missed them - the guard never narrowed, the single biggest TS2339 sub-family), and round 419 resolved properties on INTERSECTION union-members (M1.12, 1,854 → 1,808, TS2339 189 → 143: `getPropertyOfType`/`typeHasOwnProperty` bail on a `Type.Intersection` member so `PropertyAccessExpression | (ElementAccessExpression & …)` FP'd TS2339 on `.parent`; fold the constituents in property resolution + discriminant-narrowing annotation; 0 new FPs, self-compile time −17%), and round 420 resolved TYPE-ALIAS enum-member discriminants in narrowing (M1.12, 1,808 → 1,799, TS2339 143 → 134: a `.kind: <alias>` discriminant member survived a `switch (x.kind) { case }` because `enumMemberKeysOfTypeNode` handled only a direct `Enum.Member`, not a bare-Identifier alias; 0 new FPs), and round 422 burned FIVE bounded M3.4/overload families (1,799 → 1,756: TS2769 60 → 45 — overload arg-check helpers now flow-narrow reference args (mirroring B469) + a synthetic true|false union for guard-narrowed boolean args vs literal overload params; TS2339 134 → 117 — mixed enum + string-literal discriminant unions narrow on disjoint lit:s: keys (PrivateIdentifierInfo); TS18048 10 → 7 — an optional-chain discriminant access x?.kind === <non-nullish> proves the receiver non-nullish; TS2366 12 → 4 — union-.kind exhaustive switches terminate via requiredUnionDiscriminantKeys), and round 423 (a) drove TS2366 to ZERO (1,756 → 1,752: the switch RECEIVER is guard-narrowed + typed before the exhaustive-switch union walk — flow graph via the pass-dedicated implicitReturnFlowGraph, local-const-from-call typing, optional-discriminant @undefined keys, LiteralToken["kind"] indexed-access) and (b) fixed union-TARGET type guards end-to-end (1,752 → 1,708: `x is A | B` PARSED truncated so isCallOrNewExpression/isPropertyNameLiteral/isOptionalChain never narrowed; + aliased conditions `const isFrag = guard(x); if (!isFrag)` via a memoized flow back-walk; + kind-key disjointness beating the lenient relation; TS2339 117 → 104, TS2322 784 → 756) and (c) narrowed truthy optional-chain CALL conditions (1,708 → 1,707: `if (x.y?.size())` proves `x.y` non-nullish), and round 424 burned SEVEN more M3.4 flow-narrowing slices (1,707 → 1,662, TS2339 104 → 68, TS18048 5 → 1, TS2322 756 → 751: loop-entry union suppression with a STRUCTURAL wash gate, call-RHS return-annotation narrowing, closure/join/call-crossing aliased conditions, prefix-path receiver guards, asserts-with-inferred-TP via test-arg predicate inference, assignment-overwrite reset to the declaration/call-RHS resolved type, and the DebugTypeMapper this-narrowing slice (explicit-type-arg asserts re-type `this`; an overload-cluster name collision resolves to the unique TypePredicate-bearing decl) — every step by-site strictly removals), and round 425 found the TS2339 never-cluster ROOT CAUSES (1,662 → 1,600, TS2339 68 → 6: the round-411 enum-discriminant key space SPLIT across resolution paths — canonicalEnumSymbol at all four key-builder sites — plus per-candidate narrow-DOWN distribution in union-target guards/instanceof, aliased ===/switch discriminants, switch-default negative narrowing, typeof-object filtering, boolean-literal discriminant truthiness, destructuring-read narrowing, the loop-entry retry for the single-type suppression, and the positive-empty intersection fallback whose round-423 dead-end verdict was an ARTIFACT of the key split) — driving to 0, and round 426 replaced the B399 TS2563 per-file proxy with the faithful flow-walk depth trip + the asserts-callee call gate (1,600 → 1,593: TS2563 27 → 0; TS2454×20 pre-existing definite-assignment walker FPs unmasked), and round 427 burned that bucket (1,593 → 1,577: TS2454 20 → 4 via tsc's definiteness rules — logical assigns ??=/||=/&&= are DEFINITE so captured reads assume initialized, a !-asserted read assumes initialized, and the anywhere-scan's left-spine walk now applies the target rule per spine node), and round 428 took the first real M3.1 slice of the TS2322/TS2345 cores (1,577 → 1,186, −25%: nullable-union generic param inference + overloaded generic callees for tsc's `x = append(x, item)` idiom (TS2322 751 → 559), the TS2345 histogram top — explicit this-param binding, guarded optional-member args, numeric-enum→number (394 → 280) — the array-literal-vs-union-member string-layer rule (TS2322 → 501), and the body-local-shadows-function anyType registration (TS2345 → 261, TS2769 → 36)), and round 429 drove the TS2345 core 261 → 86 (1,186 → 1,000, −15.7%: call-types lexical shadowing — body-locals vs enclosing params, DESTRUCTURED params via the currentParamBindingNames side set in getTypeOfIdentifier (the round-428 "mini-repro does not reproduce" residue), arrow own-params; embedded String replace/replaceAll/search/split accept RegExp; optional-param union args + NonNull-asserted args; guard-narrowed interface/unknown Identifier args — the ~110-site dominant mechanism, never-param excluded for assertType<never> exhaustive-switch defaults; string-enum→string in isSimpleTypeRelatedTo resolving the round-410 Extension[][] deferral; rest-arg flow narrowing), and round 430 unlocked the `append`/`addRange` inference (936: the `T extends {}` constraint failed `string → {}` and killed every mapper — empty-object-target relation rule with TP-source excluded per genericPrototypeProperty3's pin; readonly-array anchors; TP-from-PREDICATE binding for the getFirstJSDocTag family); self-compile time ~110–150 s single-run band), and round 431 took the FIRST M3.2 slice — the TS7006 core 301 → 11 via contextual typing (nested-fn/lexical callee resolvability, assignment-RHS context from the LHS declared type under the B476 single-applicable-sig rule, ||/??-both/&&-comma-right propagation, intersection/Reference/extends receiver members) — plus engine return-checking in switch/try bodies behind the foreign-TP source gate (un-inferred generic call results bail instead of FP'ing; ~130 T[]/U|undefined/SearchResult<T> return sites cleared) and the TS2367 anchored-disjointness gate (936 → 551, −41%; TS2322 435 → 345, TS7019 4 → 0; by-code strictly shrinking at every landed step) — then extended the foreign-TP gate to the var-decl/assignment/property-write/conditional-return paths (551 → 482, −69 strictly removals; TS2322 → 276; a generic FUNCTION-VALUE source's sig-own TPs stay checkable per 5 corpus pins the suite gate caught), and round 435 took SEVEN more M3.1/M3.2 slices at post-perf-arc iteration speed (482 → 373, −22.6%: generator TReturn returns, fresh-objlit literal props via the freshObjLitRange relation retry, TP-literal-constraint args, the union-decomposition-transparent relation re-entry gate resolving the NodeArray-covariance family ×23, bare-`new` contextual instantiation, the foreign-TP gate on assignment TARGETS (visitor family ×15+), nullish alias-union returns; TS2322 276 → 184, TS7006 11 → 1, TS2345 86 → 79; full-dashboard baseline: compiler 482→373 / tsc-cli 484 / jsTyping 480 / deprecatedCompat 481 / typingsInstallerCore 489 / services 1,603 / server 1,894 / harness 2,193 at 29–41 s wall each), and round 436 took SEVEN more M3.1/M3.2/M3.4 slices (373 → 294, −21.2%: TP-carrying callback-return param skip (forEachEntry family ×14), destructured-LOCAL shadowing (semver/checker + 12 transformer sites), literal-return syntactic union membership (the string-fallback re-widening trap — `return false` vs `Tag | false` ×7, incl. the completely UNPINNED `return "a"` vs `"a" | "b"` shape), constraint-filtered explicit-type-arg overload selection ('true' vs 'false' ×6), overload-helper optional-param union-arg + foreign-TP rules (union-receiver TS2769 ×13), bare-string switch-case narrowing (semver ×3), guard-gated ternary return arms with the tri-state all-leaves-verified early return (×19); TS2322 184 → 158, TS2345 79 → 47, TS2769 30 → 9; round-436 dashboard baseline: compiler 373 / tsc-cli 375 / jsTyping 371 / deprecatedCompat 372 / typingsInstallerCore 380 / services 1,476 / server 1,769 / harness 2,062 at 27–41 s wall), then round 438 (2026-07-07) took FIVE more M3.1/M3.4 narrowing/relation slices (294 → 244, −17%: an empty-object `{}` union-member return-path early return; UNION-target extension of the assignment-RHS AND return-path type-guard narrowing gates (`currentSourceFile = node` / `return node` vs `SourceFile | undefined`); PROPERTY-ACCESS args in the call-arg guard-narrow-DOWN branch (`getExports(node.left)`); and nullish-strip-gated narrowing of object-literal property VALUES (`{ moduleSpecifiers: specs }` where `specs = append(specs, x)`); TS2322 158 → 116, TS2345 47 → 39), then round 439 (2026-07-07) took THREE predicate-overload / arg-narrow-DOWN slices (244 → 228, −16 strictly removals: findAncestor-style type-guard-callback overload return inference + a companion NonNull concrete-union strip, the overload arg-check non-union narrow-DOWN, and its `unknown`-via-typeof extension; TS2769 9 → 1).** Corpus suite green as a permanent regression gate: 13,855 tests / 0 failing / 3 skipped (generated tsgo-relevant corpus + local corner-case tests; count as of round 835 = 13,847 plus 8 new `NonPrimitiveApparentTypeTest` pins for (M3.0/NONPRIM.2)'s apparent-type-of-`object` missing-property elaboration; was 13,847 at round 834 = 13,833 plus 14 `NonPrimitiveObjectTypeTest` pins; was 13,824 at round 832 = 13,816 plus 8 new `AotCacheGuardTest` pins for (JIT.2b)'s fail-safe AOT-cache contract; was 13,739 at round 817 = 13,725 plus 14 pins for (JIT.1)(f)'s census RATCHET and (e)'s split of `Transformer.transform` — 9 `TransformSplitTest`, 3 `HugeMethodLimitTest` size pins and 2 whole-program census pins; round 816 = 13,706 plus 19; round 814 = 13,671 plus 16 new pins for (JIT.1)(d)'s split of the `Checker` CONSTRUCTOR — 13 `CtorSplitTest` (one arm per helper, the ORDER seam with its own positive control, and the `declarationOnly` guard seam) and 3 `HugeMethodLimitTest`; round 813 = 13,651 plus 20 new pins for (JIT.1)(d)'s split of `checkIndexSigInStatement` — 17 `CisSplitTest` (one arm per helper, three discriminating seam pins and one renamed arm pin the ablation showed to be blind, plus the entry's own dispatch branches and a negative control) and 3 `HugeMethodLimitTest`; round 812 = 13,633 plus 18 new pins for (JIT.1)(d)'s split of `checkDuplicateDeclarations` — 15 `CddSplitTest` and 3 `HugeMethodLimitTest`; round 811 = 13,612 plus 21 new pins for (JIT.1)(c)'s split of `checkSingleCallExpressionTypesCore` — 18 `CcetSplitTest` (one arm per helper plus the two seams that discriminate, four single-emission pins written as seams that measurably do not, an ordering pin, a nested-call recursion pin and a negative control) and 3 `HugeMethodLimitTest`; round 810 = 13,596 plus 16 new pins for (JIT.1)(h)'s split of `checkReturnAssignabilityCore` — 13 `CraSplitTest` (one arm per distinctive behaviour of each helper, the single-emission guard, the nested-literal recursion pin and an ordering pin) and 3 `HugeMethodLimitTest`; round 809 = 13,579 plus 17 new pins for (JIT.1)(g)'s split of `checkAssignmentExpressionCore` — 14 `CaeSplitTest` (one arm per `cae*` helper, two seam pins of which two discriminate, an ordering pin, a chained-assignment recursion pin and a negative control) and 3 `HugeMethodLimitTest`; round 808 = 13,562 plus 17 new pins for (JIT.1)(c)'s split of `checkVarDeclAssignabilityCore` — 14 `CvdaSplitTest` (one arm per `cvda*` helper, five signal pins — two of them discriminating seams — and an ordering pin) and 3 `HugeMethodLimitTest`; round 807 = 13,539 plus 23 new pins for (JIT.1)(f)'s split of `checkArgumentsAgainstSignatureCore` — 20 `CaasSplitTest` (one arm per helper plus the CAAS signal seams) and 3 `HugeMethodLimitTest`; round 806 = 13,526 plus 13 pins for (JIT.1)(d)'s split of `ccetSpineEnter`; round 805 = 13,514 plus 12 new pins for (JIT.1)(c)'s split of `checkPropertyAccessInExpr`; round 804 = 13,493 plus 21 new pins for (JIT.1)(b)'s split of `checkMemberAccessMissingCore` — 18 `CmamSplitTest` (one display per level-R section, plus the two seams a size check cannot see) and 3 `HugeMethodLimitTest`; round 803 = 13,481 plus 12 new pins for (JIT.1)(a)'s split of `forEachChild` — 3 `HugeMethodLimitTest` (the compiled `Code` attribute length, the only instrument here that can see HotSpot's 8,000-byte cliff) and 9 `ForEachChildSplitTest` (enumeration ORDER and both split seams, which the reflection oracle's child-SET comparison does not pin); round 802 = 13,476 plus 5 new `SetupPhasePartitionTest` pins for (SETUP.1)'s exhaustive-by-construction partition of `outside-pass`; round 801 = 13,461 plus 15 new `FlowScanEquivalenceTest` pins for (FRONT.2)'s bind partition — the first pins on B464's reassignment-operator semantics; round 800 = 13,454 plus 7 `IanyArmGateTest`; round 799 = 13,449 plus 5 new `IanyArmGateTest` pins for (IANY.1)'s parent-edge arm pre-gate; round 798 = 13,441 plus 8 new `IanyGateTest` pins for (IANY.1)'s unobservable-context gate; round 797 = 13,435 plus 6 new `ArgKindCensusTest` pins for (CALL.6)'s level-S sub-partition; round 796 = 13,427 plus 8 new `ArgNarrowGateTest` pins for (CALL.5)(b)'s already-relates argument-narrowing gate; round 795 = 13,419 plus 8 new `ImplRelatedDeferralTest` pins for (ENGINE.2h)'s deferred TS2793 `implRelated` probe; round 794 = 13,412 plus 7 new `UnionRetrySubstitutionTest` pins for (ENGINE.2f)'s union loop-entry-retry substitution; round 793 = 13,405 plus 7 new `CcetPrologueGateTest` pins for (ENGINE.2g)'s call-expression prologue pre-gate; round 792 = 13,399 plus 6 new `PreGateGuardTest` pins for (ENGINE.2e)'s whole-function pre-gate; round 791 = 13,389 plus 10 new `DeferredSuppressionTest` pins for (ENGINE.2d)(b)'s deferred flow suppression; round 790 = 13,380 plus 9 new `LoopEntryRetryGateTest` pins for (ENGINE.2d)(a)'s loop-entry-retry gate; round 789 = 13,371 plus 9 new `CmamSectionProbeTest` pins for (ENGINE.2c)'s level-R partition; round 788 = 13,351 plus 20 new pins — `ClosureIndexEquivalenceTest` (9) for (ENGINE.2b)(b)'s interval index and `CpaArgCtxPreGateTest` (11) for (a)'s pre-gate; round 787 = 13,342 plus 9 new `CpaSectionProbeTest` pins for (ENGINE.2)'s two-level property-access partition; round 786 = 13,334 plus 8 new `CtaSectionProbeTest` pins for (ENGINE.1)'s level-E partition of `checkAssignmentExpression`; round 785 = 13,323 plus 11 new `GuardCallNarrowsLocalTypesTest` pins for (NARROW.1)'s type-guard call arm; round 781 = 13,262 plus 13 new `ConstLiteralTypeTest` pins for (WIDEN.1)'s const-keeps-its-literal rule; earlier accretion: round 781 = 13,262 plus 13 new `ConstLiteralTypeTest` pins for (WIDEN.1)'s const-keeps-its-literal rule; round 780 = 13,252 plus 10 new `Rel4ElementAccessAndUnionSwitchTest` pins; round 779 = 13,242 plus 10 new `Rel4bGenericInferenceTest` pins; round 778 = 13,234 plus 8 new `SymbolTypeOrderTest` pins for (ORDER.1)'s order-dependent `symbolTypes` write; round 777 = 13,226 plus 8 new `IntersectionOfUnionsNarrowingTest` pins; round 776 = 13,223 plus 3 new `ProjectCrawlOrderTest` pins; round 770 = 13,207 plus 9 new `PostSwitchFallThroughNarrowingTest` pins for (REL.4)(a)'s post-switch fall-through cause — re-verified end to end on a restarted host, not carried over from the round's own commit message; round 769 = 13,192 owner-verified at `e34ecdd8` plus 15 new `NamespaceScopedEnumNarrowingTest` pins for (REL.4)(a)'s namespace-scoped-enum cause; round 768 was 13,174 owner-verified at `c853573e` plus 19 new `EnumExhaustionToNeverTest` pins for (REL.4)(a)'s three exhaustion causes, minus 1 retired stale control in `EnumAssertAndSwitchDefaultNarrowingTest`; round 756 = 13,089 owner-verified at `2f728c1e` plus 3 new `CtaSectionProbeTest` pins for (FN.1)'s expression-bodied-arrow descent; round 755 = 13,053 owner-verified at `38b79404` plus 18 new `NarrowSectionProbeTest` pins for (CALL.4)'s intra-`applyConditionNarrowing` harness and 4 added to `CtaSectionProbeTest` for (ENGINE.1)'s level-C partition; rounds 751-754 added 5 / 9 / 6 / 9, taking 13,024 → 13,053; round 750 added 6 pins for (REL.1)(c) step 5a — the single mint of the enum discriminant key space; rounds 746-749 added 18 / 8 / 4 / 6 for (c) steps 2 / 3 / 4 / 4b; round 745 added 11 pins for (REL.1)(c) step 1's value-aware enum rule; round 744 which added 15 pins for (REL.1)(b) and its two enum-free unblockers and turned the five (REL.1) `@Ignore`s ON — which is why the skipped count fell 8 → 3; round 743 added 7 for the overload picker's two bugs; round 742 added 9 pins for (REL.1)(b0) — the enum-member ACCESS EXPRESSION; round 741 added 5 for (REL.1)(a)'s enum-member type — 4 stating the base-primitive legs, 1 the `typeof === "object"` knock-on — plus one `@Ignore`d divergence pin; round 734 added 5 for the (CALL.1)(a) one; round 733 added 5 for the (SPINE.1)(a) intra-handler attribution harness; round 732 added 6 for the (DISPATCH.1)(a) derivation harness; historical accretion follows — round 658 was at 12,534 and added no pins (its 33 M04TypeParamTypedOpsSpineMigrationTest pins landed at the round-657 tail); round 657: +23 M04ArrayToClassCastSpineMigrationTest = 12,474, +33 M04TypeParamTypedOpsSpineMigrationTest at the session tail = 12,507; round 656 added no pins — its 30 M04ArgumentsInFieldSpineMigrationTest pins landed at the round-655 tail); round 655: +37 M04ImplicitAnyNewSpineMigrationTest = 12,421, +30 M04ArgumentsInFieldSpineMigrationTest at the session tail = 12,451; round 654: +40 M04IncDecTypeParamSpineMigrationTest and +9 ConflictMarkerScanTest = 12,384; round 653: +44 M04AbstractAccessSpineMigrationTest = 12,335; round 652: +36 M04ImplicitAnyYieldSpineMigrationTest = 12,291; round 651: +30 M04AbstractContextSpineMigrationTest = 12,255; round 650: +33 M04CtorParamInitSpineMigrationTest = 12,225; round 649: +46 M04DeleteOpSpineMigrationTest = 12,192; round 648: +30 M04TypeParamCastSpineMigrationTest = 12,146; round 647: +41 M04ObjLitSuperSpineMigrationTest = 12,116; round 646: +42 M04ConstLitSpineMigrationTest = 12,075; round 645: +52 M04StrictModeSpineMigrationTest = 12,033; round 644: +33 M04ExpandoSpineMigrationTest = 11,981; round 643: +56 M04TpDefaultsSpineMigrationTest = 11,948; round 642: +35 M04InvalidAssignSpineMigrationTest = 11,892; round 641: +35 M04SuperRebindSpineMigrationTest = 11,857; round 640: +32 M04UndefinedNameSpineMigrationTest = 11,822; round 639: +33 M04EvolvingArraySpineMigrationTest = 11,790; round 638: +25 M04ArgsCollisionSpineMigrationTest = 11,757; round 637: +31 M04GenericIndexWriteSpineMigrationTest = 11,732; round 636: +33 M04PropertyInitSpineMigrationTest = 11,701; round 635: +39 M04ProtectedReadSpineMigrationTest = 11,668; round 633: +15 M04NullCastSpineMigrationTest; round 632: +25 M04ConstEnumSpineMigrationTest; round 631: +31 M04BindingPatternSpineMigrationTest; round 630: +26 M04CastOverlapSpineMigrationTest; round 629: +17 M04DaFlowSpineMigrationTest; round 628: +25 M04Sym2StrSpineMigrationTest; round 627: +24 M04AbstractClassSpineMigrationTest; round 626: +17 M04FnTypedParamSpineMigrationTest; round 625: +18 M04ImplicitThisSpineMigrationTest; round 624: +14 M04SpreadSpineMigrationTest +9 M04ArrayPushSpineMigrationTest; round 623: +8 M03StructureBundleTest; round 622: +10 IntKeyMapTest; round 621: +6 NodeKindIdTest, +5 LongKeyMapTest); earlier baseline 11,347 as of round 587 (−12 net: both giants' audit scaffolding retired with their passes) — incl. 78 giant-migration pins; every per-statement emission of the legacy assignability dispatchers, class property initializers, AND the checkFunctionBody body-level walkers are spine-anchored) — **the full tsgo-relevant TypeScript compiler test suite passes.** _Round 443 (2026-07-08, Phase 17 — module-augmentation family + the module-file-local TYPE-alias leak (Blocker #3)): FOUR bounded fixes, all suppression-only. Compiler profile UNCHANGED (190) but the fixes GENERALIZE hugely: services 591 → 498 (−93), server 887 → 733 (−154), harness 1,118 → 989 (−129); suite 9,504 → 9,512 (+8 local across 3 test files, 0 regressions); 4 fix commits; services diffed via --listAll as strictly by-position removals. (1) TS2664 10 → 0: `resolveModuleSpecifierRelativeJsAware` — a `declare module "../compiler/types.js"` augmentation resolves `.js` → the `.ts` sibling (the base resolver deliberately avoids `.js` per the TS2459 gotcha, so the augmentation target never resolved); purely additive → only SUPPRESSES a false 'cannot be found'. (2) TS2564 17 → 0: a class property whose declared type INCLUDES `undefined` needs no definite assignment (tsc's strictPropertyInitialization `getFalsyFlags(type) & TypeFlags.Undefined`); `checkClassPropertyInit` missed the `| undefined` case, so `SourceFileObject`'s `nameTable: Map<...> | undefined` + siblings FP'd; reuses the existing `typeIncludesUndefined`. (3) TS2304 24 → 2: `buildNamespaceScope` gained a StringLiteralNode branch — a `declare module "X"` augmentation body sees the AUGMENTED module's exports by bare name (Node/NodeArray/SymbolFlags/TypeChecker/__String — compiler/types.ts exports NOT imported into services/types.ts); the 2 remaining are NodeJS `global` (env-legit, offline). INVESTIGATED & REVERTED (dashboard no-op): TS7006 array-element contextual typing — a correct M3.2 enabler (3 passing tests) but the real inferFromUsage.ts `Priority[]` case is B83.5-blocked (`interface Priority` nested in a function → unbound → `Priority[]` resolves to any); land it with nested-type resolution. (4) TS2339 44 → 0 (SourceFileLike, Blocker #3): a module-file-local `type X = A | B` alias leaks into globals and shadows the global `interface X` in OTHER files (TYPE-space analog of round 442's variable leak). importTracker.ts's non-exported `type SourceFileLike = SourceFile | AmbientModuleDeclaration` won the last-wins merge over compiler/types.ts's `interface SourceFileLike`, so a `sourceFile: SourceFileLike` param resolved to the bogus union (AmbientModuleDeclaration has no `.text`); pinned via an instrumented probe (whole-program-only — minimal repros don't reproduce). Built `conflatedTypeAliasFiles`; checkMemberAccessMissing bails a union-receiver TS2339 when the receiver displays as a conflated name in a NON-alias file (own-file still fires). NEXT (services @ 498, all deep): TS2322×235 / TS2339×85 (residual conflations — Type×20/Info×12) / TS2345×54 M3 relation, TS2416×11, TS2353×10, TS2740×9/TS2739×8._ 


_Round 835 (2026-08-05) — **OWNER DIRECTION: CONFORMANCE BREADTH. THE `object` KEYWORD'S *APPARENT* TYPE: tsc has NO `object`-specific elaboration — `structuredTypeRelatedTo` compares `getApparentType(source)`, which for `TypeFlags.NonPrimitive` is `emptyObjectType`, so a failing `object -> { foo: string }` is decided by `propertiesRelatedTo` against an EMPTY member table and reported as a MISSING PROPERTY whose source displays as `{}` — never as `object`. We keep `object` as a `Type.Intrinsic` and `collectMissingProperties` requires a `Type.Object` on BOTH sides, so the missing set was empty BY CONSTRUCTION and both assignability emitters degraded to the coarse TS2322. **`nonPrimitiveAssignError` is GREEN; `nonPrimitive` 7 -> 6 failing cases.** Suite **13,855 / 0 / 3** (13,847 + 8 pins, 0 regressions); all 8 profiles `added=0 removed=0` md5-for-md5 against a REBUILT before-arm._

_**WHICH GAP, RE-VERIFIED RATHER THAN INHERITED.** Round 834 handed this down as "1 diff, message-shape only, cheapest by a wide margin"; I re-measured it and the four other residual cases by extracting each fixture's source from its `.js` BASELINE (`//// [name.ts]` carries the original verbatim — the `tests/cases/conformance` tree is not in the sparse checkout) and running the project CLI against the `.errors.txt` summary. **The costing holds exactly**: 6 of 7 diagnostics already matched; the diff was line 5, `y = a`. The louder alternatives were re-measured and left on evidence — `nonPrimitiveConstraintOfIndexAccessType` (10 diffs) is one uniform rule (nothing concrete is assignable to a generic `T[P]`) but tightening it is precisely the direction the standing foreign-TP gate exists to *avoid*, and `nonPrimitiveAccessProperty` (2) needs TS2339 on an `object` receiver where the compiler profile alone carries 21 `: object` annotations incl. `lookupTable: object` and `readJson(): object`. **WHAT LANDED — one predicate, two call sites:** `nonPrimitiveMissingPropSource(source)` returns an interned empty `Type.Object` iff the SOURCE carries `NonPrimitive`, feeding `collectMissingProperties` and supplying `{}` as the displayed source for the TS2741/TS2739/TS2740/TS2696 shapes at `cvdaElaborateMismatch` and the assignment-path elaboration. The coarse TS2322 below still says `object` — that is also tsc, which reports that one from the original type. **The relation VERDICT is untouched in both directions**, so the change can neither add nor remove a diagnostic anywhere. **DELIBERATELY NOT `getApparentType`**: a NonPrimitive arm there is the obvious "proper" fix and would reach ~40 consumers at once — round 834 measured exactly what widening a shared predicate costs. The var-decl twin WAS included, because a rule applied at one of two sibling emitters is an inconsistency the next agent trips on._

_**PIN DISCRIMINATION, VERIFIED IN BOTH DIRECTIONS ON REAL BINARIES** — 8 pins, `NonPrimitiveApparentTypeTest`. Against the **rebuilt pre-change binary** (the grid's own before-arm, so no extra build was spent) exactly the **4 positive pins fail** and the 4 controls stay green. Against a **naive ablation** — the predicate returning the apparent type for EVERY source — exactly the **2 controls written for it fail** (`a primitive source keeps its own coarse message`, i.e. keying on `Type.Intrinsic` rather than the flag; `a named interface source keeps its name`) and the 4 positives stay green. The other 2 controls are **not ablated** and are recorded as arm pins, not claimed as coverage. **WHAT COST TWO BUILD CYCLES:** `Diagnostic.character` is **1-based** while `line` is not directive-stripped, so a related-info position pin must assert the column verbatim against the baseline (tsc's `2:10` is `character == 10`) and the LINE relatively. **RE-COSTING — the cheap end of `nonPrimitive` is now exhausted; none of the 6 residual cases is a message shape.** Cheapest-first: `nonPrimitiveAccessProperty` (2, and its `var { destructuring } = a` half wants the SAME apparent-type substitution), `nonPrimitiveUnionIntersection` (4, intersection REDUCTION — moves identity and display), `nonPrimitiveNarrow` (3) + `nonPrimitiveStrictNull` (12, flow narrowing on `object`), `nonPrimitiveConstraintOfIndexAccessType` (10), `nonPrimitiveAndTypeVariables` (1, `TypeParam -> TypeParam` lenience PLUS a union display-ORDER divergence, so a correct relation fix alone still leaves it red)._

_**GATES.** Suite **13,855 / 0 / 3** from a wiped results dir, `xml.etree` (13,847 + 8 pins, **0 regressions**). `cost_gate.py` **PASS — every counter +0.00%**, `output.errors` 46 unchanged, no rebaseline needed or taken. `huge_methods.py --fail-over 0` exit 0, **0 over the limit** (largest 6,353, unchanged). **8-PROFILE GRID AGAINST A GENUINELY REBUILT BEFORE-ARM** (`Checker.kt` saved, reverted, rebuilt, 8 captured; restored, rebuilt, 8 captured): every capture non-empty with no `more error(s)` truncation tell, counts 46x7 + harness 94, **`added=0 removed=0` on all eight and the sorted-capture md5 IDENTICAL on all eight** — compiler `4caacf248ce417899c2972c16a82f1ed` (the digest rounds 828/832/833/834 recorded), tsc-cli `a46fcc87`, jsTyping `e040dfbb`, deprecatedCompat `78349ce3`, typingsInstallerCore `c2c3ce67`, services `295300f6`, server `16f97bd9`, harness `0929cd11`._

_Round 834 (2026-08-05) — **OWNER DIRECTION: CONFORMANCE BREADTH. THE `object` KEYWORD GETS A REAL RELATION RULE: the one rule the whole compiler held about `object` was tsc's WRITTEN AS ITS NEGATION, which is a different predicate, and correcting it plus the three gates that hid the corrected verdict turns **two more `nonPrimitive` cases GREEN (9 -> 7)**. **THE CATEGORY STILL CANNOT BE ADOPTED, SO IT WAS NOT** — and the honest re-costing below says it is not a one-round category either. Suite **13,847 / 0 / 3** (13,833 + 14 pins, 0 regressions); **all 8 profiles byte-identical against a REBUILT before-arm binary**._

_**THE PER-SUBTEST CENSUS, RE-DERIVED RATHER THAN INHERITED** (round 833's own lesson): the category is 16 files, 1 whole-file-skipped by tsgo, **25 subtests, 9 failing, 0 emit failures** — round 831's counts exactly. But the per-CASE diffs, which no prior round captured, show the 9 are **six unrelated gaps wearing an `object` costume**, and only three of them are about `object` at all. **THE DEFECT, and it reaches further than the keyword:** `isSimpleTypeRelatedTo`'s NonPrimitive leg read `!sf.hasAny(TypeFlags.Primitive or Null or Undefined or Void)` where tsc's rule is the POSITIVE `s & TypeFlags.Object && t & TypeFlags.NonPrimitive`. Those differ because **`TypeFlags.Primitive` omits every LITERAL bit** — so a `Type.StringLiteral`/`NumberLiteral`/boolean-literal source, a bare `Type.TypeParam`, `unknown`, and the instantiable `keyof`/indexed-access types ALL satisfied `object` silently. The furthest consequence was a **false positive one type away from any `object`**: `T[P] extends V | object ? 1 : 0` answered TRUE for a numeric-literal `T[P]`, so a mapped type computed `1 | 1` where tsc computes `1 | 0`. **WHAT LANDED — four changes, every one keyed on the target's `NonPrimitive` flag:** (1) the relation leg inverted to tsc's positive form; (2) a `structuredTypeRelatedTo` arm deciding a `Type.TypeParam` source by its CONSTRAINT (unconstrained `T` rejects, `T extends object` and `T extends {}` accept); (3) `canUseTypeEngine` opened for a `Type.TypeParam` source against a NonPrimitive target (the B60.8 rule below it admits only a CONSTRAINED one — precisely the case that is not an error); (4) a new `isArgCheckableType` = `isSimpleCheckableType` + NonPrimitive, used at **exactly two** argument-position gates. **`nonPrimitiveInFunction` and `nonPrimitiveInGeneric` are GREEN.** **WHAT DID NOT WORK, all three found by measurement and reverted or narrowed:** (a) widening `isSimpleCheckableType` ITSELF — it has 45 consumers, several of them dedicated walkers whose entire FP firewall it is, and B498's `f<object>(123)` walker immediately co-emitted a DUPLICATE TS2345 at one position; hence the separate predicate and its two named call sites, with a pin (`reported exactly once`) that only an explicit type argument can discriminate. (b) opening `canUseTypeEngine` for a UNION source too: it correctly rejects `object | null -> object`, but the same gate then FPs for a union the flow HAS narrowed and we have not (`var e: object | null; e = a; a = e` is legal in tsc) — it bought 3 diagnostics in a case that stays red for 12 other reasons, so it was dropped and the gap is recorded, not hidden. (c) the **one regression the full suite found** was the co-emission CLAUDE.md predicts: `checkSymbolAsWeakTypeArg`'s KDoc claimed "purely ADDITIVE" because the general check skipped `object` params — no longer true, so 8 of `dissallowSymbolAsWeakType`'s 12 diagnostics doubled. Fixed by the prescribed retraction keyed on (code, fileName, start, MESSAGE), with the stale KDoc corrected; the walker keeps its two uniquely-owned TS2769 chains._

_**THE RE-COSTING, which is the round's deliverable as much as the code: `nonPrimitive` needs SIX more sub-steps and two of them are not `object` work at all.** The 7 residual cases: **`nonPrimitiveConstraintOfIndexAccessType`** wants 10x TS2322 `string -> T[P]` — pure INDEXED-ACCESS assignability, we emit nothing, and `object` appears in 1 of its 10 constraints; **`nonPrimitiveAndTypeVariables`** (1 diff) is blocked by the GENERAL `TypeParam -> TypeParam` lenience (`srcConstraint == null || tgtConstraint == null -> return true`) plus a union DISPLAY-ORDER divergence (tsc sorts by type id and prints `object | U`; we preserve source order); **`nonPrimitiveNarrow`** (3) and **`nonPrimitiveStrictNull`** (12) want flow narrowing on `object` — `typeof x === 'number'` to `never`, and 8 TS18047/18048/18049 nullish-access diagnostics; **`nonPrimitiveAccessProperty`** (2) wants TS2339 on an `object` receiver (through the repo's most FP-sensitive walker); **`nonPrimitiveUnionIntersection`** (4) wants `getIntersectionType` to REDUCE (`object & string` -> `never`, `object & {}` -> `object`) plus excess-property checking through such an intersection — bounded but it changes intersection identity and display; **`nonPrimitiveAssignError`** (1) is the only cheap one left, a message shape (TS2741 with the source displayed as its apparent `{}` + TS2728, where we print TS2322). **So, like round 833's `typeSatisfaction`, the 0-emit-failure count overstated tractability — and for the same reason: a category's name describes its FIXTURES, not its gaps.**_

_**GATES.** Suite **13,847 / 0 / 3** from a wiped results dir, `xml.etree` (13,833 + 14 `NonPrimitiveObjectTypeTest` pins, **0 regressions**). `cost_gate.py` **PASS — every counter +0.00%**, `output.errors` 46 unchanged, so no rebaseline was needed or taken. `huge_methods.py --fail-over 0` exit 0, **0 over the limit** (largest 6,353). **THE 8-PROFILE GRID WAS RUN AGAINST A GENUINELY REBUILT BEFORE-ARM**, not against recorded counts: `Checker.kt` backed up, reverted, rebuilt, all 8 captured, restored, rebuilt. Every capture non-empty with no `more error(s)` truncation tell; counts 46x7 + harness 94; **`added=0 removed=0` and the sorted-capture md5 IDENTICAL on all eight**, the compiler profile at `4caacf248ce417899c2972c16a82f1ed` — the digest rounds 828, 832 and 833 recorded. A count match is not a diff (round 811), which is why the before-arm was rebuilt rather than quoted._

_Round 833 (2026-08-05) — **OWNER DIRECTION: CONFORMANCE BREADTH. THE GENERAL `satisfies` CHECK LANDS — TS1360 plus the fresh object-literal excess check — closing a gap where the operator was verified NOWHERE. **NO CATEGORY WENT GREEN, SO NONE WAS ADOPTED.** Suite 13,833 / 0 / 3 (13,824 + 9 pins, 0 regressions).** **I did NOT take round 831's recommended general TS2352 rule, and the per-subtest census is why:** `asOperator` cannot go green whatever TS2352 does (`asOperatorASI` is a parser/ASI EMIT failure, and one emit failure blocks a category outright), so that rule buys **one** subtest while touching the repo's most crowded walker family (~10 TS2352 emitters) at a construct appearing thousands of times in tsc's sources — the exact shape CLAUDE.md records as regressing. `satisfies` is the opposite on every axis: **the largest single category (12 subtests)**, a genuinely UNIMPLEMENTED feature rather than a message chase, and **structurally bounded — the whole tsc source tree holds 6 `satisfies` operators, only 3 of them inside any of the 8 dashboard profiles.** **WHAT LANDED:** a `NodeKind.SATISFIES_EXPRESSION` arm on the spine's per-kind dispatch, so it costs one tableswitch case and nothing at any other kind. It is **deliberately partial by design, not by budget** — a `satisfies` operand is FRESH, so a property-VALUE verdict depends on contextual typing this site does not install and would REJECT legal code (`{ a: "a" } satisfies { a: "a" | "b" }`); only the two contextual-typing-INDEPENDENT verdicts are emitted, property NAMES on an object literal (excess ⇒ TS2353/TS2561 through the existing `checkExcessProperties`, missing required ⇒ TS1360 with its chain and TS2728 related info) and a primitive-intrinsic operand against a primitive-intrinsic target. TS1360 anchors at the `satisfies` KEYWORD (length 9). **THE PIN CAUGHT A REAL DEFECT THE CORPUS MASKED, which is the round's best argument for pins built to fail:** the excess check was ORDER-DEPENDENT — a named target's member table is lazy, so `collectTargetPropertyNames` read a null `members` and bailed unless an earlier line had already resolved that type, and `typeSatisfaction.ts` hides it exactly (its excess literal is preceded by another `satisfies I1`). The whole-suite run came back 13,833 / **1**, and the one failure was my own pin. **MOVEMENT: `typeSatisfaction` 12 → 11 failing subtests, `typeSatisfactionWithDefaultExport` GREEN**, and the two TS2353 + the TS1360 inside `typeSatisfaction.ts` now match baseline. **THE CATEGORY IS RE-COSTED UPWARD, which is the honest result:** the 11 residual subtests are three OTHER gaps in a `satisfies` costume — **4 need `Record`/`Partial`, which the EMBEDDED lib does not declare at all** (a LIB gap no `satisfies` work touches), **5 need property-VALUE conformance against an index signature**, **2 need contextual typing to flow THROUGH the operator** (one also wanting an object-literal arm in `literalTypeOfExpression`, a broad change touching every literal-preservation site — designed and **deliberately not landed** as unverifiable without another build cycle). **GATES:** suite **13,833 / 0 / 3** (`xml.etree`, wiped results dir); `huge_methods.py --fail-over 0` exit 0, **0 over the limit**; `cost_gate.py` **PASS** — 46 errors unchanged, every counter +0.00% except `typeNode.cacheable`/`bypassed` **+10 (+0.01%)** and `globals.lookups` +4, exactly the two compiler-profile `satisfies` type nodes now resolving, rebaselined with `--update` in the same commit. **8-PROFILE GRID, both directions:** all eight captures non-empty with no truncation tell, counts 46×7 + harness 94 identical to the recorded `bench/*.tsv` rows, **ZERO sightings of TS1360/TS2353/TS2561** (the only codes this change can add), and the compiler profile hashes to **`4caacf248ce417899c2972c16a82f1ed` — byte-for-byte the digest rounds 828 and 832 recorded**, so its before-arm needed no rebuild and the diff is empty by construction._

_Round 832 (2026-08-05) — **(JIT.2b), OWNER-DECIDED 2026-08-05 "BUILD THE INVALIDATION, THEN SHIP": THE JDK 25 AOT CACHE NOW SHIPS BEHIND A FAIL-SAFE GUARD, ROUND 828'S SILENT-WRONG-ANSWER MODE IS CLOSED, AND THE 1.638x SURVIVES AS **1.68x (3/3 PAIRS)**.** The hazard is the whole item: the JVM has NO invalidation — with `Checker.class` removed from the jar and a fresh mtime, the plain run dies with `NoClassDefFoundError` and **the cached run exits 0 printing `OK — 0 errors`**, `-XX:AOTMode=on` included. **THE CONTRACT — fail safe, with no opt-out:** `scripts/xtsc` passes `-XX:AOTCache=` only when a recomputed manifest matches the stored one **byte for byte** — sha-256 of every classpath entry *in order*, the JDK build (`$JAVA_HOME/release`'s digest), OS/arch, main class, a launcher-version constant, and the cache file's own size and digest; missing / mismatched / corrupt / no sha256 tool / an exploded-directory classpath all **run without the cache**, and there is deliberately no flag that makes an unverified cache usable. **NOTHING CONSULTS AN mtime, BY DESIGN** — round 828's experiment gave the mutated jar a *fresh* mtime, and a rewrite that PRESERVES one is exactly what a stat check waves through; a pin holds the other direction so a `touch` does not throw the 1.68x away. **THE COST WAS MEASURED FIRST AND SHAPED THE DESIGN: ~80 ms = 0.8%** of the ~10,000 ms delta, affordable only because the digests and `stat`s are **batched into one process each** (the per-entry loop cost 80 ms by itself, purely in `fork` — which is what would have forced the stat-based fast path and with it the one hole that matters). **VERIFICATION:** round 828's stale-jar scenario now **exits 1 with `NoClassDefFoundError`** (`SKIP no-cache-file`) where the unguarded run exits 0 with the wrong answer, and with the stale pair PLANTED under the new fingerprint's filename — defeating the name layer so only the content check can act — **`SKIP manifest-mismatch`**, `status` naming the differing line; **the pin discriminates under a ONE-mistake ablation** (manifest comparison removed → exactly 1 of 8 pins fails, reading `USE …`, the other 7 green); `--noEmit --listAll` cached-vs-uncached **diff EMPTY at round 828's own md5** `4caacf248ce417899c2972c16a82f1ed`, 46 errors both, no truncation tell; timing 27,396/17,398 · 24,853/14,708 · 24,813/14,780 → **median 1.68x, 3/3, 147 ms spread on a ~10,000 ms delta** (absolutes inflated and NOT quotable — another project's build shared the box all round). **A CORRECTION TO ROUND 828 THAT MATTERED:** the `AdapterHandlerEntry` warnings are on **stdout**, not stderr, so they land in the diagnostics stream — and two narrower flags failed first, `-Xlog:aot+codecache+stubs=error` (misses the plain-`aot` tag) and `-Xlog:aot*=error:stderr` (**adds a stderr sink without reconfiguring the default stdout one** — unified logging is per OUTPUT); shipped pair `-Xlog:aot*=off:stdout` + `-Xlog:aot*=error:stderr`, verified against a truncated cache to keep all three `[error][aot]` lines. **UNPROMPTED CONFIRMATION:** a mid-round Gradle rebuild moved the fingerprint `358cef8a…` → `aafecab2…` on its own, and the retrain reported `pruned 1 stale cache(s)`. **WHAT DID NOT WORK:** the warnings could NOT be reproduced on demand (2 / 0 / round 828's 4), so the silencer is verified by construction and said so rather than claimed; auto-training on first use was rejected (a launcher cannot pick a *representative* project, the only thing separating a 39% cache from a 5.9% one); **the corpus suite was NOT run through a cached JVM** — recorded as residue, not closed. Gates: suite **13,824 / 0 / 3** (13,816 + 8 new pins, 0 regressions, `xml.etree`), `huge_methods.py --fail-over 0` exit 0; `cost_gate.py` n/a (no checker change). `docs/perf/aot-cache.md`._
_Round 831 (2026-08-05) — **FIRST ROUND ON THE POST-v1 BACKLOG. M3.0's CONFORMANCE REDNESS TABLE RE-MEASURED AFTER 135 ROUNDS AND IT IS *UNCHANGED* — ALL NINE REMAINING CATEGORIES ARE RED, SO NO CATEGORY WAS ADOPTED, AND THAT IS THE RESULT.** The live QUEUE's two unchecked items are non-actionable by the repo's own rules — (JIT.2b) is `BLOCKED-PENDING-USER` (a Guardrail) and (WIDEN.1) carries a standing *"do not widen speculatively"* with zero measured sites — so the loop entered the Post-v1 backlog and took **M3.0**, the only pre-approved, per-category-incremental item there. **THE MEASUREMENT:** all nine remaining candidate categories adopted in ONE arm per the item's own protocol — **14,015 tests / 87 failed / 3 skipped** vs the 13,816 / 0 / 3 baseline (+199 tests, 87 red) — then reverted. **CLASSIFICATION FIRST, BECAUSE IT IS THE WHOLE DECISION: 0 of the pre-existing 13,816 regressed**, verified by mapping every failing name back to its source corpus (`git ls-tree` conformance-vs-compiler, `xml.etree` over 630 XMLs): **74 of 74 failing cases are newly-adopted, 0 compiler-corpus, 0 unmapped.** **THE TABLE IS UNCHANGED:** `asOperator` 5 -> **4** · `any` **6** · `conditional` **8** · `nonPrimitive` **9** · `labeledStatements` **9** · `typeAliases` **9** · `contextualTyping` **9** · `typeSatisfaction` **12** · `optionalChaining` **21**; **exactly one subtest moved in 135 rounds**, the expected result of an arc that was entirely perf work — so a future round should treat round 695's table as CURRENT rather than re-derive it. **WHAT THIS ROUND ADDS AND 695 NEVER RECORDED — the failing-CASE count and the EMIT-vs-ERRORS split, which is what decides adoptability:** 87 subtests come from **74 cases**, and `conformanceDeferredErrorBaselines` defers only `.errors.txt`, so **ONE failing JS-emit subtest blocks its whole category**. `cases/files (emit-affected)`: `asOperator` 4/9 (**1**) · `any` 6/9 (**0**) · `conditional` 7/10 (**2**) · `labeledStatements` 5/8 (**4**) · `nonPrimitive` 9/16 (**0**) · `typeAliases` 9/15 (**0**) · `contextualTyping` 9/17 (**0**) · `typeSatisfaction` 10/16 (**0**) · `optionalChaining` 15/25 (**12**). Bucketed: **62 subtests are missing CHECKS** (diagnostic-set mismatch, no emit involvement) and **25 across 19 cases are JS-emit/downlevel gaps** that no deferral can carry. **WHAT I DID NOT LAND, AND WHY — both routes are closed:** "narrow to the green categories" lands NOTHING (no category is green; the floor is `asOperator` at 4 cases), and "adopt and defer" is available for five zero-emit categories (43 cases) but is exactly the mass-parking the item's own rule forbids — the live deferred set holds **2** entries, each triaged in a paragraph. Landing 87 red was never on the table, and `LogicalParityDivergence` is explicitly NOT the mechanism (form-vs-meaning, not unimplemented behaviour). **THE NEXT TARGET IS PRICED, NOT GUESSED:** `asOperator`'s two TS2352 cases want a **general comparability rule** where today TS2352 is ~10 dedicated narrow walkers and no general rule exists (M3.1-depth, corpus + `--listAll` x8); `asOperatorASI` is a parser/ASI gap. **WHAT LANDED IN CODE: one line, declared honestly as NOT a demonstrated fix** — `cloneTypeScriptRepo` derives its sparse checkout from `conformanceCategories` but never declared it as an input; **the attempt to demonstrate the resulting stale-checkout failure was uninterpretable** (removing an input property is itself a Gradle invalidation), so it is declared because it is true. **WHAT WENT WRONG, recorded because it cost ~40 minutes:** the measurement suite was launched as `nohup ./gradlew jvmTest &` INSIDE a `run_in_background` call — double-detached, so the next command's `rm -rf build/test-results/jvmTest` deleted the results dir out from under a still-live run, which died *after* the tests had completed and looked exactly like a build that never produced results. Gates: corpus re-verified from a wiped results dir at **13,816 / 0 / 3**, identical to round 829; `cost_gate.py` / `huge_methods.py` not applicable (no `src/` change)._


