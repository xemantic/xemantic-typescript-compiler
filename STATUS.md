# Status

**Phase 4 — Checker buildout: COMPLETE. Phase 17 — self-compile the TypeScript compiler itself (re-scoped 2026-07-03; "any TypeScript project" is post-v1) — is the LIVE mission (see PLAN-PHASE-5.md; v1 exit = all 8 tsc-source profiles at zero FPs; dashboard: bench/*.tsv, self-compile diagnostics 13,245 → 936 (measured at round 430 — rounds 395–412 burned bounded non-M3 histogram-tail buckets + M3.4 flow-narrowing slices the round-394 "pool picked over" triage missed, all surfaced by bucketing the full `--listAll` output by normalized message shape: TS2499 multi-base-generic misparse, TS2440 type-only-barrel-merge, TS2344 constraint-chain + cross-file `typeParamInternCache` pos-collision, TS2693/TS2709 clodule + destructuring-shadow, TS2314 enum-member-vs-generic, TS2588 nested-shadow, TS2551 Object.setPrototypeOf, SetIterator/MapIterator lib gap, TS2774 local-shadows-function, TS1100 interface-property-name, TS7023 callback-arg self-ref, TS2365 arithmetic local-shadows-function, TS2362 branded-number, TS7053 enum-reverse-mapping, round 408's TS2349 callee flow-narrowing family, and round 409's `export *`-barrel / ESM-`.js` imported-guard flow narrowing (M3.4, TS2339 838 → 672), and round 410's TS2862 constraint-index-sig gate / assign-RHS type-guard narrowing / void-overload-return (2,443 → 2,433), and round 411's M3.4 enum-member discriminant narrowing (barrel-import-resolved, three families UpToDateStatus/TypeMapper/PrivateIdentifierInfo) + type-guard-narrows-a-member-DOWN (2,433 → 2,374, TS2339 672 → 614) + round 412's M3.4 single-type type-guard narrow-DOWN + TS18048 receiver-narrowing (2,374 → 2,373), and round 413's `export *` LEAF-EXPORT gate (barrel-imported `Debug.assert` & every barrel guard now resolves — the TRUE builder.ts blocker, NOT the round-412 depth red herring) + tsc-faithful linear flow-walk iteration + return-path narrowing (2,373 → 1,965, TS2339 614 → 237)), then rounds 414-417 burned more bounded/M3.4 buckets (TS2366 CFA fall-through, NonNull/&&-narrowing arithmetic, closure-loop narrowing, namespace-local `extends` bases: 1,965 → 1,902), and round 418 resolved NESTED type-guard functions (M1.12, 1,902 → 1,854, TS2339 237 → 189: tsc guards `isTupleType`/... are nested in `createTypeChecker` so the binder skips them and `resolveFlowCalleeDecl` missed them - the guard never narrowed, the single biggest TS2339 sub-family), and round 419 resolved properties on INTERSECTION union-members (M1.12, 1,854 → 1,808, TS2339 189 → 143: `getPropertyOfType`/`typeHasOwnProperty` bail on a `Type.Intersection` member so `PropertyAccessExpression | (ElementAccessExpression & …)` FP'd TS2339 on `.parent`; fold the constituents in property resolution + discriminant-narrowing annotation; 0 new FPs, self-compile time −17%), and round 420 resolved TYPE-ALIAS enum-member discriminants in narrowing (M1.12, 1,808 → 1,799, TS2339 143 → 134: a `.kind: <alias>` discriminant member survived a `switch (x.kind) { case }` because `enumMemberKeysOfTypeNode` handled only a direct `Enum.Member`, not a bare-Identifier alias; 0 new FPs), and round 422 burned FIVE bounded M3.4/overload families (1,799 → 1,756: TS2769 60 → 45 — overload arg-check helpers now flow-narrow reference args (mirroring B469) + a synthetic true|false union for guard-narrowed boolean args vs literal overload params; TS2339 134 → 117 — mixed enum + string-literal discriminant unions narrow on disjoint lit:s: keys (PrivateIdentifierInfo); TS18048 10 → 7 — an optional-chain discriminant access x?.kind === <non-nullish> proves the receiver non-nullish; TS2366 12 → 4 — union-.kind exhaustive switches terminate via requiredUnionDiscriminantKeys), and round 423 (a) drove TS2366 to ZERO (1,756 → 1,752: the switch RECEIVER is guard-narrowed + typed before the exhaustive-switch union walk — flow graph via the pass-dedicated implicitReturnFlowGraph, local-const-from-call typing, optional-discriminant @undefined keys, LiteralToken["kind"] indexed-access) and (b) fixed union-TARGET type guards end-to-end (1,752 → 1,708: `x is A | B` PARSED truncated so isCallOrNewExpression/isPropertyNameLiteral/isOptionalChain never narrowed; + aliased conditions `const isFrag = guard(x); if (!isFrag)` via a memoized flow back-walk; + kind-key disjointness beating the lenient relation; TS2339 117 → 104, TS2322 784 → 756) and (c) narrowed truthy optional-chain CALL conditions (1,708 → 1,707: `if (x.y?.size())` proves `x.y` non-nullish), and round 424 burned SEVEN more M3.4 flow-narrowing slices (1,707 → 1,662, TS2339 104 → 68, TS18048 5 → 1, TS2322 756 → 751: loop-entry union suppression with a STRUCTURAL wash gate, call-RHS return-annotation narrowing, closure/join/call-crossing aliased conditions, prefix-path receiver guards, asserts-with-inferred-TP via test-arg predicate inference, assignment-overwrite reset to the declaration/call-RHS resolved type, and the DebugTypeMapper this-narrowing slice (explicit-type-arg asserts re-type `this`; an overload-cluster name collision resolves to the unique TypePredicate-bearing decl) — every step by-site strictly removals), and round 425 found the TS2339 never-cluster ROOT CAUSES (1,662 → 1,600, TS2339 68 → 6: the round-411 enum-discriminant key space SPLIT across resolution paths — canonicalEnumSymbol at all four key-builder sites — plus per-candidate narrow-DOWN distribution in union-target guards/instanceof, aliased ===/switch discriminants, switch-default negative narrowing, typeof-object filtering, boolean-literal discriminant truthiness, destructuring-read narrowing, the loop-entry retry for the single-type suppression, and the positive-empty intersection fallback whose round-423 dead-end verdict was an ARTIFACT of the key split) — driving to 0, and round 426 replaced the B399 TS2563 per-file proxy with the faithful flow-walk depth trip + the asserts-callee call gate (1,600 → 1,593: TS2563 27 → 0; TS2454×20 pre-existing definite-assignment walker FPs unmasked), and round 427 burned that bucket (1,593 → 1,577: TS2454 20 → 4 via tsc's definiteness rules — logical assigns ??=/||=/&&= are DEFINITE so captured reads assume initialized, a !-asserted read assumes initialized, and the anywhere-scan's left-spine walk now applies the target rule per spine node), and round 428 took the first real M3.1 slice of the TS2322/TS2345 cores (1,577 → 1,186, −25%: nullable-union generic param inference + overloaded generic callees for tsc's `x = append(x, item)` idiom (TS2322 751 → 559), the TS2345 histogram top — explicit this-param binding, guarded optional-member args, numeric-enum→number (394 → 280) — the array-literal-vs-union-member string-layer rule (TS2322 → 501), and the body-local-shadows-function anyType registration (TS2345 → 261, TS2769 → 36)), and round 429 drove the TS2345 core 261 → 86 (1,186 → 1,000, −15.7%: call-types lexical shadowing — body-locals vs enclosing params, DESTRUCTURED params via the currentParamBindingNames side set in getTypeOfIdentifier (the round-428 "mini-repro does not reproduce" residue), arrow own-params; embedded String replace/replaceAll/search/split accept RegExp; optional-param union args + NonNull-asserted args; guard-narrowed interface/unknown Identifier args — the ~110-site dominant mechanism, never-param excluded for assertType<never> exhaustive-switch defaults; string-enum→string in isSimpleTypeRelatedTo resolving the round-410 Extension[][] deferral; rest-arg flow narrowing), and round 430 unlocked the `append`/`addRange` inference (936: the `T extends {}` constraint failed `string → {}` and killed every mapper — empty-object-target relation rule with TP-source excluded per genericPrototypeProperty3's pin; readonly-array anchors; TP-from-PREDICATE binding for the getFirstJSDocTag family); self-compile time ~110–150 s single-run band), and round 431 took the FIRST M3.2 slice — the TS7006 core 301 → 11 via contextual typing (nested-fn/lexical callee resolvability, assignment-RHS context from the LHS declared type under the B476 single-applicable-sig rule, ||/??-both/&&-comma-right propagation, intersection/Reference/extends receiver members) — plus engine return-checking in switch/try bodies behind the foreign-TP source gate (un-inferred generic call results bail instead of FP'ing; ~130 T[]/U|undefined/SearchResult<T> return sites cleared) and the TS2367 anchored-disjointness gate (936 → 551, −41%; TS2322 435 → 345, TS7019 4 → 0; by-code strictly shrinking at every landed step) — then extended the foreign-TP gate to the var-decl/assignment/property-write/conditional-return paths (551 → 482, −69 strictly removals; TS2322 → 276; a generic FUNCTION-VALUE source's sig-own TPs stay checkable per 5 corpus pins the suite gate caught), and round 435 took SEVEN more M3.1/M3.2 slices at post-perf-arc iteration speed (482 → 373, −22.6%: generator TReturn returns, fresh-objlit literal props via the freshObjLitRange relation retry, TP-literal-constraint args, the union-decomposition-transparent relation re-entry gate resolving the NodeArray-covariance family ×23, bare-`new` contextual instantiation, the foreign-TP gate on assignment TARGETS (visitor family ×15+), nullish alias-union returns; TS2322 276 → 184, TS7006 11 → 1, TS2345 86 → 79; full-dashboard baseline: compiler 482→373 / tsc-cli 484 / jsTyping 480 / deprecatedCompat 481 / typingsInstallerCore 489 / services 1,603 / server 1,894 / harness 2,193 at 29–41 s wall each), and round 436 took SEVEN more M3.1/M3.2/M3.4 slices (373 → 294, −21.2%: TP-carrying callback-return param skip (forEachEntry family ×14), destructured-LOCAL shadowing (semver/checker + 12 transformer sites), literal-return syntactic union membership (the string-fallback re-widening trap — `return false` vs `Tag | false` ×7, incl. the completely UNPINNED `return "a"` vs `"a" | "b"` shape), constraint-filtered explicit-type-arg overload selection ('true' vs 'false' ×6), overload-helper optional-param union-arg + foreign-TP rules (union-receiver TS2769 ×13), bare-string switch-case narrowing (semver ×3), guard-gated ternary return arms with the tri-state all-leaves-verified early return (×19); TS2322 184 → 158, TS2345 79 → 47, TS2769 30 → 9; round-436 dashboard baseline: compiler 373 / tsc-cli 375 / jsTyping 371 / deprecatedCompat 372 / typingsInstallerCore 380 / services 1,476 / server 1,769 / harness 2,062 at 27–41 s wall), then round 438 (2026-07-07) took FIVE more M3.1/M3.4 narrowing/relation slices (294 → 244, −17%: an empty-object `{}` union-member return-path early return; UNION-target extension of the assignment-RHS AND return-path type-guard narrowing gates (`currentSourceFile = node` / `return node` vs `SourceFile | undefined`); PROPERTY-ACCESS args in the call-arg guard-narrow-DOWN branch (`getExports(node.left)`); and nullish-strip-gated narrowing of object-literal property VALUES (`{ moduleSpecifiers: specs }` where `specs = append(specs, x)`); TS2322 158 → 116, TS2345 47 → 39), then round 439 (2026-07-07) took THREE predicate-overload / arg-narrow-DOWN slices (244 → 228, −16 strictly removals: findAncestor-style type-guard-callback overload return inference + a companion NonNull concrete-union strip, the overload arg-check non-union narrow-DOWN, and its `unknown`-via-typeof extension; TS2769 9 → 1).** Corpus suite green as a permanent regression gate: 9,817 tests / 0 failing / 3 skipped (generated tsgo-relevant corpus + local corner-case tests; count as of round 463) — **the full tsgo-relevant TypeScript compiler test suite passes.** _Round 443 (2026-07-08, Phase 17 — module-augmentation family + the module-file-local TYPE-alias leak (Blocker #3)): FOUR bounded fixes, all suppression-only. Compiler profile UNCHANGED (190) but the fixes GENERALIZE hugely: services 591 → 498 (−93), server 887 → 733 (−154), harness 1,118 → 989 (−129); suite 9,504 → 9,512 (+8 local across 3 test files, 0 regressions); 4 fix commits; services diffed via --listAll as strictly by-position removals. (1) TS2664 10 → 0: `resolveModuleSpecifierRelativeJsAware` — a `declare module "../compiler/types.js"` augmentation resolves `.js` → the `.ts` sibling (the base resolver deliberately avoids `.js` per the TS2459 gotcha, so the augmentation target never resolved); purely additive → only SUPPRESSES a false 'cannot be found'. (2) TS2564 17 → 0: a class property whose declared type INCLUDES `undefined` needs no definite assignment (tsc's strictPropertyInitialization `getFalsyFlags(type) & TypeFlags.Undefined`); `checkClassPropertyInit` missed the `| undefined` case, so `SourceFileObject`'s `nameTable: Map<...> | undefined` + siblings FP'd; reuses the existing `typeIncludesUndefined`. (3) TS2304 24 → 2: `buildNamespaceScope` gained a StringLiteralNode branch — a `declare module "X"` augmentation body sees the AUGMENTED module's exports by bare name (Node/NodeArray/SymbolFlags/TypeChecker/__String — compiler/types.ts exports NOT imported into services/types.ts); the 2 remaining are NodeJS `global` (env-legit, offline). INVESTIGATED & REVERTED (dashboard no-op): TS7006 array-element contextual typing — a correct M3.2 enabler (3 passing tests) but the real inferFromUsage.ts `Priority[]` case is B83.5-blocked (`interface Priority` nested in a function → unbound → `Priority[]` resolves to any); land it with nested-type resolution. (4) TS2339 44 → 0 (SourceFileLike, Blocker #3): a module-file-local `type X = A | B` alias leaks into globals and shadows the global `interface X` in OTHER files (TYPE-space analog of round 442's variable leak). importTracker.ts's non-exported `type SourceFileLike = SourceFile | AmbientModuleDeclaration` won the last-wins merge over compiler/types.ts's `interface SourceFileLike`, so a `sourceFile: SourceFileLike` param resolved to the bogus union (AmbientModuleDeclaration has no `.text`); pinned via an instrumented probe (whole-program-only — minimal repros don't reproduce). Built `conflatedTypeAliasFiles`; checkMemberAccessMissing bails a union-receiver TS2339 when the receiver displays as a conflated name in a NON-alias file (own-file still fires). NEXT (services @ 498, all deep): TS2322×235 / TS2339×85 (residual conflations — Type×20/Info×12) / TS2345×54 M3 relation, TS2416×11, TS2353×10, TS2740×9/TS2739×8._ 

_Round 464 (2026-07-10, Phase 17 — bounded FP burn-down: SIX fixes — three cleared by minimal repro, one by an XPROBE cascade on the real program (with a new tooling trap discovered), one a repro-pinned mechanism pair whose dashboard site needs one more layer. Dashboard: compiler 61 → 55 (−6; TS2322 10 → 6, TS2362 1 → 0, TS7006 1 → 0), services 154 → 139 (−15 cross-profile); suite 9,817 → 9,836 (+19 local across 7 new test files, 0 regressions); 6 fix commits (ecf6290d / 44e1b2ff / 6fcf7cda / f1e48c81 / 2d843068 / 6fe6406d). (1) BARREL-ENUM MEMBER NON-NULLISH (ecf6290d, −2): `receiverResolvesToRealEnum` only followed the general resolveAlias (which can't follow ESM-`.js` + `export *` barrels), so `flags = flags || NodeBuilderFlags.None` never proved `flags` non-nullish — FP'd BOTH TS2362 on the later `flags & NoTruncation` arithmetic (checker.ts:6639) and TS2322 through the objlit shorthand member vs NodeBuilderContext (checker.ts:6640); falls back to the flow-only resolveImportedEnumSymbol. (2) GENERIC INFERENCE FROM FLOW-NARROWED ARGS, M3.4 × M3.1 (44e1b2ff, −1): tryInferSingleTypeParamFromArgs bound T from the DECLARED union — `const name = cloneNode(node)` under `case SyntaxKind.Identifier:` now binds T := Identifier (typeSerializer.ts:603); gated to union-declared reference args + a non-never refinement relating to the declared type. (3) TS7006 DESTRUCTURED-SOURCE CONTEXT, M3.2 (6fcf7cda, −1): a third parallel scope stack (implicitAnyScopeDestructures) maps a destructured element to its source + property name so `parseConfigFileHost.onUnRecoverableConfigFileDiagnostic = d => …` resolves the arrow's contextual type from state's declared member (tsbuildPublic.ts:594). (4) FLOW NON-NULLISH CLUSTER (f1e48c81, −1): FOUR coupled pieces clear getTypeAtFlowNode's `return type;` (checker.ts:29132) — a ternary RHS is non-nullish iff both arms are; typeNodeDefinitelyNonNullish's globals fallback accepts an UNAMBIGUOUS barrel type ALIAS (count TypeAliasDeclarations, never list size — the merged declarations are polluted with ImportSpecifiers) and recurses its body (`FlowType = Type | IncompleteType`); an un-annotated param DEFAULTED from an annotated sibling (`initialType = declaredType`) types as the sibling's annotation (checkFunctionBody + populateParameterLocalTypes); an UN-ANNOTATED callee proves non-nullish from its body returns (bounded scan; ternary identifier leaves resolve via the callee's own params, then getTypeOfIdentifier, then the new program-wide uniqueNestedVarDeclByName — tsc's `convertAutoToAny` with its `var anyType = createIntrinsicType(…)` closure-var leaves). TOOLING TRAP: the bench files are CRLF — python text-mode offsets understate checker positions by one per line (3 probe iterations lost; CLAUDE.md gotcha added). (5) BARREL CHECKDEFINED RETURNS + PA-RHS NARROWING (2d843068, dashboard-neutral, repro-pinned): `Debug.checkDefined(x)` through the barrel resolves its RETURN as the arg minus nullish (shape-gated — no general barrel resolution in the type path); `end = importLiteral.end` filters the antecedent by the member's non-union type; program.ts:1220's chain source improves `file: any` → `file: SourceFile` but the site needs objlit-value narrowing against a TWO-object-member union target (round-438/462 context gates accept only a SOLE non-nullish member). (6) DESTRUCTURED-CONST ELEMENT TYPING + MULTI-MEMBER-UNION OBJLIT CONTEXT (55): `recordDestructuredConstElementTypes` types top-level object-destructured elements from the source's declared members (B83.5 left `const { kind, index } = ref`'s `index` anyType, so `file.referencedFiles[index]` resolved any and the pos/end destructuring-overwrite narrowing never proved — probe cascade found the ELEMENT-ACCESS index, not the receiver, was the leak; FUNCTION-shaped members stay unrecorded per the tsbuildPublic.ts:767 unmasking, detector resolves members + union constituents) + `selectUnionMemberByObjLitKeys` (a multi-object-member union return target contributes the single constituent covering every objlit property name as context) — program.ts:1220 CLEARED, strictly removals. NEXT (compiler @ 55, ~9 real excl. TS2591×43 + TS2304×2 `global` + TS2584 console): the big-objlit M3 remnants (emitter.ts:1277 select-return, moduleNameResolver.ts:1300 getPackageJsonInfoCache `| undefined`); esDecorators.ts:1309 (destructured member from un-annotated nested callee + method-call-on-destructured-receiver — two more mechanism layers); namedEvaluation.ts:434 (union∩interface kind-reduction property read); parser.ts:9581 / factory/utilities.ts:1688 (cast-instantiation identity, M3); builder.ts:2390 (callback-return inference, Blocker #2); es2020.ts:91 + core.ts:2135 (known-hard)._

_Round 463 (2026-07-10, Phase 17 — bounded FP burn-down: NINE fixes — minimal-repro fixes, one measured UN-GATE of a historical skip, and two new flow-narrowing mechanisms. Dashboard: compiler 72 → 61 (−11; TS2322 15 → 10, TS2339 2 → 1, TS2345 2 → 1, TS2353/TS7053/TS18048/TS2589 all → 0), services 154 → 145 (−9 cross-profile); suite 9,794 → 9,817 (+23 local across 9 new test files, 0 regressions); 9 fix commits (6075703a / 610bf2a0 / fd3a7003 / 78001791 / 9a55bd1a / 8e6ec34c / 83f27992 / cac642c6 / 181a850a). (1) NEVER ARRAY ELEMENT (6075703a): `[undefined!]` (never, B282) vs `Expression[]` FP'd TS2322 — the primitive-element branch skipped Null/Undefined/Void/Any but not Never (taggedTemplate.ts:50). (2) FN-TYPE ALIAS INSTANTIATION UN-GATE (610bf2a0, −3): the B50.1 skip left `TransformerFactory<SourceFile | Bundle>`'s body T an UNBOUND TypeParam that fails the relation against a conforming source (transformer.ts:83/98/100); the historical FP hazard is covered by the round-431 foreign-TP gates — measured corpus-green + strictly-removals; isFunctionTypeAliasBody deleted, CLAUDE.md gotcha rewritten. (3) LOOP-VAR GLOBAL-FN SHADOW (fd3a7003): a for-of `const patternText` colliding with core.ts's exported function resolved through globals in arg position (moduleSpecifiers.ts:929) — global-colliding Identifier locals register into currentParamBindingNames. (4) MAPPED-TYPE UNKNOWABLE KEYS (78001791): a union constraint with any non-string-literal constituent bails to anyType — partial enumeration manufactured excess TS2353 (utilities.ts:9042 createComputedCompilerOptions). (5) ANNOTATED-DECL SKIP in nearestPrecedingObjectLiteralDecl (9a55bd1a): TS7053 keyed off the initializer literal of an ANNOTATED decl (commandLineParser.ts:3466). (6) NULLISH-MIRROR OVERLOAD PAIRS, M3.4 (8e6ec34c): tsc's instantiateType idiom — first-applicable selection, sound because the shape test rejects any difference beyond threaded nullishness; + typeNodeDefinitelyNonNullish's barrel-import globals fallback (interface/class/enum only, never TypeAlias per the round-443 conflation trap) — cleared checker.ts:21170 TS18048. (7) DEFERRED-POSITION RECURSIVE UNION ALIAS CYCLE-BREAK (83f27992): WrappedExpression<T> depth-bailed → spurious TS2589 (utilities.ts:5553); indexed-access/mapped members keep the eager bail (recursivelyExpandingUnionNoStackoverflow pin caught the ungated cut). (8) TS 5.5 INFERRED TYPE PREDICATES, bounded slice (cac642c6): `filter(helpers, helper => !helper.scoped)` infers `helper is UnscopedEmitHelper` from the boolean-literal discriminant (factory/utilities.ts:713); TRAP: LiteralType true/false literals parse as Identifier nodes. (9) IDENTIFIER-RHS ASSIGNMENT NARROWING, M3.4: `result = node` filters the antecedent union by the RHS's resolved NON-UNION type (esDecorators.ts:1485; the union-RHS variant over-narrowed through the lenient relation — caught by a local pin, gated pre-commit). NEXT (compiler @ 61, ~15 real): big-objlit M3 (moduleNameResolver.ts:1300/emitter.ts:1277/checker.ts:6640); program.ts:1220 checkDefined return-type resolution; typeSerializer.ts:603 switch-narrowed generic clone chain; `string → __String` branding; esDecorators.ts:1309; cast-instantiation identity (parser.ts:9581/factory/utilities.ts:1688); core.ts:2135 IIFE-const inference (hard)._

_Round 462 (2026-07-10, Phase 17 — probe-driven burn-down: SIX fixes; FOUR needed instrumented whole-program probes (XPROBE prints + a stack-trace probe on the Diagnostic constructor; minimal repros clean/misleading). Dashboard: compiler 79 → 72 (−7; TS2322 19 → 15, TS2345 4 → 2, TS2339 3 → 2), services 165 → 154 (−11 cross-profile); suite 9,782 → 9,794 (+12 local across 5 new test files, 0 regressions); 5 fix commits (a4c8b186 / 9810df0f / f6e2456f / e2e99396 / 99181256). (1) CALL-ARG NARROW-DOWN GATE (a4c8b186, −2): `n <: declared` under-accepts — tsc legitimately narrows OUTSIDE the declared hierarchy (PropertyNameLiteral ∋ JsxNamespacedName ⊄ Expression); the gate now also accepts a narrow that makes the PARAM relation pass (utilities.ts:4066 ×2, probe: refines=false/matchesParam=true). (2) OBJLIT VALUE CONTEXTUAL NARROW-DOWN (9810df0f, −1): return-path objlit context (union → sole non-nullish object member, mirrors B83.4g) + PropertyAccess values narrow when ctx accepts and raw fails (utilities.ts:7458); PERF LANDMINE fixed pre-commit: an ungated per-value flow walk cost +352% self-compile (28.9→130.5 s), now gated behind raw-fails-the-cached-ctx-relation. (3) PREFIX-PATH TAIL SUBSTITUTION (f6e2456f, −1): the round-424 prefix-guard branch substitutes the resolved NON-UNION tail (utilities.ts:5085 `parent.parent` → Declaration); measured gates: union tails keep drop-nullish only (ungated cut net +1: checker.ts:45139/binder.ts:2498; dropping the strip regressed transformers/utilities.ts:643). (4) AMBIGUOUS BLOCK LOCALS IN THE PROPERTY-ACCESS PASS (e2e99396, −1): the round-460 anyType rule now runs at that pass's three body-entry sites — moduleNameResolver.ts:2823 `result.value` read the WRONG same-body block's `const result` (RMWFLL vs the recursive SearchResult; two probes falsified the inherited-entry hypothesis). (5) CONSTRAINT-SHAPE FOREIGN-TP GATE (99181256, −2): a callee TP sharing the enclosing fn's TP NAME was claimed own (getUpToDateStatusWorker<T extends BuilderProgram> vs forEachKey's unconstrained T) → same-named TP with mismatched constraint SHAPE is now foreign, scoped to currentTypeParamDecls-known names (round-431e sig-own pins kept); cleared tsbuildPublic.ts:1778 AND transformer.ts:271 (memoize objlit — same collision). SCOUTED: factory/utilities.ts:713 needs TS 5.5 inferred type predicates (filter's guard overload via `helper => !helper.scoped`). NEXT (compiler @ 72, ~26 real): big-objlit M3 remnant (moduleNameResolver.ts:1300/emitter.ts:1277/checker.ts:6640); TransformerFactory ×3; inferred type predicates; program.ts:1220 checkDefined return-type resolution; `string → __String` branding; mapped-type-unknowable-keys excess bail._

_Round 461 (2026-07-10, Phase 17 — bounded FP burn-down: FIVE fixes, all GENERAL checker/flow correctness that reproduce minimally. Dashboard: compiler 88 → 79 (−9; TS2322 25 → 19, TS2345 6 → 4), services 178 → 165 (−13 cross-profile); suite 9,762 → 9,782 (+20 local across 5 new test files, 0 regressions); 5 fix commits (c863d04b / 72c3abc5 / b20a695e / f8cbd1e6 / 1f3726f2). (1) TYPEPARAM-SOURCE CONSTRAINT BAIL, M3.1 (c863d04b, −2): the round-456 reverted broad rule landed as the prescribed per-site EMISSION bail — `bareTpConstraintRelatesTo` suppresses the assignment-path TS2322 when a bare-TypeParam source's constraint chain relates (classFields.ts:1800 `currentClassContainer = node`; utilities.ts:12338 bare-T call return whose T shares the enclosing fn's TP name so the foreign-TP gate passes it). (2) ELEMENT-ACCESS REFERENCE PATHS, M3.4 (72c3abc5, −1): `getReferencePath` serializes literal-index element accesses as `recv[N]` segments so `!!decls[0].initializer` guards narrow (es2015.ts:2730); companions: exact-path ElementAccess assignment-target arms, `flowPathRoot` (splits on '[' too), `pathPrefixOf` boundaries. (3) NAMESPACE-MEMBER NESTED-FN SHADOW (b20a695e, −3): `shadowNestedFunctionNames` also consults enclosing inference-namespace exports (presence-only) — parser.ts:1865's `{ currentNode }` shorthand resolved to namespace Parser's differently-shaped `currentNode` via lookupInInferenceNamespace-before-globals. (4) CHECKDEFINED-SHAPE NON-NULLISH, M3.4 (f8cbd1e6, −1): a call returning a bare OWN-TP `T` with a `T | undefined`(/null) param proves non-nullish (program.ts:4041's `sourceFile = Debug.checkDefined(…)` at an if/else join); no-explicit-type-args gated. (5) VAR-DECL + TUPLE-SLOT NARROWING CONSUMERS, M3.4 (1f3726f2, −2): checkVarDeclAssignability gets the relation-passes-gated narrowing for named-object/union/intersection targets (parser.ts:6245 after a negative isJsxNamespacedName guard); elementAssignableToSlot narrows reference elements (builder.ts:518 `isString(old) ? [old] : old[0]` vs `EmitSignature`). SCOUTED WHOLE-PROGRAM-ONLY (minimal repros clean): transformer.ts:271 memoize member, utilities.ts:4066 isPropertyNameLiteral guard, parser.ts:9581 same-union-both-sides display, factory/utilities.ts:1688 cast instantiation identity. NEXT (compiler @ 79, ~33 real): the whole-program probe batch (instrument narrowByCallPredicate on the bench project); tsbuildPublic.ts:1778 return-path T; the big-objlit M3 family; program.ts:1220 (needs the checkDefined call's RETURN TYPE, not just non-nullishness); typeSerializer.ts:603; `string → __String` branding; utilities.ts:9042 mapped-type-unknowable-keys excess bail._

_Round 460 (2026-07-09/10, Phase 17 — bounded FP burn-down: TEN fixes — 9 dashboard wins + 1 repro-pinned capability, all GENERAL checker/flow correctness. Dashboard: compiler 103 → 88 (−15; TS2322 33 → 25, TS2345 11 → 6, TS2454 1 → 0, TS2363 1 → 0), services 194 → 178 (−16 cross-profile, measured at fix 8); suite 9,731 → 9,762 (+31 local across 10 new test files, 0 regressions); 9 fix commits (9973bf9d / 4c00e5f1 / fcec70f7 / c35e24cb / 739d16ef / f2d67ef9 / 2c03d8f9 / a0765dd6 chained-assignment-RHS narrowing (destructuring.ts:113) / f1849a01 this-param signature alignment — getTypeOfFunction zipped this-DROPPED paramSymbols against this-KEPT decl.parameters, shifting every param type by one (core.ts multiMapAdd ×2; the no-this positional zip is KEPT deliberately — it carries the round-446 binding-pattern call-site alignment; an unconditional fix regressed 19 sites)). (1) BLOCK-SCOPING-AMBIGUOUS LOCALS (9973bf9d, −3): a name with ≥2 block-scoped let/const declarations in ONE function body registers anyType at body entry (`applyAmbiguousBlockScopedLocals`/`ambiguousBlockLocalNames` — the flat first-decl-wins currentLocalTypes cannot know which block's binding a later read refers to; program.ts findSourceFileWorker's three `const file`); ALSO cleared checker.ts:11321 + utilitiesPublic.ts:991, previously mis-attributed to whole-program conflation. (2) EXHAUSTIVE-SWITCH NEVER FAMILY (4c00e5f1, −3): three different root causes — a NON-union switch subject with an enum-member-union-alias `.kind` fully covered (programDiagnostics.ts:346); a FUNCTION-BODY-local alias as `Debug.type<T>` target via the new `uniqueNestedTypeAliasByName` map (declarations/diagnostics.ts:702); lib `Exclude<T, U>` union members evaluated member-wise via the new `resolveAssertTargetTypeNode` (utilities.ts:12082 HasInferredType). (3) FLATTEN DOUBLE-ARRAY ANCHOR, M3.1 (fcec70f7, −2): a union param with a `tp[][]` member is an inference anchor — `flatten(supportedTSExtensions)` binds T from the inner element (utilities.ts:9972/9978). (4) TS2454 CAPTURED READS (c35e24cb, −1): `isAssignedAtFlow` follows `FlowStart.outerFlow` — an expression-bodied arrow's captured read sees the enclosing function's assignments (checker.ts:14106 indexInfos). (5) NESTED DESTRUCTURED GLOBAL SHADOW (739d16ef, −1): binding-pattern element names in nested blocks register anyType (checker.ts:37376's `const { start, length }` vs core.ts's `length` fn). (6) ARITHMETIC MODULE-VAR-LEAK ARM (f2d67ef9, −1): arithOperandType bails a leaked cross-file module var (program.ts's `const indent = "    "` poisoned parser.ts's local `indent` → TS2363 at parser.ts:8974). (7+8) DESTRUCTURING-ASSIGNMENT OVERWRITE NARROWING + FUNCTION-LOCAL-ALIAS PREDICATE TARGETS (2c03d8f9, −1): `({ pos, end } = refs(i))` overwrites pattern names in the narrowing walk (Flow.kt threads the enclosing BinaryExpression through the literal target arms; nested default-value patterns pinned by the sourceMapValidationDestructuringForOf corpus test which caught the first cut) + a DIRECT enum-typed switch subject exhausts to never + narrowByCallPredicate resolves function-body-local alias targets (utilities.ts:11856 SelfReferenceLocation); program.ts:1220 itself stays FP (needs cross-barrel Debug.checkDefined generic-return inference — mechanism repro-pinned). NEXT (compiler @ 88, ~42 real): program.ts:1220 checkDefined inference; destructuring.ts:113 chained assignment; es2015.ts:2730 element-access reference paths; the objlit-vs-interface returns family (parser.ts:1865/emitter.ts:1277/transformer.ts:271/moduleNameResolver.ts:1300); utilities.ts:12338/tsbuildPublic.ts:1778 = the round-456 reverted TypeParam-constraint rule needing the per-site-bail variant; `string → __String` branding._


Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `docs/history/STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `docs/history/STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
