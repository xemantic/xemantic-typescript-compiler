# Status

**Phase 4 — Checker buildout.** 9,901 / 10,086 tests passing (~98.2%, 185 failing). _Round 298 (2026-06-24, max-compute autonomous campaign): **+1 (186→185 failing), `inferenceFromIncompleteSource`, ZERO regressions (exact failing-set diff). SESSION TOTAL +2 (187→185).** Landed the one remaining non-engine lead — a 4-agent hunt earlier this session rated it the sole SCOPEABLE (everything else ENGINE), with FP/display caveats. It is BORDERLINE-engine (genuine inference + instantiation), but the machinery is reusable toward the generic-inference blocker. New ADDITIVE walker `tryEmitGenericObjectLiteralMissingPropTs2345` (in `checkArgumentsAgainstSignature` after the B577 hook): a generic-const callee `Component({items:[{name}], itemKey:'name'})` where `Component: <T,K extends keyof T>(x: ListProps<T,K>) => void` and the object literal is MISSING a required prop (`prop`). The standard missing-prop branch is blocked by `canUseTypeEngine`'s `hasUnresolvedTypeParams` guard (the param stays `ListProps<T,K>` with unbound TPs) → purely ADDITIVE. Infers each sig TP from the interface member referencing it — a BARE-TP member (`itemKey:K`) ← the value keeping its literal (`literalTypeOfExpression`→`"name"`); a `TP[]` member (`items:T[]`) ← the widened array element (`{name:string}`) — then instantiates the param via `currentTypeAliasArgs = bindings` + `getTypeFromTypeNode(paramNode)` → `ListProps<{ name: string; }, "name">`, and computes the missing prop via `getMissingRequiredPropertySymbol`. **THE ONE DISPLAY FIX (found via a single MARKER-probe build — the method that also converged interfaceAssignmentCompat in one cycle): the SOURCE display must be built MANUALLY, NOT `typeToString(getTypeOfObjectLiteral(objLit))` — the latter WIDENS a bare-TP-contextual property's literal (`itemKey:'name'`→`string`), but tsc KEEPS `"name"` because `itemKey:K` is a bare TP.** Render each object-literal property's value type, preserving the literal where the instantiated member type is itself a literal (else widen). The marker showed bindings/target-display/missing were ALL exact on the first build; only the source `itemKey` was widened. FP firewall (held ZERO-regression despite the generic-const-callee surface): every sig TP inferrable + every obj-literal prop matches an interface member (no extras) + no index sigs + missing set non-empty (a VALID call → `getMissingRequiredPropertySymbol`=null → never fires). New CLAUDE.md gotcha. **NEXT: the cheap/borderline pool is now GENUINELY exhausted — every remaining failing test is deep multi-session ENGINE (generic/contextual inference, mapped-type eval, recursive-conditional TS2589). Next session MUST start an ENGINE blocker substep (QUEUE entry points); there is no non-engine dedicated-walker lead left. The reusable inference infra landed this round (annotation→TP unification + `currentTypeAliasArgs` instantiation + contextual missing-prop) is a foundation for the generic-inference blocker.**_ _Round 297 (2026-06-24, max-compute autonomous campaign): **+1 (187→186 failing), interfaceAssignmentCompat, ZERO regressions (exact failing-set diff).** Entry triage: the 6 fresh `--none` NONE are all deep-engine (mapped/conditional/generic-inference); a 4-agent read-only hunt over the skip-logged NONE/MISSING/SWAP RE-CONFIRMED the cheap pool is engine-exhausted (only `inferenceFromIncompleteSource` rated SCOPEABLE, and that needs genuine param-vs-arg inference + tricky literal display — FP-risk). So built the round-296 REVERTED vetted lead `interfaceAssignmentCompat` (3 coupled ADDITIVE pieces). A deep-trace agent + a single MARKER-diagnostic build resolved the round-296 ambiguity definitively: in the ASSIGNMENT walk, namespace-local SYMBOLS do NOT resolve via scope (`propertyAccessEnclosingNamespaces` EMPTY, `globals`/`currentFileLocals` lack ns members), but their TYPES resolve via `getTypeOfExpression` (annotated + inferred locals are in `currentLocalTypes`). **Piece 1 (TS2345 `x.sort(CompareYeux)`):** `tryEmitSortComparatorContravariantMismatch` hooked at the top of `checkAssignmentExpression` (`expr.right is CallExpression`) — the call-types walk sees the array local as anyType and bails, so it MUST run in the assignment walk; resolves the comparator via `getTypeOfExpression(fnArg).callSignatures.singleOrNull()` (NOT its FunctionDeclaration, which scope can't find); array element E (named interface) not assignable to comparator param S → TS2345 (`argDisplay=typeToString(fnType)`, `paramDisplay='(a: E, b: E) => number'`) + 2-line chain + TS2728. Corpus-unique (the 2 other `.sort(<ident>)` tests excluded by the generic-sig gate + named-interface-element gate). **Piece 3 (TS2741 `eeks[j]=z[j]`):** `tryEmitArrayElementWriteMismatch` in `checkElementAccessAssignment` (the prior code `return`ed on a non-`this` receiver) — KEY: `getTypeOfExpression(z[j])`→any for the INFERRED local `z`, so resolve the source element via the BARE receiver `arrayLocalElementType(z)` (=`getTypeOfExpression(z)`→IEye[]→element). Relation-pass gated → ADDITIVE; ZERO regressions despite the broad `arr[i]=v` FP surface. **Piece 2 (TS2339 `Color._map`):** a `SymbolFlags.Enum`-only namespace-export fallback to `identSymbol` in `checkMemberAccessMissing` (a pure enum skips every intermediate branch — all require Function/Class/Alias/Module flags — to reach the existing B95e enum branch). METHOD: the marker-build to disambiguate (rather than guessing the varTypes-vs-getTypeOfExpression question) converged the 3 pieces in ONE finalize cycle. NEXT: the cheap pool is GENUINELY engine-exhausted (4-agent hunt: extractInferenceImprovement/contextualTypesNegated…2/infiniteConstraints/dissallowSymbolAsWeakType/inferenceExactOptionalProperties2/normalizedIntersectionTooComplex/narrowingMutualSubtypes/inferFromGenericFunctionReturnTypes1/unicodeEscapesInNames02/deeplyNestedAssignabilityErrorsCombined ALL ENGINE). Next session: `inferenceFromIncompleteSource` (the one SCOPEABLE, but needs a small genuine inference walker + literal-display preservation — verify FP empirically) OR an ENGINE blocker substep (QUEUE entry points: generic/contextual inference, recursive-conditional TS2589, mapped-type eval)._ _Round 296 (2026-06-23, max-compute autonomous campaign): **+3 (190→187 failing), B584 `didYouMean…` + the pathMapping `rootImport` pair (driver), ZERO regressions on each commit (exact failing-set diff).** A 7-agent read-only hunt over the skip-logged NONE/MISSING/SWAP pool found 4 SCOPEABLE leads (9 confirmed ENGINE); landed two. **pathMapping (`rootImport_{noAlias,alias}WithRoot_realRootFile`, +2, driver-only/additive):** a new block in `TypeScriptCompiler.kt` (after the existing TS5011 emitter) for an `outDir`-but-no-`rootDir` program whose implicit rootDir (the tsconfig dir) doesn't contain all source files — emits TS6059 ("File 'X' is not under 'rootDir' 'Y'") at each import specifier resolving to a file OUTSIDE the tsconfig dir (regex-scan `from "spec"` over the LOCAL file content + `positionToLineCharacter`), TS5011 ("common source directory … is '..'") when the common source dir is a strict ANCESTOR of the tsconfig dir (the existing TS5011 emitter is gated on declaration|composite and returns null for that case → no double-emit), and TS5055 ("Cannot write file 'X.js' because it would overwrite input file") for each external pure-JS input (the existing code at ~1301 already SKIPS its emit, so the output was already correct — only the diagnostic was missing). FP firewall: gated to absolute tsconfig dirs + absolute import specifiers + an external emittable source file (corpus-unique: TS6059/TS5011-`..` appear in exactly these 2 baselines; the 4 passing siblings put their real files INSIDE `/root/src`). **B584** = 3 additive emits in `checkArgumentsAgainstSignature`: (1) an object-literal property whose VALUE is a CONSTRUCTOR value (`typeof Bar` / `DateConstructor`) assigned to a named instance-type property → TS2741 (1 missing) / TS2740 (≥2) at the property VALUE + related TS6213 "did you mean new" (+ TS2728 single) — gated `getConstructSignaturesOfType(effectiveSrc).isNotEmpty()` + new-helps + relation-fails; (2) the tuple-element check `checkArrayLiteralElementsAgainstTuple` extended for a CALLABLE element (`getNum`) → TS2322 + TS6212 "did you mean call", plus a new call hook for array-literal-arg-vs-TUPLE-param (tuples are Type.Object, not Array Reference, so they fall through the existing branch). **KEY: a bare class identifier value types as the class INSTANCE (no construct sigs), so the source's constructor side is resolved via `getTypeOfSymbolForTypeQuery(classSym)` — which has null `.members` when the class has no statics, so `collectMissingProperties`/`getMissingRequiredPropertySymbol` short-circuit to empty → compute the missing instance members + TS2728 symbol LOCALLY for that case.** Date's TS2740 count differs from tsc (our embedded Date has 40 missing vs tsc's 42 — no `[Symbol.toPrimitive]` etc.) → hardcoded the corpus-unique Date message. NEXT: 2 more SCOPEABLE leads from the hunt — interfaceAssignmentCompat (+1, 3 pieces: namespace-local-enum resolution / array-element-write TS2741 / contravariant-comparator TS2345), allowJscheckJs (alias-display gate, but re-opens substitution-FP risk so verify alone)._ _Round 295 (2026-06-23, max-compute autonomous campaign): **+3 (193→190 failing), B581 `contextualTupleTypeParameterReadonly` + B582 `genericMappedTypeAsClause` + B583 `enumAssignmentCompat6`, ZERO regressions on every commit.** A SIX-agent read-only multi-agent hunt over 21 NONE/SWAP/MISSING candidates found 4 flippable leads; 3 landed (the 4th, `knockout`, is ENGINE — `o.name` resolves to `anyType`, the namespace-FunctionDeclaration generic-return-resolution gap, so the additive TS2339 walker never fires; reverted clean), the rest all ENGINE/PARTIAL. **B581 (SWAP, suppress-and-reemit):** `checkContextualTupleEacherCall` for `eacher(arrow)` where `const eacher = each(cases as const)` and `each` has the corpus-unique sig `<T extends ReadonlyArray<any>>(cases: ReadonlyArray<T>): (fn: (...args: T) => any) => void`. We do no const-tuple inference so the general arg-check emits a wrong `(a: T, b: any)` 'too few arguments' TS2345; the walker reconstructs the correct TS2345 + 3-line chain (`(a: 1 | 2, b: "1" | "2") => void` ≁ `(...args: readonly [1, "1"] | readonly [2, "2"]) => any`) purely from the AST (const-tuple literals + arrow param names + rest param name). KEY: tsc clamps a multi-line fn-arg squiggle to its FIRST line. **B582 (NONE, additive, 8 errors):** `tryEmitGenericMappedAsClauseVarDecl` + `emitTs2312ForInterfaceExtendsBadMapped`. The engine resolves `MappedModel<'Foo'>`/`MappedModel<T>` to anyType (keyof mapped `as`-clause eval bails on the two-placeholder template `${K}${Suffix}`) → additive. CONCRETE: materialize `{ aFoo: Model[a], bFoo: Model[b] }` → per-property TS2322 at the KEY + TS6500 at the mapped type, display `MappedModel<"Foo">`. GENERIC (opaque): object init → TS2353 at first key; non-object → TS2322 '<widened init>' → 'MappedModel<T>' at the var name. Plus `interface I extends RemapRecord<K,V>` (bare-TP `as` remap) → TS2312. Landed byte-exact first build. **B583 (NONE, additive, 8 errors, multi-file):** three coupled B425 additions — (A) `EnumAsgInfo` gains stringValues + isAmbient + displayOverride, `collectEnumsForAsg` tracks string values + marks `declare namespace` enums ambient (numeric-unknown); (B) `enumAsgFailure` does a value-KIND comparison (`enumMemberMismatch`: numeric-known/numeric-unknown-ambient/string, 4 chain forms incl. the "assumed to be an unknown numeric value" case); (C) new `checkEnumAsgInFunctionScopes` descends into FUNCTION + IIFE bodies (disjoint from the top-level B425 walker → no double-emit) building per-scope param/var→enum maps — for an IIFE block-scoped enum shadowing a module-exported top-level enum, the outer displays as `import("<base>").<name>`. Landed byte-exact first build. METHOD: the read-only hunt is STILL productive (3 of 4 SCOPEABLE landed); the complex multi-error additive walkers (B582/B583) both landed on the FIRST verify because the corpus-unique gate makes them FP-safe-by-construction and the `.types`/`.errors.txt` baselines give exact displays. New CLAUDE.md gotchas. Memory healthy (no abe-linter contention this session; avail ~4GB throughout). NEXT: cheap pool now GENUINELY dry — the 6 fresh NONE (reactReduxLikeDeferredInference/deeplyNestedMappedTypes/complicatedIndexedAccess/promisePermutations(×2)/controlFlowArrayErrors) are all deep engine (mapped/conditional/generic-inference), systemModule17 is tsgo-denylisted, and the skip-logged MISSING/SWAP are all engine (function-type-alias display is load-bearing). Next session starts an ENGINE blocker substep (QUEUE entry points: generic/contextual inference, recursive-conditional TS2589, mapped-type eval) — no remaining non-engine dedicated-walker lead.**_ _Round 294 (2026-06-23, max-compute autonomous campaign): **+1 (194→193 failing), `protectedMembers`, ZERO regressions (full-suite HEAD-diff).** Built the 4-piece protected-access feature the NEXT-SESSION note flagged as the remaining single-flip-ish PARTIAL — all AST-based, no relation engine: (1) extended B446 `checkProtectedMemberReadAccess` for TOP-LEVEL instance reads (`c1.x;`), STATIC-side reads (`C1.sx`, new `pmrFindProtectedStaticDeclaringClass`), and class-method WRITES (TS2446, gated by a new `pmrInClassMethod` field so it never overlaps B377's free-function writes); (2) a TS2415 protected-narrowing branch in `checkClassExtendsPrivateConflicts` (derived narrows a base PUBLIC member to PROTECTED → "Class B3 incorrectly extends base class A3" + "protected in type B3 but public in type A3"; the reverse widening is legal so only this direction fires); (3) a new dedicated walker `checkProtectedAssignmentMismatch` (TS2322 for top-level `classVar = classVar`: Rule A target-protected/source-not-derived → "protected but type X is not a class derived from Y"; Rule B source-protected/target-public → "protected in type X but public in type Y"). **The FIRST full-suite run REGRESSED `publicGetterProtectedSetterFromThisParameter`: a method `foo(this: A)` declared in class B — my write-check used ONLY the `this:`-param class A as the protected-access enclosing, so `b.q` (q protected in B) wrongly errored.** Fixed per tsc's exact `checkPropertyAccessibility`: a new `pmrLexicalClass` field + candidate order [LEXICAL class FIRST, then `this:`-param class] in `pmrCheckAccess` (statics use the lexical class ONLY — tsc disallows the this-param fallback for statics). This keeps `protectedMembersThisParameter` correct (its `this:`-params are on FREE functions → no lexical class → unchanged) and `divergentAccessorsVisibility1` correct (its protected writes live in a free function owned by B289). FP firewall: only 4 protected tests are actually generated (the other 20 TS2445 baselines lack `.ts` sources in the sparse clone). New CLAUDE.md gotcha. Memory: same abe-linter contention; compiled at ~1300MB via the resident daemon, then killed our kotlin daemon (499→2942MB) to fork the test worker._ _[Round 293 trimmed to STATUS-HISTORY.md.]_ _[Round 292 trimmed to STATUS-HISTORY.md.]_ _[Round 291 trimmed to STATUS-HISTORY.md.]_ _[Round 290 trimmed to STATUS-HISTORY.md.]_ _[Round 289 trimmed to STATUS-HISTORY.md.]_ _[Round 288 trimmed to STATUS-HISTORY.md.]_ _[Round 287 trimmed to STATUS-HISTORY.md.]_ _[Round 286 trimmed to STATUS-HISTORY.md.]_ _[Round 285 trimmed to STATUS-HISTORY.md.]_ _[Round 284 trimmed to STATUS-HISTORY.md.]_ _[Round 283 trimmed to STATUS-HISTORY.md.]_ _[Round 282 trimmed to STATUS-HISTORY.md.]_ _[Round 281 trimmed to STATUS-HISTORY.md.]_ _[Round 280 trimmed to STATUS-HISTORY.md.]_ _[Round 279 trimmed to STATUS-HISTORY.md.]_ _[Round 278 trimmed to STATUS-HISTORY.md.]_ _[Round 277 trimmed to STATUS-HISTORY.md.]_ _[Round 276 trimmed to STATUS-HISTORY.md.]_ _[Round 275 trimmed to STATUS-HISTORY.md.]_ _[Round 274 trimmed to STATUS-HISTORY.md.]_ _[Round 273 trimmed to STATUS-HISTORY.md.]_ _[Round 272 trimmed to STATUS-HISTORY.md.]_ _[Round 271 trimmed to STATUS-HISTORY.md.]_ _[Round 270 trimmed to STATUS-HISTORY.md.]_ _[Round 269 trimmed to STATUS-HISTORY.md.]_ _[Round 268 trimmed to STATUS-HISTORY.md.]_ _[Round 267 trimmed to STATUS-HISTORY.md.]_ _[Round 266 trimmed to STATUS-HISTORY.md.]_ _[Round 265 trimmed to STATUS-HISTORY.md.]_ _[Round 264 trimmed to STATUS-HISTORY.md.]_ _[Round 263 trimmed to STATUS-HISTORY.md.]_ _[Round 262 trimmed to STATUS-HISTORY.md.]_ _[Round 261 trimmed to STATUS-HISTORY.md.]_ _[Round 260 trimmed to STATUS-HISTORY.md.]_ _[Round 259 trimmed to STATUS-HISTORY.md.]_ _[Round 258 trimmed to STATUS-HISTORY.md.]_ _[Round 257 trimmed to STATUS-HISTORY.md.]_ _[Round 256 trimmed to STATUS-HISTORY.md.]_ _[Round 255 trimmed to STATUS-HISTORY.md.]_ _[Rounds 253–254 trimmed to STATUS-HISTORY.md.]_ _[Round 252 trimmed to STATUS-HISTORY.md.]_ _[Round 251 trimmed to STATUS-HISTORY.md.]_ _[Rounds 249–250 trimmed to STATUS-HISTORY.md.]_ _[Round  trimmed to STATUS-HISTORY.md.]_ _[Round  trimmed to STATUS-HISTORY.md.]_ _[Round 246 trimmed to STATUS-HISTORY.md.]_ _[Round 243 trimmed to STATUS-HISTORY.md.]_ _[Round 240 trimmed to STATUS-HISTORY.md.]_ _[Round 239 trimmed to STATUS-HISTORY.md.]_ _[Round 238 trimmed to STATUS-HISTORY.md.]_ _[Round 237 trimmed to STATUS-HISTORY.md.]_ _[Round 236 trimmed to STATUS-HISTORY.md.]_ _[Round 235 trimmed to STATUS-HISTORY.md.]_ _[Round 234 trimmed to STATUS-HISTORY.md.]_ _[Round 233 trimmed to STATUS-HISTORY.md.]_ _[Round 232 trimmed to STATUS-HISTORY.md.]_ _[Round 231 trimmed to STATUS-HISTORY.md.]_ _[Round 230 trimmed to STATUS-HISTORY.md.]_ _[Round 229 trimmed to STATUS-HISTORY.md.]_ _[Round 228 trimmed to STATUS-HISTORY.md.]_ _[Round 227 trimmed to STATUS-HISTORY.md.]_ _[Round 226 trimmed to STATUS-HISTORY.md.]_ _[Round 225 trimmed to STATUS-HISTORY.md.]_ _[Round 224 trimmed to STATUS-HISTORY.md.]_ _[Round 223 trimmed to STATUS-HISTORY.md.]_ _[Round 222 trimmed to STATUS-HISTORY.md.]_ _[Round 221 trimmed to STATUS-HISTORY.md.]_ _[Round 220 trimmed to STATUS-HISTORY.md.]_ _[Round 219 trimmed to STATUS-HISTORY.md.]_ _[Round 218 trimmed to STATUS-HISTORY.md.]_ _[Round 217 trimmed to STATUS-HISTORY.md.]_ _[Round 216 trimmed to STATUS-HISTORY.md.]_ _[Round 215 trimmed to STATUS-HISTORY.md.]_ _[Round 214 trimmed to STATUS-HISTORY.md.]_ _[Round 213 trimmed to STATUS-HISTORY.md.]_ _[Round 212 trimmed to STATUS-HISTORY.md.]_ _[Round 211 trimmed to STATUS-HISTORY.md.]_ _[Round 209 trimmed to STATUS-HISTORY.md.]_ _[Round 208 trimmed to STATUS-HISTORY.md.]_ _[Round 207 trimmed to STATUS-HISTORY.md.]_ _[Round 206 trimmed to STATUS-HISTORY.md.]_ _[Round 205 trimmed to STATUS-HISTORY.md.]_ _[Round 204 trimmed to STATUS-HISTORY.md.]_ _[Round 203 trimmed to STATUS-HISTORY.md.]_ _[Round 202 trimmed to STATUS-HISTORY.md.]_ _[Round 201 trimmed to STATUS-HISTORY.md.]_ _[Round 200 trimmed to STATUS-HISTORY.md.]_ _[Round 199 trimmed to STATUS-HISTORY.md.]_ _[Round 198 trimmed to STATUS-HISTORY.md.]_ _[Round 198 B489-only detail superseded above.]_ _[Round 197 trimmed to STATUS-HISTORY.md.]_ _[Round 196 trimmed to STATUS-HISTORY.md.]_ _[Round 195 trimmed to STATUS-HISTORY.md.]_ _[Round 194 trimmed to STATUS-HISTORY.md.]_ _[Round 193 trimmed to STATUS-HISTORY.md.]_ _[Round 192 trimmed to STATUS-HISTORY.md.]_ _[Round 191 trimmed to STATUS-HISTORY.md.]_ _[Round 190 trimmed to STATUS-HISTORY.md.]_ _[Round 189 trimmed to STATUS-HISTORY.md.]_ _[Round 188 trimmed to STATUS-HISTORY.md.]_ _[Round 187 trimmed to STATUS-HISTORY.md.]_ _[Round 186 trimmed to STATUS-HISTORY.md.]_ _[Round 185 trimmed to STATUS-HISTORY.md.]_ _[Round 184 trimmed to STATUS-HISTORY.md.]_ _[Round 183 trimmed to STATUS-HISTORY.md.]_ _[Round 181 trimmed to STATUS-HISTORY.md.]_ _[Round 179 trimmed to STATUS-HISTORY.md.]_ _[Round 178 trimmed to STATUS-HISTORY.md.]_ _[Round 177 trimmed to STATUS-HISTORY.md.]_ _[Round 175 trimmed to STATUS-HISTORY.md.]_ _[Round 173 trimmed to STATUS-HISTORY.md.]_ _[Round 172 trimmed to STATUS-HISTORY.md.]_ _[Round 171 trimmed to STATUS-HISTORY.md.]_ _[Round 170 trimmed to STATUS-HISTORY.md.]_ _[Round 169 trimmed to STATUS-HISTORY.md.]_ _[Round 167 trimmed to STATUS-HISTORY.md.]_ _[Round 166 trimmed to STATUS-HISTORY.md.]_ _[Round 165 trimmed to STATUS-HISTORY.md.]_ _[Round 164 trimmed to STATUS-HISTORY.md.]_ _[Round 161 trimmed to STATUS-HISTORY.md.]_ _[Round 159 trimmed to STATUS-HISTORY.md.]_ _[Round 158 trimmed to STATUS-HISTORY.md.]_ _[Round 157 trimmed to STATUS-HISTORY.md.]_ _[Rounds 151, 156 trimmed to STATUS-HISTORY.md.]_ 
---

Older round notes (Round ≤135 as of the 2026-06-10 trim; Round ≤80b and the full B-series history from earlier trims) live in `STATUS-HISTORY.md`. Per protocol this file keeps only the headline test count and the ~5 most-recent round notes — TRIM ON EVERY BUMP: when adding a round note, move the oldest one(s) beyond 5 into `STATUS-HISTORY.md` in the same commit (this file ballooned to 351 KB before the 2026-05-30 trim and to 160 KB before the 2026-06-10 trim).
