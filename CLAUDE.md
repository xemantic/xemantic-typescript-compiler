# CLAUDE.md

This file captures only what cannot be inferred from the codebase itself.

## Rules for editing this file

Both developers and AI agents are expected to add entries as they encounter surprises.

- **Add an entry** when you encounter something unexpected: a build quirk, a non-obvious constraint, a dependency gotcha, or any behavior that would surprise the next agent or developer.
- **Add an entry** when a developer flags an anti-pattern produced by AI — describe the anti-pattern and the preferred alternative.
- **Do not** add codebase overviews, directory listings, or anything discoverable by reading the source.
- Keep entries concise: one line per lesson, grouped under a heading if a theme emerges.
- **Durable-invariant test** before adding a gotcha: "Would a future agent rediscover this in under 60 seconds via Grep + reading source?" If yes, it belongs in the git commit message, not here. Every entry costs tokens on every session — the bar for inclusion is **future agents will break things if they don't know this**. Per-fix narratives (test name, specific code path, fix story) go in PLAN-PHASE-4.md session notes instead.

## Known gotchas

- `project.exec {}` is not available in Gradle 9 Kotlin DSL task `doLast` blocks — use `ProcessBuilder` directly instead.
- TypeScript compiler tests are **generated** by `./gradlew generateTypeScriptTests` into `build/generated/typescript-tests/`; this task requires the TypeScript repo to be cloned first (done automatically via `cloneTypeScriptRepo` dependency). Never edit generated files manually.
- **Deprecated features skipped in JS emit tests**: Parameterized JS emit tests for `target=ES5`/`ES3` and `module=AMD`/`System`/`UMD` are not generated (deprecated in TypeScript 6.0, removed in 7.0/tsgo). Error baseline tests for these combinations ARE still generated since TS5107/TS5108 deprecation diagnostics must still fire. The `effectiveTarget` maps ES3/ES5 → ES2015 so no downlevel transforms are needed.
- **Disabled test category**: `.errors.txt` error baseline tests are commented out in `build.gradle.kts` `generateTypeScriptTests` task — search for `TODO: Re-enable when type checker is implemented`. Re-enabling adds ~9,055 tests (see PLAN.md item 4c).
- Kotlin 2.x disallows `.` in backtick-quoted JVM method names (error: "Name contains illegal characters: .."); sanitize test function names by replacing dots with underscores (e.g. `foo_ts` not `foo.ts`). Some TypeScript test base names contain dots beyond the extension (e.g. `accessors_spec_section-4.5_error-cases`), so the entire `nameWithoutExtension` must have dots replaced, not just the `.ts`/`.js` suffix.

### Scanner/Parser gotchas

- **JSX-vs-generic-arrow disambiguation in `.tsx` files**: In `parsePrimaryExpression`'s `LessThan` branch, when `isJsxFile`, peek at `<Identifier extends X...>`. If X is an Identifier / type-keyword / open delimiter / `typeof` → generic arrow, fall through to the existing generic-arrow / type-assertion detection. If X is `/`, `>`, or `=` → JSX (boolean attr shorthand `<T extends/>` or attr=value `<T extends={x}/>`). The broader gate `<Identifier extends` (without checking what follows) regresses `parseJsxExtends2_ts`. Tightened gate fixes both `parseJsxExtends2_ts` (JSX) and `declarationEmitRecursiveConditionalAliasPreserved_ts` (generic arrow).


- **`lookAhead` vs `tryScan`**: `lookAhead` ALWAYS restores scanner state. `tryScan` keeps scanner state if callback returns truthy. Use `lookAhead` for probe-and-decide patterns; use `tryScan` for "try to parse, keep results if success."
- **`lookAhead` does NOT restore diagnostics**: The parser's `lookAhead` restores scanner position and the `token` field, but any `reportError`/`parseExpected` failures during the callback permanently add to `diagnostics`. When using `lookAhead` with complex parsing (e.g., `parseComputedPropertyName()`), save `diagnostics.size` before and trim after to discard speculative errors.
- **`reScanGreaterToken`** (splitting `>>` to `>` for nested generics) was implemented but caused 4-test net regression — left disabled in the parser. The implementation in `Scanner.kt` is correct; the issue is likely subtle interaction with `tryScan` nesting. Re-enable cautiously.
- **`getPos()` in Parser** = `scanner.getTokenPos()` (start of current token), **`getEnd()`** = `scanner.getPos()` (end of current token text). After `parseExpected()`, the scanner has ALREADY advanced to the next token — so `getPos()` is the start of the NEXT token.
- **Case clause singleLine detection**: Check `source.substring(caseStartPos, firstStmtStart).contains('\n')` to determine if statements are on the same line as `case:`. Do NOT use `scanner.getPos()` after `parseExpected(Colon)` since the scanner has advanced past the colon into the next token's trivia.
- **TS1007 related info for missing close delimiters**: Use `parseExpectedClosing()` (not `parseExpected`) when closing `)` in statement-level constructs (`if`, `while`, `with`, `do-while`). This provides `!!! related TS1007` pointing back to the opening `(`. DO NOT use this globally (in `parseExpected`) — it causes 59+ FP regressions in error recovery tests because the `openTokenStack` doesn't track delimiter types and pops incorrectly in complex error recovery scenarios. For general `parseExpected(CloseParen/CloseBrace)`, only emit TS1007 at EOF (current behavior).
- **Extending squiggle span to include `;`**: When a parser diagnostic should cover the trailing `;` (e.g. `[]: number;` → entire 14-char span), call `parseSemicolon()` first (consuming `;` if present), then use `scanner.getPrevTokenEnd()` for the span end. `getEnd()` is wrong because it returns the scanner position AFTER the next token is scanned. `scanner.getPrevTokenEnd()` = position right after the last-consumed token's text, before the next token's trivia.

- **`isStartOfType` inside `scanner.lookAhead`**: Parser's cached `token` field is NOT updated inside `scanner.lookAhead`. When checking what token follows, pass `scanner.getToken()` explicitly: `isStartOfType(scanner.getToken())`. Using the parser's `token` field inside lookAhead checks the OLD token (before the lookAhead scan), not the advanced token.
- **JSDoc nullable `?` in type context**: TypeScript's parser handles `?` before/after types as JSDoc nullable syntax (TS17019/TS17020). Our parser adds error recovery in `parseNonUnionType()`: leading `?` consumed before primary type, trailing `?` consumed with lookahead guard (only when NOT followed by a type-start token — prevents eating conditional type `?`). Bare `?` with no following type returns `any`.

- **`TemplateLiteralType` parser is shallow — `templateSpans = emptyList()` always (B65.1)**: `Parser.kt:6629-6641`'s `Backtick`/`NoSubstitutionTemplateLiteral`/`TemplateHead` branch skips over the entire template-literal type via `skipTemplateType()` and stores the raw source slice in `head.rawText`. The `templateSpans` list is ALWAYS empty — even when the template contains `${infer X}` or `${TypeName}` placeholders. Consequence: any checker walker that needs to see the AST structure inside `${...}` (e.g. `collectInferTypeNames` to scope `infer X` declarations into the conditional's trueType) gets nothing useful. Proper `${...}` parsing into `TemplateLiteralTypeSpan` with `type: TypeNode` would unblock `recursiveConditionalCrash4_ts` (TS2304 for `$Rest` use after `${infer $Rest}`) and similar tests. Out of scope for surgical fixes.

### Emitter gotchas

- **Tab preservation in error squiggles**: The error baseline formatter must preserve tab characters from source lines in the squiggle indentation — tabs stay as tabs, other characters become spaces. Don't use `" ".repeat(col)`.
- **Numeric literal property access**: `1.foo` is ambiguous in JS (the `.` is a decimal point). Emit `1..foo` when the numeric literal has no decimal point, exponent, or `0x`/`0b`/`0o` prefix.
- **Yield expression operand parsing**: Use `isStartOfExpression()` check (not `!canParseSemicolon()`) to determine if yield has an operand. `canParseSemicolon()` returns false for `]` and `)` which would cause yield to try parsing them as expressions. TypeScript checks `!hasPrecedingLineBreak() && (asterisk || isStartOfExpression())`.

### Transformer gotchas

- **Orphaned comments (erased declarations)**: Only preserve a leading comment from an erased declaration if there is a blank line (≥2 newlines) between the comment's `end` position and the declaration's `pos`. Adjacent comments are part of the declaration and are dropped.
- **CommonJS transform ordering**: Applied AFTER all other transforms. `transformToCommonJS` receives already-transformed statements. The `isModuleFile` check uses the ORIGINAL source file statements.
- **`transformToCommonJS` is CJS-only**: returns early for ESM/AMD/UMD/System outputs. Any post-process placed inside it (e.g., decorator-metadata safety wraps) won't fire for those module formats. For across-module-format post-processing, add the code to `transformPhase3`'s tail (after the AMD/UMD/System branches return, just before the final `return sourceFile.copy(statements = ...)`). Symptom of getting this wrong: code compiles clean, test still fails with the same diff, debug println never prints.
- **Constructor prologue ordering**: When inserting parameter-property initializers into an existing constructor body (no `super()` call), insert AFTER prologue directives (`"use strict"`, `"ngInject"`), not at index 0.
- **Type assertion parens**: `(<T>expr)` — the Transformer (not Emitter) must drop them. Fix belongs in Transformer because `TypeAssertionExpression` is already stripped before Emitter sees it.
- **`new (<T>call())` semantics**: `new (A())` ≠ `new A()` — after stripping the type assertion, if the constructor expr becomes a `CallExpression`, it must be re-wrapped in `ParenthesizedExpression`.
- **Static-class-field async arrow defensive capture**: For `class C { static field = async (...) => ... }` under target<ES2022, TypeScript pre-emits `var _a; _a = C;` even when the async arrow body has no `this` reference. The `__awaiter` template is conceptually `this`-binding for static-field-initialized async arrows even when the actual `__awaiter` call passes `void 0`. `staticPropsWithThis` filter in `Transformer.kt` matches this shape via `prop.initializer is ArrowFunction && ModifierFlag.Async in modifiers`. Skip the gate at your peril — removing it regresses `asyncArrowInClassES5_ts`.
- **Chained safety wrap for cross-file `declare namespace` in `design:paramtypes`**: For `f(@deco arg: A.B.C.D.E): void` where `A` is `declare namespace A { ... }` in another file (no value exports), the metadata emit must defensively wrap: `typeof (_d = typeof A !== "undefined" && (_a = A.B) !== void 0 && (_b = _a.C) !== void 0 && (_c = _b.D) !== void 0 && _c.E) === "function" ? _d : Object`. `wrapDeepQualifiedNameForMetadata` in `Transformer.kt` does this. Gate is `checker.isTypeOnlyGlobalName(baseName)` — when the root is a runtime value (e.g. `declare namespace A { export var foo }`), fall through to raw `qualifiedNameToPropertyAccess`. Temp vars `_a, _b, ..., _<max>` are hoisted at file top via `maxDeepMetadataTempCount` field tracked across the file's serialization passes.
- **Async-arrow destructuring-param capture (target<ES2017)**: When `async (...) => ...` has any `ObjectBindingPattern` / `ArrayBindingPattern` parameter, TypeScript rewrites the OUTER arrow's params to simple-identifier proxies and moves the original parameters (with destructuring) into the inner `function*` generator inside `__awaiter`. Proxies pass values via `__awaiter`'s 2nd arg (args array). Identifier params get `<name>_<i+1>`; binding patterns get fresh `_a`/`_b`/... Example: `async (dispatch, { foo }) => ...` → `(dispatch_1, _a) => __awaiter(void 0, [dispatch_1, _a], void 0, function* (dispatch, { foo }) { ... })`. Gate in `Transformer.kt`'s async-arrow path: `hasBindingPattern && !hasRestParam`. Pure-Identifier async arrows keep the existing simpler emit.
- **Optional-call downlevel needs `.call(receiver, ...)` for method/index access (target<ES2020)**: For `obj.method?.(args)` or `obj[key]?.(args)`, the downleveled form must use `_a.call(obj, ...args)` rather than `_a(args)` so the `this` binding is preserved (matches the runtime semantics of `?.()`). Gate in `Transformer.kt`'s CallExpression `questionDotToken` branch: when LHS is PropertyAccess/ElementAccess with simple-Identifier receiver, emit `.call(receiver, ...args)`; otherwise fall through to the plain `_a(args)` form.
- **Non-async arrow expression-body must scope its own hoists**: For `() => obj?.foo` (any arrow whose body is an expression containing temp-var-needing transforms), push a `hoistedVarScopes` entry around body transformation. If any temps were allocated, convert the expression body to `{ var <temps>; return <body>; }`. Without this, optional-chain temp vars hoist to the outer file scope, mismatching TypeScript's emit shape. Async arrows skip this push because `__awaiter` provides its own wrapper.

### Module detection gotchas

- **TS1147 walker covers BOTH `ImportEqualsDeclaration` AND `ImportDeclaration` inside namespace bodies (B68.1)**: `walkRequireImportInNamespace` handles all three import forms: `import = require("X")`, `import * as N from "X"`, `import N from "X"`, `import {n} from "X"`, `import "X"`. The walker bails for `declare global` and string-literal-named namespaces (those are module augmentations with different diagnostics). When extending, mirror across both branches — the gate is identical (`inNamespace` boolean threaded through ModuleDeclaration recursion).
- **TS2484 re-export conflict walker (B68.2 + B68.5)**: `checkExportConflictInNamespace` delegates to `checkExportConflictInScope` which pre-computes `Map<namespaceName, Set<exportedNames>>` per scope. Recurses into nested namespace bodies (each namespace body is its own scope with its own merged-exports map). Covers `namespace Outer { namespace M { export var x } namespace M { export {x} } }` shape as well as file-scope. Squiggle lands on the ExportSpecifier's name identifier.
- **Index signature param type validity (B68.4)**: When the index sig param type is an invalid `KeywordTypeNode` (`any`/`boolean`/`void`/etc., not `string`/`number`/`symbol`), the Parser SKIPS the TS1021 "must have a type annotation" emission and the Checker emits TS1268 instead (mirroring TypeScript — they don't double-report). The Checker walker `checkIndexSigInStatement` covers class / interface / module / TypeAliasDeclaration / VariableStatement TypeLiteral annotations.
- **`moduleDetection: "force"`** makes ALL files modules regardless of content. Both `isModuleFile()` (Transformer) and `hasModuleStatements()` (Emitter) check for this.
- **`@module: none` + `@outFile` bundling rules**: (a) auxiliary `.js` files with `import`/`export` statements are NOT bundled into the outFile output — TypeScript treats them as type-info-only / allowJs-checking-only. Phase 3 of `TypeScriptCompiler.kt` skips them via `continue`. (b) Native `import()` syntax is preserved verbatim when target≥ES2020 (no CJS rewrite). For target<ES2020, the dynamic import is rewritten to `Promise.resolve().then(() => __importStar(require(...)))` AND the `./` prefix is stripped from the require path (no module resolver runtime under `module: none`). Both pieces live in `Transformer.kt`'s `transformToCommonJS` / `buildCjsDynamicImport`.
- **`.mts`/`.mjs`/`.cts`/`.cjs` extensions** are always module files. Both functions also check for these extensions.
- **`isolatedModules: true` ≠ `moduleDetection: force`**: `isolatedModules` tells TypeScript to check each file independently but does NOT force module treatment for files without imports/exports. Adding `isModuleFile = true` for `isolatedModules` causes regressions.
- **Node16/Node18/Node20/NodeNext default to CJS for `.ts` files (UNLESS a `package.json` declares `"type": "module"`)**: `isESModuleFormat` returns `false` for plain `.ts` files BY DEFAULT — only `.mts`/`.mjs` are unconditionally ESM. The `(options, fileName)` overload consults `options.packageJsonTypes` (populated by `collectPackageJsonTypes` from multi-file source `package.json` files outside `node_modules/`), walking from the file's directory upward to find the nearest enclosing `"type"` field. Use the `options`-aware overload in any new code that determines ESM vs CJS under nodenext — the legacy `(module, fileName)` overload has NO package.json context. Map keys preserve leading `/` (absolute paths) and use `""` for the implicit root.
- **JSON files re-emitted with trailing comma stripping**: `TypeScriptCompiler.kt` strips trailing commas from JSON output using `stripJsonTrailingCommas()` since TypeScript parses and re-emits JSON, naturally removing them.
- **`esModuleInterop` defaults to `true`**: TypeScript 5.5+ deprecated `esModuleInterop: false`. Test baselines without explicit `@esModuleInterop` expect helpers (`__importStar`, `__importDefault`). Only when `@esModuleInterop: false` is explicitly set should helpers be skipped for default/namespace imports.
- **`__importStar`/`__importDefault` conditional on `esModuleInterop`**: These helpers wrap `require()` only when `esModuleInterop: true`. Without it, use plain `require()`. `__exportStar`/`__createBinding` are ALWAYS used for `export * from` regardless of `esModuleInterop`.
- **Helper ordering depends on first usage**: When both `__importStar` and `__exportStar` are needed, TypeScript emits the one used first in the output before the other. Track `importStarUsedFirst` by checking which `needsXxx = true` is set first.
- **`importHelpers: true` uses tslib**: When set, don't emit inline helper functions. Instead add `const tslib_1 = require("tslib")` (CJS) or `"tslib"` dep + `tslib_1` param (AMD), and use `tslib_1.__helperName(...)` instead of bare `__helperName(...)`. The `helperExpr()` method handles this.
- **`exprContainsDynamicImport` must mirror `rewriteCjsDynExpr`**: The detection function (used by `isModuleFile`) must handle ALL expression types that the rewriting function handles. Missing `PropertyAccessExpression` caused `import("./foo").then(...)` to not be detected as a module file, so `transformToCommonJS` was never called. Both functions must handle: PropertyAccessExpression, ElementAccessExpression, ConditionalExpression, YieldExpression, NewExpression, SpreadElement, ArrayLiteralExpression, PrefixUnaryExpression, PostfixUnaryExpression.
- **`Emitter.hasModuleStatements` must mirror `Transformer.isModuleFile` for dynamic imports**: `Emitter.hasModuleStatements` decides whether to skip the top-level `"use strict";` for AMD/UMD/System module files. It must recognize dynamic `import()` calls as module signals — otherwise a file with ONLY `import("x")` (no traditional import/export) gets BOTH the System transform's inner `"use strict"` AND a stray top-level `"use strict";` emitted before `System.register(...)`. The Emitter has its own `statementContainsDynamicImport`/`expressionContainsDynamicImport` walkers (cannot share Transformer's because they're in different files); when adding new module-signal detection to `Transformer.isModuleFile`, mirror it to `Emitter.hasModuleStatements`.
- **`resolveModuleSpecifier` intentionally does NOT try `.js`/`.jsx` extensions**: Doing so globally causes TS2459 false-positives (our CJS/`module.exports` export analysis is incomplete) and other cross-file lookup regressions. For a specific check that needs JS-aware resolution under `allowJs`/`checkJs`, add a SCOPED helper (e.g. `resolvesAsJsOrJsx` used by `checkAmbientModuleAugmentations`) rather than modifying the main resolver.
- **ALL file locals merged into globals at Checker init** (`mergeSymbolTable(globals, result.locals)` for every binderResult): This conflates script-file locals (legitimately global) with module-file locals (should require an explicit import to be visible). Consequence: in file X, an identifier exported from an unrelated module file Y appears in X's scope chain via `scope.has(name)`. This blocks fine-grained module-visibility diagnostics. Splitting "true globals" from module-file locals would unblock several tests but has broad regression risk and needs per-file scope construction.

### Cross-file infrastructure gotchas

- **`mergeSymbolTable` mutates the existing target symbol's `declarations` list**: When `target[name]` already exists, the helper does `existing.flags |= symbol.flags; existing.declarations.addAll(symbol.declarations)` — so calling it twice across the same `result.locals` corpus duplicates declarations on the SHARED symbol object. The init-block merge into `globals` already does this once for every cross-file same-name pair; any additional pass building per-file scope tables MUST NOT re-merge across binderResults via this helper. Use direct `map[name] = sym` assignment with first-write-wins for shared pools and last-write-wins for own-file overrides — no `addAll`, no symbol mutation. Failure mode is silent until a downstream check (e.g. TS6133 unused-T-param, declaration count) sees the duplicated declaration list and behaves incorrectly.
- **`isValueExport`/`isTypeOnlyExportName` cross-file lookups must NOT rely on symbol flags**: `mergeSymbolTable` pollutes the target file's `locals["X"]` symbol with flags merged in from importing files (e.g., `import { T } from "./type"; const T = 0` in file A causes /type.ts's T symbol to acquire BlockScopedVariable|Alias|ExportValue flags). Code that asks "is X exported from file Y as a value or type?" MUST scan `targetResult.sourceFile.statements` directly for declarations of X, not consult `targetResult.locals[X].flags`. Mirror pattern in `isValueExport` (Checker.kt:955) walks the AST and classifies each declaration's kind. Fall back to flag-based logic only when no direct declaration is found (ambient/re-export cases).

### Binder gotchas

- **`canMerge` must include Variable+Module**: `declare const b: T; declare namespace b {}` is valid TypeScript. The binder's `canMerge` must allow `Variable + Module` and `Module + Variable` merging, otherwise the second declaration silently overwrites the first symbol. This matters for `isTypeOnlyImportRequire` which then sees only the namespace and incorrectly elides the `require()`.
- **`canMerge` must include Variable+Interface**: `interface Symbol {}` + `declare var Symbol: SymbolConstructor;` is the standard wrapper-type pattern in lib.d.ts. Without this merge, the var silently overwrites the interface — `Symbol` resolves to anyType in type position and `sym = Sym` skips type checking. Same gap blocks any user `class B extends A` where A is `interface A {}` + `declare var A: { new(): A }` (e.g. `staticMismatchBecauseOfPrototype`). Note: enabling this merge also makes lib types like `Object`/`Promise` resolve to real Type.Interface (was anyType), exposing two latent gaps in the relation engine — see `propertiesRelatedTo` OBJECT_PROTOTYPE_PROPERTIES filter and async return-type Promise unwrapping below.
- **Ambient module symbol overwritten by import alias**: `import x = require("X")` in the same file as `declare module "X"` creates an Alias symbol that overwrites the NamespaceModule in `locals["X"]` (Alias doesn't canMerge with Module). `isAmbientModuleTypeOnly` must analyze the ModuleBlock AST directly instead of looking up `locals[name]`.
- **Module augmentation vs definition**: `declare module "X"` in a module file (has imports/exports) is an augmentation. In a script file, it's a definition. `isAmbientModuleTypeOnly` must skip module files to avoid treating augmentations (which only add types) as the full module definition.
- **`SymbolFlags.ConstEnum` is set on namespaces holding only const enums**: `bindModuleDeclaration` sets `SymbolFlags.ConstEnum or NamespaceModule` when `ModuleInstanceState.ConstEnumOnly`. Cascades up: `namespace A { namespace B { namespace C { const enum E {...} } } }` makes A, B, C all carry `SymbolFlags.ConstEnum`. So `flags.hasAny(SymbolFlags.Enum)` (= ConstEnum or RegularEnum) returns TRUE for these namespaces. Any code that distinguishes "real enum" from "namespace" must check `declarations.any { it is EnumDeclaration }` rather than relying on the flag alone.

### Type system gotchas

- **Tuple types use `Type.Object` with `tupleElementTypes` field**: `getTupleType` creates a `Type.Object` with numbered properties ("0", "1", ...), a `length` property with `NumberLiteral(n)` type, and `tupleElementTypes: List<Type>?` for display and identification. `typeToString` checks `tupleElementTypes` to display as `[T1, T2, ...]`.
- **`getTypeFromTypeReference` errorType guard**: When creating `Type.Reference` from a TypeReference with type arguments, skip Reference creation if any resolved type argument is `errorType` (unresolved type parameter). Otherwise `C2<T>` displays as `C2<error>` instead of falling back to `C2<T>` display.
- **Variable initializer inference scope**: `currentLocalTypes` is populated from unannotated variable initializers (widened) during the TS2322 walk. Must skip `null`/`undefined`/`void` initializers to avoid FPs in "declare then assign" patterns. `getTypeFromTypeQuery` (typeof) must NOT use `currentLocalTypes` — function-scoped variables shouldn't be visible in type annotation positions.
- **Type subclasses are nested inside `sealed class Type`**: To avoid name conflicts with AST TypeNode subclasses (`UnionType`, `IntersectionType`, `TypeReference`, `TypeParameter` in Ast.kt), the checker's semantic type classes are nested: `Type.Intrinsic`, `Type.Union`, `Type.Intersection`, `Type.Object`, `Type.Interface`, `Type.Reference`, `Type.TypeParam`. Use `when (type) { is Type.Union -> ... }` etc.
- **`TypeFlags` companion shadows Kotlin type names**: `TypeFlags.String`, `TypeFlags.Number`, `TypeFlags.Boolean`, `TypeFlags.Object` shadow Kotlin types inside the `TypeFlags` companion. Outside the companion, `String` refers to `kotlin.String` as normal. Inside the companion, use `kotlin.String` if you need the Kotlin type.

- **Interface call/construct signatures are `MethodDeclaration` with special names**: The parser produces `MethodDeclaration(name=Identifier(""))` for call signatures and `MethodDeclaration(name=Identifier("new"))` for construct signatures in interfaces/type literals. `resolveInterfaceMembers` must detect these and add to `callSignatures`/`constructSignatures` lists, NOT as named properties. Otherwise they appear as property "" or "new" and cause FPs in TS2430 and structural comparison.
- **Call/construct signature resolution must push enclosing interface's `typeParameters` into scope**: in `resolveInterfaceMembers`'s call/construct sig branch, set `currentTypeParamScope` to include the interface's TPs (plus any sig-own TPs from `member.typeParameters`) BEFORE resolving return type and parameter type annotations. Pre-resolve param types into `symbolTypes[paramSym.id]` so later `getTypeOfSymbol(paramSym)` returns the in-scope-resolved type after scope restoration. Without this, `T` references in `interface I<T> { (a: T): T; }` resolve through `getTypeFromTypeReference` with null scope → fall to globals → `errorType` (cached in `nodeTypes` because `cacheable = currentTypeParamScope == null`). Downstream `instantiateSignature` calls become no-ops on errorType — silent failure, no diagnostic emitted. Same fix likely needed for any other site that resolves member type annotations from inside an interface/class declaration without pushing the type-param scope.
- **Class construct signatures are static-side only**: `objectTypeRelatedTo` must skip construct signature comparison for class/interface instance types (`isClassInstance` check). Construct signatures on classes belong to the static side (typeof Class), used for `new` expression checking. Comparing construct signatures during assignment causes FPs when source class has no explicit constructor but target does.
- **`getReturnTypeOfCallExpression` uses `sigs[0]` only**: No proper overload resolution. For named interfaces with multiple call signatures, returns `anyType` to avoid picking the wrong overload. Only resolves for single-signature or anonymous function types.
- **Full Object↔Object comparison blocked by recursive types**: Opening `canUseTypeEngine` for all Object↔Object causes 39+ regressions from infinite type expansion (e.g., `interface List<T> { next: List<T> }`). Safe subset: anonymous source → named Interface target. Named→named requires recursive cycle detection in structural comparison.
- **Interface base-type resolution and declaration merging**: `getDeclaredTypeOfClassOrInterface` is called at Checker init for built-ins like `Array` BEFORE user file declarations are merged via `mergeSymbolTable`. The cached Type.Interface freezes `baseTypes` at init time, so a later user declaration like `interface Array<T> extends IFoo<T>` is missed. Fix: `resolveBaseTypesLazy` iterates ALL current declarations and is called (1) eagerly at the end of `getDeclaredTypeOfClassOrInterface` and (2) lazily in `resolveInterfaceMembers` when `baseTypes` is empty. Heritage resolution pushes the interface's own type params into `currentTypeParamScope` so `extends Foo<T>` resolves T to the enclosing interface's TypeParam.
- **`resolveGenericPropertyType` walks inheritance chain**: The helper re-resolves a property's declared type with the target's type parameters in scope, then maps via `target.typeParameters → ref.resolvedTypeArguments`. For INHERITED properties it recurses: each base is instantiated via the current ref's mapper, then the helper re-enters on the concrete base. This handles passthrough (`Array<T> extends IFoo<T>`) and specialized (`class B<U> extends A<string>`) bases uniformly — the property's declared type is always resolved in the scope where it was actually declared. Callers must pair this with `getPropertyTypeForRelation` (not raw `getTypeOfSymbol`) in elaboration paths, otherwise substituted mismatches yield no elaboration chain.
- **`resolveGenericPropertyType` method signature type params**: The MethodDeclaration branch must populate `Signature.typeParameters` (with constraints/defaults resolved in a scope containing both class + method type params, then instantiated via the class mapper). Without this, `x.bar2<string>` on `C<T>.bar2<U extends T>` with `x: C<number>` cannot emit TS2344 — the constraint would stay as raw `T` forever. Use an inner `try/finally` per-signature to restore the class-only scope before the next overload.
- **TS2345 via constraint for generic param types**: When a parameter's type is a `Type.TypeParam` with a simple constraint and the argument doesn't satisfy it, emit TS2345 with the CONSTRAINT as the displayed param type (not the type parameter's name). Rationale: the type parameter would be inferred as the arg type, which would then fail the constraint check. Example: `bar2<U extends number>(y: U)` called with `""` → "Argument of type 'string' is not assignable to parameter of type 'number'". Place this check BEFORE the `isSimpleCheckableType(paramType)` guard in `checkArgumentsAgainstSignature` and `continue` after handling — don't also fall through to the standard check.
- **`implements` clauses are NOT base types for structural comparison**: `resolveBaseTypesLazy` must skip `clause.token == SyntaxKind.ImplementsKeyword` — `implements` is a structural constraint the class must satisfy, not a source of inherited members. If included, a class `C implements I` silently "inherits" I's members even when C doesn't declare them, so `propertiesRelatedTo(C, I)` trivially passes and both TS2420 (at the class declaration) and TS2344-via-constraint (at `fn<C>()` where the type param extends I) are masked. Interface `extends` clauses DO contribute members and must NOT be skipped.
- **Static-member bifurcation: `Type.Interface.staticMembers` is the static-side mirror**: For class declarations, `resolveInterfaceMembers` populates BOTH the existing `members` table and a new `staticMembers: SymbolTable?` (Type.kt) — entries carrying `ModifierFlag.Static` go to BOTH maps. Interfaces stay null (no static side). Class-side inheritance: `class C extends B` propagates `B.staticMembers` into `C.staticMembers` alongside the regular members inheritance. Instance-vs-instance structural-comparison consumers (`propertiesRelatedTo`, `collectMissingProperties`, `getMissingRequiredPropertySymbol`) iterate `target.properties` and use `getStaticMembersOfType(target)` to skip entries whose name lives on the static side — without this filter, `class C2 extends A { }` where A has `static bar()` makes C2's instance shape spuriously include `bar`, and any structural compare against C2 (e.g. `c2 = c` where C has only `foo`) emits FP TS2741. Type.Reference defers to its `target.staticMembers`. The value-position class-identifier path (`A.bar()`) still works through unchanged `getPropertyOfType` → `members[name]` — statics remain in `members` so this isn't broken.

### Checker gotchas

- **Narrowing consumers (rounds 43-45 state)**: the flow-narrowing infrastructure (FlowCondition/FlowCall/FlowSwitchClause + applyConditionNarrowing + narrowByTruthiness/Equality/CallPredicate/AssertCall/SwitchClause + multi-level optional-chain + ?? + Boolean(x) + supertype substitution) is consumed by: (a) TS2339 via `checkPropertyAccess` (round 17.x); (b) TS2454 via `walkExprForFlowTS2454` (round 17.1+). NOT consumed by `getTypeOfIdentifier` (high-yield/high-risk per gotcha) or most TS2322/TS2345 emission sites. Future flips require wiring narrowing into those sites — but doing so risks cascading FP emission since unrelated tests assume identifier type = declared type.
- **Positive-narrow supertype substitution (round 45 iter1)**: `narrowUnionByLiteral` positive direction now SUBSTITUTES the literal for its supertype member. `s: string | null` narrowed by `s === "abc"` previously filtered to empty → never (lost precision); now returns `"abc" | null` filtered to `"abc"`. New helper `isLiteralAssignableToMember` recognizes string/number/bigint literal → base, true/false → boolean, any/unknown as universal target. Negative direction unchanged — still exact-match filter.
- **Boolean(x) call narrowing (round 44 iter16)**: `applyConditionNarrowing`'s CallExpression branch special-cases bare `Boolean(x)` call to delegate to `narrowByTruthiness` (same as direct `if (x)`). Inverted via `!Boolean(x)` works through PrefixUnary's negation chain.
- **Multi-level optional-chain narrowing (round 44 iter5)**: `applyConditionNarrowing`'s PropertyAccessExpression branch extended to walk multi-level optional chains. `if (obj?.x?.y)` truthy implies the WHOLE chain succeeded, so any intermediate path matching [name] is narrowed to non-null/undefined. Walks the chain looking for: (a) intermediate node whose expression-path matches [name] AND has questionDotToken; (b) intermediate node whose path matches [name] when outer expr is optional-chain.
- **Nullish coalescing narrowing (round 44 iter9)**: `applyConditionNarrowing`'s BinaryExpression branch handles `??` (QuestionQuestion) symmetrically to `||` (BarBar): truthy → union of both sides' truthy narrowings; falsy → chained narrowing (both falsy). Note: technically `a ?? b` falsy only means "a is null/undefined AND b is falsy", but our chained-narrowing approximation is conservative.
- **iter14-pattern flip (round 43 +1 / round 44 net-zero)**: extending a class (c) safe broadening — "function-like body recursion in an expression walker that emits class-related diagnostics" — flipped 1 test (checkPropertyInitInExpr for TS2564 in `() => class C { x = 1 }`). Round 44 iter1-iter3 mirrored the pattern to 3 more walkers (checkPropertyOverrideInStatement, checkPropertyInitInStatements ClassDeclaration member recursion, checkConstructorParamInInitializersInStatements ClassDeclaration member recursion) — all net-zero. Confirms the +1 was a single matching test case; the pattern is now uniformly applied so future TS2564/TS2301/TS2663 emissions in nested class shapes will be reached.
- **Flow-graph narrowing infrastructure (Blocker #1, partial)**: `narrowTypeFromFlow` (Checker.kt:~50280) handles FlowStart / FlowUnreachable / FlowCondition / FlowBranchLabel / FlowLoopLabel (returns declaredType — back-edge safe) / FlowAssignment / FlowCall / FlowSwitchClause / FlowArrayMutation. FlowCall applies `narrowByAssertCall` for `function assertX(x): asserts x is T` (and bare `asserts x` → exclude null/undefined). FlowSwitchClause applies `narrowBySwitchClause` for `switch (X) { case <lit>: ... }` discriminant narrowing. Both helpers use `getReferencePath(arg/switchSubject) == name` for path matching. **Mirrored** to `narrowTypeFromFlowFollowLoopEntry` (loop-aware variant) for consistency. **Gates**: callee must be a bare Identifier (PropertyAccess `obj.predFn` NOT yet supported); switch subject must be a pure Identifier/PropertyAccess chain; case expressions must be literal-typed (via `literalTypeOfExpression`); fall through to declared on DefaultClause inclusion. Narrowing is consulted from limited emission sites (TS2339 via `checkPropertyAccess`, TS2454 via `walkExprForFlowTS2454`); `getTypeOfIdentifier` does NOT consult narrowing — extending it would broadcast narrowing through all type lookups but is high-risk.
- **Walker broadening direction-of-emission rule (round 36 + round 42 iter5+iter10)**: when broadening a helper or walker's wrapper/expression-kind coverage, classify the helper first: (a) helpers whose result GATES a diagnostic emission (e.g. `isLiteralLikeExpr` gates TS1166/TS1169 — `!isLiteralLikeExpr(expr)` fires; broader = LESS emission; FN risk) vs (b) walkers that DIRECTLY emit per-reached-node (e.g. `checkTypeAsValueInStatement` directly emits TS2693/TS2708 — broader = MORE emission; FP risk) vs (c) pure subtree-visitors that just route control flow without emission semantics (safest). Round 42 iter5 demonstrated (a)-class regression: adding As/Satisfies/NonNull wrappers to `isLiteralLikeExpr` suppressed expected TS1166 on a wrapper-quoted computed property name. Round 42 iter10 demonstrated (b)-class regression: adding loop/switch/try recursion to `checkTypeAsValueInStatement` over-emitted TS2693/TS2708 in newly-reached contexts (-16 net). Class (c) walkers like `walkForOfNonIterableInExpr` (iter9) are safe to broaden freely. **Rule of thumb**: if grep shows the helper's result is consumed via `!helper(...)` for emission, broadening adds FN risk; if the walker has `diagnostics.add(...)` calls directly inside, broadening adds FP risk; if it only forwards to inner walkers with no emission, it's safe.

- **Generic type alias instantiation (B50.1)**: `getTypeFromTypeReference` for a `TypeAlias` symbol with concrete type args pushes the alias's type-parameter names → arg types into `currentTypeAliasArgs` (a side stack), then re-resolves `decl.type` fresh via `getTypeFromTypeNode` (cache-bypassed). Body-internal `TypeReference(T)` lookups check the map BEFORE symbol resolution. Recursion guarded by `typeAliasResolutionDepth` (bails at 10). **Gate**: skip FunctionType / ConstructorType / TypeLiteral-with-only-call-sigs alias bodies — those would FP-emit TS2322 against generic-function-call results whose TypeParams aren't yet inferred. `getTypeFromTypeNode`'s `cacheable` condition includes `currentTypeAliasArgs == null` so the substitution path always gets a fresh resolution.
- **Alias-display map (B50.2)**: `aliasDisplayMap[Type.id] → (aliasName, args)` registered by B50.1's substitution path AFTER `getTypeFromTypeNode(decl.type)` returns. **CRITICAL filter**: skip singleton intrinsics (anyType, unknownType, etc.) — those share ids across the corpus; registering one alias's name on anyType.id corrupts every other anyType-typed expression's display. Also skip pure-function-typed results (callSigs only, no properties/members/ctorSigs) — TypeScript unfolds those at display time (the "noAlias" indirection trick in `nestedCallbackErrorNotFlattened_ts`). `typeToString` consults the map at the top and renders `aliasName<args>` if found; `typeToStringInProgress: MutableSet<Int>` guards recursive alias chains.
- **TS2689 `class extends Interface`**: lib types like `Array`, `Object`, `Promise`, etc. are BOTH `interface X<T> {}` AND `declare const X: XConstructor` in TypeScript's full lib.d.ts. Our embedded lib only includes the interface, so the binder doesn't set `SymbolFlags.Variable`. Without an allowlist, `class C extends Array<T>` emits FP TS2689. `tryClassifyExtendsInterface` (Checker.kt) consults `BUILTIN_LIB_VALUE_INTERFACES` for single-segment lookups. Add new lib types there as they surface. Single-segment only — qualified names like `M.X` fall through to the standard symbol-flag check.
- **TS6212 "Did you mean to call?" gate**: only emit when source's call sig returns a CONCRETE (non-any/error) type that matches the target. Otherwise `<T extends ...>(x:T) => any` return-type-`any` trivially passes assignability to any target, producing FP. Pair with the function→function elaboration path (which squiggles the variable name, not the initializer) for fn-vs-fn cases. Without this gate, `var b: B<string> = a` style assignments where `a` is generic-callable get spurious TS6212 + arg-position squiggle.
- **Param-with-default optional display**: `formatParameterDecl` adds `?:` for params with `initializer != null` (in type positions). TypeScript treats default-valued params as optional in type display (`(a: string = "")` renders as `(a?: string) => any`). Applies to type-position contexts (FunctionType, ConstructorType, type-literal call sigs) — not function-implementation displays where the actual `= ""` syntax matters.
- **TS2371 binding-pattern defaults**: `({first = 0}: T)` in TYPE positions (alias body, function-type annotation, var-decl FunctionType) — the default is on the BindingElement, not the param. `reportTS2371ForBindingPattern` walks Object/ArrayBindingPattern elements recursively; emits at the element name's position. Nested patterns recurse — `({a: {b = 1}}: T)` emits at `b`'s position. Squiggle is on the binding element's name only (not the full `name = default` span).
- **TS2352 source-literal display**: extended from `null` to also `undefined`. Unwrap one ParenthesizedExpression layer (for `<T>(undefined)`). Use `expr.expression`'s `expressionTrueEnd` (the outer expression) for span — includes the trailing `)`. Diagnostic message interpolates `$sourceLit` (the resolved identifier text), so adding new source literal kinds is a one-line filter change.
- **Optional-param TS2345 display widening**: under strictNullChecks, for Type.Reference param types, append ` | undefined` to the display when the param has `questionToken`. Skip for simple primitive params (`a?:string` displays as `string`, not `string | undefined` in TS2345 baselines — only Reference types get the suffix). Narrowing the gate to Type.Reference avoids regressing simple-primitive optional params like `function foo(a?: string)` which display as plain `string`.
- **TS17011 `super(super.X())`**: in derived class constructor's first super(...) call, walk each arg expression and emit TS17011 at the `super` keyword position for any `super.X` PropertyAccess found. Mirror of the param-default (B16.4cv) and `new super(...)` (B16.4cw) patterns. Three TS17011 emission sites exist; all share the convention of squiggling the `super` keyword (length 5).
- **Contextual typing in `checkPropertyAccessInExpr`**: The second-pass property-access walker uses a `contextualType` field to propagate expected types through `CallExpression → ObjectLiteralExpression → PropertyAssignment → ArrowFunction`. At the ArrowFunction branch, contextual sig params populate `currentLocalTypes` for un-annotated params (used by TS2339 checks on their `.prop` accesses inside the body). Always clear `contextualType` before walking the function body so the body doesn't inherit an outer context. The `FunctionExpression` branch must also create its own scope and shadow outer-scope bindings for un-annotated params — otherwise outer `(s: string)` leaks into inner `function (s) {}`.
- **TS2339 primitive apparent-type lookup**: In the local-fallback branch of `checkSinglePropertyAccess`, when the raw type is a primitive (string/number/boolean), use `getApparentType` to resolve to the wrapper interface, but track `displayTypeOverride = rawType` so the diagnostic displays the primitive name (`string`, not `String`). Also tighten the number-index-signature bail-out to require numeric-looking property names — but only when `displayTypeOverride != null`, otherwise `Array.isArray` static access on the Array instance type wrongly bails.
- **B69.11 literal-inferred TS2339 gate must compare types, not check `currentLocalTypes.containsKey`**: `currentLocalTypes` is populated by BOTH file-level var-decl TS2322 walks AND function/arrow param scope tracking. A simple `containsKey(identName)` check can't distinguish "file var pre-cached in currentLocalTypes" from "function param shadows file var". The correct shadow detection compares the cached type with the file-symbol's rawType: `currentLocalTypes[name] !== rawType && currentLocalTypes[name] !== getWidenedLiteralType(rawType)`. When they differ, an inner annotated param shadows; when they match, the cache entry IS the file var (no shadow). Failure mode of the naive `containsKey` check: regresses functionOverloads43_ts (destructured `[x]` param shadows file-level `var x = foo(...)`) and similar shadowing cases.
- **B69.12 enclosingNsShadow walker (namespace-local var)**: `namespace M2 { var M1 = 0; class B extends M1.A { } }` requires the local `var M1` to shadow the outer namespace M1 within M2's body. Before consulting `globals[identName]` in `checkMemberAccessMissing`, walk `propertyAccessEnclosingNamespaces` innermost-first for a Variable (NOT Module/Class) symbol matching identName. If found, use it as `enclosingNsShadow` and bypass the parent==null gate in the literal-inferred check (namespace-internal vars have non-null parent). Mirrors TypeScript's lexical scoping: the var binds to the variable, not the outer namespace.
- **B69.11 TS2872 arrow-body suppression**: TypeScript flags TS2872 for `var n3 = 5 || {}` but NOT for `(s: number) => 1 || foo()` (the literal in arrow expression body is a default-value pattern). Implement via `inArrowExprBody: Boolean` checker flag saved/restored in `checkAlwaysTruthyInExpr`'s ArrowFunction expression-body branch. Numeric-literal LHS of `||` only emits TS2872 when `!inArrowExprBody`. Block-body arrows are excluded (function body has its own statement-walker semantics).
- **LinkStore pattern for `symbol.target`**: Import alias targets are stored in `CheckerState.symbolTargets` (checker-local side map), NOT on `Symbol.target` directly. Use `getSymbolTarget(symbol)` / `setSymbolTarget(symbol, target)` helpers — never write `symbol.target = X` in checker code. This keeps binder output immutable for parallel checking. The `getSymbolTarget` helper falls back to `symbol.target` for backwards compatibility.
- **TS2322 property elaboration chain format**: Single-level mismatch: `"  Types of property 'x' are incompatible."`. Nested path (2+ levels): `"  The types of 'x.y' are incompatible between these types."`. Both followed by `"    Type 'A' is not assignable to type 'B'."`. The `getPropertyElaborationChain` recursively finds the deepest incompatible property. Hooked into `checkVarDeclAssignability`, `checkAssignmentExpression`, `checkPropertyInitAssignability`, and `checkReturnAssignability`. Must skip when function→function (handled by `getFunctionMismatchElaboration`) or union source (handled by constituent elaboration).
- **Relation cache cycle-break invalidation**: When `checkTypeRelatedTo` encounters a cycle (pair already in `relationComparisonStack`), it returns `true` speculatively. The `relationUsedCycleBreak` flag tracks this. Speculative `true` results must NOT be cached — only `false` results and non-cyclic `true` results are safe to cache. Otherwise mutually recursive types (e.g., A↔C, B↔D) get incorrect cached `true` results. `getPropertyElaborationChain` has its own cycle detection via `state.elaborationStack` and prefers leaf (non-Object) mismatches over recursive property paths.
- **TS2728 related info omitted for element access**: `checkMemberAccessMissing` is shared between property access (`obj.prop`) and element access (`obj["prop"]`/`obj[0]`). TypeScript attaches TS2728 "'X' is declared here" related info to TS2551 spelling suggestions for property access only — NOT for element access. Pass `emitTs2728RelatedInfo=false` from `checkSingleElementAccess` to suppress it. Element access squiggle: `StringLiteralNode` uses `pos` + `(rawText?.length ?: text.length) + 2` (includes quotes); `NumericLiteralNode` uses `pos` + `text.length`.

- **`isSymbolTypeOnly` for `declare namespace` with value exports**: A `ModuleInstanceState.NonInstantiated` namespace (from `declare namespace`) is NOT type-only if it has value exports. Check `symbol.exports?.values?.any { it.flags.hasAny(SymbolFlags.Value) }` — if true, the namespace is a runtime value shape even though it has no IIFE. This matters for `export = a` where `a` is a `declare namespace { export const x }`.
- **`node.end` overshoots by one token**: In this AST, `node.end` = `scanner.getPos()` AFTER calling `nextToken()`, so it includes the start/end of the NEXT scanned token — not the true end of the node's text. To compute squiggle length, use `expressionTrueEnd(expr)` which traverses the rightmost leaf: `PropertyAccessExpression → name.pos + name.text.length`, `StringLiteralNode → pos + rawText.length + 2`, `NumericLiteralNode → pos + text.length`, `Identifier → pos + text.length`. Never use `node.end - node.pos` for span length in diagnostics.
- **`ArrowFunction` block body span — prefer `closeBracePos + 1` over `body.end - 1`**: For arrow functions with block bodies, `body.end` is set after the parser advances PAST `}` into the next token's whitespace, so `body.end - 1` lands at the space/trivia AFTER the `}` (one char too far). `Block.closeBracePos + 1` gives the exact position just past `}`. Same pattern as `ObjectLiteralExpression`. Symptom: TS2345/TS2769 squiggle over an arrow-function arg is one char too wide when the source has trailing whitespace between `}` and the next token (e.g. `c.fn(1, (x) => { ... } );`). Applied in `expressionTrueEnd`'s `ArrowFunction` branch with fallback to `body.end - 1` when `closeBracePos < 0` (synthetic blocks).
- **Merged enum autoValue reset**: Each enum declaration block resets auto-increment to 0. `computeEnumSymbolValues` must have `var autoValue = 0.0` INSIDE the loop over declarations, not outside — otherwise merged enums get wrong values (e.g., `Enum1 { A0 = 100 }; Enum1 { A }` → A should be 0, not 101).
- **Nested enum value computation**: `computeAllEnumValues` must recurse into namespace `exports` to find nested const enums. Top-level `result.locals` only has the outer namespace symbols.
- **`resolveAlias` cycle detection**: Import aliases can be circular (import shadowing, re-exports). Must use a `visited: MutableSet<Int>` parameter to prevent StackOverflow.
- **Nested QualifiedName resolution**: `import I = A.B.C.E` creates a QualifiedName where `left` is another QualifiedName, not an Identifier. `resolveAlias` must handle this via `resolveQualifiedName` which recurses.
- **Const enum negative values**: TypeScript does NOT wrap negative const enum values in `ParenthesizedExpression`. The Transformer emits `PrefixUnaryExpression(-, literal)` directly. `parenthesizeForAccess` adds parens only when used as property access base (e.g., `(-1).toString()`).
- **Const enum comment `*/` escaping**: `*/` inside const enum comment labels must be escaped to `*_/` to avoid prematurely closing the `/* ... */` comment.
- **`isolatedModules` and checker const enums**: When `isolatedModules` is true, do NOT use the checker for const enum inlining — the checker has cross-file info that shouldn't be used. Only use local `collectConstEnumValues`.
- **Circular type resolution StackOverflow**: `getDeclaredTypeOfClassOrInterface` must store the type in `declaredTypes` cache BEFORE resolving base types, otherwise `class A extends B` + `class B extends A` causes infinite recursion. Similarly, type alias resolution needs a sentinel cache entry (`errorType`) before resolving the aliased type. Always wrap `getTypeFromTypeNode`/`getDeclaredTypeOfSymbol` calls in `try-catch(StackOverflowError)` when called from visitor passes.
- **TS2339 false positive prevention**: Check `this.prop` in class bodies AND `ident.prop` for non-this identifiers. For non-this: only check when the identifier is a class/namespace/module (static shapes), or a variable with interface type (not class type — class-typed variables may be narrowed via `instanceof`). Also check symbol's namespace exports for merged class+namespace symbols. Skip classes with base types, .js files, enums, anonymous types, union/intersection types, and RUNTIME_PROPERTIES (prototype, constructor, toString, etc.).
- **TS2339 static method `this`**: In static methods, `this` refers to `typeof C` (constructor), not an instance. Track `inStaticClassMethod` flag, save/restore across class member traversal, reset for nested classes. Check `isStaticMemberOfClass` which walks extends chain for inherited static members.
- **Namespace non-exported member access**: The binder puts ALL namespace members in `symbol.exports`, not just exported ones. To check if `M.prop` is valid from outside, use `isNameExportedFromNamespace` which checks `ExportValue` flag and scans `VariableStatement` modifiers (since the binder doesn't set `ExportValue` on variable declarations — the `export` keyword is on the `VariableStatement`, not the `VariableDeclaration`). Key exceptions: `declare namespace` (NamespaceModule) members are implicitly exported without `export` keyword; sub-namespace symbols (Module flag) are always accessible; import aliases should skip the namespace check entirely.
- **Module augmentation merging**: `mergeModuleAugmentations()` in checker init merges `declare module "X" { ... }` exports into the corresponding global symbols across files. Without this, namespace members added via module augmentation are invisible to type resolution.
- **TS2416 FP prevention — overloaded methods**: Skip TS2416 checking for methods that appear multiple times in the class (overloads) or where the base member has multiple declarations. Full overload compatibility checking is not yet implemented.
- **TS2416 method return type inference**: For methods with body but no return type annotation, default to `void` only if the body has no `return expr` statements. If the body returns a value, default to `any` (not `void`) since we can't infer the return type from body analysis. Using `void` unconditionally causes FPs for methods like `foo() { return 1; }`.
- **TS2416 type-predicate mismatch (B68.6)**: `getTypeFromTypeNode(TypePredicate)` resolves to `booleanType`, so the structural check sees both `(): this is X` and `(): boolean` as `() => boolean` and trivially passes. `checkClassPropertyOverrides` adds a separate predicate-mismatch detection: when base method's return type AST is `TypePredicate(!asserts)` and derived's isn't, emit TS2416 + chain "Signature '(): X' must be a type predicate." regardless of structural assignability. Display uses `methodSigDisplay(base)` (preserves the predicate via formatTypeForDisplay's TypePredicate case) and `methodSigDisplayWithBodyReturn(derived)` (which derives `() => boolean` for `method3() { return true }` despite our `() => any` internal inference — matching TypeScript's display).
- **`signatureRelatedTo` parameter count**: Must check `source.minArgumentCount > target.parameters.size` — if the source function requires more args than the target provides, they're incompatible. Without this, `(a, b) => void` would be incorrectly assignable to `() => any`.
- **OBJECT_PROTOTYPE_PROPERTIES filter**: Names: `toString`, `toLocaleString`, `valueOf`, `constructor`, `hasOwnProperty`, `isPrototypeOf`, `propertyIsEnumerable`. When the lib `Object` interface resolves to a real Type.Interface (after Variable+Interface merge), these appear in `target.properties`. Every "is this property missing" check needs to skip them — `propertiesRelatedTo`, `collectMissingProperties`, `getMissingRequiredPropertySymbol`, and the TS2344 missing-property loop in `checkConstraintsForTypeArgs`. Otherwise `{a:string}` source fails to satisfy `Object` because `constructor` is "missing".
- **B67.3 refinement: OBJECT_PROTOTYPE_PROPERTIES filter is conditional on source-explicit definition** (`propertiesRelatedTo` only): the filter rationale (prototype provides it) ONLY applies when source doesn't explicitly define the property. When source has its own `toString: 5`, fall through to the type-compatibility check so `{toString: 5}` vs `Object.toString: () => string` emits TS2322 with chain. Gate: `targetName in OBJECT_PROTOTYPE_PROPERTIES && !sourceMembers.containsKey(targetName)`. Other call sites (`collectMissingProperties`, etc.) keep the unconditional filter — they check "is this property missing FROM source", so the source-defined case is impossible there.
- **Primitive→Object via apparent type in `structuredTypeRelatedTo`**: `string` → `Object` is assignable in TypeScript via apparent type (string's apparent type is `String` wrapper, which extends `Object`). Adding the Variable+Interface merge makes Object resolve as a real interface, so `propertiesRelatedTo` is now reachable but rejects `string` (Type.Intrinsic, not Type.Object). Recover by recursing on `getApparentType(source)` when target is `Type.Interface && !is Type.Reference` and source is a primitive — but skip when target is itself a wrapper interface (Number/String/Boolean/Symbol/BigInt) so wrapper-to-mismatched-primitive still emits TS2322. Skip Reference targets too: `Promise<number>` would otherwise let `Number → Promise<number>` pass via Number-extends-Object, hiding real async-return mismatches.
- **Async function return-type Promise<T> unwrap**: `async function f(): Promise<number> { return 1 }` is valid — the `1` is implicitly wrapped. `checkReturnAssignability` must consult `inAsyncFunctionBody` (set by `checkFunctionBody` when caller passes `isAsync = ModifierFlag.Async in modifiers`). When in an async body and the declared return type is `Type.Reference(target.symbol.name == "Promise", [T])`, also try assignability against `T` — return success if either passes. Without this, the same Variable+Interface merge that lets Promise resolve as a real interface causes spurious TS2322 on every awaited primitive return.
- **`Type.Reference` interning**: Construction goes through `getOrInternReference(target, args)` (CheckerState `referenceCache` keyed on `target.id|arg1.id,arg2.id,...`). Identical instantiations share an instance and `Type.id`, which is what the id-based cycle detection in `relationComparisonStack` relies on. Do NOT call `Type.Reference(...)` directly in new code.
- **`isDeeplyNested` heuristic on parallel target stacks**: `relationSourceTargets`/`relationTargetTargets` track `target.id` of every `Type.Reference` source/target on the active comparison stack. When the same target appears 5+ times on its stack, `checkTypeRelatedTo` short-circuits to `true` (cycle break). Catches infinitely-expanding generic comparisons like `A<T> { x: A<()=>T> }` vs `B<T> { x: B<()=>T> }` whose Refs grow unboundedly and never re-occur identically. The same-target arg-shortcut (step a) ALSO defers to structural on re-entry (`countOccurrences > 1`) so the inner cycle-break can fire instead of an eager arg-mismatch `false`.
- **TypeParam vs TypeParam relates via apparent types**: `structuredTypeRelatedTo` returns `true` when both sides are `Type.TypeParam` and either is unconstrained (apparent type `{}`); when both have constraints, recursively compares those. Without this, generic-method signature comparison treats source's `K` and target's `K` (separate fresh params from `resolveGenericPropertyType`) as opaque-and-distinct → spurious TS2322.
- **`getPropertyTypeForRelation` is depth-bounded** (`relationDepth < 4`): Above depth 4 it falls back to raw `getTypeOfSymbol` because `resolveGenericPropertyType`'s MethodDeclaration branch mints a fresh `Type.Object` + `Signature` + parameter `Symbol`s per call, OOMing in deep overload-permutation comparisons before the deeply-nested heuristic can bail. The `resolvedPropertyTypes` cache (keyed on packed `(ref.id, propSym.id)`) helps for repeated identical queries but each recursion level mints a NEW outer `Type.Reference` (different `ref.id` → cache miss). Don't loosen the cap without a different allocation strategy (e.g. reusing TypeParam declarations instead of minting fresh ones).
- **`instantiateType` for `Type.Object` — partial walk after B52.3 (2026-05-25)**: As of B52.3, anonymous PROPERTY-BAG Type.Object IS walked: members get fresh Symbols with substituted types and a new Type.Object is returned. Gate: `!is Type.Interface`, `!is Type.Reference`, `symbol == null`, no `callSignatures`/`constructSignatures`, non-empty `members`. Returns input unchanged for: function-shaped Type.Object (callSigs/ctorSigs present — see `substituteOuterTypeArgsInGenericFnObject` for the in-place mutation pattern), Type.Object with a symbol, Type.Interface, Type.Reference. **Still NOT walked**: `callSignatures`/`constructSignatures` lists themselves. For function-typed properties on a generic interface (`interface I<S> { f: <T extends S>(x: T) => void }`), the inner sig's `TypeParam(T)`'s `constraint` references the outer `TypeParam(S)` un-substituted. `resolveGenericPropertyType`'s PropertyDeclaration branch routes these through `substituteOuterTypeArgsInGenericFnObject` which mutates the freshly-allocated sig's typeParameter constraints/defaults in place via the outer mapper (safe because `getTypeFromTypeNode` is uncached when `currentTypeParamScope != null`, always true inside `resolveGenericPropertyType`). If you add a similar case (e.g. callSignatures-only Type.Object property elsewhere), use the same helper. Failure mode for the SIGNATURE case is silent: per-property elaboration and TS2345-via-constraint don't fire because `paramType.constraint` is still a TypeParam, not the substituted shape.
- **`currentLocalTypes` is first-decl-wins for annotated `VariableDeclaration`**: when multiple declarations of the same name share an annotation (e.g. `var x: T` + `declare var x: T` in a script file), the FIRST resolved type stays. Each `getTypeFromTypeNode(TypeLiteral)` call produces a FRESH `Type.Object` with FRESH member Symbols, so the SAME `{...}` literal at two source positions produces two distinct member Symbols with distinct declaration positions. Without first-wins, `getTypeOfIdentifier(x)` returns whichever annotation was processed last, and TS6500 / TS2728 "declared here" hints point to the wrong source position. Matches Binder's `valueDeclaration` semantics (only set when `existing.valueDeclaration == null`).

### Overload resolution gotchas

- **`isSimpleCheckableType` NOT used for overload resolution**: For TS2769, `allArgumentsMatch`/`getFirstArgumentError`/`getFirstFailingArgPosition` check ALL types (including objects, arrays) — not just primitives. The `isSimpleCheckableType` guard is only for single-signature TS2345 checking.
- **Generic type-reference element comparison in `structuredTypeRelatedTo`**: When `source` and `target` are both `Type.Reference` with the **same target interface** (`source.target === target.target`), compare resolved type arguments directly (covariant: each source arg assignable to the matching target arg) instead of full structural comparison. Otherwise `objectTypeRelatedTo` passes trivially because member types with unresolved type params resolve to `errorType` and built-in lib methods resolve to `anyType`. **Skip the shortcut when any target arg is `void`** — the void-return-type rule (a signature returning T is compatible with one returning void) applies transitively when a type arg appears in a covariant return position (e.g. `interface B<T> { x(): T }` — `B<number>` must be assignable to `B<void>` for method-override checks to pass). Falling through to structural comparison preserves the prior trivial-pass behavior for void-arg refs. `getPropertyElaborationChain` emits the matching "  Type 'A' is not assignable to type 'B'." chain line for the first failing arg pair. Cycle detection for self-referential generics (`interface List<T> { next: List<T> }`) is handled by `relationComparisonStack` in `checkTypeRelatedTo`.
- **Generic overload guard**: Skip `checkArgumentsAgainstOverloads` when any signature has type parameters. Without generic type argument inference, parameter types resolve to `errorType` and produce false positive TS2769.
- **MethodDeclaration typeParameters**: Must include type parameters from `md.typeParameters` when creating Signatures for interface method overloads — the generic guard relies on `sig.typeParameters` being populated.
- **TS2793 conditional on implementation match**: Only emit "implementation would have succeeded" when `allArgumentsMatch(args, implSig)` returns true for the implementation signature. Otherwise the implementation may also reject the arguments (e.g., `foo({a:any}[])` still requires property 'a'). In the arity-filtered single-overload path, use the same `allArgumentsMatch` check — don't blindly attach TS2793 to TS2345.
- **Literal type widening in error display**: `getWidenedLiteralType` maps `true`→`boolean`, `false`→`boolean`, string/number/bigint literals→base types. Used in TS2769 error messages but NOT in type checking itself.
- **TS2739 vs TS2740 code selection**: TS2739 for 2-4 missing properties, TS2740 for 5+. Both use the same "Type 'X' is missing the following properties from type 'Y': a, b, ..." message format. TS2741 is for single missing property only.
- **TS1184 accessor guard**: "Modifiers cannot appear here" (TS1184) fires alongside TS1042 only for `MethodDeclaration` in object literals, NOT for `GetAccessor`/`SetAccessor`. TypeScript only emits TS1042 for accessor modifier errors.
- **`collectMissingProperties` returns empty when source.members is null**: Anonymous function types from `() => T` set `Type.Object.callSignatures` only and leave `members = null`. Treating `members ?: emptyMap()` would report every target property as "missing" (e.g. `() => void` → `[string]` → ["0", "length"] → spurious TS2739). Returning empty forces the caller's TS2739/TS2741 gate to fall through to plain TS2322 "Type '() => void' is not assignable to type '[string]'." — kind mismatch, not property-missing. Mirrors `propertiesRelatedTo`'s `source.members ?: return targetProps.isEmpty()` short-circuit.
- **Chain collapsed vs per-level form**: `getPropertyElaborationChain`'s leaf=null branch detects whether the deeper recursion produced a path-aware "The types of '$propPath' are incompatible between these types." line (COLLAPSED form — pure Object property nesting, used for `multiLineErrors_ts`-style `A2 vs A1` where `x.y` differs) vs a context-free "Type 'A[]' is not assignable to type 'B[]'." line from the same-target-ref shortcut (PER-LEVEL form — used for `typeMatch2_ts`-style `{ f2: Animal[] }` vs `{ f2: Giraffe[] }` where the chain goes through an Array element). Collapsed: return deeper directly (it already names the full path). Per-level: prepend outer "Types of property 'X' are incompatible." line and indent deeper by 2 more spaces.
- **Array-vs-array outer TS2322 suppression for array literals**: Var-decl and assignment paths must skip the outer "Type 'X[]' is not assignable to type 'Y[]'." when `init`/`expr.right is ArrayLiteralExpression` and target is a `Type.Reference` to Array. `checkArrayLiteralElementExcessProps` and related walkers already emit per-element TS2322/TS2353 at the specific offending element; the outer diagnostic would duplicate them.
- **`getTypeFromTypeNode` cache bypass for context-sensitive resolution**: `nodeTypes` cache must be bypassed when ANY context-stack is active that affects type-name resolution (`currentTypeParamScope`, `inferenceNamespaceStack`, …). A type node may resolve to different `Type` values under different contexts, and a stale cache entry (e.g. `errorType` from an earlier eager pass before the context was active) would shadow the correct context-aware resolution. Any new "lazy resolution context stack" added in the future must be checked here too — failure mode is silent (cache hit returns wrong type, downstream type checks pass falsely).
- **Lazy variable type inference must thread containing namespace**: Variables declared inside `namespace M { var x = ...; }` have `symbol.parent = M` set by the binder, but `getTypeOfIdentifier`/`getTypeFromTypeReference` don't have an AST-parent walk (no parent pointers in our AST). `getTypeOfVariableOrProperty`'s VariableDeclaration branch must `pushInferenceNamespaceFor(symbol)` before resolving annotations OR initializers so identifier/type-name lookups can consult the namespace's `exports`. Without this, namespace-internal references (e.g., `var inner; export var outer = inner`) silently bail to `anyType`, masking genuine type errors. The push covers both type annotations and initializers because both can reference namespace-local declarations.

### Inline source map gotchas

- **Inline source map uses output-to-source line matching**: `SourceMap.kt` generates VLQ mappings by matching output JS lines to source lines (not fixed offset). This handles cases where the emitter drops empty lines between statements. The Scanner tokenizes each matched line to find mapping positions.
- **VLQ mapping positions**: TypeScript maps every token start + identifier ends (pos + text.length). The `=` in variable declarations is NOT mapped. Semicolon-end (statement end) positions ARE mapped for all statement types. Keywords do NOT get end positions (only `SyntaxKind.Identifier`).
- **`mapRoot` adjusts source paths**: When `mapRoot` is a relative path (not containing `://`), source file names in the source map get `../` prefix (relative to the hypothetical map file location).
- **`sourceRoot` gets trailing `/`**: The `sourceRoot` field in the source map JSON has `/` appended: `sourceRoot?.let { "$it/" }`.

### Multi-file baseline gotchas

- **`tsconfig.json` not echoed**: The TypeScript test harness treats `tsconfig.json` as project configuration, not a source file. Never include it in the `sourceEchoes` list in `formatMultiFileBaseline`. Other JSON files (e.g. `tsconfig1.json`) ARE echoed.
- **Error baseline file ordering**: TypeScript's test harness reorders files in error baselines when the last `@Filename` file contains `require(` or `reference path`, or when `noImplicitReferences` is set. In those cases, the last file is moved to the front. This differs from JS baselines which always use `@Filename` order.
- **CRLF in test source files**: TypeScript test source files use CRLF line endings. `parseMultiFileSource` normalizes to LF early to avoid trailing `\r` characters causing extra blank lines in error baselines.
- **JS-emit file order: source-DFS vs @Filename-DFS (B52.7 + B52.9 + B52.10)**: TypeScript emits multi-file JS in two different orders depending on the kind of imports in the single graph root (the file not imported by anyone). Source-order DFS (visit the root first, then files in @Filename order) fires when the root has EITHER (a) `///<reference path>` directives — detected via `deps[root] != depsNoRefPath[root]` (e.g. `privacyTopLevelAmbientExternalModuleImport*`) — OR (b) at least one `import X = require("...")` form (`ImportEqualsDeclaration` with `ExternalModuleReference`, e.g. `isolatedModulesReExportType`). Plain ES-import-only roots (no refs, no `import = require` — e.g. `declarationsForFileShadowingGlobalNoError`, `exportStarFromEmptyModule`): @Filename input order wins, single-root DFS prepass is suppressed. `topologicalSort` takes a third arg `filesWithImportEquals: Set<String>` (pre-collected in TypeScriptCompiler.kt during deps extraction) alongside the second arg `depsNoRefPath`. Failure modes: removing either gate under-emits source order for that family; making single-root DFS unconditional regresses shadowing/exportStar (~3 tests).

### Deprecation diagnostic gotchas

- **TS5101 vs TS5102 vs TS5107**: Three different deprecation codes. TS5107 = target/module/moduleResolution deprecations. TS5101 = outFile/baseUrl/downlevelIteration deprecations (still functioning). TS5102 = removed options (out, charset, keyofStringsOnly, noImplicitUseStrict, etc.) with message "has been removed. Please remove it from your configuration."
- **TS5101 `baseUrl` migration URL**: `baseUrl` deprecation has a message chain continuation "  Visit https://aka.ms/ts6 for migration information." — requires `messageChain` in the Diagnostic.
- **`const enum` at statement level**: `parseStatement()` must check for `const enum` (not just inside `export`/`declare` contexts) — otherwise `const enum E {}` is misparse as `const` variable named `enum` + expression `E` + block.
- **ES3 target is removed (TS5108)**: When `target: "ES3"` is set, `effectiveTarget` returns `ES2015` instead — ES3 support was removed in TypeScript 5.5 and the compiler uses a modern default.

### Checker diagnostic gotchas (TS2454/TS2564/TS6133)

- **`strict: false` suppresses TS2454/TS2564**: TypeScript test baselines expect these diagnostics by default, but NOT when `@strict: false` is explicitly set. Use `options.strictExplicitlyFalse` to distinguish default-false from explicit-false.
- **Definite assignment assertion `!`**: `var x!: Type` and class property `x!: Type` skip TS2454/TS2564 checking. Check `exclamationToken` on both `VariableDeclaration` and `PropertyDeclaration`.
- **Ambient contexts**: Skip TS2454/TS2564 in `declare` namespaces, classes, and functions. A class inside a `declare namespace` is ambient even if the class itself doesn't have `declare`.
- **TS6133 non-module files**: Don't check file-level unused declarations in non-module files (no imports/exports). Only check inside namespaces, functions, blocks.
- **TS6133 write-only**: Assignment targets (`x = value`) are NOT reads. Left side of `=` operator is write-only. Compound assignments (`x += 1`) ARE reads.
- **Flow.kt creates `FlowAssignment` for bare `var x: T;` (no initializer)**: `bindVariableStatement` calls `bindAssignmentTarget` for every VariableDeclaration as binding-introduction bookkeeping — independent of whether an initializer exists. Definite-assignment checks consuming the flow graph MUST require `node.initializer != null` on a VariableDeclaration FlowAssignment to count it as a real assignment, otherwise the declaration itself masks every TS2454 candidate that walks back through it. Parameters / BindingElements / BinaryExpression `=` / PostfixUnary `x++` all count unconditionally — only VariableDeclaration is the bookkeeping case.
- **`FlowLoopLabel.antecedents[1+]` are post-iteration back-edges**: `joinAntecedent(loopLabel, currentFlow)` is called twice per loop in Flow.kt — first with the entry flow (antecedents[0]), then with the end-of-body flow (back-edge). Continue statements add additional back-edge antecedents. For walks that use OR-semantics over antecedents (e.g. "is there ANY reaching path with assignment evidence?"), naively `.any { ... }` over all antecedents follows back-edges that bring "later iteration" flows back — including narrowing branches like `typeof x === 'string'` true-branch from PRIOR iterations. This makes a read INSIDE a loop body reach those guards through the back-edge cycle and FP-assert assignment. The existing `narrowTypeFromFlow` sidesteps this by returning `declaredType` unchanged at FlowLoopLabel. For OR-semantics walks, only follow `antecedents[0]` (the loop entry). For AND-semantics walks (does EVERY path assign?), the same restriction is needed for soundness.

### Test assertion gotchas

- Avoid partial `assert("x" in result)` — always assert the full expected output.
- **Test ordering sensitivity**: Adding new checker code that increases diagnostics can cause deterministic failures in unrelated JS emit tests — tests pass individually but fail in the full suite. Always run the full suite to verify no regressions.
- **Disambig pattern for `.tsx` parser surgery**: When extending an `isJsxFile` lookahead disambig (e.g. B47.7), check what follows the disambiguating keyword (e.g. `extends`) — not just that the keyword is present. The broader gate `<Identifier extends` regresses tests where `extends` is a JSX attribute (`<T extends/>` boolean shorthand or `<T extends={x}/>` attr=value). The tighter gate requires Identifier/type-keyword/open-delim/`typeof` AFTER `extends` to fire the generic-arrow path. `/`, `>`, `=` → JSX attribute, fall back.

### TS2304 unresolved name checking gotchas

- **Kotlin property initialization order**: Properties declared after `init {}` have default values (0 for Int, false for Boolean) during init execution. **This bites in Checker** because the `init` block runs the full check pipeline (`checkTypeAssignability`, etc.) — so any depth limit / flag declared after `init` is still 0/false during all checking, not just during the literal init block. Mitigations: (a) declare the property BEFORE `init` (used for `maxCheckDepth`, `checkDepth`, `strictNullChecks`), or (b) use a companion-object `const val` (used for `NARROW_MAX_DEPTH`) — compile-time constants don't depend on instance init order.
- **KNOWN_GLOBALS coverage**: ~400 lib.d.ts names in the companion object. Missing a global causes false positive TS2304. When adding new globals, check both value and type positions.
- **KEYWORD_IDENTIFIERS**: Our parser produces `Identifier` nodes for `this`, `super`, `true`, `false`, `null`, TypeScript type keywords (`any`, `number`, `string`, etc.), and access modifiers (`public`, `private`, etc.). These must be excluded from TS2304 checking.
- **Binder scope model**: The binder only creates file-level symbol tables. Function parameters, catch variables, and block-scoped declarations are NOT in the binder's symbol table. The TS2304 checker maintains its own scope chain.
- **Test generator only processes `.ts`**: `.tsx` files are excluded from test generation (`f.extension == "ts"` in build.gradle.kts). This means JSX-related diagnostics (TS7026) are untestable.
- **`arguments` not a global**: Don't include `arguments` in KNOWN_GLOBALS — it's only available inside non-arrow functions via the `hasArguments` flag on NameScope.
- **`var` hoisting for TS2304**: `var` declarations inside nested blocks/loops are function-scoped. `collectHoistedVarNames` recursively finds them and adds to the enclosing scope.
- **`isIdentifier()` vs `isIdentifierToken()`**: Inside `scanner.lookAhead {}`, use `isIdentifierToken(scanner.getToken())` not `isIdentifier()` — the latter checks the Parser's cached `token` field which isn't updated inside lookAhead.

### TS2345 argument type checking gotchas

- **Kotlin property initialization order applies to relation instances**: `assignableRelation`, `subtypeRelation` etc. must be declared BEFORE `init {}` — otherwise they are null when `checkCallExpressionTypes()` runs, causing NPE. Same pattern as `strictNullChecks`.
- **Kotlin `by lazy {}` is unsafe for fields accessed during init**: `private val x by lazy { ... }` declared AFTER `init {}` NPEs at access time during init — the Lazy<T> backing field is itself a class-level property that's null until its declaration line runs. Symptom: `Cannot invoke "kotlin.Lazy.getValue()" because "<local1>" is null`. Fix: replace with a `private fun foo()` + `private var fooCache: T? = null` declared BEFORE init; null-check inside the function. Both `var x: T? = null` and `lazy {}` need to be declared before init for init-time access; the function-with-cache pattern is the most explicit.
- **Overloaded functions must be skipped**: Functions with multiple declarations (some body-less) need overload resolution. Detect via `symbol.declarations` having body-less `FunctionDeclaration`/`MethodDeclaration`.
- **Conservative parameter type checking**: Only check TS2345 when the parameter type is a simple/primitive type (string, number, boolean, void, null, undefined, never, bigint, symbol, and their literal variants). Checking against object/interface/union/intersection parameter types causes FPs due to incomplete structural comparison and missing generic instantiation.
- **`getArrayType` returns `anyType`**: Array types resolve to `anyType` until full generic infrastructure lands, to prevent FPs from empty `Type.Object()` comparison.

### TS2552 spelling suggestion gotchas

- **Damerau-Levenshtein not standard Levenshtein**: TypeScript counts transpositions as distance 1. Use optimal string alignment (restricted Damerau-Levenshtein).
- **Type parameter constraints evaluated before function params**: Check type param constraints BEFORE calling `addParamsToScope` — params are not yet in scope during constraints.
- **Gradle binary cache inconsistency**: When changes to Checker.kt don't seem to take effect, a full clean (`rm -rf build`) is needed.
- **Ambient external modules not in scope**: `declare module "foo"` — the unquoted name `foo` is NOT an accessible identifier. Exclude from `fileScope`.
- **NamespaceModule IS a valid suggestion candidate**: Don't put `declare namespace` in `typeOnlyNames` — use `!sym.flags.hasAny(Value or Module)`.
- **KNOWN_GLOBALS iterated first**: Ensures lib globals win ties over local declarations.
- **Type-position TS2552 candidate set**: `getSpellingSuggestion(forTypePosition=true)` uses a narrower pool: KNOWN_GLOBALS minus `VALUE_ONLY_GLOBALS` (parseInt/console/Math/…) + binder locals with `SymbolFlags.Type` + `scope.typeParamNames` + `scope.typeNames` (populated via `scope.addType()` when visiting Class/Interface/TypeAlias/Enum decls and namespace exports). Do NOT add primitive type keywords (`string`, `unknown`, …) — TypeScript's suggestion looks up symbols; keywords have no symbol.
- **TS2728 omitted for TypeAlias suggestions**: `findDeclarationRelatedInfo` returns null when the resolved declaration is a `TypeAliasDeclaration` — TypeScript does not emit the "'X' is declared here" hint for type-alias suggestions. Classes, interfaces, enums, vars/functions/imports all still get TS2728.
- **Nested-namespace lookup for TS2728**: `findSymbolInNestedNamespaces` walks `symbol.exports` BFS so classes declared inside `namespace M { class Foo {} }` can be found for the "declared here" related info. Without this, only top-level `result.locals` entries are located and TS2728 silently drops.

### Checker diagnostic gotchas (TS18004/TS1103)

- **Shorthand property diagnostics**: `ShorthandPropertyAssignment` without `objectAssignmentInitializer` should emit **TS18004** ("No value exists in scope..."), not **TS2304** ("Cannot find name..."). Handled by `checkShorthandPropertyResolved` in Checker.kt.
- **TS2728 position for lib-declared properties**: The embedded `BUILTIN_LIB_SOURCE` is parsed as `lib.es5.d.ts` and its SourceFile is retained in `builtinLibSourceFile`. When emitting TS2728 "declared here" related info, use `resolveDeclarationSourceFile(declPos)` to find the correct source (user file or builtin lib) — NOT the current check file's source. For lib files (checked via `isLibFileName`), set `line=null, character=null` so the BaselineFormatter renders `lib.es5.d.ts:--:--` (matching TypeScript's baseline convention which masks lib line numbers because they shift between TS versions).
- **`for await` in non-async (TS1103)**: `ForOfStatement` with `awaitModifier` in non-async functions gets TS1103. The related TS1356 uses `FuncRef(pos, length)` to track enclosing function position — supports named functions, anonymous function expressions, and arrow functions.
- **`verbatimModuleSyntax` suppresses const enum inlining**: When set, skip `collectConstEnumValues`, skip checker's `resolveConstEnumMemberAccess`, keep const enum IIFE bodies, and don't treat const enums as type-only.

### TS2528 multiple default exports gotchas

- **Anonymous FD/CD position**: Error at `export` keyword, NOT `function`/`class` — `FunctionDeclaration.pos` = `function` keyword, must search backwards for `export`.
- **2752/2753/6204 code exception**: When the LAST export is a `FunctionDeclaration`, codes swap: non-lasts use TS2752, last uses TS2753.
- **TS2323 vs TS2528 can fire simultaneously**: `emitTs2323 = declCount >= 2`; `emitTs2528 = hasNonDeclInline || !emitTs2323`.
- **`ExportSpecifier.name.pos` includes trivia**: Skip whitespace to get token start position for diagnostics.

### Top-level await gotchas

- **Parser `topLevelAwait` parameter**: When `module` is ES2022+, NodeNext, Preserve, or System, the Parser's `topLevelAwait` flag must be set to `true`. This sets initial `inAsyncContext = true` at the file level, enabling `await` keyword recognition at the top level. Inside function bodies, `inAsyncContext` is still reset per-function based on `async` modifier. In sync functions, `await(x)` remains a call expression (identifier).

### Const enum type-only treatment gotchas

- **Erased const enums must be type-only in ALL pre-scans**: `earlyPureTypeNames`, `pureTypeNames`, `directExportedVarNames`, and `topLevelTypeOnlyNames` — both locally-declared and imported. Without this, `export = ConstEnum` generates wrong output.
- **Binder `export { X }` must not overwrite value symbols**: For local re-exports without `from` clause, skip creating an Alias if the name is already declared as a value.
- **`.d.ts` module resolution only for relative specifiers**: Non-relative specifiers might find `.d.ts` files with augmented ambient modules, making TS2694 checks unreliable.
- **Ambient module `.d.ts` exports use inner module exports**: When resolving to a `.d.ts` with `declare module "X" {}`, use that module's exports, NOT file-level locals.
- **Default import resolution chain**: Resolve `import Foo from "./mod"` in order: (1) `locals["default"]`, (2) `ExportAssignment` (not `isExportEquals`), (3) `export { X as default } from`.

### Decorator metadata type serialization gotchas

- **`null`/`undefined`/`never` → `void 0`**: Serialize as `VoidExpression(0)`, not `Object`. `never` always excluded from unions; `null`/`undefined` only filtered when `!strictNullChecks`.
- **Numeric enum type serialization**: `E.A` (QualifiedName) and plain `E` where E is a numeric enum → `Number`. String enums → `Object`.
- **Default import metadata safety wrapper**: For `db_1.default.Foo`, TypeScript wraps it: `typeof (_a = typeof db_1.default !== "undefined" && db_1.default.Foo) === "function" ? _a : Object`. Post-process `__metadata` args AFTER `rewriteIdInStatement`.
- **`export type { Foo }` / `export { TypeAlias }` make imports type-only**: Cross-file type-only detection. Check `checker.isTypeOnlyExportName` and `checker.isValueExport` during pre-scans.
- **`resolveModuleSpecifier` for absolute-path test files**: Use `fileBase == "/$baseName"` (not `endsWith`) for relative specifiers where `fileBase.startsWith("/")`.
- **`isValueExport` returns `true` (safe default)**: Only return `false` when definitively type-only (TypeAlias, Interface, non-instantiated namespace).
- **TS2741 cross-file `import("X")` qualification**: When source and target are different `Type.Interface` instances sharing the same display name (e.g. `export interface F` in `/a.ts` and `/b.ts`, both accessed as `import * as A from './a'` / `B from './b'` then `let a: A.F`, `let b: B.F`), TypeScript renders TS2741 messages as `Property 'P' is missing in type 'import("src").T' but required in type 'import("tgt").T'`. Helper `getSymbolImportName` (Checker.kt) searches `binderResults.locals` by identity-equality to find the declaring file's basename (no path, no extension). Gate: only fires when displaySource == declaringDisplay AND both types are `Type.Interface` with different symbols AND both resolve to different declaring files. Does NOT fire for lib symbols (not in any binderResult's locals) or nested-namespace exports (need a deeper walk through `parent` chain).
- **Same-named `export interface` in different files MERGES via `mergeSymbolTable`**: At checker init, `mergeSymbolTable(globals, result.locals)` is called for every binderResult. When two files both `export interface F`, the second file's F is merged into the first's symbol — `existing.declarations.addAll(symbol.declarations)`. Downstream, both files' `let a: A.F` and `let b: B.F` resolve through the SAME merged symbol, and after `resolveStructuredTypeMembers` both Type.Interface instances carry the UNION of properties from all declarations. Consequence: structural disjointness checks (`b53.2`-style "no overlap" TS2367) silently never fire because the "missing" property has been merged into both sides. This is part of **Blocker #3 (Cross-file global scope conflation)** — fixing requires per-file scope construction with only own-file declarations in own-file resolution. Workaround patterns: diagnostics that compare ENTIRE display strings (TS2741 in B53.1) work because separate Type.Interface IDs survive merging; diagnostics that compare member-sets do not.
- **Lib-content (Array, RegExp, etc.) is NOT target-version-aware**: The embedded `BUILTIN_LIB_SOURCE` includes the FULL Array/RegExp/etc. surface from modern ES versions (ES2022+ methods like `at`, `findLast`, `findLastIndex`, plus all of ES2015+). Tests with `@target: es2015` or `@target: es5` expect ONLY the lib subset available at that target version — so missing-property counts like `arrayAssignmentTest4_ts`'s `Type 'C3' is missing the following properties from type 'any[]': length, pop, push, concat, and 25 more.` come out wrong (we emit `and 29 more` because we report the modern surface). Fixing this needs per-target lib subsetting infrastructure (track which lib entries apply at which `target`, filter symbol table accordingly), not the surgical token-level edits this session has been doing. Symptom: when a SWAP candidate's diff is JUST a number difference in "and N more" / "X, Y, Z, and N more", it's almost certainly this gotcha; skip those candidates without proportional infrastructure work.
- **Binder does NOT merge GetAccessor + SetAccessor into one symbol's declarations**: For `class C { get x() {...} set x(v) {...} }`, the binder creates a property symbol for `x` but its `declarations` list contains only ONE of the two accessor declarations (the first one bound). Symptom: `propSym.declarations.firstOrNull { it is SetAccessor }` returns null even when the class has a setter. **FIXED by B54.6 (2026-05-19)**: `resolveInterfaceMembers` now appends both GetAccessor and SetAccessor declarations to the same property symbol (was using `getOrPut` which kept only the first). Consumers can now `propSym.declarations.firstOrNull { it is SetAccessor }` reliably.
- **`x.x = v` write-context uses SETTER's param type, NOT getter's return type**: For accessor-pair properties, TypeScript's write-context type check uses the SetAccessor's parameter type (not the property's "main" type). Implemented by B54.5 (2026-05-19) in `checkPropertyAccessAssignment`: before resolving the property's type, look for a SetAccessor declaration on propSym and prefer its parameter type. Pairs with B54.6's accessor-pair declaration merging.
- **inferSimpleExprType deliberately skips Identifier variable-type lookup**: At Checker.kt:~59314 the helper returns null for `is Identifier -> ... else -> null` with comment "Don't look up variable types — causes FPs with enum/union targets". Multiple attempts (B54.7) to add Identifier-RHS varTypes fallback in the Identifier-target branch (`x = y` where x,y are same-base parameterized refs) initially regressed 2 tests. **Resolution (B54.9, 2026-05-19)**: combined with B54.3-narrowing (only fire when at least one type arg is a primitive — `string`, `number`, `boolean`, `bigint`, `symbol`, `void`, `null`, `undefined`, `any`, `unknown`, `never`), B54.7 is now safe to re-enable. The regression was from `class C<T> { private x: T }; C<X>` vs `C<Y>` (where X, Y are structurally identical interfaces) and `Promise<Foo>` vs `Promise<Bar>` (covariance). Both cases have named-type args (not primitive), so the narrower B54.9 gate excludes them. Same-base + primitive-arg mismatches like `A<string>` vs `A<number>` still fire correctly.
- **Strict-generic-checks for varTypes-based path (B55.x, 2026-05-19, +2 flips)**: The varTypes-based assignment check now emits TS2322 for `Foo<T>` vs `Foo<U>` (different TypeParams) via the same-base-different-TypeParam gate in `isAssignableTo`. `isAssignableTo` takes an optional `typeParams: Set<String>` param; when both args are different names in that set, returns false. emitTS2322 emits TS2208 "type parameter might need an `extends X` constraint" related info via the new `currentTypeParamDecls: Map<String, TypeParameter>` checker-instance field (populated at function-body and class-body entry). NOTE: This covers ONLY the varTypes path (function-body / method-body / class-body assignment checks). The Type-engine path's `structuredTypeRelatedTo` at Checker.kt:~55963 still returns `true` for distinct unconstrained TypeParam pairs via apparent-type compatibility — a separate piece that would require more careful verification against `infinitelyExpandingTypes4` and instantiated-Type.Reference comparisons.
- **`new C()` defaults TypeParams to `unknown` (B56.1, 2026-05-19, +1 flip)**: `getReturnTypeOfNewExpression` at Checker.kt:~44500 now instantiates `Type.Reference(C, [unknown, unknown, ...])` for `new C()` calls where C is a generic class with no explicit type args AND no constructor args to infer from. TypeParams with declared defaults use those instead of unknown. Gated on strictNullChecks. Pairs with B54.x stack: `var x = new C(); var r = x.x; x.x = r` now sees correct A<unknown> vs A<string> mismatch and emits TS2322. **Not** implemented for cases where `new C(args)` has args but inference fails — those still return un-instantiated calleeType. B56.3 (broader gate when newArgs non-empty) and B56.4 (broader when no Constructor decl) both regressed 1 test each — un-instantiated calleeType is structurally compatible with some targets in ways that C<unknown> isn't. The conservative gate is load-bearing.
- **`inferTypeArgsFromConstructorCall` is narrow** (Checker.kt:~43994): only matches when a constructor's parameter type annotation is EXACTLY a bare `TypeReference` whose name matches a TypeParam (no own typeArgs). Doesn't infer through `Array<T>`, `(x:T)=>U`, `{v:T}`, or other nested shapes. The function returns null if ANY TypeParam fails to match — there's no partial inference. Symptom: tests where `new C(arg)` should work via inference but C's constructor has `arg: SomeShape<T>` or similar nested form just fall through to the un-instantiated calleeType. Extending this is the natural next step for any "new-expression inference" work; the existing 17.31e function-arg inference (`tryInferSingleTypeParamFromArgs` at Checker.kt:~43925) is richer and could be a reference.
- **TS2589 emission via depth-bail flag (B57.1, 2026-05-19, +1 flip)**: The generic-alias substitution path in `getTypeFromTypeReference` (Checker.kt:~41716) bails at `typeAliasResolutionDepth >= 10` and sets a checker-instance flag `deepInstantiationBailed`. `buildFileLocalTypeMaps` saves/resets/checks the flag around each symbol's type resolution. On bail it emits TS2589 at the relevant TypeNode's position (variable annotation OR alias body) via `emitTs2589AtTypeNode` helper that trims trailing whitespace/punctuation to land at the closing `>`. **B57.1b** gates the substitution to skip when any arg fails its TypeParam's constraint — prevents FP TS2589 on `Foo<"false", {}>` when `T extends "true"` is unsatisfied (TS2344 fires elsewhere). NOT implemented for: parameter annotations, return type annotations, expression contexts. Mapped-type recursion `{ [P in K]: N<T, K> }[K]` doesn't trigger the substitution-path recursion (uses a different code path), so tests like `recursivelyExpandingUnionNoStackoverflow_ts` still fail — extending to those is a separate piece.
- **B57.1b constraint-check gate has broader impact beyond TS2589**: The pre-recursion constraint check in `getTypeFromTypeReference`'s substitution path also prevents the recursive alias body from being evaluated when args fail constraint. This means `currentLocalTypes`/cached types for `Foo<"false", ...>` resolve to `errorType` instead of a partially-expanded structure. Downstream effects on TS2322 / TS2345 diagnostics that depend on the resolved type should be considered when extending this gate. Currently scope-bounded to the depth-bail-emit case and has zero regressions in the 10078-test suite.
- **Type.TypeParam instances partial-interning (B59.1+B59.2, 3 of 10 sites)**: At Checker.kt:~49312, `getTypeFromFunctionTypeNode`-style sites do `Type.TypeParam()` per signature resolution. This means the same source-level TypeParam (e.g. `<U>` in `function qq<U>(x: T1<U>)`) gets a NEW Type.TypeParam instance each time the function's signature is resolved (and signatures get resolved multiple times during checking — once at buildFileLocalTypeMaps init, again at each assignment-check, again at each call). Downstream caches keyed by Type id (like B58.3's substitutionResultCache) miss on subsequent calls because the resolved args have fresh ids. **B59.1+B59.2** added `typeParamInternCache: Map<Int, Type.TypeParam>` (keyed by AST position) and applied at 3 creation sites: function-signature path (49321), getTypeOfFunctionExpression (43811), symbol-typeParam resolution (49888). Other 7 sites still mint fresh. The interning is opt-in per call site — extending to all sites needs per-site verification (some sites need DISTINCT instances per call, e.g. interface call signatures where each sig has its own TypeParam set that shouldn't collide).
- **errorType displays as 'any' in typeToString (B58.1)**: `Type.Intrinsic("error")` (the errorType singleton) renders as 'any' in diagnostic messages — matches TypeScript's display convention. Other Type.Intrinsic instances (anyType, stringType, etc.) still render via `intrinsicName`. Affects any diagnostic that displays types containing errorType after substitution failure (e.g. recursive aliases that hit depth bail mid-substitution).
- **Tuple-aware elaboration chain (B58.2)**: When source and target are both tuple types (Type.Object with `tupleElementTypes`) with the same arity, `getPropertyElaborationChain` emits `"Type at position N in source is not compatible with type at position N in target."` instead of the generic `"Types of property 'N' are incompatible."` used for plain objects. Matches TypeScript's tuple-mismatch chain convention. Iterates positionally over `tupleElementTypes` and short-circuits on first mismatch.
- **Recursive-alias OUTER type isn't always in aliasDisplayMap**: For `function qq<U>(x: T1<U>)` where `T1<T> = [number, T1<{x:T}>]` (recursive alias), the OUTER substitution DOES register an entry, but `typeToString` is later called with a DIFFERENT Type.Object id (verified via debug). Trace shows registration happens for ids 96, 93, 90, ... down through the recursion stack, but the type instance that `typeToString` receives in the assignment-check diagnostic has id 99 — a fresh tuple created by a subsequent re-resolution. B58.3 added `substitutionResultCache` to intern by `(symbol.id, args-id-fingerprint)`, but the cache misses on subsequent calls because the RESOLVED ARGS have different ids per call too (TypeParam(U) gets a different id each invocation — Checker.kt:~49312 creates a fresh `Type.TypeParam()` per function signature resolution). B58.4 attempted structural-id lookup but failed because element ids differ recursively. The actual fix needs either: (a) intern TypeParams by their symbol identity at Checker.kt:~49312 (so U always has the same id across function-signature resolutions), (b) recursive structural equality with cycle break in typeToString lookup, or (c) registering an `Type → (name, args)` map per assignment-check call. Direction (a) is the smallest scope fix but risks regressing many tests that rely on fresh TypeParam instances; needs careful sandboxing. Out of scope as a quick fix.

### TS2454/TS2564 gotchas

- **`any` type skips checking**: Variables/properties typed as `any` skip TS2454/TS2564.
- **`var` declarations**: TypeScript DOES check `var` for TS2454 (when strict) — only `any`-typed vars are skipped.
- **`ExportAssignment` is a use site**: `export = Foo` must be checked in `checkUsesOfUninitialized`.
- **Abstract classes still need TS2564**: Only `declare` classes are exempt, NOT `abstract`.
- **TS7030 guard**: Requires `strictNullChecks || noImplicitReturns`. TS2355 fires regardless.
- **TS2366 requires strictNullChecks**: TS2366 ("Function lacks ending return statement and return type does not include 'undefined'") fires ONLY under `strictNullChecks`. Without it, use TS7030 ("Not all code paths return a value"). Do NOT gate TS2366 on `noImplicitReturns` alone.
- **TS2355 for `undefined` in unions**: TS2355 fires for `function f(): undefined | T {}` (where T is non-void) when there are no return statements — `undefined` in a UNION does NOT satisfy "must return a value", only as a BARE `undefined` keyword (or `Promise<undefined>` for async) does. The classification in `checkBodyForImplicitReturn` (Checker.kt): `KeywordTypeNode(undefined)` and `TypeReference(Promise<KeywordTypeNode(undefined)>)` for async → "pure-undefined" (suppress all checks); UnionType-containing-undefined or `Promise<UnionType>` → "nullable" (TS2355 fires when no returns; TS7030 falls through when mixed returns).
- **`NumericLiteralNode` in method names**: All method-name extraction functions must handle it, not just `Identifier` and `StringLiteralNode`.
- **Bare specifier TS2882**: Always emit for bare (non-relative) specifiers except in AMD/System/UMD modules.

### TS1210 class strict mode gotchas

- **TS1210 vs TS1100**: TS1210 fires for `arguments`/`eval` in class bodies (always strict). TS1100 fires in external strict mode contexts (functions, modules). TypeScript NEVER emits both for the same node — TS1210 takes priority in class bodies. Suppress TS1100 inside class bodies for these names.
- **`emitDeclarationOnly` still needs checker**: Multi-file compilation with `emitDeclarationOnly` must still parse/bind/check files for diagnostics like TS1210. But use `declarationOnly = true` to limit checks — running the full checker produces FPs like TS6131 that TypeScript's declaration-only mode suppresses.
- **`isAlwaysTruthyExpr` vs `isAlwaysTruthyForOrExpr`**: In `||` contexts, TypeScript flags object-like expressions (function, arrow, object, array, class, regex) and non-empty string literals. Numeric literals and `new` expressions are NOT flagged in `||` but ARE flagged in `if`/`else if` conditions.

### Kotlin idioms

- **`*/` inside KDoc terminates the comment**: A `/** ... */` doc comment ends at the first `*/`. Embedding `*/` (e.g. when documenting a JSDoc pattern) silently breaks the rest of the declaration — a data-class body following a broken doc comment surfaces as a cascade of confusing errors like "Modifier 'override' is not applicable to 'local variable'", "No parameter with name 'pos' found", "Unresolved reference 'jsDocTagEnd'". Reword to "comment-close" or escape as `*\/`.
- **No non-stdlib dependencies in `commonMain`**: The project targets Kotlin Native (in addition to JVM/JS), so `commonMain` must use only `kotlin.*` and `kotlinx.*` packages. No `java.*`, no `BigDecimal`, no JVM-only types. Use Kotlin's built-in numeric types and stdlib math (`kotlin.math.*`).
- **Enum context resolution** (Kotlin 2.1+): When the expected type is an enum, use unqualified entry names — write `Equals`, not `SyntaxKind.Equals`. This applies to `when` branch conditions, named arguments (`operator = Equals`), comparisons (`flags == VarKeyword`), and any other position where the enum type is inferred. Caveat: if a data class has the same name as an enum entry (e.g. `LabeledStatement`), keep the `SyntaxKind.` prefix to avoid ambiguity.
- **`in 0..<x` range checks**: Prefer `pos in 0..<end` over `pos >= 0 && end > pos` for range validation — uses Kotlin's `rangeUntil` (`..<`) operator for exclusive upper bound.
- **No JVM-only APIs in `commonMain`**: `Map.putIfAbsent` → use `getOrPut`; `Math.pow` → use `kotlin.math.pow` extension. Always use Kotlin stdlib equivalents for multiplatform compatibility.
- **`when` guard conditions** (Kotlin 2.1+): Use `when (val ch = x) { '/' if condition -> ... }` instead of `when { ch == '/' && condition -> ... }`. The `if` guard after the match value keeps pattern matching readable and avoids nested `when`/`if` blocks.

## AI agent mission

**Phase 16: Fundamental Type System Features.** Pipeline: Scanner → Parser → **Binder → Checker → Transformer → Emitter**. Phase 16 builds core type system features: contextual typing, structural comparison, overload resolution, control flow narrowing. Live test count is in `STATUS.md` (kept separate to avoid churn in this file). See `PLAN-PHASE-4.md` § "Known architectural blockers" for recurring multi-test issues that won't yield to surgical fixes.

### Execution protocol

PLAN-PHASE-4.md contains the **live QUEUE** under the heading `## Phase 16 — Fundamental Type System Features` → `### QUEUE — prioritized by unblocking potential, then by implementation cost`. (Earlier `### QUEUE` sections in the file are archived from prior phases — do not work them.) Live session notes also live under that heading, with the most recent at the top. Default order is top-to-bottom, **fixing as many items per session as the budget allows** — do not stop after a single item if there is remaining context and more tractable work ahead. The outer loop:

1. Pick the first unchecked (`- [ ]`) item in the live queue (or next unfinished sub-step of an `IN PROGRESS` item). If the top item appears blocked on missing infrastructure, **do NOT skip it** — instead, promote the unblocker to the top of the queue and work that (see autonomous-decision policy below). The original item stays unchecked and gets picked up automatically once the unblocker lands. Skipping past a blocker is reserved for the narrow user-policy cases listed under "Guardrails."
2. Implement it — the item describes the deliverable.
3. Run the full suite (`rm -rf build/test-results/jvmTest/binary && ./gradlew jvmTest 2>&1 | grep -a "tests completed"`).
4. Verify no regressions from the currently passing test count.
5. Check off the item (`- [x]`), **bump the test count in STATUS.md** to reflect the new passing total, add a session note to PLAN-PHASE-4.md (under the live notes heading, most recent on top), add a CLAUDE.md gotcha if applicable, **commit and push** (one commit per sub-step — keeps history bisectable and lets the next agent pick up mid-stream without re-running everything). STATUS.md drift is a real failure mode — past sessions committed `(+N)` work without bumping the count, so the next agent saw a stale baseline.
6. **Loop back to step 1** and pick up the next item. Keep going until one of these stop conditions:
   - Context budget is running out — finish the current item cleanly, commit, then stop.
   - Every remaining queue item is gated on user-policy decisions (Guardrails) AND no in-scope unblocker work remains. This should be rare — under the promote-unblocker default, blockers turn into queue items, so the queue rarely runs dry.
7. End the session with a concise summary of items completed, unblockers promoted (with what they unblock), items left pending user decisions, and net test count delta.

**Autonomous-decision policy (when an item appears blocked):**

When an item can't be implemented surgically — e.g. the diagnostic relies on infrastructure that doesn't exist yet (super-keyword resolution, full flow-graph, generic argument inference) — **promote the unblocker, do not skip the item**:

- **Promote-unblocker (default)**: identify the smallest concrete infrastructure piece that would unblock the item, insert it at the TOP of the queue with a one-line description and a back-pointer to the original item (and any other items it would also unblock — list them). Then start work on the unblocker. The original item stays unchecked; it gets picked up automatically on a future loop. Cross-phase work (binder + checker together, scope-chain restructuring, new cache layers) is in scope — that is exactly the work this policy exists to drive.
- **Decompose first if large**: if the unblocker is itself a multi-step change, split it into the smallest standalone sub-step that lands as a single commit without regressions, queue that, and only that. Do not attempt the whole infrastructure piece in one pass. Subsequent sub-steps get queued as separate items as the work progresses.
- **Time budget per attempt**: cap each attempt at one full-suite verification cycle. If it regresses, revert. After revert, you may either (a) try a different decomposition for the same unblocker, or (b) move to the next queue item — but record both attempts in the session note. Do not iterate more than twice in one session on the same regressing approach; if neither works, write up what you learned (which paths broke, what the underlying coupling is) and queue a follow-up "investigate X" item.
- **Skip is reserved for Guardrails only**: the only legitimate skip is when the unblocker requires a user-level decision listed under Guardrails below. For those, leave a `BLOCKED-PENDING-USER:` marker on the item with a one-paragraph proposal for the user to react to, then continue with the next queue item. Do NOT end the session.
- **No silent abandonment**: every promote/decompose/skip writes a session note (PLAN-PHASE-4.md, most recent on top) with root cause, what was attempted, and what landed. Never leave an item half-done in the working tree.

**Anti-loop rule — recon-only sessions are a protocol failure:**

A "recon-only session" is one that runs `find_candidates.py --fresh`, gets `0/0/0` (or similarly empty), spot-checks a handful of candidates, confirms they are all architectural, commits a `chore(status): refresh ... empty pool` note, and ends without landing any code change. **This is not a legitimate session outcome when the queue contains unchecked Blocker items.**

- **Hard rule**: if `find_candidates.py --fresh` returns `0/0/0` (or the surgical pool is otherwise confirmed empty for the current top item) AND the queue contains any unchecked `- [ ]` Blocker substep, you MUST start work on the next unchecked Blocker substep in the same session. Do NOT commit a recon note and stop.
- **Diagnostic signal**: before starting a session, run `git log --oneline -10`. If three or more of the last 10 commits match `chore(status): refresh ... empty pool` (or equivalent recon-only commits), the queue is mis-structured — the agent in earlier sessions failed to promote a blocker to a checkbox. **Your first action is to fix the queue**: open PLAN-PHASE-4.md's "Known architectural blockers" section, pick the highest-yield blocker that has no corresponding `- [ ]` queue item, decompose it into the smallest standalone substep, insert it at the top of the QUEUE, commit that restructure (`chore(queue): promote Blocker #N to queue item`), and only THEN start work on it.
- **Recon is allowed as part of step 1**, not as a session deliverable. If a recon turns up surgical candidates, fine — work them. If it doesn't, that's the trigger to attack the next Blocker substep, not to wrap up.
- **Forbidden commit message patterns** when the queue has unchecked Blocker items: `chore(status): refresh post-N.N recon`, `... empty surgical pool`, `... N+ consecutive empty pool`. These are signals that the protocol has failed. The only acceptable session-end commits are (a) a feature commit landing real code, (b) a `chore(queue): promote ...` restructure that prepares the next session, (c) a `chore(maint)`-class action like a stale-skip-log audit when one is genuinely warranted (≤ once per ~5 sessions), or (d) a revert of a regressing attempt.
- **STATUS.md "consecutive empty pool" counters are a code smell, not a metric**: bumping `19+ → 20+ → 21+ → 22+` across consecutive sessions means each of those sessions failed the anti-loop rule. If you find such a counter on entry, treat it as the diagnostic signal above and fix the queue first.

**Guardrails requiring user input** (do NOT decide autonomously):
- Build system changes (Gradle config, dependency add/remove/upgrade, Kotlin version bump).
- Test-generation pipeline changes (`generateTypeScriptTests`, baseline format, skipped-tests log structure).
- Re-enabling currently-disabled test categories (e.g. `.errors.txt` → +9k tests).

**Other rules:**
- **Do NOT switch items** mid-task — finish the current item (commit + push) before starting the next.
- **Commit between items** — never bundle two unrelated sub-steps into one commit. Each committed sub-step leaves the repo in a clean state for the next agent.
- **Analysis items** (item 0) should produce written artifacts (design docs, categorized lists) before any code is written.
- **No regressions** — the currently passing tests must continue to pass after every change. Re-run the full suite between sub-steps; a +1 / -2 swap is still a regression.
- **Full-suite run caveat**: a clean JVM test run takes ~4-6 minutes. Budget accordingly when deciding whether to attempt another item.

### Reference TypeScript sources

The original TypeScript compiler source is in `typescript-repo/src/compiler/` (if cloned — only test fixtures are present in the sparse clone). When a fix is ambiguous or behavior is unclear, consult TypeScript's public documentation and source on GitHub. The 1M context window can accommodate large files when needed.

## AI agent workflow

- Keep this file and `PLAN-PHASE-4.md` up to date after each session so the next agent/developer starts with accurate state.
- `PLAN-PHASE-4.md` contains the live queue + blockers + workflow; `PLAN-PHASE-4-HISTORY.md` has archived session notes, archived completed queue items, and the full Phase 5–15 record. `STATUS.md` keeps the headline test count plus the last ~5 B-entries; older entries live in `STATUS-HISTORY.md`. `PLAN-PHASE-3.md` has deferred Phase 3 items.
- For subagent-based parallel work (rarely needed — most surgical fixes are faster done in-line), see `docs/subagent-workflow.md`.

## How to run tests

```bash
# Full suite (always clean binary cache first — stale cache inflates failure count):
rm -rf build/test-results/jvmTest/binary && ./gradlew jvmTest 2>&1 | grep -a "tests completed"

# Single test (get expected vs actual diff):
rm -rf build/test-results/jvmTest/binary && ./gradlew jvmTest --tests '*.<TestName>*' 2>&1 | grep -a -A 40 "message" | head -50

# Candidate finder (must have fresh full-suite XMLs first):
python3 scripts/find_candidates.py --fresh      # hide tests already in skipped log
python3 scripts/find_candidates.py              # show all, with [SKIP] markers
```

**Note:** All failures are deterministic (confirmed via 5-run study). Count variance between runs is caused entirely by dirty binary cache from interrupted runs, not JVM instability. **Gradle wipes XMLs when run with `--tests '*Name*'`** — the full suite must be re-run before `find_candidates.py` can report accurate results; the script warns if the XML count is below the expected ~27.

## Anti-patterns to avoid

- Do not add content to this file that is already discoverable by reading the source or build scripts — that inflates context without adding signal, reducing AI agent task success rates (see [arxiv 2602.11988](https://arxiv.org/abs/2602.11988)).
- Do not use `grep` (without `-a` flag) on Gradle test output — it may contain binary content. Always use `grep -a`.
- **Do not re-analyze what to fix next.** PLAN.md is already the prioritized plan. Pick the top unfinished item, implement it, done. Do not scan lists of failing tests or explore "low-hanging fruits" — that is wasted analysis time that could be implementation time.
