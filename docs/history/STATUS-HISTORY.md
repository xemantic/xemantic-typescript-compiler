_Round 715 (2026-07-26) — **measuring `expressions/asOperator` for adoption turned up a SILENT WRONG-OUTPUT bug, which landed; the category did not. Corpus 12,761 → 12,765 / 0 / 3.** `(x + 1 as number) * 3` emitted `x + 1 * 3` — erasing the cast dropped the parentheses with it and the `*` re-associated into the sum, so the emitted program MEANS something else. `typeAssertionResultNeedsParens` listed class/arrow/unary/typeof/void/delete/await/yield but not BinaryExpression or ConditionalExpression, exactly the kinds with a precedence to lose; the conformance case says as much in a comment ("Must emit as (x + 1) * 3"). **The category is not adopted, for the reason round 695's table warned about:** its remaining blocker (`asOperatorASI`) is a JS-EMIT test and the deferral mechanism only covers `.errors.txt`, so failure KIND decides adoptability, not count — it needs the documented `hasPrecedingLineBreak` ASI divergence fixed in the parser first. **General point for the table:** it ranks by failure COUNT, and a 5-failure category can be unadoptable while a 9-failure one whose failures are all error baselines is fine — check the kinds first._

