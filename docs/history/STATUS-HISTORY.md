_Round 759 (2026-07-29) — **(AUDIT.2)+(AUDIT.3): THE ARC'S LAST TWO UNVERIFIED ROWS, MEASURED — AND ONE IS SECTION 0's LAW'S FIRST COUNTER-EXAMPLE.** `argument-check-attribution.md` section 3's "only 27% reach the relation, yet all 37,379 pay for the full `argType` computation" is **TRUE AND UNDERSTATED: the 72% that exit early carry 89% of the argument-typing time — 613 ms of 689 — at 22,604 ns each against 7,134 ns for the 28% that do, 3.2x THE WRONG WAY.** Round 758 predicted "< 40%" and this round predicted 35%; both out by ~2.5x in the same direction, because both applied "what you could skip cheaply was already cheap" where **the exit predicate (a property of the PARAMETER) and the cost (a property of the ARGUMENT) do not share a cause** — complex parameters attract complex arguments. **Mechanism MEASURED, not a named residual:** `getTypeOfExpression` **8,252 ns** non-relating vs **1,344 ns** relating (**6.1x**), and **7,917 of the 9,674 narrowing walks (82%)** are non-relating; those two rows are 80% of the total and the remaining 140 ms ships LABELLED RESIDUAL. **NO LEVER, for a reason unrelated to the size:** *paying for `argType` is not wasting it* — eleven blocks below consume the value and the assignability relation is the CHEAPEST consumer in the function at 19 ms (the third independent finding, after 735's 48x and 738's 65x, that the relation engine is not where the call path's time is); the only arguably-skippable subset is 275 ms of narrowing = **1.0%, half a band**. The row's defining number had ALSO fallen **924 -> 689 ms (-25%)** while the item sat in the queue, and the exit profile shifted under it (foreign-TP 14,663 -> 11,689, fn-vs-fn 12,280 -> 15,140). **(AUDIT.3): the globals-lookup population is 36-71 ms = 0.13-0.26%**, so section 0's asserted "<=0.2%" is the right order and the last asserted-never-measured population there closes. A nested span could not do it (an ~89 ns timestamp pair around a ~37 ns probe), so the instrument **amplifies the signal instead of shrinking itself** — `--globalsAmp r` brackets r reads under ONE pair and two values of r cancel the pair's cost: **cold ~37 ns, warm 9.1 ns**, three independent slopes within 7%. **THE INSTRUMENT WAS FALSIFIED, NOT TRUSTED:** DCE is ruled out **arithmetically** (the sink is an exact multiple of the hit count at every r — 16x17,928 and 64x17,928 to the unit) and the sink independently reproduces the 98.1% miss rate; the wall-time cross-check agrees but is reported as WEAK, since the +-13% box drift could never have falsified it. **Predictions 2 of 6** — the healthy rate round 758 said its own 5-of-5 lacked. No behaviour change; additive opt-in instrumentation only (production cost: **zero instructions**). Gates: compiler profile `--listAll` **46**, composition unchanged (TS2591x43 / TS2304x2 / TS2584x1), production vs `--argSections` sorted-line diff **IDENTICAL** on the final binary; filtered probe batch **70 / 0**; warning-clean. Full suite and cost gate are the owner's to run. Artifact: `docs/perf/claim-audit-round758.md` sections 10-14; section 0/0.1, `argument-check-attribution.md` and CLAUDE.md's POPULATION-vs-FREQUENCY entry corrected IN PLACE._

_Round 758 (2026-07-29) — **(AUDIT.1): THE ATTRIBUTION ARC AUDITED ITS OWN NUMBERS. 57 load-bearing claims classified POPULATION / FREQUENCY / TIME / RESIDUAL — 40 stand, 11 stale-or-weak, 4 falsified, 2 unverified; THREE OF THE FOUR FALSIFIED ARE A FREQUENCY SPENT AS A POPULATION AND ALL THREE SHRINK.** `IDENTIFIER` is 44.5% of the NODES and **8.4%** of the spine's TIME (1,853 ms of 22,104) — **5.3x**, and that is the claim ARCHITECTURE-RETHINK § 0 built "the measured lever is consultation, not computation" on. `getCalleeType`'s "half its results are thrown away" is 50.6% of the CALLS and **8-10%** of the TIME (1,452 ns per discarded resolution vs 16,491 ns per kept one) — the implied ~237 ms is **38 ms**, **6.2x**, closing (CALL.1) § 6's last forward-pointer. A MEASUREMENT BUG in the instrument: `--passTiming`'s per-kind table printed `enter+leave` and summed ENTER ONLY — fixed, and the corrected counter now reproduces round 732's INDEPENDENT `--dispatchProbe` per-kind numbers to 0.2-5% (the broken one was 1.7-4.0x low). The corrected concentration is the OPPOSITE shape to § 0's: the five statement-anchor kinds are **10% of the nodes and 40% of the spine**. A fourth falsified claim is not a frequency but a RESIDUAL GIVEN A NAME — § 0.1's "remove ALL dispatch overhead → 66 units, 1.5x" should read `100 → ~99, 2.4x`, **out by ~34x**, and it is the first step of the parity argument. The map was re-measured for the first time since round 716: `checkSpine` **22,104 ms = 84.4% of checker-init, 74% of the compile**, the ~400 tail passes **10.4%**, globals lookups 961,213 at 98.1% miss. **"Single digits remain" SURVIVES and is better supported — every re-derived population came in BELOW its citation, none above; NO parked item is revived.** Predictions **5 of 5 held**, which the artifact records as a warning sign rather than a credential. No behaviour change; new artifact `docs/perf/claim-audit-round758.md`; § 0/§ 0.1 and four `docs/perf/` artifacts corrected IN PLACE. Compiler profile `--listAll` **46**, composition unchanged, production vs `--callSections` sorted-line diff IDENTICAL; filtered probe batch **66 / 0**; warning-clean. Full suite + cost gate are the owner's to run._

_Round 757 (2026-07-29) — **(FN.1) FIXED: an expression-bodied arrow's body is walked now, and the census number that motivated it was 146x too big.** `walkFunctionBodiesInExpr`'s `ArrowFunction` arm was `(expr.body as? Block)?.let { … }` — an EXPRESSION body walked NOTHING and did not descend, so a function body nested inside it never reached `checkFunctionBody`. It now descends under the arrow's own parameter/type-parameter scope (zero-parameter arrows skip the install; the scope is the FP firewall, since the nested `checkFunctionBody` inherits `currentLocalTypes`/`varTypes`). **Verified against unmodified `2f728c1e`: four fixtures SILENT on pristine now fire — the paren-wrapped function expression, a block-bodied arrow passed as a call argument, a chain of expression-bodied arrows, and the parameter-shadowing shape — while four controls are identical on both and a correct nested body stays silent.** A scope pin I first wrote was VACUOUS (`v.b` on `{a:number}` emits nothing here, so its `none 2339` half could not fail); replaced by a TWO-SIDED discriminator whose swapped annotations give opposite verdicts. **THE FINDING: the compiler profile is byte-identical at 46 and all 8,837 corpus baselines are unchanged, because the 874 expression-bodied arrows contain exactly SIX block bodies between them** (arm census: block arrows 374 → 380, function expressions 262 → 262) and all six are clean — round 756's "874 of 1,510 (58%)" counted how often the ARM IS TAKEN, not what was behind it. Predictions 4 of 5 falsified, all in the same direction. Cost: new `D_ARROW_EXPR_SCOPE` row = 8 ms over 707 installs, +3.5% nodes walked, **≈10 ms ≈ 0.04% of the compile** (walker 0.68% → ~0.72%); no `LogicalParityDivergence` needed. Gates: profile `--listAll` **46 byte-identical**; whole generated corpus (25 classes) **8,837 / 0**; filtered batch **2,335 / 0 / 0**; warning-clean. +3 pins (`CtaSectionProbeTest` 20 → 23; the two round-756 gap pins FLIPPED, not deleted). Addendum: `docs/perf/walk-function-bodies-attribution.md` § 11._

_Round 756 (2026-07-29) — **(TYPE.3) CLOSED AS A MEASUREMENT: `walkFunctionBodiesInExpr` spends 36% of itself on the work it is NAMED for, and the arm census found a FALSE NEGATIVE the milliseconds could not.** Compiler profile `--listAll` **46**, composition unchanged (TS2591x43 / TS2304x2 / TS2584x1, zero TS2322), `--listAll --ctaSections` **byte-identical** to it; `CtaSectionProbeTest` **20 / 0 / 0** (+9 pins); filtered batch (`*Cta*` `*ObjLit*` `*ObjectLiteral*` `*Arrow*` `*Inv0*` `*Spine*` `*FunctionExpr*` `*Contextual*`) **2,405 / 0 / 0**; build warning-clean; **nothing landed beyond the harness, correctly**. **THE NUMBER REPRODUCES EXACTLY** — round 738's 181 ms / 28,940 openings / 6,280 ns each, all three digits, because this population is AST SHAPE and not declared types (round 755's target had halved). **THE PARTITION** (a new **level D** in `CtaSections` — the arc's first RECURSIVE partition, so it could not use levels A-C's `depth != 1` shape; `beginD` hands the caller's running row back and `endD` reopens it, making every row SELF time): net of a 168 ns/boundary charge, **the walk itself 65 ms (36%), `calleeDeclaredCtxParams` 49 ms (27%), `checkFunctionBody` for arrows 49 ms (27%) and for function expressions 16 ms (9%)** — sum **179 vs 181 measured independently**, and level D's outermost invocations are **28,940 = level A's `A_WALKFN` openings exactly**, a cross-check from the other side. **WHY IT IS SMALL IS NOT WHAT ROUND 738 GUESSED:** the walk visits **199,131 nodes to reach 1,510 function-like ones (0.76%)**, only **636** with a block body, and those bodies cost **61-131 us each** — **it spends more finding them than checking them**. **THE CENSUS FOUND A FALSE NEGATIVE:** **874 of the 1,510 (58%) are EXPRESSION-BODIED ARROWS** whose arm walks nothing, and the obvious defence ("the spine anchors the inner statement anyway") was TESTED and is FALSE — `const f = () => (function () { const s: string = 5; return s })` is **SILENT** where tsc reports TS2322, while all three controls fire; pinned as it stands with those controls and queued as **(FN.1)**. **TWO ZERO-POPULATION FINDINGS:** the whole B150/B585 object-literal-method machinery is JS-gated and **never runs** on tsc's `.ts` source despite 755 objlit arm entries (a `.js` control lights it, so the zeros measure the input), and **`invocationsDOutside == 0`** — the walker has exactly ONE live entry point. **THE SIZE:** 181 ms = **0.68%** of a 26,800 ms compile, largest row **0.24%**, band **±2.0%** — deleting the whole walker is a third of one noise band; the one named candidate (a pre-test skipping the 29,787 unconditional callee resolutions that exist for 636 bodies) is **49 ms = 0.18%** and unproved sound. **PREDICTIONS 2 OF 4 WRONG** — P0/P2/P3 held, P1 falsified on both clauses. **WHAT DID NOT WORK:** the ON-vs-COARSE differential is too noisy to price the boundary (Δ29 ms against a 26 ms within-mode spread; usable calibration came from comparing both modes against the level-D-free binary) — *a differential is only as sharp as the smaller of its two spreads*. Derivation: `docs/perf/walk-function-bodies-attribution.md`. **PART 2 — (LIB.1) IS DONE: (c) landed and took the ZERO-CORPUS-IMPACT design.** `Resolution.unknownNames` had **zero consumers**, so `"lib": ["esnext.arary"]` resolved to NOTHING, the program was checked against no lib at all, and not one word was said. TS6046 is now raised from the resolution itself (`bindRealLibs` records them, a new `pass("checkLibOption")` emits one per bad entry, message enumerating the valid names as tsc does and deliberately NOT naming the offender), positioned from `tsconfigOptionPositions["lib"]` when tsconfig-driven and file-less otherwise. **The corpus cannot move by construction** — it runs the embedded lib and never resolves real libs — and that property is a PIN, not a comment; a raw `options.lib` x `libMap` check would reach all 259 `@lib:` cases. The (b) trap applies and the control obeys it: silence IS the failure mode, so the control is a MEMBER probe. Verified on unmodified HEAD — three targets FAIL, both controls PASS. Gates: `--listAll` **byte-identical at 46**, `*LibOption*`/`*RealLib*`/`*Inv0*`/`*Cta*` **120 / 0**, corpus slice `_L`/`_M`/`_N` **1,075 / 0**; +5 pins (`LibOptionUnknownNameTest`)._

_Round 755 (2026-07-29) — **(CALL.4) CLOSED AS A MEASUREMENT: the 21,708 ns is 80% ONE CALLEE, and the item's own defining number had HALVED while it sat in the queue.** Compiler profile `--listAll` **46, byte-identical with the probe at its deepest** (TS2591x43 / TS2304x2 / TS2584x1, zero TS2322); filtered suite (`*Narrow*` `*Flow*` `*Guard*` `*Inv0*`) **504 / 0 / 0**; +18 pins; build warning-clean; **nothing landed beyond the harness, correctly**. **FIRST, THE ITEM'S DEFINING NUMBER DOES NOT REPRODUCE:** round 736's "33,307 genuinely-narrowing calls at 21,708 ns = 723 ms" is now **21,970 at 20,085 ns = 441 ms** — the per-call cost is stable (-7.5%) but the COUNT fell **34%** while the total call count ROSE 2.5%, downstream of (REL.1)/round 754 changing what the declared types are. *An item defined by a measured number must re-measure it before a round is spent inside it.* **THE SPLIT:** eleven per-leaf sub-measures folded per outermost call into NARROWING and IDENTITY columns (the two populations differ 14x per call, so one mean answers nothing) — **`narrowByCallPredicate` 351 ms over 23,138 calls at 15,181 ns = 80%**, `narrowByEquality` 50, `narrowByTruthiness` 16, everything else <= 6, and **the dispatcher's own residue 9 ms = 2%**; replicated across two independent timing runs (the 80% moves 1.2 points while small rows move +/-25%). `applyConditionNarrowing` is **not a function with an expensive body — it is a `when` that reaches one expensive callee**, and that callee does type-predicate RESOLUTION, i.e. M3.1 work. **THE SIZE, AGAINST A BAND RE-DERIVED TODAY:** 5 interleaved NULL pairs give median **-0.05%**, spread **1,095 ms**, range **[-526, +569]** on a 26,778 ms compile => band **+/-2.0%**; the whole population is **441 ms = 1.6%**, *smaller than one pair's noise*, and a perfect memo over the entire leaf is capped at 1.75%, in-band before it costs anything. **TWO CORRECTIONS TO ROUND 736:** its rejected "does this condition mention the name" pre-test is not merely in-band but **UNSOUND** (99,002 identity calls are the aliased-condition path, whose whole point is a condition that does NOT mention the walked reference; 1,873 of them narrow), and its "identity calls are the cheap tail" hid that **73% of the identity time sits in three leaves that RESOLVE something**. **CENSUS ODDITIES:** `instanceof` and `in` are **38 and 42 invocations in the entire compile**; the `=` arm is reached only by OTHER references walking past (a walk for the assignment's own target breaks out of the fast-forward loop at the `FlowAssignment` the same expression produced) — both facts came out of test fixtures that FAILED first with zero invocations. **PROBE COST MEASURED NOT ASSERTED:** ON->DEEP adds exactly 172,957 boundary pairs for +47 ms => **<=271 ns/pair, an upper bound only**; the DEEP row's own attributed span is 81 ns, so a pair ATTRIBUTES a third of what it COSTS. LANDED: `NarrowSections` `C_*` rows + arm census + `--narrowSectionsDeep` (opt-in, behaviour-free when off) and `NarrowSectionProbeTest` (arm mirror pinned SHAPE BY SHAPE with confusable arms asserted zero). Derivation: `docs/perf/condition-narrowing-attribution.md`. **PART 2 — (ENGINE.1) SITE 2 MEASURED AND E1-E4 SCORED.** `checkReturnAssignability` partitioned into 15 rows (new level C in `CtaSections`, own index space, wrapper + `...Core` so every early return closes its row); **cross-check: level C totals 741 ms over 10,119 invocations against level A's INDEPENDENT row at 740 ms** — two partitions from opposite sides of the same call agreeing to 0.1%; calibrated differentially at **739 ns per boundary** (ON 144,179 boundaries / 741 ms vs COARSE 10,119 / 642 ms). **Net of probe: engine 446 ms (71.6%), dedicated-walker layer 177 ms (28.4%), bookkeeping 0** => the layer is **0.66% of the compile, 0.40x the engine work** (site 1: 1.21%, 0.67x). **E1 HOLDS** (28.4%, inside the predicted 25-50%); **E2 FAILS** (4.5x, not the predicted >=10x — the 14x was an artifact of site 1's relation being 2.2% of its function); **E3 HOLDS BY A BOUND** (sites 1+2 = 503 ms and site 3's WHOLE function is 373 ms, so the three-site layer is capped at 876 ms = 3.3%; honest estimate ~605-630 ms = **2.3%**); **E4 HOLDS AT BOTH** (weak types at site 1, excess-property checking at site 2 — tsc implements both inside `checkTypeRelatedTo`, so both MOVE rather than delete). **OWNER-FACING: the scope change is worth ~2.3% of a check-only compile against a +/-2.0% band — one to two noise bands — and less after the rules that move.** Two findings site 1 could not give: the **218-line TS2322 elaboration runs ONCE in the whole compile**, and the **legacy string checker is not a rare fallback — 85% of invocations (8,587/10,119) exit inside it**, running the full engine block and then re-checking, for 15 ms. **Site 3 (`checkAssignmentExpression`) remains unmeasured; it can sharpen the number, not change it.** Filtered gates: 866 local + 1,290 corpus tests, 0 failures. Derivation: `docs/perf/engine-rule-price.md` §§ 5-8._

_Round 754 (2026-07-28) — **(PERF.HW.a) CLOSED: `--workers N` IS BEHAVIOUR-PRESERVING AGAIN, AND THE ROUND'S DELIVERABLE IS THAT ROUND 740's CLASSIFICATION WAS WRONG.** Compiler profile `--listAll` **46, sorted-identical to a pre-change build** (TS2591x43 / TS2304x2 / TS2584x1, zero TS2322); **`--partitionCheck 2` EQUIVALENT on ALL EIGHT profiles**; **`--workers 2` and `--workers 4` both 46 and byte-identical to sequential** — the item's acceptance criterion; services 47 / server 47 / harness 95, each diffed byte-identical against a build of unmodified HEAD; `cost_gate.py` **+0.00% on 19 of 20 counters** (`typeNode.bypassed` -0.01%), no rebaseline; +9 pins; build warning-clean. **IT IS NOT THE ROUND-609 PARTITION-COLLECTOR CLASS.** `--partitionCheck 2` reproduced the divergence sequentially (`full=46 merged=62 extra=16`), and a probe printing source type, target type and relation verdict at the emission site in BOTH views showed the two views resolve the SAME two types and disagree only on the VERDICT — `tgtProps=[value:any]` in the full run against `[value:T]` in the partition. **No collector is starved because there is no cross-file context involved:** a reference that omits the type arguments of a generic whose every parameter has a default resolved to the RAW `Type.Interface`, whose members keep the un-substituted `T`, and nothing relates a `Type.Reference` to that (round 726's trap from the other side). **The sequential run was right BY ACCIDENT** — a resolution-ORDER accident had cached that member as `any`, making the relation vacuously true; reorder the files, which is exactly what `--partitionCheck N` and `--workers N` do, and it stops. **A two-file scratch project reproduces the false positive with NO partitioning at all.** **THE FIX (`cac22abd`) IS AT THE RELATION, AND THE CORPUS IS WHY.** The principled site — filling the defaults in `getTypeFromTypeReference`, as tsc's `fillMissingTypeArguments` does — passed every profile gate and then cost 8 baseline lines of `typeVariableConstraintedToAliasNotAssignableToUnion`: filling at resolution makes a bare `TableClass` and an explicit `TableClass<any>` the SAME interned instance and `aliasDisplayMap` excludes `Type.Reference` on purpose, so `type Table = TableClass` stopped displaying as `Table`. `defaultedInstantiationOfOpenGeneric` in `checkTypeRelatedToCore` changes the answer and no display. **THE FIX ALSO CLOSES A SILENT LENIENCY, which is what makes the pins non-vacuous: 4 of 9 fail on pristine HEAD and TWO OF THOSE FOUR ARE THE NEGATIVE CONTROLS** — a bare `Box` target used to ACCEPT `Box<string>` where `Box<T = number>`, because the raw target's `value: T` is a bare type parameter. **NEGATIVE RESULT: services' TS2345 is NOT this round's** — 47 against a round-739 TSV row of 46 is drift from rounds 741-753 (unmodified HEAD emits the identical 47 lines); the site is `completions.ts:2237`, an `isIdentifier(node)` guard failing to narrow, unrelated to enums or defaults. **PART 1 (`8c10a780`) reconciled the queue**: (CALL.2) ticked (done round 735, artifact committed); (LIB.1) kept open with its (a)/(b) halves marked closed (rounds 730/731) so a scan does not re-do them; (CALL.4) and (ENGINE.1) kept open with one line each on what genuinely remains._

_Round 753 (2026-07-28) — **(REL.1)(c) STEPS 5b AND 5c COMPLETE: THE LAST READER FLIPS, THE `.kind` DOMAIN VETO FAMILY IS DELETED, AND THE ARC IS DONE. The round's real deliverable is that ONE ABLATION SAID KEEP AND THE OTHER SAID CUT — measured, not assumed, and by the same instrument.** Compiler profile `--listAll` **BYTE-IDENTICAL at 46** after both commits — whole output diffed sorted line-by-line, composition unchanged (TS2591x43 / TS2304x2 / TS2584x1, zero TS2322); **8,203 tests** (7,068 generated corpus over A/C/D/E/G/I/N/O/P/S/T/U + 1,135 local over the enum/narrowing/discriminant/switch/objlit/guard/conditional/relation classes), **0 failures**; +6 pins; build warning-clean. **COMMIT 1 (`1310bfef`) — THE OBJLIT SELECTOR GOES TYPE-FIRST, AND ITS FALLBACK STAYS.** `selectUnionMemberByObjLitDiscriminant` now reads through `discriminantKeysOfMember`; probe first, as instructed: **292 sightings, 292 AGREE, 0 mismatched, 0 blind, 0 gained**, instrument falsified in the same run, `canonicalEnumSymbol` memo-bypass 342 mints / 0 divergences. **Round 752 predicted this site would still need the annotation walk and the prediction was TESTED IN BOTH DIRECTIONS rather than trusted.** With the fallback cut the profile stays byte-identical at 46 — **and that is worth nothing here**: the round-475 `TypeQuery` shape (`kind: typeof CloseTag` over a top-level `const`, tsc's editorServices `eventName` pattern) resolves to the WIDENED `string`, carries no key, and the probe reports it **TYPEBLIND with a real decision difference (ast=true type=false)**; an ablated build emits a wrong-constituent TS2353 on it. So the byte-identity is a statement about the profile, not about the fallback. The docstring's pre-(REL.1)(a) premise ("the resolved property type cannot decide this") is corrected with the flip. **COMMIT 2 (`4300de65`) — (5c) DELETES THE VETO FAMILY, -194 LINES**: `kindDomainProvesNotSubtype`, `kindDomainKeysExceed`, `kindDomainKeysOfType` (+ its Type.id memo), `kindDomainKeysFromTypeNode`, `kindDomainTypeDeclSymbol`, `ifaceKindDomainKeys`, `discriminantPropAnnotation`, and all three call sites including round 729's `evaluateConditional` patch. **THE ABLATION WAS INSTRUMENTED TO COUNT WHAT IT SUPPRESSED, which is the whole difference between the two verdicts this round: the veto FIRED 11,667 times on the compiler profile (of 40,648 consultations) and changed no output.** An ablation that never fires cannot tell dead code from load-bearing code — and the objlit fallback in the same round is the counter-example that proves it matters. **Round 729's OWN pin is the sharpest evidence** for the conditional call site: `DistributiveConditionalTypeTest.a sibling AST interface is not excluded just because it is structurally compatible` is the test that patch was written for, and it passes without it (all 8 in that class pass). Mechanism: every one of these was a veto layered over the structural relation, written when an enum member resolved to `anyType`; (REL.1)(a)/(b) removed the premise. **SCOPE CORRECTION, recorded because the queue said otherwise: (5c) also called for deleting "the AST key helpers" and THEY STAY** — `enumMemberKeysOfTypeNode`/`enumSwitchKeysFromTypeNode` still have live consumers (the exhaustive-switch coverage readers, and the objlit fallback measured load-bearing above). Only `discriminantPropAnnotation` became unreachable. The cascade lands as ONE commit because each deletion orphans the next and the build is warning-clean. Three pin classes (`KindDomainTypeReadTest`, `NegativeGuardKindDomainTest`, `KindDomainMemoConsistencyTest`) were UPDATED rather than deleted — they always stated invariants about narrowing, not about the veto, so they now pin the relation alone, which is the end state round 752 predicted for them in their own docs. **(REL.1) IS COMPLETE: (a), (b0), (b) and (c) steps 1-5 all landed.**_

_Round 752 (2026-07-28) — **(REL.1)(c) STEP 5b: four more readers go TYPE-FIRST in three commits, and the round's real deliverable is a NEGATIVE RESULT about the last two.** Compiler profile `--listAll` **BYTE-IDENTICAL at 46** after every commit — whole output diffed sorted line-by-line, composition unchanged (TS2591x43 / TS2304x2 / TS2584x1, zero TS2322); **8,173 generated corpus tests** over every class carrying a TS2366/TS7030/TS2355 or enum baseline plus **630 local tests**, **0 failures**; +9 pins, and round 751's "not flipped yet" negative control inverted as it said it must be. **COMMIT 1 (`e717aba2`) — THE EXHAUSTIVE-SWITCH GATE, and the probe FORCED ITS SHAPE.** The obvious flip — read enum-MEMBER types only, as round 751's site does — measured **204 AGREE but 9 TYPEBLIND**, the type path LOSING keys the AST path had: 7x a bare `kind: SyntaxKind` expanding to 396 members, 2x `newLine: NewLineKind`. Landing it would have started reporting switches tsc accepts. The fix is a PAIR mirroring the AST pair arm for arm — `enumSwitchKeysFromType` twins `enumSwitchKeysFromTypeNode` (bare enum → whole member domain), `enumDiscriminantKeysOfType` twins `enumMemberKeysOfTypeNode` — re-measured **213 sightings, 213 AGREE, 0 mismatched, 0 blind, 0 gained**, and **with the AST fallback ABLATED the profile stayed byte-identical at 46**. The parenthesized-discriminant control moved exactly as round 751 predicted, TS2366 → silent. **COMMIT 2 (`3d8119dc`) — THE `neverType` DEFAULT-CLAUSE GATE**, substituted with round 751's shared `discriminantKeysOfMember` (whose fallback IS that site's former composition, so it can only ANSWER where the site was blind): 4/4 AGREE, and the ablation was measured **per sighting** — the type half alone reproduced all four. **Pin non-vacuity was ATTRIBUTED, not assumed**: a build of `e717aba2` with only that site left unflipped emits `TS2345: Argument of type 'Reason' is not assignable to parameter of type 'never'`, so the pin belongs to that reader and not to commit 1's. **COMMIT 3 (`6c988338`) — THE TWO `.kind` DOMAIN READERS, AND THE NEGATIVE RESULT.** `kindDomainKeysOfType` (207/207 AGREE) and `discriminantKindKeys` (738/738, type half alone reproducing all 738) both flip cleanly, **but both are UNOBSERVABLE**: three shapes built to expose a before/after — a negative guard over a wide-domain subject, a positive guard over a three-member union, a conditional-type exclusion, each with the parenthesized `kind` that discriminated all three earlier flips — are SILENT on a build of pristine `3d8119dc` and on the flipped build alike. **The mechanism: both are VETOES layered over the structural relation, written when an enum member resolved to `anyType`; (REL.1)(a)/(b) gave the relation that ability, so a veto that declines to answer no longer changes an outcome.** Their pins ship declared as non-discriminating regression guards rather than passed off as proof. **AND IT RE-SIZES (5c): `kindDomainKeysOfType`'s ONLY consumer is `kindDomainKeysExceed`, which (5c) intends to DELETE**, so that flip is scaffolding kept uniform until the deletion lands; `discriminantKindKeys`' consumer (`typeGuardMemberDisjoint`) survives that cut, which is what puts it on (5c)'s critical path. **CORRECTION, recorded because the commit is pushed: `6c988338`'s message claims "all five readers now flipped" and that is WRONG** — the OBJLIT discriminant-candidate filter (`selectUnionMemberByObjLitDiscriminant`) is still on the annotation walk (the miscount treated the exhaustive-switch gate's TWO readers as two of the five), so **(5c) is NOT unblocked; one reader remains.** The memo-bypass probe was re-run at every mint each time: **0 divergences**._

_Round 751 (2026-07-28) — **(REL.1)(c) STEP 5b: the discriminant reader is TYPE-FIRST at the first of its five call sites, and the probe that had to come first says the SEVENTH producer lands in the same key space.** Compiler profile `--listAll` **BYTE-IDENTICAL at 46** — whole output diffed sorted line-by-line against a pre-flip build, composition unchanged (TS2591x43 / TS2304x2 / TS2584x1, zero TS2322); **5,901 generated corpus tests** over the `C/D/E/I/N/P/S/T/U` classes plus **1,476** over every enum / narrowing / discriminant class, **0 failures**; +5 pins. **THE PROBE RAN FIRST, AS INSTRUCTED, AND IT IS THE ROUND'S DECISIVE MEASUREMENT: 198 distinct (property, key-set) sightings over the whole compiler profile — 198 AGREE, 0 mismatched, 0 where the type path LOST a key the AST path had, 0 where it GAINED one**, across 5 discriminant properties (`kind` 173, `type` 19, `operator` 3, `token` 2, `keywordToken` 1) and 7 enum symbols. **THE INSTRUMENT WAS FALSIFIED IN THE SAME RUN**, because a probe reporting AGREE while inert is exactly the failure mode to rule out: re-running the categorisation against a deliberately perturbed key set reported MISMATCH-REACHABLE. **AND THE MEMOIZATION HAZARD WAS MEASURED DIRECTLY — 0 divergences — WHICH ROUND 750'S MEASUREMENT COULD NOT DO.** `canonicalEnumSymbol` memoizes per `sym.id` a decision that reads MUTABLE state (`globals[name]`, `enumValues[global.id] == null`), so a first touch before an enum's values exist freezes the non-canonical answer — and **a frozen-wrong entry reports `canonical(sym) === sym`, indistinguishable from an already-canonical one, so "153 canonical, 0 redirected" never ruled it out.** The only way to see it is to recompute the decision UNCACHED and diff, which this round did at EVERY mint over the whole profile. **THE FLIP ITSELF:** `discriminantKeysOfMember` reads `getTypeOfSymbol(propSym)` through the new `enumDiscriminantKeysOfType` — keying an `EnumLiteral` type as `enumDiscriminantKey(memberSym.parent, memberSym.name)`, distributing over a union — with the annotation walk kept only as a fallback; both paths start from the SAME property symbol (`discriminantPropSymbol`, extracted from `discriminantPropAnnotation` behaviour-preservingly), so this is a re-derivation of one answer rather than a second lookup that can drift. **RESTRICTED to a property that HAS an annotation**, per the standing constraint, since `PropertyDeclaration.type` is blind to an inferred enum-member type and an unrestricted reader would be a WIDENING. **THE ABLATION IS WHAT SHOWS THE TYPE PATH IS DOING THE WORK RATHER THAN RIDING THE FALLBACK: with the AST fallback CUT OUT ENTIRELY the profile stayed byte-identical at 46** — so on the compiler profile the annotation walk this site consumes is already dead, which is the evidence (5c)'s deletion needs. It is kept for corpus shapes type resolution cannot answer (chiefly `p: typeof X` for a top-level const string, which resolves to the widened `string` while the annotation still yields a `lit:s:` key). **NON-VACUITY: 4 of the 5 pins FAIL on a build of unmodified `db730b95`.** The witness shape is a PARENTHESIZED annotation — legal TypeScript with no `ParenthesizedType` arm in the AST reader, so it silently yields no keys and the member is conservatively KEPT, i.e. narrowing quietly stops matching. **TWO OF THOSE PINS WERE WRONG WHEN FIRST WRITTEN AND THE PRISTINE RUN CAUGHT THEM** — asserting that a TS2339 APPEARS passes on both builds for opposite reasons (unflipped: the union was never narrowed; flipped: the member was correctly removed), so they were rewritten to read the DEFAULT branch and assert `none`, which only a build that actually subtracted the constituent can satisfy. **A THIRD INTENDED PIN WAS DROPPED AS MISLABELLED:** the `annotatedOnly` restriction is currently UNOBSERVABLE — an unannotated `readonly kind = Kind.Alpha` widens to `Kind` before reaching the reader (verified by how it prints: `Type 'Kind'` against `'Kind.Alpha'` for the annotated sibling), so a pin claiming to guard the restriction would really have been pinning the widening; it ships saying so. **The remaining four readers are unflipped, and one is now pinned as a known divergence** — the exhaustive-switch `neverType` gate still reads the annotation, so a parenthesized discriminant narrows correctly but is not accepted as exhaustive (TS2366). That is the next flip._


_Round 750 (2026-07-28) — **(REL.1)(c) STEP 5a: the `"<enumSymId>#<member>"` key space has ONE mint — and the risk the step existed to remove was ALREADY ABSENT, which is the round's deliverable.** Compiler profile `--listAll` **BYTE-IDENTICAL at 46**, diffed sorted line-by-line against a build of pristine `de24c764`, composition unchanged (TS2591x43 / TS2304x2 / TS2584x1, zero TS2322); **740 local tests over 123 classes + 4,669 generated corpus tests** over the `C/D/E/N/S/T/U` classes, **0 failures**; +6 pins. **THE MINT-EQUALITY PROBE, run FIRST as instructed, is the measurement:** a temporary probe inside the single mint printed one deduped line per incoming enum `Symbol` instance across the whole compiler profile — **153 distinct instances, 153 already canonical, 0 needing redirection.** The flip therefore changed no key, which is why the profile is byte-identical rather than merely the same count, and that zero is the evidence (5b) needs before it may compare the AST key space against the type key space. **ROUND 749'S STATED RISK PREMISE IS FALSE and must not be re-derived:** the queue said the AST readers key on `resolveEnumSymbolForDiscriminant(...).id` while the round-741 type interning keys on `canonicalEnumSymbol(...).id` — but `resolveEnumSymbolForDiscriminant` **returns `canonicalEnumSymbol(target)` at BOTH of its return sites**, so they are the same space and the flip as literally specified would have wrapped an already-canonical symbol. **A PRODUCER CENSUS FOUND TWO SITES ROUND 749 DID NOT NAME, and they are why the round is not vacuous:** the key space had **six** producers, not two — `enumSwitchKeysFromTypeNode` (the bare-enum-name expansion to all members) and `enumSwitchKeysFromType` (the RESOLVED-TYPE producer) alongside `enumMemberKeyOfExpr`, `enumMemberKeysOfTypeNode`, `getDeclaredTypeOfEnumMember`'s `enumMemberTypes` interning and the exhaustive-switch `neverType` coverage probe. Each canonicalized on its own, so they agreed **incidentally, at six sites, with nothing making a seventh inherit it** — exactly the omission that would make 5b unsafe, since 5b adds the seventh. **So what landed is the invariant made STRUCTURAL:** `enumDiscriminantKey(enumSym, member)` is the only place the string is built, all six sites call it, and the interning site passes the RAW `owner` rather than its pre-canonicalized local (one canonicalizing hop is correct by construction; two rely on an idempotence nothing pins). **THE PINS PASS ON BOTH BUILDS, AND THAT IS THE CORRECT RESULT** — 5a has no behaviour delta, so a pin FAILING on pristine `de24c764` would mean the step changed an answer it must not; all six were run there (Checker.kt stashed, the test class kept) and all six pass. They are regression guards for 5b, each covering a different PAIR of producers, and the cross-FILE shape is load-bearing in every one because within a single file every path tends to find the same `Symbol` instance. **The sharpest is the negative control:** canonicalization is DECLARATION-IDENTITY based, never name-based, so a value-shifted same-named twin must stay TS2322 — were canonicalization ever "simplified" to merge by name, `getDeclaredTypeOfEnumMember` would intern both to one `Type` and that pin would go silent. **Recorded, not patched:** `canonicalEnumSymbol` memoizes per `sym.id` a decision that reads MUTABLE state (`globals[name]`, `enumValues[global.id] == null`), so a first touch before an enum's values exist freezes the non-canonical answer for that instance — 0/153 on the profile, but 5b's producer touches enums from a different phase._

_Round 749 (2026-07-28) — **(REL.1)(c) STEP 4b: `checkEnumAsgInFunctionScopes` (B583) RETIRES with the `import("<base>")` display rule — the FIFTH enum pass this arc replaces, and the LAST one blocking (c)'s walker sweep.** Compiler profile `--listAll` **BYTE-IDENTICAL at 46**, diffed sorted line-by-line against a build of round 748's tree, composition unchanged (TS2591x43 / TS2304x2 / TS2584x1, zero TS2322). Filtered suite **8,978 tests, 0 failures, 0 skipped** over the 19 generated classes carrying every TS2416 or enum-named baseline plus every `*Enum*` local class; +6 pins. **THE ABLATION, on `enumAssignmentCompat6` run whole (both files, all eight diagnostics):** with B583 live, 8 = the tsc baseline; PassLab-disabled on round 748's build, 8 but the two `f.ts` lines printing `DiagnosticCategory` on BOTH sides; with the rule alone and the pass disabled, **8 byte-identical to the baseline**; with the rule live and the pass's RETRACTION removed, **16 — every one of the eight DUPLICATED, both copies byte-identical**; with the pass deleted, **8 byte-identical** — message, chain, position and length. That fourth row is the strongest form of the evidence, and it needs the retraction gone to be visible at all: unlike B425 and B463, B583 wiped the general TS2322 at its own position before re-adding its own, so the co-emission was masked. **THE DISPLAY DECOMPOSED INTO ONE PART, NOT TWO OR THREE** (B266's had three, B463's two): the container walk in `enumTypeQualifiedDisplay` simply continues one step further, from the enum symbol into the FILE. `enumModuleImportPrefix` is tsc's `getFullyQualifiedName` reaching the source-file module symbol, which `symbolToString` renders `import("f")`; the gate is round 746's `enumCollisionQualifiedDisplays`, **reused unchanged** — the first round in this arc that needed no new gate, because a same-string pair is exactly what a shadowed enum produces. **THE CONDITION IS tsc's, TRANSCRIBED**: in tsc a symbol has a `parent` only when it lives in a container's `exports`, so a top-level EXPORTED declaration of an external module reaches the module symbol while a file-local one — and every function-body declaration, INV.2(c) scope-space symbols included — has none and stays bare. Having no module symbol to walk to, we ask the same question of the declaration: top level, `export`ed, in a file with module syntax. **The `declare namespace` value-KIND split B583 was also credited with turned out to be the relation's already** (`enumMemberEntries`, round 746) — the ablation shows a.ts's `ambients` lines byte-identical with the walker gone, so the walker's measured ownership at the end was the `import("f")` display and nothing else. Non-vacuity: the three RULE pins use containers B583 could not structurally reach (a plain function body, an arrow held by a `const`, an assignment inside an `if`) and all three FAIL on a build of unmodified `d92ebe6a`, printing `Type 'DC' is not assignable to type 'DC'`; the three control pins and both retirement pins pass on BOTH builds, which is what distinguishes reproducing the walker from approximating it. **DELIBERATELY NARROWED, and it is a known divergence**: an EXPORTED namespace of a module file would be `import("f").ns.E` in tsc and stays `ns.E` here — the file step is taken only when the namespace walk found nothing. No corpus baseline asks for it. **NOT FIXED, found while measuring:** the var-decl TS2322 site has neither the collision retry nor the enum elaboration, so `let z: DC = x` inside the shadowing body still prints `Type 'DC' is not assignable to type 'DC'` with no chain — a pre-existing gap B583 never covered either._

_Round 748 (2026-07-28) — **(REL.1)(c) STEP 4: a function-body-scoped `enum` is RESOLVABLE IN TYPE POSITION, and B583's blocker moves from RESOLUTION to DISPLAY.** Compiler profile `--listAll` **BYTE-IDENTICAL at 46** — the whole output, diffed sorted line-by-line against a build of pristine `7ab9b215`, composition unchanged (TS2591x43 / TS2304x2 / TS2584x1, no TS2322). Filtered suite **4,427 tests, 0 failures, 0 skipped** over the six generated classes covering every corpus source that declares a block-scoped enum, plus the Inv2/Inv3/Inv4 name-resolution classes and all `*Enum*` pins; +4 pins. **THE ROUND IS A SIZING FIRST, per the round-740 rule — and the fix was only written because the sizing came back contained.** A static census over the 6,455 corpus sources and the 78 tsc-compiler sources asked how many places a B83.5-unbound declaration is used in TYPE position: **enum = 2 corpus files and 2 profile sites, and that is the whole blast radius.** Corpus baselines changed: **ZERO**. The two corpus files are one of each failure mode — `enumAssignmentCompat6` is the SHADOWING mode, `unusedLocalsInMethod4` the silent-`any` one — and both profile sites (`debug.ts`'s `Connection`, `program.ts`'s `SeenPackageName`, both `const enum`s in function bodies) are silent-`any` and produce no new diagnostic once resolved, including `Connection`'s heavy bit-flag `|=`/`&` arithmetic. **IT IS A RESOLUTION-ORDER CHANGE, NOT A FALLBACK, AND THE SHADOWING MODE IS WHY IT CANNOT BE ONE** — a unique name resolved to nothing, but a name shadowing an outer one resolved to the OUTER symbol, a wrong answer with no miss to detect. `lexicalTypeSymbolForNode` is consulted FIRST in `resolveTypeNameToSymbol`'s Identifier branch: an innermost-first ancestor walk over the INV.2(c) `lexicalScopes` table of the node's OWNING file. **SCOPE-SPACE ONLY (`scope.symbols`, never `existing`) is the whole containment argument** — `declareLexical` skips any name the main binder already bound, so a conventionally-bound name is absent from `symbols` everywhere and resolves exactly as before, and the innermost-first order IS the shadowing rule for free. `computeAllEnumValues` gained a scope-space leg (id-keyed, and scope ids are a disjoint negative space) or the relation would receive a value-less enum. **THE PAYOFF, MEASURED BY ABLATION:** with B583 PassLab-disabled the general path went from **emitting NOTHING** (round 747) to emitting **both verdicts at both correct positions**, printing `Type 'DiagnosticCategory' is not assignable to type 'DiagnosticCategory'` — i.e. exactly the state B266 was in before round 746 and B463 before round 747, with only the `import("f").DC` display left. **IT DOES NOT GENERALISE, DELIBERATELY.** The same census says the other B83.5 kinds are **62x more numerous** in type position (interface 51 corpus files, type 41, class 33, against enum's 2), so widening this to them is a different and much larger change; a negative-control pin holds a block-scoped `interface` unresolved on purpose. Non-vacuity: three of the four pins FAIL on a build of unmodified `7ab9b215` — two because the diagnostic does not fire there at all, the third because it fires when it must not (the outer enum's `Warning = 7` rejecting a `0` the inner one's `Warning = 0` accepts), which is the direction that proves the ORDER changed rather than a gap being filled._

_Round 746 (2026-07-28) — **(REL.1)(c) STEP 2: the relation answers `isEnumTypeRelatedTo`, and BOTH `checkEnumToEnumAssignments` (B425) and `checkNamespaceEnumUnionAssignments` (B266) RETIRE with it.** Compiler profile `--listAll` **at 46, diffed LINE-BY-LINE** (composition unchanged: TS2591x43 / TS2304x2 / TS2584x1, no TS2322), cost gate **+0.00% on all 20 counters** — no rebaseline; +18 pins; 766 filtered tests (the whole generated `TypeScriptCompilerTests_E` plus the new pins) at 0 failures. **THE ABLATION, PER PASS, BOTH WAYS.** B425 on `enumAssignmentCompat3`: 12 with it, **0** PassLab-disabled on the pre-746 build (the relation could not answer), **24 — every one duplicated** with the rule live and the pass still in, and **12 byte-identical to the baseline** once deleted — message, CHAIN, position and length. B266 on `enumLiteralAssignableToEnumInsideUnion`: 5 with it, **3 with the WRONG display** disabled pre-746, **5 verdicts but still the wrong display** with the rule alone, **5 byte-identical** once the DISPLAY rule moved too. That middle row is the round's lesson: **the verdict and the display are separate deliverables**, and a pass can be replaceable on one and not the other. **THE RULE HAD TO GO IN `checkTypeRelatedToCore`, NOT `isSimpleTypeRelatedTo`** — every enum-flavored type is a MEMBER-LESS `Type.Object`, so a `false` in the fast path merely falls through to the structural engine, which relates two empty objects VACUOUSLY (written there first, measured zero change). `enumTypesRelation` returns a `EnumRelFailure?` rather than a Boolean because the elaboration needs the same walk. **The domain trap is PER-MEMBER now (`enumMemberEntries`)**: an ambient non-const member with no initializer has NO value in tsc, and reading our auto-numbers instead would reject `declare enum D { Red, Green }` against a concrete `{ Red = 5 }` — a program tsc accepts, and 2 of the 8 baseline lines of `enumAssignmentCompat6`. Two display rules moved with the passes, each **SPLIT rather than widened** per round 745: `enumCollisionQualifiedDisplays` (tsc's `getTypeNamesForErrorDisplay` — same string on both sides ⇒ requalify both, gated to enum-flavored pairs) and `enumUnionTargetDisplay` (members print qualified, a consecutive full-coverage run collapses to the bare enum name, non-enum constituents sort first). **WHAT DID NOT WORK, and it is the finding: a value-aware relation makes EVERY dedicated enum walker co-emit, not just the two in scope.** The suite caught two the ablations did not predict — B583 (`checkEnumAsgInFunctionScopes`, all six of `enumAssignmentCompat6` duplicated byte-identically) and B463's TS2416 (`enumAssignmentCompat7`) — both resolved by the round-744 RETRACTION rather than more deletion, because each still owns the only correct DISPLAY (`import("f").DiagnosticCategory`; `(param: second.E) => void`). **They are the next retirements, and both are now blocked on a DISPLAY rule rather than on a verdict** — the state B266 was in when this round started. Non-vacuity measured against a build of unmodified `8a120fc6`: four pins are silent there, and the fifth fails in the OTHER direction — round 744's verdict rejected a value-identical twin's member as an FP, so the retirement **removes an FP** as well._

_Round 745 (2026-07-28) — **(REL.1)(c) STEP 1: the relation judges an enum by VALUE, and `checkEnumLiteralAssignments` (B203) is RETIRED in the same commit.** Corpus **12,971 -> 12,982 / 0 failures / 3 skipped** (+11 pins), compiler profile `--listAll` **at 46, diffed line-by-line** (composition unchanged: TS2591x43 / TS2304x2 / TS2584x1, no TS2322), cost gate **+0.00% on all 20 counters** — no rebaseline. **THE ABLATION IS THE EVIDENCE, BOTH WAYS.** Before: disabling B203 through PassLab left `enumAssignmentCompat5` emitting **nothing** — the relation could not answer. After: the relation alone emits all three baseline lines **byte-identically** (message, position and length), and with B203 still enabled the run shows **6 diagnostics, each of the three duplicated** — exactly the co-emission round 740 predicted, which is why the rule and the retirement are ONE commit. **THE RULE IS tsc's, VERBATIM:** the wide `number` stays assignable to any numeric enum or member (the bit-flag compatibility rule, so `a = n` is still legal), but a numeric LITERAL is assignable only to a member whose value it EQUALS, or to any member of an enum whose domain is not fully known. That last clause is `enumValueDomainIsComplete`, and it is where tsc's *computed* enum lives: **a member with no initializer in an AMBIENT non-const enum has NO value in tsc**, so `declare enum D { X, Y }; let d: D = 7` is legal — we auto-number those because the Transformer needs a value to emit, and B203, which had no ambient notion at all, reported it. The retirement therefore **removes an FP** as well as adding four shapes B203's top-level-var scan never saw (function bodies, string enums, negative literals, `const enum`). **WHAT DID NOT WORK, and it cost a profile run: the message needs the source's literal VALUE, and the obvious way to get it — admitting enums to `propTypeContainsLiteral` — is WRONG.** That predicate has ~25 consumers and most are not about display: several choose `literalTypeOfExpression` over `getTypeOfExpression` for the SOURCE, so `languageVersion = undefined!` (parser.ts:1790) reached the relation as the pre-assertion `undefined` and the profile went **46 -> 50**. Split into `ts2322KeepsSourceLiteral` (display) and `enumNumericLiteralSource` (source, restricted to a `Type.NumberLiteral` by construction) it is 46 again. An enum-MEMBER target also prints QUALIFIED now (`E.A`, not `A`) — `formatTypeForDisplay` reduces a `QualifiedName` to its last name, which is right for a namespaced interface and wrong for an enum member. **STILL LOAD-BEARING, unchanged:** `checkEnumToEnumAssignments` (all 12 of `enumAssignmentCompat3`) and `checkNamespaceEnumUnionAssignments` (2 of 5 of `enumLiteralAssignableToEnumInsideUnion`) — both are enum-to-enum VALUE identity, a different rule from this one._

_Round 744 (2026-07-28) — **(REL.1)(b) LANDED: an enum MEMBER now relates only to the SAME member, and ALL FIVE `@Ignore`s are ON.** Corpus **12,967 -> 12,971 / 0 failures / 3 skipped** (+15 pins across three commits, -5 skipped as the `@Ignore`s flip); compiler profile `--listAll` **BYTE-IDENTICAL at 46** — the whole output, not just the count. **The two blocking profile FPs were BOTH enum-free, and both reduced on the first attempt once the model stopped copying the diagnostic message.** `declarations.ts:846` reported `T | undefined` vs `T | StringLiteral` and was really about neither: narrowing a bare TYPE PARAMETER by a type predicate REPLACED it with the candidate instead of intersecting, so the branch join lost `T` — the failing constituent was `NoSubstitutionTemplateLiteral`, and an EMPTY guard body reproduces it. `utilities.ts:4175` was an intersection SOURCE carrying a union not DISTRIBUTING (`A & (B | C)` denotes `(A & B) | (A & C)`), invisible until the union constituents are written in a DIFFERENT ORDER on the two sides, which is exactly what tsc's `getNameOrArgument` does. Both are pre-existing defects independent of enums — verified against an ablation build of pristine main — so they landed on main as their own commits BEFORE (b). (b) additionally RETIRED the round-459 AST enum-member key gate (its three original pins keep passing unchanged: the ablation evidence that the type path really answers), added a CONSTRAINT check to round 481's bare-`Type.TypeParam` overload lenience, deleted (a)'s string TARGET leg, and made B266 retract the general TS2322 it now co-emits with. **(c) IS BLOCKED, MEASURED NOT GUESSED**: a PassLab ablation census of the three AST-only enum passes shows every one uniquely load-bearing — `checkEnumToEnumAssignments` owns all 12 of `enumAssignmentCompat3`, `checkEnumLiteralAssignments` all 3 of `enumAssignmentCompat5`, `checkNamespaceEnumUnionAssignments` 2 of 5 of `enumLiteralAssignableToEnumInsideUnion`. All three are the VALUE-equality rule (a) deliberately did not model, so (c)'s first deletion needs a value-aware disjointness rule first. Cost gate: A and B +0.00% on all 20 counters; (b) rebaselined one — `narrow.memoServed` +3.16%, the round-459 retire's trade, against `globals.lookups` **-1.64%** as `canonicalEnumSymbol`'s consult disappears._

_Round 743 (2026-07-28) — **(REL.1) PART 1 DONE: the overload picker was doing TWO things wrong, and neither is about enums.** Corpus **12,949 -> 12,956 / 0 failures / 8 skipped** (+7 pins), compiler profile `--listAll` **BYTE-IDENTICAL at 46** across both commits. **(1) The B136 concrete-overload SWAP re-picked an overload `resolveCallOverload` had ALREADY REJECTED.** When the chosen signature's return still carries an un-inferred type parameter, the swap looks for a non-generic overload with a concrete return — and took the FIRST such overload **without ever asking whether it accepts the arguments**, which is how `factory.createToken(SyntaxKind.ReadonlyKeyword)` selected the correct generic `<TKind extends ModifierSyntaxKind>` overload and was then swapped back to `createToken(token: SyntaxKind.SuperKeyword): SuperExpression` (checker.ts:7997). The omission was invisible for the Array predicate pair the rule was written for, where the non-generic overload accepts whatever the generic one does. The type-based verdict is now extracted as `signatureAcceptsArgs` so any site adopting a signature asks the SAME question. **THE REDUCTION NEEDED THE GENERIC OVERLOAD** — two same-arity NON-generic overloads distinguished only by an enum-member parameter already selected correctly via the round-459 AST gate, which is why round 742's two earlier reductions came back clean and blamed the barrel and the enum size. **(2) Overload selection could not see an ASSERT narrow.** `getTypeOfIdentifier` answers from `currentLocalTypes` and the declaration tables: an `if (isFoo(x))` narrow is recorded there, an `asserts` narrow lives ONLY in the flow graph behind `getNarrowedTypeForReference`, which every emission site opts into individually and selection never did — so `Debug.assert(isKeywordOrPunctuation(kind)); tokenToString(kind)` picked the `string | undefined` overload (parser.ts:2494). Fixed as a SECOND CHANCE on the rejecting path only, so it can only turn a rejection into an acceptance. **Both reductions are ENUM-FREE**, which is what makes the pins bite on main. Cost gate: +0.16% on commit 1; on commit 2 `narrow.memoServed` +9.93% and `typeNode.bypassed` +23.26%, **justified and rebaselined** — 87% of the extra narrowing requests are served by the round-664 memo (`narrow.walks` only +0.83%) and the wall is flat interleaved (26,098/26,593 vs 26,312/26,394 ms). **PART 2, (REL.1)(b): 4 profile FPs -> 2, still parked, now on `wip/round743-rel1b`** (all five `@Ignore`s flip ON there). Remaining: `utilities.ts:4175` (intersection-over-union distribution) and `declarations.ts:846` (`if (!input) return` failing to strip `undefined` from a `T | undefined` where T is a constrained type parameter). **Neither reduced — a hand-built model of each shape passes**, which per round 742's rule means a missing ingredient, not an absent bug. (c) the scaffolding deletions stay open._

_Round 742 (2026-07-28) — **(REL.1)(b0) DONE: an enum-member EXPRESSION is a member type, not `any` — and that was the blocker under (b), not the relation.** Step (a) minted the member type but only ANNOTATIONS could reach it: an enum's own `Type.Object` carries no member table, so `getPropertyOfType` missed `E.A` and `computeRawTypeOfPropertyAccess` fell through to its `anyType` tail. That is why round 741 could flip three of the four `EnumMemberRelationTest` expectations from the relation but not `const e: E.X = E.Y` — the relation was being handed `any`, however sharp its rule was — and why round 740's `take(Ext.Dts)` control was vacuous. **Corpus 12,940 -> 12,949 / 0 failures / 8 skipped (+9 pins), compiler profile `--listAll` back to 46 (byte-identical output), cost gate max +0.35% on the first commit (rebaselined) and +0.00% on the second.** **THE AUDIT, NOT SITE-BY-SITE FIREFIGHTING.** Round 741's gotcha says every classifier whose rule is "an enum value is a number/string at runtime, never an object" must accept `Enum or EnumMember`; six more sites moved to `isEnumFlavoredObjectType` and the two arithmetic classifiers now answer PER MEMBER from the member's own constant. **Two were WRONG in a direction a mechanical widening would have missed:** `comparabilityCategory`'s blanket `EnumLiteral -> number` would have called a STRING member a number and FP'd a cross-category TS2365, and the discriminant filter's "an object can never strictly-equal a primitive literal" rule silently stopped excluding enums the moment a discriminant was a member type. **THREE PRE-EXISTING BUGS THE `any` HAD BEEN MASKING — fixed at the root, because they ARE the content.** (i) `isComparableType` never resolved a TYPE PARAMETER's constraint, so `token >= SyntaxKind.FirstToken` with `TKind extends SyntaxKind` FP'd 6 TS2365 (the sibling rule had always been in `isValidArithmeticOperand`; only the relational path lacked it). (ii) The arithmetic pass's first-wins local recording refused a nested body's own `const state = exception.state` because an ENCLOSING body's `let state: Identifier` was still in the INHERITED locals map — `spineArithInheritedName` decides inheritance by instance identity against the innermost saved map, so first-wins still holds within a scope. (iii) `checkVarDeclAssignabilityCore` has its OWN inline literal-widener, separate from `widenType`, and it is what the TS2322 assignment check reads as the target's declared type — so `let flags = TransformFlags.None; flags = TransformFlags.ContainsESNext`, the way tsc starts every flags accumulator, became an error; the same widening must also DISTRIBUTE over a union (`let variance = cond ? VarianceFlags.Invariant : ...`). Those two widening paths alone were **23 of the 28** FPs that (b)-on-top-of-(b0) showed. Member types also now print QUALIFIED (`SyntaxKind.Identifier`), as tsc does — the bare member name is also an unrelated interface's name. **(b) IS PARKED AGAIN, on `wip/round742-rel1b`, where ALL FIVE `@Ignore`s flip ON** (including the fourth, which (b0) unblocked, and the fifth, which the deletion of step (a)'s string TARGET leg unblocks — tsc rejects `string -> Ext.Dts`). **What blocks it is EXACTLY round 741's four FPs** (checker.ts:7997 TS2769, parser.ts:2494 TS2345, declarations.ts:846 TS2322, utilities.ts:4175 TS2322): that the count returns to FOUR after a change putting member types into every enum-member expression in the program is itself evidence for round 741's read that these are relation/narrowing gaps passing vacuously, not enum work. **Diagnosed, not fixed: checker.ts:7997 is an OVERLOAD-PICK bug with the enum as witness** — `factory.createToken(SyntaxKind.ReadonlyKeyword)` picks the FIRST overload (`token: SyntaxKind.SuperKeyword`) and returns `SuperExpression`; reduced to a standalone repro, two same-arity overloads whose parameters are DIFFERENT enum members pick the first, while the same pair distinguished by ARITY picks correctly. (c) the scaffolding deletions stay open._

_Round 741 (2026-07-28) — **(REL.1) STEP (a) DONE: the enum-MEMBER type exists, and it changed no answer.** `SK.A` used to resolve to **`anyType`** — `getDeclaredTypeOfSymbolWorker` had no `SymbolFlags.EnumMember` branch — so `kind: SK.A` and `kind: SK.B` were literally the SAME `Type` instance and the relation was never asked a question it could get wrong. It now mints a distinct `Type.Object(Object or EnumLiteral)` per member, **interned on `"<canonicalEnumSymbol.id>#<memberName>"`** (a per-SYMBOL mint would hand two non-equal types to one member — the exact catastrophe `canonicalEnumSymbol`'s own doc records — because the same enum arrives as the merged global, a file-local and a barrel-resolved alias), and the enum's own type is flagged `Enum`, so **`TypeFlags.EnumLiteral` has a writer for the first time and the widening rule written for it at Checker.kt:143100 finally executes.** **Behaviour-preserving by construction and by measurement: corpus 12,935 -> 12,940 / 0 failures / 8 skipped (+5 pins, +1 `@Ignore`), compiler profile `--listAll` BYTE-IDENTICAL at 46 errors (whole output), cost gate max +0.40% (rebaselined, justified: unions that used to collapse to one `anyType` member now carry their real constituents).** The entire measured gap was **one rule family, enum member <-> its base primitive, in both directions** — and the mechanism is one line: `isNumericEnumObjectType`/`isStringEnumObjectType` classify by `SymbolFlags.Enum` on the type's symbol, which a MEMBER symbol does not carry, so neither pre-existing rule could fire. An ablation build (legs deleted) fails all four leg pins. The legs are deliberately **value-blind**: `let a: E.A = 2` where `A === 0` is still `checkEnumLiteralAssignments`' error, and the relation co-emitting it is the 4 spurious TS2322 round 740's probe measured. **The one knock-on was not union collapse but a CLASSIFIER: `typeof x === "object"` asks `symbol.flags.hasAny(SymbolFlags.Enum)`, false for a member, so `ModuleKind.ESNext` survived a narrow it must not survive (program.ts:1341 TS2339). Closed with `isEnumFlavoredObjectType` at both sites of that family — recorded as a CLAUDE.md gotcha, because every "an enum value is a number/string, never an object" classifier now has to accept `Enum or EnumMember`.** Both round-740 latent hazards closed too (`SK.A` was looked up under the bare `"A"` in `currentTypeParamScope`/`currentTypeAliasArgs` and in the round-444 `globals[name]` recovery). Also found: one of round 740's four positive controls (`take(Ext.Dts)`) is VACUOUS — an enum-member EXPRESSION types as the enum, never as a member — which is why the new pins annotate through `declare const d: Ext.Dts`. **(b) WAS attempted and is PARKED on `wip/round741-rel1b-disjointness`: the one-line rule flips 3 of the 4 `@Ignore`s ON but costs 4 profile FPs (46 -> 50) in one family — an enum-member union that used to collapse to a single `anyType` member now has real constituents, so a relation/narrowing path that passed VACUOUSLY stops passing (overload pick, assertion-narrow, intersection-over-union). The 4th pin cannot flip from the relation at all: an enum-member ACCESS EXPRESSION types as the ENUM, so only ANNOTATIONS reach the new type. And the obvious hypothesis for the FPs — one member splitting into several `Type` instances because `canonicalEnumSymbol` cannot canonicalize a module-scoped enum post-INV.3(d) — is measured FALSE: tsc's structural `isEnumTypeRelatedTo` verdict leaves the profile at the same 4.** (c) the scaffolding deletions stay open._

_Round 740 (2026-07-28) — **(PERF.HW) DONE: the cores are REAL and the item's premise is still wrong — a SEQUENTIAL xtsc run already consumes 3.15 of the 4 cores, so 79% of the machine is spoken for before the first worker exists.** The item asked "does this VPS have real cores, so M2 is worth reviving", with unpark condition ">= 8 real cores". **The box: `nproc` 4, AMD EPYC-Rome @ 2445 MHz, 4 distinct `core id`s with ONE thread sibling each (no SMT), NO cgroup `cpu.max`, and steal time 0.0 mean / 0 max over 72 vmstat samples.** Steal alone does not settle it (a hard quota need not be accounted as steal), so the cores were tested directly with a tiny-working-set pure-CPU loop: **1.00x / 1.56x / 3.45x / 3.61x at 1/2/4/8-way — four real, independent, unthrottled cores.** **THE SCALING TABLE** (compiler profile, `--noEmit`, -Xmx4g, 3 reps, ROUND-ROBIN interleaved across levels rather than round 666's blocks): **w1 27,126 ms | w2 24,452 (per-rep median -11.67%, wins 3/3) | w4 25,976 (-4.24%, wins 2/3, per-rep deltas STRADDLE ZERO = undecided) | w8 32,212 (+19.37%, wins 0/3)**, drift band re-derived from w1's own reps at **+-2.87%**. That reproduces round 666 (seq 27,873 / w2 24,669 / w4 flat) and adds the w8 point. **THE EXPLANATION, which no round had measured: 85.6 s of USER CPU for a 27.1 s wall = 3.15 cores in a run everybody calls single-threaded.** Attributed by starvation: `-XX:CICompilerCount=2` drops it to 2.34 cores / 62.8 s user (**JIT ~21.7 s of CPU**), `-XX:ParallelGCThreads=1 -XX:ConcGCThreads=1` to 3.06 (**GC only ~2.7 s**) — C2 compiling a ~110k-line `Checker.kt` never finishes inside a 27 s run. **Self time is FLAT across all four configurations (26.6-27.8 s), so the JIT is not stealing from the compile thread** (round 618's "flag hunting is dead" holds) — it is consuming the cores a WORKER would need, leaving ~0.85 free. Every level then saturates at the same **~3.6-core ceiling** (3.49/3.57/3.65) and what actually changes with worker count is TOTAL WORK: **user CPU w1 85.6 s -> w2 85.3 (+0%) -> w4 92.6 (+8%) -> w8 117.7 (+37%)**, because each worker re-binds every file and runs all ~318 collectors. w2 divides without adding and banks -11.7%; w4/w8 add CPU to a machine with none left, and it converts straight into wall. **NOT a JIT artifact — tested and negative: freeing ~0.34 cores at w4 (`CICompilerCount=2`, 3.62 -> 3.28 cores) left the wall unmoved (25,870 -> 25,574).** Four INDEPENDENT concurrent compiles took 3.85x each (aggregate throughput 1.03x) — which is **82% parallel efficiency** once the 3.15 figure is applied (4 x 85.6 core-seconds on 4 cores floors at 86 s; measured 105 s). **The box parallelises fine; our compile does not, because one copy already occupies 79% of it.** Amdahl: the w1/w2 fit gives P = 5,348 ms divisible (**19.7%**), R = 21,778 ms — infinite-worker floor **-19.7% (1.25x) ever**; the w1/w4 fit gives P = 5.7%, disagreeing 3.5x, so per the rule fixed before the run the model is contention-broken beyond w2 (and 19.7% vs round 666's 23% is NOT resolvable — P is twice a delta whose per-rep spread is 1,554 ms). **ALSO FOUND, a correctness gap: `--workers N` IS NOT BEHAVIOUR-PRESERVING — 62 diagnostics vs sequential 46.** The 16 extras are one family in one file (`utilities.ts:11349..11410`, TS2322 `EvaluatorResult<T>` not assignable to `EvaluatorResult`), **identical at w2/w4/w8** and deterministic across reps — the round-609 signature (a program-wide collector iterating the INV.6 partition view instead of `binderResults`), not a race. **VERDICT: M2 stays parked, and the unpark condition is REWRITTEN** — ">= 8 real cores" is necessary but insufficient; the measured requirement is >= 8 cores *net of the ~3.2 the JVM's own JIT/GC consume*, i.e. realistically **>= 12**, and that tax is FIXED per JVM (it does not grow with workers), so a larger host simply out-sizes it. Revival order: close the divergence, shrink R, only then re-probe. Predictions **5 of 6** (P5 falsified: w8 peak RSS 2,240 MB fits -Xmx4g easily, GC 1.1 s of the 5.4 s regression — **no level was skipped for want of RAM**; memory was never the constraint). No `src/` change -> no suite run (12,927 stands). Artifact: `docs/perf/worker-scaling-round740.md`. **PART 2, (REL.1) DECOMPOSED AND SIZED (fix deliberately NOT attempted): the blast radius everybody feared is ONE corpus baseline, and the root cause is not "the relation is lenient" — it is `anyType`.** The item said "every enum-typed comparison in the corpus goes through this, TS2322/TS2367/TS2345 baselines included". Measured: **12,927 tests, 1 failure.** **ROOT CAUSE LOCATED:** `getTypeFromTypeReference` reduces `SK.A` to the BARE member name `"A"`, resolves it to a `SymbolFlags.EnumMember` symbol, and `getDeclaredTypeOfSymbolWorker` **has no branch for that flag** — it falls to `else -> anyType`, so `SK.A` and `SK.B` are *the same `Type` instance* and the relation was never asked a question it could get wrong. **Corollary: `TypeFlags.EnumLiteral` is SET NOWHERE — all ~11 read sites are dead, including a widening rule at Checker.kt:143100 that is already written and waiting for a flag that never arrives.** **MEASURED with a throwaway 3-edit probe, then REVERTED** (proof: `git diff src/commonMain` empty + profile `--listAll` byte-identical at 46 errors + cost gate all 20 counters +0.00%): corpus **1 failure**, compiler profile **46 -> 52 (+6)**, **no perf cost** (26,192 ms in the 26.5-27.1 s band). **AND THE ONE FAILURE IS THE MISSING LEG, NOT THE ADDED ONE** — 4 spurious `Type 'number' is not assignable to type 'A'`: a numeric enum member must stay assignable FROM `number` and from a literal equal to its value. The profile's +6 is the same family (`Extension.Dts`, a STRING member, not assignable to `string`). **The entire measured gap is ONE rule family: enum member <-> its base primitive — so this is a SESSION, not an arc.** Decomposition written into the queue item: (a) mint the type + the base-primitive legs, behaviour-preserving by construction; (b) add disjointness (acceptance = the four `@Ignore`s flip on); (c) delete the scaffolding one walker per commit. **Deletable: `discriminantPropAnnotation`, `kindDomainProvesNotSubtype`, `kindDomainKeysExceed` and THREE whole AST-only passes** (`checkEnumLiteralAssignments`, `checkNamespaceEnumUnionAssignments`, `checkEnumToEnumAssignments`), plus twelve AST key-space helpers; **but `canonicalEnumSymbol` SURVIVES** — its duplicate-`Symbol`-instance problem is independent of the relation and a naive per-symbol mint reproduces the catastrophe its doc records. Two latent hazards recorded (`SK.A` is looked up under the bare name `"A"` in `currentTypeParamScope`/`currentTypeAliasArgs` and falls back to `globals["A"]` — invisible only because everything collapses to `any`). Suite **12,927 -> 12,935 / 0 / 7** (+8 `EnumMemberRelationTest`, 4 `@Ignore`d pins naming the item + 4 NOT-ignored positive controls that are the FP firewall for step (a))._

_Round 739 (2026-07-28) — **(BENCH.1) DONE: the yardstick was broken somewhere else, and round 738's own correction is RETRACTED.** 738 inferred that "every published xtsc-vs-tsc `--no-emit` ratio compared our check+emit against tsc's check-only, so the honest gap is ~2.15x". **Reading the scripts instead of assuming: `bench-3way.sh` runs xtsc, tsc AND tsgo WITH EMIT**, so the published 2.4x was already like-for-like and `skipEmitOutputs` (set only from `ProjectCompiler`'s own `noEmit`) cannot move it. 738's arithmetic applied our emit fraction while implicitly taking tsc's as ZERO — the ratio's FLOOR, not its value, and the error ran in the direction that flattered us. Retracted in place in six files. **THE REAL MISMATCH, STILL OPEN: two different compiles.** ARCHITECTURE-RETHINK section 0.1's budget is a `--noEmit` compile; the 2.4x it is compared against is emit-on-both-sides — indistinguishable before 738 (our `--noEmit` emitted anyway, it just did not WRITE), divergent after. Measured, same binary, 4 interleaved pairs: **check-only 26,896 ms vs emit 29,194 ms = the emit work is 8.5% of a check-only compile, B slower 4/4**. **AND THE COLUMN THAT MATTERS WAS NEVER MEASURED: the bench has never run tsc/tsgo with `--noEmit`.** It is bounded, not free — `R_ck = R_emit x 0.921 / (1 - s_tsc)`, i.e. **>= 2.21x and > 2.4x as soon as tsc's emit exceeds 7.9% of its run**. **CONFIRMED EMPIRICALLY MID-ROUND: CI posted a 3-way row for `3570483cf3da` — round 738's OWN HEAD, the commit that landed the gate — at 30.82s/12.69s = **2.43x**, against **2.44x** for the commit immediately before it. The gate does not move the published column, as predicted from the code path.** **HONEST PUBLISHED RATIO, with basis: 2.28x median over ALL 341 CI runs / 2.40x over the last 30, EMIT mode, wall clock — per-row spread 1.87x-2.72x, because xtsc is ONE cold JVM run per row against tsc's median of three, so no single row is the ratio.** SECOND BUG, same yardstick: the LOC parse also matched `throughput: N LOC/s` and took it, so **all 340 archived reports published THROUGHPUT as their LOC count** ("7,004 LOC" for a 194,702-LOC program). LANDED: `bench-3way.sh` now measures BOTH modes on all three compilers (`--modes`), LOC/wall parses fixed, `bench-history/README.md` restructured into a marked two-mode table over a labelled pre-739 archive, all 8 profiles re-baselined check-only (compiler **26,518 ms**) with a MODE-DISCONTINUITY block in each TSV — `emitted=0` alone does not distinguish "did not emit" from "emitted and discarded". NOT POSSIBLE HERE: the 3-way itself (no node/tsc/tsgo, no network — CI installs them), so the check-only ratio stays a BOUND until the next CI run; mixing a local wall against a CI tsc number would be the very error this round fixes. ALSO: **`bench/` is gitignored** — that "dashboard" is local-only. **And (FRONT.1)'s own headline, honestly: three estimates of the same quantity are 2,623 ms (phase), 2,298 ms (mode A/B) and -3,570 ms (738's cross-binary A/B, -11.42%); two of three cluster at 8-9%, so state it as ~8-9% with 11.42% the high end.** No `src/` change -> no suite run (12,927 stands). Full derivation: `docs/ARCHITECTURE-RETHINK.md` section 0.2. **PART 2, (ENGINE.1) — the 14x does not survive contact with its OWN site, and that needed no new measurement.** 265/19 compares the FP-firewall walkers against **the final relation call alone**, which is **2.2% of the function it lives in**; a general rule engine must still resolve the target node, compute the source type, infer unannotated initializers and narrow — all already measured in round 738's own partition. Re-classified: **engine work 483 ms (55.4%), dedicated-walker layer 326 ms (37.4%), bookkeeping 54 ms — the layer is 0.67x the engine work, not 14x it**, and the quantity a scope decision turns on is **326 ms of a 26,896 ms check-only compile = 1.21%**. **Deletable is less**: the weak-type rule is 165 ms = HALF the layer and is real TypeScript semantics tsc implements inside `checkTypeRelatedTo`, so it MOVES rather than vanishes -> honest range **0.6-1.2%** for this site. Method correction for the two remaining sites (else they are not comparable): report ms and share of the COMPILE, never a ratio against the relation; split "re-implements a rule tsc also has" from "corrects our own relation". **A grep census would have scored site 2 at ZERO** — `checkReturnAssignability` (802 lines) has no `tryEmit*` calls at all, its firewall is inline `if (...) return` guards. Census: 805 `check*` + 181 `emit*` + 60 `tryEmit*` = 1,046 functions / 171,934 lines. **Sites 2-3 NOT started — deliberately, not thin**; predictions E1-E4 recorded for scoring. Artifact: `docs/perf/engine-rule-price.md`._

_Round 738b (2026-07-28) — **(FRONT.1): the FIRST front-end attribution, and the arc's LARGEST landed win: `-11.42%` median, B wins 6/6, per-pair range [-4,099, -2,773] ms against a +-590 ms band.** Section 0.1 stage 5 said "the front end, ~20%, unprofiled". **The front end is 11.0%** — crawl WALL 1,683 ms (5.4%, and it already contains the read, decode AND PARSE of all 9,977,097 chars, 16 in flight), bind 1,622 ms (5.2%), config 102 ms, extractRelativeImports 17 ms, **core parse loop 0 ms (78 of 78 pre-parses reused)** — and it has **no lever in it**. **The OTHER 9.2% of that never-measured region was `Transformer.transform` + `Emitter.emit` producing JavaScript a `--noEmit` build immediately threw away**: `noEmit` was consulted only where outputs are WRITTEN, so the core emitted all 78 files regardless — **2,623 ms of a 31,235 ms compile (8.4%)**. Gated by a NEW `CompilerOptions.skipEmitOutputs` set only by `ProjectCompiler`, deliberately NOT `options.noEmit` (440 corpus tests set it as a DIRECTIVE); the Phase-3 loop was verified to contribute NO diagnostics first, so the skip is neutral by construction. **THIS IS A SCOPE CORRECTION, NOT AN ALGORITHMIC SPEED-UP: real `tsc --noEmit` does not emit either.** (The follow-on claim that the published ratio was therefore inflated — "~2.15x, not 2.4x" — is **RETRACTED by round 739**: the CI 3-way runs ALL THREE compilers with emit, so it was already like-for-like and this gate does not move it.) **SECTION 0.1's STAGED PLAN, HONESTLY: stage 1 ~0.3-1% against a claimed 11-19%; stage 2's premise VOID (priced as "stage 1 unlocks it"); stage 3 STRUCK (2.9% ceiling); stage 4 the only one that paid (-4.53%, ~2% left); stage 5 measured and empty. "~1.4-1.7x of today" is NOT SUPPORTED — the plan's remaining honest value is single digits.** Suite 12,923 -> **12,927 / 0 / 3** (+4 `SkipEmitOutputsTest` incl. a directive negative control), `--listAll` byte-identical, cost gate 18/20 at +0.00% with globals.lookups -9.03% / globals.misses -9.11% FALLING (the Transformer's own checker queries) and rebaselined in the same commit. Artifact: `docs/perf/front-end-attribution.md`._

_Round 738 (2026-07-27) — **(TYPE.2) DONE: BOTH of the item's priors FALSE, the second by 65x, and `checkVarDeclAssignability` is not what its name says.** A new two-level partition (`CtaSections`, `--ctaSections{,Coarse}`, opt-in, behaviour-free when off) opened the third-largest spine handler for the first time. Prior (i) "the 36 us per initializer is mostly FLOW NARROWING": narrowing is **1 ms of 872 ms (0.11%)**. Prior (ii) "the ~2,470 ms outside the typing is the assignability RELATION": `checkTypeRelatedTo` is **13 ms (1.5%)** — round 735 found the SAME prior wrong by 48x one function over, so it is now falsified in BOTH of the compiler's largest assignability sites. **What is there is the exit profile: 12,960 of 15,116 invocations (86%) never reach an assignability check** — they are UNANNOTATED declarations whose whole job is `getTypeOfExpression(init)` plus a map write, **405 ms = 46% of the function**; only 797 (5.3%) reach the target-type computation. Two populations sharing a name: **12,960 unannotated at 34 us each, 1,881 annotated at 227 us each** (round 737's 36 us was their mean). **The handler is 2,363 ms and 85% of it is four callees' OWN checking work** (checkVarDeclAssignability 891, checkReturnAssignability 615, checkAssignmentExpression 318, walkFunctionBodiesInExpr 181); level A was opened on the HANDLER so the eligibility gate + parent-chain climbs are a ROW — **194 ms over all 856,976 nodes = 212 ns each** — and with the 158 ms ambient scaffolding that is **1.2% of the compile**. **NOTHING LANDED**: the only candidate (hoist the unannotated branch above the ~18-walker prologue) is worth **~0** because every prologue walker already bails on `decl.type ?: return false`; the prologue's 265 ms is spent on the 1,881 ANNOTATED decls and is **14x the relation it exists to correct**. Predictions 3 of 4 (P3 falsified: walkFunctionBodiesInExpr 7.7%, not >=15%). Suite 12,916 -> **12,923 / 0 / 3** (+7 `CtaSectionProbeTest`), `--listAll` byte-identical in all three modes, cost gate all 20 counters +0.00%. Artifact: `docs/perf/var-decl-attribution.md`._

_Round 736 (2026-07-27) — **(CALL.3) DONE, and the M5 attribution arc has its FIRST LANDED WIN: `-4.53%` median wall on the compiler profile, B wins 6/6 interleaved pairs, outside the +-2% drift band by 2.3x.** Round 735 handed forward 394 narrowing walks costing 1,485 ms and forbade designing before two numbers existed; both were measured with a new opt-in probe (`NarrowSections`/`NarrowProbe`, `--narrowSections{,Coarse}`, behaviour-free when off). **(i) ARRIVALS vs DISTINCT: a `>= 1 ms` walk arrives at 1,900 flow nodes but only 214 DISTINCT ones — revisit factor 8.85 against 1.48 for a typical walk. The tail was never a bigger graph; it is the same small graph walked nine times. (ii) THE PER-ARRIVAL SPLIT: 51% of the entire narrowing population is `applyConditionNarrowing` (1,412 ms / 759,784 calls / 1,858 ns), and the tail's arrivals are 6.3x costlier purely because of their MIX — `FlowCondition` is 41% of tail arrivals against 18% overall, `FlowBranchLabel` 22% against 9%, while cheap `FlowCall` pass-throughs fall 57% -> 19%.** **THE CAUSE WAS ONE CONDITION.** The intra-walk memo `NarrowFlowMemo.served(id, depth)` answered only when `depth <= storedDepth`, so a flow node reached again by a LONGER path recomputed its whole antecedent subtree — **631,585 arrivals compile-wide, 426,753 of them at `FlowCondition` nodes, against 290,011 serves.** That condition guards exactly one thing (a deeper entry has less budget before `NARROW_MAX_DEPTH`=2000), and that is DECIDABLE, not approximable: `depth` influences the result through no other channel, and only non-truncated results are ever stored, so an entry now carries `hi` = the max depth its own subtree reached and serves a deeper probe iff `depth + (hi - storedDepth) < maxDepth`. The one non-obvious part: a served hit must fold `depth + servedHeight` into the CALLER's height or the disjunct goes unsound under nesting. Mirrored into `narrowTypeFromFlowFollowLoopEntry`. **EFFECT: invocations 1,455,915 -> 659,592 (-55%); arrivals 4,759,476 -> 3,500,214 (-26%) with DISTINCT nodes UNCHANGED at 3,212,764; `applyConditionNarrowing` calls 759,784 -> 333,031 (-56%); the `>= 1 ms` tail 429 -> 230 walks and its arrivals 815,259 -> 34,490 (-96%).** Distinct unchanged to the node is the signature of a correct memoisation change. Context that made the argument checkable: maxDepth reached is 249 of 2000 and depth/budget/cycle truncations are 0/0/0. **WHAT DID NOT WORK, both priced: (1) a "does this condition mention the name" pre-test — 95.6% of `applyConditionNarrowing` calls return their INPUT unchanged, but they are the CHEAP tail at 949 ns against 21,708 ns for the calls that narrow, so the whole population is ~410 ms net = 1.3%, INSIDE the band before the pre-test's own cost; (2) memoising the fast-forward chain's pass-throughs — `FlowCall` is 57% of arrivals and the biggest miss population in the table, and also the cheapest at ~131 ns each, so its entire revisit share is <=120 ms.** Corpus 12,899 -> **12,910 / 0 / 3** (+4 `IntKeyMapTest` height pins, +7 `NarrowMemoDepthTest`); profile `--listAll` **byte-identical A vs B** (46 errors); cost gate `output.errors`/`spine.nodes`/`narrow.walks`/`typeOfExpr.distinct`/`mapped.*` all +0.00% and **four counters FELL and were rebaselined in the same commit** (`typeNode.cacheable` -10.6%, `typeNode.cacheHits` -14.6%, `globals.lookups` -8.4%, `globals.misses` -8.5%, all downstream of the 56% cut). Full derivation: `docs/perf/narrow-walk-attribution.md`. Follow-on queued as **(CALL.4)**._

_Round 732 (2026-07-27) — **(DISPATCH.1) step (a) DONE, and the derivation FALSIFIES the item: the per-kind spine handler table removes 64% of the handler consultations and 4.8% of the time.** The table was DERIVED by instrumentation (`SpineDispatch`, opt-in `--dispatchProbe`/`--dispatchGated`, behaviour-free when off) and VERIFIED by running the WHOLE corpus suite with it applied to every node (12,882 / 0 / 3) plus a byte-identical compiler-profile `--listAll`. Measured prize: **883 ms of an 18.5 s spine — an UPPER bound, inflated by the probe's own `when(h)` indirection; production-realistic ~100–300 ms (0.3–1%)**, against the 1.0–2.5 s / 6–14% the item predicted. Consultations fall 59/node → 21.65/node; `--dispatchGated` measured SLOWER than production (spine enter 11,699 vs 11,157 ms, leave 8,683 vs 8,087). **Round 716's estimate inferred consultation overhead from "IDENTIFIER costs 2,746 ns and almost no handler wants it" — in fact 22 of the 59 handlers ACT at an identifier**, because handlers keyed on PARENT edges, FRAME-owner identity and nodeId REGISTRIES cannot be closed by the node's own kind and are also the expensive ones; the "skip `spineEnterNode` for bare Identifiers → byte-identical" probe skipped real work the compiler profile happens not to need. Table: 46 enter handlers 35 closed / 11 OPEN, 13 leave 3 closed / 10 OPEN; a closure is accepted only on a SYNTACTIC justification or an `is Statement` gate, never on an observed work set. Two handlers look closed and are not — `spineArithLeaveNode` (frame pop after its own-kind `when`, 29 working kinds) and `spineNpLeaveNode` (`else` arm reaches every Expression kind). **THE FINDING TO CARRY FORWARD: six handlers are 71% of the spine** — `cpaSpineLeave` 4,366 ms, `ccetSpineLeave` 3,046 ms, `spineCtaM3StatementAnchor` 2,900 ms, `spineIanyEnterNode` 1,025 ms, `ccetSpineEnter` 920 ms, `ctaSpineEnter` 586 ms, against ~4.8 s for the other 53 combined; `cpaSpineLeave` costs 5.4 µs PER NODE OF THE PROGRAM. That is cta/cpa/ccet legacy-parity frame bookkeeping — a per-handler problem, queued as (SPINE.1). Full derivation: `docs/perf/dispatch-table.md`. Corpus 12,876 → **12,882 / 0 / 3** (+6 pins); cost gate all 20 counters +0.00%._


_Round 734 (2026-07-27) — **(CALL.1) DONE, and the measurement chose BRANCH B by 4×: `checkSingleCallExpressionTypes` is type-system work, not emission pre-work.** The function was split into disjoint timed sections (`CallSections`, opt-in `--callSections`, behaviour-free when off) and **78% of it (2,007 of 2,564 ms raw) is type-system work** — `checkArgumentsAgainstSignature` **1,357 ms (53%)**, `getCalleeType` **474 ms (18%)**, the TS2793 impl probe 101, `checkArgumentsAgainstOverloads` 53, `getCallSignaturesOfType` 19. **Branch A ("the never-firing emission sites' pre-work dominates → hoisting removes 1–2 s") is disproved twice.** STATICALLY: all 22 `getLineAndCharacterOfPosition`, all 17 `expressionTrueEnd` and all 11 `typeToString` sites are DOWNSTREAM of the emission decision (16 literally inside `if (length > 0) {`) — there is nothing pre-gate to hoist, the gates already ARE the cheap pre-test. DYNAMICALLY: everything non-type-system totals **557 ms**, ~70 ms of it the probe → a theoretical maximum of **~490 ms = 1.6% of a 30.5 s compile, INSIDE the ±2% drift band (~610 ms)**, so no A/B is offered and **nothing was optimised**. **The exit profile falls out of the partition for free: of 52,413 invocations, 26,496 (50.6%) leave at the any/error bail AFTER `getCalleeType` has run in full**, 22,145 reach the single-signature branch, 3,640 the overload branch, and **0 reach the ~240-line `signatures.isEmpty()` branch** (7 emission sites, never entered here); **0 of the seven prologue walkers fire and they still cost 253 ms** — that cost is gate evaluation. **Calibration failed twice and the fix is a METHOD:** a single empty span across the wrapper's call read 922 ns (a cold transition, not a timestamp pair) and drove six sections negative; `repeat(8)` read 360 ns because a loop **back-edge safepoint poll** sits inside every empty span; unrolled, 306 ns. **Price a boundary DIFFERENTIALLY** — the same prologue measured as 7 sections (280 ms) and as 1 span (253 ms) gives **86 ns**, agreeing with the `--passTiming` A/B (`checkSpine` +29 ms, +0.1%). Non-finding worth keeping: the "HotSpot won't JIT a 920-line method" theory dies to one `javap` — the core is 3,587 bytecodes, well under the 8,000 limit. **Next lever, queued as (CALL.2): `checkArgumentsAgainstSignature`, 61 µs per call in a 1,534-line function — now the largest single measured cost in this compiler.** Full derivation: `docs/perf/call-expression-attribution.md`. Corpus 12,887 → **12,892 / 0 / 3** (+5 pins); cost gate all 20 counters +0.00%; profile `--listAll` identical probe-on vs probe-off (46 errors)._

_Round 733 (2026-07-27) — **(SPINE.1) step (a) DONE, and the intra-handler attribution FALSIFIES the item TWICE.** `cpaSpineLeave` + `ccetSpineLeave` were split into disjoint timed sections (`SpineSections`, opt-in `--spineSections`, behaviour-free when off), and the item's premise does not survive: **they are NOT "legacy-parity frame bookkeeping" — 88.4% of their 8,195 ms (7,241 ms) is the cpa and ccet passes' OWN checking work** (`checkPropertyAccessInExpr`, `checkSingleCallExpressionTypes`) inside the frame-ambient block; the ambient install+restore is 360 ms and the whole non-work scaffolding ~950 ms. **The named target — the ancestor climbs — is 176 ms** (`cpaM2ChainOk` 77 + `cpaM2StmtPosition` 8 + `ccetM3ChainOk` 91), against a predicted 1–3 s: wrong by 6–17×, and § 0's law forbids the memo (932 ns per climb over mean ancestor depth 6). The second hypothesis, a parent-kindId dispatch axis, prices at ≤60 ms — five of cpaSpineLeave's seven sections cost 281 ms across 856,962 consultations EACH, so consultation is not the expense here either. **NOTHING was optimised, deliberately:** every candidate measures below the 560 ms drift band of a 28 s compile. **What the numbers DO point at, queued as (CALL.1): `checkSingleCallExpressionTypes` costs 53.6 µs per CallExpression — 2,931 ms over 52,413 anchors, a 920-line straight-line function with 18 `diagnostics.add` sites executed in full for every call expression in the program, ~10% of a ~28 s compile in ONE function** and the largest per-node cost measured anywhere here. Calibration trap recorded: the first probe calibrated at JVM STARTUP and read 40,573 ns per timestamp pair (the cold interpreter), making every net figure negative — it is now calibrated IN SITU at 42 ns. Full derivation: `docs/perf/spine-leave-attribution.md`. Corpus 12,882 → **12,887 / 0 / 3** (+5 pins); cost gate all 20 counters +0.00%; profile `--listAll` identical probe-on vs probe-off (46 errors)._

_Round 731 (2026-07-27) — **(LIB.1)(b) LANDED: the DOM, webworker and scripthost lib sets are SHIPPED, so a browser or worker project is actually type-CHECKED instead of silently running unchecked.** `RealLibFiles` carried only the non-DOM ES set, so `"lib": ["dom"]` resolved into `Resolution.unavailable` — a list nothing outside RealLibs.kt ever consumed, hence no diagnostic and no failure: `HTMLElement` degraded to `any` and DOM code compiled clean. All 108 `src/lib` files are embedded now (566 KB → 3.71 MB of lib source; dom.generated alone is 2.35 MB, 66% of it MDN doc comments), and `keyToDistFileName` maps the `*.generated` SOURCE names back to the names tsc distributes (`dom.generated` → `lib.dom.d.ts`) so a lib-declaration diagnostic cannot print a file name that exists in no TypeScript distribution. **THE BUILD COST WAS REAL AND IS NOW PAID: all 3.71 MB in ONE generated file with ONE `buildMap { }` lambda fails with "Not enough memory to run compilation" after 7m34s at the 5 GB (BUILD.1) gives the Kotlin daemon** — the emission is split over 16 `RealLibFilesPart*.kt` of ~250 KB which the facade concatenates once and cuts apart by recorded char lengths, and a COLD `compileKotlinJvm` then takes 2m25s (under the ~6.5 min BUILD.1 records for the old single-file shape), so the split is not a workaround, it is cheaper. Corpus 12,857 → 12,876 / 0 / 3 (+19 pins). Cost gate PASSES with EVERY counter at +0.00%, and the dashboard is unmoved (compiler 46, services 46, harness 94): the bench profiles pin `"lib": ["es2020"]` and the corpus keeps the embedded lib, so neither requests a host lib. **THE DECISIVE PROBE IS A MEMBER PROBE** — "does `HTMLElement` resolve" passes just as happily when the whole DOM is missing, because an unknown name degrades to `any` and `any` is silent; all 6 targets (unknown member reported, member type honoured, method arity enforced, across DOM/webworker/scripthost) were verified FAILING on unmodified HEAD with all 7 controls passing. **One honest limitation found and pinned as such:** the DOM TS2339 target uses `Screen`, not `HTMLElement`, because the walker only reports a missing member once it can enumerate the receiver's complete member set and gives up on a lib interface with a heritage clause (the pre-existing B153 limit, FN-not-FP) — HTMLElement is pinned on the axis that does discriminate for it, the type of its own members. (LIB.1)(c) — report a REQUESTED lib that is unavailable — is the remaining leg._

_Round 730 (2026-07-27) — **(LIB.1) IS CLOSED: a real project build now uses the REAL TypeScript libs, and the flip is FREE — all 8 profiles report the SAME diagnostics code for code under both lib sets (46/46/46/46/46/46/46/94).** The gating measurement (four arms × 8 profiles) first showed the flip costing +5 on services/server/harness; all five were ONE lib-free defect, now fixed: **a `this` pseudo-parameter is not a call argument**, but `buildSignatureForFunctionLikeTypeNode` counted it into `minArgumentCount` while `getParameterSymbols` dropped it from `parameters`, so `minArgumentCount` EXCEEDED `parameters.size` and every arity gate read the signature as "target provides too few arguments" — a `this`-carrying function type was not assignable to ITSELF (the tell is the self-contradictory "Expected 3 or more, but got 3"). The same builder also zipped the surviving symbols POSITIONALLY against the declaration list, shifting every parameter type by one. Round 460 had fixed both at the function-DECLARATION site only; the arity rule is now one `requiredParameterCount` helper at all 19 signature builders (16 were counting `this`). Under real project conditions (`types: ["node"]`) the real arm is strictly BETTER: server 18 → 13, harness 48 → 43, everything else 13 (all env-legit TS2591). `projectDefaults()` carries the flip; the CONSTRUCTOR default stays false so the corpus path is untouched, and a tsconfig may still opt out. Corpus 12,842 → 12,857 / 0 / 3 (+15 pins). Cost gate REBASELINED with a real, justified movement — `mapped.keyed` +3.81% is the price of resolving the real lib's mapped utility types (the embedded lib declares none), `typeNode.bypassed` −7.74% is an improvement, `spine.nodes`/`globals.lookups`/`output.errors` unmoved. **The decisive control is a REQUIREDNESS/MEMBER probe, never name resolution — an unknown name degrades to `any` and is silent.**_

_Round 729 (2026-07-27) — **(LIB.1) IS DONE: the real-lib false-positive count is ZERO.** Arm C 16 → 13, and all 13 remaining are the env-legit TS2591 — the burn-down that started at 35 (round 717) is finished. Corpus 12,822 → 12,842 / 0 / 3 (+20 pins); cost gate PASSES UNCHANGED TO THE DIGIT at every step (largest counter −0.68%, an improvement, nothing rebaselined); embedded-lib compiler profile still 46. **Round 728's reading of parser.ts:3558 as a TYPE-PARAMETER NAME COLLISION is DISPROVEN** — instrumenting the inference showed the constraint check failing IDENTICALLY under both names (candidate `Intersection[TypeParam(T)#38, {m:1}]` vs constraint `NodeX` either way); the renamed variant was only ACCIDENTALLY clean, its equally-un-inferred return type swallowed downstream. The real cause is round 725's rule at a THIRD site: `tryInferSingleTypeParamFromArgs` answered "does the inferred type satisfy the callee's constraint" with a bare `checkTypeRelatedTo`, which cannot see through an intersection to its TypeParam constituent — and on failure it bails WHOLESALE, so the callee's return type comes back UN-INSTANTIATED and the mismatch surfaces as a TS2322 whose two sides print nearly the same text. **The second commit is the session's real find: `Exclude<T, U>` was an IDENTITY FUNCTION.** The distribution loop evaluated each constituent's branch under the UNSHIFTED alias-argument map, so `T` in a branch still meant the WHOLE union and every non-matching constituent handed it all back. Fixing it closed utilities.ts:4258 AND utilities.ts:12082 — the `assertType<never>` site round 728 failed to reproduce in two attempts, because `HasInferredType` is built with `Exclude`. **TWO COMPANION DEFECTS surfaced by the FIRST arm-C measurement of that change, neither optional:** (a) enum-member types do not discriminate in our relation AT ALL (`const k: SK.Identifier = SK.PrivateIdentifier` is silent), so sibling AST interfaces differing only in `kind` read as mutually assignable and a working `Exclude` promptly DROPPED them — `Exclude<PropertyName, PrivateIdentifier>` lost `Identifier` too, two brand-new FPs at factory/utilities.ts:1056/1061 — closed by applying round 472's `.kind` DOMAIN veto inside the conditional evaluator, with the underlying relation gap QUEUED, not fixed; (b) a USER type alias shadowing a lib one merges onto ONE symbol and the generic substitution path took `firstOrNull`, i.e. the LIB's, so a local `type Omit<T,K> = {[P in keyof T]: T[P]}` resolved through `Pick<T, Exclude<keyof T, K>>` — its own pin had been passing only because Exclude was inert. **THE METHOD, six-for-six: instrument the emission site and print STRUCTURE.** The display string lied again — `NodeArrayX<T>` on both sides of the TS2322 — and one dump of type ids, kinds and constituents named the true cause after a whole prior round of matrix-narrowing had named the wrong one. Note the wrapping trap: a `Checker.kt` stack-trace line number is mod 65536, so frame `:32118` is really line 97654. **A `git stash` A/B against unmodified HEAD is worth its two compiles** — it showed the discriminating control's MESSAGE changing (`NodeArrayX<T>` → `NodeArrayX<T & { m: 1; }>`), which is the only proof that inference BOUND the intersection rather than being waved through, and it showed three other "controls" to be vacuous on both sides._

_Round 728 (2026-07-27) — **THREE causes cracked in one session, and the three that remain are now ISOLATED IN LIB-FREE REPROS instead of described. Real-lib false positives 7 → 3 (arm C 20 → 16, MEASURED both sides; TS2349 ×2 → 0, TS2769 ×1 → 0, TS2322 ×3 → ×2, not one other code moved); corpus 12,804 → 12,822 / 0 / 3 (+18 pins); cost gate PASSES with the largest counter −0.68% (an improvement), nothing rebaselined; embedded-lib compiler profile still 46.** Triage-by-cause is now five-for-five, and again the histogram lied: three of the seven sites reproduced in 5–8 line synthetic probes on the FIRST attempt, none needing the real libs. **(1) TS2349 ×2** — the B516 union-of-callables gate required every parameter to be REQUIRED (`minArgumentCount == parameters.size`), so every lib method with a trailing optional — `forEach(callbackfn, thisArg?)` above all — fell through to the incorrect "none of those signatures are compatible with each other". tsc's `combineSignaturesOfUnionMembers` demands nothing of the kind: longest parameter list, position-wise intersection, `minArgumentCount` = the MAX. Only three corpus baselines pin that message and all three survive on other gates. **(2) TS2322 ×1 — round 718's defect in the mirror**: a mapped type has FOUR modifiers and the materializer recorded three (`readonly`, `-readonly`, `-?`) — never the plain `?`, so `Partial<T>`'s member of a REQUIRED source property stayed required and tsc's own `lookupFromPackageJson(): Partial<CreateSourceFileOptions>` was rejected for omitting `languageVersion`. The marker is a `SymbolFlags` BIT, not the id-keyed set its `-?` sibling uses: the arm that must consult it is the HOT one (every declared-required property reaches it). **(3) TS2769 ×1, found by a probe MATRIX** — five variants of `new Intl.Collator(locale, { usage: "sort", … })` showed single-call-signature, two-optional-params and single-construct-signature all CLEAN; only the OVERLOADED forms reproduce, because overload resolution compares each candidate against the RAW argument type while the single-signature path contextually types the literal. The `overloadingOnConstants2` rule now has a per-property analogue, evaluated only inside the already-failing branch (round 720/725's free shape). **THE THREE THAT REMAIN, now diagnosable rather than described:** parser.ts:3558 is a TYPE-PARAMETER NAME COLLISION — a four-case matrix pins it to "the argument's element type is an intersection whose TypeParam constituent SHARES ITS NAME with the callee's type parameter"; rename either side and it is clean, and `NonNullable` is incidental (`T & {}`), so round 726's reading of this site is disproven. utilities.ts:4258 is `Exclude<…>` never being evaluated outside `resolveAssertTargetTypeNode` (repro needs an INTERSECTION member; the gating trap is that `isLibOnlyTypeName` is TRUE for an UNDECLARED name). utilities.ts:12082 did NOT reproduce in two shapes — and the same file's other `assertType<never>` does not false-positive, so the discriminator is `HasInferredType` itself, not exhaustive-switch narrowing. **CONTROL DISCIPLINE:** cause (1)'s discriminating control runs in the direction round 727 recommended — combining makes `number & boolean` = `never`, so a bad argument must report **TS2345, a diagnostic that did not exist on HEAD**; a fix that makes a diagnostic APPEAR is far easier to prove than one that removes a class of them._

_Round 727 (2026-07-27) — **TWO families cracked in one session, both by triaging all eight remaining sites by CAUSE before touching anything. Real-lib false positives 14 → 7 (arm C 27 → 23 → 20, MEASURED at every step; TS2339 ×4 → 0 and TS2345 ×4 → ×1, not one other code moved); corpus 12,795 → 12,804 / 0 / 3 (+9 pins); cost gate PASSES with the largest counter −0.68% (an improvement); embedded-lib compiler profile still 46.** THE DEFECT: `narrowByCallPredicate`'s SINGLE-TYPE positive branch compared the reference against the guard target AS A WHOLE (`candidate <: t ? candidate : t <: candidate ? t : candidate`), so when the target is a UNION and neither whole-union relation holds it handed back the ENTIRE candidate union. tsc's `getNarrowedType` never compares against the whole candidate — it `mapType`s over it and keeps, per constituent, whichever of the two is the subtype, dropping constituents unrelated in both directions. The live shape is esDecorators.ts `visitAssignmentElement`'s `isAssignmentExpression(node, true) && isNamedEvaluation(node, …)`, where `NamedEvaluation` is a 9-member union of unrelated `X & { … }` intersections; a union's property set is the INTERSECTION of its members', so every later `node.left`/`node.right`/`node.operatorToken` FP'd. The fix filters the constituents on the previously-`else` path only (both relating branches stay byte-identical) and keeps the whole-union fallback when nothing survives. **STATED PLAINLY because it is invisible in the numbers:** in this shape exactly ONE constituent survives, and our TS2339 walker does not fire on an intersection (or plain-interface) receiver at all — so part of the suppression comes from a diagnostic class that does not exist. The narrowing itself is independently correct, dumped via revealing TS2322s, and where two constituents survive the result is a union on which TS2339 does fire; that is what the second pin exercises. **PROBE DISCIPLINE, first attempt useless:** two negative controls came back silent WITH the fix and read as "it deleted a diagnostic class" — they had landed on that same intersection gate and would have been silent on HEAD too. The discriminating control keeps TWO constituents alive, so a dropped constituent's property becomes legal while a property on only one survivor STAYS an error; both targets were then verified FAILING on unmodified HEAD. **THE SECOND FAMILY, same session, same method:** the TS2345 ×3 are `(x ||= new Set()).add(path)` — the REAL lib's `SetConstructor { new <T = any>(…): Set<T> }` let the CONSTRUCT SIGNATURE's own type parameter escape, so the receiver was `Set<Path> | Set<T>` and the B516 union-of-callables rule CORRECTLY intersected the two `add` parameters into `Path & T`; the combining rule was never the bug, the leaked TP was. Fixed by substituting an uninferred signature type parameter's DECLARED DEFAULT — TypeScript's own rule — only where one exists and only at the return reference's top level. The corpus cannot see it (the EMBEDDED lib's `SetConstructor` is non-generic `new(): Set<any>`), which is what made it safe; `new Map()` does not reproduce even under real libs, because `MapConstructor` has a non-generic `new()` overload that wins. Its pin's discriminating control runs the OPPOSITE way — with `new <T = number>()` the substitution makes a diagnostic APPEAR, which is far easier to prove than one that removes a class of them. **OPERATIONAL:** two builds lost to OOM under 1 GB available, one a full `jvmTest` that ran 7m31s and wrote ZERO result XMLs — read an empty results directory as "the build died", not as "everything failed"._

_Round 726 (2026-07-27) — **the TS2322 family cracked by DUMPING the relation instead of guessing a third time. Real-lib false positives 18 → 14 (arm C 31 → 27, BOTH SIDES MEASURED; TS2322 ×7 → ×3 and not one other code moved); corpus 12,788 → 12,795 / 0 / 3 (+7 pins); cost gate PASSES with the largest counter −0.68% (an improvement); embedded-lib compiler profile still 46.** THE DEFECT: `getTypeFromTypeReference` instantiated `Iface<A>` into a `Type.Reference` only when EVERY type argument resolved, and otherwise fell through to `getDeclaredTypeOfSymbol(Iface)` — the RAW OPEN GENERIC, which carries its OWN type parameter and is a different type from any instantiation of it. Nothing relates a `Type.Reference` to that, so every comparison against such an annotation FP'd. On the real libs the unresolved argument is the function's OWN type parameter: a return annotation is resolved with `currentTypeParamScope == null`, so its `T` comes back errorType — which is exactly why the message read `Type 'NodeArray<T>' is not assignable to type 'NodeArray<T>'`, identical text, because the DISPLAY renders the annotation while the COMPARISON used the raw generic. THE FIX mirrors tsc, whose `errorType` is Any-flagged and instantiates regardless: substitute `any` for the unresolved positions and instantiate anyway (the unresolved name is already reported by TS2304 — this only stops the cascade). Fixed sites: parser.ts:3583, watchPublic.ts:371/383, utilities.ts:12378. **WHY TWO ROUNDS OF GUESSING MISSED IT, and the transferable lesson:** the display string was the trap all along, and three separate synthetic probes (overloads, callback-parameter inference, cross-file generics, real libs on) came back CLEAN — the shape only becomes visible when the interface reaches its type parameter through a GENERIC BASE (`extends ReadonlyArray<T>`, real `NodeArray`'s shape); a flat `interface Box<T> { v: T }` relates to its own raw form anyway, so a pin built on one is silent BEFORE and AFTER and proves nothing. **THE INSTRUMENTATION LESSON that cost two compiles:** a debug helper reading a `private val` declared AFTER the class's `init` block reads NULL, because `init` runs the passes — the probe printed nothing and looked like "wrong code path". Hooking `Diagnostic`'s constructor instead named the true emission site in one run. **PINNED by three targets and four negative controls** — a resolvable-but-wrong argument still errors, an unresolvable argument does not mask an unrelated source, does not mask missing members (TS2740 still fires), and the good case stays silent._

_Round 725 (2026-07-27) — **round 724's confirmed defect FIXED, and it cost nothing. Real-lib false positives 22 → 18 (arm C 35 → 31, BOTH SIDES MEASURED; TS2344 ×4 → 0 and not one other code moved); corpus 12,781 → 12,788 / 0 / 3; cost gate +0.00% on all 20 counters; embedded-lib compiler profile still 46.** THE DEFECT: the real lib declares `NonNullable<T> = T & {}`, so a `Visitor<NonNullable<TIn>, …>` type argument resolves to `Intersection[TypeParam(TIn), {}]` — and `checkConstraintsForTypeArgs` applies the constraint chain only to a BARE `Type.TypeParam` arg (and, since 440-b, a `Type.Union` arg). Wrapped in an intersection the constituent's constraint is never consulted at all, which is why round 724's obvious hypothesis ("strip the nullish part of `Node | undefined`") was DISPROVEN by the case where TIn is already non-null: nothing to strip, same failure. The relation cannot cover it either — the engine has no TypeParam-source-via-constraint rule on purpose (round 456 measured adding one as net-zero and reverted it), so this is the THIRD arm of a per-emission-site rule that already had two. THE FIX evaluates only inside the already-failing branch, hence free: take each TypeParam constituent's constraint (`T ⊆ constraint(T)`) and compare it, with a SEPARATELY GATED second step dropping nullish members when an `{}` constituent is present, since `X & {}` is tsc's non-nullish marker. **THE TRAP, pinned by four negative controls:** a blanket `is Type.Intersection -> continue` passes every target case and silently deletes a real diagnostic class — controls cover `A & B` with no type parameter, `NonNullable` of an unconstrained parameter, `NonNullable` of a parameter constrained to an unrelated type, and a nullable-constrained parameter used WITHOUT `NonNullable`. Fixed sites: parser.ts:3491/3492, visitorPublic.ts:124/144; the 18 left are TS2322 ×7, TS2345 ×4, TS2339 ×4, TS2349 ×2, TS2769 ×1. **OPERATIONAL, correcting round 720's rule for subagents:** a subagent cannot wait on a detached build — `nohup … &` hides it from the harness and ending a turn terminates the agent rather than parking it; run gradle in the FOREGROUND, one command per call. Combining both spawned two concurrent builds racing on one output directory. And a `ps` pattern for a gradle client must match `gradle-wrapper.jar`, not `GradleWrapperMain` — the wrong one reports "nothing running" while a build is live._

_Round 723 (2026-07-27) — **the round-722 defect FIXED, and it took two false positives with it, not one. Real-lib FPs 24 → 22; corpus 12,775 → 12,781 / 0 / 3; cost gate tripped, justified, rebaselined in the same commit.** THE FIX: `getTypeOfObjectLiteral` named a computed key via `computedLiteralKey(n) ?: continue`, and that helper accepts only string/numeric literals — so `[Symbol.iterator]: …` was silently DROPPED (not mis-typed: absent) and the target correctly reported it missing. New `computedSymbolKey` names a DOTTED path as `[Symbol.iterator]`, matching how symbol members are named everywhere else (it is what TS2739 prints); wired into BOTH the PropertyAssignment and MethodDeclaration branches — the method form handled no computed names at all. **Deliberately narrow:** a bare `[foo]` dynamic key is still skipped, since naming it would let any literal satisfy a symbol-keyed target — pinned by its own control, because the over-broad version would have passed everything else. **It fixed MORE than predicted:** TS2739 ×1 → 0 AND TS2322 8 → 7, so one structural gap was surfacing under two codes — the by-code histogram overstates how many distinct causes remain in the other 22. **COST.1 did its job:** failed on `mapped.keyed` +3.12% / `mapped.hits` +6.66% (+744 keyed lookups, +372 hits, ≈1.6 ms) — the honest price of typing members previously discarded, accepted and rebaselined; the percentages look large only because that counter is small._

_Round 722 (2026-07-27) — **a CONFIRMED, isolated defect behind (LIB.1)'s TS2739, with a four-line repro and a live control, parked on `wip/round722-symbol-keyed-members`; main clean at 12,775 / 0 / 3.** THE DEFECT: tsc's `core.ts` builds a `Set<TElement>` object literal ending `[Symbol.iterator]: () => {…}, [Symbol.toStringTag]: …`, and we report TS2739 "missing `[Symbol.iterator]`, `[Symbol.toStringTag]`" — reading the source confirms the literal DOES declare them, so **computed well-known-symbol keys in an object literal are not registered as members**. The control that omits the key still reports it missing, so the probe discriminates. **SECOND HYPOTHESIS ELIMINATED:** an interface extending `ReadonlyArray<T>` IS self-assignable; with round 721's result, both explanations for the parser.ts:3583 / watchPublic.ts TS2322 are now closed — dump the real relation failure rather than guess a third time. **THE TIME-KEEPING ERROR, correcting my own rounds 718–721 notes:** a backgrounded `sleep` is PREEMPTED by the next command, so the "16-, 25-, 40-minute compiles" never happened — real time passes only when a wait is launched and the turn ENDS. Gradle started 01:48:27; after two nominal 20-minute waits the clock read 01:49:12. The same build then finished in **2m 57s**. What survives: the heap ladder was independently proven (4g fails with an explicit "Not enough memory to run compilation", 5g completes) — the heap really was too small AND my clock was wrong._

_Round 721 (2026-07-27) — **a NEGATIVE result that kills the most attractive hypothesis about (LIB.1)'s remaining 24 real-lib false positives, and the first fast build cycles since the heap fix (a filtered test run: 1m 56s). Corpus 12,770 → 12,775 / 0 / 3; no production code changed.** THE HYPOTHESIS looked strong: three of the eight remaining TS2322 print source and target as the SAME TEXT (`NodeArray<T>` → `NodeArray<T>`; `WatchCompilerHostOfFilesAndCompilerOptions<T>` → a union whose FIRST MEMBER it is), and "a generic reference does not relate to itself" would explain all three plus be a perf win. **It is wrong:** five probes — self-return, constrained self-return, member-of-union, return-through-a-local — all pass, with a live control that still errors. Pinned as `GenericSelfAssignabilityTest`. **THE SURVIVING LEAD is symbol-keyed members:** these FPs exist only under real libs, the same run reports TS2739 "missing `[Symbol.iterator]`, `[Symbol.toStringTag]` from `Set<T>`", and `NodeArray<T> extends ReadonlyArray<T>` carries exactly those in the real lib but not the curated one — so probe that under `@useRealLibs`. **OPERATIONAL:** an IDLE daemon from the previous build squats on its heap for a 2-hour timeout — 4.33 GB held while the next build tried to start, leaving 297 MB free and starving it; stop previous daemons before a from-scratch compile. And a `pkill -f <name>` matching your own waiting tasks kills those too._

_Round 720 (2026-07-26/27) — **the owner approved the (BUILD.1) heap raise and everything stuck landed at once. Corpus 12,765 → 12,770 / 0 / 3; real-lib false positives 35 → 24; cost gate reports ZERO movement on all 20 counters; embedded-lib profile unchanged at 46.** THE FIX (round 718's work, finally gated): `Required<T>` is `{ [P in keyof T]-?: T[P] }` and the parser recorded `-?` as a plain `?`, so it behaved as `Partial<T>` — inverted. The mechanism is not type-level: TS2722 gates on `isOptionalProperty`, and a homomorphic mapped member carries its SOURCE declaration, so the source's `?` is what the scan sees; fixed exactly as M1.10 fixed `-readonly`, with a `mappedRequiredMemberIds` side-channel probed only on the already-optional branch — hence zero cost. Real-lib arm C 48 → 37, TS2722 ×11 → 0, no other code moved. **BUILD.1 settled with a ladder:** 2g GC-thrashes forever, 3g dies mid-compile, 4g fails in ~6 min with an explicit `Not enough memory to run compilation`, 5g succeeds in ~6.5 min; Gradle's own daemon cut 2g → 1g because 5g + 2g oversubscribed the 7.7 GB box and the kernel killed the compile daemon silently. **TWO OF MY OWN DIAGNOSES WERE WRONG AND ARE CORRECTED:** round 719 blamed a local bench loop (those commits are `github-actions[bot]` — remote CI), and rounds 718–719 recorded "40-minute compiles" that were ~6-minute ones **restarted by my own polling**, since each Bash call preempted the previous backgrounded one and killed the build with its shell. Rule: launch long builds DETACHED (`nohup … &`) and wait on ONE timer; if what you have awaited for 25 minutes is 90 seconds old, you are killing it._

_Round 718 (2026-07-26) — **a complete fix for 11 of (LIB.1)'s 35 real-lib false positives — diagnosed, written, and NOT LANDED, because four cold compiles in one session and none would gate it. Parked on `wip/round718-required-minus-optional`; main clean and unchanged at 12,765 / 0 / 3.** THE BUG: `Required<T>` is `{ [P in keyof T]-?: T[P] }`, and the parser records `-?` as a plain `?` — so `Required<T>` behaves as `Partial<T>`, inverted. `-readonly` got its own flag in M1.10; the `?` analogue never did. It costs 11 FPs because tsc declares `tracker: Required<Pick<SymbolTracker, "reportInferenceFallback">>`. **THE MECHANISM IS NOT THE OBVIOUS ONE:** TS2722 reads like a type question, but the emitter gates on `isOptionalProperty(propSym)` and the codebase deliberately never adds `| undefined` for optional-member access — what bites is the M1.10 trap repeating, a homomorphic mapped member CARRYING ITS SOURCE DECLARATION, so the source's `?` is what the scan sees. Fix is M1.10's mirror: a `mappedRequiredMemberIds` side-channel probed only when the declaration says optional. **TWO DEAD ENDS, BOTH CAUGHT BY CONTROLS:** hand-rolling the mapped types locally reproduces NOTHING (we emit nothing for user-defined mapped types — the controls came back empty and three target assertions were passing vacuously, and would have 'confirmed' no fix at all); asserting assignability through `Partial` measures an axis we do not model. Only `@useRealLibs` + TS2722 reproduces, verified against unmodified HEAD with the target failing and the control passing. **WHY IT DID NOT LAND:** `-Xmx2g` hung, `-Xmx3g` died and restarted after 16 min, only `-Xmx4g` got the main compile through — so (BUILD.1) is escalated from nuisance to the binding constraint on the edit-test loop, its proposal revised 3g → 4g, with a bigger box offered as the alternative. **Also:** the embedded lib declares NO utility types at all, which is why this family is invisible by default — the name degrades to `any`, and `any` is silent._

_Round 717 (2026-07-26) — **the two GATE items landed: logical parity became a MECHANISM with two build-failing controls, and cost became a counter diff. The determinism check on that gate caught a racy counter on its first use — the very counter (DISPATCH.1) was told to build its table from. Corpus 12,765 / 0 / 3, unchanged; generated corpus byte-identical.** **(PARITY.1):** `docs/logical-parity.md` carries the form-vs-meaning procedure as two ALLOWLIST tables (7 meaning axes / 6 form axes with their equivalence obligations; anything in neither is MEANING by default), and `logicalParityDivergences` in build.gradle.kts is the single source of truth — the generator emits a switched-off baseline `@Ignore`d so it stays VISIBLE as skipped (a vanished test hides behind an unchanged total), regenerates the ledger into the doc so it cannot drift, and FAILS the build on either rot mode: a baseline matching no generated test, or a `pinnedBy` class that does not exist. That second control is what makes "replace it with a test pinning the logic" mechanical rather than aspirational. Empty list is the healthy state and the generated tree is byte-identical with it. **(COST.1):** `scripts/cost_gate.py` — 20 deterministic `--passTiming` counters diffed against `docs/perf/cost-counters.txt`, failing above ±2%; counters, never wall time. Baseline: 46 errors, 856,962 spine nodes, 696,933 getTypeOfExpression calls over 250,057 distinct nodes, 69,903 narrowing walks, 89,883 bypassed type-node resolutions, 1,377,511 globals lookups at 98.9% miss. It also pins the compiler's ANSWER, because a cost drop that changes the output is not a win. **THE FINDING:** two runs of the same binary agreed bit-for-bit on 19 of 20 counters and disagreed on the AST census (857,350 vs 854,550) — `indexSourceFile` runs on the crawl's concurrent parse threads while `PassTiming.nodeKindHistogram` is a plain HashMap, so it loses increments and always undercounts; harmless for "which kinds dominate", fatal for the exact per-kind population DISPATCH.1 needs. Excluded from the gate, warned at the source, flagged on that item. **Plus a build trap worth half an hour:** the documented `pkill -9 KotlinCompileDaemon` ritual makes the next compile COLD, which does not fit the inherited `-Xmx2g` and HANGS rather than failing (350% CPU, RSS at the ceiling, zero classes written for 14 min); `-Dkotlin.daemon.jvmargs=-Xmx3g` finished it in 2m 33s — queued as (BUILD.1), owner-gated. Warning-clean restored (3 stale Checker.kt warnings)._

_Round 735 (2026-07-27) — **(CALL.2) DONE: the prior holds by 48x, and its supporting evidence named the wrong term.** `checkArgumentsAgainstSignature` was split into 17 disjoint timed sections plus 8 nested sub-measures (`ArgSections`, opt-in `--argSections`, behaviour-free when off). The item's falsifiable expectation — "most of the 61 us is argument TYPE computation, not `checkTypeRelatedTo`" — **HOLDS: 924 ms of the function's 1,624 ms raw is the `argType` computation, against 19 ms for the whole `checkTypeRelatedTo`+TS2345 section (10 ms for the relation call itself).** But the prior reasoned from `getTypeOfExpression` (3,911 ms, recompute x2.7), and **inside this function that is only 196 ms (12%) while FLOW NARROWING is 600 ms (37%)** — 9,615 walks at 62 us each (B469 union-arg site 284 ms/2,339; M3.4 site 316 ms/7,271), of which **86% (8,299) return the INPUT type unchanged for 237 ms**. **So (CALL.2) does NOT reach ARCHITECTURE-RETHINK 0.1 stage 3 — this function types each argument exactly ONCE and makes 5.3% of the compile's `getTypeOfExpression` calls at the compile-mean cost, so it is not a recompute site. It reaches stage 4.** **THE ROUND'S REAL PRODUCT is compile-wide: of 70,037 narrowing walks, 394 (0.56%) cost 1,485 ms — 47% of ALL flow narrowing and 4.9% of a 30.5 s compile**, the first single target measured ABOVE the +-2% drift band (~610 ms) since round 731, by 2.4x (WK_NARROW 336, WK_NARROW_LOOP 47, WK_BASE_EXPR 11). **Three mechanisms for those 394 were hypothesised and ALL THREE are disproved by the same run:** they do not TRIP (`narrowWalk tripped: 0`), they do not exhaust the 1,000,000-visit budget (630,641 arrivals total, 1,601 each, max 19,515), and the walk memo cannot reach them (`walkMiss cold=69,968` of 70,037 — essentially every walk is a first sighting). What is left is arithmetic: all walks average **372 ns per node arrival**, the monsters **2,354 ns** — 13x more arrivals AND 6.3x more expensive arrivals. **NOTHING was optimised**, per the same restraint as rounds 733/734. Also recorded: only **10,146 of 38,247 loop iterations (27%) ever reach the assignability check**, yet all 37,379 pay the full `argType` computation, because every intervening block consumes it. **Calibration, third round running: the in-situ unrolled empty span read 391 ns against a differential of 89 ns — 4.4x too high** (734 saw 306 vs 86); the fix is now a MODE, `--argSectionsCoarse` (anchors only), so ON-vs-COARSE prices a boundary rather than a code path — total probe inflation 55 ms of 1,624 = 3.4%. Follow-on queued as **(CALL.3)**. Full derivation: `docs/perf/argument-check-attribution.md`. Corpus 12,892 -> **12,899 / 0 / 3** (+7 pins); cost gate all 20 counters +0.00%; profile `--listAll` identical in production, `--argSections` and `--argSectionsCoarse` (46 errors)._

_Round 737 (2026-07-27) — **(TYPE.1) DONE: ARCHITECTURE-RETHINK section 0.1 STAGE 3 IS STRUCK — its MECHANISM is exactly right and its SIZE is wrong by 3.2x.** The 701,463 `getTypeOfExpression` calls were attributed BY CALLER for the first time (`--typeOfExprCallers`, opt-in, behaviour-free when off; only the OUTERMOST call walks the stack, so a whole expression subtree is attributed to the handler that ASKED for it and the recursion cannot inflate a caller's own factor). **The claim "several handlers independently type the same node" is CONFIRMED and pervasive: 177 sites initiate typing, 45.2% of the 254,069 typed nodes (114,750) carry MORE THAN ONE origin — modal count three, maximum 17 — and 75.8% of all calls land on them. The x2.76 factor decomposes as 2.05x CROSS-HANDLER times 1.34x recursion, and per-caller factors are 1.00-1.11 everywhere: no handler re-types anything by itself.** **But the money is not there.** A PERFECT per-node cache — the ceiling for this stage in ANY shape — saves **823 ms = 2.9% of a 28.7 s compile**; single-visit discipline **670 ms = 2.3%**; the largest handler-pair merge in the compiler **166 ms = 0.58%** against a re-derived +-2% band of ~590 ms; and the SOUND memo re-measures at **46 ms**. **NOTHING WAS LANDED**, correctly. **Two corrections forced: (1) the often-quoted "getTypeOfExpression = 3,911 ms" is a DOUBLE COUNT** (`typeOfExprNanos` charges a subtree once per nesting level) — the true total cost of all expression typing is **2,439 ms = 8.5% of checker-init**, which was always this stage's ceiling; **(2) 74.4% of the calls are OUTERMOST**, so recursion never was the explanation. **Section 0's law again, in its sharpest instance and with no cache in it: sorted by COUNT the co-occurrence head is 141,388 repeat typings worth 71 ms (0.5 us each) — the property-access receiver trio at 90,948 repeats for 51 ms, `getTypeOfObjectLiteral` re-entering itself at 30,040 for 6 ms — while sorted by TIME the head is 2,603 repeat typings worth 166 ms (64 us each). The two orderings share nothing; what is redundantly recomputed is what was already cheap.** Prediction scored: P1 FALSIFIED (>50% nested; really 25.6%), P2 HELD, P3 HELD (top-3 pairs 41% of the redundant time), P4 FALSIFIED (top-5 origins 52.5%, not >=60%). Method note: the probe costs **16.7 us per attributed outermost call** (`StackWalker`), 190x round 735's 89 ns timestamp read, so the round is deliberately COUNT-heavy — every deterministic counter is byte-identical to the plain run and to `cost-counters.txt`. Corpus 12,910 -> **12,916 / 0 / 3** (+6 `TypeOfExprCallerAttributionTest` pins); profile `--listAll` byte-identical production vs attribution (46 errors); cost gate all 20 counters +0.00%. Full derivation: `docs/perf/type-of-expression-attribution.md`. Follow-on queued as **(TYPE.2)** — `checkVarDeclAssignability` under `spineCtaM3StatementAnchor`, the largest single typing origin (431 ms over 11,933 initializers = 36 us each) inside the third-largest spine handler (2,900 ms) that no round has opened._

_Round 747 (2026-07-28) — **(REL.1)(c) STEP 3: the relation PRINTS the enum, and B463's TS2416 (`encmCheckClassesAndOverloads` piece B) RETIRES with the display rule.** Compiler profile `--listAll` **at 46, diffed LINE-BY-LINE** against round 746's (composition unchanged: TS2591x43 / TS2304x2 / TS2584x1, no TS2322); filtered suite over the SIX generated classes that carry every TS2416 baseline plus the new pins — **3,910 tests, 0 failures**, `interfaceExtendsClassWithPrivate2` included; +8 pins. **THE ABLATION.** `enumAssignmentCompat7`: 3 with the pass, **1 with it PassLab-disabled pre-747 and that one printing `(param: E) => void` on BOTH sides** (the verdict was already the relation's since 746 — only the display was not), **1 byte-identical to the baseline with the rule alone**, **4 with both live and the guards removed — the TS2416 DUPLICATED, both copies byte-identical**, and **3 byte-identical once piece B is deleted** (message, chain, position, length). **THE RULE IS ONE FLAG.** `enumDisplayFullyQualified` is tsc's `TypeFormatFlags.UseFullyQualifiedType` restricted to enum names and consulted by `typeToString` at ANY depth, so an enum nested inside a rendered FUNCTION type qualifies too; `enumQualifiedRelationDisplays` is the `getTypeNamesForErrorDisplay` retry for a pair that is not itself enum-flavored — same string on both sides ⇒ re-render both — SPLIT from round 746's `enumCollisionQualifiedDisplays` because that one is fed displays which may come from ANNOTATION TEXT. It is **self-gating**: with no enum in the pair the re-render returns the same string and the caller keeps its originals, which is why a colliding INTERFACE pair is deliberately untouched. Applied at BOTH relation errors of the chain, because tsc retries at each independently. **Retiring it also removes a DIVERGENCE**: B463 rendered every enum through `EnumAsgInfo.qualifiedDisplay`, which qualifies UNCONDITIONALLY, so it printed `(param: third.Other) => void` where tsc prints `(param: Other) => void` — measured as a pin that fails on unmodified `fa3308a4` in the OTHER direction, the third retirement in this arc to also remove a wrong output. **B583 DID NOT RETIRE, and round 746's read of it is CORRECTED: it is blocked on RESOLUTION, not on a display rule.** Ablated, the relation reproduces SIX of `enumAssignmentCompat6`'s eight diagnostics byte-for-byte and emits NOTHING for the other two — because an `enum` declared inside a function/arrow body is never conventionally bound (B83.5) and is therefore invisible in TYPE position: a unique name degrades to `any` (silently — the INV.4(c)(iii) family still finds it through the INV.2(c) lexical scopes, so there is no TS2304), and **a name that SHADOWS an outer one resolves to the OUTER symbol**, which is a wrong answer rather than a missing one. `import("f").DC` is the SECOND blocker behind that. Measured ownership: over the same 3,904 corpus tests, B583 PassLab-disabled fails exactly ONE — `enumAssignmentCompat6`, and only its two `f.ts` lines. Both halves pinned (`EnumShadowedInFunctionScopeTest`), the retirement target landed early._

_Round 760 (2026-07-29) — **THE 8x2 DASHBOARD RE-MEASURED END TO END FOR THE FIRST TIME SINCE ROUND 730, AND IT BARELY MOVED — five profiles are unchanged CODE FOR CODE across ~50 commits.** Arm A (embedded libs, `types: []`): compiler / tsc-cli / jsTyping / deprecatedCompat / typingsInstallerCore **46** (`TS2591x43 / TS2304x2 / TS2584x1`, composition identical to round 730), services **47**, server **47**, harness **95**. **The only movement anywhere on the grid is ONE line, not three**: services / server / harness are exactly the three profiles containing `src/services`, and each carries the byte-identical `completions.ts:2237` TS2345 — UP, so a regression (tsc reports zero on its own source). Every other line on the grid, both arms, is env-legit. **Arm C is recorded with its caveat rather than compared**: round 730 measured it against the real `@types/node` v22.20.1, `bench-compile-tsc.sh` `rm -rf`s that directory on any run without `--node-stub`, and there is none on this box — this round's arm C uses the all-`any` STUB, which can only suppress, so 13 -> 1 is a FLOOR and is NOT claimed as an improvement. **The reduction is 12 lines and the fix is not the reduction.** Four ingredients, each shown necessary by ablation: an ENUM discriminant, TWO guards on one reference, an EARLY RETURN in the first, the guards declared in ANOTHER file. Measured with a `const s: string = node` probe (a washed reference is SILENT, because `never` is assignable to `string`): after `!isMod(node)` the type was `never` and is now `Node`; inside the next guard it was `never` and is now `Ident`. **Round 753's stated premise is falsified, and the comment saying so was written the same round** — `getDeclaredTypeOfEnumMember` mints member types with NO members, so `Node <: Mod` relates VACUOUSLY; round 472 vetoed the resulting `never`-collapse, round 753 deleted the veto claiming (REL.1)(a)/(b) had taught the relation to decide for itself. It had, for member-vs-member; `checkTypeRelatedToCore` already recorded that the enum-vs-MEMBER direction was left open. `enumMemberDomainProvesNotSubtype` restores that one verdict at that one site. **The global rule was WRITTEN, COMPILED, MEASURED AND REVERTED — a sized refusal with the size**: closing enum -> MEMBER properly costs **compiler 46 -> 52, services 47 -> 57**, because the vacuous `true` masks that many flow-narrowing gaps (a guard used as a TERNARY CONDITION; `===` narrowing across `||`), so the unit of work is the NARROWING FEATURES, not the relation rule — queued as (REL.2) with all six sites. **The real root cause is a SUBSTITUTION defect and it is not enum-specific — queued as (REL.3)**: `interface FwdA extends Fwd<K.A>` where `Fwd<T> extends Box<T>` resolves `.v` to **`any`**, while `BoxA extends Box<K.A>` resolves it to `K.A` — one heritage hop substitutes, a second hop through an intermediate FORWARDING its own type parameter does not, which is tsc's `AbstractKeyword -> KeywordToken<TKind> -> Token<TKind>` exactly, so every keyword node's `kind` is `any`. That is why the landed veto leaves the profile line in place and the 8 profiles are byte-identical — stated as a LIMIT ON THE EVIDENCE, not a credential. **The emit path, un-exercised for 22 rounds, is confirmed working**: the compiler profile with emit ON writes **78 `.js` files**, exactly the `emitted=78` of every pre-738 TSV row, with the same 46 diagnostics; tree sha256 recorded in the artifact. Every pin run against unmodified `aef21e76`: **three TARGETS FAIL, three NEGATIVE CONTROLS PASS**. Filtered batches **858 + 1052** hand-written and **the ENTIRE generated corpus, all 25 classes**, 0 failures; the two changed files compile warning-clean. **Predictions 3 of 6** — the two misses are the ones that moved the map. The full suite and the cost gate are the owner's to run. Artifact: `docs/perf/dashboard-round760.md`._
