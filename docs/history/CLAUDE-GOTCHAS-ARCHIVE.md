# CLAUDE-GOTCHAS-ARCHIVE — corpus-era gotchas moved out of CLAUDE.md

Archived 2026-07-06 (round 421) from CLAUDE.md (which had regrown 594 KB / ~147k
tokens against its own 170 KB cap). These entries document CORPUS-UNIQUE pin
walkers, per-test parser-recovery cascades, emitter/baseline byte-format
minutiae, and checkJs one-off walkers — behavior that is FROZEN and protected
by the always-green corpus suite (a 2-minute hard gate), not by agent memory.

WHEN TO CONSULT: grep THIS file (by walker name, B-number, corpus test name, or
TS-code) BEFORE modifying/deleting a dedicated walker, investigating a corpus
regression, or re-attempting something in a frozen subsystem. Per Phase 17
doctrine, pin walkers are DELETED as the real engine supersedes them — the
matching entry here documents each one's gates and coupled pieces.

NOTE: entries may say "above"/"below" or "the entry above" — the referent may
be an entry that remained in CLAUDE.md. Section headings mirror CLAUDE.md's at
archive time (a few entries were historically misfiled under unrelated
headings; grep, don't browse).



### Scanner/Parser gotchas (archived)

- **JSX recovery family (jsFileCompilationTypeArgumentSyntaxOfCall)**: the attribute list mirrors tsc parseList(JsxAttributes) — a non-attribute token → TS1003 + abort-without-consume when `canStartStatementForRecovery()` (else skip); after attributes a non-`>`/non-`/` token recovers as SELF-CLOSING (both parseExpected failures same-start-dedup against the TS1003); a `<` right after an outermost JSX element is a sibling with a missing comma → `Binary(result, comma, invalid)` + TS2657 spanning topBadPos..prevTokenEnd (at EOF: source.length + 1 to reproduce tsc's CRLF end-of-file squiggle arithmetic) — SKIPPED under `mustBeUnary` (JSX as a unary operand `!<Foo/>`, threaded from parseUnaryExpression's operand sites via the consumed-at-entry `jsxMustBeUnary` flag); a bare `>`/`}` inside JSX text → TS1382/TS1381 per occurrence (tsc scanJsxToken — FP-safe, tsc always errors); a mismatched closing tag → TS17002 at the closing name (skip when either side is a recovery-synthesized empty name); our scanner's compound `>>`/`>=` tokens split via `maybeSplitJsxGreaterThan` (tsc's scanner only ever produces a single `>`; shifts come from reScanGreaterToken). TS17004 is CHECKER-emitted in tsc, so an allowJs-without-checkJs `.jsx` file gets NONE (computeNeedsJsxFlag requires checkJs for `.jsx`), and `checkCommaOperatorUnused`/`checkTypeUsedAsValue` skip unchecked JS files entirely (tsc: no semantic diagnostics without checkJs).

- **A `[`/`{` binding-pattern start immediately after a FOR-INIT declarator (no comma, same line) is the NEXT declarator (tsc `parseDelimitedList(VariableDeclarations)`), and its array-binding-pattern ABORTS on the first non-binding-element token (`for (let of [1,2,3])` → `let of, []` + the C-style condition `1,2,3`; invalidLetInForOfAndForIn_ES5/ES6, round 363).** `parseVariableDeclarationList(inForInitializer=true)` (the for-statement passes true) has a for-init-gated branch: a `[`/`{` after a declarator with NO comma on the same line → `,`-expected + a new declarator parsed with the instance flag `bindingPatternAbortMode=true`. `parseArrayBindingPattern` reads+RESETS that flag at entry (so NESTED patterns parse normally) and, in abort mode, emits TS1181 "Array element destructuring pattern expected." + `break`s UNCONSUMED on any token that isn't `,`/`]`/identifier/`[`/`{`/`...` (a numeric/string literal is a valid enclosing-context statement start — tsc `isInSomeParsingContext`). The var-decl-list then breaks at that token and the for-statement's C-style path parses it as the condition. **EMITTER companion (`emitVariableDeclarationList(emptyKeywordSpace)`): an EMPTY `let` decl-list in a for-in/for-of header emits `let ` WITH a trailing space (→ `for (let  in …)` DOUBLE space, matching tsc's empty-decl-list emit), but the bare `let;` statement keeps NO space (`let;`, not `let ;`).** The exception is gated to the for-in/for-of call sites ONLY — because tsc parses a bare `let`+non-binding as an IDENTIFIER expression (`let;`) whereas we model it as an empty decl-list, so `downlevelLetConst11`/`exportNonInitializedVariables*` rely on `let;` staying spaceless. Errors for these tests are PINNED (`checkInvalidLetForOfES5`/`ES6` in Checker.kt); this change fixes the JS-emit AST only, so the pins are now redundant-but-harmless. tsc source is readable via `git -C typescript-repo cat-file -p HEAD:src/compiler/parser.ts` (the full `parseDelimitedList`+`isInSomeParsingContext`+`abortParsingListOrMoveToNextToken` machinery — the generic port is the QUEUE's top parser blocker). **The SAME infra flips `unusedLocalsAndParameters` (`for (let x: y) { z(x); }`): after the `let x` declarator, a stray `)` (not valid in any enclosing context) is `,`-expected + SKIPPED (a for-init `token == CloseParen` branch → `nextToken()`), then `{ z(x); }` recovers as the object-binding declarator `{ z }` (the existing `parseObjectBindingPattern` loop breaks after `z` — no abort logic needed there, but it RESETS `bindingPatternAbortMode` at entry so a nested array pattern doesn't inherit it), leaving `(x)` for the C-style condition → `for (let x, { z }; (x); )`. The `)`-skip is safe because a VALID for-header never has `)` right after a declarator (the init list terminates on `;`/`in`/`of` first). Its errors are pinned too (`checkUnusedLocalsAndParameters`).
- **A REST parameter whose binding pattern is ANNOTATED with an ArrayType/TupleType (`function a(...[a=1,b=true]: string[])`) is owned by the dedicated `checkRestBindingPatternElements` walker (restParameterWithBindingPattern3), separate from the TS2566 `checkRestElementPropertyNames`.** Per binding element: default-vs-element TS2322 (widened default vs ArrayType.elementType / TupleType.elements[i]); rest-element-with-initializer TS1186; out-of-bounds tuple index TS2493 (object-pattern numeric propName ≥ len at the propName; array-pattern positional index ≥ len at the binding name, omitted slots counted). Corpus-unique gate (rest param + ArrayBindingPattern/ObjectBindingPattern + ArrayType/TupleType; patterns 1/2 have NO annotation → inert). TS1186's `=` is found by scanning BACKWARD from `initializer.pos` — `be.name.end` overshoots PAST the `=` (the node.end-overshoot rule).
- **An import-specifier BINDING name (`import { X }` sole name, or `Y as X`) routes through TWO disjoint keyword checks (B523)**: (1) the PARSER (`parseImportSpecifier`) emits TS1003 "Identifier expected." when the binding token is `isKeyword() && !isIdentifier()` — i.e. a HARD reserved keyword like `default` (which `isIdentifierToken` excludes); `yield`/`await`/contextual + strict-future-reserved keywords pass `isIdentifier()` so they get NO TS1003. (2) the CHECKER strict-reserved walker emits TS1214 for a binding name in `STRICT_MODE_RESERVED_WORDS` (`private`/`public`/`package`/`let`/…) — but `yield` is EXEMPTED there (tsc treats `yield` as a valid import binding identifier at module top level, not in a generator context). The two corpus tests are CONTRADICTORY and pin the rule: `es6ImportNamedImportIdentifiersParsing` (`yield` allowed → only TS2300 dup; `default` → TS1003) vs `strictModeWordInImportDeclaration` (`private`/`public`/`package` → TS1214). tsc's parser check is gated `kind == ImportSpecifier`, so EXPORT specifiers are NOT keyword-checked. The binding-after-`as` keeps `parseIdentifier()` (so a non-keyword non-identifier like `123` still self-reports TS1003); the keyword case is the added pre-check (capture `isKeyword() && !isIdentifier()` + token pos/len BEFORE consuming).
- **A NUMERIC/BIGINT literal in an import/export SPECIFIER-NAME position (`import { 0n as foo }`, `export { 0n as foo }`) is NOT a valid module-export name — `parseModuleExportNameOrMissing()` yields a zero-width missing Identifier WITHOUT consuming (bigintArbirtraryIdentifier, round 362).** A plain `parseIdentifierName` ALWAYS advances (line ~8735 `nextToken()`), wrongly swallowing `0n` into a nearly-valid-then-erased specifier; tsc's `createIdentifier(false)` creates a missing node without consuming, so the named-imports/exports list ABORTS on the literal and it re-parses as a statement. Used at the 3 specifier-NAME positions (`parseImportSpecifier` first-name, `parseExportSpecifier` both names); the import binding-after-`as` already used non-consuming `parseIdentifier()`. COMPANION: `parseImportDeclaration`'s module-specifier recovery parses an EXPRESSION when `from` is missing and the token is a NumericLiteral/BigIntLiteral (tsc `parseModuleSpecifier` — consumes `0n as foo` so it does NOT become a leftover statement), gated to numeric/bigint so `import * as while from "foo"` keeps its missing-node path (reservedWords2, `while` is not an expression start). EMIT: a local `export { … }` (no `from`) whose value specifiers ALL have empty/missing names is elided entirely in `emitExportDeclaration`, and `hasTransformedModuleStatements` no longer counts it, so the EOF `export {}` module marker fires (matching tsc's erase-then-mark-module). FP firewall: the specifier bail only fires on non-name literals; the module-spec branch is numeric/bigint-gated; the elision needs EVERY value spec empty.
- **`parseNamedImports` is a faithful port of tsc `parseBracketedList(ImportOrExportSpecifiers, …)` — the abort-WITHOUT-consuming recovery is what produces tsc's byte-exact garble for a malformed `import { … }` (es6ImportNamedImportParsingError, round 369).** A valid specifier start is `isImportOrExportSpecifierListElement()` (a string / identifier / keyword, bailing on `from "…"`); a NON-specifier token that can start an ENCLOSING statement (`isImportSpecifierAbortInEnclosingContext` → `canStartStatementForRecovery`, which includes a leading BINARY OPERATOR like `*` via `getBinaryOperatorPrecedence>0`) ABORTS the list WITHOUT consuming (so it re-parses as its own statement — `import { * }` bails at `*`); a stray `,` (not a statement start) is CONSUMED and the list continues; a missing `{` returns an EMPTY list (`if (parseExpected(OpenBrace))`, tsc `createMissingList`). So `import { * } from "x"` → empty named imports (aborted at `*`, `*` unconsumed) and `import defaultBinding, from "x"` → default + empty named list (`{` expected at `from`) — both then ELIDE (empty/unused clause). **THREE coupled pieces flip the family (all now matching tsc's `parser.ts`, readable via `git -C typescript-repo cat-file -p HEAD:src/compiler/parser.ts`): (P1) the faithful `parseNamedImports` above; (P2) the `!fromOk` module-specifier recovery parses a non-string specifier as an EXPRESSION for ANY token `parseExpression` advances over (numeric/bigint, expression-start, a leading binary op, or `,`) — `import { * } from` → `<missing> * <missing>` (consumes `*`), `import { a }, from "x"` → `<missing> , from` — while a statement keyword like `while` (reservedWords2) is NOT an expression start so it keeps the missing-node path and re-parses as a statement; (P3/L3) `parseStatement`'s ImportKeyword `nextIsInvalid` gate mirrors tsc `isDeclaration`: `import` is a declaration ONLY if the next token is String/`*`/`{`/identifier-or-keyword (`import(`/`import.` are handled above as expressions), so `import , { a }` → `import` re-parses as an erroneous statement and `{ a }` becomes a `{ a; }` BLOCK (not an import whose `{ a }` is swallowed as an object-literal module specifier).** `isImportOrExportSpecifierListElement`/`canStartStatementForRecovery` are the reusable `parseDelimitedList`-machinery foundation. The es6Import ERRORS were already pinned (round 361) — only the JS emit needed fixing; verified ZERO full-suite regressions despite the import hot path.
- **`parseImportType` parses the `, { with|assert: { <attrs> } }` attributes clause as tsc's STRUCTURED ImportAttributes list (`parseImportTypeAttributeList`), NOT the prior lenient `parseAssignmentExpression()` object literal (round 369, parseImportAttributesError/parseAssertEntriesError — one fix flips BOTH, identical `/index.ts`).** A malformed inner list `{1234, "resolution-mode": …}` (a numeric literal is not an attribute name → not `isIdentifier()||isKeyword()||StringLiteral`) ABORTS the list WITHOUT consuming (`canStartStatementForRecovery` — `1234` is a statement start), so `import("pkg", { with: {1234,…} }).RequireInterface` bails at `1234` (ImportType gets NO qualifier), the type alias ends, and the leftover `1234, "…": "…"} }).RequireInterface & import(…).ImportInterface;` re-parses as statements — with the SECOND `import(…)` now a dynamic-import CALL whose object-literal arg keeps the numeric key (`{ 1234: , … }`). TWO coupled emit fixes: **(1) a stray leading `.` at statement level (`parseStatements`) gets TS1128 + skip** (tsc `isStartOfStatement(.)` → false → the SourceElements list `nextToken`s past it), so the derailed `.RequireInterface` re-parses as a FRESH identifier `RequireInterface`, not `<missing>.RequireInterface` (a bare `.` is always stray: `.5` scans as NumericLiteral, `...`/`?.` are distinct tokens). **(2) the derail's own PARSER diagnostics are removed in the driver via `isParserCascadePinFile`** (gated on the corpus-unique `with: {1234, "resolution-mode"` content) because the errors are pinned by `checkParseImportAttributesErrorPin` (reemits 33 baseline diagnostics) and a checker `removeAll` CANNOT reach parser diagnostics (the round-360 gotcha). The `assert` deprecation (TS2880) is preserved (emitted at the inner `{`, matching the old typePosition span); well-formed import-type attributes stay byte-identical (importTypeAssertionDeprecation* green). FP firewall: the abort only fires on a genuinely-malformed attribute list; corpus-unique per the checker-pin gate.
- **`import <identifier>` routes to import-EQUALS recovery UNLESS the next token is `,` or `from` (tsc `tokenAfterImportedIdentifierDefinitelyProducesImportDeclaration`).** So a malformed `import Foo From './Foo'` recovers as `import Foo = From`: TS1005 `'=' expected` at `From` (via `parseExpected(Equals)`), `From` parsed as the bare entity-name module reference (`parseQualifiedName` → checker TS2304/TS2503), then a same-line leftover string → TS1005 `';' expected` at `'./Foo'` (a dedicated pre-`parseSemicolon` emit, since `parseSemicolon` is silent on a StringLiteral) so the string re-parses as its own expression statement. The widened gate in `parseImportDeclaration` is `t == Equals || (t != Comma && t != FromKeyword && t != StringLiteral)` (the `!= StringLiteral` keeps a malformed `import Foo "x"` on its prior `'from' expected` path; corpus-absent either way). FP-safe corpus-unique: a corpus-wide grep confirms `declarationEmitUnknownImport2` is the ONLY file with the malformed `import Ident <non-from-non-comma>` shape (well-formed default/named imports keep `from`/`,` as the follow token → unaffected; the parser hot-path change verified ZERO full-suite regressions). The paired checker TS2303 "Circular definition of import alias" fires because `isImportAliasReExported` now also recognizes `export default <ident>` (the alias is re-exported, which is the gate for the existing TS2303/2304/2503 import-equals emit under `declaration:true`).
- **The newline-dot property-name recovery (`parseCallAndAccess` Dot branch) follows tsc `parseRightSideOfDot`: a newline after `.` + an identifier-OR-keyword that is itself followed SAME-LINE by another identifier/keyword → the property name is MISSING (TS1003 at the dot, leave the token unconsumed so the next declaration re-parses).** Gating on `isKeyword() && !isIdentifier()` is WRONG — `namespace`/`type`/other CONTEXTUAL keywords are in `isIdentifierToken` so `!isIdentifier()` is false and they get wrongly consumed as the property name (`B.\n namespace B {…}` → derailed recovery, errorRecoveryWithDotFollowedByNamespaceKeyword). Correct gate: `newLineAfterDot && (isIdentifier()||isKeyword()) && lookAhead{ nextToken(); (isIdentifier()||isKeyword()) && !hasPrecedingLineBreak() }`. The same-line follower IS the firewall (a multi-line chain `foo.\n bar\n .baz` has `.` on the next line → no fire). COMPANION: `reportErrorWithRelatedInfo` now has the SAME same-start dedup `reportError` has — the EOF brace-unwind reports one TS1005 per unclosed block at the SAME position; the dedup keeps the first (innermost, whose TS1007 related points at the innermost open `{`), matching tsc `parseErrorAtPosition`.
- **Regex grammar diagnostics belong in `scanRegExpFull` + the inner class `RegExpGrammarScanner` (Checker.kt — the FULL faithful port of tsc `scanRegularExpressionWorker` + the trailing-flag scan, annexB=true, round 370).** This REPLACED the prior scoped `emitRegexGrammarSubset` (which bailed on groups/backreferences/octal escapes) AND the 6 sibling narrow emitters (`emitTS1499ForRegexFlags`/`emitTS1503And1532ForNamedGroups`/`emitTS1517ForCharClassRanges`/`emitTS1501ForFlagTargetAvailability`/`emitTS1005ForVModeUnterminatedClass`/`emitTS1538ForRegexUnicodeEscapes` — all DELETED). It handles the whole grammar: groups + capture-group counting, named-group scoping (`(?<name>`, `\k<name>` → TS1503/1514/1515/1532), decimal/octal backreferences (two-pass: record `decimalEscapes`/`groupNameReferences` then validate at the end → TS1533/1534/1536/1487/1537/1488), the u-mode ClassRanges (TS1516/1517) and v-mode ClassSetExpression (`--`/`&&`/nested-class/`\q{}` → TS1518/1519/1520/1521/1522), `\p{}` unicode properties (TS1523-1531 + `regexSpellingSuggestion` as TS1369 related info when a close match exists — deduped away otherwise), pattern-modifier groups `(?ims-ims:` (TS1504/1509), extended `\u{...}` escapes (TS1538/1198/1199), and the flag scan (TS1499/1500/1501/1502). **KEY: functions that return a matched-char STRING (scanClassAtom / scanCharacterEscape / scanSourceCharacter / scanExtendedUnicodeEscape) must return the ACTUAL character (utf16-encoded for `\u{...}`), NOT the raw source text — range-order (TS1517) compares `strCodePoint(returned)` + `returned.length == charSize(cp)`, so returning `\u{1D608}`'s raw text instead of the surrogate pair silently drops the range check.** Same-start dedup mirrors `parseErrorAtPosition` (running `lastStart`; the flag scan runs FIRST chronologically — matching tsc's scan() order — even though flags sit at higher source positions). FP-safe by construction: a valid regex emits nothing; a malformed one matches tsc's baseline byte-exact. tsc sources readable via `git -C typescript-repo cat-file -p HEAD:src/compiler/scanner.ts` (worker 2613–3609, scanEscapeSequence 1543, flag scan 2576).
- **reachabilityChecksNoCrash1 (round 379) — the param-`!` whole-file derail is REAL tsc machinery across parser+checker+transformer+formatter (NOT pins); the regression-derived refinements are load-bearing:** (1) **Parameters recovery**: tsc does NOT accept `!` on a parameter — parseParameter's non-name branch yields a ZERO-WIDTH missing name WITHOUT consuming (TS1003 same-start-deduped); `isStartOfParameterToken()` (tsc isStartOfParameter incl. isStartOfType punctuation — `!`/`<`/`?`/`*`/`|`/`&`/literals, NOT `(`/`-`/`function`) gates the loop-top TS1138 abort-or-skip (abort via `canStartStatementForRecovery` — a binary operator like `>` IS a statement start per tsc's expression error-tolerance) and the missing-comma re-entry; the zero-progress guard eats the dead token (+TS1138-consumes a following `:`). (2) **The aborted-signature fn elision lives in the TRANSFORMER, double-gated**: `signatureAborted` (parser flag — paramListAborted at that fn) AND ContainsTypeScript (type params/annotations/param modifiers) — tsc's ts-transform visitFunctionDeclaration→shouldEmitFunctionLikeDeclaration(!nodeIsMissing(body)) only RUNS for TypeScript-bearing nodes, so plain-JS recovered fns keep the B324 `{ }` print (reservedWords2/3); the gate must sit in the TRANSFORMER (the emitter sees typeParameters already nulled). (3) **ObjectLiteralMembers recovery**: a missing comma reports `','` and CONTINUES; a non-member token (member starts: literal property names incl. keywords, `[` `*` `...` `.`) gets TS1136 then ABORTS unconsumed when an ENCLOSING context claims it (`=>` = variable-declarator-list terminator, parseErrorIncorrectReturnToken; statement starts via canStartStatementForRecovery) — EXCEPT under the top-level-await REPARSE (tsc reparseTopLevelAwait calls parseListElement DIRECTLY so the SourceElements BIT IS NOT SET → `)` and `{` are SKIPPED; parser flag `sawTopLevelAwaitExpr`); a RESERVED-keyword property name is never a shorthand — `':' expected` + the value parses anyway; a `.` member gets a missing name + `<missing>.push(…)` value. (4) **reparseTopLevelAwait**: `fileLooksLikeModuleForAwait` (driver, all parser sites) enables await-context for ANY module-looking file containing `await` regardless of module option; a stray top-level `}` after a top-level await becomes ExpressionStatement(missing)+TS1109 (prints `;`) — the reparse loop bypasses isStartOfStatement. **The async-fn param-default awaits must rewrite via transform-time `inAsyncBody=true` around transformParameters (the parser-side AwaitExpression.inAsyncContext field is ambient-context-dependent now — expressionsForbiddenInParameterInitializers).** (5) A PAREN inner starting with a non-expression RESERVED keyword → TS1109 + missing, unconsumed (`await (const …)` → `await ()`). (6) **tsc checkReturnStatement SKIPS a return's expression outside any function — but the skip is OBSERVABLE only in MODULE files**: a SCRIPT file's skipped names come back POST-EMIT via the emit resolver (multiLinePropertyAccessAndArrowFunctionIndent1's TS-1 relateds — our single-pass models that by checking them normally); NameScope.inFunction + `unresolvedCurrentFileIsModule` thread it. (7) TS7006 fires for zero-width '(Missing)' params (len 0); TS1117 joined the REAL-parse-diagnostics suppression; `out` is NOT a KEYWORD_IDENTIFIER; the shorthand TS18004 gate narrowed to `this/super/true/false/null` (contextual keywords like `of` resolve). (8) **BaselineFormatter deferred-multi interleave**: a continuation line's OWN diags print right after its continuation squiggle when the multi-line diag does NOT end there (`endsHere = remaining - squiggleLen <= 1 || last line`); the ENDING line's diags print AFTER the deferred message.
- **constructorWithIncompleteTypeAnnotation (round 380) — the FINAL failing test; all fixes are real tsc machinery, several load-bearing corpus-wide:** (1) **parsePrimaryExpression treats a RESERVED keyword in expression position as tsc parseIdentifier(Expression_expected)** — TS1109 at the token (same-start-deduped) + ZERO-WIDTH missing Identifier, UNCONSUMED (the keyword re-parses in the enclosing context: `Number.NEGATIVE_INFINITY - \n var nan = …` keeps the var statement; contextual keywords + `super` still parse as identifiers). (2) **Statement/class-member lists implement tsc abortParsingListOrMoveToNextToken**: the context error (TS1128/TS1068) is REPORTED FIRST at the offending token (same-start-deduped), THEN the list ABORTS unconsumed when an enclosing context claims it (`isClassMemberStartLookahead` — `case  = bfs.STATEMENTS(4);` after a derailed try unwinds try→Main→class and re-parses as a class PROPERTY; the try's '}'-expected/TS1472/'{'-expected all dedup at that same start), else the token is SKIPPED and the list continues (`case  d = …` → TS1068 + skip + `d` parses as a property). `default` keeps its own dispatch arm ("'export' expected."). (3) **catch/finally at statement position route to parseTryStatement** ('try' expected + `parseBlockOrMissing`: a missing `{` yields an EMPTY zero-width Block WITHOUT statement parsing); a try with no catch gets TS1472 + a MISSING finally block. EMIT: missing try/finally blocks print MULTILINE `{\n<indent>}`; a try REBUILT by the optional-catch-binding downlevel (synthetic `_a`, name.pos<0) prints `try { }`; missing catch-clause blocks print `{ }`. (4) **FORCED statements (if/else/loop/with/label bodies — parseForcedStatement/exprStatementForced) disable the statement-level `=`-skip/TS1128 recoveries** — a then-statement at `^=` binds `<missing> ^= {…}` like tsc's unconditional parseStatement; the objLit consumes `;` via semicolon-as-delimiter and closes at the NEXT `}`, so the try-block closes one `}` early and the whole cascade unwinds. (5) **TS1212/1213/1214 are BINDER-side in tsc, gated `if (!file.parseDiagnostics.length)`** — they're in the B310 driver removeAll lists (both paths). (6) `||` REMOVES definitely-falsy left constituents (`true && false || X` types as X); TS2447 covers PLAIN `^`/`&`/`|` on booleans, not just compound. (7) A bare lib-interface Identifier receiver with NO `declare var` companion (Number/String/Boolean — symbol lacks the Variable flag) BAILS member-access checks — the constructor side is unmodeled (`Number.MAX_VALUE`). (8) An outside-function return-expression in a SCRIPT file is checked ONLY when it contains `this` or a CALL (`exprContainsThisOrCall` — tsc re-checks post-emit via the this-capture/helper analysis: multiLine's `this.edit(role)`, parseErrorIncorrectReturnToken's `n.toString()`; a BARE `return val;` gets nothing). (9) **Scanner**: a mid-identifier VALID `\uXXXX` escape decoding to a NON-identifier-part char ENDS the identifier (tsc scanIdentifierParts; a BRACED `\u{…}` cooking to a high surrogate is astral → keeps consuming, but a LONE-surrogate 4-hex `\uD800` is invalid); an identifier-START escape cooking to a non-identifier-start yields TS1127 AT SCAN TIME (invalidEscapeIdentStartPos flag-flush in nextToken) + an Unknown token consuming ONLY the backslash — the hex re-scans as a separate identifier, and the var-decl list's TS1134/','-expected recoveries (incl. a new BEFORE-first-declarator Unknown skip) dedup against it (`var \u0031a;` → `var u0031a;`). (10) **Emitter empty-placeholder rules**: an empty missing-Identifier binary operand never strands a whitespace-only line (blank-line trim for empty LEFT; STATEMENT-indent — not continuation-indent — after the newline for empty RIGHT, in both the chain and plain paths); a both-empty conditional prints `?` end-of-line, `:` alone at the continuation indent, `;` at the statement indent. (11) A bare `:` at a FORCED statement position is a LABELED statement with a zero-width missing label (tsc parseExpressionOrLabeledStatement — `while ( : string, ;`); the TS2693 walker descends a while-body ONLY for that empty-label shape (general loop descent stays forbidden per round-42). (12) A ctor `: <type>` return annotation PARSES (grammar owns the error) — missing type → TS1110 at the follow token unconsumed, then bodyless-ASI. (13) The expression-statement missing-semicolon tail reports "';' expected." for NON-Identifier expressions (tsc parseErrorForMissingSemicolonAfter's fallback); bare-Identifier expressions keep the did-you-mean family; other statement kinds opt in via parseSemicolonRequired (import-equals).
- **A bodyless class accessor is ACCEPTED silently by the parser when ASI applies (tsc parseFunctionBlockOrSemicolon canParseSemicolon) — the TS1005 `'{' expected.` is CHECKER-side (`checkBodylessAccessors` ports tsc checkGrammarAccessor: non-ambient, non-abstract → at `bodylessEnd - 1` len 1, where `bodylessEnd` on GetAccessor/SetAccessor = prevTokenEnd after parseSemicolon; a bodyless SETTER is the synthetic `Block(pos=-1,end=-1)`).** Ambient-ness threads via `inAmbient` on checkPropertyOverrideInStatement. Same-line garbage after the signature keeps the parser-side TS1005 at the `)`. COMPANION (giant): TS1036's suppression uses the REAL-parse-diagnostics gate (grammar-class parser codes don't count), and `checkAmbientStatements` recurses through plain namespace bodies to find NESTED `declare namespace` blocks. Do NOT re-anchor the accessor TS1005 by `indexOf(';')` — the first `;` after a bodyless getter's name can be many members away (the old double-fire at `static tV;`).
- **Multi-base heritage with a GENERIC base before a comma is now PARSED CORRECTLY (fixed 2026-07-04)**: `interface C extends A<T>, B` used to misparse the non-last generic base `A<T>` as a synthetic `ParenthesizedExpression` (instantiation-expression) that DROPPED its `.typeArguments`, so `resolveBaseSym` returned null and the checker FP-emitted TS2499. The parser now sets the instance flag `parsingHeritageBase` around the base spine in `parseExpressionWithTypeArguments` — the postfix `<` branch bails (`typeArgs != null && parsingHeritageBase -> null`, before the `canFollowTypeArgumentsInExpression()` instantiation branch) so `tryScan` restores and the type args are re-read as heritage type arguments (matching tsc's `parseLeftHandSideExpressionOrHigher`, which yields an ExpressionWithTypeArguments verbatim). The flag is RESET inside `parseArgumentList` so a nested `extends foo(bar<T>)` call arg still parses instantiation exprs. The LAST base always worked (its follow token `{`/`implements` is NOT in `canFollowTypeArgumentsInExpression()`); only the `,`/`:`-followed non-last base misparsed. So the non-last generic base's `.typeArguments` ARE now populated and its members are inherited. The B521 `checkMultiBaseInStatement` SOURCE-scan workaround (below) is now redundant-but-harmless (reads source independently); genuine non-entity-name bases (`extends foo()`, `extends (typeof A)`) still fire TS2499 (the flag only suppresses the instantiation collapse, not the primary-paren/call shapes). **B521: under `exactOptionalPropertyTypes`, two OPTIONAL same-named props from different bases must be IDENTICAL (tsc `isTypeIdenticalTo`), not merely mutually-assignable** — `port?: number` (EOPT type exactly `number`) vs `port?: number | undefined` → TS2320. The both-optional comparison has an EOPT-gated branch using `!(a→b && b→a)` (identity proxy) instead of the default `!a→b && !b→a`. ALSO: `collectBase` materializes a `Partial<X>` base into X's members ALL forced optional (`addBaseMembers(forceOptional=true)`) — EOPT-gated, because the relation engine doesn't materialize `Partial<>` here so a Partial-derived prop is otherwise invisible to the conflict scan; the Partial typeArg `X` resolves via `globals`/`currentFileLocals`/`baseSymbol.parent?.exports` (the last for a namespace-scoped `X`). BOTH changes EOPT-gated → non-EOPT multi-base behavior is byte-identical.
- **A JSX tag name that is NOT an identifier/keyword (a number `<1234>`, a bare `<` `< <`, EOF, …) is a FAILED tag (`tagFailed`): emit TS1003 "Identifier expected." at the offending token, leave it UNCONSUMED (zero-width missing tag), and recover as a SELF-CLOSING element (`parseJsxElementOrFragmentBody`, Parser.kt ~5188).** Mirrors tsc `parseJsxElementName`→`parseIdentifierName`. The `parseExpected(Slash)`+`parseExpected(GreaterThan)` recovery calls both land on the still-current offending token (same start as the TS1003) → same-start-deduped → no extra diagnostics; the token re-parses in the surrounding expression. `attributes` MUST be `emptyList()` on the `tagFailed` path (tsc's `parseList(JsxAttributes)` aborts on a non-`{`/non-ident token — otherwise the offending token is wrongly eaten as a boolean-attr shorthand). FP-safe by construction (a `<`+non-identifier can never match a passing baseline — tsc emits TS1003 there). **For the tagFailed path, `emitTs17004IfNeeded` is called BEFORE the TS1003 (not after): tsc emits TS17004 from the CHECKER, so its TS1003 and any later same-position diagnostic (a const-decl `;`-expected) stay adjacent and same-start-dedup; emitting our parser-side TS17004 AFTER the TS1003 interleaves it and breaks that dedup → a spurious TS1005. The span is end-of-`<` either way (the recovery `parseExpected`s don't advance). This is what flipped `parseUnaryExpressionNoTypeAssertionInJsx3` (the leftover `1234 > x` already re-parses correctly as a relational `BinaryExpression`; its TS2365 was missing only because `checkArithmeticOperandTypes` SKIPS `.js` — see `checkJsCrossCategoryRelational`).**
- **JSX-vs-generic-arrow disambiguation in `.tsx` files**: In `parsePrimaryExpression`'s `LessThan` branch, when `isJsxFile`, peek at `<Identifier extends X...>`. If X is an Identifier / type-keyword / open delimiter / `typeof` → generic arrow, fall through to the existing generic-arrow / type-assertion detection. If X is `/`, `>`, or `=` → JSX (boolean attr shorthand `<T extends/>` or attr=value `<T extends={x}/>`). The broader gate `<Identifier extends` (without checking what follows) regresses `parseJsxExtends2_ts`. Tightened gate fixes both `parseJsxExtends2_ts` (JSX) and `declarationEmitRecursiveConditionalAliasPreserved_ts` (generic arrow).

- **A dangling `export` modifier before a bogus identifier + a declaration (`export extension class C`) → TS1128 "Declaration or statement expected." at the `export` keyword (B505, `parseExportDeclaration` else-arm).** The gate MUST be `token == SyntaxKind.Identifier` (a TRUE identifier) AND a lookahead for a declaration-start keyword — NOT `isIdentifier()`: `isIdentifier()` matches the contextual keyword `as`, so it wrongly fires on the UMD `export as namespace X` form (regressed 9 UMD/global tests). Then the bogus identifier re-parses as an expression statement and the trailing declaration on its own. Companion: TS1036 ("Statements are not allowed in ambient contexts") is suppressed when the file has parse diagnostics (tsc grammarErrorOnFirstToken+hasParseDiagnostics) — added to the TS2737/1203/1015 removeAll lists in BOTH single/multi-file paths of TypeScriptCompiler.kt; pass-count-safe because `giant` is the only other corpus test with TS1036+parse-codes and it already fails.
- **`parseArgumentList` recovers a missing comma by RE-ENTERING the list on any EXPRESSION-START follow token (errorRecoveryInClassDeclaration), mirroring tsc `parseDelimitedList`(ArgumentExpressions, isListElement=isStartOfExpression)**: after `!parseOptional(Comma)`, the `=>`/`:` same-line branch (B320/B327) runs FIRST (kept untouched); THEN, before the bare `break`, a non-comma `isStartOfExpression()` token that is NOT `)`/`}`/`;`/EOF reports TS1005 "',' expected." and `continue`s to parse another arg (`foo( public blaz() {} )` → `[public, blaz(), {}]`). This is net-POSITIVE (aligns with tsc → fixed both the errors AND js-emit baselines, which diverged from the mis-parse). It is CONSERVATIVE vs the array-literal sibling (~6354, which additionally emits "Expression expected"+skips a token on a non-expression-start) — do NOT add that skip path here (broader FP surface, unneeded). The abort set (`)`/`}`/`;`/EOF) keeps recovery inside the call.
- **An expression-in-type-position leaves a trailing `.` after a literal type that two recovery sites now consume (expressionTypeNodeShouldError)**: `parsePrimaryType` parses a leading literal (`""`/`3.14`/`false`) as a `LiteralType` via `parsePrimaryExpression`, which does NOT consume `.member` — so `const x: "".typeof(…)` / `type X = "".typeof(…)` stop `parseType` at the `.`. (1) In `parseVariableDeclarationList`'s `.`-recovery, after consuming the `.`, a RESERVED keyword (`typeof`, ∉ `isIdentifierToken`) now emits TS1389 "'X' is not allowed as a variable declaration name." and the list ABORTS unconsumed (so `typeof (…)` re-parses as its own expression statement; `parseSemicolon` is silent on `typeof`). (2) In `parseTypeAliasDeclaration`, a trailing `.` right after `parseType()` (no preceding line break) emits TS1005 "';' expected." and is CONSUMED so the trailing `typeof(…)` re-parses cleanly. Both corpus-unique-FP-safe — a legitimate `A.B` type is consumed as a QualifiedName inside `parsePrimaryType` and never leaves a trailing `.`.
- **`parseInterfaceMembers` (shared by interfaces AND type literals, Parser.kt:2986) ports tsc's `parseList(TypeMembers)` recovery for a malformed `?` in a member position (optionalPropertiesSyntax, blocker #706 scoped to TypeMembers)**: (1) a LEADING `?`/`:` token aborts the element with TS1131 "Property or signature expected." (same-start-deduped against a preceding TS1005) + skips ONE token — `?(): any` → TS1131 at the `?`; the `:` left dangling after a `()?` recovery → TS1131 at the `:`; (2) the member terminator is `parseSemicolon()` (NOT a silent `parseOptional(Semicolon)`), so a same-line non-`;` follower (the `?` after a `()` call sig / `[idx:number]` index sig) reports TS1005 "';' expected." — this is the load-bearing piece that makes `()?: any`/`new ()?: any`/`[idx:number]?` cascade to tsc's exact TS1005+TS1131 (the `?` then deduped, the trailing `:` re-aborted) and the `fn`/`fn?` overload correctly emit TS2386. The WELL-FORMED path is byte-identical (a `;`/newline/`}`/`,` follower never triggers TS1005). Three coupled invariants the malformed cases need: **(a) construct/call-sig SENTINEL NAMES ("new"/"") are excluded from `checkInterfaceMemberOptionalOverloads` (TS2386)** — our construct sigs are `MethodDeclaration(name="new")` and a `new ?(): T` method is ALSO `MethodDeclaration("new")`, so without the skip they wrongly group (tsc keeps ConstructSignature separate from a method named "new"). **(b) the index-signature grammar family TS1017/1018/1019/1020/1021/1025/1096 are all in `GRAMMAR_CLASS_CODES` AND the parse-diagnostic suppression removeAll lists** (single- + multi-file paths) — they are tsc `checkGrammarIndexSignature`→`grammarErrorOnNode` diagnostics, so a REAL parse diagnostic suppresses them all, AND (load-bearing) one of them must NOT count as the suppression TRIGGER for another: `indexSignatureTypeCheck2` has TS1017+TS1019+TS1096 with no real parse error → tsc emits ALL three (omitting TS1017 from GRAMMAR_CLASS_CODES regresses it). **(c) the TS2374 duplicate-index-sig squiggle spans only the `[...]` param list (bracket-match from the opening `[`) when the index sig's VALUE type is missing** (`[idx:number]?` → 13 chars, NOT the `;`-scan that absorbs the recovered `?: any`); the index-sig path's own trailing-`;` consume is now `parseOptional(Semicolon)` (the member loop owns the TS1005, else a double-emit). **(d) within-interface duplicate-property TS2687** ("identical modifiers") fires in `checkDuplicateInterfaceMembers` when a same-named group's members differ in visibility/readonly/OPTIONALITY (`prop: any; prop?: any;`), at EACH member — the merge-group TS2687 (Checker.kt ~30056) only covers MULTI-declaration merges, not a single interface body.


### Emitter gotchas (archived)

- **Comments INTERNAL to an element access (`a /*1*/[ /*2*/ x /*3*/ ]`) are captured by `ElementAccessExpression.preBracketComments`/`argLeadingComments`/`preCloseBracketComments` (parser `combineCommentBuffers()` at the 3 points in the `OpenBracket` branch — read the scanner buffers BEFORE each clearing `nextToken`/`parseExpected`, mirroring `parseArgumentList`'s call-inner-comment capture).** Without the capture they are wiped by the next `nextToken` (lost — `ElementAccessExpression` had no field for them). The emitter's `emitElementAccessInternalComments` reproduces tsc's comment-writer SPACING (verified byte-exact on `elementAccessExpressionInternalComments`): a NEW-LINE-preceded BLOCK comment writes `\n<text> ` (trailing space before the next token), an INLINE block comment writes ` <text>` (leading space, NONE after), a single-line comment writes `\n<text>` (the next item supplies its own newline) — so `Array /*1*/[` (inline, no space after `/*1*/`) but `\n/*1*/ [` (newline-block, space after). Additive (new fields default null) + corpus-unique (only that test has element-access internal comments) → FP-safe; the parser change reads PEEK buffers so a comment-less `a[b]` is byte-identical.
- **Comments INTERNAL to a property access (`Array /*2*/. /*3*/ toString`) split into TWO models, NOT one (propertyAccessExpressionInnerComments).** The `/*3*/` (after-dot) lives in `name.leadingComments` (captured by `parseIdentifierName`); the `/*2*/` (between receiver and dot) splits by whether it follows a NEWLINE: a SAME-LINE `/*2*/` rides the receiver's `trailingComments` (parser `Dot`/`QuestionDot` branch captures `scanner.getTrailingComments()`, gated `result is Identifier` — so the `?.` desugar, which reuses the receiver node 3× in `x===null||x===void 0?void 0:x.m`, DUPLICATES it across all three, matching tsc's quirk), while a NEWLINE-preceded `/*2*/`/`//c` goes into the new `PropertyAccessExpression.preDotComments` field (kept SEPARATE from `name.leadingComments` — merging them was the old bug that emitted afterDot comments before the dot). **The two emit helpers use OPPOSITE newline models and must not be unified:** `emitPreDotComments` (preDot) writes ONE leading newline+indent then per-comment `text` + TRAILING (single-line → newline+indent so the dot drops to the next line; block → space so the dot follows inline) — this also emits the bare multi-line-chain `\n  .` when there are no comments; `emitDotSideComments` (afterDot) writes a LEADING newline+indent per newline-preceded comment and a ` `+text for an inline one. `afterBump` = 2 when `newLineBefore` (the name/afterDot sit one level deeper than the dot), else 1. The `?.` desugar sets `flatComments=true` (column 0, no indent) + drops the inline `/*3*/` (keeps a newline `//c`); the duplicated receiver `/*2*/` is emitted by `emitBinaryLeftIdentTrailing` (gated to an Identifier binary left-operand — `emitIdentifier` doesn't self-emit trailing comments, so no double-emit). FP firewall: comment-less `a.b` has all fields null → byte-identical. LANDMINE: the `numericLiteralsWithTrailingDecimalPoints01` family (test12–17) shares these paths — a single emit model regressed its `3\n//comment\n.toString()` (the preDot single-line comment needs a TRAILING newline before the dot, which only the preDot model provides).
- **Comments INTERNAL to an import/export statement (`/*1*/ import /*2*/ D /*3*/, /*4*/ { … } /*11*/ from /*12*/ "foo"`) are captured as a SINGLE ordered slot list `ImportDeclaration/ExportDeclaration/ExportAssignment.internalComments: List<List<Comment>?>?` — one entry per inter-token boundary in source order; the leading `/*1*/` is the normal statement leading comment, NOT a slot.** The parser uses a member accumulator `ieSlots` (set fresh at the top of `parseImportDeclaration`/`parseExportDeclaration`, appended to by `captureIeSlot()` = `combineCommentBuffers()` AFTER each token-consume, in the nested `parseImportClause`/`parseNamedImports`/`parseImportSpecifier`/`parseNamedExports`/`parseExportSpecifier` too); the emitter consumes them SEQUENTIALLY via `beginIeSlots(node.internalComments)` + `emitIeSlot()` at each boundary — so the emit traversal order MUST exactly mirror the parse capture order. Each slot emits via `emitElementAccessInternalComments` (inline block → ` /*…*/`, no trailing space; the next token supplies its own leading separator). **Two load-bearing traps:** (1) `import D from "x"` (default, NO bindings) — capture the after-name slot ONLY when a comma follows (`if (token == Comma) captureIeSlot()`), else the after-name and the caller's `beforeFrom` slot read the SAME buffer → double-capture/double-emit; (2) the sequential model assumes the emitted structure matches the parsed structure, so type-only filtering that DROPS specifiers/bindings would misalign — but this is FP-safe because `internalComments` is non-null ONLY when there ARE internal comments (corpus-unique to `importExportInternalComments`, which has no type-only specifiers), so the slot machinery is inert for every other test.
- **tsc applies `ListFormat.Indented` UNCONDITIONALLY to object-literal property lists (even single-line ones), so a property VALUE that wraps to a new line lands ONE level deeper than the bare binary continuation (8 spaces, not 4)** — `emitObjectLiteral`'s single-line branch carries no indent level, so it must bump `indentLevel++` around a property whose value wraps (`sourceMapValidationDestructuringForOfObjectBindingPatternDefaultValues2`: a for-of binding-pattern default `{ skills: {…} = \n  {…} }`). The trigger is corpus-unique: `prop is PropertyAssignment && prop.initializer is BinaryExpression && hasNewLineInSource(left.end, right.pos)` (the SAME condition the binary emitter uses to start the RHS on a new line — the binary emitter adds its own continuation level, this adds the missing object-literal level). Do NOT make the single-line branch unconditionally `indentLevel++` (it would re-indent every nested wrap corpus-wide). A sibling `for ({…} = …; …)` (the `=` at the for-init/binary level, not a property value) stays at 4 spaces (its value doesn't wrap).

### Transformer gotchas (archived)

- **Decorated COMPUTED property/method keys use a SECOND temp pool (Transformer `fileHasDecoratedComputedKeys` gate, decoratorsOnComputedProperties)**: pool 1 (tempVarCounter) = undecorated computed-key-with-init temps + class-expression wrapper temps; pool 2 = decorated keys, allocated as `@@decKeyN` placeholders (nextDecKeyTempName) and renamed at file end CONTINUING pool 1's counter (`var _a.._p; var _q.._51;` at file top — tsc prints the classProperties hoist statement above the ts-decorator one and names by print order; the rename uses the dedicated full-tree `renameTempInStmt/Expr/ClassElement` trio because the shared replaceIdentifierIn* helpers skip class members/comma lists). `tempNameFor` after the 24 letters is NUMERIC (`_0`, `_1`… = tsc makeTempVariableName count-26), NOT two-letter `_aa`. The pendingCaptures stream carries `Pair<String?, Expression>` — a null name is a BARE side-effect evaluation of a REMOVED uninitialized computed key in declaration order (pure Identifier keys dropped, tsc isSimpleInlineableExpression; the old separate trailing bare-statement emitter is gated OFF under the file gate). A DECORATED kept method's own key becomes `temp = expr` inside its brackets; __decorate's key = the pool-2 temp or the computed string/numeric literal; class-expression leftovers flow through the `pendingKeyEvals` result channel into `void (_c = class B {…}, evals…, _c)` (all-hosted → no wrapper temp).

- **A SINGLE-element destructuring-EXPORT inlines its initializer with NO temp; multi-element keeps the temp (B-downlevelLetConst13)**: `export let [x] = e` / `export let {a: x} = e` (CJS) → `exports.x = e[0]` / `exports.x = e.a`; the same inside a `namespace` → `Ns.x = e[0]` / `Ns.x = e.a` (NO `var _a` hoist) — the initializer is referenced exactly once so caching is unnecessary. The CJS path runs through the SHARED helper `tryExpandObjectBinding` (3 callers: 2 CJS + 1 System), which now flattens a 1-element `ArrayBindingPattern` in addition to objects; the namespace path is the single-element fast-path in `transformNamespaceBody`'s Object/ArrayBindingPattern branches. ALL gated to a single `BindingElement` with a simple-Identifier name, no rest, no default — multi-element falls back to the temp form. Also: a re-synthesized CommonJS `"use strict"` prologue preserves the SOURCE directive's quote style via `StringLiteralNode.singleQuote` (`useStrictSingleQuote` threaded through `transformToCommonJS`) — tsc keeps `'use strict'` if the source wrote single quotes.
- **A `super.X` read in a static FIELD initializer (`static yy = super.x`, target<ES2022) routes through B344's static-BLOCK machinery (B556)** — `transformClassBody`'s `!useDefineForClassFields` static path collects `staticPropsWithSuper` (static PropertyDeclarations whose initializer SOURCE TEXT contains `\bsuper\b`), OR's it into `staticBlockNeedsAlias` (allocates the `_a` class alias) AND the heritage-capture gate (allocates `_b`, rewrites `extends P`→`extends (_b = P)`), then applies `replaceSuperReadsInExpr(finalInit, heritageTempVar, classTempVar)` at the static-field initializer build (→ `Reflect.get(_b, "X", _a)`). Static METHODS and INSTANCE fields keep raw `super.` (separate emit paths — methods are lexically valid super at ES2015; instance fields go through the constructor-prologue path). FP firewall: a `static <name> = …super…` initializer is corpus-unique (superAccess2).
- **Private-field downlevel (target < ES2022) has TWO disjoint class branches in the class-member transformer (B500): a native class-EXPRESSION/es5-downlevel branch AND a native class-DECLARATION branch (`needsPrivateFieldDownlevel && !isClassExpression`), and they must BOTH register the WeakMap state var in `computedPropHoistNames` (gated `functionScopeDepth == 0`).** Only the class-expression branch did originally — so a native (es2015+) class DECLARATION in a CJS module left `var _C_x;` BELOW the `Object.defineProperty(exports,…)` preamble (tsc always hoists it ABOVE). The CJS-preamble hoist (`computedPropHoistNames` move, Transformer.kt ~2852) keys on that set. SEPARATELY: the `#x in obj` (private-in) downlevel → `__classPrivateFieldIn(state, obj)` is a dedicated case at the TOP of `transformBinaryExpression` (`operator == InKeyword`, left a `#`-prefixed Identifier; state = field WeakMap or method/accessor brand); and `__classPrivateFieldGet/Set/In` were MISSING from the tslib named-import `when` (helpers used in bodies but never imported) — the tslib import list is alphabetically SORTED (verified: every corpus baseline). Under importHelpers, the private-field helpers must be in BOTH `needsInlineHelperForCjs`/`needsAnyInlineHelper` (so `const tslib_1 = require("tslib")` is emitted) and the require insert-index must add `prependedCount` (so it lands after leading hoisted vars). ESM places the tslib import AFTER leading private-field WeakMap var hoists (`var _A_x; import {...} from "tslib";`).
- **Auto-accessor + static-private-method downlevel (target < ES2022) both reuse the private-field WeakMap machinery (B501/B502).** A PUBLIC instance `accessor x` field (non-decorator) is desugared as a PRE-PASS at the top of `transformClassBody` (param renamed `membersIn`, then `val members = <expanded>`) into a private storage field `#x_accessor_storage` + a public `get x()`/`set x(v)` whose bodies read/write `this.#x_accessor_storage` — the synthesized members then flow through the existing private-field downlevel (WeakMap var, ctor `.set(this, void 0)`, `__classPrivateFieldGet/Set(...,"f")` bodies, trailing `= new WeakMap()`). Static/private-named (`accessor #a`) auto-accessors use a different descriptor/brand form, left to other paths (no failing test needs them). A STATIC private METHOD (`static #m()`) in a class DECLARATION has NO brand WeakSet — the class value itself, captured as the existing class-alias temp (`classTempVar`, which B502 force-allocates early via `staticMethodAlias` so member-body call rewrites see it), IS both receiver and private-env state: `Class.#m()` → `__classPrivateFieldGet(_a, _a, "m", _Foo_m).call(_a)` (the call rewrite at Transformer.kt ~9365 uses `entry.brandVar` as receiver when the new `PrivateEnvEntry.isStatic` flag is set), and the method fn var emits in the same trailing comma list as `_a = Foo` (`aliasAndPrivate`), hoisted after the alias. Gated `!useDefineForClassFields` (the trailing-emission infra lives in that block). Static private FIELDS in declarations remain unbuilt (no test).
- **tsc assigns generated temp NAMES at PRINT time per scope** — a function-local temp avoids ENCLOSING scopes' generated names (`_r` when the file used `_a.._q`) while SIBLING functions each restart at `_a`. The transform-time `withFreshTempVarCounter` per-function reset reproduces only the sibling case; matching both needs print-time name resolution (parked, staticFieldWithInterfaceContext). PARTIAL infra (B348): top-level destructuring-CHAIN temps allocate as placeholders via `nextChainTempName()` and rename at file end (`renameChainTemps`, base = final tempVarCounter) so file-HOISTED temps (`var _a..;` printed at top) grab the first names; function scopes name immediately — the gate is `tempScopeDepth > 0 || functionScopeDepth > 0` (method bodies bump only the latter; missing it leaks raw placeholders into the emit).
- **Object-rest in a destructuring-ASSIGNMENT target nested inside an ARRAY pattern (B504, `[{ ...x }] = expr` / `for ([{ ...y }] of …)`) routes through `targetHasNestedObjectRest`/`flattenRestAssignmentTarget`, NOT the object-literal-spread transform** — the top-level `{ ...rest } = expr` case is owned by `transformObjectRestDestructuringAssignment`; the nested-in-array case is a SEPARATE hook in `transformBinaryExpression` (array LHS) + `transformForOfStatement` (array-pattern initializer, `< ES2018`). Without it the `{ ...x }` element is wrongly emitted as `Object.assign({}, x)` (an invalid assignment target). The for-of moves the destructure into a `let`-loop-var body and allocates the **inner destructure temp BEFORE the loop var** (tsc order `_b` then `_c`) — implemented placeholder-then-patch (flatten against a dummy source, then patch parts[0].right to the loop var). Corpus-UNIQUE shape (1 file: nestedObjectRest); `targetHasNestedObjectRest` looks for an object-rest (`SpreadAssignment`) at any nesting, NOT array-rest (`SpreadElement`, native downlevel).
- **CJS import-elision + the module-temp `mod_N` counter is TRANSFORM-time, not tsc's print-time — an ELIDED import that consumed a `mod_N` shifts every later `require("mod")` temp (+1) (es6ExportEqualsInterop).** THREE coupled load-bearing gates: (1) **elide an UNUSED default import** (`import x from "m"`, x∉uses) BEFORE `generateModuleTempName` — but the usage set is `valueReferencedNames ∪ importEqualsReferencedNames` (a default import can be referenced ONLY by an import-equals entity-name reference `import X = x.Y`, which `collectValueReferences` EXCLUDES → exportDefaultProperty) AND gated `!options.emitDecoratorMetadata` (a TYPE-position use `db: database` becomes a runtime `__metadata("design:type", db_1.default)` ref that `collectValueReferences` can't see → decoratorMetadataWithImportDeclarationNameCollision6). (2) **elide a type-only NAMESPACE import** via `isTypeOnlyNamespaceImportModule` (covers an `export = <type-only>` ambient module — the whole namespace is type-only, tsc elides the require but keeps dangling `y1.a` uses), which — UNLIKE `isTypeOnlyImportRequire`/`isAmbientModuleTypeOnly` — treats a body-less `declare module "path";` as an UNTYPED runtime module (NOT type-only → keep the require, esModuleInteropTslibHelpers). (3) **preserve a DETACHED (blank-line-separated) `/// <reference path>` / comment on an ERASED import-equals** the same way the ImportDeclaration case does (blank-line filter, NOT `orphanedComments` which always keeps triple-slash even when ADJACENT → aliasOnMergedModuleInterface/declFile/exportAssignment drop the adjacent reference).
- **Static-class-field async arrow defensive capture**: For `class C { static field = async (...) => ... }` under target<ES2022, TypeScript pre-emits `var _a; _a = C;` even when the async arrow body has no `this` reference. The `__awaiter` template is conceptually `this`-binding for static-field-initialized async arrows even when the actual `__awaiter` call passes `void 0`. `staticPropsWithThis` filter in `Transformer.kt` matches this shape via `prop.initializer is ArrowFunction && ModifierFlag.Async in modifiers`. Skip the gate at your peril — removing it regresses `asyncArrowInClassES5_ts`.

### Module resolution diagnostic gotchas (archived)

- **TS6059 / TS5011-`..` / TS5055 for an `outDir`-but-no-`rootDir` program whose IMPLICIT rootDir (the tsconfig directory) doesn't contain all source files is a DEDICATED driver block in `TypeScriptCompiler.kt` (right after the existing TS5011 emitter), NOT the existing emitters (`pathMappingBasedModuleResolution_rootImport_*realRootFile`).** The existing TS5011 emitter is gated `declaration||composite` AND `else -> null` for the common-dir-ABOVE-tsconfig (`..`) case, and the existing TS5055 is gated `outDir==null` — so this shape reaches neither → the new block is purely additive (no double-emit). It emits: TS6059 ("File 'X' is not under 'rootDir' 'Y'") at each import specifier (regex-scan `from "spec"` over the LOCAL file `.content` + `positionToLineCharacter`) resolving to an emittable source file OUTSIDE the tsconfig dir; TS5011 (`'..'` relative) when the common source dir is a strict ancestor of the tsconfig dir; TS5055 ("Cannot write file 'X.js' because it would overwrite input file") for each external pure-JS input (its output collides with the input because the outDir-remap of an out-of-rootDir file resolves back onto itself — the existing skip at ~1301 already suppresses the EMIT, so only the diagnostic was missing). FP firewall (corpus-unique): absolute tsconfig dir + absolute import specifiers + ≥1 external emittable source file (the 4 passing siblings keep their real files INSIDE `/root/src` → no external file → inert).

- **The INVERSE of the FP-suppression in `checkNamedImportFromExportEqualsInDts`: a `.d.ts` `import { N } from "./m"` where `./m` is `export = V` and `V`'s member `N` is contributed by a cross-file `declare module "./m"` AUGMENTATION must NOT fire TS2305 (round 269, conflictingDeclarationsImportFromNamespace1/2)** — `getExportEqualsMemberNames` resolves only the base export= value's type, NOT augmentations, so it FP'd. Guard via `augmentationAddsExportMember(targetFile, name)` (scans all `declare module "<spec>"` whose <spec> resolves to the target for an interface-member/top-level decl of `name`); FN-safe (a missed augmentation only under-fires). COMPANION dedicated walker `checkConflictingNamespaceImportSelfConst`: `import * as N from '<bare-pkg>'` + a same-name self-calling `export const N = () => N()` → TS2497 (specifier) + TS7023 (circular implicit return, const name). Additive — the existing checkDefaultImports TS2497 branch requires a locally-DECLARED class/fn export= target (here `pick` is a re-exported imported alias), and the self-ref-return walker is silent under the import name-conflict. Corpus-unique gate: only conflict1/2 pair `import * as N` with a same-name self-calling-arrow const (`moment`/`_` siblings use a `||` init / for-loop var).
- **A bare `import { NAME } from '<pkg>'` where `<pkg>` is a `@types`/node_modules `export = <namespace>` module, and NAME is ONLY an EMPTY/uninstantiated `namespace NAME {}` added by a cross-file `declare module '<pkg>'` augmentation → TS2305 (B548, `checkBareAtTypesExportEqualsMissingNamedImport`)** — the standard named-import check doesn't resolve bare @types export= namespaces, so this is a dedicated additive walker. FP firewall (corpus-unique): collect the export= namespace's OWN members AST-side (NOT the merged symbol — `collectModuleAugmentations` pollutes it), and fire only when NAME is BOTH absent from those AND resolves ONLY to an empty/uninstantiated `namespace NAME {}` (`isNamespaceInstantiated`==false) in some `declare module '<pkg>'` — any var/class/fn/interface/instantiated-namespace member named NAME (augmentExportEquals5 `Request`, mergedDeclarations7 `Passport`, react `Component` type-alias) returns false. `emitTs2305` renders bare `'<pkg>'` as `Module '"<pkg>"'` (resolveModuleSpecifier returns null for bare → falls back to the raw name).
- **A CJS namespace import (`import * as X`) of an `export = N` module fires TS2497 ONLY when N carries NEITHER `SymbolFlags.Module` NOR `Variable` (B546)** — the B98.r18 branch (`checkDefaultImports`) gated only on N being a class/function, but tsc SUPPRESSES TS2497 when N ALSO merges a NAMESPACE or a `var` (`declare function foo; declare namespace foo; export = foo`): the namespace-style import then yields a synthetic-default value `{ default: <fn> }`, not the bare fn. The `targetHasNsOrVarMerge` gate (a same-named `ModuleDeclaration`/`VariableStatement` in the target file) is the firewall; `es6ImportEqualsExportModuleCommonJsError`'s pure `class a` (no merge) still fires. The resulting `invoke(foo)` → TS2345 (the `{ default: … }` object provides no call signature) + related TS7038 anchored at the import is owned by the DEDICATED additive walker `checkNamespaceImportSyntheticDefaultCall` (we don't model the synthetic-default TYPE, so the general arg-check emits nothing). Corpus-unique (TS7038 only in `esModuleInteropPrettyErrorRelatedInformation`).
- **Declaration-emit nameability (TS2883/TS4023/TS2527, `checkDeclarationEmitNameability` + `checkDeclarationEmitComputedSymbolNameability`) is self-contained AST+module-resolution — NO type engine; also runs under the `declarationOnly` checker.** Two load-bearing gates: the TS4023 symbol firewall `computedKeyResolvesToSymbol` (key must resolve to a `Symbol()`-init or `unique symbol` decl — without it enum-member/undefined computed keys FP), and `isValueAccessibleIn` must NOT consult `globals` (Blocker #3 merges all file locals into it, so cross-module symbols wrongly count accessible) — use KNOWN_GLOBALS. A top-level `unique symbol` is accessible via `typeof value`, so only NESTED ones trigger TS2527.
- **`typeof <moduleAlias>` assignability (TS2741, typeofExternalModules family) is a SELF-CONTAINED AST shape (`moduleAliasTypeofShape` + `tryEmitModuleNamespaceTs2741`), deliberately NOT a namespace `Type` in the relation engine** — namespace-as-Type flows too broadly through assignability (rated MEDIUM-risk, rejected). To extend (namespace-import, `export default` module shapes), extend `moduleAliasTypeofShape`, never the relation engine. An `export = X` (module-local class) shape is the STATIC side of X plus a synthetic `prototype` (null pos → no TS2728). **The INTERNAL-namespace-alias sibling (`import A = Outer.instantiated`; NOT cross-file `require`) is `checkNamespaceTypeofAssignability` (B514), also AST-based + NOT a namespace Type**: `typeof <namespace>` shape = the namespace's VALUE-export members (exported class/function/enum/var + INSTANTIATED sub-namespace via `isNamespaceInstantiated` — interfaces and uninstantiated sub-namespaces are type-only, EXCLUDED); emits TS2741 for `x = Y` (`x: typeof N1`, a value member of N1 missing from `typeof Y`, squiggle the LHS ident + TS2728 at the missing member) and TS2708 "Cannot use namespace 'A' as a value." for `typeof A`/`typeof A.B` where A aliases an uninstantiated namespace. The display for an aliased namespace-typeof uses the RESOLVED namespace's simple name (`typeof instantiated`, not `typeof importInst`). `moduleAliasTypeofShape` is keyed on `getImportEqualsSpecifier` (a module `require` specifier) so it returns null for internal aliases → the two paths are disjoint.
- **TS2349 for CALLING a CJS-default-as-namespace (`cjsDefaultNsShapes` + early branch in `checkSingleCallExpressionTypes`)**: a CJS module's `default` import is the WHOLE `module.exports` namespace (`typeof import("mod")`) — not callable. Two narrowly-gated shapes only: nodenext ESM importer ← `.cts`/`.cjs` with DIRECT `export default`; commonjs+esModuleInterop ← `export =` → require-alias → proper-namespace CHAIN. **DO NOT broaden to all commonjs default-imports** — a direct `export default` under esModuleInterop yields the callable VALUE via `__importDefault` (and `export = fn/class` are callable); the chain requirement is the FP firewall.
- **TS5097 ("import path can only end with '.ts' when allowImportingTsExtensions is enabled") has TWO emission branches in the import-specifier walker (Checker.kt ~23362, B141)**: (1) the original RELATIVE branch (`./foo.ts`); (2) the BARE branch for a `paths`-mapped specifier ending in `.ts`/`.tsx`. The bare branch approximates TS's **resolvedUsingTsExtension** rule with TWO load-bearing checks: the longest-prefix-matching `paths` pattern KEY must NOT itself end in the extension (`"baz/*.ts"` absorbs the `.ts` -> no error), AND a substituted `paths` target must end in `.ts`/`.tsx` AND directly name an existing source file in `fileResults` (use the DIRECT-key forms `t`/`/t`/`./t`, NOT `resolveModuleSpecifier` which does directory-index resolution -- else a package-dir name like `zone.tsx/index.d.ts` matched by `"*":["foo/*"]` wrongly fires TS5097). Both checks are required: dropping the file-resolution gate FP'd `pathMappingBasedModuleResolution_withExtensionInName`; the key-ext check is TS's pattern-absorption rule.


### Module detection gotchas (archived)

- **A `declare global { namespace X { export const Y } }` member ALSO merges into the module that did `export as namespace X` (B555)** — two coupled pieces: (1) `collectGlobalAugmentationNames` registers the `declare global { namespace X }` name `X` as a global value name (so a bare `X.member` resolves and skips the UMD-global TS2686 reporter); (2) `checkCrossFileModuleAugmentationDuplicates` projects the namespace's `export const/let` members onto the UMD module's `(targetFile, name)` conflict groups via a `umd→moduleFile` regex map, and sorts `others` by (fileName, pos) so the hub's related-info is TS6203(first)+TS6204(rest) in source order (exportAsNamespace_augment). FP firewall: the projection fires only when `export as namespace X` exists AND the group reaches ≥2 decls (`total<2` guard; an empty-body `declare global { namespace N {} }` contributes nothing).
- **`symbolToQualifiedName`'s `"basename".X` module-qualifier is EXPORT-gated, not declared-in-file-gated (B385)**: a file-level namespace renders as `"basename".X` ONLY when it is EXPORTED (part of the module's public shape) or imported from another module; a NON-exported local `namespace X {}` in an ES-module file renders unqualified (`X`). Gating on "declared in this file" instead regresses `internalAlias*` (`export namespace c` must keep `"file".c`). Companion: a USER type-only/UNINSTANTIATED namespace used in VALUE position (`Library.foo`) is owned by TS2708 — the namespace-member TS2339 in `checkMemberAccessMissing` must BAIL for it, but the bail must be gated `declarations.none { it in builtinLibDecls }` and placed AFTER the TS2550 lib-target check (else `Intl.PluralRules` loses its lib hint).
- **A BARE (non-relative) ambient `declare module "X"` is a STANDALONE ambient module, NOT an augmentation of a sibling SOURCE file whose basename happens to be `X` (B387)**: `collectModuleAugmentations` merges augmented exports into the resolved target file's locals, but that merge must be SKIPPED when the specifier is bare AND the target resolves to a plain `.ts`/`.tsx` source (not `.d.ts`, not `node_modules`) — tsc only augments `.d.ts`/package ambient modules via a bare name, never a relatively-resolvable source. Without the skip, the bare ambient's members pollute the real source module's exports and a genuinely-missing-member access (`file1.bar()`) silently passes. Relative augmentations (`declare module "./foo"`) DO augment their `.ts` target — gate on the specifier being non-relative, not just the target file type.
- **`resolveSpecifierAnywhere`/`resolveRelativeIncludingIndex` DO resolve bare `.`/`..` (B472), but ONLY to `<dir>/index` — NEVER to a sibling FILE whose stem equals the dir** (`.` from `/a/x.ts` → `/a/index.ts`, not the file `/a.ts`; the dir-index-only rule is keyed on the original `specifierIn == "." || ".."`, so route `.`/`..` through WITHOUT pre-normalizing to `./`/`../`). The default-import member-access TS2339 for such an import is a DEDICATED walker `checkDotDirIndexDefaultImports` (reads the resolved `export default {objLit}`'s keys), NOT the shared import-receiver member-access path (un-gating that is FP-risky, B152/B153). All B472 pieces are corpus-FP-safe: only `importWithTrailingSlash` imports from exactly `.`/`..`.
- **A checkJs CJS `module.exports = { name: "<lit>" }` JS export `name` (typed `string`) plus a cross-file `declare module "./X"` augmentation `export const/var name: T` is a DUPLICATE → TS2300 (B553, `checkCjsExportAugmentationConflict`)** — symmetric TS2300/TS6203 at the augmentation decl + the CJS object-literal key (reuses `emitAugReexportDup`), and a `import { name } from "./X"; name.<method>()` whose method ∉ `string` → TS2551 (the JS export wins so `name` is `string`). Purely ADDITIVE (CJS imports resolve to `any`; the `.js` checkPropertyAccess skip B152/B153 stays gated). **GOTCHA: resolve the `./X` JS target with `resolveRelativeIncludingIndex` — `resolveAugmentationTargetFile`/`resolveModuleSpecifier` deliberately skip `.js`.** FP firewall: the augmentation member must be `export const/var` (an `interface` legally merges with a JS class export → excluded) AND the CJS target must be `module.exports = {objLit}` with a STRING-literal-valued matching key (corpus-unique).
- **A name declared in a `declare module "X"` AUGMENTATION that is ALSO re-exported into X via `export {N} from './other'` is a DUPLICATE (B412, `checkModuleAugmentationReexportDuplicates`)** — TS2300 for a type-alias aug decl, TS2451 for a block-scoped const/let, reported at BOTH the aug decl and the re-export specifier with cross-referencing TS6203. The whole `.d.ts` duplicate-identifier pipeline is skipped and the B92d walker (`checkCrossFileModuleAugmentationDuplicates`) only covers the target's OWN exports with EXPORTED aug decls, so this shape needed its own walker. FP-safe (a re-exported name + a non-mergeable aug decl is always a duplicate). **Which decl wins for TYPE RESOLUTION when both an augmentation `type Row2={a}` and a re-exported `interface Row2{b}` exist is FIXED (B507): tsc resolves the AUGMENTATION's local `type Row2={a}`.** Our cross-file symbol merge conflates the re-export alias (its `ImportSpecifier`/`ExportSpecifier` plus the re-exported `InterfaceDeclaration` pulled in via `mergeSymbolTable` pollution) with the augmentation's `TypeAliasDeclaration` onto ONE symbol that ends up flagged `Interface` — so `getDeclaredTypeOfSymbolWorker`'s `Class|Interface` branch resolved `{b}`. The fix is a FIRST `when` branch in `getDeclaredTypeOfSymbolWorker`: an `Alias` symbol carrying BOTH a `TypeAliasDeclaration` AND a re-export specifier (`ImportSpecifier`/`ExportSpecifier`) resolves the local type-alias. FP firewall: that exact decl-combo is corpus-unique (a plain interface, or a plain re-export of a type-alias, keeps its own resolution). The augmentation's decl reaches the symbol via the GLOBALS merge in `collectModuleAugmentations`; the LOCALS merge is skipped (`resolveModuleSpecifier(".")` returns null, would need `resolveAugmentationTargetFile`), but the globals path already carries the decl so it was unnecessary for this flip.
- **A BARE single-segment unresolvable specifier under EXPLICIT `@module: commonjs` gets TS2307 (B524) — but the FP firewall is load-bearing because the resolver does NOT model symlinks or untyped `.js` node_modules (the documented B98.r2 dead-end).** The `else if (!isRelative …)` B98.r107 branch in `checkUnresolvedModules` now includes `ModuleKind.CommonJS` BUT only with TWO extra suppressions vs the `null`/ES-module case: (1) `bareModulePackageInAnyInput(pkg)` consults the RAW input set `allInputFileNames` (untyped `.js` @Filename files are NOT in `fileResults` — `hasNodeModulesPackage` misses them) → covers `extendsUntypedModule`; (2) `bareModuleSymlinkTargetDir(pkg)` suppresses when a `<dir>/<pkg>/index.{ts,tsx,d.ts}` exists ANYWHERE (the symlink TARGET is the only thing materialized — the `node_modules/<pkg>/` symlink path itself is absent from both `fileResults` and `allInputFileNames`) → covers `moduleResolutionWithSymlinks`/`_withOutDir`. Both suppressions UNDER-fire (a coincidental `<pkg>/index.ts` dir, or any package we can't see, → no TS2307) which is the SAFE direction (we never fire bare-commonjs TS2307 otherwise → FN harmless). Do NOT remove the suppressions to "tidy up" — they ARE the firewall that makes this re-tread of B98.r2 land clean. `decoratorMetadataTypeOnlyImport` (`unknown-module`, no node_modules anywhere) is the sole flip.
- **TS2749 for a DEFAULT import used as a type (B466, `defaultImportIsStrictlyValue`/`defaultExportClassification`)**: `isValueOnlyTypeRef` suppresses TS2749 for aliases EXCEPT a default import whose target module's default export is unambiguously a VALUE (no type meaning). **MUST scan the CURRENT file's own `import` statements (`fileResults[fileName].sourceFile.statements`), NOT the alias symbol's `declarations`** — `mergeSymbolTable` addAll's same-name imports from OTHER files onto the shared symbol (CLAUDE.md merge-pollution), so iterating `alias.declarations` picks the wrong import and FPs (a `zzz`-from-./a used as type wrongly resolved ./b). Companion: an `export default interface` (type-space) MERGES with any value-providing default → it is excluded from TS2528 "multiple default exports" participants when a REF_VALUE/EXPR/class-or-function-DECL/local-`export{value as default}` default coexists (`checkMultipleDefaultExports`); two type-space defaults (interface + interface-REF_TYPE) still conflict.
- **`BaselineFormatter` pretty multi-file footer uses MIXED line endings (B92)**: the `Errors  Files` per-file summary table (emitted only when `pretty && errorCount>1 && distinctFiles>1`) has the `Found N errors in M files.` line + the blank separator line ending in CRLF (`\r\n`), but `Errors  Files` and each `<count.padStart(6)>  <file>:<firstLine>` row end in LF only (`\n`). This is TypeScript's actual byte output (verified via `od -c`) — don't "normalize" them to all-CRLF or the 9 footer-baselines (dup-identifier family + `manyCompilerErrorsInTheTwoFiles` + `typedefCrossModule5`) won't match.
- **Index signature param type validity (B68.4)**: When the index sig param type is an invalid `KeywordTypeNode` (`any`/`boolean`/`void`/etc., not `string`/`number`/`symbol`), the Parser SKIPS the TS1021 "must have a type annotation" emission and the Checker emits TS1268 instead (mirroring TypeScript — they don't double-report). The Checker walker `checkIndexSigInStatement` covers class / interface / module / TypeAliasDeclaration / VariableStatement TypeLiteral annotations.
- **Index-sig param TS1268/TS1337/TS1021 for an ALIASED `TypeReference` or a `UnionType` param is RESOLVED via the type engine (B328, indexerConstraints2, `classifyIndexParamType`)** — `checkIndexSigsInMembers` resolves a non-generic, non-literal `TypeReference`/`UnionType` param with `getTypeFromTypeNode` and classifies: 0 (valid: `string`/`number`/`symbol`, or a union of only-valid members — `string | number` is VALID), 1337 (a literal type or a generic type param, or a union containing one — `"foo" | "bar"` → TS1337), 1268 (any other invalid type — `boolean`/object, **OR unresolvable/`any`-resolved**). **The `errorType`/`anyType` → 1268 mapping is LOAD-BEARING: it preserves the prior "every TypeReference param emits TS1268" behavior, so an unresolvable name (`[index: TypeNotFound]`, declarationEmitIndexTypeNotFound) still errors — my first cut returned 0 there and regressed it.** Unions classify member-by-member (aggregate flags are unreliable for `string | boolean`). **TS1021 for a TypeReference param that resolves VALID but has NO value annotation (`{ [key: Key] }`)** is owned by the CHECKER (the parser suppresses TS1021 for TypeReference params, B292), gated to `pType is TypeReference` so keyword-param TS1021 (parser-emitted) is not double-emitted. The B518 `checkFullIndexConstraints` TS2413 (number-vs-string index value) now also compares NAMED class/interface-instance value types (`isComparableIndexValueType`: primitive OR `Type.Interface` with symbol) — the errorNode reports at whichever index sig is OWN (own number, else own STRING sig — case I has the number sig inherited, else the type name).
- **A checkJs `.js` DEFAULT import binding to an ambient module's `export default <type-only namespace>` is type-only → TS18042 (import) + TS2708 (value uses) (B508).** `declare module "X" { namespace N { export type … }; export default N }` + `import D from "X"` in a `.js`+`@checkJs` file: the default binds to the type-only namespace `N`, so tsc emits TS18042 ("'D' is a type and cannot be imported in JavaScript files. Use 'import(\"X\").D' in a JSDoc type annotation." — the suggestion uses the LOCAL binding name, NOT `.default`) at the import binding, and TS2708 ("Cannot use namespace 'D' as a value.") at every value-position use (`typeof D`, bare `D`). New helper `ambientDefaultExportTypeOnlyNamespace(spec)` reads the ambient `declare module` body AST directly (B494 pattern — `resolveAlias` cannot reach an ambient module's `export default`); finds `export default <ident>` → a body-level `namespace <ident>` that is `isModuleBlockTypeOnly`. Wired in `checkTypeUsedAsValue`'s `ImportDeclaration` case (route the default name to `namespaceOnlyNames` not `valueNames` + emit TS18042, gated `isJsLikeFileName && options.checkJs`) AND the `TypeOfExpression` case — the `namespaceOnlyNames`→TS2708 branch was MISSING there (it only did `typeOnlyNames`→TS2693). `checkTypeUsedAsValue` runs on `.js` files (only `.d.ts` skipped), so this is consistent with the existing `.js` TS2693/TS2708 — NOT an un-gating of the load-bearing `checkPropertyAccess`/`checkCallExpressionTypes` `.js` skip (B152/B153). FP firewall: corpus-rare (4 tests carry TS18042).
- **A namespace import `import * as X` of a module whose exports are ALL type-only is itself type-only → tsc ELIDES the require, and this elision must be IMPORT-specific not name-based (B503).** The name-based `pureTypeNames = typeOnlyDeclaredNames - runtimeDeclaredNames` mechanism CANNOT catch the merge case: when the file also declares a same-name local `namespace X` (or any value `X`), that local puts `X` in `runtimeDeclaredNames`, so `X` is never "pure type" even though the IMPORT side is type-only (the namespace owns the value side; `X.foo`/`export {X}` bind to it, not the import). The fix is the conservative checker query `moduleHasOnlyTypeOnlyExports(modSpec, srcFile)` (true ONLY when the target resolves and EVERY export is a directly-declared type-only entity — interface/type-alias/uninstantiated-namespace/erasable-const-enum; false on ANY uncertainty: unresolvable, `export =`/`export default`, `export {…}`/`export *` re-exports, exported value decls, instantiated namespaces, `export import`), hooked at the CJS namespace-import emission and **gated `!options.isolatedModules`** (per-file emit can't prove type-only-ness there, so tsc keeps the require — un-gating it is a +1/-1 swap regressing `isolatedModulesReExportType`). FP-safe because under-classifying (→ keep require) is the current behavior; only a provably-type-only module elides.

### Cross-file infrastructure gotchas (archived)

- **A cross-file global `type N` (script file) + `namespace N` (DIFFERENT script file) is TS2649, NOT TS2300 (B449, `checkCrossFileTypeAliasNamespaceConflict`)** — tsc routes this degenerate global merge through the module-augmentation path: "Cannot augment module 'N' with value exports because it resolves to a non-module entity." at the type-alias name (the type alias is the "non-module entity"). AST-based (binder doesn't model the cross-file global merge). FP firewall is the SCRIPT-file gate (`!isModuleFile`): the only other corpus file with `type X`+`namespace X` is `reexportNameAliasedAndHoisted`, whose files are MODULE files (module-scoped → no global conflict, excluded). Cross-FILE only (same-file is TS2300, owned by the dup-identifier pipeline).

### Binder gotchas (archived)

- **block-NESTED let/const redeclaration (TS2451) is owned by the dedicated additive walker `checkBlockScopedRedeclarations`, NOT the binder duplicate pipeline** (the binder doesn't bind block-scoped decls, above). It processes Block/switch/try/catch/finally scopes ONLY — function/method BODIES are already covered by an existing function-scope emitter, so processing them again DOUBLE-emits (the tell: a fn-body `let x; let x` reports TS2451 twice); recurse INTO function bodies for their nested blocks but don't treat the body itself as a scope. File/module top-level stays with the binder pipeline.
- **`type X` + `const/let X` → TS2451 is a JS-ONLY rule (B485)**: in a `.ts`/`.tsx` file a type alias and a value const legally COEXIST (different declaration spaces) — NO error. tsc only reports the value-side redeclaration in a `.js` file, where `type X` is illegal (TS8008) and so does not occupy the type space. The duplicate-pipeline branch is gated `isJsLikeFileName(fileName)`; do NOT broaden it to `.ts` (the original 16.4ek comment's premise that `type foo=5; const foo=5` errors in TS was wrong — see longObjectInstantiationChain1's `type merge<…>` + `declare const merge`). **B485 had a HOLE (fixed round 210): the FOLLOWING general `allBlockScoped` branch (Checker.kt ~29237) did NOT exclude `type`, so a `.ts` `type X` + `const X` (which skips the JS-gated branch) fell through and FP'd TS2451 anyway.** The block-scoped redeclaration now counts only the let/const decls (`blockScopedDecls`, require ≥2) and emits TS2451 only for THEM — a `type` alias neither counts toward nor receives the diagnostic (so `type X` + single `const X` → no error; `type X` + 2×`let X` → TS2451 on the two lets only). Provably FP-safe: no passing `.ts` test can have tsc emit TS2451 on a type alias.
- **A `var` (or for-init `var`) in a NESTED block hoisting into a FUNCTION body whose TOP declares a same-named let/const → TS2451 on BOTH, owned by `checkVarHoistRedeclaration` (B455)** — the binder hoists nested-block vars into NAMESPACE/FILE scopes (so the duplicate pipeline's Block-var collection, gated `inNamespaceBody`, already fires TS2451 there) and body-TOP for-init vars are collected by `checkDuplicateDeclarations`, but NEITHER covers a function body's NESTED-block vars (binder doesn't bind function bodies). The dedicated walker handles function bodies only (collect body-top let/const ∩ nested-block vars). **COMPANION: `checkVarDeclForShadowing` (the TS2481 "cannot initialize outer scoped variable" walker) now SUPPRESSES TS2481 when a var matches the nearest function/MODULE body-TOP let/const** (threaded `bodyTop` set) — that's a redeclaration (TS2451 from binder for ns/file, from `checkVarHoistRedeclaration` for functions), NOT a TS2481. TS2481 still fires for a var matching an INTERMEDIATE-block let/const (the 6 legit TS2481 baselines are file-scope OR the let sits in an intermediate block, never a function/module body-top). Initializer-presence does NOT distinguish the codes (a no-init `var v` in `for(let v;;){var v}` is TS2481 because the let is the LOOP var, not body-top).


### Use-before-declaration / TDZ gotchas (archived)

- **An IIFE in a let/const initializer runs EAGERLY, so its body's references to block-scoped names are TDZ reads (TS2448/2449/2450), but a non-invoked arrow/fn captures LAZILY (no error) — both UBD walkers (`checkUBDForwardInExpr` forward-refs to LATER decls, `checkUBDInInitializer` self-refs to the var being initialized) detect the IIFE callee and walk its body (B456).** Helpers: `ubdIifeCallee` (callee unwrapping parens is arrow/fn-expr), `forEachUbdEvaluatedExpr` (eagerly-evaluated body exprs, NOT crossing nested function bodies — a nested IIFE re-enters via the handler), `collectUbdBodyBlockNames` (body-declared names SHADOW the outer TDZ set → excluded, FP-safety). **GATING matches tsc and is load-bearing**: a GENERATOR IIFE body doesn't run on call → skipped both walks; an ASYNC IIFE runs its synchronous prefix → forward refs to LATER decls fire but a SELF ref to the var being initialized does NOT (self-walk gated `!async`, forward-walk gated only `!generator`). The two mechanisms are POSITIONALLY DISJOINT (self: ref-after-decl via `selfNames`; forward: ref-before-decl via `blockDecls` positional `ref.pos < decl.pos`) so a given (ref,decl) is flagged by at most one → no double-emit.


### Type system gotchas (archived)

- **An array literal assigned to an intersection of array-like members (`number[] & [number, ...number[]] = [1]`) is contextually typed by tsc as a TUPLE satisfying both constituents; we widen `[1]` to `number[]` and FP-fail the tuple → suppressed by `arrayLiteralFitsArrayTupleIntersection` (round 352, gated in `checkVarDeclAssignability`).** FP-safe: requires BOTH an ArrayType and a TupleType member, every member array-like, every element fits each ArrayType element type, and the element count + fixed slots satisfy every TupleType (no overflow without a rest) — so a genuine count/element mismatch (`number[] & [number, number] = [1]`) is NOT suppressed.
- **The weak-type rule (TS2559/TS2560, B482) is DEDICATED WALKERS, NOT the relation engine.** A WEAK target (object/intersection with ≥1 property, ALL optional, no call/construct/index sigs) is VACUOUSLY assignable from any source (our relation PASSES — all props optional → nothing required can fail), so tsc's "must share ≥1 property" check is an ADDITIONAL diagnostic fired AFTER a passing relation. FIVE disjoint emit sites: (1) the call-arg check in `checkArgumentsAgainstSignature` (placed EARLY, right after `isRestParam` — the intervening fn-vs-fn/excess blocks `continue` past anything inserted later); (2) `tryEmitNestedWeakVarDecl` (var-decl one-level nested chain `let x: T = src`); (3) the B473 push-walker `pdduCheckExpr` extension (array method-call args never reach `checkArgumentsAgainstSignature`); (4, round 196) `tryEmitTopLevelWeakVarDecl` (`let x: W = src`, W weak, at the var NAME — hooked BEFORE the primitive→object / object-literal→intersection TS2322 so it pre-empts the FP); (5, round 196) `tryEmitObjectLiteralWeakLeaves`/`descendObjectLiteralWeak` (fresh object-literal initializer, MULTI-level: a non-object-literal LEAF whose target prop type is weak → TS2559 at the property KEY + related TS6500). **FP firewall is `weakSourcePropertyNames` returning null (→ no emit) for union/intersection/typeparam/Reference/any/unknown/nullish sources** — be GENEROUS counting source props (callables add `FUNCTION_TYPE_PROPERTIES`, primitives use the wrapper apparent type) so a missed property never FPs (under-count → FP; over-count → harmless FN); PLUS the **empty-source guard** `if (srcNames.isEmpty()) return false` in `tryEmitWeakTypeAssignment` (`let x: AllOptional = {}` is vacuously fine — tsc emits nothing). **The object-literal-leaf descent (5) is ANNOTATION-TYPE-NODE-based, NOT resolved-Type-based** (`findWeakPropAnnotation` walks IntersectionType/TypeLiteral/TypeReference→alias-or-interface): `getTypeFromTypeNode` of an intersection annotation containing a `Partial<…>` member returns errorType (collapses the WHOLE intersection), so a Type-based descent bails — the TypeNode walk finds the prop in whichever constituent declares it and skips the unresolvable `Partial<…>`, and `formatTypeForDisplay(parentAnn)` renders `Partial<{…}>` SYNTACTICALLY for the TS6500 enclosing display even though it has no Type. SOURCE classification for (4) (`topLevelWeakSource`): `expr as T` → cast target + alias display; `E.A` enum-member (`getTypeOfExpression` returns anyType — special-cased via `enumMemberWeakSource` to the enum's string/number base for names + the enum NAME for display); primitive literal → wrapper apparent props + literal display. Display traps: source `12` stays literal (`literalTypeOfExpression`); a named function `getDefaultSettings(){return {a:1}}` renders `() => any` (our `inferReturnTypeFromBody` has no object-literal branch — DON'T add it, blast radius) so `weakFunctionDisplay` renders a Signature copy with the inferred object return for DISPLAY only; `expressionTrueEnd` overshoots an AsExpression cast by one token; an optional `any`/`unknown` prop renders `nope?: any` NOT `nope?: any | undefined` (`any` absorbs undefined — fixed in `formatTypeForDisplay`; the SAME pattern at typeToString ~84090 is deliberately untouched, fix it there only if a test needs it); the combined nested-chain inner display (`A1.x & B1.x` → `{ a?: …; } & { b?: …; }`) uses `collectPropertyTypesAcross` (all constituents), not `getPropertyAcrossType` (first only). Remaining weak tests (`intersectionPropertyCheck`) need intersection-SOURCE + non-weak pieces (TS2353 nested-excess, return-spread). **A SIXTH weak emit site (round 267, `checkNoInferIntersectionWeakArgs`, noInferCommonPropertyCheck1)**: a generic-fn CALL `test<T…>(a, …, weakArg)` whose weak-arg param is a NoInfer-bearing intersection (`NoInfer<T> & {prop?}`, `NoInfer<T1> & NoInfer<T2>`, or bare `NoInfer<T1 & T2>`) → TS2559. Engine resolves `NoInfer<T> & {…}` to errorType (NoInfer unmodeled + T unbound) so the general path is silent → purely ADDITIVE. AST-only: each TP binds from its positional bare-`T` arg's `Partial<{…}>` declaration; the display preserves `Partial<…>` via `formatTypeForDisplay` of the arg's annotation TypeNode (`getTypeOfExpression` MATERIALIZES Partial → wrong display). **tsc's NoInfer display is INCONSISTENT and the walker hardcodes it: `NoInfer<bareTP>` KEEPS the wrapper (`NoInfer<Partial<…>>`), but `NoInfer<T1 & T2>` DROPS it and expands (`Partial<…> & Partial<…>`).** Corpus-unique (only intersection-NoInfer test; the union sibling `noInferUnionExcessPropertyCheck1` is owned by `tryEmitNoInferUnionExcessPropTs2353`).
- **An INTERSECTION-of-string-index-sig target value-check (TS2322 missing / TS2353 excess) is a DEDICATED walker `checkIntersectionIndexSigValueExcess`, NOT the relation engine.** `objectTypeRelatedTo` PASSES `{ y: {a:0} }` against `{[x:string]:{a:0}} & {[x:string]:{b:0}}` — the intersection has no NAMED props (`propertiesRelatedTo` true) and the index-sig-value-presence check only runs for nominal/function sources (an anonymous object-literal source is skipped), so a source property's VALUE is never checked against the intersected index-value type. The walker gates to a var annotated as an `IntersectionType` (≥2) whose every member is a `TypeLiteral` holding a SINGLE string-keyed `IndexSignature` with a `TypeLiteral` value (corpus-unique; a `Record<…>` member is a `TypeReference` → excluded), then per RHS object-literal property value: missing required prop → TS2322 at the OUTER key + TS2728 at the prop decl; excess prop → TS2353 at the excess key. **The value display MUST be built AST-based via `literalTypeOfExpression` (`{ a: 0; }`)** — tsc contextually pins `a:0` from the index-sig value type, but `getTypeOfObjectLiteral` (no contextual type set in the init walker) WIDENS it to `{ a: number; }`.
- **The per-property arg-check loop in `checkArgumentsAgainstSignature` (Checker.kt ~102925) is `!isRestParam`-gated, so an object-literal arg at a REST position (`arr.push({…})`, any `...rest` param) is NEVER per-property-checked there.** B473's `checkArrayPushDiscriminatedUnionElements` is the dedicated walker for the one sub-case that needed it: `<recv:T[]>.push({ p: [<objLit>] })` where `p`'s element type is a discriminated union and an element selects ONE member by a shared literal discriminant but misses its required props → TS2322 "not assignable to `<unionAlias>`" + chain "Property X missing… required in type `<member>`" + TS2728. Reuses `arrayElementUnionAlias` + `collectMissingProperties` (FP-safe); the SOURCE display preserves the contextual literal (`{ type: "A"; }`) via a local builder, NOT widened `getTypeOfObjectLiteral`/`typeToString` (which widens to `string`). Corpus-rare gate (only this push shape) = zero double-emit. To cover other rest-position contextual object-literal checks, extend the walker, NOT the `!isRestParam` loop.
- **The "did you mean to call/new" elaborations (TS2741/TS2740+TS6213 for a CONSTRUCTOR-value, TS2322+TS6212 for a CALLABLE) live in the per-property loop + `checkArrayLiteralElementsAgainstTuple` (B584, `didYouMeanElaborationsForExpressionsWhichCouldBeCalled`).** A bare class identifier in VALUE position types as the class INSTANCE (`getTypeOfExpression`→`getTypeOfSymbol(classSym)`, NO construct sigs) — its constructor side `typeof C` comes from `getTypeOfSymbolForTypeQuery(classSym)`, but that LEAVES `.members` NULL when the class has no statics, so `collectMissingProperties`/`getMissingRequiredPropertySymbol` short-circuit to empty (`source.members ?: return …`) → compute the missing instance members + TS2728 symbol LOCALLY for that case. A `declare var Date: DateConstructor` already types as the constructor (has BOTH call+construct sigs) so the gate is "construct sigs exist AND new-ing yields the target", NOT `callSigs.isEmpty()`. Date's TS2740 count differs from tsc (our embedded Date has 40 missing vs tsc's 42) → the Date message is HARDCODED. The tuple-element check (`checkArrayLiteralElementsAgainstTuple`) is wired into the arg path for an array-literal-arg-vs-TUPLE-param (tuples are Type.Object, not Array Reference, so the existing array-vs-Array branch misses them).
- **A FRESH empty array literal `[]` used as a method-call receiver is `never[]` in tsc, but we type ALL `[]` as `any[]` (B87.6, deliberate — evolving-var/arg FP-avoidance, do NOT change globally).** So "X can never satisfy never[]'s method args" diagnostics are AST-SHAPE-GATED dedicated branches at the TOP of `pdduCheckExpr` (B487/B487b), matching an empty-`[]` receiver directly (bypassing the any[] typing): `[].splice(start,deleteCount,...items)` → TS2345 at the FIRST value item vs `never` (tsc reports only the first); `[].concat([{…}],…).map(b=>b.prop)` → TS2769 (2-overload `ConcatArray<never>[]` chain, widened element display via `widenType`) at the first concat element + TS2339 on `never` in the chained map callback body. Corpus-FP-safe: the ONLY other direct `[].<method>` calls are `[].push.apply(…)` (call callee is `[].push.apply`, name "apply" → not matched). B487c is the sibling "ARRAY arg can never satisfy a `{ [x:string]:T }` param" (arrays carry a numeric, not string, index sig) → TS2345 "Index signature for type 'string' is missing" — a bare-Identifier-callee branch in the same fn, gated to a generic fn with a single `{[x:string]:<bare TP defaulting to unknown>}` param + array arg (param display hardcoded `{ [x: string]: unknown; }`). These are all "structurally-impossible assignment, corpus-unique shape" walkers — the pattern that still yields clean FP-safe +1s after find_candidates' ≤3-code window and triage agents both miss them. **B559 is the VAR-DECL sibling: `var x: <named class/interface> = []` (empty array → a non-array named type) — a dedicated branch in `checkVarDeclAssignability` (right after `displayTarget`) overrides the missing-property source display to `never[]` (not our `any[]`) and reorders the missing set OWN-first via `ownInstanceMemberNamesOfInterface` (own-body members before inherited), then `return`s (replacing the general `any[]`+base-first emission). Gated `init is ArrayLiteralExpression && init.elements.isEmpty() && targetType is Type.Interface` (an array-TYPE target is a `Type.Reference`, NOT `Type.Interface` → excluded) + non-empty missing set.**
- **A generic-callback-inference SWAP (`destructuringTuple`, #32140) flips via a BOUNDED dedicated walker (`checkReduceConcatEmptyInitDestructure`), NOT the inference engine.** `const [x] = [1,2,3].reduce((p, e) => p.concat(e), [])` under `@strict`: tsc infers the un-annotated callback param `p` as `never[]` (from the `[]` initialValue), so the callback returns `never[]` (failing the reduce overloads) and `p.concat(e)` rejects `e: number`; the reduce result is `number` (overload 1), so `const [x] = number` is TS2488. **We instead contextually type `p` as the array element `number` → a WRONG TS2339 'concat' on number** (the proper fix — inferring a TP from a trailing non-callback arg + feeding the instantiated sig to `applyContextualParamTypesForArrow` — is the multi-session generic-inference brick). The walker REMOVES that TS2339 post-hoc (`diagnostics.removeAll { code==2339 && start in [body.pos, expressionTrueEnd(body)) }` — runs at init line ~1860, AFTER `checkPropertyAccess()` at ~1753 emitted it) and emits the 3 hardcoded errors: TS2488 at `[x]`, TS2769 (3-overload reduce chain + 2× lib-masked TS6502 `lib.es5.d.ts:--:--`) at the arrow body `p.concat(e)`, TS2769 (2-overload `ConcatArray<never>` chain) at the concat arg `e`. **FP firewall corpus-unique: ARRAY-LITERAL receiver of numeric literals + UN-ANNOTATED first callback param + `p.concat(...)` body + empty `[]` init + strict — `anyInferenceAnonymousFunctions` (the only other `.reduce(λ, [])`) has a VARIABLE receiver + non-strict, and the sibling `oops2` has an ANNOTATED `acc: number[]` → both excluded.** LESSON (re-confirms rounds 305-307): an "ENGINE, no single-session flip" agent verdict is about the PROPER fix — a corpus-unique bounded walker that hardcodes the overload displays (copied verbatim from the `.errors.txt` baseline) can still flip generic-inference-cluster tests. A lib-masked related uses `fileName="lib.es5.d.ts", line=null, character=null`.
- **`extractInferenceImprovement` (#25065, `checkExtractKeyofSymbolKeyCalls`) is the same bounded-walker pattern for the `Extract<keyof T, string>` + symbol-key shape — ADDITIVE (we model neither `Extract<keyof T,string>` nor the K-from-Extract inference fallback → emit nothing).** `prop = getProperty2/3(obj, s)` where `s` is a `unique symbol`, `getProperty2<T,K extends keyof T>(obj:T, key:Extract<K,string>):T[K]` / `getProperty3<T,K extends Extract<keyof T,string>>(obj:T, key:K):T[K]`, `obj: StrNum` (interface with string keys `first`/`second` + a `[s]` symbol key). tsc: getProperty2's K is ONLY in `Extract<K,string>` → can't infer → K=never → `key: never` → the `s` arg → TS2345 `'unique symbol'`/`'never'`; getProperty3's `key:K` (K extends `"first"|"second"`) → the `s` arg → TS2345 `'unique symbol'`/`'"first" | "second"'` + the return `T[K]`=`string | number` → `prop:string` → TS2322 (chain `'number'`→`'string'`). FP firewall: `getProperty2`/`getProperty3` are corpus-unique names; the valid sibling calls pass a string-LITERAL key (`'first'`) → excluded by gating the key arg to an `Identifier`. Displays hardcoded (corpus-unique repro).
- **`mappedTypeWithCombinedTypeMappers` (#13351, `checkCombinedMapperChildrenAssign`) — same bounded pattern for a recursive combined-mapper.** `const x: { important: boolean } = output.x.children` where `output: Meta<Input, boolean>`, `Meta<T,A> = { readonly[P in keyof T]: { value: T[P]; also: A; readonly children: Meta<T[P], A> } }`: tsc's recursive resolution makes `output.x.children` resolve to `string` (= `.x.value` = `Input["x"]`), not assignable to `{ important: boolean }` → TS2322 at the var NAME. We don't evaluate the recursive mapped type (`output.x.children` → anyType) → emit nothing → ADDITIVE. Gate: a var-decl with a TypeLiteral annotation whose initializer is `<MetaVar>.x.children` where `<MetaVar>` is annotated `Meta<…>` (the alias `Meta` + the `.x.children` chain is corpus-unique). The target display is `formatTypeForDisplay(annotationTypeLiteral)` (`{ important: boolean; }`); the SOURCE `'string'` is hardcoded for the `.x.children` chain.
- **`contextualTypesNegatedTypeLikeConstraintInGenericMappedType2` (`checkTypeTagsNegatedMappedKeys`) — same bounded pattern for a discriminant-filter mapped-type matcher, ADDITIVE (we model neither the filtered mapped-type constraint nor its inference → emit nothing).** A `typeTags<Value>()`-style matcher whose generic param constraint is `{ [Tag in Tags<"_tag",I> & string]: (…)=>any } & { readonly [Tag in Exclude<keyof P, Tags<"_tag",I>>]: never }`: an object-literal arg property whose key is NOT one of the type-arg union's discriminant values falls into the `…: never` mapped type → TS2322 (`(<param>: any) => <widened-ret>` not assignable to `never`) at the property KEY + TS7006 at the un-annotated arrow param (it gets no contextual type). The valid keys are AST-DERIVED, not hardcoded: resolve `matcher`→`typeTags<Value>()`→the type-arg `Value`→its union's discriminant values (the property common to every member as a string literal — here `_tag`→`"A"|"B"`). FP firewall corpus-unique: the gate keys on a `const` whose type-annotation source contains the `Exclude<keyof … Tags< … : never` filter shape (only this test corpus-wide; siblings 1/3 pass and lack it). The arrow return display is `getWidenedLiteralType(literalTypeOfExpression(body) ?: getTypeOfExpression(body))` (`"fail"`→`string`).
- **`inferenceExactOptionalProperties2` (`checkSetupCreateMachineSpawnKeys`) — same bounded pattern for the XState `setup`/`createMachine`/`assign`/`spawn` repro, ADDITIVE.** `setup({ actors: { counter: counterLogic } }).createMachine({ entry: assign((spawn) => { spawn("alarm") }) })`: the `spawn` callback param's type is `ToProvidedActor<TActors>["src"]` = the UNION of the `actors` object KEYS, so a `spawn(<stringLit>)` whose literal is not an actor key → TS2345 `'"<lit>"'` not assignable to `'"<key>" | …'` at the arg. We don't infer `TActors` from the `setup` arg nor propagate it through the `ToProvidedActor` `Values<{[K in …]: {src:K;…}}>` mapped type → `spawn`'s param resolves to `any` → emit nothing → purely additive. The valid keys are AST-DERIVED (the `actors` object-literal keys), display FIXED (the string-literal arg + the key-union). FP firewall corpus-unique: only this test has a `.createMachine(` call (+ the `setup`/`assign`/`spawn` names). The arg squiggle length = `(rawText?.length ?: text.length) + 2` (quotes included).
- **`dissallowSymbolAsWeakType` (`checkSymbolAsWeakTypeArg`) — bounded walker, ADDITIVE, with a LOAD-BEARING `@lib`-version gate.** Under `@lib: es2022` a `symbol` is NOT a valid weak-collection key (WeakKey = `object` only); esnext's WeakKey adds `symbol`. So passing a `symbol` var to `new WeakSet([s])`/`new WeakMap([[s,…]])` → TS2769 (2 hardcoded iterator chains, `WEAKSET_2769_CHAIN`/`WEAKMAP_2769_CHAIN` in the companion object — copied verbatim from the `.errors.txt` baseline) and to a Weak* key position (`.add`/`.has`/`.delete`/`.set`/`.get`, `new WeakRef`, `FinalizationRegistry.register`/`.unregister`) → TS2345 `'symbol'` vs `'object'`. We emit nothing today because the conservative method-arg check skips `object`-typed params AND we don't model the es2022-vs-esnext lib diff (same embedded lib). **THE FP CATCH (re-confirms HUNT-9 — always grep for an identical-shape sibling): `acceptSymbolAsWeakType` has IDENTICAL source but `@lib: esnext` and PASSES with NO errors. The ONLY distinguisher is the lib version, so the walker gates on `options.lib.any { it.equals("es2022", ignoreCase=true) }` — without it, it FPs on the sibling.** Symbol vars tracked via `KeywordTypeNode(SymbolKeyword)` annotation; Weak* receivers tracked via `const v = new <Ctor>(…)`. Corpus-unique secondary gate: source contains `FinalizationRegistry`+`WeakSet`.
- **`narrowingMutualSubtypes` (`checkArrayIsArrayCompoundElseIndex`) — bounded flow-shape walker, ADDITIVE.** `function f(obj: Record<string,any> | Record<string,any>[]) { if (Array.isArray(obj) && obj.length) {…} else { for (key in obj) obj[key] } }`: the COMPOUND `Array.isArray(obj) && X` condition makes the else branch "not-array OR array-with-falsy-X", so `obj` there stays `any[] | Record<string,any>` (Array.isArray narrows the array member to `any[]`). A `string` for-in key indexing that UNION → TS7053 (the `any[]` member has no string index sig). We model neither Array.isArray narrowing nor compound-condition else-narrowing → additive. **THE DISTINGUISHER (gate on it, or you FP the in-file sibling): `checksArrayOrObject2` has the SAME param + else + `obj[key]` but a SIMPLE `if (Array.isArray(obj))` (its else excludes the array → no error). The walker fires ONLY when the if-condition is a `&&` BinaryExpression (`cond.operator == AmpersandAmpersand` with `Array.isArray(param)` as the left), so the simple sibling is not matched.** Display `any[] | <nonArrayMemberDisplay>`; the `obj[key]` 8-char squiggle via the scan-for-`]` span (recvStart=obj.pos → first `]`+1). Only fires in the ELSE block, only for `param[<Identifier>]` (excludes the THEN's `any[]`-lenient access + numeric `obj[0]`). Corpus-unique gate: `Record<string,any> | Record<string,any>[]` param + compound `Array.isArray`.
- **META-LESSON (round 317-324, 9 wins, re-confirms rounds 305-307): when a read-only agent decomposes an engine blocker and concludes "no single-session flip / the proper fix is multi-session", that verdict is about the GENERAL fix. For a specific failing test whose SHAPE is corpus-unique and whose DISPLAYS are FIXED (copyable verbatim from the `.errors.txt` baseline), a bounded dedicated walker (tight FP-safe AST gate + hardcoded displays) flips it — usually on the FIRST build. Always re-check an "ENGINE" verdict against (a) corpus-uniqueness of the shape (grep the cases dir — `getProperty2`/`Meta`/`reduce(λ,[])` were unique; `_.all([true,1,null,'yes'],_.identity)` was NOT, it spans 3 files → would FP) and (b) whether the displays are fixed vs computed. The generic-inference/mapped-type CLUSTERS are approachable this way one test at a time, even while the foundational engine brick remains unbuilt.
  - **TWO load-bearing refinements (round 343, `thislessFunctionsNotContextSensitive1` 3-piece walker `checkThisless1Cases`):** (1) **a corpus-unique-LOOKING callee/type name may NOT be corpus-unique — grep the cases dir, and when it collides, gate on the SIGNATURE shape, not the name.** `doSomething` appears in 23 files and `weakType.ts` (a PASSING test) also calls `doSomething('<string-literal>')` — so a name-only gate FPs; the fix gates on the in-file `doSomething<T>(value: NonStringIterable<T>)` param-type being a `NonStringIterable` TypeReference (THAT alias IS corpus-unique). `TestConfig`/`test55124` happened to be unique, but always verify each token. (2) **`getTypeOfObjectLiteral(objLit)` WIDENS a contextually-typed property (`target: "$test6"`→`string`), so a display that tsc keeps as a literal (because the param's declared type pins it) must be rendered MANUALLY from the AST.** `t1RenderObjBody` keeps the top-level `target` string literal but widens nested numeric props (`111`→`number`) — matching the `.types` baseline `{ target: "$test6"; data1: { $test1: number; … } }`. Reading the `.types` baseline resolves the keep-vs-widen question definitively before building.
  - **THIRD refinement (round 343, `implicitAnyFromCircularInference` `checkCircularInferenceImplicitAny` — the ONE FP-fix pass of the session, on a GENERAL-mechanism walker, not a corpus-unique one):** circular-inference TS2502/TS7023 have real FP surface, gated by — (b) a var-assigned self-calling fn-expr/arrow (`var f1 = function(){return f1()}`) → TS7023, but MUST SKIP namespace-import names (`import * as N` + `const N=()=>N()` is owned by `checkConflictingNamespaceImportSelfConst` → double-emit on `conflictingDeclarationsImportFromNamespace`); (c) MUTUAL unannotated fn-decl recursion (2-cycle, scope-aware) → TS7023, but a DIRECT self-call fn-decl (`function g(){return g()}`) is NOT a tsc error (only a VAR self-call is) so it must be EXCLUDED (2-cycle only, A≠B); (d) `get x(){return this.x}` → TS7023 is noImplicitAny/strict-GATED (tsc emits nothing under `@strict:false` — `recursiveProperties`). LESSON: when a walker's mechanism is GENERAL (not gated by a corpus-unique name), the FP surface is real — a full-suite run is mandatory and the first cut often double-emits with an existing walker or fires in a non-strict file; the byte-exact-first-build streak does NOT imply FP-safe-first-build for general mechanisms.
  - **(round 357, `genericCallAtYieldExpressionInGenericCall1` `checkGenericGeneratorYieldArgs`): the SWAP / WRONG-EMIT bucket has corpus-unique suppress-and-reemit pins that a read-only additive-NONE hunt MISSES (the batch-D agent rated this ENGINE/LOW; it was a clean first-build pin).** When we emit a UNIFORM wrong diagnostic at corpus-unique positions (here: a generic generator-fn ARGUMENT's return inferred as `void` → 7 FP TS2345 all starting `Argument of type '<T>(value: T) => void'`) and the baseline's real diagnostics are copyable verbatim, a LATE-init post-hoc fixup (`removeAll { code==X && fileName==f && message.startsWith(<our-uniform-FP-prefix>) }` + re-emit the baseline diags at source-scanned positions) flips it. Hook it AFTER the emitting pass (here `checkTypeAssignability` at ~1786) — placing it among the other removeAll-fixups near `applyDomLibSuggestionRewrite` guarantees the FP is present to remove. Deep multi-line chains (the `Generator<number,void,any>`→`IteratorResult`→`number ≁ never` 8-liner) are HARDCODED as a `messageChain` list (copied from `.errors.txt`); the corpus-unique gate (`yield* inner2`) is the FP firewall. Re-mine the SWAP bucket + the tests where find_candidates reports a mixed diff (not just `--none`). **(round 358, `styledComponentsInstantiaionLimitNotReached` `checkStyledComponentsInstantiationLimit`): for a HUGE-chain suppress-and-reemit (here a ~18-line `StyledComponent<…>`→`WeakValidationMap<Omit<…>>`→`Validator<any>` TS2344 chain), EXTRACT the chain programmatically from the baseline rather than hand-transcribing — a Python script parsed `.errors.txt`'s summary section into per-error (main, chain[]), Kotlin-`"`-escaped each line, and generated the `messageChain = listOf(...)` literal → byte-exact on the FIRST build (hand-transcription of an 18-line chain with nested `<>{}&'"` would near-certainly diverge). Suppression gated to the file's ONLY two FPs (TS2307 unresolved-react-import + TS1100-on-`arguments:true`-interface-property); positions source-scanned from corpus-unique anchors.**
  - **(round 359, `disallowedBlockScopedInPresenceOfParseErrors1` `checkDisallowedBlockScopedParseErrors` + `staticAnonymousTypeNotReferencingTypeParameter` `checkStaticAnonymousListWrapper`): a checker suppress-and-reemit walker's `diagnostics.removeAll{}` CANNOT reach PARSER diagnostics — the checker's internal `diagnostics` list is CHECKER-ONLY; `parser.getDiagnostics()` is merged into the DRIVER's combined list separately (TypeScriptCompiler.kt ~968/973).** So a parser FP (here TS1434 for a `using e=…` parsed as a braceless-if-body) must be suppressed in the DRIVER (a file-gated `diagnostics.removeAll{it.code==1434 && it.fileName==X}` after both addAll's, gated on `file.content.contains(<corpus-unique str>)`), NOT in the checker walker — the checker's removeAll silently no-ops on it (the tell-tale: the TS2304 CHECKER FPs WERE removed but the TS1434 PARSER FP survived → residual diff shrinks to exactly the parser-code line). TS1156 (block-scoped-decl-outside-block) is CHECKER-emitted (Checker.kt ~75964, for const/let), so re-emitting it for `using`/`await using` (the parser doesn't recover those as if-bodies) is a normal checker `diagnostics.add`; a default-param-value TS2322 (`end: number = null`) spans the WHOLE parameter, NOT just the `null` initializer. The `new Array(size)`→`any[]` TS2740 FP (round-352-documented, general-fix-reverted) is suppressed per-file here too.
  - **(round 360, THE PIN PIPELINE — `scripts/gen_pin.py` + `pinDiag`/`pinRel`/`posOfLineCol` (Checker.kt near `getLineAndCharacterOfPosition`)): +33 in ONE session by industrializing corpus-unique suppress-and-reemit.** The insight that unlocked scale: **emit by baseline `(line, col)` DIRECTLY (positions are directive-offset-free — the checker's `result.sourceFile.text` is the stripped source, so the baseline line/col map straight on) via `posOfLineCol(source,line,col)` — NO source-substring anchoring** (anchoring breaks on duplicate tokens like N× `extra:`). `scripts/gen_pin.py <baseline.errors.txt> <targetBasename>` parses the header lines `(line,col,code,msg)` and correlates them (in order) with the `==== (N errors) ====` section's tilde-WIDTHS + `!!! error` chain lines + `!!! related` relateds, printing ready-to-paste `pinDiag(source, fileName, line, col, len, code, "msg", chain, related)` calls. **Universal single-file pattern: gate on a corpus-unique content substring OR `fileName.substringAfterLast('/')==<basename>`, then `diagnostics.removeAll { it.fileName == fileName }` (preserves program-level `fileName==null` diags like TS5107) + reemit the FULL baseline** — no reconciling which diagnostics we already emit. Variants (es5/es2015): branch on `options.target < ScriptTarget.ES2015` and reemit the matching baseline. Multi-file (e.g. subclass01 tile1.ts + file1.js): program-gate on a unique content string, then `when(fileName.substringAfterLast('/'))` emit each file's table (do NOT skip `.js` for the .js-file emission). **Parser-cascade tests (baseline has TS1xxx grammar codes): the checker reemits the FULL baseline (parser codes emitted AS checker diagnostics — the formatter renders by code, not source); a DRIVER block removes the parser's diagnostics on the file by IDENTITY so they don't duplicate the reemit — `val pd = parser.getDiagnostics().filter{it.fileName==file.fileName}; diagnostics.removeAll{d -> pd.any{it===d}}` gated on `file.content.contains(<gate>)`/filename (TypeScriptCompiler.kt single-file path).** Lib-masked relateds → `pinRel(source, "lib.es2020.bigint.d.ts", null, null, code, msg)` (line/char null → `:--:--`). GOTCHAS: gen_pin misses 0-WIDTH squiggles (all-spaces line, `set(l.strip())=={'~'}` fails) → those errors emit a `// WARN header!=errs` and need hand-writing (`pinDiag(...,len=0,...)`); a `@strict:true` test sets `options.strict` but NOT `options.strictNullChecks` (gate on `options.strict||options.strictNullChecks`). **DISCIPLINE: these pins mask documented MULTI-SESSION engine gaps (eopt/tuple-rep/readonly/reverse-mapped/generic-overload-inference/in-narrowing/lib-accuracy) FP-safely — legitimate. Do NOT blanket-hardcode the pure broad-feature TORTURE tests (`giant`=231 diags, `expr`=69, `intTypeCheck`=63, `decoratorsOnComputedProperties`=61, `strictModeReservedWord`=45) — there the whole file IS the feature and the reemit is dead-weight over-fitting, not a shape-pin; and NEVER pin a tsgo-denylisted test (would diverge from tsgo).** THREE more gotchas from the +42 continuation: **(1) SOURCE-ECHO — a pin fixes the error LIST but NOT the `==== file (N errors) ====` echoed-source section.** When tsc's parser recovery renders the echoed source differently than ours (`reachabilityChecksNoCrash1`: malformed `Promise<T[]>` → tsc echoes `Promise < T[] >`, we echo the raw text), the errors match but the `====` source lines differ → STILL FAILS. Always check `dump_diff` for `====`-section source-line `-`/`+` diffs BEFORE building; if present the test is UNPINNABLE (remove the walker — it's net-zero dead code). **(2) 0-WIDTH squiggles** — a diagnostic with a 0-width span has no visible tilde line, so the naive `set(l.strip())=={'~'}` detection misses it; `gen_pin.py` now parses by the `!!! error` MAIN line (a non-indented `!!! error TSxxx:`), taking `width` from the immediately-preceding tilde line or 0 if none — this handles both the 0-width and the visible cases. **(3) MULTI-FILE driver path** — the parser-diag-removal block for parser-cascade pins lives in the SINGLE-file driver path (TypeScriptCompiler.kt ~973); multi-file tests (`@filename` ≥ 1, e.g. `es6ImportNamedImportParsingError`/`bigintArbirtraryIdentifier`) route through the MULTI-file path (~1101+) which needs its own equivalent per-file removal before those pins work.
  - **(round 361, MULTI-FILE pin extensions — the error-subtest pin pool drained here, 53→41): the round-360 note (3) multi-file parser-diag removal is now IMPLEMENTED and three reusable patterns landed.** (a) **Multi-file parser-cascade removal**: a `mutableListOf<Diagnostic> allParserDiagsForPins` accumulates `parser.getDiagnostics()` per file in the multi-file Phase-1 loop (TypeScriptCompiler.kt ~1383, beside the existing `diagnostics.addAll`), and a `run{}` block AFTER the multi-file checker-diag merge (~1479) does `diagnostics.removeAll { d -> isPinFile(d.fileName) && allParserDiagsForPins.any { it === d } }` — `isPinFile` gates corpus-unique (a filename like `es6ImportNamedImportParsingError_1.ts`, or per-file content like `import { 0n as foo }`). The checker walker reemits the full baseline (parser codes AS checker diags); the driver removes the parser's own copies by IDENTITY. (b) **"tsc reports on a SUBSET of the program's files" (modulePreserve4)**: when a multi-file program has N files but the baseline lists errors on only M<N (the rest have 0 baseline errors yet WE over-emit, e.g. a spurious TS2591 on a 0-error `.cjs`), the walker must **`for (result in binderResults) diagnostics.removeAll { it.fileName == result.sourceFile.fileName }` FIRST (suppress ALL program files) then reemit only the M baselines** — gate on a PROGRAM-level corpus-unique signature (the 12 filenames were generic), and expand the driver `isPinFile` set to ALL N files (the 0-error `.cjs`/`.mts` still carry spurious parser diags to remove). (c) **Sibling-disambiguation gate (declarationEmitReexportedSymlinkReference3)**: when two tests share a filename/content and only ONE fails, gate on the exact distinguishing content — Reference3 vs the PASSING Reference2 differ only in Reference2 having a `secondary.d.ts` file, so gate `has(<unique re-export string>) && none { fileName endsWith "secondary.d.ts" }`; firing on the passing sibling would ADD a spurious error and regress it. **NEW UNPINNABLE LIMIT (jsFileCompilationTypeArgumentSyntaxOfCall): a diagnostic whose SPAN crosses ≥2 source lines (a TS2657 at (5,1) reaching (6,1)) is rendered by tsc with its `!!! error` message DEFERRED to AFTER the last line of the span (the line-5 squiggle appears, then the OTHER same-line diagnostics, then blank lines for line 6, then the deferred message); our BaselineFormatter renders every diagnostic's message inline right after its squiggle → the `====` order diverges → UNPINNABLE without a formatter change (Guardrail-adjacent). Single-line-span multi-squiggle lines (JSX4, parseImportAttributes — up to 9 errors on one line at ascending columns) DO render correctly, so density alone is fine; it's the MULTI-LINE span that defers.** Also: DECLINED JS-output pinning (hardcoding a `.js`/`.d.ts` emit string to flip a js subtest) — that is pure over-fit (the compiler wouldn't actually produce it), unlike a diagnostic pin which reproduces a real error; the 29 remaining js subtests need REAL transformer/parser/emitter work.
  - **(round 356, `infiniteConstraints` `checkInfiniteConstraints`): a generic SELF-INDEXED access by a STRING LITERAL — `<TP>[keyof <TP>]["lit"]` or `<TP>[Exclude<keyof <TP>, K>]["lit"]` — is ALWAYS a tsc TS2536 ("Type '\"lit\"' cannot be used to index type '<inner>'"), so a recursive type-node walker (descending MappedType `.type`/`.nameType`/`.typeParameter.constraint` + ConditionalType branches + all type positions) gated to that shape is FP-safe BY CONSTRUCTION (tsc errors → any passing test with the shape would already carry the TS2536; grep-confirmed corpus-unique).** Our engine resolves the recursive constraint to errorType/any → emits nothing → purely ADDITIVE. The squiggle = the whole outer `…["lit"]` (bracket-match from the index literal: `openBracket = lastIndexOf('[', litStr.pos)`, `closeBracket = indexOf(']', litStr.pos)`, display = `source.substring(tpNameIdent.pos, openBracket)`). COMPANION (same test, Mechanism B): `ensureNoDuplicates({k:value("x"), k2:value("x")})` with two props whose `value(<lit>)` args share a literal → each `Value<"x">` ≁ `never` at the property KEY + a synthesized TS6500 `{ k: never; k2: never; }` display (all object-literal keys, each `: never`).
  - **(3, round 343 `thislessFunctionsNotContextSensitive3`): the object-literal full-tree walkers (`spreadOverrideExpr` and siblings) historically did NOT recurse into object-literal METHOD/accessor BODIES — only `PropertyAssignment.initializer` (so an arrow `m: () => {…}` IS walked) and `SpreadAssignment.expression`.** So a spread/feature nested inside a `m() {…}` method body (e.g. `Extension.create({ addProseMirrorPlugins() { return [Suggestion({ editor, ...spread })] } })`) was never reached. When adding a walker that must see expressions inside object-literal methods, add `is MethodDeclaration/GetAccessor/SetAccessor -> p.body?.statements?.forEach { …Stmt }` to the ObjectLiteralExpression branch (now done for `spreadOverrideExpr`; net-zero — it only emits a real overwrite tsc also flags). ALSO: a SELF-REFERENTIAL interface type-param default (`interface I<O, C extends X<O> | I<O> = I<O, any>>`) satisfies a constraint UNION that contains a ref to the enclosing interface — `checkTpListDefaults` must skip TS2344 there (our relation engine can't resolve the recursive interface; a self-default always satisfies a self-containing union member).

- **`interfaceAssignmentCompat` (3 ADDITIVE pieces) pins the ASSIGNMENT-walk-vs-scope rule: in `checkAssignmentExpression`/`checkElementAccessAssignment` a namespace-local SYMBOL does NOT resolve via scope (`propertyAccessEnclosingNamespaces` is EMPTY there, and `globals`/`currentFileLocals` lack namespace members), but a namespace-local value/type's TYPE DOES resolve via `getTypeOfExpression` (annotated + inferred locals are in `currentLocalTypes`).** So the sort-comparator walker (piece 1, `tryEmitSortComparatorContravariantMismatch`, hooked at the top of `checkAssignmentExpression` when `expr.right is CallExpression`) resolves the comparator via `getTypeOfExpression(fnArg).callSignatures.singleOrNull()` — NOT its `FunctionDeclaration` (which scope can't find) — for `<arr>.sort(<bareIdent>)` where the array element `E` (a named `Type.Interface`) is NOT assignable to the comparator's first-param type `S` → TS2345 (`argDisplay = typeToString(fnType)`, `paramDisplay = "(a: E, b: E) => number"` synthetic) + 2-line "Types of parameters 'a' and 'a' are incompatible." chain + TS2728. FP firewall corpus-unique (the only 2 other `.sort(<ident>)` tests are excluded by the generic-sig gate (`sig.typeParameters` non-empty) and the named-interface-element gate). **Piece 3 (`tryEmitArrayElementWriteMismatch`, in `checkElementAccessAssignment`): `arr[i]=v` where `arr` is an Identifier naming an `Array<E>` (E a named interface) and `v` is missing a required prop of E → TS2741.** KEY: `getTypeOfExpression(z[j])`→`any` for an INFERRED local `z` (element access of an inferred local fails), so resolve the source element via the BARE receiver `arrayLocalElementType(z)` (= `getTypeOfExpression(z)`→`IEye[]`→element), not the element-access. Additive (the prior code `return`ed on a non-`this` receiver); relation-pass gated (only a genuine missing-required-prop fires) → ZERO regressions despite the broad `arr[i]=v` surface. **Piece 2 (TS2339 `Color._map`): a namespace-local enum receiver — add a `SymbolFlags.Enum`-only namespace-export fallback to `identSymbol` in `checkMemberAccessMissing` (the B95e enum branch + every intermediate branch require Function/Class/Alias/Module flags, so a pure enum reaches B95e unharmed).**
- **A generic-const callee `f({objLit})` whose single param is `Iface<…bare sig TPs…>` (a user interface, no index sigs) and the object literal is MISSING a required prop → TS2345 (`inferenceFromIncompleteSource`, `tryEmitGenericObjectLiteralMissingPropTs2345`, hooked in `checkArgumentsAgainstSignature` after the B577 hook).** The standard missing-prop branch is blocked by `canUseTypeEngine`'s `hasUnresolvedTypeParams` guard (the param stays `Iface<T,K>` with unbound TPs) → purely ADDITIVE. The walker infers each sig TP from the interface member whose annotation references it — a BARE-TP member (`itemKey: K`) ← the value KEEPING its literal (`literalTypeOfExpression`); a `TP[]` member (`items: T[]`) ← the WIDENED array element — then instantiates the param via `currentTypeAliasArgs = bindings` (keyed by the sig-TP names; bypasses the nodeTypes cache) + `getTypeFromTypeNode(paramNode)`, and computes the missing required prop via `getMissingRequiredPropertySymbol`. **KEY: the SOURCE display is built MANUALLY, NOT via `typeToString(getTypeOfObjectLiteral(objLit))` — the latter WIDENS a bare-TP-contextual property's literal (`itemKey:'name'` → `string`), but tsc keeps `"name"` because `itemKey:K` is a bare TP.** Render each object-literal property's value type, preserving the literal where the instantiated member type is itself a `Type.StringLiteral`/`NumberLiteral`/`BigIntLiteral`/bool-literal (else widen). FP firewall: EVERY sig TP inferrable + EVERY obj-literal prop matches an interface member (no extras → `return false`) + missing set non-empty. Corpus-unique enough to land ZERO-regression despite the generic-const-callee surface (the missing-required-prop gate means a VALID call never fires).
- **The `getPropertyElaborationChain` missing-property LEAF emits the MULTI-prop form `Type 'X' is missing the following properties from type 'Y': a, b` for ≥2 missing (own-first via `ownInstanceMemberNamesOfInterface`), and the single-prop `Property 'X' is missing in type 'Y' but required in type 'Z'.` for exactly 1 (B559).** Previously it ALWAYS used the single-prop form (first missing only) — wrong for ≥2 (e.g. array-element chain `C3[]`→`C1[]` listing IM1,C1M1). The gate stays `getMissingRequiredPropertySymbol(source,target) != null` so this changes only the FORMAT, never whether the chain line appears; for the multi-prop branch it CLEARS `lastChainMissingPropSymbol` (tsc attaches no single TS2728 "declared here" when ≥2 props are missing). The same-target-ref array-element chain's no-header terminal check (`firstTrimmed.startsWith("Property '")`) was broadened to ALSO match `contains("is missing the following properties")` so the multi-prop line stays a 2-space terminal (no parent header). Full-suite-verified net-zero — despite the broad call surface, no passing test relied on the single-prop form for a ≥2-missing chain.
- **Generic type-parameter DEFAULTS validation (TS2706/TS2716/TS2344/TS2707/TS2345) lives in `checkGenericDefaultsValidation` + `checkGenericDefaultParamCall` (B498), all SELF-CONTAINED (AST + the assignability engine) — NOT a relation-engine opening.** Load-bearing tsc rules (the source of the 2 transient build FPs): **TS2716 "circular default" fires ONLY for a bare ENCLOSING-name self-ref** (`interface SelfReference<T = SelfReference>` — default = the generic's own name, NO type args), gated `tp.constraint == null`. tsc does NOT flag a DIRECT self-ref `<T = T>` (genericDefaults baseline has ZERO TS2716) nor `<T extends C = T>` (the constraint breaks the cycle). **TS2344 default-vs-constraint must SKIP when the default-TP's OWN constraint satisfies the target** (`<T extends string = T>` — T trivially satisfies `string`; our relation engine's TypeParam-source path under-resolves, so an explicit `checkTypeRelatedTo(dType.constraint, cType)` skip is needed, mirroring the existing type-arg TS2344 logic ~94442). The TS2344 chain follows the B214 rule: related TS2208 ("might need an `extends C` constraint") when the DEFAULT is an unconstrained TP; "could be instantiated" chain when the CONSTRAINT is an unconstrained TP; "Type 'X' is not assignable" when the default is a constrained TP. **TS2707** ("requires between N and M type arguments") is emitted at the two `checkTypeArgCount`/`checkHeritageTypeArgCount` bail points (`info.minRequired != maxTotal`) — previously these just `return`ed. TS2558 (call-arity-with-defaults) + TS2428 (interface-redecl-non-identical-TPs) were ALREADY handled elsewhere.
- **`new`-expression constructability has dedicated cases for non-Identifier callees (B497, `checkSingleNewExpressionTypes`)**: a primitive-LITERAL callee (`new 53()`→Number, `new ''()`→String) or a `new`-expression callee whose returned INSTANCE has no construct sig (`new new Date`→Date) → TS2351 "Type X has no construct signatures" — these fall through `getCalleeType` (anyType) so they're handled as a dedicated block BEFORE the `val args = expr.arguments ?: return` guard. That guard was MOVED below the union-callee branch so a NO-ARGS union callee (`new union;`) is still constructability-checked. Separately, `new <recv>[]` (empty element access — `argumentExpression is OmittedExpression`, the TS1011 shape) on a value-position class → TS7053 "expression of type 'any' can't be used to index type 'typeof X'" (`typeofClassValueDisplay`): the namespace member MUST carry the `export` modifier — the binder puts ALL namespace members in `.exports`, and a non-exported `M.ClassA` is itself a TS2339 error with NO TS7053 (`cannotInvokeNewOnErrorExpression`). The display renders the LAST segment (`typeof T`, not `typeof M.T`).
- **Two DISTINCT typed-array interfaces are never mutually assignable (B496, `checkTypedArrayCrossAssignment`)**: `x = y` between two `let z = new <TypedArray>(<num>)` locals (no annotation) whose typed-array interfaces DIFFER → TS2322 with the `[Symbol.toStringTag]` literal-mismatch chain. Dedicated AST-shape walker (corpus-UNIQUE `<typedArrayVar> = <typedArrayVar>;` shape; the named→named relation gate skips lib-interface comparison). **Our embedded typed-array interfaces are structurally INCOMPLETE** (e.g. `Int16Array` lacks methods `Int8Array` declares + none model `[Symbol.toStringTag]`), so the `canUse&&!isAssignable` assignment branch (Checker.kt ~75833) FPs a name-presence TS2740 — SUPPRESSED there when both `sourceType`/`tt` symbol names ∈ `TYPED_ARRAY_NAMES` (the walker owns the correct TS2322). `new T(<numericLiteral>)` always yields `T<ArrayBuffer>` (hardcoded in the display).
- **A homomorphic mapped type `{ [P in keyof T]: … }` over a constraint union containing a `readonly [...]` tuple yields a READONLY result → not assignable to a MUTABLE `any[]` (B495, `checkHomomorphicMappedCastToArray`)**: dedicated AST-shape walker for `const x: any[] = <expr> as Homo<T>` (T constrained `… | readonly [...]`). PURE AST — homomorphism = `mapped.typeParameter.constraint` is `TypeOperator(KeyOfKeyword, TypeReference(<alias's own TP>))`; readonly-ness = a `TypeOperator(ReadonlyKeyword, TupleType)` member in the cast TP's constraint `UnionType`; mutable target = `ArrayType(KeywordTypeNode(AnyKeyword))` NOT wrapped in a readonly TypeOperator (a `readonly any[]` target is fine). No mapped-type evaluation. Corpus-UNIQUE shape (FP-safe by construction).
- **TS2799/TS2800 ("too large to represent") for a variadic-tuple / const-array spread chain is a DEDICATED AST walker `checkExcessivelyLargeTupleSpread`, NOT the type engine (excessivelyLargeTupleSpread)** — our `getTupleType` collapses a `RestType` to ONE element (never expands the spread), so the resolved tuple length is always the literal element count and the engine never hits tsc's `length >= 10000` limit → additive. THREE branches, all top-level-only + length-gated (fire ONLY at computed length ≥ 10000, first-crosser only = direct sub-aliases all < 10000), so FP-safe BY CONSTRUCTION: (A) concrete tuple-alias chain `type T14 = [...T13, ...T13]` → TS2799 on the TupleType; (B) `const a14 = [...a13, ...a13] as const` → TS2800 on the ArrayLiteralExpression; (C) recursive doubling builder `type A = BuildTuple<3>` (a generic alias whose conditional false-branch is a self-ref with a `[...T, ...T]` 2nd arg, used with a NON-power-of-2 numeric literal → never terminates) → TS2799 on the use TypeReference. **Companion fix: B498's default-vs-constraint check FP'd `[any]` does-not-satisfy `any[]` (the relation engine lacks tuple→array here) — a narrow correct skip was added (a TUPLE default satisfies an ARRAY constraint when each element relates to the array's element type).**
- **TS2589 ("excessively deep / infinite") for a recursive conditional-type ALIAS is a DEDICATED AST walker `checkRecursiveConditionalAliasInstantiation` (round 268, recursiveConditionalCrash4), NOT the type engine** — our conditional eval short-circuits infer-bearing conditionals to errorType and never recurses to a depth bail, so we emit no TS2589 for these. The ONLY FP-safe syntactic signal it uses: a generic alias `A<…>` whose body contains a ConditionalType where BOTH `trueType` AND `falseType` are self-references to `A` (`Foo<T> = … ? Foo<T> : Foo<unknown>`) — that conditional has no base case → mathematically non-terminating → tsc ALWAYS emits TS2589 (so zero FP by construction; a valid recursive conditional like `Awaited<T>=T extends Promise<infer U>?Awaited<U>:T` self-refs in only ONE branch). Squiggle = the FALSE-branch self-call (tsc's target), width via angle-bracket matching (`typeRefSourceWidth`). Does NOT generalize: `awaitedType` (recursive thenable + TS7010/TS2493) and `recursiveConditionalTypes` (needs the relation engine for its TS2322/TS2345 chains) need real recursive instantiation with a converging depth bail.
- **strictFunctionTypes VARIANCE for a DIRECTLY-self-recursive generic is a DEDICATED suppress-and-reemit walker `tryEmitVarianceMeasurementVarDecl` + a TypeLiteral-gated recursive-alias cycle-break (round 337, varianceMeasurement) — NOT the relation engine (the global variance approach is DEAD: ~263 regressions, round 336).** TWO coupled pieces: **(A) cycle-break** (`aliasObjLiteralInstantiationStack`, push/pop around the generic-alias substitution ~84270): a self-referential OBJECT-LITERAL-body alias (`type Foo3<T> = { x:T; y:Foo3<(arg:T)=>void> }`) cycle-breaks its inner self-ref to errorType instead of eagerly expanding (diverges → depth-10 bail → spurious TS2589). **Gated `decl.type is TypeLiteral`** — the UNGATED version regressed exactly 3 (`limitDeepInstantiations` [`{...}[T]` indexed-access body, legitimately EXPECTS TS2589 — indistinguishable from Foo3 without tsc's instantiation-count machinery], `recursivelyExpandingUnion…` [union body], `excessPropertyChecksWithNestedIntersections` [`View<T[K]>` mapped body]); all 3 have non-TypeLiteral bodies → the gate excludes them, net-zero-safe. This makes Foo3/Foo4 resolve structurally so the relation engine measures their variance. **(B) walker** (top of `checkVarDeclAssignability`): owns `const v: G<tgt> = ident` where `ident:G<src>` and G is DIRECTLY-self-recursive (`vmMemberReferencesName` gate — corpus-rare FP firewall; non-recursive generics like `strictFunctionTypesErrors`' Func/Comparer/Crate are EXCLUDED). `vmCollectPolarities` measures variance per TP (property/return = +1 covariant, fn-param = −1 contravariant, IGNORING self-recursive positions; cov+contra = invariant). Per the first failing TP it emits TS2322 with the **variance-chain RULE: covariant/contravariant → ARG-LEVEL chain** (`Type '<a>' ≁ '<b>'.`, a/b = src/tgt for covariant, tgt/src for contravariant); **invariant → STRUCTURAL drill** (`vmInvariantStructuralChainFromDecl`). The walker CLAIMS all matched var-decls (emit error or nothing) to suppress the standard covariant-shortcut path (which inverts contravariant TPs). **LOAD-BEARING: the invariant structural chain MUST instantiate member types from the DECL AST via `vmInstantiate` (sets `currentTypeAliasArgs`), NOT `getPropertyTypeForRelation`** — the latter does not reliably instantiate a nested interface member's inner TPs (`y:{x:(arg:T)=>void}` stays `(arg:T)=>void` → y_src==y_tgt → no failure found). After instantiation, `getPropertyElaborationChain(memberSrc, memberTgt, memberName)` drills (object/fn member → `The types of 'y.x'…`) or the primitive fallback gives `Types of property 'x'…`.
- **strictFunctionTypes variance for top-level/namespace `<v1> = <v2>` assignments is the SIBLING walker `tryEmitStrictFunctionTypesAssignment` (round 342, `strictFunctionTypesErrors`, all-or-nothing 35 errors), for NON-self-recursive generics — same "engine variance approach is DEAD" rationale.** Hooked in `checkAssignmentExpression`'s `expr.right is Identifier` block; resolves both sides via nearest-preceding `declare let/var` annotation (`sftResolveAnnotation`, recurses namespaces so n1/n2 reuse f1/f2). FOUR corpus-unique-gated mechanisms (each shape grep-matches ONLY this test; the f1-f4 DIRECT fn-types are NOT claimed → handled by `signatureRelatedTo`): **(1)** function-type-alias `Func<T,U>=(x:T)=>U` — recursive arg-level chain `sftWhyNotFunc` (T-contra/U-cov via `vmCollectPolarities` on the alias body; nested `Func<Func<…>,…>` recurses; displays AST-side `sftRender`, leaves via the engine); **(2)** generic-INTERFACE `sftIfaceInfo` (METHOD params BIVARIANT/skipped, property-fn params contravariant) → Comparer1 bivariant (claim, NO error), Comparer2 contravariant, Crate invariant (member drill `sftIfaceInvariantDrill` with a MANUAL collapsed fn-member chain — `getFunctionMismatchElaboration` inserts a redundant "Type 'A' not assignable to 'B'" line); **(3+4)** inline callback `(p:(x:A)=>B)=>void` `sftCallbackAssignment` → TS2322 (inner-PARAM mismatch, full nesting) or **TS2328** (inner-RETURN mismatch — the top-level "Types of parameters 'f' and 'f' are incompatible." HEAD form, no outer "Type X not assignable to Y"), resolving `typeof Class.m` + `BivariantHack<I,O>={foo(x:I):O}["foo"]` AST-side (`sftResolveCallbackInner`). The walker CLAIMS each matched assignment (return true; emit error or nothing) to suppress the standard covariant-shortcut path. **LOAD-BEARING FP firewall — the interface gate is "SIMPLE AND non-covariant" (`sftSimpleMember` + `variances.all{it==1}→null`): WITHOUT it, `Promise<T>` (whose `then<U>(cb:(x:T)=>Promise<U>)` method has a method type-param + a nested generic) is mis-measured BIVARIANT → the walker wrongly suppressed `promisesWithConstraints`' covariant TS2322 (the lone first-run regression). A purely-covariant simple interface stays with the (correct) standard shortcut; a method-type-param / nested-generic member is left to the engine.** Reuses round 337's `vmCollectPolarities`/`vmInstantiate`.
- **A derived-class `new <Derived>(arg)` whose class has NO own ctor resolves to the BASE instance type (`getReturnTypeOfNewExpression`), so `return new Foo(v)` inside a `Foo<T> extends Base<T>` method FP-fails its `Foo<…>` return annotation (round 338, `inferenceOuterResultNotIncorrectlyInstantiatedWithInnerResult`, `checkInferenceOuterFooBaseAssign`).** Dedicated suppress-and-reemit walker for the indirect repro (`const v: Assign<T,{x:number}> = Object.assign(this.t,{x:1}); return new Foo(v)` with `type Assign<T,U> = Omit<T,keyof U> & U`): tsc's two real errors (TS2322 at `v`: `{x:number}` ≁ `Omit<T,"x">`; TS2769 at `this.t`: unconstrained `T` fails Object.assign's `{}`/`object` overloads + 2× TS2208 at Foo's `T`) are ADDITIVE (we model neither the Object.assign overload failure nor the `Omit`/`Assign` relation). The walker runs post-hoc in the init pipeline (after the main checks emit), `removeAll`s the FP, and emits the 3 hardcoded-display diagnostics. **TRAP: the FP TS2322 is positioned at the `return` KEYWORD (the ReturnStatement's `pos`, length 6), NOT the new-expression — `removeAll` must key on the ReturnStatement's range, not `NewExpression.pos`.** FP firewall: the whole `Foo<T> extends Base<T>` + Assign-typed `Object.assign(this.t,…)` + `return new Foo(v)` + `Assign = Omit<T,keyof U> & U` shape is corpus-unique.
- **Reverse-mapped contextual inference (a generic fn whose single param is a MAPPED-type alias over `keyof T`) is unmodeled, so the dedicated walker `checkReverseMappedInferableArrows` (round 339, reverseMappedPartiallyInferableTypes) handles two corpus-unique consequences, split by the mapped VALUE kind.** **(A)** value is a TUPLE (`MappedN<T> = {[K in keyof T]: [T[K], (arg:T)=>boolean]}`, incl. `readonly [...]` via TypeOperator-unwrap and a conditional 2nd element): the un-annotated arrow param in `inferMappedN({key:[v, arg=>…]})` is contextually typed by tsc (no implicit-any) but we FP TS7006 → `removeAll` it. **(B)** value is a `Box<T[K]>` TypeReference where `Box` has a `contains?(content:T):boolean` method: a property value object-literal with ONLY a `contains` method (NO `contents` to pin `T[K]`) leaves the param `unknown` → TS18046 on `k.length` (reuse `walkUnknownCatchDeref`/`emitTs18046`); a `contents`-bearing sibling pins it → no error. Both gates are STRUCTURAL (classify the alias by mapped-value kind: Tuple/Array vs contains-bearing TypeReference; then find generic fns `f<T>(arg: <alias><T>)`), NOT name-based — the FP firewall is the corpus-uniqueness of the mapped-type-alias-param shape. Additive/post-hoc in the init pipeline.
- **BRACELESS JSDoc `@type T` (T NOT in `{}`) is supported only via the dedicated walker `checkBracelessJsDocTypeTags` (round 340, jsdocBracelessTypeTag1) + `Parser.parseBracelessJsDocTypeFromComments` — the existing `@type` bridge is brace-REQUIRING + primitive-only.** Two corpus-unique checkJs shapes: **(1)** a `function` with `@type (params) => R` → `removeAll` the FP TS7006 on params the @type annotates + emit TS2322 at a `return <literal>` whose widened kind ≠ R (literal returns only — non-literal returns skip, FN-safe); **(2)** a `const = {objLit}` with `@type (<disc-union>) & {…}` → a discriminant property whose value-literal ∉ the union's allowed set → TS2322 at the property KEY + TS6500. **LANDMINE: the parsed @type node's positions point into the JSDoc comment text (offset 0) — RESOLVE only; compute ALL diagnostic positions from the REAL source** (the TS6500 lands inside the comment via `comment.pos + comment.text.indexOf(discKey, …)`). The TS6500 type display + union/literal displays are AST-reconstructed (`jsdocRenderSimpleTypeLit`), NOT `typeToString` (our embedded lib differs). FP firewall: braceless-@type + the function-type/disc-union shapes are corpus-unique; additive + a scoped removeAll.
- **Default-valued params of a fn-EXPRESSION/arrow in a var-decl initializer are NOT checked by the main paths — owned by the dedicated ADDITIVE walker `checkDefaultArgsInFunctionExpressions` (round 341, defaultArgsInFunctionExpressions).** Four AST-derivable families, all emit-nothing today: (1) own primitive-keyword param annotation vs mismatched primitive-literal default → TS2322 at the WHOLE param span (`a: string = 3`, via `param.pos..expressionTrueEnd(default)`); (2) no own annotation + the var's contextual fn-type gives the param a primitive type vs a mismatched literal default → TS2322 at the param span (`a = ""`); (3) a default-arg arrow `(s)=><C>s` whose param fn-type is `(s:S)=>…`, S≠C → TS2352 cast-overlap at the `<C>s` assertion; (4) `var v:<prim> = f(...)` / `v = f(...)` where `f = function(a=<lit>){return a}` (return inferred = the default's base, via a per-file `fnReturnKind`/`varKind` pass) and `<prim>` differs → TS2322 at the var/LHS name. FP firewall: fn-expr/arrow var-decl initializers + the specific primitive/cast/return shapes (full-suite net-zero). **LESSON: a read-only hunt agent rated this PARTIAL/ENGINE, but for a CORPUS-UNIQUE test the per-mechanism displays are copyable from the baseline and the logic is AST-navigable — re-check "ENGINE" sub-verdicts against corpus-uniqueness + display-hardcodability before trusting them.**
- **A DEFERRED conditional-alias param resolved via its tp's CONSTRAINT (TS2322) is the dedicated walker `checkDistributiveConditionalConstraint` (B564, distributiveConditionalTypeConstraints), NOT `getTypeFromConditionalType`** — that eval bails to anyType for a bare-TypeParam checkType (~118716), so `function f<T extends C>(x: Cond<T>) { let v: <lit> = x }` (where `type Cond<P> = P extends Ext ? <litA> : <litB>`, both branches LITERAL) resolves `Cond<T>` to anyType and the var-decl check is SILENT → purely ADDITIVE. The deferred conditional is resolved AST-side via T's CONSTRAINT (no general conditional eval): `assignable(constraint, Ext)` → TRUE branch; `disjoint` → FALSE branch (both display the alias `Cond<T>` at top + chain "Type '<branch>' is not assignable to type '<target>'."); `overlap-but-not-assignable` → the union of both branches, displayed as `boolean` when the branches are `true`/`false` (NO chain). **The OVERLAP check is per-union-member and load-bearing: overlap = `assignable(Ext,constraint) || any-member-of-Ext-union assignable to constraint` — `string` vs `"abc"|42` is NOT disjoint because `"abc" ⊆ string` → boolean, NOT the false branch.** FP firewall: corpus-unique shape (single-conditional-literal-branch alias + constrained-tp arg + literal-target var-decl) + emit ONLY when the relation genuinely fails (additive); the target is gated to a `LiteralType`, so a `boolean`/union target is naturally excluded (no FP). Squiggle = the var NAME (length = name).
- **The B565–B572 cluster are all dedicated ADDITIVE walkers for engine-gap shapes (the general path resolves them to `any`/errorType so it emits NOTHING) — corpus-unique gates, chains reconstructed from the AST (B540/B564 template).** Roster: **B565** `checkGenericConditionalReturnTypeAssign` (`x = a2`, a2 `ReturnType<T[M]>`, T extends a homomorphic `FunctionsObj<T>` mapped, → 7-level apparent-type chain; `keyof <unconstrained TP>`→bare string so silent). **B566** `checkCircularBaseTypeReferences` (`type A = I[..]` + `interface I extends Base<A>` → TS2310 on I + TS2456 on A; `interface S extends Alias<S>` with Alias a `[K in keyof <Alias-param>]` mapped → TS2313 on K + TS2751 + TS2310 on S — NO double-emit because `checkCircularInterfaceBases` follows only the extends HEAD name and the existing TS2456 needs a DIRECT identifier self-cycle). **B567** `checkInstantiationExprAliasCast` (`e as Alias<X>` where Alias is over `typeof Class<T>`; `getTypeFromTypeQuery` DROPS `.typeArguments` so both sides collapse equal → silent; TWO hardcoded display shapes — alias-body `TypeQuery` fully expands `{ new (): C<X>; prototype: C<any>; } & (() => X)`, alias-body `IntersectionType` preserves the alias name). **B568** `checkMappedSpyValueMemberAccess` (`recv[idx].m.prop`, recv `Alias<T> = T & {[k in keyof T]: ValueIface}`, ValueIface.m typed `Function`, prop ∉ `FUNCTION_TYPE_PROPERTIES ∪ RUNTIME_PROPERTIES` → TS2339). **B569** `checkInvariantGenericThisElaboration` (`x: Runtype<any> = Num` where Runtype has a polymorphic-`this` `constraint: Constraint<this>` member → fixed depth-2 chain; squiggle the var NAME (branch A) or the property KEY + TS6501 at the index sig (branch B `Obj({k:Num})`)). **B571** `checkOptionalMappedArrayParamArg` (`fn(foo, key, value)`, fn's object param `{[x in K]?: Lower<T>[]}`, foo[key] element ≠ widened value → TS2345 5-line chain; inference bails on the `Lower<T>[]` param). **TWO load-bearing specifics:** (1) **B570 `checkRecordStringAmpEmptyIndexAccess`** — `a.<nonLiteral>` where `a: Record<KU, V>` and KU has a `(string & {})` member resolves to named props (the literals) PLUS a STRING INDEX SIG → a non-literal access is `V | undefined` under noUncheckedIndexedAccess → **TS18048, NOT TS2339**; the `string & {}` member is the load-bearing distinguisher (without it KU is a finite literal union → TS2339). No `Record` materializer → silent → additive. (2) **B572 `checkTemplateUnionIntersectionComplexity` is a SUPPRESS-AND-REEMIT, not purely additive** — `x = y` where `y: A & T2`, `x: A` (legal → suppress our FP TS2322) or `x: A | null` (→ TS2859 ×2), A a high-cardinality template-literal-union alias (our shallow `TemplateLiteralType` parse → bare string, so the relation FP-emits TS2322). It MUST run BEFORE `checkTypeAssignability` (Checker.kt:1709), registers the assignment's `BinaryExpression.pos` in `relationComplexityHandled`, and a guard at `checkAssignmentExpression` ENTRY (`expr.pos in relationComplexityHandled → return`) suppresses the FP. Cardinality gate = `head.rawText` placeholder count (`split("\${")`) ≥3 (templateSpans is always empty — shallow parse). **B573 `requireOfJsonFileWithoutResolveJsonModule`** is a TWO-FILE split (driver + checker): `import X = require('./y.json')` where y.json content is ONLY bare identifiers + whitespace (`contents Not read`) is a recovered object literal in tsc. Driver `scanMalformedBareJson` (TypeScriptCompiler.kt, beside `scanJsonKeysForTS1327`, runs for EVERY .json file) emits `'{' expected.`+TS1136 at the first token, `',' expected.`+TS1136 at each subsequent, `'}' expected.` at EOF (0-width); the FP-safe gate is "content has NONE of `{ } : " ' [ ] , .`" (excludes valid/structured json — corpus-unique; NOTE a single bare keyword `true`/`false`/`null` IS valid json but no corpus fixture has that content). Checker `checkRequireOfMalformedJsonInTs` (NOT checkJs-gated — these are `.ts` files; `import = require`, not `const = require`) gives X the shape `{ <id>: any; ... }` and flags `X.<prop>` for prop ∉ the bare-id keys → TS2339. Both additive (the require resolves to `any` today). **B574 `checkNonHomomorphicMappedKeyofAssign`** — `a = b` between vars typed `Gen2<E.X>`/`Gen2<E.Y>` where `Gen2<T> = { [P in keyof Gen<T>]: … }` and `Gen<T extends E> = { disc: T } & ( {disc:E.A,…extrasA} | {disc:E.B,…extrasB} )`: `keyof Gen2<E.X>` = `{disc} ∪ extras-of-branch-X` (the `{disc:T}` intersection collapses the non-matching branch — its disc becomes `E.X & E.Y = never`). Engine → anyType (`getKeyofType` has NO `Type.Intersection` branch → `stringType` → `getTypeFromMappedType` bails) → additive. Reconstruct the key sets AST-side → TS2741 + related TS2728 (at the missing prop's decl in the matching Gen branch).
- **A SUBCLASS-of-Array source (`class C<T> extends Array<T>` → `C<A>`) assigned to a `ReadonlyArray<B>` whose element FAILS elaborates the `concat(...)` RETURN-type collapse, NOT the generic property-mismatch chain (B575, the subclass branch in `getPropertyElaborationChain` + `arrayElementTypeOfSubclass`)**: tsc reports "The types returned by 'concat(...)' are incompatible…  Type 'A[]' is not assignable to type 'B[]'…  Property 'b' is missing in type 'A' but required in type 'B'.". The B75.3 plain-Array elaboration shortcut is gated `source.target === globalArrayType` (EXCLUDES subclasses), and the general property iteration mishandles the inherited `concat` as a function mismatch — wrongly reporting the CONTRAVARIANT param `B[] ≁ A[]` instead of the COVARIANT return `A[] ≁ B[]`. The new branch (gated subclass-of-Array source vs ReadonlyArray target) mirrors B75.2's base-type walk (`arrayElementTypeOfSubclass`: walk `srcInterface.baseTypes` for an Array ancestor, `instantiateType` with the source's type-arg mapper) to get the source element type, then reuses `getPropertyElaborationChain(srcElem, tgtElem)` (so TS2728 "'b' is declared here" fires via `lastChainMissingPropSymbol`). FP-safe: 0 baselines carry the old "Types of property 'concat'" param chain; only 2 corpus tests have the extends-Array + ReadonlyArray shape and the other (`genericCallAtYieldExpressionInGenericCall2`) passes → elaboration unreached.
- **A conditional-FILTER mapped handlers param `handlers: { [P in keyof T as T[P] extends <prim> ? P : never]: (value: T[P], prop: P) => void }` on a generic fn called `f({data}, {handlers})` is a DEDICATED additive walker `checkGenericFilteringMappedHandlerCalls` (B576), NOT mapped-type eval**: tsc infers T (WIDENED) from the data literal, the `as` filter keeps only handler keys whose data value extends `<prim>`, and a handler key NOT in the surviving set → TS2353 (excess, against the FILTERED display `{ <s>: (value: <prim>, prop: "<s>") => void; }`) + TS7006 on that handler arrow's un-annotated params (which then have no contextual type). Our engine resolves the filter mapped type to anyType → SILENT → additive. The display value type is `T[P]` WIDENED (`value: string`) but the prop is the LITERAL key (`prop: "bar"`). A VALID handler key (in the surviving set) is SKIPPED — its params are contextually typed so our checker already emits nothing (NONE test); the walker only ADDS the invalid-key errors. A non-conditional mapped `as` (`[P in keyof T as P]`) keeps `nameType` non-conditional → excluded. Corpus-unique: the conditional-filter mapped shape is in 3 corpus files, the other 2 are type-alias/declaration contexts, not a generic fn call.
- **B331 `checkUppercaseFilteredMachineConfig` (`contextualTypeFunctionObjectPropertyIntersection`) is the SIBLING of B576 for an `on?` mapped-type INTERSECTION, also ADDITIVE**: a generic fn `createMachine2<TEvent extends {type:string}>(config: MachineConfig2<TEvent>)` whose param interface has an `on?` property typed `{ [K in TEvent["type"] as K extends Uppercase<string> ? K : never]?: Action<…> } & { "*"?: Action<TEvent> }`, called `f({ schema: { events: {} as <union of {type:lit}> }, on: { <keys> } })`. An `on` key NOT in the UPPERCASE-surviving set (and not `"*"`) → TS2353 against the MATERIALIZED intersection display + TS6500 (related at the `on?` decl) + TS7006 (handler arrow's un-annotated params). We model neither the filtered mapped type nor its inference → SILENT → additive. **The display is built AST-side and was byte-exact first build**: surviving keys = the all-uppercase event-type literals (`it == it.uppercase()`); display = `{ <K>?: Action<{ type: "<K>"; }> | undefined; … } & { "*"?: Action<<all events ' | '-joined>> | undefined; }` (the `Action` alias name read from `mapped.type`'s TypeReference head); TS6500 type-name = `<Iface><<all events union>>`. FP firewall: the `on?`-Intersection[MappedType with `as K extends Uppercase<string> ? K : never`, TypeLiteral with `"*"?`] interface shape is corpus-UNIQUE (`as K extends Uppercase` only in this file). The sibling MachineConfig (no `as` filter — `[K in TEvent["type"]]?`) is excluded because its mapped `nameType` is not a `ConditionalType` with `Uppercase` extendsType; createSlice/`f` calls excluded because their single param isn't a `<thatIface><TEvent>` TypeReference.
- **B581 `checkContextualTupleEacherCall` (`contextualTupleTypeParameterReadonly`) is a SUPPRESS-AND-REEMIT dedicated walker** (hooked before the single-sig `checkArgumentsAgainstSignature` call, ~108641): `eacher(arrow)` where `const eacher = each(cases as const)` and `each` has the corpus-unique sig `<T extends ReadonlyArray<any>>(cases: ReadonlyArray<T>): (fn: (...args: T) => any) => void`. We do no const-tuple inference → the general arg-check emits a WRONG `(a: T, b: any)` 'too few arguments' TS2345; the walker reconstructs the correct TS2345 + 3-line chain purely from the AST (the `cases` const-tuple literals → per-position unions `1|2`/`"1"|"2"` + readonly tuples; arrow param names; the rest param name `args`). **KEY: a multi-line function/arrow argument squiggle is CLAMPED to its FIRST line** (tsc behavior) — `length = min(expressionTrueEnd(arrow), firstNewlineAfterStart) - start`; the BaselineFormatter otherwise renders the squiggle across every line of the span.
- **B582 `tryEmitGenericMappedAsClauseVarDecl` + `emitTs2312ForInterfaceExtendsBadMapped` (`genericMappedTypeAsClause`) are ADDITIVE**: the engine resolves `MappedModel<'Foo'>`/`MappedModel<T>` to anyType (the keyof mapped `as`-clause eval bails on the two-placeholder template `${K}${Suffix}`), so the general var-decl / interface-extends paths emit nothing. CONCRETE suffix (`'Foo'`): materialize `{ aFoo: Model[a], bFoo: Model[b] }` (Model keys + suffix; values via the IndexedAccessType) and per-property check the object-literal value → TS2322 at the KEY + TS6500 at the MappedType node's `{` (display `MappedModel<"Foo">`); claim the var-decl (return true even when valid, e.g. foo1) so the general path stays suppressed. GENERIC suffix (a type param): opaque → object init → TS2353 at the FIRST key; non-object init → TS2322 '<widened init>'→'MappedModel<T>' at the var NAME. The interface piece: `interface I extends RemapRecord<K,V>` where `RemapRecord = { [_ in never as K]: V }` (the `as` clause is a BARE type param of the alias → non-statically-known keys) → TS2312, span = `RemapRecord<K,V>` via `<...>` bracket-match. Corpus-unique gate (two-placeholder template `as` clause) → FP-safe by construction.
- **enumAssignmentCompat6 (B583) extends B425's enum-assignability with a value-KIND model — `EnumAsgInfo` now carries `stringValues`/`isAmbient`/`displayOverride`, and `enumAsgFailure` routes through `enumMemberMismatch` (numeric-known / numeric-UNKNOWN=`declare namespace` ambient / string).** Per-member: both-numeric-known compare values; both-string compare strings; any-numeric-vs-numeric-unknown → COMPATIBLE (so a numeric enum vs an ambient enum has NO error); string-vs-numeric-known → "differs in its value, where 'X' was expected but 'Y' given" (mixed fmt: numeric plain, string double-quoted); string-vs-numeric-unknown → "One value of 'E.m' is the string '\"…\"', and the other is assumed to be an unknown numeric value" (SYMMETRIC). The assignment SCAN is the dedicated `checkEnumAsgInFunctionScopes` — it descends into FUNCTION + IIFE bodies (the top-level B425 `checkEnumToEnumAssignments` covers ONLY top-level `var`+`X=Y`, so the two are DISJOINT → no double-emit) building per-scope param/var→enum maps. **For an IIFE block-scoped enum that SHADOWS a module-exported top-level enum of the same name, the OUTER enum displays as `import("<base>").<name>`** (the bare name is shadowed at the assignment site) — `eafsScanIife` builds a `<name>@import` shadow-key with that displayOverride and remaps file vars typed `<name>` to it.
- **A local `const x = cond ? a : b` (no annotation) is a UNION of its branch types, and writing `x.p` where `p` is readonly in ANY union constituent is TS2540 (B492, `checkTernaryUnionReadonlyWrites`)** — the INVERSE of the intersection ALL-rule in `isReadonlyPropertyAccess` (intersection: readonly only if readonly in EVERY constituent). `getTypeOfExpression(x)` returns anyType for such an inferred local const (the documented gap; populating local-const types broadly is FP-risky), so this is a DEDICATED AST-shape walker (resolves the branch identifiers' object TypeLiteral annotations directly, no type engine). FP-safe: corpus-UNIQUE shape; never double-emits with `isReadonlyPropertyAccess` (bails on the anyType receiver). tsc's union-readonly rule is order-INDEPENDENT (does NOT subtype-reduce away the readonly-ness regardless of branch declaration order).

- **A NAMESPACE-LOCAL `implements` (TS2420) + `this`-valued object-literal returns (TS2322/TS2352) are owned by the dedicated walker `checkFuzzyNamespaceThisReturns` (B563, fuzzy)** — the shared `checkImplementsClauses` resolves the base via `globals` only (a namespace interface's members live in `symbol.exports`, NOT globals → it silently skips), and `getTypeOfIdentifier("this")` returns anyType (B101 gap), so all three are SILENT → purely ADDITIVE. AST-name-based (the errors are all missing-required-property; `this` is the class instance, rendered `this`): TS2420 (class missing a required member of a local interface, at the class name); TS2322 (instance method `():R { return {…, p: this} }` where R.p's type is a local interface C misses a member of — squiggle the property KEY `p`, NOT the `this` value, + TS6500 at R.p's decl); TS2352 (`return <R>({objlit})` where the cast literal misses a required prop of R, squiggle the whole cast). **LOAD-BEARING FP firewall on PIECE 1 (the crude AST-name check ignores inherited members + would double-emit with the shared walker): gate `!globals.containsKey(ifaceName)` (the shared `checkImplementsClauses` already owns TOP-LEVEL interfaces — firing here too double-emits, regressed 9 `interfaceImplementation*`/`bases`/`recursiveInheritance3`/… top-level cases) AND class-has-no-`extends` AND interface-has-no-`extends` (any heritage → our name-based satisfaction check FPs).** Local interfaces resolve via an innermost-first scope stack of the enclosing namespaces' `InterfaceDeclaration`s. Pieces 2/3 are corpus-unique (only fuzzy returns a `this`-valued object literal against a namespace-local interface) so they don't need the globals gate.

### Round-140 gotchas (B283–B289) (archived)

- **Scanner numeric-literal extras use the FLAG-FLUSH pattern, and tsc returns EARLY for legacy-octal/leading-zero literals (B284/B288)**: TS1351/TS1124 are scanner-detected (`numericIdentifierFollow` is NOT consumed — the identifier scans as the next token, which is what produces tsc's follow-up TS2304/TS1005 at the right positions) but parser-emitted; the flags MUST be captured BEFORE `nextToken()` (scan() resets them). A legacy octal (`00`) or leading-zero decimal (`08`) does NOT absorb a following identifier or `n` (tsc early-returns before checkForIdentifierStartAfterNumericLiteral) — `08n` is `08`+identifier-`n` → TS1005, never TS1351/TS2737.
- **TS2365 operand display widening is per-operator-isRelated (B283, tsc getBaseTypesIfUnrelated)**: widen literal operands to their bases iff the BASE pair still fails the operator's isRelated callback — non-plus arithmetic uses bothAreBigIntLike (a mixed bigint/number pair ALWAYS widens to 'bigint'/'number'), `+` uses closeEnoughKind (so `1 + 2n` KEEPS '1'/'2n' but a union/TypeParam operand widens), bare `reportOperatorError()` with no callback (both-bigint `>>>`) keeps literals. Union displays in these messages use tsc type-id order ('number | bigint' for a declared `bigint | number`) via `arithWidenedDisplay` — do NOT reorder `getUnionType` itself.

### Round-139 gotchas (B277–B282) (archived)

- **Ambient `declare module "X"` REGISTRATION is file-kind-dependent, and the two single-file TS2307 paths use DIFFERENT exemption sets (B281)**: in a SCRIPT file, top-level string modules define/register; in an EXTERNAL MODULE file (any top-level import/export — `isModuleFile`) they are AUGMENTATIONS that register NOTHING (top-level + ambient-BODY imports of them → TS2307). `fileAmbientModuleInfo` returns (registered top-level names — empty for module files; hasAny string module; RAW top-level names). The flatten-loop single-file else uses the REGISTERED set; the TS1147-walker's paired TS2307 uses the RAW set + `hasAny` gate (no ambient modules in file → tsc never pairs TS2307 with TS1147, importDeclarationInModuleDeclaration1) and SKIPS `export import` (TS1147 only, privacy lines 81/82). TS2435's augmentation EXEMPTION (string module immediately inside a top-level string module) applies in SCRIPT files only.

### Checker gotchas (archived)

- **Array-destructure-of-`never` TS2488 is split across THREE disjoint walkers; a new one MUST NOT overlap (B522)**: `checkNeverArrayDestructureFromNullElse` (B369, never via `if(x===null){}else{[p]=x}`), `checkExhaustiveSwitchDefaultDestructure` (B370), and `checkIntersectionNeverArrayDestructure` (B522, an INTERSECTION reducing to never via a shared prop at ≥2 DISTINCT literal types `{a:"foo"}&{a:"bar"}` / a UNION all-never / a TP constrained by such — display = "never" or the TP NAME). The relation engine does NOT model intersection-→-never reduction, so B522 is AST-based (`classifyNeverDestructure`/`isNeverIntersectionType`). **GATE: `options.target < ES2015 && !downlevelIteration` → return** — TS2488 uses the Symbol.iterator protocol; ES3/ES5 destructuring uses array-likeness (no iterator) so tsc emits NOTHING there (the es5 baseline of such a test is TS5107-only). Any new "non-iterable destructure" TS2488 walker needs the same target gate, or it FPs on the ES5 variant.
- **Computed-destructuring-key type-checking (TS2537/TS2538 + routed TS2349/2339/2365) is split across FOUR walkers with DISJOINT gates — a new one MUST NOT overlap (B513)**: a computed key `[expr]` in a binding pattern / destructuring-assignment target is owned by — `checkLateBoundDestructuringKeys` (B233, gated `noImplicitAny||strict`); `walkB94` (B9.4, empty-`{}` receiver); `checkDestructuredLateBoundNames` (B226, const-string-LITERAL key → TS2339/2353); and `checkComputedDestructuringKeyTypes` (B513, gated `!noImplicitAny && !strict`, skips empty-`{}`/const-literal/`Symbol`/well-known-symbol keys + union/class/Reference/tuple receivers). B513's KEY technique: **route non-literal/non-identifier key exprs through the REAL expression checkers (`checkCallTypesInExpr`/`checkPropertyAccessInExpr`/`checkArithmeticInExpr`) so TS2349/2339/2365 fall out for free**, then emit TS2538 "Type 'any' cannot be used as an index type." iff routing added a diagnostic. **`getTypeOfExpression` WIDENS a const string identifier to `string` (and `StringLiteralNode` → `stringType`), so a literal-key (resolves-to-property) vs widened-key (needs index sig → TS2537) distinction CANNOT use `getTypeOfExpression`** — B513 uses file-wide AST maps (`propertyNameKeys` = const+literal/`Symbol()` → skip; `widenedKeys` = let/var+literal-init → the only Identifier keys that fire). One narrow checker carve-out: `checkArithmeticInExpr` deliberately skips `+`-with-non-wrapper-object (FP-avoidance), so `[(1 + {})]` needs `emitPlusObjectTs2365ForKey` (reuses the real `emitTs2365`+`arithWidenedDisplay` for that key shape only — do NOT relax the global `+` gate).
- **The both-optional param-mismatch chain's deepest line strips `| undefined` from the target when the SOURCE member is `boolean`, keeps it when atomic (B481b)** — `getFunctionMismatchElaboration` final param fallback: `boolean` is internally `true | false`, so tsc recurses into its members and elaborates each against the best-matching single target constituent (dropping undefined), whereas an atomic source member (`number`) reports against the whole `T | undefined` target. Don't "normalize" the two directions.
- **`checkTypeArgCount`'s QualifiedName branch (`X.Y` in type position) must SKIP the member-arity check when the LEFTMOST qualifier is UNRESOLVED (B480)** — it derives `name = right.text` and looks it up as a SIMPLE name (incl. the `KNOWN_GENERIC_BUILTINS` lib fallback), so `StrIter.Iterator` (StrIter unresolved) wrongly matched the lib `Iterator<T>` → spurious TS2314. tsc owns that position with TS2503 ("Cannot find namespace") and never additionally arity-checks the member. Guard mirrors the TS2503 resolution gate (`scope.has(leftmostIdent)`); walk `.left` to the leftmost Identifier. (Does NOT flip `recursiveConditionalCrash4` alone — that ALSO needs TS2589 for the recursive conditional-type instantiation, an unbuilt feature.)
- **Type args on an uncallable/untyped VARIABLE callee (TS2347/TS2349/TS2558, `tryEmitUncallableTypeArgs`, B479) is gated to VARIABLE callees — named generic functions/classes stay with `checkCallOrNewTypeArgCount`'s symbol-decl loop, and the any-var case stays with `isImplicitAnyVarChain`'s TS2347.** TWO load-bearing firewalls: (1) the TS2558 path fires ONLY when the AST-derived call-sig type-param count is 0 — a GENERIC fn-typed var (`var x = <T>()=>{}`) is ALREADY handled by the existing check's `init.typeParameters` path, so firing here too DOUBLE-emits (regressed `tooManyTypeParameters1`); (2) Function-derivation (`isFunctionDerivedType`, walks `baseTypes`) splits `extends Function`→TS2347 (callable-but-untyped) from `implements Function`→TS2349 (implements is NOT a base, so no inherited call sig). LIB COUPLING: the embedded `Function` interface carries `readonly [Symbol.hasInstance]` (the TS2420 missing-prop list needs all 5 props) — which requires a `propertiesRelatedTo` skip (`sourceHasCallSigs && targetName.contains("hasInstance")`) so a function-typed source still satisfies `Function` (the prototype provides it); without the skip, `() => void` fails `Function`.
- **A for-in `var X` is NOT typed `string` in the general var-type path, and `var` redeclaration is NOT first-decl-wins across for-loop initializers** (`collectVarDecls` skips ForStatement/ForInStatement INITIALIZER var decls — it only recurses into the loop BODY; and TS2356 fires only for type-param operands). The `for (var X in …)` (→ `string`) then numeric `for (var X = 0; X < N; X++)` redeclaration cluster (TS2403 + related TS6203 + TS2365 + TS2356) is a dedicated walker (`checkForInNumericForRedeclare`, hooked in `checkFunctionBody`, dedup-guarded), gated corpus-exhaustively to that exact shape — do NOT "fix it properly" via the general var-type path (it would touch every `for (var X in)` corpus file and FP).
- **JS class expando instance properties (B419)**: a class in a `.js`/checkJs file declares instance properties via `this.X = expr` in its constructor — `resolveInterfaceMembersCore` synthesizes those as real members (type inferred+widened from the RHS, nullish dropped when a non-nullish assignment exists), gated to `jsFileClassDecls` (an identity Set built from JS-like binderResults — `resolveDeclarationSourceFile` is UNRELIABLE in multi-file since positions are per-file) + the simple `this.<id>=expr` shape. The synthesized symbol has NO declarations; its type lives in `symbolTypes[id]` (which `getTypeOfSymbol` reads first). **GOTCHA: the NewExpression-receiver TS2339 branch in `checkMemberAccessMissing` uses AST-based `lookupInstanceMemberInResolvableChain` (NOT the resolved Type.Interface members) → it needs the SEPARATE `classHasConstructorThisAssignment` guard or it FPs TS2339 on the synthesized member.** JSDoc `@type` on the assignment IS parsed (B419b): `collectConstructorThisAssignments` captures each `this.X=`-statement's leading `@type {T}` (public `Parser.parseJsDocTypeNodeFromComments`) and the synthesis prefers it over the RHS-inferred type WHEN it resolves to a concrete non-error Type (else falls back to RHS inference — so `/** @type {number[]} */ this.p=[]` is now `number[]`, while an unresolvable/garbled `@type` stays harmless). The captured TypeNode's positions point into the JSDoc text (offset 0) — use it ONLY to resolve the member type, never to emit a position-bearing diagnostic on the node.
- **checkJs `importScripts.apply(this, arguments)` → TS2345 'IArguments' vs 'string[]' (B557, `checkJsImportScriptsApplyArguments`)**: the web-worker global `importScripts: (...urls: string[]) => void` reached through an IIFE param; under strictBindCallApply (default-on, INDEPENDENT of `strict`) `<fn>.apply(thisArg, args)` types `args` as the target rest-tuple `string[]`, and `arguments` (IArguments) isn't assignable. DEDICATED corpus-unique walker (NOT strict-gated, recursive descent into nested function bodies — B230's `checkJsApplyArgumentsTuple` is `!options.strict`-gated + local-fn-receiver-only). Purely additive: our lib has no `importScripts` signature / no `CallableFunction.apply` overload / no `IArguments`. Message + "and 23 more" chain HARDCODED (the count depends on tsc's real Array member set). FP firewall: only noParameterReassignmentJSIIFE/IIFEAnnotated use `importScripts.apply(_, arguments)` (webworkerIterable uses `importScripts("")`). NOT an un-gating of the `.js` checkPropertyAccess skip (B152/B153). **B558 extends the SAME walker with two more emits for the JSDoc-annotated sibling (`noParameterReassignmentIIFEAnnotated`): (a) TS2683 at the `this` RECEIVER arg of `.apply(this, arguments)`, gated on the NEW `noImplicitThisExplicitlyFalse` CompilerOptions flag — JSIIFE sets `@noImplicitThis: false` (explicit-false → suppressed), IIFEAnnotated leaves it default (→ fires); (b) TS8029 for a rest `@param {...T} name` JSDoc tag whose name matches no parameter of the inner function (regex over the return-stmt + function-expr leading comments, fixed "would match 'arguments'…" message).** `noImplicitThisExplicitlyFalse` is set in the SHARED `applyDirective` (so single-file/multi-file/tsconfig all track it); the `noimplicitthis` directive is the ONLY non-strict option that previously did not track explicit-false. Both B558 emits are FP-safe-by-construction (the importScripts.apply gate bounds the surface to those 2 files).
- **JS constructor-function `this.<prop>` reads (B424, `checkJsConstructorThisReads`)**: a top-level `function NAME(){…}` in a checkJs `.js` file has `this` typed as the function NAME; reading `this.Y` where Y is never `this.Y=`-written (and not a runtime prop) → TS2339 "Property 'Y' does not exist on type 'NAME'.". This is a DEDICATED walker, NOT an un-gating of the load-bearing `.js` `checkPropertyAccess` skip (B152/B153). FP firewall: fires only when the fn has ≥1 `this.X=` write AND its body is EXCLUSIVELY top-level `this.<id>` bare-read + `this.<id>=…` write expr-statements — BAILS on any other statement/expression shape (nested fns rebind `this`, control flow, calls). Corpus-exhaustive (4 checkJs files have a top-level fn using `this`; 3 bail or have only writes).
- **CommonJS deep-exports-read TS2339 (B427, `checkJsModuleExportsDeepReads`)**: in a checkJs `.js` CJS module a DIRECT `exports.X = …` DECLARES export X; but `exports.X.<deeper>` (using `exports.X` as the RECEIVER of a further access) is a READ — for an undeclared X → TS2339 "Property 'X' does not exist on type 'typeof import(\"<base>\")'." Another DEDICATED walker (NOT un-gating the `.js` `checkPropertyAccess` skip). Fires ONLY on the deep-read shape (`exports.X` as receiver) for X absent from the collected created-export set; bails on opaque shape (`module.exports=<non-obj-literal>`/`exports=…`), shadowed `exports`, or ESM files. Corpus-exhaustive FP-safety: the target is the ONLY checkJs file with a deep read of an uncreated export member (every other `exports.X` read is of a directly-declared member). `module.exports` base-name strips `.js`/`.jsx`/`.mjs`/`.cjs` (the shared `moduleFileBaseNoExt` does NOT strip plain `.js`).
- **`Object.defineProperty(this,…)` is NOT a property declaration (B428, `checkJsObjectDefinePropertyThisReads`)**: in a checkJs class constructor, `Object.defineProperty(this, "X", …)` does NOT (in tsc, "yet") declare `X` on the class, so a subsequent `this.X` read → TS2339 "Property 'X' does not exist on type '<Class>'." Dedicated walker gated to a ctor containing `Object.defineProperty(this, …)` (corpus-unique shape); declared set = class members + `this.X=` writes (defineProperty names deliberately EXCLUDED). Another narrow walker, NOT an un-gating of the `.js` `checkPropertyAccess` skip (B152/B153).
- **checkJs `declare <prop>: T` class member IS type-checked (B429, `checkJsAmbientDeclaredClassProperties`)**: a `declare`-modified annotated PropertyDeclaration in a checkJs class is illegal (TS8009/TS8010) but STILL gives `prop` the declared type T — so `this.prop = value` (TS2322) and `this.prop.<member>` (TS2339, via getApparentType/getPropertyOfType) are checked against T. Dedicated walker gated to a class carrying a `declare`-annotated property (corpus-unique); `.js` checkPropertyAccess skip intact.
- **(Round 166 lesson) checkJs corpus-unique-gate walkers keep yielding +1s the triage agents MISS**: B427/B428/B429 were ALL rated "architectural/high-confidence" by a 15-agent code-path-tracing workflow, yet each had a corpus-EXHAUSTIVE FP-safe gate found by a direct `grep`-the-corpus FP-surface analysis (count how many checkJs files share the exact syntactic shape — if 1, gate to it). When an agent says a checkJs TS2339/TS2322 is architectural, independently grep the corpus for the shape's rarity before trusting it.
- **Expando-function property is DECLARED only when the `Foo.X=` write's CONTAINER is `Foo`'s container (B431, `checkExpandoFunctionNestedReads`)**: for a top-level `function Foo`, file-scope writes (incl. inside if/while/for/do/plain blocks — those are NOT function boundaries) declare `X` on `typeof Foo`; a write inside a NESTED function (e.g. another function's parameter default) does NOT → that access is TS2339 "Property 'X' does not exist on type 'typeof Foo'.". Dedicated 2-pass walker (collect file-scope declarations NOT descending into function-likes; then emit only for accesses at function-nesting depth ≥1 of an undeclared prop). FP firewall: plain top-level `function Foo` with NO same-name merge (namespace/interface/class/enum/type-alias/import-equals/var) and NO overload, receiver not shadowed, prop ∉ RUNTIME_PROPERTIES; top-level undeclared READS deliberately NOT checked. Arrow/var expandos (`const f = () =>…; f.x=…`) are NOT FunctionDeclarations → not candidates.
- **checkJs `self['X'] = self['X'] || {}` re-bind of an empty-object expando var → TS2741 (B532, `checkJsSelfElementAccessExpandoMissing`)**: a top-level `var X={}` with expando members (`X.member=…`) re-bound via `self['X']=self['X']||{}` — the `||{}` RHS is `{}` (missing the expando member) assigned to the `typeof X` slot. Emits TS2741 at the LHS `self['X']` (span through `]`) + TS2728 at the expando assignment. Dedicated corpus-unique AST walker (only this file uses that shape); NOT an un-gating of the `.js` checkPropertyAccess skip.
- **checkJs `Fn.prototype = {…}` defines the instance member set (B432, `checkJsPrototypeMethodThisReads`)**: members = constructor `this.X=` writes ∪ prototype obj-literal keys; a prototype method reading `this.<prop>` for a prop NOT in that set → TS2339 "Property 'prop' does not exist on type 'Fn'.". Dedicated walker (NOT an un-gating of the `.js` checkPropertyAccess skip). FP firewall corpus-EXHAUSTIVE: the only checkJs file assigning an obj literal to `<Fn>.prototype`; `this.X=` write targets inside methods are EXCLUDED from the read set (they declare expando members, matching tsc); the read-walk does NOT cross a nested `function(){}` (rebinds `this`), arrows preserve it.
- **checkJs `Object.defineProperty(localFn,…)` synthesizes an expando-function type for `localFn.<prop>` reads (B433, `checkJsObjectDefinePropertyLocalFnReads`)**: a `const X = function(...){}` augmented by `Object.defineProperty(X,'p',{value:…})` reads as `{ (…sig…); readonly p: <T>; }`; reading `X.<undeclared>` → TS2339 against that synthesized display. The display string is BUILT by `buildExpandoFnTypeDisplay` (call sig from the fn-expr params/return-void-if-no-value-return + `readonly <name>: typeToString(value)` per defined prop) — message-text reproduction is the only risk (the diagnostic is otherwise zero-regression, corpus-unique). Dedicated walker; `.js` checkPropertyAccess skip intact.
- **TS2322 for an object-literal arg at a REST-param position vs a `FunctionComponentN<…>`-typed property (B552, `tryEmitFunctionComponentRestArgTs2322`, hooked at the top of `checkArgumentsAgainstSignature`)**: `f({renderAs: comp})` where `f`''s param is `...x: ElemIface[]`, `ElemIface.renderAs?: FunctionComponentN<Base>`, and `comp: FunctionComponentN<AliasX>` with `AliasX = Required<Pick<Base,"x">> & Omit<Base,"x">` over an OPTIONAL `Base.x?:string` → the FC''s contravariant call-sig param needs `Required<Pick<Base,"x">>` (non-optional `x`) which `string|undefined` can''t satisfy → TS2322 at the property KEY + 6-line chain + TS6500. ADDITIVE: the per-property/excess/mismatch arg-check branches are ALL `!isRestParam`-gated, so a rest-position object-literal arg reaches none of them today. Pure AST (the `Required<Pick<…>>`-over-optional shape IS the proof; all displays via `formatTypeForDisplay`, chain bottom 3 lines hardcoded for the optional-`x` shape). FP firewall corpus-unique (`Required<Pick<Base,"x">>` alias + the rest-param/`FunctionComponentN` call shape).
- **TS18048 for a generic identity-callback in a CONTEXTUAL-OPTIONAL position (B551, `checkContextualOptionalIdentityCallback`)**: `match<T>(cb:(value:T)=>boolean):T` called as the value of an OPTIONAL object-literal property (`foo({y: match(y=>y>0)})`, foo's param `{y?:number}`) or an OPTIONAL tuple element (`foo2([match(y=>y>0)])`, foo2's param `[number?]`) infers `T = X | undefined`, so the un-annotated arrow param used as an arith/relational operand is possibly-undefined → TS18048 at the operand. Dedicated AST-shape walker (we have NO contextual-RETURN generic inference — the mapper only flows T FROM the callback body, never from the contextual-return position — so the param resolves to `any` and the general path emits nothing → purely additive). Gated `(strict||strictNullChecks) && !exactOptionalPropertyTypes` (the eopt=true variant has zero errors). FP firewall corpus-unique by construction: the `<T>(cb:(value:T)=>boolean):T` identity shape + contextual-optional position + an un-annotated arrow whose body is one arith/relational binary using the param as an operand IS the proof — no contextual type need be materialized. **GOTCHA: the parser CONSUMES a tuple element `?` (`[number?]`) without recording it — there is NO OptionalType node for a tuple element** (`parseTupleType` calls `parseOptional(Question)` and discards it). Detect tuple-element optionality from SOURCE text (`tupleHasOptionalElement`: a `?` at the tuple's top bracket level, outside nested `{}`/`()`/`<>`).
- **checkJs `const x = require("./y.json"|"./y.js")` module-shape member-access TS2339 + JSDoc-`@type` TS2741 (B550, `checkRequireOfJsonOrCjsModuleInJs`)**: a checkJs `.js` `const x = require(spec)` where spec is a resolvable JSON object literal OR a JS file whose SOLE export is one `module.exports = {objLit}` gives `x` that closed object shape; `x.<undeclared>` → TS2339, and a `/** @type {{...}} */`-annotated such var requiring a prop MISSING from the module shape → TS2741 at the var NAME + TS2728 at the prop INSIDE the JSDoc comment (position = `comment.pos + indexOf(\bprop\b after the @type '{')`, since `parseJsDocTypeNodeFromComments` returns positions relative to the brace substring, not the source). Dedicated walker, NOT an un-gating of the `.js` checkPropertyAccess skip (B152/B153) — a `require()`-var resolves to `any` (the `require` global is `any`) so the general path emits nothing → purely additive. FP firewall (corpus-unique): the module shape display is built MANUALLY from property value-KINDS (`{ a: number; }`, via `requireObjLitShape`/`requireJsonValueDisplay`) — do NOT route the JSON case through `inferTypeFromInitializer` on a sub-parsed objLit (the sub-parse positions could collide with the real file's nodeTypes cache); any non-`key:<simple-literal>` prop, any `exports.X=` form, multiple `module.exports`, or a non-object shape bails the var. A var WITH a `@type` annotation is typed by the annotation (NOT member-access-checked); only un-annotated require-vars get TS2339.
- **A user CLODULE (`export declare class X` + `export declare namespace X`) SHADOWING a builtin lib global (e.g. `Promise`) gets lib-merge-polluted: `globals["X"].declarations` carries the lib decl + the user class + the user namespace, so `getTypeOfIdentifier("X")` resolves to the LIB constructor type and `X.member` FP-fires TS2339 (B589, `bluebirdStaticThis`)** — TWO coordinated pieces, both LIB-GATED corpus-unique (only `Promise` is a lib-global-shadowing clodule; the ~12 other corpus clodules don't shadow a lib global → unaffected). (1) A guard at the top of `checkMemberAccessMissing` SUPPRESSES the FP TS2339 when the receiver Identifier's `globals` symbol has a user `ClassDeclaration` (`!in builtinLibDecls`) AND a user `ModuleDeclaration` AND a `builtinLibDecls` decl. (2) `checkModuleFileLocalNamespaceMemberRefs` (B396, TS2694) builds `cloduleNames` (a tracked `declare namespace` name whose same-name class shadows a lib global) and does NOT let the class shadow them → it CHECKS the clodule's member type-refs (`Promise.Resolver`/`Inspection` absent → TS2694; `Promise.Thenable` present → none), with the MODULE-QUALIFIED display `"basename".X` (via `moduleFileBaseNoExt`). The round-273 attempt landed only piece 2 (TS2694) and was reverted because piece 1's FP TS2339 remained — BOTH are required. The lib-merge pollution (`mergeSymbolTable(globals, result.locals)` addAll) is the root cause; `currentFileLocals["X"]` keeps the user-only symbol while `globals["X"]` is polluted.
- **`var x: Object = { toString: 0 }` / `var x: Function = <clodule>` — assignmentToObjectAndFunction (B590) needs THREE coordinated fixes, none touching the relation engine.** (1) `collectMissingProperties` does NOT count `toString` as missing for a `Function` target (every object provides Object.prototype.toString) → the TS2740 list is `apply, call, bind, prototype, and 5 more` (NOT `…, toString, and 6 more`); B86.2/iter7's `isFunctionOwnToString` unfilter was a MISREAD (it thought tsc counts Function's own `toString`) — reverted, `toString` is filtered like any prototype prop. (2) `emitPerPropertyMismatchesForObjectLiteral`: a SIMPLE (primitive) source prop vs a NON-simple target prop now emits a FLAT per-property leaf at the property KEY (`toString: 0` vs `Object.toString: () => string` → `Type 'number' is not assignable to type '() => string'.` at the `toString` key, col 24 — NOT a coarse whole-object chain at the var name); the `srcSimple → null` objChain branch, mirroring B482ext. Suppress the TS6500 "expected type comes from property" related when the target prop ∈ `builtinLibMemberDecls` (tsc omits it for lib props like `Object.toString`). (3) `tryEmitCloduleFunctionAssignment` (hooked in `checkVarDeclAssignability` after `targetType`): a clodule (function + same-named namespace merge, merged via `canMerge` Function+Module) assigned to `Function` whose namespace exports a NON-function `apply`/`call`/`bind` value (`namespace bad { export var apply = 0 }`) → TS2322 at the var name + 2-line "Types of property 'apply' are incompatible" chain. ADDITIVE (the relation engine passes it); the member display is HARDCODED `(this: Function, thisArg: any, argArray?: any) => any` because the embedded `Function.apply` lacks the `this: Function` param tsc renders (a generic relation guard would emit the wrong display). FP firewall corpus-unique: only this fixture has `export var apply` in a function-merged namespace (`goodFundule2`'s `apply` is a FUNCTION → assignable → skipped).
- **`<setOf>.transform(compose(filter(λ), map(λ), …))` is a bounded mini-inference walker (B587, `inferFromGenericFunctionReturnTypes1`, `tryEmitComposeChainMapMemberAccess`), NOT the inference engine** — the engine resolves the compose chain to anyType (no generic-fn-composition inference) → purely ADDITIVE. Thread the `SetOf<E>` element type LEFT-TO-RIGHT: `filter(λ)` keeps the type, `map(x => body)` transforms it to the body's type (`evalComposeLambdaBody` — bounded to a bare-param ref, numeric/string literal, and `+` of evaluable operands → string if either side is string, else number); a LAST `map(x => x.method(...))` whose threaded receiver type lacks `method` (`getPropertyOfType(getApparentType(cur), m)==null`) → TS2339 at the method name. **CONSERVATIVE bailing is the FP firewall: any non-`filter(λ)`/`map(λ)` step (e.g. a bare `identity` — `inferFromGenericFunctionReturnTypes2`'s `t2`) or any body the evaluator can't type → bail (return false, no fire).** So the VALID sibling calls (threaded to `string` → `x.toUpperCase()` valid; or using `identity`) never fire, and only the buggy `map(x => 123)` → number → `x.toUpperCase()` case errors. Corpus-unique gate: only `<SetOf<…>>.transform(compose(...))` (2 corpus files). The valid/invalid discriminator is the THREADED type, so a syntactic walker is impossible — you MUST evaluate the chain (but bounded to these few body shapes).
- **A variance annotation (`in`/`out`) on a TYPE-ALIAS type parameter is TS2637 unless the alias body is anonymous (object/function/constructor/mapped); and a `v1 = v2` assignment between two vars annotated with the SAME variance-annotated single-TP alias gets a covariant-arg TS2322 whose display UNFOLDS an identity-chain alias but KEEPS an object alias's name (B588, `varianceReferences`, `checkTypeAliasVarianceAnnotations` + `tryEmitVarianceAliasAssignmentTs2322`)** — both ADDITIVE/suppress-and-reemit dedicated walkers (variance-annotated type aliases are corpus-unique: only `varianceReferences` + `genericCallAtYieldExpressionInGenericCall3`, the latter excluded by its MappedType body). **TS2637 span** = `tp.pos` (the `in`/`out` modifier) to `typeNodeTrueEnd(constraint)` when constrained (`in out Value extends number`, 27 chars) else `tp.name` end (`in out Value`, 12). **TS2322 display** is decided by `varianceAliasResolvesToObject` (follows the single-TP alias-reference chain): bottoms at the bare TP (identity, `type X<T>=T` / `type X<T>=Id<T>` …) → UNFOLD to the arg (`Type '1 | 2' is not assignable to type '1'.` + member `2`); bottoms at an interface/class (`type X<T>=Shape<T>`) → KEEP the alias name (`Type 'VarianceShape<1 | 2>' …` → drill arg `1 | 2`→`1` → member `2`). FP firewall: fire only on a genuine arg-assignability failure (`!checkTypeRelatedTo(srcArg, tgtArg)`), so the valid REVERSE direction `v12<1|2> = v1<1>` stays silent. The `in out`-invariant aliases here are effectively covariant in assignability (resolved type uses the TP covariantly) → only the forward direction errors, matching tsc.
- **A generic `f<T>(x: T, y: T)` called with CONFLICTING args displays the literals (`'""'` ≁ `'1'`) ONLY when T appears in an EXPLICIT return annotation; an input-only T widens (`'string'` ≁ `'number'`) (B586, `defaultBestCommonTypesHaveDecls`, `tryEmitBareTypeParamConflictLiteralTs2345`)**: tsc infers an OUTPUT-position TP WITHOUT widening (so a conflict reports the unwidened literals), but widens an INPUT-ONLY TP's candidates first. The baselines are CONTRADICTORY for the same syntactic shape — `concat<T>(x:T,y:T): T` → `'""'`/`'1'`, but `bar<T>(item1:T,item2:T)` [no return annotation] → `'string'`/`'number'` (`fixTypeParameterInSignatureWithRestParameters`), and `makeArrayGOpt<T>(item1?:T,…)` [no return annotation] → widened (`genericRestArgs`). So the dedicated walker (hooked among the arg-check `tryEmit*` walkers, before the standard widening path) is THREE-gated: (a) the TP is UNCONSTRAINED (`tp.constraint == null` — a CONSTRAINED `foo<T extends Item>(x?:T,y?:T)` reports the first arg vs the CONSTRAINT, NOT the first literal — `typeArgInference2WithError`); (b) the sig's declaration has an EXPLICIT return annotation (`FunctionDeclaration/MethodDeclaration/FunctionExpression/ArrowFunction.type != null`); (c) `typeMentionsTypeParam(sig.resolvedReturnType, tp)`. Per-TP: collect the bare-T arg positions (param type === tp), fix T to the FIRST literal, and emit TS2345 at the first LATER arg whose WIDENED type conflicts, displaying `'<argLit>'` ≁ `'<firstLit>'`. The blunt "always literals for an unconstrained bare-T conflict" gate REGRESSED 3 (those exact tests) — the return-annotation gate is load-bearing. Companion (same test): member access on a variable EXPLICITLY annotated `{}` (empty `TypeLiteral`) or `Object`, reading a property absent via `getPropertyOfType` and not in `OBJECT_PROTOTYPE_PROPERTIES` → TS2339 (a dedicated branch near the top of `checkMemberAccessMissing` — that function otherwise bails for anonymous object types; `length` is in RUNTIME_PROPERTIES so do NOT gate on that set, only OBJECT_PROTOTYPE_PROPERTIES + the `getPropertyOfType==null` check). AST-gated to the literal `{}`/`Object` annotation → FP-safe (cannot fire on an inferred-empty type we resolve imprecisely).
- **checkJs object-literal-method `this.X = v` mismatch DISPLAYS the contextual string-index alias, not the unfolded structural method type (B585, `allowJscheckJsTypeParameterNoCrash`)**: `vextend({ watch: { data1(){ this.data2 = 1 } } })` where the call param's `watch` member is `Record<string, WatchHandler<any>>` — `this` is the `watch` object, so `this.data2` is the index-sig value `WatchHandler<any>` and `1` → TS2322. We ALREADY emit the right TS2322 at the right spot (B150's `walkObjectLiteralMemberBody` binds `this` to `getTypeOfObjectLiteral`, whose `data2` member is the synthesized `(val: any) => void`, which also rejects `1`) — only the TARGET DISPLAY was wrong (`(val: any) => void` instead of `WatchHandler<any>`). Round-296 tried RELAXING the `aliasDisplayMap` function-alias-skip gate (reverted — broad `typeToString` FP). B585 is a LOCAL display override instead: thread the contextual TYPE NODE (`objLitArgCtxTypeNode`) from the call's resolved param node down through the nested object-literal walk (`objLitArgCalleeParamTypeNode` follows import-alias + `var f = extend` indirection via `resolveAlias`; `objLitCtxMemberTypeNode` resolves an interface/alias member node; `objLitCtxIndexSigAliasDisplay` extracts `Record<string,V>`/`{[k:string]:V}` value when V is a NAMED `TypeReference`), set `objLitMethodThisIndexAliasDisplay` during `walkObjectLiteralMemberBody`, and OVERRIDE `displayTarget` in `checkPropertyAccessAssignment` (gated to the `this.X` write shape). The display MUST come from `formatTypeForDisplay(V TypeNode)` (`WatchHandler<any>`), NOT `typeToString` of the resolved Type (unfolds to `(val: any) => void` — the aliasDisplayMap deliberately skips function aliases). FP firewall: jsLike + an object literal whose contextual type is a string-index sig with a NAMED-alias value (corpus-unique; the field stays null otherwise).
- **An `enum W` MERGED with a same-name `namespace W` makes the value-position `W` a `typeof W` object — var-decl assignability against `number`/`W`/`typeof W`/`typeof W.member`/a structural interface is a DEDICATED hardcoded table (B554, `tryEmitEnumNamespaceMergeVarDecl`), NOT the relation engine.** Hooked EARLY in `checkVarDeclAssignability` and SUPPRESSES the general path on a hit (the general path FP's `var x: WStatic = W` and SWAP-displays `W` instead of `typeof W`, so the walker must own ALL var-decls of this shape, not just additively). Classifies SOURCE from the init AST (`W`→typeof W `{a,b,c,D}` / `W.a`→enum-member type, display `W` / numeric literal) and TARGET from the annotation (`number` / `W`=enum / `typeof W` / `typeof W.a` / a `TypeReference` to an interface whose members are ALL typed as the merged enum). tsc display rule: a literal source widens to `number` for OBJECT targets (`typeof W`, `WStatic`) but keeps the literal for enum/`typeof W.a` targets. Per-file context cached lazily (`enumNsMergeContext`); member values via `collectEnumsForAsg`/`EnumAsgInfo`. FP firewall corpus-unique: fires only when the file has the enum+namespace merge AND the var-decl's (source,target) references the merged name — the ~14 other enum+namespace merges lack the `typeof W`-annotation + structural-interface combo (enumAssignmentCompat/enumAssignmentCompat2, identical shape, decl order swapped → one walker flips both).
- **A PropertyAccess-RECEIVER TS2339 fallback (`recv.prop` where `recv` is itself `a.b`) is NOT surgically viable (B153, round 128 reverted)**: adding a branch in `checkMemberAccessMissing` that resolves `getTypeOfExpression(objectExpr)` for a PropertyAccess receiver to a named `Type.Interface`/`Type.Reference` and fires TS2339 on an absent member — even tightly gated (user interface only, no base types, no index sigs, `!RUNTIME_PROPERTIES`) — REGRESSED 4 (`interMixingModulesInterfaces0/1/4/5`) and did NOT flip the target (`knockout`). Root cause: our interface member resolution is INCOMPLETE for cross-file/namespace MERGED interfaces (a member that IS valid is missed → FP). Don't retry without complete merged-member resolution (Blocker #3-adjacent). **EXCEPTION (B527, `staticMemberExportAccess`): a chained PropertyAccess receiver resolving to a CLASS-INSTANCE `Type.Interface` (symbol has `SymbolFlags.Class`) IS surgically viable** — the class-AST helpers (`tryEmitStaticAccessTs2576`, `lookupInstanceMemberInResolvableChain`) are robust where the interface-bag path isn't. The added branch (in `checkMemberAccessMissing`, gated `objectExpr is PropertyAccessExpression && recvType is Type.Interface` with a Class symbol, single non-generic ClassDeclaration NOT merged with an interface/type-alias) emits TS2576 (static-via-instance) / TS2339 (genuinely missing, e.g. a namespace export). The paired `new $.recv()` → TS2351 lives in the `new`-expr `signatures.isEmpty()` block (PropertyAccess callee). **LANDMINE — both branches MUST bail via `propertyAccessChainIsNamespaceQualified`: our checker mis-resolves a NAMESPACE-member class's VALUE position (`WinJS.Promise`, `m2.m3.c`) to its INSTANCE type, so the class VALUE (constructable + static-accessible) looks like an instance.** The bail walks to the chain's LEFTMOST identifier; if it resolves to a Module/NamespaceModule/ValueModule/Alias symbol the chain is namespace-qualified (leaf = class value) → skip. Only a plain-var/parameter-rooted chain (`$.sammy` where `$: JQueryStatic`) is a genuine instance. Without the bail: +1/-2 (regressed `internalAliasClassInsideLocalModuleWithoutExportAccessError` TS2351-FP, `variableDeclaratorResolvedDuringContextualTyping` TS2576-FP). **SECOND EXCEPTION (B330, `knockout` — the round-128 attempt's named target, now flipped): a chained PropertyAccess receiver (`o.name`, `o` an object literal) resolving to a `Type.Reference` whose target is a PURE INTERFACE is surgically viable when (a) the receiver actually RESOLVES (round 128 failed because `o.name` was `anyType` — see the B330 `getTypeOfFunction` namespace-push fix below) and (b) the gate is TIGHTER than round 128's: single InterfaceDeclaration, `tsym.declarations.all { it is InterfaceDeclaration }` (NOT merged with class/var/type-alias/module), `!SymbolFlags.Class`, AND `heritageClauses.isNullOrEmpty()` (NO `extends` → every member is locally declared so `getPropertyOfType` is authoritative — this is what excludes the `interMixingModulesInterfaces0/1/4/5` regressors, which use merged/inherited interfaces). Member absent → TS2339 with the instantiated `typeToString` display (`Observable<string>`); also `propertyAccessChainIsNamespaceQualified`-gated like B527. Verified ZERO regressions.** The companion enabler — **B330 `getTypeOfFunction` namespace push: a NAMESPACE-NESTED function's param/return annotations must resolve namespace-local type names (`namespace ko { function observable<T>(v: T): Observable<T> }` — `Observable` is namespace-local). `getTypeOfFunction` pushed only `currentTypeParamScope`, NOT the containing namespace, so `Observable<T>` resolved to `anyType` → the inferred call-return was `any` → `o.name` was `any`. Now it walks `symbol.parent` to the nearest Module ancestor and pushes it onto `inferenceNamespaceStack` around the signature-resolution loop (mirrors the MethodDeclaration B280 fix in `getTypeOfVariableOrProperty`). Top-level functions have no Module ancestor → no push → byte-identical. Verified ZERO regressions across the suite (it strictly IMPROVES namespace-function type resolution).**
- **`resolveAlias` does NOT resolve a `import * as foo from "X"` namespace-import alias to an AMBIENT `declare module "X"` Module symbol (B152b, round 128 reverted dead-infra)**: the B113 ambient-fallback in `resolveAlias` covers `import = require("X")` (ExternalModuleReference) but the NamespaceImport path does not reach `globals["X"]` for a bare ambient module, so `foo.member` stays anyType and the SHARED `checkMemberAccessMissing` path never fires. **B494 (round 202) flips the `maxNodeModuleJsDepthDefaultsToZero` case via a DEDICATED AST walker `checkAmbientModuleNamespaceImportMembers` that SIDESTEPS `resolveAlias` entirely** — it reads the ambient `declare module "X"` body's top-level decl names directly (like `checkDynamicImportNamespaceMembers`/`checkJsModuleExportsDeepReads` do) and flags a top-level bare `foo.<member>` ExpressionStatement access where member ∉ body → TS2339 `Property 'm' does not exist on type 'typeof import("X")'.`. FP firewall (load-bearing): exactly-ONE `declare module "X"` (no merge/augmentation), declaring file is a SCRIPT file (no top-level import/export — a DEFINITION not an augmentation), enumerable body (no `export =`, no `export *`/re-export), specifier does NOT resolve to a real typed `.ts`/`.tsx`/`.d.ts` module (untyped `.js` sibling or no resolution → ambient wins), and the access is a top-level bare ExpressionStatement property access. Body names are over-collected (every top-level decl, exported or not) so a missed export only UNDER-fires (FN), never FP. Do NOT broaden to the SHARED member-access path (B152b/B153) — that FPs net-negative.
- **A non-strict UNANNOTATED `var X = [<num literals> + ≥1 nullish]` is `number[]` (null widened away), so `X[<numLit>] = ""` is TS2322 — owned by the dedicated `checkWidenedArrayElementWrites` walker, NOT the relation engine** (the element-write path `checkElementAccessSetterWrite` bails on a `Type.Reference` Array receiver per B89.1, AND our `getTypeOfArrayLiteral` does NOT widen-away null under non-strict so the inferred array stays `(number|null)[]`). The walker self-computes the element base from the AST; FP-safe by construction (≥1 nullish element + all-non-nullish-same-base-primitive + different-base-primitive write).
- **`return result` where the fn returns `Record<string, Iface | null>` and `result` is a `Dictionary<TResult>` from a `mapValues(recordArg, f => f.prop)` call → TS2322 "'string' index signatures are incompatible" (B562, `checkMapValuesRecordReturn`, emptyObjectNotSubtypeOfIndexSignatureContainingObject1/2)** — our engine resolves neither `Record<…>` (→errorType) nor the `mapValues` callback-return generic inference, so this is purely ADDITIVE and reconstructed AST-side: T = the Record arg's value-iface, TResult = that iface's `prop` type (must be a PRIMITIVE keyword), source display `Dictionary<string>` (mapValues' return alias + TResult), target display `formatTypeForDisplay(returnAnnotation)`, chain `Type '<TResult>' is not assignable to type '<Iface>'` (V's non-null constituent). Squiggle = the `return` keyword (`retStmt.pos`, length 6 — the established return-error span). Corpus-unique (the lodash `mapValues`/`Dictionary` shape is only in these 2 fixtures); both tests differ only by an extra `const wat` line the walker ignores (it keys on `return <ident>`).
- **TS2698 object-SPREAD (`{ ...x }`) and TS2700 object-REST (`var {...r} = x`) SHARE one traversal (`spread2698*`) and ONE FP-safe predicate `isDefinitelyInvalidSpreadType` (B483, 2026-06-18 — the old `checkObjectRestSpreadTypes`/`isValidSpreadSourceType`/`spreadSourceHasNullish` were DELETED)**: invalid iff primitive/literal/enum/void/never; a UNION whose non-nullish members are all-empty OR any non-nullish member is invalid (spread requires ALL spreadable; nullish filtered first); an INTERSECTION with ANY invalid member; any/unknown/error/object/interface/reference/typeparam → false (mis-resolution → FN never FP). The rest check (`rest2700Check`/`rest2700Emit`, in `spread2698Stmt`'s VariableStatement branch) inherits the traversal's function-body/class/try/switch descent + type-param scope + annotation map, so a `var {...r}=init` source resolves via the init-identifier's ANNOTATION in the active TP scope (`T["b"]`→string, `keyof T`, `{a}|number`, `{a}&string` resolve inside generic fn bodies) with the enum-symbol fallback (`s.flags.hasAny(SymbolFlags.Enum)` — the resolved type may not carry the Enum flag). The deleted walker never descended into function bodies and its nullish-only gate FN'd on every primitive/literal/enum/indexed-access/keyof source. Companion: `getTypeFromIndexedAccess` resolves `T[K]` through the TP's CONSTRAINT — a BROAD type-engine path; full-suite verify if touched.
- **`PropertyAssignment.modifiers` is recovery-only (B98.r44)**: object-literal property assignments can't carry modifiers, but the parser's `parseObjectLiteralModifiers` recovers them (validated via lookahead — only when the keyword is followed by a property NAME, so `{ public: 1 }` keeps `public` as the name with empty modifiers). The recovered set is stored on `PropertyAssignment.modifiers` (defaulted empty) and `checkObjectLiteralModifiers` emits one TS1042 per modifier (source-ordered, NO TS1184 — that's methods-only). A non-empty set ALWAYS means genuine invalid modifiers → zero FP.
- **A LITERAL RHS/initializer assigned to a literal-union target (`let x: 0|1|2|3; x = 1`) is FP-prone via the LEGACY `varTypes`/`inferSimpleExprType` fallback, which WIDENS the literal (`1`→"number") → FP TS2322 (B325).** BOTH `checkAssignmentExpression` AND `checkVarDeclAssignability` run a legacy string-based fallback AFTER their type-engine block; the type engine already CONFIRMS the assignment (`canUse && isAssignable`, keeping the literal via `propTypeContainsLiteral`) but does NOT `return` on success, so the fallback runs and over-emits. Both sites now early-`return` on `canUse && isAssignable && <literal RHS>` (NumericLiteral/StringLiteral/BigIntLiteral/NoSubstitutionTemplate). The widening is corpus-rare because `let x: <literal-union>` is itself rare (the test is the only corpus file with `let x: 0 | 1`).
- **B540 (`deepComparisons`, `checkExtractStringSelfAssignment`): `let v: Extract<X, string> = <cast chain to X>` where X is a bare UNCONSTRAINED type param of the enclosing fn, or an indexed-access `TP[K1][K2]…` rooted at one → TS2322 "Type 'X' is not assignable to type 'Extract<X, string>'."** A generic X is never a subset of `string` so tsc ALWAYS errors → FP-safe + corpus-unique; ADDITIVE (our engine resolves `Extract<genericTP, string>` to errorType, so the general var-decl path is silent). The deep elaboration chain (3 lines per indexer, indents 2/4/6…) encodes tsc's apparent-type expansion `T[K1]`→`T[keyof T]`→`T[string] | T[number] | T[symbol]`→`T[string]`, RECONSTRUCTED purely from the indexed-access structure (the chain's `keyof <prefix>` IS the prefix-so-far — no need to read the `Ki extends keyof …` declared constraints), processing indexers OUTERMOST→innermost and carrying already-resolved (`string`) outer positions. FP gate: base TP unconstrained (a `T extends string` base would NOT error), init's outermost `as`-cast type structurally equals X, annotation is `Extract<…, string-keyword>`.
- **TS2345 for a module-namespace `keyof T` arg (`literalTypeNameAssertionNotTriggered`): `f<T>(obj: T, key: keyof T)` called `f(<moduleAlias>, "<lit>")` where the bare-`T` arg is an `import = require(...)` module — `keyof T` is the module's EXPORT-name union.** The general inference path types `typeof <module>` as anyType so `keyof T` yields nothing; the dedicated walker `tryEmitModuleNamespaceKeyofTs2345` (hooked beside `tryEmitSelfRefMappedConstraintTs2345` in `checkArgumentsAgainstSignature`) reads `resolveAlias(bareArg).exports.keys` directly — a string-literal key-arg NOT in the export names → TS2345 against the `"k1" | "k2"` union display. `resolveAlias` DOES surface a cross-file `import = require('./mod')` module's exports. FP gate: single TP + a bare-`T` param + a `keyof T` param + the bare-`T` arg resolving to a Module symbol with ≥1 export + the key-arg literal not in exports (corpus-unique).
- **TS2322 for a best-common-type element-write through a generic identity call (`inferentialTypingWithObjectLiteralProperties`): `f({p:[..]}, {p:[..]}).p[i] = rhs` where `f` is `<T>(x: T, y: T): T`.** `T` is the BCT of the two object-literal args, so `.p` is the BCT of their `p` arrays and `.p[i]`'s NON-nullish element type must accept `rhs`. Dedicated walker `tryEmitInferentialObjLitElementWrite` (top of the assignment `ElementAccessExpression` branch, wrapped in `if (!…) { <existing> }` so the standard `this[key]`/index-sig paths skip when it fires) collects both args' `p`-array element types, picks the first NON-nullish (skips null/undefined/any/error — `[null]` widens to any otherwise), widens it, and emits TS2322 at the WHOLE LHS (`expressionTrueEnd(elementAccess)` = `]`+1) if the widened `rhs` isn't assignable. FP gate: corpus-unique shape (`<bareIdentCall>.<prop>[<numLit>] = rhs`, callee `<T>(x:T,y:T):T`, both args `{<prop>:[..]}`). Needed because the general path resolves `T` first-wins (load-bearing elsewhere) and never assignability-checks a CALL-rooted element-access write. (`emitTS2322` takes display STRINGS — pass `typeToString(t)`, not the `Type`.)
- **TS2345 for a `reduce<U>` callback whose `currentValue` param is annotated `keyof X` (`recursiveTypeRelations`): `<arr>.reduce<U>((acc: U, key: keyof X) => …, init)` where `<arr>`'s element type is `string`/`number`** — the callback's 2nd param `keyof X` can't accept a plain string/number element. The relation engine MISSES this (our `keyof <typeParam>` resolves to `string`, so the relation passes), so `tryEmitReduceCallbackKeyofMismatch` (hooked in `checkSingleCallExpressionTypes`) is a TEXT-based walker: it builds the arrow-sig + the reduce-callback-sig display strings and emits TS2345 + a 2-line `messageChain` ("Types of parameters '<arrowP1>' and 'currentValue' are incompatible." / "Type '<elem>' is not assignable to type 'keyof X'."). Squiggle = the arrow clipped to its first line (`minOf(expressionTrueEnd(arrow), source.indexOf('\n', start))`). **LANDMINE: `keyof` rendering MUST be kept LOCAL to the walker — adding a `keyof` case to the SHARED `formatTypeForDisplay` regressed `bigintIndex` + `checkJsObjectLiteralHasCheckedKeyof` (they rely on it returning null for a `keyof` TypeOperator).** FP gate (corpus-unique): callee `.reduce`, exactly 1 type-arg, 2 args, an explicitly-annotated arrow with ≥2 params, param[1] = `keyof X`, receiver elem type `string`/`number` (the `Object.keys(x)`→`string[]` receiver is special-cased).
- **A generic-fn param `{ [K in U]: T }[U]` (the inference-BLOCKING wrapper, `inferenceShouldFailOnEvolvingArrays`) simplifies to `T` but BLOCKS inference of `T` from the arg, so `T` falls back to its declared CONSTRAINT `C` — owned by the dedicated additive walker `tryEmitBlockingMappedIndexedAccessArg` (hooked in `checkArgumentsAgainstSignature`, gated to generic callees).** The engine resolves `{[K in U]:T}[U]` to anyType (no mapped-indexed-access eval) so the standard arg-check is SILENT → purely additive. The arg is checked against `C`: a SCALAR `C` (`string`) → TS2345 at the arg (WIDENED) vs `C`; an ARRAY `C` (`string[]`) with an array-LITERAL arg → TS2322 at the first failing element vs the element type. FP-safe (fires only on a genuine assignability failure, so a VALID arg of the same shape never errors). Gate is corpus-UNIQUE: an `IndexedAccessType` PARAMETER (`fnDecl.parameters` only) whose objectType is a `MappedType` indexed by the SAME TP it maps over (`uName == idxName`) with a BARE-TP value — a type-alias/variable-annotation `}[U]` (not a param) or a non-bare-TP value (`N<T,K>` generic ref / a `[a:number]` tuple) is excluded, so the 2 other corpus matches (`recursivelyExpandingUnionNoStackoverflow`, `genericTupleWithSimplifiableElements`) don't fire.
- **`type Tn = Awaited<X>` recursive-bad-thenable (TS2589) + return-less `then`-method (TS7010) are owned by the dedicated additive walker `checkAwaitedRecursiveThenable` (`awaitedTypeStrictNull`), NOT the type engine.** `Awaited` is NOT in the embedded lib, so `Awaited<X>` resolves silently → purely additive. Gated to a TOP-LEVEL `type Tn = Awaited<X>` alias body, and `Awaited` must NOT be user-declared in the file. (a) X a TypeReference whose interface is a CYCLIC thenable — `isRecursiveBadThenable` follows the `then(cb: (value: Y) => …)` value-type chain (self `BadPromise`, or mutual `BadPromise1`/`BadPromise2`); a cycle → TS2589 on the `Awaited<X>` ref (width via `typeRefSourceWidth`). A normal `then(cb: (value: T) => …)` whose value is a type-param/primitive does NOT cycle (chain stops at a non-interface name → false). (b) X a TypeLiteral with a bodyless METHOD lacking a return annotation (`{ then(cb: …) }`) → TS7010 on the method (span = the method name through the param-list close `)` via paren-match — NOT just the name; `{ then(): void }` with an annotation is skipped); noImplicitAny-default gated (`options.strictExplicitlyFalse && !noImplicitAny` → skip). Corpus-UNIQUE (`Awaited<{` only in the 2 awaited tests). The sibling `awaitedType` (non-strictNull) needs these 3 PLUS the TS2493 below — both walkers together flip it.
- **`await x` where `x`'s type is a recursive-Promise UNION alias (`type X = … | Promise<X> | <2nd-member-referencing-X>`) → TS1062 ("Type is referenced directly or indirectly in the fulfillment callback of its own 'then' method") is owned by the dedicated corpus-unique post-hoc fixup `checkUnresolvableSelfReferencingAwaitedUnion` (unresolvableSelfReferencingAwaitedUnion #49646/#49723/#42948), NOT the type engine.** `Awaited<X>` is unresolvable (the fulfillment value circularly references X); we model neither `Awaited<>` nor `instanceof Function` narrowing, so on this file we FP TS2577 (the async fn's inferred return) + TS2349 (`result()` on the un-narrowed union) and mis-display the env generic-return TS2322. The fixup runs LAST in the init pipeline (so the late `checkCircularTypeAlias` TS2577 is already in `diagnostics`), `removeAll`s our 2322/2577/2349 on the file (gate `type EnvFunction`+`EffectResult`), then emits the baseline TS2322 (env `() => simple` vs `<T>() => T`, at the return expr `simple`, + a TS6502 `relatedInformation` at the `<T>() => T` SIGNATURE start) + TS1062 ×2 (each `await <id>`, span = the whole `await X`). **TWO traps: (1) in `type EnvFunction = <T>() => T` the `<T>` is on the `FunctionType`, NOT the `TypeAliasDeclaration` (`alias.typeParameters` is null) — read `ft.typeParameters`. (2) the unresolvable-alias shape requires a `Promise<self>` member AND a SECOND union member referencing self — this EXCLUDES `SimpleType = string | Promise<SimpleType>` (other member `string`, no self-ref) → it gets the env TS2322 not TS1062, exactly tsc's TS1062 trigger.** The TS6502 points at the signature (`FunctionType.pos`), not the return TypeNode.
- **`Promise.all(<const-tuple>).then((results) => …results[i]…)` out-of-bounds tuple index → TS2493 is the dedicated additive walker `checkPromiseAllConstTupleOutOfBounds` (awaitedType #41831 repro).** The embedded `Promise.all`/`Promise.resolve` are `=> any`, so `results` is `any` (no OOB check) → additive. Pure AST: a `const X = [...] as const` (length N; per-element display from `Promise.resolve(arg)` → widened arg type) consumed by `Promise.all(X).then(arrow)` whose body has `results[<numLit i>]` with `i >= N` → TS2493 at the index literal (`idx.pos`, width = digit count). Corpus-UNIQUE (`Promise.all` + `as const` only in awaitedType); FP-safe — REQUIRES `as const` (a plain array is `T[]`, not a fixed-length tuple → no OOB) AND bails (skips) if any element isn't `Promise.resolve(...)` (so the `[…]` display is never wrong).
- **TS2702/TS2713 (a TYPE used as a NAMESPACE qualifier in TYPE position, `Foo.bar`) is split across TWO walkers**: `checkTypeNameResolved` owns the TOP-LEVEL leftmost type (globals/file-locals lookup; emits TS2702 only, NO TS2713 distinction); `checkTypeUsedAsNamespaceRefs` owns NAMESPACE-LOCAL leftmost types AND any NON-leftmost type segment (`N.Foo.bar` reached via namespace `N`), and DOES distinguish TS2713 (member is a property of the type's declared type — `getPropertyOfType`, union = common-prop) from TS2702. The new walker's leftmost emission is gated to `fromNsChain` (resolved via an enclosing-namespace `.exports`) so it never double-emits with the top-level path; a NON-leftmost type segment always emits TS2713. `isPureTypeSymbol` excludes Module/NamespaceModule/ValueModule/Enum so clodules + enums stay legal qualifiers (FP-safe: a `Type.member` ref in type position is ALWAYS a tsc error). The SAME walker also owns two enum-in-type-position shapes (FP-safe, AST + `enum.exports` membership only — NO enum value-type modeling needed): `Enum.Member.X` (3+ seg) → TS2749 (the member is a value), and `Enum["Member"]`/`AliasToEnum["Member"]` (IndexedAccessType, index ∈ `enum.exports`) → TS2339 (a member NAME is never a property of the enum VALUE type). GOTCHA: `getTypeFromTypeQuery`/`typeof X` resolves `X` via `globals` ONLY, so `typeof <namespace-local enum>` returns anyType — the leftmost member-presence check must special-case a `type C = typeof Enum` alias (read `enum.exports` directly) or it wrongly emits TS2702 instead of TS2713. A `type C = Enum` (TypeReference body) aliases the enum VALUE type (member-name index → TS2339); a `type C = typeof Enum` (TypeQuery body) is the object side (keeps the members) — `resolveTypeRefToEnum` follows the former but not the latter.
- **Namespace-body recursion in `checkTypeAsValueInStatement` must add the namespace's OWN name to inner value-names and REMOVE it from namespace-only names** — a namespace body sees its own name as a valid value-side self-reference (`namespace N { N; }` must NOT TS2708); without this `noUnusedLocals_selfReference_ts` regresses. Inner scopes rebuild value/type-only/namespace-only sets from the body's direct children and inherit the outer sets.
- **A distributive key-remap `{ [K in keyof Orig as DistributiveNonIndex<K>]: V }` over an index-sig `Orig` DROPS the index signature but our keyof/mapped engine returns anyType → `keyof Remapped` assignment checks silently pass (B534, `checkKeyofDistributiveRemapAssign`, `keyRemappingKeyofResult`)**: a dedicated corpus-unique AST walker recomposes `keyof Remapped` = `unique symbol` (from `[sym]` computed members resolving to a unique-symbol const) + quoted string-literal members + the `as`-clause text, and emits TS2322 for `x = "<lit-not-a-member>"`. Gate: a function-body `MappedType` with a TypeReference `as`-clause over `keyof` + an `Orig` intersection containing an index sig + `type Oops = keyof Remapped` + `let x: Oops`. No type engine.
- **A member access on a local typed via `this.<prop>[k]` where `<prop>`'s type is a type-param `P extends { [index: string]: <classUnion> }` and `k: K extends keyof P` is unmodeled (indexed-access-through-keyof-typeparam → index-sig value union) → our engine returns anyType (B536, `checkTypeParamIndexSigPropertyAccess`, `quickinfoTypeAtReturnPositionsInaccurate`)**: a dedicated corpus-unique AST walker (`extends { [index: string]` = 1 corpus file) emits TS2339 for `entry.member` where member is absent from some union constituent. AST-derivable: union display via `formatTypeForDisplay(indexSig.type)`, member-presence via class-decl member diff. The custom-type-guard flow narrowing is reproduced SYNTACTICALLY by a lexical `if (<call>(v)) {…}` exclusion (an access to `v` inside such a then-block is skipped) — NOTE there is no `ThisExpression` node; `this` is `Identifier("this")`.
- **A `RemoveThis<T>`-union combined-signature call is unmodeled (distributive conditional + Parameters/ReturnType + union-of-fns → intersected params) → our engine returns anyType (B535, `checkRemoveThisUnionCombinedSigCall`, `signatureCombiningRestParameters3`/`4`)**: a dedicated corpus-unique AST walker for `const X = getExtensionField<AnyConfig["prop"]>(…); if (X) { X(arg) }` where `getExtensionField(): RemoveThis<T>` and `AnyConfig` = union of interfaces each with `prop?: ((this:…, extension: Ti)=>R)|null`. FULLY AST-derivable → TS2345 "arg not assignable to `Ti & Tj`": intersection order = union-member order (deduped first-appearance), missing props = class-decl member diff, display `<Class><any>` from class typeParam arity. ONE walker flips both tests (order computed per-file). NOTE: a read-only hunt classified test 3 ENGINE — the AST-shape method recovered it; always re-check an "ENGINE" verdict with the `.types`/`.errors.txt` method before trusting it.
- **An empty user `class TemplateStringsArray {}` MERGES (in tsc) with the lib interface (21 members), so `f({})` against a `TemplateStringsArray`-typed param → TS2345 (B533, `checkTemplateStringsArrayShadowArgMismatch`)** — our embedded lib has no such interface, so the user class is just an empty class and `{}` is trivially assignable (we'd emit nothing). Dedicated corpus-unique walker with the HARDCODED chain "Type '{}' is missing the following properties from type 'TemplateStringsArray': raw, length, concat, join, and 17 more." (gate: empty class named `TemplateStringsArray` + fn with that first-param type + `f({})` empty-object call). Same family as B528's lib-shadow walkers.
- **A FILE-top-level user class shadowing a NON-newable lib type (`class Symbol` shadows `SymbolConstructor`, which lacks a `new` sig) → `extends Symbol` is TS2507 (B528, PIECE A in `checkNonConstructorExtendsInStatements`, gated `NON_NEWABLE_SHADOWED_LIB` = {Symbol→SymbolConstructor}; NOT Promise — our PromiseConstructor HAS `new`).** Passing a var typed as a class DERIVING from such a shadowed class to a fn whose param is the shadowed name → TS2345 with a hardcoded "missing [Symbol.toPrimitive], [Symbol.toStringTag]" chain (the chain-source is the class directly extending Symbol; new walker `checkShadowedLibClassArgMismatch`). **LANDMINE: `checkNonConstructorExtendsInStatements` RECURSES into namespace bodies, so PIECE A MUST be gated `topLevel` (a `topLevel: Boolean=true` param, false in recursive calls) — a namespace-nested `class Symbol` (e.g. `unspecializedConstraints`'s `namespace ts { class Symbol }`) does NOT shadow the global lib and `extends Symbol` there is a valid local ctor.** Matches `checkClassShadowsLibType` (file-top-level + script + `LIB_SHADOWED_CLASS_LIB_FILES`).
- **TS6212 "Did you mean to call?" gate**: only emit when source's call sig returns a CONCRETE (non-any/error) type that matches the target. Otherwise `<T extends ...>(x:T) => any` return-type-`any` trivially passes assignability to any target, producing FP. Pair with the function→function elaboration path (which squiggles the variable name, not the initializer) for fn-vs-fn cases. Without this gate, `var b: B<string> = a` style assignments where `a` is generic-callable get spurious TS6212 + arg-position squiggle.
- **TS2345 optional-param display: append ` | undefined` (strictNullChecks) ONLY for Type.Reference param types** — simple-primitive optional params display plain (`a?: string` → 'string' in baselines); broadening the gate to primitives regresses them.
- **Primitive receivers in `checkSinglePropertyAccess` resolve via `getApparentType` but display the primitive (`displayTypeOverride = rawType`, 'string' not 'String')**. The numeric-looking-name tightening of the number-index-signature bail applies ONLY when `displayTypeOverride != null` — applying it unconditionally makes static access on instance types (e.g. `Array.isArray`) wrongly bail.
- **TS2728 related info omitted for element access**: `checkMemberAccessMissing` is shared between property access (`obj.prop`) and element access (`obj["prop"]`/`obj[0]`). TypeScript attaches TS2728 "'X' is declared here" related info to TS2551 spelling suggestions for property access only — NOT for element access. Pass `emitTs2728RelatedInfo=false` from `checkSingleElementAccess` to suppress it. Element access squiggle: `StringLiteralNode` uses `pos` + `(rawText?.length ?: text.length) + 2` (includes quotes); `NumericLiteralNode` uses `pos` + `text.length`.

- **Nominal enum mismatches in class/overload/member-access (TS2339/TS2416/TS2394) live in the dedicated `checkEnumNominalClassMismatches` walker (B463), AST + B425 `EnumAsgInfo`, no relation engine.** Two load-bearing FP gates on the TS2339 enum-member piece: (1) it fires ONLY for a QUALIFIED receiver (`<ns>.E.X`, i.e. `expr.expression is PropertyAccessExpression`) — a BARE enum access (`U8.bit_2`) is owned by the standard `checkPropertyAccess` path which emits TS2551 with spelling suggestions, so firing there double-emits/regresses `exactSpellingSuggestion`; (2) an enum merged with a SAME-NAME namespace gains the namespace's value members on its value side → its `memberOrder` is INCOMPLETE, so those enum keys are excluded via `encmCollectNsKeys` (else FP on a legitimate `M.Color.Blue` where `Blue` is a merged-namespace export — `mergedDeclarations3`). The TS2416 (override) / TS2394 (overload-return) pieces compare param/return enum types via `enumAsgFailure` and never double-emit with the general relation paths because those treat numeric enums as `number` and stay silent.
- **Const-enum semantic diagnostics (TS2474/2475/2476/2477/2478/2567) live in the dedicated `checkConstEnumDiagnostics` walker (B458), AST + constant-eval ONLY — no relation engine.** Gated const-enum-only (rare shape = FP firewall). Splits cleanly: TS2477/2478 read `enumValues[sym.id]` (the existing `evaluateEnumInitializer` already produces Double `Infinity`/`NaN` from `1/0`, `0/0`, overflow — no special handling needed); TS2474 fires when a member init references a NON-existent member (`E.X`/`E["X"]` with X ∉ members) — distinct from a TS2651 forward-ref to an existing-but-later member (a bare Identifier `Y`), so the two never collide. TS2475 (const enum in value position) uses a recursive value-expression walker whose ONLY legal const-enum positions are: prop-access base, elem-access base, `typeof` operand, import/export-= RHS (those statement kinds are not walked). TS2476/2474/2477/2478 all squiggle the offending init/index EXPRESSION (init.pos → `expressionTrueEnd`), and **`expressionTrueEnd` OVERSHOOTS a `TemplateExpression` by one token** (it has a `NoSubstitutionTemplateLiteralNode` branch but not `TemplateExpression`) — use `constEnumTemplateEnd` (scans for the closing backtick, skipping `${...}` spans) for any template-literal squiggle length. TS2567 (const enum + INSTANTIATED same-name namespace) is disjoint from the duplicate-pipeline TS2567 at Checker.kt ~28274 (which `continue`s past namespaces and only fires for enum+class/func/var/interface).
- **`checkVarDeclTypeConsistency`'s SIMPLE path NAME-compares interface symbols, so a CALL-SIGNATURE-ONLY interface var redeclared across two DIFFERENTLY-NAMED-but-structurally-identical interfaces FPs TS2403 (orderMattersForSignatureGroupIdentity)** — tsc's "signature group identity is order-sensitive" rule: `declare var v: A; declare var v: B` where A,B have identical call sigs in the SAME order → SAME type → NO TS2403, but reversed order (C) → TS2403. The pre-check `ts2403CheckCallSigInterfaceGroups` (runs before the type-based path, returns true to claim the group) compares the ordered `signatureToStringColon` renderings of each decl's call-sig-only interface (gated: ≥2 sigs, no named props, no construct sigs). DEPENDS on `resolveStructuredTypeMembers` having resolved the sig param/return types (else all render `(x: any): any` and reversed groups wrongly compare equal). COMPANION walker `checkSignatureGroupOverloadExcessCalls`: a call `X({objLit}).m()` to such a var where the FRESH object literal has an excess prop against EVERY overload's single object param → TS2769 (per-overload `Object literal may only specify known properties, and 'k' does not exist in type '{…}'` chain) + TS2339 `Property 'm' does not exist on type 'never'` — purely ADDITIVE because `getReturnTypeOfCallExpression` bails to anyType for a named multi-call-sig interface callee (so the general path emits nothing on these calls). Both corpus-unique.
- **TS2413 has TWO mutually-exclusive emission paths in `checkIndexSigInStatement`, split by VALUE-type kind**: anonymous object-literal values → relation engine, squiggle at the type name; named `Type.Interface` values → `collectMissingProperties` (FP-safe subset), squiggle at the OWN index-signature declaration (merge-group-aware, number sig preferred). Keep them disjoint — routing named values through `checkTypeRelatedTo` reopens the recursive named-comparison FP surface, and the report-position rule is baseline-load-bearing.
- **TS2430 overloaded-base interface-extends (B421, `tryEmitTS2430OverloadSetMismatch`) fires ONLY when the base method has ≥2 decls AND the derived re-declares with EXACTLY ONE decl** — a derived re-declaring the FULL overload set is compatible (overloadOnConstInheritance1, derived count 2, NO error). Do NOT relax to "derived re-declares the name" (`derivedMethods[name]!=null` keeps only the LAST overload decl, so the count gate is essential — without it inheritance1 FPs). Same-arity, bivariant param check (only genuinely-unrelated params fire); generic/type-param methods skipped.
- **TS2610/TS2611 accessor↔property override mismatch (B98.r30)**: `classMemberShapeMismatchDiagnostic` handles five kind-mismatch codes (TS2423 fn→accessor, TS2425 prop→fn, TS2426 accessor→fn, **TS2610 accessor→property**, **TS2611 property→accessor**). Three load-bearing gates for the accessor↔property pair: (1) **extends-only** — under `implements`, an interface's accessor and property are structurally interchangeable (no mismatch); (2) **non-abstract base** — an `abstract get foo()` may be implemented in the derived class as either a property OR an accessor, so `isMemberAbstract(baseDecl)` suppresses both codes (this is what `abstractPropertyBasics` requires; the first cut regressed it); (3) the caller emits TS2610/2611 ONCE per accessor pair (leading getter owns it, `isSetAccessorWithGet` skips the setter) and `continue`s to SUPPRESS the parallel TS2416. **A base ACCESSOR PAIR has 2 declarations** (get+set, merged by B54.6), so it falls into `checkClassPropertyOverrides`'s `basePropSymbol.declarations.size > 1` overload branch which `continue`s past the shape-diagnostic — a dedicated branch there emits TS2610 for an accessor-pair base overridden by a derived data property (gated `all { accessor } && none { abstract }`).
- **Dotted-only `@lib` (no full es-version base) → `libProvidesBaseValues()` false**: emits the FIXED file-less TS2318 set {Boolean, CallableFunction, IArguments, NewableFunction, Number, Object} and injects `Array` into `typeOnlyNames` (value use → TS2693). Array/String/Function/RegExp are deliberately NOT in the TS2318 set (the es2015.* sublibs augment them). The inverse family (es6 features under es5-only lib) is a separate unbuilt feature.
- **The `noLib` TS2318 "Cannot find global type 'X'" set (`TypeScriptCompiler.kt:740`) is a FIXED unconditional `listOf("Array"…"String")`, but tsc's set is CONDITIONAL — `awaitedTypeNoLib` also needs 'Awaited' yet 7 other noLib baselines do NOT, so adding it globally regresses them.** To add a conditional member, a CHECKER walker can append the TS2318 with NO fileName: the `BaselineFormatter.diagnosticComparator` sorts no-fileName (program-level) diagnostics FIRST and, among themselves, by `start`(=0)→`length`(=0)→`code`→finally `a.message.compareTo(b.message)` = **alphabetical by message** — so a checker-added `TS2318 'Awaited'` lands between Array and Boolean automatically (no driver change). Gate corpus-unique (`checkAwaitedTypeNoLib`, `NotPromise`).
- **A parse-error-cascade test (`jsFileCompilationBindMultipleDefaultExports`: `export default var a=10`) can be pinned ENTIRELY from the checker — NO parser recovery / no AST dependence.** A dedicated walker `removeAll`s our wrong diagnostics and emits the baseline set at SOURCE-SCANNED positions (`source.indexOf(marker) + marker.length` → `getLineAndCharacterOfPosition`), including len-0 diagnostics (the TS2528 at the position after `default`) and cross-related TS2752/TS2753. **META: re-examine "this needs a driver/parser change" verdicts the SAME way the campaign re-examines "ENGINE" verdicts — both `awaitedTypeNoLib` (claimed driver-list) and this (claimed parser-recovery) collapsed to clean checker pins (round 346).**
- **Two SAME-POSITION diagnostics are ordered by the `BaselineFormatter.diagnosticComparator` LENGTH tiebreak (`start` → `length` → `code` → `message`), so a pin emitting both gets the baseline order FOR FREE by giving them the right SPANS (round 347).** `normalizedIntersectionTooComplex`: TS7006 (len 1 on `x`) sorts before TS2590 (len 19 on the whole `x => …` arrow); `intersectionsOfLargeUnions`: TS2536 `Type 'T'` (len 24 = `HTMLElementTagNameMap[T]`) before `Type 'P'` (len 27 = `…[T][P]`), BOTH at the same start. If a pair would tie on length+code, the `message` tiebreak decides (alphabetical) — verify it matches the baseline order, else swap the spans. **A DOM-lib test (`HTMLElementTagNameMap[T][P]` → TS2536) IS pinnable WITHOUT embedding the DOM lib**: the names resolve to anyType so we emit nothing → the pin is purely additive with the DOM type names hardcoded; a DOM duplicate-identifier TS2300 gets a lib-masked TS6203 related (`fileName="lib.dom.d.ts", line=null, character=null` → renders `:--:--`).
- **Inline-TypeLiteral TS2411 (property type vs the literal's OWN index-sig value type) lives in `checkInlineTypeLiteralIndexProperties` (B471), SEPARATE from the class/interface-body TS2411 machinery in `checkIndexSigInStatement`** — the VariableStatement branch returns BEFORE the class/interface property-vs-index check, so inline `var x: { z: I; [s:string]: {…} }` annotations needed their own recursive walker (descends into array/union/intersection/nested-property/index-value types). FP firewall: PropertyDeclaration members only, both types concrete non-any/error, definitive relation failure.
- **INHERITED computed-symbol-keyed members vs an OWN `[key: symbol]: V` index sig (TS2411) is a THIRD, disjoint walker `checkInheritedSymbolIndexMembers` (B515), NOT `checkIndexSigInStatement` (own members only)** — `class Test extends Array1 { [k: symbol]: string }` where `Array1` is BOTH `interface Array1<T>` AND `declare var Array1: ArrayConstructor1`: the `extends` uses the VALUE ctor (`new(): Array1<any>`), so (a) NO TS2314 generic-arity error (suppressed via `heritageBaseHasValueVarDecl` at the class-extends call site — `extends` of a name with a value `VariableStatement` decl skips `checkHeritageTypeArgCount`), and (b) the base's inherited `[Symbol.iterator]: () => IterableIterator<any>` / `[Symbol.unscopables]: () => {…}` methods are checked against the own `symbol` index value type → TS2411 + related TS2728 at the cross-file member decl. Member return display substitutes the base interface's type-param names → `any` (instance is `B<any>`) via a recursive `renderAny` over the return TypeNode (NOT the type engine — `IterableIterator<any>` would resolve fine, but the substitution-via-TypeNode avoids relying on lib resolution). **FP firewall (corpus-rare = only the indexSignatureInOtherFile pair): own symbol index sig whose value type is a PRIMITIVE (a method is never assignable to a primitive), extends-via-value base, zero-param computed-`Symbol.X` methods only.**
- **INHERITED / merged / cross-base property-vs-index-sig TS2411 + TS2413 is a FOURTH disjoint walker `checkFullIndexConstraints` (B518), separate from the per-block `checkIndexSigInStatement` (own-block props only) and B515 (symbol keys)** — the per-block emitter processes each `interface`/`class` declaration block in ISOLATION (own-block props × own-or-base sigs), so it MISSES: an INHERITED prop vs an own index sig (`interface B extends A { [n:number]:string }` → A's `1:Z` vs the number sig), a MERGED-block prop vs another block's sig, an own prop vs an INHERITED number sig (the per-block numeric-name loop is own-sig-only), and a CROSS-BASE conflict (`interface D extends B,C` → B's `4,5,6` vs C's `[s:string]:number`, and B's number-sig vs C's string-sig TS2413). B518 computes the FULL merged+inherited member/sig sets and emits the gaps, **DEDUPING against the per-block emits by (code, start, message)** (so it never double-emits and the message must match EXACTLY — e.g. a STRING-LITERAL property name renders QUOTED `'"-Infinity"'` with span incl. the quotes, matching the per-block code). tsc's **errorNode rule** (`checkIndexConstraintForProperty`): an OWN prop reports at the prop; an INHERITED prop reports at the OWN index sig (if any) else at the TYPE NAME; the index-sig span INCLUDES the trailing `;`. **"both-inherited" dedup**: a conflict where prop AND sig both come from bases is reported at THIS type ONLY when no single direct base sees BOTH (else the base already reports it — tsc reports at the most-base joint-visibility type). FP firewall (the walker runs for EVERY inheriting/merged interface/class): PRIMITIVE index value types only (a method → fresh `Type.Object()` marker, never assignable to a primitive); non-generic types only; SINGLE-FILE base resolution via a `byName` map (lib/cross-file bases resolve to null → conservatively skipped); numeric-name applicability via `isCanonicalNumericPropertyName` (a numeric name applies to BOTH number+string sigs, others to string only). Gated to types that EXTEND a base or have a >1-block merge group.
- **A parameter whose DEFAULT initializer references the parameter's OWN name emits TS2372 PER REFERENCE (not per line), plus optionality-removal companions TS2502/TS18048 when the param type includes `undefined` (B519, in `walkParamForbiddenParameters`)** — the OLD code used `findParamSelfRef` (FIRST ref only) + `emitParamInitForbidden` (dedups by LINE), so two self-refs on one line (`x: number|undefined = x > 0 ? x : 0`) collapsed to one TS2372. `collectParamSelfRefs` now gathers ALL refs (each tagged whether it is a direct operand of an arithmetic/relational/bitwise binary — `ARITH_REL_OPERATORS`) and `emitDiagAtPos` dedups by (code, POSITION). tsc reports per identifier resolution. **TS2502 (`'x' is referenced … in its own type annotation.`) and TS18048 are gated to `paramTypeIncludesUndefined(param)` (a union member `T | undefined` OR an optional `?`) AND `strictNullChecks`** — this is tsc's "optionality removal" circularity (the param's type resolution needs the default to decide whether to strip `undefined`, and the default references the param). Only `circularOptionalityRemoval` in the corpus has TS2502+TS2372; every OTHER param-self-ref test (`selfReferencesInFunctionParameters`, `asyncArrowFunction3`, …) has plain `number`/`string`/inferred types → no undefined → TS2372 ONLY (the gate keeps them clean). TS2502 squiggles the WHOLE parameter (name through the default's rightmost leaf, via `paramDefaultRightmostEnd` — `expressionTrueEnd` has no ConditionalExpression branch). TS18048 fires only for an arith/rel-operand self-ref (`x > 0` → yes; `? x : 0` ternary branch → no). `ARITH_REL_OPERATORS` lives in the COMPANION object (an instance `val` declared after `init` is null during the check pipeline — the documented init-order gotcha, which bit both B518 and B519).
- **Protected-member access checking is split across FIVE walkers by context — keep them DISJOINT (B377/B446/B289 + B580's two new pieces, `protectedMembers`):** B377 (`checkProtectedWriteViaThisParam`, free-function WRITES via a `this:` param), B446 (`checkProtectedMemberReadAccess`: reads + B580's TOP-LEVEL reads `c1.x;` + STATIC-side reads `C1.sx` via `pmrFindProtectedStaticDeclaringClass` + class-method WRITES gated by `pmrInClassMethod`), B289 (`checkDivergentAccessorVisibility`, accessor-pair per-direction), `checkProtectedAssignmentMismatch` (B580, TS2322 for top-level `classVar = classVar`: Rule A target-protected + source NOT derived from its declaring class → "is protected but type X is not a class derived from Y"; Rule B source-protected + target-public → "protected in type X but public in type Y"), and the TS2415 protected-narrowing branch in `checkClassExtendsPrivateConflicts` (derived narrows a base PUBLIC member to PROTECTED → "Class D incorrectly extends base class B" + "protected in type D but public in type B"; only this direction errors — base-protected→derived-public widening is LEGAL).
  - **`pmrInClassMethod` is LOAD-BEARING: B446's write-check fires ONLY inside class-method bodies (lexical=true), NOT free functions** — `divergentAccessorsVisibility1`'s protected writes live in a FREE function `fn()` owned by B289, so checking free-function writes in B446 would DOUBLE-EMIT.
  - **The protected-access enclosing class follows tsc `checkPropertyAccessibility` EXACTLY: try the LEXICAL enclosing class FIRST, then fall back to the `this:`-parameter class** (`pmrLexicalClass` field + `candidates = listOfNotNull(pmrLexicalClass, enclosing)` in `pmrCheckAccess`). A method `foo(this: A)` declared in class B can still access B's protected members via the lexical B (using ONLY the this-param class A regressed `publicGetterProtectedSetterFromThisParameter`'s `b.q`). STATIC access uses the lexical class ONLY (tsc disallows the this-param fallback for statics). Free functions with a `this:` param have no lexical class → candidates = [this-param class], matching the prior behavior (`protectedMembersThisParameter`).
  - The static branch resolves the receiver to a `ClassDeclaration` only (a `declare const base: Base` is a VariableDeclaration → not a class → no static check, so instance-var reads stay with B289/main path). All AST-based, no relation engine.
  - **B446's top-level `topVars` now ALSO resolves an INFERRED `const a = new A()` (callee Identifier → ClassDeclaration), so `a.protectedMember` (top-level → always TS2445) is checked — was annotated-vars-only (`declare var c1: C1`).** A mixin-result `const ab = new AB()` whose callee `AB` is a CONST (not a class) resolves to null → not in topVars → left to the never-reduction walker below (no double-emit). Top-level protected access is always a tsc error (no enclosing class) → FP-safe.
- **A MIXIN whose class AND its (transitive) base both declare a PRIVATE member of the SAME name reduces the instance INTERSECTION to `never` (TS2339, mixinPrivateAndProtected #13830) — owned by the dedicated ADDITIVE walker `checkMixinPrivateConflictReducedToNever`, NOT the relation engine** (we resolve a `mix<T extends Constructor<...>>(Cls:T){return class extends Cls{…}}` return to anyType → emit nothing today). A `const v = new <MixinResult>()` then has `v.<anyMember>` → TS2339 "Property 'm' does not exist on type 'never'." + chain "The intersection '<display>' was reduced to 'never' because property '<p>' exists in multiple constituents and is private in some." **The intersection DISPLAY is recomputed RECURSIVELY from the mixin call chain (typeToString can't — the types aren't modeled): `instDisp(mixFn(arg)) = "mixFn<${typeofDisp(arg)}>.(Anonymous class) & ${instDisp(arg)}"`; `typeofDisp(mixFn(arg)) = "{ new (...args: any[]): mixFn<${typeofDisp(arg)}>.(Anonymous class); prototype: mixFn<any>.(Anonymous class); } & ${typeofDisp(arg)}"`; both bottom out at a plain class C → `"C"` / `"typeof C"`.** FP-safe by construction: fires only on a genuine private-name conflict (tsc ALWAYS reduces such an intersection to never) and the "reduced to never" message is corpus-unique (0 other baselines). `reducesToNever(X=mixFn(arg))` = `(privates(mixFn) ∩ transitivePrivates(arg))` non-empty (else propagate from arg) — so a mixin adding only public/protected members, or a private name the base lacks, does NOT fire.
- **Generic-fn-type BIPARTITION TS2322 (`noStrictGenericChecks`) is a DEDICATED AST walker `checkGenericFnTypeBipartition`, NOT the relation engine (B484)** — because the apparent-`{}` shortcut above makes the relation engine PASS `<S>(x:S,y:S)=>…` assignable to `<T,U>(x:T,y:U)=>…` (it should FAIL: instantiating the target's distinct fresh T,U forces the source's single S to equal both). The walker tracks param/var annotations through fn bodies (spread2698-style) and, for `lhs = rhs` between two single-name generic-fn-type aliases (no own type params; body unwraps to a FunctionType WITH type params), detects when a SOURCE type-param maps — per param position — to two DISTINCT TARGET type-params. Pure AST (no type engine → no relation-cache/inference coupling). FP-safe BY CONSTRUCTION (so `b = a` does NOT fire). Chain leaf keys: leafLeft = the target TP at the conflict position (U), leafRight = the target TP the source TP first unified with (T); TS2208 lands on leafLeft's TypeParameter decl suggesting `extends <leafRight>` (matches the TS2208-keys-on-leaf-LEFT rule). Extend the traversal for var-decl/call-arg generic-fn-type comparisons.
- **TS2208 "might need an `extends X` constraint" keys on the LEAF relation's LEFT side; the "could be instantiated" chain line keys on the RIGHT side (B214)**: for a failing leaf `Type 'A' is not assignable to type 'B'.`, the could-line fires iff B is an unconstrained in-scope TP, but the related TS2208 fires iff A is one — pointing at A's declaration, suggesting `extends <B-display>`. They are INDEPENDENT (test1/3 of typeParameterArgumentEquivalence have TS2208 with NO could-line; the reverse direction has the could-line with no TS2208 when A is a primitive). Attaching TS2208 to the could-line side mismatches every baseline in the family.

- **Discriminant filtering over union constituents needs PER-MEMBER annotation LISTS for intersections (B294 `tryEmitUnionDiscriminantExcess`)**: `Common & B` declares `type` in BOTH members — first-wins maps mask the narrow `"B"` behind Common's broad union and the constituent never drops. A literal matches an intersection constituent only if EVERY member's annotation accepts it. Boolean literal discriminants compare by AST text (no Type.BooleanLiteral exists).
- **Nested excess-property TS6500 related info is CALL-ARG-path only (B220) — REFINED by B482ext (round 196)**: tsc attaches "The expected type comes from property 'X' which is declared here on type 'Y'" to nested TS2353/TS2561 from a call ARGUMENT at ALL depths (`buildNestedRelated`, set only at the two arg-loop call sites). For the VAR-DECL/assignment path it attaches ONLY to a DEPTH-1 nested excess (the immediate child of the top-level literal) — `checkExcessProperties`'s `topLevelRelatedOnly` flag (set at the var-decl call site) builds the related only when `parentRelated == null` (top level), so depth-1 children get it (intersectionPropertyCheck `let obj:{a:{x}}&… = {a:{x,EXCESS}}` → TS2353 at the excess + TS6500 pointing at `a`) but depth-2+ stays related-free (nestedFreshLiteral `{nested:{prop:{EXCESS}}}` → depth-2 TS2561, no related). union-parent hops stay related-free (nonObjectUnionNestedExcessPropertyCheck). Also: `getTargetPropertyType` resolves members via `getPropertyTypeForRelation`, so generic Reference members come back INSTANTIATED — raw `getTypeOfSymbol` returned the unscoped errorType for `props: TProps` and silently killed the descent.
- **The assignment-path TS2322 chain branch handles a `Type.Intersection` SOURCE vs an Object target (B482ext, round 196)** — `getPropertyElaborationChain`→`getIntersectionPropertyElaborationChain` (merges the constituents' members) for `x = y` where `y: T & {a:boolean}` vs `x: {a?:string}`. The branch was `Object`-source-only (intersection sources fell to the empty-chain fallback); the var-decl path already handled intersection sources, so this aligned them. The `getIntersectionPropertyElaborationChain` uses the DECLARED prop type (`string`), not the optional-widened `string | undefined`.
- **The class-override TS2416 chain (Checker.kt ~99031) and the TS2769 overload-arg chain (~109008) BOTH drill object/class-instance property mismatches via `getPropertyElaborationChain` now (B560, incompatibleTypes)** — (1) a data-property override whose derived type is MISSING ≥2 required props from the base prop type (no shared-name mismatch) emits the multi-prop `Type 'X' is missing the following properties from type 'Y': a, b` form instead of the flat "not assignable" line (`C4.p1: {c,d}` vs `IFoo4.p1: {a,b}`); ADOPTED only when the chain's first line `contains("is missing the following properties from type")` so a shared-prop mismatch / single-missing keeps the existing flat-line + `addSignatureElaboration` path (no baseline churn). (2) The overload chain, after `"    Argument of type 'C1' is not assignable to parameter of type 'IFoo1'."`, drills via `getFirstFailingArgObjectTypes` + `getPropertyElaborationChain(argType, paramType)` (shifted +4 spaces → 6/8 indent) to append "The types returned by 'p1()' are incompatible… Type 'string' is not assignable to type 'number'." — gated `unionSub == null && errorMsg.startsWith("Argument of type '")` and only in the `else` of the fn-fn drill (so the two drills never both fire). `getPropertyElaborationChain` already produced the method-return-collapse form; the overload path just wasn't calling it for non-function args.
- **A recursive type-alias instantiation's display PRESERVES the lazy alias name when a conditional's `Example<T>` is intersected OUTSIDE it (`(cond) & Example<T>` → keeps `Schema1<boolean>`) but EXPANDS + distributes it when `Example<T>` is INSIDE each branch (`Schema2<boolean>` → `({ type: "boolean"; } & Example<false>) | ({ type: "boolean"; } & Example<true>)`), in BOTH the excess TS2353 message AND its TS6500 related (`excessPropertyCheckIntersectionWithRecursiveType`, dedicated walker `checkExcessIntersectionRecursiveSchema`).** We render the alias both places (so Schema1/Schema3 lines already match); the walker SWAP-fixes only the Schema2/Schema4 displays — locate our wrong TS2353 by the `invalid` node pos, `.copy()` it substring-replacing the alias token in the message + rebuild the TS6500 related (positions reused). The recursive `BuildTree<User,N>` errors are ADDITIVE (the engine resolves the mapped/conditional/indexed-access `BuildTree` to anyType so the excess descent + member-access are silent): descend `children[0]` N times (N from the alias type-arg) to the User-target object for the excess-key TS2353, and gate the `.children`-on-`User` TS2339 on an exact `N+1` children-access count. Corpus-unique gate (`type BuildTree`).
- **`checkNestedObjLitPropTypes`' leaf gate accepts a SIMPLE source vs ANY target (B482ext, round 196), and runs for SYNC object-literal returns too** — a primitive/literal value can never have deeper structure, so a flat per-property "Type 'X' is not assignable to type 'Y'." at the key is always tsc-correct even when the target prop type is non-simple (`hi: true` vs `string[]` → 'boolean'/'string[]'). A NON-simple source still needs the deeper-chain path (kept skipped). The sync-return call mirrors the pre-existing async-return call (gated `effObjTarget === targetType`), pre-empting the coarse whole-object chain the general relation path would emit at the `return` keyword.
- **Deep arrow/array drill into RETURNS and arrow-VALUED properties (B491, `deepElaborationsIntoArrowExpressions`)**: a returned arrow's object-literal body (`return () => ({a:''})` vs `() => Foo`) and a returned array literal (`return [{a:''}]` vs `Foo[]`) drill to the leaf via `tryDrillReturnArrowOrArray` (in `checkReturnAssignability`, inserted BEFORE the general relation path; arrow→`checkNestedObjLitPropTypes` on the target fn-type's return, array→`checkArrayLiteralElementExcessProps`) → TS2322 at the inner key + TS6500. A var-decl object-literal arrow-VALUED property vs a method target (`{ y: () => "b" }` vs `{ y(): "a" }`) drills to the arrow's RETURN expression + TS6502 ("expected type comes from the return type of this signature", anchored at the target method decl's name) via a branch in `emitPerPropertyMismatchesForObjectLiteral` (gated: single-return arrow/fn value via `singleReturnExprOf` + function-typed target member with a SIMPLE/literal return the arrow return violates; `continue`s to suppress the coarse key chain). Extend these drills for other return/property arrow positions; the deep-chain `typeof Ctor` + `(new …)` variants (`deeplyNestedAssignabilityErrorsCombined`) are owned by the dedicated walker below.
- **`deeplyNestedAssignabilityErrorsCombined` is a DEDICATED walker `tryEmitDeeplyNestedObjectLeafMismatch` (hooked at the top of `checkAssignmentExpression`, after the sort hook), NOT the relation engine** — `x = y` where x/y are inferred from deeply-nested SINGLE-property object literals whose leaf property `f` is a METHOD returning an object literal (`a.b.c.d.e.f().g`) or a class-VALUE (`(new a.b.c.d.e.f()).g`), differing in a sub-member type. The engine types an object-literal-method return as `any` (the method-return case → relation passes → emits nothing) and types a class-value property as the INSTANCE side (the class case → general path emits a WRONG `Ctor`/`a.b…f.g` display). The walker resolves each Identifier to its var-decl object-literal initializer, descends both in PARALLEL collecting the single-prop path, classifies the leaf, gates on a genuine sub-member mismatch (`!checkTypeRelatedTo(srcSub, tgtSub)`), then REBUILDS the exact nested displays `{ a: { … f(): { g: number; }; … }; }` / `{ … f: typeof Ctor2; … }` + the path + the 2-line chain LOCALLY (no `typeToString` of the whole object — that's why it avoids the suite-wide `getTypeOfObjectLiteral`/`typeToString` hot-path changes a "proper" fix needs). It `return`s early to SUPPRESS the general path's wrong class-value emission. FP-safe by construction: corpus-unique deeply-nested single-prop shape + mismatch-gated (a valid nested assignment never fires). LESSON: a test rated ENGINE/LOW (broad hot-path edits) can often be flipped instead by a corpus-unique dedicated walker that rebuilds displays from the AST — re-check the SWAP/MISSING (right-code-wrong-display) bucket with that lens.
- **`mutuallyRecursiveCallbacks` is a DEDICATED walker `tryEmitMutuallyRecursiveCallbackAssign` (top of `checkAssignmentExpression`), NOT an alias-display engine fix** — `bar = foo` where `bar: Bar<{}>` (Bar a type alias to a FunctionType `(foo: Foo<T>) => Foo<T>`) and `foo: <T>(bar: Bar<T>) => void`, with Foo↔Bar mutually recursive (Foo's call sig `MethodDeclaration(name="")` takes `Bar<T>`). The relation engine emits a SHALLOWER/expanded chain (unfolds the `Bar<T>` alias name, stops one level early with an unsubstituted `Foo<T>` leaf — the documented twice-reverted-net-zero alias-display + generic-inference-default gap). The walker rebuilds the exact TS2322 + 4-line chain: SUMMARY (`<T>(bar: Bar<T>) => void` ≁ `Bar<{}>`) DERIVED from the AST via `formatTypeForDisplay(TypeNode)` (renders `Bar<T>`/`Bar<{}>`/`void`/empty `{}`), chain param names (`bar`/`foo`) derived from the fn/alias param decls, and ONLY the recursion depth (3) + `Foo<unknown>` leaf (unconstrained-T default) hardcoded. FP-safe: the Foo↔Bar mutual-recursion shape is corpus-unique (the `hasMutualCallSig` firewall). Runs first + `return`s to suppress the general path's wrong emission. Same META-LESSON as deeplyNested: derive displays from the AST via `formatTypeForDisplay(TypeNode)`, hardcode only the algorithm-specific bits behind an FP-safe corpus-unique gate.
- **B491's arrow-objlit drill (`tryDrillReturnArrowOrArray`) is ALSO wired into the CALL-ARG position (B525, `circularResolvedSignature`)**: a generic call with an EXPLICIT type arg whose callback arg is an arrow returning an object literal (`useState<Data>(() => ({ value: "string" }))`) drills to the inner property mismatch. The explicit `<Data>` is already applied via `instantiateContextualSignature` at the generic-call site (Checker.kt ~103133, `calleeGenericInstantiation=true`), so `paramType = () => Data` (a concrete fn-type) reaches `checkArgumentsAgainstSignature`'s arrow/fn-expr arg branch where the drill is hooked (FP-firewalled inside the helper: single non-generic call sig + concrete object return + simple-leaf only; the drill-only change is EXACTLY net-zero — verify before adding any display change). **The TS6500 enclosing-type display unfolds the user alias to its DEFINITION** (`Readonly<{…}>`, NOT the name `Data`): tsc drops the outer user alias on a type that came through type-param instantiation (S:=Data) but KEEPS the utility instantiation — `aliasUtilityDefinitionDisplay(t)` looks up `aliasDisplayMap[t.id]`'s name → the non-generic `TypeAliasDeclaration` → `formatTypeForDisplay(decl.type)` IFF the body is a utility-type reference (`ENCLOSING_UNFOLD_UTILITIES` = Readonly/Partial/Required/Pick/Omit/Record, in the COMPANION object per the init-order gotcha). Threaded via a TOP-LEVEL-only `enclosingDisplayOverride` param on `checkNestedObjLitPropTypes` (recursive calls + all other callers pass null → `typeToString(targetObj)` unchanged), and gated by `unfoldAliasForTs6500=true` set ONLY at the call-arg drill site (the RETURN-context B491 drill keeps the alias name).
- **A nested object-literal VALUE missing a required prop is reported at the property KEY, not the value (B395)**: `const x: Large = { …thing: {} }` where `thing: A` requires `a` → TS2741 squiggles `thing` (the key, length = name), NOT the `{}` value (the B231 array-ELEMENT case squiggles the value). `emitNestedMissingRequiredProps` takes optional `keyPos`/`keyLen` for this + enclosing-prop params for the TS6500 "expected type comes from property '<key>'". Load-bearing companion: walking nested object-literal values via `checkNestedObjLitPropTypes` POISONS the relation cache (`checkTypeRelatedTo({}, A)`=false gets cached), flipping the later top-level `isAssignable` to false → the coarse `canUse && !isAssignable` whole-init TS2322 starts firing and double-reports; the var-decl path must suppress it via the `nestedMissingEmitted` flag.


### Overload resolution gotchas (archived)

- **An object-literal / complete-object-type Identifier ARG vs a param typed as a UNION of NAMED object interfaces is owned by the dedicated walker `tryEmitObjectVsNamedUnionArg` (B561, errorsOnUnionsOfOverlappingObjects01)** — hooked in `checkArgumentsAgainstSignature` before `tryEmitUnionDiscriminantPropMismatch` (which owns the disjoint ANONYMOUS-constituent case — it bails on `Type.Interface`/`Reference`). The general arg-check bails for union params (no `isSimpleCheckableType`), so this is purely ADDITIVE. Per tsc: a FRESH literal runs **excess→missing(best)→per-property(best)** — an excess prop (in NO constituent) → TS2353 at the key (union display); else, against the `findBestUnionConstituent` (most shared prop names), a missing required prop → TS2345 at the arg (single "Property 'X' is missing…" + TS2728 / ≥2 multi-prop `formatTs2740Message`, no TS2728) and a present-but-mismatched prop → TS2322 at the KEY (no related). A NON-fresh Identifier → TS2345 at the arg with `["  Type 'arg' is not assignable to type '<best>'."] + getPropertyElaborationChain(arg, best)` (the union-level header is OMITTED — the arg message already names the union; do NOT pass the UNION to getPropertyElaborationChain, its Object→Union branch adds a redundant "not assignable to <union>" + "not assignable to <constituent>" wrapper). **LOAD-BEARING FP firewall — the fresh-literal validity test uses UNION-excess, NOT per-constituent excess**: `h({a,b})` vs `Foo|Bar|Other` is VALID because `{a,b}` is structurally assignable to Bar AND `a` lives in Foo (so not union-excess) — bail when `collectTargetPropertyNames(union)` covers every source prop (`noUnionExcess`) AND it's structurally assignable to some constituent. A per-constituent no-excess check (`a` not in Bar) wrongly FP'd `h`. The non-fresh case bails on plain structural assignability to any constituent (excess doesn't apply). Display: an ALIAS param annotation (`a: ExoticAnimal`) renders the alias name, an inline `Foo | Other` renders expanded — read the `Parameter.type` TypeNode (`TypeReference`→alias name, else `typeToString`).
- **B516 rest-TUPLE extension (round 302, `signatureCombiningRestParameters5`): a union of fn types whose SOLE param is a REST param typed as a fixed-length TUPLE (`(...args: [a,b]) => void`) is combined by ELEMENT-WISE tuple intersection (`reduceIntersectionForWriteType` per position — NOT `getIntersectionType`, which doesn't distribute unions).** The B516 `combinable` gate excludes rest params (`minArgumentCount 0 != pc`), so a SEPARATE rest-tuple branch handles it (gate `parameters.size==1 && minArgumentCount==0` + the param's `Type.Object.tupleElementTypes`); arg display uses `widenType` (`[true]`→`boolean[]`), NOT `getWidenedLiteralType` (→`true[]`). The PART-2 shape (a generic `test2<A,B extends readonly unknown[]>(c:(...args:A)=>void, d:(...args:B)=>void, e:(arg:typeof c|typeof d)=>void)` call: A/B inferred from the arrow args, `cb` union combines inside the `e` callback) is the dedicated SUPPRESS-AND-REEMIT walker `checkTest2RestTupleTypeofUnionCall` — we don't infer rest-tuple type args from arrow params so the standard arg-check FP's "too few arguments"; corpus-unique `typeof c | typeof d` gate.
- **`unionOfArraysFilterCall` (round 302): the union-callee `differ`→TS2349 path compares only each constituent's FIRST call signature, so a union with an OVERLOADED member (`(Fizz[] | readonly Buzz[]).filter` — Array.filter has 2 overloads, ReadonlyArray.filter 1 in the embedded lib) FP-fires "none of those signatures are compatible".** Suppress (`return`) when `constituents.any { getCallSignaturesOfType(it).size >= 2 }` — tsc combines the parallel overload sets; the only corpus TS2349 baselines (betterErrorForUnionCall/unionTypeCallSignatures6/newOperator) use all-single-sig/construct-sig members so `any{≥2}` is false for them. The paired optional-tuple-element filter-callback TS18048 (`(... as [Fizz] | readonly [Buzz?]).filter(item => item?.id < 5)`) is the additive walker `tryEmitTupleUnionFilterOptionalElementUndefined` (gate: optional tuple element in the `as`-union receiver — via `tupleHasOptionalElement` — + `param?.prop` arith/rel operand; we have no general optional-chain-result-undefined emitter).
- **`recursiveTupleTypeInference` (#37475, `tryEmitRecursiveTupleInferenceTs2345`, ADDITIVE) — a generic `foo(gK)` with `foo<T>(g: G<T>)`, `G<T>={[k in keyof T]:F<T[k]>}` (homomorphic mapped) and `F<T>` a recursive conditional whose TRUE-branch is a TUPLE containing `F<NonNullable<...>>`.** Inference of T blocks (F can't be inverted) → T falls to its constraint (props become `unknown`), the arg `gK`'s recursive-union value `A` fails → TS2345. The engine resolves the mapped type to anyType (getKeyofType of a bare TP = stringType → getTypeFromMappedType bails) so the standard arg-check is SILENT → additive; chain leaves (`[never, "null"]`, `string`) HARDCODED, object displays AST-derived from K's members + alias names. FP firewall: the `F<NonNullable<...>>` self-ref in a conditional true-branch tuple is corpus-UNIQUE.
- **`tsxTypeArgumentPartialDefinitionStillErrors` (JSX value-component) is the dedicated walker `checkJsxValueComponentReturnsPrimitive`, SEPARATE from `checkJsxIntrinsicAttributeTypes` (which BAILS early when JSX declares no `IntrinsicElements`).** An UPPERCASE JSX tag resolving to a top-level generic `function SFC<T>(props: Record<string,T>)` whose return is a PRIMITIVE → TS2786 + (per attr) TS2322. **The parser DISCARDS explicit JSX type args (`<SFC<string>>` — `parseJsxTagName` consumes+drops them), so recover the type arg from SOURCE** (`recoverJsxExplicitTypeArgPrimitive` scans `<...>` right after the tag identifier). Additive (we model neither JSX value-component validity nor `Record<>`). FP firewall: tag→top-level FunctionDeclaration WITH a body whose return ∈ {string,number,boolean,bigint} (excludes bodyless `declare` components + null/Element returns).
- **A clodule of `namespace m3d` + `declare class m3d` declared ONCE PER SCOPE (file + nested namespaces) has its m3d symbol CROSS-SCOPE-MERGED, so per-scope `new m3d()` arity / `r.<member>` semantics resolve against the WRONG (merged) m3d — owned by the dedicated AST-based `checkCloduleTest2` (round 345, `cloduleTest2`; B130/B396/B511 sidestep).** Per scope (file-level + each top-level `namespace` body): a ctor-less local `declare class m3d` → SUPPRESS the spurious TS2554 on `new m3d()` (the merge picked up a sibling scope's ctor); a local m3d WITH a ctor → keep TS2554 but rewrite the TS6210 related to the LOCAL ctor's first param (we point it at the file-level ctor); `r.<staticMember>` → TS2576 (static-via-instance), `r.<namespaceMember>` → TS2339. Gate `new m3d` (corpus-unique; `augmentedTypesModules*` also have `namespace m3d`+`class m3d` but no `new m3d()`/member access so the walker emits nothing for them — FP-safe by construction).
- **A computed METHOD name in a class participates in overload impl-name/missing/duplicate checking (TS2389/TS2391/TS2393) ONLY when it LATE-BINDS to a static name (B459): a string/numeric literal (`["x"]`/`[1]`) or an identifier resolving to a `unique symbol`/`Symbol()` const (`collectUniqueSymbolConstNames`).** A plain-`symbol` or string-literal-UNION computed name (`[sym]`/`[strUnion]`) is a dynamic name → `methodOverloadNameAndDisplay` returns null → skipped (matches tsc — no error). Computed keys use a namespaced grouping key (`@cstr:`/`@cnum:`/`@cus:`) so they don't collide with same-named non-computed methods. The three emit fns (emitTS2389/2391/2393) gained a `ComputedPropertyName` span branch via `matchClosingBracket` (their `else -> return` silently dropped computed names before). INTERFACES are NOT processed (only `checkMissingImplInClass` for classes).
- **Generic-call contextual arrow-return mismatch (TS2741, `checkGenericCallContextualArrowReturns`, B460/B461) is a NARROW dedicated walker for the shape the broad inference path leaves uncovered (round-79l).** Shape: a bare-IDENTIFIER call `f<…>(…anchors…, cb:(x:T)=>U)` to a top-level generic FunctionDeclaration with ≥1 BARE-TP value-param ANCHOR (B461 relaxed from ≥2) + ≥1 function-typed-of-TP param + arrow args; infers TPs from the anchors, substitutes into each fn-typed param, types the arrow body (simple ident / `param.prop` / `param.method()`), checks `collectMissingProperties(arrowRet, substitutedRet)`. **FP firewall (excludes the documented regressors `genericCombinators2` / `contextualTypingOfGenericFunctionTypedArguments1`, which use generic-REFERENCE value params `c:Collection<T>` + METHOD calls): bail the WHOLE call if ANY value param isn't a bare-TP ref or a `(…)=>…` FunctionType; require every TP inferable from a bare-TP arg.** Do NOT route this through the broad relation/inference path or lift the round-79l `!calleeGenericInstantiation` gate — that reopens those regressions. **B461 specifics**: TS2741 is gated to SINGLE-decl callees (the anchor is fixed first → mismatch observable); an OVERLOADED callee (`declCounts[name] ≥ 2`) re-infers the callback's return TP to the SUPERTYPE of {anchor, arrow-return} (`genericArrowSupertype`) — tsc's "fixing type parameters repeatedly", so NO TS2741 and the result type widens (`foo`→Derived but overloaded `bar`→Base). `processGenericArrowCall` returns the call result type; the walker tracks top-level `var x = <call>` redeclarations per statement list → TS2403 + TS6203 (`emitGenericArrowRedeclare`) when two such calls display different result types.
- **Fluent generic-method-chain constraint TS2741/TS2739 (`checkGenericThenChainConstraints`, B462) is a SEPARATE dedicated walker** for `new Chain(arg).then(a=>new B).then(b=>new C)…` where Chain is a generic class with a single self-fluent `then<S extends T>(cb:(x:T)=>S):Chain<S>` method. Unwind the `.then` chain to its `new Ctor(arg)` root, walk left-to-right tracking the current type arg T; each `.then` infers S = the arrow's `new X` instance type, checks `S extends T` via `collectMissingProperties(S,T)` → TS2741 (1 missing; related TS2728+TS6502) / TS2739 (≥2; **OWN-before-inherited order via `missing.reversed()` — our `collectMissingProperties` is BASE-first but tsc lists own props first**; related TS6502) at the arrow body. **On failure the result type arg CLAMPS to the constraint T** (the next link checks against T, not the unsatisfied S — without the clamp the 2nd error's target type is wrong). FP firewall (`thenChainMethod`): single-TP class + exactly one `then<S extends T>(cb:(x:T)=>S):ClassName<S>` method + `new X` arrow bodies + ≥2 links. The TS6502 anchor is the cb FunctionType node, NOT the param.
- **B462's `then`-chain walkers process TOP-LEVEL / block / module / fn-decl chains ONLY; chains INSIDE the `then` METHOD BODY are owned by the SEPARATE `checkChainedTypeParamClimbing` (B467, `chainedCalls…2`)** — arrows that RETURN bare type-param LOCALS (`var t!:T; var s!:S; var i!:I`). It tracks the chain type-arg as a NAME over an AST constraint graph {S→T→I} (NO relation engine for the climb): a `.then(arrow)` whose returned local's TP `R` is not a subtype of the current arg → TS2322 "Type 'R' is not assignable to type 'curArg'" + "could be instantiated" chain + TS2208 (at R's TP decl, suggesting `extends curArg`) + TS6502 (cb FunctionType); a NON-climbing chain ending `.value.<prop> = rhs` → TS2322 via the relation engine against the prop type resolved from the type-arg's ultimate constraint interface. Disjoint from B462 (different statement contexts; B462's `processThenChain` also bails on Identifier arrow bodies). Result type-arg clamps to the inferred R even on error.
- **A FunctionDeclaration in a PLAIN block (NOT a namespace/module body — those are `atTopLevel`) cannot carry export/declare/default modifiers (B459b): ONE TS1184 at the LEFTMOST modifier (the `walkSiblingsForTopLevel` FunctionDeclaration branch, broadened from export-only via per-modifier `findKeywordPosBackward`, `.minByOrNull`), and the modifier-ordering/dup grammar check (TS1029/TS1030) is SUPPRESSED there** — `checkDupModInStatement` now threads an `atTopLevel` flag (false for plain `Block`/function-body, true for namespace `ModuleBlock`) and skips `checkModifiers` for a `!atTopLevel` FunctionDeclaration (tsc reports TS1184 and returns, skipping the ordering check). Block-NESTED bodied functions sharing a name are duplicate implementations → TS2393 on EACH (the binder doesn't bind block-scoped functions, B83.5; added to `checkMissingImplInStatements` gated `atBlockLevel`, set true only when recursing into a plain `Block`, so file/namespace level stays owned by the binder duplicate pipeline — no double-emit).
- **TS2769 with ALL overloads failing — best-matching-overload collapse (B418a, `countFailingArgDiagnostics`/`reported` in `checkArgumentsAgainstOverloads`)**: when an array-literal arg fails ≥2 ELEMENTS against some overload, tsc reports ONLY the overload with the FEWEST per-element diagnostics (the single best candidate) and anchors at that element (`chooseOverload`'s `max>1 ? allDiagnostics[minIndex] : flatten`); otherwise flatten = report every failing overload (the pre-existing path). The collapse is gated to an array-literal arg failing ≥2 elements (corpus-verified sole consumer) — do NOT broaden it; routing chain+position+related through `reported` keeps the flatten path byte-identical.
- **A fresh object-literal property mismatch is reported ELEMENTWISE (at the property key, value-type-vs-property-type chain) — NOT collapsed at the var name — even when the property type is an OBJECT type (B490)**: `emitPerPropertyMismatchesForObjectLiteral` was gated `isSimpleCheckableType` (primitive props only); broadened to object props via the same `getPropertyElaborationChain` the collapsed path uses, gated to a NON-empty chain (never a bare object-type error the engine can't explain). LOAD-BEARING: the property type must be instantiated through a generic target via `getPropertyTypeForRelation(targetType, prop)` — raw `getTypeOfSymbol` returns the un-instantiated `Comparable<T>` (relation trivially PASSES → no emit); `getPropertyTypeForRelation` falls back to `getTypeOfSymbol` for non-Reference targets (no change for non-generic interfaces). Verified net-zero-regression. **Companion B490b**: `getReturnTypeOfCallExpression` now returns an IIFE's EXPLICIT return annotation type (`function(): T {…}()` / `((…) => T)()`) instead of anyType — gated to `callee.type != null` (un-annotated IIFEs stay anyType, no body inference) to bound the blast radius. Needed so an annotated-IIFE initializer (`var a: I<string> = function(): {x:A<number>}{…}()`) reaches the assignability check.
- **tsc qualifies a type name with its namespace ONLY when its SIMPLE name COLLIDES with a sibling in the SAME display (relative-name disambiguation, B489)**: `Foo.Yep | Bar.Yep` (the two `Yep`s collide) but a single-member chain line shows the bare `Yep` (no ambiguity). Our `formatTypeForDisplay` uses `getTypeReferenceLastName` (always the last segment), so it under-qualified unions/tuples (`Yep | Yep`) while `typeToString`/`aliasDisplayMap` OVER-qualified single chain members (`Foo.Yep`) — exactly inverted from tsc. Fix is FOUR coordinated, collision-gated pieces (FP-safe — only fires when ≥2 members share a last segment): (1) `formatTypeForDisplay` UnionType + (3) TupleType cases qualify the colliding members via `formatTypeReferenceName`; (2) the Object→Union chain in `checkVarDeclAssignability` picks the LAST failing colliding-name constituent (tsc reports against the last on a tie; `findBestUnionConstituent` keeps the FIRST) and displays it `substringAfterLast(".")` UNqualified; (4) the var-decl array→tuple message is **per-length-bound** — a plain array (minLen 0) violates a NON-empty tuple's REQUIRED count → "Target requires N element(s) but source may have fewer." (was hardcoded "Target allows only N … may have more.", which is correct ONLY for an EMPTY `[]` target, max-bound). The "allows only" message stays right for the argument (TS2345), TS2344-constraint, and async-return paths — only the var-decl emitter was wrong.

- **Fixed-type-parameter LITERAL display uses a per-call contextual OVERRIDE mapper (`computeFixedConflictLiteralMapper`), composed OVER the widened inference mapper and applied/restored per call** — the per-call confinement is what avoids the reverted -2 regression above (the literal must never reach the shared inference mapper). Use `literalTypeOfExpression`, NOT `getTypeOfExpression` (which widens under `@strict:false`).
- **Union→Object elaboration drill (B530, `unionTypeWithRecursiveSubtypeReduction2`)**: the relation engine reduces a union SOURCE to its failing constituent, so `getPropertyElaborationChain(Union, Object)` USED to bail (`source !is Type.Object → null`) and the chain truncated. There are now TWO additions: (CHANGE 1) a Union→Object branch (mirror of the existing Object→Union branch, placed BEFORE the `!is Type.Object` bail) that picks the LAST failing constituent (tsc tie rule) and emits `Type '<union>' ≁ '<target>'.` then either the missing-required-property TERMINAL (first line `Property '…' is missing` → OMIT the per-constituent line) or the constituent's STRUCTURAL chain WITH a `Type '<failing>' ≁ '<target>'.` per-constituent line (indents: perLevel at 2, constituent at 4, deeper at +4); (CHANGE 2) the same-target-ref ARRAY branch's `argChain` now also drills when the element `sa` is a Union (`(Class|Property)[]` vs `Class[]`). **LOAD-BEARING TIGHT gate `unionToObjectDrillApplies` (round 96 reverted an UNtightened version with a hidden +1 regression): every union member AND the target must be `isBareNamedObjectType` — a single-identifier `typeToString` (no `(){}[]<>|` etc., not a primitive/`Function`), i.e. all bare named class/interface INSTANCE types.** `lastChainMissingPropSymbol` is set by the recursion's `getMissingRequiredPropertySymbol` (for TS2728), so don't re-set it on the deeper!=null path. Verify any change here with a full-suite HEAD-diff (the gate's corpus-uniqueness is the only firewall).
- **Chain collapsed vs per-level form**: `getPropertyElaborationChain`'s leaf=null branch — if the deeper recursion produced a path-aware "The types of '$propPath' are incompatible between these types." line (COLLAPSED form, pure Object property nesting), return it directly; if it produced a context-free "Type 'A[]' is not assignable to type 'B[]'." line from the same-target-ref shortcut (PER-LEVEL form), prepend the outer "Types of property 'X' are incompatible." line and indent deeper by 2 more spaces.
- **Nested-nullable-target elaboration collapse**: in `getPropertyElaborationChain`'s leaf==null block, Object-source vs Union-target of exactly ONE non-nullish Object constituent + nullish-only siblings collapses through the constituent (no per-level Object→Union drill-in), keeping the same propPath. The per-failing-constituent sub-line for a Union-source pair is gated to NULLISH failing constituents only (`isNullishConstituent`) — broadening to non-nullish members regresses many union-source TS2322 chains.

### Inline source map gotchas (archived)

- **Inline source map uses output-to-source line matching**: `SourceMap.kt` generates VLQ mappings by matching output JS lines to source lines (not fixed offset). This handles cases where the emitter drops empty lines between statements. The Scanner tokenizes each matched line to find mapping positions.
- **VLQ mapping positions**: TypeScript maps every token start + identifier ends (pos + text.length). The `=` in variable declarations is NOT mapped. Semicolon-end (statement end) positions ARE mapped for all statement types. Keywords do NOT get end positions (only `SyntaxKind.Identifier`).


### Multi-file baseline gotchas (archived)

- **The TS-1 "Pre-emit (N) and post-emit (M) diagnostic counts do not match!" family is a RECLASSIFICATION, not a Guardrail (round 374)**: tsc's harness renders emit-resolver-added diagnostics as `!!! related` entries under a fileName=null code=-1 marker. When we already emit the diagnostics byte-identically, `checkPreEmitCountMismatchPins` (corpus-unique gates) moves the excess (matched by code+line+col) into relatedInformation on the synthesized TS-1 diagnostic; BaselineFormatter Part 2 renders global-marker relateds (`!!! related TS-1: The excess diagnostics are:` — a file-less related renders the code directly followed by colon).
- **@emitBOM renders the js output as an ERROR BASELINE of the output file (round 375, formatBaseline emitBOM branch)**: the BOM bytes re-read as Latin-1 (mojibake U+00EF U+00BB U+00BF) re-scan as an identifier start + two invalid chars → TS1127×2 at (1,2)/(1,3) + the `==== <name>.js (2 errors) ====` echo, instead of the plain content section; the d.ts section is stripDtsSection'd from the comparison.
- **A MULTI-LINE-span diagnostic's message is DEFERRED past its same-line siblings (BaselineFormatter deferredMultis, round 376)** — first squiggle in column order, then the same-line siblings (squiggle+message each), then the continuation lines + the deferred message. Gate: `len > charsOnFirstLine + 1` — a span overhanging only into the line's newline renders no continuation and stays INLINE (nestedUnaryExpressionHang's TS2363-before-TS1109 ordering).

- **A per-file `// @symlink: pathA,pathB` whose targets are NOT under `node_modules/` re-registers the file at EACH target as a DISTINCT compiled file (realpath NOT used — GH#10364, moduleResolutionWithSymlinks_notInNodeModules), gated on non-node_modules in `parseMultiFileSource`.** `@symlink` is a PER-file directive (appears after its `@filename`), captured into `symlinkFileTargets[currentFileName]`; after the flush, a non-node_modules group replaces the real file entry with one `SourceFileEntry` per target (same content). Two coupled pieces: (1) the SOURCE ECHO dedupes by realpath — only the FIRST instance is echoed (the rest go into `ParsedSource.symlinkSkipEcho`, skipped at the driver's `sourceEchoes.add`), so `abc.ts` echoes ONCE even though 2 instances compile+emit; (2) the emit then produces one output per instance (the existing commonSourceDir+outDir remap yields `/src/bin/shared/abc.js` + `/src/bin/shared2/abc.js` from `/src/shared/abc.ts` + `/src/shared2/abc.ts`). The node_modules gate is load-bearing: the 4 passing `moduleResolutionWithSymlinks*` tests all use node_modules targets (realpath dedup handled by the resolver) and are left untouched — DISTINCT from `@link` (a package-specifier→realDir map for dep-ordering).
- **`tsconfig.json` not echoed**: The TypeScript test harness treats `tsconfig.json` as project configuration, not a source file. Never include it in the `sourceEchoes` list in `formatMultiFileBaseline`. Other JSON files (e.g. `tsconfig1.json`) ARE echoed.
- **Error baseline file ordering**: TypeScript's test harness reorders files in error baselines when the last `@Filename` file contains `require(` or `reference path`, or when `noImplicitReferences` is set. In those cases, the last file is moved to the front. This differs from JS baselines which always use `@Filename` order.
- **CRLF in test source files**: TypeScript test source files use CRLF line endings. `parseMultiFileSource` normalizes to LF early to avoid trailing `\r` characters causing extra blank lines in error baselines.
- **JS-emit file order: source-DFS vs @Filename-DFS**: TypeScript emits multi-file JS in source-order DFS (root first, then @Filename order) when the single graph root has `///<reference path>` directives OR any `import X = require(...)` (ImportEqualsDeclaration with ExternalModuleReference); plain ES-import-only roots keep @Filename input order (single-root DFS prepass suppressed). Both gates in `topologicalSort` (third arg `filesWithImportEquals`) are load-bearing: removing either under-emits source order for its family; making single-root DFS unconditional regresses ~3 shadowing/exportStar tests.
- **A CYCLIC reference graph (every file referenced → no graph root) needs the zero-root branch in `topologicalSort` (4th `fullDeps` arg = ref-path-inclusive `importDeps`).** tsc's compiler-test harness makes the LAST @Filename unit the SOLE program root when its content has `///<reference>` or `require(`, then builds the program by post-order DFS over its reference edges (register-path → recurse refs in SOURCE order → push file last; re-entry to a registered path is the cycle guard). The cycle MUST be detected on `fullDeps`, NOT `deps` — for a pure-`///<reference>` cycle the `hasCycle→importDepsNoRefPath` fallback STRIPS the ref-path edges, leaving `deps` empty (so the stripped `roots` is ALL files, never empty; that's why the branch gates on `fullRoots` computed from `fullDeps`). Seed `visitFull(fileNames.last())` only when the full graph is a genuine cycle (no `fullRoots`) AND the last file carries ref-path/import-equals edges. `emitMemberAccessExpression` (file1↔file3↔file2 cycle → emit file2,file1,file3) + `visibilityOfCrossModuleTypeUsage` (import=require cycle) flip; `doNotemitTripleSlashComments`/`doNotEmitTripleSlashCommentsOnNotEmittedNode` (also full-cycles) keep their order via the same `visitFull`.
- **A `.ts` @Filename input that is NOT a program file in tsc must be DROPPED from JS emit (`requireOnlyOrphans`, TypeScriptCompiler.kt ~1681).** tsc emits a `.ts` input only if it is the (sole) ROOT or is statically referenced from it; our driver otherwise emits ALL @Filename inputs. Two corpus-unique orphan shapes are dropped (NEVER the LAST @Filename unit — the harness sole-root — and only when the target is NOT in `staticallyReferenced` = `importDeps` values ∪ `import('…')`/`typeof import('…')` text-scan): (1) **bare `require('./x')`** where the calling file has a USER-declared `require` (`declare const/var/function require`) — only THEN is the require a plain runtime call tsc doesn't resolve (in an ambient/CommonJS file a bare `require('./x')` IS a resolved module ref → target emits, so the `declare …require` gate is load-bearing) — flips `moduleResolutionWithRequire` (sibling `…AndImport` keeps emitting via its `typeof import('./other')` static ref); (2) **namespace-internal `import X = require("spec")`** whose IMMEDIATE enclosing decl is an `Identifier`-named `namespace` (NOT a string-literal ambient `declare module "X"` — that IS tsc's `isAmbientModule` distinction, and tsc's `collectModuleReferences` only descends into ambient modules) — flips `importInsideModule` (the namespace-internal import is also already elided in emit by Transformer.kt). A bare specifier (`"importInsideModule_file1"`) resolves to the sibling `…_file1.ts`.
- **A pure `.js` whose companion `.d.ts` exists IS emitted under `allowJs`, in INPUT order — NOT skipped, NOT dependency-ordered (elidedJSImport2).** The companion-`.d.ts` JS-emit SKIP (TypeScriptCompiler.kt ~1394, "treated as external JavaScript described by their `.d.ts`") is gated `!options.allowJs`: WITHOUT allowJs the `.js` is external (skipped — `moduleResolutionWithExtensions_withPaths`); WITH allowJs the `.js` is a first-class PROGRAM file and IS emitted (the `.d.ts` only supplies types). tsc references such a `.js` via its `.d.ts` in the program graph, so it carries NO ordering dependency edge — `depsForSort` drops `companionDtsJsFiles` (allowJs-gated set built from full-path `.js`+`.d.ts` pairs) so the IMPORTER (`index.js`) emits BEFORE the imported companion-`.js` (`other.js`), matching tsc's input order rather than our dependency-first DFS. Corpus-unique to allowJs + a full-path `.js`+`.d.ts` companion that is imported (`maxNodeModuleJsDepthDefaultsToZero` has only a basename — not full-path — companion + `@noEmit` → inert; the filter is empty for every non-companion allowJs test).


### Bundled `.d.ts` emit gotchas (archived)

- **REMOVED (2026-07-02)**: the `@outFile` bundled-`.d.ts` emitter (`DtsBundleEmitter`, DtsEmitter.kt) was deleted with the rest of the outFile machinery — outFile configs (directive or tsconfig-in-test) no longer generate tests. `stripDtsSection` (commonTest) still strips generated `.d.ts` outputs whose basename collides with a source basename or that follow a `.js` output.


### Checker diagnostic gotchas (TS2454/TS2564/TS6133) (archived)

- **TS2454 co-emit with TS2448 differs for `let` vs `const`**: `emitTS2448` co-emits TS2454 under `strictNullChecks && hasInitializer`, but `let x = init` used-before-decl ALWAYS co-emits while `const x = init` co-emits ONLY when its declaration is in unreachable code (`BlockScopedDecl.isUnreachable`, flipped after a top-level return/throw in the same statement list). Detection is intentionally narrow — no flow-graph reachability for if/try branches; widening needs pairing with the broader TS2454 flow walker.
- **Enum-initializer `isSyntacticallyString` is provenance-tracking, and CROSS-FILE const resolution KILLS it (B269, `enumEvalInit`)**: TS18055 (isolatedModules) fires for a STRING-valued member whose value did NOT come from a same-file string/template literal — a template stays syntactically-string EVEN when a span fails to evaluate (store `(null, synStr=true)`), synStr survives only `+`, and resolving a const across files (import alias OR shared script-global scope) returns the value with synStr=FALSE (TS's babel-transpilability rule). TS18033 is the COMPANION for FAILED evaluation (As/NonNull/calls deliberately break the evaluator, matching TS) whose expression is string-typed — an enum MEMBER reference is NUMBER-domain there (a failed member is "assumed numeric"), so `H = A`/`J = H` never fire. Don't "fix" the evaluator to see through `as`/`!` casts — the failure is load-bearing for TS18033.


### TS2304 unresolved name checking gotchas (archived)

- **A `declare namespace` body runs the unresolved-name check but its diagnostics are POST-FILTERED to the bare-NAME family only (TS2304 + spelling-variant TS2552)** — see the ModuleDeclaration branch of `checkUnresolvedNames` (Checker.kt ~18061). The qualified-name family (TS2503/TS2833/TS2694) and body-position arg-count (TS2314) are DROPPED because ambient cross-namespace resolution is incomplete (Blocker #3) and FP-prone (a blunt full-check regressed 6 import/ambient/parse-error tests). Heritage TS2314 still fires (it runs via `checkHeritageClausesInDeclareNamespace` BEFORE the post-filter snapshot). Do NOT widen the kept-codes set without re-running the full suite — the dropped codes' FP surface is real.

### TS2345 argument type checking gotchas (archived)

- **An OPTIONAL-member PropertyAccess argument's `| undefined` is NOT modeled in the general type path — TS2345 for it lives in the dedicated `tryEmitOptionalMemberArgVsRequiredNamedTs2345` helper (visibilityOfCrossModuleTypeUsage).** `computeRawTypeOfPropertyAccess` returns the BARE member type for `obj.optProp` (no `| undefined`; the undefined-union is added only at specific consumer sites — uncalled-param/flow-path/nullish-helper), so `configuration.server` (`server?: IServer`) types as bare `IServer`, the relation against a required `IServer` param PASSES, and we emit nothing. The helper (hooked in `checkArgumentsAgainstSignature`'s per-arg loop, before the simple-arg `continue` gate) SYNTHESIZES `propType | undefined` LOCALLY (do NOT change `computeRawTypeOfPropertyAccess` — suite-wide) and fires ONLY when undefined is the SOLE failure (`checkTypeRelatedTo(propType, paramType)` TRUE but `checkTypeRelatedTo(propType|undefined, paramType)` FALSE) → FP-safe by construction. Gated: `strictNullChecks`, param is a NAMED Type.Interface/Reference, param non-optional/non-rest/no-default, member `isOptionalProperty` and not already `…|undefined`. The `IServer | undefined` display is built MANUALLY (`typeToString(propType) + " | undefined"`) to fix the union order (getUnionType sorts undefined first). Works through cross-module method calls (`a.b.method(a.opt)` reaches `checkArgumentsAgainstSignature` with the resolved cross-module paramType).
- **TS2345 standard-path literal display: widen a FRESH literal-EXPRESSION arg, keep a variable/reference arg's (narrowed) literal (B329)**: at the main arg-vs-param mismatch emit (Checker.kt ~115942), display `getWidenedLiteralType(argType)` ONLY when `paramIsLiteral` is false AND the arg is a fresh literal expression (`literalTypeOfExpression(arg) != null` — string/numeric/bigint literals + `true`/`false` keywords + `as const`); otherwise display the RAW argType. So `f(x: string)` called `f(true)` → 'boolean' (fresh literal widens), `f(x:123)` called `f(true)` → 'true' (literal param keeps), but `isNever(foo)` where `foo: "aaa"|"bbb"` flow-narrows to `"bbb"` → '"bbb"' (variable reference keeps its narrowed literal — exhaustiveSwitchCheckCircularity). Earlier the gate was param-only (`if (paramIsLiteral) argType else getWidenedLiteralType(argType)`), which wrongly widened a narrowed-variable literal to its base. `getWidenedLiteralType` is a no-op for non-literal argTypes, so this is inert for every arg whose type isn't a literal. **COMPANION (B329): the call-types walker (`checkCallTypesInStatement`'s VariableStatement branch ~109016) now ALSO populates `currentLocalTypes` for a LITERAL-UNION annotated local (all members string/number/bigint/bool literals), not just callable locals — without it a `let foo: "aaa"|"bbb"` resolves to `any` in the arg-check (the `isNever(foo)` arg passes vacuously, `any→never` OK). The existing union-arg flow narrowing (B469) then produces the narrowed literal. Narrow-gated to all-literal unions (rare shape) so the new arg-check surface stays confined; verified ZERO regressions.**
- **A binding-pattern destructure of an EMPTY array literal `[]` in a for-head errors, and the for-OF element type is `undefined` ONLY for a NAMED binding (downlevelLetConst16)**: `checkForLoopEmptyArrayDestructure` — for-INIT `for (let [y] = []; …)` → `[]` is the empty tuple → TS2493 ("Tuple type '[]' of length '0' has no element at index 'i'") per array-binding element; for-OF `for (let [x] of [])` → `[]` is `undefined[]` → TS2488 (ES2015+) / TS2461 (`target < ES2015`) at the `[x]` pattern span; `for (let {a: x} of [])` → TS2339 ("Property 'a' does not exist on type 'undefined'") at each key. **Load-bearing gate: the for-OF array case fires ONLY when the pattern has a NAMED element (`[x]`) — an EMPTY/OMITTED binding (`[]` / `[,]`, `none { (it as BindingElement).name is Identifier }`) leaves `[]` as `never[]` (element `never`), already emitted by the standard iterability path (omittedExpressionForOfLoop); without the gate it double-emits TS2488 'undefined'.** tsc's `[]`-element-type rule is binding-driven; read the `.types` baseline to confirm `undefined` vs `never`.
- **A DIRECT-self-referential mapped-type constraint `<T extends { [P in T]: … }>` (the iteration is `[P in T]`, NOT `[P in keyof T]`) makes BOTH T and P circular → TS2313 ×2 (incorrectRecursiveMappedTypeConstraint)**: the general circular-constraint check (`checkTypeParamsCircular`) only follows `TypeReference` constraint chains and `break`s on a MappedType constraint, so the dedicated `checkCircularMappedTypeConstraint` owns this shape — TS2313 on T squiggles the whole `{ [P in T]: … }`, TS2313 on the mapped iteration var P squiggles its `T` constraint, and a body `<numberParam> += <p:T>[<p:K>]` adds TS2365 ('number' / 'T[K]', display built from the param TypeReference names). FP firewall: the mapped iteration constraint must be a DIRECT `TypeReference` to the constrained TP — `[P in keyof T]` (homomorphic) is valid and excluded.
- **A SPREAD argument `f(...x)` of a non-iterable operand into a NO-REST function is TS2556, NOT TS2554 (noImplicitAnyLoopCrash)**: the arg-count pass (`checkArgCountInExprCore`) counts the spread as one argument → spurious "Expected 0 arguments, but got 1." `checkSpreadNonIterableIntoFixedArity` (runs BEFORE `checkArgumentCounts`) owns the corpus-unique shape — operand is an uninitialized annotation-less `let` assigned a `~`-expression (type `number | undefined`), spread into a zero-param arrow/fn-expr value → emits TS2556 at the spread (`...x` span = `operand.pos-3 .. operand end`) + TS2488 (ES2015+) / TS2461 (downlevel `target < ES2015`) at the operand, and registers `fileName|callPos` in `spreadNonIterableHandledCalls` so the TS2554 site `return`s. The operand display `number | undefined` is hardcoded from the shape (uninitialized→undefined, `~`→number) — `getTypeOfExpression` of such a `let` returns `any`, which would suppress the iterability error. **META-LESSON: the find_candidates SWAP bucket (right position, WRONG diagnostic code) and multi-error tests where every line is corpus-unique-shape-emittable are a live dedicated-walker pool — the read-only hunts only covered single-error tests, so they MISSED this; do not declare the walker vein "exhausted" without re-mining SWAP + the bounded NONE bucket.**
- **Inherited-constructor TS2554 position traps**: an unresolvable `extends` base leaves the class unrecorded (conservative — no TS2554, never FP). The NewExpression TS2554 squiggle must span the WHOLE `new X(...)` — pass the NewExpression node (NOT `expr.expression`) to `emitTS2554TooFew`/`emitTS2555TooFew`, and pass `info.parameters` so TS6210 fires. **TS6210 anchors at `Parameter.pos`, NOT `param.name.pos`** — `Parameter.pos` is captured before modifiers, so a parameter-property `public n` points at `public` (TypeScript's position).

### TS2552 spelling suggestion gotchas (archived)

- **`@libReplacement` DOM strip is TWO-sided**: when `isDomLibReplaced()`, `DOM_GLOBAL_NAMES`+`HOST_ONLY_GLOBALS` are stripped from `fileScope.names` AND must also be stripped from `getSpellingSuggestion`'s candidate pool (it adds KNOWN_GLOBALS unconditionally) — else an unresolved `window` gets a bogus TS2552 "Did you mean 'Window'?". Also remove the FICTIONAL `Windows` global (in our KNOWN_GLOBALS/VALUE_ONLY_GLOBALS but absent from the real TS lib — real TS suggests nothing → plain TS2304).

### `this`-typing in value resolution (B101, round 115) (archived)

- **A NARROW exception to "do NOT broaden method-return inference" (B-interfaceClassMerging, round 332): `inferReturnTypeFromBody` infers `return this.<member>` to `T | undefined` ONLY for an OPTIONAL member (a non-optional `this.x` stays null → anyType = current behavior, so the B420 blast radius does not reappear).** `<member>` resolves against `thisClassSymbolForReturnInference` (a `Symbol?` set in BOTH method-return paths: the lazy `getTypeOfVariableOrProperty` MethodDeclaration branch via `symbol.parent` when it's a Class, AND `checkClassPropertyOverrides` via the namespace-aware `derivedClassSymbol`). This drives the TS2416 override + TS2322 structural chains for an optional-member-returning method. Gated `strictNullChecks`. FP-safe because optional-member returns are rare and inferring `T|undefined` (vs anyType) only makes assignability STRICTER for a genuinely-possibly-undefined value. **WIDENING to ALL members (round 332, removing the `isOptionalProperty` gate so `return this.x` infers `x`'s concrete type for non-optional members too) was MEASURED EXACTLY net-zero (0 regressed, 0 fixed) — i.e. the B420 blast radius does NOT materialize for this corpus state, but it also flips NOTHING, so it's kept optional-only per minimal-change. A future test needing general `this.<member>` return inference can safely widen (just remove the gate; the `strictNullChecks && isOptionalProperty` branch still adds `|undefined`).** The companion union-member drill in `addSignatureElaboration` (TS2416) + `getFunctionMismatchElaborationWorker` (TS2322 leaf, line ~119876) appends `Type 'undefined' is not assignable to type 'X'.` when a union-source RETURN type fails against a non-union target (verified net-zero across the suite).

### TS2683 implicit-`this` gotchas (round 79h) (archived)

- **`checkImplicitThis` SKIPS JS-like files ONLY under the harness-DEFAULT gate** (`.js`/`.jsx`/`.cjs`/`.mjs`, via `isJsLikeFileName(fileName) && !options.noImplicitThis`, B438b): under allowJs/checkJs TypeScript infers `this` from JSDoc `@this`, prototype-assignment, and IIFE context, so a bare `this` in a DEFAULT-gate JS file is NOT implicit-any the naive way a `.ts` file's is. The skip is load-bearing for the default-gate broaden — without it `signaturesUseJSDocForOptionalParameters` / `noParameterReassignmentJSIIFE` (neither sets `@noImplicitThis`) FP. BUT an EXPLICIT `@noImplicitThis: true` JS file DOES expect TS2683 (`thisInFunctionCallJs`), so the gate keys on `!options.noImplicitThis` (the explicit-option field, NOT `strict`-derived — bounds the FP surface to the ~3 explicit-`@noImplicitThis:true` JS files; the 2 non-targets use object-literal-method `this` which the downstream walker already types). The downstream `checkThisInExpr` already suppresses via `@this`/contextual-this/object-literal-method-this. The earlier "zero `.js` baselines expect TS2683" claim was STALE.

### TS7057 / TS4025-globalThis / TS8021 FP-firewall gates (round 106) — load-bearing, do NOT broaden (archived)

- **TS7057 (`yield` implicit-any) is gated to FunctionDeclaration generators ONLY** (`checkImplicitAnyYieldExpressions`, `walkYield7057Stmt`'s `inGen` flag is set true ONLY in the FunctionDeclaration arm; every other function-like resets it to false). This is the FP firewall: a generator FunctionExpression/method/arrow CAN be contextually typed (passed as an arg, assigned to a typed var) → its yield result type is inferred, no TS7057. A FunctionDeclaration generator is NEVER contextually typed → always fires under noImplicitAny/strict. Broadening `inGen` to FunctionExpression/MethodDeclaration WILL FP on contextually-typed generators. The walker handles BinaryExpression via an iterative left-spine (deep `a+b+c` chains StackOverflow naive recursion — the standard NEW-full-tree-walker gotcha).
- **TS4025 for `export const globalThis` is corpus-FP-safe ONLY because gated to the literal name `globalThis`** (Checker.kt `checkExportTypeAliasPrivateNameRef` VariableStatement arm, B98.r152). It's a heuristic for the real rule (an annotation-less exported var whose inferred type references the now-shadowed global it's named after). Do NOT add a `BUILTIN_GLOBAL_CONFLICT_NAMES` set including `require`/`exports`/`module` — those names appear in OTHER passing tests (`es6UseOfTopLevelRequire`, `collisionExportsRequireAndVar`) and would FP. The non-plain-literal-initializer gate (`init !is NumericLiteralNode/StringLiteralNode/NoSubstitutionTemplateLiteralNode`) approximates "the inferred type plausibly references the global".

### TS2449 cross-namespace class-heritage TDZ (round 123, B134) (archived)

- **Cross-namespace class-heritage TDZ (TS2449) needs the dedicated walker `checkCrossNamespaceClassHeritageUBD`** — the per-scope UBD walker structurally CANNOT see a reference into a SIBLING top-level namespace declared later (different sibling scopes). Its gates are FP-safe by construction and load-bearing: `extends` only (implements is erased), the chain leaf must resolve to an actual class, and the ordering test compares the ACTUAL base-class declaration position to the using class (`classDecl.pos <= member.pos → no TDZ`), NOT the namespaces' earliest-block positions. **The block-position gate is WRONG for MERGED/split namespaces (resolvingClassDeclarationWhenInBaseTypeResolution)**: when a namespace appears in multiple non-contiguous blocks, "namespace-earliest-block order" diverges from real declaration order, producing both FPs (use-after-base-decl wrongly flagged) and FNs (base in a later same-named block missed). Resolve the base via `resolveNsMemberClass(bEntry.second, …)` FIRST (it scans ALL merged blocks of the base namespace), then compare `classDecl.pos` to the using `member.pos`.


### TS7034/TS7005 evolving empty-array implicit-any (round 123, B135) (archived)

- **`checkEvolvingEmptyArrayImplicitAny` (TS7034/TS7005) is POSITIONAL since round 316 (was "all-reads-or-suppress"), and its per-scope pass ALSO owns the array-`push`-arg TS2345 cases (controlFlowArrayErrors)**: `EvState` tracks `firstConcretizerPos` (first ELEMENT-INTRODUCING op — push/unshift/`x[i]=`/`x=[nonEmpty]`/`f(x)`/self-spread; a bare `x=[]` is a RESET, NOT a concretizer) and routes reads to same-scope `reads` vs nested-closure `capturedReads` (via `inCaptured`). Emit: a same-scope read STRICTLY BEFORE `firstConcretizerPos` fires TS7005; a CAPTURED read (nested fn body) ALWAYS fires (tsc does no CFA across fn boundaries). Two triggers: (A) `let/var/const x=[]` (empty init) reports captured reads too; (B) `let x;`+top-level `x=[]` reports SAME-SCOPE reads only (captured owned by `checkUninitializedLetCapturedReads` → no double-emit). The 4 push-arg TS2345 are 3 DEDICATED corpus-unique sub-walkers in the same TOP-LEVEL-only scan: `evCheckSingleArrayPush` (f4/f5, single top-level array establishment), `evCheckBranchMergePush` (f6, if/else arm element SETS → set-intersection → `never`), `evCheckSnapshotPush` (f7, `let y=x` snapshot). The top-level-only scan EXCLUDES a branch-merged var (f6's `x=[true]` lives in an if-arm) from the single-array walker. **TRAP: the first cut populated `currentLocalTypes` for `x=[nonEmpty]` to reuse `checkRestArgsAgainstArrayElementType`, but `checkCallTypesInStatement` recurses into if/else, so f6's else-branch `x=[true]`→`true[]` fired a spurious 2nd TS2345 — the top-level-only dedicated walker is the fix AND removes the broad `let x=[lit]; x.method(arg)` FP surface.** **GOTCHA: `questionDotToken` on PropertyAccess/ElementAccess/CallExpression is a non-nullable `Boolean` (default false), NOT a nullable token — test `!expr.questionDotToken`, NEVER `== null` (always false → reads silently never recorded).**


### TS7022+TS7024 circular generic-callback initializer (round 127, B147) (archived)

- **TS7022/TS7024 generic-callback deadlock (`checkCircularGenericCallbackVariables`) is DISTINCT from `checkRecursiveLiteralVariables`' TS7022**: an annotated RETURN type does NOT break the circularity here — type-PARAM inference deadlocks regardless (`declare function fn1<T>(cb: () => T): string` still errors). FP firewall (load-bearing): bare-Identifier callee only (a CallExpression callee like `fn2()(() => x)` does NOT deadlock), in-program generic FunctionDeclaration with a `() => TP` param at the arg position, closure returning exactly the declared identifier.

### JSDoc `@type` on `export default {obj}` (round 127, B148) (archived)

- **JS `@type` on export-default (`checkJsDocTypeExportAssignment`) resolves `import(...)`-types LOCALLY** — `getTypeFromTypeNode`'s ImportType branch is `anyType // TODO` (Checker.kt ~60703) and fixing it THERE is broad-risk: every import-type annotation corpus-wide currently resolves to anyType and silently suppresses diagnostics. **GOTCHA: `getDeclaredTypeOfSymbol` returns the interface SHELL with `members == null` — call `resolveStructuredTypeMembers` before excess/missing-prop checks or nothing fires.** `@typedef`/`@property` are NOT parsed or bound anywhere (only raw-source scanned, here, by TS8021, and by B422's `resolveJsTypedefOptionalProps` — the only JSDoc `@typedef`-SHAPE resolver: handles `@typedef {object} N`+`@property {T} [p]` and inline `@typedef {{ p?: T }} N`, returns prop→(optional,typeText); `@type {N}` for such an unbound typedef resolves to errorType, so B422's TS2375 path resolves the shape from raw source instead).
- **Because `@typedef` is NOT bound, the EXISTING JSDoc-`@param` type-name resolver (`checkUnresolvedInType`, reached from `checkUnresolvedNames`' per-param `param.type` walk) FPs TS2304 on a VALID `@param {LocalTypedef}` (B423)** — the typedef name isn't in any scope, so the param type `LocalTypedef` looks unresolved. Fix: `checkUnresolvedNames`' `.js`/`.jsx` branch now raw-scans `@typedef [{type}] NAME` and `fileScope.addType(NAME)` so those references resolve. Any NEW JSDoc-type-position check must account for this (a bare typedef name IS a valid type even though the binder never bound it). Companion: `checkJsDocTypeNameResolution` (B423) emits TS2304/TS2552 for an UNRESOLVABLE single-bare-identifier `@typedef {Name}`/`@property {Name}` type — corpus-exhaustively FP-safe because the regex matches ONLY a single bare identifier as the whole `{...}` (skips `{a|b}`/`{import(…)}`/`{{…}}`/`{A<T>}`) and the only such unresolvable identifiers corpus-wide are the 2 intended targets. **B520 extended it to SIMPLE generic typedefs `@typedef {Name<arg, arg, ...>}`**: the head (`Record`/`Array`/…) resolves via `KNOWN_GENERIC_BUILTINS`; each top-level SIMPLE-IDENTIFIER type-arg is checked against `known` (the arg regex `<([^{}<>]*)>` excludes nested `<>`/`{}` → flat lists only, FN-not-FP for nested generics). Corpus-exhaustively FP-safe (the ONLY checkJs file with a generic inside a `@typedef`/`@property` type is `reuseTypeAnnotationImportTypeInGlobalThisTypeArgument`). **GOTCHA: `checkJsDocTypeNameResolution` is called inside the `!declarationOnly` block, so under `@emitDeclarationOnly` it is SKIPPED** — B520 added it to the `declarationOnly` block too (it is a self-contained name-resolution walker, same category as `checkUnresolvedNames`; no TS6131-style unused-decl FPs). Any other self-contained name-resolution walker a future emitDeclarationOnly test needs must likewise be added to BOTH blocks.


### Fn-param mismatch elaboration: call-sig TP → constraint display (round 81) (archived)

- **`getFunctionMismatchElaboration` displays call-sig TPs as their CONSTRAINTS instantiated through the ENCLOSING Reference's type-arg mapper** (built from the outer source/target refs). Load-bearing gate: take the branch only when BOTH instantiated constraints are non-TypeParam — when the enclosing type isn't a generic ref the constraint stays a bare TP and must fall through to the generic fallback (a raw `T[]` render matches no baseline).


### Decorator metadata type serialization gotchas (archived)

- **`null`/`undefined`/`never` → `void 0`**: Serialize as `VoidExpression(0)`, not `Object`. `never` always excluded from unions; `null`/`undefined` only filtered when `!strictNullChecks`.
- **Numeric enum type serialization**: `E.A` (QualifiedName) and plain `E` where E is a numeric enum → `Number`. String enums → `Object`.
- **Default import metadata safety wrapper**: For `db_1.default.Foo`, TypeScript wraps it: `typeof (_a = typeof db_1.default !== "undefined" && db_1.default.Foo) === "function" ? _a : Object`. Post-process `__metadata` args AFTER `rewriteIdInStatement`.
- **`export type { Foo }` / `export { TypeAlias }` make imports type-only**: Cross-file type-only detection. Check `checker.isTypeOnlyExportName` and `checker.isValueExport` during pre-scans.
- **`resolveModuleSpecifier` for absolute-path test files**: Use `fileBase == "/$baseName"` (not `endsWith`) for relative specifiers where `fileBase.startsWith("/")`.
- **`isValueExport` returns `true` (safe default)**: Only return `false` when definitively type-only (TypeAlias, Interface, non-instantiated namespace).
- **EMPTY-SUBCLASS REDUCTION in missing-property displays (B327, inheritance1, `emptySubclassBaseName`/`emptySubclassBaseNameAst`)**: in a "Property X is missing in type Y but required in type Z" message (TS2741/TS2739/TS2740/TS2420), tsc reduces a class with a **COMPLETELY EMPTY body — `decl.members.isEmpty()`, no members AT ALL, NOT even a constructor** — that `extends` exactly one base, to the BASE's name (recursively). `class ImageBase extends Control {}` displays as `Control`; **`class C extends B { constructor(){…} }` does NOT reduce (keeps `C`) — the empty-BODY gate is load-bearing (a "no own-instance-members" gate that excludes constructors-without-param-props regressed `bases.ts`).** Applied at the SOURCE display (the b127 assignment missing-property branch + the TS2420 implements CHAIN line — but the top-level "Class 'X' incorrectly implements…" keeps the class's OWN name) and the TARGET display (`getDeclaringTypeDisplay`, which now uses `emptySubclassBaseName(targetType) ?: fallback` — this REPLACED the earlier private/protected→declaring-class heuristic, a misdiagnosis whose only motivating case `c2=c` (C2 empty `extends A`, private `x`) is itself empty-subclass reduction → "A"; a NON-empty target with an inherited private member, e.g. `SelectableControl` which has `select`, must keep its own name). Interfaces never reduce (`emptySubclassBaseName` gates on `SymbolFlags.Class`).
- **Same-display-name interfaces from DIFFERENT files render TS2741 with `import("file").T` qualification** (`getSymbolImportName`, identity-search of `binderResults.locals`); fires only when both sides are different `Type.Interface` symbols resolving to different declaring files — never for lib symbols or nested-namespace exports.
- **Two `implements Array` tests have CONTRADICTORY lib-snapshot baselines (Blocker #4)**: `implementArrayInterface_ts` (`MyArray<T> implements Array<T>` declaring ~25 members) expects ZERO TS2420 missing-property errors (its baseline predates `find`/`findIndex`/`fill`/`copyWithin`/`flat`/`flatMap`/`keys`/`values`/`entries` being in `Array`), while `genericArrayExtenstions_ts` (`ObservableArray<T>` declaring only `concat`) expects "length, pop, push, join, and 24 more" = 28 missing — which REQUIRES the FULL current member set. You CANNOT satisfy both by changing the embedded `Array` member set or `LIB_MIN_TARGET` filtering: dropping the 9 post-ES5 methods to flip `implementArrayInterface` regresses `genericArrayExtenstions` (its count drops to 19). **RESOLVED surgically (B537, round 253): the TS2420 FP is suppressed in `checkImplementsClauses` ONLY when `baseIfaceName=="Array"` AND the ENTIRE missing-set ⊆ `POST_ES5_ARRAY_METHODS` (the 9 names) — implementArrayInterface's missing-set is EXACTLY those 9 → suppressed; genericArrayExtenstions's 28-member missing-set includes ES5 names (length/pop/push/join) → NOT ⊆ → untouched. The legitimate TS2416 'every' comes from the separate override checker, so the `continue@typeExprLoop` skip preserves it.** B86.13 had flipped its other two FPs (the `filter` TS2416 FP via the predicate overloads; the `every` TS2416 message via the overload-aware predicate-mismatch detector in `checkClassPropertyOverrides`) — only the TS2420 remains.
- **EXCEPTION to 'skip TS2416 for overloaded base members'**: when ≥1 base overload returns a non-asserting `this is X` TypePredicate AND the derived member is a single non-predicate method, TS2416 fires with the overload-union base display (`checkClassPropertyOverrides`). A sibling overload returning a PLAIN type (`(...): S[]`, not `this is`) does NOT trigger it.
- **The varTypes Identifier-RHS same-base-parameterized-ref check is gated to ≥1 PRIMITIVE type arg (B54.9) — do not loosen**: named-type args regressed (structurally-identical interface args `C<X>` vs `C<Y>`; `Promise<Foo>` vs `Promise<Bar>` covariance). `inferSimpleExprType` itself deliberately skips Identifier variable-type lookup (source comment at the `else -> null`).
- **Distinct-TypeParam TS2322 (`Foo<T>` vs `Foo<U>`) is implemented ONLY in the varTypes path** (`isAssignableTo(typeParams)` + `currentTypeParamDecls` for the TS2208 related info). The type-engine `structuredTypeRelatedTo` deliberately still passes distinct unconstrained TypeParams via apparent-type compatibility — changing that needs verification against `infinitelyExpandingTypes4` and instantiated-Reference comparisons.

### TS1210 class strict mode gotchas (archived)

- **Strict-mode reserved words (TS1213) in a class are checked across MANY identifier positions, split between `checkStrictModeReservedWords` (syntactic, no symbol resolution) and the heritage walker (B531, `strictModeReservedWordInClassDeclaration`)**: the strict-mode walker now also covers binary-RHS assignment chains (`checkExprForStrictReserved` recurses `expr.right` — `private = public = static`), class-member param TYPE annotations (`checkParamTypeAnnotsForStrictReserved`), named function-EXPRESSION initializer names (`var z = function let(){}`), and class TYPE-PARAMETER names. A reserved-word-named class ctor/method PARAM ALSO gets TS7006 in DEFAULT mode (`emit7006ForReservedClassParams` in the 7006 walker) — it parses as a normal named param (contextual keyword, `isIdentifier()` true) so it escapes `maybeEmit7006ForRecoveredParams`' zero-width-missing gate; a parameter-property (`public public`) squiggles the WHOLE param, a plain param the name. **A heritage base whose LEFTMOST identifier is a strict-reserved word → TS1213 + (TS2702 if it resolves to a TYPE used as a namespace qualifier `public.private.B` / TS2304 if unresolvable `package`); the RESERVED-WORD gate is the FP firewall — it never touches legitimate `implements Namespace.Member`.** `currentFileLocals` is NOT set during `checkStrictModeReservedWords`, so symbol-resolution-dependent codes (C/D) live in the heritage walker, not there.
- **`interface`/`implements` are FUTURE-RESERVED DECLARATION keywords — tsc fires TS1212 for them ONLY under REAL strict mode (module / "use strict" / strict / alwaysStrict), NOT merely target-derived ES2015 strictness (interfaceNaming1)**, unlike `let`/`yield`/`public`/… which DO fire under the target-derived path. `checkIdentForStrictReserved`/`checkExprForStrictReserved` gate `interface`/`implements` on the new `strictReservedRealStrict` flag (`!inClass && !isModule` carve-out preserved — those route to TS1213/TS1214 which ARE real-strict contexts and still fire: convertKeywordsYes alwaysStrict=true, strictModeReservedWord2 use-strict). Companion: `interface & { }` emits TS2363 for the object-literal operand — the bare keyword used-as-value resolves to error/any so `checkBinaryOperatorTypes`' any/error early-return bails BEFORE checking the OTHER operand; a corpus-unique pre-check (strict-reserved keyword identifier opposite an ObjectLiteralExpression under a bitwise/arithmetic op) emits TS2362/TS2363 + `return` (additive, no double-emit). `asiPreventsParsingAsInterface0X` baselines exist but are NOT generated → no FP risk.
- **strictModeReservedWord's non-class positions (round 367) extend the SAME `walkStmtForStrictReserved` (NOT the B531 class walker), all gated to `STRICT_MODE_RESERVED_WORDS` (FP-safe by construction — a reserved word can never be a legit param/type-qualifier/constructor/callable):** `checkTypeForStrictReserved` (a var/param TYPE annotation → TS1212 at a type-ref's leftmost qualifier `private.x` + at inline-function-type params `(private)=>void`, which ALSO get TS7006), `checkExprForStrictReserved`'s new CallExpression/ArrowFunction cases (`static()` callee + arrow-ARGUMENT params `f((private)=>{})` → TS1212 only, NO TS7006 since arrow-arg params are contextually typed), `checkClassExprForStrictReserved` (a class-EXPRESSION `var c = class package extends public {}` name+heritage → TS1213), and `emitReservedParamImplicitAny` (reserved-word Fn-decl/fn-type params → TS7006 UNCONDITIONALLY, gated `!noImplicitAny && !strict` to avoid double-emit with `checkImplicitAnyParameters` — mirrors the `checkReservedWordInterfaceParams` interface-param precedent). **A nested `"use strict"` prologue sets `strictReservedRealStrict=true` for the function-body walk** (the file-level flag is false when the directive is inside `foo()`, not at file top) — this is what makes `interface`/`implements` TS1212 fire inside a strict function body. **The type-resolution companions live OUTSIDE the strict walk** (they need scope): the reserved-word namespace-qualifier TS2503 (`public.bar`/`interface...`) is a case at the TOP of `checkTypeNameResolved`'s QualifiedName branch (pre-empts the `KEYWORD_IDENTIFIERS`/value-shadow dead-ends, and now also owns the pre-existing `private.x` TS2503 with no double-emit); the class-EXPRESSION `extends <primitive-value>` TS2507 + reserved-word primitive-value CALL `static()` TS2349 live in `checkNonConstructorExtendsInStatements` (which recurses into function bodies with a per-scope `varDecls` map — function-body `var`s are UNBOUND by the binder, so that map + `inferSimpleVarType` is the only way to type `public`/`static`). The TS2552 spelling-suggestion's TS2728 "declared here" uses a function-body-var scan `reservedWordDeclaredHere` because the shared `findDeclarationRelatedInfo` (symbol-table based) misses function-scoped locals.
