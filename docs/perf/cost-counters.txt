# COST.1 baseline — deterministic checker cost counters.
#
# Produced by: scripts/cost_gate.py --update  (profile: compiler)
# Recorded:    2026-09-06 at 0ee5b74b6420+dirty
#
# These are COUNTS, not times: they are identical across runs and boxes,
# which is the whole point — a laptop's wall clock swings +/-13% and cannot
# see a 70 ms regression. Any change here is a real change in how much work
# the compiler does. Rebaselining is allowed and expected; doing it WITHOUT
# a justification in the session note is the failure mode this file exists
# to prevent.
#
# ONE CAVEAT, measured round 776: typeNode.bypassed and mapped.keyed are also
# a function of PROGRAM ORDER — which file first touches a shared type node
# decides whether the INV.5(c) cache calls the resolution own-file or foreign.
# Three orders of the same 78 files move them ~1% with the AST and all 46
# diagnostics bit-identical. Program order is the crawl's, and the crawl sorts
# only since round 776 (before that it was the filesystem's readdir order), so
# a pre-776 value of these two is not comparable across boxes.
output.errors            46
output.programFiles      78
preparse.reused          78
preparse.fresh           0
spine.nodes              856962
typeOfExpr.calls         613091
typeOfExpr.distinct      232597
typeOfExpr.outsideInit   0
narrow.walks             37074
narrow.walksOutsideInit  0
narrow.memoServed        47908
typeNode.cacheable       182823
typeNode.cacheHits       123374
typeNode.bypassed        146428
mapped.keyed             26113
mapped.hits              6749
ctxFingerprint.builds    0
globals.lookups          803175
globals.conflated        0
globals.misses           786619
