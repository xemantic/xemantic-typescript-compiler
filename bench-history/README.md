# Bench history

Three-way compile bench — **xtsc** vs the reference JS **tsc** vs the native **tsgo** — on the pinned TypeScript `compiler` profile (`src/compiler` @ typeScriptCommit). Generated by [`scripts/bench-3way.sh`](../scripts/bench-3way.sh) via [`.github/workflows/bench.yml`](../.github/workflows/bench.yml). One row per run, newest first. Wall-clock, cold, with emit — CI runners are shared so absolute numbers drift; the **ratios** and the **trend** are the signal.

| Date | xtsc rev | xtsc | tsc | tsgo | xtsc/tsc | xtsc/tsgo | xtsc err | Run |
|---|---|---:|---:|---:|---:|---:|---:|---|
