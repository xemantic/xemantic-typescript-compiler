# Bench run 20260915T122709Z

- **xtsc revision**: [`6b2baff0033f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/6b2baff0033f498c4b90b534aba84d97be6e03db)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 6b2baff0033f498c4b90b534aba84d97be6e03db

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6b2baff0033f | **6.17s** | 31,561 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6b2baff0033f | **5.61s** | 34,724 LOC/s | 46 |
| xtsc (cold JVM) | 6b2baff0033f | 27.30s | 7,132 LOC/s | 46 |
| tsc | 6.0.3 | 11.15s | 17,462 LOC/s | 65 |
| tsgo | 7.0.2 | 1.76s | 110,689 LOC/s | 65 |
| xtsc-native | 6b2baff0033f (AOT) | 9.68s | 20,107 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.81x FASTER than tsc; 3.51x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.61s**, **-9.1%** against `temurin26` on identical bytecode — 0.50× tsc, 3.19× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.45×** tsc and **15.52×** tsgo; tsc is **6.34×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.15s**.

Ahead-of-time (GraalVM native-image): **9.68s** — **0.87×** tsc, **5.50×** tsgo, **2.82×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6b2baff0033f | **7.14s** | 27,273 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6b2baff0033f | **6.15s** | 31,669 LOC/s | 46 |
| xtsc (cold JVM) | 6b2baff0033f | 33.16s | 5,871 LOC/s | 46 |
| tsc | 6.0.3 | 13.80s | 14,110 LOC/s | 65 |
| tsgo | 7.0.2 | 2.52s | 77,232 LOC/s | 65 |
| xtsc-native | 6b2baff0033f (AOT) | 11.76s | 16,553 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.93x FASTER than tsc; 2.83x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.15s**, **-13.9%** against `temurin26` on identical bytecode — 0.45× tsc, 2.44× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.40×** tsc and **13.15×** tsgo; tsc is **5.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **33.02s**.

Ahead-of-time (GraalVM native-image): **11.76s** — **0.85×** tsc, **4.67×** tsgo, **2.82×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
