# Bench run 20260901T205222Z

- **xtsc revision**: [`333ca8bec14c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/333ca8bec14c4730a1065b34912dfb7946d187d8)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 333ca8bec14c4730a1065b34912dfb7946d187d8

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 333ca8bec14c | **3.24s** | 60,111 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 333ca8bec14c | **3.19s** | 61,035 LOC/s | 46 |
| xtsc (cold JVM) | 333ca8bec14c | 17.90s | 10,877 LOC/s | 46 |
| tsc | 6.0.3 | 7.99s | 24,362 LOC/s | 65 |
| tsgo | 7.0.2 | 1.23s | 158,037 LOC/s | 65 |
| xtsc-native | 333ca8bec14c (AOT) | 5.45s | 35,712 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.47x FASTER than tsc; 2.63x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.19s**, **-1.5%** against `temurin26` on identical bytecode — 0.40× tsc, 2.59× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.24×** tsc and **14.53×** tsgo; tsc is **6.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.79s**.

Ahead-of-time (GraalVM native-image): **5.45s** — **0.68×** tsc, **4.43×** tsgo, **3.28×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 333ca8bec14c | **3.97s** | 49,006 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 333ca8bec14c | **3.84s** | 50,677 LOC/s | 46 |
| xtsc (cold JVM) | 333ca8bec14c | 19.11s | 10,189 LOC/s | 46 |
| tsc | 6.0.3 | 10.07s | 19,332 LOC/s | 65 |
| tsgo | 7.0.2 | 1.80s | 107,868 LOC/s | 65 |
| xtsc-native | 333ca8bec14c (AOT) | 6.55s | 29,730 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.53x FASTER than tsc; 2.20x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.84s**, **-3.3%** against `temurin26` on identical bytecode — 0.38× tsc, 2.13× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.90×** tsc and **10.59×** tsgo; tsc is **5.58×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.02s**.

Ahead-of-time (GraalVM native-image): **6.55s** — **0.65×** tsc, **3.63×** tsgo, **2.92×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
