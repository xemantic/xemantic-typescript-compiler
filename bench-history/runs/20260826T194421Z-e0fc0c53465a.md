# Bench run 20260826T194421Z

- **xtsc revision**: [`e0fc0c53465a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/e0fc0c53465a14675946f096a698cd9dfd197229)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push e0fc0c53465a14675946f096a698cd9dfd197229

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e0fc0c53465a | **3.87s** | 50,271 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e0fc0c53465a | **3.80s** | 51,264 LOC/s | 46 |
| xtsc (cold JVM) | e0fc0c53465a | 22.75s | 8,560 LOC/s | 46 |
| tsc | 6.0.3 | 10.69s | 18,221 LOC/s | 65 |
| tsgo | 7.0.2 | 1.63s | 119,522 LOC/s | 65 |
| xtsc-native | e0fc0c53465a (AOT) | 7.06s | 27,566 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.76x FASTER than tsc; 2.38x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.80s**, **-1.9%** against `temurin26` on identical bytecode — 0.36× tsc, 2.33× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.13×** tsc and **13.96×** tsgo; tsc is **6.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.63s**.

Ahead-of-time (GraalVM native-image): **7.06s** — **0.66×** tsc, **4.34×** tsgo, **3.22×** the COLD JVM arm and **1.82×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e0fc0c53465a | **5.01s** | 38,885 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e0fc0c53465a | **4.54s** | 42,923 LOC/s | 46 |
| xtsc (cold JVM) | e0fc0c53465a | 24.91s | 7,814 LOC/s | 46 |
| tsc | 6.0.3 | 12.54s | 15,531 LOC/s | 65 |
| tsgo | 7.0.2 | 2.35s | 82,922 LOC/s | 65 |
| xtsc-native | e0fc0c53465a (AOT) | 8.05s | 24,186 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.50x FASTER than tsc; 2.13x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.54s**, **-9.4%** against `temurin26` on identical bytecode — 0.36× tsc, 1.93× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.99×** tsc and **10.61×** tsgo; tsc is **5.34×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.80s**.

Ahead-of-time (GraalVM native-image): **8.05s** — **0.64×** tsc, **3.43×** tsgo, **3.10×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
