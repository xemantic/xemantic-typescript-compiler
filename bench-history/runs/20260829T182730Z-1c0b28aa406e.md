# Bench run 20260829T182730Z

- **xtsc revision**: [`1c0b28aa406e`](https://github.com/xemantic/xemantic-typescript-compiler/commit/1c0b28aa406e3ffa8b0a10e9a31663ced3864a91)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 1c0b28aa406e3ffa8b0a10e9a31663ced3864a91

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1c0b28aa406e | **4.78s** | 40,724 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1c0b28aa406e | **4.52s** | 43,104 LOC/s | 46 |
| xtsc (cold JVM) | 1c0b28aa406e | 24.33s | 8,002 LOC/s | 46 |
| tsc | 6.0.3 | 11.40s | 17,077 LOC/s | 65 |
| tsgo | 7.0.2 | 1.77s | 109,876 LOC/s | 65 |
| xtsc-native | 1c0b28aa406e (AOT) | 7.84s | 24,847 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.38x FASTER than tsc; 2.70x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.52s**, **-5.5%** against `temurin26` on identical bytecode — 0.40× tsc, 2.55× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.13×** tsc and **13.73×** tsgo; tsc is **6.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.21s**.

Ahead-of-time (GraalVM native-image): **7.84s** — **0.69×** tsc, **4.42×** tsgo, **3.11×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1c0b28aa406e | **5.06s** | 38,493 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1c0b28aa406e | **4.92s** | 39,541 LOC/s | 46 |
| xtsc (cold JVM) | 1c0b28aa406e | 28.75s | 6,773 LOC/s | 46 |
| tsc | 6.0.3 | 13.32s | 14,618 LOC/s | 65 |
| tsgo | 7.0.2 | 2.40s | 81,193 LOC/s | 65 |
| xtsc-native | 1c0b28aa406e (AOT) | 8.14s | 23,913 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.63x FASTER than tsc; 2.11x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.92s**, **-2.6%** against `temurin26` on identical bytecode — 0.37× tsc, 2.05× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.16×** tsc and **11.99×** tsgo; tsc is **5.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.62s**.

Ahead-of-time (GraalVM native-image): **8.14s** — **0.61×** tsc, **3.40×** tsgo, **3.53×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
