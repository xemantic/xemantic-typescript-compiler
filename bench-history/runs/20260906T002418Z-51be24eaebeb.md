# Bench run 20260906T002418Z

- **xtsc revision**: [`51be24eaebeb`](https://github.com/xemantic/xemantic-typescript-compiler/commit/51be24eaebeb2ecfd162782f3c758af46a0aee3b)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 51be24eaebeb2ecfd162782f3c758af46a0aee3b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 51be24eaebeb | **12.63s** | 15,420 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 51be24eaebeb | **13.64s** | 14,272 LOC/s | 46 |
| xtsc (cold JVM) | 51be24eaebeb | 30.36s | 6,412 LOC/s | 46 |
| tsc | 6.0.3 | 9.54s | 20,415 LOC/s | 65 |
| tsgo | 7.0.2 | 1.48s | 131,733 LOC/s | 65 |
| xtsc-native | 51be24eaebeb (AOT) | 21.80s | 8,930 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.32x tsc — parity at 1.00x; 8.54x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **13.64s**, **+8.0%** against `temurin26` on identical bytecode — 1.43× tsc, 9.23× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.18×** tsc and **20.54×** tsgo; tsc is **6.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.24s**.

Ahead-of-time (GraalVM native-image): **21.80s** — **2.29×** tsc, **14.75×** tsgo, **1.39×** the COLD JVM arm and **1.73×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 51be24eaebeb | **13.96s** | 13,943 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 51be24eaebeb | **14.44s** | 13,487 LOC/s | 46 |
| xtsc (cold JVM) | 51be24eaebeb | 32.13s | 6,059 LOC/s | 46 |
| tsc | 6.0.3 | 11.68s | 16,675 LOC/s | 65 |
| tsgo | 7.0.2 | 2.13s | 91,495 LOC/s | 65 |
| xtsc-native | 51be24eaebeb (AOT) | 22.87s | 8,511 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.20x tsc — parity at 1.00x; 6.56x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **14.44s**, **+3.4%** against `temurin26` on identical bytecode — 1.24× tsc, 6.78× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.75×** tsc and **15.10×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **32.01s**.

Ahead-of-time (GraalVM native-image): **22.87s** — **1.96×** tsc, **10.75×** tsgo, **1.40×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
