# Bench run 20260807T095914Z

- **xtsc revision**: [`d1058c8d9bfb`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d1058c8d9bfb21426d87216ca1cad5ebb4abce83)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push d1058c8d9bfb21426d87216ca1cad5ebb4abce83

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | d1058c8d9bfb | 27.33s | 7,124 LOC/s | 46 |
| tsc | 6.0.3 | 11.00s | 17,693 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.75s | 111,385 LOC/s | 65 |
| xtsc-native | d1058c8d9bfb (AOT) | 11.65s | 16,714 LOC/s | 46 |

Relative (wall): xtsc is **2.48×** tsc and **15.63×** tsgo; tsc is **6.30×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.22s**.

Ahead-of-time (GraalVM native-image): **11.65s**, **2.35×** the JVM arm, **1.06×** tsc, **6.66×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | d1058c8d9bfb | 31.16s | 6,249 LOC/s | 46 |
| tsc | 6.0.3 | 14.21s | 13,703 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.51s | 77,601 LOC/s | 65 |
| xtsc-native | d1058c8d9bfb (AOT) | 14.52s | 13,412 LOC/s | 46 |

Relative (wall): xtsc is **2.19×** tsc and **12.42×** tsgo; tsc is **5.66×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **31.05s**.

Ahead-of-time (GraalVM native-image): **14.52s**, **2.15×** the JVM arm, **1.02×** tsc, **5.79×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
