# Bench run 20260823T165822Z

- **xtsc revision**: [`7b1cc32395c0`](https://github.com/xemantic/xemantic-typescript-compiler/commit/7b1cc32395c07f29a925753037c41c627230865d)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 7b1cc32395c07f29a925753037c41c627230865d

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7b1cc32395c0 | **3.85s** | 50,585 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7b1cc32395c0 | **3.69s** | 52,793 LOC/s | 46 |
| xtsc (cold JVM) | 7b1cc32395c0 | 22.03s | 8,838 LOC/s | 46 |
| tsc | 6.0.3 | 10.69s | 18,211 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,344 LOC/s | 65 |
| xtsc-native | 7b1cc32395c0 (AOT) | 6.01s | 32,396 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.78x FASTER than tsc; 2.28x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.69s**, **-4.2%** against `temurin26` on identical bytecode — 0.34× tsc, 2.18× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.06×** tsc and **13.05×** tsgo; tsc is **6.33×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.92s**.

Ahead-of-time (GraalVM native-image): **6.01s** — **0.56×** tsc, **3.56×** tsgo, **3.67×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7b1cc32395c0 | **4.67s** | 41,736 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7b1cc32395c0 | **4.29s** | 45,332 LOC/s | 46 |
| xtsc (cold JVM) | 7b1cc32395c0 | 23.48s | 8,293 LOC/s | 46 |
| tsc | 6.0.3 | 13.23s | 14,715 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,738 LOC/s | 65 |
| xtsc-native | 7b1cc32395c0 (AOT) | 7.36s | 26,443 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.84x FASTER than tsc; 1.96x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.29s**, **-7.9%** against `temurin26` on identical bytecode — 0.32× tsc, 1.80× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.77×** tsc and **9.86×** tsgo; tsc is **5.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.36s**.

Ahead-of-time (GraalVM native-image): **7.36s** — **0.56×** tsc, **3.09×** tsgo, **3.19×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
