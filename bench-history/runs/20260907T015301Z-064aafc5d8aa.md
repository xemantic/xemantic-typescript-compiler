# Bench run 20260907T015301Z

- **xtsc revision**: [`064aafc5d8aa`](https://github.com/xemantic/xemantic-typescript-compiler/commit/064aafc5d8aa405f7e2d7eb3edb798ed44c0af87)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 064aafc5d8aa405f7e2d7eb3edb798ed44c0af87

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 064aafc5d8aa | **15.37s** | 12,667 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 064aafc5d8aa | **14.94s** | 13,028 LOC/s | 46 |
| xtsc (cold JVM) | 064aafc5d8aa | 33.65s | 5,785 LOC/s | 46 |
| tsc | 6.0.3 | 10.41s | 18,708 LOC/s | 65 |
| tsgo | 7.0.2 | 1.55s | 125,857 LOC/s | 65 |
| xtsc-native | 064aafc5d8aa (AOT) | 24.21s | 8,042 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.48x tsc — parity at 1.00x; 9.94x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **14.94s**, **-2.8%** against `temurin26` on identical bytecode — 1.44× tsc, 9.66× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.23×** tsc and **21.75×** tsgo; tsc is **6.73×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **33.53s**.

Ahead-of-time (GraalVM native-image): **24.21s** — **2.33×** tsc, **15.65×** tsgo, **1.39×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 064aafc5d8aa | **15.45s** | 12,602 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 064aafc5d8aa | **15.72s** | 12,388 LOC/s | 46 |
| xtsc (cold JVM) | 064aafc5d8aa | 35.71s | 5,452 LOC/s | 46 |
| tsc | 6.0.3 | 12.46s | 15,629 LOC/s | 65 |
| tsgo | 7.0.2 | 2.26s | 86,265 LOC/s | 65 |
| xtsc-native | 064aafc5d8aa (AOT) | 25.31s | 7,692 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.24x tsc — parity at 1.00x; 6.85x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **15.72s**, **+1.7%** against `temurin26` on identical bytecode — 1.26× tsc, 6.96× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.87×** tsc and **15.82×** tsgo; tsc is **5.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **35.58s**.

Ahead-of-time (GraalVM native-image): **25.31s** — **2.03×** tsc, **11.21×** tsgo, **1.41×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
