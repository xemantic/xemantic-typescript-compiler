# Bench run 20260831T201706Z

- **xtsc revision**: [`8489b6aedb28`](https://github.com/xemantic/xemantic-typescript-compiler/commit/8489b6aedb280ee90e52627a71f9eb08de5d35e9)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 8489b6aedb280ee90e52627a71f9eb08de5d35e9

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8489b6aedb28 | **4.28s** | 45,512 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8489b6aedb28 | **3.96s** | 49,216 LOC/s | 46 |
| xtsc (cold JVM) | 8489b6aedb28 | 25.26s | 7,708 LOC/s | 46 |
| tsc | 6.0.3 | 10.65s | 18,280 LOC/s | 65 |
| tsgo | 7.0.2 | 1.63s | 119,375 LOC/s | 65 |
| xtsc-native | 8489b6aedb28 (AOT) | 6.70s | 29,068 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.49x FASTER than tsc; 2.62x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.96s**, **-7.5%** against `temurin26` on identical bytecode — 0.37× tsc, 2.43× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.37×** tsc and **15.49×** tsgo; tsc is **6.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.14s**.

Ahead-of-time (GraalVM native-image): **6.70s** — **0.63×** tsc, **4.11×** tsgo, **3.77×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8489b6aedb28 | **5.12s** | 38,064 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8489b6aedb28 | **4.63s** | 42,052 LOC/s | 46 |
| xtsc (cold JVM) | 8489b6aedb28 | 25.58s | 7,610 LOC/s | 46 |
| tsc | 6.0.3 | 13.19s | 14,761 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,841 LOC/s | 65 |
| xtsc-native | 8489b6aedb28 (AOT) | 7.62s | 25,564 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.58x FASTER than tsc; 2.15x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.63s**, **-9.5%** against `temurin26` on identical bytecode — 0.35× tsc, 1.95× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.94×** tsc and **10.75×** tsgo; tsc is **5.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.44s**.

Ahead-of-time (GraalVM native-image): **7.62s** — **0.58×** tsc, **3.20×** tsgo, **3.36×** the COLD JVM arm and **1.49×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
