# Bench run 20260830T201556Z

- **xtsc revision**: [`65b8a3381975`](https://github.com/xemantic/xemantic-typescript-compiler/commit/65b8a3381975fcff4a716075fe8ac097508eeb5e)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 65b8a3381975fcff4a716075fe8ac097508eeb5e

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 65b8a3381975 | **4.39s** | 44,351 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 65b8a3381975 | **4.10s** | 47,465 LOC/s | 46 |
| xtsc (cold JVM) | 65b8a3381975 | 24.13s | 8,069 LOC/s | 46 |
| tsc | 6.0.3 | 10.91s | 17,849 LOC/s | 65 |
| tsgo | 7.0.2 | 1.68s | 116,170 LOC/s | 65 |
| xtsc-native | 65b8a3381975 (AOT) | 6.90s | 28,225 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.48x FASTER than tsc; 2.62x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.10s**, **-6.6%** against `temurin26` on identical bytecode — 0.38× tsc, 2.45× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.21×** tsc and **14.40×** tsgo; tsc is **6.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.02s**.

Ahead-of-time (GraalVM native-image): **6.90s** — **0.63×** tsc, **4.12×** tsgo, **3.50×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 65b8a3381975 | **5.34s** | 36,433 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 65b8a3381975 | **4.97s** | 39,143 LOC/s | 46 |
| xtsc (cold JVM) | 65b8a3381975 | 28.19s | 6,907 LOC/s | 46 |
| tsc | 6.0.3 | 13.43s | 14,495 LOC/s | 65 |
| tsgo | 7.0.2 | 2.39s | 81,363 LOC/s | 65 |
| xtsc-native | 65b8a3381975 (AOT) | 7.62s | 25,548 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.51x FASTER than tsc; 2.23x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.97s**, **-6.9%** against `temurin26` on identical bytecode — 0.37× tsc, 2.08× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.10×** tsc and **11.78×** tsgo; tsc is **5.61×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.02s**.

Ahead-of-time (GraalVM native-image): **7.62s** — **0.57×** tsc, **3.18×** tsgo, **3.70×** the COLD JVM arm and **1.43×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
