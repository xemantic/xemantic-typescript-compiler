# Bench run 20260911T013238Z

- **xtsc revision**: [`5c4dc1f33eda`](https://github.com/xemantic/xemantic-typescript-compiler/commit/5c4dc1f33eda1bdf852e26a8311c6ebaf46b89c5)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 5c4dc1f33eda1bdf852e26a8311c6ebaf46b89c5

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5c4dc1f33eda | **3.77s** | 51,590 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5c4dc1f33eda | **3.53s** | 55,125 LOC/s | 46 |
| xtsc (cold JVM) | 5c4dc1f33eda | 20.52s | 9,487 LOC/s | 46 |
| tsc | 6.0.3 | 7.84s | 24,843 LOC/s | 65 |
| tsgo | 7.0.2 | 1.23s | 158,423 LOC/s | 65 |
| xtsc-native | 5c4dc1f33eda (AOT) | 5.94s | 32,789 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.08x FASTER than tsc; 3.07x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.53s**, **-6.4%** against `temurin26` on identical bytecode — 0.45× tsc, 2.87× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.62×** tsc and **16.70×** tsgo; tsc is **6.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.44s**.

Ahead-of-time (GraalVM native-image): **5.94s** — **0.76×** tsc, **4.83×** tsgo, **3.46×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5c4dc1f33eda | **4.45s** | 43,753 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5c4dc1f33eda | **4.25s** | 45,855 LOC/s | 46 |
| xtsc (cold JVM) | 5c4dc1f33eda | 22.70s | 8,576 LOC/s | 46 |
| tsc | 6.0.3 | 9.66s | 20,163 LOC/s | 65 |
| tsgo | 7.0.2 | 1.81s | 107,629 LOC/s | 65 |
| xtsc-native | 5c4dc1f33eda (AOT) | 7.14s | 27,261 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.17x FASTER than tsc; 2.46x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.25s**, **-4.6%** against `temurin26` on identical bytecode — 0.44× tsc, 2.35× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.35×** tsc and **12.55×** tsgo; tsc is **5.34×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.59s**.

Ahead-of-time (GraalVM native-image): **7.14s** — **0.74×** tsc, **3.95×** tsgo, **3.18×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
