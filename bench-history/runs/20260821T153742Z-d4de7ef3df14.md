# Bench run 20260821T153742Z

- **xtsc revision**: [`d4de7ef3df14`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d4de7ef3df14b35a26e83d90de196113b3698e1e)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push d4de7ef3df14b35a26e83d90de196113b3698e1e

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d4de7ef3df14 | **3.96s** | 49,117 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d4de7ef3df14 | **3.79s** | 51,440 LOC/s | 46 |
| xtsc (cold JVM) | d4de7ef3df14 | 22.06s | 8,826 LOC/s | 46 |
| tsc | 6.0.3 | 10.73s | 18,142 LOC/s | 65 |
| tsgo | 7.0.2 | 1.70s | 114,261 LOC/s | 65 |
| xtsc-native | d4de7ef3df14 (AOT) | 6.00s | 32,434 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.71x FASTER than tsc; 2.33x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.79s**, **-4.5%** against `temurin26` on identical bytecode — 0.35× tsc, 2.22× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.06×** tsc and **12.95×** tsgo; tsc is **6.30×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.95s**.

Ahead-of-time (GraalVM native-image): **6.00s** — **0.56×** tsc, **3.52×** tsgo, **3.67×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d4de7ef3df14 | **4.82s** | 40,361 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d4de7ef3df14 | **4.38s** | 44,452 LOC/s | 46 |
| xtsc (cold JVM) | d4de7ef3df14 | 24.43s | 7,970 LOC/s | 46 |
| tsc | 6.0.3 | 13.16s | 14,793 LOC/s | 65 |
| tsgo | 7.0.2 | 2.44s | 79,697 LOC/s | 65 |
| xtsc-native | d4de7ef3df14 (AOT) | 7.06s | 27,593 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.73x FASTER than tsc; 1.97x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.38s**, **-9.2%** against `temurin26` on identical bytecode — 0.33× tsc, 1.79× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.86×** tsc and **10.00×** tsgo; tsc is **5.39×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.32s**.

Ahead-of-time (GraalVM native-image): **7.06s** — **0.54×** tsc, **2.89×** tsgo, **3.46×** the COLD JVM arm and **1.46×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
