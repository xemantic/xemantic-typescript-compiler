# Bench run 20260923T080634Z

- **xtsc revision**: [`8552c3214e1e`](https://github.com/xemantic/xemantic-typescript-compiler/commit/8552c3214e1e6a79d712e40eaee1e072e30e1776)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 8552c3214e1e6a79d712e40eaee1e072e30e1776

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8552c3214e1e | **5.95s** | 32,701 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8552c3214e1e | **5.61s** | 34,693 LOC/s | 46 |
| xtsc (cold JVM) | 8552c3214e1e | 30.21s | 6,444 LOC/s | 46 |
| tsc | 6.0.3 | 11.31s | 17,218 LOC/s | 65 |
| tsgo | 7.0.2 | 1.76s | 110,375 LOC/s | 65 |
| xtsc-native | 8552c3214e1e (AOT) | 9.86s | 19,756 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.90x FASTER than tsc; 3.38x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.61s**, **-5.7%** against `temurin26` on identical bytecode — 0.50× tsc, 3.18× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.67×** tsc and **17.13×** tsgo; tsc is **6.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.09s**.

Ahead-of-time (GraalVM native-image): **9.86s** — **0.87×** tsc, **5.59×** tsgo, **3.07×** the COLD JVM arm and **1.66×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8552c3214e1e | **7.07s** | 27,543 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8552c3214e1e | **6.01s** | 32,380 LOC/s | 46 |
| xtsc (cold JVM) | 8552c3214e1e | 29.53s | 6,594 LOC/s | 46 |
| tsc | 6.0.3 | 13.32s | 14,615 LOC/s | 65 |
| tsgo | 7.0.2 | 2.41s | 80,722 LOC/s | 65 |
| xtsc-native | 8552c3214e1e (AOT) | 10.82s | 18,001 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.88x FASTER than tsc; 2.93x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.01s**, **-14.9%** against `temurin26` on identical bytecode — 0.45× tsc, 2.49× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.22×** tsc and **12.24×** tsgo; tsc is **5.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.39s**.

Ahead-of-time (GraalVM native-image): **10.82s** — **0.81×** tsc, **4.48×** tsgo, **2.73×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
