# Bench run 20260818T155016Z

- **xtsc revision**: [`29564def202e`](https://github.com/xemantic/xemantic-typescript-compiler/commit/29564def202e9f2c86de6007e3424d45f3341a7b)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 29564def202e9f2c86de6007e3424d45f3341a7b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 29564def202e | **4.03s** | 48,349 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 29564def202e | **3.83s** | 50,796 LOC/s | 46 |
| xtsc (cold JVM) | 29564def202e | 23.62s | 8,242 LOC/s | 46 |
| tsc | 6.0.3 | 10.76s | 18,096 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,481 LOC/s | 65 |
| xtsc-native | 29564def202e (AOT) | 6.08s | 32,033 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.67x FASTER than tsc; 2.39x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.83s**, **-4.8%** against `temurin26` on identical bytecode — 0.36× tsc, 2.27× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.20×** tsc and **14.01×** tsgo; tsc is **6.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.51s**.

Ahead-of-time (GraalVM native-image): **6.08s** — **0.56×** tsc, **3.60×** tsgo, **3.89×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 29564def202e | **4.68s** | 41,638 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 29564def202e | **4.45s** | 43,723 LOC/s | 46 |
| xtsc (cold JVM) | 29564def202e | 24.82s | 7,843 LOC/s | 46 |
| tsc | 6.0.3 | 13.38s | 14,554 LOC/s | 65 |
| tsgo | 7.0.2 | 2.46s | 79,179 LOC/s | 65 |
| xtsc-native | 29564def202e (AOT) | 7.41s | 26,282 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.86x FASTER than tsc; 1.90x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.45s**, **-4.8%** against `temurin26` on identical bytecode — 0.33× tsc, 1.81× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.86×** tsc and **10.10×** tsgo; tsc is **5.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.68s**.

Ahead-of-time (GraalVM native-image): **7.41s** — **0.55×** tsc, **3.01×** tsgo, **3.35×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
