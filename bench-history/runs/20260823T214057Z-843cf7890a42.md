# Bench run 20260823T214057Z

- **xtsc revision**: [`843cf7890a42`](https://github.com/xemantic/xemantic-typescript-compiler/commit/843cf7890a42716dbfffb8610282b2e6ed1549c2)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 843cf7890a42716dbfffb8610282b2e6ed1549c2

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 843cf7890a42 | **4.65s** | 41,898 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 843cf7890a42 | **4.09s** | 47,639 LOC/s | 46 |
| xtsc (cold JVM) | 843cf7890a42 | 23.14s | 8,415 LOC/s | 46 |
| tsc | 6.0.3 | 11.46s | 16,995 LOC/s | 65 |
| tsgo | 7.0.2 | 1.76s | 110,312 LOC/s | 65 |
| xtsc-native | 843cf7890a42 (AOT) | 6.74s | 28,887 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.47x FASTER than tsc; 2.63x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.09s**, **-12.1%** against `temurin26` on identical bytecode — 0.36× tsc, 2.32× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.02×** tsc and **13.11×** tsgo; tsc is **6.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.02s**.

Ahead-of-time (GraalVM native-image): **6.74s** — **0.59×** tsc, **3.82×** tsgo, **3.43×** the COLD JVM arm and **1.45×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 843cf7890a42 | **5.34s** | 36,433 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 843cf7890a42 | **4.87s** | 39,963 LOC/s | 46 |
| xtsc (cold JVM) | 843cf7890a42 | 29.51s | 6,598 LOC/s | 46 |
| tsc | 6.0.3 | 14.36s | 13,556 LOC/s | 65 |
| tsgo | 7.0.2 | 2.58s | 75,465 LOC/s | 65 |
| xtsc-native | 843cf7890a42 (AOT) | 8.64s | 22,521 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.69x FASTER than tsc; 2.07x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.87s**, **-8.8%** against `temurin26` on identical bytecode — 0.34× tsc, 1.89× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.05×** tsc and **11.44×** tsgo; tsc is **5.57×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.38s**.

Ahead-of-time (GraalVM native-image): **8.64s** — **0.60×** tsc, **3.35×** tsgo, **3.41×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
