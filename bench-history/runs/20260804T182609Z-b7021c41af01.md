# Bench run 20260804T182609Z

- **xtsc revision**: [`b7021c41af01`](https://github.com/xemantic/xemantic-typescript-compiler/commit/b7021c41af0191c279d1a0f9ec4a8c87f78fe3d0)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push b7021c41af0191c279d1a0f9ec4a8c87f78fe3d0

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | b7021c41af01 | 26.25s | 7,416 LOC/s | 46 |
| tsc | 6.0.3 | 11.16s | 17,444 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.76s | 110,437 LOC/s | 65 |
| xtsc-native | b7021c41af01 (AOT) | 10.74s | 18,123 LOC/s | 46 |

Relative (wall): xtsc is **2.35×** tsc and **14.89×** tsgo; tsc is **6.33×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.14s**.

Ahead-of-time (GraalVM native-image): **10.74s**, **2.44×** the JVM arm, **0.96×** tsc, **6.09×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | b7021c41af01 | 29.08s | 6,694 LOC/s | 46 |
| tsc | 6.0.3 | 13.64s | 14,278 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.47s | 78,826 LOC/s | 65 |
| xtsc-native | b7021c41af01 (AOT) | 13.30s | 14,641 LOC/s | 46 |

Relative (wall): xtsc is **2.13×** tsc and **11.77×** tsgo; tsc is **5.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.97s**.

Ahead-of-time (GraalVM native-image): **13.30s**, **2.19×** the JVM arm, **0.98×** tsc, **5.38×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
