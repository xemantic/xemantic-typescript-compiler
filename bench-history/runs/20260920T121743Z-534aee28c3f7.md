# Bench run 20260920T121743Z

- **xtsc revision**: [`534aee28c3f7`](https://github.com/xemantic/xemantic-typescript-compiler/commit/534aee28c3f720bbf198d68672d76a932a0c6600)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 534aee28c3f720bbf198d68672d76a932a0c6600

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 534aee28c3f7 | **6.06s** | 32,134 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 534aee28c3f7 | **5.30s** | 36,750 LOC/s | 46 |
| xtsc (cold JVM) | 534aee28c3f7 | 28.44s | 6,845 LOC/s | 46 |
| tsc | 6.0.3 | 11.10s | 17,534 LOC/s | 65 |
| tsgo | 7.0.2 | 1.78s | 109,321 LOC/s | 65 |
| xtsc-native | 534aee28c3f7 (AOT) | 9.62s | 20,232 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.83x FASTER than tsc; 3.40x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.30s**, **-12.6%** against `temurin26` on identical bytecode — 0.48× tsc, 2.97× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.56×** tsc and **15.97×** tsgo; tsc is **6.23×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.32s**.

Ahead-of-time (GraalVM native-image): **9.62s** — **0.87×** tsc, **5.40×** tsgo, **2.96×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 534aee28c3f7 | **6.99s** | 27,874 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 534aee28c3f7 | **6.28s** | 30,998 LOC/s | 46 |
| xtsc (cold JVM) | 534aee28c3f7 | 30.96s | 6,289 LOC/s | 46 |
| tsc | 6.0.3 | 13.97s | 13,933 LOC/s | 65 |
| tsgo | 7.0.2 | 2.56s | 76,144 LOC/s | 65 |
| xtsc-native | 534aee28c3f7 (AOT) | 11.53s | 16,893 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.00x FASTER than tsc; 2.73x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.28s**, **-10.1%** against `temurin26` on identical bytecode — 0.45× tsc, 2.46× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.22×** tsc and **12.11×** tsgo; tsc is **5.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.83s**.

Ahead-of-time (GraalVM native-image): **11.53s** — **0.82×** tsc, **4.51×** tsgo, **2.69×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
