# Bench run 20260823T001229Z

- **xtsc revision**: [`97780ff13da6`](https://github.com/xemantic/xemantic-typescript-compiler/commit/97780ff13da6f053de4dfccd5c4024b661f2a568)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 97780ff13da6f053de4dfccd5c4024b661f2a568

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 97780ff13da6 | **4.18s** | 46,579 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 97780ff13da6 | **4.11s** | 47,407 LOC/s | 46 |
| xtsc (cold JVM) | 97780ff13da6 | 23.55s | 8,269 LOC/s | 46 |
| tsc | 6.0.3 | 11.63s | 16,739 LOC/s | 65 |
| tsgo | 7.0.2 | 1.85s | 104,960 LOC/s | 65 |
| xtsc-native | 97780ff13da6 (AOT) | 7.16s | 27,193 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.78x FASTER than tsc; 2.25x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.11s**, **-1.7%** against `temurin26` on identical bytecode — 0.35× tsc, 2.21× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.02×** tsc and **12.69×** tsgo; tsc is **6.27×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.41s**.

Ahead-of-time (GraalVM native-image): **7.16s** — **0.62×** tsc, **3.86×** tsgo, **3.29×** the COLD JVM arm and **1.71×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 97780ff13da6 | **5.36s** | 36,304 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 97780ff13da6 | **4.81s** | 40,453 LOC/s | 46 |
| xtsc (cold JVM) | 97780ff13da6 | 25.34s | 7,684 LOC/s | 46 |
| tsc | 6.0.3 | 14.89s | 13,073 LOC/s | 65 |
| tsgo | 7.0.2 | 2.66s | 73,113 LOC/s | 65 |
| xtsc-native | 97780ff13da6 (AOT) | 9.33s | 20,870 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.78x FASTER than tsc; 2.01x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.81s**, **-10.3%** against `temurin26` on identical bytecode — 0.32× tsc, 1.81× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.70×** tsc and **9.51×** tsgo; tsc is **5.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.21s**.

Ahead-of-time (GraalVM native-image): **9.33s** — **0.63×** tsc, **3.50×** tsgo, **2.72×** the COLD JVM arm and **1.74×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
