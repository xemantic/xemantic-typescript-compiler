# Bench run 20260913T123921Z

- **xtsc revision**: [`8220da295309`](https://github.com/xemantic/xemantic-typescript-compiler/commit/8220da29530940a0d4e83a7fcd74c9934e148882)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 8220da29530940a0d4e83a7fcd74c9934e148882

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8220da295309 | **5.76s** | 33,796 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8220da295309 | **5.41s** | 36,022 LOC/s | 46 |
| xtsc (cold JVM) | 8220da295309 | 26.94s | 7,228 LOC/s | 46 |
| tsc | 6.0.3 | 10.65s | 18,273 LOC/s | 65 |
| tsgo | 7.0.2 | 1.62s | 120,334 LOC/s | 65 |
| xtsc-native | 8220da295309 (AOT) | 9.95s | 19,577 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.85x FASTER than tsc; 3.56x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.41s**, **-6.2%** against `temurin26` on identical bytecode — 0.51× tsc, 3.34× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.53×** tsc and **16.65×** tsgo; tsc is **6.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.82s**.

Ahead-of-time (GraalVM native-image): **9.95s** — **0.93×** tsc, **6.15×** tsgo, **2.71×** the COLD JVM arm and **1.73×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8220da295309 | **6.45s** | 30,186 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8220da295309 | **5.98s** | 32,569 LOC/s | 46 |
| xtsc (cold JVM) | 8220da295309 | 27.54s | 7,071 LOC/s | 46 |
| tsc | 6.0.3 | 13.12s | 14,834 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,773 LOC/s | 65 |
| xtsc-native | 8220da295309 (AOT) | 10.85s | 17,939 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.03x FASTER than tsc; 2.71x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.98s**, **-7.3%** against `temurin26` on identical bytecode — 0.46× tsc, 2.51× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.10×** tsc and **11.56×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.41s**.

Ahead-of-time (GraalVM native-image): **10.85s** — **0.83×** tsc, **4.56×** tsgo, **2.54×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
