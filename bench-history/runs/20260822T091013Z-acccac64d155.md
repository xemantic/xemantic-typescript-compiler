# Bench run 20260822T091013Z

- **xtsc revision**: [`acccac64d155`](https://github.com/xemantic/xemantic-typescript-compiler/commit/acccac64d155a953568a4b3e4df58a466311aeae)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push acccac64d155a953568a4b3e4df58a466311aeae

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | acccac64d155 | **3.45s** | 56,386 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | acccac64d155 | **3.37s** | 57,792 LOC/s | 46 |
| xtsc (cold JVM) | acccac64d155 | 19.36s | 10,058 LOC/s | 46 |
| tsc | 6.0.3 | 8.80s | 22,132 LOC/s | 65 |
| tsgo | 7.0.2 | 1.33s | 145,844 LOC/s | 65 |
| xtsc-native | acccac64d155 (AOT) | 5.88s | 33,140 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.55x FASTER than tsc; 2.59x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.37s**, **-2.4%** against `temurin26` on identical bytecode — 0.38× tsc, 2.52× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.20×** tsc and **14.50×** tsgo; tsc is **6.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.26s**.

Ahead-of-time (GraalVM native-image): **5.88s** — **0.67×** tsc, **4.40×** tsgo, **3.29×** the COLD JVM arm and **1.70×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | acccac64d155 | **4.07s** | 47,850 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | acccac64d155 | **3.84s** | 50,730 LOC/s | 46 |
| xtsc (cold JVM) | acccac64d155 | 22.20s | 8,768 LOC/s | 46 |
| tsc | 6.0.3 | 10.23s | 19,032 LOC/s | 65 |
| tsgo | 7.0.2 | 1.87s | 104,230 LOC/s | 65 |
| xtsc-native | acccac64d155 (AOT) | 6.41s | 30,398 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.51x FASTER than tsc; 2.18x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.84s**, **-5.7%** against `temurin26` on identical bytecode — 0.38× tsc, 2.05× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.17×** tsc and **11.89×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.10s**.

Ahead-of-time (GraalVM native-image): **6.41s** — **0.63×** tsc, **3.43×** tsgo, **3.47×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
