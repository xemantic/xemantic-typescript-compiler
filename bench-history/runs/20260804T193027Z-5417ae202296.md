# Bench run 20260804T193027Z

- **xtsc revision**: [`5417ae202296`](https://github.com/xemantic/xemantic-typescript-compiler/commit/5417ae202296e228d727d65f37bf7e74db956a79)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 5417ae202296e228d727d65f37bf7e74db956a79

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 5417ae202296 | 26.42s | 7,370 LOC/s | 46 |
| tsc | 6.0.3 | 11.41s | 17,058 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.75s | 111,067 LOC/s | 65 |
| xtsc-native | 5417ae202296 (AOT) | 11.52s | 16,904 LOC/s | 46 |

Relative (wall): xtsc is **2.31×** tsc and **15.07×** tsgo; tsc is **6.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.31s**.

Ahead-of-time (GraalVM native-image): **11.52s**, **2.29×** the JVM arm, **1.01×** tsc, **6.57×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 5417ae202296 | 30.39s | 6,407 LOC/s | 46 |
| tsc | 6.0.3 | 13.80s | 14,111 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.51s | 77,447 LOC/s | 65 |
| xtsc-native | 5417ae202296 (AOT) | 12.67s | 15,372 LOC/s | 46 |

Relative (wall): xtsc is **2.20×** tsc and **12.09×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.28s**.

Ahead-of-time (GraalVM native-image): **12.67s**, **2.40×** the JVM arm, **0.92×** tsc, **5.04×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
