# Bench run 20260915T223947Z

- **xtsc revision**: [`eb6dcb175679`](https://github.com/xemantic/xemantic-typescript-compiler/commit/eb6dcb17567955e6c8c512a6983a3a5654a2bb19)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push eb6dcb17567955e6c8c512a6983a3a5654a2bb19

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | eb6dcb175679 | **5.67s** | 34,320 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | eb6dcb175679 | **5.28s** | 36,882 LOC/s | 46 |
| xtsc (cold JVM) | eb6dcb175679 | 23.79s | 8,183 LOC/s | 46 |
| tsc | 6.0.3 | 10.41s | 18,708 LOC/s | 65 |
| tsgo | 7.0.2 | 1.59s | 122,377 LOC/s | 65 |
| xtsc-native | eb6dcb175679 (AOT) | 9.27s | 21,001 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.83x FASTER than tsc; 3.57x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.28s**, **-6.9%** against `temurin26` on identical bytecode — 0.51× tsc, 3.32× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.29×** tsc and **14.95×** tsgo; tsc is **6.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.67s**.

Ahead-of-time (GraalVM native-image): **9.27s** — **0.89×** tsc, **5.83×** tsgo, **2.57×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | eb6dcb175679 | **6.47s** | 30,102 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | eb6dcb175679 | **5.80s** | 33,551 LOC/s | 46 |
| xtsc (cold JVM) | eb6dcb175679 | 27.78s | 7,009 LOC/s | 46 |
| tsc | 6.0.3 | 12.52s | 15,548 LOC/s | 65 |
| tsgo | 7.0.2 | 2.25s | 86,380 LOC/s | 65 |
| xtsc-native | eb6dcb175679 (AOT) | 10.43s | 18,662 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.94x FASTER than tsc; 2.87x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.80s**, **-10.3%** against `temurin26` on identical bytecode — 0.46× tsc, 2.57× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.22×** tsc and **12.32×** tsgo; tsc is **5.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.66s**.

Ahead-of-time (GraalVM native-image): **10.43s** — **0.83×** tsc, **4.63×** tsgo, **2.66×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
