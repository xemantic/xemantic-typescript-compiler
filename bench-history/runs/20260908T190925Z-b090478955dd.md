# Bench run 20260908T190925Z

- **xtsc revision**: [`b090478955dd`](https://github.com/xemantic/xemantic-typescript-compiler/commit/b090478955dd98bf3ee81f95a58307d9fa7238ab)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push b090478955dd98bf3ee81f95a58307d9fa7238ab

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b090478955dd | **4.28s** | 45,459 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b090478955dd | **4.15s** | 46,870 LOC/s | 46 |
| xtsc (cold JVM) | b090478955dd | 24.69s | 7,886 LOC/s | 46 |
| tsc | 6.0.3 | 9.38s | 20,746 LOC/s | 65 |
| tsgo | 7.0.2 | 1.46s | 133,266 LOC/s | 65 |
| xtsc-native | b090478955dd (AOT) | 6.92s | 28,156 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.19x FASTER than tsc; 2.93x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.15s**, **-3.0%** against `temurin26` on identical bytecode — 0.44× tsc, 2.84× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.63×** tsc and **16.90×** tsgo; tsc is **6.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.59s**.

Ahead-of-time (GraalVM native-image): **6.92s** — **0.74×** tsc, **4.73×** tsgo, **3.57×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b090478955dd | **5.42s** | 35,942 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b090478955dd | **5.07s** | 38,380 LOC/s | 46 |
| xtsc (cold JVM) | b090478955dd | 24.60s | 7,916 LOC/s | 46 |
| tsc | 6.0.3 | 11.70s | 16,638 LOC/s | 65 |
| tsgo | 7.0.2 | 2.18s | 89,394 LOC/s | 65 |
| xtsc-native | b090478955dd (AOT) | 8.67s | 22,451 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.16x FASTER than tsc; 2.49x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.07s**, **-6.4%** against `temurin26` on identical bytecode — 0.43× tsc, 2.33× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.10×** tsc and **11.29×** tsgo; tsc is **5.37×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.49s**.

Ahead-of-time (GraalVM native-image): **8.67s** — **0.74×** tsc, **3.98×** tsgo, **2.84×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
