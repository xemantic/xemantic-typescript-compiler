# Bench run 20260902T111654Z

- **xtsc revision**: [`59808eb5d6eb`](https://github.com/xemantic/xemantic-typescript-compiler/commit/59808eb5d6eb0ef412c66d219bf8b714ad781313)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 59808eb5d6eb0ef412c66d219bf8b714ad781313

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 59808eb5d6eb | **3.70s** | 52,579 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 59808eb5d6eb | **3.46s** | 56,255 LOC/s | 46 |
| xtsc (cold JVM) | 59808eb5d6eb | 23.69s | 8,218 LOC/s | 46 |
| tsc | 6.0.3 | 9.87s | 19,720 LOC/s | 65 |
| tsgo | 7.0.2 | 1.53s | 127,007 LOC/s | 65 |
| xtsc-native | 59808eb5d6eb (AOT) | 6.00s | 32,461 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.67x FASTER than tsc; 2.42x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.46s**, **-6.5%** against `temurin26` on identical bytecode — 0.35× tsc, 2.26× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.40×** tsc and **15.45×** tsgo; tsc is **6.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.58s**.

Ahead-of-time (GraalVM native-image): **6.00s** — **0.61×** tsc, **3.91×** tsgo, **3.95×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 59808eb5d6eb | **4.58s** | 42,483 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 59808eb5d6eb | **4.26s** | 45,704 LOC/s | 46 |
| xtsc (cold JVM) | 59808eb5d6eb | 22.95s | 8,483 LOC/s | 46 |
| tsc | 6.0.3 | 11.89s | 16,372 LOC/s | 65 |
| tsgo | 7.0.2 | 2.24s | 86,920 LOC/s | 65 |
| xtsc-native | 59808eb5d6eb (AOT) | 7.37s | 26,411 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.59x FASTER than tsc; 2.05x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.26s**, **-7.0%** against `temurin26` on identical bytecode — 0.36× tsc, 1.90× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.93×** tsc and **10.25×** tsgo; tsc is **5.31×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.64s**.

Ahead-of-time (GraalVM native-image): **7.37s** — **0.62×** tsc, **3.29×** tsgo, **3.11×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
