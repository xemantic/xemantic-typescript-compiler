# Bench run 20260923T094430Z

- **xtsc revision**: [`4c090d2922a8`](https://github.com/xemantic/xemantic-typescript-compiler/commit/4c090d2922a8c8a311215b477263d4d989ef1c38)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 4c090d2922a8c8a311215b477263d4d989ef1c38

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 4c090d2922a8 | **5.90s** | 33,017 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 4c090d2922a8 | **5.41s** | 36,015 LOC/s | 46 |
| xtsc (cold JVM) | 4c090d2922a8 | 25.96s | 7,499 LOC/s | 46 |
| tsc | 6.0.3 | 10.33s | 18,853 LOC/s | 65 |
| tsgo | 7.0.2 | 1.61s | 120,857 LOC/s | 65 |
| xtsc-native | 4c090d2922a8 (AOT) | 9.58s | 20,332 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.75x FASTER than tsc; 3.66x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.41s**, **-8.3%** against `temurin26` on identical bytecode — 0.52× tsc, 3.36× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.51×** tsc and **16.11×** tsgo; tsc is **6.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.84s**.

Ahead-of-time (GraalVM native-image): **9.58s** — **0.93×** tsc, **5.94×** tsgo, **2.71×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 4c090d2922a8 | **6.98s** | 27,906 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 4c090d2922a8 | **6.23s** | 31,257 LOC/s | 46 |
| xtsc (cold JVM) | 4c090d2922a8 | 30.13s | 6,461 LOC/s | 46 |
| tsc | 6.0.3 | 13.04s | 14,933 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,979 LOC/s | 65 |
| xtsc-native | 4c090d2922a8 (AOT) | 11.08s | 17,575 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.87x FASTER than tsc; 2.94x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.23s**, **-10.7%** against `temurin26` on identical bytecode — 0.48× tsc, 2.62× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.31×** tsc and **12.69×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.01s**.

Ahead-of-time (GraalVM native-image): **11.08s** — **0.85×** tsc, **4.66×** tsgo, **2.72×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
