# Bench run 20260822T180239Z

- **xtsc revision**: [`dff37e6a53ce`](https://github.com/xemantic/xemantic-typescript-compiler/commit/dff37e6a53ce9b90b611725a7ac2040b5f134e96)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push dff37e6a53ce9b90b611725a7ac2040b5f134e96

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | dff37e6a53ce | **4.33s** | 44,934 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | dff37e6a53ce | **4.00s** | 48,651 LOC/s | 46 |
| xtsc (cold JVM) | dff37e6a53ce | 23.35s | 8,336 LOC/s | 46 |
| tsc | 6.0.3 | 11.32s | 17,195 LOC/s | 65 |
| tsgo | 7.0.2 | 1.76s | 110,563 LOC/s | 65 |
| xtsc-native | dff37e6a53ce (AOT) | 6.92s | 28,119 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.61x FASTER than tsc; 2.46x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.00s**, **-7.6%** against `temurin26` on identical bytecode — 0.35× tsc, 2.27× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.06×** tsc and **13.26×** tsgo; tsc is **6.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.24s**.

Ahead-of-time (GraalVM native-image): **6.92s** — **0.61×** tsc, **3.93×** tsgo, **3.37×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | dff37e6a53ce | **4.92s** | 39,557 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | dff37e6a53ce | **4.67s** | 41,683 LOC/s | 46 |
| xtsc (cold JVM) | dff37e6a53ce | 25.94s | 7,506 LOC/s | 46 |
| tsc | 6.0.3 | 13.89s | 14,019 LOC/s | 65 |
| tsgo | 7.0.2 | 2.56s | 76,114 LOC/s | 65 |
| xtsc-native | dff37e6a53ce (AOT) | 8.33s | 23,365 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.82x FASTER than tsc; 1.92x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.67s**, **-5.1%** against `temurin26` on identical bytecode — 0.34× tsc, 1.83× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.87×** tsc and **10.14×** tsgo; tsc is **5.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.80s**.

Ahead-of-time (GraalVM native-image): **8.33s** — **0.60×** tsc, **3.26×** tsgo, **3.11×** the COLD JVM arm and **1.69×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
