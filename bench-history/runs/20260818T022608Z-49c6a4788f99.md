# Bench run 20260818T022608Z

- **xtsc revision**: [`49c6a4788f99`](https://github.com/xemantic/xemantic-typescript-compiler/commit/49c6a4788f99c5de30ed99406bf704a46be09472)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 49c6a4788f99c5de30ed99406bf704a46be09472

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 49c6a4788f99 | **3.61s** | 53,874 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 49c6a4788f99 | **3.35s** | 58,172 LOC/s | 46 |
| xtsc (cold JVM) | 49c6a4788f99 | 22.75s | 8,559 LOC/s | 46 |
| tsc | 6.0.3 | 9.91s | 19,639 LOC/s | 65 |
| tsgo | 7.0.2 | 1.53s | 127,506 LOC/s | 65 |
| xtsc-native | 49c6a4788f99 (AOT) | 5.58s | 34,874 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.74x FASTER than tsc; 2.37x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.35s**, **-7.4%** against `temurin26` on identical bytecode — 0.34× tsc, 2.19× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.29×** tsc and **14.90×** tsgo; tsc is **6.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.64s**.

Ahead-of-time (GraalVM native-image): **5.58s** — **0.56×** tsc, **3.66×** tsgo, **4.07×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 49c6a4788f99 | **4.28s** | 45,544 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 49c6a4788f99 | **4.16s** | 46,803 LOC/s | 46 |
| xtsc (cold JVM) | 49c6a4788f99 | 23.46s | 8,300 LOC/s | 46 |
| tsc | 6.0.3 | 12.00s | 16,229 LOC/s | 65 |
| tsgo | 7.0.2 | 2.29s | 85,059 LOC/s | 65 |
| xtsc-native | 49c6a4788f99 (AOT) | 6.89s | 28,254 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.81x FASTER than tsc; 1.87x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.16s**, **-2.7%** against `temurin26` on identical bytecode — 0.35× tsc, 1.82× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.96×** tsc and **10.25×** tsgo; tsc is **5.24×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.35s**.

Ahead-of-time (GraalVM native-image): **6.89s** — **0.57×** tsc, **3.01×** tsgo, **3.40×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
