# Bench run 20260826T213010Z

- **xtsc revision**: [`11316e74480f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/11316e74480f4d5d3e618d03566ccc6c265a3748)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 11316e74480f4d5d3e618d03566ccc6c265a3748

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 11316e74480f | **4.03s** | 48,277 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 11316e74480f | **3.69s** | 52,822 LOC/s | 46 |
| xtsc (cold JVM) | 11316e74480f | 23.32s | 8,348 LOC/s | 46 |
| tsc | 6.0.3 | 10.60s | 18,371 LOC/s | 65 |
| tsgo | 7.0.2 | 1.64s | 118,431 LOC/s | 65 |
| xtsc-native | 11316e74480f (AOT) | 6.04s | 32,256 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.63x FASTER than tsc; 2.45x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.69s**, **-8.6%** against `temurin26` on identical bytecode — 0.35× tsc, 2.24× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.20×** tsc and **14.19×** tsgo; tsc is **6.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.20s**.

Ahead-of-time (GraalVM native-image): **6.04s** — **0.57×** tsc, **3.67×** tsgo, **3.86×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 11316e74480f | **4.86s** | 40,045 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 11316e74480f | **4.70s** | 41,417 LOC/s | 46 |
| xtsc (cold JVM) | 11316e74480f | 23.46s | 8,298 LOC/s | 46 |
| tsc | 6.0.3 | 13.40s | 14,534 LOC/s | 65 |
| tsgo | 7.0.2 | 2.40s | 81,058 LOC/s | 65 |
| xtsc-native | 11316e74480f (AOT) | 7.75s | 25,122 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.76x FASTER than tsc; 2.02x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.70s**, **-3.3%** against `temurin26` on identical bytecode — 0.35× tsc, 1.96× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.75×** tsc and **9.77×** tsgo; tsc is **5.58×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.31s**.

Ahead-of-time (GraalVM native-image): **7.75s** — **0.58×** tsc, **3.23×** tsgo, **3.03×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
