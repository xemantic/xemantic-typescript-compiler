# Bench run 20260831T165255Z

- **xtsc revision**: [`ae6ad9191f3d`](https://github.com/xemantic/xemantic-typescript-compiler/commit/ae6ad9191f3d7c42ac1f97b6c9ded9912c817da9)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push ae6ad9191f3d7c42ac1f97b6c9ded9912c817da9

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ae6ad9191f3d | **2.35s** | 82,922 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ae6ad9191f3d | **2.32s** | 83,995 LOC/s | 46 |
| xtsc (cold JVM) | ae6ad9191f3d | 14.65s | 13,288 LOC/s | 46 |
| tsc | 6.0.3 | 6.19s | 31,454 LOC/s | 65 |
| tsgo | 7.0.2 | 0.90s | 216,095 LOC/s | 65 |
| xtsc-native | ae6ad9191f3d (AOT) | 4.04s | 48,145 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.64x FASTER than tsc; 2.61x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.32s**, **-1.3%** against `temurin26` on identical bytecode — 0.37× tsc, 2.57× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.37×** tsc and **16.26×** tsgo; tsc is **6.87×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **14.57s**.

Ahead-of-time (GraalVM native-image): **4.04s** — **0.65×** tsc, **4.49×** tsgo, **3.62×** the COLD JVM arm and **1.72×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ae6ad9191f3d | **2.89s** | 67,394 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ae6ad9191f3d | **2.81s** | 69,387 LOC/s | 46 |
| xtsc (cold JVM) | ae6ad9191f3d | 15.37s | 12,670 LOC/s | 46 |
| tsc | 6.0.3 | 7.40s | 26,311 LOC/s | 65 |
| tsgo | 7.0.2 | 1.38s | 141,498 LOC/s | 65 |
| xtsc-native | ae6ad9191f3d (AOT) | 4.74s | 41,119 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.56x FASTER than tsc; 2.10x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.81s**, **-2.9%** against `temurin26` on identical bytecode — 0.38× tsc, 2.04× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.08×** tsc and **11.17×** tsgo; tsc is **5.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **15.27s**.

Ahead-of-time (GraalVM native-image): **4.74s** — **0.64×** tsc, **3.44×** tsgo, **3.25×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
