# Bench run 20260802T051620Z

- **xtsc revision**: [`a9ecdb8c12df`](https://github.com/xemantic/xemantic-typescript-compiler/commit/a9ecdb8c12dfb097ff83ec3e90dd41f83bb4d733)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push a9ecdb8c12dfb097ff83ec3e90dd41f83bb4d733

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | a9ecdb8c12df | 27.85s | 6,990 LOC/s | 46 |
| tsc | 6.0.3 | 10.74s | 18,120 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.73s | 112,870 LOC/s | 65 |
| xtsc-native | a9ecdb8c12df (AOT) | 11.06s | 17,607 LOC/s | 46 |

Relative (wall): xtsc is **2.59×** tsc and **16.15×** tsgo; tsc is **6.23×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.75s**.

Ahead-of-time (GraalVM native-image): **11.06s**, **2.52×** the JVM arm, **1.03×** tsc, **6.41×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | a9ecdb8c12df | 31.67s | 6,147 LOC/s | 46 |
| tsc | 6.0.3 | 13.33s | 14,601 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.42s | 80,455 LOC/s | 65 |
| xtsc-native | a9ecdb8c12df (AOT) | 13.29s | 14,653 LOC/s | 46 |

Relative (wall): xtsc is **2.38×** tsc and **13.09×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **31.56s**.

Ahead-of-time (GraalVM native-image): **13.29s**, **2.38×** the JVM arm, **1.00×** tsc, **5.49×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
