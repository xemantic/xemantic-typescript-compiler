# Bench run 20260923T213507Z

- **xtsc revision**: [`c74a155b0f6c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c74a155b0f6cc3ab250d2a2001e2b4c013ab7b05)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push c74a155b0f6cc3ab250d2a2001e2b4c013ab7b05

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c74a155b0f6c | **5.81s** | 33,528 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c74a155b0f6c | **5.23s** | 37,199 LOC/s | 46 |
| xtsc (cold JVM) | c74a155b0f6c | 25.89s | 7,520 LOC/s | 46 |
| tsc | 6.0.3 | 10.19s | 19,099 LOC/s | 65 |
| tsgo | 7.0.2 | 1.56s | 124,889 LOC/s | 65 |
| xtsc-native | c74a155b0f6c (AOT) | 9.72s | 20,020 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.76x FASTER than tsc; 3.72x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.23s**, **-9.9%** against `temurin26` on identical bytecode — 0.51× tsc, 3.36× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.54×** tsc and **16.61×** tsgo; tsc is **6.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.77s**.

Ahead-of-time (GraalVM native-image): **9.72s** — **0.95×** tsc, **6.24×** tsgo, **2.66×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c74a155b0f6c | **6.74s** | 28,887 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c74a155b0f6c | **6.08s** | 32,002 LOC/s | 46 |
| xtsc (cold JVM) | c74a155b0f6c | 29.69s | 6,557 LOC/s | 46 |
| tsc | 6.0.3 | 12.61s | 15,445 LOC/s | 65 |
| tsgo | 7.0.2 | 2.32s | 83,923 LOC/s | 65 |
| xtsc-native | c74a155b0f6c (AOT) | 10.81s | 18,004 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.87x FASTER than tsc; 2.91x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.08s**, **-9.7%** against `temurin26` on identical bytecode — 0.48× tsc, 2.62× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.36×** tsc and **12.80×** tsgo; tsc is **5.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.54s**.

Ahead-of-time (GraalVM native-image): **10.81s** — **0.86×** tsc, **4.66×** tsgo, **2.75×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
