# Bench run 20260904T185735Z

- **xtsc revision**: [`1d2bfa490cfc`](https://github.com/xemantic/xemantic-typescript-compiler/commit/1d2bfa490cfc3d006b04e72aff6551bc5bafc65c)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 1d2bfa490cfc3d006b04e72aff6551bc5bafc65c

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1d2bfa490cfc | **4.03s** | 48,373 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1d2bfa490cfc | **3.94s** | 49,379 LOC/s | 46 |
| xtsc (cold JVM) | 1d2bfa490cfc | 23.98s | 8,120 LOC/s | 46 |
| tsc | 6.0.3 | 10.03s | 19,419 LOC/s | 65 |
| tsgo | 7.0.2 | 1.50s | 130,148 LOC/s | 65 |
| xtsc-native | 1d2bfa490cfc (AOT) | 6.76s | 28,780 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.49x FASTER than tsc; 2.69x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.94s**, **-2.0%** against `temurin26` on identical bytecode — 0.39× tsc, 2.64× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.39×** tsc and **16.03×** tsgo; tsc is **6.70×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.86s**.

Ahead-of-time (GraalVM native-image): **6.76s** — **0.67×** tsc, **4.52×** tsgo, **3.54×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1d2bfa490cfc | **4.84s** | 40,202 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1d2bfa490cfc | **4.60s** | 42,308 LOC/s | 46 |
| xtsc (cold JVM) | 1d2bfa490cfc | 24.61s | 7,913 LOC/s | 46 |
| tsc | 6.0.3 | 12.33s | 15,796 LOC/s | 65 |
| tsgo | 7.0.2 | 2.29s | 85,022 LOC/s | 65 |
| xtsc-native | 1d2bfa490cfc (AOT) | 8.09s | 24,078 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.55x FASTER than tsc; 2.11x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.60s**, **-5.0%** against `temurin26` on identical bytecode — 0.37× tsc, 2.01× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.00×** tsc and **10.74×** tsgo; tsc is **5.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.49s**.

Ahead-of-time (GraalVM native-image): **8.09s** — **0.66×** tsc, **3.53×** tsgo, **3.04×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
