# Bench run 20260822T202145Z

- **xtsc revision**: [`73c009ef2b8d`](https://github.com/xemantic/xemantic-typescript-compiler/commit/73c009ef2b8d9add4656385533db89937d5d5cac)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 73c009ef2b8d9add4656385533db89937d5d5cac

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 73c009ef2b8d | **3.41s** | 57,164 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 73c009ef2b8d | **3.29s** | 59,179 LOC/s | 46 |
| xtsc (cold JVM) | 73c009ef2b8d | 20.83s | 9,345 LOC/s | 46 |
| tsc | 6.0.3 | 9.80s | 19,871 LOC/s | 65 |
| tsgo | 7.0.2 | 1.50s | 130,148 LOC/s | 65 |
| xtsc-native | 73c009ef2b8d (AOT) | 5.54s | 35,163 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.88x FASTER than tsc; 2.28x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.29s**, **-3.4%** against `temurin26` on identical bytecode — 0.34× tsc, 2.20× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.13×** tsc and **13.93×** tsgo; tsc is **6.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.73s**.

Ahead-of-time (GraalVM native-image): **5.54s** — **0.57×** tsc, **3.70×** tsgo, **3.76×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 73c009ef2b8d | **4.34s** | 44,851 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 73c009ef2b8d | **3.80s** | 51,277 LOC/s | 46 |
| xtsc (cold JVM) | 73c009ef2b8d | 23.63s | 8,240 LOC/s | 46 |
| tsc | 6.0.3 | 11.66s | 16,696 LOC/s | 65 |
| tsgo | 7.0.2 | 2.21s | 88,020 LOC/s | 65 |
| xtsc-native | 73c009ef2b8d (AOT) | 6.72s | 28,986 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.69x FASTER than tsc; 1.96x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.80s**, **-12.5%** against `temurin26` on identical bytecode — 0.33× tsc, 1.72× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.03×** tsc and **10.68×** tsgo; tsc is **5.27×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.51s**.

Ahead-of-time (GraalVM native-image): **6.72s** — **0.58×** tsc, **3.04×** tsgo, **3.52×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
