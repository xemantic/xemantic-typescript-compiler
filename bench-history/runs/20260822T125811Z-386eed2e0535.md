# Bench run 20260822T125811Z

- **xtsc revision**: [`386eed2e0535`](https://github.com/xemantic/xemantic-typescript-compiler/commit/386eed2e05352c1514df65addc30d3f575fe55b3)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 386eed2e05352c1514df65addc30d3f575fe55b3

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 386eed2e0535 | **4.40s** | 44,210 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 386eed2e0535 | **4.14s** | 47,018 LOC/s | 46 |
| xtsc (cold JVM) | 386eed2e0535 | 24.80s | 7,850 LOC/s | 46 |
| tsc | 6.0.3 | 11.51s | 16,923 LOC/s | 65 |
| tsgo | 7.0.2 | 1.81s | 107,629 LOC/s | 65 |
| xtsc-native | 386eed2e0535 (AOT) | 6.92s | 28,144 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.61x FASTER than tsc; 2.43x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.14s**, **-6.0%** against `temurin26` on identical bytecode — 0.36× tsc, 2.29× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.16×** tsc and **13.71×** tsgo; tsc is **6.36×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.68s**.

Ahead-of-time (GraalVM native-image): **6.92s** — **0.60×** tsc, **3.82×** tsgo, **3.58×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 386eed2e0535 | **5.25s** | 37,107 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 386eed2e0535 | **4.88s** | 39,865 LOC/s | 46 |
| xtsc (cold JVM) | 386eed2e0535 | 25.32s | 7,690 LOC/s | 46 |
| tsc | 6.0.3 | 14.03s | 13,877 LOC/s | 65 |
| tsgo | 7.0.2 | 2.59s | 75,232 LOC/s | 65 |
| xtsc-native | 386eed2e0535 (AOT) | 8.66s | 22,485 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.67x FASTER than tsc; 2.03x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.88s**, **-6.9%** against `temurin26` on identical bytecode — 0.35× tsc, 1.89× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.80×** tsc and **9.78×** tsgo; tsc is **5.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.18s**.

Ahead-of-time (GraalVM native-image): **8.66s** — **0.62×** tsc, **3.35×** tsgo, **2.92×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
