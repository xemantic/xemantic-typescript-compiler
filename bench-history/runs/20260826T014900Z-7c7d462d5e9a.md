# Bench run 20260826T014900Z

- **xtsc revision**: [`7c7d462d5e9a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/7c7d462d5e9ae05ed12ed2e3c0b350d6c419fbdd)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 7c7d462d5e9ae05ed12ed2e3c0b350d6c419fbdd

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7c7d462d5e9a | **2.70s** | 72,031 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7c7d462d5e9a | **2.61s** | 74,598 LOC/s | 46 |
| xtsc (cold JVM) | 7c7d462d5e9a | 17.75s | 10,966 LOC/s | 46 |
| tsc | 6.0.3 | 7.99s | 24,365 LOC/s | 65 |
| tsgo | 7.0.2 | 1.26s | 154,525 LOC/s | 65 |
| xtsc-native | 7c7d462d5e9a (AOT) | 4.36s | 44,635 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.96x FASTER than tsc; 2.15x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.61s**, **-3.4%** against `temurin26` on identical bytecode — 0.33× tsc, 2.07× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.22×** tsc and **14.09×** tsgo; tsc is **6.34×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.67s**.

Ahead-of-time (GraalVM native-image): **4.36s** — **0.55×** tsc, **3.46×** tsgo, **4.07×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7c7d462d5e9a | **3.50s** | 55,629 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7c7d462d5e9a | **3.14s** | 61,967 LOC/s | 46 |
| xtsc (cold JVM) | 7c7d462d5e9a | 20.58s | 9,462 LOC/s | 46 |
| tsc | 6.0.3 | 9.61s | 20,268 LOC/s | 65 |
| tsgo | 7.0.2 | 1.81s | 107,273 LOC/s | 65 |
| xtsc-native | 7c7d462d5e9a (AOT) | 5.21s | 37,363 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.74x FASTER than tsc; 1.93x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.14s**, **-10.2%** against `temurin26` on identical bytecode — 0.33× tsc, 1.73× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.14×** tsc and **11.34×** tsgo; tsc is **5.29×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.48s**.

Ahead-of-time (GraalVM native-image): **5.21s** — **0.54×** tsc, **2.87×** tsgo, **3.95×** the COLD JVM arm and **1.49×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
