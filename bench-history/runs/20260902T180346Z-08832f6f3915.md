# Bench run 20260902T180346Z

- **xtsc revision**: [`08832f6f3915`](https://github.com/xemantic/xemantic-typescript-compiler/commit/08832f6f39156d22a64867ab9e97ef9f7beb15b9)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 08832f6f39156d22a64867ab9e97ef9f7beb15b9

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 08832f6f3915 | **4.62s** | 42,143 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 08832f6f3915 | **4.06s** | 47,956 LOC/s | 46 |
| xtsc (cold JVM) | 08832f6f3915 | 24.81s | 7,847 LOC/s | 46 |
| tsc | 6.0.3 | 10.74s | 18,132 LOC/s | 65 |
| tsgo | 7.0.2 | 1.70s | 114,396 LOC/s | 65 |
| xtsc-native | 08832f6f3915 (AOT) | 6.96s | 27,966 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.32x FASTER than tsc; 2.71x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.06s**, **-12.1%** against `temurin26` on identical bytecode — 0.38× tsc, 2.39× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.31×** tsc and **14.58×** tsgo; tsc is **6.31×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.69s**.

Ahead-of-time (GraalVM native-image): **6.96s** — **0.65×** tsc, **4.09×** tsgo, **3.56×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 08832f6f3915 | **5.31s** | 36,673 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 08832f6f3915 | **4.91s** | 39,678 LOC/s | 46 |
| xtsc (cold JVM) | 08832f6f3915 | 26.85s | 7,252 LOC/s | 46 |
| tsc | 6.0.3 | 13.11s | 14,852 LOC/s | 65 |
| tsgo | 7.0.2 | 2.51s | 77,570 LOC/s | 65 |
| xtsc-native | 08832f6f3915 (AOT) | 8.30s | 23,449 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.47x FASTER than tsc; 2.12x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.91s**, **-7.6%** against `temurin26` on identical bytecode — 0.37× tsc, 1.95× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.05×** tsc and **10.70×** tsgo; tsc is **5.22×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.73s**.

Ahead-of-time (GraalVM native-image): **8.30s** — **0.63×** tsc, **3.31×** tsgo, **3.23×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
