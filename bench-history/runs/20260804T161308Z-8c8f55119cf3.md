# Bench run 20260804T161308Z

- **xtsc revision**: [`8c8f55119cf3`](https://github.com/xemantic/xemantic-typescript-compiler/commit/8c8f55119cf3059000506aa272106f09a61c0566)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 8c8f55119cf3059000506aa272106f09a61c0566

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 8c8f55119cf3 | 25.24s | 7,712 LOC/s | 46 |
| tsc | 6.0.3 | 10.85s | 17,953 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.67s | 116,448 LOC/s | 65 |
| xtsc-native | 8c8f55119cf3 (AOT) | 10.50s | 18,544 LOC/s | 46 |

Relative (wall): xtsc is **2.33×** tsc and **15.10×** tsgo; tsc is **6.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.14s**.

Ahead-of-time (GraalVM native-image): **10.50s**, **2.40×** the JVM arm, **0.97×** tsc, **6.28×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 8c8f55119cf3 | 27.88s | 6,982 LOC/s | 46 |
| tsc | 6.0.3 | 13.10s | 14,866 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.39s | 81,397 LOC/s | 65 |
| xtsc-native | 8c8f55119cf3 (AOT) | 12.60s | 15,453 LOC/s | 46 |

Relative (wall): xtsc is **2.13×** tsc and **11.66×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.77s**.

Ahead-of-time (GraalVM native-image): **12.60s**, **2.21×** the JVM arm, **0.96×** tsc, **5.27×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
