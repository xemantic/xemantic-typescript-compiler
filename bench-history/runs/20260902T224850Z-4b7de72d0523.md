# Bench run 20260902T224850Z

- **xtsc revision**: [`4b7de72d0523`](https://github.com/xemantic/xemantic-typescript-compiler/commit/4b7de72d05239d84d3a1b9ec5c8e72dbd15f7f9e)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 4b7de72d05239d84d3a1b9ec5c8e72dbd15f7f9e

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 4b7de72d0523 | **4.33s** | 44,924 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 4b7de72d0523 | **4.13s** | 47,131 LOC/s | 46 |
| xtsc (cold JVM) | 4b7de72d0523 | 26.13s | 7,451 LOC/s | 46 |
| tsc | 6.0.3 | 11.07s | 17,589 LOC/s | 65 |
| tsgo | 7.0.2 | 1.74s | 112,155 LOC/s | 65 |
| xtsc-native | 4b7de72d0523 (AOT) | 7.24s | 26,903 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.55x FASTER than tsc; 2.50x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.13s**, **-4.7%** against `temurin26` on identical bytecode — 0.37× tsc, 2.38× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.36×** tsc and **15.05×** tsgo; tsc is **6.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.01s**.

Ahead-of-time (GraalVM native-image): **7.24s** — **0.65×** tsc, **4.17×** tsgo, **3.61×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 4b7de72d0523 | **5.50s** | 35,419 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 4b7de72d0523 | **4.64s** | 41,961 LOC/s | 46 |
| xtsc (cold JVM) | 4b7de72d0523 | 26.35s | 7,387 LOC/s | 46 |
| tsc | 6.0.3 | 13.38s | 14,552 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,979 LOC/s | 65 |
| xtsc-native | 4b7de72d0523 (AOT) | 8.13s | 23,942 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.43x FASTER than tsc; 2.31x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.64s**, **-15.6%** against `temurin26` on identical bytecode — 0.35× tsc, 1.95× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.97×** tsc and **11.10×** tsgo; tsc is **5.63×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.24s**.

Ahead-of-time (GraalVM native-image): **8.13s** — **0.61×** tsc, **3.42×** tsgo, **3.24×** the COLD JVM arm and **1.48×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
