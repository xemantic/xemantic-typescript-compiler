# Bench run 20260924T132022Z

- **xtsc revision**: [`8c272d0990e6`](https://github.com/xemantic/xemantic-typescript-compiler/commit/8c272d0990e6d8ff9a9779238e074727a90c03c8)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 8c272d0990e6d8ff9a9779238e074727a90c03c8

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8c272d0990e6 | **4.41s** | 44,120 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8c272d0990e6 | **4.17s** | 46,646 LOC/s | 46 |
| xtsc (cold JVM) | 8c272d0990e6 | 20.41s | 9,540 LOC/s | 46 |
| tsc | 6.0.3 | 7.88s | 24,717 LOC/s | 65 |
| tsgo | 7.0.2 | 1.21s | 161,310 LOC/s | 65 |
| xtsc-native | 8c272d0990e6 (AOT) | 7.28s | 26,730 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.78x FASTER than tsc; 3.66x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.17s**, **-5.4%** against `temurin26` on identical bytecode — 0.53× tsc, 3.46× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.59×** tsc and **16.91×** tsgo; tsc is **6.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.32s**.

Ahead-of-time (GraalVM native-image): **7.28s** — **0.92×** tsc, **6.03×** tsgo, **2.80×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8c272d0990e6 | **5.18s** | 37,587 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8c272d0990e6 | **4.71s** | 41,364 LOC/s | 46 |
| xtsc (cold JVM) | 8c272d0990e6 | 21.56s | 9,031 LOC/s | 46 |
| tsc | 6.0.3 | 9.65s | 20,165 LOC/s | 65 |
| tsgo | 7.0.2 | 1.77s | 110,125 LOC/s | 65 |
| xtsc-native | 8c272d0990e6 (AOT) | 8.33s | 23,373 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.86x FASTER than tsc; 2.93x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.71s**, **-9.1%** against `temurin26` on identical bytecode — 0.49× tsc, 2.66× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.23×** tsc and **12.19×** tsgo; tsc is **5.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.45s**.

Ahead-of-time (GraalVM native-image): **8.33s** — **0.86×** tsc, **4.71×** tsgo, **2.59×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
