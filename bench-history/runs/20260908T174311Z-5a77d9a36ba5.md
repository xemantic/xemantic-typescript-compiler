# Bench run 20260908T174311Z

- **xtsc revision**: [`5a77d9a36ba5`](https://github.com/xemantic/xemantic-typescript-compiler/commit/5a77d9a36ba58d30db56c70fde810b6c8bcf45b8)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 5a77d9a36ba58d30db56c70fde810b6c8bcf45b8

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5a77d9a36ba5 | **5.34s** | 36,474 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5a77d9a36ba5 | **4.85s** | 40,111 LOC/s | 46 |
| xtsc (cold JVM) | 5a77d9a36ba5 | 25.92s | 7,512 LOC/s | 46 |
| tsc | 6.0.3 | 10.14s | 19,193 LOC/s | 65 |
| tsgo | 7.0.2 | 1.54s | 126,020 LOC/s | 65 |
| xtsc-native | 5a77d9a36ba5 (AOT) | 8.81s | 22,092 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.90x FASTER than tsc; 3.46x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.85s**, **-9.1%** against `temurin26` on identical bytecode — 0.48× tsc, 3.14× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.55×** tsc and **16.77×** tsgo; tsc is **6.57×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.80s**.

Ahead-of-time (GraalVM native-image): **8.81s** — **0.87×** tsc, **5.70×** tsgo, **2.94×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5a77d9a36ba5 | **6.30s** | 30,885 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5a77d9a36ba5 | **5.55s** | 35,056 LOC/s | 46 |
| xtsc (cold JVM) | 5a77d9a36ba5 | 26.43s | 7,368 LOC/s | 46 |
| tsc | 6.0.3 | 12.47s | 15,617 LOC/s | 65 |
| tsgo | 7.0.2 | 2.32s | 84,031 LOC/s | 65 |
| xtsc-native | 5a77d9a36ba5 (AOT) | 10.15s | 19,190 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.98x FASTER than tsc; 2.72x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.55s**, **-11.9%** against `temurin26` on identical bytecode — 0.45× tsc, 2.40× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.12×** tsc and **11.40×** tsgo; tsc is **5.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.30s**.

Ahead-of-time (GraalVM native-image): **10.15s** — **0.81×** tsc, **4.38×** tsgo, **2.60×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
