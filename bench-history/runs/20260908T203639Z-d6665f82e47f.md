# Bench run 20260908T203639Z

- **xtsc revision**: [`d6665f82e47f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d6665f82e47fd083a798a1f8e359091206c73545)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push d6665f82e47fd083a798a1f8e359091206c73545

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d6665f82e47f | **5.91s** | 32,972 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d6665f82e47f | **5.12s** | 38,027 LOC/s | 46 |
| xtsc (cold JVM) | d6665f82e47f | 26.99s | 7,213 LOC/s | 46 |
| tsc | 6.0.3 | 11.04s | 17,632 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,072 LOC/s | 65 |
| xtsc-native | d6665f82e47f (AOT) | 9.13s | 21,327 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.87x FASTER than tsc; 3.49x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.12s**, **-13.3%** against `temurin26` on identical bytecode — 0.46× tsc, 3.03× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.44×** tsc and **15.95×** tsgo; tsc is **6.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.88s**.

Ahead-of-time (GraalVM native-image): **9.13s** — **0.83×** tsc, **5.40×** tsgo, **2.96×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d6665f82e47f | **6.51s** | 29,889 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d6665f82e47f | **5.74s** | 33,932 LOC/s | 46 |
| xtsc (cold JVM) | d6665f82e47f | 27.82s | 6,998 LOC/s | 46 |
| tsc | 6.0.3 | 13.18s | 14,771 LOC/s | 65 |
| tsgo | 7.0.2 | 2.41s | 80,856 LOC/s | 65 |
| xtsc-native | d6665f82e47f (AOT) | 9.97s | 19,532 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.02x FASTER than tsc; 2.71x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.74s**, **-11.9%** against `temurin26` on identical bytecode — 0.44× tsc, 2.38× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.11×** tsc and **11.55×** tsgo; tsc is **5.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.68s**.

Ahead-of-time (GraalVM native-image): **9.97s** — **0.76×** tsc, **4.14×** tsgo, **2.79×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
