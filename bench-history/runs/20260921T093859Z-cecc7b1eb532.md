# Bench run 20260921T093859Z

- **xtsc revision**: [`cecc7b1eb532`](https://github.com/xemantic/xemantic-typescript-compiler/commit/cecc7b1eb53251d1af9481d55cd0afa2f0115e59)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push cecc7b1eb53251d1af9481d55cd0afa2f0115e59

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cecc7b1eb532 | **5.84s** | 33,316 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cecc7b1eb532 | **5.35s** | 36,406 LOC/s | 46 |
| xtsc (cold JVM) | cecc7b1eb532 | 26.87s | 7,245 LOC/s | 46 |
| tsc | 6.0.3 | 11.00s | 17,693 LOC/s | 65 |
| tsgo | 7.0.2 | 1.70s | 114,800 LOC/s | 65 |
| xtsc-native | cecc7b1eb532 (AOT) | 9.24s | 21,073 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.88x FASTER than tsc; 3.45x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.35s**, **-8.5%** against `temurin26` on identical bytecode — 0.49× tsc, 3.15× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.44×** tsc and **15.84×** tsgo; tsc is **6.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.75s**.

Ahead-of-time (GraalVM native-image): **9.24s** — **0.84×** tsc, **5.45×** tsgo, **2.91×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cecc7b1eb532 | **6.72s** | 28,951 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cecc7b1eb532 | **6.17s** | 31,551 LOC/s | 46 |
| xtsc (cold JVM) | cecc7b1eb532 | 29.93s | 6,505 LOC/s | 46 |
| tsc | 6.0.3 | 13.43s | 14,498 LOC/s | 65 |
| tsgo | 7.0.2 | 2.42s | 80,322 LOC/s | 65 |
| xtsc-native | cecc7b1eb532 (AOT) | 10.98s | 17,737 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.00x FASTER than tsc; 2.77x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.17s**, **-8.2%** against `temurin26` on identical bytecode — 0.46× tsc, 2.55× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.23×** tsc and **12.35×** tsgo; tsc is **5.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.80s**.

Ahead-of-time (GraalVM native-image): **10.98s** — **0.82×** tsc, **4.53×** tsgo, **2.73×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
