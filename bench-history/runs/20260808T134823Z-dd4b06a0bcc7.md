# Bench run 20260808T134823Z

- **xtsc revision**: [`dd4b06a0bcc7`](https://github.com/xemantic/xemantic-typescript-compiler/commit/dd4b06a0bcc70889b67e3a0dc58d07fa98043ce7)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push dd4b06a0bcc70889b67e3a0dc58d07fa98043ce7

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | dd4b06a0bcc7 | 25.82s | 7,541 LOC/s | 46 |
| tsc | 6.0.3 | 10.34s | 18,831 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.64s | 118,865 LOC/s | 65 |
| xtsc-native | dd4b06a0bcc7 (AOT) | 10.41s | 18,705 LOC/s | 46 |

Relative (wall): xtsc is **2.50×** tsc and **15.76×** tsgo; tsc is **6.31×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.70s**.

Ahead-of-time (GraalVM native-image): **10.41s**, **2.48×** the JVM arm, **1.01×** tsc, **6.35×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | dd4b06a0bcc7 | 25.62s | 7,599 LOC/s | 46 |
| tsc | 6.0.3 | 12.42s | 15,679 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.25s | 86,418 LOC/s | 65 |
| xtsc-native | dd4b06a0bcc7 (AOT) | 13.20s | 14,751 LOC/s | 46 |

Relative (wall): xtsc is **2.06×** tsc and **11.37×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.50s**.

Ahead-of-time (GraalVM native-image): **13.20s**, **1.94×** the JVM arm, **1.06×** tsc, **5.86×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
