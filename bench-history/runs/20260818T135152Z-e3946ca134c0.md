# Bench run 20260818T135152Z

- **xtsc revision**: [`e3946ca134c0`](https://github.com/xemantic/xemantic-typescript-compiler/commit/e3946ca134c0f457ce108d6486948990e57e1743)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push e3946ca134c0f457ce108d6486948990e57e1743

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e3946ca134c0 | **3.90s** | 49,910 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e3946ca134c0 | **3.84s** | 50,677 LOC/s | 46 |
| xtsc (cold JVM) | e3946ca134c0 | 22.53s | 8,642 LOC/s | 46 |
| tsc | 6.0.3 | 10.55s | 18,453 LOC/s | 65 |
| tsgo | 7.0.2 | 1.63s | 119,669 LOC/s | 65 |
| xtsc-native | e3946ca134c0 (AOT) | 6.56s | 29,675 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.70x FASTER than tsc; 2.40x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.84s**, **-1.5%** against `temurin26` on identical bytecode — 0.36× tsc, 2.36× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.14×** tsc and **13.85×** tsgo; tsc is **6.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.41s**.

Ahead-of-time (GraalVM native-image): **6.56s** — **0.62×** tsc, **4.03×** tsgo, **3.43×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e3946ca134c0 | **4.96s** | 39,222 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e3946ca134c0 | **4.45s** | 43,743 LOC/s | 46 |
| xtsc (cold JVM) | e3946ca134c0 | 24.67s | 7,892 LOC/s | 46 |
| tsc | 6.0.3 | 12.84s | 15,164 LOC/s | 65 |
| tsgo | 7.0.2 | 2.30s | 84,506 LOC/s | 65 |
| xtsc-native | e3946ca134c0 (AOT) | 7.62s | 25,564 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.59x FASTER than tsc; 2.15x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.45s**, **-10.3%** against `temurin26` on identical bytecode — 0.35× tsc, 1.93× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.92×** tsc and **10.71×** tsgo; tsc is **5.57×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.55s**.

Ahead-of-time (GraalVM native-image): **7.62s** — **0.59×** tsc, **3.31×** tsgo, **3.24×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
