# Bench run 20260801T004805Z

- **xtsc revision**: [`c418d10dffb6`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c418d10dffb68e5646b3cef3c58ce9e9bf69b01f)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push c418d10dffb68e5646b3cef3c58ce9e9bf69b01f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | c418d10dffb6 | 32.20s | 6,047 LOC/s | 46 |
| tsc | 6.0.3 | 10.84s | 17,966 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.72s | 113,528 LOC/s | 65 |
| xtsc-native | c418d10dffb6 (AOT) | 12.53s | 15,541 LOC/s | 46 |

Relative (wall): xtsc is **2.97×** tsc and **18.77×** tsgo; tsc is **6.32×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **32.08s**.

Ahead-of-time (GraalVM native-image): **12.53s**, **2.57×** the JVM arm, **1.16×** tsc, **7.30×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | c418d10dffb6 | 33.91s | 5,741 LOC/s | 46 |
| tsc | 6.0.3 | 13.13s | 14,825 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.40s | 81,227 LOC/s | 65 |
| xtsc-native | c418d10dffb6 (AOT) | 14.25s | 13,662 LOC/s | 46 |

Relative (wall): xtsc is **2.58×** tsc and **14.15×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **33.81s**.

Ahead-of-time (GraalVM native-image): **14.25s**, **2.38×** the JVM arm, **1.09×** tsc, **5.95×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
