# Bench run 20260818T113516Z

- **xtsc revision**: [`0d544d30d2c5`](https://github.com/xemantic/xemantic-typescript-compiler/commit/0d544d30d2c55ed94f8ca5b099546d2a4ee56cc5)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 0d544d30d2c55ed94f8ca5b099546d2a4ee56cc5

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0d544d30d2c5 | **3.96s** | 49,179 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0d544d30d2c5 | **3.76s** | 51,823 LOC/s | 46 |
| xtsc (cold JVM) | 0d544d30d2c5 | 22.35s | 8,711 LOC/s | 46 |
| tsc | 6.0.3 | 10.96s | 17,768 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,413 LOC/s | 65 |
| xtsc-native | 0d544d30d2c5 (AOT) | 6.16s | 31,602 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.77x FASTER than tsc; 2.35x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.76s**, **-5.1%** against `temurin26` on identical bytecode — 0.34× tsc, 2.23× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.04×** tsc and **13.25×** tsgo; tsc is **6.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.23s**.

Ahead-of-time (GraalVM native-image): **6.16s** — **0.56×** tsc, **3.65×** tsgo, **3.63×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0d544d30d2c5 | **4.99s** | 39,002 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0d544d30d2c5 | **4.46s** | 43,655 LOC/s | 46 |
| xtsc (cold JVM) | 0d544d30d2c5 | 26.70s | 7,290 LOC/s | 46 |
| tsc | 6.0.3 | 13.40s | 14,525 LOC/s | 65 |
| tsgo | 7.0.2 | 2.41s | 80,655 LOC/s | 65 |
| xtsc-native | 0d544d30d2c5 (AOT) | 7.26s | 26,803 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.69x FASTER than tsc; 2.07x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.46s**, **-10.7%** against `temurin26` on identical bytecode — 0.33× tsc, 1.85× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.99×** tsc and **11.06×** tsgo; tsc is **5.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.59s**.

Ahead-of-time (GraalVM native-image): **7.26s** — **0.54×** tsc, **3.01×** tsgo, **3.68×** the COLD JVM arm and **1.46×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
