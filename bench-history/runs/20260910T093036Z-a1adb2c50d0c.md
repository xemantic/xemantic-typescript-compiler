# Bench run 20260910T093036Z

- **xtsc revision**: [`a1adb2c50d0c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/a1adb2c50d0cd9d6a125c40b40c7e37723f54522)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push a1adb2c50d0cd9d6a125c40b40c7e37723f54522

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a1adb2c50d0c | **2.96s** | 65,777 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a1adb2c50d0c | **2.76s** | 70,646 LOC/s | 46 |
| xtsc (cold JVM) | a1adb2c50d0c | 15.60s | 12,484 LOC/s | 46 |
| tsc | 6.0.3 | 5.79s | 33,621 LOC/s | 65 |
| tsgo | 7.0.2 | 0.88s | 222,262 LOC/s | 65 |
| xtsc-native | a1adb2c50d0c (AOT) | 4.93s | 39,493 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.96x FASTER than tsc; 3.38x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.76s**, **-6.9%** against `temurin26` on identical bytecode — 0.48× tsc, 3.15× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.69×** tsc and **17.80×** tsgo; tsc is **6.61×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **15.52s**.

Ahead-of-time (GraalVM native-image): **4.93s** — **0.85×** tsc, **5.63×** tsgo, **3.16×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a1adb2c50d0c | **3.51s** | 55,470 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a1adb2c50d0c | **3.19s** | 61,130 LOC/s | 46 |
| xtsc (cold JVM) | a1adb2c50d0c | 14.99s | 12,991 LOC/s | 46 |
| tsc | 6.0.3 | 7.14s | 27,273 LOC/s | 65 |
| tsgo | 7.0.2 | 1.39s | 140,376 LOC/s | 65 |
| xtsc-native | a1adb2c50d0c (AOT) | 5.76s | 33,773 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.03x FASTER than tsc; 2.53x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.19s**, **-9.3%** against `temurin26` on identical bytecode — 0.45× tsc, 2.30× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.10×** tsc and **10.81×** tsgo; tsc is **5.15×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **14.91s**.

Ahead-of-time (GraalVM native-image): **5.76s** — **0.81×** tsc, **4.16×** tsgo, **2.60×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
