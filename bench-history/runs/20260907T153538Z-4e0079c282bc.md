# Bench run 20260907T153538Z

- **xtsc revision**: [`4e0079c282bc`](https://github.com/xemantic/xemantic-typescript-compiler/commit/4e0079c282bca6a2231c20d7f8d53d1e1c74d1a9)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 4e0079c282bca6a2231c20d7f8d53d1e1c74d1a9

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 4e0079c282bc | **14.45s** | 13,475 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 4e0079c282bc | **14.98s** | 12,997 LOC/s | 46 |
| xtsc (cold JVM) | 4e0079c282bc | 34.43s | 5,655 LOC/s | 46 |
| tsc | 6.0.3 | 10.18s | 19,133 LOC/s | 65 |
| tsgo | 7.0.2 | 1.56s | 124,889 LOC/s | 65 |
| xtsc-native | 4e0079c282bc (AOT) | 24.63s | 7,905 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.42x tsc — parity at 1.00x; 9.27x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **14.98s**, **+3.7%** against `temurin26` on identical bytecode — 1.47× tsc, 9.61× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.38×** tsc and **22.08×** tsgo; tsc is **6.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **34.31s**.

Ahead-of-time (GraalVM native-image): **24.63s** — **2.42×** tsc, **15.80×** tsgo, **1.40×** the COLD JVM arm and **1.70×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 4e0079c282bc | **15.26s** | 12,755 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 4e0079c282bc | **15.85s** | 12,281 LOC/s | 46 |
| xtsc (cold JVM) | 4e0079c282bc | 36.25s | 5,370 LOC/s | 46 |
| tsc | 6.0.3 | 12.67s | 15,364 LOC/s | 65 |
| tsgo | 7.0.2 | 2.27s | 85,620 LOC/s | 65 |
| xtsc-native | 4e0079c282bc (AOT) | 25.64s | 7,593 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.20x tsc — parity at 1.00x; 6.71x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **15.85s**, **+3.9%** against `temurin26` on identical bytecode — 1.25× tsc, 6.97× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.86×** tsc and **15.94×** tsgo; tsc is **5.57×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **36.10s**.

Ahead-of-time (GraalVM native-image): **25.64s** — **2.02×** tsc, **11.27×** tsgo, **1.41×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
