# Bench run 20260805T012922Z

- **xtsc revision**: [`78ef65762ebf`](https://github.com/xemantic/xemantic-typescript-compiler/commit/78ef65762ebf96ed5a465908ec711b64919aee44)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 78ef65762ebf96ed5a465908ec711b64919aee44

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 78ef65762ebf | 24.41s | 7,975 LOC/s | 46 |
| tsc | 6.0.3 | 10.67s | 18,252 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.69s | 115,140 LOC/s | 65 |
| xtsc-native | 78ef65762ebf (AOT) | 10.17s | 19,150 LOC/s | 46 |

Relative (wall): xtsc is **2.29×** tsc and **14.44×** tsgo; tsc is **6.31×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.31s**.

Ahead-of-time (GraalVM native-image): **10.17s**, **2.40×** the JVM arm, **0.95×** tsc, **6.01×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 78ef65762ebf | 26.70s | 7,292 LOC/s | 46 |
| tsc | 6.0.3 | 13.11s | 14,854 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.39s | 81,601 LOC/s | 65 |
| xtsc-native | 78ef65762ebf (AOT) | 12.58s | 15,475 LOC/s | 46 |

Relative (wall): xtsc is **2.04×** tsc and **11.19×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.58s**.

Ahead-of-time (GraalVM native-image): **12.58s**, **2.12×** the JVM arm, **0.96×** tsc, **5.27×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
