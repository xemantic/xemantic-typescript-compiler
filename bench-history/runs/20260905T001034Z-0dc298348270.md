# Bench run 20260905T001034Z

- **xtsc revision**: [`0dc298348270`](https://github.com/xemantic/xemantic-typescript-compiler/commit/0dc29834827023d9db9530c6edf1500291aed390)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 0dc29834827023d9db9530c6edf1500291aed390

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0dc298348270 | **4.16s** | 46,792 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0dc298348270 | **3.98s** | 48,981 LOC/s | 46 |
| xtsc (cold JVM) | 0dc298348270 | 23.49s | 8,288 LOC/s | 46 |
| tsc | 6.0.3 | 10.73s | 18,152 LOC/s | 65 |
| tsgo | 7.0.2 | 1.55s | 125,776 LOC/s | 65 |
| xtsc-native | 0dc298348270 (AOT) | 6.57s | 29,639 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.58x FASTER than tsc; 2.69x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.98s**, **-4.5%** against `temurin26` on identical bytecode — 0.37× tsc, 2.57× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.19×** tsc and **15.18×** tsgo; tsc is **6.93×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.37s**.

Ahead-of-time (GraalVM native-image): **6.57s** — **0.61×** tsc, **4.24×** tsgo, **3.58×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0dc298348270 | **5.08s** | 38,349 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0dc298348270 | **4.56s** | 42,669 LOC/s | 46 |
| xtsc (cold JVM) | 0dc298348270 | 24.17s | 8,056 LOC/s | 46 |
| tsc | 6.0.3 | 12.53s | 15,542 LOC/s | 65 |
| tsgo | 7.0.2 | 2.32s | 83,850 LOC/s | 65 |
| xtsc-native | 0dc298348270 (AOT) | 8.43s | 23,088 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.47x FASTER than tsc; 2.19x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.56s**, **-10.1%** against `temurin26` on identical bytecode — 0.36× tsc, 1.97× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.93×** tsc and **10.41×** tsgo; tsc is **5.39×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.05s**.

Ahead-of-time (GraalVM native-image): **8.43s** — **0.67×** tsc, **3.63×** tsgo, **2.87×** the COLD JVM arm and **1.66×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
