# Bench run 20260912T132543Z

- **xtsc revision**: [`eba54e4f94cd`](https://github.com/xemantic/xemantic-typescript-compiler/commit/eba54e4f94cd9fe73cc7504ed38f7dc4e0fc417f)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push eba54e4f94cd9fe73cc7504ed38f7dc4e0fc417f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | eba54e4f94cd | **5.82s** | 33,448 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | eba54e4f94cd | **5.06s** | 38,493 LOC/s | 46 |
| xtsc (cold JVM) | eba54e4f94cd | 24.87s | 7,829 LOC/s | 46 |
| tsc | 6.0.3 | 10.81s | 18,004 LOC/s | 65 |
| tsgo | 7.0.2 | 1.75s | 111,131 LOC/s | 65 |
| xtsc-native | eba54e4f94cd (AOT) | 9.11s | 21,379 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.86x FASTER than tsc; 3.32x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.06s**, **-13.1%** against `temurin26` on identical bytecode — 0.47× tsc, 2.89× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.30×** tsc and **14.19×** tsgo; tsc is **6.17×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.75s**.

Ahead-of-time (GraalVM native-image): **9.11s** — **0.84×** tsc, **5.20×** tsgo, **2.73×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | eba54e4f94cd | **6.47s** | 30,083 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | eba54e4f94cd | **5.71s** | 34,086 LOC/s | 46 |
| xtsc (cold JVM) | eba54e4f94cd | 30.47s | 6,389 LOC/s | 46 |
| tsc | 6.0.3 | 13.30s | 14,642 LOC/s | 65 |
| tsgo | 7.0.2 | 2.44s | 79,632 LOC/s | 65 |
| xtsc-native | eba54e4f94cd (AOT) | 10.43s | 18,669 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.05x FASTER than tsc; 2.65x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.71s**, **-11.7%** against `temurin26` on identical bytecode — 0.43× tsc, 2.34× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.29×** tsc and **12.46×** tsgo; tsc is **5.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.34s**.

Ahead-of-time (GraalVM native-image): **10.43s** — **0.78×** tsc, **4.27×** tsgo, **2.92×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
