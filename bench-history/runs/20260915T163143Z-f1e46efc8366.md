# Bench run 20260915T163143Z

- **xtsc revision**: [`f1e46efc8366`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f1e46efc83662c3eef47a7194b064c4d549b7331)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push f1e46efc83662c3eef47a7194b064c4d549b7331

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f1e46efc8366 | **4.56s** | 42,697 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f1e46efc8366 | **4.17s** | 46,679 LOC/s | 46 |
| xtsc (cold JVM) | f1e46efc8366 | 20.34s | 9,572 LOC/s | 46 |
| tsc | 6.0.3 | 8.27s | 23,543 LOC/s | 65 |
| tsgo | 7.0.2 | 1.19s | 163,615 LOC/s | 65 |
| xtsc-native | f1e46efc8366 (AOT) | 7.62s | 25,558 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.81x FASTER than tsc; 3.83x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.17s**, **-8.5%** against `temurin26` on identical bytecode — 0.50× tsc, 3.51× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.46×** tsc and **17.09×** tsgo; tsc is **6.95×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.25s**.

Ahead-of-time (GraalVM native-image): **7.62s** — **0.92×** tsc, **6.40×** tsgo, **2.67×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f1e46efc8366 | **5.30s** | 36,763 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f1e46efc8366 | **4.80s** | 40,529 LOC/s | 46 |
| xtsc (cold JVM) | f1e46efc8366 | 22.16s | 8,785 LOC/s | 46 |
| tsc | 6.0.3 | 10.00s | 19,472 LOC/s | 65 |
| tsgo | 7.0.2 | 1.81s | 107,451 LOC/s | 65 |
| xtsc-native | f1e46efc8366 (AOT) | 8.46s | 23,008 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.89x FASTER than tsc; 2.92x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.80s**, **-9.3%** against `temurin26` on identical bytecode — 0.48× tsc, 2.65× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.22×** tsc and **12.23×** tsgo; tsc is **5.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.06s**.

Ahead-of-time (GraalVM native-image): **8.46s** — **0.85×** tsc, **4.67×** tsgo, **2.62×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
