# Bench run 20260824T185239Z

- **xtsc revision**: [`2fa8a39f94f1`](https://github.com/xemantic/xemantic-typescript-compiler/commit/2fa8a39f94f1d89f4b23ec774376888766e8b9ec)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 2fa8a39f94f1d89f4b23ec774376888766e8b9ec

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 2fa8a39f94f1 | **3.82s** | 50,982 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 2fa8a39f94f1 | **3.59s** | 54,249 LOC/s | 46 |
| xtsc (cold JVM) | 2fa8a39f94f1 | 20.63s | 9,438 LOC/s | 46 |
| tsc | 6.0.3 | 10.14s | 19,195 LOC/s | 65 |
| tsgo | 7.0.2 | 1.55s | 125,210 LOC/s | 65 |
| xtsc-native | 2fa8a39f94f1 (AOT) | 5.95s | 32,717 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.66x FASTER than tsc; 2.46x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.59s**, **-6.0%** against `temurin26` on identical bytecode — 0.35× tsc, 2.31× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.03×** tsc and **13.27×** tsgo; tsc is **6.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.52s**.

Ahead-of-time (GraalVM native-image): **5.95s** — **0.59×** tsc, **3.83×** tsgo, **3.47×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 2fa8a39f94f1 | **4.57s** | 42,632 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 2fa8a39f94f1 | **4.16s** | 46,814 LOC/s | 46 |
| xtsc (cold JVM) | 2fa8a39f94f1 | 22.48s | 8,659 LOC/s | 46 |
| tsc | 6.0.3 | 12.29s | 15,847 LOC/s | 65 |
| tsgo | 7.0.2 | 2.25s | 86,457 LOC/s | 65 |
| xtsc-native | 2fa8a39f94f1 (AOT) | 7.56s | 25,744 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.69x FASTER than tsc; 2.03x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.16s**, **-8.9%** against `temurin26` on identical bytecode — 0.34× tsc, 1.85× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.83×** tsc and **9.98×** tsgo; tsc is **5.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.37s**.

Ahead-of-time (GraalVM native-image): **7.56s** — **0.62×** tsc, **3.36×** tsgo, **2.97×** the COLD JVM arm and **1.66×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
