# Bench run 20260822T143906Z

- **xtsc revision**: [`dc000db22a4e`](https://github.com/xemantic/xemantic-typescript-compiler/commit/dc000db22a4e0fda217ba843d483d9e801f31a23)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push dc000db22a4e0fda217ba843d483d9e801f31a23

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | dc000db22a4e | **3.96s** | 49,129 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | dc000db22a4e | **3.80s** | 51,237 LOC/s | 46 |
| xtsc (cold JVM) | dc000db22a4e | 23.57s | 8,262 LOC/s | 46 |
| tsc | 6.0.3 | 10.87s | 17,915 LOC/s | 65 |
| tsgo | 7.0.2 | 1.70s | 114,597 LOC/s | 65 |
| xtsc-native | dc000db22a4e (AOT) | 6.03s | 32,310 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.74x FASTER than tsc; 2.33x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.80s**, **-4.1%** against `temurin26` on identical bytecode — 0.35× tsc, 2.24× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.17×** tsc and **13.87×** tsgo; tsc is **6.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.45s**.

Ahead-of-time (GraalVM native-image): **6.03s** — **0.55×** tsc, **3.55×** tsgo, **3.91×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | dc000db22a4e | **4.83s** | 40,336 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | dc000db22a4e | **4.34s** | 44,851 LOC/s | 46 |
| xtsc (cold JVM) | dc000db22a4e | 26.85s | 7,250 LOC/s | 46 |
| tsc | 6.0.3 | 13.22s | 14,732 LOC/s | 65 |
| tsgo | 7.0.2 | 2.42s | 80,488 LOC/s | 65 |
| xtsc-native | dc000db22a4e (AOT) | 7.33s | 26,558 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.74x FASTER than tsc; 2.00x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.34s**, **-10.1%** against `temurin26` on identical bytecode — 0.33× tsc, 1.79× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.03×** tsc and **11.10×** tsgo; tsc is **5.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.74s**.

Ahead-of-time (GraalVM native-image): **7.33s** — **0.55×** tsc, **3.03×** tsgo, **3.66×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
