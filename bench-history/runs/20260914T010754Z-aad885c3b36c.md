# Bench run 20260914T010754Z

- **xtsc revision**: [`aad885c3b36c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/aad885c3b36ca90f94b71d09123cd5b4b9e29efd)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push aad885c3b36ca90f94b71d09123cd5b4b9e29efd

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | aad885c3b36c | **4.89s** | 39,840 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | aad885c3b36c | **4.64s** | 41,979 LOC/s | 46 |
| xtsc (cold JVM) | aad885c3b36c | 23.34s | 8,340 LOC/s | 46 |
| tsc | 6.0.3 | 9.53s | 20,424 LOC/s | 65 |
| tsgo | 7.0.2 | 1.42s | 137,501 LOC/s | 65 |
| xtsc-native | aad885c3b36c (AOT) | 7.53s | 25,870 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.95x FASTER than tsc; 3.45x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.64s**, **-5.1%** against `temurin26` on identical bytecode — 0.49× tsc, 3.28× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.45×** tsc and **16.49×** tsgo; tsc is **6.73×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.24s**.

Ahead-of-time (GraalVM native-image): **7.53s** — **0.79×** tsc, **5.31×** tsgo, **3.10×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | aad885c3b36c | **5.60s** | 34,749 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | aad885c3b36c | **5.29s** | 36,833 LOC/s | 46 |
| xtsc (cold JVM) | aad885c3b36c | 31.26s | 6,228 LOC/s | 46 |
| tsc | 6.0.3 | 11.43s | 17,031 LOC/s | 65 |
| tsgo | 7.0.2 | 2.18s | 89,353 LOC/s | 65 |
| xtsc-native | aad885c3b36c (AOT) | 8.98s | 21,691 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.04x FASTER than tsc; 2.57x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.29s**, **-5.7%** against `temurin26` on identical bytecode — 0.46× tsc, 2.43× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.73×** tsc and **14.35×** tsgo; tsc is **5.25×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **31.14s**.

Ahead-of-time (GraalVM native-image): **8.98s** — **0.79×** tsc, **4.12×** tsgo, **3.48×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
