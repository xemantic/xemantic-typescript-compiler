# Bench run 20260801T131953Z

- **xtsc revision**: [`3f153788a32c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/3f153788a32c8176213c4fb8f9b130a83dbaff66)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 3f153788a32c8176213c4fb8f9b130a83dbaff66

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 3f153788a32c | 30.48s | 6,387 LOC/s | 46 |
| tsc | 6.0.3 | 10.63s | 18,309 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.68s | 115,894 LOC/s | 65 |
| xtsc-native | 3f153788a32c (AOT) | 10.53s | 18,495 LOC/s | 46 |

Relative (wall): xtsc is **2.87×** tsc and **18.14×** tsgo; tsc is **6.33×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.38s**.

Ahead-of-time (GraalVM native-image): **10.53s**, **2.90×** the JVM arm, **0.99×** tsc, **6.27×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 3f153788a32c | 33.18s | 5,868 LOC/s | 46 |
| tsc | 6.0.3 | 12.97s | 15,008 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.43s | 80,157 LOC/s | 65 |
| xtsc-native | 3f153788a32c (AOT) | 13.38s | 14,548 LOC/s | 46 |

Relative (wall): xtsc is **2.56×** tsc and **13.66×** tsgo; tsc is **5.34×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **33.07s**.

Ahead-of-time (GraalVM native-image): **13.38s**, **2.48×** the JVM arm, **1.03×** tsc, **5.51×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
