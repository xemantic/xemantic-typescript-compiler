# Bench run 20260902T212004Z

- **xtsc revision**: [`fb1768f9f606`](https://github.com/xemantic/xemantic-typescript-compiler/commit/fb1768f9f606fb476bf3d31564486a0d2d5d40b3)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push fb1768f9f606fb476bf3d31564486a0d2d5d40b3

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | fb1768f9f606 | **4.29s** | 45,363 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | fb1768f9f606 | **4.09s** | 47,604 LOC/s | 46 |
| xtsc (cold JVM) | fb1768f9f606 | 24.09s | 8,080 LOC/s | 46 |
| tsc | 6.0.3 | 10.80s | 18,019 LOC/s | 65 |
| tsgo | 7.0.2 | 1.65s | 118,144 LOC/s | 65 |
| xtsc-native | fb1768f9f606 (AOT) | 6.40s | 30,426 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.52x FASTER than tsc; 2.60x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.09s**, **-4.7%** against `temurin26` on identical bytecode — 0.38× tsc, 2.48× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.23×** tsc and **14.62×** tsgo; tsc is **6.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.97s**.

Ahead-of-time (GraalVM native-image): **6.40s** — **0.59×** tsc, **3.88×** tsgo, **3.77×** the COLD JVM arm and **1.49×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | fb1768f9f606 | **5.09s** | 38,244 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | fb1768f9f606 | **4.56s** | 42,735 LOC/s | 46 |
| xtsc (cold JVM) | fb1768f9f606 | 27.89s | 6,980 LOC/s | 46 |
| tsc | 6.0.3 | 13.02s | 14,959 LOC/s | 65 |
| tsgo | 7.0.2 | 2.39s | 81,533 LOC/s | 65 |
| xtsc-native | fb1768f9f606 (AOT) | 7.94s | 24,509 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.56x FASTER than tsc; 2.13x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.56s**, **-10.5%** against `temurin26` on identical bytecode — 0.35× tsc, 1.91× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.14×** tsc and **11.68×** tsgo; tsc is **5.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.76s**.

Ahead-of-time (GraalVM native-image): **7.94s** — **0.61×** tsc, **3.33×** tsgo, **3.51×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
