# Bench run 20260814T223954Z

- **xtsc revision**: [`27a03c4c44d0`](https://github.com/xemantic/xemantic-typescript-compiler/commit/27a03c4c44d094bb9d8c250c1d26135c54e19728)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 27a03c4c44d094bb9d8c250c1d26135c54e19728

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 27a03c4c44d0 | **3.96s** | 49,154 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 27a03c4c44d0 | **3.73s** | 52,226 LOC/s | 46 |
| xtsc (cold JVM) | 27a03c4c44d0 | 22.60s | 8,613 LOC/s | 46 |
| tsc | 6.0.3 | 10.84s | 17,961 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,481 LOC/s | 65 |
| xtsc-native | 27a03c4c44d0 (AOT) | 5.88s | 33,112 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.74x FASTER than tsc; 2.35x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.73s**, **-5.9%** against `temurin26` on identical bytecode — 0.34× tsc, 2.21× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.09×** tsc and **13.41×** tsgo; tsc is **6.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.48s**.

Ahead-of-time (GraalVM native-image): **5.88s** — **0.54×** tsc, **3.49×** tsgo, **3.84×** the COLD JVM arm and **1.48×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 27a03c4c44d0 | **4.94s** | 39,421 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 27a03c4c44d0 | **4.43s** | 43,960 LOC/s | 46 |
| xtsc (cold JVM) | 27a03c4c44d0 | 24.79s | 7,853 LOC/s | 46 |
| tsc | 6.0.3 | 13.40s | 14,526 LOC/s | 65 |
| tsgo | 7.0.2 | 2.39s | 81,329 LOC/s | 65 |
| xtsc-native | 27a03c4c44d0 (AOT) | 7.48s | 26,022 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.71x FASTER than tsc; 2.06x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.43s**, **-10.3%** against `temurin26` on identical bytecode — 0.33× tsc, 1.85× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.85×** tsc and **10.36×** tsgo; tsc is **5.60×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.66s**.

Ahead-of-time (GraalVM native-image): **7.48s** — **0.56×** tsc, **3.13×** tsgo, **3.31×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
