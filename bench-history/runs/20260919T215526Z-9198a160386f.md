# Bench run 20260919T215526Z

- **xtsc revision**: [`9198a160386f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/9198a160386f3f55f0365525d2848edc11f5e65f)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 9198a160386f3f55f0365525d2848edc11f5e65f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9198a160386f | **5.91s** | 32,933 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9198a160386f | **5.23s** | 37,235 LOC/s | 46 |
| xtsc (cold JVM) | 9198a160386f | 24.48s | 7,953 LOC/s | 46 |
| tsc | 6.0.3 | 10.84s | 17,954 LOC/s | 65 |
| tsgo | 7.0.2 | 1.75s | 111,258 LOC/s | 65 |
| xtsc-native | 9198a160386f (AOT) | 9.72s | 20,022 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.83x FASTER than tsc; 3.38x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.23s**, **-11.6%** against `temurin26` on identical bytecode — 0.48× tsc, 2.99× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.26×** tsc and **13.99×** tsgo; tsc is **6.20×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.36s**.

Ahead-of-time (GraalVM native-image): **9.72s** — **0.90×** tsc, **5.56×** tsgo, **2.52×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9198a160386f | **6.68s** | 29,164 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9198a160386f | **5.79s** | 33,621 LOC/s | 46 |
| xtsc (cold JVM) | 9198a160386f | 31.18s | 6,244 LOC/s | 46 |
| tsc | 6.0.3 | 13.10s | 14,868 LOC/s | 65 |
| tsgo | 7.0.2 | 2.42s | 80,488 LOC/s | 65 |
| xtsc-native | 9198a160386f (AOT) | 10.35s | 18,815 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.96x FASTER than tsc; 2.76x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.79s**, **-13.3%** against `temurin26` on identical bytecode — 0.44× tsc, 2.39× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.38×** tsc and **12.89×** tsgo; tsc is **5.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **31.04s**.

Ahead-of-time (GraalVM native-image): **10.35s** — **0.79×** tsc, **4.28×** tsgo, **3.01×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
