# Bench run 20260901T073747Z

- **xtsc revision**: [`5473af5fe77a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/5473af5fe77a20a79530a4c88bb242b585f04519)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 5473af5fe77a20a79530a4c88bb242b585f04519

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5473af5fe77a | **4.41s** | 44,170 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5473af5fe77a | **4.38s** | 44,442 LOC/s | 46 |
| xtsc (cold JVM) | 5473af5fe77a | 24.79s | 7,855 LOC/s | 46 |
| tsc | 6.0.3 | 11.40s | 17,073 LOC/s | 65 |
| tsgo | 7.0.2 | 1.78s | 109,199 LOC/s | 65 |
| xtsc-native | 5473af5fe77a (AOT) | 7.57s | 25,733 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.59x FASTER than tsc; 2.47x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.38s**, **-0.6%** against `temurin26` on identical bytecode — 0.38× tsc, 2.46× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.17×** tsc and **13.90×** tsgo; tsc is **6.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.67s**.

Ahead-of-time (GraalVM native-image): **7.57s** — **0.66×** tsc, **4.24×** tsgo, **3.28×** the COLD JVM arm and **1.72×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5473af5fe77a | **5.34s** | 36,426 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5473af5fe77a | **5.01s** | 38,878 LOC/s | 46 |
| xtsc (cold JVM) | 5473af5fe77a | 26.87s | 7,245 LOC/s | 46 |
| tsc | 6.0.3 | 13.91s | 14,001 LOC/s | 65 |
| tsgo | 7.0.2 | 2.46s | 79,018 LOC/s | 65 |
| xtsc-native | 5473af5fe77a (AOT) | 8.73s | 22,305 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.60x FASTER than tsc; 2.17x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.01s**, **-6.3%** against `temurin26` on identical bytecode — 0.36× tsc, 2.03× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.93×** tsc and **10.91×** tsgo; tsc is **5.64×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.75s**.

Ahead-of-time (GraalVM native-image): **8.73s** — **0.63×** tsc, **3.54×** tsgo, **3.08×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
