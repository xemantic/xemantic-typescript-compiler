# Bench run 20260823T145348Z

- **xtsc revision**: [`aca8a60f30dd`](https://github.com/xemantic/xemantic-typescript-compiler/commit/aca8a60f30dd3bc0491787404595bb5399a9152f)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push aca8a60f30dd3bc0491787404595bb5399a9152f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | aca8a60f30dd | **4.07s** | 47,838 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | aca8a60f30dd | **3.84s** | 50,716 LOC/s | 46 |
| xtsc (cold JVM) | aca8a60f30dd | 22.50s | 8,652 LOC/s | 46 |
| tsc | 6.0.3 | 10.62s | 18,326 LOC/s | 65 |
| tsgo | 7.0.2 | 1.59s | 122,762 LOC/s | 65 |
| xtsc-native | aca8a60f30dd (AOT) | 6.75s | 28,857 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.61x FASTER than tsc; 2.57x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.84s**, **-5.7%** against `temurin26` on identical bytecode — 0.36× tsc, 2.42× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.12×** tsc and **14.19×** tsgo; tsc is **6.70×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.38s**.

Ahead-of-time (GraalVM native-image): **6.75s** — **0.64×** tsc, **4.25×** tsgo, **3.34×** the COLD JVM arm and **1.66×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | aca8a60f30dd | **5.24s** | 37,178 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | aca8a60f30dd | **4.60s** | 42,317 LOC/s | 46 |
| xtsc (cold JVM) | aca8a60f30dd | 24.89s | 7,820 LOC/s | 46 |
| tsc | 6.0.3 | 12.98s | 15,002 LOC/s | 65 |
| tsgo | 7.0.2 | 2.37s | 82,152 LOC/s | 65 |
| xtsc-native | aca8a60f30dd (AOT) | 8.03s | 24,240 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.48x FASTER than tsc; 2.21x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.60s**, **-12.1%** against `temurin26` on identical bytecode — 0.35× tsc, 1.94× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.92×** tsc and **10.50×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.78s**.

Ahead-of-time (GraalVM native-image): **8.03s** — **0.62×** tsc, **3.39×** tsgo, **3.10×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
