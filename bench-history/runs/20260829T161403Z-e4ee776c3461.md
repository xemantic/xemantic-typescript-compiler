# Bench run 20260829T161403Z

- **xtsc revision**: [`e4ee776c3461`](https://github.com/xemantic/xemantic-typescript-compiler/commit/e4ee776c3461da4f350a5b27f08003ae237e5cfe)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push e4ee776c3461da4f350a5b27f08003ae237e5cfe

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e4ee776c3461 | **4.24s** | 45,898 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e4ee776c3461 | **4.04s** | 48,217 LOC/s | 46 |
| xtsc (cold JVM) | e4ee776c3461 | 25.04s | 7,775 LOC/s | 46 |
| tsc | 6.0.3 | 10.62s | 18,342 LOC/s | 65 |
| tsgo | 7.0.2 | 1.68s | 115,963 LOC/s | 65 |
| xtsc-native | e4ee776c3461 (AOT) | 6.84s | 28,465 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.50x FASTER than tsc; 2.53x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.04s**, **-4.8%** against `temurin26` on identical bytecode — 0.38× tsc, 2.41× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.36×** tsc and **14.91×** tsgo; tsc is **6.32×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.89s**.

Ahead-of-time (GraalVM native-image): **6.84s** — **0.64×** tsc, **4.07×** tsgo, **3.66×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e4ee776c3461 | **5.25s** | 37,057 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e4ee776c3461 | **4.67s** | 41,701 LOC/s | 46 |
| xtsc (cold JVM) | e4ee776c3461 | 26.78s | 7,270 LOC/s | 46 |
| tsc | 6.0.3 | 13.34s | 14,595 LOC/s | 65 |
| tsgo | 7.0.2 | 2.41s | 80,722 LOC/s | 65 |
| xtsc-native | e4ee776c3461 (AOT) | 7.97s | 24,417 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.54x FASTER than tsc; 2.18x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.67s**, **-11.1%** against `temurin26` on identical bytecode — 0.35× tsc, 1.94× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.01×** tsc and **11.10×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.66s**.

Ahead-of-time (GraalVM native-image): **7.97s** — **0.60×** tsc, **3.31×** tsgo, **3.36×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
