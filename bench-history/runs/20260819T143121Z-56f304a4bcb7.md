# Bench run 20260819T143121Z

- **xtsc revision**: [`56f304a4bcb7`](https://github.com/xemantic/xemantic-typescript-compiler/commit/56f304a4bcb7a9018730d75d0f613774fe251c48)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 56f304a4bcb7a9018730d75d0f613774fe251c48

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 56f304a4bcb7 | **4.10s** | 47,442 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 56f304a4bcb7 | **3.75s** | 51,906 LOC/s | 46 |
| xtsc (cold JVM) | 56f304a4bcb7 | 22.91s | 8,499 LOC/s | 46 |
| tsc | 6.0.3 | 10.51s | 18,523 LOC/s | 65 |
| tsgo | 7.0.2 | 1.59s | 122,146 LOC/s | 65 |
| xtsc-native | 56f304a4bcb7 (AOT) | 6.44s | 30,219 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.56x FASTER than tsc; 2.57x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.75s**, **-8.6%** against `temurin26` on identical bytecode — 0.36× tsc, 2.35× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.18×** tsc and **14.37×** tsgo; tsc is **6.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.79s**.

Ahead-of-time (GraalVM native-image): **6.44s** — **0.61×** tsc, **4.04×** tsgo, **3.56×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 56f304a4bcb7 | **5.04s** | 38,593 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 56f304a4bcb7 | **4.64s** | 41,961 LOC/s | 46 |
| xtsc (cold JVM) | 56f304a4bcb7 | 24.07s | 8,089 LOC/s | 46 |
| tsc | 6.0.3 | 12.92s | 15,073 LOC/s | 65 |
| tsgo | 7.0.2 | 2.33s | 83,527 LOC/s | 65 |
| xtsc-native | 56f304a4bcb7 (AOT) | 7.63s | 25,511 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.56x FASTER than tsc; 2.16x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.64s**, **-8.0%** against `temurin26` on identical bytecode — 0.36× tsc, 1.99× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.86×** tsc and **10.33×** tsgo; tsc is **5.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.91s**.

Ahead-of-time (GraalVM native-image): **7.63s** — **0.59×** tsc, **3.27×** tsgo, **3.15×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
