# Bench run 20260803T012521Z

- **xtsc revision**: [`22a1921370b5`](https://github.com/xemantic/xemantic-typescript-compiler/commit/22a1921370b571bc6ccb104d7a8484de027c92e3)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 22a1921370b571bc6ccb104d7a8484de027c92e3

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 22a1921370b5 | 26.85s | 7,251 LOC/s | 46 |
| tsc | 6.0.3 | 10.68s | 18,227 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.76s | 110,500 LOC/s | 65 |
| xtsc-native | 22a1921370b5 (AOT) | 11.20s | 17,379 LOC/s | 46 |

Relative (wall): xtsc is **2.51×** tsc and **15.24×** tsgo; tsc is **6.06×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.75s**.

Ahead-of-time (GraalVM native-image): **11.20s**, **2.40×** the JVM arm, **1.05×** tsc, **6.36×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 22a1921370b5 | 29.83s | 6,526 LOC/s | 46 |
| tsc | 6.0.3 | 13.39s | 14,540 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.49s | 78,256 LOC/s | 65 |
| xtsc-native | 22a1921370b5 (AOT) | 13.48s | 14,446 LOC/s | 46 |

Relative (wall): xtsc is **2.23×** tsc and **11.99×** tsgo; tsc is **5.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.73s**.

Ahead-of-time (GraalVM native-image): **13.48s**, **2.21×** the JVM arm, **1.01×** tsc, **5.42×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
