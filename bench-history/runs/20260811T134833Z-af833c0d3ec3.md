# Bench run 20260811T134833Z

- **xtsc revision**: [`af833c0d3ec3`](https://github.com/xemantic/xemantic-typescript-compiler/commit/af833c0d3ec3fe662ab9bc0498adaee9d286ae80)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push af833c0d3ec3fe662ab9bc0498adaee9d286ae80

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | af833c0d3ec3 | **3.39s** | 57,468 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | af833c0d3ec3 | **3.25s** | 59,834 LOC/s | 46 |
| xtsc (cold JVM) | af833c0d3ec3 | 19.52s | 9,975 LOC/s | 46 |
| tsc | 6.0.3 | 8.76s | 22,228 LOC/s | 65 |
| tsgo | 7.0.2 | 1.34s | 145,408 LOC/s | 65 |
| xtsc-native | af833c0d3ec3 (AOT) | 5.41s | 36,015 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.59x FASTER than tsc; 2.53x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.25s**, **-4.0%** against `temurin26` on identical bytecode — 0.37× tsc, 2.43× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.23×** tsc and **14.58×** tsgo; tsc is **6.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.39s**.

Ahead-of-time (GraalVM native-image): **5.41s** — **0.62×** tsc, **4.04×** tsgo, **3.61×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | af833c0d3ec3 | **4.20s** | 46,357 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | af833c0d3ec3 | **3.82s** | 50,995 LOC/s | 46 |
| xtsc (cold JVM) | af833c0d3ec3 | 24.28s | 8,017 LOC/s | 46 |
| tsc | 6.0.3 | 10.32s | 18,873 LOC/s | 65 |
| tsgo | 7.0.2 | 1.92s | 101,354 LOC/s | 65 |
| xtsc-native | af833c0d3ec3 (AOT) | 5.91s | 32,972 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.46x FASTER than tsc; 2.19x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.82s**, **-9.1%** against `temurin26` on identical bytecode — 0.37× tsc, 1.99× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.35×** tsc and **12.64×** tsgo; tsc is **5.37×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.17s**.

Ahead-of-time (GraalVM native-image): **5.91s** — **0.57×** tsc, **3.07×** tsgo, **4.11×** the COLD JVM arm and **1.41×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
