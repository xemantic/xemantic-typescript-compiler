# Bench run 20260911T123821Z

- **xtsc revision**: [`e9196002481b`](https://github.com/xemantic/xemantic-typescript-compiler/commit/e9196002481b69be9d2c266a045af19a19c8cd47)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push e9196002481b69be9d2c266a045af19a19c8cd47

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e9196002481b | **5.83s** | 33,425 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e9196002481b | **5.21s** | 37,370 LOC/s | 46 |
| xtsc (cold JVM) | e9196002481b | 25.49s | 7,637 LOC/s | 46 |
| tsc | 6.0.3 | 11.08s | 17,575 LOC/s | 65 |
| tsgo | 7.0.2 | 1.72s | 112,936 LOC/s | 65 |
| xtsc-native | e9196002481b (AOT) | 8.93s | 21,810 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.90x FASTER than tsc; 3.38x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.21s**, **-10.6%** against `temurin26` on identical bytecode — 0.47× tsc, 3.02× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.30×** tsc and **14.79×** tsgo; tsc is **6.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.37s**.

Ahead-of-time (GraalVM native-image): **8.93s** — **0.81×** tsc, **5.18×** tsgo, **2.86×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e9196002481b | **6.50s** | 29,972 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e9196002481b | **5.75s** | 33,831 LOC/s | 46 |
| xtsc (cold JVM) | e9196002481b | 30.78s | 6,326 LOC/s | 46 |
| tsc | 6.0.3 | 13.55s | 14,372 LOC/s | 65 |
| tsgo | 7.0.2 | 2.44s | 79,632 LOC/s | 65 |
| xtsc-native | e9196002481b (AOT) | 10.24s | 19,017 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.09x FASTER than tsc; 2.66x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.75s**, **-11.4%** against `temurin26` on identical bytecode — 0.42× tsc, 2.35× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.27×** tsc and **12.59×** tsgo; tsc is **5.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.66s**.

Ahead-of-time (GraalVM native-image): **10.24s** — **0.76×** tsc, **4.19×** tsgo, **3.01×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
