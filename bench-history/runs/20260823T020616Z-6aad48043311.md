# Bench run 20260823T020616Z

- **xtsc revision**: [`6aad48043311`](https://github.com/xemantic/xemantic-typescript-compiler/commit/6aad480433116b20ed0236e6e9e83336bc17040e)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 6aad480433116b20ed0236e6e9e83336bc17040e

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6aad48043311 | **2.62s** | 74,228 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6aad48043311 | **2.56s** | 76,204 LOC/s | 46 |
| xtsc (cold JVM) | 6aad48043311 | 18.19s | 10,702 LOC/s | 46 |
| tsc | 6.0.3 | 7.84s | 24,824 LOC/s | 65 |
| tsgo | 7.0.2 | 1.23s | 157,781 LOC/s | 65 |
| xtsc-native | 6aad48043311 (AOT) | 4.32s | 45,028 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.99x FASTER than tsc; 2.13x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.56s**, **-2.6%** against `temurin26` on identical bytecode — 0.33× tsc, 2.07× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.32×** tsc and **14.74×** tsgo; tsc is **6.36×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.09s**.

Ahead-of-time (GraalVM native-image): **4.32s** — **0.55×** tsc, **3.50×** tsgo, **4.21×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6aad48043311 | **3.32s** | 58,715 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6aad48043311 | **3.18s** | 61,304 LOC/s | 46 |
| xtsc (cold JVM) | 6aad48043311 | 20.11s | 9,683 LOC/s | 46 |
| tsc | 6.0.3 | 9.59s | 20,298 LOC/s | 65 |
| tsgo | 7.0.2 | 1.83s | 106,162 LOC/s | 65 |
| xtsc-native | 6aad48043311 (AOT) | 5.29s | 36,798 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.89x FASTER than tsc; 1.81x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.18s**, **-4.2%** against `temurin26` on identical bytecode — 0.33× tsc, 1.73× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.10×** tsc and **10.96×** tsgo; tsc is **5.23×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.99s**.

Ahead-of-time (GraalVM native-image): **5.29s** — **0.55×** tsc, **2.88×** tsgo, **3.80×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
