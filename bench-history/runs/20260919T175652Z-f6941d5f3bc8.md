# Bench run 20260919T175652Z

- **xtsc revision**: [`f6941d5f3bc8`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f6941d5f3bc8c1ead6380461b3ea25e9ae589c10)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push f6941d5f3bc8c1ead6380461b3ea25e9ae589c10

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f6941d5f3bc8 | **3.16s** | 61,673 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f6941d5f3bc8 | **2.96s** | 65,688 LOC/s | 46 |
| xtsc (cold JVM) | f6941d5f3bc8 | 14.60s | 13,337 LOC/s | 46 |
| tsc | 6.0.3 | 6.00s | 32,450 LOC/s | 65 |
| tsgo | 7.0.2 | 0.93s | 209,808 LOC/s | 65 |
| xtsc-native | f6941d5f3bc8 (AOT) | 5.44s | 35,777 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.90x FASTER than tsc; 3.40x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.96s**, **-6.1%** against `temurin26` on identical bytecode — 0.49× tsc, 3.19× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.43×** tsc and **15.73×** tsgo; tsc is **6.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **14.51s**.

Ahead-of-time (GraalVM native-image): **5.44s** — **0.91×** tsc, **5.86×** tsgo, **2.68×** the COLD JVM arm and **1.72×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f6941d5f3bc8 | **3.78s** | 51,454 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f6941d5f3bc8 | **3.40s** | 57,214 LOC/s | 46 |
| xtsc (cold JVM) | f6941d5f3bc8 | 17.42s | 11,175 LOC/s | 46 |
| tsc | 6.0.3 | 7.40s | 26,296 LOC/s | 65 |
| tsgo | 7.0.2 | 1.35s | 143,903 LOC/s | 65 |
| xtsc-native | f6941d5f3bc8 (AOT) | 6.25s | 31,172 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.96x FASTER than tsc; 2.80x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.40s**, **-10.1%** against `temurin26` on identical bytecode — 0.46× tsc, 2.52× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.35×** tsc and **12.88×** tsgo; tsc is **5.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.33s**.

Ahead-of-time (GraalVM native-image): **6.25s** — **0.84×** tsc, **4.62×** tsgo, **2.79×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
