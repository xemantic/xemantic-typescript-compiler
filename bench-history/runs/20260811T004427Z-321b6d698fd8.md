# Bench run 20260811T004427Z

- **xtsc revision**: [`321b6d698fd8`](https://github.com/xemantic/xemantic-typescript-compiler/commit/321b6d698fd8fd08e1150b04d833ce358f5f6e62)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 321b6d698fd8fd08e1150b04d833ce358f5f6e62

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 321b6d698fd8 | **4.56s** | 42,697 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 321b6d698fd8 | **4.42s** | 44,020 LOC/s | 46 |
| xtsc (cold JVM) | 321b6d698fd8 | 24.09s | 8,081 LOC/s | 46 |
| tsc | 6.0.3 | 11.27s | 17,280 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.71s | 113,927 LOC/s | 65 |
| xtsc-native | 321b6d698fd8 (AOT) | 7.36s | 26,450 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.47x FASTER than tsc; 2.67x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.42s**, **-3.0%** against `temurin26` on identical bytecode — 0.39× tsc, 2.59× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.14×** tsc and **14.10×** tsgo; tsc is **6.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.98s**.

Ahead-of-time (GraalVM native-image): **7.36s** — **0.65×** tsc, **4.31×** tsgo, **3.27×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 321b6d698fd8 | **5.63s** | 34,558 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 321b6d698fd8 | **5.12s** | 38,020 LOC/s | 46 |
| xtsc (cold JVM) | 321b6d698fd8 | 29.69s | 6,558 LOC/s | 46 |
| tsc | 6.0.3 | 13.67s | 14,244 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.48s | 78,477 LOC/s | 65 |
| xtsc-native | 321b6d698fd8 (AOT) | 9.10s | 21,407 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.43x FASTER than tsc; 2.27x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.12s**, **-9.1%** against `temurin26` on identical bytecode — 0.37× tsc, 2.06× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.17×** tsc and **11.97×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.57s**.

Ahead-of-time (GraalVM native-image): **9.10s** — **0.67×** tsc, **3.67×** tsgo, **3.26×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
