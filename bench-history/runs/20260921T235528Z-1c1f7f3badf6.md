# Bench run 20260921T235528Z

- **xtsc revision**: [`1c1f7f3badf6`](https://github.com/xemantic/xemantic-typescript-compiler/commit/1c1f7f3badf6737d4b434501e8f37bfb2fd5f0ab)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 1c1f7f3badf6737d4b434501e8f37bfb2fd5f0ab

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1c1f7f3badf6 | **4.43s** | 43,960 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1c1f7f3badf6 | **4.07s** | 47,791 LOC/s | 46 |
| xtsc (cold JVM) | 1c1f7f3badf6 | 20.28s | 9,599 LOC/s | 46 |
| tsc | 6.0.3 | 8.06s | 24,153 LOC/s | 65 |
| tsgo | 7.0.2 | 1.27s | 153,308 LOC/s | 65 |
| xtsc-native | 1c1f7f3badf6 (AOT) | 7.19s | 27,064 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.82x FASTER than tsc; 3.49x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.07s**, **-8.0%** against `temurin26` on identical bytecode — 0.51× tsc, 3.21× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.52×** tsc and **15.97×** tsgo; tsc is **6.35×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.18s**.

Ahead-of-time (GraalVM native-image): **7.19s** — **0.89×** tsc, **5.66×** tsgo, **2.82×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1c1f7f3badf6 | **5.49s** | 35,497 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1c1f7f3badf6 | **4.86s** | 40,037 LOC/s | 46 |
| xtsc (cold JVM) | 1c1f7f3badf6 | 24.23s | 8,036 LOC/s | 46 |
| tsc | 6.0.3 | 9.58s | 20,321 LOC/s | 65 |
| tsgo | 7.0.2 | 1.83s | 106,278 LOC/s | 65 |
| xtsc-native | 1c1f7f3badf6 (AOT) | 8.00s | 24,340 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.75x FASTER than tsc; 2.99x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.86s**, **-11.3%** against `temurin26` on identical bytecode — 0.51× tsc, 2.65× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.53×** tsc and **13.22×** tsgo; tsc is **5.23×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.12s**.

Ahead-of-time (GraalVM native-image): **8.00s** — **0.83×** tsc, **4.37×** tsgo, **3.03×** the COLD JVM arm and **1.46×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
