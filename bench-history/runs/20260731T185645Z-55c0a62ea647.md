# Bench run 20260731T185645Z

- **xtsc revision**: [`55c0a62ea647`](https://github.com/xemantic/xemantic-typescript-compiler/commit/55c0a62ea647d739139c3f095f55757121b3df84)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 55c0a62ea647d739139c3f095f55757121b3df84

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 55c0a62ea647 | 28.39s | 6,858 LOC/s | 46 |
| tsc | 6.0.3 | 11.07s | 17,586 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.80s | 108,408 LOC/s | 65 |
| xtsc-native | 55c0a62ea647 (AOT) | 13.64s | 14,278 LOC/s | 46 |

Relative (wall): xtsc is **2.56×** tsc and **15.81×** tsgo; tsc is **6.16×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.27s**.

Ahead-of-time (GraalVM native-image): **13.64s**, **2.08×** the JVM arm, **1.23×** tsc, **7.59×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 55c0a62ea647 | 33.98s | 5,730 LOC/s | 46 |
| tsc | 6.0.3 | 13.85s | 14,058 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.60s | 74,943 LOC/s | 65 |
| xtsc-native | 55c0a62ea647 (AOT) | 13.71s | 14,196 LOC/s | 46 |

Relative (wall): xtsc is **2.45×** tsc and **13.08×** tsgo; tsc is **5.33×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **33.86s**.

Ahead-of-time (GraalVM native-image): **13.71s**, **2.48×** the JVM arm, **0.99×** tsc, **5.28×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
