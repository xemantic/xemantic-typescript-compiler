# Bench run 20260921T012629Z

- **xtsc revision**: [`b772ca34daf0`](https://github.com/xemantic/xemantic-typescript-compiler/commit/b772ca34daf0f53c05aab5e065b60c6d11a116a2)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push b772ca34daf0f53c05aab5e065b60c6d11a116a2

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b772ca34daf0 | **5.75s** | 33,884 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b772ca34daf0 | **5.33s** | 36,495 LOC/s | 46 |
| xtsc (cold JVM) | b772ca34daf0 | 24.36s | 7,993 LOC/s | 46 |
| tsc | 6.0.3 | 10.04s | 19,394 LOC/s | 65 |
| tsgo | 7.0.2 | 1.56s | 124,969 LOC/s | 65 |
| xtsc-native | b772ca34daf0 (AOT) | 9.41s | 20,686 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.75x FASTER than tsc; 3.69x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.33s**, **-7.2%** against `temurin26` on identical bytecode — 0.53× tsc, 3.42× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.43×** tsc and **15.63×** tsgo; tsc is **6.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.24s**.

Ahead-of-time (GraalVM native-image): **9.41s** — **0.94×** tsc, **6.04×** tsgo, **2.59×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b772ca34daf0 | **6.40s** | 30,422 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b772ca34daf0 | **5.82s** | 33,459 LOC/s | 46 |
| xtsc (cold JVM) | b772ca34daf0 | 29.52s | 6,595 LOC/s | 46 |
| tsc | 6.0.3 | 12.35s | 15,769 LOC/s | 65 |
| tsgo | 7.0.2 | 2.23s | 87,193 LOC/s | 65 |
| xtsc-native | b772ca34daf0 (AOT) | 10.27s | 18,952 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.93x FASTER than tsc; 2.87x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.82s**, **-9.1%** against `temurin26` on identical bytecode — 0.47× tsc, 2.61× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.39×** tsc and **13.22×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.39s**.

Ahead-of-time (GraalVM native-image): **10.27s** — **0.83×** tsc, **4.60×** tsgo, **2.87×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
