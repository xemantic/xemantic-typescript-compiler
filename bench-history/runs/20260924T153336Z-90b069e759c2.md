# Bench run 20260924T153336Z

- **xtsc revision**: [`90b069e759c2`](https://github.com/xemantic/xemantic-typescript-compiler/commit/90b069e759c2063e7311732cfc38e1debaef9b8f)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 90b069e759c2063e7311732cfc38e1debaef9b8f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 90b069e759c2 | **5.86s** | 33,236 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 90b069e759c2 | **5.50s** | 35,387 LOC/s | 46 |
| xtsc (cold JVM) | 90b069e759c2 | 29.60s | 6,577 LOC/s | 46 |
| tsc | 6.0.3 | 10.82s | 17,996 LOC/s | 65 |
| tsgo | 7.0.2 | 1.68s | 116,101 LOC/s | 65 |
| xtsc-native | 90b069e759c2 (AOT) | 9.48s | 20,542 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.85x FASTER than tsc; 3.49x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.50s**, **-6.1%** against `temurin26` on identical bytecode — 0.51× tsc, 3.28× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.74×** tsc and **17.65×** tsgo; tsc is **6.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.48s**.

Ahead-of-time (GraalVM native-image): **9.48s** — **0.88×** tsc, **5.65×** tsgo, **3.12×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 90b069e759c2 | **7.14s** | 27,257 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 90b069e759c2 | **6.07s** | 32,060 LOC/s | 46 |
| xtsc (cold JVM) | 90b069e759c2 | 32.95s | 5,909 LOC/s | 46 |
| tsc | 6.0.3 | 13.23s | 14,721 LOC/s | 65 |
| tsgo | 7.0.2 | 2.39s | 81,465 LOC/s | 65 |
| xtsc-native | 90b069e759c2 (AOT) | 10.88s | 17,888 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.85x FASTER than tsc; 2.99x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.07s**, **-15.0%** against `temurin26` on identical bytecode — 0.46× tsc, 2.54× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.49×** tsc and **13.79×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **32.82s**.

Ahead-of-time (GraalVM native-image): **10.88s** — **0.82×** tsc, **4.55×** tsgo, **3.03×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
