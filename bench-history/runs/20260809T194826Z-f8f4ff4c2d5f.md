# Bench run 20260809T194826Z

- **xtsc revision**: [`f8f4ff4c2d5f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f8f4ff4c2d5f5f9f96ac554a4cfdf6222269828f)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push f8f4ff4c2d5f5f9f96ac554a4cfdf6222269828f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | f8f4ff4c2d5f | 15.73s | 12,378 LOC/s | 46 |
| tsc | 6.0.3 | 6.86s | 28,386 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.04s | 186,674 LOC/s | 65 |
| xtsc-native | f8f4ff4c2d5f (AOT) | 6.40s | 30,412 LOC/s | 46 |

Relative (wall): xtsc is **2.29×** tsc and **15.08×** tsgo; tsc is **6.58×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **15.65s**.

Ahead-of-time (GraalVM native-image): **6.40s**, **2.46×** the JVM arm, **0.93×** tsc, **6.14×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | f8f4ff4c2d5f | 18.00s | 10,816 LOC/s | 46 |
| tsc | 6.0.3 | 8.16s | 23,866 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.56s | 124,889 LOC/s | 65 |
| xtsc-native | f8f4ff4c2d5f (AOT) | 8.33s | 23,379 LOC/s | 46 |

Relative (wall): xtsc is **2.21×** tsc and **11.55×** tsgo; tsc is **5.23×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.92s**.

Ahead-of-time (GraalVM native-image): **8.33s**, **2.16×** the JVM arm, **1.02×** tsc, **5.34×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
