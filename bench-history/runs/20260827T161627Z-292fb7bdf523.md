# Bench run 20260827T161627Z

- **xtsc revision**: [`292fb7bdf523`](https://github.com/xemantic/xemantic-typescript-compiler/commit/292fb7bdf523bab7e9f4e1a0bf45a2dbce80fcfe)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 292fb7bdf523bab7e9f4e1a0bf45a2dbce80fcfe

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 292fb7bdf523 | **2.61s** | 74,627 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 292fb7bdf523 | **2.38s** | 81,670 LOC/s | 46 |
| xtsc (cold JVM) | 292fb7bdf523 | 14.70s | 13,241 LOC/s | 46 |
| tsc | 6.0.3 | 6.66s | 29,243 LOC/s | 65 |
| tsgo | 7.0.2 | 1.03s | 188,299 LOC/s | 65 |
| xtsc-native | 292fb7bdf523 (AOT) | 3.87s | 50,284 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.55x FASTER than tsc; 2.52x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.38s**, **-8.6%** against `temurin26` on identical bytecode — 0.36× tsc, 2.31× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.21×** tsc and **14.22×** tsgo; tsc is **6.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **14.62s**.

Ahead-of-time (GraalVM native-image): **3.87s** — **0.58×** tsc, **3.74×** tsgo, **3.80×** the COLD JVM arm and **1.48×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 292fb7bdf523 | **3.22s** | 60,541 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 292fb7bdf523 | **3.07s** | 63,420 LOC/s | 46 |
| xtsc (cold JVM) | 292fb7bdf523 | 17.52s | 11,111 LOC/s | 46 |
| tsc | 6.0.3 | 8.32s | 23,390 LOC/s | 65 |
| tsgo | 7.0.2 | 1.61s | 120,857 LOC/s | 65 |
| xtsc-native | 292fb7bdf523 (AOT) | 4.99s | 39,049 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.59x FASTER than tsc; 2.00x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.07s**, **-4.5%** against `temurin26` on identical bytecode — 0.37× tsc, 1.91× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.11×** tsc and **10.88×** tsgo; tsc is **5.17×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.40s**.

Ahead-of-time (GraalVM native-image): **4.99s** — **0.60×** tsc, **3.09×** tsgo, **3.51×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
