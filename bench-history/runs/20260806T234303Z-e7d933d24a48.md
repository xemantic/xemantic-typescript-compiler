# Bench run 20260806T234303Z

- **xtsc revision**: [`e7d933d24a48`](https://github.com/xemantic/xemantic-typescript-compiler/commit/e7d933d24a48bffe9a022ed52884b6033e60c408)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push e7d933d24a48bffe9a022ed52884b6033e60c408

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | e7d933d24a48 | 29.55s | 6,588 LOC/s | 46 |
| tsc | 6.0.3 | 13.50s | 14,420 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.07s | 94,195 LOC/s | 65 |
| xtsc-native | e7d933d24a48 (AOT) | 14.38s | 13,544 LOC/s | 46 |

Relative (wall): xtsc is **2.19×** tsc and **14.30×** tsgo; tsc is **6.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.43s**.

Ahead-of-time (GraalVM native-image): **14.38s**, **2.06×** the JVM arm, **1.06×** tsc, **6.95×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | e7d933d24a48 | 32.67s | 5,959 LOC/s | 46 |
| tsc | 6.0.3 | 16.18s | 12,031 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.98s | 65,446 LOC/s | 65 |
| xtsc-native | e7d933d24a48 (AOT) | 16.08s | 12,110 LOC/s | 46 |

Relative (wall): xtsc is **2.02×** tsc and **10.98×** tsgo; tsc is **5.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **32.54s**.

Ahead-of-time (GraalVM native-image): **16.08s**, **2.03×** the JVM arm, **0.99×** tsc, **5.40×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
