# Bench run 20260824T112616Z

- **xtsc revision**: [`ab3063c554d2`](https://github.com/xemantic/xemantic-typescript-compiler/commit/ab3063c554d2f48d8b54405b0f2162fb3fc36839)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push ab3063c554d2f48d8b54405b0f2162fb3fc36839

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ab3063c554d2 | **3.70s** | 52,664 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ab3063c554d2 | **3.46s** | 56,288 LOC/s | 46 |
| xtsc (cold JVM) | ab3063c554d2 | 21.55s | 9,035 LOC/s | 46 |
| tsc | 6.0.3 | 10.43s | 18,660 LOC/s | 65 |
| tsgo | 7.0.2 | 1.65s | 118,001 LOC/s | 65 |
| xtsc-native | ab3063c554d2 (AOT) | 5.91s | 32,950 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.82x FASTER than tsc; 2.24x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.46s**, **-6.4%** against `temurin26` on identical bytecode — 0.33× tsc, 2.10× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.07×** tsc and **13.06×** tsgo; tsc is **6.32×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.45s**.

Ahead-of-time (GraalVM native-image): **5.91s** — **0.57×** tsc, **3.58×** tsgo, **3.65×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ab3063c554d2 | **4.50s** | 43,295 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ab3063c554d2 | **4.22s** | 46,116 LOC/s | 46 |
| xtsc (cold JVM) | ab3063c554d2 | 23.38s | 8,328 LOC/s | 46 |
| tsc | 6.0.3 | 12.35s | 15,770 LOC/s | 65 |
| tsgo | 7.0.2 | 2.37s | 82,222 LOC/s | 65 |
| xtsc-native | ab3063c554d2 (AOT) | 6.86s | 28,390 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.75x FASTER than tsc; 1.90x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.22s**, **-6.1%** against `temurin26` on identical bytecode — 0.34× tsc, 1.78× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.89×** tsc and **9.87×** tsgo; tsc is **5.21×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.26s**.

Ahead-of-time (GraalVM native-image): **6.86s** — **0.56×** tsc, **2.90×** tsgo, **3.41×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
