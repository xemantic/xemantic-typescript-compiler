# Bench run 20260916T170825Z

- **xtsc revision**: [`c60da597b155`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c60da597b155dde279df1c3a172e6c44a42a3474)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push c60da597b155dde279df1c3a172e6c44a42a3474

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c60da597b155 | **3.23s** | 60,223 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c60da597b155 | **3.17s** | 61,400 LOC/s | 46 |
| xtsc (cold JVM) | c60da597b155 | 16.74s | 11,630 LOC/s | 46 |
| tsc | 6.0.3 | 6.37s | 30,560 LOC/s | 65 |
| tsgo | 7.0.2 | 0.96s | 202,182 LOC/s | 65 |
| xtsc-native | c60da597b155 (AOT) | 5.96s | 32,690 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.97x FASTER than tsc; 3.36x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.17s**, **-1.9%** against `temurin26` on identical bytecode — 0.50× tsc, 3.29× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.63×** tsc and **17.38×** tsgo; tsc is **6.62×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **16.66s**.

Ahead-of-time (GraalVM native-image): **5.96s** — **0.93×** tsc, **6.18×** tsgo, **2.81×** the COLD JVM arm and **1.84×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c60da597b155 | **4.06s** | 47,979 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c60da597b155 | **3.63s** | 53,651 LOC/s | 46 |
| xtsc (cold JVM) | c60da597b155 | 23.61s | 8,247 LOC/s | 46 |
| tsc | 6.0.3 | 7.77s | 25,048 LOC/s | 65 |
| tsgo | 7.0.2 | 1.45s | 133,907 LOC/s | 65 |
| xtsc-native | c60da597b155 (AOT) | 6.82s | 28,557 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.92x FASTER than tsc; 2.79x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.63s**, **-10.6%** against `temurin26` on identical bytecode — 0.47× tsc, 2.50× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.04×** tsc and **16.24×** tsgo; tsc is **5.35×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.51s**.

Ahead-of-time (GraalVM native-image): **6.82s** — **0.88×** tsc, **4.69×** tsgo, **3.46×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
