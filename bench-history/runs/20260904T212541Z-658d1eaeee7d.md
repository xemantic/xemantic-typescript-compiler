# Bench run 20260904T212541Z

- **xtsc revision**: [`658d1eaeee7d`](https://github.com/xemantic/xemantic-typescript-compiler/commit/658d1eaeee7df7c6749d2288e2bc6d21dd5099ab)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 658d1eaeee7df7c6749d2288e2bc6d21dd5099ab

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 658d1eaeee7d | **3.41s** | 57,030 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 658d1eaeee7d | **3.30s** | 59,000 LOC/s | 46 |
| xtsc (cold JVM) | 658d1eaeee7d | 18.77s | 10,370 LOC/s | 46 |
| tsc | 6.0.3 | 8.22s | 23,680 LOC/s | 65 |
| tsgo | 7.0.2 | 1.24s | 156,638 LOC/s | 65 |
| xtsc-native | 658d1eaeee7d (AOT) | 5.79s | 33,598 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.41x FASTER than tsc; 2.75x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.30s**, **-3.3%** against `temurin26` on identical bytecode — 0.40× tsc, 2.65× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.28×** tsc and **15.10×** tsgo; tsc is **6.61×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.68s**.

Ahead-of-time (GraalVM native-image): **5.79s** — **0.70×** tsc, **4.66×** tsgo, **3.24×** the COLD JVM arm and **1.70×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 658d1eaeee7d | **4.12s** | 47,292 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 658d1eaeee7d | **3.69s** | 52,707 LOC/s | 46 |
| xtsc (cold JVM) | 658d1eaeee7d | 18.97s | 10,264 LOC/s | 46 |
| tsc | 6.0.3 | 9.95s | 19,566 LOC/s | 65 |
| tsgo | 7.0.2 | 1.81s | 107,748 LOC/s | 65 |
| xtsc-native | 658d1eaeee7d (AOT) | 6.64s | 29,335 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.42x FASTER than tsc; 2.28x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.69s**, **-10.3%** against `temurin26` on identical bytecode — 0.37× tsc, 2.04× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.91×** tsc and **10.50×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.87s**.

Ahead-of-time (GraalVM native-image): **6.64s** — **0.67×** tsc, **3.67×** tsgo, **2.86×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
