# Bench run 20260807T231002Z

- **xtsc revision**: [`eb42b853f02e`](https://github.com/xemantic/xemantic-typescript-compiler/commit/eb42b853f02e07d717fc4966ff8ea88de89637d2)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push eb42b853f02e07d717fc4966ff8ea88de89637d2

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | eb42b853f02e | 25.02s | 7,780 LOC/s | 46 |
| tsc | 6.0.3 | 10.10s | 19,283 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.63s | 119,742 LOC/s | 65 |
| xtsc-native | eb42b853f02e (AOT) | 10.18s | 19,135 LOC/s | 46 |

Relative (wall): xtsc is **2.48×** tsc and **15.39×** tsgo; tsc is **6.21×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.93s**.

Ahead-of-time (GraalVM native-image): **10.18s**, **2.46×** the JVM arm, **1.01×** tsc, **6.26×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | eb42b853f02e | 25.98s | 7,494 LOC/s | 46 |
| tsc | 6.0.3 | 12.43s | 15,661 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.38s | 81,841 LOC/s | 65 |
| xtsc-native | eb42b853f02e (AOT) | 12.07s | 16,128 LOC/s | 46 |

Relative (wall): xtsc is **2.09×** tsc and **10.92×** tsgo; tsc is **5.23×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.88s**.

Ahead-of-time (GraalVM native-image): **12.07s**, **2.15×** the JVM arm, **0.97×** tsc, **5.07×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
