# Bench run 20260905T051229Z

- **xtsc revision**: [`172ba3c43829`](https://github.com/xemantic/xemantic-typescript-compiler/commit/172ba3c43829e99ee0d6262cdad18e96adee65d0)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 172ba3c43829e99ee0d6262cdad18e96adee65d0

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 172ba3c43829 | **4.18s** | 46,523 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 172ba3c43829 | **3.99s** | 48,834 LOC/s | 46 |
| xtsc (cold JVM) | 172ba3c43829 | 21.92s | 8,880 LOC/s | 46 |
| tsc | 6.0.3 | 10.56s | 18,441 LOC/s | 65 |
| tsgo | 7.0.2 | 1.68s | 115,756 LOC/s | 65 |
| xtsc-native | 172ba3c43829 (AOT) | 6.29s | 30,968 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.52x FASTER than tsc; 2.49x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.99s**, **-4.7%** against `temurin26` on identical bytecode — 0.38× tsc, 2.37× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.08×** tsc and **13.03×** tsgo; tsc is **6.28×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.78s**.

Ahead-of-time (GraalVM native-image): **6.29s** — **0.60×** tsc, **3.74×** tsgo, **3.49×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 172ba3c43829 | **5.25s** | 37,086 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 172ba3c43829 | **4.67s** | 41,665 LOC/s | 46 |
| xtsc (cold JVM) | 172ba3c43829 | 25.69s | 7,578 LOC/s | 46 |
| tsc | 6.0.3 | 13.23s | 14,713 LOC/s | 65 |
| tsgo | 7.0.2 | 2.37s | 82,118 LOC/s | 65 |
| xtsc-native | 172ba3c43829 (AOT) | 8.24s | 23,617 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.52x FASTER than tsc; 2.21x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.67s**, **-11.0%** against `temurin26` on identical bytecode — 0.35× tsc, 1.97× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.94×** tsc and **10.84×** tsgo; tsc is **5.58×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.56s**.

Ahead-of-time (GraalVM native-image): **8.24s** — **0.62×** tsc, **3.48×** tsgo, **3.12×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
