# Bench run 20260909T191927Z

- **xtsc revision**: [`d29d2197017b`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d29d2197017b7bf068fd72d1505ef43058e80f26)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push d29d2197017b7bf068fd72d1505ef43058e80f26

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d29d2197017b | **2.92s** | 66,564 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d29d2197017b | **2.91s** | 66,976 LOC/s | 46 |
| xtsc (cold JVM) | d29d2197017b | 16.95s | 11,486 LOC/s | 46 |
| tsc | 6.0.3 | 5.94s | 32,800 LOC/s | 65 |
| tsgo | 7.0.2 | 0.87s | 222,771 LOC/s | 65 |
| xtsc-native | d29d2197017b (AOT) | 4.94s | 39,429 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.03x FASTER than tsc; 3.35x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.91s**, **-0.6%** against `temurin26` on identical bytecode — 0.49× tsc, 3.33× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.86×** tsc and **19.39×** tsgo; tsc is **6.79×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **16.86s**.

Ahead-of-time (GraalVM native-image): **4.94s** — **0.83×** tsc, **5.65×** tsgo, **3.43×** the COLD JVM arm and **1.69×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d29d2197017b | **3.58s** | 54,310 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d29d2197017b | **3.17s** | 61,497 LOC/s | 46 |
| xtsc (cold JVM) | d29d2197017b | 16.20s | 12,017 LOC/s | 46 |
| tsc | 6.0.3 | 7.39s | 26,346 LOC/s | 65 |
| tsgo | 7.0.2 | 1.38s | 140,782 LOC/s | 65 |
| xtsc-native | d29d2197017b (AOT) | 6.24s | 31,202 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.06x FASTER than tsc; 2.59x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.17s**, **-11.7%** against `temurin26` on identical bytecode — 0.43× tsc, 2.29× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.19×** tsc and **11.72×** tsgo; tsc is **5.34×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **16.11s**.

Ahead-of-time (GraalVM native-image): **6.24s** — **0.84×** tsc, **4.51×** tsgo, **2.60×** the COLD JVM arm and **1.74×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
