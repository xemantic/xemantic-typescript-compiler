# Bench run 20260914T174131Z

- **xtsc revision**: [`0db1e2f58a7a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/0db1e2f58a7ac4029cc8e73c0fc97f2b5ccd07c5)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 0db1e2f58a7ac4029cc8e73c0fc97f2b5ccd07c5

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0db1e2f58a7a | **3.77s** | 51,604 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0db1e2f58a7a | **3.56s** | 54,753 LOC/s | 46 |
| xtsc (cold JVM) | 0db1e2f58a7a | 17.01s | 11,448 LOC/s | 46 |
| tsc | 6.0.3 | 6.94s | 28,042 LOC/s | 65 |
| tsgo | 7.0.2 | 1.10s | 176,680 LOC/s | 65 |
| xtsc-native | 0db1e2f58a7a (AOT) | 6.07s | 32,086 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.84x FASTER than tsc; 3.42x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.56s**, **-5.8%** against `temurin26` on identical bytecode — 0.51× tsc, 3.23× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.45×** tsc and **15.43×** tsgo; tsc is **6.30×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **16.92s**.

Ahead-of-time (GraalVM native-image): **6.07s** — **0.87×** tsc, **5.51×** tsgo, **2.80×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0db1e2f58a7a | **4.41s** | 44,140 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0db1e2f58a7a | **4.46s** | 43,694 LOC/s | 46 |
| xtsc (cold JVM) | 0db1e2f58a7a | 20.82s | 9,351 LOC/s | 46 |
| tsc | 6.0.3 | 8.81s | 22,107 LOC/s | 65 |
| tsgo | 7.0.2 | 1.71s | 113,595 LOC/s | 65 |
| xtsc-native | 0db1e2f58a7a (AOT) | 7.04s | 27,672 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.00x FASTER than tsc; 2.57x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.46s**, **+1.0%** against `temurin26` on identical bytecode — 0.51× tsc, 2.60× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.36×** tsc and **12.15×** tsgo; tsc is **5.14×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.71s**.

Ahead-of-time (GraalVM native-image): **7.04s** — **0.80×** tsc, **4.11×** tsgo, **2.96×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
