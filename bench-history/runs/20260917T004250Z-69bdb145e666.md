# Bench run 20260917T004250Z

- **xtsc revision**: [`69bdb145e666`](https://github.com/xemantic/xemantic-typescript-compiler/commit/69bdb145e6666dd1a73a385e630b0090285c40fd)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 69bdb145e6666dd1a73a385e630b0090285c40fd

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 69bdb145e666 | **4.45s** | 43,782 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 69bdb145e666 | **4.23s** | 46,017 LOC/s | 46 |
| xtsc (cold JVM) | 69bdb145e666 | 19.90s | 9,783 LOC/s | 46 |
| tsc | 6.0.3 | 8.10s | 24,049 LOC/s | 65 |
| tsgo | 7.0.2 | 1.22s | 159,853 LOC/s | 65 |
| xtsc-native | 69bdb145e666 (AOT) | 7.46s | 26,099 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.82x FASTER than tsc; 3.65x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.23s**, **-4.9%** against `temurin26` on identical bytecode — 0.52× tsc, 3.47× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.46×** tsc and **16.34×** tsgo; tsc is **6.65×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.81s**.

Ahead-of-time (GraalVM native-image): **7.46s** — **0.92×** tsc, **6.12×** tsgo, **2.67×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 69bdb145e666 | **5.35s** | 36,399 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 69bdb145e666 | **4.78s** | 40,715 LOC/s | 46 |
| xtsc (cold JVM) | 69bdb145e666 | 29.25s | 6,656 LOC/s | 46 |
| tsc | 6.0.3 | 9.81s | 19,847 LOC/s | 65 |
| tsgo | 7.0.2 | 1.77s | 109,815 LOC/s | 65 |
| xtsc-native | 69bdb145e666 (AOT) | 8.36s | 23,295 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.83x FASTER than tsc; 3.02x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.78s**, **-10.6%** against `temurin26` on identical bytecode — 0.49× tsc, 2.70× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.98×** tsc and **16.50×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.13s**.

Ahead-of-time (GraalVM native-image): **8.36s** — **0.85×** tsc, **4.71×** tsgo, **3.50×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
