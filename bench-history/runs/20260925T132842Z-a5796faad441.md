# Bench run 20260925T132842Z

- **xtsc revision**: [`a5796faad441`](https://github.com/xemantic/xemantic-typescript-compiler/commit/a5796faad441e3fcac855b6dbd8edbf49b2114c4)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push a5796faad441e3fcac855b6dbd8edbf49b2114c4

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a5796faad441 | **4.98s** | 39,120 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a5796faad441 | **4.83s** | 40,310 LOC/s | 46 |
| xtsc (cold JVM) | a5796faad441 | 21.89s | 8,896 LOC/s | 46 |
| tsc | 6.0.3 | 8.93s | 21,798 LOC/s | 65 |
| tsgo | 7.0.2 | 1.31s | 148,287 LOC/s | 65 |
| xtsc-native | a5796faad441 (AOT) | 8.69s | 22,407 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.79x FASTER than tsc; 3.79x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.83s**, **-3.0%** against `temurin26` on identical bytecode — 0.54× tsc, 3.68× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.45×** tsc and **16.67×** tsgo; tsc is **6.80×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.77s**.

Ahead-of-time (GraalVM native-image): **8.69s** — **0.97×** tsc, **6.62×** tsgo, **2.52×** the COLD JVM arm and **1.75×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a5796faad441 | **5.73s** | 33,991 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a5796faad441 | **5.49s** | 35,477 LOC/s | 46 |
| xtsc (cold JVM) | a5796faad441 | 24.41s | 7,975 LOC/s | 46 |
| tsc | 6.0.3 | 10.96s | 17,766 LOC/s | 65 |
| tsgo | 7.0.2 | 2.00s | 97,156 LOC/s | 65 |
| xtsc-native | a5796faad441 (AOT) | 10.09s | 19,302 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.91x FASTER than tsc; 2.86x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.49s**, **-4.2%** against `temurin26` on identical bytecode — 0.50× tsc, 2.74× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.23×** tsc and **12.18×** tsgo; tsc is **5.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.29s**.

Ahead-of-time (GraalVM native-image): **10.09s** — **0.92×** tsc, **5.03×** tsgo, **2.42×** the COLD JVM arm and **1.76×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
