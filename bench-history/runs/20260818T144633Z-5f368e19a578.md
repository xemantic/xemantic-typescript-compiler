# Bench run 20260818T144633Z

- **xtsc revision**: [`5f368e19a578`](https://github.com/xemantic/xemantic-typescript-compiler/commit/5f368e19a57819fd7a1e9743a139f849f5a87e13)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 5f368e19a57819fd7a1e9743a139f849f5a87e13

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5f368e19a578 | **4.06s** | 47,908 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5f368e19a578 | **3.68s** | 52,908 LOC/s | 46 |
| xtsc (cold JVM) | 5f368e19a578 | 23.20s | 8,392 LOC/s | 46 |
| tsc | 6.0.3 | 10.71s | 18,174 LOC/s | 65 |
| tsgo | 7.0.2 | 1.66s | 117,573 LOC/s | 65 |
| xtsc-native | 5f368e19a578 (AOT) | 5.86s | 33,236 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.64x FASTER than tsc; 2.45x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.68s**, **-9.4%** against `temurin26` on identical bytecode — 0.34× tsc, 2.22× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.17×** tsc and **14.01×** tsgo; tsc is **6.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.09s**.

Ahead-of-time (GraalVM native-image): **5.86s** — **0.55×** tsc, **3.54×** tsgo, **3.96×** the COLD JVM arm and **1.44×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5f368e19a578 | **4.88s** | 39,914 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5f368e19a578 | **4.28s** | 45,459 LOC/s | 46 |
| xtsc (cold JVM) | 5f368e19a578 | 25.00s | 7,789 LOC/s | 46 |
| tsc | 6.0.3 | 13.13s | 14,831 LOC/s | 65 |
| tsgo | 7.0.2 | 2.37s | 82,326 LOC/s | 65 |
| xtsc-native | 5f368e19a578 (AOT) | 6.92s | 28,152 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.69x FASTER than tsc; 2.06x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.28s**, **-12.2%** against `temurin26` on identical bytecode — 0.33× tsc, 1.81× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.90×** tsc and **10.57×** tsgo; tsc is **5.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.88s**.

Ahead-of-time (GraalVM native-image): **6.92s** — **0.53×** tsc, **2.92×** tsgo, **3.61×** the COLD JVM arm and **1.42×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
