# Bench run 20260815T024608Z

- **xtsc revision**: [`f151ce11dac3`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f151ce11dac31036f664580ad8a32627779984ed)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push f151ce11dac31036f664580ad8a32627779984ed

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f151ce11dac3 | **4.41s** | 44,120 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f151ce11dac3 | **3.78s** | 51,467 LOC/s | 46 |
| xtsc (cold JVM) | f151ce11dac3 | 24.50s | 7,948 LOC/s | 46 |
| tsc | 6.0.3 | 10.94s | 17,802 LOC/s | 65 |
| tsgo | 7.0.2 | 1.73s | 112,414 LOC/s | 65 |
| xtsc-native | f151ce11dac3 (AOT) | 6.46s | 30,125 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.48x FASTER than tsc; 2.55x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.78s**, **-14.3%** against `temurin26` on identical bytecode — 0.35× tsc, 2.18× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.24×** tsc and **14.14×** tsgo; tsc is **6.31×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.38s**.

Ahead-of-time (GraalVM native-image): **6.46s** — **0.59×** tsc, **3.73×** tsgo, **3.79×** the COLD JVM arm and **1.46×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f151ce11dac3 | **4.86s** | 40,086 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f151ce11dac3 | **4.76s** | 40,869 LOC/s | 46 |
| xtsc (cold JVM) | f151ce11dac3 | 27.07s | 7,193 LOC/s | 46 |
| tsc | 6.0.3 | 13.58s | 14,333 LOC/s | 65 |
| tsgo | 7.0.2 | 2.42s | 80,355 LOC/s | 65 |
| xtsc-native | f151ce11dac3 (AOT) | 7.80s | 24,945 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.80x FASTER than tsc; 2.00x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.76s**, **-1.9%** against `temurin26` on identical bytecode — 0.35× tsc, 1.97× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.99×** tsc and **11.17×** tsgo; tsc is **5.61×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.94s**.

Ahead-of-time (GraalVM native-image): **7.80s** — **0.57×** tsc, **3.22×** tsgo, **3.47×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
