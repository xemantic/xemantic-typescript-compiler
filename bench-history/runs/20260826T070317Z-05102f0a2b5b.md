# Bench run 20260826T070317Z

- **xtsc revision**: [`05102f0a2b5b`](https://github.com/xemantic/xemantic-typescript-compiler/commit/05102f0a2b5b721b758231039396fe745270026a)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 05102f0a2b5b721b758231039396fe745270026a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 05102f0a2b5b | **4.34s** | 44,903 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 05102f0a2b5b | **4.12s** | 47,234 LOC/s | 46 |
| xtsc (cold JVM) | 05102f0a2b5b | 22.45s | 8,671 LOC/s | 46 |
| tsc | 6.0.3 | 11.38s | 17,101 LOC/s | 65 |
| tsgo | 7.0.2 | 1.82s | 107,214 LOC/s | 65 |
| xtsc-native | 05102f0a2b5b (AOT) | 6.67s | 29,199 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.63x FASTER than tsc; 2.39x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.12s**, **-4.9%** against `temurin26` on identical bytecode — 0.36× tsc, 2.27× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.97×** tsc and **12.36×** tsgo; tsc is **6.27×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.33s**.

Ahead-of-time (GraalVM native-image): **6.67s** — **0.59×** tsc, **3.67×** tsgo, **3.37×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 05102f0a2b5b | **5.03s** | 38,746 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 05102f0a2b5b | **5.02s** | 38,785 LOC/s | 46 |
| xtsc (cold JVM) | 05102f0a2b5b | 27.33s | 7,123 LOC/s | 46 |
| tsc | 6.0.3 | 13.92s | 13,990 LOC/s | 65 |
| tsgo | 7.0.2 | 2.52s | 77,109 LOC/s | 65 |
| xtsc-native | 05102f0a2b5b (AOT) | 8.38s | 23,225 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.77x FASTER than tsc; 1.99x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.02s**, **-0.1%** against `temurin26` on identical bytecode — 0.36× tsc, 1.99× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.96×** tsc and **10.82×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.21s**.

Ahead-of-time (GraalVM native-image): **8.38s** — **0.60×** tsc, **3.32×** tsgo, **3.26×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
