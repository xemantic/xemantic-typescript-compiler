# Bench run 20260826T052405Z

- **xtsc revision**: [`f1bdbc25646e`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f1bdbc25646eab96d01bea12b744704cbb9d9d12)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push f1bdbc25646eab96d01bea12b744704cbb9d9d12

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f1bdbc25646e | **2.50s** | 77,974 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f1bdbc25646e | **2.49s** | 78,130 LOC/s | 46 |
| xtsc (cold JVM) | f1bdbc25646e | 16.61s | 11,722 LOC/s | 46 |
| tsc | 6.0.3 | 7.32s | 26,605 LOC/s | 65 |
| tsgo | 7.0.2 | 1.22s | 159,200 LOC/s | 65 |
| xtsc-native | f1bdbc25646e (AOT) | 4.24s | 45,974 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.93x FASTER than tsc; 2.04x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.49s**, **-0.2%** against `temurin26` on identical bytecode — 0.34× tsc, 2.04× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.27×** tsc and **13.58×** tsgo; tsc is **5.98×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **16.52s**.

Ahead-of-time (GraalVM native-image): **4.24s** — **0.58×** tsc, **3.46×** tsgo, **3.92×** the COLD JVM arm and **1.70×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f1bdbc25646e | **3.38s** | 57,621 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f1bdbc25646e | **3.04s** | 64,131 LOC/s | 46 |
| xtsc (cold JVM) | f1bdbc25646e | 18.85s | 10,329 LOC/s | 46 |
| tsc | 6.0.3 | 8.62s | 22,581 LOC/s | 65 |
| tsgo | 7.0.2 | 1.64s | 119,011 LOC/s | 65 |
| xtsc-native | f1bdbc25646e (AOT) | 5.32s | 36,632 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.55x FASTER than tsc; 2.07x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.04s**, **-10.2%** against `temurin26` on identical bytecode — 0.35× tsc, 1.86× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.19×** tsc and **11.52×** tsgo; tsc is **5.27×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.63s**.

Ahead-of-time (GraalVM native-image): **5.32s** — **0.62×** tsc, **3.25×** tsgo, **3.55×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
