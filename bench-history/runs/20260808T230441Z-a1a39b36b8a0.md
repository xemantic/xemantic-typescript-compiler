# Bench run 20260808T230441Z

- **xtsc revision**: [`a1a39b36b8a0`](https://github.com/xemantic/xemantic-typescript-compiler/commit/a1a39b36b8a04f63bb51c28d14030b7c9813a5c3)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push a1a39b36b8a04f63bb51c28d14030b7c9813a5c3

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | a1a39b36b8a0 | 24.85s | 7,835 LOC/s | 46 |
| tsc | 6.0.3 | 10.13s | 19,220 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.56s | 124,489 LOC/s | 65 |
| xtsc-native | a1a39b36b8a0 (AOT) | 11.11s | 17,529 LOC/s | 46 |

Relative (wall): xtsc is **2.45×** tsc and **15.89×** tsgo; tsc is **6.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.74s**.

Ahead-of-time (GraalVM native-image): **11.11s**, **2.24×** the JVM arm, **1.10×** tsc, **7.10×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | a1a39b36b8a0 | 26.88s | 7,243 LOC/s | 46 |
| tsc | 6.0.3 | 12.37s | 15,739 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.27s | 85,583 LOC/s | 65 |
| xtsc-native | a1a39b36b8a0 (AOT) | 13.26s | 14,686 LOC/s | 46 |

Relative (wall): xtsc is **2.17×** tsc and **11.82×** tsgo; tsc is **5.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.77s**.

Ahead-of-time (GraalVM native-image): **13.26s**, **2.03×** the JVM arm, **1.07×** tsc, **5.83×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
