# Bench run 20260801T044839Z

- **xtsc revision**: [`dcc610939908`](https://github.com/xemantic/xemantic-typescript-compiler/commit/dcc6109399089a043853adffb50559faa390a617)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push dcc6109399089a043853adffb50559faa390a617

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | dcc610939908 | 31.56s | 6,169 LOC/s | 46 |
| tsc | 6.0.3 | 11.00s | 17,701 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.73s | 112,479 LOC/s | 65 |
| xtsc-native | dcc610939908 (AOT) | 13.13s | 14,833 LOC/s | 46 |

Relative (wall): xtsc is **2.87×** tsc and **18.23×** tsgo; tsc is **6.35×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **31.44s**.

Ahead-of-time (GraalVM native-image): **13.13s**, **2.40×** the JVM arm, **1.19×** tsc, **7.58×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | dcc610939908 | 34.35s | 5,667 LOC/s | 46 |
| tsc | 6.0.3 | 13.66s | 14,253 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.48s | 78,508 LOC/s | 65 |
| xtsc-native | dcc610939908 (AOT) | 13.75s | 14,156 LOC/s | 46 |

Relative (wall): xtsc is **2.51×** tsc and **13.85×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **34.25s**.

Ahead-of-time (GraalVM native-image): **13.75s**, **2.50×** the JVM arm, **1.01×** tsc, **5.55×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
