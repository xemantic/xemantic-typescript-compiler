# Bench run 20260916T130255Z

- **xtsc revision**: [`a712d160ee11`](https://github.com/xemantic/xemantic-typescript-compiler/commit/a712d160ee110fd39a9e294f74b74ae0d6cdce52)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push a712d160ee110fd39a9e294f74b74ae0d6cdce52

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a712d160ee11 | **5.87s** | 33,163 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a712d160ee11 | **5.17s** | 37,630 LOC/s | 46 |
| xtsc (cold JVM) | a712d160ee11 | 25.08s | 7,762 LOC/s | 46 |
| tsc | 6.0.3 | 10.67s | 18,242 LOC/s | 65 |
| tsgo | 7.0.2 | 1.66s | 117,502 LOC/s | 65 |
| xtsc-native | a712d160ee11 (AOT) | 9.07s | 21,464 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.82x FASTER than tsc; 3.54x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.17s**, **-11.9%** against `temurin26` on identical bytecode — 0.48× tsc, 3.12× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.35×** tsc and **15.14×** tsgo; tsc is **6.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.97s**.

Ahead-of-time (GraalVM native-image): **9.07s** — **0.85×** tsc, **5.47×** tsgo, **2.77×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a712d160ee11 | **6.66s** | 29,234 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a712d160ee11 | **5.88s** | 33,095 LOC/s | 46 |
| xtsc (cold JVM) | a712d160ee11 | 29.13s | 6,683 LOC/s | 46 |
| tsc | 6.0.3 | 13.17s | 14,784 LOC/s | 65 |
| tsgo | 7.0.2 | 2.40s | 80,990 LOC/s | 65 |
| xtsc-native | a712d160ee11 (AOT) | 10.54s | 18,472 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.98x FASTER than tsc; 2.77x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.88s**, **-11.7%** against `temurin26` on identical bytecode — 0.45× tsc, 2.45× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.21×** tsc and **12.12×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.00s**.

Ahead-of-time (GraalVM native-image): **10.54s** — **0.80×** tsc, **4.38×** tsgo, **2.76×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
