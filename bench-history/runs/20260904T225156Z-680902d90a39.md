# Bench run 20260904T225156Z

- **xtsc revision**: [`680902d90a39`](https://github.com/xemantic/xemantic-typescript-compiler/commit/680902d90a3960d6f46fe64b159d7d03f0895705)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 680902d90a3960d6f46fe64b159d7d03f0895705

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 680902d90a39 | **4.22s** | 46,159 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 680902d90a39 | **4.03s** | 48,361 LOC/s | 46 |
| xtsc (cold JVM) | 680902d90a39 | 23.37s | 8,331 LOC/s | 46 |
| tsc | 6.0.3 | 10.29s | 18,921 LOC/s | 65 |
| tsgo | 7.0.2 | 1.56s | 124,489 LOC/s | 65 |
| xtsc-native | 680902d90a39 (AOT) | 7.00s | 27,830 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.44x FASTER than tsc; 2.70x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.03s**, **-4.6%** against `temurin26` on identical bytecode — 0.39× tsc, 2.57× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.27×** tsc and **14.94×** tsgo; tsc is **6.58×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.25s**.

Ahead-of-time (GraalVM native-image): **7.00s** — **0.68×** tsc, **4.47×** tsgo, **3.34×** the COLD JVM arm and **1.66×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 680902d90a39 | **5.19s** | 37,522 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 680902d90a39 | **4.77s** | 40,826 LOC/s | 46 |
| xtsc (cold JVM) | 680902d90a39 | 26.93s | 7,231 LOC/s | 46 |
| tsc | 6.0.3 | 12.51s | 15,561 LOC/s | 65 |
| tsgo | 7.0.2 | 2.28s | 85,246 LOC/s | 65 |
| xtsc-native | 680902d90a39 (AOT) | 8.22s | 23,697 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.41x FASTER than tsc; 2.27x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.77s**, **-8.1%** against `temurin26` on identical bytecode — 0.38× tsc, 2.09× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.15×** tsc and **11.79×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.82s**.

Ahead-of-time (GraalVM native-image): **8.22s** — **0.66×** tsc, **3.60×** tsgo, **3.28×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
