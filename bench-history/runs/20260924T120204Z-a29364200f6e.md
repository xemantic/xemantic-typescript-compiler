# Bench run 20260924T120204Z

- **xtsc revision**: [`a29364200f6e`](https://github.com/xemantic/xemantic-typescript-compiler/commit/a29364200f6ebed8f53c1c22bf4cbd97c248974c)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push a29364200f6ebed8f53c1c22bf4cbd97c248974c

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a29364200f6e | **5.79s** | 33,598 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a29364200f6e | **5.39s** | 36,149 LOC/s | 46 |
| xtsc (cold JVM) | a29364200f6e | 28.49s | 6,833 LOC/s | 46 |
| tsc | 6.0.3 | 10.86s | 17,926 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,072 LOC/s | 65 |
| xtsc-native | a29364200f6e (AOT) | 9.29s | 20,960 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.87x FASTER than tsc; 3.42x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.39s**, **-7.1%** against `temurin26` on identical bytecode — 0.50× tsc, 3.18× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.62×** tsc and **16.84×** tsgo; tsc is **6.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.37s**.

Ahead-of-time (GraalVM native-image): **9.29s** — **0.86×** tsc, **5.49×** tsgo, **3.07×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a29364200f6e | **6.80s** | 28,649 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a29364200f6e | **6.08s** | 32,002 LOC/s | 46 |
| xtsc (cold JVM) | a29364200f6e | 30.57s | 6,368 LOC/s | 46 |
| tsc | 6.0.3 | 13.20s | 14,754 LOC/s | 65 |
| tsgo | 7.0.2 | 2.39s | 81,397 LOC/s | 65 |
| xtsc-native | a29364200f6e (AOT) | 10.58s | 18,406 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.94x FASTER than tsc; 2.84x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.08s**, **-10.5%** against `temurin26` on identical bytecode — 0.46× tsc, 2.54× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.32×** tsc and **12.78×** tsgo; tsc is **5.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.44s**.

Ahead-of-time (GraalVM native-image): **10.58s** — **0.80×** tsc, **4.42×** tsgo, **2.89×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
