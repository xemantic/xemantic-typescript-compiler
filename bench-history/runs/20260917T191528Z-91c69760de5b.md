# Bench run 20260917T191528Z

- **xtsc revision**: [`91c69760de5b`](https://github.com/xemantic/xemantic-typescript-compiler/commit/91c69760de5bd6928cc2eb3151cbebcf777f3307)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 91c69760de5bd6928cc2eb3151cbebcf777f3307

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 91c69760de5b | **3.18s** | 61,207 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 91c69760de5b | **3.10s** | 62,888 LOC/s | 46 |
| xtsc (cold JVM) | 91c69760de5b | 16.76s | 11,615 LOC/s | 46 |
| tsc | 6.0.3 | 6.07s | 32,091 LOC/s | 65 |
| tsgo | 7.0.2 | 0.91s | 213,255 LOC/s | 65 |
| xtsc-native | 91c69760de5b (AOT) | 5.52s | 35,291 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.91x FASTER than tsc; 3.48x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.10s**, **-2.7%** against `temurin26` on identical bytecode — 0.51× tsc, 3.39× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.76×** tsc and **18.36×** tsgo; tsc is **6.65×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **16.67s**.

Ahead-of-time (GraalVM native-image): **5.52s** — **0.91×** tsc, **6.04×** tsgo, **3.04×** the COLD JVM arm and **1.73×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 91c69760de5b | **4.56s** | 42,716 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 91c69760de5b | **4.27s** | 45,586 LOC/s | 46 |
| xtsc (cold JVM) | 91c69760de5b | 17.77s | 10,959 LOC/s | 46 |
| tsc | 6.0.3 | 8.96s | 21,737 LOC/s | 65 |
| tsgo | 7.0.2 | 1.68s | 115,756 LOC/s | 65 |
| xtsc-native | 91c69760de5b (AOT) | 7.96s | 24,450 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.97x FASTER than tsc; 2.71x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.27s**, **-6.3%** against `temurin26` on identical bytecode — 0.48× tsc, 2.54× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.98×** tsc and **10.56×** tsgo; tsc is **5.33×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.68s**.

Ahead-of-time (GraalVM native-image): **7.96s** — **0.89×** tsc, **4.73×** tsgo, **2.23×** the COLD JVM arm and **1.75×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
