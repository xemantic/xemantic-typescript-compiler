# Bench run 20260902T104526Z

- **xtsc revision**: [`ea566b238648`](https://github.com/xemantic/xemantic-typescript-compiler/commit/ea566b2386480c4bf079dcb3ecff29eca548b0a9)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push ea566b2386480c4bf079dcb3ecff29eca548b0a9

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ea566b238648 | **4.12s** | 47,280 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ea566b238648 | **3.94s** | 49,404 LOC/s | 46 |
| xtsc (cold JVM) | ea566b238648 | 23.55s | 8,265 LOC/s | 46 |
| tsc | 6.0.3 | 10.60s | 18,371 LOC/s | 65 |
| tsgo | 7.0.2 | 1.59s | 122,454 LOC/s | 65 |
| xtsc-native | ea566b238648 (AOT) | 7.30s | 26,660 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.57x FASTER than tsc; 2.59x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.94s**, **-4.3%** against `temurin26` on identical bytecode — 0.37× tsc, 2.48× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.22×** tsc and **14.81×** tsgo; tsc is **6.67×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.45s**.

Ahead-of-time (GraalVM native-image): **7.30s** — **0.69×** tsc, **4.59×** tsgo, **3.23×** the COLD JVM arm and **1.77×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ea566b238648 | **5.40s** | 36,082 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ea566b238648 | **5.11s** | 38,139 LOC/s | 46 |
| xtsc (cold JVM) | ea566b238648 | 23.91s | 8,144 LOC/s | 46 |
| tsc | 6.0.3 | 12.87s | 15,127 LOC/s | 65 |
| tsgo | 7.0.2 | 2.27s | 85,923 LOC/s | 65 |
| xtsc-native | ea566b238648 (AOT) | 8.13s | 23,948 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.39x FASTER than tsc; 2.38x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.11s**, **-5.4%** against `temurin26` on identical bytecode — 0.40× tsc, 2.25× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.86×** tsc and **10.55×** tsgo; tsc is **5.68×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.78s**.

Ahead-of-time (GraalVM native-image): **8.13s** — **0.63×** tsc, **3.59×** tsgo, **2.94×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
