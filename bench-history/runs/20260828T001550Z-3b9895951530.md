# Bench run 20260828T001550Z

- **xtsc revision**: [`3b9895951530`](https://github.com/xemantic/xemantic-typescript-compiler/commit/3b9895951530447e36b87085c634fab325705441)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 3b9895951530447e36b87085c634fab325705441

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3b9895951530 | **4.31s** | 45,216 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3b9895951530 | **4.25s** | 45,833 LOC/s | 46 |
| xtsc (cold JVM) | 3b9895951530 | 24.31s | 8,008 LOC/s | 46 |
| tsc | 6.0.3 | 10.86s | 17,931 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,448 LOC/s | 65 |
| xtsc-native | 3b9895951530 (AOT) | 7.52s | 25,887 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.52x FASTER than tsc; 2.58x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.25s**, **-1.3%** against `temurin26` on identical bytecode — 0.39× tsc, 2.54× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.24×** tsc and **14.54×** tsgo; tsc is **6.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.20s**.

Ahead-of-time (GraalVM native-image): **7.52s** — **0.69×** tsc, **4.50×** tsgo, **3.23×** the COLD JVM arm and **1.75×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3b9895951530 | **5.07s** | 38,410 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3b9895951530 | **4.65s** | 41,880 LOC/s | 46 |
| xtsc (cold JVM) | 3b9895951530 | 27.48s | 7,085 LOC/s | 46 |
| tsc | 6.0.3 | 12.82s | 15,184 LOC/s | 65 |
| tsgo | 7.0.2 | 2.32s | 83,959 LOC/s | 65 |
| xtsc-native | 3b9895951530 (AOT) | 8.12s | 23,981 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.53x FASTER than tsc; 2.19x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.65s**, **-8.3%** against `temurin26` on identical bytecode — 0.36× tsc, 2.00× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.14×** tsc and **11.85×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.36s**.

Ahead-of-time (GraalVM native-image): **8.12s** — **0.63×** tsc, **3.50×** tsgo, **3.38×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
