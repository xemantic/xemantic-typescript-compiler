# Bench run 20260923T142211Z

- **xtsc revision**: [`bd47ceeb2778`](https://github.com/xemantic/xemantic-typescript-compiler/commit/bd47ceeb27787d5c4d54e736a0de7553546680f4)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push bd47ceeb27787d5c4d54e736a0de7553546680f4

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | bd47ceeb2778 | **6.02s** | 32,337 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | bd47ceeb2778 | **5.39s** | 36,109 LOC/s | 46 |
| xtsc (cold JVM) | bd47ceeb2778 | 28.23s | 6,896 LOC/s | 46 |
| tsc | 6.0.3 | 10.92s | 17,831 LOC/s | 65 |
| tsgo | 7.0.2 | 1.73s | 112,740 LOC/s | 65 |
| xtsc-native | bd47ceeb2778 (AOT) | 9.42s | 20,671 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.81x FASTER than tsc; 3.49x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.39s**, **-10.4%** against `temurin26` on identical bytecode — 0.49× tsc, 3.12× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.59×** tsc and **16.35×** tsgo; tsc is **6.32×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.12s**.

Ahead-of-time (GraalVM native-image): **9.42s** — **0.86×** tsc, **5.45×** tsgo, **3.00×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | bd47ceeb2778 | **6.81s** | 28,582 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | bd47ceeb2778 | **6.05s** | 32,192 LOC/s | 46 |
| xtsc (cold JVM) | bd47ceeb2778 | 29.73s | 6,548 LOC/s | 46 |
| tsc | 6.0.3 | 13.28s | 14,666 LOC/s | 65 |
| tsgo | 7.0.2 | 2.37s | 82,291 LOC/s | 65 |
| xtsc-native | bd47ceeb2778 (AOT) | 11.13s | 17,499 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.95x FASTER than tsc; 2.88x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.05s**, **-11.2%** against `temurin26` on identical bytecode — 0.46× tsc, 2.56× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.24×** tsc and **12.57×** tsgo; tsc is **5.61×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.60s**.

Ahead-of-time (GraalVM native-image): **11.13s** — **0.84×** tsc, **4.70×** tsgo, **2.67×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
