# Bench run 20260906T132913Z

- **xtsc revision**: [`07afbec315f5`](https://github.com/xemantic/xemantic-typescript-compiler/commit/07afbec315f595abe6c34583e477a87119b34142)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 07afbec315f595abe6c34583e477a87119b34142

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 07afbec315f5 | **12.10s** | 16,087 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 07afbec315f5 | **11.77s** | 16,536 LOC/s | 46 |
| xtsc (cold JVM) | 07afbec315f5 | 28.23s | 6,896 LOC/s | 46 |
| tsc | 6.0.3 | 8.54s | 22,798 LOC/s | 65 |
| tsgo | 7.0.2 | 1.27s | 153,067 LOC/s | 65 |
| xtsc-native | 07afbec315f5 (AOT) | 19.64s | 9,914 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.42x tsc — parity at 1.00x; 9.51x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **11.77s**, **-2.7%** against `temurin26` on identical bytecode — 1.38× tsc, 9.26× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.31×** tsc and **22.19×** tsgo; tsc is **6.71×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.09s**.

Ahead-of-time (GraalVM native-image): **19.64s** — **2.30×** tsc, **15.44×** tsgo, **1.44×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 07afbec315f5 | **12.62s** | 15,425 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 07afbec315f5 | **12.60s** | 15,453 LOC/s | 46 |
| xtsc (cold JVM) | 07afbec315f5 | 28.12s | 6,923 LOC/s | 46 |
| tsc | 6.0.3 | 10.52s | 18,509 LOC/s | 65 |
| tsgo | 7.0.2 | 1.92s | 101,460 LOC/s | 65 |
| xtsc-native | 07afbec315f5 (AOT) | 20.82s | 9,350 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.20x tsc — parity at 1.00x; 6.58x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **12.60s**, **-0.2%** against `temurin26` on identical bytecode — 1.20× tsc, 6.57× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.67×** tsc and **14.65×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.02s**.

Ahead-of-time (GraalVM native-image): **20.82s** — **1.98×** tsc, **10.85×** tsgo, **1.35×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
