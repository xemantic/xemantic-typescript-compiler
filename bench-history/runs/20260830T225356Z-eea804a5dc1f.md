# Bench run 20260830T225356Z

- **xtsc revision**: [`eea804a5dc1f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/eea804a5dc1f9bad2f126cb49ae137e0c6ef85a5)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push eea804a5dc1f9bad2f126cb49ae137e0c6ef85a5

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | eea804a5dc1f | **3.36s** | 57,895 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | eea804a5dc1f | **3.28s** | 59,360 LOC/s | 46 |
| xtsc (cold JVM) | eea804a5dc1f | 21.20s | 9,182 LOC/s | 46 |
| tsc | 6.0.3 | 9.46s | 20,577 LOC/s | 65 |
| tsgo | 7.0.2 | 1.48s | 131,644 LOC/s | 65 |
| xtsc-native | eea804a5dc1f (AOT) | 5.52s | 35,265 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.81x FASTER than tsc; 2.27x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.28s**, **-2.5%** against `temurin26` on identical bytecode — 0.35× tsc, 2.22× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.24×** tsc and **14.34×** tsgo; tsc is **6.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.10s**.

Ahead-of-time (GraalVM native-image): **5.52s** — **0.58×** tsc, **3.73×** tsgo, **3.84×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | eea804a5dc1f | **4.42s** | 44,020 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | eea804a5dc1f | **4.02s** | 48,433 LOC/s | 46 |
| xtsc (cold JVM) | eea804a5dc1f | 23.57s | 8,261 LOC/s | 46 |
| tsc | 6.0.3 | 11.68s | 16,671 LOC/s | 65 |
| tsgo | 7.0.2 | 2.21s | 87,981 LOC/s | 65 |
| xtsc-native | eea804a5dc1f (AOT) | 6.87s | 28,353 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.64x FASTER than tsc; 2.00x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.02s**, **-9.1%** against `temurin26` on identical bytecode — 0.34× tsc, 1.82× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.02×** tsc and **10.65×** tsgo; tsc is **5.28×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.43s**.

Ahead-of-time (GraalVM native-image): **6.87s** — **0.59×** tsc, **3.10×** tsgo, **3.43×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
