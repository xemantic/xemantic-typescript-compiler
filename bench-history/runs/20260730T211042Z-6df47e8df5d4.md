# Bench run 20260730T211042Z

- **xtsc revision**: [`6df47e8df5d4`](https://github.com/xemantic/xemantic-typescript-compiler/commit/6df47e8df5d4b7c62445447dc9fbc1b502796688)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 6df47e8df5d4b7c62445447dc9fbc1b502796688

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 6df47e8df5d4 | 29.99s | 6,492 LOC/s | 46 |
| tsc | 6.0.3 | 11.69s | 16,655 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.82s | 107,037 LOC/s | 65 |
| xtsc-native | 6df47e8df5d4 (AOT) | 12.37s | 15,734 LOC/s | 46 |

Relative (wall): xtsc is **2.57×** tsc and **16.49×** tsgo; tsc is **6.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.88s**.

Ahead-of-time (GraalVM native-image): **12.37s**, **2.42×** the JVM arm, **1.06×** tsc, **6.80×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 6df47e8df5d4 | 31.43s | 6,195 LOC/s | 46 |
| tsc | 6.0.3 | 13.35s | 14,583 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.45s | 79,405 LOC/s | 65 |
| xtsc-native | 6df47e8df5d4 (AOT) | 14.43s | 13,496 LOC/s | 46 |

Relative (wall): xtsc is **2.35×** tsc and **12.82×** tsgo; tsc is **5.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **31.32s**.

Ahead-of-time (GraalVM native-image): **14.43s**, **2.18×** the JVM arm, **1.08×** tsc, **5.88×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
