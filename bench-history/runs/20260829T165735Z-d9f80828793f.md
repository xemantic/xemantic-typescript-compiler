# Bench run 20260829T165735Z

- **xtsc revision**: [`d9f80828793f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d9f80828793f47907f08ba4f0a656db2558088a9)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push d9f80828793f47907f08ba4f0a656db2558088a9

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d9f80828793f | **4.77s** | 40,800 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d9f80828793f | **4.40s** | 44,280 LOC/s | 46 |
| xtsc (cold JVM) | d9f80828793f | 25.78s | 7,552 LOC/s | 46 |
| tsc | 6.0.3 | 11.01s | 17,690 LOC/s | 65 |
| tsgo | 7.0.2 | 1.75s | 111,004 LOC/s | 65 |
| xtsc-native | d9f80828793f (AOT) | 7.82s | 24,901 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.31x FASTER than tsc; 2.72x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.40s**, **-7.9%** against `temurin26` on identical bytecode — 0.40× tsc, 2.51× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.34×** tsc and **14.70×** tsgo; tsc is **6.27×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.66s**.

Ahead-of-time (GraalVM native-image): **7.82s** — **0.71×** tsc, **4.46×** tsgo, **3.30×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d9f80828793f | **5.58s** | 34,892 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d9f80828793f | **4.93s** | 39,485 LOC/s | 46 |
| xtsc (cold JVM) | d9f80828793f | 27.69s | 7,031 LOC/s | 46 |
| tsc | 6.0.3 | 13.44s | 14,489 LOC/s | 65 |
| tsgo | 7.0.2 | 2.46s | 79,050 LOC/s | 65 |
| xtsc-native | d9f80828793f (AOT) | 8.11s | 24,013 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.41x FASTER than tsc; 2.27x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.93s**, **-11.6%** against `temurin26` on identical bytecode — 0.37× tsc, 2.00× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.06×** tsc and **11.24×** tsgo; tsc is **5.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.53s**.

Ahead-of-time (GraalVM native-image): **8.11s** — **0.60×** tsc, **3.29×** tsgo, **3.42×** the COLD JVM arm and **1.45×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
