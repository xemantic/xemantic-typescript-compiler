# Bench run 20260911T194903Z

- **xtsc revision**: [`e817f9765e2b`](https://github.com/xemantic/xemantic-typescript-compiler/commit/e817f9765e2bd584bb6554985d2f9393e130ede7)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push e817f9765e2bd584bb6554985d2f9393e130ede7

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e817f9765e2b | **5.36s** | 36,338 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e817f9765e2b | **5.19s** | 37,543 LOC/s | 46 |
| xtsc (cold JVM) | e817f9765e2b | 25.20s | 7,725 LOC/s | 46 |
| tsc | 6.0.3 | 10.58s | 18,397 LOC/s | 65 |
| tsgo | 7.0.2 | 1.57s | 123,856 LOC/s | 65 |
| xtsc-native | e817f9765e2b (AOT) | 9.10s | 21,400 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.98x FASTER than tsc; 3.41x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.19s**, **-3.2%** against `temurin26` on identical bytecode — 0.49× tsc, 3.30× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.38×** tsc and **16.03×** tsgo; tsc is **6.73×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.07s**.

Ahead-of-time (GraalVM native-image): **9.10s** — **0.86×** tsc, **5.79×** tsgo, **2.77×** the COLD JVM arm and **1.70×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e817f9765e2b | **6.50s** | 29,935 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e817f9765e2b | **5.68s** | 34,284 LOC/s | 46 |
| xtsc (cold JVM) | e817f9765e2b | 29.36s | 6,630 LOC/s | 46 |
| tsc | 6.0.3 | 12.62s | 15,424 LOC/s | 65 |
| tsgo | 7.0.2 | 2.28s | 85,246 LOC/s | 65 |
| xtsc-native | e817f9765e2b (AOT) | 10.01s | 19,444 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.94x FASTER than tsc; 2.85x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.68s**, **-12.7%** against `temurin26` on identical bytecode — 0.45× tsc, 2.49× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.33×** tsc and **12.86×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.23s**.

Ahead-of-time (GraalVM native-image): **10.01s** — **0.79×** tsc, **4.38×** tsgo, **2.93×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
