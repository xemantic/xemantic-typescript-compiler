# Bench run 20260919T173831Z

- **xtsc revision**: [`83fbfc39c07b`](https://github.com/xemantic/xemantic-typescript-compiler/commit/83fbfc39c07bb093c93aed278b4f351bdf83900b)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 83fbfc39c07bb093c93aed278b4f351bdf83900b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 83fbfc39c07b | **5.75s** | 33,878 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 83fbfc39c07b | **5.20s** | 37,478 LOC/s | 46 |
| xtsc (cold JVM) | 83fbfc39c07b | 25.20s | 7,726 LOC/s | 46 |
| tsc | 6.0.3 | 10.60s | 18,362 LOC/s | 65 |
| tsgo | 7.0.2 | 1.68s | 115,894 LOC/s | 65 |
| xtsc-native | 83fbfc39c07b (AOT) | 8.98s | 21,691 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.84x FASTER than tsc; 3.42x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.20s**, **-9.6%** against `temurin26` on identical bytecode — 0.49× tsc, 3.09× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.38×** tsc and **15.00×** tsgo; tsc is **6.31×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.07s**.

Ahead-of-time (GraalVM native-image): **8.98s** — **0.85×** tsc, **5.34×** tsgo, **2.81×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 83fbfc39c07b | **6.77s** | 28,755 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 83fbfc39c07b | **5.77s** | 33,738 LOC/s | 46 |
| xtsc (cold JVM) | 83fbfc39c07b | 32.73s | 5,948 LOC/s | 46 |
| tsc | 6.0.3 | 13.36s | 14,571 LOC/s | 65 |
| tsgo | 7.0.2 | 2.40s | 81,092 LOC/s | 65 |
| xtsc-native | 83fbfc39c07b (AOT) | 10.65s | 18,273 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.97x FASTER than tsc; 2.82x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.77s**, **-14.8%** against `temurin26` on identical bytecode — 0.43× tsc, 2.40× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.45×** tsc and **13.63×** tsgo; tsc is **5.57×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **32.59s**.

Ahead-of-time (GraalVM native-image): **10.65s** — **0.80×** tsc, **4.44×** tsgo, **3.07×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
