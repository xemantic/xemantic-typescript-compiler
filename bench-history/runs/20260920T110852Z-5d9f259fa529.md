# Bench run 20260920T110852Z

- **xtsc revision**: [`5d9f259fa529`](https://github.com/xemantic/xemantic-typescript-compiler/commit/5d9f259fa5298c185e114a4d81c1cc57993b3632)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 5d9f259fa5298c185e114a4d81c1cc57993b3632

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5d9f259fa529 | **5.60s** | 34,762 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5d9f259fa529 | **5.08s** | 38,364 LOC/s | 46 |
| xtsc (cold JVM) | 5d9f259fa529 | 25.94s | 7,507 LOC/s | 46 |
| tsc | 6.0.3 | 10.41s | 18,706 LOC/s | 65 |
| tsgo | 7.0.2 | 1.60s | 121,688 LOC/s | 65 |
| xtsc-native | 5d9f259fa529 (AOT) | 10.23s | 19,039 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.86x FASTER than tsc; 3.50x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.08s**, **-9.4%** against `temurin26` on identical bytecode — 0.49× tsc, 3.17× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.49×** tsc and **16.21×** tsgo; tsc is **6.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.79s**.

Ahead-of-time (GraalVM native-image): **10.23s** — **0.98×** tsc, **6.39×** tsgo, **2.54×** the COLD JVM arm and **1.83×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5d9f259fa529 | **7.44s** | 26,180 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5d9f259fa529 | **6.52s** | 29,843 LOC/s | 46 |
| xtsc (cold JVM) | 5d9f259fa529 | 29.29s | 6,648 LOC/s | 46 |
| tsc | 6.0.3 | 12.41s | 15,689 LOC/s | 65 |
| tsgo | 7.0.2 | 2.25s | 86,649 LOC/s | 65 |
| xtsc-native | 5d9f259fa529 (AOT) | 10.83s | 17,971 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.67x FASTER than tsc; 3.31x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.52s**, **-12.3%** against `temurin26` on identical bytecode — 0.53× tsc, 2.90× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.36×** tsc and **13.03×** tsgo; tsc is **5.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.15s**.

Ahead-of-time (GraalVM native-image): **10.83s** — **0.87×** tsc, **4.82×** tsgo, **2.70×** the COLD JVM arm and **1.46×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
