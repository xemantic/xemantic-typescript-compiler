# Bench run 20260819T122822Z

- **xtsc revision**: [`50c08ef17679`](https://github.com/xemantic/xemantic-typescript-compiler/commit/50c08ef17679c46934d6923c96c57729699369a8)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 50c08ef17679c46934d6923c96c57729699369a8

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 50c08ef17679 | **2.16s** | 90,181 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 50c08ef17679 | **2.16s** | 90,265 LOC/s | 46 |
| xtsc (cold JVM) | 50c08ef17679 | 14.05s | 13,861 LOC/s | 46 |
| tsc | 6.0.3 | 6.05s | 32,187 LOC/s | 65 |
| tsgo | 7.0.2 | 0.93s | 209,808 LOC/s | 65 |
| xtsc-native | 50c08ef17679 (AOT) | 3.69s | 52,807 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.80x FASTER than tsc; 2.33x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.16s**, **-0.1%** against `temurin26` on identical bytecode — 0.36× tsc, 2.32× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.32×** tsc and **15.14×** tsgo; tsc is **6.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **13.97s**.

Ahead-of-time (GraalVM native-image): **3.69s** — **0.61×** tsc, **3.97×** tsgo, **3.81×** the COLD JVM arm and **1.71×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 50c08ef17679 | **2.83s** | 68,921 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 50c08ef17679 | **2.73s** | 71,450 LOC/s | 46 |
| xtsc (cold JVM) | 50c08ef17679 | 15.82s | 12,310 LOC/s | 46 |
| tsc | 6.0.3 | 7.55s | 25,791 LOC/s | 65 |
| tsgo | 7.0.2 | 1.40s | 139,371 LOC/s | 65 |
| xtsc-native | 50c08ef17679 (AOT) | 4.58s | 42,474 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.67x FASTER than tsc; 2.02x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.73s**, **-3.5%** against `temurin26` on identical bytecode — 0.36× tsc, 1.95× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.10×** tsc and **11.32×** tsgo; tsc is **5.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **15.73s**.

Ahead-of-time (GraalVM native-image): **4.58s** — **0.61×** tsc, **3.28×** tsgo, **3.45×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
