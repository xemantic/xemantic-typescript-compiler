# Bench run 20260815T000337Z

- **xtsc revision**: [`e8c422e368b4`](https://github.com/xemantic/xemantic-typescript-compiler/commit/e8c422e368b48a7fc4912408bf656818ec44b622)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push e8c422e368b48a7fc4912408bf656818ec44b622

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e8c422e368b4 | **4.33s** | 44,976 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e8c422e368b4 | **3.86s** | 50,427 LOC/s | 46 |
| xtsc (cold JVM) | e8c422e368b4 | 22.89s | 8,507 LOC/s | 46 |
| tsc | 6.0.3 | 11.24s | 17,328 LOC/s | 65 |
| tsgo | 7.0.2 | 1.74s | 111,833 LOC/s | 65 |
| xtsc-native | e8c422e368b4 (AOT) | 6.65s | 29,287 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.60x FASTER than tsc; 2.49x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.86s**, **-10.8%** against `temurin26` on identical bytecode — 0.34× tsc, 2.22× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.04×** tsc and **13.15×** tsgo; tsc is **6.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.77s**.

Ahead-of-time (GraalVM native-image): **6.65s** — **0.59×** tsc, **3.82×** tsgo, **3.44×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e8c422e368b4 | **5.09s** | 38,266 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e8c422e368b4 | **4.61s** | 42,271 LOC/s | 46 |
| xtsc (cold JVM) | e8c422e368b4 | 27.51s | 7,078 LOC/s | 46 |
| tsc | 6.0.3 | 13.65s | 14,265 LOC/s | 65 |
| tsgo | 7.0.2 | 2.55s | 76,443 LOC/s | 65 |
| xtsc-native | e8c422e368b4 (AOT) | 7.64s | 25,474 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.68x FASTER than tsc; 2.00x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.61s**, **-9.5%** against `temurin26` on identical bytecode — 0.34× tsc, 1.81× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.02×** tsc and **10.80×** tsgo; tsc is **5.36×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.39s**.

Ahead-of-time (GraalVM native-image): **7.64s** — **0.56×** tsc, **3.00×** tsgo, **3.60×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
