# Bench run 20260830T111233Z

- **xtsc revision**: [`fa08081cd8f5`](https://github.com/xemantic/xemantic-typescript-compiler/commit/fa08081cd8f51be06da038c5e9596af177062a98)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push fa08081cd8f51be06da038c5e9596af177062a98

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | fa08081cd8f5 | **4.56s** | 42,679 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | fa08081cd8f5 | **4.08s** | 47,721 LOC/s | 46 |
| xtsc (cold JVM) | fa08081cd8f5 | 22.64s | 8,599 LOC/s | 46 |
| tsc | 6.0.3 | 10.78s | 18,063 LOC/s | 65 |
| tsgo | 7.0.2 | 1.70s | 114,800 LOC/s | 65 |
| xtsc-native | fa08081cd8f5 (AOT) | 7.26s | 26,822 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.36x FASTER than tsc; 2.69x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.08s**, **-10.6%** against `temurin26` on identical bytecode — 0.38× tsc, 2.41× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.10×** tsc and **13.35×** tsgo; tsc is **6.36×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.52s**.

Ahead-of-time (GraalVM native-image): **7.26s** — **0.67×** tsc, **4.28×** tsgo, **3.12×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | fa08081cd8f5 | **5.26s** | 37,008 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | fa08081cd8f5 | **4.69s** | 41,540 LOC/s | 46 |
| xtsc (cold JVM) | fa08081cd8f5 | 25.41s | 7,661 LOC/s | 46 |
| tsc | 6.0.3 | 13.29s | 14,651 LOC/s | 65 |
| tsgo | 7.0.2 | 2.40s | 80,990 LOC/s | 65 |
| xtsc-native | fa08081cd8f5 (AOT) | 7.85s | 24,790 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.53x FASTER than tsc; 2.19x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.69s**, **-10.9%** against `temurin26` on identical bytecode — 0.35× tsc, 1.95× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.91×** tsc and **10.57×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.27s**.

Ahead-of-time (GraalVM native-image): **7.85s** — **0.59×** tsc, **3.27×** tsgo, **3.24×** the COLD JVM arm and **1.49×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
