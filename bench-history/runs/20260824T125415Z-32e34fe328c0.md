# Bench run 20260824T125415Z

- **xtsc revision**: [`32e34fe328c0`](https://github.com/xemantic/xemantic-typescript-compiler/commit/32e34fe328c01d63734acc6a597bd1e5304cdae8)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 32e34fe328c01d63734acc6a597bd1e5304cdae8

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 32e34fe328c0 | **3.88s** | 50,206 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 32e34fe328c0 | **3.59s** | 54,189 LOC/s | 46 |
| xtsc (cold JVM) | 32e34fe328c0 | 22.11s | 8,807 LOC/s | 46 |
| tsc | 6.0.3 | 10.68s | 18,239 LOC/s | 65 |
| tsgo | 7.0.2 | 1.64s | 118,503 LOC/s | 65 |
| xtsc-native | 32e34fe328c0 (AOT) | 5.77s | 33,720 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.75x FASTER than tsc; 2.36x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.59s**, **-7.3%** against `temurin26` on identical bytecode — 0.34× tsc, 2.19× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.07×** tsc and **13.45×** tsgo; tsc is **6.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.99s**.

Ahead-of-time (GraalVM native-image): **5.77s** — **0.54×** tsc, **3.51×** tsgo, **3.83×** the COLD JVM arm and **1.49×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 32e34fe328c0 | **4.78s** | 40,741 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 32e34fe328c0 | **4.16s** | 46,758 LOC/s | 46 |
| xtsc (cold JVM) | 32e34fe328c0 | 25.11s | 7,754 LOC/s | 46 |
| tsc | 6.0.3 | 12.99s | 14,983 LOC/s | 65 |
| tsgo | 7.0.2 | 2.35s | 83,028 LOC/s | 65 |
| xtsc-native | 32e34fe328c0 (AOT) | 7.13s | 27,288 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.72x FASTER than tsc; 2.04x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.16s**, **-12.9%** against `temurin26` on identical bytecode — 0.32× tsc, 1.78× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.93×** tsc and **10.71×** tsgo; tsc is **5.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.98s**.

Ahead-of-time (GraalVM native-image): **7.13s** — **0.55×** tsc, **3.04×** tsgo, **3.52×** the COLD JVM arm and **1.49×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
