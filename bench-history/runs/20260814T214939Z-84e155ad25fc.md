# Bench run 20260814T214939Z

- **xtsc revision**: [`84e155ad25fc`](https://github.com/xemantic/xemantic-typescript-compiler/commit/84e155ad25fc5ddafa6dba59e2e23b7f9bafaacc)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 84e155ad25fc5ddafa6dba59e2e23b7f9bafaacc

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 84e155ad25fc | **3.63s** | 53,696 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 84e155ad25fc | **3.47s** | 56,045 LOC/s | 46 |
| xtsc (cold JVM) | 84e155ad25fc | 20.99s | 9,277 LOC/s | 46 |
| tsc | 6.0.3 | 9.93s | 19,615 LOC/s | 65 |
| tsgo | 7.0.2 | 1.54s | 126,759 LOC/s | 65 |
| xtsc-native | 84e155ad25fc (AOT) | 5.84s | 33,316 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.74x FASTER than tsc; 2.36x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.47s**, **-4.2%** against `temurin26` on identical bytecode — 0.35× tsc, 2.26× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.11×** tsc and **13.66×** tsgo; tsc is **6.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.88s**.

Ahead-of-time (GraalVM native-image): **5.84s** — **0.59×** tsc, **3.80×** tsgo, **3.59×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 84e155ad25fc | **4.47s** | 43,557 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 84e155ad25fc | **4.15s** | 46,893 LOC/s | 46 |
| xtsc (cold JVM) | 84e155ad25fc | 24.19s | 8,049 LOC/s | 46 |
| tsc | 6.0.3 | 12.22s | 15,938 LOC/s | 65 |
| tsgo | 7.0.2 | 2.20s | 88,460 LOC/s | 65 |
| xtsc-native | 84e155ad25fc (AOT) | 7.29s | 26,689 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.73x FASTER than tsc; 2.03x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.15s**, **-7.1%** against `temurin26` on identical bytecode — 0.34× tsc, 1.89× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.98×** tsc and **10.99×** tsgo; tsc is **5.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.06s**.

Ahead-of-time (GraalVM native-image): **7.29s** — **0.60×** tsc, **3.31×** tsgo, **3.32×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
