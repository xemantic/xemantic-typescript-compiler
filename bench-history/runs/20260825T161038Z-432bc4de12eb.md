# Bench run 20260825T161038Z

- **xtsc revision**: [`432bc4de12eb`](https://github.com/xemantic/xemantic-typescript-compiler/commit/432bc4de12ebd18369341584dfa216bfc039e213)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 432bc4de12ebd18369341584dfa216bfc039e213

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 432bc4de12eb | **4.09s** | 47,557 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 432bc4de12eb | **4.04s** | 48,217 LOC/s | 46 |
| xtsc (cold JVM) | 432bc4de12eb | 23.40s | 8,321 LOC/s | 46 |
| tsc | 6.0.3 | 11.29s | 17,248 LOC/s | 65 |
| tsgo | 7.0.2 | 1.74s | 111,641 LOC/s | 65 |
| xtsc-native | 432bc4de12eb (AOT) | 7.38s | 26,400 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.76x FASTER than tsc; 2.35x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.04s**, **-1.4%** against `temurin26` on identical bytecode — 0.36× tsc, 2.32× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.07×** tsc and **13.42×** tsgo; tsc is **6.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.29s**.

Ahead-of-time (GraalVM native-image): **7.38s** — **0.65×** tsc, **4.23×** tsgo, **3.17×** the COLD JVM arm and **1.80×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 432bc4de12eb | **5.39s** | 36,149 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 432bc4de12eb | **4.72s** | 41,267 LOC/s | 46 |
| xtsc (cold JVM) | 432bc4de12eb | 26.00s | 7,488 LOC/s | 46 |
| tsc | 6.0.3 | 13.95s | 13,959 LOC/s | 65 |
| tsgo | 7.0.2 | 2.49s | 78,193 LOC/s | 65 |
| xtsc-native | 432bc4de12eb (AOT) | 8.38s | 23,228 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.59x FASTER than tsc; 2.16x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.72s**, **-12.4%** against `temurin26` on identical bytecode — 0.34× tsc, 1.89× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.86×** tsc and **10.44×** tsgo; tsc is **5.60×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.85s**.

Ahead-of-time (GraalVM native-image): **8.38s** — **0.60×** tsc, **3.37×** tsgo, **3.10×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
