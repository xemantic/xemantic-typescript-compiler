# Bench run 20260923T131705Z

- **xtsc revision**: [`471205b009bd`](https://github.com/xemantic/xemantic-typescript-compiler/commit/471205b009bd864ece0740565bb458ebdbee9bcb)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 471205b009bd864ece0740565bb458ebdbee9bcb

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 471205b009bd | **5.80s** | 33,586 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 471205b009bd | **5.28s** | 36,882 LOC/s | 46 |
| xtsc (cold JVM) | 471205b009bd | 24.61s | 7,912 LOC/s | 46 |
| tsc | 6.0.3 | 10.82s | 17,991 LOC/s | 65 |
| tsgo | 7.0.2 | 1.72s | 113,462 LOC/s | 65 |
| xtsc-native | 471205b009bd (AOT) | 8.94s | 21,773 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.87x FASTER than tsc; 3.38x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.28s**, **-8.9%** against `temurin26` on identical bytecode — 0.49× tsc, 3.08× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.27×** tsc and **14.34×** tsgo; tsc is **6.31×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.49s**.

Ahead-of-time (GraalVM native-image): **8.94s** — **0.83×** tsc, **5.21×** tsgo, **2.75×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 471205b009bd | **6.74s** | 28,870 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 471205b009bd | **5.94s** | 32,778 LOC/s | 46 |
| xtsc (cold JVM) | 471205b009bd | 30.25s | 6,437 LOC/s | 46 |
| tsc | 6.0.3 | 13.24s | 14,704 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,738 LOC/s | 65 |
| xtsc-native | 471205b009bd (AOT) | 10.51s | 18,525 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.96x FASTER than tsc; 2.83x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.94s**, **-11.9%** against `temurin26` on identical bytecode — 0.45× tsc, 2.49× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.28×** tsc and **12.70×** tsgo; tsc is **5.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.12s**.

Ahead-of-time (GraalVM native-image): **10.51s** — **0.79×** tsc, **4.41×** tsgo, **2.88×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
