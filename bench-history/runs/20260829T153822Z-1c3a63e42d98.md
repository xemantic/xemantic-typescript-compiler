# Bench run 20260829T153822Z

- **xtsc revision**: [`1c3a63e42d98`](https://github.com/xemantic/xemantic-typescript-compiler/commit/1c3a63e42d980d9908c0facdaade6a6b1d596029)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 1c3a63e42d980d9908c0facdaade6a6b1d596029

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1c3a63e42d98 | **2.90s** | 67,069 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1c3a63e42d98 | **2.90s** | 67,138 LOC/s | 46 |
| xtsc (cold JVM) | 1c3a63e42d98 | 16.70s | 11,662 LOC/s | 46 |
| tsc | 6.0.3 | 7.12s | 27,361 LOC/s | 65 |
| tsgo | 7.0.2 | 1.06s | 182,818 LOC/s | 65 |
| xtsc-native | 1c3a63e42d98 (AOT) | 4.45s | 43,792 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.45x FASTER than tsc; 2.73x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.90s**, **-0.1%** against `temurin26` on identical bytecode — 0.41× tsc, 2.72× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.35×** tsc and **15.68×** tsgo; tsc is **6.68×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **16.60s**.

Ahead-of-time (GraalVM native-image): **4.45s** — **0.62×** tsc, **4.17×** tsgo, **3.76×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1c3a63e42d98 | **3.54s** | 55,031 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1c3a63e42d98 | **3.25s** | 59,871 LOC/s | 46 |
| xtsc (cold JVM) | 1c3a63e42d98 | 17.41s | 11,180 LOC/s | 46 |
| tsc | 6.0.3 | 8.49s | 22,938 LOC/s | 65 |
| tsgo | 7.0.2 | 1.71s | 113,860 LOC/s | 65 |
| xtsc-native | 1c3a63e42d98 (AOT) | 5.51s | 35,323 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.40x FASTER than tsc; 2.07x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.25s**, **-8.1%** against `temurin26` on identical bytecode — 0.38× tsc, 1.90× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.05×** tsc and **10.18×** tsgo; tsc is **4.96×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.32s**.

Ahead-of-time (GraalVM native-image): **5.51s** — **0.65×** tsc, **3.22×** tsgo, **3.16×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
