# Bench run 20260924T012841Z

- **xtsc revision**: [`01fae890200c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/01fae890200c3f00d4f80a3a4cb8fe467523c573)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 01fae890200c3f00d4f80a3a4cb8fe467523c573

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 01fae890200c | **4.40s** | 44,210 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 01fae890200c | **4.07s** | 47,897 LOC/s | 46 |
| xtsc (cold JVM) | 01fae890200c | 20.80s | 9,358 LOC/s | 46 |
| tsc | 6.0.3 | 7.95s | 24,487 LOC/s | 65 |
| tsgo | 7.0.2 | 1.20s | 162,658 LOC/s | 65 |
| xtsc-native | 01fae890200c (AOT) | 7.20s | 27,045 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.81x FASTER than tsc; 3.68x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.07s**, **-7.7%** against `temurin26` on identical bytecode — 0.51× tsc, 3.40× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.62×** tsc and **17.38×** tsgo; tsc is **6.64×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.71s**.

Ahead-of-time (GraalVM native-image): **7.20s** — **0.91×** tsc, **6.01×** tsgo, **2.89×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 01fae890200c | **5.36s** | 36,304 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 01fae890200c | **4.90s** | 39,726 LOC/s | 46 |
| xtsc (cold JVM) | 01fae890200c | 23.04s | 8,449 LOC/s | 46 |
| tsc | 6.0.3 | 9.85s | 19,772 LOC/s | 65 |
| tsgo | 7.0.2 | 1.77s | 109,815 LOC/s | 65 |
| xtsc-native | 01fae890200c (AOT) | 8.38s | 23,223 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.84x FASTER than tsc; 3.02x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.90s**, **-8.6%** against `temurin26` on identical bytecode — 0.50× tsc, 2.76× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.34×** tsc and **13.00×** tsgo; tsc is **5.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.94s**.

Ahead-of-time (GraalVM native-image): **8.38s** — **0.85×** tsc, **4.73×** tsgo, **2.75×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
