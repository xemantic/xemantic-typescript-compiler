# Bench run 20260902T051541Z

- **xtsc revision**: [`3a5480300b4b`](https://github.com/xemantic/xemantic-typescript-compiler/commit/3a5480300b4b78cb6f201bc98f6567ab5f796f6f)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 3a5480300b4b78cb6f201bc98f6567ab5f796f6f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3a5480300b4b | **3.61s** | 53,949 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3a5480300b4b | **3.36s** | 57,981 LOC/s | 46 |
| xtsc (cold JVM) | 3a5480300b4b | 21.72s | 8,965 LOC/s | 46 |
| tsc | 6.0.3 | 9.87s | 19,726 LOC/s | 65 |
| tsgo | 7.0.2 | 1.53s | 127,256 LOC/s | 65 |
| xtsc-native | 3a5480300b4b (AOT) | 5.78s | 33,685 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.73x FASTER than tsc; 2.36x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.36s**, **-7.0%** against `temurin26` on identical bytecode — 0.34× tsc, 2.19× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.20×** tsc and **14.19×** tsgo; tsc is **6.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.59s**.

Ahead-of-time (GraalVM native-image): **5.78s** — **0.59×** tsc, **3.78×** tsgo, **3.76×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3a5480300b4b | **4.60s** | 42,326 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3a5480300b4b | **4.14s** | 47,052 LOC/s | 46 |
| xtsc (cold JVM) | 3a5480300b4b | 24.94s | 7,807 LOC/s | 46 |
| tsc | 6.0.3 | 12.02s | 16,199 LOC/s | 65 |
| tsgo | 7.0.2 | 2.26s | 86,037 LOC/s | 65 |
| xtsc-native | 3a5480300b4b (AOT) | 7.03s | 27,703 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.61x FASTER than tsc; 2.03x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.14s**, **-10.0%** against `temurin26` on identical bytecode — 0.34× tsc, 1.83× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.07×** tsc and **11.02×** tsgo; tsc is **5.31×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.80s**.

Ahead-of-time (GraalVM native-image): **7.03s** — **0.58×** tsc, **3.11×** tsgo, **3.55×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
