# Bench run 20260905T205330Z

- **xtsc revision**: [`c7b8c66795fa`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c7b8c66795fa073f6d6040e99503aaae416d27c0)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push c7b8c66795fa073f6d6040e99503aaae416d27c0

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c7b8c66795fa | **11.79s** | 16,516 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c7b8c66795fa | **11.31s** | 17,219 LOC/s | 46 |
| xtsc (cold JVM) | c7b8c66795fa | 26.57s | 7,328 LOC/s | 46 |
| tsc | 6.0.3 | 8.01s | 24,298 LOC/s | 65 |
| tsgo | 7.0.2 | 1.22s | 159,591 LOC/s | 65 |
| xtsc-native | c7b8c66795fa (AOT) | 19.10s | 10,191 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.47x tsc — parity at 1.00x; 9.66x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **11.31s**, **-4.1%** against `temurin26` on identical bytecode — 1.41× tsc, 9.27× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.32×** tsc and **21.78×** tsgo; tsc is **6.57×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.45s**.

Ahead-of-time (GraalVM native-image): **19.10s** — **2.38×** tsc, **15.66×** tsgo, **1.39×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c7b8c66795fa | **11.96s** | 16,286 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c7b8c66795fa | **12.27s** | 15,874 LOC/s | 46 |
| xtsc (cold JVM) | c7b8c66795fa | 28.29s | 6,883 LOC/s | 46 |
| tsc | 6.0.3 | 9.73s | 20,006 LOC/s | 65 |
| tsgo | 7.0.2 | 1.79s | 109,015 LOC/s | 65 |
| xtsc-native | c7b8c66795fa (AOT) | 19.53s | 9,969 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.23x tsc — parity at 1.00x; 6.69x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **12.27s**, **+2.6%** against `temurin26` on identical bytecode — 1.26× tsc, 6.87× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.91×** tsc and **15.84×** tsgo; tsc is **5.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.16s**.

Ahead-of-time (GraalVM native-image): **19.53s** — **2.01×** tsc, **10.93×** tsgo, **1.45×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
