# Bench run 20260917T050301Z

- **xtsc revision**: [`7eb42ce0608c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/7eb42ce0608c6699256d6add3061f296f6ba77a2)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 7eb42ce0608c6699256d6add3061f296f6ba77a2

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7eb42ce0608c | **5.76s** | 33,802 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7eb42ce0608c | **5.47s** | 35,562 LOC/s | 46 |
| xtsc (cold JVM) | 7eb42ce0608c | 27.14s | 7,174 LOC/s | 46 |
| tsc | 6.0.3 | 10.95s | 17,777 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,276 LOC/s | 65 |
| xtsc-native | 7eb42ce0608c (AOT) | 9.58s | 20,325 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.90x FASTER than tsc; 3.41x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.47s**, **-4.9%** against `temurin26` on identical bytecode — 0.50× tsc, 3.24× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.48×** tsc and **16.07×** tsgo; tsc is **6.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.02s**.

Ahead-of-time (GraalVM native-image): **9.58s** — **0.87×** tsc, **5.67×** tsgo, **2.83×** the COLD JVM arm and **1.66×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7eb42ce0608c | **6.68s** | 29,129 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7eb42ce0608c | **5.86s** | 33,242 LOC/s | 46 |
| xtsc (cold JVM) | 7eb42ce0608c | 32.78s | 5,939 LOC/s | 46 |
| tsc | 6.0.3 | 13.25s | 14,690 LOC/s | 65 |
| tsgo | 7.0.2 | 2.41s | 80,923 LOC/s | 65 |
| xtsc-native | 7eb42ce0608c (AOT) | 10.77s | 18,081 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.98x FASTER than tsc; 2.78x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.86s**, **-12.4%** against `temurin26` on identical bytecode — 0.44× tsc, 2.43× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.47×** tsc and **13.62×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **32.65s**.

Ahead-of-time (GraalVM native-image): **10.77s** — **0.81×** tsc, **4.48×** tsgo, **3.04×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
