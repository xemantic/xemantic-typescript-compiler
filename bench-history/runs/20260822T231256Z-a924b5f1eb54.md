# Bench run 20260822T231256Z

- **xtsc revision**: [`a924b5f1eb54`](https://github.com/xemantic/xemantic-typescript-compiler/commit/a924b5f1eb541bcd482cd56c8d68d166925cf8f5)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push a924b5f1eb541bcd482cd56c8d68d166925cf8f5

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a924b5f1eb54 | **4.42s** | 44,040 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a924b5f1eb54 | **4.06s** | 47,908 LOC/s | 46 |
| xtsc (cold JVM) | a924b5f1eb54 | 23.29s | 8,358 LOC/s | 46 |
| tsc | 6.0.3 | 11.52s | 16,898 LOC/s | 65 |
| tsgo | 7.0.2 | 1.80s | 107,987 LOC/s | 65 |
| xtsc-native | a924b5f1eb54 (AOT) | 7.09s | 27,446 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.61x FASTER than tsc; 2.45x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.06s**, **-8.1%** against `temurin26` on identical bytecode — 0.35× tsc, 2.25× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.02×** tsc and **12.92×** tsgo; tsc is **6.39×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.17s**.

Ahead-of-time (GraalVM native-image): **7.09s** — **0.62×** tsc, **3.93×** tsgo, **3.28×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a924b5f1eb54 | **5.16s** | 37,762 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a924b5f1eb54 | **4.77s** | 40,783 LOC/s | 46 |
| xtsc (cold JVM) | a924b5f1eb54 | 27.76s | 7,013 LOC/s | 46 |
| tsc | 6.0.3 | 13.90s | 14,004 LOC/s | 65 |
| tsgo | 7.0.2 | 2.54s | 76,805 LOC/s | 65 |
| xtsc-native | a924b5f1eb54 (AOT) | 8.46s | 23,022 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.70x FASTER than tsc; 2.03x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.77s**, **-7.4%** against `temurin26` on identical bytecode — 0.34× tsc, 1.88× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.00×** tsc and **10.95×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.61s**.

Ahead-of-time (GraalVM native-image): **8.46s** — **0.61×** tsc, **3.34×** tsgo, **3.28×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
