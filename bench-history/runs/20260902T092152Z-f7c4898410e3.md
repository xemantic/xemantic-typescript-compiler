# Bench run 20260902T092152Z

- **xtsc revision**: [`f7c4898410e3`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f7c4898410e3c368c4d160e7a1b81f3280e19285)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push f7c4898410e3c368c4d160e7a1b81f3280e19285

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f7c4898410e3 | **4.10s** | 47,499 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f7c4898410e3 | **3.90s** | 49,898 LOC/s | 46 |
| xtsc (cold JVM) | f7c4898410e3 | 21.99s | 8,853 LOC/s | 46 |
| tsc | 6.0.3 | 10.08s | 19,323 LOC/s | 65 |
| tsgo | 7.0.2 | 1.54s | 126,184 LOC/s | 65 |
| xtsc-native | f7c4898410e3 (AOT) | 6.37s | 30,570 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.46x FASTER than tsc; 2.66x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.90s**, **-4.8%** against `temurin26` on identical bytecode — 0.39× tsc, 2.53× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.18×** tsc and **14.25×** tsgo; tsc is **6.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.88s**.

Ahead-of-time (GraalVM native-image): **6.37s** — **0.63×** tsc, **4.13×** tsgo, **3.45×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f7c4898410e3 | **5.06s** | 38,493 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f7c4898410e3 | **4.54s** | 42,876 LOC/s | 46 |
| xtsc (cold JVM) | f7c4898410e3 | 25.20s | 7,725 LOC/s | 46 |
| tsc | 6.0.3 | 12.32s | 15,801 LOC/s | 65 |
| tsgo | 7.0.2 | 2.24s | 86,765 LOC/s | 65 |
| xtsc-native | f7c4898410e3 (AOT) | 7.99s | 24,365 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.44x FASTER than tsc; 2.25x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.54s**, **-10.2%** against `temurin26` on identical bytecode — 0.37× tsc, 2.02× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.05×** tsc and **11.23×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.09s**.

Ahead-of-time (GraalVM native-image): **7.99s** — **0.65×** tsc, **3.56×** tsgo, **3.15×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
