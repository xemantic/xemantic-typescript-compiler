# Bench run 20260902T183017Z

- **xtsc revision**: [`a1ad99a91962`](https://github.com/xemantic/xemantic-typescript-compiler/commit/a1ad99a9196283fe96ee9dc2a6cb22a95312de9c)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push a1ad99a9196283fe96ee9dc2a6cb22a95312de9c

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a1ad99a91962 | **4.06s** | 47,979 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a1ad99a91962 | **3.85s** | 50,624 LOC/s | 46 |
| xtsc (cold JVM) | a1ad99a91962 | 23.22s | 8,385 LOC/s | 46 |
| tsc | 6.0.3 | 9.92s | 19,619 LOC/s | 65 |
| tsgo | 7.0.2 | 1.53s | 127,090 LOC/s | 65 |
| xtsc-native | a1ad99a91962 (AOT) | 6.23s | 31,232 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.45x FASTER than tsc; 2.65x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.85s**, **-5.2%** against `temurin26` on identical bytecode — 0.39× tsc, 2.51× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.34×** tsc and **15.16×** tsgo; tsc is **6.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.10s**.

Ahead-of-time (GraalVM native-image): **6.23s** — **0.63×** tsc, **4.07×** tsgo, **3.72×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a1ad99a91962 | **4.95s** | 39,325 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a1ad99a91962 | **4.53s** | 42,999 LOC/s | 46 |
| xtsc (cold JVM) | a1ad99a91962 | 27.34s | 7,121 LOC/s | 46 |
| tsc | 6.0.3 | 12.44s | 15,651 LOC/s | 65 |
| tsgo | 7.0.2 | 2.23s | 87,193 LOC/s | 65 |
| xtsc-native | a1ad99a91962 (AOT) | 7.76s | 25,080 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.51x FASTER than tsc; 2.22x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.53s**, **-8.5%** against `temurin26` on identical bytecode — 0.36× tsc, 2.03× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.20×** tsc and **12.24×** tsgo; tsc is **5.57×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.20s**.

Ahead-of-time (GraalVM native-image): **7.76s** — **0.62×** tsc, **3.48×** tsgo, **3.52×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
