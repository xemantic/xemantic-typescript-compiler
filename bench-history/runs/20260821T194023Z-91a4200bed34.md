# Bench run 20260821T194023Z

- **xtsc revision**: [`91a4200bed34`](https://github.com/xemantic/xemantic-typescript-compiler/commit/91a4200bed34b2c98885c4cdf6b084eedd8ccdea)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 91a4200bed34b2c98885c4cdf6b084eedd8ccdea

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 91a4200bed34 | **2.85s** | 68,340 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 91a4200bed34 | **2.72s** | 71,529 LOC/s | 46 |
| xtsc (cold JVM) | 91a4200bed34 | 18.99s | 10,251 LOC/s | 46 |
| tsc | 6.0.3 | 8.55s | 22,772 LOC/s | 65 |
| tsgo | 7.0.2 | 1.30s | 150,117 LOC/s | 65 |
| xtsc-native | 91a4200bed34 (AOT) | 4.38s | 44,442 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 3.00x FASTER than tsc; 2.20x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.72s**, **-4.5%** against `temurin26` on identical bytecode — 0.32× tsc, 2.10× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.22×** tsc and **14.64×** tsgo; tsc is **6.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.89s**.

Ahead-of-time (GraalVM native-image): **4.38s** — **0.51×** tsc, **3.38×** tsgo, **4.34×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 91a4200bed34 | **3.83s** | 50,822 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 91a4200bed34 | **3.35s** | 58,050 LOC/s | 46 |
| xtsc (cold JVM) | 91a4200bed34 | 18.86s | 10,322 LOC/s | 46 |
| tsc | 6.0.3 | 10.28s | 18,936 LOC/s | 65 |
| tsgo | 7.0.2 | 1.93s | 101,038 LOC/s | 65 |
| xtsc-native | 91a4200bed34 (AOT) | 5.60s | 34,768 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.68x FASTER than tsc; 1.99x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.35s**, **-12.5%** against `temurin26` on identical bytecode — 0.33× tsc, 1.74× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.83×** tsc and **9.79×** tsgo; tsc is **5.34×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.77s**.

Ahead-of-time (GraalVM native-image): **5.60s** — **0.54×** tsc, **2.91×** tsgo, **3.37×** the COLD JVM arm and **1.46×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
