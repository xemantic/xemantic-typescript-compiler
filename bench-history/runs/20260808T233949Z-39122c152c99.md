# Bench run 20260808T233949Z

- **xtsc revision**: [`39122c152c99`](https://github.com/xemantic/xemantic-typescript-compiler/commit/39122c152c99f587f461e9a48eae8b6b956a2bc8)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 39122c152c99f587f461e9a48eae8b6b956a2bc8

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 39122c152c99 | 27.07s | 7,192 LOC/s | 46 |
| tsc | 6.0.3 | 10.93s | 17,818 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.68s | 115,825 LOC/s | 65 |
| xtsc-native | 39122c152c99 (AOT) | 11.05s | 17,621 LOC/s | 46 |

Relative (wall): xtsc is **2.48×** tsc and **16.10×** tsgo; tsc is **6.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.96s**.

Ahead-of-time (GraalVM native-image): **11.05s**, **2.45×** the JVM arm, **1.01×** tsc, **6.57×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 39122c152c99 | 26.38s | 7,379 LOC/s | 46 |
| tsc | 6.0.3 | 13.11s | 14,854 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.38s | 81,738 LOC/s | 65 |
| xtsc-native | 39122c152c99 (AOT) | 12.74s | 15,282 LOC/s | 46 |

Relative (wall): xtsc is **2.01×** tsc and **11.08×** tsgo; tsc is **5.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.26s**.

Ahead-of-time (GraalVM native-image): **12.74s**, **2.07×** the JVM arm, **0.97×** tsc, **5.35×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
