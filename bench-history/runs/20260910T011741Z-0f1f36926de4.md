# Bench run 20260910T011741Z

- **xtsc revision**: [`0f1f36926de4`](https://github.com/xemantic/xemantic-typescript-compiler/commit/0f1f36926de4fc61c31496c3d6ee7e8d05a32d6f)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 0f1f36926de4fc61c31496c3d6ee7e8d05a32d6f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0f1f36926de4 | **5.34s** | 36,440 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0f1f36926de4 | **4.71s** | 41,338 LOC/s | 46 |
| xtsc (cold JVM) | 0f1f36926de4 | 26.98s | 7,216 LOC/s | 46 |
| tsc | 6.0.3 | 10.07s | 19,329 LOC/s | 65 |
| tsgo | 7.0.2 | 1.55s | 125,695 LOC/s | 65 |
| xtsc-native | 0f1f36926de4 (AOT) | 8.43s | 23,093 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.89x FASTER than tsc; 3.45x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.71s**, **-11.8%** against `temurin26` on identical bytecode — 0.47× tsc, 3.04× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.68×** tsc and **17.42×** tsgo; tsc is **6.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.86s**.

Ahead-of-time (GraalVM native-image): **8.43s** — **0.84×** tsc, **5.44×** tsgo, **3.20×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0f1f36926de4 | **6.33s** | 30,753 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0f1f36926de4 | **5.67s** | 34,369 LOC/s | 46 |
| xtsc (cold JVM) | 0f1f36926de4 | 27.47s | 7,088 LOC/s | 46 |
| tsc | 6.0.3 | 12.59s | 15,469 LOC/s | 65 |
| tsgo | 7.0.2 | 2.31s | 84,177 LOC/s | 65 |
| xtsc-native | 0f1f36926de4 (AOT) | 10.39s | 18,744 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.99x FASTER than tsc; 2.74x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.67s**, **-10.5%** against `temurin26` on identical bytecode — 0.45× tsc, 2.45× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.18×** tsc and **11.88×** tsgo; tsc is **5.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.33s**.

Ahead-of-time (GraalVM native-image): **10.39s** — **0.83×** tsc, **4.49×** tsgo, **2.64×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
