# Bench run 20260824T225705Z

- **xtsc revision**: [`6a54f2585624`](https://github.com/xemantic/xemantic-typescript-compiler/commit/6a54f2585624ae9667ac301c424b846cff069406)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 6a54f2585624ae9667ac301c424b846cff069406

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6a54f2585624 | **3.19s** | 61,015 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6a54f2585624 | **2.96s** | 65,889 LOC/s | 46 |
| xtsc (cold JVM) | 6a54f2585624 | 18.76s | 10,379 LOC/s | 46 |
| tsc | 6.0.3 | 9.11s | 21,369 LOC/s | 65 |
| tsgo | 7.0.2 | 1.46s | 133,540 LOC/s | 65 |
| xtsc-native | 6a54f2585624 (AOT) | 4.92s | 39,565 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.86x FASTER than tsc; 2.19x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.96s**, **-7.4%** against `temurin26` on identical bytecode — 0.32× tsc, 2.03× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.06×** tsc and **12.87×** tsgo; tsc is **6.25×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.65s**.

Ahead-of-time (GraalVM native-image): **4.92s** — **0.54×** tsc, **3.38×** tsgo, **3.81×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6a54f2585624 | **4.25s** | 45,769 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6a54f2585624 | **3.54s** | 54,922 LOC/s | 46 |
| xtsc (cold JVM) | 6a54f2585624 | 24.97s | 7,798 LOC/s | 46 |
| tsc | 6.0.3 | 11.04s | 17,642 LOC/s | 65 |
| tsgo | 7.0.2 | 2.11s | 92,363 LOC/s | 65 |
| xtsc-native | 6a54f2585624 (AOT) | 5.92s | 32,861 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.59x FASTER than tsc; 2.02x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.54s**, **-16.7%** against `temurin26` on identical bytecode — 0.32× tsc, 1.68× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.26×** tsc and **11.84×** tsgo; tsc is **5.24×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.86s**.

Ahead-of-time (GraalVM native-image): **5.92s** — **0.54×** tsc, **2.81×** tsgo, **4.21×** the COLD JVM arm and **1.39×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
