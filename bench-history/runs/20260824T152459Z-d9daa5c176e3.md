# Bench run 20260824T152459Z

- **xtsc revision**: [`d9daa5c176e3`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d9daa5c176e33df1e4590481c5210d21cb58248a)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push d9daa5c176e33df1e4590481c5210d21cb58248a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d9daa5c176e3 | **4.47s** | 43,557 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d9daa5c176e3 | **4.24s** | 45,920 LOC/s | 46 |
| xtsc (cold JVM) | d9daa5c176e3 | 21.65s | 8,993 LOC/s | 46 |
| tsc | 6.0.3 | 11.40s | 17,082 LOC/s | 65 |
| tsgo | 7.0.2 | 1.76s | 110,689 LOC/s | 65 |
| xtsc-native | d9daa5c176e3 (AOT) | 7.21s | 27,011 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.55x FASTER than tsc; 2.54x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.24s**, **-5.1%** against `temurin26` on identical bytecode — 0.37× tsc, 2.41× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.90×** tsc and **12.31×** tsgo; tsc is **6.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.53s**.

Ahead-of-time (GraalVM native-image): **7.21s** — **0.63×** tsc, **4.10×** tsgo, **3.00×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d9daa5c176e3 | **5.25s** | 37,107 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d9daa5c176e3 | **4.86s** | 40,078 LOC/s | 46 |
| xtsc (cold JVM) | d9daa5c176e3 | 25.10s | 7,756 LOC/s | 46 |
| tsc | 6.0.3 | 14.02s | 13,891 LOC/s | 65 |
| tsgo | 7.0.2 | 2.54s | 76,533 LOC/s | 65 |
| xtsc-native | d9daa5c176e3 (AOT) | 8.53s | 22,822 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.67x FASTER than tsc; 2.06x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.86s**, **-7.4%** against `temurin26` on identical bytecode — 0.35× tsc, 1.91× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.79×** tsc and **9.87×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.98s**.

Ahead-of-time (GraalVM native-image): **8.53s** — **0.61×** tsc, **3.35×** tsgo, **2.94×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
