# Bench run 20260829T180814Z

- **xtsc revision**: [`7aeff60b57f7`](https://github.com/xemantic/xemantic-typescript-compiler/commit/7aeff60b57f7c2f752a72598b54d8d1ea3a34710)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 7aeff60b57f7c2f752a72598b54d8d1ea3a34710

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7aeff60b57f7 | **4.62s** | 42,152 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7aeff60b57f7 | **4.41s** | 44,150 LOC/s | 46 |
| xtsc (cold JVM) | 7aeff60b57f7 | 24.78s | 7,858 LOC/s | 46 |
| tsc | 6.0.3 | 11.40s | 17,080 LOC/s | 65 |
| tsgo | 7.0.2 | 1.78s | 109,260 LOC/s | 65 |
| xtsc-native | 7aeff60b57f7 (AOT) | 7.90s | 24,645 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.47x FASTER than tsc; 2.59x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.41s**, **-4.5%** against `temurin26` on identical bytecode — 0.39× tsc, 2.47× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.17×** tsc and **13.90×** tsgo; tsc is **6.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.62s**.

Ahead-of-time (GraalVM native-image): **7.90s** — **0.69×** tsc, **4.43×** tsgo, **3.14×** the COLD JVM arm and **1.71×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7aeff60b57f7 | **5.48s** | 35,555 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7aeff60b57f7 | **4.94s** | 39,437 LOC/s | 46 |
| xtsc (cold JVM) | 7aeff60b57f7 | 28.11s | 6,926 LOC/s | 46 |
| tsc | 6.0.3 | 13.32s | 14,616 LOC/s | 65 |
| tsgo | 7.0.2 | 2.45s | 79,340 LOC/s | 65 |
| xtsc-native | 7aeff60b57f7 (AOT) | 8.13s | 23,960 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.43x FASTER than tsc; 2.23x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.94s**, **-9.8%** against `temurin26` on identical bytecode — 0.37× tsc, 2.01× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.11×** tsc and **11.45×** tsgo; tsc is **5.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.97s**.

Ahead-of-time (GraalVM native-image): **8.13s** — **0.61×** tsc, **3.31×** tsgo, **3.46×** the COLD JVM arm and **1.48×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
