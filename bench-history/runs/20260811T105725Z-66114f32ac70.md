# Bench run 20260811T105725Z

- **xtsc revision**: [`66114f32ac70`](https://github.com/xemantic/xemantic-typescript-compiler/commit/66114f32ac705725dfa008722272c3164c091b3c)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 66114f32ac705725dfa008722272c3164c091b3c

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 66114f32ac70 | **4.94s** | 39,397 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 66114f32ac70 | **4.52s** | 43,037 LOC/s | 46 |
| xtsc (cold JVM) | 66114f32ac70 | 25.81s | 7,543 LOC/s | 46 |
| tsc | 6.0.3 | 11.18s | 17,415 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.78s | 109,383 LOC/s | 65 |
| xtsc-native | 66114f32ac70 (AOT) | 7.72s | 25,227 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.26x FASTER than tsc; 2.78x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.52s**, **-8.5%** against `temurin26` on identical bytecode — 0.40× tsc, 2.54× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.31×** tsc and **14.50×** tsgo; tsc is **6.28×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.69s**.

Ahead-of-time (GraalVM native-image): **7.72s** — **0.69×** tsc, **4.34×** tsgo, **3.34×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 66114f32ac70 | **5.65s** | 34,448 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 66114f32ac70 | **5.22s** | 37,277 LOC/s | 46 |
| xtsc (cold JVM) | 66114f32ac70 | 29.89s | 6,513 LOC/s | 46 |
| tsc | 6.0.3 | 13.50s | 14,420 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.51s | 77,508 LOC/s | 65 |
| xtsc-native | 66114f32ac70 (AOT) | 8.95s | 21,754 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.39x FASTER than tsc; 2.25x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.22s**, **-7.6%** against `temurin26` on identical bytecode — 0.39× tsc, 2.08× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.21×** tsc and **11.90×** tsgo; tsc is **5.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.77s**.

Ahead-of-time (GraalVM native-image): **8.95s** — **0.66×** tsc, **3.56×** tsgo, **3.34×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
