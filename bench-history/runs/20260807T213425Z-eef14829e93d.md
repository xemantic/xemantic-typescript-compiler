# Bench run 20260807T213425Z

- **xtsc revision**: [`eef14829e93d`](https://github.com/xemantic/xemantic-typescript-compiler/commit/eef14829e93d2a35a3f64f2d987dfabc2dc4859a)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push eef14829e93d2a35a3f64f2d987dfabc2dc4859a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | eef14829e93d | 26.88s | 7,243 LOC/s | 46 |
| tsc | 6.0.3 | 11.34s | 17,174 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.77s | 110,001 LOC/s | 65 |
| xtsc-native | eef14829e93d (AOT) | 11.48s | 16,954 LOC/s | 46 |

Relative (wall): xtsc is **2.37×** tsc and **15.19×** tsgo; tsc is **6.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.77s**.

Ahead-of-time (GraalVM native-image): **11.48s**, **2.34×** the JVM arm, **1.01×** tsc, **6.49×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | eef14829e93d | 28.24s | 6,894 LOC/s | 46 |
| tsc | 6.0.3 | 14.07s | 13,837 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.58s | 75,495 LOC/s | 65 |
| xtsc-native | eef14829e93d (AOT) | 14.02s | 13,884 LOC/s | 46 |

Relative (wall): xtsc is **2.01×** tsc and **10.95×** tsgo; tsc is **5.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.13s**.

Ahead-of-time (GraalVM native-image): **14.02s**, **2.01×** the JVM arm, **1.00×** tsc, **5.44×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
