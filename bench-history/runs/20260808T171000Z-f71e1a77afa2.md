# Bench run 20260808T171000Z

- **xtsc revision**: [`f71e1a77afa2`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f71e1a77afa232439a0840cc3dda5255588e64d9)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push f71e1a77afa232439a0840cc3dda5255588e64d9

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | f71e1a77afa2 | 23.42s | 8,312 LOC/s | 46 |
| tsc | 6.0.3 | 10.45s | 18,633 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.57s | 124,330 LOC/s | 65 |
| xtsc-native | f71e1a77afa2 (AOT) | 12.46s | 15,624 LOC/s | 46 |

Relative (wall): xtsc is **2.24×** tsc and **14.96×** tsgo; tsc is **6.67×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.31s**.

Ahead-of-time (GraalVM native-image): **12.46s**, **1.88×** the JVM arm, **1.19×** tsc, **7.96×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | f71e1a77afa2 | 28.24s | 6,894 LOC/s | 46 |
| tsc | 6.0.3 | 12.52s | 15,550 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.33s | 83,455 LOC/s | 65 |
| xtsc-native | f71e1a77afa2 (AOT) | 12.46s | 15,623 LOC/s | 46 |

Relative (wall): xtsc is **2.26×** tsc and **12.11×** tsgo; tsc is **5.37×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.12s**.

Ahead-of-time (GraalVM native-image): **12.46s**, **2.27×** the JVM arm, **1.00×** tsc, **5.34×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
