# Bench run 20260811T020603Z

- **xtsc revision**: [`b8040571d31c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/b8040571d31ca7cb1263c52f0d43789a6aa39579)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push b8040571d31ca7cb1263c52f0d43789a6aa39579

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b8040571d31c | **4.47s** | 43,567 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b8040571d31c | **4.38s** | 44,472 LOC/s | 46 |
| xtsc (cold JVM) | b8040571d31c | 23.72s | 8,207 LOC/s | 46 |
| tsc | 6.0.3 | 10.88s | 17,903 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.73s | 112,349 LOC/s | 65 |
| xtsc-native | b8040571d31c (AOT) | 7.12s | 27,353 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.43x FASTER than tsc; 2.58x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.38s**, **-2.0%** against `temurin26` on identical bytecode — 0.40× tsc, 2.53× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.18×** tsc and **13.69×** tsgo; tsc is **6.28×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.58s**.

Ahead-of-time (GraalVM native-image): **7.12s** — **0.65×** tsc, **4.11×** tsgo, **3.33×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b8040571d31c | **5.48s** | 35,555 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b8040571d31c | **4.81s** | 40,503 LOC/s | 46 |
| xtsc (cold JVM) | b8040571d31c | 26.37s | 7,382 LOC/s | 46 |
| tsc | 6.0.3 | 13.40s | 14,528 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.40s | 81,024 LOC/s | 65 |
| xtsc-native | b8040571d31c (AOT) | 8.38s | 23,225 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.45x FASTER than tsc; 2.28x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.81s**, **-12.2%** against `temurin26` on identical bytecode — 0.36× tsc, 2.00× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.97×** tsc and **10.97×** tsgo; tsc is **5.58×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.23s**.

Ahead-of-time (GraalVM native-image): **8.38s** — **0.63×** tsc, **3.49×** tsgo, **3.15×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
