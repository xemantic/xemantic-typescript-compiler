# Bench run 20260818T192340Z

- **xtsc revision**: [`116d9295b08f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/116d9295b08fb4163b43773d39019bba52511dd6)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 116d9295b08fb4163b43773d39019bba52511dd6

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 116d9295b08f | **4.18s** | 46,624 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 116d9295b08f | **3.59s** | 54,249 LOC/s | 46 |
| xtsc (cold JVM) | 116d9295b08f | 24.39s | 7,982 LOC/s | 46 |
| tsc | 6.0.3 | 10.75s | 18,105 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,208 LOC/s | 65 |
| xtsc-native | 116d9295b08f (AOT) | 6.00s | 32,466 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.58x FASTER than tsc; 2.47x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.59s**, **-14.1%** against `temurin26` on identical bytecode — 0.33× tsc, 2.12× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.27×** tsc and **14.43×** tsgo; tsc is **6.36×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.24s**.

Ahead-of-time (GraalVM native-image): **6.00s** — **0.56×** tsc, **3.55×** tsgo, **4.07×** the COLD JVM arm and **1.44×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 116d9295b08f | **4.86s** | 40,078 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 116d9295b08f | **4.28s** | 45,480 LOC/s | 46 |
| xtsc (cold JVM) | 116d9295b08f | 24.85s | 7,836 LOC/s | 46 |
| tsc | 6.0.3 | 13.27s | 14,676 LOC/s | 65 |
| tsgo | 7.0.2 | 2.45s | 79,535 LOC/s | 65 |
| xtsc-native | 116d9295b08f (AOT) | 7.22s | 26,963 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.73x FASTER than tsc; 1.98x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.28s**, **-11.9%** against `temurin26` on identical bytecode — 0.32× tsc, 1.75× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.87×** tsc and **10.15×** tsgo; tsc is **5.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.73s**.

Ahead-of-time (GraalVM native-image): **7.22s** — **0.54×** tsc, **2.95×** tsgo, **3.44×** the COLD JVM arm and **1.49×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
