# Bench run 20260924T204526Z

- **xtsc revision**: [`3f236da6b82a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/3f236da6b82aabb9ac6ee3702a826e4174013b8b)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 3f236da6b82aabb9ac6ee3702a826e4174013b8b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3f236da6b82a | **5.87s** | 33,185 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3f236da6b82a | **5.54s** | 35,132 LOC/s | 46 |
| xtsc (cold JVM) | 3f236da6b82a | 26.64s | 7,308 LOC/s | 46 |
| tsc | 6.0.3 | 10.80s | 18,027 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,481 LOC/s | 65 |
| xtsc-native | 3f236da6b82a (AOT) | 9.19s | 21,179 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.84x FASTER than tsc; 3.48x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.54s**, **-5.5%** against `temurin26` on identical bytecode — 0.51× tsc, 3.29× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.47×** tsc and **15.80×** tsgo; tsc is **6.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.52s**.

Ahead-of-time (GraalVM native-image): **9.19s** — **0.85×** tsc, **5.45×** tsgo, **2.90×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3f236da6b82a | **6.75s** | 28,844 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3f236da6b82a | **6.07s** | 32,091 LOC/s | 46 |
| xtsc (cold JVM) | 3f236da6b82a | 31.03s | 6,274 LOC/s | 46 |
| tsc | 6.0.3 | 13.06s | 14,911 LOC/s | 65 |
| tsgo | 7.0.2 | 2.39s | 81,431 LOC/s | 65 |
| xtsc-native | 3f236da6b82a (AOT) | 10.68s | 18,232 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.93x FASTER than tsc; 2.82x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.07s**, **-10.1%** against `temurin26` on identical bytecode — 0.46× tsc, 2.54× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.38×** tsc and **12.98×** tsgo; tsc is **5.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.90s**.

Ahead-of-time (GraalVM native-image): **10.68s** — **0.82×** tsc, **4.47×** tsgo, **2.91×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
