# Bench run 20260920T140916Z

- **xtsc revision**: [`6c8bdea02d6a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/6c8bdea02d6a01eee2bb245cdf53c115fe5d0ed4)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 6c8bdea02d6a01eee2bb245cdf53c115fe5d0ed4

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6c8bdea02d6a | **4.43s** | 43,921 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6c8bdea02d6a | **4.19s** | 46,501 LOC/s | 46 |
| xtsc (cold JVM) | 6c8bdea02d6a | 21.18s | 9,190 LOC/s | 46 |
| tsc | 6.0.3 | 8.30s | 23,466 LOC/s | 65 |
| tsgo | 7.0.2 | 1.26s | 155,017 LOC/s | 65 |
| xtsc-native | 6c8bdea02d6a (AOT) | 7.88s | 24,692 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.87x FASTER than tsc; 3.53x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.19s**, **-5.5%** against `temurin26` on identical bytecode — 0.50× tsc, 3.33× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.55×** tsc and **16.87×** tsgo; tsc is **6.61×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.10s**.

Ahead-of-time (GraalVM native-image): **7.88s** — **0.95×** tsc, **6.28×** tsgo, **2.69×** the COLD JVM arm and **1.78×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6c8bdea02d6a | **5.18s** | 37,558 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6c8bdea02d6a | **4.94s** | 39,381 LOC/s | 46 |
| xtsc (cold JVM) | 6c8bdea02d6a | 23.51s | 8,280 LOC/s | 46 |
| tsc | 6.0.3 | 10.21s | 19,077 LOC/s | 65 |
| tsgo | 7.0.2 | 1.83s | 106,278 LOC/s | 65 |
| xtsc-native | 6c8bdea02d6a (AOT) | 8.63s | 22,568 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.97x FASTER than tsc; 2.83x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.94s**, **-4.6%** against `temurin26` on identical bytecode — 0.48× tsc, 2.70× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.30×** tsc and **12.83×** tsgo; tsc is **5.57×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.41s**.

Ahead-of-time (GraalVM native-image): **8.63s** — **0.85×** tsc, **4.71×** tsgo, **2.73×** the COLD JVM arm and **1.66×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
