# Bench run 20260824T094103Z

- **xtsc revision**: [`745217fda917`](https://github.com/xemantic/xemantic-typescript-compiler/commit/745217fda917e4d1a8ee2dc8a07bf69080d1a441)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 745217fda917e4d1a8ee2dc8a07bf69080d1a441

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 745217fda917 | **3.73s** | 52,170 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 745217fda917 | **3.53s** | 55,203 LOC/s | 46 |
| xtsc (cold JVM) | 745217fda917 | 22.17s | 8,780 LOC/s | 46 |
| tsc | 6.0.3 | 10.42s | 18,678 LOC/s | 65 |
| tsgo | 7.0.2 | 1.65s | 118,287 LOC/s | 65 |
| xtsc-native | 745217fda917 (AOT) | 5.75s | 33,855 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.79x FASTER than tsc; 2.27x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.53s**, **-5.5%** against `temurin26` on identical bytecode — 0.34× tsc, 2.14× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.13×** tsc and **13.47×** tsgo; tsc is **6.33×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.07s**.

Ahead-of-time (GraalVM native-image): **5.75s** — **0.55×** tsc, **3.49×** tsgo, **3.86×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 745217fda917 | **4.67s** | 41,727 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 745217fda917 | **4.21s** | 46,225 LOC/s | 46 |
| xtsc (cold JVM) | 745217fda917 | 22.94s | 8,488 LOC/s | 46 |
| tsc | 6.0.3 | 12.84s | 15,167 LOC/s | 65 |
| tsgo | 7.0.2 | 2.39s | 81,567 LOC/s | 65 |
| xtsc-native | 745217fda917 (AOT) | 7.11s | 27,403 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.75x FASTER than tsc; 1.95x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.21s**, **-9.7%** against `temurin26` on identical bytecode — 0.33× tsc, 1.76× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.79×** tsc and **9.61×** tsgo; tsc is **5.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.82s**.

Ahead-of-time (GraalVM native-image): **7.11s** — **0.55×** tsc, **2.98×** tsgo, **3.23×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
