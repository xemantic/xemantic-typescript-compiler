# Bench run 20260801T161307Z

- **xtsc revision**: [`2c3d4d28a1be`](https://github.com/xemantic/xemantic-typescript-compiler/commit/2c3d4d28a1be42728f2e3000633e13fbe9af1459)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 2c3d4d28a1be42728f2e3000633e13fbe9af1459

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 2c3d4d28a1be | 20.37s | 9,560 LOC/s | 46 |
| tsc | 6.0.3 | 8.31s | 23,429 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.26s | 154,648 LOC/s | 65 |
| xtsc-native | 2c3d4d28a1be (AOT) | 9.71s | 20,059 LOC/s | 46 |

Relative (wall): xtsc is **2.45×** tsc and **16.18×** tsgo; tsc is **6.60×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.28s**.

Ahead-of-time (GraalVM native-image): **9.71s**, **2.10×** the JVM arm, **1.17×** tsc, **7.71×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 2c3d4d28a1be | 22.72s | 8,571 LOC/s | 46 |
| tsc | 6.0.3 | 10.15s | 19,184 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.85s | 105,073 LOC/s | 65 |
| xtsc-native | 2c3d4d28a1be (AOT) | 11.77s | 16,538 LOC/s | 46 |

Relative (wall): xtsc is **2.24×** tsc and **12.26×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.63s**.

Ahead-of-time (GraalVM native-image): **11.77s**, **1.93×** the JVM arm, **1.16×** tsc, **6.35×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
