# Bench run 20260916T145005Z

- **xtsc revision**: [`7fcead17d734`](https://github.com/xemantic/xemantic-typescript-compiler/commit/7fcead17d73489371538f08b05cd6109952378e9)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 7fcead17d73489371538f08b05cd6109952378e9

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7fcead17d734 | **5.44s** | 35,771 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7fcead17d734 | **5.04s** | 38,646 LOC/s | 46 |
| xtsc (cold JVM) | 7fcead17d734 | 26.64s | 7,309 LOC/s | 46 |
| tsc | 6.0.3 | 9.96s | 19,546 LOC/s | 65 |
| tsgo | 7.0.2 | 1.56s | 125,129 LOC/s | 65 |
| xtsc-native | 7fcead17d734 (AOT) | 8.97s | 21,710 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.83x FASTER than tsc; 3.50x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.04s**, **-7.4%** against `temurin26` on identical bytecode — 0.51× tsc, 3.24× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.67×** tsc and **17.12×** tsgo; tsc is **6.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.52s**.

Ahead-of-time (GraalVM native-image): **8.97s** — **0.90×** tsc, **5.76×** tsgo, **2.97×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7fcead17d734 | **6.40s** | 30,412 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7fcead17d734 | **5.67s** | 34,308 LOC/s | 46 |
| xtsc (cold JVM) | 7fcead17d734 | 27.64s | 7,042 LOC/s | 46 |
| tsc | 6.0.3 | 12.32s | 15,808 LOC/s | 65 |
| tsgo | 7.0.2 | 2.24s | 86,765 LOC/s | 65 |
| xtsc-native | 7fcead17d734 (AOT) | 10.42s | 18,678 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.92x FASTER than tsc; 2.85x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.67s**, **-11.4%** against `temurin26` on identical bytecode — 0.46× tsc, 2.53× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.24×** tsc and **12.32×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.51s**.

Ahead-of-time (GraalVM native-image): **10.42s** — **0.85×** tsc, **4.65×** tsgo, **2.65×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
