# Bench run 20260802T093048Z

- **xtsc revision**: [`d26c69881696`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d26c69881696bbe8305d3ff0b71a86d150433003)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push d26c69881696bbe8305d3ff0b71a86d150433003

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | d26c69881696 | 27.51s | 7,077 LOC/s | 46 |
| tsc | 6.0.3 | 10.19s | 19,114 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.57s | 123,856 LOC/s | 65 |
| xtsc-native | d26c69881696 (AOT) | 10.76s | 18,103 LOC/s | 46 |

Relative (wall): xtsc is **2.70×** tsc and **17.50×** tsgo; tsc is **6.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.40s**.

Ahead-of-time (GraalVM native-image): **10.76s**, **2.56×** the JVM arm, **1.06×** tsc, **6.84×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | d26c69881696 | 29.15s | 6,680 LOC/s | 46 |
| tsc | 6.0.3 | 12.35s | 15,765 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.31s | 84,286 LOC/s | 65 |
| xtsc-native | d26c69881696 (AOT) | 13.60s | 14,319 LOC/s | 46 |

Relative (wall): xtsc is **2.36×** tsc and **12.62×** tsgo; tsc is **5.35×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.03s**.

Ahead-of-time (GraalVM native-image): **13.60s**, **2.14×** the JVM arm, **1.10×** tsc, **5.89×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
