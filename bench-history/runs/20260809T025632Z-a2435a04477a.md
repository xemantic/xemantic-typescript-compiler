# Bench run 20260809T025632Z

- **xtsc revision**: [`a2435a04477a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/a2435a04477a3330a673a7de1dc88f9b1b98396a)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push a2435a04477a3330a673a7de1dc88f9b1b98396a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | a2435a04477a | 24.75s | 7,866 LOC/s | 46 |
| tsc | 6.0.3 | 11.31s | 17,221 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.73s | 112,740 LOC/s | 65 |
| xtsc-native | a2435a04477a (AOT) | 10.68s | 18,233 LOC/s | 46 |

Relative (wall): xtsc is **2.19×** tsc and **14.33×** tsgo; tsc is **6.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.64s**.

Ahead-of-time (GraalVM native-image): **10.68s**, **2.32×** the JVM arm, **0.94×** tsc, **6.18×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | a2435a04477a | 29.39s | 6,625 LOC/s | 46 |
| tsc | 6.0.3 | 13.62s | 14,300 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.46s | 79,082 LOC/s | 65 |
| xtsc-native | a2435a04477a (AOT) | 13.41s | 14,522 LOC/s | 46 |

Relative (wall): xtsc is **2.16×** tsc and **11.94×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.25s**.

Ahead-of-time (GraalVM native-image): **13.41s**, **2.19×** the JVM arm, **0.98×** tsc, **5.45×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
