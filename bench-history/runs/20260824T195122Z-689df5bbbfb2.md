# Bench run 20260824T195122Z

- **xtsc revision**: [`689df5bbbfb2`](https://github.com/xemantic/xemantic-typescript-compiler/commit/689df5bbbfb2632eaedcfd5c32419b34b83033f8)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 689df5bbbfb2632eaedcfd5c32419b34b83033f8

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 689df5bbbfb2 | **4.30s** | 45,226 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 689df5bbbfb2 | **4.14s** | 46,995 LOC/s | 46 |
| xtsc (cold JVM) | 689df5bbbfb2 | 25.65s | 7,591 LOC/s | 46 |
| tsc | 6.0.3 | 11.73s | 16,595 LOC/s | 65 |
| tsgo | 7.0.2 | 1.88s | 103,399 LOC/s | 65 |
| xtsc-native | 689df5bbbfb2 (AOT) | 7.35s | 26,497 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.73x FASTER than tsc; 2.29x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.14s**, **-3.8%** against `temurin26` on identical bytecode — 0.35× tsc, 2.20× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.19×** tsc and **13.62×** tsgo; tsc is **6.23×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.52s**.

Ahead-of-time (GraalVM native-image): **7.35s** — **0.63×** tsc, **3.90×** tsgo, **3.49×** the COLD JVM arm and **1.71×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 689df5bbbfb2 | **5.67s** | 34,320 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 689df5bbbfb2 | **5.01s** | 38,893 LOC/s | 46 |
| xtsc (cold JVM) | 689df5bbbfb2 | 25.32s | 7,689 LOC/s | 46 |
| tsc | 6.0.3 | 14.29s | 13,624 LOC/s | 65 |
| tsgo | 7.0.2 | 2.63s | 74,087 LOC/s | 65 |
| xtsc-native | 689df5bbbfb2 (AOT) | 8.61s | 22,608 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.52x FASTER than tsc; 2.16x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.01s**, **-11.8%** against `temurin26` on identical bytecode — 0.35× tsc, 1.90× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.77×** tsc and **9.64×** tsgo; tsc is **5.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.19s**.

Ahead-of-time (GraalVM native-image): **8.61s** — **0.60×** tsc, **3.28×** tsgo, **2.94×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
