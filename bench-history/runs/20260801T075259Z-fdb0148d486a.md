# Bench run 20260801T075259Z

- **xtsc revision**: [`fdb0148d486a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/fdb0148d486a4f8825276e699cf8da890d9dfca2)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push fdb0148d486a4f8825276e699cf8da890d9dfca2

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | fdb0148d486a | 31.12s | 6,256 LOC/s | 46 |
| tsc | 6.0.3 | 11.33s | 17,186 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.70s | 114,463 LOC/s | 65 |
| xtsc-native | fdb0148d486a (AOT) | 13.47s | 14,451 LOC/s | 46 |

Relative (wall): xtsc is **2.75×** tsc and **18.30×** tsgo; tsc is **6.66×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **31.02s**.

Ahead-of-time (GraalVM native-image): **13.47s**, **2.31×** the JVM arm, **1.19×** tsc, **7.92×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | fdb0148d486a | 32.46s | 5,998 LOC/s | 46 |
| tsc | 6.0.3 | 13.50s | 14,426 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.48s | 78,603 LOC/s | 65 |
| xtsc-native | fdb0148d486a (AOT) | 13.67s | 14,238 LOC/s | 46 |

Relative (wall): xtsc is **2.40×** tsc and **13.10×** tsgo; tsc is **5.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **32.35s**.

Ahead-of-time (GraalVM native-image): **13.67s**, **2.37×** the JVM arm, **1.01×** tsc, **5.52×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
