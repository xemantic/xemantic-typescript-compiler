# Bench run 20260731T113202Z

- **xtsc revision**: [`81b99ee64479`](https://github.com/xemantic/xemantic-typescript-compiler/commit/81b99ee6447952dad847ad0073d19f3afc5ca9ff)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 81b99ee6447952dad847ad0073d19f3afc5ca9ff

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 81b99ee64479 | 27.29s | 7,135 LOC/s | 46 |
| tsc | 6.0.3 | 10.26s | 18,984 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.52s | 127,925 LOC/s | 65 |
| xtsc-native | 81b99ee64479 (AOT) | 11.88s | 16,390 LOC/s | 46 |

Relative (wall): xtsc is **2.66×** tsc and **17.93×** tsgo; tsc is **6.74×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.19s**.

Ahead-of-time (GraalVM native-image): **11.88s**, **2.30×** the JVM arm, **1.16×** tsc, **7.80×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 81b99ee64479 | 31.46s | 6,188 LOC/s | 46 |
| tsc | 6.0.3 | 12.88s | 15,122 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.32s | 83,959 LOC/s | 65 |
| xtsc-native | 81b99ee64479 (AOT) | 15.04s | 12,947 LOC/s | 46 |

Relative (wall): xtsc is **2.44×** tsc and **13.57×** tsgo; tsc is **5.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **31.35s**.

Ahead-of-time (GraalVM native-image): **15.04s**, **2.09×** the JVM arm, **1.17×** tsc, **6.48×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
