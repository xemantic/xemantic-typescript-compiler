# Bench run 20260916T011706Z

- **xtsc revision**: [`3cf391eef10a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/3cf391eef10acd2c99d318f095b923257742a632)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 3cf391eef10acd2c99d318f095b923257742a632

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3cf391eef10a | **5.63s** | 34,607 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3cf391eef10a | **5.23s** | 37,249 LOC/s | 46 |
| xtsc (cold JVM) | 3cf391eef10a | 24.55s | 7,930 LOC/s | 46 |
| tsc | 6.0.3 | 10.61s | 18,343 LOC/s | 65 |
| tsgo | 7.0.2 | 1.59s | 122,377 LOC/s | 65 |
| xtsc-native | 3cf391eef10a (AOT) | 9.62s | 20,239 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.89x FASTER than tsc; 3.54x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.23s**, **-7.1%** against `temurin26` on identical bytecode — 0.49× tsc, 3.29× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.31×** tsc and **15.43×** tsgo; tsc is **6.67×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.44s**.

Ahead-of-time (GraalVM native-image): **9.62s** — **0.91×** tsc, **6.05×** tsgo, **2.55×** the COLD JVM arm and **1.71×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3cf391eef10a | **6.84s** | 28,444 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3cf391eef10a | **5.98s** | 32,542 LOC/s | 46 |
| xtsc (cold JVM) | 3cf391eef10a | 30.92s | 6,296 LOC/s | 46 |
| tsc | 6.0.3 | 12.59s | 15,461 LOC/s | 65 |
| tsgo | 7.0.2 | 2.28s | 85,545 LOC/s | 65 |
| xtsc-native | 3cf391eef10a (AOT) | 10.70s | 18,189 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.84x FASTER than tsc; 3.01x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.98s**, **-12.6%** against `temurin26` on identical bytecode — 0.48× tsc, 2.63× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.46×** tsc and **13.59×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.80s**.

Ahead-of-time (GraalVM native-image): **10.70s** — **0.85×** tsc, **4.70×** tsgo, **2.89×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
