# Bench run 20260909T105626Z

- **xtsc revision**: [`302890b4c47b`](https://github.com/xemantic/xemantic-typescript-compiler/commit/302890b4c47b6d2422680ed010b98941383053d8)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 302890b4c47b6d2422680ed010b98941383053d8

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 302890b4c47b | **5.82s** | 33,448 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 302890b4c47b | **5.25s** | 37,100 LOC/s | 46 |
| xtsc (cold JVM) | 302890b4c47b | 27.23s | 7,150 LOC/s | 46 |
| tsc | 6.0.3 | 10.91s | 17,847 LOC/s | 65 |
| tsgo | 7.0.2 | 1.77s | 110,001 LOC/s | 65 |
| xtsc-native | 302890b4c47b (AOT) | 9.18s | 21,211 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.87x FASTER than tsc; 3.29x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.25s**, **-9.8%** against `temurin26` on identical bytecode — 0.48× tsc, 2.96× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.50×** tsc and **15.38×** tsgo; tsc is **6.16×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.12s**.

Ahead-of-time (GraalVM native-image): **9.18s** — **0.84×** tsc, **5.19×** tsgo, **2.97×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 302890b4c47b | **6.75s** | 28,831 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 302890b4c47b | **5.84s** | 33,350 LOC/s | 46 |
| xtsc (cold JVM) | 302890b4c47b | 32.30s | 6,028 LOC/s | 46 |
| tsc | 6.0.3 | 13.56s | 14,354 LOC/s | 65 |
| tsgo | 7.0.2 | 2.42s | 80,355 LOC/s | 65 |
| xtsc-native | 302890b4c47b (AOT) | 10.48s | 18,583 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.01x FASTER than tsc; 2.79x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.84s**, **-13.5%** against `temurin26` on identical bytecode — 0.43× tsc, 2.41× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.38×** tsc and **13.33×** tsgo; tsc is **5.60×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **32.16s**.

Ahead-of-time (GraalVM native-image): **10.48s** — **0.77×** tsc, **4.32×** tsgo, **3.08×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
