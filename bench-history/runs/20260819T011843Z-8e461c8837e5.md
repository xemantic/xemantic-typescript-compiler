# Bench run 20260819T011843Z

- **xtsc revision**: [`8e461c8837e5`](https://github.com/xemantic/xemantic-typescript-compiler/commit/8e461c8837e53f101a4179ba5fbc01b8d46ef833)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 8e461c8837e53f101a4179ba5fbc01b8d46ef833

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8e461c8837e5 | **3.73s** | 52,226 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8e461c8837e5 | **3.53s** | 55,172 LOC/s | 46 |
| xtsc (cold JVM) | 8e461c8837e5 | 22.39s | 8,697 LOC/s | 46 |
| tsc | 6.0.3 | 10.12s | 19,248 LOC/s | 65 |
| tsgo | 7.0.2 | 1.55s | 125,939 LOC/s | 65 |
| xtsc-native | 8e461c8837e5 (AOT) | 5.85s | 33,259 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.71x FASTER than tsc; 2.41x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.53s**, **-5.3%** against `temurin26` on identical bytecode — 0.35× tsc, 2.28× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.21×** tsc and **14.48×** tsgo; tsc is **6.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.27s**.

Ahead-of-time (GraalVM native-image): **5.85s** — **0.58×** tsc, **3.79×** tsgo, **3.82×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8e461c8837e5 | **4.76s** | 40,938 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8e461c8837e5 | **4.20s** | 46,401 LOC/s | 46 |
| xtsc (cold JVM) | 8e461c8837e5 | 24.80s | 7,851 LOC/s | 46 |
| tsc | 6.0.3 | 12.39s | 15,713 LOC/s | 65 |
| tsgo | 7.0.2 | 2.23s | 87,232 LOC/s | 65 |
| xtsc-native | 8e461c8837e5 (AOT) | 6.99s | 27,850 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.61x FASTER than tsc; 2.13x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.20s**, **-11.8%** against `temurin26` on identical bytecode — 0.34× tsc, 1.88× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.00×** tsc and **11.11×** tsgo; tsc is **5.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.67s**.

Ahead-of-time (GraalVM native-image): **6.99s** — **0.56×** tsc, **3.13×** tsgo, **3.55×** the COLD JVM arm and **1.47×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
