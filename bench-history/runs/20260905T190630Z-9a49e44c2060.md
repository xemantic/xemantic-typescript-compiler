# Bench run 20260905T190630Z

- **xtsc revision**: [`9a49e44c2060`](https://github.com/xemantic/xemantic-typescript-compiler/commit/9a49e44c20609619e2992916758041d7e82e25ee)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 9a49e44c20609619e2992916758041d7e82e25ee

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9a49e44c2060 | **14.56s** | 13,373 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9a49e44c2060 | **14.95s** | 13,025 LOC/s | 46 |
| xtsc (cold JVM) | 9a49e44c2060 | 35.87s | 5,427 LOC/s | 46 |
| tsc | 6.0.3 | 11.27s | 17,277 LOC/s | 65 |
| tsgo | 7.0.2 | 1.75s | 111,194 LOC/s | 65 |
| xtsc-native | 9a49e44c2060 (AOT) | 23.79s | 8,184 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.29x tsc — parity at 1.00x; 8.31x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **14.95s**, **+2.7%** against `temurin26` on identical bytecode — 1.33× tsc, 8.54× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.18×** tsc and **20.49×** tsgo; tsc is **6.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **35.74s**.

Ahead-of-time (GraalVM native-image): **23.79s** — **2.11×** tsc, **13.59×** tsgo, **1.51×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9a49e44c2060 | **15.37s** | 12,671 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9a49e44c2060 | **14.78s** | 13,176 LOC/s | 46 |
| xtsc (cold JVM) | 9a49e44c2060 | 35.74s | 5,447 LOC/s | 46 |
| tsc | 6.0.3 | 13.48s | 14,445 LOC/s | 65 |
| tsgo | 7.0.2 | 2.48s | 78,414 LOC/s | 65 |
| xtsc-native | 9a49e44c2060 (AOT) | 24.73s | 7,873 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.14x tsc — parity at 1.00x; 6.19x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **14.78s**, **-3.8%** against `temurin26` on identical bytecode — 1.10× tsc, 5.95× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.65×** tsc and **14.40×** tsgo; tsc is **5.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **35.61s**.

Ahead-of-time (GraalVM native-image): **24.73s** — **1.83×** tsc, **9.96×** tsgo, **1.45×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
