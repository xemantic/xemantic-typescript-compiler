# Bench run 20260801T070453Z

- **xtsc revision**: [`8308601b79c6`](https://github.com/xemantic/xemantic-typescript-compiler/commit/8308601b79c6ad3a02ba8ae6fa74acc4bd79db94)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 8308601b79c6ad3a02ba8ae6fa74acc4bd79db94

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 8308601b79c6 | 28.41s | 6,853 LOC/s | 46 |
| tsc | 6.0.3 | 10.46s | 18,622 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.59s | 122,685 LOC/s | 65 |
| xtsc-native | 8308601b79c6 (AOT) | 13.81s | 14,098 LOC/s | 46 |

Relative (wall): xtsc is **2.72×** tsc and **17.90×** tsgo; tsc is **6.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.30s**.

Ahead-of-time (GraalVM native-image): **13.81s**, **2.06×** the JVM arm, **1.32×** tsc, **8.70×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 8308601b79c6 | 32.20s | 6,045 LOC/s | 46 |
| tsc | 6.0.3 | 12.82s | 15,184 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.33s | 83,455 LOC/s | 65 |
| xtsc-native | 8308601b79c6 (AOT) | 15.18s | 12,830 LOC/s | 46 |

Relative (wall): xtsc is **2.51×** tsc and **13.80×** tsgo; tsc is **5.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **32.10s**.

Ahead-of-time (GraalVM native-image): **15.18s**, **2.12×** the JVM arm, **1.18×** tsc, **6.50×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
