# Bench run 20260829T122226Z

- **xtsc revision**: [`fda4b72519c4`](https://github.com/xemantic/xemantic-typescript-compiler/commit/fda4b72519c42aab957d98a9966661bca11e9b84)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push fda4b72519c42aab957d98a9966661bca11e9b84

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | fda4b72519c4 | **3.90s** | 49,936 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | fda4b72519c4 | **3.87s** | 50,284 LOC/s | 46 |
| xtsc (cold JVM) | fda4b72519c4 | 25.60s | 7,605 LOC/s | 46 |
| tsc | 6.0.3 | 10.25s | 18,989 LOC/s | 65 |
| tsgo | 7.0.2 | 1.66s | 117,008 LOC/s | 65 |
| xtsc-native | fda4b72519c4 (AOT) | 6.45s | 30,205 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.63x FASTER than tsc; 2.34x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.87s**, **-0.7%** against `temurin26` on identical bytecode — 0.38× tsc, 2.33× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.50×** tsc and **15.39×** tsgo; tsc is **6.16×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.50s**.

Ahead-of-time (GraalVM native-image): **6.45s** — **0.63×** tsc, **3.87×** tsgo, **3.97×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | fda4b72519c4 | **4.78s** | 40,715 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | fda4b72519c4 | **4.49s** | 43,392 LOC/s | 46 |
| xtsc (cold JVM) | fda4b72519c4 | 24.87s | 7,829 LOC/s | 46 |
| tsc | 6.0.3 | 12.70s | 15,332 LOC/s | 65 |
| tsgo | 7.0.2 | 2.42s | 80,355 LOC/s | 65 |
| xtsc-native | fda4b72519c4 (AOT) | 7.91s | 24,614 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.66x FASTER than tsc; 1.97x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.49s**, **-6.2%** against `temurin26` on identical bytecode — 0.35× tsc, 1.85× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.96×** tsc and **10.26×** tsgo; tsc is **5.24×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.75s**.

Ahead-of-time (GraalVM native-image): **7.91s** — **0.62×** tsc, **3.26×** tsgo, **3.14×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
