# Bench run 20260924T100222Z

- **xtsc revision**: [`e6aac3912058`](https://github.com/xemantic/xemantic-typescript-compiler/commit/e6aac39120584e59673d2e29cdc97672d4403bcb)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push e6aac39120584e59673d2e29cdc97672d4403bcb

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e6aac3912058 | **6.03s** | 32,310 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e6aac3912058 | **5.64s** | 34,533 LOC/s | 46 |
| xtsc (cold JVM) | e6aac3912058 | 28.54s | 6,822 LOC/s | 46 |
| tsc | 6.0.3 | 11.19s | 17,396 LOC/s | 65 |
| tsgo | 7.0.2 | 1.76s | 110,815 LOC/s | 65 |
| xtsc-native | e6aac3912058 (AOT) | 9.89s | 19,680 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.86x FASTER than tsc; 3.43x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.64s**, **-6.4%** against `temurin26` on identical bytecode — 0.50× tsc, 3.21× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.55×** tsc and **16.24×** tsgo; tsc is **6.37×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.40s**.

Ahead-of-time (GraalVM native-image): **9.89s** — **0.88×** tsc, **5.63×** tsgo, **2.88×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e6aac3912058 | **7.02s** | 27,719 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e6aac3912058 | **6.38s** | 30,503 LOC/s | 46 |
| xtsc (cold JVM) | e6aac3912058 | 32.65s | 5,964 LOC/s | 46 |
| tsc | 6.0.3 | 13.52s | 14,404 LOC/s | 65 |
| tsgo | 7.0.2 | 2.49s | 78,287 LOC/s | 65 |
| xtsc-native | e6aac3912058 (AOT) | 11.44s | 17,026 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.92x FASTER than tsc; 2.82x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.38s**, **-9.1%** against `temurin26` on identical bytecode — 0.47× tsc, 2.57× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.42×** tsc and **13.13×** tsgo; tsc is **5.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **32.50s**.

Ahead-of-time (GraalVM native-image): **11.44s** — **0.85×** tsc, **4.60×** tsgo, **2.85×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
