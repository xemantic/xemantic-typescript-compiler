# Bench run 20260912T113354Z

- **xtsc revision**: [`f847f6ed674a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f847f6ed674aa39f979528a123d592e39f40ec13)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push f847f6ed674aa39f979528a123d592e39f40ec13

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f847f6ed674a | **3.71s** | 52,409 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f847f6ed674a | **3.48s** | 55,981 LOC/s | 46 |
| xtsc (cold JVM) | f847f6ed674a | 20.37s | 9,557 LOC/s | 46 |
| tsc | 6.0.3 | 7.36s | 26,461 LOC/s | 65 |
| tsgo | 7.0.2 | 1.11s | 174,934 LOC/s | 65 |
| xtsc-native | f847f6ed674a (AOT) | 5.68s | 34,254 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.98x FASTER than tsc; 3.34x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.48s**, **-6.4%** against `temurin26` on identical bytecode — 0.47× tsc, 3.12× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.77×** tsc and **18.30×** tsgo; tsc is **6.61×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.29s**.

Ahead-of-time (GraalVM native-image): **5.68s** — **0.77×** tsc, **5.11×** tsgo, **3.58×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f847f6ed674a | **4.34s** | 44,882 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f847f6ed674a | **4.28s** | 45,523 LOC/s | 46 |
| xtsc (cold JVM) | f847f6ed674a | 21.54s | 9,038 LOC/s | 46 |
| tsc | 6.0.3 | 8.88s | 21,928 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,413 LOC/s | 65 |
| xtsc-native | f847f6ed674a (AOT) | 7.00s | 27,806 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.05x FASTER than tsc; 2.57x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.28s**, **-1.4%** against `temurin26` on identical bytecode — 0.48× tsc, 2.54× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.43×** tsc and **12.77×** tsgo; tsc is **5.26×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.45s**.

Ahead-of-time (GraalVM native-image): **7.00s** — **0.79×** tsc, **4.15×** tsgo, **3.08×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
