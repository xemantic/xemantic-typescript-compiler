# Bench run 20260818T070734Z

- **xtsc revision**: [`f405afdf7964`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f405afdf796460f15984171395a1969d4f89532b)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push f405afdf796460f15984171395a1969d4f89532b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f405afdf7964 | **3.48s** | 55,884 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f405afdf7964 | **3.26s** | 59,724 LOC/s | 46 |
| xtsc (cold JVM) | f405afdf7964 | 21.06s | 9,243 LOC/s | 46 |
| tsc | 6.0.3 | 10.06s | 19,361 LOC/s | 65 |
| tsgo | 7.0.2 | 1.62s | 120,409 LOC/s | 65 |
| xtsc-native | f405afdf7964 (AOT) | 5.39s | 36,096 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.89x FASTER than tsc; 2.15x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.26s**, **-6.4%** against `temurin26` on identical bytecode — 0.32× tsc, 2.02× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.09×** tsc and **13.03×** tsgo; tsc is **6.22×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.96s**.

Ahead-of-time (GraalVM native-image): **5.39s** — **0.54×** tsc, **3.34×** tsgo, **3.91×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f405afdf7964 | **4.47s** | 43,518 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f405afdf7964 | **4.12s** | 47,280 LOC/s | 46 |
| xtsc (cold JVM) | f405afdf7964 | 23.71s | 8,211 LOC/s | 46 |
| tsc | 6.0.3 | 12.06s | 16,143 LOC/s | 65 |
| tsgo | 7.0.2 | 2.28s | 85,246 LOC/s | 65 |
| xtsc-native | f405afdf7964 (AOT) | 6.92s | 28,123 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.70x FASTER than tsc; 1.96x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.12s**, **-8.0%** against `temurin26` on identical bytecode — 0.34× tsc, 1.80× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.97×** tsc and **10.38×** tsgo; tsc is **5.28×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.57s**.

Ahead-of-time (GraalVM native-image): **6.92s** — **0.57×** tsc, **3.03×** tsgo, **3.42×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
