# Bench run 20260924T001153Z

- **xtsc revision**: [`42634820a507`](https://github.com/xemantic/xemantic-typescript-compiler/commit/42634820a507f88352d7fd6bc57401e787032610)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 42634820a507f88352d7fd6bc57401e787032610

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 42634820a507 | **4.46s** | 43,616 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 42634820a507 | **4.15s** | 46,927 LOC/s | 46 |
| xtsc (cold JVM) | 42634820a507 | 22.00s | 8,848 LOC/s | 46 |
| tsc | 6.0.3 | 8.05s | 24,180 LOC/s | 65 |
| tsgo | 7.0.2 | 1.21s | 161,578 LOC/s | 65 |
| xtsc-native | 42634820a507 (AOT) | 7.37s | 26,407 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.80x FASTER than tsc; 3.70x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.15s**, **-7.1%** against `temurin26` on identical bytecode — 0.52× tsc, 3.44× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.73×** tsc and **18.26×** tsgo; tsc is **6.68×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.91s**.

Ahead-of-time (GraalVM native-image): **7.37s** — **0.92×** tsc, **6.12×** tsgo, **2.98×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 42634820a507 | **5.00s** | 38,971 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 42634820a507 | **4.71s** | 41,294 LOC/s | 46 |
| xtsc (cold JVM) | 42634820a507 | 23.19s | 8,397 LOC/s | 46 |
| tsc | 6.0.3 | 9.71s | 20,053 LOC/s | 65 |
| tsgo | 7.0.2 | 1.75s | 111,385 LOC/s | 65 |
| xtsc-native | 42634820a507 (AOT) | 8.37s | 23,267 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.94x FASTER than tsc; 2.86x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.71s**, **-5.6%** against `temurin26` on identical bytecode — 0.49× tsc, 2.70× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.39×** tsc and **13.26×** tsgo; tsc is **5.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.09s**.

Ahead-of-time (GraalVM native-image): **8.37s** — **0.86×** tsc, **4.79×** tsgo, **2.77×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
