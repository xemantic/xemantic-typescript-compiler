# Bench run 20260922T152414Z

- **xtsc revision**: [`db951cd7e64f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/db951cd7e64f88f5d68db3fe4b79da3952aaa192)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push db951cd7e64f88f5d68db3fe4b79da3952aaa192

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | db951cd7e64f | **4.43s** | 43,911 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | db951cd7e64f | **4.15s** | 46,882 LOC/s | 46 |
| xtsc (cold JVM) | db951cd7e64f | 21.66s | 8,988 LOC/s | 46 |
| tsc | 6.0.3 | 8.00s | 24,325 LOC/s | 65 |
| tsgo | 7.0.2 | 1.21s | 160,512 LOC/s | 65 |
| xtsc-native | db951cd7e64f (AOT) | 7.34s | 26,540 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.81x FASTER than tsc; 3.66x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.15s**, **-6.3%** against `temurin26` on identical bytecode — 0.52× tsc, 3.42× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.71×** tsc and **17.86×** tsgo; tsc is **6.60×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.57s**.

Ahead-of-time (GraalVM native-image): **7.34s** — **0.92×** tsc, **6.05×** tsgo, **2.95×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | db951cd7e64f | **5.17s** | 37,696 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | db951cd7e64f | **4.92s** | 39,549 LOC/s | 46 |
| xtsc (cold JVM) | db951cd7e64f | 23.66s | 8,227 LOC/s | 46 |
| tsc | 6.0.3 | 10.13s | 19,226 LOC/s | 65 |
| tsgo | 7.0.2 | 1.85s | 105,187 LOC/s | 65 |
| xtsc-native | db951cd7e64f (AOT) | 8.92s | 21,837 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.96x FASTER than tsc; 2.79x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.92s**, **-4.7%** against `temurin26` on identical bytecode — 0.49× tsc, 2.66× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.34×** tsc and **12.78×** tsgo; tsc is **5.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.56s**.

Ahead-of-time (GraalVM native-image): **8.92s** — **0.88×** tsc, **4.82×** tsgo, **2.65×** the COLD JVM arm and **1.73×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
