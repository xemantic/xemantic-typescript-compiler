# Bench run 20260801T112744Z

- **xtsc revision**: [`65c3158551e7`](https://github.com/xemantic/xemantic-typescript-compiler/commit/65c3158551e7a5f5e793ea5bf1cc68e4823a186f)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 65c3158551e7a5f5e793ea5bf1cc68e4823a186f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 65c3158551e7 | 29.37s | 6,630 LOC/s | 46 |
| tsc | 6.0.3 | 10.68s | 18,223 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.65s | 118,001 LOC/s | 65 |
| xtsc-native | 65c3158551e7 (AOT) | 12.09s | 16,111 LOC/s | 46 |

Relative (wall): xtsc is **2.75×** tsc and **17.80×** tsgo; tsc is **6.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.25s**.

Ahead-of-time (GraalVM native-image): **12.09s**, **2.43×** the JVM arm, **1.13×** tsc, **7.32×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 65c3158551e7 | 31.66s | 6,150 LOC/s | 46 |
| tsc | 6.0.3 | 12.86s | 15,141 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.35s | 83,028 LOC/s | 65 |
| xtsc-native | 65c3158551e7 (AOT) | 13.87s | 14,042 LOC/s | 46 |

Relative (wall): xtsc is **2.46×** tsc and **13.50×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **31.54s**.

Ahead-of-time (GraalVM native-image): **13.87s**, **2.28×** the JVM arm, **1.08×** tsc, **5.91×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
