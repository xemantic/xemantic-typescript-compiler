# Bench run 20260808T154858Z

- **xtsc revision**: [`d1fbe9633de6`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d1fbe9633de6dd4c127268cc1984dfa278cb82c7)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push d1fbe9633de6dd4c127268cc1984dfa278cb82c7

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | d1fbe9633de6 | 27.22s | 7,153 LOC/s | 46 |
| tsc | 6.0.3 | 11.16s | 17,452 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.74s | 112,026 LOC/s | 65 |
| xtsc-native | d1fbe9633de6 (AOT) | 11.81s | 16,484 LOC/s | 46 |

Relative (wall): xtsc is **2.44×** tsc and **15.66×** tsgo; tsc is **6.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.10s**.

Ahead-of-time (GraalVM native-image): **11.81s**, **2.30×** the JVM arm, **1.06×** tsc, **6.80×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | d1fbe9633de6 | 29.81s | 6,532 LOC/s | 46 |
| tsc | 6.0.3 | 13.51s | 14,409 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.46s | 79,276 LOC/s | 65 |
| xtsc-native | d1fbe9633de6 (AOT) | 13.69s | 14,221 LOC/s | 46 |

Relative (wall): xtsc is **2.21×** tsc and **12.14×** tsgo; tsc is **5.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.68s**.

Ahead-of-time (GraalVM native-image): **13.69s**, **2.18×** the JVM arm, **1.01×** tsc, **5.57×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
