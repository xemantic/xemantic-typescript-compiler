# Bench run 20260911T183336Z

- **xtsc revision**: [`df592f2e974d`](https://github.com/xemantic/xemantic-typescript-compiler/commit/df592f2e974d01bd9b48b3df4be4f30bc3f567e4)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push df592f2e974d01bd9b48b3df4be4f30bc3f567e4

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | df592f2e974d | **5.33s** | 36,536 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | df592f2e974d | **5.12s** | 38,005 LOC/s | 46 |
| xtsc (cold JVM) | df592f2e974d | 27.01s | 7,207 LOC/s | 46 |
| tsc | 6.0.3 | 10.82s | 17,991 LOC/s | 65 |
| tsgo | 7.0.2 | 1.66s | 117,502 LOC/s | 65 |
| xtsc-native | df592f2e974d (AOT) | 8.62s | 22,595 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.03x FASTER than tsc; 3.22x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.12s**, **-3.9%** against `temurin26` on identical bytecode — 0.47× tsc, 3.09× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.50×** tsc and **16.30×** tsgo; tsc is **6.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.87s**.

Ahead-of-time (GraalVM native-image): **8.62s** — **0.80×** tsc, **5.20×** tsgo, **3.13×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | df592f2e974d | **6.50s** | 29,949 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | df592f2e974d | **5.71s** | 34,092 LOC/s | 46 |
| xtsc (cold JVM) | df592f2e974d | 28.57s | 6,814 LOC/s | 46 |
| tsc | 6.0.3 | 13.13s | 14,829 LOC/s | 65 |
| tsgo | 7.0.2 | 2.40s | 81,159 LOC/s | 65 |
| xtsc-native | df592f2e974d (AOT) | 9.90s | 19,660 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.02x FASTER than tsc; 2.71x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.71s**, **-12.2%** against `temurin26` on identical bytecode — 0.43× tsc, 2.38× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.18×** tsc and **11.91×** tsgo; tsc is **5.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.45s**.

Ahead-of-time (GraalVM native-image): **9.90s** — **0.75×** tsc, **4.13×** tsgo, **2.88×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
