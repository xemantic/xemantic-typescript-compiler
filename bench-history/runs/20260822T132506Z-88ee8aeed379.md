# Bench run 20260822T132506Z

- **xtsc revision**: [`88ee8aeed379`](https://github.com/xemantic/xemantic-typescript-compiler/commit/88ee8aeed3799df3e85bc8b986b5231692e50238)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 88ee8aeed3799df3e85bc8b986b5231692e50238

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 88ee8aeed379 | **2.84s** | 68,629 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 88ee8aeed379 | **2.73s** | 71,397 LOC/s | 46 |
| xtsc (cold JVM) | 88ee8aeed379 | 17.83s | 10,917 LOC/s | 46 |
| tsc | 6.0.3 | 9.02s | 21,587 LOC/s | 65 |
| tsgo | 7.0.2 | 1.31s | 148,627 LOC/s | 65 |
| xtsc-native | 88ee8aeed379 (AOT) | 4.60s | 42,363 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 3.18x FASTER than tsc; 2.17x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.73s**, **-3.9%** against `temurin26` on identical bytecode — 0.30× tsc, 2.08× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.98×** tsc and **13.61×** tsgo; tsc is **6.88×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.75s**.

Ahead-of-time (GraalVM native-image): **4.60s** — **0.51×** tsc, **3.51×** tsgo, **3.88×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 88ee8aeed379 | **3.63s** | 53,622 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 88ee8aeed379 | **3.39s** | 57,468 LOC/s | 46 |
| xtsc (cold JVM) | 88ee8aeed379 | 22.29s | 8,736 LOC/s | 46 |
| tsc | 6.0.3 | 10.24s | 19,013 LOC/s | 65 |
| tsgo | 7.0.2 | 1.93s | 101,091 LOC/s | 65 |
| xtsc-native | 88ee8aeed379 (AOT) | 5.38s | 36,210 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.82x FASTER than tsc; 1.89x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.39s**, **-6.7%** against `temurin26` on identical bytecode — 0.33× tsc, 1.76× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.18×** tsc and **11.57×** tsgo; tsc is **5.32×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.19s**.

Ahead-of-time (GraalVM native-image): **5.38s** — **0.53×** tsc, **2.79×** tsgo, **4.14×** the COLD JVM arm and **1.48×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
