# Bench run 20260909T001731Z

- **xtsc revision**: [`41037c0b22ba`](https://github.com/xemantic/xemantic-typescript-compiler/commit/41037c0b22ba21d2576ac1e21c944c2b8e53da9e)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 41037c0b22ba21d2576ac1e21c944c2b8e53da9e

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 41037c0b22ba | **5.17s** | 37,681 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 41037c0b22ba | **4.86s** | 40,070 LOC/s | 46 |
| xtsc (cold JVM) | 41037c0b22ba | 25.18s | 7,732 LOC/s | 46 |
| tsc | 6.0.3 | 10.25s | 18,999 LOC/s | 65 |
| tsgo | 7.0.2 | 1.54s | 126,676 LOC/s | 65 |
| xtsc-native | 41037c0b22ba (AOT) | 8.73s | 22,307 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.98x FASTER than tsc; 3.36x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.86s**, **-6.0%** against `temurin26` on identical bytecode — 0.47× tsc, 3.16× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.46×** tsc and **16.38×** tsgo; tsc is **6.67×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.06s**.

Ahead-of-time (GraalVM native-image): **8.73s** — **0.85×** tsc, **5.68×** tsgo, **2.88×** the COLD JVM arm and **1.69×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 41037c0b22ba | **6.41s** | 30,393 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 41037c0b22ba | **5.88s** | 33,129 LOC/s | 46 |
| xtsc (cold JVM) | 41037c0b22ba | 29.12s | 6,686 LOC/s | 46 |
| tsc | 6.0.3 | 13.44s | 14,486 LOC/s | 65 |
| tsgo | 7.0.2 | 2.41s | 80,655 LOC/s | 65 |
| xtsc-native | 41037c0b22ba (AOT) | 10.95s | 17,784 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.10x FASTER than tsc; 2.65x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.88s**, **-8.3%** against `temurin26` on identical bytecode — 0.44× tsc, 2.43× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.17×** tsc and **12.06×** tsgo; tsc is **5.57×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.00s**.

Ahead-of-time (GraalVM native-image): **10.95s** — **0.81×** tsc, **4.54×** tsgo, **2.66×** the COLD JVM arm and **1.71×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
