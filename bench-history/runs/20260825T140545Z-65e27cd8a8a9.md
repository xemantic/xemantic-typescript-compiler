# Bench run 20260825T140545Z

- **xtsc revision**: [`65e27cd8a8a9`](https://github.com/xemantic/xemantic-typescript-compiler/commit/65e27cd8a8a96ad562e01fc1d1ec6f6b804f1530)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 65e27cd8a8a96ad562e01fc1d1ec6f6b804f1530

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 65e27cd8a8a9 | **4.30s** | 45,269 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 65e27cd8a8a9 | **4.20s** | 46,379 LOC/s | 46 |
| xtsc (cold JVM) | 65e27cd8a8a9 | 24.05s | 8,096 LOC/s | 46 |
| tsc | 6.0.3 | 11.34s | 17,171 LOC/s | 65 |
| tsgo | 7.0.2 | 1.76s | 110,563 LOC/s | 65 |
| xtsc-native | 65e27cd8a8a9 (AOT) | 7.20s | 27,026 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.64x FASTER than tsc; 2.44x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.20s**, **-2.4%** against `temurin26` on identical bytecode — 0.37× tsc, 2.38× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.12×** tsc and **13.66×** tsgo; tsc is **6.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.94s**.

Ahead-of-time (GraalVM native-image): **7.20s** — **0.64×** tsc, **4.09×** tsgo, **3.34×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 65e27cd8a8a9 | **5.24s** | 37,171 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 65e27cd8a8a9 | **4.66s** | 41,781 LOC/s | 46 |
| xtsc (cold JVM) | 65e27cd8a8a9 | 27.01s | 7,208 LOC/s | 46 |
| tsc | 6.0.3 | 14.03s | 13,879 LOC/s | 65 |
| tsgo | 7.0.2 | 2.59s | 75,290 LOC/s | 65 |
| xtsc-native | 65e27cd8a8a9 (AOT) | 8.51s | 22,887 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.68x FASTER than tsc; 2.03x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.66s**, **-11.0%** against `temurin26` on identical bytecode — 0.33× tsc, 1.80× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.93×** tsc and **10.45×** tsgo; tsc is **5.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.89s**.

Ahead-of-time (GraalVM native-image): **8.51s** — **0.61×** tsc, **3.29×** tsgo, **3.18×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
