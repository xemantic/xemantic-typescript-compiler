# Bench run 20260923T204034Z

- **xtsc revision**: [`f8bed9f84b36`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f8bed9f84b360b3e15380988b1c6b710f0e7ab64)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push f8bed9f84b360b3e15380988b1c6b710f0e7ab64

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f8bed9f84b36 | **5.92s** | 32,861 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f8bed9f84b36 | **5.65s** | 34,460 LOC/s | 46 |
| xtsc (cold JVM) | f8bed9f84b36 | 29.75s | 6,543 LOC/s | 46 |
| tsc | 6.0.3 | 10.88s | 17,888 LOC/s | 65 |
| tsgo | 7.0.2 | 1.74s | 111,833 LOC/s | 65 |
| xtsc-native | f8bed9f84b36 (AOT) | 9.46s | 20,575 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.84x FASTER than tsc; 3.40x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.65s**, **-4.6%** against `temurin26` on identical bytecode — 0.52× tsc, 3.25× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.73×** tsc and **17.09×** tsgo; tsc is **6.25×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.61s**.

Ahead-of-time (GraalVM native-image): **9.46s** — **0.87×** tsc, **5.44×** tsgo, **3.14×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f8bed9f84b36 | **7.01s** | 27,786 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f8bed9f84b36 | **6.15s** | 31,664 LOC/s | 46 |
| xtsc (cold JVM) | f8bed9f84b36 | 31.94s | 6,095 LOC/s | 46 |
| tsc | 6.0.3 | 13.48s | 14,438 LOC/s | 65 |
| tsgo | 7.0.2 | 2.46s | 78,986 LOC/s | 65 |
| xtsc-native | f8bed9f84b36 (AOT) | 10.91s | 17,842 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.92x FASTER than tsc; 2.84x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.15s**, **-12.2%** against `temurin26` on identical bytecode — 0.46× tsc, 2.49× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.37×** tsc and **12.96×** tsgo; tsc is **5.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **31.81s**.

Ahead-of-time (GraalVM native-image): **10.91s** — **0.81×** tsc, **4.43×** tsgo, **2.93×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
