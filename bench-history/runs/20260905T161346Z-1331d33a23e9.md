# Bench run 20260905T161346Z

- **xtsc revision**: [`1331d33a23e9`](https://github.com/xemantic/xemantic-typescript-compiler/commit/1331d33a23e99c25311b51b80f062ee6c841b862)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 1331d33a23e99c25311b51b80f062ee6c841b862

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1331d33a23e9 | **2.68s** | 72,568 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1331d33a23e9 | **2.75s** | 70,929 LOC/s | 46 |
| xtsc (cold JVM) | 1331d33a23e9 | 17.05s | 11,417 LOC/s | 46 |
| tsc | 6.0.3 | 6.61s | 29,442 LOC/s | 65 |
| tsgo | 7.0.2 | 1.00s | 194,702 LOC/s | 65 |
| xtsc-native | 1331d33a23e9 (AOT) | 4.96s | 39,278 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.46x FASTER than tsc; 2.68x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.75s**, **+2.3%** against `temurin26` on identical bytecode — 0.42× tsc, 2.75× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.58×** tsc and **17.05×** tsgo; tsc is **6.61×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **16.95s**.

Ahead-of-time (GraalVM native-image): **4.96s** — **0.75×** tsc, **4.96×** tsgo, **3.44×** the COLD JVM arm and **1.85×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1331d33a23e9 | **3.33s** | 58,486 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1331d33a23e9 | **3.04s** | 64,025 LOC/s | 46 |
| xtsc (cold JVM) | 1331d33a23e9 | 17.91s | 10,871 LOC/s | 46 |
| tsc | 6.0.3 | 7.87s | 24,746 LOC/s | 65 |
| tsgo | 7.0.2 | 1.52s | 127,841 LOC/s | 65 |
| xtsc-native | 1331d33a23e9 (AOT) | 5.40s | 36,029 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.36x FASTER than tsc; 2.19x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.04s**, **-8.7%** against `temurin26` on identical bytecode — 0.39× tsc, 2.00× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.28×** tsc and **11.76×** tsgo; tsc is **5.17×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.82s**.

Ahead-of-time (GraalVM native-image): **5.40s** — **0.69×** tsc, **3.55×** tsgo, **3.31×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
