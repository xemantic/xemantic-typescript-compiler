# Bench run 20260906T182127Z

- **xtsc revision**: [`abbd34e7e53f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/abbd34e7e53f91c46edcf30e911992cae5ccf0b4)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push abbd34e7e53f91c46edcf30e911992cae5ccf0b4

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | abbd34e7e53f | **11.35s** | 17,155 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | abbd34e7e53f | **11.71s** | 16,622 LOC/s | 46 |
| xtsc (cold JVM) | abbd34e7e53f | 28.83s | 6,754 LOC/s | 46 |
| tsc | 6.0.3 | 8.08s | 24,099 LOC/s | 65 |
| tsgo | 7.0.2 | 1.24s | 157,017 LOC/s | 65 |
| xtsc-native | abbd34e7e53f (AOT) | 20.39s | 9,550 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.40x tsc — parity at 1.00x; 9.15x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **11.71s**, **+3.2%** against `temurin26` on identical bytecode — 1.45× tsc, 9.45× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.57×** tsc and **23.25×** tsgo; tsc is **6.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.74s**.

Ahead-of-time (GraalVM native-image): **20.39s** — **2.52×** tsc, **16.44×** tsgo, **1.41×** the COLD JVM arm and **1.80×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | abbd34e7e53f | **12.60s** | 15,451 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | abbd34e7e53f | **12.51s** | 15,567 LOC/s | 46 |
| xtsc (cold JVM) | abbd34e7e53f | 27.29s | 7,133 LOC/s | 46 |
| tsc | 6.0.3 | 9.84s | 19,794 LOC/s | 65 |
| tsgo | 7.0.2 | 1.77s | 110,063 LOC/s | 65 |
| xtsc-native | abbd34e7e53f (AOT) | 19.77s | 9,846 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.28x tsc — parity at 1.00x; 7.12x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **12.51s**, **-0.7%** against `temurin26` on identical bytecode — 1.27× tsc, 7.07× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.77×** tsc and **15.43×** tsgo; tsc is **5.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.19s**.

Ahead-of-time (GraalVM native-image): **19.77s** — **2.01×** tsc, **11.18×** tsgo, **1.38×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
