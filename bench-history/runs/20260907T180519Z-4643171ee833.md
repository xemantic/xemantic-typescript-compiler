# Bench run 20260907T180519Z

- **xtsc revision**: [`4643171ee833`](https://github.com/xemantic/xemantic-typescript-compiler/commit/4643171ee833a9582f03fd4bdfe44e830e3ffcdf)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 4643171ee833a9582f03fd4bdfe44e830e3ffcdf

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 4643171ee833 | **15.54s** | 12,527 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 4643171ee833 | **15.10s** | 12,890 LOC/s | 46 |
| xtsc (cold JVM) | 4643171ee833 | 36.01s | 5,406 LOC/s | 46 |
| tsc | 6.0.3 | 9.98s | 19,515 LOC/s | 65 |
| tsgo | 7.0.2 | 1.55s | 125,533 LOC/s | 65 |
| xtsc-native | 4643171ee833 (AOT) | 24.65s | 7,897 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.56x tsc — parity at 1.00x; 10.02x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **15.10s**, **-2.8%** against `temurin26` on identical bytecode — 1.51× tsc, 9.74× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.61×** tsc and **23.22×** tsgo; tsc is **6.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **35.90s**.

Ahead-of-time (GraalVM native-image): **24.65s** — **2.47×** tsc, **15.89×** tsgo, **1.46×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 4643171ee833 | **14.96s** | 13,014 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 4643171ee833 | **15.75s** | 12,359 LOC/s | 46 |
| xtsc (cold JVM) | 4643171ee833 | 38.72s | 5,028 LOC/s | 46 |
| tsc | 6.0.3 | 12.36s | 15,751 LOC/s | 65 |
| tsgo | 7.0.2 | 2.28s | 85,283 LOC/s | 65 |
| xtsc-native | 4643171ee833 (AOT) | 25.89s | 7,519 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.21x tsc — parity at 1.00x; 6.55x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **15.75s**, **+5.3%** against `temurin26` on identical bytecode — 1.27× tsc, 6.90× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.13×** tsc and **16.96×** tsgo; tsc is **5.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **38.60s**.

Ahead-of-time (GraalVM native-image): **25.89s** — **2.09×** tsc, **11.34×** tsgo, **1.50×** the COLD JVM arm and **1.73×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
