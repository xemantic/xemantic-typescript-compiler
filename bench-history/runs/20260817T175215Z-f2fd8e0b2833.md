# Bench run 20260817T175215Z

- **xtsc revision**: [`f2fd8e0b2833`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f2fd8e0b28331e67f59e3f31e3c4565d373dcc35)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push f2fd8e0b28331e67f59e3f31e3c4565d373dcc35

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f2fd8e0b2833 | **3.93s** | 49,504 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f2fd8e0b2833 | **3.67s** | 53,052 LOC/s | 46 |
| xtsc (cold JVM) | f2fd8e0b2833 | 22.79s | 8,545 LOC/s | 46 |
| tsc | 6.0.3 | 10.67s | 18,247 LOC/s | 65 |
| tsgo | 7.0.2 | 1.65s | 118,072 LOC/s | 65 |
| xtsc-native | f2fd8e0b2833 (AOT) | 5.73s | 33,997 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.71x FASTER than tsc; 2.39x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.67s**, **-6.7%** against `temurin26` on identical bytecode — 0.34× tsc, 2.23× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.14×** tsc and **13.82×** tsgo; tsc is **6.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.68s**.

Ahead-of-time (GraalVM native-image): **5.73s** — **0.54×** tsc, **3.47×** tsgo, **3.98×** the COLD JVM arm and **1.46×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f2fd8e0b2833 | **4.72s** | 41,232 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f2fd8e0b2833 | **4.21s** | 46,291 LOC/s | 46 |
| xtsc (cold JVM) | f2fd8e0b2833 | 23.58s | 8,258 LOC/s | 46 |
| tsc | 6.0.3 | 13.08s | 14,883 LOC/s | 65 |
| tsgo | 7.0.2 | 2.32s | 83,814 LOC/s | 65 |
| xtsc-native | f2fd8e0b2833 (AOT) | 6.96s | 27,994 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.77x FASTER than tsc; 2.03x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.21s**, **-10.9%** against `temurin26` on identical bytecode — 0.32× tsc, 1.81× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.80×** tsc and **10.15×** tsgo; tsc is **5.63×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.46s**.

Ahead-of-time (GraalVM native-image): **6.96s** — **0.53×** tsc, **2.99×** tsgo, **3.39×** the COLD JVM arm and **1.47×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
