# Bench run 20260902T135108Z

- **xtsc revision**: [`ba358788b6b5`](https://github.com/xemantic/xemantic-typescript-compiler/commit/ba358788b6b54f3f8ed9a74321e6a026f51ad885)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push ba358788b6b54f3f8ed9a74321e6a026f51ad885

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ba358788b6b5 | **3.36s** | 57,895 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ba358788b6b5 | **3.04s** | 64,131 LOC/s | 46 |
| xtsc (cold JVM) | ba358788b6b5 | 17.98s | 10,827 LOC/s | 46 |
| tsc | 6.0.3 | 8.67s | 22,459 LOC/s | 65 |
| tsgo | 7.0.2 | 1.36s | 143,374 LOC/s | 65 |
| xtsc-native | ba358788b6b5 (AOT) | 5.26s | 37,015 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.58x FASTER than tsc; 2.48x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.04s**, **-9.7%** against `temurin26` on identical bytecode — 0.35× tsc, 2.24× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.07×** tsc and **13.24×** tsgo; tsc is **6.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.89s**.

Ahead-of-time (GraalVM native-image): **5.26s** — **0.61×** tsc, **3.87×** tsgo, **3.42×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ba358788b6b5 | **4.01s** | 48,542 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ba358788b6b5 | **3.70s** | 52,607 LOC/s | 46 |
| xtsc (cold JVM) | ba358788b6b5 | 22.87s | 8,513 LOC/s | 46 |
| tsc | 6.0.3 | 10.60s | 18,368 LOC/s | 65 |
| tsgo | 7.0.2 | 2.12s | 91,754 LOC/s | 65 |
| xtsc-native | ba358788b6b5 (AOT) | 6.62s | 29,424 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.64x FASTER than tsc; 1.89x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.70s**, **-7.7%** against `temurin26` on identical bytecode — 0.35× tsc, 1.74× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.16×** tsc and **10.78×** tsgo; tsc is **5.00×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.76s**.

Ahead-of-time (GraalVM native-image): **6.62s** — **0.62×** tsc, **3.12×** tsgo, **3.46×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
