# Bench run 20260921T172703Z

- **xtsc revision**: [`f9caa002a0a5`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f9caa002a0a54838e3e35f9e5fe8d9d89d368a22)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push f9caa002a0a54838e3e35f9e5fe8d9d89d368a22

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f9caa002a0a5 | **6.00s** | 32,434 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f9caa002a0a5 | **5.55s** | 35,049 LOC/s | 46 |
| xtsc (cold JVM) | f9caa002a0a5 | 28.43s | 6,847 LOC/s | 46 |
| tsc | 6.0.3 | 11.33s | 17,189 LOC/s | 65 |
| tsgo | 7.0.2 | 1.73s | 112,609 LOC/s | 65 |
| xtsc-native | f9caa002a0a5 (AOT) | 9.60s | 20,277 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.89x FASTER than tsc; 3.47x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.55s**, **-7.5%** against `temurin26` on identical bytecode — 0.49× tsc, 3.21× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.51×** tsc and **16.45×** tsgo; tsc is **6.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.32s**.

Ahead-of-time (GraalVM native-image): **9.60s** — **0.85×** tsc, **5.55×** tsgo, **2.96×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f9caa002a0a5 | **6.76s** | 28,780 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f9caa002a0a5 | **6.10s** | 31,918 LOC/s | 46 |
| xtsc (cold JVM) | f9caa002a0a5 | 28.29s | 6,881 LOC/s | 46 |
| tsc | 6.0.3 | 13.60s | 14,315 LOC/s | 65 |
| tsgo | 7.0.2 | 2.43s | 80,091 LOC/s | 65 |
| xtsc-native | f9caa002a0a5 (AOT) | 11.35s | 17,161 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.01x FASTER than tsc; 2.78x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.10s**, **-9.8%** against `temurin26` on identical bytecode — 0.45× tsc, 2.51× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.08×** tsc and **11.64×** tsgo; tsc is **5.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.14s**.

Ahead-of-time (GraalVM native-image): **11.35s** — **0.83×** tsc, **4.67×** tsgo, **2.49×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
