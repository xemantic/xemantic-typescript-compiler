# Bench run 20260805T125129Z

- **xtsc revision**: [`49da5ba90d5e`](https://github.com/xemantic/xemantic-typescript-compiler/commit/49da5ba90d5ecbfc8f253a339c07e19818c0a10b)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 49da5ba90d5ecbfc8f253a339c07e19818c0a10b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 49da5ba90d5e | 27.59s | 7,055 LOC/s | 46 |
| tsc | 6.0.3 | 11.10s | 17,539 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.71s | 113,794 LOC/s | 65 |
| xtsc-native | 49da5ba90d5e (AOT) | 11.05s | 17,616 LOC/s | 46 |

Relative (wall): xtsc is **2.49×** tsc and **16.13×** tsgo; tsc is **6.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.48s**.

Ahead-of-time (GraalVM native-image): **11.05s**, **2.50×** the JVM arm, **1.00×** tsc, **6.46×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 49da5ba90d5e | 28.04s | 6,944 LOC/s | 46 |
| tsc | 6.0.3 | 13.55s | 14,367 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.49s | 78,287 LOC/s | 65 |
| xtsc-native | 49da5ba90d5e (AOT) | 13.51s | 14,413 LOC/s | 46 |

Relative (wall): xtsc is **2.07×** tsc and **11.27×** tsgo; tsc is **5.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.93s**.

Ahead-of-time (GraalVM native-image): **13.51s**, **2.08×** the JVM arm, **1.00×** tsc, **5.43×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
