# Bench run 20260818T011123Z

- **xtsc revision**: [`eda1d8c839e2`](https://github.com/xemantic/xemantic-typescript-compiler/commit/eda1d8c839e2d2d5926a41de77d38f94130f084b)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push eda1d8c839e2d2d5926a41de77d38f94130f084b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | eda1d8c839e2 | **4.08s** | 47,744 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | eda1d8c839e2 | **3.81s** | 51,049 LOC/s | 46 |
| xtsc (cold JVM) | eda1d8c839e2 | 24.48s | 7,954 LOC/s | 46 |
| tsc | 6.0.3 | 11.12s | 17,507 LOC/s | 65 |
| tsgo | 7.0.2 | 1.76s | 110,689 LOC/s | 65 |
| xtsc-native | eda1d8c839e2 (AOT) | 6.40s | 30,436 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.73x FASTER than tsc; 2.32x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.81s**, **-6.5%** against `temurin26` on identical bytecode — 0.34× tsc, 2.17× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.20×** tsc and **13.92×** tsgo; tsc is **6.32×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.36s**.

Ahead-of-time (GraalVM native-image): **6.40s** — **0.58×** tsc, **3.64×** tsgo, **3.83×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | eda1d8c839e2 | **5.08s** | 38,342 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | eda1d8c839e2 | **4.54s** | 42,933 LOC/s | 46 |
| xtsc (cold JVM) | eda1d8c839e2 | 26.27s | 7,410 LOC/s | 46 |
| tsc | 6.0.3 | 13.59s | 14,332 LOC/s | 65 |
| tsgo | 7.0.2 | 2.49s | 78,130 LOC/s | 65 |
| xtsc-native | eda1d8c839e2 (AOT) | 7.57s | 25,733 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.68x FASTER than tsc; 2.04x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.54s**, **-10.7%** against `temurin26` on identical bytecode — 0.33× tsc, 1.82× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.93×** tsc and **10.54×** tsgo; tsc is **5.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.14s**.

Ahead-of-time (GraalVM native-image): **7.57s** — **0.56×** tsc, **3.04×** tsgo, **3.47×** the COLD JVM arm and **1.49×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
