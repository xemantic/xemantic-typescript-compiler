# Bench run 20260807T003139Z

- **xtsc revision**: [`e355a990bfaa`](https://github.com/xemantic/xemantic-typescript-compiler/commit/e355a990bfaa8f368c9fad348d7b4a533709e54a)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push e355a990bfaa8f368c9fad348d7b4a533709e54a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | e355a990bfaa | 25.45s | 7,649 LOC/s | 46 |
| tsc | 6.0.3 | 10.57s | 18,416 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.62s | 120,112 LOC/s | 65 |
| xtsc-native | e355a990bfaa (AOT) | 10.54s | 18,481 LOC/s | 46 |

Relative (wall): xtsc is **2.41×** tsc and **15.70×** tsgo; tsc is **6.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.35s**.

Ahead-of-time (GraalVM native-image): **10.54s**, **2.42×** the JVM arm, **1.00×** tsc, **6.50×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | e355a990bfaa | 30.41s | 6,403 LOC/s | 46 |
| tsc | 6.0.3 | 13.23s | 14,715 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.38s | 81,910 LOC/s | 65 |
| xtsc-native | e355a990bfaa (AOT) | 12.84s | 15,167 LOC/s | 46 |

Relative (wall): xtsc is **2.30×** tsc and **12.79×** tsgo; tsc is **5.57×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.29s**.

Ahead-of-time (GraalVM native-image): **12.84s**, **2.37×** the JVM arm, **0.97×** tsc, **5.40×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
