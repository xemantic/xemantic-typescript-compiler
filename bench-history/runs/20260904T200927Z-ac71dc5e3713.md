# Bench run 20260904T200927Z

- **xtsc revision**: [`ac71dc5e3713`](https://github.com/xemantic/xemantic-typescript-compiler/commit/ac71dc5e3713023147390ea0a8fc263012d32852)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push ac71dc5e3713023147390ea0a8fc263012d32852

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ac71dc5e3713 | **4.16s** | 46,769 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ac71dc5e3713 | **4.28s** | 45,501 LOC/s | 46 |
| xtsc (cold JVM) | ac71dc5e3713 | 22.52s | 8,645 LOC/s | 46 |
| tsc | 6.0.3 | 10.48s | 18,581 LOC/s | 65 |
| tsgo | 7.0.2 | 1.57s | 124,172 LOC/s | 65 |
| xtsc-native | ac71dc5e3713 (AOT) | 6.96s | 27,990 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.52x FASTER than tsc; 2.65x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.28s**, **+2.8%** against `temurin26` on identical bytecode — 0.41× tsc, 2.73× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.15×** tsc and **14.36×** tsgo; tsc is **6.68×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.40s**.

Ahead-of-time (GraalVM native-image): **6.96s** — **0.66×** tsc, **4.44×** tsgo, **3.24×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ac71dc5e3713 | **4.95s** | 39,357 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ac71dc5e3713 | **4.72s** | 41,241 LOC/s | 46 |
| xtsc (cold JVM) | ac71dc5e3713 | 24.63s | 7,903 LOC/s | 46 |
| tsc | 6.0.3 | 12.45s | 15,639 LOC/s | 65 |
| tsgo | 7.0.2 | 2.22s | 87,703 LOC/s | 65 |
| xtsc-native | ac71dc5e3713 (AOT) | 7.88s | 24,698 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.52x FASTER than tsc; 2.23x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.72s**, **-4.6%** against `temurin26` on identical bytecode — 0.38× tsc, 2.13× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.98×** tsc and **11.10×** tsgo; tsc is **5.61×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.52s**.

Ahead-of-time (GraalVM native-image): **7.88s** — **0.63×** tsc, **3.55×** tsgo, **3.12×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
