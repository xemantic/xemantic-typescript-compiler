# Bench run 20260901T092607Z

- **xtsc revision**: [`e65bcd93b163`](https://github.com/xemantic/xemantic-typescript-compiler/commit/e65bcd93b163e194f83d881973e3ce3a84a3c24e)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push e65bcd93b163e194f83d881973e3ce3a84a3c24e

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e65bcd93b163 | **4.32s** | 45,111 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e65bcd93b163 | **4.60s** | 42,354 LOC/s | 46 |
| xtsc (cold JVM) | e65bcd93b163 | 24.49s | 7,949 LOC/s | 46 |
| tsc | 6.0.3 | 12.25s | 15,895 LOC/s | 65 |
| tsgo | 7.0.2 | 1.82s | 106,920 LOC/s | 65 |
| xtsc-native | e65bcd93b163 (AOT) | 8.46s | 23,006 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.84x FASTER than tsc; 2.37x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.60s**, **+6.5%** against `temurin26` on identical bytecode — 0.38× tsc, 2.52× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.00×** tsc and **13.45×** tsgo; tsc is **6.73×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.37s**.

Ahead-of-time (GraalVM native-image): **8.46s** — **0.69×** tsc, **4.65×** tsgo, **2.89×** the COLD JVM arm and **1.96×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e65bcd93b163 | **5.78s** | 33,691 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e65bcd93b163 | **5.17s** | 37,638 LOC/s | 46 |
| xtsc (cold JVM) | e65bcd93b163 | 28.22s | 6,898 LOC/s | 46 |
| tsc | 6.0.3 | 14.78s | 13,170 LOC/s | 65 |
| tsgo | 7.0.2 | 2.51s | 77,663 LOC/s | 65 |
| xtsc-native | e65bcd93b163 (AOT) | 8.70s | 22,374 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.56x FASTER than tsc; 2.31x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.17s**, **-10.5%** against `temurin26` on identical bytecode — 0.35× tsc, 2.06× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.91×** tsc and **11.26×** tsgo; tsc is **5.90×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.09s**.

Ahead-of-time (GraalVM native-image): **8.70s** — **0.59×** tsc, **3.47×** tsgo, **3.24×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
