# Bench run 20260828T144256Z

- **xtsc revision**: [`c4679d402ac9`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c4679d402ac9870798fcc92770da5697bf6f0547)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push c4679d402ac9870798fcc92770da5697bf6f0547

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c4679d402ac9 | **3.10s** | 62,726 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c4679d402ac9 | **2.80s** | 69,536 LOC/s | 46 |
| xtsc (cold JVM) | c4679d402ac9 | 18.41s | 10,577 LOC/s | 46 |
| tsc | 6.0.3 | 8.03s | 24,255 LOC/s | 65 |
| tsgo | 7.0.2 | 1.24s | 157,653 LOC/s | 65 |
| xtsc-native | c4679d402ac9 (AOT) | 4.92s | 39,613 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.59x FASTER than tsc; 2.51x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.80s**, **-9.8%** against `temurin26` on identical bytecode — 0.35× tsc, 2.27× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.29×** tsc and **14.91×** tsgo; tsc is **6.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.32s**.

Ahead-of-time (GraalVM native-image): **4.92s** — **0.61×** tsc, **3.98×** tsgo, **3.75×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c4679d402ac9 | **3.74s** | 52,017 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c4679d402ac9 | **3.67s** | 52,994 LOC/s | 46 |
| xtsc (cold JVM) | c4679d402ac9 | 18.64s | 10,447 LOC/s | 46 |
| tsc | 6.0.3 | 9.83s | 19,802 LOC/s | 65 |
| tsgo | 7.0.2 | 1.80s | 108,408 LOC/s | 65 |
| xtsc-native | c4679d402ac9 (AOT) | 5.90s | 33,011 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.63x FASTER than tsc; 2.08x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.67s**, **-1.8%** against `temurin26` on identical bytecode — 0.37× tsc, 2.05× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.90×** tsc and **10.38×** tsgo; tsc is **5.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.52s**.

Ahead-of-time (GraalVM native-image): **5.90s** — **0.60×** tsc, **3.28×** tsgo, **3.16×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
