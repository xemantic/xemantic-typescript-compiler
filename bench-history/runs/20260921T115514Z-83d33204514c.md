# Bench run 20260921T115514Z

- **xtsc revision**: [`83d33204514c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/83d33204514c3a54bd76df796ab1c992b38b13cd)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 83d33204514c3a54bd76df796ab1c992b38b13cd

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 83d33204514c | **5.88s** | 33,090 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 83d33204514c | **5.42s** | 35,942 LOC/s | 46 |
| xtsc (cold JVM) | 83d33204514c | 27.00s | 7,211 LOC/s | 46 |
| tsc | 6.0.3 | 10.32s | 18,860 LOC/s | 65 |
| tsgo | 7.0.2 | 1.62s | 120,334 LOC/s | 65 |
| xtsc-native | 83d33204514c (AOT) | 9.90s | 19,666 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.75x FASTER than tsc; 3.64x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.42s**, **-7.9%** against `temurin26` on identical bytecode — 0.52× tsc, 3.35× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.62×** tsc and **16.69×** tsgo; tsc is **6.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.88s**.

Ahead-of-time (GraalVM native-image): **9.90s** — **0.96×** tsc, **6.12×** tsgo, **2.73×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 83d33204514c | **7.00s** | 27,794 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 83d33204514c | **6.03s** | 32,315 LOC/s | 46 |
| xtsc (cold JVM) | 83d33204514c | 27.98s | 6,959 LOC/s | 46 |
| tsc | 6.0.3 | 12.83s | 15,173 LOC/s | 65 |
| tsgo | 7.0.2 | 2.35s | 82,993 LOC/s | 65 |
| xtsc-native | 83d33204514c (AOT) | 11.10s | 17,545 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.83x FASTER than tsc; 2.99x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.03s**, **-14.0%** against `temurin26` on identical bytecode — 0.47× tsc, 2.57× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.18×** tsc and **11.92×** tsgo; tsc is **5.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.83s**.

Ahead-of-time (GraalVM native-image): **11.10s** — **0.86×** tsc, **4.73×** tsgo, **2.52×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
