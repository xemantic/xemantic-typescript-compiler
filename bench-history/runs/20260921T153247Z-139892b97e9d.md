# Bench run 20260921T153247Z

- **xtsc revision**: [`139892b97e9d`](https://github.com/xemantic/xemantic-typescript-compiler/commit/139892b97e9def124c27af4448c9c2108706efac)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 139892b97e9def124c27af4448c9c2108706efac

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 139892b97e9d | **4.00s** | 48,639 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 139892b97e9d | **3.50s** | 55,549 LOC/s | 46 |
| xtsc (cold JVM) | 139892b97e9d | 17.61s | 11,059 LOC/s | 46 |
| tsc | 6.0.3 | 6.96s | 27,994 LOC/s | 65 |
| tsgo | 7.0.2 | 1.06s | 183,335 LOC/s | 65 |
| xtsc-native | 139892b97e9d (AOT) | 5.97s | 32,591 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.74x FASTER than tsc; 3.77x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.50s**, **-12.4%** against `temurin26` on identical bytecode — 0.50× tsc, 3.30× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.53×** tsc and **16.58×** tsgo; tsc is **6.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.38s**.

Ahead-of-time (GraalVM native-image): **5.97s** — **0.86×** tsc, **5.63×** tsgo, **2.95×** the COLD JVM arm and **1.49×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 139892b97e9d | **4.45s** | 43,802 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 139892b97e9d | **4.22s** | 46,159 LOC/s | 46 |
| xtsc (cold JVM) | 139892b97e9d | 20.09s | 9,691 LOC/s | 46 |
| tsc | 6.0.3 | 8.21s | 23,706 LOC/s | 65 |
| tsgo | 7.0.2 | 1.57s | 124,251 LOC/s | 65 |
| xtsc-native | 139892b97e9d (AOT) | 6.54s | 29,766 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.85x FASTER than tsc; 2.84x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.22s**, **-5.1%** against `temurin26` on identical bytecode — 0.51× tsc, 2.69× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.45×** tsc and **12.82×** tsgo; tsc is **5.24×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.82s**.

Ahead-of-time (GraalVM native-image): **6.54s** — **0.80×** tsc, **4.17×** tsgo, **3.07×** the COLD JVM arm and **1.47×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
