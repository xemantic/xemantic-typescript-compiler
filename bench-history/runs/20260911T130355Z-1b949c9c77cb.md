# Bench run 20260911T130355Z

- **xtsc revision**: [`1b949c9c77cb`](https://github.com/xemantic/xemantic-typescript-compiler/commit/1b949c9c77cb48fc3eb66e96dcbaf925beaf656c)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 1b949c9c77cb48fc3eb66e96dcbaf925beaf656c

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1b949c9c77cb | **5.37s** | 36,291 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1b949c9c77cb | **4.94s** | 39,381 LOC/s | 46 |
| xtsc (cold JVM) | 1b949c9c77cb | 27.12s | 7,178 LOC/s | 46 |
| tsc | 6.0.3 | 11.00s | 17,693 LOC/s | 65 |
| tsgo | 7.0.2 | 1.75s | 111,004 LOC/s | 65 |
| xtsc-native | 1b949c9c77cb (AOT) | 9.45s | 20,609 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.05x FASTER than tsc; 3.06x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.94s**, **-7.8%** against `temurin26` on identical bytecode — 0.45× tsc, 2.82× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.46×** tsc and **15.46×** tsgo; tsc is **6.27×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.00s**.

Ahead-of-time (GraalVM native-image): **9.45s** — **0.86×** tsc, **5.39×** tsgo, **2.87×** the COLD JVM arm and **1.76×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1b949c9c77cb | **6.68s** | 29,155 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1b949c9c77cb | **5.74s** | 33,932 LOC/s | 46 |
| xtsc (cold JVM) | 1b949c9c77cb | 28.18s | 6,908 LOC/s | 46 |
| tsc | 6.0.3 | 13.61s | 14,307 LOC/s | 65 |
| tsgo | 7.0.2 | 2.53s | 76,835 LOC/s | 65 |
| xtsc-native | 1b949c9c77cb (AOT) | 10.38s | 18,753 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.04x FASTER than tsc; 2.64x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.74s**, **-14.1%** against `temurin26` on identical bytecode — 0.42× tsc, 2.26× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.07×** tsc and **11.12×** tsgo; tsc is **5.37×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.05s**.

Ahead-of-time (GraalVM native-image): **10.38s** — **0.76×** tsc, **4.10×** tsgo, **2.71×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
