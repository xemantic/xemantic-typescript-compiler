# Bench run 20260911T134949Z

- **xtsc revision**: [`22d05a2c8f62`](https://github.com/xemantic/xemantic-typescript-compiler/commit/22d05a2c8f623079c2a5e2a90b3f683bb1b1db66)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 22d05a2c8f623079c2a5e2a90b3f683bb1b1db66

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 22d05a2c8f62 | **5.74s** | 33,949 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 22d05a2c8f62 | **5.11s** | 38,079 LOC/s | 46 |
| xtsc (cold JVM) | 22d05a2c8f62 | 26.17s | 7,440 LOC/s | 46 |
| tsc | 6.0.3 | 11.06s | 17,596 LOC/s | 65 |
| tsgo | 7.0.2 | 1.68s | 115,825 LOC/s | 65 |
| xtsc-native | 22d05a2c8f62 (AOT) | 8.99s | 21,655 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.93x FASTER than tsc; 3.41x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.11s**, **-10.8%** against `temurin26` on identical bytecode — 0.46× tsc, 3.04× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.37×** tsc and **15.57×** tsgo; tsc is **6.58×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.05s**.

Ahead-of-time (GraalVM native-image): **8.99s** — **0.81×** tsc, **5.35×** tsgo, **2.91×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 22d05a2c8f62 | **6.60s** | 29,486 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 22d05a2c8f62 | **5.60s** | 34,768 LOC/s | 46 |
| xtsc (cold JVM) | 22d05a2c8f62 | 29.30s | 6,646 LOC/s | 46 |
| tsc | 6.0.3 | 13.20s | 14,749 LOC/s | 65 |
| tsgo | 7.0.2 | 2.41s | 80,789 LOC/s | 65 |
| xtsc-native | 22d05a2c8f62 (AOT) | 9.96s | 19,540 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.00x FASTER than tsc; 2.74x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.60s**, **-15.2%** against `temurin26` on identical bytecode — 0.42× tsc, 2.32× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.22×** tsc and **12.16×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.16s**.

Ahead-of-time (GraalVM native-image): **9.96s** — **0.75×** tsc, **4.13×** tsgo, **2.94×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
