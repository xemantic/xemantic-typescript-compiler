# Bench run 20260901T153826Z

- **xtsc revision**: [`0704d64a563a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/0704d64a563a31d6335d76dbef0acc52a0d19f47)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 0704d64a563a31d6335d76dbef0acc52a0d19f47

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0704d64a563a | **4.26s** | 45,651 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0704d64a563a | **4.00s** | 48,626 LOC/s | 46 |
| xtsc (cold JVM) | 0704d64a563a | 24.72s | 7,875 LOC/s | 46 |
| tsc | 6.0.3 | 10.86s | 17,931 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,938 LOC/s | 65 |
| xtsc-native | 0704d64a563a (AOT) | 6.68s | 29,160 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.55x FASTER than tsc; 2.56x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.00s**, **-6.1%** against `temurin26` on identical bytecode — 0.37× tsc, 2.40× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.28×** tsc and **14.85×** tsgo; tsc is **6.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.59s**.

Ahead-of-time (GraalVM native-image): **6.68s** — **0.61×** tsc, **4.01×** tsgo, **3.70×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0704d64a563a | **5.12s** | 38,064 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0704d64a563a | **4.68s** | 41,594 LOC/s | 46 |
| xtsc (cold JVM) | 0704d64a563a | 26.17s | 7,441 LOC/s | 46 |
| tsc | 6.0.3 | 13.51s | 14,408 LOC/s | 65 |
| tsgo | 7.0.2 | 2.45s | 79,502 LOC/s | 65 |
| xtsc-native | 0704d64a563a (AOT) | 8.62s | 22,584 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.64x FASTER than tsc; 2.09x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.68s**, **-8.5%** against `temurin26` on identical bytecode — 0.35× tsc, 1.91× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.94×** tsc and **10.68×** tsgo; tsc is **5.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.05s**.

Ahead-of-time (GraalVM native-image): **8.62s** — **0.64×** tsc, **3.52×** tsgo, **3.04×** the COLD JVM arm and **1.69×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
