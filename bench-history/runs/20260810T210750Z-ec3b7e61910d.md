# Bench run 20260810T210750Z

- **xtsc revision**: [`ec3b7e61910d`](https://github.com/xemantic/xemantic-typescript-compiler/commit/ec3b7e61910d1d52c2e4f25a7a27487f1bb313a7)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push ec3b7e61910d1d52c2e4f25a7a27487f1bb313a7

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm JVM)** | ec3b7e61910d | **4.56s** | 42,679 LOC/s | 46 |
| xtsc (cold JVM) | ec3b7e61910d | 25.95s | 7,503 LOC/s | 46 |
| tsc | 6.0.3 | 10.88s | 17,888 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.69s | 115,276 LOC/s | 65 |
| xtsc-native | ec3b7e61910d (AOT) | 5.61s | 34,693 LOC/s | 46 |

**Warm JVM — the target metric: 2.39x FASTER than tsc; 2.70x tsgo — parity at 1.00x.**

Cold JVM (one-shot CLI): **2.38×** tsc and **15.36×** tsgo; tsc is **6.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.84s**.

Ahead-of-time (GraalVM native-image, Oracle + PGO): **5.61s** — **0.52×** tsc, **3.32×** tsgo, **4.62×** the COLD JVM arm and **1.23×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm JVM)** | ec3b7e61910d | **5.41s** | 36,002 LOC/s | 46 |
| xtsc (cold JVM) | ec3b7e61910d | 30.67s | 6,347 LOC/s | 46 |
| tsc | 6.0.3 | 13.41s | 14,519 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.43s | 80,025 LOC/s | 65 |
| xtsc-native | ec3b7e61910d (AOT) | 6.51s | 29,921 LOC/s | 46 |

**Warm JVM — the target metric: 2.48x FASTER than tsc; 2.22x tsgo — parity at 1.00x.**

Cold JVM (one-shot CLI): **2.29×** tsc and **12.61×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.52s**.

Ahead-of-time (GraalVM native-image, Oracle + PGO): **6.51s** — **0.49×** tsc, **2.67×** tsgo, **4.71×** the COLD JVM arm and **1.20×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
