# Bench run 20260830T214447Z

- **xtsc revision**: [`a740fd2a9b24`](https://github.com/xemantic/xemantic-typescript-compiler/commit/a740fd2a9b24e497aa7fed3b23017bb238a58710)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push a740fd2a9b24e497aa7fed3b23017bb238a58710

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a740fd2a9b24 | **4.13s** | 47,120 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a740fd2a9b24 | **3.83s** | 50,809 LOC/s | 46 |
| xtsc (cold JVM) | a740fd2a9b24 | 25.44s | 7,654 LOC/s | 46 |
| tsc | 6.0.3 | 10.67s | 18,247 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,867 LOC/s | 65 |
| xtsc-native | a740fd2a9b24 (AOT) | 6.09s | 31,986 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.58x FASTER than tsc; 2.48x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.83s**, **-7.3%** against `temurin26` on identical bytecode — 0.36× tsc, 2.30× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.38×** tsc and **15.27×** tsgo; tsc is **6.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.33s**.

Ahead-of-time (GraalVM native-image): **6.09s** — **0.57×** tsc, **3.65×** tsgo, **4.18×** the COLD JVM arm and **1.47×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a740fd2a9b24 | **4.96s** | 39,230 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a740fd2a9b24 | **4.52s** | 43,066 LOC/s | 46 |
| xtsc (cold JVM) | a740fd2a9b24 | 28.06s | 6,939 LOC/s | 46 |
| tsc | 6.0.3 | 12.99s | 14,986 LOC/s | 65 |
| tsgo | 7.0.2 | 2.34s | 83,170 LOC/s | 65 |
| xtsc-native | a740fd2a9b24 (AOT) | 7.49s | 26,012 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.62x FASTER than tsc; 2.12x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.52s**, **-8.9%** against `temurin26` on identical bytecode — 0.35× tsc, 1.93× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.16×** tsc and **11.99×** tsgo; tsc is **5.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.93s**.

Ahead-of-time (GraalVM native-image): **7.49s** — **0.58×** tsc, **3.20×** tsgo, **3.75×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
