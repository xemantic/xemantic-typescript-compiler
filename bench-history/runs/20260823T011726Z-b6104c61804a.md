# Bench run 20260823T011726Z

- **xtsc revision**: [`b6104c61804a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/b6104c61804a999f6fcef0efb44d797616002bef)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push b6104c61804a999f6fcef0efb44d797616002bef

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b6104c61804a | **3.87s** | 50,336 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b6104c61804a | **3.64s** | 53,533 LOC/s | 46 |
| xtsc (cold JVM) | b6104c61804a | 21.13s | 9,214 LOC/s | 46 |
| tsc | 6.0.3 | 10.69s | 18,220 LOC/s | 65 |
| tsgo | 7.0.2 | 1.68s | 115,825 LOC/s | 65 |
| xtsc-native | b6104c61804a (AOT) | 5.87s | 33,174 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.76x FASTER than tsc; 2.30x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.64s**, **-6.0%** against `temurin26` on identical bytecode — 0.34× tsc, 2.16× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.98×** tsc and **12.57×** tsgo; tsc is **6.36×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.02s**.

Ahead-of-time (GraalVM native-image): **5.87s** — **0.55×** tsc, **3.49×** tsgo, **3.60×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b6104c61804a | **4.77s** | 40,809 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b6104c61804a | **4.28s** | 45,469 LOC/s | 46 |
| xtsc (cold JVM) | b6104c61804a | 23.53s | 8,275 LOC/s | 46 |
| tsc | 6.0.3 | 13.11s | 14,849 LOC/s | 65 |
| tsgo | 7.0.2 | 2.48s | 78,603 LOC/s | 65 |
| xtsc-native | b6104c61804a (AOT) | 7.76s | 25,090 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.75x FASTER than tsc; 1.93x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.28s**, **-10.2%** against `temurin26` on identical bytecode — 0.33× tsc, 1.73× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.79×** tsc and **9.50×** tsgo; tsc is **5.29×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.40s**.

Ahead-of-time (GraalVM native-image): **7.76s** — **0.59×** tsc, **3.13×** tsgo, **3.03×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
