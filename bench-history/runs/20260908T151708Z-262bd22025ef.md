# Bench run 20260908T151708Z

- **xtsc revision**: [`262bd22025ef`](https://github.com/xemantic/xemantic-typescript-compiler/commit/262bd22025eff744a34aa050a88085eca41b693c)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 262bd22025eff744a34aa050a88085eca41b693c

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 262bd22025ef | **3.84s** | 50,716 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 262bd22025ef | **3.64s** | 53,504 LOC/s | 46 |
| xtsc (cold JVM) | 262bd22025ef | 19.12s | 10,181 LOC/s | 46 |
| tsc | 6.0.3 | 7.43s | 26,187 LOC/s | 65 |
| tsgo | 7.0.2 | 1.11s | 175,249 LOC/s | 65 |
| xtsc-native | 262bd22025ef (AOT) | 6.01s | 32,401 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.94x FASTER than tsc; 3.46x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.64s**, **-5.2%** against `temurin26` on identical bytecode — 0.49× tsc, 3.28× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.57×** tsc and **17.21×** tsgo; tsc is **6.69×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.04s**.

Ahead-of-time (GraalVM native-image): **6.01s** — **0.81×** tsc, **5.41×** tsgo, **3.18×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 262bd22025ef | **4.45s** | 43,753 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 262bd22025ef | **4.26s** | 45,747 LOC/s | 46 |
| xtsc (cold JVM) | 262bd22025ef | 20.63s | 9,436 LOC/s | 46 |
| tsc | 6.0.3 | 8.90s | 21,871 LOC/s | 65 |
| tsgo | 7.0.2 | 1.76s | 110,626 LOC/s | 65 |
| xtsc-native | 262bd22025ef (AOT) | 7.24s | 26,911 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.00x FASTER than tsc; 2.53x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.26s**, **-4.4%** against `temurin26` on identical bytecode — 0.48× tsc, 2.42× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.32×** tsc and **11.72×** tsgo; tsc is **5.06×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.54s**.

Ahead-of-time (GraalVM native-image): **7.24s** — **0.81×** tsc, **4.11×** tsgo, **2.85×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
