# Bench run 20260916T183745Z

- **xtsc revision**: [`4bbe6bbd657d`](https://github.com/xemantic/xemantic-typescript-compiler/commit/4bbe6bbd657d2a60ac7a464f9e5cd682b5323420)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 4bbe6bbd657d2a60ac7a464f9e5cd682b5323420

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 4bbe6bbd657d | **5.94s** | 32,761 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 4bbe6bbd657d | **5.22s** | 37,299 LOC/s | 46 |
| xtsc (cold JVM) | 4bbe6bbd657d | 25.14s | 7,743 LOC/s | 46 |
| tsc | 6.0.3 | 11.02s | 17,676 LOC/s | 65 |
| tsgo | 7.0.2 | 1.71s | 114,194 LOC/s | 65 |
| xtsc-native | 4bbe6bbd657d (AOT) | 9.45s | 20,596 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.85x FASTER than tsc; 3.49x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.22s**, **-12.2%** against `temurin26` on identical bytecode — 0.47× tsc, 3.06× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.28×** tsc and **14.75×** tsgo; tsc is **6.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.03s**.

Ahead-of-time (GraalVM native-image): **9.45s** — **0.86×** tsc, **5.54×** tsgo, **2.66×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 4bbe6bbd657d | **6.56s** | 29,680 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 4bbe6bbd657d | **5.74s** | 33,902 LOC/s | 46 |
| xtsc (cold JVM) | 4bbe6bbd657d | 28.04s | 6,942 LOC/s | 46 |
| tsc | 6.0.3 | 12.93s | 15,060 LOC/s | 65 |
| tsgo | 7.0.2 | 2.33s | 83,491 LOC/s | 65 |
| xtsc-native | 4bbe6bbd657d (AOT) | 9.85s | 19,774 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.97x FASTER than tsc; 2.81x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.74s**, **-12.5%** against `temurin26` on identical bytecode — 0.44× tsc, 2.46× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.17×** tsc and **12.03×** tsgo; tsc is **5.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.92s**.

Ahead-of-time (GraalVM native-image): **9.85s** — **0.76×** tsc, **4.22×** tsgo, **2.85×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
