# Bench run 20260902T155004Z

- **xtsc revision**: [`d9fe8d4e2b85`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d9fe8d4e2b85e23d5576b5f5d8bc839e0f1a43c2)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push d9fe8d4e2b85e23d5576b5f5d8bc839e0f1a43c2

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d9fe8d4e2b85 | **4.44s** | 43,861 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d9fe8d4e2b85 | **4.22s** | 46,170 LOC/s | 46 |
| xtsc (cold JVM) | d9fe8d4e2b85 | 25.10s | 7,756 LOC/s | 46 |
| tsc | 6.0.3 | 10.48s | 18,585 LOC/s | 65 |
| tsgo | 7.0.2 | 1.59s | 122,685 LOC/s | 65 |
| xtsc-native | d9fe8d4e2b85 (AOT) | 7.39s | 26,335 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.36x FASTER than tsc; 2.80x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.22s**, **-5.0%** against `temurin26` on identical bytecode — 0.40× tsc, 2.66× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.40×** tsc and **15.82×** tsgo; tsc is **6.60×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.98s**.

Ahead-of-time (GraalVM native-image): **7.39s** — **0.71×** tsc, **4.66×** tsgo, **3.40×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d9fe8d4e2b85 | **5.32s** | 36,618 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d9fe8d4e2b85 | **4.87s** | 39,947 LOC/s | 46 |
| xtsc (cold JVM) | d9fe8d4e2b85 | 25.38s | 7,671 LOC/s | 46 |
| tsc | 6.0.3 | 12.79s | 15,222 LOC/s | 65 |
| tsgo | 7.0.2 | 2.30s | 84,616 LOC/s | 65 |
| xtsc-native | d9fe8d4e2b85 (AOT) | 8.54s | 22,793 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.41x FASTER than tsc; 2.31x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.87s**, **-8.3%** against `temurin26` on identical bytecode — 0.38× tsc, 2.12× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.98×** tsc and **11.03×** tsgo; tsc is **5.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.25s**.

Ahead-of-time (GraalVM native-image): **8.54s** — **0.67×** tsc, **3.71×** tsgo, **2.97×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
