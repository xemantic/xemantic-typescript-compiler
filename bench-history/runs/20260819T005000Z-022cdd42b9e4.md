# Bench run 20260819T005000Z

- **xtsc revision**: [`022cdd42b9e4`](https://github.com/xemantic/xemantic-typescript-compiler/commit/022cdd42b9e46b64ed60e202f4dd9cc70002c31b)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 022cdd42b9e46b64ed60e202f4dd9cc70002c31b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 022cdd42b9e4 | **4.02s** | 48,433 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 022cdd42b9e4 | **3.84s** | 50,690 LOC/s | 46 |
| xtsc (cold JVM) | 022cdd42b9e4 | 21.58s | 9,023 LOC/s | 46 |
| tsc | 6.0.3 | 10.73s | 18,147 LOC/s | 65 |
| tsgo | 7.0.2 | 1.68s | 116,170 LOC/s | 65 |
| xtsc-native | 022cdd42b9e4 (AOT) | 6.09s | 31,976 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.67x FASTER than tsc; 2.40x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.84s**, **-4.5%** against `temurin26` on identical bytecode — 0.36× tsc, 2.29× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.01×** tsc and **12.87×** tsgo; tsc is **6.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.46s**.

Ahead-of-time (GraalVM native-image): **6.09s** — **0.57×** tsc, **3.63×** tsgo, **3.54×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 022cdd42b9e4 | **4.61s** | 42,243 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 022cdd42b9e4 | **4.40s** | 44,260 LOC/s | 46 |
| xtsc (cold JVM) | 022cdd42b9e4 | 24.25s | 8,030 LOC/s | 46 |
| tsc | 6.0.3 | 13.11s | 14,846 LOC/s | 65 |
| tsgo | 7.0.2 | 2.37s | 82,152 LOC/s | 65 |
| xtsc-native | 022cdd42b9e4 (AOT) | 6.92s | 28,115 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.85x FASTER than tsc; 1.94x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.40s**, **-4.6%** against `temurin26` on identical bytecode — 0.34× tsc, 1.86× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.85×** tsc and **10.23×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.12s**.

Ahead-of-time (GraalVM native-image): **6.92s** — **0.53×** tsc, **2.92×** tsgo, **3.50×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
