# Bench run 20260817T160007Z

- **xtsc revision**: [`e245043bda57`](https://github.com/xemantic/xemantic-typescript-compiler/commit/e245043bda571d1ad14f0b9a76d720daf986c645)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push e245043bda571d1ad14f0b9a76d720daf986c645

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e245043bda57 | **4.00s** | 48,736 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e245043bda57 | **3.76s** | 51,741 LOC/s | 46 |
| xtsc (cold JVM) | e245043bda57 | 22.27s | 8,740 LOC/s | 46 |
| tsc | 6.0.3 | 10.18s | 19,129 LOC/s | 65 |
| tsgo | 7.0.2 | 1.56s | 125,129 LOC/s | 65 |
| xtsc-native | e245043bda57 (AOT) | 5.96s | 32,679 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.55x FASTER than tsc; 2.57x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.76s**, **-5.8%** against `temurin26` on identical bytecode — 0.37× tsc, 2.42× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.19×** tsc and **14.32×** tsgo; tsc is **6.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.17s**.

Ahead-of-time (GraalVM native-image): **5.96s** — **0.59×** tsc, **3.83×** tsgo, **3.74×** the COLD JVM arm and **1.49×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e245043bda57 | **5.03s** | 38,677 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e245043bda57 | **4.46s** | 43,704 LOC/s | 46 |
| xtsc (cold JVM) | e245043bda57 | 25.44s | 7,652 LOC/s | 46 |
| tsc | 6.0.3 | 12.51s | 15,562 LOC/s | 65 |
| tsgo | 7.0.2 | 2.31s | 84,286 LOC/s | 65 |
| xtsc-native | e245043bda57 (AOT) | 7.94s | 24,524 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.49x FASTER than tsc; 2.18x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.46s**, **-11.5%** against `temurin26` on identical bytecode — 0.36× tsc, 1.93× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.03×** tsc and **11.01×** tsgo; tsc is **5.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.32s**.

Ahead-of-time (GraalVM native-image): **7.94s** — **0.63×** tsc, **3.44×** tsgo, **3.20×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
