# Bench run 20260902T151902Z

- **xtsc revision**: [`1ff84f6ab826`](https://github.com/xemantic/xemantic-typescript-compiler/commit/1ff84f6ab8266d519e2a44e87562fff620835b40)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 1ff84f6ab8266d519e2a44e87562fff620835b40

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1ff84f6ab826 | **4.45s** | 43,802 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1ff84f6ab826 | **4.18s** | 46,601 LOC/s | 46 |
| xtsc (cold JVM) | 1ff84f6ab826 | 23.91s | 8,141 LOC/s | 46 |
| tsc | 6.0.3 | 10.97s | 17,743 LOC/s | 65 |
| tsgo | 7.0.2 | 1.70s | 114,530 LOC/s | 65 |
| xtsc-native | 1ff84f6ab826 (AOT) | 6.72s | 28,977 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.47x FASTER than tsc; 2.61x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.18s**, **-6.0%** against `temurin26` on identical bytecode — 0.38× tsc, 2.46× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.18×** tsc and **14.07×** tsgo; tsc is **6.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.80s**.

Ahead-of-time (GraalVM native-image): **6.72s** — **0.61×** tsc, **3.95×** tsgo, **3.56×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1ff84f6ab826 | **5.38s** | 36,210 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1ff84f6ab826 | **4.61s** | 42,207 LOC/s | 46 |
| xtsc (cold JVM) | 1ff84f6ab826 | 26.08s | 7,464 LOC/s | 46 |
| tsc | 6.0.3 | 13.12s | 14,844 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,773 LOC/s | 65 |
| xtsc-native | 1ff84f6ab826 (AOT) | 8.18s | 23,802 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.44x FASTER than tsc; 2.26x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.61s**, **-14.2%** against `temurin26` on identical bytecode — 0.35× tsc, 1.94× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.99×** tsc and **10.95×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.97s**.

Ahead-of-time (GraalVM native-image): **8.18s** — **0.62×** tsc, **3.44×** tsgo, **3.19×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
