# Bench run 20260909T172954Z

- **xtsc revision**: [`83ff78736aa4`](https://github.com/xemantic/xemantic-typescript-compiler/commit/83ff78736aa42cc0790e239fe16f7c03d3d75f5e)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 83ff78736aa42cc0790e239fe16f7c03d3d75f5e

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 83ff78736aa4 | **5.49s** | 35,458 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 83ff78736aa4 | **5.10s** | 38,154 LOC/s | 46 |
| xtsc (cold JVM) | 83ff78736aa4 | 25.47s | 7,645 LOC/s | 46 |
| tsc | 6.0.3 | 11.20s | 17,381 LOC/s | 65 |
| tsgo | 7.0.2 | 1.72s | 113,067 LOC/s | 65 |
| xtsc-native | 83ff78736aa4 (AOT) | 8.89s | 21,908 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.04x FASTER than tsc; 3.19x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.10s**, **-7.1%** against `temurin26` on identical bytecode — 0.46× tsc, 2.96× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.27×** tsc and **14.79×** tsgo; tsc is **6.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.34s**.

Ahead-of-time (GraalVM native-image): **8.89s** — **0.79×** tsc, **5.16×** tsgo, **2.87×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 83ff78736aa4 | **6.56s** | 29,666 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 83ff78736aa4 | **5.83s** | 33,425 LOC/s | 46 |
| xtsc (cold JVM) | 83ff78736aa4 | 28.18s | 6,909 LOC/s | 46 |
| tsc | 6.0.3 | 13.43s | 14,495 LOC/s | 65 |
| tsgo | 7.0.2 | 2.43s | 80,190 LOC/s | 65 |
| xtsc-native | 83ff78736aa4 (AOT) | 10.22s | 19,045 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.05x FASTER than tsc; 2.70x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.83s**, **-11.2%** against `temurin26` on identical bytecode — 0.43× tsc, 2.40× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.10×** tsc and **11.61×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.04s**.

Ahead-of-time (GraalVM native-image): **10.22s** — **0.76×** tsc, **4.21×** tsgo, **2.76×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
