# Bench run 20260921T110930Z

- **xtsc revision**: [`5a6288124178`](https://github.com/xemantic/xemantic-typescript-compiler/commit/5a62881241781d1294ae54a7395e96c0cf0f6988)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 5a62881241781d1294ae54a7395e96c0cf0f6988

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5a6288124178 | **5.86s** | 33,225 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5a6288124178 | **5.29s** | 36,819 LOC/s | 46 |
| xtsc (cold JVM) | 5a6288124178 | 25.51s | 7,632 LOC/s | 46 |
| tsc | 6.0.3 | 10.67s | 18,247 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,481 LOC/s | 65 |
| xtsc-native | 5a6288124178 (AOT) | 8.91s | 21,844 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.82x FASTER than tsc; 3.48x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.29s**, **-9.8%** against `temurin26` on identical bytecode — 0.50× tsc, 3.14× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.39×** tsc and **15.13×** tsgo; tsc is **6.33×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.40s**.

Ahead-of-time (GraalVM native-image): **8.91s** — **0.84×** tsc, **5.29×** tsgo, **2.86×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5a6288124178 | **6.63s** | 29,344 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5a6288124178 | **5.99s** | 32,520 LOC/s | 46 |
| xtsc (cold JVM) | 5a6288124178 | 29.11s | 6,688 LOC/s | 46 |
| tsc | 6.0.3 | 13.13s | 14,824 LOC/s | 65 |
| tsgo | 7.0.2 | 2.40s | 81,024 LOC/s | 65 |
| xtsc-native | 5a6288124178 (AOT) | 10.53s | 18,497 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.98x FASTER than tsc; 2.76x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.99s**, **-9.8%** against `temurin26` on identical bytecode — 0.46× tsc, 2.49× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.22×** tsc and **12.11×** tsgo; tsc is **5.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.98s**.

Ahead-of-time (GraalVM native-image): **10.53s** — **0.80×** tsc, **4.38×** tsgo, **2.77×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
