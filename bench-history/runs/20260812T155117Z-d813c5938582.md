# Bench run 20260812T155117Z

- **xtsc revision**: [`d813c5938582`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d813c5938582e3128d2562e7a378526d009b6311)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push d813c5938582e3128d2562e7a378526d009b6311

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d813c5938582 | **4.14s** | 46,984 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d813c5938582 | **3.87s** | 50,362 LOC/s | 46 |
| xtsc (cold JVM) | d813c5938582 | 22.52s | 8,646 LOC/s | 46 |
| tsc | 6.0.3 | 10.88s | 17,901 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,309 LOC/s | 65 |
| xtsc-native | d813c5938582 (AOT) | 6.10s | 31,902 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.62x FASTER than tsc; 2.48x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.87s**, **-6.7%** against `temurin26` on identical bytecode — 0.36× tsc, 2.31× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.07×** tsc and **13.45×** tsgo; tsc is **6.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.39s**.

Ahead-of-time (GraalVM native-image): **6.10s** — **0.56×** tsc, **3.65×** tsgo, **3.69×** the COLD JVM arm and **1.47×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d813c5938582 | **4.80s** | 40,571 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d813c5938582 | **4.39s** | 44,310 LOC/s | 46 |
| xtsc (cold JVM) | d813c5938582 | 26.09s | 7,462 LOC/s | 46 |
| tsc | 6.0.3 | 13.22s | 14,726 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,670 LOC/s | 65 |
| xtsc-native | d813c5938582 (AOT) | 7.32s | 26,602 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.75x FASTER than tsc; 2.01x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.39s**, **-8.4%** against `temurin26` on identical bytecode — 0.33× tsc, 1.84× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.97×** tsc and **10.94×** tsgo; tsc is **5.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.98s**.

Ahead-of-time (GraalVM native-image): **7.32s** — **0.55×** tsc, **3.07×** tsgo, **3.56×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
