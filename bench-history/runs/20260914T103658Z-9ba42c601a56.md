# Bench run 20260914T103658Z

- **xtsc revision**: [`9ba42c601a56`](https://github.com/xemantic/xemantic-typescript-compiler/commit/9ba42c601a566fac12a959600f4417c66b8d9560)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 9ba42c601a566fac12a959600f4417c66b8d9560

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9ba42c601a56 | **3.92s** | 49,681 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9ba42c601a56 | **3.68s** | 52,908 LOC/s | 46 |
| xtsc (cold JVM) | 9ba42c601a56 | 18.75s | 10,385 LOC/s | 46 |
| tsc | 6.0.3 | 7.83s | 24,853 LOC/s | 65 |
| tsgo | 7.0.2 | 1.23s | 158,552 LOC/s | 65 |
| xtsc-native | 9ba42c601a56 (AOT) | 6.11s | 31,876 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.00x FASTER than tsc; 3.19x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.68s**, **-6.1%** against `temurin26` on identical bytecode — 0.47× tsc, 3.00× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.39×** tsc and **15.27×** tsgo; tsc is **6.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.66s**.

Ahead-of-time (GraalVM native-image): **6.11s** — **0.78×** tsc, **4.97×** tsgo, **3.07×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9ba42c601a56 | **4.57s** | 42,585 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9ba42c601a56 | **4.35s** | 44,789 LOC/s | 46 |
| xtsc (cold JVM) | 9ba42c601a56 | 21.68s | 8,978 LOC/s | 46 |
| tsc | 6.0.3 | 9.49s | 20,507 LOC/s | 65 |
| tsgo | 7.0.2 | 1.79s | 109,015 LOC/s | 65 |
| xtsc-native | 9ba42c601a56 (AOT) | 7.22s | 26,978 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.08x FASTER than tsc; 2.56x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.35s**, **-4.9%** against `temurin26` on identical bytecode — 0.46× tsc, 2.43× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.28×** tsc and **12.14×** tsgo; tsc is **5.32×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.57s**.

Ahead-of-time (GraalVM native-image): **7.22s** — **0.76×** tsc, **4.04×** tsgo, **3.00×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
