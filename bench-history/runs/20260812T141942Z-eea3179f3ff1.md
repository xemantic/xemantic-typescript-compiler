# Bench run 20260812T141942Z

- **xtsc revision**: [`eea3179f3ff1`](https://github.com/xemantic/xemantic-typescript-compiler/commit/eea3179f3ff16a1a7db74c520c45a0351edbd32e)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push eea3179f3ff16a1a7db74c520c45a0351edbd32e

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | eea3179f3ff1 | **3.05s** | 63,920 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | eea3179f3ff1 | **2.92s** | 66,701 LOC/s | 46 |
| xtsc (cold JVM) | eea3179f3ff1 | 20.48s | 9,506 LOC/s | 46 |
| tsc | 6.0.3 | 8.93s | 21,795 LOC/s | 65 |
| tsgo | 7.0.2 | 1.36s | 143,058 LOC/s | 65 |
| xtsc-native | eea3179f3ff1 (AOT) | 4.67s | 41,647 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.93x FASTER than tsc; 2.24x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.92s**, **-4.2%** against `temurin26` on identical bytecode — 0.33× tsc, 2.14× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.29×** tsc and **15.05×** tsgo; tsc is **6.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.38s**.

Ahead-of-time (GraalVM native-image): **4.67s** — **0.52×** tsc, **3.43×** tsgo, **4.38×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | eea3179f3ff1 | **3.82s** | 50,942 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | eea3179f3ff1 | **3.54s** | 55,047 LOC/s | 46 |
| xtsc (cold JVM) | eea3179f3ff1 | 20.54s | 9,479 LOC/s | 46 |
| tsc | 6.0.3 | 10.90s | 17,869 LOC/s | 65 |
| tsgo | 7.0.2 | 2.02s | 96,244 LOC/s | 65 |
| xtsc-native | eea3179f3ff1 (AOT) | 5.85s | 33,299 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.85x FASTER than tsc; 1.89x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.54s**, **-7.5%** against `temurin26` on identical bytecode — 0.32× tsc, 1.75× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.89×** tsc and **10.15×** tsgo; tsc is **5.39×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.43s**.

Ahead-of-time (GraalVM native-image): **5.85s** — **0.54×** tsc, **2.89×** tsgo, **3.51×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
