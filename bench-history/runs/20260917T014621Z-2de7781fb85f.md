# Bench run 20260917T014621Z

- **xtsc revision**: [`2de7781fb85f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/2de7781fb85f2cc2d9d8ed8e9a3edf12047aafa8)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 2de7781fb85f2cc2d9d8ed8e9a3edf12047aafa8

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 2de7781fb85f | **4.12s** | 47,200 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 2de7781fb85f | **3.82s** | 50,929 LOC/s | 46 |
| xtsc (cold JVM) | 2de7781fb85f | 21.09s | 9,229 LOC/s | 46 |
| tsc | 6.0.3 | 8.07s | 24,135 LOC/s | 65 |
| tsgo | 7.0.2 | 1.29s | 151,166 LOC/s | 65 |
| xtsc-native | 2de7781fb85f (AOT) | 6.42s | 30,313 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.96x FASTER than tsc; 3.20x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.82s**, **-7.3%** against `temurin26` on identical bytecode — 0.47× tsc, 2.97× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.61×** tsc and **16.38×** tsgo; tsc is **6.26×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.00s**.

Ahead-of-time (GraalVM native-image): **6.42s** — **0.80×** tsc, **4.99×** tsgo, **3.28×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 2de7781fb85f | **4.80s** | 40,529 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 2de7781fb85f | **4.39s** | 44,320 LOC/s | 46 |
| xtsc (cold JVM) | 2de7781fb85f | 24.04s | 8,098 LOC/s | 46 |
| tsc | 6.0.3 | 9.82s | 19,821 LOC/s | 65 |
| tsgo | 7.0.2 | 1.86s | 104,678 LOC/s | 65 |
| xtsc-native | 2de7781fb85f (AOT) | 7.60s | 25,632 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.04x FASTER than tsc; 2.58x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.39s**, **-8.6%** against `temurin26` on identical bytecode — 0.45× tsc, 2.36× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.45×** tsc and **12.93×** tsgo; tsc is **5.28×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.94s**.

Ahead-of-time (GraalVM native-image): **7.60s** — **0.77×** tsc, **4.08×** tsgo, **3.17×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
