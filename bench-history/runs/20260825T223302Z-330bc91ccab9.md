# Bench run 20260825T223302Z

- **xtsc revision**: [`330bc91ccab9`](https://github.com/xemantic/xemantic-typescript-compiler/commit/330bc91ccab973579f17cb44eaea595e3bbe54ea)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 330bc91ccab973579f17cb44eaea595e3bbe54ea

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 330bc91ccab9 | **3.91s** | 49,821 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 330bc91ccab9 | **3.56s** | 54,630 LOC/s | 46 |
| xtsc (cold JVM) | 330bc91ccab9 | 21.80s | 8,929 LOC/s | 46 |
| tsc | 6.0.3 | 10.22s | 19,049 LOC/s | 65 |
| tsgo | 7.0.2 | 1.54s | 126,759 LOC/s | 65 |
| xtsc-native | 330bc91ccab9 (AOT) | 6.40s | 30,441 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.62x FASTER than tsc; 2.54x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.56s**, **-8.8%** against `temurin26` on identical bytecode — 0.35× tsc, 2.32× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.13×** tsc and **14.20×** tsgo; tsc is **6.65×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.69s**.

Ahead-of-time (GraalVM native-image): **6.40s** — **0.63×** tsc, **4.16×** tsgo, **3.41×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 330bc91ccab9 | **4.70s** | 41,452 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 330bc91ccab9 | **4.32s** | 45,069 LOC/s | 46 |
| xtsc (cold JVM) | 330bc91ccab9 | 23.88s | 8,151 LOC/s | 46 |
| tsc | 6.0.3 | 12.40s | 15,696 LOC/s | 65 |
| tsgo | 7.0.2 | 2.28s | 85,545 LOC/s | 65 |
| xtsc-native | 330bc91ccab9 (AOT) | 7.87s | 24,736 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.64x FASTER than tsc; 2.06x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.32s**, **-8.0%** against `temurin26` on identical bytecode — 0.35× tsc, 1.90× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.93×** tsc and **10.49×** tsgo; tsc is **5.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.76s**.

Ahead-of-time (GraalVM native-image): **7.87s** — **0.63×** tsc, **3.46×** tsgo, **3.03×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
