# Bench run 20260818T081249Z

- **xtsc revision**: [`4d7b5673d280`](https://github.com/xemantic/xemantic-typescript-compiler/commit/4d7b5673d280adf9abed0b1e051266bdf4fba4bc)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 4d7b5673d280adf9abed0b1e051266bdf4fba4bc

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 4d7b5673d280 | **2.59s** | 75,261 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 4d7b5673d280 | **2.63s** | 73,890 LOC/s | 46 |
| xtsc (cold JVM) | 4d7b5673d280 | 16.46s | 11,827 LOC/s | 46 |
| tsc | 6.0.3 | 7.27s | 26,781 LOC/s | 65 |
| tsgo | 7.0.2 | 1.10s | 177,647 LOC/s | 65 |
| xtsc-native | 4d7b5673d280 (AOT) | 4.17s | 46,635 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.81x FASTER than tsc; 2.36x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.63s**, **+1.9%** against `temurin26` on identical bytecode — 0.36× tsc, 2.40× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.26×** tsc and **15.02×** tsgo; tsc is **6.63×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **16.29s**.

Ahead-of-time (GraalVM native-image): **4.17s** — **0.57×** tsc, **3.81×** tsgo, **3.94×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 4d7b5673d280 | **3.43s** | 56,764 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 4d7b5673d280 | **3.15s** | 61,869 LOC/s | 46 |
| xtsc (cold JVM) | 4d7b5673d280 | 17.48s | 11,135 LOC/s | 46 |
| tsc | 6.0.3 | 8.75s | 22,241 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,797 LOC/s | 65 |
| xtsc-native | 4d7b5673d280 (AOT) | 5.28s | 36,896 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.55x FASTER than tsc; 2.06x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.15s**, **-8.3%** against `temurin26` on identical bytecode — 0.36× tsc, 1.89× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.00×** tsc and **10.49×** tsgo; tsc is **5.25×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.26s**.

Ahead-of-time (GraalVM native-image): **5.28s** — **0.60×** tsc, **3.17×** tsgo, **3.31×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
