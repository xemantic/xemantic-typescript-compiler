# Bench run 20260809T011514Z

- **xtsc revision**: [`9595f183babd`](https://github.com/xemantic/xemantic-typescript-compiler/commit/9595f183babd780c92c47454706897c63261e95c)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 9595f183babd780c92c47454706897c63261e95c

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 9595f183babd | 24.55s | 7,929 LOC/s | 46 |
| tsc | 6.0.3 | 10.78s | 18,068 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.67s | 116,448 LOC/s | 65 |
| xtsc-native | 9595f183babd (AOT) | 10.81s | 18,017 LOC/s | 46 |

Relative (wall): xtsc is **2.28×** tsc and **14.69×** tsgo; tsc is **6.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.44s**.

Ahead-of-time (GraalVM native-image): **10.81s**, **2.27×** the JVM arm, **1.00×** tsc, **6.46×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 9595f183babd | 29.47s | 6,606 LOC/s | 46 |
| tsc | 6.0.3 | 13.30s | 14,635 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.42s | 80,488 LOC/s | 65 |
| xtsc-native | 9595f183babd (AOT) | 12.99s | 14,989 LOC/s | 46 |

Relative (wall): xtsc is **2.22×** tsc and **12.18×** tsgo; tsc is **5.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.33s**.

Ahead-of-time (GraalVM native-image): **12.99s**, **2.27×** the JVM arm, **0.98×** tsc, **5.37×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
