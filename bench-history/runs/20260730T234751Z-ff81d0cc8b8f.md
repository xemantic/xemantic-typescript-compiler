# Bench run 20260730T234751Z

- **xtsc revision**: [`ff81d0cc8b8f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/ff81d0cc8b8f18e5788e9ac52bf8ef6ebffe1e5d)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push ff81d0cc8b8f18e5788e9ac52bf8ef6ebffe1e5d

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | ff81d0cc8b8f | 27.29s | 7,134 LOC/s | 46 |
| tsc | 6.0.3 | 10.73s | 18,138 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.70s | 114,597 LOC/s | 65 |
| xtsc-native | ff81d0cc8b8f (AOT) | 12.66s | 15,376 LOC/s | 46 |

Relative (wall): xtsc is **2.54×** tsc and **16.06×** tsgo; tsc is **6.32×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.19s**.

Ahead-of-time (GraalVM native-image): **12.66s**, **2.16×** the JVM arm, **1.18×** tsc, **7.45×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | ff81d0cc8b8f | 34.10s | 5,709 LOC/s | 46 |
| tsc | 6.0.3 | 13.39s | 14,540 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.64s | 73,806 LOC/s | 65 |
| xtsc-native | ff81d0cc8b8f (AOT) | 14.90s | 13,070 LOC/s | 46 |

Relative (wall): xtsc is **2.55×** tsc and **12.93×** tsgo; tsc is **5.08×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **34.00s**.

Ahead-of-time (GraalVM native-image): **14.90s**, **2.29×** the JVM arm, **1.11×** tsc, **5.65×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
