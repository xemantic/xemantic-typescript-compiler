# Bench run 20260828T183240Z

- **xtsc revision**: [`562bddd72595`](https://github.com/xemantic/xemantic-typescript-compiler/commit/562bddd7259527738e26070d4d01e4f1fbf5429e)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 562bddd7259527738e26070d4d01e4f1fbf5429e

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 562bddd72595 | **2.96s** | 65,799 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 562bddd72595 | **2.63s** | 74,031 LOC/s | 46 |
| xtsc (cold JVM) | 562bddd72595 | 17.15s | 11,354 LOC/s | 46 |
| tsc | 6.0.3 | 7.02s | 27,743 LOC/s | 65 |
| tsgo | 7.0.2 | 1.07s | 181,964 LOC/s | 65 |
| xtsc-native | 562bddd72595 (AOT) | 4.37s | 44,533 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.37x FASTER than tsc; 2.77x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.63s**, **-11.1%** against `temurin26` on identical bytecode — 0.37× tsc, 2.46× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.44×** tsc and **16.03×** tsgo; tsc is **6.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.04s**.

Ahead-of-time (GraalVM native-image): **4.37s** — **0.62×** tsc, **4.09×** tsgo, **3.92×** the COLD JVM arm and **1.48×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 562bddd72595 | **3.47s** | 56,110 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 562bddd72595 | **3.33s** | 58,416 LOC/s | 46 |
| xtsc (cold JVM) | 562bddd72595 | 16.55s | 11,761 LOC/s | 46 |
| tsc | 6.0.3 | 8.60s | 22,631 LOC/s | 65 |
| tsgo | 7.0.2 | 1.60s | 121,917 LOC/s | 65 |
| xtsc-native | 562bddd72595 (AOT) | 5.21s | 37,370 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.48x FASTER than tsc; 2.17x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.33s**, **-3.9%** against `temurin26` on identical bytecode — 0.39× tsc, 2.09× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.92×** tsc and **10.37×** tsgo; tsc is **5.39×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **16.46s**.

Ahead-of-time (GraalVM native-image): **5.21s** — **0.61×** tsc, **3.26×** tsgo, **3.18×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
