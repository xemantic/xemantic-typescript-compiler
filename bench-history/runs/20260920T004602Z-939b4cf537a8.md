# Bench run 20260920T004602Z

- **xtsc revision**: [`939b4cf537a8`](https://github.com/xemantic/xemantic-typescript-compiler/commit/939b4cf537a8b92d2183c897a7c637bbb37fe5b1)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 939b4cf537a8b92d2183c897a7c637bbb37fe5b1

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 939b4cf537a8 | **5.85s** | 33,299 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 939b4cf537a8 | **5.23s** | 37,256 LOC/s | 46 |
| xtsc (cold JVM) | 939b4cf537a8 | 24.87s | 7,828 LOC/s | 46 |
| tsc | 6.0.3 | 10.85s | 17,941 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,276 LOC/s | 65 |
| xtsc-native | 939b4cf537a8 (AOT) | 9.24s | 21,073 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.86x FASTER than tsc; 3.46x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.23s**, **-10.6%** against `temurin26` on identical bytecode — 0.48× tsc, 3.09× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.29×** tsc and **14.73×** tsgo; tsc is **6.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.76s**.

Ahead-of-time (GraalVM native-image): **9.24s** — **0.85×** tsc, **5.47×** tsgo, **2.69×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 939b4cf537a8 | **6.63s** | 29,371 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 939b4cf537a8 | **5.87s** | 33,180 LOC/s | 46 |
| xtsc (cold JVM) | 939b4cf537a8 | 30.42s | 6,399 LOC/s | 46 |
| tsc | 6.0.3 | 13.28s | 14,663 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,807 LOC/s | 65 |
| xtsc-native | 939b4cf537a8 (AOT) | 10.99s | 17,717 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.00x FASTER than tsc; 2.79x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.87s**, **-11.5%** against `temurin26` on identical bytecode — 0.44× tsc, 2.47× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.29×** tsc and **12.78×** tsgo; tsc is **5.58×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.27s**.

Ahead-of-time (GraalVM native-image): **10.99s** — **0.83×** tsc, **4.62×** tsgo, **2.77×** the COLD JVM arm and **1.66×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
