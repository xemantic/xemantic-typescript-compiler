# Bench run 20260920T190342Z

- **xtsc revision**: [`8d9d7c0dc476`](https://github.com/xemantic/xemantic-typescript-compiler/commit/8d9d7c0dc476bd0dea4514021bf3d0bf48648716)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 8d9d7c0dc476bd0dea4514021bf3d0bf48648716

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8d9d7c0dc476 | **6.09s** | 31,976 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8d9d7c0dc476 | **5.41s** | 35,982 LOC/s | 46 |
| xtsc (cold JVM) | 8d9d7c0dc476 | 27.30s | 7,131 LOC/s | 46 |
| tsc | 6.0.3 | 10.96s | 17,766 LOC/s | 65 |
| tsgo | 7.0.2 | 1.79s | 108,650 LOC/s | 65 |
| xtsc-native | 8d9d7c0dc476 (AOT) | 9.46s | 20,581 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.80x FASTER than tsc; 3.40x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.41s**, **-11.1%** against `temurin26` on identical bytecode — 0.49× tsc, 3.02× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.49×** tsc and **15.24×** tsgo; tsc is **6.12×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.19s**.

Ahead-of-time (GraalVM native-image): **9.46s** — **0.86×** tsc, **5.28×** tsgo, **2.89×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8d9d7c0dc476 | **6.83s** | 28,523 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8d9d7c0dc476 | **6.13s** | 31,751 LOC/s | 46 |
| xtsc (cold JVM) | 8d9d7c0dc476 | 28.59s | 6,809 LOC/s | 46 |
| tsc | 6.0.3 | 13.57s | 14,346 LOC/s | 65 |
| tsgo | 7.0.2 | 2.43s | 80,124 LOC/s | 65 |
| xtsc-native | 8d9d7c0dc476 (AOT) | 10.87s | 17,920 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.99x FASTER than tsc; 2.81x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.13s**, **-10.2%** against `temurin26` on identical bytecode — 0.45× tsc, 2.52× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.11×** tsc and **11.77×** tsgo; tsc is **5.58×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.46s**.

Ahead-of-time (GraalVM native-image): **10.87s** — **0.80×** tsc, **4.47×** tsgo, **2.63×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
