# Bench run 20260805T234916Z

- **xtsc revision**: [`97edf882c018`](https://github.com/xemantic/xemantic-typescript-compiler/commit/97edf882c01894ff03064872b9d1ce14eb15c718)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 97edf882c01894ff03064872b9d1ce14eb15c718

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 97edf882c018 | 21.74s | 8,957 LOC/s | 46 |
| tsc | 6.0.3 | 8.38s | 23,239 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.28s | 152,110 LOC/s | 65 |
| xtsc-native | 97edf882c018 (AOT) | 9.58s | 20,317 LOC/s | 46 |

Relative (wall): xtsc is **2.59×** tsc and **16.98×** tsgo; tsc is **6.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.65s**.

Ahead-of-time (GraalVM native-image): **9.58s**, **2.27×** the JVM arm, **1.14×** tsc, **7.49×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 97edf882c018 | 20.40s | 9,546 LOC/s | 46 |
| tsc | 6.0.3 | 10.23s | 19,032 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.85s | 105,301 LOC/s | 65 |
| xtsc-native | 97edf882c018 (AOT) | 10.04s | 19,388 LOC/s | 46 |

Relative (wall): xtsc is **1.99×** tsc and **11.03×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.31s**.

Ahead-of-time (GraalVM native-image): **10.04s**, **2.03×** the JVM arm, **0.98×** tsc, **5.43×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
