# Bench run 20260808T185837Z

- **xtsc revision**: [`d99a33211582`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d99a332115826f5783e1e491f8aaaa7bd83f04bc)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push d99a332115826f5783e1e491f8aaaa7bd83f04bc

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | d99a33211582 | 20.35s | 9,566 LOC/s | 46 |
| tsc | 6.0.3 | 8.08s | 24,093 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.24s | 157,271 LOC/s | 65 |
| xtsc-native | d99a33211582 (AOT) | 7.47s | 26,054 LOC/s | 46 |

Relative (wall): xtsc is **2.52×** tsc and **16.44×** tsgo; tsc is **6.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.27s**.

Ahead-of-time (GraalVM native-image): **7.47s**, **2.72×** the JVM arm, **0.92×** tsc, **6.04×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | d99a33211582 | 22.17s | 8,781 LOC/s | 46 |
| tsc | 6.0.3 | 9.82s | 19,831 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.85s | 105,017 LOC/s | 65 |
| xtsc-native | d99a33211582 (AOT) | 8.91s | 21,854 LOC/s | 46 |

Relative (wall): xtsc is **2.26×** tsc and **11.96×** tsgo; tsc is **5.30×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.08s**.

Ahead-of-time (GraalVM native-image): **8.91s**, **2.49×** the JVM arm, **0.91×** tsc, **4.81×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
