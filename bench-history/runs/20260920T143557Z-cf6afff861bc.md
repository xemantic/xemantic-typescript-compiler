# Bench run 20260920T143557Z

- **xtsc revision**: [`cf6afff861bc`](https://github.com/xemantic/xemantic-typescript-compiler/commit/cf6afff861bc79e015bf7138b2b233bc421c01b9)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push cf6afff861bc79e015bf7138b2b233bc421c01b9

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cf6afff861bc | **4.97s** | 39,191 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cf6afff861bc | **4.40s** | 44,290 LOC/s | 46 |
| xtsc (cold JVM) | cf6afff861bc | 22.05s | 8,828 LOC/s | 46 |
| tsc | 6.0.3 | 9.47s | 20,555 LOC/s | 65 |
| tsgo | 7.0.2 | 1.45s | 134,277 LOC/s | 65 |
| xtsc-native | cf6afff861bc (AOT) | 7.52s | 25,901 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.91x FASTER than tsc; 3.43x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.40s**, **-11.5%** against `temurin26` on identical bytecode — 0.46× tsc, 3.03× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.33×** tsc and **15.21×** tsgo; tsc is **6.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.95s**.

Ahead-of-time (GraalVM native-image): **7.52s** — **0.79×** tsc, **5.18×** tsgo, **2.93×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cf6afff861bc | **6.69s** | 29,094 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cf6afff861bc | **5.25s** | 37,086 LOC/s | 46 |
| xtsc (cold JVM) | cf6afff861bc | 35.02s | 5,559 LOC/s | 46 |
| tsc | 6.0.3 | 11.52s | 16,896 LOC/s | 65 |
| tsgo | 7.0.2 | 2.17s | 89,807 LOC/s | 65 |
| xtsc-native | cf6afff861bc (AOT) | 9.04s | 21,530 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.72x FASTER than tsc; 3.09x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.25s**, **-21.5%** against `temurin26` on identical bytecode — 0.46× tsc, 2.42× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.04×** tsc and **16.15×** tsgo; tsc is **5.32×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **34.90s**.

Ahead-of-time (GraalVM native-image): **9.04s** — **0.78×** tsc, **4.17×** tsgo, **3.87×** the COLD JVM arm and **1.35×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
