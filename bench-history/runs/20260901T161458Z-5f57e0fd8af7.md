# Bench run 20260901T161458Z

- **xtsc revision**: [`5f57e0fd8af7`](https://github.com/xemantic/xemantic-typescript-compiler/commit/5f57e0fd8af7029d3b74390ddaba8dbbe9126fb0)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 5f57e0fd8af7029d3b74390ddaba8dbbe9126fb0

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5f57e0fd8af7 | **4.15s** | 46,882 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5f57e0fd8af7 | **3.93s** | 49,529 LOC/s | 46 |
| xtsc (cold JVM) | 5f57e0fd8af7 | 21.87s | 8,902 LOC/s | 46 |
| tsc | 6.0.3 | 10.62s | 18,340 LOC/s | 65 |
| tsgo | 7.0.2 | 1.63s | 119,229 LOC/s | 65 |
| xtsc-native | 5f57e0fd8af7 (AOT) | 6.25s | 31,147 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.56x FASTER than tsc; 2.54x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.93s**, **-5.3%** against `temurin26` on identical bytecode — 0.37× tsc, 2.41× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.06×** tsc and **13.39×** tsgo; tsc is **6.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.76s**.

Ahead-of-time (GraalVM native-image): **6.25s** — **0.59×** tsc, **3.83×** tsgo, **3.50×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5f57e0fd8af7 | **5.11s** | 38,124 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5f57e0fd8af7 | **4.59s** | 42,409 LOC/s | 46 |
| xtsc (cold JVM) | 5f57e0fd8af7 | 26.11s | 7,457 LOC/s | 46 |
| tsc | 6.0.3 | 12.86s | 15,135 LOC/s | 65 |
| tsgo | 7.0.2 | 2.37s | 82,187 LOC/s | 65 |
| xtsc-native | 5f57e0fd8af7 (AOT) | 7.80s | 24,968 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.52x FASTER than tsc; 2.16x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.59s**, **-10.1%** against `temurin26` on identical bytecode — 0.36× tsc, 1.94× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.03×** tsc and **11.02×** tsgo; tsc is **5.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.97s**.

Ahead-of-time (GraalVM native-image): **7.80s** — **0.61×** tsc, **3.29×** tsgo, **3.35×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
