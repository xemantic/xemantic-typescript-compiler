# Bench run 20260907T122241Z

- **xtsc revision**: [`1917f1ca3c40`](https://github.com/xemantic/xemantic-typescript-compiler/commit/1917f1ca3c40034d99f7a3bad000a4c4b289319f)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 1917f1ca3c40034d99f7a3bad000a4c4b289319f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1917f1ca3c40 | **14.44s** | 13,484 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1917f1ca3c40 | **14.80s** | 13,151 LOC/s | 46 |
| xtsc (cold JVM) | 1917f1ca3c40 | 34.97s | 5,567 LOC/s | 46 |
| tsc | 6.0.3 | 10.79s | 18,037 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,378 LOC/s | 65 |
| xtsc-native | 1917f1ca3c40 (AOT) | 23.12s | 8,420 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.34x tsc — parity at 1.00x; 8.63x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **14.80s**, **+2.5%** against `temurin26` on identical bytecode — 1.37× tsc, 8.85× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.24×** tsc and **20.90×** tsgo; tsc is **6.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **34.84s**.

Ahead-of-time (GraalVM native-image): **23.12s** — **2.14×** tsc, **13.82×** tsgo, **1.51×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1917f1ca3c40 | **15.64s** | 12,446 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1917f1ca3c40 | **15.73s** | 12,375 LOC/s | 46 |
| xtsc (cold JVM) | 1917f1ca3c40 | 37.96s | 5,129 LOC/s | 46 |
| tsc | 6.0.3 | 13.81s | 14,101 LOC/s | 65 |
| tsgo | 7.0.2 | 2.47s | 78,762 LOC/s | 65 |
| xtsc-native | 1917f1ca3c40 (AOT) | 24.55s | 7,932 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.13x tsc — parity at 1.00x; 6.33x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **15.73s**, **+0.6%** against `temurin26` on identical bytecode — 1.14× tsc, 6.36× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.75×** tsc and **15.36×** tsgo; tsc is **5.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **37.82s**.

Ahead-of-time (GraalVM native-image): **24.55s** — **1.78×** tsc, **9.93×** tsgo, **1.55×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
