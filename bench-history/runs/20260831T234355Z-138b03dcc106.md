# Bench run 20260831T234355Z

- **xtsc revision**: [`138b03dcc106`](https://github.com/xemantic/xemantic-typescript-compiler/commit/138b03dcc1063156ba21311dec81a99c13f7d7c8)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 138b03dcc1063156ba21311dec81a99c13f7d7c8

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 138b03dcc106 | **4.23s** | 46,007 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 138b03dcc106 | **3.96s** | 49,167 LOC/s | 46 |
| xtsc (cold JVM) | 138b03dcc106 | 21.33s | 9,127 LOC/s | 46 |
| tsc | 6.0.3 | 10.06s | 19,350 LOC/s | 65 |
| tsgo | 7.0.2 | 1.55s | 125,452 LOC/s | 65 |
| xtsc-native | 138b03dcc106 (AOT) | 6.64s | 29,309 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.38x FASTER than tsc; 2.73x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.96s**, **-6.4%** against `temurin26` on identical bytecode — 0.39× tsc, 2.55× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.12×** tsc and **13.74×** tsgo; tsc is **6.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.22s**.

Ahead-of-time (GraalVM native-image): **6.64s** — **0.66×** tsc, **4.28×** tsgo, **3.21×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 138b03dcc106 | **4.89s** | 39,791 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 138b03dcc106 | **4.45s** | 43,782 LOC/s | 46 |
| xtsc (cold JVM) | 138b03dcc106 | 26.70s | 7,291 LOC/s | 46 |
| tsc | 6.0.3 | 12.27s | 15,862 LOC/s | 65 |
| tsgo | 7.0.2 | 2.25s | 86,380 LOC/s | 65 |
| xtsc-native | 138b03dcc106 (AOT) | 8.02s | 24,277 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.51x FASTER than tsc; 2.17x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.45s**, **-9.1%** against `temurin26` on identical bytecode — 0.36× tsc, 1.97× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.18×** tsc and **11.85×** tsgo; tsc is **5.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.57s**.

Ahead-of-time (GraalVM native-image): **8.02s** — **0.65×** tsc, **3.56×** tsgo, **3.33×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
