# Bench run 20260730T230656Z

- **xtsc revision**: [`857a9070be94`](https://github.com/xemantic/xemantic-typescript-compiler/commit/857a9070be943dd92618d358b9f104954b95e772)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 857a9070be943dd92618d358b9f104954b95e772

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 857a9070be94 | 28.93s | 6,729 LOC/s | 46 |
| tsc | 6.0.3 | 10.57s | 18,421 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.66s | 117,290 LOC/s | 65 |
| xtsc-native | 857a9070be94 (AOT) | 11.35s | 17,161 LOC/s | 46 |

Relative (wall): xtsc is **2.74×** tsc and **17.43×** tsgo; tsc is **6.37×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.83s**.

Ahead-of-time (GraalVM native-image): **11.35s**, **2.55×** the JVM arm, **1.07×** tsc, **6.83×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 857a9070be94 | 28.05s | 6,941 LOC/s | 46 |
| tsc | 6.0.3 | 13.07s | 14,899 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.35s | 82,887 LOC/s | 65 |
| xtsc-native | 857a9070be94 (AOT) | 13.89s | 14,018 LOC/s | 46 |

Relative (wall): xtsc is **2.15×** tsc and **11.94×** tsgo; tsc is **5.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.95s**.

Ahead-of-time (GraalVM native-image): **13.89s**, **2.02×** the JVM arm, **1.06×** tsc, **5.91×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
