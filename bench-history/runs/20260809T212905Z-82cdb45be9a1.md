# Bench run 20260809T212905Z

- **xtsc revision**: [`82cdb45be9a1`](https://github.com/xemantic/xemantic-typescript-compiler/commit/82cdb45be9a1f246812c87f59cec6b74dbbe8649)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 82cdb45be9a1f246812c87f59cec6b74dbbe8649

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 82cdb45be9a1 | 25.34s | 7,683 LOC/s | 46 |
| tsc | 6.0.3 | 10.98s | 17,727 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.70s | 114,530 LOC/s | 65 |
| xtsc-native | 82cdb45be9a1 (AOT) | 9.94s | 19,597 LOC/s | 46 |

Relative (wall): xtsc is **2.31×** tsc and **14.91×** tsgo; tsc is **6.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.23s**.

Ahead-of-time (GraalVM native-image): **9.94s**, **2.55×** the JVM arm, **0.90×** tsc, **5.84×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 82cdb45be9a1 | 28.73s | 6,777 LOC/s | 46 |
| tsc | 6.0.3 | 13.99s | 13,920 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.51s | 77,539 LOC/s | 65 |
| xtsc-native | 82cdb45be9a1 (AOT) | 12.85s | 15,148 LOC/s | 46 |

Relative (wall): xtsc is **2.05×** tsc and **11.44×** tsgo; tsc is **5.57×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.61s**.

Ahead-of-time (GraalVM native-image): **12.85s**, **2.23×** the JVM arm, **0.92×** tsc, **5.12×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
