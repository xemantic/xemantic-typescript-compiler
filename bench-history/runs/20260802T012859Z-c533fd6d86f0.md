# Bench run 20260802T012859Z

- **xtsc revision**: [`c533fd6d86f0`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c533fd6d86f06a8fe2a18c709e060f16d67cdbfb)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push c533fd6d86f06a8fe2a18c709e060f16d67cdbfb

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | c533fd6d86f0 | 26.34s | 7,391 LOC/s | 46 |
| tsc | 6.0.3 | 11.04s | 17,639 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.73s | 112,609 LOC/s | 65 |
| xtsc-native | c533fd6d86f0 (AOT) | 11.14s | 17,479 LOC/s | 46 |

Relative (wall): xtsc is **2.39×** tsc and **15.24×** tsgo; tsc is **6.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.24s**.

Ahead-of-time (GraalVM native-image): **11.14s**, **2.36×** the JVM arm, **1.01×** tsc, **6.44×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | c533fd6d86f0 | 33.90s | 5,744 LOC/s | 46 |
| tsc | 6.0.3 | 13.65s | 14,268 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.51s | 77,601 LOC/s | 65 |
| xtsc-native | c533fd6d86f0 (AOT) | 13.59s | 14,328 LOC/s | 46 |

Relative (wall): xtsc is **2.48×** tsc and **13.51×** tsgo; tsc is **5.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **33.79s**.

Ahead-of-time (GraalVM native-image): **13.59s**, **2.49×** the JVM arm, **1.00×** tsc, **5.42×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
