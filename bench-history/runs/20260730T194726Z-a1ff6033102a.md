# Bench run 20260730T194726Z

- **xtsc revision**: [`a1ff6033102a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/a1ff6033102a2fe98878b4922ae9e450266ebc61)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push a1ff6033102a2fe98878b4922ae9e450266ebc61

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | a1ff6033102a | 30.24s | 6,439 LOC/s | 46 |
| tsc | 6.0.3 | 10.20s | 19,086 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.62s | 119,964 LOC/s | 65 |
| xtsc-native | a1ff6033102a (AOT) | 12.12s | 16,063 LOC/s | 46 |

Relative (wall): xtsc is **2.96×** tsc and **18.63×** tsgo; tsc is **6.29×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.13s**.

Ahead-of-time (GraalVM native-image): **12.12s**, **2.49×** the JVM arm, **1.19×** tsc, **7.47×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | a1ff6033102a | 29.01s | 6,712 LOC/s | 46 |
| tsc | 6.0.3 | 12.41s | 15,689 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.30s | 84,800 LOC/s | 65 |
| xtsc-native | a1ff6033102a (AOT) | 14.51s | 13,423 LOC/s | 46 |

Relative (wall): xtsc is **2.34×** tsc and **12.63×** tsgo; tsc is **5.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.89s**.

Ahead-of-time (GraalVM native-image): **14.51s**, **2.00×** the JVM arm, **1.17×** tsc, **6.32×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
