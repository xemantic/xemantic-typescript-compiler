# Bench run 20260829T200144Z

- **xtsc revision**: [`8fdc639bde28`](https://github.com/xemantic/xemantic-typescript-compiler/commit/8fdc639bde28c8f311c74ce7a0ec94f24120bc22)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 8fdc639bde28c8f311c74ce7a0ec94f24120bc22

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8fdc639bde28 | **4.41s** | 44,110 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8fdc639bde28 | **4.18s** | 46,612 LOC/s | 46 |
| xtsc (cold JVM) | 8fdc639bde28 | 23.69s | 8,220 LOC/s | 46 |
| tsc | 6.0.3 | 10.91s | 17,852 LOC/s | 65 |
| tsgo | 7.0.2 | 1.71s | 114,127 LOC/s | 65 |
| xtsc-native | 8fdc639bde28 (AOT) | 6.96s | 27,962 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.47x FASTER than tsc; 2.59x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.18s**, **-5.4%** against `temurin26` on identical bytecode — 0.38× tsc, 2.45× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.17×** tsc and **13.88×** tsgo; tsc is **6.39×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.57s**.

Ahead-of-time (GraalVM native-image): **6.96s** — **0.64×** tsc, **4.08×** tsgo, **3.40×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8fdc639bde28 | **5.20s** | 37,457 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8fdc639bde28 | **4.92s** | 39,605 LOC/s | 46 |
| xtsc (cold JVM) | 8fdc639bde28 | 29.00s | 6,713 LOC/s | 46 |
| tsc | 6.0.3 | 13.30s | 14,638 LOC/s | 65 |
| tsgo | 7.0.2 | 2.39s | 81,363 LOC/s | 65 |
| xtsc-native | 8fdc639bde28 (AOT) | 7.87s | 24,749 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.56x FASTER than tsc; 2.17x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.92s**, **-5.4%** against `temurin26` on identical bytecode — 0.37× tsc, 2.05× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.18×** tsc and **12.12×** tsgo; tsc is **5.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.86s**.

Ahead-of-time (GraalVM native-image): **7.87s** — **0.59×** tsc, **3.29×** tsgo, **3.69×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
