# Bench run 20260828T040154Z

- **xtsc revision**: [`8da2cb400b87`](https://github.com/xemantic/xemantic-typescript-compiler/commit/8da2cb400b87322dfb6f2ddac4576f7c11e8daa3)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 8da2cb400b87322dfb6f2ddac4576f7c11e8daa3

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8da2cb400b87 | **3.78s** | 51,522 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8da2cb400b87 | **3.56s** | 54,630 LOC/s | 46 |
| xtsc (cold JVM) | 8da2cb400b87 | 24.07s | 8,088 LOC/s | 46 |
| tsc | 6.0.3 | 10.08s | 19,308 LOC/s | 65 |
| tsgo | 7.0.2 | 1.54s | 126,676 LOC/s | 65 |
| xtsc-native | 8da2cb400b87 (AOT) | 5.98s | 32,537 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.67x FASTER than tsc; 2.46x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.56s**, **-5.7%** against `temurin26` on identical bytecode — 0.35× tsc, 2.32× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.39×** tsc and **15.66×** tsgo; tsc is **6.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.96s**.

Ahead-of-time (GraalVM native-image): **5.98s** — **0.59×** tsc, **3.89×** tsgo, **4.02×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8da2cb400b87 | **4.67s** | 41,665 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8da2cb400b87 | **4.29s** | 45,342 LOC/s | 46 |
| xtsc (cold JVM) | 8da2cb400b87 | 24.05s | 8,095 LOC/s | 46 |
| tsc | 6.0.3 | 12.17s | 15,999 LOC/s | 65 |
| tsgo | 7.0.2 | 2.22s | 87,624 LOC/s | 65 |
| xtsc-native | 8da2cb400b87 (AOT) | 7.33s | 26,580 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.60x FASTER than tsc; 2.10x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.29s**, **-8.1%** against `temurin26` on identical bytecode — 0.35× tsc, 1.93× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.98×** tsc and **10.82×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.93s**.

Ahead-of-time (GraalVM native-image): **7.33s** — **0.60×** tsc, **3.30×** tsgo, **3.28×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
