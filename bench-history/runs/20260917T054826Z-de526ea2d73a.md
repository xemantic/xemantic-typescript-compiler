# Bench run 20260917T054826Z

- **xtsc revision**: [`de526ea2d73a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/de526ea2d73af9ff4121df2d1cdbb3f582ea4dce)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push de526ea2d73af9ff4121df2d1cdbb3f582ea4dce

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | de526ea2d73a | **4.08s** | 47,686 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | de526ea2d73a | **3.86s** | 50,401 LOC/s | 46 |
| xtsc (cold JVM) | de526ea2d73a | 20.21s | 9,634 LOC/s | 46 |
| tsc | 6.0.3 | 8.25s | 23,591 LOC/s | 65 |
| tsgo | 7.0.2 | 1.25s | 155,141 LOC/s | 65 |
| xtsc-native | de526ea2d73a (AOT) | 6.43s | 30,294 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.02x FASTER than tsc; 3.25x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.86s**, **-5.4%** against `temurin26` on identical bytecode — 0.47× tsc, 3.08× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.45×** tsc and **16.10×** tsgo; tsc is **6.58×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.09s**.

Ahead-of-time (GraalVM native-image): **6.43s** — **0.78×** tsc, **5.12×** tsgo, **3.14×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | de526ea2d73a | **4.92s** | 39,597 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | de526ea2d73a | **4.54s** | 42,867 LOC/s | 46 |
| xtsc (cold JVM) | de526ea2d73a | 21.75s | 8,953 LOC/s | 46 |
| tsc | 6.0.3 | 9.91s | 19,650 LOC/s | 65 |
| tsgo | 7.0.2 | 1.86s | 104,509 LOC/s | 65 |
| xtsc-native | de526ea2d73a (AOT) | 7.72s | 25,210 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.02x FASTER than tsc; 2.64x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.54s**, **-7.6%** against `temurin26` on identical bytecode — 0.46× tsc, 2.44× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.19×** tsc and **11.67×** tsgo; tsc is **5.32×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.65s**.

Ahead-of-time (GraalVM native-image): **7.72s** — **0.78×** tsc, **4.15×** tsgo, **2.82×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
