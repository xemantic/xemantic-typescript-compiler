# Bench run 20260901T124733Z

- **xtsc revision**: [`550019b6ffdc`](https://github.com/xemantic/xemantic-typescript-compiler/commit/550019b6ffdc3b6b25cd955f693dbc4ef8b713b3)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 550019b6ffdc3b6b25cd955f693dbc4ef8b713b3

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 550019b6ffdc | **4.37s** | 44,574 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 550019b6ffdc | **4.06s** | 47,908 LOC/s | 46 |
| xtsc (cold JVM) | 550019b6ffdc | 24.43s | 7,968 LOC/s | 46 |
| tsc | 6.0.3 | 10.80s | 18,031 LOC/s | 65 |
| tsgo | 7.0.2 | 1.64s | 118,720 LOC/s | 65 |
| xtsc-native | 550019b6ffdc (AOT) | 6.92s | 28,127 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.47x FASTER than tsc; 2.66x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.06s**, **-7.0%** against `temurin26` on identical bytecode — 0.38× tsc, 2.48× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.26×** tsc and **14.90×** tsgo; tsc is **6.58×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.32s**.

Ahead-of-time (GraalVM native-image): **6.92s** — **0.64×** tsc, **4.22×** tsgo, **3.53×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 550019b6ffdc | **5.27s** | 36,973 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 550019b6ffdc | **4.57s** | 42,613 LOC/s | 46 |
| xtsc (cold JVM) | 550019b6ffdc | 25.89s | 7,518 LOC/s | 46 |
| tsc | 6.0.3 | 13.42s | 14,506 LOC/s | 65 |
| tsgo | 7.0.2 | 2.48s | 78,382 LOC/s | 65 |
| xtsc-native | 550019b6ffdc (AOT) | 8.88s | 21,933 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.55x FASTER than tsc; 2.12x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.57s**, **-13.2%** against `temurin26` on identical bytecode — 0.34× tsc, 1.84× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.93×** tsc and **10.42×** tsgo; tsc is **5.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.76s**.

Ahead-of-time (GraalVM native-image): **8.88s** — **0.66×** tsc, **3.57×** tsgo, **2.92×** the COLD JVM arm and **1.69×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
