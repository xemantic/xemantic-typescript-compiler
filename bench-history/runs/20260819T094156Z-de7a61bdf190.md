# Bench run 20260819T094156Z

- **xtsc revision**: [`de7a61bdf190`](https://github.com/xemantic/xemantic-typescript-compiler/commit/de7a61bdf190549c4d90310ef64cd46cac47ea5f)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push de7a61bdf190549c4d90310ef64cd46cac47ea5f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | de7a61bdf190 | **4.01s** | 48,493 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | de7a61bdf190 | **3.72s** | 52,283 LOC/s | 46 |
| xtsc (cold JVM) | de7a61bdf190 | 24.51s | 7,942 LOC/s | 46 |
| tsc | 6.0.3 | 10.87s | 17,906 LOC/s | 65 |
| tsgo | 7.0.2 | 1.72s | 113,330 LOC/s | 65 |
| xtsc-native | de7a61bdf190 (AOT) | 6.21s | 31,358 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.71x FASTER than tsc; 2.34x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.72s**, **-7.2%** against `temurin26` on identical bytecode — 0.34× tsc, 2.17× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.25×** tsc and **14.27×** tsgo; tsc is **6.33×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.39s**.

Ahead-of-time (GraalVM native-image): **6.21s** — **0.57×** tsc, **3.61×** tsgo, **3.95×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | de7a61bdf190 | **4.67s** | 41,718 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | de7a61bdf190 | **4.33s** | 45,007 LOC/s | 46 |
| xtsc (cold JVM) | de7a61bdf190 | 24.01s | 8,110 LOC/s | 46 |
| tsc | 6.0.3 | 13.06s | 14,909 LOC/s | 65 |
| tsgo | 7.0.2 | 2.43s | 80,157 LOC/s | 65 |
| xtsc-native | de7a61bdf190 (AOT) | 7.14s | 27,261 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.80x FASTER than tsc; 1.92x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.33s**, **-7.3%** against `temurin26` on identical bytecode — 0.33× tsc, 1.78× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.84×** tsc and **9.88×** tsgo; tsc is **5.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.89s**.

Ahead-of-time (GraalVM native-image): **7.14s** — **0.55×** tsc, **2.94×** tsgo, **3.36×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
