# Bench run 20260730T203103Z

- **xtsc revision**: [`7028481cdfbd`](https://github.com/xemantic/xemantic-typescript-compiler/commit/7028481cdfbddf06427143c65f36b6af29e32b21)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 7028481cdfbddf06427143c65f36b6af29e32b21

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 7028481cdfbd | 30.85s | 6,312 LOC/s | 46 |
| tsc | 6.0.3 | 10.66s | 18,268 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.69s | 114,936 LOC/s | 65 |
| xtsc-native | 7028481cdfbd (AOT) | 12.29s | 15,838 LOC/s | 46 |

Relative (wall): xtsc is **2.89×** tsc and **18.21×** tsgo; tsc is **6.29×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.74s**.

Ahead-of-time (GraalVM native-image): **12.29s**, **2.51×** the JVM arm, **1.15×** tsc, **7.26×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 7028481cdfbd | 29.35s | 6,634 LOC/s | 46 |
| tsc | 6.0.3 | 13.05s | 14,919 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.36s | 82,570 LOC/s | 65 |
| xtsc-native | 7028481cdfbd (AOT) | 13.82s | 14,086 LOC/s | 46 |

Relative (wall): xtsc is **2.25×** tsc and **12.45×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.24s**.

Ahead-of-time (GraalVM native-image): **13.82s**, **2.12×** the JVM arm, **1.06×** tsc, **5.86×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
