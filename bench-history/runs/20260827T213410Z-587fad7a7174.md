# Bench run 20260827T213410Z

- **xtsc revision**: [`587fad7a7174`](https://github.com/xemantic/xemantic-typescript-compiler/commit/587fad7a7174f2fa7b03d48af6ac02a8d872ca1b)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 587fad7a7174f2fa7b03d48af6ac02a8d872ca1b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 587fad7a7174 | **4.00s** | 48,724 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 587fad7a7174 | **3.96s** | 49,192 LOC/s | 46 |
| xtsc (cold JVM) | 587fad7a7174 | 21.66s | 8,988 LOC/s | 46 |
| tsc | 6.0.3 | 10.87s | 17,910 LOC/s | 65 |
| tsgo | 7.0.2 | 1.70s | 114,733 LOC/s | 65 |
| xtsc-native | 587fad7a7174 (AOT) | 6.72s | 28,973 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.72x FASTER than tsc; 2.35x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.96s**, **-1.0%** against `temurin26` on identical bytecode — 0.36× tsc, 2.33× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.99×** tsc and **12.76×** tsgo; tsc is **6.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.55s**.

Ahead-of-time (GraalVM native-image): **6.72s** — **0.62×** tsc, **3.96×** tsgo, **3.22×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 587fad7a7174 | **4.99s** | 39,041 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 587fad7a7174 | **4.49s** | 43,373 LOC/s | 46 |
| xtsc (cold JVM) | 587fad7a7174 | 26.86s | 7,247 LOC/s | 46 |
| tsc | 6.0.3 | 13.36s | 14,576 LOC/s | 65 |
| tsgo | 7.0.2 | 2.40s | 80,990 LOC/s | 65 |
| xtsc-native | 587fad7a7174 (AOT) | 7.66s | 25,404 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.68x FASTER than tsc; 2.07x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.49s**, **-10.0%** against `temurin26` on identical bytecode — 0.34× tsc, 1.87× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.01×** tsc and **11.17×** tsgo; tsc is **5.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.73s**.

Ahead-of-time (GraalVM native-image): **7.66s** — **0.57×** tsc, **3.19×** tsgo, **3.51×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
