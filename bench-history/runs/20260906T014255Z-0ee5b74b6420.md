# Bench run 20260906T014255Z

- **xtsc revision**: [`0ee5b74b6420`](https://github.com/xemantic/xemantic-typescript-compiler/commit/0ee5b74b6420242dc560a3db3e4e5f482cd498b6)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 0ee5b74b6420242dc560a3db3e4e5f482cd498b6

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0ee5b74b6420 | **14.51s** | 13,420 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0ee5b74b6420 | **14.49s** | 13,438 LOC/s | 46 |
| xtsc (cold JVM) | 0ee5b74b6420 | 32.33s | 6,022 LOC/s | 46 |
| tsc | 6.0.3 | 10.23s | 19,026 LOC/s | 65 |
| tsgo | 7.0.2 | 1.56s | 124,489 LOC/s | 65 |
| xtsc-native | 0ee5b74b6420 (AOT) | 23.68s | 8,221 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.42x tsc — parity at 1.00x; 9.28x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **14.49s**, **-0.1%** against `temurin26` on identical bytecode — 1.42× tsc, 9.26× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.16×** tsc and **20.67×** tsgo; tsc is **6.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **32.19s**.

Ahead-of-time (GraalVM native-image): **23.68s** — **2.31×** tsc, **15.14×** tsgo, **1.37×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0ee5b74b6420 | **14.96s** | 13,019 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0ee5b74b6420 | **14.90s** | 13,066 LOC/s | 46 |
| xtsc (cold JVM) | 0ee5b74b6420 | 37.28s | 5,222 LOC/s | 46 |
| tsc | 6.0.3 | 12.46s | 15,622 LOC/s | 65 |
| tsgo | 7.0.2 | 2.31s | 84,359 LOC/s | 65 |
| xtsc-native | 0ee5b74b6420 (AOT) | 25.11s | 7,752 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.20x tsc — parity at 1.00x; 6.48x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **14.90s**, **-0.4%** against `temurin26` on identical bytecode — 1.20× tsc, 6.46× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.99×** tsc and **16.15×** tsgo; tsc is **5.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **37.16s**.

Ahead-of-time (GraalVM native-image): **25.11s** — **2.02×** tsc, **10.88×** tsgo, **1.48×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
