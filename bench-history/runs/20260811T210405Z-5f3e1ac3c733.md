# Bench run 20260811T210405Z

- **xtsc revision**: [`5f3e1ac3c733`](https://github.com/xemantic/xemantic-typescript-compiler/commit/5f3e1ac3c733d12c9586e861f21930b40301b9c3)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 5f3e1ac3c733d12c9586e861f21930b40301b9c3

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5f3e1ac3c733 | **4.32s** | 45,111 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5f3e1ac3c733 | **4.06s** | 47,920 LOC/s | 46 |
| xtsc (cold JVM) | 5f3e1ac3c733 | 26.99s | 7,214 LOC/s | 46 |
| tsc | 6.0.3 | 11.66s | 16,696 LOC/s | 65 |
| tsgo | 7.0.2 | 1.74s | 112,155 LOC/s | 65 |
| xtsc-native | 5f3e1ac3c733 (AOT) | 6.87s | 28,357 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.70x FASTER than tsc; 2.49x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.06s**, **-5.9%** against `temurin26` on identical bytecode — 0.35× tsc, 2.34× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.31×** tsc and **15.55×** tsgo; tsc is **6.72×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.88s**.

Ahead-of-time (GraalVM native-image): **6.87s** — **0.59×** tsc, **3.96×** tsgo, **3.93×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5f3e1ac3c733 | **5.60s** | 34,774 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5f3e1ac3c733 | **4.83s** | 40,285 LOC/s | 46 |
| xtsc (cold JVM) | 5f3e1ac3c733 | 28.81s | 6,757 LOC/s | 46 |
| tsc | 6.0.3 | 14.01s | 13,895 LOC/s | 65 |
| tsgo | 7.0.2 | 2.56s | 75,907 LOC/s | 65 |
| xtsc-native | 5f3e1ac3c733 (AOT) | 8.40s | 23,189 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.50x FASTER than tsc; 2.18x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.83s**, **-13.7%** against `temurin26` on identical bytecode — 0.34× tsc, 1.88× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.06×** tsc and **11.23×** tsgo; tsc is **5.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.67s**.

Ahead-of-time (GraalVM native-image): **8.40s** — **0.60×** tsc, **3.27×** tsgo, **3.43×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
