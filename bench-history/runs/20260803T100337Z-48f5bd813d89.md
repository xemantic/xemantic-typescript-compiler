# Bench run 20260803T100337Z

- **xtsc revision**: [`48f5bd813d89`](https://github.com/xemantic/xemantic-typescript-compiler/commit/48f5bd813d8968e499df7e09a4e4ddebf36c9b7d)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 48f5bd813d8968e499df7e09a4e4ddebf36c9b7d

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 48f5bd813d89 | 23.26s | 8,371 LOC/s | 46 |
| tsc | 6.0.3 | 10.48s | 18,571 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.66s | 117,431 LOC/s | 65 |
| xtsc-native | 48f5bd813d89 (AOT) | 9.76s | 19,953 LOC/s | 46 |

Relative (wall): xtsc is **2.22×** tsc and **14.03×** tsgo; tsc is **6.32×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.16s**.

Ahead-of-time (GraalVM native-image): **9.76s**, **2.38×** the JVM arm, **0.93×** tsc, **5.89×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 48f5bd813d89 | 30.20s | 6,447 LOC/s | 46 |
| tsc | 6.0.3 | 13.11s | 14,857 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.34s | 83,205 LOC/s | 65 |
| xtsc-native | 48f5bd813d89 (AOT) | 12.70s | 15,328 LOC/s | 46 |

Relative (wall): xtsc is **2.30×** tsc and **12.91×** tsgo; tsc is **5.60×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.10s**.

Ahead-of-time (GraalVM native-image): **12.70s**, **2.38×** the JVM arm, **0.97×** tsc, **5.43×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
