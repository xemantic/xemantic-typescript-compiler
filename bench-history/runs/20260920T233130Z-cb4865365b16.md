# Bench run 20260920T233130Z

- **xtsc revision**: [`cb4865365b16`](https://github.com/xemantic/xemantic-typescript-compiler/commit/cb4865365b16faf30825b9cfc1b6a5f6dc622930)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push cb4865365b16faf30825b9cfc1b6a5f6dc622930

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cb4865365b16 | **5.73s** | 33,961 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cb4865365b16 | **5.26s** | 36,987 LOC/s | 46 |
| xtsc (cold JVM) | cb4865365b16 | 27.08s | 7,190 LOC/s | 46 |
| tsc | 6.0.3 | 11.00s | 17,706 LOC/s | 65 |
| tsgo | 7.0.2 | 1.70s | 114,463 LOC/s | 65 |
| xtsc-native | cb4865365b16 (AOT) | 9.36s | 20,805 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.92x FASTER than tsc; 3.37x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.26s**, **-8.2%** against `temurin26` on identical bytecode — 0.48× tsc, 3.09× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.46×** tsc and **15.92×** tsgo; tsc is **6.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.96s**.

Ahead-of-time (GraalVM native-image): **9.36s** — **0.85×** tsc, **5.50×** tsgo, **2.89×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cb4865365b16 | **6.96s** | 27,982 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cb4865365b16 | **6.17s** | 31,571 LOC/s | 46 |
| xtsc (cold JVM) | cb4865365b16 | 29.98s | 6,494 LOC/s | 46 |
| tsc | 6.0.3 | 13.45s | 14,479 LOC/s | 65 |
| tsgo | 7.0.2 | 2.42s | 80,422 LOC/s | 65 |
| xtsc-native | cb4865365b16 (AOT) | 11.08s | 17,569 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.93x FASTER than tsc; 2.87x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.17s**, **-11.4%** against `temurin26` on identical bytecode — 0.46× tsc, 2.55× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.23×** tsc and **12.38×** tsgo; tsc is **5.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.84s**.

Ahead-of-time (GraalVM native-image): **11.08s** — **0.82×** tsc, **4.58×** tsgo, **2.71×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
