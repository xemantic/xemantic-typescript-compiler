# Bench run 20260822T142007Z

- **xtsc revision**: [`a2da0ad0d4d5`](https://github.com/xemantic/xemantic-typescript-compiler/commit/a2da0ad0d4d5c1267551231f4f7cf5ad7d6aa18a)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push a2da0ad0d4d5c1267551231f4f7cf5ad7d6aa18a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a2da0ad0d4d5 | **3.69s** | 52,707 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a2da0ad0d4d5 | **3.56s** | 54,753 LOC/s | 46 |
| xtsc (cold JVM) | a2da0ad0d4d5 | 22.62s | 8,607 LOC/s | 46 |
| tsc | 6.0.3 | 9.94s | 19,579 LOC/s | 65 |
| tsgo | 7.0.2 | 1.54s | 126,676 LOC/s | 65 |
| xtsc-native | a2da0ad0d4d5 (AOT) | 5.93s | 32,827 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.69x FASTER than tsc; 2.40x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.56s**, **-3.7%** against `temurin26` on identical bytecode — 0.36× tsc, 2.31× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.27×** tsc and **14.72×** tsgo; tsc is **6.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.51s**.

Ahead-of-time (GraalVM native-image): **5.93s** — **0.60×** tsc, **3.86×** tsgo, **3.81×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a2da0ad0d4d5 | **4.78s** | 40,749 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a2da0ad0d4d5 | **4.32s** | 45,028 LOC/s | 46 |
| xtsc (cold JVM) | a2da0ad0d4d5 | 24.94s | 7,805 LOC/s | 46 |
| tsc | 6.0.3 | 12.26s | 15,887 LOC/s | 65 |
| tsgo | 7.0.2 | 2.23s | 87,232 LOC/s | 65 |
| xtsc-native | a2da0ad0d4d5 (AOT) | 7.24s | 26,903 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.56x FASTER than tsc; 2.14x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.32s**, **-9.5%** against `temurin26` on identical bytecode — 0.35× tsc, 1.94× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.04×** tsc and **11.18×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.83s**.

Ahead-of-time (GraalVM native-image): **7.24s** — **0.59×** tsc, **3.24×** tsgo, **3.45×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
