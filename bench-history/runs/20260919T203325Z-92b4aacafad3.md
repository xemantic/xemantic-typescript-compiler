# Bench run 20260919T203325Z

- **xtsc revision**: [`92b4aacafad3`](https://github.com/xemantic/xemantic-typescript-compiler/commit/92b4aacafad309987e152b038ccda54451818380)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 92b4aacafad309987e152b038ccda54451818380

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 92b4aacafad3 | **5.24s** | 37,142 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 92b4aacafad3 | **4.92s** | 39,565 LOC/s | 46 |
| xtsc (cold JVM) | 92b4aacafad3 | 27.25s | 7,146 LOC/s | 46 |
| tsc | 6.0.3 | 10.26s | 18,976 LOC/s | 65 |
| tsgo | 7.0.2 | 1.65s | 118,216 LOC/s | 65 |
| xtsc-native | 92b4aacafad3 (AOT) | 8.23s | 23,646 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.96x FASTER than tsc; 3.18x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.92s**, **-6.1%** against `temurin26` on identical bytecode — 0.48× tsc, 2.99× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.66×** tsc and **16.54×** tsgo; tsc is **6.23×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.11s**.

Ahead-of-time (GraalVM native-image): **8.23s** — **0.80×** tsc, **5.00×** tsgo, **3.31×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 92b4aacafad3 | **6.30s** | 30,900 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 92b4aacafad3 | **5.60s** | 34,762 LOC/s | 46 |
| xtsc (cold JVM) | 92b4aacafad3 | 25.97s | 7,498 LOC/s | 46 |
| tsc | 6.0.3 | 12.38s | 15,733 LOC/s | 65 |
| tsgo | 7.0.2 | 2.36s | 82,361 LOC/s | 65 |
| xtsc-native | 92b4aacafad3 (AOT) | 9.71s | 20,059 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.96x FASTER than tsc; 2.67x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.60s**, **-11.1%** against `temurin26` on identical bytecode — 0.45× tsc, 2.37× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.10×** tsc and **10.98×** tsgo; tsc is **5.23×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.84s**.

Ahead-of-time (GraalVM native-image): **9.71s** — **0.78×** tsc, **4.11×** tsgo, **2.68×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
