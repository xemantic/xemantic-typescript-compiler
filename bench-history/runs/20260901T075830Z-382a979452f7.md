# Bench run 20260901T075830Z

- **xtsc revision**: [`382a979452f7`](https://github.com/xemantic/xemantic-typescript-compiler/commit/382a979452f709c417302c782c590b75cf90564a)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 382a979452f709c417302c782c590b75cf90564a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 382a979452f7 | **4.61s** | 42,234 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 382a979452f7 | **4.08s** | 47,721 LOC/s | 46 |
| xtsc (cold JVM) | 382a979452f7 | 23.37s | 8,331 LOC/s | 46 |
| tsc | 6.0.3 | 11.11s | 17,523 LOC/s | 65 |
| tsgo | 7.0.2 | 1.80s | 108,408 LOC/s | 65 |
| xtsc-native | 382a979452f7 (AOT) | 7.66s | 25,434 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.41x FASTER than tsc; 2.57x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.08s**, **-11.5%** against `temurin26` on identical bytecode — 0.37× tsc, 2.27× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.10×** tsc and **13.01×** tsgo; tsc is **6.19×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.25s**.

Ahead-of-time (GraalVM native-image): **7.66s** — **0.69×** tsc, **4.26×** tsgo, **3.05×** the COLD JVM arm and **1.66×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 382a979452f7 | **5.40s** | 36,042 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 382a979452f7 | **4.90s** | 39,759 LOC/s | 46 |
| xtsc (cold JVM) | 382a979452f7 | 28.82s | 6,756 LOC/s | 46 |
| tsc | 6.0.3 | 13.56s | 14,360 LOC/s | 65 |
| tsgo | 7.0.2 | 2.48s | 78,414 LOC/s | 65 |
| xtsc-native | 382a979452f7 (AOT) | 8.51s | 22,876 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.51x FASTER than tsc; 2.18x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.90s**, **-9.3%** against `temurin26` on identical bytecode — 0.36× tsc, 1.97× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.13×** tsc and **11.60×** tsgo; tsc is **5.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.68s**.

Ahead-of-time (GraalVM native-image): **8.51s** — **0.63×** tsc, **3.43×** tsgo, **3.39×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
