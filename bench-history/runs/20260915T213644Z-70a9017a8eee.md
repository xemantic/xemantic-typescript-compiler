# Bench run 20260915T213644Z

- **xtsc revision**: [`70a9017a8eee`](https://github.com/xemantic/xemantic-typescript-compiler/commit/70a9017a8eeeef15dc04c65574e15ce9f01b876c)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 70a9017a8eeeef15dc04c65574e15ce9f01b876c

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 70a9017a8eee | **5.95s** | 32,745 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 70a9017a8eee | **5.33s** | 36,522 LOC/s | 46 |
| xtsc (cold JVM) | 70a9017a8eee | 27.09s | 7,187 LOC/s | 46 |
| tsc | 6.0.3 | 10.78s | 18,058 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,378 LOC/s | 65 |
| xtsc-native | 70a9017a8eee (AOT) | 9.03s | 21,552 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.81x FASTER than tsc; 3.55x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.33s**, **-10.3%** against `temurin26` on identical bytecode — 0.49× tsc, 3.19× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.51×** tsc and **16.19×** tsgo; tsc is **6.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.97s**.

Ahead-of-time (GraalVM native-image): **9.03s** — **0.84×** tsc, **5.40×** tsgo, **3.00×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 70a9017a8eee | **6.79s** | 28,674 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 70a9017a8eee | **6.16s** | 31,633 LOC/s | 46 |
| xtsc (cold JVM) | 70a9017a8eee | 30.68s | 6,347 LOC/s | 46 |
| tsc | 6.0.3 | 13.14s | 14,812 LOC/s | 65 |
| tsgo | 7.0.2 | 2.39s | 81,499 LOC/s | 65 |
| xtsc-native | 70a9017a8eee (AOT) | 10.46s | 18,613 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.94x FASTER than tsc; 2.84x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.16s**, **-9.4%** against `temurin26` on identical bytecode — 0.47× tsc, 2.58× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.33×** tsc and **12.84×** tsgo; tsc is **5.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.54s**.

Ahead-of-time (GraalVM native-image): **10.46s** — **0.80×** tsc, **4.38×** tsgo, **2.93×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
