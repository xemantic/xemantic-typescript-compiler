# Bench run 20260924T044749Z

- **xtsc revision**: [`4fbd72b17d8f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/4fbd72b17d8ff141f97f5f218ac4e1ef13d750bb)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 4fbd72b17d8ff141f97f5f218ac4e1ef13d750bb

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 4fbd72b17d8f | **5.42s** | 35,922 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 4fbd72b17d8f | **5.16s** | 37,740 LOC/s | 46 |
| xtsc (cold JVM) | 4fbd72b17d8f | 25.92s | 7,511 LOC/s | 46 |
| tsc | 6.0.3 | 10.01s | 19,460 LOC/s | 65 |
| tsgo | 7.0.2 | 1.54s | 126,184 LOC/s | 65 |
| xtsc-native | 4fbd72b17d8f (AOT) | 9.01s | 21,607 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.85x FASTER than tsc; 3.51x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.16s**, **-4.8%** against `temurin26` on identical bytecode — 0.52× tsc, 3.34× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.59×** tsc and **16.80×** tsgo; tsc is **6.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.80s**.

Ahead-of-time (GraalVM native-image): **9.01s** — **0.90×** tsc, **5.84×** tsgo, **2.88×** the COLD JVM arm and **1.66×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 4fbd72b17d8f | **6.46s** | 30,120 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 4fbd72b17d8f | **5.73s** | 33,955 LOC/s | 46 |
| xtsc (cold JVM) | 4fbd72b17d8f | 27.88s | 6,984 LOC/s | 46 |
| tsc | 6.0.3 | 12.17s | 15,994 LOC/s | 65 |
| tsgo | 7.0.2 | 2.24s | 87,076 LOC/s | 65 |
| xtsc-native | 4fbd72b17d8f (AOT) | 9.98s | 19,499 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.88x FASTER than tsc; 2.89x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.73s**, **-11.3%** against `temurin26` on identical bytecode — 0.47× tsc, 2.56× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.29×** tsc and **12.47×** tsgo; tsc is **5.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.75s**.

Ahead-of-time (GraalVM native-image): **9.98s** — **0.82×** tsc, **4.47×** tsgo, **2.79×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
