# Bench run 20260808T121714Z

- **xtsc revision**: [`c69be04cf260`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c69be04cf26043cab250a331b6b22e934b6383c4)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push c69be04cf26043cab250a331b6b22e934b6383c4

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | c69be04cf260 | 25.83s | 7,538 LOC/s | 46 |
| tsc | 6.0.3 | 10.54s | 18,467 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.73s | 112,870 LOC/s | 65 |
| xtsc-native | c69be04cf260 (AOT) | 10.77s | 18,071 LOC/s | 46 |

Relative (wall): xtsc is **2.45×** tsc and **14.97×** tsgo; tsc is **6.11×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.71s**.

Ahead-of-time (GraalVM native-image): **10.77s**, **2.40×** the JVM arm, **1.02×** tsc, **6.25×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | c69be04cf260 | 29.30s | 6,645 LOC/s | 46 |
| tsc | 6.0.3 | 13.54s | 14,376 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.50s | 77,725 LOC/s | 65 |
| xtsc-native | c69be04cf260 (AOT) | 13.44s | 14,486 LOC/s | 46 |

Relative (wall): xtsc is **2.16×** tsc and **11.70×** tsgo; tsc is **5.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.19s**.

Ahead-of-time (GraalVM native-image): **13.44s**, **2.18×** the JVM arm, **0.99×** tsc, **5.37×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
