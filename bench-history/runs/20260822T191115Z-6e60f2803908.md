# Bench run 20260822T191115Z

- **xtsc revision**: [`6e60f2803908`](https://github.com/xemantic/xemantic-typescript-compiler/commit/6e60f280390887b1656b32c3c3cf262c94c7b68e)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 6e60f280390887b1656b32c3c3cf262c94c7b68e

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6e60f2803908 | **3.21s** | 60,654 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6e60f2803908 | **3.00s** | 64,900 LOC/s | 46 |
| xtsc (cold JVM) | 6e60f2803908 | 17.56s | 11,087 LOC/s | 46 |
| tsc | 6.0.3 | 8.11s | 23,995 LOC/s | 65 |
| tsgo | 7.0.2 | 1.24s | 156,638 LOC/s | 65 |
| xtsc-native | 6e60f2803908 (AOT) | 5.10s | 38,169 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.53x FASTER than tsc; 2.58x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.00s**, **-6.5%** against `temurin26` on identical bytecode — 0.37× tsc, 2.41× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.16×** tsc and **14.13×** tsgo; tsc is **6.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.46s**.

Ahead-of-time (GraalVM native-image): **5.10s** — **0.63×** tsc, **4.10×** tsgo, **3.44×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6e60f2803908 | **3.97s** | 49,018 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6e60f2803908 | **3.55s** | 54,814 LOC/s | 46 |
| xtsc (cold JVM) | 6e60f2803908 | 20.64s | 9,432 LOC/s | 46 |
| tsc | 6.0.3 | 9.98s | 19,515 LOC/s | 65 |
| tsgo | 7.0.2 | 1.78s | 109,137 LOC/s | 65 |
| xtsc-native | 6e60f2803908 (AOT) | 6.04s | 32,219 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.51x FASTER than tsc; 2.23x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.55s**, **-10.6%** against `temurin26` on identical bytecode — 0.36× tsc, 1.99× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.07×** tsc and **11.57×** tsgo; tsc is **5.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.54s**.

Ahead-of-time (GraalVM native-image): **6.04s** — **0.61×** tsc, **3.39×** tsgo, **3.42×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
