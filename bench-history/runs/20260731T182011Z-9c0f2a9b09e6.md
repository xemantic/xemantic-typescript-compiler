# Bench run 20260731T182011Z

- **xtsc revision**: [`9c0f2a9b09e6`](https://github.com/xemantic/xemantic-typescript-compiler/commit/9c0f2a9b09e6f3536b6324fd320a823e8149ff75)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 9c0f2a9b09e6f3536b6324fd320a823e8149ff75

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 9c0f2a9b09e6 | 26.06s | 7,470 LOC/s | 46 |
| tsc | 6.0.3 | 10.40s | 18,723 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.57s | 124,251 LOC/s | 65 |
| xtsc-native | 9c0f2a9b09e6 (AOT) | 12.17s | 16,001 LOC/s | 46 |

Relative (wall): xtsc is **2.51×** tsc and **16.63×** tsgo; tsc is **6.64×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.95s**.

Ahead-of-time (GraalVM native-image): **12.17s**, **2.14×** the JVM arm, **1.17×** tsc, **7.77×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 9c0f2a9b09e6 | 28.99s | 6,716 LOC/s | 46 |
| tsc | 6.0.3 | 12.71s | 15,316 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.21s | 88,300 LOC/s | 65 |
| xtsc-native | 9c0f2a9b09e6 (AOT) | 15.25s | 12,769 LOC/s | 46 |

Relative (wall): xtsc is **2.28×** tsc and **13.15×** tsgo; tsc is **5.77×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.88s**.

Ahead-of-time (GraalVM native-image): **15.25s**, **1.90×** the JVM arm, **1.20×** tsc, **6.91×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
