# Bench run 20260827T151456Z

- **xtsc revision**: [`de4c3fa130b4`](https://github.com/xemantic/xemantic-typescript-compiler/commit/de4c3fa130b4a195603f8818f5953973083c2442)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push de4c3fa130b4a195603f8818f5953973083c2442

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | de4c3fa130b4 | **4.27s** | 45,586 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | de4c3fa130b4 | **4.33s** | 44,913 LOC/s | 46 |
| xtsc (cold JVM) | de4c3fa130b4 | 22.85s | 8,522 LOC/s | 46 |
| tsc | 6.0.3 | 10.92s | 17,829 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,448 LOC/s | 65 |
| xtsc-native | de4c3fa130b4 (AOT) | 7.00s | 27,818 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.56x FASTER than tsc; 2.55x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.33s**, **+1.5%** against `temurin26` on identical bytecode — 0.40× tsc, 2.59× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.09×** tsc and **13.66×** tsgo; tsc is **6.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.70s**.

Ahead-of-time (GraalVM native-image): **7.00s** — **0.64×** tsc, **4.19×** tsgo, **3.26×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | de4c3fa130b4 | **4.92s** | 39,557 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | de4c3fa130b4 | **4.61s** | 42,234 LOC/s | 46 |
| xtsc (cold JVM) | de4c3fa130b4 | 25.16s | 7,738 LOC/s | 46 |
| tsc | 6.0.3 | 12.83s | 15,170 LOC/s | 65 |
| tsgo | 7.0.2 | 2.37s | 82,048 LOC/s | 65 |
| xtsc-native | de4c3fa130b4 (AOT) | 8.09s | 24,064 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.61x FASTER than tsc; 2.07x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.61s**, **-6.3%** against `temurin26` on identical bytecode — 0.36× tsc, 1.94× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.96×** tsc and **10.60×** tsgo; tsc is **5.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.04s**.

Ahead-of-time (GraalVM native-image): **8.09s** — **0.63×** tsc, **3.41×** tsgo, **3.11×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
