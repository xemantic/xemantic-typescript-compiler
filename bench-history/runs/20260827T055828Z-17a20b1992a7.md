# Bench run 20260827T055828Z

- **xtsc revision**: [`17a20b1992a7`](https://github.com/xemantic/xemantic-typescript-compiler/commit/17a20b1992a70027c42014d41955ac40addae97f)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 17a20b1992a70027c42014d41955ac40addae97f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 17a20b1992a7 | **3.15s** | 61,751 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 17a20b1992a7 | **3.12s** | 62,344 LOC/s | 46 |
| xtsc (cold JVM) | 17a20b1992a7 | 17.86s | 10,901 LOC/s | 46 |
| tsc | 6.0.3 | 8.33s | 23,379 LOC/s | 65 |
| tsgo | 7.0.2 | 1.28s | 152,468 LOC/s | 65 |
| xtsc-native | 17a20b1992a7 (AOT) | 5.60s | 34,774 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.64x FASTER than tsc; 2.47x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.12s**, **-1.0%** against `temurin26` on identical bytecode — 0.38× tsc, 2.45× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.14×** tsc and **13.99×** tsgo; tsc is **6.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.77s**.

Ahead-of-time (GraalVM native-image): **5.60s** — **0.67×** tsc, **4.38×** tsgo, **3.19×** the COLD JVM arm and **1.78×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 17a20b1992a7 | **3.88s** | 50,116 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 17a20b1992a7 | **3.90s** | 49,962 LOC/s | 46 |
| xtsc (cold JVM) | 17a20b1992a7 | 19.73s | 9,867 LOC/s | 46 |
| tsc | 6.0.3 | 10.14s | 19,203 LOC/s | 65 |
| tsgo | 7.0.2 | 1.83s | 106,394 LOC/s | 65 |
| xtsc-native | 17a20b1992a7 (AOT) | 6.63s | 29,384 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.61x FASTER than tsc; 2.12x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.90s**, **+0.3%** against `temurin26` on identical bytecode — 0.38× tsc, 2.13× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.95×** tsc and **10.78×** tsgo; tsc is **5.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.60s**.

Ahead-of-time (GraalVM native-image): **6.63s** — **0.65×** tsc, **3.62×** tsgo, **2.98×** the COLD JVM arm and **1.71×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
