# Bench run 20260904T175105Z

- **xtsc revision**: [`00f705d42bb5`](https://github.com/xemantic/xemantic-typescript-compiler/commit/00f705d42bb5016f0d2862de8bd9a098bf04da58)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 00f705d42bb5016f0d2862de8bd9a098bf04da58

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 00f705d42bb5 | **3.71s** | 52,536 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 00f705d42bb5 | **3.62s** | 53,770 LOC/s | 46 |
| xtsc (cold JVM) | 00f705d42bb5 | 22.73s | 8,566 LOC/s | 46 |
| tsc | 6.0.3 | 9.80s | 19,857 LOC/s | 65 |
| tsgo | 7.0.2 | 1.53s | 127,007 LOC/s | 65 |
| xtsc-native | 00f705d42bb5 (AOT) | 6.00s | 32,471 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.65x FASTER than tsc; 2.42x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.62s**, **-2.3%** against `temurin26` on identical bytecode — 0.37× tsc, 2.36× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.32×** tsc and **14.83×** tsgo; tsc is **6.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.62s**.

Ahead-of-time (GraalVM native-image): **6.00s** — **0.61×** tsc, **3.91×** tsgo, **3.79×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 00f705d42bb5 | **4.42s** | 44,100 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 00f705d42bb5 | **4.09s** | 47,546 LOC/s | 46 |
| xtsc (cold JVM) | 00f705d42bb5 | 22.53s | 8,642 LOC/s | 46 |
| tsc | 6.0.3 | 11.70s | 16,636 LOC/s | 65 |
| tsgo | 7.0.2 | 2.21s | 88,260 LOC/s | 65 |
| xtsc-native | 00f705d42bb5 (AOT) | 7.06s | 27,589 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.65x FASTER than tsc; 2.00x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.09s**, **-7.2%** against `temurin26` on identical bytecode — 0.35× tsc, 1.86× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.93×** tsc and **10.21×** tsgo; tsc is **5.31×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.39s**.

Ahead-of-time (GraalVM native-image): **7.06s** — **0.60×** tsc, **3.20×** tsgo, **3.19×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
