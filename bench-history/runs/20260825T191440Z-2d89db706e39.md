# Bench run 20260825T191440Z

- **xtsc revision**: [`2d89db706e39`](https://github.com/xemantic/xemantic-typescript-compiler/commit/2d89db706e39a44c68c32afcee9d10f9f0871a66)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 2d89db706e39a44c68c32afcee9d10f9f0871a66

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 2d89db706e39 | **3.98s** | 48,895 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 2d89db706e39 | **3.66s** | 53,240 LOC/s | 46 |
| xtsc (cold JVM) | 2d89db706e39 | 24.17s | 8,055 LOC/s | 46 |
| tsc | 6.0.3 | 11.03s | 17,660 LOC/s | 65 |
| tsgo | 7.0.2 | 1.73s | 112,479 LOC/s | 65 |
| xtsc-native | 2d89db706e39 (AOT) | 6.19s | 31,469 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.77x FASTER than tsc; 2.30x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.66s**, **-8.2%** against `temurin26` on identical bytecode — 0.33× tsc, 2.11× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.19×** tsc and **13.96×** tsgo; tsc is **6.37×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.06s**.

Ahead-of-time (GraalVM native-image): **6.19s** — **0.56×** tsc, **3.57×** tsgo, **3.91×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 2d89db706e39 | **4.97s** | 39,175 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 2d89db706e39 | **4.42s** | 44,050 LOC/s | 46 |
| xtsc (cold JVM) | 2d89db706e39 | 24.93s | 7,809 LOC/s | 46 |
| tsc | 6.0.3 | 13.36s | 14,570 LOC/s | 65 |
| tsgo | 7.0.2 | 2.44s | 79,730 LOC/s | 65 |
| xtsc-native | 2d89db706e39 (AOT) | 7.69s | 25,315 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.69x FASTER than tsc; 2.04x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.42s**, **-11.1%** against `temurin26` on identical bytecode — 0.33× tsc, 1.81× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.87×** tsc and **10.21×** tsgo; tsc is **5.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.80s**.

Ahead-of-time (GraalVM native-image): **7.69s** — **0.58×** tsc, **3.15×** tsgo, **3.24×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
