# Bench run 20260905T224640Z

- **xtsc revision**: [`c081b86adedd`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c081b86adeddffa73bf25ed1e76a711e62ccdbfb)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push c081b86adeddffa73bf25ed1e76a711e62ccdbfb

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c081b86adedd | **10.10s** | 19,277 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c081b86adedd | **9.67s** | 20,138 LOC/s | 46 |
| xtsc (cold JVM) | c081b86adedd | 24.80s | 7,852 LOC/s | 46 |
| tsc | 6.0.3 | 7.29s | 26,708 LOC/s | 65 |
| tsgo | 7.0.2 | 1.09s | 177,972 LOC/s | 65 |
| xtsc-native | c081b86adedd (AOT) | 14.54s | 13,387 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.39x tsc — parity at 1.00x; 9.23x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **9.67s**, **-4.3%** against `temurin26` on identical bytecode — 1.33× tsc, 8.84× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.40×** tsc and **22.67×** tsgo; tsc is **6.66×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.71s**.

Ahead-of-time (GraalVM native-image): **14.54s** — **2.00×** tsc, **13.29×** tsgo, **1.70×** the COLD JVM arm and **1.44×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c081b86adedd | **9.91s** | 19,647 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c081b86adedd | **10.59s** | 18,388 LOC/s | 46 |
| xtsc (cold JVM) | c081b86adedd | 24.29s | 8,017 LOC/s | 46 |
| tsc | 6.0.3 | 8.55s | 22,761 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,550 LOC/s | 65 |
| xtsc-native | c081b86adedd (AOT) | 16.02s | 12,153 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.16x tsc — parity at 1.00x; 5.88x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **10.59s**, **+6.8%** against `temurin26` on identical bytecode — 1.24× tsc, 6.28× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.84×** tsc and **14.41×** tsgo; tsc is **5.08×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.18s**.

Ahead-of-time (GraalVM native-image): **16.02s** — **1.87×** tsc, **9.51×** tsgo, **1.52×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
