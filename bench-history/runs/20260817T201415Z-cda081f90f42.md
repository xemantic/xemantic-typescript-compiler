# Bench run 20260817T201415Z

- **xtsc revision**: [`cda081f90f42`](https://github.com/xemantic/xemantic-typescript-compiler/commit/cda081f90f4204ce0256aa6403861606bdfa9c02)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push cda081f90f4204ce0256aa6403861606bdfa9c02

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cda081f90f42 | **3.28s** | 59,378 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cda081f90f42 | **3.19s** | 60,939 LOC/s | 46 |
| xtsc (cold JVM) | cda081f90f42 | 19.14s | 10,173 LOC/s | 46 |
| tsc | 6.0.3 | 8.56s | 22,753 LOC/s | 65 |
| tsgo | 7.0.2 | 1.28s | 151,755 LOC/s | 65 |
| xtsc-native | cda081f90f42 (AOT) | 5.47s | 35,562 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.61x FASTER than tsc; 2.56x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.19s**, **-2.6%** against `temurin26` on identical bytecode — 0.37× tsc, 2.49× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.24×** tsc and **14.92×** tsgo; tsc is **6.67×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.05s**.

Ahead-of-time (GraalVM native-image): **5.47s** — **0.64×** tsc, **4.27×** tsgo, **3.50×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cda081f90f42 | **4.01s** | 48,554 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cda081f90f42 | **3.89s** | 50,090 LOC/s | 46 |
| xtsc (cold JVM) | cda081f90f42 | 21.22s | 9,177 LOC/s | 46 |
| tsc | 6.0.3 | 10.54s | 18,477 LOC/s | 65 |
| tsgo | 7.0.2 | 1.87s | 103,896 LOC/s | 65 |
| xtsc-native | cda081f90f42 (AOT) | 6.45s | 30,167 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.63x FASTER than tsc; 2.14x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.89s**, **-3.1%** against `temurin26` on identical bytecode — 0.37× tsc, 2.07× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.01×** tsc and **11.32×** tsgo; tsc is **5.62×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.12s**.

Ahead-of-time (GraalVM native-image): **6.45s** — **0.61×** tsc, **3.44×** tsgo, **3.29×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
