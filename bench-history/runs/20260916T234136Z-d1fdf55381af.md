# Bench run 20260916T234136Z

- **xtsc revision**: [`d1fdf55381af`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d1fdf55381af5071fd100e760d0997e1ecc1b8b5)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push d1fdf55381af5071fd100e760d0997e1ecc1b8b5

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d1fdf55381af | **3.46s** | 56,304 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d1fdf55381af | **3.26s** | 59,761 LOC/s | 46 |
| xtsc (cold JVM) | d1fdf55381af | 15.31s | 12,713 LOC/s | 46 |
| tsc | 6.0.3 | 6.64s | 29,327 LOC/s | 65 |
| tsgo | 7.0.2 | 0.99s | 195,680 LOC/s | 65 |
| xtsc-native | d1fdf55381af (AOT) | 6.38s | 30,507 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.92x FASTER than tsc; 3.48x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.26s**, **-5.8%** against `temurin26` on identical bytecode — 0.49× tsc, 3.27× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.31×** tsc and **15.39×** tsgo; tsc is **6.67×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **15.23s**.

Ahead-of-time (GraalVM native-image): **6.38s** — **0.96×** tsc, **6.41×** tsgo, **2.40×** the COLD JVM arm and **1.85×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d1fdf55381af | **3.97s** | 48,993 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d1fdf55381af | **4.00s** | 48,614 LOC/s | 46 |
| xtsc (cold JVM) | d1fdf55381af | 17.77s | 10,956 LOC/s | 46 |
| tsc | 6.0.3 | 8.16s | 23,872 LOC/s | 65 |
| tsgo | 7.0.2 | 1.55s | 125,210 LOC/s | 65 |
| xtsc-native | d1fdf55381af (AOT) | 7.20s | 27,038 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.05x FASTER than tsc; 2.56x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.00s**, **+0.8%** against `temurin26` on identical bytecode — 0.49× tsc, 2.58× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.18×** tsc and **11.43×** tsgo; tsc is **5.25×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.68s**.

Ahead-of-time (GraalVM native-image): **7.20s** — **0.88×** tsc, **4.63×** tsgo, **2.47×** the COLD JVM arm and **1.81×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
