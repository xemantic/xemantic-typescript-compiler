# Bench run 20260804T221529Z

- **xtsc revision**: [`b76a53d0d3a7`](https://github.com/xemantic/xemantic-typescript-compiler/commit/b76a53d0d3a77bdc42471ccf4c4fdd1b9625806f)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push b76a53d0d3a77bdc42471ccf4c4fdd1b9625806f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | b76a53d0d3a7 | 15.76s | 12,354 LOC/s | 46 |
| tsc | 6.0.3 | 6.95s | 28,030 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.06s | 183,335 LOC/s | 65 |
| xtsc-native | b76a53d0d3a7 (AOT) | 7.09s | 27,457 LOC/s | 46 |

Relative (wall): xtsc is **2.27×** tsc and **14.84×** tsgo; tsc is **6.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **15.69s**.

Ahead-of-time (GraalVM native-image): **7.09s**, **2.22×** the JVM arm, **1.02×** tsc, **6.68×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | b76a53d0d3a7 | 18.95s | 10,271 LOC/s | 46 |
| tsc | 6.0.3 | 8.50s | 22,908 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.65s | 118,359 LOC/s | 65 |
| xtsc-native | b76a53d0d3a7 (AOT) | 9.98s | 19,503 LOC/s | 46 |

Relative (wall): xtsc is **2.23×** tsc and **11.52×** tsgo; tsc is **5.17×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.88s**.

Ahead-of-time (GraalVM native-image): **9.98s**, **1.90×** the JVM arm, **1.17×** tsc, **6.07×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
