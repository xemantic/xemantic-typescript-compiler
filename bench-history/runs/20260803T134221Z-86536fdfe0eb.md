# Bench run 20260803T134221Z

- **xtsc revision**: [`86536fdfe0eb`](https://github.com/xemantic/xemantic-typescript-compiler/commit/86536fdfe0eb5c0950a779a2f07578a7e7d1a312)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 86536fdfe0eb5c0950a779a2f07578a7e7d1a312

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 86536fdfe0eb | 25.94s | 7,506 LOC/s | 46 |
| tsc | 6.0.3 | 11.29s | 17,239 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.67s | 116,448 LOC/s | 65 |
| xtsc-native | 86536fdfe0eb (AOT) | 10.99s | 17,721 LOC/s | 46 |

Relative (wall): xtsc is **2.30×** tsc and **15.51×** tsgo; tsc is **6.75×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.83s**.

Ahead-of-time (GraalVM native-image): **10.99s**, **2.36×** the JVM arm, **0.97×** tsc, **6.57×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 86536fdfe0eb | 27.14s | 7,173 LOC/s | 46 |
| tsc | 6.0.3 | 13.29s | 14,650 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.42s | 80,588 LOC/s | 65 |
| xtsc-native | 86536fdfe0eb (AOT) | 13.55s | 14,372 LOC/s | 46 |

Relative (wall): xtsc is **2.04×** tsc and **11.23×** tsgo; tsc is **5.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.02s**.

Ahead-of-time (GraalVM native-image): **13.55s**, **2.00×** the JVM arm, **1.02×** tsc, **5.61×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
