# Bench run 20260913T143246Z

- **xtsc revision**: [`b55566106e58`](https://github.com/xemantic/xemantic-typescript-compiler/commit/b55566106e583babecdbe99c0079cdce50e27725)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push b55566106e583babecdbe99c0079cdce50e27725

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b55566106e58 | **5.96s** | 32,684 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b55566106e58 | **5.26s** | 37,036 LOC/s | 46 |
| xtsc (cold JVM) | b55566106e58 | 28.12s | 6,924 LOC/s | 46 |
| tsc | 6.0.3 | 10.91s | 17,842 LOC/s | 65 |
| tsgo | 7.0.2 | 1.75s | 111,194 LOC/s | 65 |
| xtsc-native | b55566106e58 (AOT) | 9.85s | 19,770 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.83x FASTER than tsc; 3.40x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.26s**, **-11.8%** against `temurin26` on identical bytecode — 0.48× tsc, 3.00× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.58×** tsc and **16.06×** tsgo; tsc is **6.23×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.00s**.

Ahead-of-time (GraalVM native-image): **9.85s** — **0.90×** tsc, **5.62×** tsgo, **2.85×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b55566106e58 | **6.85s** | 28,415 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b55566106e58 | **6.00s** | 32,477 LOC/s | 46 |
| xtsc (cold JVM) | b55566106e58 | 30.52s | 6,380 LOC/s | 46 |
| tsc | 6.0.3 | 13.16s | 14,791 LOC/s | 65 |
| tsgo | 7.0.2 | 2.44s | 79,697 LOC/s | 65 |
| xtsc-native | b55566106e58 (AOT) | 10.62s | 18,328 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.92x FASTER than tsc; 2.80x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.00s**, **-12.5%** against `temurin26` on identical bytecode — 0.46× tsc, 2.45× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.32×** tsc and **12.49×** tsgo; tsc is **5.39×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.38s**.

Ahead-of-time (GraalVM native-image): **10.62s** — **0.81×** tsc, **4.35×** tsgo, **2.87×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
