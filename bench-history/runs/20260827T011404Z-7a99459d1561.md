# Bench run 20260827T011404Z

- **xtsc revision**: [`7a99459d1561`](https://github.com/xemantic/xemantic-typescript-compiler/commit/7a99459d1561779958892da9cff67fb051fc37e0)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 7a99459d1561779958892da9cff67fb051fc37e0

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7a99459d1561 | **3.98s** | 48,981 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7a99459d1561 | **3.79s** | 51,304 LOC/s | 46 |
| xtsc (cold JVM) | 7a99459d1561 | 21.37s | 9,112 LOC/s | 46 |
| tsc | 6.0.3 | 10.60s | 18,361 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,797 LOC/s | 65 |
| xtsc-native | 7a99459d1561 (AOT) | 6.08s | 32,044 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.67x FASTER than tsc; 2.38x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.79s**, **-4.5%** against `temurin26` on identical bytecode — 0.36× tsc, 2.28× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.01×** tsc and **12.82×** tsgo; tsc is **6.36×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.26s**.

Ahead-of-time (GraalVM native-image): **6.08s** — **0.57×** tsc, **3.64×** tsgo, **3.52×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7a99459d1561 | **4.91s** | 39,662 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7a99459d1561 | **4.49s** | 43,353 LOC/s | 46 |
| xtsc (cold JVM) | 7a99459d1561 | 26.24s | 7,420 LOC/s | 46 |
| tsc | 6.0.3 | 13.11s | 14,850 LOC/s | 65 |
| tsgo | 7.0.2 | 2.37s | 82,152 LOC/s | 65 |
| xtsc-native | 7a99459d1561 (AOT) | 7.40s | 26,318 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.67x FASTER than tsc; 2.07x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.49s**, **-8.5%** against `temurin26` on identical bytecode — 0.34× tsc, 1.89× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.00×** tsc and **11.07×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.12s**.

Ahead-of-time (GraalVM native-image): **7.40s** — **0.56×** tsc, **3.12×** tsgo, **3.55×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
