# Bench run 20260923T023005Z

- **xtsc revision**: [`b9adedcc189c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/b9adedcc189ccd9172226047d82dfc879292a513)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push b9adedcc189ccd9172226047d82dfc879292a513

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b9adedcc189c | **5.78s** | 33,662 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b9adedcc189c | **5.29s** | 36,791 LOC/s | 46 |
| xtsc (cold JVM) | b9adedcc189c | 24.55s | 7,930 LOC/s | 46 |
| tsc | 6.0.3 | 10.40s | 18,726 LOC/s | 65 |
| tsgo | 7.0.2 | 1.56s | 125,049 LOC/s | 65 |
| xtsc-native | b9adedcc189c (AOT) | 9.40s | 20,719 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.80x FASTER than tsc; 3.71x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.29s**, **-8.5%** against `temurin26` on identical bytecode — 0.51× tsc, 3.40× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.36×** tsc and **15.77×** tsgo; tsc is **6.68×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.44s**.

Ahead-of-time (GraalVM native-image): **9.40s** — **0.90×** tsc, **6.04×** tsgo, **2.61×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b9adedcc189c | **6.96s** | 27,962 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b9adedcc189c | **6.05s** | 32,160 LOC/s | 46 |
| xtsc (cold JVM) | b9adedcc189c | 29.20s | 6,668 LOC/s | 46 |
| tsc | 6.0.3 | 12.61s | 15,435 LOC/s | 65 |
| tsgo | 7.0.2 | 2.27s | 85,620 LOC/s | 65 |
| xtsc-native | b9adedcc189c (AOT) | 10.69s | 18,218 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.81x FASTER than tsc; 3.06x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.05s**, **-13.1%** against `temurin26` on identical bytecode — 0.48× tsc, 2.66× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.31×** tsc and **12.84×** tsgo; tsc is **5.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.07s**.

Ahead-of-time (GraalVM native-image): **10.69s** — **0.85×** tsc, **4.70×** tsgo, **2.73×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
