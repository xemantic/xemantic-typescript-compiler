# Bench run 20260906T193919Z

- **xtsc revision**: [`ca4495817cdb`](https://github.com/xemantic/xemantic-typescript-compiler/commit/ca4495817cdb38ba92c407fa6ef853f46d45ed1b)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push ca4495817cdb38ba92c407fa6ef853f46d45ed1b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ca4495817cdb | **14.13s** | 13,783 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ca4495817cdb | **14.68s** | 13,262 LOC/s | 46 |
| xtsc (cold JVM) | ca4495817cdb | 34.81s | 5,593 LOC/s | 46 |
| tsc | 6.0.3 | 10.50s | 18,541 LOC/s | 65 |
| tsgo | 7.0.2 | 1.54s | 126,184 LOC/s | 65 |
| xtsc-native | ca4495817cdb (AOT) | 23.21s | 8,386 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.35x tsc — parity at 1.00x; 9.15x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **14.68s**, **+3.9%** against `temurin26` on identical bytecode — 1.40× tsc, 9.51× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.31×** tsc and **22.56×** tsgo; tsc is **6.81×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **34.69s**.

Ahead-of-time (GraalVM native-image): **23.21s** — **2.21×** tsc, **15.05×** tsgo, **1.50×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ca4495817cdb | **14.64s** | 13,298 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ca4495817cdb | **15.24s** | 12,777 LOC/s | 46 |
| xtsc (cold JVM) | ca4495817cdb | 36.17s | 5,382 LOC/s | 46 |
| tsc | 6.0.3 | 12.86s | 15,137 LOC/s | 65 |
| tsgo | 7.0.2 | 2.33s | 83,527 LOC/s | 65 |
| xtsc-native | ca4495817cdb (AOT) | 24.38s | 7,985 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.14x tsc — parity at 1.00x; 6.28x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **15.24s**, **+4.1%** against `temurin26` on identical bytecode — 1.18× tsc, 6.54× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.81×** tsc and **15.52×** tsgo; tsc is **5.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **36.05s**.

Ahead-of-time (GraalVM native-image): **24.38s** — **1.90×** tsc, **10.46×** tsgo, **1.48×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
