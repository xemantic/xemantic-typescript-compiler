# Bench run 20260923T175236Z

- **xtsc revision**: [`bded70061e21`](https://github.com/xemantic/xemantic-typescript-compiler/commit/bded70061e21e132e7665245befa8a10da9e6129)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push bded70061e21e132e7665245befa8a10da9e6129

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | bded70061e21 | **3.73s** | 52,226 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | bded70061e21 | **3.54s** | 54,922 LOC/s | 46 |
| xtsc (cold JVM) | bded70061e21 | 17.54s | 11,098 LOC/s | 46 |
| tsc | 6.0.3 | 6.96s | 27,966 LOC/s | 65 |
| tsgo | 7.0.2 | 1.08s | 180,112 LOC/s | 65 |
| xtsc-native | bded70061e21 (AOT) | 6.68s | 29,142 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.87x FASTER than tsc; 3.45x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.54s**, **-4.9%** against `temurin26` on identical bytecode — 0.51× tsc, 3.28× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.52×** tsc and **16.23×** tsgo; tsc is **6.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.45s**.

Ahead-of-time (GraalVM native-image): **6.68s** — **0.96×** tsc, **6.18×** tsgo, **2.63×** the COLD JVM arm and **1.79×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | bded70061e21 | **4.72s** | 41,232 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | bded70061e21 | **4.48s** | 43,479 LOC/s | 46 |
| xtsc (cold JVM) | bded70061e21 | 20.00s | 9,737 LOC/s | 46 |
| tsc | 6.0.3 | 9.12s | 21,346 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,072 LOC/s | 65 |
| xtsc-native | bded70061e21 (AOT) | 8.03s | 24,240 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.93x FASTER than tsc; 2.79x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.48s**, **-5.2%** against `temurin26` on identical bytecode — 0.49× tsc, 2.65× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.19×** tsc and **11.82×** tsgo; tsc is **5.39×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.90s**.

Ahead-of-time (GraalVM native-image): **8.03s** — **0.88×** tsc, **4.75×** tsgo, **2.49×** the COLD JVM arm and **1.70×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
