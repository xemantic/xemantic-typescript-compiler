# Bench run 20260901T150204Z

- **xtsc revision**: [`25a873cade48`](https://github.com/xemantic/xemantic-typescript-compiler/commit/25a873cade48de3530ce6a5dd87515ca4b0c5397)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 25a873cade48de3530ce6a5dd87515ca4b0c5397

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 25a873cade48 | **4.22s** | 46,170 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 25a873cade48 | **4.43s** | 43,960 LOC/s | 46 |
| xtsc (cold JVM) | 25a873cade48 | 25.99s | 7,490 LOC/s | 46 |
| tsc | 6.0.3 | 11.01s | 17,688 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,588 LOC/s | 65 |
| xtsc-native | 25a873cade48 (AOT) | 7.80s | 24,968 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.61x FASTER than tsc; 2.53x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.43s**, **+5.0%** against `temurin26` on identical bytecode — 0.40× tsc, 2.65× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.36×** tsc and **15.57×** tsgo; tsc is **6.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.87s**.

Ahead-of-time (GraalVM native-image): **7.80s** — **0.71×** tsc, **4.67×** tsgo, **3.33×** the COLD JVM arm and **1.85×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 25a873cade48 | **5.58s** | 34,867 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 25a873cade48 | **4.94s** | 39,381 LOC/s | 46 |
| xtsc (cold JVM) | 25a873cade48 | 28.10s | 6,928 LOC/s | 46 |
| tsc | 6.0.3 | 13.07s | 14,893 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,876 LOC/s | 65 |
| xtsc-native | 25a873cade48 (AOT) | 8.90s | 21,876 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.34x FASTER than tsc; 2.35x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.94s**, **-11.5%** against `temurin26` on identical bytecode — 0.38× tsc, 2.08× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.15×** tsc and **11.82×** tsgo; tsc is **5.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.97s**.

Ahead-of-time (GraalVM native-image): **8.90s** — **0.68×** tsc, **3.74×** tsgo, **3.16×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
