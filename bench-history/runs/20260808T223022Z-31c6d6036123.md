# Bench run 20260808T223022Z

- **xtsc revision**: [`31c6d6036123`](https://github.com/xemantic/xemantic-typescript-compiler/commit/31c6d603612312adbcea7f6517c144daa2075639)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 31c6d603612312adbcea7f6517c144daa2075639

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 31c6d6036123 | 27.65s | 7,040 LOC/s | 46 |
| tsc | 6.0.3 | 10.74s | 18,123 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.69s | 115,072 LOC/s | 65 |
| xtsc-native | 31c6d6036123 (AOT) | 10.58s | 18,399 LOC/s | 46 |

Relative (wall): xtsc is **2.57×** tsc and **16.34×** tsgo; tsc is **6.35×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.54s**.

Ahead-of-time (GraalVM native-image): **10.58s**, **2.61×** the JVM arm, **0.99×** tsc, **6.25×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 31c6d6036123 | 25.84s | 7,534 LOC/s | 46 |
| tsc | 6.0.3 | 13.04s | 14,936 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.42s | 80,388 LOC/s | 65 |
| xtsc-native | 31c6d6036123 (AOT) | 11.54s | 16,877 LOC/s | 46 |

Relative (wall): xtsc is **1.98×** tsc and **10.67×** tsgo; tsc is **5.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.73s**.

Ahead-of-time (GraalVM native-image): **11.54s**, **2.24×** the JVM arm, **0.89×** tsc, **4.76×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
