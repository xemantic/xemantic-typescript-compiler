# Bench run 20260919T190657Z

- **xtsc revision**: [`10c2a909f585`](https://github.com/xemantic/xemantic-typescript-compiler/commit/10c2a909f5852eb73eed63689d4017d9d8e80687)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 10c2a909f5852eb73eed63689d4017d9d8e80687

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 10c2a909f585 | **4.74s** | 41,059 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 10c2a909f585 | **4.29s** | 45,374 LOC/s | 46 |
| xtsc (cold JVM) | 10c2a909f585 | 23.88s | 8,153 LOC/s | 46 |
| tsc | 6.0.3 | 9.27s | 21,014 LOC/s | 65 |
| tsgo | 7.0.2 | 1.44s | 135,586 LOC/s | 65 |
| xtsc-native | 10c2a909f585 (AOT) | 7.14s | 27,250 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.95x FASTER than tsc; 3.30x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.29s**, **-9.5%** against `temurin26` on identical bytecode — 0.46× tsc, 2.99× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.58×** tsc and **16.63×** tsgo; tsc is **6.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.78s**.

Ahead-of-time (GraalVM native-image): **7.14s** — **0.77×** tsc, **4.98×** tsgo, **3.34×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 10c2a909f585 | **5.72s** | 34,044 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 10c2a909f585 | **5.00s** | 38,917 LOC/s | 46 |
| xtsc (cold JVM) | 10c2a909f585 | 25.38s | 7,670 LOC/s | 46 |
| tsc | 6.0.3 | 11.23s | 17,340 LOC/s | 65 |
| tsgo | 7.0.2 | 2.10s | 92,538 LOC/s | 65 |
| xtsc-native | 10c2a909f585 (AOT) | 8.61s | 22,608 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.96x FASTER than tsc; 2.72x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.00s**, **-12.5%** against `temurin26` on identical bytecode — 0.45× tsc, 2.38× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.26×** tsc and **12.06×** tsgo; tsc is **5.34×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.27s**.

Ahead-of-time (GraalVM native-image): **8.61s** — **0.77×** tsc, **4.09×** tsgo, **2.95×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
