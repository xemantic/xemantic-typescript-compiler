# Bench run 20260905T064750Z

- **xtsc revision**: [`beae1151ebfa`](https://github.com/xemantic/xemantic-typescript-compiler/commit/beae1151ebfa23c8a49112ad1703ed718fddc123)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push beae1151ebfa23c8a49112ad1703ed718fddc123

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | beae1151ebfa | **3.09s** | 63,010 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | beae1151ebfa | **2.97s** | 65,578 LOC/s | 46 |
| xtsc (cold JVM) | beae1151ebfa | 20.71s | 9,401 LOC/s | 46 |
| tsc | 6.0.3 | 8.49s | 22,943 LOC/s | 65 |
| tsgo | 7.0.2 | 1.32s | 147,613 LOC/s | 65 |
| xtsc-native | beae1151ebfa (AOT) | 4.86s | 40,062 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.75x FASTER than tsc; 2.34x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.97s**, **-3.9%** against `temurin26` on identical bytecode — 0.35× tsc, 2.25× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.44×** tsc and **15.70×** tsgo; tsc is **6.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.61s**.

Ahead-of-time (GraalVM native-image): **4.86s** — **0.57×** tsc, **3.68×** tsgo, **4.26×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | beae1151ebfa | **3.88s** | 50,129 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | beae1151ebfa | **3.56s** | 54,722 LOC/s | 46 |
| xtsc (cold JVM) | beae1151ebfa | 24.03s | 8,103 LOC/s | 46 |
| tsc | 6.0.3 | 10.15s | 19,180 LOC/s | 65 |
| tsgo | 7.0.2 | 1.92s | 101,407 LOC/s | 65 |
| xtsc-native | beae1151ebfa (AOT) | 5.88s | 33,084 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.61x FASTER than tsc; 2.02x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.56s**, **-8.4%** against `temurin26` on identical bytecode — 0.35× tsc, 1.85× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.37×** tsc and **12.51×** tsgo; tsc is **5.29×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.92s**.

Ahead-of-time (GraalVM native-image): **5.88s** — **0.58×** tsc, **3.07×** tsgo, **4.08×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
