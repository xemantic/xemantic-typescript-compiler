# Bench run 20260914T032949Z

- **xtsc revision**: [`a2d22ad491dd`](https://github.com/xemantic/xemantic-typescript-compiler/commit/a2d22ad491dd2293cb6a4d2e57e2357da328f80d)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push a2d22ad491dd2293cb6a4d2e57e2357da328f80d

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a2d22ad491dd | **5.83s** | 33,367 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a2d22ad491dd | **5.44s** | 35,810 LOC/s | 46 |
| xtsc (cold JVM) | a2d22ad491dd | 27.61s | 7,052 LOC/s | 46 |
| tsc | 6.0.3 | 10.93s | 17,807 LOC/s | 65 |
| tsgo | 7.0.2 | 1.64s | 118,503 LOC/s | 65 |
| xtsc-native | a2d22ad491dd (AOT) | 9.46s | 20,583 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.87x FASTER than tsc; 3.55x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.44s**, **-6.8%** against `temurin26` on identical bytecode — 0.50× tsc, 3.31× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.52×** tsc and **16.80×** tsgo; tsc is **6.65×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.50s**.

Ahead-of-time (GraalVM native-image): **9.46s** — **0.87×** tsc, **5.76×** tsgo, **2.92×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a2d22ad491dd | **6.79s** | 28,695 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a2d22ad491dd | **6.14s** | 31,694 LOC/s | 46 |
| xtsc (cold JVM) | a2d22ad491dd | 28.78s | 6,765 LOC/s | 46 |
| tsc | 6.0.3 | 13.52s | 14,404 LOC/s | 65 |
| tsgo | 7.0.2 | 2.46s | 79,147 LOC/s | 65 |
| xtsc-native | a2d22ad491dd (AOT) | 10.94s | 17,792 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.99x FASTER than tsc; 2.76x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.14s**, **-9.5%** against `temurin26` on identical bytecode — 0.45× tsc, 2.50× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.13×** tsc and **11.70×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.64s**.

Ahead-of-time (GraalVM native-image): **10.94s** — **0.81×** tsc, **4.45×** tsgo, **2.63×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
