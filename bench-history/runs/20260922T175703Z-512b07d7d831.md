# Bench run 20260922T175703Z

- **xtsc revision**: [`512b07d7d831`](https://github.com/xemantic/xemantic-typescript-compiler/commit/512b07d7d83155f71767ff1ccccc72da48fc05db)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 512b07d7d83155f71767ff1ccccc72da48fc05db

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 512b07d7d831 | **4.98s** | 39,081 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 512b07d7d831 | **4.92s** | 39,549 LOC/s | 46 |
| xtsc (cold JVM) | 512b07d7d831 | 21.86s | 8,905 LOC/s | 46 |
| tsc | 6.0.3 | 8.92s | 21,822 LOC/s | 65 |
| tsgo | 7.0.2 | 1.36s | 143,585 LOC/s | 65 |
| xtsc-native | 512b07d7d831 (AOT) | 8.53s | 22,838 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.79x FASTER than tsc; 3.67x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.92s**, **-1.2%** against `temurin26` on identical bytecode — 0.55× tsc, 3.63× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.45×** tsc and **16.12×** tsgo; tsc is **6.58×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.76s**.

Ahead-of-time (GraalVM native-image): **8.53s** — **0.96×** tsc, **6.29×** tsgo, **2.56×** the COLD JVM arm and **1.71×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 512b07d7d831 | **6.01s** | 32,390 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 512b07d7d831 | **5.33s** | 36,495 LOC/s | 46 |
| xtsc (cold JVM) | 512b07d7d831 | 27.29s | 7,134 LOC/s | 46 |
| tsc | 6.0.3 | 11.09s | 17,551 LOC/s | 65 |
| tsgo | 7.0.2 | 2.01s | 97,011 LOC/s | 65 |
| xtsc-native | 512b07d7d831 (AOT) | 9.58s | 20,319 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.85x FASTER than tsc; 3.00x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.33s**, **-11.2%** against `temurin26` on identical bytecode — 0.48× tsc, 2.66× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.46×** tsc and **13.60×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.17s**.

Ahead-of-time (GraalVM native-image): **9.58s** — **0.86×** tsc, **4.77×** tsgo, **2.85×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
