# Bench run 20260829T235922Z

- **xtsc revision**: [`deaa4d7187b4`](https://github.com/xemantic/xemantic-typescript-compiler/commit/deaa4d7187b480955843a43209e89d7e7ffb6c5e)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push deaa4d7187b480955843a43209e89d7e7ffb6c5e

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | deaa4d7187b4 | **4.16s** | 46,769 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | deaa4d7187b4 | **4.03s** | 48,361 LOC/s | 46 |
| xtsc (cold JVM) | deaa4d7187b4 | 23.19s | 8,396 LOC/s | 46 |
| tsc | 6.0.3 | 10.57s | 18,423 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,309 LOC/s | 65 |
| xtsc-native | deaa4d7187b4 (AOT) | 6.22s | 31,322 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.54x FASTER than tsc; 2.49x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.03s**, **-3.3%** against `temurin26` on identical bytecode — 0.38× tsc, 2.41× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.19×** tsc and **13.85×** tsgo; tsc is **6.31×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.07s**.

Ahead-of-time (GraalVM native-image): **6.22s** — **0.59×** tsc, **3.71×** tsgo, **3.73×** the COLD JVM arm and **1.49×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | deaa4d7187b4 | **5.05s** | 38,524 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | deaa4d7187b4 | **4.61s** | 42,243 LOC/s | 46 |
| xtsc (cold JVM) | deaa4d7187b4 | 28.01s | 6,951 LOC/s | 46 |
| tsc | 6.0.3 | 12.95s | 15,034 LOC/s | 65 |
| tsgo | 7.0.2 | 2.37s | 82,083 LOC/s | 65 |
| xtsc-native | deaa4d7187b4 (AOT) | 7.63s | 25,517 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.56x FASTER than tsc; 2.13x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.61s**, **-8.8%** against `temurin26` on identical bytecode — 0.36× tsc, 1.94× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.16×** tsc and **11.81×** tsgo; tsc is **5.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.87s**.

Ahead-of-time (GraalVM native-image): **7.63s** — **0.59×** tsc, **3.22×** tsgo, **3.67×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
