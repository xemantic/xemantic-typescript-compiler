# Bench run 20260809T042713Z

- **xtsc revision**: [`255a347b2819`](https://github.com/xemantic/xemantic-typescript-compiler/commit/255a347b2819767b3672a389e6d5be0fe87d107a)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 255a347b2819767b3672a389e6d5be0fe87d107a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 255a347b2819 | 25.35s | 7,679 LOC/s | 46 |
| tsc | 6.0.3 | 11.11s | 17,520 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.67s | 116,867 LOC/s | 65 |
| xtsc-native | 255a347b2819 (AOT) | 10.60s | 18,375 LOC/s | 46 |

Relative (wall): xtsc is **2.28×** tsc and **15.22×** tsgo; tsc is **6.67×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.22s**.

Ahead-of-time (GraalVM native-image): **10.60s**, **2.39×** the JVM arm, **0.95×** tsc, **6.36×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 255a347b2819 | 29.93s | 6,505 LOC/s | 46 |
| tsc | 6.0.3 | 13.62s | 14,297 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.49s | 78,256 LOC/s | 65 |
| xtsc-native | 255a347b2819 (AOT) | 12.42s | 15,681 LOC/s | 46 |

Relative (wall): xtsc is **2.20×** tsc and **12.03×** tsgo; tsc is **5.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.78s**.

Ahead-of-time (GraalVM native-image): **12.42s**, **2.41×** the JVM arm, **0.91×** tsc, **4.99×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
