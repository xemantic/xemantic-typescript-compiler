# Bench run 20260909T114127Z

- **xtsc revision**: [`0e15badf9b4d`](https://github.com/xemantic/xemantic-typescript-compiler/commit/0e15badf9b4d18e48240a11978b6f8278f59d93a)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 0e15badf9b4d18e48240a11978b6f8278f59d93a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0e15badf9b4d | **5.37s** | 36,230 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0e15badf9b4d | **4.86s** | 40,078 LOC/s | 46 |
| xtsc (cold JVM) | 0e15badf9b4d | 26.38s | 7,379 LOC/s | 46 |
| tsc | 6.0.3 | 10.63s | 18,319 LOC/s | 65 |
| tsgo | 7.0.2 | 1.65s | 117,787 LOC/s | 65 |
| xtsc-native | 0e15badf9b4d (AOT) | 8.40s | 23,170 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.98x FASTER than tsc; 3.25x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.86s**, **-9.6%** against `temurin26` on identical bytecode — 0.46× tsc, 2.94× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.48×** tsc and **15.96×** tsgo; tsc is **6.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.27s**.

Ahead-of-time (GraalVM native-image): **8.40s** — **0.79×** tsc, **5.08×** tsgo, **3.14×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0e15badf9b4d | **6.42s** | 30,303 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0e15badf9b4d | **5.45s** | 35,698 LOC/s | 46 |
| xtsc (cold JVM) | 0e15badf9b4d | 26.79s | 7,268 LOC/s | 46 |
| tsc | 6.0.3 | 13.07s | 14,896 LOC/s | 65 |
| tsgo | 7.0.2 | 2.39s | 81,601 LOC/s | 65 |
| xtsc-native | 0e15badf9b4d (AOT) | 9.66s | 20,147 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.03x FASTER than tsc; 2.69x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.45s**, **-15.1%** against `temurin26` on identical bytecode — 0.42× tsc, 2.29× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.05×** tsc and **11.23×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.65s**.

Ahead-of-time (GraalVM native-image): **9.66s** — **0.74×** tsc, **4.05×** tsgo, **2.77×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
