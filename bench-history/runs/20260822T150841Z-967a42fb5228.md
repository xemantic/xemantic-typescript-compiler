# Bench run 20260822T150841Z

- **xtsc revision**: [`967a42fb5228`](https://github.com/xemantic/xemantic-typescript-compiler/commit/967a42fb5228e6a70478eac3616d668decab9543)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 967a42fb5228e6a70478eac3616d668decab9543

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 967a42fb5228 | **3.12s** | 62,404 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 967a42fb5228 | **3.11s** | 62,585 LOC/s | 46 |
| xtsc (cold JVM) | 967a42fb5228 | 19.00s | 10,245 LOC/s | 46 |
| tsc | 6.0.3 | 8.26s | 23,565 LOC/s | 65 |
| tsgo | 7.0.2 | 1.25s | 155,264 LOC/s | 65 |
| xtsc-native | 967a42fb5228 (AOT) | 5.32s | 36,591 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.65x FASTER than tsc; 2.49x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.11s**, **-0.3%** against `temurin26` on identical bytecode — 0.38× tsc, 2.48× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.30×** tsc and **15.15×** tsgo; tsc is **6.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.91s**.

Ahead-of-time (GraalVM native-image): **5.32s** — **0.64×** tsc, **4.24×** tsgo, **3.57×** the COLD JVM arm and **1.71×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 967a42fb5228 | **3.94s** | 49,454 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 967a42fb5228 | **3.74s** | 52,059 LOC/s | 46 |
| xtsc (cold JVM) | 967a42fb5228 | 20.98s | 9,282 LOC/s | 46 |
| tsc | 6.0.3 | 10.17s | 19,137 LOC/s | 65 |
| tsgo | 7.0.2 | 1.84s | 105,586 LOC/s | 65 |
| xtsc-native | 967a42fb5228 (AOT) | 6.13s | 31,756 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.58x FASTER than tsc; 2.14x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.74s**, **-5.0%** against `temurin26` on identical bytecode — 0.37× tsc, 2.03× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.06×** tsc and **11.38×** tsgo; tsc is **5.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.88s**.

Ahead-of-time (GraalVM native-image): **6.13s** — **0.60×** tsc, **3.32×** tsgo, **3.42×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
