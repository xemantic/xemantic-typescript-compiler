# Bench run 20260906T151140Z

- **xtsc revision**: [`6bdcd647adbc`](https://github.com/xemantic/xemantic-typescript-compiler/commit/6bdcd647adbc0f3df7467fbade1005fa91f86a1a)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 6bdcd647adbc0f3df7467fbade1005fa91f86a1a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6bdcd647adbc | **14.56s** | 13,371 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6bdcd647adbc | **15.01s** | 12,974 LOC/s | 46 |
| xtsc (cold JVM) | 6bdcd647adbc | 37.19s | 5,235 LOC/s | 46 |
| tsc | 6.0.3 | 11.08s | 17,575 LOC/s | 65 |
| tsgo | 7.0.2 | 1.76s | 110,563 LOC/s | 65 |
| xtsc-native | 6bdcd647adbc (AOT) | 24.61s | 7,910 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.31x tsc — parity at 1.00x; 8.27x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **15.01s**, **+3.1%** against `temurin26` on identical bytecode — 1.35× tsc, 8.52× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.36×** tsc and **21.12×** tsgo; tsc is **6.29×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **37.07s**.

Ahead-of-time (GraalVM native-image): **24.61s** — **2.22×** tsc, **13.98×** tsgo, **1.51×** the COLD JVM arm and **1.69×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6bdcd647adbc | **16.20s** | 12,017 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6bdcd647adbc | **15.57s** | 12,506 LOC/s | 46 |
| xtsc (cold JVM) | 6bdcd647adbc | 39.03s | 4,988 LOC/s | 46 |
| tsc | 6.0.3 | 13.32s | 14,620 LOC/s | 65 |
| tsgo | 7.0.2 | 2.44s | 79,665 LOC/s | 65 |
| xtsc-native | 6bdcd647adbc (AOT) | 25.20s | 7,725 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.22x tsc — parity at 1.00x; 6.63x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **15.57s**, **-3.9%** against `temurin26` on identical bytecode — 1.17× tsc, 6.37× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.93×** tsc and **15.97×** tsgo; tsc is **5.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **38.90s**.

Ahead-of-time (GraalVM native-image): **25.20s** — **1.89×** tsc, **10.31×** tsgo, **1.55×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
