# Bench run 20260829T213650Z

- **xtsc revision**: [`7c583c1d50fb`](https://github.com/xemantic/xemantic-typescript-compiler/commit/7c583c1d50fbe70f08c8048cda77117b6d716f77)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 7c583c1d50fbe70f08c8048cda77117b6d716f77

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7c583c1d50fb | **2.76s** | 70,620 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7c583c1d50fb | **2.62s** | 74,398 LOC/s | 46 |
| xtsc (cold JVM) | 7c583c1d50fb | 18.18s | 10,711 LOC/s | 46 |
| tsc | 6.0.3 | 7.20s | 27,030 LOC/s | 65 |
| tsgo | 7.0.2 | 1.11s | 175,407 LOC/s | 65 |
| xtsc-native | 7c583c1d50fb (AOT) | 4.43s | 43,901 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.61x FASTER than tsc; 2.48x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.62s**, **-5.1%** against `temurin26` on identical bytecode — 0.36× tsc, 2.36× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.52×** tsc and **16.38×** tsgo; tsc is **6.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.09s**.

Ahead-of-time (GraalVM native-image): **4.43s** — **0.62×** tsc, **4.00×** tsgo, **4.10×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7c583c1d50fb | **3.41s** | 57,130 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7c583c1d50fb | **3.39s** | 57,451 LOC/s | 46 |
| xtsc (cold JVM) | 7c583c1d50fb | 18.66s | 10,431 LOC/s | 46 |
| tsc | 6.0.3 | 8.92s | 21,834 LOC/s | 65 |
| tsgo | 7.0.2 | 1.62s | 120,409 LOC/s | 65 |
| xtsc-native | 7c583c1d50fb (AOT) | 5.29s | 36,791 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.62x FASTER than tsc; 2.11x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.39s**, **-0.6%** against `temurin26` on identical bytecode — 0.38× tsc, 2.10× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.09×** tsc and **11.54×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.58s**.

Ahead-of-time (GraalVM native-image): **5.29s** — **0.59×** tsc, **3.27×** tsgo, **3.53×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
