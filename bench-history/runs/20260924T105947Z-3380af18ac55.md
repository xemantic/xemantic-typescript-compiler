# Bench run 20260924T105947Z

- **xtsc revision**: [`3380af18ac55`](https://github.com/xemantic/xemantic-typescript-compiler/commit/3380af18ac557e362d58f286f8a2052a9fedabc3)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 3380af18ac557e362d58f286f8a2052a9fedabc3

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3380af18ac55 | **6.42s** | 30,308 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3380af18ac55 | **5.67s** | 34,345 LOC/s | 46 |
| xtsc (cold JVM) | 3380af18ac55 | 28.81s | 6,757 LOC/s | 46 |
| tsc | 6.0.3 | 11.20s | 17,381 LOC/s | 65 |
| tsgo | 7.0.2 | 1.75s | 111,194 LOC/s | 65 |
| xtsc-native | 3380af18ac55 (AOT) | 9.79s | 19,898 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.74x FASTER than tsc; 3.67x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.67s**, **-11.8%** against `temurin26` on identical bytecode — 0.51× tsc, 3.24× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.57×** tsc and **16.45×** tsgo; tsc is **6.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.69s**.

Ahead-of-time (GraalVM native-image): **9.79s** — **0.87×** tsc, **5.59×** tsgo, **2.94×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3380af18ac55 | **6.98s** | 27,906 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3380af18ac55 | **6.41s** | 30,388 LOC/s | 46 |
| xtsc (cold JVM) | 3380af18ac55 | 31.53s | 6,175 LOC/s | 46 |
| tsc | 6.0.3 | 13.77s | 14,136 LOC/s | 65 |
| tsgo | 7.0.2 | 2.47s | 78,858 LOC/s | 65 |
| xtsc-native | 3380af18ac55 (AOT) | 11.48s | 16,954 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.97x FASTER than tsc; 2.83x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.41s**, **-8.2%** against `temurin26` on identical bytecode — 0.47× tsc, 2.59× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.29×** tsc and **12.77×** tsgo; tsc is **5.58×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **31.40s**.

Ahead-of-time (GraalVM native-image): **11.48s** — **0.83×** tsc, **4.65×** tsgo, **2.75×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
