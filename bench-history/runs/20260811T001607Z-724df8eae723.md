# Bench run 20260811T001607Z

- **xtsc revision**: [`724df8eae723`](https://github.com/xemantic/xemantic-typescript-compiler/commit/724df8eae7230cb6b4cdca187d4e774b4f456c0f)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 724df8eae7230cb6b4cdca187d4e774b4f456c0f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 724df8eae723 | **3.71s** | 52,508 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 724df8eae723 | **3.36s** | 57,981 LOC/s | 46 |
| xtsc (cold JVM) | 724df8eae723 | 18.06s | 10,779 LOC/s | 46 |
| tsc | 6.0.3 | 8.10s | 24,037 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.23s | 158,681 LOC/s | 65 |
| xtsc-native | 724df8eae723 (AOT) | 5.62s | 34,650 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.18x FASTER than tsc; 3.02x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.36s**, **-9.4%** against `temurin26` on identical bytecode — 0.41× tsc, 2.74× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.23×** tsc and **14.72×** tsgo; tsc is **6.60×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.96s**.

Ahead-of-time (GraalVM native-image): **5.62s** — **0.69×** tsc, **4.58×** tsgo, **3.21×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 724df8eae723 | **4.32s** | 45,049 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 724df8eae723 | **3.88s** | 50,167 LOC/s | 46 |
| xtsc (cold JVM) | 724df8eae723 | 22.52s | 8,645 LOC/s | 46 |
| tsc | 6.0.3 | 9.95s | 19,568 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.77s | 109,876 LOC/s | 65 |
| xtsc-native | 724df8eae723 (AOT) | 6.95s | 27,998 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.30x FASTER than tsc; 2.44x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.88s**, **-10.2%** against `temurin26` on identical bytecode — 0.39× tsc, 2.19× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.26×** tsc and **12.71×** tsgo; tsc is **5.62×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.42s**.

Ahead-of-time (GraalVM native-image): **6.95s** — **0.70×** tsc, **3.92×** tsgo, **3.24×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
