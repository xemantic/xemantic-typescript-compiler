# Bench run 20260817T205947Z

- **xtsc revision**: [`db56a15e8b83`](https://github.com/xemantic/xemantic-typescript-compiler/commit/db56a15e8b83e6aa56f0dc99e3c5bc70bfb74c32)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push db56a15e8b83e6aa56f0dc99e3c5bc70bfb74c32

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | db56a15e8b83 | **2.24s** | 86,920 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | db56a15e8b83 | **2.23s** | 87,349 LOC/s | 46 |
| xtsc (cold JVM) | db56a15e8b83 | 14.05s | 13,861 LOC/s | 46 |
| tsc | 6.0.3 | 5.83s | 33,402 LOC/s | 65 |
| tsgo | 7.0.2 | 0.89s | 219,012 LOC/s | 65 |
| xtsc-native | db56a15e8b83 (AOT) | 3.57s | 54,599 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.60x FASTER than tsc; 2.52x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.23s**, **-0.5%** against `temurin26` on identical bytecode — 0.38× tsc, 2.51× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.41×** tsc and **15.80×** tsgo; tsc is **6.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **13.96s**.

Ahead-of-time (GraalVM native-image): **3.57s** — **0.61×** tsc, **4.01×** tsgo, **3.94×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | db56a15e8b83 | **2.73s** | 71,424 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | db56a15e8b83 | **2.71s** | 71,766 LOC/s | 46 |
| xtsc (cold JVM) | db56a15e8b83 | 13.91s | 13,999 LOC/s | 46 |
| tsc | 6.0.3 | 7.25s | 26,862 LOC/s | 65 |
| tsgo | 7.0.2 | 1.34s | 145,735 LOC/s | 65 |
| xtsc-native | db56a15e8b83 (AOT) | 4.35s | 44,728 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.66x FASTER than tsc; 2.04x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.71s**, **-0.5%** against `temurin26` on identical bytecode — 0.37× tsc, 2.03× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.92×** tsc and **10.41×** tsgo; tsc is **5.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **13.81s**.

Ahead-of-time (GraalVM native-image): **4.35s** — **0.60×** tsc, **3.26×** tsgo, **3.20×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
