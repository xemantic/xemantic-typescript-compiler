# Bench run 20260818T100543Z

- **xtsc revision**: [`cd0750d2a0fa`](https://github.com/xemantic/xemantic-typescript-compiler/commit/cd0750d2a0fa1329c81742a293ff86a2dc88a07f)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push cd0750d2a0fa1329c81742a293ff86a2dc88a07f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cd0750d2a0fa | **4.14s** | 47,074 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cd0750d2a0fa | **3.82s** | 50,982 LOC/s | 46 |
| xtsc (cold JVM) | cd0750d2a0fa | 21.75s | 8,950 LOC/s | 46 |
| tsc | 6.0.3 | 10.82s | 17,997 LOC/s | 65 |
| tsgo | 7.0.2 | 1.71s | 114,194 LOC/s | 65 |
| xtsc-native | cd0750d2a0fa (AOT) | 6.61s | 29,442 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.62x FASTER than tsc; 2.43x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.82s**, **-7.7%** against `temurin26` on identical bytecode — 0.35× tsc, 2.24× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.01×** tsc and **12.76×** tsgo; tsc is **6.34×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.63s**.

Ahead-of-time (GraalVM native-image): **6.61s** — **0.61×** tsc, **3.88×** tsgo, **3.29×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cd0750d2a0fa | **4.87s** | 39,988 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cd0750d2a0fa | **4.39s** | 44,320 LOC/s | 46 |
| xtsc (cold JVM) | cd0750d2a0fa | 27.75s | 7,017 LOC/s | 46 |
| tsc | 6.0.3 | 13.20s | 14,754 LOC/s | 65 |
| tsgo | 7.0.2 | 2.46s | 79,114 LOC/s | 65 |
| xtsc-native | cd0750d2a0fa (AOT) | 7.22s | 26,952 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.71x FASTER than tsc; 1.98x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.39s**, **-9.8%** against `temurin26` on identical bytecode — 0.33× tsc, 1.79× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.10×** tsc and **11.27×** tsgo; tsc is **5.36×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.63s**.

Ahead-of-time (GraalVM native-image): **7.22s** — **0.55×** tsc, **2.94×** tsgo, **3.84×** the COLD JVM arm and **1.48×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
