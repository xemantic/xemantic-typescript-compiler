# Bench run 20260922T221422Z

- **xtsc revision**: [`36305af3ae94`](https://github.com/xemantic/xemantic-typescript-compiler/commit/36305af3ae94485358c8fba8d4d84620f43e8ec2)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 36305af3ae94485358c8fba8d4d84620f43e8ec2

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 36305af3ae94 | **5.99s** | 32,515 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 36305af3ae94 | **5.40s** | 36,029 LOC/s | 46 |
| xtsc (cold JVM) | 36305af3ae94 | 26.68s | 7,296 LOC/s | 46 |
| tsc | 6.0.3 | 10.85s | 17,951 LOC/s | 65 |
| tsgo | 7.0.2 | 1.68s | 115,756 LOC/s | 65 |
| xtsc-native | 36305af3ae94 (AOT) | 9.51s | 20,471 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.81x FASTER than tsc; 3.56x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.40s**, **-9.8%** against `temurin26` on identical bytecode — 0.50× tsc, 3.21× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.46×** tsc and **15.87×** tsgo; tsc is **6.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.56s**.

Ahead-of-time (GraalVM native-image): **9.51s** — **0.88×** tsc, **5.65×** tsgo, **2.81×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 36305af3ae94 | **6.79s** | 28,687 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 36305af3ae94 | **6.13s** | 31,762 LOC/s | 46 |
| xtsc (cold JVM) | 36305af3ae94 | 30.84s | 6,313 LOC/s | 46 |
| tsc | 6.0.3 | 12.98s | 15,000 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,876 LOC/s | 65 |
| xtsc-native | 36305af3ae94 (AOT) | 10.54s | 18,476 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.91x FASTER than tsc; 2.85x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.13s**, **-9.7%** against `temurin26` on identical bytecode — 0.47× tsc, 2.58× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.38×** tsc and **12.97×** tsgo; tsc is **5.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.69s**.

Ahead-of-time (GraalVM native-image): **10.54s** — **0.81×** tsc, **4.43×** tsgo, **2.93×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
