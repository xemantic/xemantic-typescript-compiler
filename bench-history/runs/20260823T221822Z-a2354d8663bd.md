# Bench run 20260823T221822Z

- **xtsc revision**: [`a2354d8663bd`](https://github.com/xemantic/xemantic-typescript-compiler/commit/a2354d8663bdb5b9826e3a0894b610338e388090)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push a2354d8663bdb5b9826e3a0894b610338e388090

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a2354d8663bd | **4.10s** | 47,523 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a2354d8663bd | **3.92s** | 49,706 LOC/s | 46 |
| xtsc (cold JVM) | a2354d8663bd | 25.20s | 7,726 LOC/s | 46 |
| tsc | 6.0.3 | 11.26s | 17,296 LOC/s | 65 |
| tsgo | 7.0.2 | 1.79s | 108,832 LOC/s | 65 |
| xtsc-native | a2354d8663bd (AOT) | 6.99s | 27,838 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.75x FASTER than tsc; 2.29x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.92s**, **-4.4%** against `temurin26` on identical bytecode — 0.35× tsc, 2.19× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.24×** tsc and **14.09×** tsgo; tsc is **6.29×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.09s**.

Ahead-of-time (GraalVM native-image): **6.99s** — **0.62×** tsc, **3.91×** tsgo, **3.60×** the COLD JVM arm and **1.71×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a2354d8663bd | **5.22s** | 37,292 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a2354d8663bd | **4.55s** | 42,754 LOC/s | 46 |
| xtsc (cold JVM) | a2354d8663bd | 27.72s | 7,024 LOC/s | 46 |
| tsc | 6.0.3 | 13.90s | 14,008 LOC/s | 65 |
| tsgo | 7.0.2 | 2.49s | 78,130 LOC/s | 65 |
| xtsc-native | a2354d8663bd (AOT) | 7.97s | 24,429 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.66x FASTER than tsc; 2.10x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.55s**, **-12.8%** against `temurin26` on identical bytecode — 0.33× tsc, 1.83× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.99×** tsc and **11.12×** tsgo; tsc is **5.58×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.58s**.

Ahead-of-time (GraalVM native-image): **7.97s** — **0.57×** tsc, **3.20×** tsgo, **3.48×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
