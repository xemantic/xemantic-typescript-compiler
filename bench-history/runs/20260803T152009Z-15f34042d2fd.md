# Bench run 20260803T152009Z

- **xtsc revision**: [`15f34042d2fd`](https://github.com/xemantic/xemantic-typescript-compiler/commit/15f34042d2fdac55bbcb4020615ae40a73948325)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 15f34042d2fdac55bbcb4020615ae40a73948325

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 15f34042d2fd | 29.16s | 6,677 LOC/s | 46 |
| tsc | 6.0.3 | 11.13s | 17,499 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.67s | 116,727 LOC/s | 65 |
| xtsc-native | 15f34042d2fd (AOT) | 11.53s | 16,888 LOC/s | 46 |

Relative (wall): xtsc is **2.62×** tsc and **17.48×** tsgo; tsc is **6.67×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.05s**.

Ahead-of-time (GraalVM native-image): **11.53s**, **2.53×** the JVM arm, **1.04×** tsc, **6.91×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 15f34042d2fd | 26.66s | 7,302 LOC/s | 46 |
| tsc | 6.0.3 | 12.71s | 15,321 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.39s | 81,329 LOC/s | 65 |
| xtsc-native | 15f34042d2fd (AOT) | 14.31s | 13,608 LOC/s | 46 |

Relative (wall): xtsc is **2.10×** tsc and **11.14×** tsgo; tsc is **5.31×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.55s**.

Ahead-of-time (GraalVM native-image): **14.31s**, **1.86×** the JVM arm, **1.13×** tsc, **5.98×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
