# Bench run 20260811T085620Z

- **xtsc revision**: [`7c7ac71a6c9f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/7c7ac71a6c9ff4a06f51c6edbb1aac4ba44add73)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 7c7ac71a6c9ff4a06f51c6edbb1aac4ba44add73

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7c7ac71a6c9f | **4.40s** | 44,220 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7c7ac71a6c9f | **4.10s** | 47,465 LOC/s | 46 |
| xtsc (cold JVM) | 7c7ac71a6c9f | 23.84s | 8,168 LOC/s | 46 |
| tsc | 6.0.3 | 10.67s | 18,249 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.68s | 116,170 LOC/s | 65 |
| xtsc-native | 7c7ac71a6c9f (AOT) | 6.74s | 28,887 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.42x FASTER than tsc; 2.63x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.10s**, **-6.8%** against `temurin26` on identical bytecode — 0.38× tsc, 2.45× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.23×** tsc and **14.22×** tsgo; tsc is **6.37×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.73s**.

Ahead-of-time (GraalVM native-image): **6.74s** — **0.63×** tsc, **4.02×** tsgo, **3.54×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7c7ac71a6c9f | **5.59s** | 34,824 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7c7ac71a6c9f | **5.00s** | 38,909 LOC/s | 46 |
| xtsc (cold JVM) | 7c7ac71a6c9f | 25.83s | 7,538 LOC/s | 46 |
| tsc | 6.0.3 | 13.26s | 14,681 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.37s | 82,291 LOC/s | 65 |
| xtsc-native | 7c7ac71a6c9f (AOT) | 8.40s | 23,165 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.37x FASTER than tsc; 2.36x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.00s**, **-10.5%** against `temurin26` on identical bytecode — 0.38× tsc, 2.11× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.95×** tsc and **10.92×** tsgo; tsc is **5.61×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.69s**.

Ahead-of-time (GraalVM native-image): **8.40s** — **0.63×** tsc, **3.55×** tsgo, **3.07×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
