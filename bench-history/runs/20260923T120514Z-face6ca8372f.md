# Bench run 20260923T120514Z

- **xtsc revision**: [`face6ca8372f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/face6ca8372f174d1b63ffbea123847f4d20886e)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push face6ca8372f174d1b63ffbea123847f4d20886e

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | face6ca8372f | **4.70s** | 41,470 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | face6ca8372f | **4.43s** | 43,960 LOC/s | 46 |
| xtsc (cold JVM) | face6ca8372f | 20.11s | 9,679 LOC/s | 46 |
| tsc | 6.0.3 | 8.34s | 23,359 LOC/s | 65 |
| tsgo | 7.0.2 | 1.25s | 155,512 LOC/s | 65 |
| xtsc-native | face6ca8372f (AOT) | 7.83s | 24,869 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.78x FASTER than tsc; 3.75x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.43s**, **-5.7%** against `temurin26` on identical bytecode — 0.53× tsc, 3.54× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.41×** tsc and **16.07×** tsgo; tsc is **6.66×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.85s**.

Ahead-of-time (GraalVM native-image): **7.83s** — **0.94×** tsc, **6.25×** tsgo, **2.57×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | face6ca8372f | **5.31s** | 36,687 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | face6ca8372f | **4.82s** | 40,402 LOC/s | 46 |
| xtsc (cold JVM) | face6ca8372f | 23.66s | 8,230 LOC/s | 46 |
| tsc | 6.0.3 | 9.95s | 19,571 LOC/s | 65 |
| tsgo | 7.0.2 | 1.80s | 107,927 LOC/s | 65 |
| xtsc-native | face6ca8372f (AOT) | 8.52s | 22,852 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.87x FASTER than tsc; 2.94x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.82s**, **-9.2%** against `temurin26` on identical bytecode — 0.48× tsc, 2.67× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.38×** tsc and **13.11×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.54s**.

Ahead-of-time (GraalVM native-image): **8.52s** — **0.86×** tsc, **4.72×** tsgo, **2.78×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
