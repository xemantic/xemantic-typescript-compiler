# Bench run 20260804T180231Z

- **xtsc revision**: [`42d8f48cfaf8`](https://github.com/xemantic/xemantic-typescript-compiler/commit/42d8f48cfaf80c466f9e051da5c8bc2c155de822)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 42d8f48cfaf80c466f9e051da5c8bc2c155de822

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 42d8f48cfaf8 | 25.54s | 7,622 LOC/s | 46 |
| tsc | 6.0.3 | 11.39s | 17,098 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.78s | 109,629 LOC/s | 65 |
| xtsc-native | 42d8f48cfaf8 (AOT) | 12.83s | 15,173 LOC/s | 46 |

Relative (wall): xtsc is **2.24×** tsc and **14.38×** tsgo; tsc is **6.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.45s**.

Ahead-of-time (GraalVM native-image): **12.83s**, **1.99×** the JVM arm, **1.13×** tsc, **7.23×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 42d8f48cfaf8 | 29.96s | 6,497 LOC/s | 46 |
| tsc | 6.0.3 | 14.09s | 13,821 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.69s | 72,353 LOC/s | 65 |
| xtsc-native | 42d8f48cfaf8 (AOT) | 13.84s | 14,067 LOC/s | 46 |

Relative (wall): xtsc is **2.13×** tsc and **11.14×** tsgo; tsc is **5.23×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.85s**.

Ahead-of-time (GraalVM native-image): **13.84s**, **2.16×** the JVM arm, **0.98×** tsc, **5.14×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
