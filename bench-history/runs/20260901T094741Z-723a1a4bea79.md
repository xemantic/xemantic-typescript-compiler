# Bench run 20260901T094741Z

- **xtsc revision**: [`723a1a4bea79`](https://github.com/xemantic/xemantic-typescript-compiler/commit/723a1a4bea79956be68269e8b790b98f81a428ce)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 723a1a4bea79956be68269e8b790b98f81a428ce

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 723a1a4bea79 | **4.22s** | 46,126 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 723a1a4bea79 | **4.27s** | 45,586 LOC/s | 46 |
| xtsc (cold JVM) | 723a1a4bea79 | 22.26s | 8,745 LOC/s | 46 |
| tsc | 6.0.3 | 10.51s | 18,520 LOC/s | 65 |
| tsgo | 7.0.2 | 1.56s | 124,808 LOC/s | 65 |
| xtsc-native | 723a1a4bea79 (AOT) | 6.75s | 28,849 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.49x FASTER than tsc; 2.71x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.27s**, **+1.2%** against `temurin26` on identical bytecode — 0.41× tsc, 2.74× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.12×** tsc and **14.27×** tsgo; tsc is **6.74×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.15s**.

Ahead-of-time (GraalVM native-image): **6.75s** — **0.64×** tsc, **4.33×** tsgo, **3.30×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 723a1a4bea79 | **5.15s** | 37,820 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 723a1a4bea79 | **4.78s** | 40,715 LOC/s | 46 |
| xtsc (cold JVM) | 723a1a4bea79 | 26.29s | 7,406 LOC/s | 46 |
| tsc | 6.0.3 | 12.72s | 15,305 LOC/s | 65 |
| tsgo | 7.0.2 | 2.28s | 85,508 LOC/s | 65 |
| xtsc-native | 723a1a4bea79 (AOT) | 8.15s | 23,895 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.47x FASTER than tsc; 2.26x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.78s**, **-7.1%** against `temurin26` on identical bytecode — 0.38× tsc, 2.10× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.07×** tsc and **11.55×** tsgo; tsc is **5.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.16s**.

Ahead-of-time (GraalVM native-image): **8.15s** — **0.64×** tsc, **3.58×** tsgo, **3.23×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
