# Bench run 20260811T192013Z

- **xtsc revision**: [`6e62e4be2f7e`](https://github.com/xemantic/xemantic-typescript-compiler/commit/6e62e4be2f7e33d32a858d7f2256a5a54f9f2d96)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 6e62e4be2f7e33d32a858d7f2256a5a54f9f2d96

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6e62e4be2f7e | **4.41s** | 44,110 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6e62e4be2f7e | **3.96s** | 49,167 LOC/s | 46 |
| xtsc (cold JVM) | 6e62e4be2f7e | 25.53s | 7,625 LOC/s | 46 |
| tsc | 6.0.3 | 10.28s | 18,934 LOC/s | 65 |
| tsgo | 7.0.2 | 1.58s | 123,463 LOC/s | 65 |
| xtsc-native | 6e62e4be2f7e (AOT) | 6.64s | 29,335 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.33x FASTER than tsc; 2.80x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.96s**, **-10.3%** against `temurin26` on identical bytecode — 0.39× tsc, 2.51× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.48×** tsc and **16.19×** tsgo; tsc is **6.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.41s**.

Ahead-of-time (GraalVM native-image): **6.64s** — **0.65×** tsc, **4.21×** tsgo, **3.85×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6e62e4be2f7e | **5.19s** | 37,514 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6e62e4be2f7e | **4.73s** | 41,198 LOC/s | 46 |
| xtsc (cold JVM) | 6e62e4be2f7e | 25.62s | 7,600 LOC/s | 46 |
| tsc | 6.0.3 | 12.62s | 15,428 LOC/s | 65 |
| tsgo | 7.0.2 | 2.28s | 85,395 LOC/s | 65 |
| xtsc-native | 6e62e4be2f7e (AOT) | 8.29s | 23,492 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.43x FASTER than tsc; 2.28x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.73s**, **-8.9%** against `temurin26` on identical bytecode — 0.37× tsc, 2.07× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.03×** tsc and **11.24×** tsgo; tsc is **5.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.48s**.

Ahead-of-time (GraalVM native-image): **8.29s** — **0.66×** tsc, **3.64×** tsgo, **3.09×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
