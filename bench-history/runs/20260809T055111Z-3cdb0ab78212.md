# Bench run 20260809T055111Z

- **xtsc revision**: [`3cdb0ab78212`](https://github.com/xemantic/xemantic-typescript-compiler/commit/3cdb0ab782129d336ca321f14bec55b1645abdda)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 3cdb0ab782129d336ca321f14bec55b1645abdda

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 3cdb0ab78212 | 25.43s | 7,656 LOC/s | 46 |
| tsc | 6.0.3 | 11.69s | 16,662 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.76s | 110,626 LOC/s | 65 |
| xtsc-native | 3cdb0ab78212 (AOT) | 11.69s | 16,654 LOC/s | 46 |

Relative (wall): xtsc is **2.18×** tsc and **14.45×** tsgo; tsc is **6.64×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.31s**.

Ahead-of-time (GraalVM native-image): **11.69s**, **2.18×** the JVM arm, **1.00×** tsc, **6.64×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 3cdb0ab78212 | 30.19s | 6,448 LOC/s | 46 |
| tsc | 6.0.3 | 13.98s | 13,931 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.52s | 77,262 LOC/s | 65 |
| xtsc-native | 3cdb0ab78212 (AOT) | 13.47s | 14,457 LOC/s | 46 |

Relative (wall): xtsc is **2.16×** tsc and **11.98×** tsgo; tsc is **5.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.07s**.

Ahead-of-time (GraalVM native-image): **13.47s**, **2.24×** the JVM arm, **0.96×** tsc, **5.34×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
