# Bench run 20260915T184251Z

- **xtsc revision**: [`f4b8a8325dfc`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f4b8a8325dfc63019a4a3d537fa4b6003bd29a3a)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push f4b8a8325dfc63019a4a3d537fa4b6003bd29a3a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f4b8a8325dfc | **3.94s** | 49,441 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f4b8a8325dfc | **3.73s** | 52,143 LOC/s | 46 |
| xtsc (cold JVM) | f4b8a8325dfc | 21.08s | 9,235 LOC/s | 46 |
| tsc | 6.0.3 | 7.82s | 24,901 LOC/s | 65 |
| tsgo | 7.0.2 | 1.23s | 158,294 LOC/s | 65 |
| xtsc-native | f4b8a8325dfc (AOT) | 6.01s | 32,369 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.99x FASTER than tsc; 3.20x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.73s**, **-5.2%** against `temurin26` on identical bytecode — 0.48× tsc, 3.04× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.70×** tsc and **17.14×** tsgo; tsc is **6.36×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.99s**.

Ahead-of-time (GraalVM native-image): **6.01s** — **0.77×** tsc, **4.89×** tsgo, **3.50×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f4b8a8325dfc | **4.72s** | 41,267 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f4b8a8325dfc | **4.39s** | 44,371 LOC/s | 46 |
| xtsc (cold JVM) | f4b8a8325dfc | 20.79s | 9,367 LOC/s | 46 |
| tsc | 6.0.3 | 9.57s | 20,334 LOC/s | 65 |
| tsgo | 7.0.2 | 1.84s | 105,529 LOC/s | 65 |
| xtsc-native | f4b8a8325dfc (AOT) | 7.34s | 26,529 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.03x FASTER than tsc; 2.56x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.39s**, **-7.0%** against `temurin26` on identical bytecode — 0.46× tsc, 2.38× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.17×** tsc and **11.27×** tsgo; tsc is **5.19×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.66s**.

Ahead-of-time (GraalVM native-image): **7.34s** — **0.77×** tsc, **3.98×** tsgo, **2.83×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
