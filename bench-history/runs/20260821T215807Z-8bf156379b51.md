# Bench run 20260821T215807Z

- **xtsc revision**: [`8bf156379b51`](https://github.com/xemantic/xemantic-typescript-compiler/commit/8bf156379b51e3025b5caa4feecb782229b7f7ee)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 8bf156379b51e3025b5caa4feecb782229b7f7ee

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8bf156379b51 | **3.38s** | 57,587 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8bf156379b51 | **3.43s** | 56,747 LOC/s | 46 |
| xtsc (cold JVM) | 8bf156379b51 | 18.54s | 10,503 LOC/s | 46 |
| tsc | 6.0.3 | 8.95s | 21,764 LOC/s | 65 |
| tsgo | 7.0.2 | 1.31s | 148,287 LOC/s | 65 |
| xtsc-native | 8bf156379b51 (AOT) | 5.93s | 32,822 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.65x FASTER than tsc; 2.58x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.43s**, **+1.5%** against `temurin26` on identical bytecode — 0.38× tsc, 2.61× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.07×** tsc and **14.12×** tsgo; tsc is **6.81×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.44s**.

Ahead-of-time (GraalVM native-image): **5.93s** — **0.66×** tsc, **4.52×** tsgo, **3.12×** the COLD JVM arm and **1.75×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8bf156379b51 | **4.29s** | 45,438 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8bf156379b51 | **4.04s** | 48,193 LOC/s | 46 |
| xtsc (cold JVM) | 8bf156379b51 | 21.37s | 9,111 LOC/s | 46 |
| tsc | 6.0.3 | 10.80s | 18,032 LOC/s | 65 |
| tsgo | 7.0.2 | 1.91s | 101,991 LOC/s | 65 |
| xtsc-native | 8bf156379b51 (AOT) | 6.72s | 28,969 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.52x FASTER than tsc; 2.24x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.04s**, **-5.7%** against `temurin26` on identical bytecode — 0.37× tsc, 2.12× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.98×** tsc and **11.19×** tsgo; tsc is **5.66×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.27s**.

Ahead-of-time (GraalVM native-image): **6.72s** — **0.62×** tsc, **3.52×** tsgo, **3.18×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
