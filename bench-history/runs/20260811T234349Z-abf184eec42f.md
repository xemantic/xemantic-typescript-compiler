# Bench run 20260811T234349Z

- **xtsc revision**: [`abf184eec42f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/abf184eec42f499eff6b2d0b86544d4c820a862f)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push abf184eec42f499eff6b2d0b86544d4c820a862f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | abf184eec42f | **2.49s** | 78,068 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | abf184eec42f | **2.31s** | 84,140 LOC/s | 46 |
| xtsc (cold JVM) | abf184eec42f | 14.64s | 13,301 LOC/s | 46 |
| tsc | 6.0.3 | 6.82s | 28,565 LOC/s | 65 |
| tsgo | 7.0.2 | 1.18s | 165,281 LOC/s | 65 |
| xtsc-native | abf184eec42f (AOT) | 4.02s | 48,421 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.73x FASTER than tsc; 2.12x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.31s**, **-7.2%** against `temurin26` on identical bytecode — 0.34× tsc, 1.96× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.15×** tsc and **12.43×** tsgo; tsc is **5.79×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **14.56s**.

Ahead-of-time (GraalVM native-image): **4.02s** — **0.59×** tsc, **3.41×** tsgo, **3.64×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | abf184eec42f | **3.33s** | 58,469 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | abf184eec42f | **2.99s** | 65,183 LOC/s | 46 |
| xtsc (cold JVM) | abf184eec42f | 17.79s | 10,947 LOC/s | 46 |
| tsc | 6.0.3 | 8.08s | 24,093 LOC/s | 65 |
| tsgo | 7.0.2 | 1.54s | 126,429 LOC/s | 65 |
| xtsc-native | abf184eec42f (AOT) | 4.88s | 39,889 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.43x FASTER than tsc; 2.16x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.99s**, **-10.3%** against `temurin26` on identical bytecode — 0.37× tsc, 1.94× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.20×** tsc and **11.55×** tsgo; tsc is **5.25×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.70s**.

Ahead-of-time (GraalVM native-image): **4.88s** — **0.60×** tsc, **3.17×** tsgo, **3.64×** the COLD JVM arm and **1.47×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
