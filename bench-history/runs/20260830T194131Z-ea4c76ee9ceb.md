# Bench run 20260830T194131Z

- **xtsc revision**: [`ea4c76ee9ceb`](https://github.com/xemantic/xemantic-typescript-compiler/commit/ea4c76ee9ceb0ad4dfcd4ebc61dcc3a9437e0025)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push ea4c76ee9ceb0ad4dfcd4ebc61dcc3a9437e0025

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ea4c76ee9ceb | **3.20s** | 60,901 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ea4c76ee9ceb | **3.04s** | 64,004 LOC/s | 46 |
| xtsc (cold JVM) | ea4c76ee9ceb | 21.78s | 8,937 LOC/s | 46 |
| tsc | 6.0.3 | 9.05s | 21,502 LOC/s | 65 |
| tsgo | 7.0.2 | 1.39s | 139,972 LOC/s | 65 |
| xtsc-native | ea4c76ee9ceb (AOT) | 5.20s | 37,471 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.83x FASTER than tsc; 2.30x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.04s**, **-4.8%** against `temurin26` on identical bytecode — 0.34× tsc, 2.19× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.41×** tsc and **15.66×** tsgo; tsc is **6.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.69s**.

Ahead-of-time (GraalVM native-image): **5.20s** — **0.57×** tsc, **3.74×** tsgo, **4.19×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ea4c76ee9ceb | **4.08s** | 47,662 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ea4c76ee9ceb | **3.85s** | 50,571 LOC/s | 46 |
| xtsc (cold JVM) | ea4c76ee9ceb | 20.87s | 9,328 LOC/s | 46 |
| tsc | 6.0.3 | 10.83s | 17,974 LOC/s | 65 |
| tsgo | 7.0.2 | 2.10s | 92,582 LOC/s | 65 |
| xtsc-native | ea4c76ee9ceb (AOT) | 6.38s | 30,531 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.65x FASTER than tsc; 1.94x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.85s**, **-5.8%** against `temurin26` on identical bytecode — 0.36× tsc, 1.83× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.93×** tsc and **9.92×** tsgo; tsc is **5.15×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.75s**.

Ahead-of-time (GraalVM native-image): **6.38s** — **0.59×** tsc, **3.03×** tsgo, **3.27×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
