# Bench run 20260915T232958Z

- **xtsc revision**: [`584e7955aa13`](https://github.com/xemantic/xemantic-typescript-compiler/commit/584e7955aa137ce90a1ed2324d9504225cfd2152)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 584e7955aa137ce90a1ed2324d9504225cfd2152

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 584e7955aa13 | **5.58s** | 34,899 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 584e7955aa13 | **5.28s** | 36,910 LOC/s | 46 |
| xtsc (cold JVM) | 584e7955aa13 | 26.85s | 7,251 LOC/s | 46 |
| tsc | 6.0.3 | 10.79s | 18,046 LOC/s | 65 |
| tsgo | 7.0.2 | 1.68s | 116,240 LOC/s | 65 |
| xtsc-native | 584e7955aa13 (AOT) | 9.29s | 20,967 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.93x FASTER than tsc; 3.33x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.28s**, **-5.4%** against `temurin26` on identical bytecode — 0.49× tsc, 3.15× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.49×** tsc and **16.03×** tsgo; tsc is **6.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.75s**.

Ahead-of-time (GraalVM native-image): **9.29s** — **0.86×** tsc, **5.54×** tsgo, **2.89×** the COLD JVM arm and **1.66×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 584e7955aa13 | **6.50s** | 29,958 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 584e7955aa13 | **5.83s** | 33,413 LOC/s | 46 |
| xtsc (cold JVM) | 584e7955aa13 | 30.30s | 6,425 LOC/s | 46 |
| tsc | 6.0.3 | 13.18s | 14,777 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,670 LOC/s | 65 |
| xtsc-native | 584e7955aa13 (AOT) | 10.24s | 19,017 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.03x FASTER than tsc; 2.73x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.83s**, **-10.3%** against `temurin26` on identical bytecode — 0.44× tsc, 2.44× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.30×** tsc and **12.71×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.18s**.

Ahead-of-time (GraalVM native-image): **10.24s** — **0.78×** tsc, **4.29×** tsgo, **2.96×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
