# Bench run 20260924T191759Z

- **xtsc revision**: [`9c48ea974228`](https://github.com/xemantic/xemantic-typescript-compiler/commit/9c48ea974228379ad8eb952714dd6e609361cf97)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 9c48ea974228379ad8eb952714dd6e609361cf97

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9c48ea974228 | **6.07s** | 32,081 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9c48ea974228 | **5.67s** | 34,332 LOC/s | 46 |
| xtsc (cold JVM) | 9c48ea974228 | 30.06s | 6,476 LOC/s | 46 |
| tsc | 6.0.3 | 11.24s | 17,319 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,344 LOC/s | 65 |
| xtsc-native | 9c48ea974228 (AOT) | 9.75s | 19,969 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.85x FASTER than tsc; 3.60x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.67s**, **-6.6%** against `temurin26` on identical bytecode — 0.50× tsc, 3.36× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.67×** tsc and **17.81×** tsgo; tsc is **6.66×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.94s**.

Ahead-of-time (GraalVM native-image): **9.75s** — **0.87×** tsc, **5.78×** tsgo, **3.08×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9c48ea974228 | **6.88s** | 28,320 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9c48ea974228 | **6.45s** | 30,200 LOC/s | 46 |
| xtsc (cold JVM) | 9c48ea974228 | 32.65s | 5,963 LOC/s | 46 |
| tsc | 6.0.3 | 13.55s | 14,372 LOC/s | 65 |
| tsgo | 7.0.2 | 2.51s | 77,508 LOC/s | 65 |
| xtsc-native | 9c48ea974228 (AOT) | 11.36s | 17,136 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.97x FASTER than tsc; 2.74x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.45s**, **-6.2%** against `temurin26` on identical bytecode — 0.48× tsc, 2.57× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.41×** tsc and **13.00×** tsgo; tsc is **5.39×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **32.50s**.

Ahead-of-time (GraalVM native-image): **11.36s** — **0.84×** tsc, **4.52×** tsgo, **2.87×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
