# Bench run 20260809T103611Z

- **xtsc revision**: [`7088747fe978`](https://github.com/xemantic/xemantic-typescript-compiler/commit/7088747fe978fed959d574113c2ba358ce5b9780)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 7088747fe978fed959d574113c2ba358ce5b9780

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 7088747fe978 | 17.60s | 11,063 LOC/s | 46 |
| tsc | 6.0.3 | 7.17s | 27,139 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.12s | 173,841 LOC/s | 65 |
| xtsc-native | 7088747fe978 (AOT) | 7.38s | 26,393 LOC/s | 46 |

Relative (wall): xtsc is **2.45×** tsc and **15.71×** tsgo; tsc is **6.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.52s**.

Ahead-of-time (GraalVM native-image): **7.38s**, **2.39×** the JVM arm, **1.03×** tsc, **6.59×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 7088747fe978 | 20.23s | 9,622 LOC/s | 46 |
| tsc | 6.0.3 | 8.62s | 22,576 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.66s | 117,149 LOC/s | 65 |
| xtsc-native | 7088747fe978 (AOT) | 9.41s | 20,682 LOC/s | 46 |

Relative (wall): xtsc is **2.35×** tsc and **12.18×** tsgo; tsc is **5.19×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.05s**.

Ahead-of-time (GraalVM native-image): **9.41s**, **2.15×** the JVM arm, **1.09×** tsc, **5.66×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
