# Bench run 20260809T082946Z

- **xtsc revision**: [`71654f25b84f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/71654f25b84fbdb459d722d289fe6a154afc7c08)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 71654f25b84fbdb459d722d289fe6a154afc7c08

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 71654f25b84f | 23.82s | 8,173 LOC/s | 46 |
| tsc | 6.0.3 | 10.39s | 18,739 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.56s | 124,969 LOC/s | 65 |
| xtsc-native | 71654f25b84f (AOT) | 11.07s | 17,586 LOC/s | 46 |

Relative (wall): xtsc is **2.29×** tsc and **15.29×** tsgo; tsc is **6.67×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.70s**.

Ahead-of-time (GraalVM native-image): **11.07s**, **2.15×** the JVM arm, **1.07×** tsc, **7.11×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 71654f25b84f | 28.25s | 6,892 LOC/s | 46 |
| tsc | 6.0.3 | 12.89s | 15,107 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.30s | 84,579 LOC/s | 65 |
| xtsc-native | 71654f25b84f (AOT) | 13.75s | 14,158 LOC/s | 46 |

Relative (wall): xtsc is **2.19×** tsc and **12.27×** tsgo; tsc is **5.60×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.12s**.

Ahead-of-time (GraalVM native-image): **13.75s**, **2.05×** the JVM arm, **1.07×** tsc, **5.97×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
