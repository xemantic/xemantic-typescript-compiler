# Bench run 20260807T214522Z

- **xtsc revision**: [`d04c9fa0ed29`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d04c9fa0ed29ef5385996384963f3fd05c32804a)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push d04c9fa0ed29ef5385996384963f3fd05c32804a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | d04c9fa0ed29 | 26.44s | 7,364 LOC/s | 46 |
| tsc | 6.0.3 | 11.37s | 17,119 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.74s | 111,897 LOC/s | 65 |
| xtsc-native | d04c9fa0ed29 (AOT) | 11.27s | 17,282 LOC/s | 46 |

Relative (wall): xtsc is **2.32×** tsc and **15.19×** tsgo; tsc is **6.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.33s**.

Ahead-of-time (GraalVM native-image): **11.27s**, **2.35×** the JVM arm, **0.99×** tsc, **6.47×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | d04c9fa0ed29 | 28.29s | 6,881 LOC/s | 46 |
| tsc | 6.0.3 | 13.45s | 14,474 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.45s | 79,600 LOC/s | 65 |
| xtsc-native | d04c9fa0ed29 (AOT) | 13.23s | 14,715 LOC/s | 46 |

Relative (wall): xtsc is **2.10×** tsc and **11.57×** tsgo; tsc is **5.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.14s**.

Ahead-of-time (GraalVM native-image): **13.23s**, **2.14×** the JVM arm, **0.98×** tsc, **5.41×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
