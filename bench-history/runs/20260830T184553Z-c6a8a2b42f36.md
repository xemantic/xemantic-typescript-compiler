# Bench run 20260830T184553Z

- **xtsc revision**: [`c6a8a2b42f36`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c6a8a2b42f3602323967bcb63df921a2602d1129)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push c6a8a2b42f3602323967bcb63df921a2602d1129

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c6a8a2b42f36 | **3.26s** | 59,761 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c6a8a2b42f36 | **3.31s** | 58,733 LOC/s | 46 |
| xtsc (cold JVM) | c6a8a2b42f36 | 19.39s | 10,042 LOC/s | 46 |
| tsc | 6.0.3 | 8.26s | 23,568 LOC/s | 65 |
| tsgo | 7.0.2 | 1.24s | 156,512 LOC/s | 65 |
| xtsc-native | c6a8a2b42f36 (AOT) | 5.70s | 34,152 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.54x FASTER than tsc; 2.62x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.31s**, **+1.7%** against `temurin26` on identical bytecode — 0.40× tsc, 2.66× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.35×** tsc and **15.59×** tsgo; tsc is **6.64×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.30s**.

Ahead-of-time (GraalVM native-image): **5.70s** — **0.69×** tsc, **4.58×** tsgo, **3.40×** the COLD JVM arm and **1.75×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c6a8a2b42f36 | **3.96s** | 49,154 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c6a8a2b42f36 | **3.92s** | 49,668 LOC/s | 46 |
| xtsc (cold JVM) | c6a8a2b42f36 | 20.14s | 9,669 LOC/s | 46 |
| tsc | 6.0.3 | 10.15s | 19,176 LOC/s | 65 |
| tsgo | 7.0.2 | 1.84s | 105,586 LOC/s | 65 |
| xtsc-native | c6a8a2b42f36 (AOT) | 6.85s | 28,440 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.56x FASTER than tsc; 2.15x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.92s**, **-1.0%** against `temurin26` on identical bytecode — 0.39× tsc, 2.13× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.98×** tsc and **10.92×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.03s**.

Ahead-of-time (GraalVM native-image): **6.85s** — **0.67×** tsc, **3.71×** tsgo, **2.94×** the COLD JVM arm and **1.73×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
