# Bench run 20260922T132356Z

- **xtsc revision**: [`fc1ceb55eb0b`](https://github.com/xemantic/xemantic-typescript-compiler/commit/fc1ceb55eb0b3c6d6cc1b76acf392ffcdda144a7)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push fc1ceb55eb0b3c6d6cc1b76acf392ffcdda144a7

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | fc1ceb55eb0b | **5.39s** | 36,109 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | fc1ceb55eb0b | **5.05s** | 38,547 LOC/s | 46 |
| xtsc (cold JVM) | fc1ceb55eb0b | 27.10s | 7,184 LOC/s | 46 |
| tsc | 6.0.3 | 10.24s | 19,017 LOC/s | 65 |
| tsgo | 7.0.2 | 1.62s | 120,260 LOC/s | 65 |
| xtsc-native | fc1ceb55eb0b (AOT) | 8.17s | 23,837 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.90x FASTER than tsc; 3.33x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.05s**, **-6.3%** against `temurin26` on identical bytecode — 0.49× tsc, 3.12× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.65×** tsc and **16.74×** tsgo; tsc is **6.32×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.00s**.

Ahead-of-time (GraalVM native-image): **8.17s** — **0.80×** tsc, **5.05×** tsgo, **3.32×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | fc1ceb55eb0b | **6.32s** | 30,787 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | fc1ceb55eb0b | **5.62s** | 34,619 LOC/s | 46 |
| xtsc (cold JVM) | fc1ceb55eb0b | 29.47s | 6,607 LOC/s | 46 |
| tsc | 6.0.3 | 12.19s | 15,968 LOC/s | 65 |
| tsgo | 7.0.2 | 2.36s | 82,500 LOC/s | 65 |
| xtsc-native | fc1ceb55eb0b (AOT) | 9.48s | 20,529 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.93x FASTER than tsc; 2.68x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.62s**, **-11.1%** against `temurin26` on identical bytecode — 0.46× tsc, 2.38× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.42×** tsc and **12.49×** tsgo; tsc is **5.17×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.34s**.

Ahead-of-time (GraalVM native-image): **9.48s** — **0.78×** tsc, **4.02×** tsgo, **3.11×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
