# Bench run 20260924T163731Z

- **xtsc revision**: [`085180207e9a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/085180207e9a913f55e0813d5bc9af8d7b04294a)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 085180207e9a913f55e0813d5bc9af8d7b04294a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 085180207e9a | **3.95s** | 49,254 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 085180207e9a | **3.77s** | 51,604 LOC/s | 46 |
| xtsc (cold JVM) | 085180207e9a | 18.05s | 10,784 LOC/s | 46 |
| tsc | 6.0.3 | 7.20s | 27,045 LOC/s | 65 |
| tsgo | 7.0.2 | 1.07s | 182,134 LOC/s | 65 |
| xtsc-native | 085180207e9a (AOT) | 6.22s | 31,307 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.82x FASTER than tsc; 3.70x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.77s**, **-4.6%** against `temurin26` on identical bytecode — 0.52× tsc, 3.53× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.51×** tsc and **16.89×** tsgo; tsc is **6.73×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.94s**.

Ahead-of-time (GraalVM native-image): **6.22s** — **0.86×** tsc, **5.82×** tsgo, **2.90×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 085180207e9a | **4.60s** | 42,308 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 085180207e9a | **4.55s** | 42,791 LOC/s | 46 |
| xtsc (cold JVM) | 085180207e9a | 19.63s | 9,919 LOC/s | 46 |
| tsc | 6.0.3 | 8.73s | 22,294 LOC/s | 65 |
| tsgo | 7.0.2 | 1.63s | 119,522 LOC/s | 65 |
| xtsc-native | 085180207e9a (AOT) | 7.01s | 27,774 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.90x FASTER than tsc; 2.83x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.55s**, **-1.1%** against `temurin26` on identical bytecode — 0.52× tsc, 2.79× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.25×** tsc and **12.05×** tsgo; tsc is **5.36×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.53s**.

Ahead-of-time (GraalVM native-image): **7.01s** — **0.80×** tsc, **4.30×** tsgo, **2.80×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
