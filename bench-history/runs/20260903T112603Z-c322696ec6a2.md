# Bench run 20260903T112603Z

- **xtsc revision**: [`c322696ec6a2`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c322696ec6a24d1c32b407c00794248623c1e008)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push c322696ec6a24d1c32b407c00794248623c1e008

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c322696ec6a2 | **3.07s** | 63,379 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c322696ec6a2 | **2.83s** | 68,847 LOC/s | 46 |
| xtsc (cold JVM) | c322696ec6a2 | 18.59s | 10,474 LOC/s | 46 |
| tsc | 6.0.3 | 8.31s | 23,432 LOC/s | 65 |
| tsgo | 7.0.2 | 1.24s | 157,144 LOC/s | 65 |
| xtsc-native | c322696ec6a2 (AOT) | 4.92s | 39,573 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.70x FASTER than tsc; 2.48x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.83s**, **-7.9%** against `temurin26` on identical bytecode — 0.34× tsc, 2.28× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.24×** tsc and **15.00×** tsgo; tsc is **6.71×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.49s**.

Ahead-of-time (GraalVM native-image): **4.92s** — **0.59×** tsc, **3.97×** tsgo, **3.78×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c322696ec6a2 | **3.63s** | 53,577 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c322696ec6a2 | **3.58s** | 54,355 LOC/s | 46 |
| xtsc (cold JVM) | c322696ec6a2 | 19.75s | 9,860 LOC/s | 46 |
| tsc | 6.0.3 | 9.81s | 19,855 LOC/s | 65 |
| tsgo | 7.0.2 | 1.87s | 104,174 LOC/s | 65 |
| xtsc-native | c322696ec6a2 (AOT) | 5.86s | 33,248 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.70x FASTER than tsc; 1.94x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.58s**, **-1.4%** against `temurin26` on identical bytecode — 0.37× tsc, 1.92× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.01×** tsc and **10.56×** tsgo; tsc is **5.25×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.63s**.

Ahead-of-time (GraalVM native-image): **5.86s** — **0.60×** tsc, **3.13×** tsgo, **3.37×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
