# Bench run 20260802T195929Z

- **xtsc revision**: [`172c2935d12c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/172c2935d12c6cc454c76034520ac299c3aa2878)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 172c2935d12c6cc454c76034520ac299c3aa2878

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 172c2935d12c | 18.58s | 10,480 LOC/s | 46 |
| tsc | 6.0.3 | 8.00s | 24,346 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.20s | 161,981 LOC/s | 65 |
| xtsc-native | 172c2935d12c (AOT) | 8.17s | 23,825 LOC/s | 46 |

Relative (wall): xtsc is **2.32×** tsc and **15.46×** tsgo; tsc is **6.65×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.49s**.

Ahead-of-time (GraalVM native-image): **8.17s**, **2.27×** the JVM arm, **1.02×** tsc, **6.80×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 172c2935d12c | 22.99s | 8,468 LOC/s | 46 |
| tsc | 6.0.3 | 9.73s | 20,018 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.76s | 110,815 LOC/s | 65 |
| xtsc-native | 172c2935d12c (AOT) | 10.61s | 18,354 LOC/s | 46 |

Relative (wall): xtsc is **2.36×** tsc and **13.09×** tsgo; tsc is **5.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.91s**.

Ahead-of-time (GraalVM native-image): **10.61s**, **2.17×** the JVM arm, **1.09×** tsc, **6.04×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
