# Bench run 20260902T120440Z

- **xtsc revision**: [`df16dfe1a8c2`](https://github.com/xemantic/xemantic-typescript-compiler/commit/df16dfe1a8c2f2dd1a08a019192119af7cb9c1dc)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push df16dfe1a8c2f2dd1a08a019192119af7cb9c1dc

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | df16dfe1a8c2 | **4.47s** | 43,596 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | df16dfe1a8c2 | **4.17s** | 46,646 LOC/s | 46 |
| xtsc (cold JVM) | df16dfe1a8c2 | 23.09s | 8,434 LOC/s | 46 |
| tsc | 6.0.3 | 10.96s | 17,761 LOC/s | 65 |
| tsgo | 7.0.2 | 1.75s | 111,385 LOC/s | 65 |
| xtsc-native | df16dfe1a8c2 (AOT) | 7.24s | 26,888 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.45x FASTER than tsc; 2.55x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.17s**, **-6.5%** against `temurin26` on identical bytecode — 0.38× tsc, 2.39× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.11×** tsc and **13.21×** tsgo; tsc is **6.27×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.97s**.

Ahead-of-time (GraalVM native-image): **7.24s** — **0.66×** tsc, **4.14×** tsgo, **3.19×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | df16dfe1a8c2 | **5.28s** | 36,868 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | df16dfe1a8c2 | **5.09s** | 38,236 LOC/s | 46 |
| xtsc (cold JVM) | df16dfe1a8c2 | 29.32s | 6,641 LOC/s | 46 |
| tsc | 6.0.3 | 13.50s | 14,420 LOC/s | 65 |
| tsgo | 7.0.2 | 2.47s | 78,890 LOC/s | 65 |
| xtsc-native | df16dfe1a8c2 (AOT) | 8.52s | 22,857 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.56x FASTER than tsc; 2.14x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.09s**, **-3.6%** against `temurin26` on identical bytecode — 0.38× tsc, 2.06× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.17×** tsc and **11.88×** tsgo; tsc is **5.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.19s**.

Ahead-of-time (GraalVM native-image): **8.52s** — **0.63×** tsc, **3.45×** tsgo, **3.44×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
