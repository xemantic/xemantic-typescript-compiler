# Bench run 20260818T151542Z

- **xtsc revision**: [`7144ddb4eb72`](https://github.com/xemantic/xemantic-typescript-compiler/commit/7144ddb4eb72f8650dc832892e06274250445742)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 7144ddb4eb72f8650dc832892e06274250445742

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7144ddb4eb72 | **4.01s** | 48,542 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7144ddb4eb72 | **3.86s** | 50,427 LOC/s | 46 |
| xtsc (cold JVM) | 7144ddb4eb72 | 24.08s | 8,086 LOC/s | 46 |
| tsc | 6.0.3 | 10.51s | 18,532 LOC/s | 65 |
| tsgo | 7.0.2 | 1.63s | 119,595 LOC/s | 65 |
| xtsc-native | 7144ddb4eb72 (AOT) | 6.58s | 29,607 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.62x FASTER than tsc; 2.46x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.86s**, **-3.7%** against `temurin26` on identical bytecode — 0.37× tsc, 2.37× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.29×** tsc and **14.79×** tsgo; tsc is **6.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.96s**.

Ahead-of-time (GraalVM native-image): **6.58s** — **0.63×** tsc, **4.04×** tsgo, **3.66×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7144ddb4eb72 | **5.05s** | 38,570 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7144ddb4eb72 | **4.38s** | 44,401 LOC/s | 46 |
| xtsc (cold JVM) | 7144ddb4eb72 | 27.54s | 7,068 LOC/s | 46 |
| tsc | 6.0.3 | 12.95s | 15,031 LOC/s | 65 |
| tsgo | 7.0.2 | 2.34s | 83,312 LOC/s | 65 |
| xtsc-native | 7144ddb4eb72 (AOT) | 7.92s | 24,574 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.57x FASTER than tsc; 2.16x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.38s**, **-13.1%** against `temurin26` on identical bytecode — 0.34× tsc, 1.88× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.13×** tsc and **11.79×** tsgo; tsc is **5.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.42s**.

Ahead-of-time (GraalVM native-image): **7.92s** — **0.61×** tsc, **3.39×** tsgo, **3.48×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
