# Bench run 20260913T112423Z

- **xtsc revision**: [`3c89bfcb8735`](https://github.com/xemantic/xemantic-typescript-compiler/commit/3c89bfcb87352f9ae3a57a704dce8f580bc9bc51)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 3c89bfcb87352f9ae3a57a704dce8f580bc9bc51

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3c89bfcb8735 | **5.83s** | 33,390 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3c89bfcb8735 | **5.52s** | 35,278 LOC/s | 46 |
| xtsc (cold JVM) | 3c89bfcb8735 | 26.66s | 7,303 LOC/s | 46 |
| tsc | 6.0.3 | 10.46s | 18,622 LOC/s | 65 |
| tsgo | 7.0.2 | 1.58s | 122,917 LOC/s | 65 |
| xtsc-native | 3c89bfcb8735 (AOT) | 9.73s | 20,018 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.79x FASTER than tsc; 3.68x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.52s**, **-5.4%** against `temurin26` on identical bytecode — 0.53× tsc, 3.48× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.55×** tsc and **16.83×** tsgo; tsc is **6.60×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.55s**.

Ahead-of-time (GraalVM native-image): **9.73s** — **0.93×** tsc, **6.14×** tsgo, **2.74×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3c89bfcb8735 | **6.43s** | 30,294 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3c89bfcb8735 | **5.69s** | 34,218 LOC/s | 46 |
| xtsc (cold JVM) | 3c89bfcb8735 | 27.99s | 6,956 LOC/s | 46 |
| tsc | 6.0.3 | 12.43s | 15,667 LOC/s | 65 |
| tsgo | 7.0.2 | 2.29s | 85,171 LOC/s | 65 |
| xtsc-native | 3c89bfcb8735 (AOT) | 10.53s | 18,483 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.93x FASTER than tsc; 2.81x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.69s**, **-11.5%** against `temurin26` on identical bytecode — 0.46× tsc, 2.49× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.25×** tsc and **12.24×** tsgo; tsc is **5.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.86s**.

Ahead-of-time (GraalVM native-image): **10.53s** — **0.85×** tsc, **4.61×** tsgo, **2.66×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
