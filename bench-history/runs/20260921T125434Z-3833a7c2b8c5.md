# Bench run 20260921T125434Z

- **xtsc revision**: [`3833a7c2b8c5`](https://github.com/xemantic/xemantic-typescript-compiler/commit/3833a7c2b8c5032e72c3bc710341e842ddf71b52)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 3833a7c2b8c5032e72c3bc710341e842ddf71b52

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3833a7c2b8c5 | **4.56s** | 42,688 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3833a7c2b8c5 | **4.36s** | 44,615 LOC/s | 46 |
| xtsc (cold JVM) | 3833a7c2b8c5 | 23.01s | 8,461 LOC/s | 46 |
| tsc | 6.0.3 | 9.19s | 21,195 LOC/s | 65 |
| tsgo | 7.0.2 | 1.41s | 138,380 LOC/s | 65 |
| xtsc-native | 3833a7c2b8c5 (AOT) | 7.09s | 27,442 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.01x FASTER than tsc; 3.24x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.36s**, **-4.3%** against `temurin26` on identical bytecode — 0.48× tsc, 3.10× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.50×** tsc and **16.35×** tsgo; tsc is **6.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.92s**.

Ahead-of-time (GraalVM native-image): **7.09s** — **0.77×** tsc, **5.04×** tsgo, **3.24×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3833a7c2b8c5 | **5.17s** | 37,638 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3833a7c2b8c5 | **4.92s** | 39,613 LOC/s | 46 |
| xtsc (cold JVM) | 3833a7c2b8c5 | 28.35s | 6,866 LOC/s | 46 |
| tsc | 6.0.3 | 10.79s | 18,037 LOC/s | 65 |
| tsgo | 7.0.2 | 2.04s | 95,442 LOC/s | 65 |
| xtsc-native | 3833a7c2b8c5 (AOT) | 8.57s | 22,713 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.09x FASTER than tsc; 2.54x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.92s**, **-5.0%** against `temurin26` on identical bytecode — 0.46× tsc, 2.41× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.63×** tsc and **13.90×** tsgo; tsc is **5.29×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.22s**.

Ahead-of-time (GraalVM native-image): **8.57s** — **0.79×** tsc, **4.20×** tsgo, **3.31×** the COLD JVM arm and **1.66×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
