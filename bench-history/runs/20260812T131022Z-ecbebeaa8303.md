# Bench run 20260812T131022Z

- **xtsc revision**: [`ecbebeaa8303`](https://github.com/xemantic/xemantic-typescript-compiler/commit/ecbebeaa830380589e7c79ca3281d3fe2aa443f9)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push ecbebeaa830380589e7c79ca3281d3fe2aa443f9

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ecbebeaa8303 | **2.97s** | 65,512 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ecbebeaa8303 | **2.90s** | 67,115 LOC/s | 46 |
| xtsc (cold JVM) | ecbebeaa8303 | 18.47s | 10,543 LOC/s | 46 |
| tsc | 6.0.3 | 8.46s | 23,000 LOC/s | 65 |
| tsgo | 7.0.2 | 1.35s | 143,797 LOC/s | 65 |
| xtsc-native | ecbebeaa8303 (AOT) | 5.76s | 33,820 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.85x FASTER than tsc; 2.19x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.90s**, **-2.4%** against `temurin26` on identical bytecode — 0.34× tsc, 2.14× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.18×** tsc and **13.64×** tsgo; tsc is **6.25×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.37s**.

Ahead-of-time (GraalVM native-image): **5.76s** — **0.68×** tsc, **4.25×** tsgo, **3.21×** the COLD JVM arm and **1.94×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ecbebeaa8303 | **4.07s** | 47,873 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ecbebeaa8303 | **3.65s** | 53,299 LOC/s | 46 |
| xtsc (cold JVM) | ecbebeaa8303 | 20.49s | 9,503 LOC/s | 46 |
| tsc | 6.0.3 | 10.00s | 19,472 LOC/s | 65 |
| tsgo | 7.0.2 | 1.92s | 101,672 LOC/s | 65 |
| xtsc-native | ecbebeaa8303 (AOT) | 6.78s | 28,704 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.46x FASTER than tsc; 2.12x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.65s**, **-10.2%** against `temurin26` on identical bytecode — 0.37× tsc, 1.91× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.05×** tsc and **10.70×** tsgo; tsc is **5.22×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.39s**.

Ahead-of-time (GraalVM native-image): **6.78s** — **0.68×** tsc, **3.54×** tsgo, **3.02×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
