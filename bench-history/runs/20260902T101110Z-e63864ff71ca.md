# Bench run 20260902T101110Z

- **xtsc revision**: [`e63864ff71ca`](https://github.com/xemantic/xemantic-typescript-compiler/commit/e63864ff71cae7c3dec87d66d2a5996dc5b3cda2)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push e63864ff71cae7c3dec87d66d2a5996dc5b3cda2

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e63864ff71ca | **4.62s** | 42,179 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e63864ff71ca | **4.30s** | 45,311 LOC/s | 46 |
| xtsc (cold JVM) | e63864ff71ca | 22.84s | 8,524 LOC/s | 46 |
| tsc | 6.0.3 | 11.16s | 17,440 LOC/s | 65 |
| tsgo | 7.0.2 | 1.70s | 114,328 LOC/s | 65 |
| xtsc-native | e63864ff71ca (AOT) | 7.30s | 26,675 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.42x FASTER than tsc; 2.71x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.30s**, **-6.9%** against `temurin26` on identical bytecode — 0.38× tsc, 2.52× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.05×** tsc and **13.41×** tsgo; tsc is **6.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.73s**.

Ahead-of-time (GraalVM native-image): **7.30s** — **0.65×** tsc, **4.29×** tsgo, **3.13×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e63864ff71ca | **5.30s** | 36,708 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e63864ff71ca | **4.87s** | 39,947 LOC/s | 46 |
| xtsc (cold JVM) | e63864ff71ca | 27.63s | 7,046 LOC/s | 46 |
| tsc | 6.0.3 | 13.47s | 14,452 LOC/s | 65 |
| tsgo | 7.0.2 | 2.48s | 78,635 LOC/s | 65 |
| xtsc-native | e63864ff71ca (AOT) | 8.66s | 22,475 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.54x FASTER than tsc; 2.14x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.87s**, **-8.1%** against `temurin26` on identical bytecode — 0.36× tsc, 1.97× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.05×** tsc and **11.16×** tsgo; tsc is **5.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.51s**.

Ahead-of-time (GraalVM native-image): **8.66s** — **0.64×** tsc, **3.50×** tsgo, **3.19×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
