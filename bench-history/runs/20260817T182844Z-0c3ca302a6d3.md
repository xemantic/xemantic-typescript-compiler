# Bench run 20260817T182844Z

- **xtsc revision**: [`0c3ca302a6d3`](https://github.com/xemantic/xemantic-typescript-compiler/commit/0c3ca302a6d3c12f61357351ef81171bb29038d9)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 0c3ca302a6d3c12f61357351ef81171bb29038d9

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0c3ca302a6d3 | **3.96s** | 49,154 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0c3ca302a6d3 | **3.76s** | 51,727 LOC/s | 46 |
| xtsc (cold JVM) | 0c3ca302a6d3 | 23.78s | 8,188 LOC/s | 46 |
| tsc | 6.0.3 | 10.27s | 18,962 LOC/s | 65 |
| tsgo | 7.0.2 | 1.55s | 125,371 LOC/s | 65 |
| xtsc-native | 0c3ca302a6d3 (AOT) | 6.05s | 32,176 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.59x FASTER than tsc; 2.55x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.76s**, **-5.0%** against `temurin26` on identical bytecode — 0.37× tsc, 2.42× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.32×** tsc and **15.31×** tsgo; tsc is **6.61×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.66s**.

Ahead-of-time (GraalVM native-image): **6.05s** — **0.59×** tsc, **3.90×** tsgo, **3.93×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0c3ca302a6d3 | **4.72s** | 41,241 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0c3ca302a6d3 | **4.42s** | 44,010 LOC/s | 46 |
| xtsc (cold JVM) | 0c3ca302a6d3 | 24.21s | 8,042 LOC/s | 46 |
| tsc | 6.0.3 | 12.40s | 15,697 LOC/s | 65 |
| tsgo | 7.0.2 | 2.24s | 86,765 LOC/s | 65 |
| xtsc-native | 0c3ca302a6d3 (AOT) | 7.14s | 27,250 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.63x FASTER than tsc; 2.10x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.42s**, **-6.3%** against `temurin26` on identical bytecode — 0.36× tsc, 1.97× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.95×** tsc and **10.79×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.09s**.

Ahead-of-time (GraalVM native-image): **7.14s** — **0.58×** tsc, **3.18×** tsgo, **3.39×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
