# Bench run 20260811T093713Z

- **xtsc revision**: [`241d44552361`](https://github.com/xemantic/xemantic-typescript-compiler/commit/241d44552361c5ebd462c657d76961fe28f94704)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 241d44552361c5ebd462c657d76961fe28f94704

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 241d44552361 | **4.62s** | 42,106 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 241d44552361 | **4.14s** | 47,063 LOC/s | 46 |
| xtsc (cold JVM) | 241d44552361 | 22.48s | 8,660 LOC/s | 46 |
| tsc | 6.0.3 | 10.87s | 17,911 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.70s | 114,597 LOC/s | 65 |
| xtsc-native | 241d44552361 (AOT) | 6.52s | 29,857 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.35x FASTER than tsc; 2.72x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.14s**, **-10.5%** against `temurin26` on identical bytecode — 0.38× tsc, 2.43× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.07×** tsc and **13.23×** tsgo; tsc is **6.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.35s**.

Ahead-of-time (GraalVM native-image): **6.52s** — **0.60×** tsc, **3.84×** tsgo, **3.45×** the COLD JVM arm and **1.41×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 241d44552361 | **5.51s** | 35,348 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 241d44552361 | **4.89s** | 39,775 LOC/s | 46 |
| xtsc (cold JVM) | 241d44552361 | 27.33s | 7,123 LOC/s | 46 |
| tsc | 6.0.3 | 13.10s | 14,866 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.38s | 81,841 LOC/s | 65 |
| xtsc-native | 241d44552361 (AOT) | 8.36s | 23,289 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.38x FASTER than tsc; 2.32x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.89s**, **-11.1%** against `temurin26` on identical bytecode — 0.37× tsc, 2.06× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.09×** tsc and **11.49×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.20s**.

Ahead-of-time (GraalVM native-image): **8.36s** — **0.64×** tsc, **3.51×** tsgo, **3.27×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
