# Bench run 20260819T115447Z

- **xtsc revision**: [`7f0b5126fc36`](https://github.com/xemantic/xemantic-typescript-compiler/commit/7f0b5126fc36d628df3003da896f8b100e22cff7)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 7f0b5126fc36d628df3003da896f8b100e22cff7

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7f0b5126fc36 | **3.77s** | 51,699 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7f0b5126fc36 | **3.52s** | 55,234 LOC/s | 46 |
| xtsc (cold JVM) | 7f0b5126fc36 | 22.67s | 8,589 LOC/s | 46 |
| tsc | 6.0.3 | 10.15s | 19,182 LOC/s | 65 |
| tsgo | 7.0.2 | 1.53s | 126,841 LOC/s | 65 |
| xtsc-native | 7f0b5126fc36 (AOT) | 5.88s | 33,140 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.70x FASTER than tsc; 2.45x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.52s**, **-6.4%** against `temurin26` on identical bytecode — 0.35× tsc, 2.30× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.23×** tsc and **14.77×** tsgo; tsc is **6.61×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.53s**.

Ahead-of-time (GraalVM native-image): **5.88s** — **0.58×** tsc, **3.83×** tsgo, **3.86×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7f0b5126fc36 | **4.96s** | 39,230 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7f0b5126fc36 | **4.45s** | 43,753 LOC/s | 46 |
| xtsc (cold JVM) | 7f0b5126fc36 | 24.37s | 7,989 LOC/s | 46 |
| tsc | 6.0.3 | 12.59s | 15,459 LOC/s | 65 |
| tsgo | 7.0.2 | 2.30s | 84,800 LOC/s | 65 |
| xtsc-native | 7f0b5126fc36 (AOT) | 7.42s | 26,229 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.54x FASTER than tsc; 2.16x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.45s**, **-10.3%** against `temurin26` on identical bytecode — 0.35× tsc, 1.94× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.94×** tsc and **10.61×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.23s**.

Ahead-of-time (GraalVM native-image): **7.42s** — **0.59×** tsc, **3.23×** tsgo, **3.28×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
