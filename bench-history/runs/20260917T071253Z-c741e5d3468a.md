# Bench run 20260917T071253Z

- **xtsc revision**: [`c741e5d3468a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c741e5d3468a6e4ce3cb3735394a080cf8113db7)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push c741e5d3468a6e4ce3cb3735394a080cf8113db7

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c741e5d3468a | **5.46s** | 35,633 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c741e5d3468a | **5.01s** | 38,839 LOC/s | 46 |
| xtsc (cold JVM) | c741e5d3468a | 24.89s | 7,822 LOC/s | 46 |
| tsc | 6.0.3 | 10.18s | 19,124 LOC/s | 65 |
| tsgo | 7.0.2 | 1.51s | 128,686 LOC/s | 65 |
| xtsc-native | c741e5d3468a (AOT) | 8.87s | 21,963 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.86x FASTER than tsc; 3.61x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.01s**, **-8.3%** against `temurin26` on identical bytecode — 0.49× tsc, 3.31× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.44×** tsc and **16.45×** tsgo; tsc is **6.73×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.78s**.

Ahead-of-time (GraalVM native-image): **8.87s** — **0.87×** tsc, **5.86×** tsgo, **2.81×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c741e5d3468a | **6.43s** | 30,266 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c741e5d3468a | **5.75s** | 33,872 LOC/s | 46 |
| xtsc (cold JVM) | c741e5d3468a | 27.83s | 6,996 LOC/s | 46 |
| tsc | 6.0.3 | 12.29s | 15,847 LOC/s | 65 |
| tsgo | 7.0.2 | 2.22s | 87,743 LOC/s | 65 |
| xtsc-native | c741e5d3468a (AOT) | 10.31s | 18,892 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.91x FASTER than tsc; 2.90x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.75s**, **-10.6%** against `temurin26` on identical bytecode — 0.47× tsc, 2.59× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.27×** tsc and **12.54×** tsgo; tsc is **5.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.70s**.

Ahead-of-time (GraalVM native-image): **10.31s** — **0.84×** tsc, **4.64×** tsgo, **2.70×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
