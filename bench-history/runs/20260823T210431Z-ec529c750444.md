# Bench run 20260823T210431Z

- **xtsc revision**: [`ec529c750444`](https://github.com/xemantic/xemantic-typescript-compiler/commit/ec529c750444dee4bd63a974b2dabe70b3c1f9fa)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push ec529c750444dee4bd63a974b2dabe70b3c1f9fa

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ec529c750444 | **4.22s** | 46,137 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ec529c750444 | **4.24s** | 45,909 LOC/s | 46 |
| xtsc (cold JVM) | ec529c750444 | 22.70s | 8,577 LOC/s | 46 |
| tsc | 6.0.3 | 11.14s | 17,479 LOC/s | 65 |
| tsgo | 7.0.2 | 1.62s | 120,334 LOC/s | 65 |
| xtsc-native | ec529c750444 (AOT) | 7.54s | 25,822 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.64x FASTER than tsc; 2.61x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.24s**, **+0.5%** against `temurin26` on identical bytecode — 0.38× tsc, 2.62× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.04×** tsc and **14.03×** tsgo; tsc is **6.88×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.54s**.

Ahead-of-time (GraalVM native-image): **7.54s** — **0.68×** tsc, **4.66×** tsgo, **3.01×** the COLD JVM arm and **1.79×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ec529c750444 | **5.34s** | 36,474 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ec529c750444 | **4.93s** | 39,453 LOC/s | 46 |
| xtsc (cold JVM) | ec529c750444 | 28.33s | 6,872 LOC/s | 46 |
| tsc | 6.0.3 | 13.49s | 14,427 LOC/s | 65 |
| tsgo | 7.0.2 | 2.50s | 77,756 LOC/s | 65 |
| xtsc-native | ec529c750444 (AOT) | 9.06s | 21,499 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.53x FASTER than tsc; 2.13x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.93s**, **-7.5%** against `temurin26` on identical bytecode — 0.37× tsc, 1.97× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.10×** tsc and **11.31×** tsgo; tsc is **5.39×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.20s**.

Ahead-of-time (GraalVM native-image): **9.06s** — **0.67×** tsc, **3.62×** tsgo, **3.13×** the COLD JVM arm and **1.70×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
