# Bench run 20260731T202952Z

- **xtsc revision**: [`277c0c4df905`](https://github.com/xemantic/xemantic-typescript-compiler/commit/277c0c4df905b171a02a5536eb363c2bde753a76)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 277c0c4df905b171a02a5536eb363c2bde753a76

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 277c0c4df905 | 20.20s | 9,636 LOC/s | 46 |
| tsc | 6.0.3 | 7.76s | 25,103 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.19s | 163,752 LOC/s | 65 |
| xtsc-native | 277c0c4df905 (AOT) | 9.88s | 19,700 LOC/s | 46 |

Relative (wall): xtsc is **2.61×** tsc and **16.99×** tsgo; tsc is **6.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.12s**.

Ahead-of-time (GraalVM native-image): **9.88s**, **2.04×** the JVM arm, **1.27×** tsc, **8.31×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 277c0c4df905 | 24.44s | 7,967 LOC/s | 46 |
| tsc | 6.0.3 | 9.56s | 20,355 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.89s | 103,290 LOC/s | 65 |
| xtsc-native | 277c0c4df905 (AOT) | 11.64s | 16,728 LOC/s | 46 |

Relative (wall): xtsc is **2.55×** tsc and **12.96×** tsgo; tsc is **5.07×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.33s**.

Ahead-of-time (GraalVM native-image): **11.64s**, **2.10×** the JVM arm, **1.22×** tsc, **6.17×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
