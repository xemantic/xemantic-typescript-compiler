# Bench run 20260902T164040Z

- **xtsc revision**: [`82f9b4ec8845`](https://github.com/xemantic/xemantic-typescript-compiler/commit/82f9b4ec8845af70c17a254a8f215962eaea187b)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 82f9b4ec8845af70c17a254a8f215962eaea187b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 82f9b4ec8845 | **4.40s** | 44,290 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 82f9b4ec8845 | **4.06s** | 47,920 LOC/s | 46 |
| xtsc (cold JVM) | 82f9b4ec8845 | 25.76s | 7,559 LOC/s | 46 |
| tsc | 6.0.3 | 10.85s | 17,941 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,797 LOC/s | 65 |
| xtsc-native | 82f9b4ec8845 (AOT) | 7.13s | 27,288 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.47x FASTER than tsc; 2.64x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.06s**, **-7.6%** against `temurin26` on identical bytecode — 0.37× tsc, 2.44× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.37×** tsc and **15.45×** tsgo; tsc is **6.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.64s**.

Ahead-of-time (GraalVM native-image): **7.13s** — **0.66×** tsc, **4.28×** tsgo, **3.61×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 82f9b4ec8845 | **5.09s** | 38,281 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 82f9b4ec8845 | **4.91s** | 39,670 LOC/s | 46 |
| xtsc (cold JVM) | 82f9b4ec8845 | 28.28s | 6,885 LOC/s | 46 |
| tsc | 6.0.3 | 13.42s | 14,507 LOC/s | 65 |
| tsgo | 7.0.2 | 2.43s | 80,025 LOC/s | 65 |
| xtsc-native | 82f9b4ec8845 (AOT) | 8.48s | 22,952 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.64x FASTER than tsc; 2.09x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.91s**, **-3.5%** against `temurin26` on identical bytecode — 0.37× tsc, 2.02× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.11×** tsc and **11.62×** tsgo; tsc is **5.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.16s**.

Ahead-of-time (GraalVM native-image): **8.48s** — **0.63×** tsc, **3.49×** tsgo, **3.33×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
