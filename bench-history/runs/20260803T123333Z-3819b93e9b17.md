# Bench run 20260803T123333Z

- **xtsc revision**: [`3819b93e9b17`](https://github.com/xemantic/xemantic-typescript-compiler/commit/3819b93e9b1714c76fb0437ee6d441f054ddb514)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 3819b93e9b1714c76fb0437ee6d441f054ddb514

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 3819b93e9b17 | 26.93s | 7,231 LOC/s | 46 |
| tsc | 6.0.3 | 10.28s | 18,932 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.57s | 123,935 LOC/s | 65 |
| xtsc-native | 3819b93e9b17 (AOT) | 10.42s | 18,692 LOC/s | 46 |

Relative (wall): xtsc is **2.62×** tsc and **17.14×** tsgo; tsc is **6.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.82s**.

Ahead-of-time (GraalVM native-image): **10.42s**, **2.58×** the JVM arm, **1.01×** tsc, **6.63×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 3819b93e9b17 | 28.91s | 6,735 LOC/s | 46 |
| tsc | 6.0.3 | 12.90s | 15,089 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.25s | 86,418 LOC/s | 65 |
| xtsc-native | 3819b93e9b17 (AOT) | 12.32s | 15,805 LOC/s | 46 |

Relative (wall): xtsc is **2.24×** tsc and **12.83×** tsgo; tsc is **5.73×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.80s**.

Ahead-of-time (GraalVM native-image): **12.32s**, **2.35×** the JVM arm, **0.95×** tsc, **5.47×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
