# Bench run 20260824T174124Z

- **xtsc revision**: [`46ee8bf4e61b`](https://github.com/xemantic/xemantic-typescript-compiler/commit/46ee8bf4e61bc8013fb7f9ad7093f2de109056e7)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 46ee8bf4e61bc8013fb7f9ad7093f2de109056e7

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 46ee8bf4e61b | **2.64s** | 73,694 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 46ee8bf4e61b | **2.60s** | 74,827 LOC/s | 46 |
| xtsc (cold JVM) | 46ee8bf4e61b | 18.32s | 10,628 LOC/s | 46 |
| tsc | 6.0.3 | 7.94s | 24,518 LOC/s | 65 |
| tsgo | 7.0.2 | 1.24s | 157,525 LOC/s | 65 |
| xtsc-native | 46ee8bf4e61b (AOT) | 4.25s | 45,779 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 3.01x FASTER than tsc; 2.14x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.60s**, **-1.5%** against `temurin26` on identical bytecode — 0.33× tsc, 2.11× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.31×** tsc and **14.82×** tsgo; tsc is **6.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.23s**.

Ahead-of-time (GraalVM native-image): **4.25s** — **0.54×** tsc, **3.44×** tsgo, **4.31×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 46ee8bf4e61b | **3.66s** | 53,211 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 46ee8bf4e61b | **3.21s** | 60,617 LOC/s | 46 |
| xtsc (cold JVM) | 46ee8bf4e61b | 19.14s | 10,171 LOC/s | 46 |
| tsc | 6.0.3 | 9.83s | 19,802 LOC/s | 65 |
| tsgo | 7.0.2 | 1.88s | 103,509 LOC/s | 65 |
| xtsc-native | 46ee8bf4e61b (AOT) | 5.49s | 35,471 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.69x FASTER than tsc; 1.95x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.21s**, **-12.2%** against `temurin26` on identical bytecode — 0.33× tsc, 1.71× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.95×** tsc and **10.18×** tsgo; tsc is **5.23×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.06s**.

Ahead-of-time (GraalVM native-image): **5.49s** — **0.56×** tsc, **2.92×** tsgo, **3.49×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
