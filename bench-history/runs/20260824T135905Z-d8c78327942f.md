# Bench run 20260824T135905Z

- **xtsc revision**: [`d8c78327942f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d8c78327942fb4ca9d6a8658d610b61f5c900bde)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push d8c78327942fb4ca9d6a8658d610b61f5c900bde

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d8c78327942f | **3.83s** | 50,849 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d8c78327942f | **3.63s** | 53,563 LOC/s | 46 |
| xtsc (cold JVM) | d8c78327942f | 22.68s | 8,583 LOC/s | 46 |
| tsc | 6.0.3 | 10.63s | 18,321 LOC/s | 65 |
| tsgo | 7.0.2 | 1.66s | 117,502 LOC/s | 65 |
| xtsc-native | d8c78327942f (AOT) | 5.84s | 33,310 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.78x FASTER than tsc; 2.31x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.63s**, **-5.1%** against `temurin26` on identical bytecode — 0.34× tsc, 2.19× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.13×** tsc and **13.69×** tsgo; tsc is **6.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.57s**.

Ahead-of-time (GraalVM native-image): **5.84s** — **0.55×** tsc, **3.53×** tsgo, **3.88×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d8c78327942f | **4.78s** | 40,715 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d8c78327942f | **4.37s** | 44,523 LOC/s | 46 |
| xtsc (cold JVM) | d8c78327942f | 25.91s | 7,514 LOC/s | 46 |
| tsc | 6.0.3 | 13.73s | 14,184 LOC/s | 65 |
| tsgo | 7.0.2 | 2.47s | 78,922 LOC/s | 65 |
| xtsc-native | d8c78327942f (AOT) | 7.61s | 25,588 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.87x FASTER than tsc; 1.94x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.37s**, **-8.6%** against `temurin26` on identical bytecode — 0.32× tsc, 1.77× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.89×** tsc and **10.50×** tsgo; tsc is **5.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.77s**.

Ahead-of-time (GraalVM native-image): **7.61s** — **0.55×** tsc, **3.08×** tsgo, **3.41×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
