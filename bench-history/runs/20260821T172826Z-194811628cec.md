# Bench run 20260821T172826Z

- **xtsc revision**: [`194811628cec`](https://github.com/xemantic/xemantic-typescript-compiler/commit/194811628cecf71ec96d31944bf319bf2af40993)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 194811628cecf71ec96d31944bf319bf2af40993

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 194811628cec | **3.68s** | 52,865 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 194811628cec | **3.54s** | 55,062 LOC/s | 46 |
| xtsc (cold JVM) | 194811628cec | 22.03s | 8,839 LOC/s | 46 |
| tsc | 6.0.3 | 10.04s | 19,400 LOC/s | 65 |
| tsgo | 7.0.2 | 1.55s | 125,939 LOC/s | 65 |
| xtsc-native | 194811628cec (AOT) | 5.92s | 32,911 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.72x FASTER than tsc; 2.38x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.54s**, **-4.0%** against `temurin26` on identical bytecode — 0.35× tsc, 2.29× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.19×** tsc and **14.25×** tsgo; tsc is **6.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.90s**.

Ahead-of-time (GraalVM native-image): **5.92s** — **0.59×** tsc, **3.83×** tsgo, **3.72×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 194811628cec | **4.50s** | 43,219 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 194811628cec | **4.29s** | 45,332 LOC/s | 46 |
| xtsc (cold JVM) | 194811628cec | 24.54s | 7,934 LOC/s | 46 |
| tsc | 6.0.3 | 12.24s | 15,901 LOC/s | 65 |
| tsgo | 7.0.2 | 2.25s | 86,611 LOC/s | 65 |
| xtsc-native | 194811628cec (AOT) | 7.07s | 27,539 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.72x FASTER than tsc; 2.00x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.29s**, **-4.7%** against `temurin26` on identical bytecode — 0.35× tsc, 1.91× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.00×** tsc and **10.92×** tsgo; tsc is **5.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.39s**.

Ahead-of-time (GraalVM native-image): **7.07s** — **0.58×** tsc, **3.15×** tsgo, **3.47×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
