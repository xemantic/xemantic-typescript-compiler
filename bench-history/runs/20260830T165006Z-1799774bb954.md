# Bench run 20260830T165006Z

- **xtsc revision**: [`1799774bb954`](https://github.com/xemantic/xemantic-typescript-compiler/commit/1799774bb954e572fb351f6eb04ebdaf4fd9da11)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 1799774bb954e572fb351f6eb04ebdaf4fd9da11

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1799774bb954 | **3.64s** | 53,548 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1799774bb954 | **3.52s** | 55,328 LOC/s | 46 |
| xtsc (cold JVM) | 1799774bb954 | 22.41s | 8,688 LOC/s | 46 |
| tsc | 6.0.3 | 9.81s | 19,845 LOC/s | 65 |
| tsgo | 7.0.2 | 1.51s | 129,027 LOC/s | 65 |
| xtsc-native | 1799774bb954 (AOT) | 5.90s | 32,983 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.70x FASTER than tsc; 2.41x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.52s**, **-3.2%** against `temurin26` on identical bytecode — 0.36× tsc, 2.33× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.28×** tsc and **14.85×** tsgo; tsc is **6.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.30s**.

Ahead-of-time (GraalVM native-image): **5.90s** — **0.60×** tsc, **3.91×** tsgo, **3.80×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1799774bb954 | **4.47s** | 43,557 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1799774bb954 | **4.01s** | 48,554 LOC/s | 46 |
| xtsc (cold JVM) | 1799774bb954 | 25.35s | 7,679 LOC/s | 46 |
| tsc | 6.0.3 | 11.71s | 16,631 LOC/s | 65 |
| tsgo | 7.0.2 | 2.25s | 86,457 LOC/s | 65 |
| xtsc-native | 1799774bb954 (AOT) | 7.01s | 27,759 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.62x FASTER than tsc; 1.98x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.01s**, **-10.3%** against `temurin26` on identical bytecode — 0.34× tsc, 1.78× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.17×** tsc and **11.26×** tsgo; tsc is **5.20×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.23s**.

Ahead-of-time (GraalVM native-image): **7.01s** — **0.60×** tsc, **3.11×** tsgo, **3.61×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
