# Bench run 20260902T144040Z

- **xtsc revision**: [`b6cbf59500e3`](https://github.com/xemantic/xemantic-typescript-compiler/commit/b6cbf59500e3c750dce2eeea27f33279164bf963)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push b6cbf59500e3c750dce2eeea27f33279164bf963

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b6cbf59500e3 | **4.06s** | 47,967 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b6cbf59500e3 | **3.78s** | 51,481 LOC/s | 46 |
| xtsc (cold JVM) | b6cbf59500e3 | 21.09s | 9,234 LOC/s | 46 |
| tsc | 6.0.3 | 10.20s | 19,094 LOC/s | 65 |
| tsgo | 7.0.2 | 1.54s | 126,265 LOC/s | 65 |
| xtsc-native | b6cbf59500e3 (AOT) | 6.24s | 31,207 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.51x FASTER than tsc; 2.63x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.78s**, **-6.8%** against `temurin26` on identical bytecode — 0.37× tsc, 2.45× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.07×** tsc and **13.67×** tsgo; tsc is **6.61×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.97s**.

Ahead-of-time (GraalVM native-image): **6.24s** — **0.61×** tsc, **4.05×** tsgo, **3.38×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b6cbf59500e3 | **4.92s** | 39,613 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b6cbf59500e3 | **4.53s** | 42,980 LOC/s | 46 |
| xtsc (cold JVM) | b6cbf59500e3 | 26.87s | 7,246 LOC/s | 46 |
| tsc | 6.0.3 | 12.23s | 15,923 LOC/s | 65 |
| tsgo | 7.0.2 | 2.20s | 88,662 LOC/s | 65 |
| xtsc-native | b6cbf59500e3 (AOT) | 7.89s | 24,661 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.49x FASTER than tsc; 2.24x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.53s**, **-7.8%** against `temurin26` on identical bytecode — 0.37× tsc, 2.06× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.20×** tsc and **12.23×** tsgo; tsc is **5.57×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.73s**.

Ahead-of-time (GraalVM native-image): **7.89s** — **0.65×** tsc, **3.60×** tsgo, **3.40×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
