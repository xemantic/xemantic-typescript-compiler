# Bench run 20260902T045654Z

- **xtsc revision**: [`d932f279f0cf`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d932f279f0cf7ff441c2a26ca29723bdf7fe5ce6)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push d932f279f0cf7ff441c2a26ca29723bdf7fe5ce6

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d932f279f0cf | **2.66s** | 73,113 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d932f279f0cf | **2.61s** | 74,598 LOC/s | 46 |
| xtsc (cold JVM) | d932f279f0cf | 16.28s | 11,957 LOC/s | 46 |
| tsc | 6.0.3 | 6.96s | 27,974 LOC/s | 65 |
| tsgo | 7.0.2 | 1.08s | 180,781 LOC/s | 65 |
| xtsc-native | d932f279f0cf (AOT) | 4.27s | 45,608 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.61x FASTER than tsc; 2.47x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.61s**, **-2.0%** against `temurin26` on identical bytecode — 0.38× tsc, 2.42× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.34×** tsc and **15.12×** tsgo; tsc is **6.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **16.20s**.

Ahead-of-time (GraalVM native-image): **4.27s** — **0.61×** tsc, **3.96×** tsgo, **3.81×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d932f279f0cf | **3.49s** | 55,788 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d932f279f0cf | **3.19s** | 61,054 LOC/s | 46 |
| xtsc (cold JVM) | d932f279f0cf | 17.18s | 11,330 LOC/s | 46 |
| tsc | 6.0.3 | 8.47s | 22,973 LOC/s | 65 |
| tsgo | 7.0.2 | 1.60s | 121,385 LOC/s | 65 |
| xtsc-native | d932f279f0cf (AOT) | 5.32s | 36,605 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.43x FASTER than tsc; 2.18x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.19s**, **-8.6%** against `temurin26` on identical bytecode — 0.38× tsc, 1.99× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.03×** tsc and **10.71×** tsgo; tsc is **5.28×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.10s**.

Ahead-of-time (GraalVM native-image): **5.32s** — **0.63×** tsc, **3.32×** tsgo, **3.23×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
