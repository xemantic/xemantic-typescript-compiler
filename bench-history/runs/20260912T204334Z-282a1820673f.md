# Bench run 20260912T204334Z

- **xtsc revision**: [`282a1820673f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/282a1820673fef64f7302b945e45f2036b32f75a)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 282a1820673fef64f7302b945e45f2036b32f75a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 282a1820673f | **3.98s** | 48,907 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 282a1820673f | **3.60s** | 54,098 LOC/s | 46 |
| xtsc (cold JVM) | 282a1820673f | 19.90s | 9,785 LOC/s | 46 |
| tsc | 6.0.3 | 8.32s | 23,393 LOC/s | 65 |
| tsgo | 7.0.2 | 1.30s | 149,425 LOC/s | 65 |
| xtsc-native | 282a1820673f (AOT) | 6.13s | 31,782 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.09x FASTER than tsc; 3.06x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.60s**, **-9.6%** against `temurin26` on identical bytecode — 0.43× tsc, 2.76× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.39×** tsc and **15.27×** tsgo; tsc is **6.39×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.80s**.

Ahead-of-time (GraalVM native-image): **6.13s** — **0.74×** tsc, **4.70×** tsgo, **3.25×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 282a1820673f | **4.72s** | 41,267 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 282a1820673f | **4.36s** | 44,615 LOC/s | 46 |
| xtsc (cold JVM) | 282a1820673f | 22.66s | 8,590 LOC/s | 46 |
| tsc | 6.0.3 | 10.14s | 19,193 LOC/s | 65 |
| tsgo | 7.0.2 | 1.95s | 99,642 LOC/s | 65 |
| xtsc-native | 282a1820673f (AOT) | 7.56s | 25,757 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.15x FASTER than tsc; 2.41x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.36s**, **-7.5%** against `temurin26` on identical bytecode — 0.43× tsc, 2.23× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.23×** tsc and **11.60×** tsgo; tsc is **5.19×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.57s**.

Ahead-of-time (GraalVM native-image): **7.56s** — **0.75×** tsc, **3.87×** tsgo, **3.00×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
