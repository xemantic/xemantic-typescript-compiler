# Bench run 20260923T010618Z

- **xtsc revision**: [`c5d179313cf9`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c5d179313cf90365da3c509e11f06a5a4db89e3f)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push c5d179313cf90365da3c509e11f06a5a4db89e3f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c5d179313cf9 | **3.23s** | 60,260 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c5d179313cf9 | **3.08s** | 63,297 LOC/s | 46 |
| xtsc (cold JVM) | c5d179313cf9 | 15.59s | 12,491 LOC/s | 46 |
| tsc | 6.0.3 | 6.05s | 32,160 LOC/s | 65 |
| tsgo | 7.0.2 | 0.93s | 210,034 LOC/s | 65 |
| xtsc-native | c5d179313cf9 (AOT) | 5.52s | 35,284 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.87x FASTER than tsc; 3.49x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.08s**, **-4.8%** against `temurin26` on identical bytecode — 0.51× tsc, 3.32× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.57×** tsc and **16.81×** tsgo; tsc is **6.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **15.51s**.

Ahead-of-time (GraalVM native-image): **5.52s** — **0.91×** tsc, **5.95×** tsgo, **2.82×** the COLD JVM arm and **1.71×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c5d179313cf9 | **3.73s** | 52,198 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c5d179313cf9 | **3.56s** | 54,768 LOC/s | 46 |
| xtsc (cold JVM) | c5d179313cf9 | 22.79s | 8,544 LOC/s | 46 |
| tsc | 6.0.3 | 7.50s | 25,967 LOC/s | 65 |
| tsgo | 7.0.2 | 1.38s | 141,088 LOC/s | 65 |
| xtsc-native | c5d179313cf9 (AOT) | 6.41s | 30,360 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.01x FASTER than tsc; 2.70x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.56s**, **-4.7%** against `temurin26` on identical bytecode — 0.47× tsc, 2.58× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.04×** tsc and **16.51×** tsgo; tsc is **5.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.68s**.

Ahead-of-time (GraalVM native-image): **6.41s** — **0.86×** tsc, **4.65×** tsgo, **3.55×** the COLD JVM arm and **1.72×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
