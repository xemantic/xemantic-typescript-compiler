# Bench run 20260818T090234Z

- **xtsc revision**: [`9acffb567e6f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/9acffb567e6f867ccae17df42b502fef1007c4e6)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 9acffb567e6f867ccae17df42b502fef1007c4e6

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9acffb567e6f | **4.04s** | 48,217 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9acffb567e6f | **3.82s** | 50,955 LOC/s | 46 |
| xtsc (cold JVM) | 9acffb567e6f | 24.17s | 8,054 LOC/s | 46 |
| tsc | 6.0.3 | 10.39s | 18,732 LOC/s | 65 |
| tsgo | 7.0.2 | 1.57s | 123,935 LOC/s | 65 |
| xtsc-native | 9acffb567e6f (AOT) | 6.13s | 31,767 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.57x FASTER than tsc; 2.57x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.82s**, **-5.4%** against `temurin26` on identical bytecode — 0.37× tsc, 2.43× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.33×** tsc and **15.39×** tsgo; tsc is **6.62×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.06s**.

Ahead-of-time (GraalVM native-image): **6.13s** — **0.59×** tsc, **3.90×** tsgo, **3.94×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9acffb567e6f | **4.83s** | 40,319 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9acffb567e6f | **4.54s** | 42,895 LOC/s | 46 |
| xtsc (cold JVM) | 9acffb567e6f | 26.11s | 7,457 LOC/s | 46 |
| tsc | 6.0.3 | 12.83s | 15,173 LOC/s | 65 |
| tsgo | 7.0.2 | 2.39s | 81,431 LOC/s | 65 |
| xtsc-native | 9acffb567e6f (AOT) | 7.96s | 24,472 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.66x FASTER than tsc; 2.02x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.54s**, **-6.0%** against `temurin26` on identical bytecode — 0.35× tsc, 1.90× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.03×** tsc and **10.92×** tsgo; tsc is **5.37×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.98s**.

Ahead-of-time (GraalVM native-image): **7.96s** — **0.62×** tsc, **3.33×** tsgo, **3.28×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
