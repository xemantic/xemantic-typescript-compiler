# Bench run 20260812T115852Z

- **xtsc revision**: [`7a859f0051f8`](https://github.com/xemantic/xemantic-typescript-compiler/commit/7a859f0051f8fe6ac5333d0804bb612db700dc05)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 7a859f0051f8fe6ac5333d0804bb612db700dc05

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7a859f0051f8 | **3.96s** | 49,117 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7a859f0051f8 | **3.77s** | 51,672 LOC/s | 46 |
| xtsc (cold JVM) | 7a859f0051f8 | 23.98s | 8,119 LOC/s | 46 |
| tsc | 6.0.3 | 10.48s | 18,574 LOC/s | 65 |
| tsgo | 7.0.2 | 1.59s | 122,454 LOC/s | 65 |
| xtsc-native | 7a859f0051f8 (AOT) | 6.15s | 31,664 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.64x FASTER than tsc; 2.49x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.77s**, **-4.9%** against `temurin26` on identical bytecode — 0.36× tsc, 2.37× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.29×** tsc and **15.08×** tsgo; tsc is **6.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.86s**.

Ahead-of-time (GraalVM native-image): **6.15s** — **0.59×** tsc, **3.87×** tsgo, **3.90×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7a859f0051f8 | **4.83s** | 40,294 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7a859f0051f8 | **4.44s** | 43,851 LOC/s | 46 |
| xtsc (cold JVM) | 7a859f0051f8 | 25.10s | 7,757 LOC/s | 46 |
| tsc | 6.0.3 | 12.63s | 15,417 LOC/s | 65 |
| tsgo | 7.0.2 | 2.31s | 84,286 LOC/s | 65 |
| xtsc-native | 7a859f0051f8 (AOT) | 7.54s | 25,805 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.61x FASTER than tsc; 2.09x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.44s**, **-8.1%** against `temurin26` on identical bytecode — 0.35× tsc, 1.92× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.99×** tsc and **10.87×** tsgo; tsc is **5.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.97s**.

Ahead-of-time (GraalVM native-image): **7.54s** — **0.60×** tsc, **3.27×** tsgo, **3.33×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
