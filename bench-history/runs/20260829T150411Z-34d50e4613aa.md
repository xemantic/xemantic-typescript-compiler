# Bench run 20260829T150411Z

- **xtsc revision**: [`34d50e4613aa`](https://github.com/xemantic/xemantic-typescript-compiler/commit/34d50e4613aa32dd03bd6089cefe90c2b22599aa)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 34d50e4613aa32dd03bd6089cefe90c2b22599aa

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 34d50e4613aa | **4.34s** | 44,882 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 34d50e4613aa | **4.15s** | 46,916 LOC/s | 46 |
| xtsc (cold JVM) | 34d50e4613aa | 25.05s | 7,772 LOC/s | 46 |
| tsc | 6.0.3 | 10.92s | 17,834 LOC/s | 65 |
| tsgo | 7.0.2 | 1.71s | 114,194 LOC/s | 65 |
| xtsc-native | 34d50e4613aa (AOT) | 7.41s | 26,293 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.52x FASTER than tsc; 2.54x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.15s**, **-4.3%** against `temurin26` on identical bytecode — 0.38× tsc, 2.43× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.29×** tsc and **14.69×** tsgo; tsc is **6.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.93s**.

Ahead-of-time (GraalVM native-image): **7.41s** — **0.68×** tsc, **4.34×** tsgo, **3.38×** the COLD JVM arm and **1.71×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 34d50e4613aa | **5.36s** | 36,331 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 34d50e4613aa | **4.95s** | 39,317 LOC/s | 46 |
| xtsc (cold JVM) | 34d50e4613aa | 25.88s | 7,522 LOC/s | 46 |
| tsc | 6.0.3 | 13.41s | 14,514 LOC/s | 65 |
| tsgo | 7.0.2 | 2.46s | 79,082 LOC/s | 65 |
| xtsc-native | 34d50e4613aa (AOT) | 8.30s | 23,455 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.50x FASTER than tsc; 2.18x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.95s**, **-7.6%** against `temurin26` on identical bytecode — 0.37× tsc, 2.01× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.93×** tsc and **10.51×** tsgo; tsc is **5.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.77s**.

Ahead-of-time (GraalVM native-image): **8.30s** — **0.62×** tsc, **3.37×** tsgo, **3.12×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
