# Bench run 20260901T132235Z

- **xtsc revision**: [`0abf23c18ddc`](https://github.com/xemantic/xemantic-typescript-compiler/commit/0abf23c18ddc1741cadc8f76a6e3f29fe0a4e36b)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 0abf23c18ddc1741cadc8f76a6e3f29fe0a4e36b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0abf23c18ddc | **4.14s** | 47,040 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0abf23c18ddc | **4.18s** | 46,624 LOC/s | 46 |
| xtsc (cold JVM) | 0abf23c18ddc | 22.84s | 8,526 LOC/s | 46 |
| tsc | 6.0.3 | 10.63s | 18,321 LOC/s | 65 |
| tsgo | 7.0.2 | 1.58s | 123,541 LOC/s | 65 |
| xtsc-native | 0abf23c18ddc (AOT) | 7.23s | 26,922 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.57x FASTER than tsc; 2.63x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.18s**, **+0.9%** against `temurin26` on identical bytecode — 0.39× tsc, 2.65× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.15×** tsc and **14.49×** tsgo; tsc is **6.74×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.72s**.

Ahead-of-time (GraalVM native-image): **7.23s** — **0.68×** tsc, **4.59×** tsgo, **3.16×** the COLD JVM arm and **1.75×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0abf23c18ddc | **5.13s** | 37,961 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0abf23c18ddc | **4.78s** | 40,758 LOC/s | 46 |
| xtsc (cold JVM) | 0abf23c18ddc | 26.45s | 7,360 LOC/s | 46 |
| tsc | 6.0.3 | 12.70s | 15,333 LOC/s | 65 |
| tsgo | 7.0.2 | 2.31s | 84,396 LOC/s | 65 |
| xtsc-native | 0abf23c18ddc (AOT) | 8.63s | 22,571 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.48x FASTER than tsc; 2.22x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.78s**, **-6.9%** against `temurin26` on identical bytecode — 0.38× tsc, 2.07× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.08×** tsc and **11.47×** tsgo; tsc is **5.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.33s**.

Ahead-of-time (GraalVM native-image): **8.63s** — **0.68×** tsc, **3.74×** tsgo, **3.07×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
