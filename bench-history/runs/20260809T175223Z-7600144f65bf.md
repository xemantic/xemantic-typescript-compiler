# Bench run 20260809T175223Z

- **xtsc revision**: [`7600144f65bf`](https://github.com/xemantic/xemantic-typescript-compiler/commit/7600144f65bf0abeba196d3fbc9b84db562ca739)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 7600144f65bf0abeba196d3fbc9b84db562ca739

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 7600144f65bf | 19.54s | 9,966 LOC/s | 46 |
| tsc | 6.0.3 | 8.41s | 23,140 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.31s | 148,062 LOC/s | 65 |
| xtsc-native | 7600144f65bf (AOT) | 7.12s | 27,357 LOC/s | 46 |

Relative (wall): xtsc is **2.32×** tsc and **14.86×** tsgo; tsc is **6.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.42s**.

Ahead-of-time (GraalVM native-image): **7.12s**, **2.74×** the JVM arm, **0.85×** tsc, **5.41×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 7600144f65bf | 23.45s | 8,301 LOC/s | 46 |
| tsc | 6.0.3 | 10.17s | 19,148 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.95s | 99,847 LOC/s | 65 |
| xtsc-native | 7600144f65bf (AOT) | 8.38s | 23,239 LOC/s | 46 |

Relative (wall): xtsc is **2.31×** tsc and **12.03×** tsgo; tsc is **5.21×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.33s**.

Ahead-of-time (GraalVM native-image): **8.38s**, **2.80×** the JVM arm, **0.82×** tsc, **4.30×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
