# Bench run 20260826T030409Z

- **xtsc revision**: [`5c64abd8aab0`](https://github.com/xemantic/xemantic-typescript-compiler/commit/5c64abd8aab0e0d593cf08b6784f2f6cf118903d)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 5c64abd8aab0e0d593cf08b6784f2f6cf118903d

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5c64abd8aab0 | **4.10s** | 47,534 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5c64abd8aab0 | **3.70s** | 52,579 LOC/s | 46 |
| xtsc (cold JVM) | 5c64abd8aab0 | 23.43s | 8,308 LOC/s | 46 |
| tsc | 6.0.3 | 10.27s | 18,963 LOC/s | 65 |
| tsgo | 7.0.2 | 1.61s | 120,932 LOC/s | 65 |
| xtsc-native | 5c64abd8aab0 (AOT) | 6.21s | 31,358 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.51x FASTER than tsc; 2.54x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.70s**, **-9.6%** against `temurin26` on identical bytecode — 0.36× tsc, 2.30× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.28×** tsc and **14.56×** tsgo; tsc is **6.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.30s**.

Ahead-of-time (GraalVM native-image): **6.21s** — **0.60×** tsc, **3.86×** tsgo, **3.77×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5c64abd8aab0 | **4.75s** | 40,955 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5c64abd8aab0 | **4.58s** | 42,548 LOC/s | 46 |
| xtsc (cold JVM) | 5c64abd8aab0 | 23.47s | 8,296 LOC/s | 46 |
| tsc | 6.0.3 | 13.06s | 14,905 LOC/s | 65 |
| tsgo | 7.0.2 | 2.34s | 83,099 LOC/s | 65 |
| xtsc-native | 5c64abd8aab0 (AOT) | 8.32s | 23,390 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.75x FASTER than tsc; 2.03x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.58s**, **-3.7%** against `temurin26` on identical bytecode — 0.35× tsc, 1.95× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.80×** tsc and **10.02×** tsgo; tsc is **5.57×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.34s**.

Ahead-of-time (GraalVM native-image): **8.32s** — **0.64×** tsc, **3.55×** tsgo, **2.82×** the COLD JVM arm and **1.75×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
