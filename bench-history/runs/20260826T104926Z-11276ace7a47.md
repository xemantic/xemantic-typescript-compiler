# Bench run 20260826T104926Z

- **xtsc revision**: [`11276ace7a47`](https://github.com/xemantic/xemantic-typescript-compiler/commit/11276ace7a47a69307141828771763de6c2693b0)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 11276ace7a47a69307141828771763de6c2693b0

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 11276ace7a47 | **4.07s** | 47,826 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 11276ace7a47 | **3.68s** | 52,922 LOC/s | 46 |
| xtsc (cold JVM) | 11276ace7a47 | 21.91s | 8,887 LOC/s | 46 |
| tsc | 6.0.3 | 10.59s | 18,380 LOC/s | 65 |
| tsgo | 7.0.2 | 1.65s | 118,287 LOC/s | 65 |
| xtsc-native | 11276ace7a47 (AOT) | 5.97s | 32,624 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.60x FASTER than tsc; 2.47x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.68s**, **-9.6%** against `temurin26` on identical bytecode — 0.35× tsc, 2.24× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.07×** tsc and **13.31×** tsgo; tsc is **6.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.80s**.

Ahead-of-time (GraalVM native-image): **5.97s** — **0.56×** tsc, **3.63×** tsgo, **3.67×** the COLD JVM arm and **1.47×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 11276ace7a47 | **4.68s** | 41,611 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 11276ace7a47 | **4.33s** | 44,924 LOC/s | 46 |
| xtsc (cold JVM) | 11276ace7a47 | 27.07s | 7,191 LOC/s | 46 |
| tsc | 6.0.3 | 12.93s | 15,055 LOC/s | 65 |
| tsgo | 7.0.2 | 2.36s | 82,570 LOC/s | 65 |
| xtsc-native | 11276ace7a47 (AOT) | 7.30s | 26,686 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.76x FASTER than tsc; 1.98x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.33s**, **-7.4%** against `temurin26` on identical bytecode — 0.34× tsc, 1.84× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.09×** tsc and **11.48×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.96s**.

Ahead-of-time (GraalVM native-image): **7.30s** — **0.56×** tsc, **3.09×** tsgo, **3.71×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
