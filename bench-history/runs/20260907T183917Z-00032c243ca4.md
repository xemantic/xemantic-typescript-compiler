# Bench run 20260907T183917Z

- **xtsc revision**: [`00032c243ca4`](https://github.com/xemantic/xemantic-typescript-compiler/commit/00032c243ca459e8dd8907121f6662cbc94ad79d)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 00032c243ca459e8dd8907121f6662cbc94ad79d

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 00032c243ca4 | **14.98s** | 12,997 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 00032c243ca4 | **15.01s** | 12,975 LOC/s | 46 |
| xtsc (cold JVM) | 00032c243ca4 | 36.94s | 5,270 LOC/s | 46 |
| tsc | 6.0.3 | 10.44s | 18,653 LOC/s | 65 |
| tsgo | 7.0.2 | 1.54s | 126,594 LOC/s | 65 |
| xtsc-native | 00032c243ca4 (AOT) | 24.65s | 7,897 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.44x tsc — parity at 1.00x; 9.74x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **15.01s**, **+0.2%** against `temurin26` on identical bytecode — 1.44× tsc, 9.76× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.54×** tsc and **24.02×** tsgo; tsc is **6.79×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **36.82s**.

Ahead-of-time (GraalVM native-image): **24.65s** — **2.36×** tsc, **16.03×** tsgo, **1.50×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 00032c243ca4 | **15.41s** | 12,637 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 00032c243ca4 | **15.81s** | 12,313 LOC/s | 46 |
| xtsc (cold JVM) | 00032c243ca4 | 40.39s | 4,820 LOC/s | 46 |
| tsc | 6.0.3 | 12.30s | 15,826 LOC/s | 65 |
| tsgo | 7.0.2 | 2.27s | 85,658 LOC/s | 65 |
| xtsc-native | 00032c243ca4 (AOT) | 25.22s | 7,719 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.25x tsc — parity at 1.00x; 6.78x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **15.81s**, **+2.6%** against `temurin26` on identical bytecode — 1.29× tsc, 6.96× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.28×** tsc and **17.77×** tsgo; tsc is **5.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **40.25s**.

Ahead-of-time (GraalVM native-image): **25.22s** — **2.05×** tsc, **11.10×** tsgo, **1.60×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
