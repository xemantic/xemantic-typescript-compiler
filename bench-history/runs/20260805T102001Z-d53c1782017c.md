# Bench run 20260805T102001Z

- **xtsc revision**: [`d53c1782017c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d53c1782017c01fd9b87684134127275d8b01875)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push d53c1782017c01fd9b87684134127275d8b01875

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | d53c1782017c | 24.48s | 7,953 LOC/s | 46 |
| tsc | 6.0.3 | 10.35s | 18,804 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.61s | 120,857 LOC/s | 65 |
| xtsc-native | d53c1782017c (AOT) | 11.51s | 16,920 LOC/s | 46 |

Relative (wall): xtsc is **2.36×** tsc and **15.20×** tsgo; tsc is **6.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.38s**.

Ahead-of-time (GraalVM native-image): **11.51s**, **2.13×** the JVM arm, **1.11×** tsc, **7.14×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | d53c1782017c | 28.12s | 6,922 LOC/s | 46 |
| tsc | 6.0.3 | 12.73s | 15,293 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.32s | 83,959 LOC/s | 65 |
| xtsc-native | d53c1782017c (AOT) | 13.99s | 13,919 LOC/s | 46 |

Relative (wall): xtsc is **2.21×** tsc and **12.13×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.02s**.

Ahead-of-time (GraalVM native-image): **13.99s**, **2.01×** the JVM arm, **1.10×** tsc, **6.03×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
