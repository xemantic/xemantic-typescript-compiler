# Bench run 20260907T165046Z

- **xtsc revision**: [`a6f79040a335`](https://github.com/xemantic/xemantic-typescript-compiler/commit/a6f79040a33537bdf5a6b3380cdb427af4f7b565)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push a6f79040a33537bdf5a6b3380cdb427af4f7b565

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a6f79040a335 | **11.55s** | 16,860 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a6f79040a335 | **11.83s** | 16,458 LOC/s | 46 |
| xtsc (cold JVM) | a6f79040a335 | 28.65s | 6,796 LOC/s | 46 |
| tsc | 6.0.3 | 8.05s | 24,192 LOC/s | 65 |
| tsgo | 7.0.2 | 1.22s | 159,853 LOC/s | 65 |
| xtsc-native | a6f79040a335 (AOT) | 19.79s | 9,838 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.43x tsc — parity at 1.00x; 9.48x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **11.83s**, **+2.4%** against `temurin26` on identical bytecode — 1.47× tsc, 9.71× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.56×** tsc and **23.52×** tsgo; tsc is **6.61×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.55s**.

Ahead-of-time (GraalVM native-image): **19.79s** — **2.46×** tsc, **16.25×** tsgo, **1.45×** the COLD JVM arm and **1.71×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a6f79040a335 | **12.20s** | 15,959 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a6f79040a335 | **12.63s** | 15,417 LOC/s | 46 |
| xtsc (cold JVM) | a6f79040a335 | 28.24s | 6,895 LOC/s | 46 |
| tsc | 6.0.3 | 10.08s | 19,317 LOC/s | 65 |
| tsgo | 7.0.2 | 1.82s | 106,803 LOC/s | 65 |
| xtsc-native | a6f79040a335 (AOT) | 21.15s | 9,205 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.21x tsc — parity at 1.00x; 6.69x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **12.63s**, **+3.5%** against `temurin26` on identical bytecode — 1.25× tsc, 6.93× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.80×** tsc and **15.49×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.13s**.

Ahead-of-time (GraalVM native-image): **21.15s** — **2.10×** tsc, **11.60×** tsgo, **1.33×** the COLD JVM arm and **1.73×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
