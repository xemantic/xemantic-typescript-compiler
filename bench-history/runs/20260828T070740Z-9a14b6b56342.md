# Bench run 20260828T070740Z

- **xtsc revision**: [`9a14b6b56342`](https://github.com/xemantic/xemantic-typescript-compiler/commit/9a14b6b56342c0f353930e7bfcf885e67ce80ed0)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 9a14b6b56342c0f353930e7bfcf885e67ce80ed0

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9a14b6b56342 | **4.15s** | 46,893 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9a14b6b56342 | **3.79s** | 51,413 LOC/s | 46 |
| xtsc (cold JVM) | 9a14b6b56342 | 21.52s | 9,045 LOC/s | 46 |
| tsc | 6.0.3 | 10.32s | 18,864 LOC/s | 65 |
| tsgo | 7.0.2 | 1.58s | 123,463 LOC/s | 65 |
| xtsc-native | 9a14b6b56342 (AOT) | 6.75s | 28,861 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.49x FASTER than tsc; 2.63x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.79s**, **-8.8%** against `temurin26` on identical bytecode — 0.37× tsc, 2.40× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.09×** tsc and **13.65×** tsgo; tsc is **6.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.41s**.

Ahead-of-time (GraalVM native-image): **6.75s** — **0.65×** tsc, **4.28×** tsgo, **3.19×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9a14b6b56342 | **4.84s** | 40,244 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9a14b6b56342 | **4.46s** | 43,645 LOC/s | 46 |
| xtsc (cold JVM) | 9a14b6b56342 | 26.39s | 7,377 LOC/s | 46 |
| tsc | 6.0.3 | 12.56s | 15,501 LOC/s | 65 |
| tsgo | 7.0.2 | 2.23s | 87,427 LOC/s | 65 |
| xtsc-native | 9a14b6b56342 (AOT) | 7.66s | 25,411 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.60x FASTER than tsc; 2.17x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.46s**, **-7.8%** against `temurin26` on identical bytecode — 0.36× tsc, 2.00× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.10×** tsc and **11.85×** tsgo; tsc is **5.64×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.27s**.

Ahead-of-time (GraalVM native-image): **7.66s** — **0.61×** tsc, **3.44×** tsgo, **3.44×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
