# Bench run 20260910T170020Z

- **xtsc revision**: [`6f9461c7e3c3`](https://github.com/xemantic/xemantic-typescript-compiler/commit/6f9461c7e3c30d95e46ad87b7a4fa71b934109ae)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 6f9461c7e3c30d95e46ad87b7a4fa71b934109ae

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6f9461c7e3c3 | **5.41s** | 36,015 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6f9461c7e3c3 | **4.99s** | 39,034 LOC/s | 46 |
| xtsc (cold JVM) | 6f9461c7e3c3 | 26.48s | 7,353 LOC/s | 46 |
| tsc | 6.0.3 | 10.27s | 18,954 LOC/s | 65 |
| tsgo | 7.0.2 | 1.59s | 122,070 LOC/s | 65 |
| xtsc-native | 6f9461c7e3c3 (AOT) | 8.60s | 22,639 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.90x FASTER than tsc; 3.39x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.99s**, **-7.7%** against `temurin26` on identical bytecode — 0.49× tsc, 3.13× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.58×** tsc and **16.60×** tsgo; tsc is **6.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.36s**.

Ahead-of-time (GraalVM native-image): **8.60s** — **0.84×** tsc, **5.39×** tsgo, **3.08×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6f9461c7e3c3 | **6.17s** | 31,561 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6f9461c7e3c3 | **5.57s** | 34,968 LOC/s | 46 |
| xtsc (cold JVM) | 6f9461c7e3c3 | 27.91s | 6,976 LOC/s | 46 |
| tsc | 6.0.3 | 12.36s | 15,758 LOC/s | 65 |
| tsgo | 7.0.2 | 2.28s | 85,433 LOC/s | 65 |
| xtsc-native | 6f9461c7e3c3 (AOT) | 9.81s | 19,845 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.00x FASTER than tsc; 2.71x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.57s**, **-9.7%** against `temurin26` on identical bytecode — 0.45× tsc, 2.44× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.26×** tsc and **12.25×** tsgo; tsc is **5.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.79s**.

Ahead-of-time (GraalVM native-image): **9.81s** — **0.79×** tsc, **4.30×** tsgo, **2.84×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
