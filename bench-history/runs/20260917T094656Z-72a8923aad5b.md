# Bench run 20260917T094656Z

- **xtsc revision**: [`72a8923aad5b`](https://github.com/xemantic/xemantic-typescript-compiler/commit/72a8923aad5ba121ee1f69432463db2e888fcb13)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 72a8923aad5ba121ee1f69432463db2e888fcb13

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 72a8923aad5b | **4.54s** | 42,848 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 72a8923aad5b | **4.70s** | 41,461 LOC/s | 46 |
| xtsc (cold JVM) | 72a8923aad5b | 20.73s | 9,392 LOC/s | 46 |
| tsc | 6.0.3 | 8.78s | 22,170 LOC/s | 65 |
| tsgo | 7.0.2 | 1.31s | 148,968 LOC/s | 65 |
| xtsc-native | 72a8923aad5b (AOT) | 8.02s | 24,292 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.93x FASTER than tsc; 3.48x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.70s**, **+3.3%** against `temurin26` on identical bytecode — 0.53× tsc, 3.59× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.36×** tsc and **15.86×** tsgo; tsc is **6.72×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.54s**.

Ahead-of-time (GraalVM native-image): **8.02s** — **0.91×** tsc, **6.13×** tsgo, **2.59×** the COLD JVM arm and **1.76×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 72a8923aad5b | **5.65s** | 34,484 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 72a8923aad5b | **5.08s** | 38,349 LOC/s | 46 |
| xtsc (cold JVM) | 72a8923aad5b | 24.03s | 8,102 LOC/s | 46 |
| tsc | 6.0.3 | 10.45s | 18,633 LOC/s | 65 |
| tsgo | 7.0.2 | 1.87s | 104,007 LOC/s | 65 |
| xtsc-native | 72a8923aad5b (AOT) | 9.13s | 21,320 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.85x FASTER than tsc; 3.02x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.08s**, **-10.1%** against `temurin26` on identical bytecode — 0.49× tsc, 2.71× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.30×** tsc and **12.84×** tsgo; tsc is **5.58×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.93s**.

Ahead-of-time (GraalVM native-image): **9.13s** — **0.87×** tsc, **4.88×** tsgo, **2.63×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
