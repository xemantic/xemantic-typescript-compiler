# Bench run 20260909T095320Z

- **xtsc revision**: [`84923dec39bd`](https://github.com/xemantic/xemantic-typescript-compiler/commit/84923dec39bd8c650487a0a9b330cfd5095c1fea)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 84923dec39bd8c650487a0a9b330cfd5095c1fea

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 84923dec39bd | **5.62s** | 34,669 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 84923dec39bd | **5.17s** | 37,630 LOC/s | 46 |
| xtsc (cold JVM) | 84923dec39bd | 25.67s | 7,583 LOC/s | 46 |
| tsc | 6.0.3 | 10.95s | 17,779 LOC/s | 65 |
| tsgo | 7.0.2 | 1.64s | 118,793 LOC/s | 65 |
| xtsc-native | 84923dec39bd (AOT) | 9.70s | 20,070 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.95x FASTER than tsc; 3.43x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.17s**, **-7.9%** against `temurin26` on identical bytecode — 0.47× tsc, 3.16× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.34×** tsc and **15.66×** tsgo; tsc is **6.68×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.55s**.

Ahead-of-time (GraalVM native-image): **9.70s** — **0.89×** tsc, **5.92×** tsgo, **2.65×** the COLD JVM arm and **1.73×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 84923dec39bd | **6.64s** | 29,318 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 84923dec39bd | **5.50s** | 35,426 LOC/s | 46 |
| xtsc (cold JVM) | 84923dec39bd | 29.93s | 6,505 LOC/s | 46 |
| tsc | 6.0.3 | 12.53s | 15,535 LOC/s | 65 |
| tsgo | 7.0.2 | 2.16s | 90,056 LOC/s | 65 |
| xtsc-native | 84923dec39bd (AOT) | 10.26s | 18,976 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.89x FASTER than tsc; 3.07x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.50s**, **-17.2%** against `temurin26` on identical bytecode — 0.44× tsc, 2.54× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.39×** tsc and **13.84×** tsgo; tsc is **5.80×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.81s**.

Ahead-of-time (GraalVM native-image): **10.26s** — **0.82×** tsc, **4.75×** tsgo, **2.92×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
