# Bench run 20260829T070640Z

- **xtsc revision**: [`b5bb097761ee`](https://github.com/xemantic/xemantic-typescript-compiler/commit/b5bb097761ee87b2da242c13fe9398887595862a)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push b5bb097761ee87b2da242c13fe9398887595862a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b5bb097761ee | **2.60s** | 74,799 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b5bb097761ee | **2.56s** | 76,204 LOC/s | 46 |
| xtsc (cold JVM) | b5bb097761ee | 15.76s | 12,355 LOC/s | 46 |
| tsc | 6.0.3 | 6.95s | 28,002 LOC/s | 65 |
| tsgo | 7.0.2 | 1.10s | 177,162 LOC/s | 65 |
| xtsc-native | b5bb097761ee (AOT) | 4.25s | 45,844 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.67x FASTER than tsc; 2.37x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.56s**, **-1.8%** against `temurin26` on identical bytecode — 0.37× tsc, 2.32× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.27×** tsc and **14.34×** tsgo; tsc is **6.33×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **15.67s**.

Ahead-of-time (GraalVM native-image): **4.25s** — **0.61×** tsc, **3.86×** tsgo, **3.71×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b5bb097761ee | **3.46s** | 56,337 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b5bb097761ee | **3.08s** | 63,132 LOC/s | 46 |
| xtsc (cold JVM) | b5bb097761ee | 17.03s | 11,434 LOC/s | 46 |
| tsc | 6.0.3 | 8.36s | 23,292 LOC/s | 65 |
| tsgo | 7.0.2 | 1.57s | 124,330 LOC/s | 65 |
| xtsc-native | b5bb097761ee (AOT) | 5.06s | 38,509 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.42x FASTER than tsc; 2.21x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.08s**, **-10.8%** against `temurin26` on identical bytecode — 0.37× tsc, 1.97× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.04×** tsc and **10.87×** tsgo; tsc is **5.34×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **16.94s**.

Ahead-of-time (GraalVM native-image): **5.06s** — **0.60×** tsc, **3.23×** tsgo, **3.37×** the COLD JVM arm and **1.46×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
