# Bench run 20260904T160113Z

- **xtsc revision**: [`9f399809314b`](https://github.com/xemantic/xemantic-typescript-compiler/commit/9f399809314b0359dff761905b876270b5116b12)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 9f399809314b0359dff761905b876270b5116b12

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9f399809314b | **4.02s** | 48,457 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9f399809314b | **3.77s** | 51,617 LOC/s | 46 |
| xtsc (cold JVM) | 9f399809314b | 21.31s | 9,136 LOC/s | 46 |
| tsc | 6.0.3 | 9.85s | 19,770 LOC/s | 65 |
| tsgo | 7.0.2 | 1.61s | 120,857 LOC/s | 65 |
| xtsc-native | 9f399809314b (AOT) | 6.20s | 31,393 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.45x FASTER than tsc; 2.49x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.77s**, **-6.1%** against `temurin26` on identical bytecode — 0.38× tsc, 2.34× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.16×** tsc and **13.23×** tsgo; tsc is **6.11×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.21s**.

Ahead-of-time (GraalVM native-image): **6.20s** — **0.63×** tsc, **3.85×** tsgo, **3.44×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9f399809314b | **4.58s** | 42,529 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9f399809314b | **4.21s** | 46,280 LOC/s | 46 |
| xtsc (cold JVM) | 9f399809314b | 26.52s | 7,340 LOC/s | 46 |
| tsc | 6.0.3 | 11.78s | 16,530 LOC/s | 65 |
| tsgo | 7.0.2 | 2.22s | 87,782 LOC/s | 65 |
| xtsc-native | 9f399809314b (AOT) | 7.13s | 27,303 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.57x FASTER than tsc; 2.06x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.21s**, **-8.1%** against `temurin26` on identical bytecode — 0.36× tsc, 1.90× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.25×** tsc and **11.96×** tsgo; tsc is **5.31×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.41s**.

Ahead-of-time (GraalVM native-image): **7.13s** — **0.61×** tsc, **3.22×** tsgo, **3.72×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
