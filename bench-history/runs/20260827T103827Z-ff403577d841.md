# Bench run 20260827T103827Z

- **xtsc revision**: [`ff403577d841`](https://github.com/xemantic/xemantic-typescript-compiler/commit/ff403577d84162a7f4a58de1786469613fad9ee0)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push ff403577d84162a7f4a58de1786469613fad9ee0

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ff403577d841 | **2.68s** | 72,677 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ff403577d841 | **2.64s** | 73,778 LOC/s | 46 |
| xtsc (cold JVM) | ff403577d841 | 18.55s | 10,498 LOC/s | 46 |
| tsc | 6.0.3 | 7.99s | 24,365 LOC/s | 65 |
| tsgo | 7.0.2 | 1.24s | 157,017 LOC/s | 65 |
| xtsc-native | ff403577d841 (AOT) | 4.38s | 44,442 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.98x FASTER than tsc; 2.16x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.64s**, **-1.5%** against `temurin26` on identical bytecode — 0.33× tsc, 2.13× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.32×** tsc and **14.96×** tsgo; tsc is **6.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.46s**.

Ahead-of-time (GraalVM native-image): **4.38s** — **0.55×** tsc, **3.53×** tsgo, **4.23×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ff403577d841 | **3.58s** | 54,370 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ff403577d841 | **3.27s** | 59,450 LOC/s | 46 |
| xtsc (cold JVM) | ff403577d841 | 19.54s | 9,965 LOC/s | 46 |
| tsc | 6.0.3 | 9.66s | 20,161 LOC/s | 65 |
| tsgo | 7.0.2 | 1.82s | 106,803 LOC/s | 65 |
| xtsc-native | ff403577d841 (AOT) | 5.17s | 37,645 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.70x FASTER than tsc; 1.96x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.27s**, **-8.5%** against `temurin26` on identical bytecode — 0.34× tsc, 1.80× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.02×** tsc and **10.72×** tsgo; tsc is **5.30×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.45s**.

Ahead-of-time (GraalVM native-image): **5.17s** — **0.54×** tsc, **2.84×** tsgo, **3.78×** the COLD JVM arm and **1.44×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
