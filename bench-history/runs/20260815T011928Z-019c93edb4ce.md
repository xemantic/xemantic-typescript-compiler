# Bench run 20260815T011928Z

- **xtsc revision**: [`019c93edb4ce`](https://github.com/xemantic/xemantic-typescript-compiler/commit/019c93edb4ce9bda1c9d4afc2fc96f31442d9023)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 019c93edb4ce9bda1c9d4afc2fc96f31442d9023

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 019c93edb4ce | **3.09s** | 62,928 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 019c93edb4ce | **2.94s** | 66,202 LOC/s | 46 |
| xtsc (cold JVM) | 019c93edb4ce | 17.42s | 11,177 LOC/s | 46 |
| tsc | 6.0.3 | 8.02s | 24,277 LOC/s | 65 |
| tsgo | 7.0.2 | 1.23s | 158,037 LOC/s | 65 |
| xtsc-native | 019c93edb4ce (AOT) | 4.98s | 39,088 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.59x FASTER than tsc; 2.51x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.94s**, **-4.9%** against `temurin26` on identical bytecode — 0.37× tsc, 2.39× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.17×** tsc and **14.14×** tsgo; tsc is **6.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.33s**.

Ahead-of-time (GraalVM native-image): **4.98s** — **0.62×** tsc, **4.04×** tsgo, **3.50×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 019c93edb4ce | **4.01s** | 48,554 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 019c93edb4ce | **3.46s** | 56,207 LOC/s | 46 |
| xtsc (cold JVM) | 019c93edb4ce | 20.57s | 9,465 LOC/s | 46 |
| tsc | 6.0.3 | 9.74s | 19,987 LOC/s | 65 |
| tsgo | 7.0.2 | 1.76s | 110,437 LOC/s | 65 |
| xtsc-native | 019c93edb4ce (AOT) | 5.95s | 32,734 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.43x FASTER than tsc; 2.27x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.46s**, **-13.6%** against `temurin26` on identical bytecode — 0.36× tsc, 1.96× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.11×** tsc and **11.67×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.48s**.

Ahead-of-time (GraalVM native-image): **5.95s** — **0.61×** tsc, **3.37×** tsgo, **3.46×** the COLD JVM arm and **1.48×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
