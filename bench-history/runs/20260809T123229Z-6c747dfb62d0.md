# Bench run 20260809T123229Z

- **xtsc revision**: [`6c747dfb62d0`](https://github.com/xemantic/xemantic-typescript-compiler/commit/6c747dfb62d052fbf5007298702e2c2af87676a3)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 6c747dfb62d052fbf5007298702e2c2af87676a3

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 6c747dfb62d0 | 27.93s | 6,970 LOC/s | 46 |
| tsc | 6.0.3 | 11.09s | 17,559 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.72s | 113,396 LOC/s | 65 |
| xtsc-native | 6c747dfb62d0 (AOT) | 9.70s | 20,066 LOC/s | 46 |

Relative (wall): xtsc is **2.52×** tsc and **16.27×** tsgo; tsc is **6.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.82s**.

Ahead-of-time (GraalVM native-image): **9.70s**, **2.88×** the JVM arm, **0.88×** tsc, **5.65×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 6c747dfb62d0 | 25.96s | 7,501 LOC/s | 46 |
| tsc | 6.0.3 | 13.26s | 14,687 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.45s | 79,373 LOC/s | 65 |
| xtsc-native | 6c747dfb62d0 (AOT) | 12.11s | 16,080 LOC/s | 46 |

Relative (wall): xtsc is **1.96×** tsc and **10.58×** tsgo; tsc is **5.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.84s**.

Ahead-of-time (GraalVM native-image): **12.11s**, **2.14×** the JVM arm, **0.91×** tsc, **4.94×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
