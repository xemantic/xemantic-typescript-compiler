# Bench run 20260818T202731Z

- **xtsc revision**: [`8466cdd22039`](https://github.com/xemantic/xemantic-typescript-compiler/commit/8466cdd2203984643d321a81edf8b085bb9384ec)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 8466cdd2203984643d321a81edf8b085bb9384ec

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8466cdd22039 | **3.95s** | 49,266 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8466cdd22039 | **3.85s** | 50,611 LOC/s | 46 |
| xtsc (cold JVM) | 8466cdd22039 | 23.20s | 8,391 LOC/s | 46 |
| tsc | 6.0.3 | 10.94s | 17,805 LOC/s | 65 |
| tsgo | 7.0.2 | 1.71s | 114,127 LOC/s | 65 |
| xtsc-native | 8466cdd22039 (AOT) | 5.92s | 32,866 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.77x FASTER than tsc; 2.32x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.85s**, **-2.7%** against `temurin26` on identical bytecode — 0.35× tsc, 2.25× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.12×** tsc and **13.60×** tsgo; tsc is **6.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.09s**.

Ahead-of-time (GraalVM native-image): **5.92s** — **0.54×** tsc, **3.47×** tsgo, **3.92×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8466cdd22039 | **4.93s** | 39,469 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8466cdd22039 | **4.33s** | 45,007 LOC/s | 46 |
| xtsc (cold JVM) | 8466cdd22039 | 26.46s | 7,359 LOC/s | 46 |
| tsc | 6.0.3 | 13.15s | 14,806 LOC/s | 65 |
| tsgo | 7.0.2 | 2.37s | 82,222 LOC/s | 65 |
| xtsc-native | 8466cdd22039 (AOT) | 7.13s | 27,311 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.67x FASTER than tsc; 2.08x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.33s**, **-12.3%** against `temurin26` on identical bytecode — 0.33× tsc, 1.83× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.01×** tsc and **11.17×** tsgo; tsc is **5.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.33s**.

Ahead-of-time (GraalVM native-image): **7.13s** — **0.54×** tsc, **3.01×** tsgo, **3.71×** the COLD JVM arm and **1.45×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
