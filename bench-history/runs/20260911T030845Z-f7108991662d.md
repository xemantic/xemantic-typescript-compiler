# Bench run 20260911T030845Z

- **xtsc revision**: [`f7108991662d`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f7108991662d2051eb30b8c6d9f92353688c7829)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push f7108991662d2051eb30b8c6d9f92353688c7829

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f7108991662d | **5.32s** | 36,611 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f7108991662d | **4.96s** | 39,262 LOC/s | 46 |
| xtsc (cold JVM) | f7108991662d | 26.65s | 7,305 LOC/s | 46 |
| tsc | 6.0.3 | 10.79s | 18,051 LOC/s | 65 |
| tsgo | 7.0.2 | 1.68s | 115,825 LOC/s | 65 |
| xtsc-native | f7108991662d (AOT) | 8.70s | 22,387 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.03x FASTER than tsc; 3.16x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.96s**, **-6.8%** against `temurin26` on identical bytecode — 0.46× tsc, 2.95× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.47×** tsc and **15.85×** tsgo; tsc is **6.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.54s**.

Ahead-of-time (GraalVM native-image): **8.70s** — **0.81×** tsc, **5.17×** tsgo, **3.06×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f7108991662d | **6.49s** | 30,014 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f7108991662d | **5.52s** | 35,297 LOC/s | 46 |
| xtsc (cold JVM) | f7108991662d | 27.70s | 7,028 LOC/s | 46 |
| tsc | 6.0.3 | 13.08s | 14,882 LOC/s | 65 |
| tsgo | 7.0.2 | 2.40s | 81,261 LOC/s | 65 |
| xtsc-native | f7108991662d (AOT) | 9.71s | 20,041 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.02x FASTER than tsc; 2.71x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.52s**, **-15.0%** against `temurin26` on identical bytecode — 0.42× tsc, 2.30× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.12×** tsc and **11.56×** tsgo; tsc is **5.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.59s**.

Ahead-of-time (GraalVM native-image): **9.71s** — **0.74×** tsc, **4.05×** tsgo, **2.85×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
