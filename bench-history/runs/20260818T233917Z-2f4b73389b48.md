# Bench run 20260818T233917Z

- **xtsc revision**: [`2f4b73389b48`](https://github.com/xemantic/xemantic-typescript-compiler/commit/2f4b73389b4863fc0683dfa9ffa3e6e2f6a1791c)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 2f4b73389b4863fc0683dfa9ffa3e6e2f6a1791c

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 2f4b73389b48 | **3.77s** | 51,617 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 2f4b73389b48 | **3.53s** | 55,172 LOC/s | 46 |
| xtsc (cold JVM) | 2f4b73389b48 | 22.75s | 8,558 LOC/s | 46 |
| tsc | 6.0.3 | 9.95s | 19,564 LOC/s | 65 |
| tsgo | 7.0.2 | 1.53s | 127,007 LOC/s | 65 |
| xtsc-native | 2f4b73389b48 (AOT) | 5.81s | 33,500 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.64x FASTER than tsc; 2.46x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.53s**, **-6.4%** against `temurin26` on identical bytecode — 0.35× tsc, 2.30× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.29×** tsc and **14.84×** tsgo; tsc is **6.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.63s**.

Ahead-of-time (GraalVM native-image): **5.81s** — **0.58×** tsc, **3.79×** tsgo, **3.91×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 2f4b73389b48 | **4.46s** | 43,664 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 2f4b73389b48 | **4.11s** | 47,361 LOC/s | 46 |
| xtsc (cold JVM) | 2f4b73389b48 | 25.61s | 7,601 LOC/s | 46 |
| tsc | 6.0.3 | 12.16s | 16,006 LOC/s | 65 |
| tsgo | 7.0.2 | 2.24s | 86,998 LOC/s | 65 |
| xtsc-native | 2f4b73389b48 (AOT) | 6.96s | 27,986 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.73x FASTER than tsc; 1.99x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.11s**, **-7.8%** against `temurin26` on identical bytecode — 0.34× tsc, 1.84× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.11×** tsc and **11.44×** tsgo; tsc is **5.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.50s**.

Ahead-of-time (GraalVM native-image): **6.96s** — **0.57×** tsc, **3.11×** tsgo, **3.68×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
