# Bench run 20260916T224232Z

- **xtsc revision**: [`887522087ff4`](https://github.com/xemantic/xemantic-typescript-compiler/commit/887522087ff46bd4fb04098ac85ff00f972d4f68)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 887522087ff46bd4fb04098ac85ff00f972d4f68

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 887522087ff4 | **5.66s** | 34,399 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 887522087ff4 | **5.19s** | 37,536 LOC/s | 46 |
| xtsc (cold JVM) | 887522087ff4 | 28.62s | 6,801 LOC/s | 46 |
| tsc | 6.0.3 | 11.04s | 17,639 LOC/s | 65 |
| tsgo | 7.0.2 | 1.75s | 111,258 LOC/s | 65 |
| xtsc-native | 887522087ff4 (AOT) | 9.62s | 20,232 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.95x FASTER than tsc; 3.23x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.19s**, **-8.4%** against `temurin26` on identical bytecode — 0.47× tsc, 2.96× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.59×** tsc and **16.36×** tsgo; tsc is **6.31×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.52s**.

Ahead-of-time (GraalVM native-image): **9.62s** — **0.87×** tsc, **5.50×** tsgo, **2.97×** the COLD JVM arm and **1.70×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 887522087ff4 | **6.87s** | 28,336 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 887522087ff4 | **5.93s** | 32,816 LOC/s | 46 |
| xtsc (cold JVM) | 887522087ff4 | 31.57s | 6,166 LOC/s | 46 |
| tsc | 6.0.3 | 13.24s | 14,703 LOC/s | 65 |
| tsgo | 7.0.2 | 2.39s | 81,533 LOC/s | 65 |
| xtsc-native | 887522087ff4 (AOT) | 10.71s | 18,187 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.93x FASTER than tsc; 2.88x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.93s**, **-13.7%** against `temurin26` on identical bytecode — 0.45× tsc, 2.48× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.38×** tsc and **13.22×** tsgo; tsc is **5.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **31.43s**.

Ahead-of-time (GraalVM native-image): **10.71s** — **0.81×** tsc, **4.48×** tsgo, **2.95×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
