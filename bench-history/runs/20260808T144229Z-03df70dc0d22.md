# Bench run 20260808T144229Z

- **xtsc revision**: [`03df70dc0d22`](https://github.com/xemantic/xemantic-typescript-compiler/commit/03df70dc0d228e93a2709f0242a0c532c43ee22b)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 03df70dc0d228e93a2709f0242a0c532c43ee22b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 03df70dc0d22 | 25.14s | 7,743 LOC/s | 46 |
| tsc | 6.0.3 | 10.67s | 18,254 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.68s | 116,101 LOC/s | 65 |
| xtsc-native | 03df70dc0d22 (AOT) | 10.69s | 18,210 LOC/s | 46 |

Relative (wall): xtsc is **2.36×** tsc and **14.99×** tsgo; tsc is **6.36×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.02s**.

Ahead-of-time (GraalVM native-image): **10.69s**, **2.35×** the JVM arm, **1.00×** tsc, **6.38×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 03df70dc0d22 | 28.31s | 6,877 LOC/s | 46 |
| tsc | 6.0.3 | 13.07s | 14,901 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.40s | 80,957 LOC/s | 65 |
| xtsc-native | 03df70dc0d22 (AOT) | 12.82s | 15,186 LOC/s | 46 |

Relative (wall): xtsc is **2.17×** tsc and **11.77×** tsgo; tsc is **5.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.20s**.

Ahead-of-time (GraalVM native-image): **12.82s**, **2.21×** the JVM arm, **0.98×** tsc, **5.33×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
