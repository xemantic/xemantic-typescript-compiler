# Bench run 20260823T034229Z

- **xtsc revision**: [`9a7e97461303`](https://github.com/xemantic/xemantic-typescript-compiler/commit/9a7e97461303db47e9ff85548ff12e921d282983)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 9a7e97461303db47e9ff85548ff12e921d282983

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9a7e97461303 | **3.04s** | 64,088 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9a7e97461303 | **2.88s** | 67,511 LOC/s | 46 |
| xtsc (cold JVM) | 9a7e97461303 | 17.58s | 11,076 LOC/s | 46 |
| tsc | 6.0.3 | 8.09s | 24,061 LOC/s | 65 |
| tsgo | 7.0.2 | 1.18s | 165,422 LOC/s | 65 |
| xtsc-native | 9a7e97461303 (AOT) | 4.87s | 39,996 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.66x FASTER than tsc; 2.58x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.88s**, **-5.1%** against `temurin26` on identical bytecode — 0.36× tsc, 2.45× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.17×** tsc and **14.93×** tsgo; tsc is **6.88×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.49s**.

Ahead-of-time (GraalVM native-image): **4.87s** — **0.60×** tsc, **4.14×** tsgo, **3.61×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9a7e97461303 | **3.79s** | 51,399 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9a7e97461303 | **3.52s** | 55,297 LOC/s | 46 |
| xtsc (cold JVM) | 9a7e97461303 | 24.69s | 7,885 LOC/s | 46 |
| tsc | 6.0.3 | 9.77s | 19,922 LOC/s | 65 |
| tsgo | 7.0.2 | 1.77s | 109,876 LOC/s | 65 |
| xtsc-native | 9a7e97461303 (AOT) | 6.15s | 31,679 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.58x FASTER than tsc; 2.14x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.52s**, **-7.0%** against `temurin26` on identical bytecode — 0.36× tsc, 1.99× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.53×** tsc and **13.93×** tsgo; tsc is **5.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.57s**.

Ahead-of-time (GraalVM native-image): **6.15s** — **0.63×** tsc, **3.47×** tsgo, **4.02×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
