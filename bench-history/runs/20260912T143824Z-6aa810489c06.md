# Bench run 20260912T143824Z

- **xtsc revision**: [`6aa810489c06`](https://github.com/xemantic/xemantic-typescript-compiler/commit/6aa810489c068806da69f3ed04fc24f6d4d87f75)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 6aa810489c068806da69f3ed04fc24f6d4d87f75

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6aa810489c06 | **5.46s** | 35,659 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6aa810489c06 | **5.04s** | 38,623 LOC/s | 46 |
| xtsc (cold JVM) | 6aa810489c06 | 27.36s | 7,117 LOC/s | 46 |
| tsc | 6.0.3 | 10.82s | 17,986 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,344 LOC/s | 65 |
| xtsc-native | 6aa810489c06 (AOT) | 8.53s | 22,820 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.98x FASTER than tsc; 3.23x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.04s**, **-7.7%** against `temurin26` on identical bytecode — 0.47× tsc, 2.99× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.53×** tsc and **16.21×** tsgo; tsc is **6.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.23s**.

Ahead-of-time (GraalVM native-image): **8.53s** — **0.79×** tsc, **5.05×** tsgo, **3.21×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6aa810489c06 | **6.39s** | 30,465 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6aa810489c06 | **5.54s** | 35,176 LOC/s | 46 |
| xtsc (cold JVM) | 6aa810489c06 | 30.64s | 6,355 LOC/s | 46 |
| tsc | 6.0.3 | 13.04s | 14,932 LOC/s | 65 |
| tsgo | 7.0.2 | 2.36s | 82,500 LOC/s | 65 |
| xtsc-native | 6aa810489c06 (AOT) | 9.92s | 19,625 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.04x FASTER than tsc; 2.71x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.54s**, **-13.4%** against `temurin26` on identical bytecode — 0.42× tsc, 2.35× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.35×** tsc and **12.98×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.50s**.

Ahead-of-time (GraalVM native-image): **9.92s** — **0.76×** tsc, **4.20×** tsgo, **3.09×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
