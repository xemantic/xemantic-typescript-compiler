# Bench run 20260817T225044Z

- **xtsc revision**: [`cebbceb84979`](https://github.com/xemantic/xemantic-typescript-compiler/commit/cebbceb84979b5c91269b1ee93f9ff0a7919d542)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push cebbceb84979b5c91269b1ee93f9ff0a7919d542

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cebbceb84979 | **4.09s** | 47,627 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cebbceb84979 | **3.70s** | 52,622 LOC/s | 46 |
| xtsc (cold JVM) | cebbceb84979 | 22.16s | 8,788 LOC/s | 46 |
| tsc | 6.0.3 | 10.89s | 17,874 LOC/s | 65 |
| tsgo | 7.0.2 | 1.74s | 111,769 LOC/s | 65 |
| xtsc-native | cebbceb84979 (AOT) | 6.15s | 31,653 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.66x FASTER than tsc; 2.35x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.70s**, **-9.5%** against `temurin26` on identical bytecode — 0.34× tsc, 2.12× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.03×** tsc and **12.72×** tsgo; tsc is **6.25×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.05s**.

Ahead-of-time (GraalVM native-image): **6.15s** — **0.56×** tsc, **3.53×** tsgo, **3.60×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cebbceb84979 | **4.66s** | 41,781 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cebbceb84979 | **4.52s** | 43,037 LOC/s | 46 |
| xtsc (cold JVM) | cebbceb84979 | 24.95s | 7,804 LOC/s | 46 |
| tsc | 6.0.3 | 13.30s | 14,642 LOC/s | 65 |
| tsgo | 7.0.2 | 2.40s | 81,024 LOC/s | 65 |
| xtsc-native | cebbceb84979 (AOT) | 7.33s | 26,551 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.85x FASTER than tsc; 1.94x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.52s**, **-2.9%** against `temurin26` on identical bytecode — 0.34× tsc, 1.88× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.88×** tsc and **10.38×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.82s**.

Ahead-of-time (GraalVM native-image): **7.33s** — **0.55×** tsc, **3.05×** tsgo, **3.40×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
