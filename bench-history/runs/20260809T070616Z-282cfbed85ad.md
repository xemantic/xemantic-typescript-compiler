# Bench run 20260809T070616Z

- **xtsc revision**: [`282cfbed85ad`](https://github.com/xemantic/xemantic-typescript-compiler/commit/282cfbed85ad75e3110eeabce9f986093c4fa78e)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 282cfbed85ad75e3110eeabce9f986093c4fa78e

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 282cfbed85ad | 27.37s | 7,112 LOC/s | 46 |
| tsc | 6.0.3 | 10.69s | 18,211 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.68s | 115,756 LOC/s | 65 |
| xtsc-native | 282cfbed85ad (AOT) | 10.49s | 18,562 LOC/s | 46 |

Relative (wall): xtsc is **2.56×** tsc and **16.27×** tsgo; tsc is **6.36×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.26s**.

Ahead-of-time (GraalVM native-image): **10.49s**, **2.61×** the JVM arm, **0.98×** tsc, **6.24×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 282cfbed85ad | 30.57s | 6,369 LOC/s | 46 |
| tsc | 6.0.3 | 13.16s | 14,793 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.39s | 81,601 LOC/s | 65 |
| xtsc-native | 282cfbed85ad (AOT) | 12.40s | 15,695 LOC/s | 46 |

Relative (wall): xtsc is **2.32×** tsc and **12.81×** tsgo; tsc is **5.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.42s**.

Ahead-of-time (GraalVM native-image): **12.40s**, **2.46×** the JVM arm, **0.94×** tsc, **5.20×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
