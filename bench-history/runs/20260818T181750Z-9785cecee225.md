# Bench run 20260818T181750Z

- **xtsc revision**: [`9785cecee225`](https://github.com/xemantic/xemantic-typescript-compiler/commit/9785cecee22579a4a87427385fb479ce7de5db6e)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 9785cecee22579a4a87427385fb479ce7de5db6e

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9785cecee225 | **2.81s** | 69,387 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9785cecee225 | **2.78s** | 70,112 LOC/s | 46 |
| xtsc (cold JVM) | 9785cecee225 | 14.11s | 13,800 LOC/s | 46 |
| tsc | 6.0.3 | 7.12s | 27,338 LOC/s | 65 |
| tsgo | 7.0.2 | 1.08s | 179,946 LOC/s | 65 |
| xtsc-native | 9785cecee225 (AOT) | 4.95s | 39,325 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.54x FASTER than tsc; 2.59x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.78s**, **-1.0%** against `temurin26` on identical bytecode — 0.39× tsc, 2.57× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.98×** tsc and **13.04×** tsgo; tsc is **6.58×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **14.02s**.

Ahead-of-time (GraalVM native-image): **4.95s** — **0.70×** tsc, **4.58×** tsgo, **2.85×** the COLD JVM arm and **1.76×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9785cecee225 | **3.46s** | 56,337 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9785cecee225 | **3.22s** | 60,428 LOC/s | 46 |
| xtsc (cold JVM) | 9785cecee225 | 19.27s | 10,104 LOC/s | 46 |
| tsc | 6.0.3 | 8.50s | 22,895 LOC/s | 65 |
| tsgo | 7.0.2 | 1.54s | 126,594 LOC/s | 65 |
| xtsc-native | 9785cecee225 (AOT) | 5.97s | 32,635 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.46x FASTER than tsc; 2.25x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.22s**, **-6.8%** against `temurin26` on identical bytecode — 0.38× tsc, 2.09× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.27×** tsc and **12.53×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.18s**.

Ahead-of-time (GraalVM native-image): **5.97s** — **0.70×** tsc, **3.88×** tsgo, **3.23×** the COLD JVM arm and **1.73×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
