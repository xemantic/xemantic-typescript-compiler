# Bench run 20260912T223756Z

- **xtsc revision**: [`cf6494e44056`](https://github.com/xemantic/xemantic-typescript-compiler/commit/cf6494e44056aec06039e93d3b4b1da4d8155d5d)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push cf6494e44056aec06039e93d3b4b1da4d8155d5d

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cf6494e44056 | **5.60s** | 34,743 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cf6494e44056 | **5.03s** | 38,677 LOC/s | 46 |
| xtsc (cold JVM) | cf6494e44056 | 29.02s | 6,708 LOC/s | 46 |
| tsc | 6.0.3 | 11.01s | 17,685 LOC/s | 65 |
| tsgo | 7.0.2 | 1.70s | 114,530 LOC/s | 65 |
| xtsc-native | cf6494e44056 (AOT) | 8.94s | 21,771 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.96x FASTER than tsc; 3.30x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.03s**, **-10.2%** against `temurin26` on identical bytecode — 0.46× tsc, 2.96× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.64×** tsc and **17.07×** tsgo; tsc is **6.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.91s**.

Ahead-of-time (GraalVM native-image): **8.94s** — **0.81×** tsc, **5.26×** tsgo, **3.25×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cf6494e44056 | **6.47s** | 30,111 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cf6494e44056 | **5.70s** | 34,146 LOC/s | 46 |
| xtsc (cold JVM) | cf6494e44056 | 29.57s | 6,583 LOC/s | 46 |
| tsc | 6.0.3 | 13.27s | 14,676 LOC/s | 65 |
| tsgo | 7.0.2 | 2.40s | 81,261 LOC/s | 65 |
| xtsc-native | cf6494e44056 (AOT) | 10.30s | 18,899 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.05x FASTER than tsc; 2.70x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.70s**, **-11.8%** against `temurin26` on identical bytecode — 0.43× tsc, 2.38× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.23×** tsc and **12.34×** tsgo; tsc is **5.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.45s**.

Ahead-of-time (GraalVM native-image): **10.30s** — **0.78×** tsc, **4.30×** tsgo, **2.87×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
