# Bench run 20260902T215603Z

- **xtsc revision**: [`432cd2afd52c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/432cd2afd52cae62c20c02e385aadee5519c0316)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 432cd2afd52cae62c20c02e385aadee5519c0316

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 432cd2afd52c | **3.68s** | 52,951 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 432cd2afd52c | **3.50s** | 55,629 LOC/s | 46 |
| xtsc (cold JVM) | 432cd2afd52c | 24.34s | 8,000 LOC/s | 46 |
| tsc | 6.0.3 | 9.62s | 20,228 LOC/s | 65 |
| tsgo | 7.0.2 | 1.49s | 130,409 LOC/s | 65 |
| xtsc-native | 432cd2afd52c (AOT) | 5.80s | 33,592 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.62x FASTER than tsc; 2.46x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.50s**, **-4.8%** against `temurin26` on identical bytecode — 0.36× tsc, 2.34× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.53×** tsc and **16.30×** tsgo; tsc is **6.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.24s**.

Ahead-of-time (GraalVM native-image): **5.80s** — **0.60×** tsc, **3.88×** tsgo, **4.20×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 432cd2afd52c | **4.49s** | 43,353 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 432cd2afd52c | **4.05s** | 48,086 LOC/s | 46 |
| xtsc (cold JVM) | 432cd2afd52c | 24.39s | 7,982 LOC/s | 46 |
| tsc | 6.0.3 | 11.68s | 16,676 LOC/s | 65 |
| tsgo | 7.0.2 | 2.20s | 88,460 LOC/s | 65 |
| xtsc-native | 432cd2afd52c (AOT) | 7.11s | 27,388 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.60x FASTER than tsc; 2.04x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.05s**, **-9.8%** against `temurin26` on identical bytecode — 0.35× tsc, 1.84× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.09×** tsc and **11.08×** tsgo; tsc is **5.30×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.29s**.

Ahead-of-time (GraalVM native-image): **7.11s** — **0.61×** tsc, **3.23×** tsgo, **3.43×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
