# Bench run 20260831T001744Z

- **xtsc revision**: [`e383d4bdd4a4`](https://github.com/xemantic/xemantic-typescript-compiler/commit/e383d4bdd4a49e258bd84e02be01917ecd3991e9)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push e383d4bdd4a49e258bd84e02be01917ecd3991e9

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e383d4bdd4a4 | **4.53s** | 42,999 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e383d4bdd4a4 | **4.12s** | 47,303 LOC/s | 46 |
| xtsc (cold JVM) | e383d4bdd4a4 | 24.45s | 7,962 LOC/s | 46 |
| tsc | 6.0.3 | 11.42s | 17,049 LOC/s | 65 |
| tsgo | 7.0.2 | 1.76s | 110,437 LOC/s | 65 |
| xtsc-native | e383d4bdd4a4 (AOT) | 7.62s | 25,551 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.52x FASTER than tsc; 2.57x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.12s**, **-9.1%** against `temurin26` on identical bytecode — 0.36× tsc, 2.33× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.14×** tsc and **13.87×** tsgo; tsc is **6.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.33s**.

Ahead-of-time (GraalVM native-image): **7.62s** — **0.67×** tsc, **4.32×** tsgo, **3.21×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e383d4bdd4a4 | **5.60s** | 34,762 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e383d4bdd4a4 | **4.83s** | 40,319 LOC/s | 46 |
| xtsc (cold JVM) | e383d4bdd4a4 | 29.38s | 6,626 LOC/s | 46 |
| tsc | 6.0.3 | 13.49s | 14,428 LOC/s | 65 |
| tsgo | 7.0.2 | 2.43s | 80,190 LOC/s | 65 |
| xtsc-native | e383d4bdd4a4 (AOT) | 8.14s | 23,913 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.41x FASTER than tsc; 2.31x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.83s**, **-13.8%** against `temurin26` on identical bytecode — 0.36× tsc, 1.99× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.18×** tsc and **12.10×** tsgo; tsc is **5.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.26s**.

Ahead-of-time (GraalVM native-image): **8.14s** — **0.60×** tsc, **3.35×** tsgo, **3.61×** the COLD JVM arm and **1.45×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
