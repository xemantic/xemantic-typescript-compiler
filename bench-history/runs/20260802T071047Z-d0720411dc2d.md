# Bench run 20260802T071047Z

- **xtsc revision**: [`d0720411dc2d`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d0720411dc2dadd4d4c156924c8f11f80ac50a42)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push d0720411dc2dadd4d4c156924c8f11f80ac50a42

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | d0720411dc2d | 27.68s | 7,033 LOC/s | 46 |
| tsc | 6.0.3 | 10.40s | 18,714 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.57s | 123,620 LOC/s | 65 |
| xtsc-native | d0720411dc2d (AOT) | 11.70s | 16,644 LOC/s | 46 |

Relative (wall): xtsc is **2.66×** tsc and **17.58×** tsgo; tsc is **6.61×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.57s**.

Ahead-of-time (GraalVM native-image): **11.70s**, **2.37×** the JVM arm, **1.12×** tsc, **7.43×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | d0720411dc2d | 28.66s | 6,793 LOC/s | 46 |
| tsc | 6.0.3 | 12.73s | 15,293 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.31s | 84,104 LOC/s | 65 |
| xtsc-native | d0720411dc2d (AOT) | 12.68s | 15,356 LOC/s | 46 |

Relative (wall): xtsc is **2.25×** tsc and **12.38×** tsgo; tsc is **5.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.55s**.

Ahead-of-time (GraalVM native-image): **12.68s**, **2.26×** the JVM arm, **1.00×** tsc, **5.48×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
