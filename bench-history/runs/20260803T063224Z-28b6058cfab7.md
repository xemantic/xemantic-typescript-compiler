# Bench run 20260803T063224Z

- **xtsc revision**: [`28b6058cfab7`](https://github.com/xemantic/xemantic-typescript-compiler/commit/28b6058cfab7d6b8c711f9dfbd99c9fc0f02905f)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 28b6058cfab7d6b8c711f9dfbd99c9fc0f02905f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 28b6058cfab7 | 25.09s | 7,760 LOC/s | 46 |
| tsc | 6.0.3 | 10.35s | 18,804 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.66s | 117,644 LOC/s | 65 |
| xtsc-native | 28b6058cfab7 (AOT) | 10.35s | 18,819 LOC/s | 46 |

Relative (wall): xtsc is **2.42×** tsc and **15.16×** tsgo; tsc is **6.26×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.98s**.

Ahead-of-time (GraalVM native-image): **10.35s**, **2.42×** the JVM arm, **1.00×** tsc, **6.25×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 28b6058cfab7 | 29.22s | 6,664 LOC/s | 46 |
| tsc | 6.0.3 | 12.59s | 15,461 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.48s | 78,667 LOC/s | 65 |
| xtsc-native | 28b6058cfab7 (AOT) | 12.65s | 15,387 LOC/s | 46 |

Relative (wall): xtsc is **2.32×** tsc and **11.80×** tsgo; tsc is **5.09×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.12s**.

Ahead-of-time (GraalVM native-image): **12.65s**, **2.31×** the JVM arm, **1.00×** tsc, **5.11×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
