# Bench run 20260812T101846Z

- **xtsc revision**: [`d90755ddef31`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d90755ddef31fa8c36877b1c978a7454b1455b06)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push d90755ddef31fa8c36877b1c978a7454b1455b06

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d90755ddef31 | **3.99s** | 48,821 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d90755ddef31 | **3.78s** | 51,508 LOC/s | 46 |
| xtsc (cold JVM) | d90755ddef31 | 25.19s | 7,728 LOC/s | 46 |
| tsc | 6.0.3 | 10.94s | 17,790 LOC/s | 65 |
| tsgo | 7.0.2 | 1.74s | 112,155 LOC/s | 65 |
| xtsc-native | d90755ddef31 (AOT) | 5.96s | 32,662 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.74x FASTER than tsc; 2.30x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.78s**, **-5.2%** against `temurin26` on identical bytecode — 0.35× tsc, 2.18× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.30×** tsc and **14.51×** tsgo; tsc is **6.30×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.07s**.

Ahead-of-time (GraalVM native-image): **5.96s** — **0.54×** tsc, **3.43×** tsgo, **4.23×** the COLD JVM arm and **1.49×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d90755ddef31 | **4.91s** | 39,670 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d90755ddef31 | **4.50s** | 43,247 LOC/s | 46 |
| xtsc (cold JVM) | d90755ddef31 | 26.33s | 7,393 LOC/s | 46 |
| tsc | 6.0.3 | 13.16s | 14,793 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,704 LOC/s | 65 |
| xtsc-native | d90755ddef31 (AOT) | 7.38s | 26,371 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.68x FASTER than tsc; 2.06x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.50s**, **-8.3%** against `temurin26` on identical bytecode — 0.34× tsc, 1.89× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.00×** tsc and **11.05×** tsgo; tsc is **5.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.22s**.

Ahead-of-time (GraalVM native-image): **7.38s** — **0.56×** tsc, **3.10×** tsgo, **3.57×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
