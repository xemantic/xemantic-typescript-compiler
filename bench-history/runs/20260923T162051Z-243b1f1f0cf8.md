# Bench run 20260923T162051Z

- **xtsc revision**: [`243b1f1f0cf8`](https://github.com/xemantic/xemantic-typescript-compiler/commit/243b1f1f0cf8fe50bfa9addd4fb9066501337ecd)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 243b1f1f0cf8fe50bfa9addd4fb9066501337ecd

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 243b1f1f0cf8 | **4.54s** | 42,876 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 243b1f1f0cf8 | **4.26s** | 45,715 LOC/s | 46 |
| xtsc (cold JVM) | 243b1f1f0cf8 | 22.39s | 8,695 LOC/s | 46 |
| tsc | 6.0.3 | 8.13s | 23,954 LOC/s | 65 |
| tsgo | 7.0.2 | 1.22s | 159,461 LOC/s | 65 |
| xtsc-native | 243b1f1f0cf8 (AOT) | 7.30s | 26,671 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.79x FASTER than tsc; 3.72x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.26s**, **-6.2%** against `temurin26` on identical bytecode — 0.52× tsc, 3.49× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.75×** tsc and **18.34×** tsgo; tsc is **6.66×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.27s**.

Ahead-of-time (GraalVM native-image): **7.30s** — **0.90×** tsc, **5.98×** tsgo, **3.07×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 243b1f1f0cf8 | **5.21s** | 37,392 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 243b1f1f0cf8 | **4.82s** | 40,361 LOC/s | 46 |
| xtsc (cold JVM) | 243b1f1f0cf8 | 25.93s | 7,507 LOC/s | 46 |
| tsc | 6.0.3 | 9.87s | 19,732 LOC/s | 65 |
| tsgo | 7.0.2 | 1.82s | 106,803 LOC/s | 65 |
| xtsc-native | 243b1f1f0cf8 (AOT) | 8.46s | 23,003 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.89x FASTER than tsc; 2.86x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.82s**, **-7.4%** against `temurin26` on identical bytecode — 0.49× tsc, 2.65× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.63×** tsc and **14.23×** tsgo; tsc is **5.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.83s**.

Ahead-of-time (GraalVM native-image): **8.46s** — **0.86×** tsc, **4.64×** tsgo, **3.06×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
