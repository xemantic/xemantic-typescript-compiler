# Bench run 20260804T165925Z

- **xtsc revision**: [`c925848600d0`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c925848600d03e7e6fe4eece52fb6e6977f3eb0f)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push c925848600d03e7e6fe4eece52fb6e6977f3eb0f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | c925848600d0 | 26.23s | 7,424 LOC/s | 46 |
| tsc | 6.0.3 | 10.48s | 18,569 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.67s | 116,867 LOC/s | 65 |
| xtsc-native | c925848600d0 (AOT) | 9.89s | 19,688 LOC/s | 46 |

Relative (wall): xtsc is **2.50×** tsc and **15.74×** tsgo; tsc is **6.29×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.12s**.

Ahead-of-time (GraalVM native-image): **9.89s**, **2.65×** the JVM arm, **0.94×** tsc, **5.94×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | c925848600d0 | 27.96s | 6,964 LOC/s | 46 |
| tsc | 6.0.3 | 12.94s | 15,048 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.39s | 81,533 LOC/s | 65 |
| xtsc-native | c925848600d0 (AOT) | 12.73s | 15,293 LOC/s | 46 |

Relative (wall): xtsc is **2.16×** tsc and **11.71×** tsgo; tsc is **5.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.85s**.

Ahead-of-time (GraalVM native-image): **12.73s**, **2.20×** the JVM arm, **0.98×** tsc, **5.33×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
