# Bench run 20260828T155839Z

- **xtsc revision**: [`a379c92d955e`](https://github.com/xemantic/xemantic-typescript-compiler/commit/a379c92d955ede9913bdc2b564fc3f6b5bef370f)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push a379c92d955ede9913bdc2b564fc3f6b5bef370f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a379c92d955e | **4.13s** | 47,109 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a379c92d955e | **3.88s** | 50,142 LOC/s | 46 |
| xtsc (cold JVM) | a379c92d955e | 24.58s | 7,921 LOC/s | 46 |
| tsc | 6.0.3 | 9.99s | 19,479 LOC/s | 65 |
| tsgo | 7.0.2 | 1.53s | 127,589 LOC/s | 65 |
| xtsc-native | a379c92d955e (AOT) | 6.87s | 28,324 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.42x FASTER than tsc; 2.71x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.88s**, **-6.0%** against `temurin26` on identical bytecode — 0.39× tsc, 2.54× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.46×** tsc and **16.11×** tsgo; tsc is **6.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.45s**.

Ahead-of-time (GraalVM native-image): **6.87s** — **0.69×** tsc, **4.50×** tsgo, **3.58×** the COLD JVM arm and **1.66×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | a379c92d955e | **5.02s** | 38,800 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | a379c92d955e | **4.48s** | 43,431 LOC/s | 46 |
| xtsc (cold JVM) | a379c92d955e | 24.09s | 8,082 LOC/s | 46 |
| tsc | 6.0.3 | 12.27s | 15,869 LOC/s | 65 |
| tsgo | 7.0.2 | 2.26s | 86,075 LOC/s | 65 |
| xtsc-native | a379c92d955e (AOT) | 7.52s | 25,898 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.44x FASTER than tsc; 2.22x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.48s**, **-10.7%** against `temurin26` on identical bytecode — 0.37× tsc, 1.98× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.96×** tsc and **10.65×** tsgo; tsc is **5.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.97s**.

Ahead-of-time (GraalVM native-image): **7.52s** — **0.61×** tsc, **3.32×** tsgo, **3.20×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
