# Bench run 20260912T191612Z

- **xtsc revision**: [`b0236442ad49`](https://github.com/xemantic/xemantic-typescript-compiler/commit/b0236442ad49d327e5c6660a883451ddf0b73e92)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push b0236442ad49d327e5c6660a883451ddf0b73e92

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b0236442ad49 | **5.63s** | 34,601 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b0236442ad49 | **5.07s** | 38,380 LOC/s | 46 |
| xtsc (cold JVM) | b0236442ad49 | 26.10s | 7,458 LOC/s | 46 |
| tsc | 6.0.3 | 10.60s | 18,376 LOC/s | 65 |
| tsgo | 7.0.2 | 1.63s | 119,595 LOC/s | 65 |
| xtsc-native | b0236442ad49 (AOT) | 9.21s | 21,137 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.88x FASTER than tsc; 3.46x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.07s**, **-9.8%** against `temurin26` on identical bytecode — 0.48× tsc, 3.12× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.46×** tsc and **16.03×** tsgo; tsc is **6.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.99s**.

Ahead-of-time (GraalVM native-image): **9.21s** — **0.87×** tsc, **5.66×** tsgo, **2.83×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b0236442ad49 | **6.14s** | 31,684 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b0236442ad49 | **5.65s** | 34,478 LOC/s | 46 |
| xtsc (cold JVM) | b0236442ad49 | 28.84s | 6,752 LOC/s | 46 |
| tsc | 6.0.3 | 12.72s | 15,311 LOC/s | 65 |
| tsgo | 7.0.2 | 2.35s | 82,993 LOC/s | 65 |
| xtsc-native | b0236442ad49 (AOT) | 10.40s | 18,721 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.07x FASTER than tsc; 2.62x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.65s**, **-8.1%** against `temurin26` on identical bytecode — 0.44× tsc, 2.41× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.27×** tsc and **12.29×** tsgo; tsc is **5.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.71s**.

Ahead-of-time (GraalVM native-image): **10.40s** — **0.82×** tsc, **4.43×** tsgo, **2.77×** the COLD JVM arm and **1.69×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
