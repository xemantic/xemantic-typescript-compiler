# Bench run 20260806T113605Z

- **xtsc revision**: [`454bcd126213`](https://github.com/xemantic/xemantic-typescript-compiler/commit/454bcd126213626a50d1bcc23ca00031fcc58d7f)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 454bcd126213626a50d1bcc23ca00031fcc58d7f

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 454bcd126213 | 20.69s | 9,410 LOC/s | 46 |
| tsc | 6.0.3 | 8.04s | 24,201 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.23s | 158,294 LOC/s | 65 |
| xtsc-native | 454bcd126213 (AOT) | 8.35s | 23,317 LOC/s | 46 |

Relative (wall): xtsc is **2.57×** tsc and **16.82×** tsgo; tsc is **6.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.60s**.

Ahead-of-time (GraalVM native-image): **8.35s**, **2.48×** the JVM arm, **1.04×** tsc, **6.79×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 454bcd126213 | 22.17s | 8,783 LOC/s | 46 |
| tsc | 6.0.3 | 9.75s | 19,973 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.78s | 109,506 LOC/s | 65 |
| xtsc-native | 454bcd126213 (AOT) | 10.94s | 17,790 LOC/s | 46 |

Relative (wall): xtsc is **2.27×** tsc and **12.47×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.07s**.

Ahead-of-time (GraalVM native-image): **10.94s**, **2.03×** the JVM arm, **1.12×** tsc, **6.16×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
