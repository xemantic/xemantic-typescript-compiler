# Bench run 20260829T063257Z

- **xtsc revision**: [`f581e59cf802`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f581e59cf8025ca4bf3736d19e6c52db2d9910a5)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push f581e59cf8025ca4bf3736d19e6c52db2d9910a5

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f581e59cf802 | **3.51s** | 55,454 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f581e59cf802 | **3.31s** | 58,804 LOC/s | 46 |
| xtsc (cold JVM) | f581e59cf802 | 19.71s | 9,877 LOC/s | 46 |
| tsc | 6.0.3 | 8.35s | 23,320 LOC/s | 65 |
| tsgo | 7.0.2 | 1.26s | 154,648 LOC/s | 65 |
| xtsc-native | f581e59cf802 (AOT) | 5.98s | 32,553 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.38x FASTER than tsc; 2.79x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.31s**, **-5.7%** against `temurin26` on identical bytecode — 0.40× tsc, 2.63× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.36×** tsc and **15.66×** tsgo; tsc is **6.63×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.62s**.

Ahead-of-time (GraalVM native-image): **5.98s** — **0.72×** tsc, **4.75×** tsgo, **3.30×** the COLD JVM arm and **1.70×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f581e59cf802 | **4.21s** | 46,225 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f581e59cf802 | **4.15s** | 46,938 LOC/s | 46 |
| xtsc (cold JVM) | f581e59cf802 | 21.43s | 9,085 LOC/s | 46 |
| tsc | 6.0.3 | 10.47s | 18,590 LOC/s | 65 |
| tsgo | 7.0.2 | 1.90s | 102,636 LOC/s | 65 |
| xtsc-native | f581e59cf802 (AOT) | 7.25s | 26,855 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.49x FASTER than tsc; 2.22x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.15s**, **-1.5%** against `temurin26` on identical bytecode — 0.40× tsc, 2.19× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.05×** tsc and **11.30×** tsgo; tsc is **5.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.32s**.

Ahead-of-time (GraalVM native-image): **7.25s** — **0.69×** tsc, **3.82×** tsgo, **2.96×** the COLD JVM arm and **1.72×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
