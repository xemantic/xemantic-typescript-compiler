# Bench run 20260808T210742Z

- **xtsc revision**: [`213292cb8719`](https://github.com/xemantic/xemantic-typescript-compiler/commit/213292cb8719e8b934801491b8502c55068458ad)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 213292cb8719e8b934801491b8502c55068458ad

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 213292cb8719 | 18.19s | 10,701 LOC/s | 46 |
| tsc | 6.0.3 | 7.87s | 24,755 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.21s | 161,177 LOC/s | 65 |
| xtsc-native | 213292cb8719 (AOT) | 8.93s | 21,798 LOC/s | 46 |

Relative (wall): xtsc is **2.31×** tsc and **15.06×** tsgo; tsc is **6.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.11s**.

Ahead-of-time (GraalVM native-image): **8.93s**, **2.04×** the JVM arm, **1.14×** tsc, **7.39×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 213292cb8719 | 21.42s | 9,090 LOC/s | 46 |
| tsc | 6.0.3 | 9.70s | 20,074 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.75s | 111,004 LOC/s | 65 |
| xtsc-native | 213292cb8719 (AOT) | 10.84s | 17,963 LOC/s | 46 |

Relative (wall): xtsc is **2.21×** tsc and **12.21×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.32s**.

Ahead-of-time (GraalVM native-image): **10.84s**, **1.98×** the JVM arm, **1.12×** tsc, **6.18×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
