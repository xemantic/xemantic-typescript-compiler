# Bench run 20260910T002604Z

- **xtsc revision**: [`ba405ba9a7ec`](https://github.com/xemantic/xemantic-typescript-compiler/commit/ba405ba9a7ec654bdae8810ea19900a780df2b63)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push ba405ba9a7ec654bdae8810ea19900a780df2b63

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ba405ba9a7ec | **5.36s** | 36,311 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ba405ba9a7ec | **4.82s** | 40,402 LOC/s | 46 |
| xtsc (cold JVM) | ba405ba9a7ec | 26.28s | 7,407 LOC/s | 46 |
| tsc | 6.0.3 | 10.52s | 18,513 LOC/s | 65 |
| tsgo | 7.0.2 | 1.64s | 118,793 LOC/s | 65 |
| xtsc-native | ba405ba9a7ec (AOT) | 8.38s | 23,231 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.96x FASTER than tsc; 3.27x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.82s**, **-10.1%** against `temurin26` on identical bytecode — 0.46× tsc, 2.94× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.50×** tsc and **16.04×** tsgo; tsc is **6.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.17s**.

Ahead-of-time (GraalVM native-image): **8.38s** — **0.80×** tsc, **5.11×** tsgo, **3.14×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ba405ba9a7ec | **6.22s** | 31,302 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ba405ba9a7ec | **5.40s** | 36,082 LOC/s | 46 |
| xtsc (cold JVM) | ba405ba9a7ec | 29.79s | 6,535 LOC/s | 46 |
| tsc | 6.0.3 | 13.05s | 14,917 LOC/s | 65 |
| tsgo | 7.0.2 | 2.37s | 82,222 LOC/s | 65 |
| xtsc-native | ba405ba9a7ec (AOT) | 9.84s | 19,780 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.10x FASTER than tsc; 2.63x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.40s**, **-13.2%** against `temurin26` on identical bytecode — 0.41× tsc, 2.28× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.28×** tsc and **12.58×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.65s**.

Ahead-of-time (GraalVM native-image): **9.84s** — **0.75×** tsc, **4.16×** tsgo, **3.03×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
