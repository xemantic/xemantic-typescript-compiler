# Bench run 20260914T230242Z

- **xtsc revision**: [`989d5a075023`](https://github.com/xemantic/xemantic-typescript-compiler/commit/989d5a075023a627a35f8b08288682e9675c49c5)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 989d5a075023a627a35f8b08288682e9675c49c5

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 989d5a075023 | **5.70s** | 34,158 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 989d5a075023 | **5.26s** | 37,022 LOC/s | 46 |
| xtsc (cold JVM) | 989d5a075023 | 27.05s | 7,197 LOC/s | 46 |
| tsc | 6.0.3 | 10.28s | 18,939 LOC/s | 65 |
| tsgo | 7.0.2 | 1.56s | 124,889 LOC/s | 65 |
| xtsc-native | 989d5a075023 (AOT) | 9.19s | 21,190 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.80x FASTER than tsc; 3.66x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.26s**, **-7.7%** against `temurin26` on identical bytecode — 0.51× tsc, 3.37× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.63×** tsc and **17.35×** tsgo; tsc is **6.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.93s**.

Ahead-of-time (GraalVM native-image): **9.19s** — **0.89×** tsc, **5.89×** tsgo, **2.94×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 989d5a075023 | **6.46s** | 30,144 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 989d5a075023 | **5.96s** | 32,651 LOC/s | 46 |
| xtsc (cold JVM) | 989d5a075023 | 31.19s | 6,242 LOC/s | 46 |
| tsc | 6.0.3 | 12.54s | 15,532 LOC/s | 65 |
| tsgo | 7.0.2 | 2.31s | 84,432 LOC/s | 65 |
| xtsc-native | 989d5a075023 (AOT) | 10.65s | 18,288 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.94x FASTER than tsc; 2.80x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.96s**, **-7.7%** against `temurin26` on identical bytecode — 0.48× tsc, 2.59× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.49×** tsc and **13.53×** tsgo; tsc is **5.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **31.05s**.

Ahead-of-time (GraalVM native-image): **10.65s** — **0.85×** tsc, **4.62×** tsgo, **2.93×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
