# Bench run 20260819T082445Z

- **xtsc revision**: [`d269c7339ff5`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d269c7339ff5b6f063da864d800c39a68b35fdcd)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push d269c7339ff5b6f063da864d800c39a68b35fdcd

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d269c7339ff5 | **3.98s** | 48,870 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d269c7339ff5 | **3.73s** | 52,240 LOC/s | 46 |
| xtsc (cold JVM) | d269c7339ff5 | 23.23s | 8,380 LOC/s | 46 |
| tsc | 6.0.3 | 10.95s | 17,784 LOC/s | 65 |
| tsgo | 7.0.2 | 1.71s | 113,661 LOC/s | 65 |
| xtsc-native | d269c7339ff5 (AOT) | 6.21s | 31,378 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.75x FASTER than tsc; 2.33x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.73s**, **-6.5%** against `temurin26` on identical bytecode — 0.34× tsc, 2.18× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.12×** tsc and **13.56×** tsgo; tsc is **6.39×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.12s**.

Ahead-of-time (GraalVM native-image): **6.21s** — **0.57×** tsc, **3.62×** tsgo, **3.74×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d269c7339ff5 | **4.92s** | 39,581 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d269c7339ff5 | **4.39s** | 44,381 LOC/s | 46 |
| xtsc (cold JVM) | d269c7339ff5 | 27.06s | 7,194 LOC/s | 46 |
| tsc | 6.0.3 | 13.21s | 14,743 LOC/s | 65 |
| tsgo | 7.0.2 | 2.40s | 81,295 LOC/s | 65 |
| xtsc-native | d269c7339ff5 (AOT) | 7.13s | 27,292 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.68x FASTER than tsc; 2.05x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.39s**, **-10.8%** against `temurin26` on identical bytecode — 0.33× tsc, 1.83× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.05×** tsc and **11.30×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.93s**.

Ahead-of-time (GraalVM native-image): **7.13s** — **0.54×** tsc, **2.98×** tsgo, **3.79×** the COLD JVM arm and **1.45×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
