# Bench run 20260914T193023Z

- **xtsc revision**: [`df21df3f696d`](https://github.com/xemantic/xemantic-typescript-compiler/commit/df21df3f696d5aab2816fcb96d8d2f81f13d3c19)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push df21df3f696d5aab2816fcb96d8d2f81f13d3c19

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | df21df3f696d | **5.84s** | 33,322 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | df21df3f696d | **5.38s** | 36,216 LOC/s | 46 |
| xtsc (cold JVM) | df21df3f696d | 25.30s | 7,694 LOC/s | 46 |
| tsc | 6.0.3 | 10.43s | 18,672 LOC/s | 65 |
| tsgo | 7.0.2 | 1.60s | 121,385 LOC/s | 65 |
| xtsc-native | df21df3f696d (AOT) | 9.72s | 20,031 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.78x FASTER than tsc; 3.64x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.38s**, **-8.0%** against `temurin26` on identical bytecode — 0.52× tsc, 3.35× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.43×** tsc and **15.78×** tsgo; tsc is **6.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.15s**.

Ahead-of-time (GraalVM native-image): **9.72s** — **0.93×** tsc, **6.06×** tsgo, **2.60×** the COLD JVM arm and **1.66×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | df21df3f696d | **6.56s** | 29,693 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | df21df3f696d | **6.04s** | 32,251 LOC/s | 46 |
| xtsc (cold JVM) | df21df3f696d | 29.18s | 6,673 LOC/s | 46 |
| tsc | 6.0.3 | 12.79s | 15,224 LOC/s | 65 |
| tsgo | 7.0.2 | 2.33s | 83,384 LOC/s | 65 |
| xtsc-native | df21df3f696d (AOT) | 11.15s | 17,457 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.95x FASTER than tsc; 2.81x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.04s**, **-7.9%** against `temurin26` on identical bytecode — 0.47× tsc, 2.59× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.28×** tsc and **12.50×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.04s**.

Ahead-of-time (GraalVM native-image): **11.15s** — **0.87×** tsc, **4.78×** tsgo, **2.62×** the COLD JVM arm and **1.70×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
