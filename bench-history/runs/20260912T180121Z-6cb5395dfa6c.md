# Bench run 20260912T180121Z

- **xtsc revision**: [`6cb5395dfa6c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/6cb5395dfa6cdf213c4e2566ca023558e89f1f99)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 6cb5395dfa6cdf213c4e2566ca023558e89f1f99

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6cb5395dfa6c | **5.60s** | 34,762 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6cb5395dfa6c | **4.98s** | 39,081 LOC/s | 46 |
| xtsc (cold JVM) | 6cb5395dfa6c | 26.56s | 7,331 LOC/s | 46 |
| tsc | 6.0.3 | 10.65s | 18,276 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,867 LOC/s | 65 |
| xtsc-native | 6cb5395dfa6c (AOT) | 8.25s | 23,608 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.90x FASTER than tsc; 3.36x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.98s**, **-11.1%** against `temurin26` on identical bytecode — 0.47× tsc, 2.99× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.49×** tsc and **15.94×** tsgo; tsc is **6.39×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.45s**.

Ahead-of-time (GraalVM native-image): **8.25s** — **0.77×** tsc, **4.95×** tsgo, **3.22×** the COLD JVM arm and **1.47×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6cb5395dfa6c | **6.27s** | 31,052 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6cb5395dfa6c | **5.66s** | 34,411 LOC/s | 46 |
| xtsc (cold JVM) | 6cb5395dfa6c | 29.30s | 6,645 LOC/s | 46 |
| tsc | 6.0.3 | 13.19s | 14,764 LOC/s | 65 |
| tsgo | 7.0.2 | 2.40s | 81,058 LOC/s | 65 |
| xtsc-native | 6cb5395dfa6c (AOT) | 10.16s | 19,156 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.10x FASTER than tsc; 2.61x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.66s**, **-9.8%** against `temurin26` on identical bytecode — 0.43× tsc, 2.36× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.22×** tsc and **12.20×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.15s**.

Ahead-of-time (GraalVM native-image): **10.16s** — **0.77×** tsc, **4.23×** tsgo, **2.88×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
