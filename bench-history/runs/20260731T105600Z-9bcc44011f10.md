# Bench run 20260731T105600Z

- **xtsc revision**: [`9bcc44011f10`](https://github.com/xemantic/xemantic-typescript-compiler/commit/9bcc44011f10aab804b2bfbb366b2a9692ca3bfa)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 9bcc44011f10aab804b2bfbb366b2a9692ca3bfa

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 9bcc44011f10 | 30.50s | 6,384 LOC/s | 46 |
| tsc | 6.0.3 | 10.34s | 18,822 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.64s | 118,793 LOC/s | 65 |
| xtsc-native | 9bcc44011f10 (AOT) | 11.94s | 16,313 LOC/s | 46 |

Relative (wall): xtsc is **2.95×** tsc and **18.61×** tsgo; tsc is **6.31×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.41s**.

Ahead-of-time (GraalVM native-image): **11.94s**, **2.56×** the JVM arm, **1.15×** tsc, **7.28×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 9bcc44011f10 | 33.07s | 5,887 LOC/s | 46 |
| tsc | 6.0.3 | 12.39s | 15,710 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.40s | 81,193 LOC/s | 65 |
| xtsc-native | 9bcc44011f10 (AOT) | 12.78s | 15,230 LOC/s | 46 |

Relative (wall): xtsc is **2.67×** tsc and **13.79×** tsgo; tsc is **5.17×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **32.98s**.

Ahead-of-time (GraalVM native-image): **12.78s**, **2.59×** the JVM arm, **1.03×** tsc, **5.33×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
