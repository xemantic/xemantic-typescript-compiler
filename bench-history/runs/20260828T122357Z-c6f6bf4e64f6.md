# Bench run 20260828T122357Z

- **xtsc revision**: [`c6f6bf4e64f6`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c6f6bf4e64f61d38b77c2a74f5a201829fef4a26)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push c6f6bf4e64f61d38b77c2a74f5a201829fef4a26

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c6f6bf4e64f6 | **4.31s** | 45,174 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c6f6bf4e64f6 | **4.12s** | 47,315 LOC/s | 46 |
| xtsc (cold JVM) | c6f6bf4e64f6 | 22.05s | 8,830 LOC/s | 46 |
| tsc | 6.0.3 | 10.50s | 18,546 LOC/s | 65 |
| tsgo | 7.0.2 | 1.59s | 122,223 LOC/s | 65 |
| xtsc-native | c6f6bf4e64f6 (AOT) | 7.08s | 27,480 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.44x FASTER than tsc; 2.71x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.12s**, **-4.5%** against `temurin26` on identical bytecode — 0.39× tsc, 2.58× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.10×** tsc and **13.84×** tsgo; tsc is **6.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.93s**.

Ahead-of-time (GraalVM native-image): **7.08s** — **0.67×** tsc, **4.45×** tsgo, **3.11×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c6f6bf4e64f6 | **5.30s** | 36,757 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c6f6bf4e64f6 | **4.75s** | 40,955 LOC/s | 46 |
| xtsc (cold JVM) | c6f6bf4e64f6 | 26.94s | 7,226 LOC/s | 46 |
| tsc | 6.0.3 | 12.86s | 15,141 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,945 LOC/s | 65 |
| xtsc-native | c6f6bf4e64f6 (AOT) | 8.58s | 22,697 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.43x FASTER than tsc; 2.23x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.75s**, **-10.3%** against `temurin26` on identical bytecode — 0.37× tsc, 2.00× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.10×** tsc and **11.34×** tsgo; tsc is **5.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.82s**.

Ahead-of-time (GraalVM native-image): **8.58s** — **0.67×** tsc, **3.61×** tsgo, **3.14×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
