# Bench run 20260906T224856Z

- **xtsc revision**: [`183cce3a1493`](https://github.com/xemantic/xemantic-typescript-compiler/commit/183cce3a1493ad4a8cf0a444594fd6803f4bac2a)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 183cce3a1493ad4a8cf0a444594fd6803f4bac2a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 183cce3a1493 | **14.04s** | 13,872 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 183cce3a1493 | **14.58s** | 13,356 LOC/s | 46 |
| xtsc (cold JVM) | 183cce3a1493 | 35.09s | 5,549 LOC/s | 46 |
| tsc | 6.0.3 | 10.68s | 18,230 LOC/s | 65 |
| tsgo | 7.0.2 | 1.63s | 119,595 LOC/s | 65 |
| xtsc-native | 183cce3a1493 (AOT) | 22.45s | 8,671 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.31x tsc — parity at 1.00x; 8.62x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **14.58s**, **+3.9%** against `temurin26` on identical bytecode — 1.36× tsc, 8.95× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.29×** tsc and **21.55×** tsgo; tsc is **6.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **34.97s**.

Ahead-of-time (GraalVM native-image): **22.45s** — **2.10×** tsc, **13.79×** tsgo, **1.56×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 183cce3a1493 | **15.93s** | 12,222 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 183cce3a1493 | **15.49s** | 12,571 LOC/s | 46 |
| xtsc (cold JVM) | 183cce3a1493 | 35.49s | 5,485 LOC/s | 46 |
| tsc | 6.0.3 | 12.96s | 15,022 LOC/s | 65 |
| tsgo | 7.0.2 | 2.35s | 82,746 LOC/s | 65 |
| xtsc-native | 183cce3a1493 (AOT) | 23.56s | 8,264 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.23x tsc — parity at 1.00x; 6.77x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **15.49s**, **-2.8%** against `temurin26` on identical bytecode — 1.19× tsc, 6.58× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.74×** tsc and **15.08×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **35.36s**.

Ahead-of-time (GraalVM native-image): **23.56s** — **1.82×** tsc, **10.01×** tsgo, **1.51×** the COLD JVM arm and **1.48×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
