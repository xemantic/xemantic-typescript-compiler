# Bench run 20260901T164818Z

- **xtsc revision**: [`11993c3d97d4`](https://github.com/xemantic/xemantic-typescript-compiler/commit/11993c3d97d49eb36c9cb813bad39956b5912df0)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 11993c3d97d49eb36c9cb813bad39956b5912df0

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 11993c3d97d4 | **3.34s** | 58,311 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 11993c3d97d4 | **3.12s** | 62,304 LOC/s | 46 |
| xtsc (cold JVM) | 11993c3d97d4 | 18.99s | 10,253 LOC/s | 46 |
| tsc | 6.0.3 | 7.98s | 24,389 LOC/s | 65 |
| tsgo | 7.0.2 | 1.23s | 158,940 LOC/s | 65 |
| xtsc-native | 11993c3d97d4 (AOT) | 5.49s | 35,464 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.39x FASTER than tsc; 2.73x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.12s**, **-6.4%** against `temurin26` on identical bytecode — 0.39× tsc, 2.55× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.38×** tsc and **15.50×** tsgo; tsc is **6.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.90s**.

Ahead-of-time (GraalVM native-image): **5.49s** — **0.69×** tsc, **4.48×** tsgo, **3.46×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 11993c3d97d4 | **4.01s** | 48,505 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 11993c3d97d4 | **3.68s** | 52,879 LOC/s | 46 |
| xtsc (cold JVM) | 11993c3d97d4 | 19.34s | 10,067 LOC/s | 46 |
| tsc | 6.0.3 | 9.81s | 19,839 LOC/s | 65 |
| tsgo | 7.0.2 | 1.76s | 110,437 LOC/s | 65 |
| xtsc-native | 11993c3d97d4 (AOT) | 6.38s | 30,531 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.44x FASTER than tsc; 2.28x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.68s**, **-8.3%** against `temurin26` on identical bytecode — 0.38× tsc, 2.09× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.97×** tsc and **10.97×** tsgo; tsc is **5.57×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.25s**.

Ahead-of-time (GraalVM native-image): **6.38s** — **0.65×** tsc, **3.62×** tsgo, **3.03×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
