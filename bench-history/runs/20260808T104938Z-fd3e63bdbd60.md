# Bench run 20260808T104938Z

- **xtsc revision**: [`fd3e63bdbd60`](https://github.com/xemantic/xemantic-typescript-compiler/commit/fd3e63bdbd601c2e8eddbd28bbdc006a00af81f0)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push fd3e63bdbd601c2e8eddbd28bbdc006a00af81f0

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | fd3e63bdbd60 | 25.96s | 7,499 LOC/s | 46 |
| tsc | 6.0.3 | 10.58s | 18,399 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.59s | 122,531 LOC/s | 65 |
| xtsc-native | fd3e63bdbd60 (AOT) | 11.56s | 16,841 LOC/s | 46 |

Relative (wall): xtsc is **2.45×** tsc and **16.34×** tsgo; tsc is **6.66×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.85s**.

Ahead-of-time (GraalVM native-image): **11.56s**, **2.25×** the JVM arm, **1.09×** tsc, **7.28×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | fd3e63bdbd60 | 29.55s | 6,590 LOC/s | 46 |
| tsc | 6.0.3 | 12.70s | 15,333 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.31s | 84,213 LOC/s | 65 |
| xtsc-native | fd3e63bdbd60 (AOT) | 13.92s | 13,988 LOC/s | 46 |

Relative (wall): xtsc is **2.33×** tsc and **12.78×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.42s**.

Ahead-of-time (GraalVM native-image): **13.92s**, **2.12×** the JVM arm, **1.10×** tsc, **6.02×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
