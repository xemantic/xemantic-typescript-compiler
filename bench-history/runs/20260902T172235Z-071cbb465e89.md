# Bench run 20260902T172235Z

- **xtsc revision**: [`071cbb465e89`](https://github.com/xemantic/xemantic-typescript-compiler/commit/071cbb465e89e31b74e7b6a5f90a2b78ffc4cb36)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 071cbb465e89e31b74e7b6a5f90a2b78ffc4cb36

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 071cbb465e89 | **3.55s** | 54,799 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 071cbb465e89 | **3.69s** | 52,736 LOC/s | 46 |
| xtsc (cold JVM) | 071cbb465e89 | 18.91s | 10,297 LOC/s | 46 |
| tsc | 6.0.3 | 8.20s | 23,755 LOC/s | 65 |
| tsgo | 7.0.2 | 1.23s | 158,423 LOC/s | 65 |
| xtsc-native | 071cbb465e89 (AOT) | 5.81s | 33,505 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.31x FASTER than tsc; 2.89x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.69s**, **+3.9%** against `temurin26` on identical bytecode — 0.45× tsc, 3.00× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.31×** tsc and **15.38×** tsgo; tsc is **6.67×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.79s**.

Ahead-of-time (GraalVM native-image): **5.81s** — **0.71×** tsc, **4.73×** tsgo, **3.25×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 071cbb465e89 | **4.37s** | 44,595 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 071cbb465e89 | **3.81s** | 51,076 LOC/s | 46 |
| xtsc (cold JVM) | 071cbb465e89 | 22.56s | 8,630 LOC/s | 46 |
| tsc | 6.0.3 | 9.94s | 19,595 LOC/s | 65 |
| tsgo | 7.0.2 | 1.84s | 105,529 LOC/s | 65 |
| xtsc-native | 071cbb465e89 (AOT) | 6.73s | 28,921 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.28x FASTER than tsc; 2.37x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.81s**, **-12.7%** against `temurin26` on identical bytecode — 0.38× tsc, 2.07× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.27×** tsc and **12.23×** tsgo; tsc is **5.39×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.46s**.

Ahead-of-time (GraalVM native-image): **6.73s** — **0.68×** tsc, **3.65×** tsgo, **3.35×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
