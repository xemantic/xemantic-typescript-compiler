# Bench run 20260803T074603Z

- **xtsc revision**: [`4f7cc8f352c0`](https://github.com/xemantic/xemantic-typescript-compiler/commit/4f7cc8f352c07b51196a5f9019b9a4de5d216482)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 4f7cc8f352c07b51196a5f9019b9a4de5d216482

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 4f7cc8f352c0 | 25.71s | 7,573 LOC/s | 46 |
| tsc | 6.0.3 | 10.77s | 18,079 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.70s | 114,665 LOC/s | 65 |
| xtsc-native | 4f7cc8f352c0 (AOT) | 10.76s | 18,103 LOC/s | 46 |

Relative (wall): xtsc is **2.39×** tsc and **15.14×** tsgo; tsc is **6.34×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.60s**.

Ahead-of-time (GraalVM native-image): **10.76s**, **2.39×** the JVM arm, **1.00×** tsc, **6.33×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 4f7cc8f352c0 | 30.13s | 6,462 LOC/s | 46 |
| tsc | 6.0.3 | 13.21s | 14,736 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.43s | 80,025 LOC/s | 65 |
| xtsc-native | 4f7cc8f352c0 (AOT) | 13.53s | 14,388 LOC/s | 46 |

Relative (wall): xtsc is **2.28×** tsc and **12.38×** tsgo; tsc is **5.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.00s**.

Ahead-of-time (GraalVM native-image): **13.53s**, **2.23×** the JVM arm, **1.02×** tsc, **5.56×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
