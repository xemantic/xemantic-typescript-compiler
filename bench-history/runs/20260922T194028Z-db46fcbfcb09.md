# Bench run 20260922T194028Z

- **xtsc revision**: [`db46fcbfcb09`](https://github.com/xemantic/xemantic-typescript-compiler/commit/db46fcbfcb09bf676efa62e11a84c4bd745988a9)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push db46fcbfcb09bf676efa62e11a84c4bd745988a9

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | db46fcbfcb09 | **5.72s** | 34,050 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | db46fcbfcb09 | **5.44s** | 35,764 LOC/s | 46 |
| xtsc (cold JVM) | db46fcbfcb09 | 26.80s | 7,265 LOC/s | 46 |
| tsc | 6.0.3 | 10.40s | 18,717 LOC/s | 65 |
| tsgo | 7.0.2 | 1.57s | 123,698 LOC/s | 65 |
| xtsc-native | db46fcbfcb09 (AOT) | 9.69s | 20,091 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.82x FASTER than tsc; 3.63x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.44s**, **-4.8%** against `temurin26` on identical bytecode — 0.52× tsc, 3.46× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.58×** tsc and **17.03×** tsgo; tsc is **6.61×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.68s**.

Ahead-of-time (GraalVM native-image): **9.69s** — **0.93×** tsc, **6.16×** tsgo, **2.77×** the COLD JVM arm and **1.69×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | db46fcbfcb09 | **6.81s** | 28,607 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | db46fcbfcb09 | **6.04s** | 32,251 LOC/s | 46 |
| xtsc (cold JVM) | db46fcbfcb09 | 27.70s | 7,028 LOC/s | 46 |
| tsc | 6.0.3 | 13.12s | 14,838 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,704 LOC/s | 65 |
| xtsc-native | db46fcbfcb09 (AOT) | 11.35s | 17,149 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.93x FASTER than tsc; 2.86x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.04s**, **-11.3%** against `temurin26` on identical bytecode — 0.46× tsc, 2.53× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.11×** tsc and **11.62×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.57s**.

Ahead-of-time (GraalVM native-image): **11.35s** — **0.87×** tsc, **4.76×** tsgo, **2.44×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
