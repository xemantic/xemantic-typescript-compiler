# Bench run 20260911T120637Z

- **xtsc revision**: [`f840db1c3c26`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f840db1c3c26c35585425b0c1ab895d5c2e543e7)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push f840db1c3c26c35585425b0c1ab895d5c2e543e7

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f840db1c3c26 | **5.60s** | 34,793 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f840db1c3c26 | **5.05s** | 38,516 LOC/s | 46 |
| xtsc (cold JVM) | f840db1c3c26 | 26.79s | 7,267 LOC/s | 46 |
| tsc | 6.0.3 | 11.07s | 17,583 LOC/s | 65 |
| tsgo | 7.0.2 | 1.71s | 113,661 LOC/s | 65 |
| xtsc-native | f840db1c3c26 (AOT) | 9.18s | 21,220 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.98x FASTER than tsc; 3.27x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.05s**, **-9.7%** against `temurin26` on identical bytecode — 0.46× tsc, 2.95× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.42×** tsc and **15.64×** tsgo; tsc is **6.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.67s**.

Ahead-of-time (GraalVM native-image): **9.18s** — **0.83×** tsc, **5.36×** tsgo, **2.92×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f840db1c3c26 | **6.65s** | 29,287 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f840db1c3c26 | **5.62s** | 34,669 LOC/s | 46 |
| xtsc (cold JVM) | f840db1c3c26 | 28.39s | 6,857 LOC/s | 46 |
| tsc | 6.0.3 | 13.47s | 14,454 LOC/s | 65 |
| tsgo | 7.0.2 | 2.49s | 78,068 LOC/s | 65 |
| xtsc-native | f840db1c3c26 (AOT) | 10.54s | 18,465 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.03x FASTER than tsc; 2.67x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.62s**, **-15.5%** against `temurin26` on identical bytecode — 0.42× tsc, 2.25× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.11×** tsc and **11.38×** tsgo; tsc is **5.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.25s**.

Ahead-of-time (GraalVM native-image): **10.54s** — **0.78×** tsc, **4.23×** tsgo, **2.69×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
