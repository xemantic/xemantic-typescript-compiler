# Bench run 20260915T203158Z

- **xtsc revision**: [`fe059ea99d65`](https://github.com/xemantic/xemantic-typescript-compiler/commit/fe059ea99d65de0823684627a47a71354b68ebfb)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push fe059ea99d65de0823684627a47a71354b68ebfb

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | fe059ea99d65 | **5.69s** | 34,218 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | fe059ea99d65 | **5.18s** | 37,551 LOC/s | 46 |
| xtsc (cold JVM) | fe059ea99d65 | 24.78s | 7,856 LOC/s | 46 |
| tsc | 6.0.3 | 10.30s | 18,910 LOC/s | 65 |
| tsgo | 7.0.2 | 1.58s | 122,917 LOC/s | 65 |
| xtsc-native | fe059ea99d65 (AOT) | 9.50s | 20,499 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.81x FASTER than tsc; 3.59x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.18s**, **-8.9%** against `temurin26` on identical bytecode — 0.50× tsc, 3.27× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.41×** tsc and **15.64×** tsgo; tsc is **6.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.67s**.

Ahead-of-time (GraalVM native-image): **9.50s** — **0.92×** tsc, **6.00×** tsgo, **2.61×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | fe059ea99d65 | **6.56s** | 29,662 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | fe059ea99d65 | **5.90s** | 33,000 LOC/s | 46 |
| xtsc (cold JVM) | fe059ea99d65 | 30.68s | 6,345 LOC/s | 46 |
| tsc | 6.0.3 | 12.64s | 15,408 LOC/s | 65 |
| tsgo | 7.0.2 | 2.29s | 85,022 LOC/s | 65 |
| xtsc-native | fe059ea99d65 (AOT) | 10.63s | 18,311 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.93x FASTER than tsc; 2.87x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.90s**, **-10.1%** against `temurin26` on identical bytecode — 0.47× tsc, 2.58× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.43×** tsc and **13.40×** tsgo; tsc is **5.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.54s**.

Ahead-of-time (GraalVM native-image): **10.63s** — **0.84×** tsc, **4.64×** tsgo, **2.89×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
