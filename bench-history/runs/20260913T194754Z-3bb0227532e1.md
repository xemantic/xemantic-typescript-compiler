# Bench run 20260913T194754Z

- **xtsc revision**: [`3bb0227532e1`](https://github.com/xemantic/xemantic-typescript-compiler/commit/3bb0227532e1c617cfefb18a5134d10f0257f439)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 3bb0227532e1c617cfefb18a5134d10f0257f439

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3bb0227532e1 | **5.73s** | 33,967 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3bb0227532e1 | **5.31s** | 36,680 LOC/s | 46 |
| xtsc (cold JVM) | 3bb0227532e1 | 25.91s | 7,513 LOC/s | 46 |
| tsc | 6.0.3 | 10.36s | 18,786 LOC/s | 65 |
| tsgo | 7.0.2 | 1.55s | 125,371 LOC/s | 65 |
| xtsc-native | 3bb0227532e1 (AOT) | 9.65s | 20,174 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.81x FASTER than tsc; 3.69x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.31s**, **-7.4%** against `temurin26` on identical bytecode — 0.51× tsc, 3.42× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.50×** tsc and **16.69×** tsgo; tsc is **6.67×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.79s**.

Ahead-of-time (GraalVM native-image): **9.65s** — **0.93×** tsc, **6.21×** tsgo, **2.68×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3bb0227532e1 | **6.63s** | 29,384 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3bb0227532e1 | **5.96s** | 32,690 LOC/s | 46 |
| xtsc (cold JVM) | 3bb0227532e1 | 28.06s | 6,938 LOC/s | 46 |
| tsc | 6.0.3 | 12.62s | 15,434 LOC/s | 65 |
| tsgo | 7.0.2 | 2.34s | 83,063 LOC/s | 65 |
| xtsc-native | 3bb0227532e1 (AOT) | 10.80s | 18,031 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.90x FASTER than tsc; 2.83x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.96s**, **-10.1%** against `temurin26` on identical bytecode — 0.47× tsc, 2.54× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.22×** tsc and **11.97×** tsgo; tsc is **5.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.91s**.

Ahead-of-time (GraalVM native-image): **10.80s** — **0.86×** tsc, **4.61×** tsgo, **2.60×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
