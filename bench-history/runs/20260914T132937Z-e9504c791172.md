# Bench run 20260914T132937Z

- **xtsc revision**: [`e9504c791172`](https://github.com/xemantic/xemantic-typescript-compiler/commit/e9504c791172d47605292217c266451a36acb1e1)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push e9504c791172d47605292217c266451a36acb1e1

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e9504c791172 | **5.60s** | 34,786 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e9504c791172 | **5.26s** | 37,001 LOC/s | 46 |
| xtsc (cold JVM) | e9504c791172 | 28.05s | 6,941 LOC/s | 46 |
| tsc | 6.0.3 | 10.77s | 18,073 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,448 LOC/s | 65 |
| xtsc-native | e9504c791172 (AOT) | 9.06s | 21,480 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.92x FASTER than tsc; 3.35x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.26s**, **-6.0%** against `temurin26` on identical bytecode — 0.49× tsc, 3.15× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.60×** tsc and **16.78×** tsgo; tsc is **6.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.94s**.

Ahead-of-time (GraalVM native-image): **9.06s** — **0.84×** tsc, **5.42×** tsgo, **3.09×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | e9504c791172 | **6.53s** | 29,821 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | e9504c791172 | **5.76s** | 33,825 LOC/s | 46 |
| xtsc (cold JVM) | e9504c791172 | 30.78s | 6,325 LOC/s | 46 |
| tsc | 6.0.3 | 13.03s | 14,944 LOC/s | 65 |
| tsgo | 7.0.2 | 2.37s | 82,326 LOC/s | 65 |
| xtsc-native | e9504c791172 (AOT) | 10.30s | 18,897 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.00x FASTER than tsc; 2.76x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.76s**, **-11.8%** against `temurin26` on identical bytecode — 0.44× tsc, 2.43× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.36×** tsc and **13.02×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.64s**.

Ahead-of-time (GraalVM native-image): **10.30s** — **0.79×** tsc, **4.36×** tsgo, **2.99×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
