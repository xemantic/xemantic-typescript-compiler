# Bench run 20260808T163154Z

- **xtsc revision**: [`f79956736a21`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f79956736a21164acfdd70dddd73db58e184ed12)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push f79956736a21164acfdd70dddd73db58e184ed12

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | f79956736a21 | 24.55s | 7,932 LOC/s | 46 |
| tsc | 6.0.3 | 11.12s | 17,515 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.68s | 115,825 LOC/s | 65 |
| xtsc-native | f79956736a21 (AOT) | 12.15s | 16,022 LOC/s | 46 |

Relative (wall): xtsc is **2.21×** tsc and **14.60×** tsgo; tsc is **6.61×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.42s**.

Ahead-of-time (GraalVM native-image): **12.15s**, **2.02×** the JVM arm, **1.09×** tsc, **7.23×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | f79956736a21 | 27.28s | 7,136 LOC/s | 46 |
| tsc | 6.0.3 | 13.89s | 14,016 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.48s | 78,350 LOC/s | 65 |
| xtsc-native | f79956736a21 (AOT) | 13.64s | 14,275 LOC/s | 46 |

Relative (wall): xtsc is **1.96×** tsc and **10.98×** tsgo; tsc is **5.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.14s**.

Ahead-of-time (GraalVM native-image): **13.64s**, **2.00×** the JVM arm, **0.98×** tsc, **5.49×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
