# Bench run 20260827T073632Z

- **xtsc revision**: [`45eb6d074547`](https://github.com/xemantic/xemantic-typescript-compiler/commit/45eb6d074547619579941db10ce221fa748898ef)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 45eb6d074547619579941db10ce221fa748898ef

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 45eb6d074547 | **2.72s** | 71,634 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 45eb6d074547 | **2.43s** | 80,223 LOC/s | 46 |
| xtsc (cold JVM) | 45eb6d074547 | 15.57s | 12,502 LOC/s | 46 |
| tsc | 6.0.3 | 6.75s | 28,827 LOC/s | 65 |
| tsgo | 7.0.2 | 1.06s | 183,162 LOC/s | 65 |
| xtsc-native | 45eb6d074547 (AOT) | 3.98s | 48,920 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.48x FASTER than tsc; 2.56x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.43s**, **-10.7%** against `temurin26` on identical bytecode — 0.36× tsc, 2.28× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.31×** tsc and **14.65×** tsgo; tsc is **6.35×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **15.45s**.

Ahead-of-time (GraalVM native-image): **3.98s** — **0.59×** tsc, **3.74×** tsgo, **3.91×** the COLD JVM arm and **1.46×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 45eb6d074547 | **3.24s** | 60,056 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 45eb6d074547 | **3.10s** | 62,786 LOC/s | 46 |
| xtsc (cold JVM) | 45eb6d074547 | 17.21s | 11,314 LOC/s | 46 |
| tsc | 6.0.3 | 8.15s | 23,881 LOC/s | 65 |
| tsgo | 7.0.2 | 1.60s | 121,461 LOC/s | 65 |
| xtsc-native | 45eb6d074547 (AOT) | 5.05s | 38,516 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.51x FASTER than tsc; 2.02x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.10s**, **-4.3%** against `temurin26` on identical bytecode — 0.38× tsc, 1.93× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.11×** tsc and **10.73×** tsgo; tsc is **5.09×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.12s**.

Ahead-of-time (GraalVM native-image): **5.05s** — **0.62×** tsc, **3.15×** tsgo, **3.40×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
