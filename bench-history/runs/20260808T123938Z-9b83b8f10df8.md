# Bench run 20260808T123938Z

- **xtsc revision**: [`9b83b8f10df8`](https://github.com/xemantic/xemantic-typescript-compiler/commit/9b83b8f10df8e08e98d9eadaaef1e2a59558f1d3)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 9b83b8f10df8e08e98d9eadaaef1e2a59558f1d3

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 9b83b8f10df8 | 26.27s | 7,411 LOC/s | 46 |
| tsc | 6.0.3 | 11.32s | 17,202 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.62s | 119,890 LOC/s | 65 |
| xtsc-native | 9b83b8f10df8 (AOT) | 10.81s | 18,017 LOC/s | 46 |

Relative (wall): xtsc is **2.32×** tsc and **16.18×** tsgo; tsc is **6.97×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.15s**.

Ahead-of-time (GraalVM native-image): **10.81s**, **2.43×** the JVM arm, **0.95×** tsc, **6.65×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 9b83b8f10df8 | 25.79s | 7,548 LOC/s | 46 |
| tsc | 6.0.3 | 13.12s | 14,844 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.38s | 81,704 LOC/s | 65 |
| xtsc-native | 9b83b8f10df8 (AOT) | 12.54s | 15,527 LOC/s | 46 |

Relative (wall): xtsc is **1.97×** tsc and **10.82×** tsgo; tsc is **5.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.68s**.

Ahead-of-time (GraalVM native-image): **12.54s**, **2.06×** the JVM arm, **0.96×** tsc, **5.26×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
