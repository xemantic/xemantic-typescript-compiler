# Bench run 20260829T204002Z

- **xtsc revision**: [`6529336107ef`](https://github.com/xemantic/xemantic-typescript-compiler/commit/6529336107ef0064296b16adaa4be5572fabe154)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 6529336107ef0064296b16adaa4be5572fabe154

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6529336107ef | **4.32s** | 45,111 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6529336107ef | **4.22s** | 46,116 LOC/s | 46 |
| xtsc (cold JVM) | 6529336107ef | 22.04s | 8,835 LOC/s | 46 |
| tsc | 6.0.3 | 11.31s | 17,218 LOC/s | 65 |
| tsgo | 7.0.2 | 1.77s | 110,001 LOC/s | 65 |
| xtsc-native | 6529336107ef (AOT) | 7.54s | 25,825 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.62x FASTER than tsc; 2.44x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.22s**, **-2.2%** against `temurin26` on identical bytecode — 0.37× tsc, 2.39× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.95×** tsc and **12.45×** tsgo; tsc is **6.39×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.92s**.

Ahead-of-time (GraalVM native-image): **7.54s** — **0.67×** tsc, **4.26×** tsgo, **2.92×** the COLD JVM arm and **1.75×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 6529336107ef | **5.45s** | 35,705 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 6529336107ef | **4.88s** | 39,938 LOC/s | 46 |
| xtsc (cold JVM) | 6529336107ef | 25.34s | 7,682 LOC/s | 46 |
| tsc | 6.0.3 | 13.67s | 14,238 LOC/s | 65 |
| tsgo | 7.0.2 | 2.45s | 79,567 LOC/s | 65 |
| xtsc-native | 6529336107ef (AOT) | 7.77s | 25,048 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.51x FASTER than tsc; 2.23x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.88s**, **-10.6%** against `temurin26` on identical bytecode — 0.36× tsc, 1.99× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.85×** tsc and **10.36×** tsgo; tsc is **5.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.21s**.

Ahead-of-time (GraalVM native-image): **7.77s** — **0.57×** tsc, **3.18×** tsgo, **3.26×** the COLD JVM arm and **1.43×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
