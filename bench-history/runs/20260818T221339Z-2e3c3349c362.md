# Bench run 20260818T221339Z

- **xtsc revision**: [`2e3c3349c362`](https://github.com/xemantic/xemantic-typescript-compiler/commit/2e3c3349c362c20451a7edcff743625ed49c73d8)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 2e3c3349c362c20451a7edcff743625ed49c73d8

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 2e3c3349c362 | **4.00s** | 48,687 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 2e3c3349c362 | **3.73s** | 52,240 LOC/s | 46 |
| xtsc (cold JVM) | 2e3c3349c362 | 23.56s | 8,265 LOC/s | 46 |
| tsc | 6.0.3 | 10.91s | 17,847 LOC/s | 65 |
| tsgo | 7.0.2 | 1.71s | 113,794 LOC/s | 65 |
| xtsc-native | 2e3c3349c362 (AOT) | 6.16s | 31,612 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.73x FASTER than tsc; 2.34x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.73s**, **-6.8%** against `temurin26` on identical bytecode — 0.34× tsc, 2.18× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.16×** tsc and **13.77×** tsgo; tsc is **6.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.43s**.

Ahead-of-time (GraalVM native-image): **6.16s** — **0.56×** tsc, **3.60×** tsgo, **3.82×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 2e3c3349c362 | **4.89s** | 39,775 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 2e3c3349c362 | **4.45s** | 43,792 LOC/s | 46 |
| xtsc (cold JVM) | 2e3c3349c362 | 24.86s | 7,831 LOC/s | 46 |
| tsc | 6.0.3 | 13.46s | 14,466 LOC/s | 65 |
| tsgo | 7.0.2 | 2.50s | 78,036 LOC/s | 65 |
| xtsc-native | 2e3c3349c362 (AOT) | 7.97s | 24,423 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.75x FASTER than tsc; 1.96x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.45s**, **-9.2%** against `temurin26` on identical bytecode — 0.33× tsc, 1.78× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.85×** tsc and **9.96×** tsgo; tsc is **5.39×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.72s**.

Ahead-of-time (GraalVM native-image): **7.97s** — **0.59×** tsc, **3.20×** tsgo, **3.12×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
