# Bench run 20260807T010036Z

- **xtsc revision**: [`31e297e82b5f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/31e297e82b5f40310c7ef01543fd01d312425326)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 31e297e82b5f40310c7ef01543fd01d312425326

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 31e297e82b5f | 18.73s | 10,395 LOC/s | 46 |
| tsc | 6.0.3 | 8.21s | 23,700 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.27s | 152,947 LOC/s | 65 |
| xtsc-native | 31e297e82b5f (AOT) | 8.22s | 23,689 LOC/s | 46 |

Relative (wall): xtsc is **2.28×** tsc and **14.71×** tsgo; tsc is **6.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.64s**.

Ahead-of-time (GraalVM native-image): **8.22s**, **2.28×** the JVM arm, **1.00×** tsc, **6.46×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 31e297e82b5f | 21.68s | 8,981 LOC/s | 46 |
| tsc | 6.0.3 | 10.11s | 19,252 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.88s | 103,509 LOC/s | 65 |
| xtsc-native | 31e297e82b5f (AOT) | 9.12s | 21,351 LOC/s | 46 |

Relative (wall): xtsc is **2.14×** tsc and **11.52×** tsgo; tsc is **5.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.58s**.

Ahead-of-time (GraalVM native-image): **9.12s**, **2.38×** the JVM arm, **0.90×** tsc, **4.85×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
