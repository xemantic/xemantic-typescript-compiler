# Bench run 20260914T084857Z

- **xtsc revision**: [`fb8e86d0ee48`](https://github.com/xemantic/xemantic-typescript-compiler/commit/fb8e86d0ee48c87cd528cd4ec326c8d7b7aa4c43)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push fb8e86d0ee48c87cd528cd4ec326c8d7b7aa4c43

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | fb8e86d0ee48 | **6.03s** | 32,299 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | fb8e86d0ee48 | **5.59s** | 34,817 LOC/s | 46 |
| xtsc (cold JVM) | fb8e86d0ee48 | 29.11s | 6,688 LOC/s | 46 |
| tsc | 6.0.3 | 11.31s | 17,213 LOC/s | 65 |
| tsgo | 7.0.2 | 1.76s | 110,500 LOC/s | 65 |
| xtsc-native | fb8e86d0ee48 (AOT) | 9.83s | 19,814 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.88x FASTER than tsc; 3.42x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.59s**, **-7.2%** against `temurin26` on identical bytecode — 0.49× tsc, 3.17× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.57×** tsc and **16.52×** tsgo; tsc is **6.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.99s**.

Ahead-of-time (GraalVM native-image): **9.83s** — **0.87×** tsc, **5.58×** tsgo, **2.96×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | fb8e86d0ee48 | **7.11s** | 27,384 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | fb8e86d0ee48 | **6.21s** | 31,347 LOC/s | 46 |
| xtsc (cold JVM) | fb8e86d0ee48 | 29.69s | 6,557 LOC/s | 46 |
| tsc | 6.0.3 | 13.53s | 14,389 LOC/s | 65 |
| tsgo | 7.0.2 | 2.44s | 79,665 LOC/s | 65 |
| xtsc-native | fb8e86d0ee48 (AOT) | 11.08s | 17,572 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.90x FASTER than tsc; 2.91x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.21s**, **-12.6%** against `temurin26` on identical bytecode — 0.46× tsc, 2.54× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.19×** tsc and **12.15×** tsgo; tsc is **5.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.56s**.

Ahead-of-time (GraalVM native-image): **11.08s** — **0.82×** tsc, **4.53×** tsgo, **2.68×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
