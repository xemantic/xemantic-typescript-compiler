# Bench run 20260808T125127Z

- **xtsc revision**: [`68818a707dcb`](https://github.com/xemantic/xemantic-typescript-compiler/commit/68818a707dcb30b53ac480ab6f2aad52fcc16d40)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 68818a707dcb30b53ac480ab6f2aad52fcc16d40

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 68818a707dcb | 20.74s | 9,388 LOC/s | 46 |
| tsc | 6.0.3 | 7.94s | 24,534 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.23s | 158,940 LOC/s | 65 |
| xtsc-native | 68818a707dcb (AOT) | 7.20s | 27,060 LOC/s | 46 |

Relative (wall): xtsc is **2.61×** tsc and **16.93×** tsgo; tsc is **6.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.65s**.

Ahead-of-time (GraalVM native-image): **7.20s**, **2.88×** the JVM arm, **0.91×** tsc, **5.87×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 68818a707dcb | 21.91s | 8,888 LOC/s | 46 |
| tsc | 6.0.3 | 9.55s | 20,383 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.81s | 107,451 LOC/s | 65 |
| xtsc-native | 68818a707dcb (AOT) | 8.61s | 22,626 LOC/s | 46 |

Relative (wall): xtsc is **2.29×** tsc and **12.09×** tsgo; tsc is **5.27×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.82s**.

Ahead-of-time (GraalVM native-image): **8.61s**, **2.55×** the JVM arm, **0.90×** tsc, **4.75×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
