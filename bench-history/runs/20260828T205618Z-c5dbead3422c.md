# Bench run 20260828T205618Z

- **xtsc revision**: [`c5dbead3422c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c5dbead3422c82e5f43eb767c0708103245a4758)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push c5dbead3422c82e5f43eb767c0708103245a4758

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c5dbead3422c | **4.40s** | 44,250 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c5dbead3422c | **3.95s** | 49,329 LOC/s | 46 |
| xtsc (cold JVM) | c5dbead3422c | 22.51s | 8,649 LOC/s | 46 |
| tsc | 6.0.3 | 10.79s | 18,053 LOC/s | 65 |
| tsgo | 7.0.2 | 1.70s | 114,396 LOC/s | 65 |
| xtsc-native | c5dbead3422c (AOT) | 6.99s | 27,862 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.45x FASTER than tsc; 2.59x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.95s**, **-10.3%** against `temurin26` on identical bytecode — 0.37× tsc, 2.32× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.09×** tsc and **13.23×** tsgo; tsc is **6.34×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.40s**.

Ahead-of-time (GraalVM native-image): **6.99s** — **0.65×** tsc, **4.11×** tsgo, **3.22×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c5dbead3422c | **5.26s** | 37,043 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c5dbead3422c | **4.72s** | 41,276 LOC/s | 46 |
| xtsc (cold JVM) | c5dbead3422c | 25.44s | 7,652 LOC/s | 46 |
| tsc | 6.0.3 | 13.40s | 14,533 LOC/s | 65 |
| tsgo | 7.0.2 | 2.46s | 79,050 LOC/s | 65 |
| xtsc-native | c5dbead3422c (AOT) | 8.11s | 24,022 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.55x FASTER than tsc; 2.13x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.72s**, **-10.3%** against `temurin26` on identical bytecode — 0.35× tsc, 1.92× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.90×** tsc and **10.33×** tsgo; tsc is **5.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.32s**.

Ahead-of-time (GraalVM native-image): **8.11s** — **0.60×** tsc, **3.29×** tsgo, **3.14×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
