# Bench run 20260915T151131Z

- **xtsc revision**: [`16baa565332c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/16baa565332c96456e045996514bf954a4e5ca40)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 16baa565332c96456e045996514bf954a4e5ca40

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 16baa565332c | **6.25s** | 31,157 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 16baa565332c | **5.62s** | 34,669 LOC/s | 46 |
| xtsc (cold JVM) | 16baa565332c | 25.96s | 7,500 LOC/s | 46 |
| tsc | 6.0.3 | 11.45s | 16,998 LOC/s | 65 |
| tsgo | 7.0.2 | 1.77s | 109,815 LOC/s | 65 |
| xtsc-native | 16baa565332c (AOT) | 9.99s | 19,493 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.83x FASTER than tsc; 3.52x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.62s**, **-10.1%** against `temurin26` on identical bytecode — 0.49× tsc, 3.17× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.27×** tsc and **14.64×** tsgo; tsc is **6.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.84s**.

Ahead-of-time (GraalVM native-image): **9.99s** — **0.87×** tsc, **5.63×** tsgo, **2.60×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 16baa565332c | **7.20s** | 27,053 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 16baa565332c | **6.25s** | 31,137 LOC/s | 46 |
| xtsc (cold JVM) | 16baa565332c | 32.42s | 6,004 LOC/s | 46 |
| tsc | 6.0.3 | 13.85s | 14,057 LOC/s | 65 |
| tsgo | 7.0.2 | 2.54s | 76,805 LOC/s | 65 |
| xtsc-native | 16baa565332c (AOT) | 11.53s | 16,883 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.92x FASTER than tsc; 2.84x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.25s**, **-13.1%** against `temurin26` on identical bytecode — 0.45× tsc, 2.47× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.34×** tsc and **12.79×** tsgo; tsc is **5.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **32.28s**.

Ahead-of-time (GraalVM native-image): **11.53s** — **0.83×** tsc, **4.55×** tsgo, **2.81×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
