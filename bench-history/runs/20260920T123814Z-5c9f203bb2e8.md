# Bench run 20260920T123814Z

- **xtsc revision**: [`5c9f203bb2e8`](https://github.com/xemantic/xemantic-typescript-compiler/commit/5c9f203bb2e8e3ee48e7e2c781502d38c927899a)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 5c9f203bb2e8e3ee48e7e2c781502d38c927899a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5c9f203bb2e8 | **5.70s** | 34,134 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5c9f203bb2e8 | **5.24s** | 37,178 LOC/s | 46 |
| xtsc (cold JVM) | 5c9f203bb2e8 | 26.64s | 7,308 LOC/s | 46 |
| tsc | 6.0.3 | 10.60s | 18,373 LOC/s | 65 |
| tsgo | 7.0.2 | 1.68s | 116,240 LOC/s | 65 |
| xtsc-native | 5c9f203bb2e8 (AOT) | 9.19s | 21,183 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.86x FASTER than tsc; 3.41x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.24s**, **-8.2%** against `temurin26` on identical bytecode — 0.49× tsc, 3.13× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.51×** tsc and **15.91×** tsgo; tsc is **6.33×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.53s**.

Ahead-of-time (GraalVM native-image): **9.19s** — **0.87×** tsc, **5.49×** tsgo, **2.90×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5c9f203bb2e8 | **6.69s** | 29,116 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5c9f203bb2e8 | **6.16s** | 31,628 LOC/s | 46 |
| xtsc (cold JVM) | 5c9f203bb2e8 | 28.77s | 6,766 LOC/s | 46 |
| tsc | 6.0.3 | 13.26s | 14,684 LOC/s | 65 |
| tsgo | 7.0.2 | 2.48s | 78,414 LOC/s | 65 |
| xtsc-native | 5c9f203bb2e8 (AOT) | 10.99s | 17,722 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.98x FASTER than tsc; 2.69x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.16s**, **-7.9%** against `temurin26` on identical bytecode — 0.46× tsc, 2.48× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.17×** tsc and **11.59×** tsgo; tsc is **5.34×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.64s**.

Ahead-of-time (GraalVM native-image): **10.99s** — **0.83×** tsc, **4.42×** tsgo, **2.62×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
