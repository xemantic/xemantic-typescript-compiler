# Bench run 20260920T090453Z

- **xtsc revision**: [`8b79d3a0d8f2`](https://github.com/xemantic/xemantic-typescript-compiler/commit/8b79d3a0d8f2efad16e856f636f49660c86e570c)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 8b79d3a0d8f2efad16e856f636f49660c86e570c

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8b79d3a0d8f2 | **5.71s** | 34,122 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8b79d3a0d8f2 | **5.31s** | 36,680 LOC/s | 46 |
| xtsc (cold JVM) | 8b79d3a0d8f2 | 26.17s | 7,439 LOC/s | 46 |
| tsc | 6.0.3 | 10.85s | 17,948 LOC/s | 65 |
| tsgo | 7.0.2 | 1.64s | 119,083 LOC/s | 65 |
| xtsc-native | 8b79d3a0d8f2 (AOT) | 9.39s | 20,737 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.90x FASTER than tsc; 3.49x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.31s**, **-7.0%** against `temurin26` on identical bytecode — 0.49× tsc, 3.25× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.41×** tsc and **16.01×** tsgo; tsc is **6.63×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.06s**.

Ahead-of-time (GraalVM native-image): **9.39s** — **0.87×** tsc, **5.74×** tsgo, **2.79×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8b79d3a0d8f2 | **6.68s** | 29,133 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8b79d3a0d8f2 | **5.88s** | 33,118 LOC/s | 46 |
| xtsc (cold JVM) | 8b79d3a0d8f2 | 30.22s | 6,443 LOC/s | 46 |
| tsc | 6.0.3 | 13.27s | 14,669 LOC/s | 65 |
| tsgo | 7.0.2 | 2.42s | 80,588 LOC/s | 65 |
| xtsc-native | 8b79d3a0d8f2 (AOT) | 10.72s | 18,165 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.99x FASTER than tsc; 2.77x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.88s**, **-12.0%** against `temurin26` on identical bytecode — 0.44× tsc, 2.43× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.28×** tsc and **12.51×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.08s**.

Ahead-of-time (GraalVM native-image): **10.72s** — **0.81×** tsc, **4.44×** tsgo, **2.82×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
