# Bench run 20260919T235942Z

- **xtsc revision**: [`7b4232902df6`](https://github.com/xemantic/xemantic-typescript-compiler/commit/7b4232902df615591714be45c46d9fc9173fe3ad)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 7b4232902df615591714be45c46d9fc9173fe3ad

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7b4232902df6 | **6.20s** | 31,398 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7b4232902df6 | **5.32s** | 36,618 LOC/s | 46 |
| xtsc (cold JVM) | 7b4232902df6 | 27.95s | 6,966 LOC/s | 46 |
| tsc | 6.0.3 | 11.08s | 17,573 LOC/s | 65 |
| tsgo | 7.0.2 | 1.73s | 112,740 LOC/s | 65 |
| xtsc-native | 7b4232902df6 (AOT) | 9.45s | 20,601 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.79x FASTER than tsc; 3.59x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.32s**, **-14.3%** against `temurin26` on identical bytecode — 0.48× tsc, 3.08× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.52×** tsc and **16.18×** tsgo; tsc is **6.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.83s**.

Ahead-of-time (GraalVM native-image): **9.45s** — **0.85×** tsc, **5.47×** tsgo, **2.96×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7b4232902df6 | **7.12s** | 27,326 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7b4232902df6 | **6.07s** | 32,091 LOC/s | 46 |
| xtsc (cold JVM) | 7b4232902df6 | 29.41s | 6,620 LOC/s | 46 |
| tsc | 6.0.3 | 13.46s | 14,464 LOC/s | 65 |
| tsgo | 7.0.2 | 2.47s | 78,890 LOC/s | 65 |
| xtsc-native | 7b4232902df6 (AOT) | 10.90s | 17,856 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.89x FASTER than tsc; 2.89x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.07s**, **-14.8%** against `temurin26` on identical bytecode — 0.45× tsc, 2.46× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.18×** tsc and **11.92×** tsgo; tsc is **5.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.28s**.

Ahead-of-time (GraalVM native-image): **10.90s** — **0.81×** tsc, **4.42×** tsgo, **2.70×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
