# Bench run 20260817T235129Z

- **xtsc revision**: [`3f3efd2e9c8c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/3f3efd2e9c8c60ffe24f80ad4fe95db71be8fa52)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 3f3efd2e9c8c60ffe24f80ad4fe95db71be8fa52

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3f3efd2e9c8c | **3.67s** | 52,980 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3f3efd2e9c8c | **3.59s** | 54,279 LOC/s | 46 |
| xtsc (cold JVM) | 3f3efd2e9c8c | 23.33s | 8,344 LOC/s | 46 |
| tsc | 6.0.3 | 10.27s | 18,965 LOC/s | 65 |
| tsgo | 7.0.2 | 1.64s | 118,648 LOC/s | 65 |
| xtsc-native | 3f3efd2e9c8c (AOT) | 5.77s | 33,726 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.79x FASTER than tsc; 2.24x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.59s**, **-2.4%** against `temurin26` on identical bytecode — 0.35× tsc, 2.19× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.27×** tsc and **14.22×** tsgo; tsc is **6.26×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.23s**.

Ahead-of-time (GraalVM native-image): **5.77s** — **0.56×** tsc, **3.52×** tsgo, **4.04×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3f3efd2e9c8c | **4.52s** | 43,037 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3f3efd2e9c8c | **4.17s** | 46,713 LOC/s | 46 |
| xtsc (cold JVM) | 3f3efd2e9c8c | 25.01s | 7,786 LOC/s | 46 |
| tsc | 6.0.3 | 12.41s | 15,685 LOC/s | 65 |
| tsgo | 7.0.2 | 2.39s | 81,397 LOC/s | 65 |
| xtsc-native | 3f3efd2e9c8c (AOT) | 7.03s | 27,695 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.74x FASTER than tsc; 1.89x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.17s**, **-7.9%** against `temurin26` on identical bytecode — 0.34× tsc, 1.74× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.01×** tsc and **10.45×** tsgo; tsc is **5.19×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.87s**.

Ahead-of-time (GraalVM native-image): **7.03s** — **0.57×** tsc, **2.94×** tsgo, **3.56×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
