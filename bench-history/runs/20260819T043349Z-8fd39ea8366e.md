# Bench run 20260819T043349Z

- **xtsc revision**: [`8fd39ea8366e`](https://github.com/xemantic/xemantic-typescript-compiler/commit/8fd39ea8366e9f4039ca615a1c8a6a7791fc4fcc)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 8fd39ea8366e9f4039ca615a1c8a6a7791fc4fcc

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8fd39ea8366e | **4.13s** | 47,097 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8fd39ea8366e | **3.88s** | 50,167 LOC/s | 46 |
| xtsc (cold JVM) | 8fd39ea8366e | 22.58s | 8,621 LOC/s | 46 |
| tsc | 6.0.3 | 11.26s | 17,296 LOC/s | 65 |
| tsgo | 7.0.2 | 1.72s | 113,264 LOC/s | 65 |
| xtsc-native | 8fd39ea8366e (AOT) | 6.33s | 30,734 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.72x FASTER than tsc; 2.40x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.88s**, **-6.1%** against `temurin26` on identical bytecode — 0.34× tsc, 2.26× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.01×** tsc and **13.14×** tsgo; tsc is **6.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.47s**.

Ahead-of-time (GraalVM native-image): **6.33s** — **0.56×** tsc, **3.69×** tsgo, **3.56×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8fd39ea8366e | **4.89s** | 39,832 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8fd39ea8366e | **4.48s** | 43,440 LOC/s | 46 |
| xtsc (cold JVM) | 8fd39ea8366e | 24.47s | 7,956 LOC/s | 46 |
| tsc | 6.0.3 | 13.45s | 14,474 LOC/s | 65 |
| tsgo | 7.0.2 | 2.45s | 79,502 LOC/s | 65 |
| xtsc-native | 8fd39ea8366e (AOT) | 7.88s | 24,692 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.75x FASTER than tsc; 2.00x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.48s**, **-8.3%** against `temurin26` on identical bytecode — 0.33× tsc, 1.83× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.82×** tsc and **9.99×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.35s**.

Ahead-of-time (GraalVM native-image): **7.88s** — **0.59×** tsc, **3.22×** tsgo, **3.10×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
