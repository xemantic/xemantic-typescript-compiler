# Bench run 20260811T011729Z

- **xtsc revision**: [`94f1b6b4a7b3`](https://github.com/xemantic/xemantic-typescript-compiler/commit/94f1b6b4a7b32f284c8b27bcd900e7883f99bf18)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 94f1b6b4a7b32f284c8b27bcd900e7883f99bf18

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 94f1b6b4a7b3 | **4.46s** | 43,616 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 94f1b6b4a7b3 | **4.24s** | 45,952 LOC/s | 46 |
| xtsc (cold JVM) | 94f1b6b4a7b3 | 25.48s | 7,641 LOC/s | 46 |
| tsc | 6.0.3 | 10.46s | 18,615 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.57s | 124,093 LOC/s | 65 |
| xtsc-native | 94f1b6b4a7b3 (AOT) | 6.52s | 29,848 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.34x FASTER than tsc; 2.85x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.24s**, **-5.1%** against `temurin26` on identical bytecode — 0.41× tsc, 2.70× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.44×** tsc and **16.24×** tsgo; tsc is **6.67×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.36s**.

Ahead-of-time (GraalVM native-image): **6.52s** — **0.62×** tsc, **4.16×** tsgo, **3.91×** the COLD JVM arm and **1.46×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 94f1b6b4a7b3 | **5.07s** | 38,395 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 94f1b6b4a7b3 | **4.68s** | 41,602 LOC/s | 46 |
| xtsc (cold JVM) | 94f1b6b4a7b3 | 25.00s | 7,789 LOC/s | 46 |
| tsc | 6.0.3 | 12.22s | 15,930 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.24s | 86,959 LOC/s | 65 |
| xtsc-native | 94f1b6b4a7b3 (AOT) | 8.14s | 23,919 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.41x FASTER than tsc; 2.26x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.68s**, **-7.7%** against `temurin26` on identical bytecode — 0.38× tsc, 2.09× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.05×** tsc and **11.16×** tsgo; tsc is **5.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.88s**.

Ahead-of-time (GraalVM native-image): **8.14s** — **0.67×** tsc, **3.64×** tsgo, **3.07×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
