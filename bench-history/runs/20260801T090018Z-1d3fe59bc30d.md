# Bench run 20260801T090018Z

- **xtsc revision**: [`1d3fe59bc30d`](https://github.com/xemantic/xemantic-typescript-compiler/commit/1d3fe59bc30deb2d4139aed2cfd98713c4f8446e)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 1d3fe59bc30deb2d4139aed2cfd98713c4f8446e

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 1d3fe59bc30d | 21.34s | 9,122 LOC/s | 46 |
| tsc | 6.0.3 | 7.84s | 24,837 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.20s | 162,116 LOC/s | 65 |
| xtsc-native | 1d3fe59bc30d (AOT) | 9.02s | 21,587 LOC/s | 46 |

Relative (wall): xtsc is **2.72×** tsc and **17.77×** tsgo; tsc is **6.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.26s**.

Ahead-of-time (GraalVM native-image): **9.02s**, **2.37×** the JVM arm, **1.15×** tsc, **7.51×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 1d3fe59bc30d | 23.39s | 8,325 LOC/s | 46 |
| tsc | 6.0.3 | 9.63s | 20,216 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.76s | 110,312 LOC/s | 65 |
| xtsc-native | 1d3fe59bc30d (AOT) | 11.84s | 16,451 LOC/s | 46 |

Relative (wall): xtsc is **2.43×** tsc and **13.25×** tsgo; tsc is **5.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.30s**.

Ahead-of-time (GraalVM native-image): **11.84s**, **1.98×** the JVM arm, **1.23×** tsc, **6.71×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
