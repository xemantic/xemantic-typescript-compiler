# Bench run 20260824T010252Z

- **xtsc revision**: [`f1a311c5efc7`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f1a311c5efc714f3a3bba340e2f900b17bb3ae95)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push f1a311c5efc714f3a3bba340e2f900b17bb3ae95

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f1a311c5efc7 | **2.85s** | 68,316 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f1a311c5efc7 | **2.70s** | 72,218 LOC/s | 46 |
| xtsc (cold JVM) | f1a311c5efc7 | 16.99s | 11,457 LOC/s | 46 |
| tsc | 6.0.3 | 8.59s | 22,666 LOC/s | 65 |
| tsgo | 7.0.2 | 1.34s | 145,626 LOC/s | 65 |
| xtsc-native | f1a311c5efc7 (AOT) | 4.70s | 41,390 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 3.01x FASTER than tsc; 2.13x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.70s**, **-5.4%** against `temurin26` on identical bytecode — 0.31× tsc, 2.02× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.98×** tsc and **12.71×** tsgo; tsc is **6.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **16.90s**.

Ahead-of-time (GraalVM native-image): **4.70s** — **0.55×** tsc, **3.52×** tsgo, **3.61×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f1a311c5efc7 | **3.56s** | 54,645 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f1a311c5efc7 | **3.22s** | 60,504 LOC/s | 46 |
| xtsc (cold JVM) | f1a311c5efc7 | 19.02s | 10,236 LOC/s | 46 |
| tsc | 6.0.3 | 10.11s | 19,260 LOC/s | 65 |
| tsgo | 7.0.2 | 1.88s | 103,509 LOC/s | 65 |
| xtsc-native | f1a311c5efc7 (AOT) | 5.33s | 36,495 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.84x FASTER than tsc; 1.89x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.22s**, **-9.7%** against `temurin26` on identical bytecode — 0.32× tsc, 1.71× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.88×** tsc and **10.11×** tsgo; tsc is **5.37×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.92s**.

Ahead-of-time (GraalVM native-image): **5.33s** — **0.53×** tsc, **2.84×** tsgo, **3.57×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
