# Bench run 20260801T145111Z

- **xtsc revision**: [`e19b32d7a28e`](https://github.com/xemantic/xemantic-typescript-compiler/commit/e19b32d7a28e2fe0528d5866dff373ccabc1b141)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push e19b32d7a28e2fe0528d5866dff373ccabc1b141

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | e19b32d7a28e | 32.46s | 5,998 LOC/s | 46 |
| tsc | 6.0.3 | 11.73s | 16,592 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.78s | 109,383 LOC/s | 65 |
| xtsc-native | e19b32d7a28e (AOT) | 12.81s | 15,199 LOC/s | 46 |

Relative (wall): xtsc is **2.77×** tsc and **18.24×** tsgo; tsc is **6.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **32.33s**.

Ahead-of-time (GraalVM native-image): **12.81s**, **2.53×** the JVM arm, **1.09×** tsc, **7.20×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | e19b32d7a28e | 30.25s | 6,436 LOC/s | 46 |
| tsc | 6.0.3 | 14.13s | 13,779 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.58s | 75,465 LOC/s | 65 |
| xtsc-native | e19b32d7a28e (AOT) | 14.63s | 13,311 LOC/s | 46 |

Relative (wall): xtsc is **2.14×** tsc and **11.73×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.14s**.

Ahead-of-time (GraalVM native-image): **14.63s**, **2.07×** the JVM arm, **1.04×** tsc, **5.67×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
