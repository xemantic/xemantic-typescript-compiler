# Bench run 20260808T000546Z

- **xtsc revision**: [`c3f96cba8ef3`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c3f96cba8ef3d71b6a3dbd1b437e85d2cc5505ec)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push c3f96cba8ef3d71b6a3dbd1b437e85d2cc5505ec

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | c3f96cba8ef3 | 25.50s | 7,635 LOC/s | 46 |
| tsc | 6.0.3 | 10.70s | 18,199 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.69s | 115,276 LOC/s | 65 |
| xtsc-native | c3f96cba8ef3 (AOT) | 10.48s | 18,580 LOC/s | 46 |

Relative (wall): xtsc is **2.38×** tsc and **15.10×** tsgo; tsc is **6.33×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.39s**.

Ahead-of-time (GraalVM native-image): **10.48s**, **2.43×** the JVM arm, **0.98×** tsc, **6.20×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | c3f96cba8ef3 | 28.59s | 6,810 LOC/s | 46 |
| tsc | 6.0.3 | 13.05s | 14,923 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.40s | 80,957 LOC/s | 65 |
| xtsc-native | c3f96cba8ef3 (AOT) | 11.47s | 16,974 LOC/s | 46 |

Relative (wall): xtsc is **2.19×** tsc and **11.89×** tsgo; tsc is **5.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.48s**.

Ahead-of-time (GraalVM native-image): **11.47s**, **2.49×** the JVM arm, **0.88×** tsc, **4.77×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
