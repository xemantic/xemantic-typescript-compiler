# Bench run 20260911T154042Z

- **xtsc revision**: [`cab774f286f2`](https://github.com/xemantic/xemantic-typescript-compiler/commit/cab774f286f268e3482d056a0159ed1ad4ef06d5)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push cab774f286f268e3482d056a0159ed1ad4ef06d5

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cab774f286f2 | **5.58s** | 34,892 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cab774f286f2 | **5.13s** | 37,975 LOC/s | 46 |
| xtsc (cold JVM) | cab774f286f2 | 25.10s | 7,758 LOC/s | 46 |
| tsc | 6.0.3 | 10.83s | 17,978 LOC/s | 65 |
| tsgo | 7.0.2 | 1.63s | 119,302 LOC/s | 65 |
| xtsc-native | cab774f286f2 (AOT) | 9.38s | 20,754 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.94x FASTER than tsc; 3.42x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.13s**, **-8.1%** against `temurin26` on identical bytecode — 0.47× tsc, 3.14× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.32×** tsc and **15.38×** tsgo; tsc is **6.64×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.98s**.

Ahead-of-time (GraalVM native-image): **9.38s** — **0.87×** tsc, **5.75×** tsgo, **2.68×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cab774f286f2 | **6.52s** | 29,866 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cab774f286f2 | **5.92s** | 32,883 LOC/s | 46 |
| xtsc (cold JVM) | cab774f286f2 | 28.99s | 6,716 LOC/s | 46 |
| tsc | 6.0.3 | 13.15s | 14,800 LOC/s | 65 |
| tsgo | 7.0.2 | 2.37s | 82,326 LOC/s | 65 |
| xtsc-native | cab774f286f2 (AOT) | 10.68s | 18,228 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.02x FASTER than tsc; 2.76x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.92s**, **-9.2%** against `temurin26` on identical bytecode — 0.45× tsc, 2.50× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.20×** tsc and **12.26×** tsgo; tsc is **5.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.87s**.

Ahead-of-time (GraalVM native-image): **10.68s** — **0.81×** tsc, **4.52×** tsgo, **2.71×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
