# Bench run 20260817T214046Z

- **xtsc revision**: [`8c09b213204a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/8c09b213204ab7224f9080cf6c47eaaa4be1af48)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 8c09b213204ab7224f9080cf6c47eaaa4be1af48

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8c09b213204a | **4.33s** | 44,955 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8c09b213204a | **4.13s** | 47,097 LOC/s | 46 |
| xtsc (cold JVM) | 8c09b213204a | 24.79s | 7,855 LOC/s | 46 |
| tsc | 6.0.3 | 11.37s | 17,128 LOC/s | 65 |
| tsgo | 7.0.2 | 1.72s | 113,462 LOC/s | 65 |
| xtsc-native | 8c09b213204a (AOT) | 6.77s | 28,763 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.62x FASTER than tsc; 2.52x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.13s**, **-4.5%** against `temurin26` on identical bytecode — 0.36× tsc, 2.41× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.18×** tsc and **14.44×** tsgo; tsc is **6.62×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.67s**.

Ahead-of-time (GraalVM native-image): **6.77s** — **0.60×** tsc, **3.94×** tsgo, **3.66×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8c09b213204a | **5.19s** | 37,500 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8c09b213204a | **4.69s** | 41,549 LOC/s | 46 |
| xtsc (cold JVM) | 8c09b213204a | 27.86s | 6,988 LOC/s | 46 |
| tsc | 6.0.3 | 13.87s | 14,035 LOC/s | 65 |
| tsgo | 7.0.2 | 2.36s | 82,430 LOC/s | 65 |
| xtsc-native | 8c09b213204a (AOT) | 8.19s | 23,770 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.67x FASTER than tsc; 2.20x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.69s**, **-9.7%** against `temurin26` on identical bytecode — 0.34× tsc, 1.98× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.01×** tsc and **11.80×** tsgo; tsc is **5.87×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.72s**.

Ahead-of-time (GraalVM native-image): **8.19s** — **0.59×** tsc, **3.47×** tsgo, **3.40×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
