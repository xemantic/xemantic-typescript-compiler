# Bench run 20260924T175803Z

- **xtsc revision**: [`da113a40a938`](https://github.com/xemantic/xemantic-typescript-compiler/commit/da113a40a9383ecc88874cdb778787b027c5895c)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push da113a40a9383ecc88874cdb778787b027c5895c

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | da113a40a938 | **5.95s** | 32,717 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | da113a40a938 | **5.60s** | 34,786 LOC/s | 46 |
| xtsc (cold JVM) | da113a40a938 | 27.04s | 7,200 LOC/s | 46 |
| tsc | 6.0.3 | 10.86s | 17,921 LOC/s | 65 |
| tsgo | 7.0.2 | 1.72s | 113,198 LOC/s | 65 |
| xtsc-native | da113a40a938 (AOT) | 9.65s | 20,168 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.83x FASTER than tsc; 3.46x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.60s**, **-5.9%** against `temurin26` on identical bytecode — 0.52× tsc, 3.25× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.49×** tsc and **15.72×** tsgo; tsc is **6.32×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.93s**.

Ahead-of-time (GraalVM native-image): **9.65s** — **0.89×** tsc, **5.61×** tsgo, **2.80×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | da113a40a938 | **6.95s** | 28,010 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | da113a40a938 | **6.18s** | 31,500 LOC/s | 46 |
| xtsc (cold JVM) | da113a40a938 | 30.42s | 6,400 LOC/s | 46 |
| tsc | 6.0.3 | 13.33s | 14,610 LOC/s | 65 |
| tsgo | 7.0.2 | 2.42s | 80,388 LOC/s | 65 |
| xtsc-native | da113a40a938 (AOT) | 11.35s | 17,149 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.92x FASTER than tsc; 2.87x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.18s**, **-11.1%** against `temurin26` on identical bytecode — 0.46× tsc, 2.55× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.28×** tsc and **12.56×** tsgo; tsc is **5.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.29s**.

Ahead-of-time (GraalVM native-image): **11.35s** — **0.85×** tsc, **4.69×** tsgo, **2.68×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
