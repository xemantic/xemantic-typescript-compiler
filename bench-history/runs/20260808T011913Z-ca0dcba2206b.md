# Bench run 20260808T011913Z

- **xtsc revision**: [`ca0dcba2206b`](https://github.com/xemantic/xemantic-typescript-compiler/commit/ca0dcba2206b89a203b71efc0ba7fae37251d7dc)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push ca0dcba2206b89a203b71efc0ba7fae37251d7dc

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | ca0dcba2206b | 25.32s | 7,689 LOC/s | 46 |
| tsc | 6.0.3 | 10.55s | 18,456 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.65s | 118,287 LOC/s | 65 |
| xtsc-native | ca0dcba2206b (AOT) | 10.36s | 18,788 LOC/s | 46 |

Relative (wall): xtsc is **2.40×** tsc and **15.38×** tsgo; tsc is **6.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.20s**.

Ahead-of-time (GraalVM native-image): **10.36s**, **2.44×** the JVM arm, **0.98×** tsc, **6.30×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | ca0dcba2206b | 25.48s | 7,641 LOC/s | 46 |
| tsc | 6.0.3 | 12.77s | 15,243 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.32s | 83,923 LOC/s | 65 |
| xtsc-native | ca0dcba2206b (AOT) | 12.72s | 15,307 LOC/s | 46 |

Relative (wall): xtsc is **1.99×** tsc and **10.98×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.37s**.

Ahead-of-time (GraalVM native-image): **12.72s**, **2.00×** the JVM arm, **1.00×** tsc, **5.48×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
