# Bench run 20260831T114754Z

- **xtsc revision**: [`1d55f9c3337e`](https://github.com/xemantic/xemantic-typescript-compiler/commit/1d55f9c3337e39131fdaffcf1ec1e3d79f75c0fe)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 1d55f9c3337e39131fdaffcf1ec1e3d79f75c0fe

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1d55f9c3337e | **4.35s** | 44,728 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1d55f9c3337e | **4.15s** | 46,961 LOC/s | 46 |
| xtsc (cold JVM) | 1d55f9c3337e | 25.04s | 7,774 LOC/s | 46 |
| tsc | 6.0.3 | 11.17s | 17,426 LOC/s | 65 |
| tsgo | 7.0.2 | 1.76s | 110,689 LOC/s | 65 |
| xtsc-native | 1d55f9c3337e (AOT) | 7.33s | 26,576 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.57x FASTER than tsc; 2.47x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.15s**, **-4.8%** against `temurin26` on identical bytecode — 0.37× tsc, 2.36× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.24×** tsc and **14.24×** tsgo; tsc is **6.35×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.93s**.

Ahead-of-time (GraalVM native-image): **7.33s** — **0.66×** tsc, **4.16×** tsgo, **3.42×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1d55f9c3337e | **5.31s** | 36,687 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1d55f9c3337e | **4.99s** | 39,049 LOC/s | 46 |
| xtsc (cold JVM) | 1d55f9c3337e | 30.23s | 6,440 LOC/s | 46 |
| tsc | 6.0.3 | 13.99s | 13,917 LOC/s | 65 |
| tsgo | 7.0.2 | 2.66s | 73,306 LOC/s | 65 |
| xtsc-native | 1d55f9c3337e (AOT) | 9.09s | 21,419 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.64x FASTER than tsc; 2.00x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.99s**, **-6.0%** against `temurin26` on identical bytecode — 0.36× tsc, 1.88× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.16×** tsc and **11.38×** tsgo; tsc is **5.27×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.09s**.

Ahead-of-time (GraalVM native-image): **9.09s** — **0.65×** tsc, **3.42×** tsgo, **3.33×** the COLD JVM arm and **1.71×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
