# Bench run 20260908T114412Z

- **xtsc revision**: [`2e1d6ac115f7`](https://github.com/xemantic/xemantic-typescript-compiler/commit/2e1d6ac115f7b90a004914d967050a007fe6b309)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 2e1d6ac115f7b90a004914d967050a007fe6b309

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 2e1d6ac115f7 | **3.75s** | 51,920 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 2e1d6ac115f7 | **3.56s** | 54,737 LOC/s | 46 |
| xtsc (cold JVM) | 2e1d6ac115f7 | 20.24s | 9,621 LOC/s | 46 |
| tsc | 6.0.3 | 8.06s | 24,165 LOC/s | 65 |
| tsgo | 7.0.2 | 1.27s | 152,707 LOC/s | 65 |
| xtsc-native | 2e1d6ac115f7 (AOT) | 5.94s | 32,756 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.15x FASTER than tsc; 2.94x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.56s**, **-5.1%** against `temurin26` on identical bytecode — 0.44× tsc, 2.79× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.51×** tsc and **15.87×** tsgo; tsc is **6.32×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.14s**.

Ahead-of-time (GraalVM native-image): **5.94s** — **0.74×** tsc, **4.66×** tsgo, **3.40×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 2e1d6ac115f7 | **4.50s** | 43,286 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 2e1d6ac115f7 | **4.21s** | 46,214 LOC/s | 46 |
| xtsc (cold JVM) | 2e1d6ac115f7 | 22.66s | 8,590 LOC/s | 46 |
| tsc | 6.0.3 | 9.56s | 20,366 LOC/s | 65 |
| tsgo | 7.0.2 | 1.82s | 106,861 LOC/s | 65 |
| xtsc-native | 2e1d6ac115f7 (AOT) | 6.96s | 27,974 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.13x FASTER than tsc; 2.47x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.21s**, **-6.3%** against `temurin26` on identical bytecode — 0.44× tsc, 2.31× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.37×** tsc and **12.44×** tsgo; tsc is **5.25×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.57s**.

Ahead-of-time (GraalVM native-image): **6.96s** — **0.73×** tsc, **3.82×** tsgo, **3.26×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
