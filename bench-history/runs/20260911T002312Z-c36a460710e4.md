# Bench run 20260911T002312Z

- **xtsc revision**: [`c36a460710e4`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c36a460710e4c35efb37c547d72e08b61dffd196)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push c36a460710e4c35efb37c547d72e08b61dffd196

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c36a460710e4 | **5.57s** | 34,986 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c36a460710e4 | **5.08s** | 38,304 LOC/s | 46 |
| xtsc (cold JVM) | c36a460710e4 | 24.07s | 8,087 LOC/s | 46 |
| tsc | 6.0.3 | 11.11s | 17,523 LOC/s | 65 |
| tsgo | 7.0.2 | 1.72s | 113,396 LOC/s | 65 |
| xtsc-native | c36a460710e4 (AOT) | 9.43s | 20,651 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.00x FASTER than tsc; 3.24x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.08s**, **-8.7%** against `temurin26` on identical bytecode — 0.46× tsc, 2.96× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.17×** tsc and **14.02×** tsgo; tsc is **6.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.96s**.

Ahead-of-time (GraalVM native-image): **9.43s** — **0.85×** tsc, **5.49×** tsgo, **2.55×** the COLD JVM arm and **1.69×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c36a460710e4 | **6.87s** | 28,349 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c36a460710e4 | **5.85s** | 33,271 LOC/s | 46 |
| xtsc (cold JVM) | c36a460710e4 | 30.01s | 6,488 LOC/s | 46 |
| tsc | 6.0.3 | 13.43s | 14,495 LOC/s | 65 |
| tsgo | 7.0.2 | 2.43s | 80,190 LOC/s | 65 |
| xtsc-native | c36a460710e4 (AOT) | 10.09s | 19,300 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.96x FASTER than tsc; 2.83x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.85s**, **-14.8%** against `temurin26` on identical bytecode — 0.44× tsc, 2.41× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.23×** tsc and **12.36×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.87s**.

Ahead-of-time (GraalVM native-image): **10.09s** — **0.75×** tsc, **4.15×** tsgo, **2.97×** the COLD JVM arm and **1.47×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
