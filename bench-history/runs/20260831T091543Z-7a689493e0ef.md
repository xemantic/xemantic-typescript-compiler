# Bench run 20260831T091543Z

- **xtsc revision**: [`7a689493e0ef`](https://github.com/xemantic/xemantic-typescript-compiler/commit/7a689493e0ef4b7cff9adfbe85c70a2edff49169)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 7a689493e0ef4b7cff9adfbe85c70a2edff49169

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7a689493e0ef | **4.04s** | 48,253 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7a689493e0ef | **4.11s** | 47,326 LOC/s | 46 |
| xtsc (cold JVM) | 7a689493e0ef | 26.04s | 7,477 LOC/s | 46 |
| tsc | 6.0.3 | 10.91s | 17,841 LOC/s | 65 |
| tsgo | 7.0.2 | 1.76s | 110,626 LOC/s | 65 |
| xtsc-native | 7a689493e0ef (AOT) | 7.35s | 26,475 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.70x FASTER than tsc; 2.29x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.11s**, **+2.0%** against `temurin26` on identical bytecode — 0.38× tsc, 2.34× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.39×** tsc and **14.79×** tsgo; tsc is **6.20×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.92s**.

Ahead-of-time (GraalVM native-image): **7.35s** — **0.67×** tsc, **4.18×** tsgo, **3.54×** the COLD JVM arm and **1.82×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7a689493e0ef | **5.46s** | 35,666 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7a689493e0ef | **4.80s** | 40,537 LOC/s | 46 |
| xtsc (cold JVM) | 7a689493e0ef | 27.24s | 7,148 LOC/s | 46 |
| tsc | 6.0.3 | 13.57s | 14,344 LOC/s | 65 |
| tsgo | 7.0.2 | 2.53s | 76,926 LOC/s | 65 |
| xtsc-native | 7a689493e0ef (AOT) | 8.60s | 22,631 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.49x FASTER than tsc; 2.16x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.80s**, **-12.0%** against `temurin26` on identical bytecode — 0.35× tsc, 1.90× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.01×** tsc and **10.76×** tsgo; tsc is **5.36×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.12s**.

Ahead-of-time (GraalVM native-image): **8.60s** — **0.63×** tsc, **3.40×** tsgo, **3.17×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
