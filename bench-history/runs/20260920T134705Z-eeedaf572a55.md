# Bench run 20260920T134705Z

- **xtsc revision**: [`eeedaf572a55`](https://github.com/xemantic/xemantic-typescript-compiler/commit/eeedaf572a55dd43a90cbcc29ec2b79c5c267aef)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push eeedaf572a55dd43a90cbcc29ec2b79c5c267aef

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | eeedaf572a55 | **5.75s** | 33,849 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | eeedaf572a55 | **5.34s** | 36,467 LOC/s | 46 |
| xtsc (cold JVM) | eeedaf572a55 | 27.80s | 7,003 LOC/s | 46 |
| tsc | 6.0.3 | 10.69s | 18,221 LOC/s | 65 |
| tsgo | 7.0.2 | 1.68s | 115,963 LOC/s | 65 |
| xtsc-native | eeedaf572a55 (AOT) | 9.25s | 21,042 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.86x FASTER than tsc; 3.43x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.34s**, **-7.2%** against `temurin26` on identical bytecode — 0.50× tsc, 3.18× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.60×** tsc and **16.56×** tsgo; tsc is **6.36×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.69s**.

Ahead-of-time (GraalVM native-image): **9.25s** — **0.87×** tsc, **5.51×** tsgo, **3.00×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | eeedaf572a55 | **6.71s** | 29,025 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | eeedaf572a55 | **6.34s** | 30,729 LOC/s | 46 |
| xtsc (cold JVM) | eeedaf572a55 | 30.22s | 6,443 LOC/s | 46 |
| tsc | 6.0.3 | 13.29s | 14,654 LOC/s | 65 |
| tsgo | 7.0.2 | 2.41s | 80,755 LOC/s | 65 |
| xtsc-native | eeedaf572a55 (AOT) | 10.73s | 18,138 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.98x FASTER than tsc; 2.78x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.34s**, **-5.5%** against `temurin26` on identical bytecode — 0.48× tsc, 2.63× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.27×** tsc and **12.53×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.08s**.

Ahead-of-time (GraalVM native-image): **10.73s** — **0.81×** tsc, **4.45×** tsgo, **2.81×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
