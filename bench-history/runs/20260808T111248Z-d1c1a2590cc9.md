# Bench run 20260808T111248Z

- **xtsc revision**: [`d1c1a2590cc9`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d1c1a2590cc98e97ee12975c30629ba8579fc538)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push d1c1a2590cc98e97ee12975c30629ba8579fc538

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | d1c1a2590cc9 | 24.86s | 7,830 LOC/s | 46 |
| tsc | 6.0.3 | 10.80s | 18,034 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.66s | 117,078 LOC/s | 65 |
| xtsc-native | d1c1a2590cc9 (AOT) | 10.75s | 18,116 LOC/s | 46 |

Relative (wall): xtsc is **2.30×** tsc and **14.95×** tsgo; tsc is **6.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.76s**.

Ahead-of-time (GraalVM native-image): **10.75s**, **2.31×** the JVM arm, **1.00×** tsc, **6.46×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | d1c1a2590cc9 | 29.15s | 6,679 LOC/s | 46 |
| tsc | 6.0.3 | 13.12s | 14,844 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.42s | 80,388 LOC/s | 65 |
| xtsc-native | d1c1a2590cc9 (AOT) | 13.02s | 14,958 LOC/s | 46 |

Relative (wall): xtsc is **2.22×** tsc and **12.04×** tsgo; tsc is **5.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.03s**.

Ahead-of-time (GraalVM native-image): **13.02s**, **2.24×** the JVM arm, **0.99×** tsc, **5.37×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
