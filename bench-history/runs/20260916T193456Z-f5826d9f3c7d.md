# Bench run 20260916T193456Z

- **xtsc revision**: [`f5826d9f3c7d`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f5826d9f3c7d089751f000dd4c76cb85610b8934)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push f5826d9f3c7d089751f000dd4c76cb85610b8934

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f5826d9f3c7d | **5.82s** | 33,442 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f5826d9f3c7d | **5.23s** | 37,256 LOC/s | 46 |
| xtsc (cold JVM) | f5826d9f3c7d | 25.75s | 7,561 LOC/s | 46 |
| tsc | 6.0.3 | 10.70s | 18,199 LOC/s | 65 |
| tsgo | 7.0.2 | 1.70s | 114,733 LOC/s | 65 |
| xtsc-native | f5826d9f3c7d (AOT) | 8.92s | 21,837 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.84x FASTER than tsc; 3.43x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.23s**, **-10.2%** against `temurin26` on identical bytecode — 0.49× tsc, 3.08× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.41×** tsc and **15.17×** tsgo; tsc is **6.30×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.64s**.

Ahead-of-time (GraalVM native-image): **8.92s** — **0.83×** tsc, **5.25×** tsgo, **2.89×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f5826d9f3c7d | **6.72s** | 28,986 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f5826d9f3c7d | **5.74s** | 33,937 LOC/s | 46 |
| xtsc (cold JVM) | f5826d9f3c7d | 28.86s | 6,745 LOC/s | 46 |
| tsc | 6.0.3 | 12.96s | 15,024 LOC/s | 65 |
| tsgo | 7.0.2 | 2.35s | 82,781 LOC/s | 65 |
| xtsc-native | f5826d9f3c7d (AOT) | 10.38s | 18,753 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.93x FASTER than tsc; 2.86x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.74s**, **-14.6%** against `temurin26` on identical bytecode — 0.44× tsc, 2.44× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.23×** tsc and **12.27×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.73s**.

Ahead-of-time (GraalVM native-image): **10.38s** — **0.80×** tsc, **4.41×** tsgo, **2.78×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
