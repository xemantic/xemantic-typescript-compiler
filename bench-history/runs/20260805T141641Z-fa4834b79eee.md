# Bench run 20260805T141641Z

- **xtsc revision**: [`fa4834b79eee`](https://github.com/xemantic/xemantic-typescript-compiler/commit/fa4834b79eeeb70a0f8037cbbf6830ed3de04431)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push fa4834b79eeeb70a0f8037cbbf6830ed3de04431

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | fa4834b79eee | 25.26s | 7,707 LOC/s | 46 |
| tsc | 6.0.3 | 10.36s | 18,795 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.56s | 124,489 LOC/s | 65 |
| xtsc-native | fa4834b79eee (AOT) | 10.55s | 18,455 LOC/s | 46 |

Relative (wall): xtsc is **2.44×** tsc and **16.15×** tsgo; tsc is **6.62×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.16s**.

Ahead-of-time (GraalVM native-image): **10.55s**, **2.39×** the JVM arm, **1.02×** tsc, **6.75×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | fa4834b79eee | 29.27s | 6,652 LOC/s | 46 |
| tsc | 6.0.3 | 12.41s | 15,690 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.37s | 82,014 LOC/s | 65 |
| xtsc-native | fa4834b79eee (AOT) | 12.02s | 16,202 LOC/s | 46 |

Relative (wall): xtsc is **2.36×** tsc and **12.33×** tsgo; tsc is **5.23×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.16s**.

Ahead-of-time (GraalVM native-image): **12.02s**, **2.44×** the JVM arm, **0.97×** tsc, **5.06×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
