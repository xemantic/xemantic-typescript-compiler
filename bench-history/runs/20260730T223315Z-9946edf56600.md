# Bench run 20260730T223315Z

- **xtsc revision**: [`9946edf56600`](https://github.com/xemantic/xemantic-typescript-compiler/commit/9946edf56600eab46f39c859d8a921a09743efd2)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 9946edf56600eab46f39c859d8a921a09743efd2

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 9946edf56600 | 30.24s | 6,438 LOC/s | 46 |
| tsc | 6.0.3 | 10.94s | 17,797 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.70s | 114,463 LOC/s | 65 |
| xtsc-native | 9946edf56600 (AOT) | 11.80s | 16,501 LOC/s | 46 |

Relative (wall): xtsc is **2.76×** tsc and **17.78×** tsgo; tsc is **6.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.14s**.

Ahead-of-time (GraalVM native-image): **11.80s**, **2.56×** the JVM arm, **1.08×** tsc, **6.94×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 9946edf56600 | 35.34s | 5,510 LOC/s | 46 |
| tsc | 6.0.3 | 13.79s | 14,119 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.52s | 77,293 LOC/s | 65 |
| xtsc-native | 9946edf56600 (AOT) | 15.09s | 12,903 LOC/s | 46 |

Relative (wall): xtsc is **2.56×** tsc and **14.03×** tsgo; tsc is **5.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **35.23s**.

Ahead-of-time (GraalVM native-image): **15.09s**, **2.34×** the JVM arm, **1.09×** tsc, **5.99×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
