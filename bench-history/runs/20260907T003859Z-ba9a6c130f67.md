# Bench run 20260907T003859Z

- **xtsc revision**: [`ba9a6c130f67`](https://github.com/xemantic/xemantic-typescript-compiler/commit/ba9a6c130f67c8b8b99390ea347ae29d782a4da0)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push ba9a6c130f67c8b8b99390ea347ae29d782a4da0

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ba9a6c130f67 | **14.35s** | 13,567 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ba9a6c130f67 | **14.64s** | 13,301 LOC/s | 46 |
| xtsc (cold JVM) | ba9a6c130f67 | 35.99s | 5,409 LOC/s | 46 |
| tsc | 6.0.3 | 11.08s | 17,577 LOC/s | 65 |
| tsgo | 7.0.2 | 1.72s | 113,001 LOC/s | 65 |
| xtsc-native | ba9a6c130f67 (AOT) | 23.47s | 8,295 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.30x tsc — parity at 1.00x; 8.33x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **14.64s**, **+2.0%** against `temurin26` on identical bytecode — 1.32× tsc, 8.50× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.25×** tsc and **20.89×** tsgo; tsc is **6.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **35.88s**.

Ahead-of-time (GraalVM native-image): **23.47s** — **2.12×** tsc, **13.62×** tsgo, **1.53×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ba9a6c130f67 | **16.16s** | 12,046 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ba9a6c130f67 | **15.44s** | 12,614 LOC/s | 46 |
| xtsc (cold JVM) | ba9a6c130f67 | 38.12s | 5,106 LOC/s | 46 |
| tsc | 6.0.3 | 13.29s | 14,644 LOC/s | 65 |
| tsgo | 7.0.2 | 2.40s | 80,990 LOC/s | 65 |
| xtsc-native | ba9a6c130f67 (AOT) | 24.34s | 7,999 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.22x tsc — parity at 1.00x; 6.72x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **15.44s**, **-4.5%** against `temurin26` on identical bytecode — 1.16× tsc, 6.42× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.87×** tsc and **15.86×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **37.99s**.

Ahead-of-time (GraalVM native-image): **24.34s** — **1.83×** tsc, **10.12×** tsgo, **1.57×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
