# Bench run 20260807T094512Z

- **xtsc revision**: [`4c1f859a1639`](https://github.com/xemantic/xemantic-typescript-compiler/commit/4c1f859a16394cca2c43f1461cdbf6d8e9262cdc)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 4c1f859a16394cca2c43f1461cdbf6d8e9262cdc

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 4c1f859a1639 | 25.06s | 7,769 LOC/s | 46 |
| tsc | 6.0.3 | 10.80s | 18,032 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.73s | 112,544 LOC/s | 65 |
| xtsc-native | 4c1f859a1639 (AOT) | 11.23s | 17,329 LOC/s | 46 |

Relative (wall): xtsc is **2.32×** tsc and **14.49×** tsgo; tsc is **6.24×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.96s**.

Ahead-of-time (GraalVM native-image): **11.23s**, **2.23×** the JVM arm, **1.04×** tsc, **6.49×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 4c1f859a1639 | 26.84s | 7,253 LOC/s | 46 |
| tsc | 6.0.3 | 13.45s | 14,478 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.45s | 79,470 LOC/s | 65 |
| xtsc-native | 4c1f859a1639 (AOT) | 13.39s | 14,538 LOC/s | 46 |

Relative (wall): xtsc is **2.00×** tsc and **10.96×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.73s**.

Ahead-of-time (GraalVM native-image): **13.39s**, **2.00×** the JVM arm, **1.00×** tsc, **5.47×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
