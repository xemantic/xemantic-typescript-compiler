# Bench run 20260812T013457Z

- **xtsc revision**: [`63819970e318`](https://github.com/xemantic/xemantic-typescript-compiler/commit/63819970e3189ba2ab307be45bd2a5529b232188)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 63819970e3189ba2ab307be45bd2a5529b232188

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 63819970e318 | **4.20s** | 46,357 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 63819970e318 | **4.18s** | 46,624 LOC/s | 46 |
| xtsc (cold JVM) | 63819970e318 | 23.91s | 8,144 LOC/s | 46 |
| tsc | 6.0.3 | 10.63s | 18,312 LOC/s | 65 |
| tsgo | 7.0.2 | 1.64s | 118,865 LOC/s | 65 |
| xtsc-native | 63819970e318 (AOT) | 6.56s | 29,666 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.53x FASTER than tsc; 2.56x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.18s**, **-0.6%** against `temurin26` on identical bytecode — 0.39× tsc, 2.55× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.25×** tsc and **14.60×** tsgo; tsc is **6.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.79s**.

Ahead-of-time (GraalVM native-image): **6.56s** — **0.62×** tsc, **4.01×** tsgo, **3.64×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 63819970e318 | **4.97s** | 39,191 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 63819970e318 | **4.87s** | 39,979 LOC/s | 46 |
| xtsc (cold JVM) | 63819970e318 | 27.65s | 7,041 LOC/s | 46 |
| tsc | 6.0.3 | 13.11s | 14,846 LOC/s | 65 |
| tsgo | 7.0.2 | 2.36s | 82,535 LOC/s | 65 |
| xtsc-native | 63819970e318 (AOT) | 8.42s | 23,112 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.64x FASTER than tsc; 2.11x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.87s**, **-2.0%** against `temurin26` on identical bytecode — 0.37× tsc, 2.06× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.11×** tsc and **11.72×** tsgo; tsc is **5.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.53s**.

Ahead-of-time (GraalVM native-image): **8.42s** — **0.64×** tsc, **3.57×** tsgo, **3.28×** the COLD JVM arm and **1.70×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
