# Bench run 20260917T172344Z

- **xtsc revision**: [`9b2f84a5fa8a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/9b2f84a5fa8a4b02e6980c71a9c913e9ccd2125b)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 9b2f84a5fa8a4b02e6980c71a9c913e9ccd2125b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9b2f84a5fa8a | **5.94s** | 32,772 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9b2f84a5fa8a | **5.54s** | 35,151 LOC/s | 46 |
| xtsc (cold JVM) | 9b2f84a5fa8a | 28.01s | 6,951 LOC/s | 46 |
| tsc | 6.0.3 | 10.94s | 17,803 LOC/s | 65 |
| tsgo | 7.0.2 | 1.70s | 114,868 LOC/s | 65 |
| xtsc-native | 9b2f84a5fa8a (AOT) | 9.32s | 20,884 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.84x FASTER than tsc; 3.51x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.54s**, **-6.8%** against `temurin26` on identical bytecode — 0.51× tsc, 3.27× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.56×** tsc and **16.52×** tsgo; tsc is **6.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.89s**.

Ahead-of-time (GraalVM native-image): **9.32s** — **0.85×** tsc, **5.50×** tsgo, **3.00×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9b2f84a5fa8a | **6.73s** | 28,913 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9b2f84a5fa8a | **6.04s** | 32,240 LOC/s | 46 |
| xtsc (cold JVM) | 9b2f84a5fa8a | 31.25s | 6,231 LOC/s | 46 |
| tsc | 6.0.3 | 13.36s | 14,575 LOC/s | 65 |
| tsgo | 7.0.2 | 2.46s | 79,147 LOC/s | 65 |
| xtsc-native | 9b2f84a5fa8a (AOT) | 10.66s | 18,259 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.98x FASTER than tsc; 2.74x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.04s**, **-10.3%** against `temurin26` on identical bytecode — 0.45× tsc, 2.45× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.34×** tsc and **12.70×** tsgo; tsc is **5.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **31.11s**.

Ahead-of-time (GraalVM native-image): **10.66s** — **0.80×** tsc, **4.33×** tsgo, **2.93×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
