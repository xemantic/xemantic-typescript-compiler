# Bench run 20260920T102539Z

- **xtsc revision**: [`0c7c55cb53c7`](https://github.com/xemantic/xemantic-typescript-compiler/commit/0c7c55cb53c76640c559c7d9438b4f2de2fd69a1)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 0c7c55cb53c76640c559c7d9438b4f2de2fd69a1

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0c7c55cb53c7 | **5.92s** | 32,888 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0c7c55cb53c7 | **5.21s** | 37,370 LOC/s | 46 |
| xtsc (cold JVM) | 0c7c55cb53c7 | 26.42s | 7,369 LOC/s | 46 |
| tsc | 6.0.3 | 10.80s | 18,027 LOC/s | 65 |
| tsgo | 7.0.2 | 1.71s | 113,927 LOC/s | 65 |
| xtsc-native | 0c7c55cb53c7 (AOT) | 9.16s | 21,246 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.82x FASTER than tsc; 3.46x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.21s**, **-12.0%** against `temurin26` on identical bytecode — 0.48× tsc, 3.05× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.45×** tsc and **15.46×** tsgo; tsc is **6.32×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.31s**.

Ahead-of-time (GraalVM native-image): **9.16s** — **0.85×** tsc, **5.36×** tsgo, **2.88×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0c7c55cb53c7 | **6.67s** | 29,181 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0c7c55cb53c7 | **6.04s** | 32,230 LOC/s | 46 |
| xtsc (cold JVM) | 0c7c55cb53c7 | 29.14s | 6,682 LOC/s | 46 |
| tsc | 6.0.3 | 13.35s | 14,587 LOC/s | 65 |
| tsgo | 7.0.2 | 2.43s | 80,223 LOC/s | 65 |
| xtsc-native | 0c7c55cb53c7 (AOT) | 10.73s | 18,142 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.00x FASTER than tsc; 2.75x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.04s**, **-9.5%** against `temurin26` on identical bytecode — 0.45× tsc, 2.49× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.18×** tsc and **12.00×** tsgo; tsc is **5.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.00s**.

Ahead-of-time (GraalVM native-image): **10.73s** — **0.80×** tsc, **4.42×** tsgo, **2.71×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
