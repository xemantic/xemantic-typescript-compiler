# Bench run 20260811T102556Z

- **xtsc revision**: [`8528b26309cb`](https://github.com/xemantic/xemantic-typescript-compiler/commit/8528b26309cb6a65861dacd2de08dd7a1a14bacc)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 8528b26309cb6a65861dacd2de08dd7a1a14bacc

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8528b26309cb | **3.34s** | 58,224 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8528b26309cb | **2.97s** | 65,578 LOC/s | 46 |
| xtsc (cold JVM) | 8528b26309cb | 18.62s | 10,454 LOC/s | 46 |
| tsc | 6.0.3 | 8.18s | 23,796 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.24s | 157,017 LOC/s | 65 |
| xtsc-native | 8528b26309cb (AOT) | 4.89s | 39,824 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.45x FASTER than tsc; 2.70x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.97s**, **-11.2%** against `temurin26` on identical bytecode — 0.36× tsc, 2.39× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.28×** tsc and **15.02×** tsgo; tsc is **6.60×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.53s**.

Ahead-of-time (GraalVM native-image): **4.89s** — **0.60×** tsc, **3.94×** tsgo, **3.81×** the COLD JVM arm and **1.46×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8528b26309cb | **4.05s** | 48,122 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8528b26309cb | **3.71s** | 52,452 LOC/s | 46 |
| xtsc (cold JVM) | 8528b26309cb | 22.99s | 8,468 LOC/s | 46 |
| tsc | 6.0.3 | 10.18s | 19,129 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.94s | 100,155 LOC/s | 65 |
| xtsc-native | 8528b26309cb (AOT) | 6.52s | 29,857 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.52x FASTER than tsc; 2.08x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.71s**, **-8.3%** against `temurin26` on identical bytecode — 0.36× tsc, 1.91× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.26×** tsc and **11.83×** tsgo; tsc is **5.24×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.70s**.

Ahead-of-time (GraalVM native-image): **6.52s** — **0.64×** tsc, **3.35×** tsgo, **3.53×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
