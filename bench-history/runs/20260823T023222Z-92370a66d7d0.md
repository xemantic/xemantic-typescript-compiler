# Bench run 20260823T023222Z

- **xtsc revision**: [`92370a66d7d0`](https://github.com/xemantic/xemantic-typescript-compiler/commit/92370a66d7d0e9fe78b85c07dfcca6c717f2d1d2)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 92370a66d7d0e9fe78b85c07dfcca6c717f2d1d2

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 92370a66d7d0 | **4.04s** | 48,217 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 92370a66d7d0 | **3.69s** | 52,707 LOC/s | 46 |
| xtsc (cold JVM) | 92370a66d7d0 | 21.04s | 9,254 LOC/s | 46 |
| tsc | 6.0.3 | 10.79s | 18,042 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,208 LOC/s | 65 |
| xtsc-native | 92370a66d7d0 (AOT) | 6.04s | 32,240 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.67x FASTER than tsc; 2.39x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.69s**, **-8.5%** against `temurin26` on identical bytecode — 0.34× tsc, 2.19× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.95×** tsc and **12.45×** tsgo; tsc is **6.39×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.93s**.

Ahead-of-time (GraalVM native-image): **6.04s** — **0.56×** tsc, **3.57×** tsgo, **3.48×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 92370a66d7d0 | **4.92s** | 39,533 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 92370a66d7d0 | **4.33s** | 44,986 LOC/s | 46 |
| xtsc (cold JVM) | 92370a66d7d0 | 26.95s | 7,223 LOC/s | 46 |
| tsc | 6.0.3 | 13.21s | 14,744 LOC/s | 65 |
| tsgo | 7.0.2 | 2.36s | 82,500 LOC/s | 65 |
| xtsc-native | 92370a66d7d0 (AOT) | 7.42s | 26,233 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.68x FASTER than tsc; 2.09x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.33s**, **-12.1%** against `temurin26` on identical bytecode — 0.33× tsc, 1.83× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.04×** tsc and **11.42×** tsgo; tsc is **5.60×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.81s**.

Ahead-of-time (GraalVM native-image): **7.42s** — **0.56×** tsc, **3.14×** tsgo, **3.63×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
