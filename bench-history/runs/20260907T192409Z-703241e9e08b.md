# Bench run 20260907T192409Z

- **xtsc revision**: [`703241e9e08b`](https://github.com/xemantic/xemantic-typescript-compiler/commit/703241e9e08bf669334b445955218111887c65fd)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 703241e9e08bf669334b445955218111887c65fd

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 703241e9e08b | **15.24s** | 12,771 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 703241e9e08b | **15.06s** | 12,929 LOC/s | 46 |
| xtsc (cold JVM) | 703241e9e08b | 33.39s | 5,831 LOC/s | 46 |
| tsc | 6.0.3 | 9.96s | 19,544 LOC/s | 65 |
| tsgo | 7.0.2 | 1.52s | 127,925 LOC/s | 65 |
| xtsc-native | 703241e9e08b (AOT) | 24.11s | 8,073 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.53x tsc — parity at 1.00x; 10.02x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **15.06s**, **-1.2%** against `temurin26` on identical bytecode — 1.51× tsc, 9.89× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.35×** tsc and **21.94×** tsgo; tsc is **6.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **33.27s**.

Ahead-of-time (GraalVM native-image): **24.11s** — **2.42×** tsc, **15.84×** tsgo, **1.38×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 703241e9e08b | **15.98s** | 12,181 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 703241e9e08b | **15.66s** | 12,434 LOC/s | 46 |
| xtsc (cold JVM) | 703241e9e08b | 37.43s | 5,201 LOC/s | 46 |
| tsc | 6.0.3 | 12.29s | 15,843 LOC/s | 65 |
| tsgo | 7.0.2 | 2.20s | 88,380 LOC/s | 65 |
| xtsc-native | 703241e9e08b (AOT) | 25.32s | 7,690 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.30x tsc — parity at 1.00x; 7.26x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **15.66s**, **-2.0%** against `temurin26` on identical bytecode — 1.27× tsc, 7.11× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.05×** tsc and **16.99×** tsgo; tsc is **5.58×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **37.29s**.

Ahead-of-time (GraalVM native-image): **25.32s** — **2.06×** tsc, **11.49×** tsgo, **1.48×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
