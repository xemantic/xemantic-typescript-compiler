# Bench run 20260803T171136Z

- **xtsc revision**: [`b0a899363ab1`](https://github.com/xemantic/xemantic-typescript-compiler/commit/b0a899363ab18d39be2e555a318cca2bdc472d7d)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push b0a899363ab18d39be2e555a318cca2bdc472d7d

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | b0a899363ab1 | 26.82s | 7,260 LOC/s | 46 |
| tsc | 6.0.3 | 11.56s | 16,842 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.81s | 107,510 LOC/s | 65 |
| xtsc-native | b0a899363ab1 (AOT) | 12.14s | 16,042 LOC/s | 46 |

Relative (wall): xtsc is **2.32×** tsc and **14.81×** tsgo; tsc is **6.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.70s**.

Ahead-of-time (GraalVM native-image): **12.14s**, **2.21×** the JVM arm, **1.05×** tsc, **6.70×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | b0a899363ab1 | 27.86s | 6,988 LOC/s | 46 |
| tsc | 6.0.3 | 14.23s | 13,684 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.56s | 76,085 LOC/s | 65 |
| xtsc-native | b0a899363ab1 (AOT) | 13.59s | 14,326 LOC/s | 46 |

Relative (wall): xtsc is **1.96×** tsc and **10.89×** tsgo; tsc is **5.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.76s**.

Ahead-of-time (GraalVM native-image): **13.59s**, **2.05×** the JVM arm, **0.96×** tsc, **5.31×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
