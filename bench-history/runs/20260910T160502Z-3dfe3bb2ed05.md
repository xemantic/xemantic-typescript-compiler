# Bench run 20260910T160502Z

- **xtsc revision**: [`3dfe3bb2ed05`](https://github.com/xemantic/xemantic-typescript-compiler/commit/3dfe3bb2ed058d2793a75cc9d1028180ee16dc8e)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 3dfe3bb2ed058d2793a75cc9d1028180ee16dc8e

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3dfe3bb2ed05 | **5.16s** | 37,703 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3dfe3bb2ed05 | **4.82s** | 40,419 LOC/s | 46 |
| xtsc (cold JVM) | 3dfe3bb2ed05 | 28.10s | 6,928 LOC/s | 46 |
| tsc | 6.0.3 | 10.40s | 18,723 LOC/s | 65 |
| tsgo | 7.0.2 | 1.64s | 119,011 LOC/s | 65 |
| xtsc-native | 3dfe3bb2ed05 (AOT) | 8.46s | 23,003 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.01x FASTER than tsc; 3.16x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.82s**, **-6.7%** against `temurin26` on identical bytecode — 0.46× tsc, 2.94× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.70×** tsc and **17.18×** tsgo; tsc is **6.36×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.01s**.

Ahead-of-time (GraalVM native-image): **8.46s** — **0.81×** tsc, **5.17×** tsgo, **3.32×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3dfe3bb2ed05 | **5.79s** | 33,604 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3dfe3bb2ed05 | **5.49s** | 35,445 LOC/s | 46 |
| xtsc (cold JVM) | 3dfe3bb2ed05 | 32.52s | 5,987 LOC/s | 46 |
| tsc | 6.0.3 | 12.53s | 15,542 LOC/s | 65 |
| tsgo | 7.0.2 | 2.40s | 81,024 LOC/s | 65 |
| xtsc-native | 3dfe3bb2ed05 (AOT) | 9.58s | 20,315 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.16x FASTER than tsc; 2.41x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.49s**, **-5.2%** against `temurin26` on identical bytecode — 0.44× tsc, 2.29× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.60×** tsc and **13.53×** tsgo; tsc is **5.21×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **32.40s**.

Ahead-of-time (GraalVM native-image): **9.58s** — **0.77×** tsc, **3.99×** tsgo, **3.39×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
