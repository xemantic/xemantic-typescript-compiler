# Bench run 20260811T202507Z

- **xtsc revision**: [`b8a0d4000873`](https://github.com/xemantic/xemantic-typescript-compiler/commit/b8a0d40008736206340addbf605b820d4dc6afed)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push b8a0d40008736206340addbf605b820d4dc6afed

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b8a0d4000873 | **4.54s** | 42,838 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b8a0d4000873 | **4.34s** | 44,893 LOC/s | 46 |
| xtsc (cold JVM) | b8a0d4000873 | 24.66s | 7,893 LOC/s | 46 |
| tsc | 6.0.3 | 10.90s | 17,857 LOC/s | 65 |
| tsgo | 7.0.2 | 1.62s | 119,964 LOC/s | 65 |
| xtsc-native | b8a0d4000873 (AOT) | 7.26s | 26,833 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.40x FASTER than tsc; 2.80x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.34s**, **-4.6%** against `temurin26` on identical bytecode — 0.40× tsc, 2.67× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.26×** tsc and **15.20×** tsgo; tsc is **6.72×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.55s**.

Ahead-of-time (GraalVM native-image): **7.26s** — **0.67×** tsc, **4.47×** tsgo, **3.40×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b8a0d4000873 | **5.57s** | 34,930 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b8a0d4000873 | **5.04s** | 38,623 LOC/s | 46 |
| xtsc (cold JVM) | b8a0d4000873 | 25.56s | 7,617 LOC/s | 46 |
| tsc | 6.0.3 | 13.03s | 14,938 LOC/s | 65 |
| tsgo | 7.0.2 | 2.39s | 81,533 LOC/s | 65 |
| xtsc-native | b8a0d4000873 (AOT) | 8.80s | 22,127 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.34x FASTER than tsc; 2.33x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.04s**, **-9.6%** against `temurin26` on identical bytecode — 0.39× tsc, 2.11× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.96×** tsc and **10.70×** tsgo; tsc is **5.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.41s**.

Ahead-of-time (GraalVM native-image): **8.80s** — **0.68×** tsc, **3.68×** tsgo, **2.90×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
