# Bench run 20260802T185247Z

- **xtsc revision**: [`de9a3c9241da`](https://github.com/xemantic/xemantic-typescript-compiler/commit/de9a3c9241da65b8f8f5df930edb7651a6136e97)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push de9a3c9241da65b8f8f5df930edb7651a6136e97

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | de9a3c9241da | 15.08s | 12,909 LOC/s | 46 |
| tsc | 6.0.3 | 6.16s | 31,622 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 0.89s | 219,259 LOC/s | 65 |
| xtsc-native | de9a3c9241da (AOT) | 6.22s | 31,282 LOC/s | 46 |

Relative (wall): xtsc is **2.45×** tsc and **16.98×** tsgo; tsc is **6.93×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **15.00s**.

Ahead-of-time (GraalVM native-image): **6.22s**, **2.42×** the JVM arm, **1.01×** tsc, **7.01×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | de9a3c9241da | 16.54s | 11,775 LOC/s | 46 |
| tsc | 6.0.3 | 7.46s | 26,081 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.37s | 141,911 LOC/s | 65 |
| xtsc-native | de9a3c9241da (AOT) | 7.75s | 25,139 LOC/s | 46 |

Relative (wall): xtsc is **2.22×** tsc and **12.05×** tsgo; tsc is **5.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **16.44s**.

Ahead-of-time (GraalVM native-image): **7.75s**, **2.13×** the JVM arm, **1.04×** tsc, **5.65×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
