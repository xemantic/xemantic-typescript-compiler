# Bench run 20260924T210732Z

- **xtsc revision**: [`cc35b4e7af05`](https://github.com/xemantic/xemantic-typescript-compiler/commit/cc35b4e7af0572645c3e850c8e8bc16ce89dc5d6)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push cc35b4e7af0572645c3e850c8e8bc16ce89dc5d6

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cc35b4e7af05 | **5.01s** | 38,870 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cc35b4e7af05 | **4.65s** | 41,862 LOC/s | 46 |
| xtsc (cold JVM) | cc35b4e7af05 | 25.45s | 7,648 LOC/s | 46 |
| tsc | 6.0.3 | 9.84s | 19,794 LOC/s | 65 |
| tsgo | 7.0.2 | 1.53s | 127,422 LOC/s | 65 |
| xtsc-native | cc35b4e7af05 (AOT) | 7.72s | 25,223 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.96x FASTER than tsc; 3.28x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.65s**, **-7.1%** against `temurin26` on identical bytecode — 0.47× tsc, 3.04× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.59×** tsc and **16.66×** tsgo; tsc is **6.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.36s**.

Ahead-of-time (GraalVM native-image): **7.72s** — **0.78×** tsc, **5.05×** tsgo, **3.30×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cc35b4e7af05 | **5.79s** | 33,621 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cc35b4e7af05 | **5.38s** | 36,223 LOC/s | 46 |
| xtsc (cold JVM) | cc35b4e7af05 | 29.71s | 6,553 LOC/s | 46 |
| tsc | 6.0.3 | 11.72s | 16,609 LOC/s | 65 |
| tsgo | 7.0.2 | 2.21s | 87,981 LOC/s | 65 |
| xtsc-native | cc35b4e7af05 (AOT) | 9.19s | 21,177 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.02x FASTER than tsc; 2.62x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.38s**, **-7.2%** against `temurin26` on identical bytecode — 0.46× tsc, 2.43× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.53×** tsc and **13.42×** tsgo; tsc is **5.30×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.59s**.

Ahead-of-time (GraalVM native-image): **9.19s** — **0.78×** tsc, **4.15×** tsgo, **3.23×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
