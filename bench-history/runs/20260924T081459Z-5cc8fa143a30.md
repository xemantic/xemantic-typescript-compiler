# Bench run 20260924T081459Z

- **xtsc revision**: [`5cc8fa143a30`](https://github.com/xemantic/xemantic-typescript-compiler/commit/5cc8fa143a30a2f3a88e31c0551e6cfd0e49d221)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 5cc8fa143a30a2f3a88e31c0551e6cfd0e49d221

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5cc8fa143a30 | **6.05s** | 32,192 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5cc8fa143a30 | **5.71s** | 34,110 LOC/s | 46 |
| xtsc (cold JVM) | 5cc8fa143a30 | 25.58s | 7,612 LOC/s | 46 |
| tsc | 6.0.3 | 10.44s | 18,651 LOC/s | 65 |
| tsgo | 7.0.2 | 1.60s | 121,536 LOC/s | 65 |
| xtsc-native | 5cc8fa143a30 (AOT) | 10.13s | 19,210 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.73x FASTER than tsc; 3.78x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.71s**, **-5.6%** against `temurin26` on identical bytecode — 0.55× tsc, 3.56× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.45×** tsc and **15.97×** tsgo; tsc is **6.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.45s**.

Ahead-of-time (GraalVM native-image): **10.13s** — **0.97×** tsc, **6.33×** tsgo, **2.52×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 5cc8fa143a30 | **6.87s** | 28,353 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 5cc8fa143a30 | **6.26s** | 31,117 LOC/s | 46 |
| xtsc (cold JVM) | 5cc8fa143a30 | 30.02s | 6,486 LOC/s | 46 |
| tsc | 6.0.3 | 13.01s | 14,971 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,876 LOC/s | 65 |
| xtsc-native | 5cc8fa143a30 (AOT) | 11.28s | 17,262 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.89x FASTER than tsc; 2.89x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.26s**, **-8.9%** against `temurin26` on identical bytecode — 0.48× tsc, 2.63× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.31×** tsc and **12.62×** tsgo; tsc is **5.47×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.88s**.

Ahead-of-time (GraalVM native-image): **11.28s** — **0.87×** tsc, **4.74×** tsgo, **2.66×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
