# Bench run 20260818T033828Z

- **xtsc revision**: [`27674451c607`](https://github.com/xemantic/xemantic-typescript-compiler/commit/27674451c6073a9bf09a33276002e71cb3d0d3af)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 27674451c6073a9bf09a33276002e71cb3d0d3af

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 27674451c607 | **3.89s** | 50,039 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 27674451c607 | **3.75s** | 51,989 LOC/s | 46 |
| xtsc (cold JVM) | 27674451c607 | 21.47s | 9,067 LOC/s | 46 |
| tsc | 6.0.3 | 10.78s | 18,059 LOC/s | 65 |
| tsgo | 7.0.2 | 1.63s | 119,742 LOC/s | 65 |
| xtsc-native | 27674451c607 (AOT) | 5.99s | 32,488 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.77x FASTER than tsc; 2.39x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.75s**, **-3.8%** against `temurin26` on identical bytecode — 0.35× tsc, 2.30× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.99×** tsc and **13.21×** tsgo; tsc is **6.63×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.36s**.

Ahead-of-time (GraalVM native-image): **5.99s** — **0.56×** tsc, **3.69×** tsgo, **3.58×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 27674451c607 | **4.88s** | 39,922 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 27674451c607 | **4.35s** | 44,800 LOC/s | 46 |
| xtsc (cold JVM) | 27674451c607 | 23.75s | 8,196 LOC/s | 46 |
| tsc | 6.0.3 | 13.23s | 14,721 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,910 LOC/s | 65 |
| xtsc-native | 27674451c607 (AOT) | 7.26s | 26,814 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.71x FASTER than tsc; 2.05x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.35s**, **-10.9%** against `temurin26` on identical bytecode — 0.33× tsc, 1.83× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.80×** tsc and **9.99×** tsgo; tsc is **5.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.63s**.

Ahead-of-time (GraalVM native-image): **7.26s** — **0.55×** tsc, **3.05×** tsgo, **3.27×** the COLD JVM arm and **1.49×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
