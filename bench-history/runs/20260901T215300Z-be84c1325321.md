# Bench run 20260901T215300Z

- **xtsc revision**: [`be84c1325321`](https://github.com/xemantic/xemantic-typescript-compiler/commit/be84c132532136dfe45bb722514adab1deb78ee0)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push be84c132532136dfe45bb722514adab1deb78ee0

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | be84c1325321 | **4.50s** | 43,267 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | be84c1325321 | **4.43s** | 43,921 LOC/s | 46 |
| xtsc (cold JVM) | be84c1325321 | 26.40s | 7,374 LOC/s | 46 |
| tsc | 6.0.3 | 11.22s | 17,356 LOC/s | 65 |
| tsgo | 7.0.2 | 1.75s | 110,941 LOC/s | 65 |
| xtsc-native | be84c1325321 (AOT) | 7.08s | 27,519 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.49x FASTER than tsc; 2.56x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.43s**, **-1.5%** against `temurin26` on identical bytecode — 0.40× tsc, 2.53× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.35×** tsc and **15.04×** tsgo; tsc is **6.39×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.27s**.

Ahead-of-time (GraalVM native-image): **7.08s** — **0.63×** tsc, **4.03×** tsgo, **3.73×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | be84c1325321 | **5.26s** | 37,029 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | be84c1325321 | **4.70s** | 41,443 LOC/s | 46 |
| xtsc (cold JVM) | be84c1325321 | 24.95s | 7,802 LOC/s | 46 |
| tsc | 6.0.3 | 13.22s | 14,732 LOC/s | 65 |
| tsgo | 7.0.2 | 2.40s | 81,058 LOC/s | 65 |
| xtsc-native | be84c1325321 (AOT) | 8.13s | 23,939 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.51x FASTER than tsc; 2.19x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.70s**, **-10.7%** against `temurin26` on identical bytecode — 0.36× tsc, 1.96× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.89×** tsc and **10.39×** tsgo; tsc is **5.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.83s**.

Ahead-of-time (GraalVM native-image): **8.13s** — **0.62×** tsc, **3.39×** tsgo, **3.07×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
