# Bench run 20260810T234418Z

- **xtsc revision**: [`8d5eca350a0b`](https://github.com/xemantic/xemantic-typescript-compiler/commit/8d5eca350a0bca690b5a8fbfd777dc487b59d9a7)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 8d5eca350a0bca690b5a8fbfd777dc487b59d9a7

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8d5eca350a0b | **4.15s** | 46,961 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8d5eca350a0b | **3.89s** | 50,103 LOC/s | 46 |
| xtsc (cold JVM) | 8d5eca350a0b | 25.62s | 7,599 LOC/s | 46 |
| tsc | 6.0.3 | 9.97s | 19,534 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.52s | 127,673 LOC/s | 65 |
| xtsc-native | 8d5eca350a0b (AOT) | 6.65s | 29,274 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.40x FASTER than tsc; 2.72x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.89s**, **-6.3%** against `temurin26` on identical bytecode — 0.39× tsc, 2.55× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.57×** tsc and **16.80×** tsgo; tsc is **6.54×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.51s**.

Ahead-of-time (GraalVM native-image): **6.65s** — **0.67×** tsc, **4.36×** tsgo, **3.85×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 8d5eca350a0b | **5.14s** | 37,879 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 8d5eca350a0b | **4.57s** | 42,576 LOC/s | 46 |
| xtsc (cold JVM) | 8d5eca350a0b | 27.10s | 7,185 LOC/s | 46 |
| tsc | 6.0.3 | 12.11s | 16,079 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.27s | 85,696 LOC/s | 65 |
| xtsc-native | 8d5eca350a0b (AOT) | 8.30s | 23,449 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.36x FASTER than tsc; 2.26x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.57s**, **-11.0%** against `temurin26` on identical bytecode — 0.38× tsc, 2.01× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.24×** tsc and **11.93×** tsgo; tsc is **5.33×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.98s**.

Ahead-of-time (GraalVM native-image): **8.30s** — **0.69×** tsc, **3.65×** tsgo, **3.26×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
