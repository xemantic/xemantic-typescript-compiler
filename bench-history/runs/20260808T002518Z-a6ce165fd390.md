# Bench run 20260808T002518Z

- **xtsc revision**: [`a6ce165fd390`](https://github.com/xemantic/xemantic-typescript-compiler/commit/a6ce165fd390df7cb8ab423b3f80508b66c5c0b0)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push a6ce165fd390df7cb8ab423b3f80508b66c5c0b0

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | a6ce165fd390 | 19.20s | 10,139 LOC/s | 46 |
| tsc | 6.0.3 | 8.01s | 24,298 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.25s | 155,761 LOC/s | 65 |
| xtsc-native | a6ce165fd390 (AOT) | 8.97s | 21,710 LOC/s | 46 |

Relative (wall): xtsc is **2.40×** tsc and **15.36×** tsgo; tsc is **6.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.12s**.

Ahead-of-time (GraalVM native-image): **8.97s**, **2.14×** the JVM arm, **1.12×** tsc, **7.17×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | a6ce165fd390 | 21.27s | 9,152 LOC/s | 46 |
| tsc | 6.0.3 | 9.91s | 19,647 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.75s | 111,513 LOC/s | 65 |
| xtsc-native | a6ce165fd390 (AOT) | 9.64s | 20,195 LOC/s | 46 |

Relative (wall): xtsc is **2.15×** tsc and **12.18×** tsgo; tsc is **5.68×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.18s**.

Ahead-of-time (GraalVM native-image): **9.64s**, **2.21×** the JVM arm, **0.97×** tsc, **5.52×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
