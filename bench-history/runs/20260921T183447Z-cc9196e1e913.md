# Bench run 20260921T183447Z

- **xtsc revision**: [`cc9196e1e913`](https://github.com/xemantic/xemantic-typescript-compiler/commit/cc9196e1e913ba48018a5f654c709b16c6c707ae)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push cc9196e1e913ba48018a5f654c709b16c6c707ae

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cc9196e1e913 | **4.24s** | 45,931 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cc9196e1e913 | **4.25s** | 45,823 LOC/s | 46 |
| xtsc (cold JVM) | cc9196e1e913 | 23.02s | 8,459 LOC/s | 46 |
| tsc | 6.0.3 | 8.84s | 22,017 LOC/s | 65 |
| tsgo | 7.0.2 | 1.36s | 143,268 LOC/s | 65 |
| xtsc-native | cc9196e1e913 (AOT) | 7.04s | 27,672 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.09x FASTER than tsc; 3.12x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.25s**, **+0.2%** against `temurin26` on identical bytecode — 0.48× tsc, 3.13× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.60×** tsc and **16.94×** tsgo; tsc is **6.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.91s**.

Ahead-of-time (GraalVM native-image): **7.04s** — **0.80×** tsc, **5.18×** tsgo, **3.27×** the COLD JVM arm and **1.66×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | cc9196e1e913 | **5.29s** | 36,791 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | cc9196e1e913 | **4.76s** | 40,895 LOC/s | 46 |
| xtsc (cold JVM) | cc9196e1e913 | 26.32s | 7,396 LOC/s | 46 |
| tsc | 6.0.3 | 10.49s | 18,562 LOC/s | 65 |
| tsgo | 7.0.2 | 2.00s | 97,351 LOC/s | 65 |
| xtsc-native | cc9196e1e913 (AOT) | 8.33s | 23,384 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.98x FASTER than tsc; 2.65x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.76s**, **-10.0%** against `temurin26` on identical bytecode — 0.45× tsc, 2.38× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.51×** tsc and **13.16×** tsgo; tsc is **5.24×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.20s**.

Ahead-of-time (GraalVM native-image): **8.33s** — **0.79×** tsc, **4.16×** tsgo, **3.16×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
