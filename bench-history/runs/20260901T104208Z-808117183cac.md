# Bench run 20260901T104208Z

- **xtsc revision**: [`808117183cac`](https://github.com/xemantic/xemantic-typescript-compiler/commit/808117183cacab38558da4451e0ea9c7e85c0c81)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 808117183cacab38558da4451e0ea9c7e85c0c81

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 808117183cac | **4.17s** | 46,713 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 808117183cac | **3.95s** | 49,341 LOC/s | 46 |
| xtsc (cold JVM) | 808117183cac | 22.59s | 8,617 LOC/s | 46 |
| tsc | 6.0.3 | 10.49s | 18,557 LOC/s | 65 |
| tsgo | 7.0.2 | 1.66s | 117,502 LOC/s | 65 |
| xtsc-native | 808117183cac (AOT) | 6.42s | 30,336 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.52x FASTER than tsc; 2.52x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.95s**, **-5.3%** against `temurin26` on identical bytecode — 0.38× tsc, 2.38× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.15×** tsc and **13.63×** tsgo; tsc is **6.33×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.48s**.

Ahead-of-time (GraalVM native-image): **6.42s** — **0.61×** tsc, **3.87×** tsgo, **3.52×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 808117183cac | **5.13s** | 37,983 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 808117183cac | **4.65s** | 41,862 LOC/s | 46 |
| xtsc (cold JVM) | 808117183cac | 26.02s | 7,482 LOC/s | 46 |
| tsc | 6.0.3 | 12.97s | 15,010 LOC/s | 65 |
| tsgo | 7.0.2 | 2.35s | 82,993 LOC/s | 65 |
| xtsc-native | 808117183cac (AOT) | 7.96s | 24,475 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.53x FASTER than tsc; 2.18x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.65s**, **-9.3%** against `temurin26` on identical bytecode — 0.36× tsc, 1.98× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.01×** tsc and **11.09×** tsgo; tsc is **5.53×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.89s**.

Ahead-of-time (GraalVM native-image): **7.96s** — **0.61×** tsc, **3.39×** tsgo, **3.27×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
