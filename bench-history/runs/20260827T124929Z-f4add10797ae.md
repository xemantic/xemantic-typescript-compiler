# Bench run 20260827T124929Z

- **xtsc revision**: [`f4add10797ae`](https://github.com/xemantic/xemantic-typescript-compiler/commit/f4add10797aed2e0b0eec0188b5b9af2c67683cf)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push f4add10797aed2e0b0eec0188b5b9af2c67683cf

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f4add10797ae | **4.21s** | 46,203 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f4add10797ae | **4.12s** | 47,223 LOC/s | 46 |
| xtsc (cold JVM) | f4add10797ae | 21.66s | 8,990 LOC/s | 46 |
| tsc | 6.0.3 | 10.99s | 17,719 LOC/s | 65 |
| tsgo | 7.0.2 | 1.64s | 118,865 LOC/s | 65 |
| xtsc-native | f4add10797ae (AOT) | 7.03s | 27,695 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.61x FASTER than tsc; 2.57x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.12s**, **-2.2%** against `temurin26` on identical bytecode — 0.38× tsc, 2.52× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.97×** tsc and **13.22×** tsgo; tsc is **6.71×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.54s**.

Ahead-of-time (GraalVM native-image): **7.03s** — **0.64×** tsc, **4.29×** tsgo, **3.08×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | f4add10797ae | **5.09s** | 38,236 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | f4add10797ae | **4.70s** | 41,443 LOC/s | 46 |
| xtsc (cold JVM) | f4add10797ae | 28.38s | 6,860 LOC/s | 46 |
| tsc | 6.0.3 | 12.90s | 15,094 LOC/s | 65 |
| tsgo | 7.0.2 | 2.37s | 82,222 LOC/s | 65 |
| xtsc-native | f4add10797ae (AOT) | 8.20s | 23,755 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.53x FASTER than tsc; 2.15x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.70s**, **-7.7%** against `temurin26` on identical bytecode — 0.36× tsc, 1.98× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.20×** tsc and **11.98×** tsgo; tsc is **5.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.26s**.

Ahead-of-time (GraalVM native-image): **8.20s** — **0.64×** tsc, **3.46×** tsgo, **3.46×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
