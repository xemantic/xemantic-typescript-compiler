# Bench run 20260827T141458Z

- **xtsc revision**: [`1c93ec233f7b`](https://github.com/xemantic/xemantic-typescript-compiler/commit/1c93ec233f7b5ae59246864a947af535855c81c4)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 1c93ec233f7b5ae59246864a947af535855c81c4

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1c93ec233f7b | **4.17s** | 46,724 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1c93ec233f7b | **3.96s** | 49,105 LOC/s | 46 |
| xtsc (cold JVM) | 1c93ec233f7b | 21.08s | 9,235 LOC/s | 46 |
| tsc | 6.0.3 | 10.65s | 18,281 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,657 LOC/s | 65 |
| xtsc-native | 1c93ec233f7b (AOT) | 6.42s | 30,313 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.56x FASTER than tsc; 2.50x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.96s**, **-4.8%** against `temurin26` on identical bytecode — 0.37× tsc, 2.38× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.98×** tsc and **12.63×** tsgo; tsc is **6.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.97s**.

Ahead-of-time (GraalVM native-image): **6.42s** — **0.60×** tsc, **3.85×** tsgo, **3.28×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1c93ec233f7b | **5.04s** | 38,600 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1c93ec233f7b | **4.66s** | 41,826 LOC/s | 46 |
| xtsc (cold JVM) | 1c93ec233f7b | 25.71s | 7,573 LOC/s | 46 |
| tsc | 6.0.3 | 13.05s | 14,923 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,945 LOC/s | 65 |
| xtsc-native | 1c93ec233f7b (AOT) | 7.55s | 25,781 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.59x FASTER than tsc; 2.12x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.66s**, **-7.7%** against `temurin26` on identical bytecode — 0.36× tsc, 1.96× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.97×** tsc and **10.82×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.60s**.

Ahead-of-time (GraalVM native-image): **7.55s** — **0.58×** tsc, **3.18×** tsgo, **3.40×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
