# Bench run 20260923T042537Z

- **xtsc revision**: [`185969d718a5`](https://github.com/xemantic/xemantic-typescript-compiler/commit/185969d718a54a95e33bc3089cc76a2387d83db6)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 185969d718a54a95e33bc3089cc76a2387d83db6

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 185969d718a5 | **5.95s** | 32,739 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 185969d718a5 | **5.48s** | 35,523 LOC/s | 46 |
| xtsc (cold JVM) | 185969d718a5 | 27.75s | 7,017 LOC/s | 46 |
| tsc | 6.0.3 | 11.01s | 17,692 LOC/s | 65 |
| tsgo | 7.0.2 | 1.72s | 113,198 LOC/s | 65 |
| xtsc-native | 185969d718a5 (AOT) | 9.56s | 20,372 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.85x FASTER than tsc; 3.46x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.48s**, **-7.8%** against `temurin26` on identical bytecode — 0.50× tsc, 3.19× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.52×** tsc and **16.13×** tsgo; tsc is **6.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.61s**.

Ahead-of-time (GraalVM native-image): **9.56s** — **0.87×** tsc, **5.56×** tsgo, **2.90×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 185969d718a5 | **6.88s** | 28,279 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 185969d718a5 | **6.26s** | 31,112 LOC/s | 46 |
| xtsc (cold JVM) | 185969d718a5 | 29.50s | 6,599 LOC/s | 46 |
| tsc | 6.0.3 | 13.65s | 14,260 LOC/s | 65 |
| tsgo | 7.0.2 | 2.49s | 78,193 LOC/s | 65 |
| xtsc-native | 185969d718a5 (AOT) | 11.29s | 17,242 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.98x FASTER than tsc; 2.77x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.26s**, **-9.1%** against `temurin26` on identical bytecode — 0.46× tsc, 2.51× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.16×** tsc and **11.85×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.37s**.

Ahead-of-time (GraalVM native-image): **11.29s** — **0.83×** tsc, **4.53×** tsgo, **2.61×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
