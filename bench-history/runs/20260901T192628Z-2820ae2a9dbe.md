# Bench run 20260901T192628Z

- **xtsc revision**: [`2820ae2a9dbe`](https://github.com/xemantic/xemantic-typescript-compiler/commit/2820ae2a9dbe46856cc68c2b3055d1f05e7216a8)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 2820ae2a9dbe46856cc68c2b3055d1f05e7216a8

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 2820ae2a9dbe | **4.35s** | 44,728 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 2820ae2a9dbe | **4.07s** | 47,850 LOC/s | 46 |
| xtsc (cold JVM) | 2820ae2a9dbe | 24.80s | 7,852 LOC/s | 46 |
| tsc | 6.0.3 | 10.75s | 18,113 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,550 LOC/s | 65 |
| xtsc-native | 2820ae2a9dbe (AOT) | 6.83s | 28,498 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.47x FASTER than tsc; 2.58x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.07s**, **-6.5%** against `temurin26` on identical bytecode — 0.38× tsc, 2.41× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.31×** tsc and **14.72×** tsgo; tsc is **6.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.68s**.

Ahead-of-time (GraalVM native-image): **6.83s** — **0.64×** tsc, **4.05×** tsgo, **3.63×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 2820ae2a9dbe | **5.39s** | 36,122 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 2820ae2a9dbe | **4.87s** | 39,988 LOC/s | 46 |
| xtsc (cold JVM) | 2820ae2a9dbe | 29.36s | 6,630 LOC/s | 46 |
| tsc | 6.0.3 | 13.22s | 14,724 LOC/s | 65 |
| tsgo | 7.0.2 | 2.37s | 82,048 LOC/s | 65 |
| xtsc-native | 2820ae2a9dbe (AOT) | 8.27s | 23,551 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.45x FASTER than tsc; 2.27x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.87s**, **-9.7%** against `temurin26` on identical bytecode — 0.37× tsc, 2.05× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.22×** tsc and **12.37×** tsgo; tsc is **5.57×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.23s**.

Ahead-of-time (GraalVM native-image): **8.27s** — **0.63×** tsc, **3.48×** tsgo, **3.55×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
