# Bench run 20260801T101046Z

- **xtsc revision**: [`3cbdeebd83e0`](https://github.com/xemantic/xemantic-typescript-compiler/commit/3cbdeebd83e0b57b5930eb014426c40ba8df870a)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 3cbdeebd83e0b57b5930eb014426c40ba8df870a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 3cbdeebd83e0 | 29.87s | 6,518 LOC/s | 46 |
| tsc | 6.0.3 | 10.79s | 18,049 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.65s | 118,359 LOC/s | 65 |
| xtsc-native | 3cbdeebd83e0 (AOT) | 12.69s | 15,342 LOC/s | 46 |

Relative (wall): xtsc is **2.77×** tsc and **18.16×** tsgo; tsc is **6.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.77s**.

Ahead-of-time (GraalVM native-image): **12.69s**, **2.35×** the JVM arm, **1.18×** tsc, **7.71×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 3cbdeebd83e0 | 30.07s | 6,474 LOC/s | 46 |
| tsc | 6.0.3 | 13.23s | 14,712 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.38s | 81,807 LOC/s | 65 |
| xtsc-native | 3cbdeebd83e0 (AOT) | 14.22s | 13,690 LOC/s | 46 |

Relative (wall): xtsc is **2.27×** tsc and **12.64×** tsgo; tsc is **5.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.97s**.

Ahead-of-time (GraalVM native-image): **14.22s**, **2.11×** the JVM arm, **1.07×** tsc, **5.98×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
