# Bench run 20260811T213327Z

- **xtsc revision**: [`0c34fa4e81e5`](https://github.com/xemantic/xemantic-typescript-compiler/commit/0c34fa4e81e514ad9f4fefafd6b55b31218a6b47)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 0c34fa4e81e514ad9f4fefafd6b55b31218a6b47

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0c34fa4e81e5 | **4.02s** | 48,409 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0c34fa4e81e5 | **3.73s** | 52,156 LOC/s | 46 |
| xtsc (cold JVM) | 0c34fa4e81e5 | 24.42s | 7,973 LOC/s | 46 |
| tsc | 6.0.3 | 10.62s | 18,328 LOC/s | 65 |
| tsgo | 7.0.2 | 1.66s | 117,644 LOC/s | 65 |
| xtsc-native | 0c34fa4e81e5 (AOT) | 6.05s | 32,166 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.64x FASTER than tsc; 2.43x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.73s**, **-7.2%** against `temurin26` on identical bytecode — 0.35× tsc, 2.26× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.30×** tsc and **14.75×** tsgo; tsc is **6.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.29s**.

Ahead-of-time (GraalVM native-image): **6.05s** — **0.57×** tsc, **3.66×** tsgo, **4.03×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0c34fa4e81e5 | **5.15s** | 37,806 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0c34fa4e81e5 | **4.43s** | 43,921 LOC/s | 46 |
| xtsc (cold JVM) | 0c34fa4e81e5 | 25.42s | 7,660 LOC/s | 46 |
| tsc | 6.0.3 | 13.02s | 14,950 LOC/s | 65 |
| tsgo | 7.0.2 | 2.41s | 80,755 LOC/s | 65 |
| xtsc-native | 0c34fa4e81e5 (AOT) | 7.18s | 27,105 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.53x FASTER than tsc; 2.14x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.43s**, **-13.9%** against `temurin26` on identical bytecode — 0.34× tsc, 1.84× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.95×** tsc and **10.54×** tsgo; tsc is **5.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.26s**.

Ahead-of-time (GraalVM native-image): **7.18s** — **0.55×** tsc, **2.98×** tsgo, **3.54×** the COLD JVM arm and **1.39×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
