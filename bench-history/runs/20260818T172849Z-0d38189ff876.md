# Bench run 20260818T172849Z

- **xtsc revision**: [`0d38189ff876`](https://github.com/xemantic/xemantic-typescript-compiler/commit/0d38189ff8763e22815d92614741473e4cf21684)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 0d38189ff8763e22815d92614741473e4cf21684

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0d38189ff876 | **2.20s** | 88,541 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0d38189ff876 | **2.06s** | 94,423 LOC/s | 46 |
| xtsc (cold JVM) | 0d38189ff876 | 12.79s | 15,225 LOC/s | 46 |
| tsc | 6.0.3 | 5.88s | 33,140 LOC/s | 65 |
| tsgo | 7.0.2 | 0.91s | 214,665 LOC/s | 65 |
| xtsc-native | 0d38189ff876 (AOT) | 3.48s | 55,932 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.67x FASTER than tsc; 2.42x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.06s**, **-6.2%** against `temurin26` on identical bytecode — 0.35× tsc, 2.27× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.18×** tsc and **14.10×** tsgo; tsc is **6.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **12.71s**.

Ahead-of-time (GraalVM native-image): **3.48s** — **0.59×** tsc, **3.84×** tsgo, **3.67×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0d38189ff876 | **2.71s** | 71,819 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0d38189ff876 | **2.55s** | 76,443 LOC/s | 46 |
| xtsc (cold JVM) | 0d38189ff876 | 15.39s | 12,647 LOC/s | 46 |
| tsc | 6.0.3 | 7.22s | 26,948 LOC/s | 65 |
| tsgo | 7.0.2 | 1.34s | 145,083 LOC/s | 65 |
| xtsc-native | 0d38189ff876 (AOT) | 4.30s | 45,269 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.67x FASTER than tsc; 2.02x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.55s**, **-6.0%** against `temurin26` on identical bytecode — 0.35× tsc, 1.90× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.13×** tsc and **11.47×** tsgo; tsc is **5.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **15.32s**.

Ahead-of-time (GraalVM native-image): **4.30s** — **0.60×** tsc, **3.20×** tsgo, **3.58×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
