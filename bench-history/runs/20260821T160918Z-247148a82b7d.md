# Bench run 20260821T160918Z

- **xtsc revision**: [`247148a82b7d`](https://github.com/xemantic/xemantic-typescript-compiler/commit/247148a82b7ddef509bce93f74e045926306c075)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 247148a82b7ddef509bce93f74e045926306c075

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 247148a82b7d | **4.37s** | 44,554 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 247148a82b7d | **4.19s** | 46,423 LOC/s | 46 |
| xtsc (cold JVM) | 247148a82b7d | 24.78s | 7,856 LOC/s | 46 |
| tsc | 6.0.3 | 11.55s | 16,854 LOC/s | 65 |
| tsgo | 7.0.2 | 1.78s | 109,321 LOC/s | 65 |
| xtsc-native | 247148a82b7d (AOT) | 6.94s | 28,067 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.64x FASTER than tsc; 2.45x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.19s**, **-4.0%** against `temurin26` on identical bytecode — 0.36× tsc, 2.35× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.15×** tsc and **13.91×** tsgo; tsc is **6.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.67s**.

Ahead-of-time (GraalVM native-image): **6.94s** — **0.60×** tsc, **3.90×** tsgo, **3.57×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 247148a82b7d | **5.31s** | 36,653 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 247148a82b7d | **4.79s** | 40,681 LOC/s | 46 |
| xtsc (cold JVM) | 247148a82b7d | 27.68s | 7,033 LOC/s | 46 |
| tsc | 6.0.3 | 14.08s | 13,827 LOC/s | 65 |
| tsgo | 7.0.2 | 2.50s | 77,911 LOC/s | 65 |
| xtsc-native | 247148a82b7d (AOT) | 8.01s | 24,313 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.65x FASTER than tsc; 2.13x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.79s**, **-9.9%** against `temurin26` on identical bytecode — 0.34× tsc, 1.92× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.97×** tsc and **11.08×** tsgo; tsc is **5.63×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.54s**.

Ahead-of-time (GraalVM native-image): **8.01s** — **0.57×** tsc, **3.20×** tsgo, **3.46×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
