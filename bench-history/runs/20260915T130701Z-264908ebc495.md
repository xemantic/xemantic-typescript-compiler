# Bench run 20260915T130701Z

- **xtsc revision**: [`264908ebc495`](https://github.com/xemantic/xemantic-typescript-compiler/commit/264908ebc495dfe9d968ad6fbd9f9e795e2bcfe6)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 264908ebc495dfe9d968ad6fbd9f9e795e2bcfe6

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 264908ebc495 | **5.94s** | 32,761 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 264908ebc495 | **5.43s** | 35,850 LOC/s | 46 |
| xtsc (cold JVM) | 264908ebc495 | 27.94s | 6,968 LOC/s | 46 |
| tsc | 6.0.3 | 11.01s | 17,680 LOC/s | 65 |
| tsgo | 7.0.2 | 1.71s | 113,661 LOC/s | 65 |
| xtsc-native | 264908ebc495 (AOT) | 9.36s | 20,808 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.85x FASTER than tsc; 3.47x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.43s**, **-8.6%** against `temurin26` on identical bytecode — 0.49× tsc, 3.17× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.54×** tsc and **16.31×** tsgo; tsc is **6.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.82s**.

Ahead-of-time (GraalVM native-image): **9.36s** — **0.85×** tsc, **5.46×** tsgo, **2.99×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 264908ebc495 | **6.74s** | 28,904 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 264908ebc495 | **6.07s** | 32,070 LOC/s | 46 |
| xtsc (cold JVM) | 264908ebc495 | 28.74s | 6,775 LOC/s | 46 |
| tsc | 6.0.3 | 14.06s | 13,845 LOC/s | 65 |
| tsgo | 7.0.2 | 2.56s | 75,996 LOC/s | 65 |
| xtsc-native | 264908ebc495 (AOT) | 11.66s | 16,696 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.09x FASTER than tsc; 2.63x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.07s**, **-9.9%** against `temurin26` on identical bytecode — 0.43× tsc, 2.37× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.04×** tsc and **11.22×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.59s**.

Ahead-of-time (GraalVM native-image): **11.66s** — **0.83×** tsc, **4.55×** tsgo, **2.46×** the COLD JVM arm and **1.73×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
