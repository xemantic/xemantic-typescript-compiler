# Bench run 20260801T054818Z

- **xtsc revision**: [`fb08e42bc9c4`](https://github.com/xemantic/xemantic-typescript-compiler/commit/fb08e42bc9c428769cfd2310bfdbce72c71d8845)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push fb08e42bc9c428769cfd2310bfdbce72c71d8845

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | fb08e42bc9c4 | 32.38s | 6,013 LOC/s | 46 |
| tsc | 6.0.3 | 10.97s | 17,755 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.73s | 112,805 LOC/s | 65 |
| xtsc-native | fb08e42bc9c4 (AOT) | 12.48s | 15,602 LOC/s | 46 |

Relative (wall): xtsc is **2.95×** tsc and **18.76×** tsgo; tsc is **6.35×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **32.26s**.

Ahead-of-time (GraalVM native-image): **12.48s**, **2.59×** the JVM arm, **1.14×** tsc, **7.23×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | fb08e42bc9c4 | 33.74s | 5,771 LOC/s | 46 |
| tsc | 6.0.3 | 13.06s | 14,909 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.37s | 82,187 LOC/s | 65 |
| xtsc-native | fb08e42bc9c4 (AOT) | 14.82s | 13,136 LOC/s | 46 |

Relative (wall): xtsc is **2.58×** tsc and **14.24×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **33.64s**.

Ahead-of-time (GraalVM native-image): **14.82s**, **2.28×** the JVM arm, **1.13×** tsc, **6.26×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
