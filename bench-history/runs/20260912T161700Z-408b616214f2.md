# Bench run 20260912T161700Z

- **xtsc revision**: [`408b616214f2`](https://github.com/xemantic/xemantic-typescript-compiler/commit/408b616214f2b71847700ea53d449eff68838c67)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 408b616214f2b71847700ea53d449eff68838c67

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 408b616214f2 | **6.01s** | 32,407 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 408b616214f2 | **5.33s** | 36,495 LOC/s | 46 |
| xtsc (cold JVM) | 408b616214f2 | 26.74s | 7,281 LOC/s | 46 |
| tsc | 6.0.3 | 11.20s | 17,390 LOC/s | 65 |
| tsgo | 7.0.2 | 1.77s | 109,939 LOC/s | 65 |
| xtsc-native | 408b616214f2 (AOT) | 9.53s | 20,428 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.86x FASTER than tsc; 3.39x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.33s**, **-11.2%** against `temurin26` on identical bytecode — 0.48× tsc, 3.01× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.39×** tsc and **15.10×** tsgo; tsc is **6.32×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.60s**.

Ahead-of-time (GraalVM native-image): **9.53s** — **0.85×** tsc, **5.38×** tsgo, **2.81×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 408b616214f2 | **6.73s** | 28,921 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 408b616214f2 | **6.06s** | 32,129 LOC/s | 46 |
| xtsc (cold JVM) | 408b616214f2 | 30.98s | 6,284 LOC/s | 46 |
| tsc | 6.0.3 | 13.79s | 14,118 LOC/s | 65 |
| tsgo | 7.0.2 | 2.54s | 76,503 LOC/s | 65 |
| xtsc-native | 408b616214f2 (AOT) | 11.14s | 17,469 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.05x FASTER than tsc; 2.65x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.06s**, **-10.0%** against `temurin26` on identical bytecode — 0.44× tsc, 2.38× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.25×** tsc and **12.17×** tsgo; tsc is **5.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.84s**.

Ahead-of-time (GraalVM native-image): **11.14s** — **0.81×** tsc, **4.38×** tsgo, **2.78×** the COLD JVM arm and **1.66×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
