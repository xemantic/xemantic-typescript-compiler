# Bench run 20260901T110239Z

- **xtsc revision**: [`526551a23e84`](https://github.com/xemantic/xemantic-typescript-compiler/commit/526551a23e846e66fe342a3c6648f329a18da2b9)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 526551a23e846e66fe342a3c6648f329a18da2b9

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 526551a23e84 | **4.17s** | 46,646 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 526551a23e84 | **3.99s** | 48,797 LOC/s | 46 |
| xtsc (cold JVM) | 526551a23e84 | 22.54s | 8,637 LOC/s | 46 |
| tsc | 6.0.3 | 10.66s | 18,257 LOC/s | 65 |
| tsgo | 7.0.2 | 1.71s | 113,595 LOC/s | 65 |
| xtsc-native | 526551a23e84 (AOT) | 6.96s | 27,978 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.55x FASTER than tsc; 2.44x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.99s**, **-4.4%** against `temurin26` on identical bytecode — 0.37× tsc, 2.33× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.11×** tsc and **13.15×** tsgo; tsc is **6.22×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.43s**.

Ahead-of-time (GraalVM native-image): **6.96s** — **0.65×** tsc, **4.06×** tsgo, **3.24×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 526551a23e84 | **4.99s** | 39,002 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 526551a23e84 | **4.72s** | 41,276 LOC/s | 46 |
| xtsc (cold JVM) | 526551a23e84 | 25.91s | 7,515 LOC/s | 46 |
| tsc | 6.0.3 | 13.40s | 14,525 LOC/s | 65 |
| tsgo | 7.0.2 | 2.47s | 78,954 LOC/s | 65 |
| xtsc-native | 526551a23e84 (AOT) | 8.71s | 22,366 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.69x FASTER than tsc; 2.02x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.72s**, **-5.5%** against `temurin26` on identical bytecode — 0.35× tsc, 1.91× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.93×** tsc and **10.51×** tsgo; tsc is **5.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.78s**.

Ahead-of-time (GraalVM native-image): **8.71s** — **0.65×** tsc, **3.53×** tsgo, **2.98×** the COLD JVM arm and **1.74×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
