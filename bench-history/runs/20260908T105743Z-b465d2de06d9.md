# Bench run 20260908T105743Z

- **xtsc revision**: [`b465d2de06d9`](https://github.com/xemantic/xemantic-typescript-compiler/commit/b465d2de06d97c9dde25e293a07acc069974f9d4)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push b465d2de06d97c9dde25e293a07acc069974f9d4

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b465d2de06d9 | **5.26s** | 37,022 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b465d2de06d9 | **4.87s** | 39,963 LOC/s | 46 |
| xtsc (cold JVM) | b465d2de06d9 | 25.47s | 7,643 LOC/s | 46 |
| tsc | 6.0.3 | 10.15s | 19,184 LOC/s | 65 |
| tsgo | 7.0.2 | 1.56s | 124,969 LOC/s | 65 |
| xtsc-native | b465d2de06d9 (AOT) | 8.66s | 22,493 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.93x FASTER than tsc; 3.38x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.87s**, **-7.4%** against `temurin26` on identical bytecode — 0.48× tsc, 3.13× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.51×** tsc and **16.35×** tsgo; tsc is **6.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.36s**.

Ahead-of-time (GraalVM native-image): **8.66s** — **0.85×** tsc, **5.56×** tsgo, **2.94×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b465d2de06d9 | **6.48s** | 30,028 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b465d2de06d9 | **5.64s** | 34,503 LOC/s | 46 |
| xtsc (cold JVM) | b465d2de06d9 | 27.52s | 7,075 LOC/s | 46 |
| tsc | 6.0.3 | 12.38s | 15,727 LOC/s | 65 |
| tsgo | 7.0.2 | 2.25s | 86,495 LOC/s | 65 |
| xtsc-native | b465d2de06d9 (AOT) | 9.80s | 19,863 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.91x FASTER than tsc; 2.88x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.64s**, **-13.0%** against `temurin26` on identical bytecode — 0.46× tsc, 2.51× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.22×** tsc and **12.22×** tsgo; tsc is **5.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.39s**.

Ahead-of-time (GraalVM native-image): **9.80s** — **0.79×** tsc, **4.35×** tsgo, **2.81×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
