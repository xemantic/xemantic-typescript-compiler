# Bench run 20260812T171928Z

- **xtsc revision**: [`75fd1b40bb03`](https://github.com/xemantic/xemantic-typescript-compiler/commit/75fd1b40bb0326f3d04ea8ceda187eae7a7df1aa)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 75fd1b40bb0326f3d04ea8ceda187eae7a7df1aa

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 75fd1b40bb03 | **3.14s** | 61,947 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 75fd1b40bb03 | **2.96s** | 65,755 LOC/s | 46 |
| xtsc (cold JVM) | 75fd1b40bb03 | 19.64s | 9,915 LOC/s | 46 |
| tsc | 6.0.3 | 8.02s | 24,264 LOC/s | 65 |
| tsgo | 7.0.2 | 1.26s | 153,914 LOC/s | 65 |
| xtsc-native | 75fd1b40bb03 (AOT) | 4.91s | 39,654 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.55x FASTER than tsc; 2.48x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.96s**, **-5.8%** against `temurin26` on identical bytecode — 0.37× tsc, 2.34× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.45×** tsc and **15.52×** tsgo; tsc is **6.34×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.54s**.

Ahead-of-time (GraalVM native-image): **4.91s** — **0.61×** tsc, **3.88×** tsgo, **4.00×** the COLD JVM arm and **1.56×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 75fd1b40bb03 | **3.91s** | 49,795 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 75fd1b40bb03 | **3.60s** | 54,113 LOC/s | 46 |
| xtsc (cold JVM) | 75fd1b40bb03 | 20.62s | 9,441 LOC/s | 46 |
| tsc | 6.0.3 | 9.81s | 19,847 LOC/s | 65 |
| tsgo | 7.0.2 | 1.78s | 109,444 LOC/s | 65 |
| xtsc-native | 75fd1b40bb03 (AOT) | 5.92s | 32,899 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.51x FASTER than tsc; 2.20x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.60s**, **-8.0%** against `temurin26` on identical bytecode — 0.37× tsc, 2.02× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.10×** tsc and **11.59×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.50s**.

Ahead-of-time (GraalVM native-image): **5.92s** — **0.60×** tsc, **3.33×** tsgo, **3.48×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
