# Bench run 20260830T222158Z

- **xtsc revision**: [`b511fe186d73`](https://github.com/xemantic/xemantic-typescript-compiler/commit/b511fe186d73d2d6eb4dd35b733f166b4142b438)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push b511fe186d73d2d6eb4dd35b733f166b4142b438

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b511fe186d73 | **4.28s** | 45,512 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b511fe186d73 | **3.93s** | 49,580 LOC/s | 46 |
| xtsc (cold JVM) | b511fe186d73 | 21.77s | 8,945 LOC/s | 46 |
| tsc | 6.0.3 | 10.32s | 18,873 LOC/s | 65 |
| tsgo | 7.0.2 | 1.55s | 125,452 LOC/s | 65 |
| xtsc-native | b511fe186d73 (AOT) | 6.39s | 30,450 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.41x FASTER than tsc; 2.76x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.93s**, **-8.2%** against `temurin26` on identical bytecode — 0.38× tsc, 2.53× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.11×** tsc and **14.02×** tsgo; tsc is **6.65×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.66s**.

Ahead-of-time (GraalVM native-image): **6.39s** — **0.62×** tsc, **4.12×** tsgo, **3.40×** the COLD JVM arm and **1.49×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b511fe186d73 | **5.00s** | 38,940 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b511fe186d73 | **4.63s** | 42,015 LOC/s | 46 |
| xtsc (cold JVM) | b511fe186d73 | 27.47s | 7,088 LOC/s | 46 |
| tsc | 6.0.3 | 12.43s | 15,660 LOC/s | 65 |
| tsgo | 7.0.2 | 2.27s | 85,923 LOC/s | 65 |
| xtsc-native | b511fe186d73 (AOT) | 7.93s | 24,555 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.49x FASTER than tsc; 2.21x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.63s**, **-7.3%** against `temurin26` on identical bytecode — 0.37× tsc, 2.05× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.21×** tsc and **12.12×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.32s**.

Ahead-of-time (GraalVM native-image): **7.93s** — **0.64×** tsc, **3.50×** tsgo, **3.46×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
