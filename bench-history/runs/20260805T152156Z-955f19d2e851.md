# Bench run 20260805T152156Z

- **xtsc revision**: [`955f19d2e851`](https://github.com/xemantic/xemantic-typescript-compiler/commit/955f19d2e851c5d9a719b4103d39f9042bd14615)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 955f19d2e851c5d9a719b4103d39f9042bd14615

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 955f19d2e851 | 19.07s | 10,210 LOC/s | 46 |
| tsc | 6.0.3 | 8.08s | 24,102 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.25s | 156,387 LOC/s | 65 |
| xtsc-native | 955f19d2e851 (AOT) | 9.59s | 20,294 LOC/s | 46 |

Relative (wall): xtsc is **2.36×** tsc and **15.32×** tsgo; tsc is **6.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.99s**.

Ahead-of-time (GraalVM native-image): **9.59s**, **1.99×** the JVM arm, **1.19×** tsc, **7.71×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 955f19d2e851 | 22.32s | 8,723 LOC/s | 46 |
| tsc | 6.0.3 | 10.07s | 19,334 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.93s | 100,725 LOC/s | 65 |
| xtsc-native | 955f19d2e851 (AOT) | 10.14s | 19,205 LOC/s | 46 |

Relative (wall): xtsc is **2.22×** tsc and **11.55×** tsgo; tsc is **5.21×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.24s**.

Ahead-of-time (GraalVM native-image): **10.14s**, **2.20×** the JVM arm, **1.01×** tsc, **5.24×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
