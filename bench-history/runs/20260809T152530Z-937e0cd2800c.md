# Bench run 20260809T152530Z

- **xtsc revision**: [`937e0cd2800c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/937e0cd2800cc711669ccc48c10c9ff340d382e6)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push 937e0cd2800cc711669ccc48c10c9ff340d382e6

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 937e0cd2800c | 26.15s | 7,446 LOC/s | 46 |
| tsc | 6.0.3 | 10.82s | 17,992 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.66s | 117,008 LOC/s | 65 |
| xtsc-native | 937e0cd2800c (AOT) | 9.44s | 20,629 LOC/s | 46 |

Relative (wall): xtsc is **2.42×** tsc and **15.71×** tsgo; tsc is **6.50×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.00s**.

Ahead-of-time (GraalVM native-image): **9.44s**, **2.77×** the JVM arm, **0.87×** tsc, **5.67×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | 937e0cd2800c | 28.57s | 6,814 LOC/s | 46 |
| tsc | 6.0.3 | 13.22s | 14,730 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.46s | 79,276 LOC/s | 65 |
| xtsc-native | 937e0cd2800c (AOT) | 11.60s | 16,786 LOC/s | 46 |

Relative (wall): xtsc is **2.16×** tsc and **11.63×** tsgo; tsc is **5.38×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.46s**.

Ahead-of-time (GraalVM native-image): **11.60s**, **2.46×** the JVM arm, **0.88×** tsc, **4.72×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
