# Bench run 20260822T154539Z

- **xtsc revision**: [`b021c8dec7be`](https://github.com/xemantic/xemantic-typescript-compiler/commit/b021c8dec7bed21177c70ce65e0e1aa57210bd6a)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push b021c8dec7bed21177c70ce65e0e1aa57210bd6a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b021c8dec7be | **3.96s** | 49,105 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b021c8dec7be | **3.78s** | 51,508 LOC/s | 46 |
| xtsc (cold JVM) | b021c8dec7be | 22.69s | 8,582 LOC/s | 46 |
| tsc | 6.0.3 | 10.91s | 17,844 LOC/s | 65 |
| tsgo | 7.0.2 | 1.76s | 110,563 LOC/s | 65 |
| xtsc-native | b021c8dec7be (AOT) | 6.29s | 30,978 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.75x FASTER than tsc; 2.25x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.78s**, **-4.7%** against `temurin26` on identical bytecode — 0.35× tsc, 2.15× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.08×** tsc and **12.88×** tsgo; tsc is **6.20×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.57s**.

Ahead-of-time (GraalVM native-image): **6.29s** — **0.58×** tsc, **3.57×** tsgo, **3.61×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | b021c8dec7be | **4.86s** | 40,062 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | b021c8dec7be | **4.46s** | 43,616 LOC/s | 46 |
| xtsc (cold JVM) | b021c8dec7be | 25.00s | 7,786 LOC/s | 46 |
| tsc | 6.0.3 | 13.57s | 14,352 LOC/s | 65 |
| tsgo | 7.0.2 | 2.43s | 80,256 LOC/s | 65 |
| xtsc-native | b021c8dec7be (AOT) | 7.24s | 26,885 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.79x FASTER than tsc; 2.00x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.46s**, **-8.1%** against `temurin26` on identical bytecode — 0.33× tsc, 1.84× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.84×** tsc and **10.31×** tsgo; tsc is **5.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.87s**.

Ahead-of-time (GraalVM native-image): **7.24s** — **0.53×** tsc, **2.99×** tsgo, **3.45×** the COLD JVM arm and **1.49×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
