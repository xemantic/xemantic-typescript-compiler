# Bench run 20260830T143907Z

- **xtsc revision**: [`145a9475404a`](https://github.com/xemantic/xemantic-typescript-compiler/commit/145a9475404a548cb275c8fb9f77fc2b0211e480)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 145a9475404a548cb275c8fb9f77fc2b0211e480

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 145a9475404a | **4.38s** | 44,472 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 145a9475404a | **4.22s** | 46,137 LOC/s | 46 |
| xtsc (cold JVM) | 145a9475404a | 24.91s | 7,815 LOC/s | 46 |
| tsc | 6.0.3 | 10.49s | 18,553 LOC/s | 65 |
| tsgo | 7.0.2 | 1.60s | 121,917 LOC/s | 65 |
| xtsc-native | 145a9475404a (AOT) | 7.55s | 25,791 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.40x FASTER than tsc; 2.74x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.22s**, **-3.6%** against `temurin26` on identical bytecode — 0.40× tsc, 2.64× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.37×** tsc and **15.60×** tsgo; tsc is **6.57×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.79s**.

Ahead-of-time (GraalVM native-image): **7.55s** — **0.72×** tsc, **4.73×** tsgo, **3.30×** the COLD JVM arm and **1.72×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 145a9475404a | **4.92s** | 39,549 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 145a9475404a | **4.75s** | 40,981 LOC/s | 46 |
| xtsc (cold JVM) | 145a9475404a | 27.33s | 7,123 LOC/s | 46 |
| tsc | 6.0.3 | 12.70s | 15,334 LOC/s | 65 |
| tsgo | 7.0.2 | 2.30s | 84,542 LOC/s | 65 |
| xtsc-native | 145a9475404a (AOT) | 8.31s | 23,421 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.58x FASTER than tsc; 2.14x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.75s**, **-3.5%** against `temurin26` on identical bytecode — 0.37× tsc, 2.06× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.15×** tsc and **11.87×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.21s**.

Ahead-of-time (GraalVM native-image): **8.31s** — **0.65×** tsc, **3.61×** tsgo, **3.29×** the COLD JVM arm and **1.69×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
