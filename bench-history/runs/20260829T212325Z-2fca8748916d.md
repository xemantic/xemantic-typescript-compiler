# Bench run 20260829T212325Z

- **xtsc revision**: [`2fca8748916d`](https://github.com/xemantic/xemantic-typescript-compiler/commit/2fca8748916dfe284a88bc9b6a3d94bf011a627c)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 2fca8748916dfe284a88bc9b6a3d94bf011a627c

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 2fca8748916d | **4.65s** | 41,871 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 2fca8748916d | **4.36s** | 44,635 LOC/s | 46 |
| xtsc (cold JVM) | 2fca8748916d | 24.49s | 7,950 LOC/s | 46 |
| tsc | 6.0.3 | 11.32s | 17,192 LOC/s | 65 |
| tsgo | 7.0.2 | 1.76s | 110,375 LOC/s | 65 |
| xtsc-native | 2fca8748916d (AOT) | 7.46s | 26,102 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.44x FASTER than tsc; 2.64x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.36s**, **-6.2%** against `temurin26` on identical bytecode — 0.39× tsc, 2.47× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.16×** tsc and **13.88×** tsgo; tsc is **6.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.37s**.

Ahead-of-time (GraalVM native-image): **7.46s** — **0.66×** tsc, **4.23×** tsgo, **3.28×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 2fca8748916d | **5.49s** | 35,451 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 2fca8748916d | **4.99s** | 38,994 LOC/s | 46 |
| xtsc (cold JVM) | 2fca8748916d | 29.55s | 6,588 LOC/s | 46 |
| tsc | 6.0.3 | 13.93s | 13,982 LOC/s | 65 |
| tsgo | 7.0.2 | 2.56s | 76,114 LOC/s | 65 |
| xtsc-native | 2fca8748916d (AOT) | 8.91s | 21,852 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.54x FASTER than tsc; 2.15x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.99s**, **-9.1%** against `temurin26` on identical bytecode — 0.36× tsc, 1.95× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.12×** tsc and **11.55×** tsgo; tsc is **5.44×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.43s**.

Ahead-of-time (GraalVM native-image): **8.91s** — **0.64×** tsc, **3.48×** tsgo, **3.32×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
