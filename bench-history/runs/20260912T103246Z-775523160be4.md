# Bench run 20260912T103246Z

- **xtsc revision**: [`775523160be4`](https://github.com/xemantic/xemantic-typescript-compiler/commit/775523160be4b64f2d064e06a9e9da6c3b58f1e5)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 775523160be4b64f2d064e06a9e9da6c3b58f1e5

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 775523160be4 | **4.13s** | 47,120 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 775523160be4 | **3.85s** | 50,545 LOC/s | 46 |
| xtsc (cold JVM) | 775523160be4 | 19.70s | 9,881 LOC/s | 46 |
| tsc | 6.0.3 | 7.96s | 24,456 LOC/s | 65 |
| tsgo | 7.0.2 | 1.20s | 162,251 LOC/s | 65 |
| xtsc-native | 775523160be4 (AOT) | 6.83s | 28,527 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.93x FASTER than tsc; 3.44x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.85s**, **-6.8%** against `temurin26` on identical bytecode — 0.48× tsc, 3.21× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.47×** tsc and **16.42×** tsgo; tsc is **6.63×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.61s**.

Ahead-of-time (GraalVM native-image): **6.83s** — **0.86×** tsc, **5.69×** tsgo, **2.89×** the COLD JVM arm and **1.65×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 775523160be4 | **4.82s** | 40,386 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 775523160be4 | **4.33s** | 44,934 LOC/s | 46 |
| xtsc (cold JVM) | 775523160be4 | 20.29s | 9,598 LOC/s | 46 |
| tsc | 6.0.3 | 9.71s | 20,051 LOC/s | 65 |
| tsgo | 7.0.2 | 1.79s | 108,711 LOC/s | 65 |
| xtsc-native | 775523160be4 (AOT) | 7.81s | 24,926 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.01x FASTER than tsc; 2.69x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.33s**, **-10.1%** against `temurin26` on identical bytecode — 0.45× tsc, 2.42× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.09×** tsc and **11.33×** tsgo; tsc is **5.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.18s**.

Ahead-of-time (GraalVM native-image): **7.81s** — **0.80×** tsc, **4.36×** tsgo, **2.60×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
