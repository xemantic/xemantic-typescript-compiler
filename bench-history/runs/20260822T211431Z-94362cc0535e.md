# Bench run 20260822T211431Z

- **xtsc revision**: [`94362cc0535e`](https://github.com/xemantic/xemantic-typescript-compiler/commit/94362cc0535e3d294b6564748206bf3cb0bc3257)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 94362cc0535e3d294b6564748206bf3cb0bc3257

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 94362cc0535e | **4.07s** | 47,861 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 94362cc0535e | **3.85s** | 50,532 LOC/s | 46 |
| xtsc (cold JVM) | 94362cc0535e | 22.51s | 8,649 LOC/s | 46 |
| tsc | 6.0.3 | 10.99s | 17,708 LOC/s | 65 |
| tsgo | 7.0.2 | 1.73s | 112,544 LOC/s | 65 |
| xtsc-native | 94362cc0535e (AOT) | 6.42s | 30,303 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.70x FASTER than tsc; 2.35x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.85s**, **-5.3%** against `temurin26` on identical bytecode — 0.35× tsc, 2.23× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.05×** tsc and **13.01×** tsgo; tsc is **6.36×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.36s**.

Ahead-of-time (GraalVM native-image): **6.42s** — **0.58×** tsc, **3.71×** tsgo, **3.50×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 94362cc0535e | **4.91s** | 39,646 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 94362cc0535e | **4.50s** | 43,305 LOC/s | 46 |
| xtsc (cold JVM) | 94362cc0535e | 24.98s | 7,794 LOC/s | 46 |
| tsc | 6.0.3 | 13.49s | 14,430 LOC/s | 65 |
| tsgo | 7.0.2 | 2.46s | 79,050 LOC/s | 65 |
| xtsc-native | 94362cc0535e (AOT) | 7.81s | 24,923 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.75x FASTER than tsc; 1.99x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.50s**, **-8.5%** against `temurin26` on identical bytecode — 0.33× tsc, 1.83× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.85×** tsc and **10.14×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.86s**.

Ahead-of-time (GraalVM native-image): **7.81s** — **0.58×** tsc, **3.17×** tsgo, **3.20×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
