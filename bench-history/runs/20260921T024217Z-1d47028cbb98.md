# Bench run 20260921T024217Z

- **xtsc revision**: [`1d47028cbb98`](https://github.com/xemantic/xemantic-typescript-compiler/commit/1d47028cbb9815fb015761848e5720be3e33999a)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 1d47028cbb9815fb015761848e5720be3e33999a

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1d47028cbb98 | **5.75s** | 33,855 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1d47028cbb98 | **5.30s** | 36,763 LOC/s | 46 |
| xtsc (cold JVM) | 1d47028cbb98 | 25.88s | 7,523 LOC/s | 46 |
| tsc | 6.0.3 | 10.74s | 18,123 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,378 LOC/s | 65 |
| xtsc-native | 1d47028cbb98 (AOT) | 9.01s | 21,619 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.87x FASTER than tsc; 3.44x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.30s**, **-7.9%** against `temurin26` on identical bytecode — 0.49× tsc, 3.17× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.41×** tsc and **15.47×** tsgo; tsc is **6.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.76s**.

Ahead-of-time (GraalVM native-image): **9.01s** — **0.84×** tsc, **5.38×** tsgo, **2.87×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 1d47028cbb98 | **6.46s** | 30,116 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 1d47028cbb98 | **5.96s** | 32,651 LOC/s | 46 |
| xtsc (cold JVM) | 1d47028cbb98 | 31.59s | 6,163 LOC/s | 46 |
| tsc | 6.0.3 | 13.13s | 14,827 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,636 LOC/s | 65 |
| xtsc-native | 1d47028cbb98 (AOT) | 10.58s | 18,399 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.03x FASTER than tsc; 2.71x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.96s**, **-7.8%** against `temurin26` on identical bytecode — 0.45× tsc, 2.50× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.41×** tsc and **13.25×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **31.45s**.

Ahead-of-time (GraalVM native-image): **10.58s** — **0.81×** tsc, **4.44×** tsgo, **2.99×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
