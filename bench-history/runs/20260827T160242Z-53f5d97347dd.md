# Bench run 20260827T160242Z

- **xtsc revision**: [`53f5d97347dd`](https://github.com/xemantic/xemantic-typescript-compiler/commit/53f5d97347dd7499f1b4f62e6d8f369d38a329b6)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 53f5d97347dd7499f1b4f62e6d8f369d38a329b6

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 53f5d97347dd | **4.14s** | 47,018 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 53f5d97347dd | **3.87s** | 50,297 LOC/s | 46 |
| xtsc (cold JVM) | 53f5d97347dd | 21.37s | 9,110 LOC/s | 46 |
| tsc | 6.0.3 | 10.74s | 18,126 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,309 LOC/s | 65 |
| xtsc-native | 53f5d97347dd (AOT) | 6.28s | 30,983 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.59x FASTER than tsc; 2.47x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.87s**, **-6.5%** against `temurin26` on identical bytecode — 0.36× tsc, 2.31× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.99×** tsc and **12.77×** tsgo; tsc is **6.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.26s**.

Ahead-of-time (GraalVM native-image): **6.28s** — **0.59×** tsc, **3.75×** tsgo, **3.40×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 53f5d97347dd | **4.98s** | 39,096 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 53f5d97347dd | **4.55s** | 42,782 LOC/s | 46 |
| xtsc (cold JVM) | 53f5d97347dd | 25.77s | 7,554 LOC/s | 46 |
| tsc | 6.0.3 | 13.30s | 14,635 LOC/s | 65 |
| tsgo | 7.0.2 | 2.44s | 79,763 LOC/s | 65 |
| xtsc-native | 53f5d97347dd (AOT) | 7.61s | 25,571 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.67x FASTER than tsc; 2.04x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.55s**, **-8.6%** against `temurin26` on identical bytecode — 0.34× tsc, 1.86× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.94×** tsc and **10.56×** tsgo; tsc is **5.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.64s**.

Ahead-of-time (GraalVM native-image): **7.61s** — **0.57×** tsc, **3.12×** tsgo, **3.38×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
