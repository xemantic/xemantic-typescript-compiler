# Bench run 20260812T073504Z

- **xtsc revision**: [`50f713c9c0a1`](https://github.com/xemantic/xemantic-typescript-compiler/commit/50f713c9c0a11dcea9ff6fd0ad81efe21ba40fc7)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 50f713c9c0a11dcea9ff6fd0ad81efe21ba40fc7

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 50f713c9c0a1 | **3.94s** | 49,454 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 50f713c9c0a1 | **3.71s** | 52,536 LOC/s | 46 |
| xtsc (cold JVM) | 50f713c9c0a1 | 23.16s | 8,405 LOC/s | 46 |
| tsc | 6.0.3 | 10.80s | 18,029 LOC/s | 65 |
| tsgo | 7.0.2 | 1.73s | 112,414 LOC/s | 65 |
| xtsc-native | 50f713c9c0a1 (AOT) | 6.29s | 30,978 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.74x FASTER than tsc; 2.27x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.71s**, **-5.9%** against `temurin26` on identical bytecode — 0.34× tsc, 2.14× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.15×** tsc and **13.37×** tsgo; tsc is **6.23×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.06s**.

Ahead-of-time (GraalVM native-image): **6.29s** — **0.58×** tsc, **3.63×** tsgo, **3.69×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 50f713c9c0a1 | **4.93s** | 39,501 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 50f713c9c0a1 | **4.46s** | 43,606 LOC/s | 46 |
| xtsc (cold JVM) | 50f713c9c0a1 | 26.01s | 7,485 LOC/s | 46 |
| tsc | 6.0.3 | 13.10s | 14,865 LOC/s | 65 |
| tsgo | 7.0.2 | 2.40s | 81,227 LOC/s | 65 |
| xtsc-native | 50f713c9c0a1 (AOT) | 7.36s | 26,443 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.66x FASTER than tsc; 2.06x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.46s**, **-9.4%** against `temurin26` on identical bytecode — 0.34× tsc, 1.86× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.99×** tsc and **10.85×** tsgo; tsc is **5.46×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.89s**.

Ahead-of-time (GraalVM native-image): **7.36s** — **0.56×** tsc, **3.07×** tsgo, **3.53×** the COLD JVM arm and **1.49×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
