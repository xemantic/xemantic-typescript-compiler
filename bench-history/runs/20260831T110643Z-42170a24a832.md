# Bench run 20260831T110643Z

- **xtsc revision**: [`42170a24a832`](https://github.com/xemantic/xemantic-typescript-compiler/commit/42170a24a83252b7c6f7da29b994c13b3f2bc8e0)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 42170a24a83252b7c6f7da29b994c13b3f2bc8e0

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 42170a24a832 | **4.07s** | 47,873 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 42170a24a832 | **3.99s** | 48,809 LOC/s | 46 |
| xtsc (cold JVM) | 42170a24a832 | 21.77s | 8,944 LOC/s | 46 |
| tsc | 6.0.3 | 10.63s | 18,311 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,518 LOC/s | 65 |
| xtsc-native | 42170a24a832 (AOT) | 6.79s | 28,687 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.61x FASTER than tsc; 2.43x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.99s**, **-1.9%** against `temurin26` on identical bytecode — 0.38× tsc, 2.39× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.05×** tsc and **13.03×** tsgo; tsc is **6.36×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.66s**.

Ahead-of-time (GraalVM native-image): **6.79s** — **0.64×** tsc, **4.06×** tsgo, **3.21×** the COLD JVM arm and **1.67×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 42170a24a832 | **5.09s** | 38,236 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 42170a24a832 | **4.60s** | 42,354 LOC/s | 46 |
| xtsc (cold JVM) | 42170a24a832 | 28.36s | 6,866 LOC/s | 46 |
| tsc | 6.0.3 | 13.03s | 14,947 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,945 LOC/s | 65 |
| xtsc-native | 42170a24a832 (AOT) | 7.80s | 24,955 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.56x FASTER than tsc; 2.14x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.60s**, **-9.7%** against `temurin26` on identical bytecode — 0.35× tsc, 1.93× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.18×** tsc and **11.93×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.21s**.

Ahead-of-time (GraalVM native-image): **7.80s** — **0.60×** tsc, **3.28×** tsgo, **3.63×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
