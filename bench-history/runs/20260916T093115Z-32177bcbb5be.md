# Bench run 20260916T093115Z

- **xtsc revision**: [`32177bcbb5be`](https://github.com/xemantic/xemantic-typescript-compiler/commit/32177bcbb5be1b77fab0321588b527d75c5702d8)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 32177bcbb5be1b77fab0321588b527d75c5702d8

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 32177bcbb5be | **5.75s** | 33,884 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 32177bcbb5be | **5.37s** | 36,230 LOC/s | 46 |
| xtsc (cold JVM) | 32177bcbb5be | 28.87s | 6,743 LOC/s | 46 |
| tsc | 6.0.3 | 10.69s | 18,210 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,518 LOC/s | 65 |
| xtsc-native | 32177bcbb5be (AOT) | 8.66s | 22,475 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.86x FASTER than tsc; 3.44x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.37s**, **-6.5%** against `temurin26` on identical bytecode — 0.50× tsc, 3.22× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.70×** tsc and **17.28×** tsgo; tsc is **6.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.75s**.

Ahead-of-time (GraalVM native-image): **8.66s** — **0.81×** tsc, **5.18×** tsgo, **3.33×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 32177bcbb5be | **6.76s** | 28,814 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 32177bcbb5be | **6.16s** | 31,633 LOC/s | 46 |
| xtsc (cold JVM) | 32177bcbb5be | 30.47s | 6,390 LOC/s | 46 |
| tsc | 6.0.3 | 13.20s | 14,752 LOC/s | 65 |
| tsgo | 7.0.2 | 2.38s | 81,807 LOC/s | 65 |
| xtsc-native | 32177bcbb5be (AOT) | 10.40s | 18,715 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.95x FASTER than tsc; 2.84x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.16s**, **-8.9%** against `temurin26` on identical bytecode — 0.47× tsc, 2.59× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.31×** tsc and **12.80×** tsgo; tsc is **5.55×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.32s**.

Ahead-of-time (GraalVM native-image): **10.40s** — **0.79×** tsc, **4.37×** tsgo, **2.93×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
