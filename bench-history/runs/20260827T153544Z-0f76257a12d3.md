# Bench run 20260827T153544Z

- **xtsc revision**: [`0f76257a12d3`](https://github.com/xemantic/xemantic-typescript-compiler/commit/0f76257a12d311cc3b065573f73175c0c7f5a8a0)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 0f76257a12d311cc3b065573f73175c0c7f5a8a0

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0f76257a12d3 | **4.47s** | 43,567 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0f76257a12d3 | **4.25s** | 45,758 LOC/s | 46 |
| xtsc (cold JVM) | 0f76257a12d3 | 25.08s | 7,763 LOC/s | 46 |
| tsc | 6.0.3 | 10.96s | 17,761 LOC/s | 65 |
| tsgo | 7.0.2 | 1.64s | 119,011 LOC/s | 65 |
| xtsc-native | 0f76257a12d3 (AOT) | 7.73s | 25,184 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.45x FASTER than tsc; 2.73x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.25s**, **-4.8%** against `temurin26` on identical bytecode — 0.39× tsc, 2.60× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.29×** tsc and **15.33×** tsgo; tsc is **6.70×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.96s**.

Ahead-of-time (GraalVM native-image): **7.73s** — **0.71×** tsc, **4.73×** tsgo, **3.24×** the COLD JVM arm and **1.73×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0f76257a12d3 | **5.42s** | 35,896 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0f76257a12d3 | **4.96s** | 39,262 LOC/s | 46 |
| xtsc (cold JVM) | 0f76257a12d3 | 27.68s | 7,032 LOC/s | 46 |
| tsc | 6.0.3 | 13.15s | 14,800 LOC/s | 65 |
| tsgo | 7.0.2 | 2.40s | 81,227 LOC/s | 65 |
| xtsc-native | 0f76257a12d3 (AOT) | 8.89s | 21,896 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.43x FASTER than tsc; 2.26x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.96s**, **-8.6%** against `temurin26` on identical bytecode — 0.38× tsc, 2.07× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.10×** tsc and **11.55×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.51s**.

Ahead-of-time (GraalVM native-image): **8.89s** — **0.68×** tsc, **3.71×** tsgo, **3.11×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
