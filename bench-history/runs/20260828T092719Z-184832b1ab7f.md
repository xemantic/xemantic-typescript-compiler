# Bench run 20260828T092719Z

- **xtsc revision**: [`184832b1ab7f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/184832b1ab7f0c80b21e732a141cbc7ffcba3b13)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 184832b1ab7f0c80b21e732a141cbc7ffcba3b13

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 184832b1ab7f | **2.88s** | 67,698 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 184832b1ab7f | **2.85s** | 68,436 LOC/s | 46 |
| xtsc (cold JVM) | 184832b1ab7f | 16.65s | 11,696 LOC/s | 46 |
| tsc | 6.0.3 | 8.36s | 23,295 LOC/s | 65 |
| tsgo | 7.0.2 | 1.25s | 155,264 LOC/s | 65 |
| xtsc-native | 184832b1ab7f (AOT) | 4.61s | 42,225 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.91x FASTER than tsc; 2.29x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.85s**, **-1.1%** against `temurin26` on identical bytecode — 0.34× tsc, 2.27× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.99×** tsc and **13.27×** tsgo; tsc is **6.67×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **16.56s**.

Ahead-of-time (GraalVM native-image): **4.61s** — **0.55×** tsc, **3.68×** tsgo, **3.61×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 184832b1ab7f | **3.69s** | 52,750 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 184832b1ab7f | **3.27s** | 59,505 LOC/s | 46 |
| xtsc (cold JVM) | 184832b1ab7f | 19.19s | 10,146 LOC/s | 46 |
| tsc | 6.0.3 | 9.82s | 19,825 LOC/s | 65 |
| tsgo | 7.0.2 | 1.91s | 102,045 LOC/s | 65 |
| xtsc-native | 184832b1ab7f (AOT) | 5.62s | 34,644 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.66x FASTER than tsc; 1.93x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.27s**, **-11.4%** against `temurin26` on identical bytecode — 0.33× tsc, 1.71× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.95×** tsc and **10.06×** tsgo; tsc is **5.15×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **19.08s**.

Ahead-of-time (GraalVM native-image): **5.62s** — **0.57×** tsc, **2.95×** tsgo, **3.41×** the COLD JVM arm and **1.52×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
