# Bench run 20260916T211445Z

- **xtsc revision**: [`9dc43c3dad02`](https://github.com/xemantic/xemantic-typescript-compiler/commit/9dc43c3dad0216277107fdda6b1afae9d8e17bc6)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 9dc43c3dad0216277107fdda6b1afae9d8e17bc6

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9dc43c3dad02 | **4.60s** | 42,344 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9dc43c3dad02 | **4.22s** | 46,105 LOC/s | 46 |
| xtsc (cold JVM) | 9dc43c3dad02 | 23.27s | 8,367 LOC/s | 46 |
| tsc | 6.0.3 | 8.93s | 21,815 LOC/s | 65 |
| tsgo | 7.0.2 | 1.42s | 136,825 LOC/s | 65 |
| xtsc-native | 9dc43c3dad02 (AOT) | 6.89s | 28,246 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.94x FASTER than tsc; 3.23x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.22s**, **-8.2%** against `temurin26` on identical bytecode — 0.47× tsc, 2.97× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.61×** tsc and **16.35×** tsgo; tsc is **6.27×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **23.15s**.

Ahead-of-time (GraalVM native-image): **6.89s** — **0.77×** tsc, **4.84×** tsgo, **3.38×** the COLD JVM arm and **1.50×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9dc43c3dad02 | **5.32s** | 36,570 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9dc43c3dad02 | **4.92s** | 39,573 LOC/s | 46 |
| xtsc (cold JVM) | 9dc43c3dad02 | 25.60s | 7,605 LOC/s | 46 |
| tsc | 6.0.3 | 10.82s | 17,994 LOC/s | 65 |
| tsgo | 7.0.2 | 2.08s | 93,561 LOC/s | 65 |
| xtsc-native | 9dc43c3dad02 (AOT) | 8.25s | 23,611 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.03x FASTER than tsc; 2.56x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.92s**, **-7.6%** against `temurin26` on identical bytecode — 0.45× tsc, 2.36× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.37×** tsc and **12.30×** tsgo; tsc is **5.20×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.50s**.

Ahead-of-time (GraalVM native-image): **8.25s** — **0.76×** tsc, **3.96×** tsgo, **3.10×** the COLD JVM arm and **1.55×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
