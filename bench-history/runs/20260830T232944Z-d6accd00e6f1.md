# Bench run 20260830T232944Z

- **xtsc revision**: [`d6accd00e6f1`](https://github.com/xemantic/xemantic-typescript-compiler/commit/d6accd00e6f1d9e46e55e28fc4ab1207520c6918)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push d6accd00e6f1d9e46e55e28fc4ab1207520c6918

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d6accd00e6f1 | **4.08s** | 47,721 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d6accd00e6f1 | **4.11s** | 47,338 LOC/s | 46 |
| xtsc (cold JVM) | d6accd00e6f1 | 24.84s | 7,836 LOC/s | 46 |
| tsc | 6.0.3 | 10.74s | 18,135 LOC/s | 65 |
| tsgo | 7.0.2 | 1.72s | 112,936 LOC/s | 65 |
| xtsc-native | d6accd00e6f1 (AOT) | 6.69s | 29,107 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.63x FASTER than tsc; 2.37x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.11s**, **+0.8%** against `temurin26` on identical bytecode — 0.38× tsc, 2.39× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.31×** tsc and **14.41×** tsgo; tsc is **6.23×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.73s**.

Ahead-of-time (GraalVM native-image): **6.69s** — **0.62×** tsc, **3.88×** tsgo, **3.71×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | d6accd00e6f1 | **5.12s** | 38,050 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | d6accd00e6f1 | **4.59s** | 42,455 LOC/s | 46 |
| xtsc (cold JVM) | d6accd00e6f1 | 28.62s | 6,803 LOC/s | 46 |
| tsc | 6.0.3 | 12.83s | 15,174 LOC/s | 65 |
| tsgo | 7.0.2 | 2.37s | 82,326 LOC/s | 65 |
| xtsc-native | d6accd00e6f1 (AOT) | 7.53s | 25,863 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.51x FASTER than tsc; 2.16x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.59s**, **-10.4%** against `temurin26` on identical bytecode — 0.36× tsc, 1.94× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.23×** tsc and **12.10×** tsgo; tsc is **5.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.48s**.

Ahead-of-time (GraalVM native-image): **7.53s** — **0.59×** tsc, **3.18×** tsgo, **3.80×** the COLD JVM arm and **1.47×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
