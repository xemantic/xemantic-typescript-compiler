# Bench run 20260901T194329Z

- **xtsc revision**: [`3b81666b124d`](https://github.com/xemantic/xemantic-typescript-compiler/commit/3b81666b124d9d166cdbd70e7a614438d659eb4d)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 3b81666b124d9d166cdbd70e7a614438d659eb4d

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3b81666b124d | **3.51s** | 55,407 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3b81666b124d | **3.45s** | 56,419 LOC/s | 46 |
| xtsc (cold JVM) | 3b81666b124d | 21.98s | 8,856 LOC/s | 46 |
| tsc | 6.0.3 | 9.93s | 19,607 LOC/s | 65 |
| tsgo | 7.0.2 | 1.43s | 136,345 LOC/s | 65 |
| xtsc-native | 3b81666b124d (AOT) | 5.55s | 35,062 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.83x FASTER than tsc; 2.46x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.45s**, **-1.8%** against `temurin26` on identical bytecode — 0.35× tsc, 2.42× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.21×** tsc and **15.40×** tsgo; tsc is **6.95×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.88s**.

Ahead-of-time (GraalVM native-image): **5.55s** — **0.56×** tsc, **3.89×** tsgo, **3.96×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3b81666b124d | **4.38s** | 44,472 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3b81666b124d | **4.03s** | 48,325 LOC/s | 46 |
| xtsc (cold JVM) | 3b81666b124d | 23.11s | 8,426 LOC/s | 46 |
| tsc | 6.0.3 | 11.55s | 16,861 LOC/s | 65 |
| tsgo | 7.0.2 | 2.19s | 88,742 LOC/s | 65 |
| xtsc-native | 3b81666b124d (AOT) | 6.92s | 28,156 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.64x FASTER than tsc; 2.00x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.03s**, **-8.0%** against `temurin26` on identical bytecode — 0.35× tsc, 1.84× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.00×** tsc and **10.53×** tsgo; tsc is **5.26×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.98s**.

Ahead-of-time (GraalVM native-image): **6.92s** — **0.60×** tsc, **3.15×** tsgo, **3.34×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
