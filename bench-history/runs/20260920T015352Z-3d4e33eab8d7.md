# Bench run 20260920T015352Z

- **xtsc revision**: [`3d4e33eab8d7`](https://github.com/xemantic/xemantic-typescript-compiler/commit/3d4e33eab8d71de28779d8ec59b944f6471c55d0)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 3d4e33eab8d71de28779d8ec59b944f6471c55d0

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3d4e33eab8d7 | **5.72s** | 34,020 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3d4e33eab8d7 | **5.10s** | 38,184 LOC/s | 46 |
| xtsc (cold JVM) | 3d4e33eab8d7 | 25.03s | 7,779 LOC/s | 46 |
| tsc | 6.0.3 | 10.49s | 18,564 LOC/s | 65 |
| tsgo | 7.0.2 | 1.64s | 119,011 LOC/s | 65 |
| xtsc-native | 3d4e33eab8d7 (AOT) | 8.76s | 22,228 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.83x FASTER than tsc; 3.50x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.10s**, **-10.9%** against `temurin26` on identical bytecode — 0.49× tsc, 3.12× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.39×** tsc and **15.30×** tsgo; tsc is **6.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.92s**.

Ahead-of-time (GraalVM native-image): **8.76s** — **0.84×** tsc, **5.35×** tsgo, **2.86×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 3d4e33eab8d7 | **6.49s** | 30,014 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 3d4e33eab8d7 | **5.65s** | 34,472 LOC/s | 46 |
| xtsc (cold JVM) | 3d4e33eab8d7 | 30.54s | 6,374 LOC/s | 46 |
| tsc | 6.0.3 | 12.78s | 15,230 LOC/s | 65 |
| tsgo | 7.0.2 | 2.31s | 84,140 LOC/s | 65 |
| xtsc-native | 3d4e33eab8d7 (AOT) | 10.28s | 18,941 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.97x FASTER than tsc; 2.80x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.65s**, **-12.9%** against `temurin26` on identical bytecode — 0.44× tsc, 2.44× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.39×** tsc and **13.20×** tsgo; tsc is **5.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **30.39s**.

Ahead-of-time (GraalVM native-image): **10.28s** — **0.80×** tsc, **4.44×** tsgo, **2.97×** the COLD JVM arm and **1.58×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
