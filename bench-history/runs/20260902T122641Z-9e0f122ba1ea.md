# Bench run 20260902T122641Z

- **xtsc revision**: [`9e0f122ba1ea`](https://github.com/xemantic/xemantic-typescript-compiler/commit/9e0f122ba1ea652d0e04645c976e63cfcfa9b9fb)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 9e0f122ba1ea652d0e04645c976e63cfcfa9b9fb

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9e0f122ba1ea | **4.16s** | 46,859 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9e0f122ba1ea | **4.01s** | 48,517 LOC/s | 46 |
| xtsc (cold JVM) | 9e0f122ba1ea | 21.93s | 8,880 LOC/s | 46 |
| tsc | 6.0.3 | 10.23s | 19,026 LOC/s | 65 |
| tsgo | 7.0.2 | 1.55s | 125,210 LOC/s | 65 |
| xtsc-native | 9e0f122ba1ea (AOT) | 6.99s | 27,846 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.46x FASTER than tsc; 2.67x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.01s**, **-3.4%** against `temurin26` on identical bytecode — 0.39× tsc, 2.58× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.14×** tsc and **14.10×** tsgo; tsc is **6.58×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **21.78s**.

Ahead-of-time (GraalVM native-image): **6.99s** — **0.68×** tsc, **4.50×** tsgo, **3.14×** the COLD JVM arm and **1.68×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 9e0f122ba1ea | **5.05s** | 38,577 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 9e0f122ba1ea | **4.67s** | 41,692 LOC/s | 46 |
| xtsc (cold JVM) | 9e0f122ba1ea | 26.61s | 7,317 LOC/s | 46 |
| tsc | 6.0.3 | 12.60s | 15,448 LOC/s | 65 |
| tsgo | 7.0.2 | 2.33s | 83,706 LOC/s | 65 |
| xtsc-native | 9e0f122ba1ea (AOT) | 8.18s | 23,810 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.50x FASTER than tsc; 2.17x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.67s**, **-7.5%** against `temurin26` on identical bytecode — 0.37× tsc, 2.01× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.11×** tsc and **11.44×** tsgo; tsc is **5.42×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **26.48s**.

Ahead-of-time (GraalVM native-image): **8.18s** — **0.65×** tsc, **3.52×** tsgo, **3.25×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
