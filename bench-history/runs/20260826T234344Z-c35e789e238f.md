# Bench run 20260826T234344Z

- **xtsc revision**: [`c35e789e238f`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c35e789e238f127a5f58c04ac3a0560b14c3da5b)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push c35e789e238f127a5f58c04ac3a0560b14c3da5b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c35e789e238f | **3.18s** | 61,207 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c35e789e238f | **2.91s** | 66,930 LOC/s | 46 |
| xtsc (cold JVM) | c35e789e238f | 18.32s | 10,628 LOC/s | 46 |
| tsc | 6.0.3 | 8.03s | 24,246 LOC/s | 65 |
| tsgo | 7.0.2 | 1.19s | 163,340 LOC/s | 65 |
| xtsc-native | c35e789e238f (AOT) | 5.17s | 37,659 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.52x FASTER than tsc; 2.67x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.91s**, **-8.6%** against `temurin26` on identical bytecode — 0.36× tsc, 2.44× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.28×** tsc and **15.37×** tsgo; tsc is **6.74×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.22s**.

Ahead-of-time (GraalVM native-image): **5.17s** — **0.64×** tsc, **4.34×** tsgo, **3.54×** the COLD JVM arm and **1.63×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c35e789e238f | **3.86s** | 50,440 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c35e789e238f | **3.60s** | 54,068 LOC/s | 46 |
| xtsc (cold JVM) | c35e789e238f | 20.88s | 9,324 LOC/s | 46 |
| tsc | 6.0.3 | 10.14s | 19,205 LOC/s | 65 |
| tsgo | 7.0.2 | 1.84s | 105,873 LOC/s | 65 |
| xtsc-native | c35e789e238f (AOT) | 6.25s | 31,127 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.63x FASTER than tsc; 2.10x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.60s**, **-6.7%** against `temurin26` on identical bytecode — 0.36× tsc, 1.96× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.06×** tsc and **11.35×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.76s**.

Ahead-of-time (GraalVM native-image): **6.25s** — **0.62×** tsc, **3.40×** tsgo, **3.34×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
