# Bench run 20260824T220521Z

- **xtsc revision**: [`dbbb900e3582`](https://github.com/xemantic/xemantic-typescript-compiler/commit/dbbb900e3582efbbfa8e7009685876d20a44886b)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push dbbb900e3582efbbfa8e7009685876d20a44886b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | dbbb900e3582 | **2.88s** | 67,557 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | dbbb900e3582 | **2.77s** | 70,340 LOC/s | 46 |
| xtsc (cold JVM) | dbbb900e3582 | 18.43s | 10,563 LOC/s | 46 |
| tsc | 6.0.3 | 7.91s | 24,605 LOC/s | 65 |
| tsgo | 7.0.2 | 1.21s | 160,380 LOC/s | 65 |
| xtsc-native | dbbb900e3582 (AOT) | 4.67s | 41,718 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.75x FASTER than tsc; 2.37x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.77s**, **-4.0%** against `temurin26` on identical bytecode — 0.35× tsc, 2.28× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.33×** tsc and **15.18×** tsgo; tsc is **6.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.17s**.

Ahead-of-time (GraalVM native-image): **4.67s** — **0.59×** tsc, **3.84×** tsgo, **3.95×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | dbbb900e3582 | **3.62s** | 53,859 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | dbbb900e3582 | **3.43s** | 56,747 LOC/s | 46 |
| xtsc (cold JVM) | dbbb900e3582 | 17.62s | 11,047 LOC/s | 46 |
| tsc | 6.0.3 | 9.67s | 20,128 LOC/s | 65 |
| tsgo | 7.0.2 | 1.75s | 110,941 LOC/s | 65 |
| xtsc-native | dbbb900e3582 (AOT) | 5.94s | 32,761 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.68x FASTER than tsc; 2.06x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.43s**, **-5.1%** against `temurin26` on identical bytecode — 0.35× tsc, 1.95× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **1.82×** tsc and **10.04×** tsgo; tsc is **5.51×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.54s**.

Ahead-of-time (GraalVM native-image): **5.94s** — **0.61×** tsc, **3.39×** tsgo, **2.97×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
