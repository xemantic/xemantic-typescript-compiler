# Bench run 20260906T205727Z

- **xtsc revision**: [`7170767071fe`](https://github.com/xemantic/xemantic-typescript-compiler/commit/7170767071fe5b78b5766fb4fdb352b94321acca)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 7170767071fe5b78b5766fb4fdb352b94321acca

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7170767071fe | **13.96s** | 13,946 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7170767071fe | **14.71s** | 13,237 LOC/s | 46 |
| xtsc (cold JVM) | 7170767071fe | 34.31s | 5,675 LOC/s | 46 |
| tsc | 6.0.3 | 10.76s | 18,089 LOC/s | 65 |
| tsgo | 7.0.2 | 1.70s | 114,463 LOC/s | 65 |
| xtsc-native | 7170767071fe (AOT) | 22.84s | 8,525 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.30x tsc — parity at 1.00x; 8.21x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **14.71s**, **+5.4%** against `temurin26` on identical bytecode — 1.37× tsc, 8.65× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.19×** tsc and **20.17×** tsgo; tsc is **6.33×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **34.19s**.

Ahead-of-time (GraalVM native-image): **22.84s** — **2.12×** tsc, **13.43×** tsgo, **1.50×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7170767071fe | **16.18s** | 12,034 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7170767071fe | **15.44s** | 12,611 LOC/s | 46 |
| xtsc (cold JVM) | 7170767071fe | 39.24s | 4,961 LOC/s | 46 |
| tsc | 6.0.3 | 13.37s | 14,564 LOC/s | 65 |
| tsgo | 7.0.2 | 2.47s | 78,826 LOC/s | 65 |
| xtsc-native | 7170767071fe (AOT) | 24.47s | 7,958 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.21x tsc — parity at 1.00x; 6.55x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **15.44s**, **-4.6%** against `temurin26` on identical bytecode — 1.15× tsc, 6.25× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.94×** tsc and **15.89×** tsgo; tsc is **5.41×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **39.12s**.

Ahead-of-time (GraalVM native-image): **24.47s** — **1.83×** tsc, **9.91×** tsgo, **1.60×** the COLD JVM arm and **1.51×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
