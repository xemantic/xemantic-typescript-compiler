# Bench run 20260909T182718Z

- **xtsc revision**: [`ebff300b3168`](https://github.com/xemantic/xemantic-typescript-compiler/commit/ebff300b31689834e89144ebfc74899fb6eb8898)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push ebff300b31689834e89144ebfc74899fb6eb8898

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ebff300b3168 | **3.48s** | 56,029 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ebff300b3168 | **3.39s** | 57,417 LOC/s | 46 |
| xtsc (cold JVM) | ebff300b3168 | 17.74s | 10,975 LOC/s | 46 |
| tsc | 6.0.3 | 7.20s | 27,041 LOC/s | 65 |
| tsgo | 7.0.2 | 1.10s | 176,200 LOC/s | 65 |
| xtsc-native | ebff300b3168 (AOT) | 5.70s | 34,152 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.07x FASTER than tsc; 3.14x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.39s**, **-2.4%** against `temurin26` on identical bytecode — 0.47× tsc, 3.07× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.46×** tsc and **16.05×** tsgo; tsc is **6.52×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **17.65s**.

Ahead-of-time (GraalVM native-image): **5.70s** — **0.79×** tsc, **5.16×** tsgo, **3.11×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | ebff300b3168 | **4.27s** | 45,619 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | ebff300b3168 | **3.99s** | 48,748 LOC/s | 46 |
| xtsc (cold JVM) | ebff300b3168 | 20.56s | 9,469 LOC/s | 46 |
| tsc | 6.0.3 | 8.72s | 22,325 LOC/s | 65 |
| tsgo | 7.0.2 | 1.69s | 115,344 LOC/s | 65 |
| xtsc-native | ebff300b3168 (AOT) | 6.86s | 28,398 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.04x FASTER than tsc; 2.53x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.99s**, **-6.4%** against `temurin26` on identical bytecode — 0.46× tsc, 2.37× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.36×** tsc and **12.18×** tsgo; tsc is **5.17×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **20.39s**.

Ahead-of-time (GraalVM native-image): **6.86s** — **0.79×** tsc, **4.06×** tsgo, **3.00×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
