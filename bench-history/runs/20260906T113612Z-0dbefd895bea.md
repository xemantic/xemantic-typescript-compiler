# Bench run 20260906T113612Z

- **xtsc revision**: [`0dbefd895bea`](https://github.com/xemantic/xemantic-typescript-compiler/commit/0dbefd895bea5baec4256c383ddda425a3b2fc41)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 0dbefd895bea5baec4256c383ddda425a3b2fc41

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0dbefd895bea | **15.03s** | 12,953 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0dbefd895bea | **14.58s** | 13,350 LOC/s | 46 |
| xtsc (cold JVM) | 0dbefd895bea | 35.67s | 5,457 LOC/s | 46 |
| tsc | 6.0.3 | 10.86s | 17,925 LOC/s | 65 |
| tsgo | 7.0.2 | 1.73s | 112,805 LOC/s | 65 |
| xtsc-native | 0dbefd895bea (AOT) | 23.12s | 8,422 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.38x tsc — parity at 1.00x; 8.71x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **14.58s**, **-3.0%** against `temurin26` on identical bytecode — 1.34× tsc, 8.45× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.28×** tsc and **20.67×** tsgo; tsc is **6.29×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **35.56s**.

Ahead-of-time (GraalVM native-image): **23.12s** — **2.13×** tsc, **13.39×** tsgo, **1.54×** the COLD JVM arm and **1.54×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 0dbefd895bea | **15.17s** | 12,832 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 0dbefd895bea | **15.25s** | 12,764 LOC/s | 46 |
| xtsc (cold JVM) | 0dbefd895bea | 37.73s | 5,160 LOC/s | 46 |
| tsc | 6.0.3 | 13.32s | 14,621 LOC/s | 65 |
| tsgo | 7.0.2 | 2.48s | 78,572 LOC/s | 65 |
| xtsc-native | 0dbefd895bea (AOT) | 24.62s | 7,909 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.14x tsc — parity at 1.00x; 6.12x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **15.25s**, **+0.5%** against `temurin26` on identical bytecode — 1.15× tsc, 6.16× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.83×** tsc and **15.23×** tsgo; tsc is **5.37×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **37.62s**.

Ahead-of-time (GraalVM native-image): **24.62s** — **1.85×** tsc, **9.93×** tsgo, **1.53×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
