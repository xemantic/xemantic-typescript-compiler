# Bench run 20260801T031510Z

- **xtsc revision**: [`e31a388f6e9c`](https://github.com/xemantic/xemantic-typescript-compiler/commit/e31a388f6e9cccc33cdd12f983c2018be03b593b)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: cold single process; wall-clock is the comparable metric. xtsc = median of 1, tsc/tsgo = median of 3.
- **Label**: push e31a388f6e9cccc33cdd12f983c2018be03b593b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | e31a388f6e9c | 31.28s | 6,225 LOC/s | 46 |
| tsc | 6.0.3 | 10.57s | 18,411 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 1.64s | 118,431 LOC/s | 65 |
| xtsc-native | e31a388f6e9c (AOT) | 12.49s | 15,582 LOC/s | 46 |

Relative (wall): xtsc is **2.96×** tsc and **19.02×** tsgo; tsc is **6.43×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **31.17s**.

Ahead-of-time (GraalVM native-image): **12.49s**, **2.50×** the JVM arm, **1.18×** tsc, **7.60×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| xtsc | e31a388f6e9c | 31.85s | 6,113 LOC/s | 46 |
| tsc | 6.0.3 | 13.05s | 14,916 LOC/s | 65 |
| tsgo | 7.0.0-dev.20260707.2 | 2.38s | 81,704 LOC/s | 65 |
| xtsc-native | e31a388f6e9c (AOT) | 14.76s | 13,189 LOC/s | 46 |

Relative (wall): xtsc is **2.44×** tsc and **13.36×** tsgo; tsc is **5.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **31.73s**.

Ahead-of-time (GraalVM native-image): **14.76s**, **2.16×** the JVM arm, **1.13×** tsc, **6.19×** tsgo. Same compiler, same output — the JVM arm's extra time is warm-up a one-shot CLI run never amortizes (docs/perf/aot-native-image.md).

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
