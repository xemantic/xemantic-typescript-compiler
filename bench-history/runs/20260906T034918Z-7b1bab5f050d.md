# Bench run 20260906T034918Z

- **xtsc revision**: [`7b1bab5f050d`](https://github.com/xemantic/xemantic-typescript-compiler/commit/7b1bab5f050dd49a33badb7ed557564f2ccbfd99)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 7b1bab5f050dd49a33badb7ed557564f2ccbfd99

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7b1bab5f050d | **14.83s** | 13,131 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7b1bab5f050d | **14.26s** | 13,653 LOC/s | 46 |
| xtsc (cold JVM) | 7b1bab5f050d | 34.50s | 5,643 LOC/s | 46 |
| tsc | 6.0.3 | 10.12s | 19,231 LOC/s | 65 |
| tsgo | 7.0.2 | 1.56s | 124,729 LOC/s | 65 |
| xtsc-native | 7b1bab5f050d (AOT) | 23.86s | 8,161 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.46x tsc — parity at 1.00x; 9.50x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **14.26s**, **-3.8%** against `temurin26` on identical bytecode — 1.41× tsc, 9.14× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **3.41×** tsc and **22.10×** tsgo; tsc is **6.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **34.37s**.

Ahead-of-time (GraalVM native-image): **23.86s** — **2.36×** tsc, **15.28×** tsgo, **1.45×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 7b1bab5f050d | **16.52s** | 11,783 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 7b1bab5f050d | **15.64s** | 12,444 LOC/s | 46 |
| xtsc (cold JVM) | 7b1bab5f050d | 35.06s | 5,553 LOC/s | 46 |
| tsc | 6.0.3 | 12.62s | 15,429 LOC/s | 65 |
| tsgo | 7.0.2 | 2.30s | 84,653 LOC/s | 65 |
| xtsc-native | 7b1bab5f050d (AOT) | 25.31s | 7,692 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.31x tsc — parity at 1.00x; 7.18x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **15.64s**, **-5.3%** against `temurin26` on identical bytecode — 1.24× tsc, 6.80× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.78×** tsc and **15.24×** tsgo; tsc is **5.49×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **34.92s**.

Ahead-of-time (GraalVM native-image): **25.31s** — **2.01×** tsc, **11.00×** tsgo, **1.39×** the COLD JVM arm and **1.53×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
