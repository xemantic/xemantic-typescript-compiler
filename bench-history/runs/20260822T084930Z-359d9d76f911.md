# Bench run 20260822T084930Z

- **xtsc revision**: [`359d9d76f911`](https://github.com/xemantic/xemantic-typescript-compiler/commit/359d9d76f91178311d498a42fe6761146a24a759)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 359d9d76f91178311d498a42fe6761146a24a759

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 359d9d76f911 | **3.00s** | 64,814 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 359d9d76f911 | **2.90s** | 67,208 LOC/s | 46 |
| xtsc (cold JVM) | 359d9d76f911 | 18.73s | 10,396 LOC/s | 46 |
| tsc | 6.0.3 | 8.84s | 22,027 LOC/s | 65 |
| tsgo | 7.0.2 | 1.34s | 145,191 LOC/s | 65 |
| xtsc-native | 359d9d76f911 (AOT) | 4.73s | 41,154 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.94x FASTER than tsc; 2.24x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **2.90s**, **-3.6%** against `temurin26` on identical bytecode — 0.33× tsc, 2.16× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.12×** tsc and **13.96×** tsgo; tsc is **6.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **18.63s**.

Ahead-of-time (GraalVM native-image): **4.73s** — **0.54×** tsc, **3.53×** tsgo, **3.96×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 359d9d76f911 | **3.80s** | 51,210 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 359d9d76f911 | **3.53s** | 55,187 LOC/s | 46 |
| xtsc (cold JVM) | 359d9d76f911 | 22.21s | 8,766 LOC/s | 46 |
| tsc | 6.0.3 | 10.62s | 18,326 LOC/s | 65 |
| tsgo | 7.0.2 | 2.06s | 94,515 LOC/s | 65 |
| xtsc-native | 359d9d76f911 (AOT) | 5.95s | 32,706 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.79x FASTER than tsc; 1.85x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **3.53s**, **-7.2%** against `temurin26` on identical bytecode — 0.33× tsc, 1.71× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.09×** tsc and **10.78×** tsgo; tsc is **5.16×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **22.10s**.

Ahead-of-time (GraalVM native-image): **5.95s** — **0.56×** tsc, **2.89×** tsgo, **3.73×** the COLD JVM arm and **1.57×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
