# Bench run 20260908T220442Z

- **xtsc revision**: [`da48da413d57`](https://github.com/xemantic/xemantic-typescript-compiler/commit/da48da413d575f47f3cd8dac527452bad03c2a3b)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push da48da413d575f47f3cd8dac527452bad03c2a3b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | da48da413d57 | **5.34s** | 36,454 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | da48da413d57 | **4.90s** | 39,726 LOC/s | 46 |
| xtsc (cold JVM) | da48da413d57 | 25.74s | 7,563 LOC/s | 46 |
| tsc | 6.0.3 | 10.79s | 18,048 LOC/s | 65 |
| tsgo | 7.0.2 | 1.67s | 116,867 LOC/s | 65 |
| xtsc-native | da48da413d57 (AOT) | 8.55s | 22,782 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.02x FASTER than tsc; 3.21x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.90s**, **-8.2%** against `temurin26` on identical bytecode — 0.45× tsc, 2.94× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.39×** tsc and **15.45×** tsgo; tsc is **6.48×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.62s**.

Ahead-of-time (GraalVM native-image): **8.55s** — **0.79×** tsc, **5.13×** tsgo, **3.01×** the COLD JVM arm and **1.60×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | da48da413d57 | **6.42s** | 30,341 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | da48da413d57 | **5.71s** | 34,122 LOC/s | 46 |
| xtsc (cold JVM) | da48da413d57 | 29.32s | 6,640 LOC/s | 46 |
| tsc | 6.0.3 | 13.29s | 14,645 LOC/s | 65 |
| tsgo | 7.0.2 | 2.39s | 81,465 LOC/s | 65 |
| xtsc-native | da48da413d57 (AOT) | 10.21s | 19,067 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.07x FASTER than tsc; 2.68x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.71s**, **-11.1%** against `temurin26` on identical bytecode — 0.43× tsc, 2.39× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.21×** tsc and **12.27×** tsgo; tsc is **5.56×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **29.19s**.

Ahead-of-time (GraalVM native-image): **10.21s** — **0.77×** tsc, **4.27×** tsgo, **2.87×** the COLD JVM arm and **1.59×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
