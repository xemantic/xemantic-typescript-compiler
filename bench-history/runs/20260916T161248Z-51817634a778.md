# Bench run 20260916T161248Z

- **xtsc revision**: [`51817634a778`](https://github.com/xemantic/xemantic-typescript-compiler/commit/51817634a7787b87eb190680f47ce091db15f0ec)
- **TypeScript pin**: `4d4f005c` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push 51817634a7787b87eb190680f47ce091db15f0ec

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 51817634a778 | **5.86s** | 33,225 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 51817634a778 | **5.31s** | 36,680 LOC/s | 46 |
| xtsc (cold JVM) | 51817634a778 | 25.67s | 7,584 LOC/s | 46 |
| tsc | 6.0.3 | 10.92s | 17,826 LOC/s | 65 |
| tsgo | 7.0.2 | 1.72s | 113,067 LOC/s | 65 |
| xtsc-native | 51817634a778 (AOT) | 9.49s | 20,516 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.86x FASTER than tsc; 3.40x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **5.31s**, **-9.4%** against `temurin26` on identical bytecode — 0.49× tsc, 3.08× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.35×** tsc and **14.91×** tsgo; tsc is **6.34×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **25.56s**.

Ahead-of-time (GraalVM native-image): **9.49s** — **0.87×** tsc, **5.51×** tsgo, **2.71×** the COLD JVM arm and **1.62×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | 51817634a778 | **7.01s** | 27,770 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | 51817634a778 | **6.02s** | 32,342 LOC/s | 46 |
| xtsc (cold JVM) | 51817634a778 | 28.53s | 6,823 LOC/s | 46 |
| tsc | 6.0.3 | 13.26s | 14,678 LOC/s | 65 |
| tsgo | 7.0.2 | 2.46s | 79,243 LOC/s | 65 |
| xtsc-native | 51817634a778 (AOT) | 11.31s | 17,208 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 1.89x FASTER than tsc; 2.85x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **6.02s**, **-14.1%** against `temurin26` on identical bytecode — 0.45× tsc, 2.45× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.15×** tsc and **11.61×** tsgo; tsc is **5.40×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **28.38s**.

Ahead-of-time (GraalVM native-image): **11.31s** — **0.85×** tsc, **4.60×** tsgo, **2.52×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
