# Bench run 20260902T142145Z

- **xtsc revision**: [`c54f8084780e`](https://github.com/xemantic/xemantic-typescript-compiler/commit/c54f8084780ebee0cc92868efeff5b37a4ffa55b)
- **TypeScript pin**: `637d5746` (typeScriptCommit)
- **Profile**: `compiler` — 78 files, 194,702 LOC
- **Runner**: ubuntu-latest, JDK 26 (GitHub-hosted, shared)
- **Method**: xtsc **warm** = median of 5 in-process rebuilds after 6 warm-up rebuilds (the headline); xtsc **cold** = median of 1 single-shot process(es); tsc/tsgo = median of 3 processes.
- **Label**: push c54f8084780ebee0cc92868efeff5b37a4ffa55b

## check-only

every compiler runs with `--noEmit`: type-check only, nothing transformed, emitted or written. **This is the mode the perf arc profiles** (`ab-interleaved.sh` / `cost_gate.py` both pass `--noEmit`).

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c54f8084780e | **4.65s** | 41,898 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c54f8084780e | **4.43s** | 43,940 LOC/s | 46 |
| xtsc (cold JVM) | c54f8084780e | 24.79s | 7,854 LOC/s | 46 |
| tsc | 6.0.3 | 10.53s | 18,497 LOC/s | 65 |
| tsgo | 7.0.2 | 1.63s | 119,302 LOC/s | 65 |
| xtsc-native | c54f8084780e (AOT) | 7.49s | 25,998 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.27x FASTER than tsc; 2.85x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.43s**, **-4.6%** against `temurin26` on identical bytecode — 0.42× tsc, 2.72× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.36×** tsc and **15.19×** tsgo; tsc is **6.45×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **24.66s**.

Ahead-of-time (GraalVM native-image): **7.49s** — **0.71×** tsc, **4.59×** tsgo, **3.31×** the COLD JVM arm and **1.61×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

## emit

every compiler type-checks AND emits JavaScript to an `outDir`.

| Compiler | Version | Wall | Throughput | Errors |
|---|---|---:|---:|---:|
| **xtsc (warm, temurin26)** | c54f8084780e | **5.29s** | 36,833 LOC/s | 46 |
| **xtsc (warm, graal-oracle25)** | c54f8084780e | **4.83s** | 40,310 LOC/s | 46 |
| xtsc (cold JVM) | c54f8084780e | 27.63s | 7,045 LOC/s | 46 |
| tsc | 6.0.3 | 13.15s | 14,806 LOC/s | 65 |
| tsgo | 7.0.2 | 2.35s | 82,746 LOC/s | 65 |
| xtsc-native | c54f8084780e (AOT) | 8.64s | 22,524 LOC/s | 46 |

**Warm JVM (temurin26) — the target metric: 2.49x FASTER than tsc; 2.25x tsgo — parity at 1.00x.**
Second warm JVM (`graal-oracle25`): **4.83s**, **-8.6%** against `temurin26` on identical bytecode — 0.37× tsc, 2.05× tsgo. One run is not a verdict on a few-percent difference (see docs/perf/jdk-jit-vendor-comparison.md); read the trend across rows.

Cold JVM (one-shot CLI): **2.10×** tsc and **11.74×** tsgo; tsc is **5.59×** tsgo. xtsc self-reported time (excl. JVM startup/JIT): **27.49s**.

Ahead-of-time (GraalVM native-image): **8.64s** — **0.66×** tsc, **3.67×** tsgo, **3.20×** the COLD JVM arm and **1.64×** the WARM one. Same compiler, same output. The AOT image is expected to TRAIL the warm JVM (measured 8.4 s vs 6.5 s on the dev box) — its value is that it needs no warm-up at all, so it is the one-shot CLI answer while the warm JVM is the daemon answer (docs/perf/aot-native-image.md). NOTE: CI publishes the NON-PGO base image here; the shipped artifact (native.yml, Oracle + PGO) is ~14% faster check-only and ~20% faster on emit.

> Compare a ratio only against a ratio of the SAME mode. A single xtsc run carries the full spread of one cold JVM (the per-run ratio has ranged 1.9–2.7 on an unchanged compiler), so read the median across runs, not one row.

> Errors are all env-legit offline `@types/node` artifacts (no `node_modules/@types` in CI); real tsc reports 0 on its own source. Counts differ slightly by how each compiler models the missing ambient env, not by real diagnostics.
