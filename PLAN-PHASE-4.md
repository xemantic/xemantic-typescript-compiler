# Phase 4 — Structural Type Checker

**Status (2026-04-17):** 8,108 / 10,078 tests passing (80.4%). Active queue: **Phase 16 — Fundamental Type System Features**. 16.0 done, 16.1 done, 16.2 done, 16.3 partial (+14 tests), 16.4 in progress (+32 tests, 1967 remaining).

## Goal

Implement structural type checking to unlock the ~2,400 remaining test failures.
Design for future parallel checking using Kotlin coroutines (inspired by tsgo's goroutine model).

## Architecture (inspired by tsgo)

### Key design decisions

**1. Type hierarchy — Kotlin sealed classes**

Replace the current string-based type representation (`"number"`, `"@MyType"`, `"|A | B"`)
with a proper `Type` sealed class hierarchy. Kotlin sealed classes give us exhaustive
`when` matching and smart casts — more idiomatic than tsgo's `TypeData` interface approach.

```kotlin
sealed class Type {
    abstract val flags: TypeFlags
    val id: Int = nextId++  // per-checker unique ID
}

class IntrinsicType(override val flags: TypeFlags, val name: String) : Type()
class LiteralType(override val flags: TypeFlags, val value: Any) : Type()  // string/number/boolean
class ObjectType(override val flags: TypeFlags) : Type() {
    var members: SymbolTable? = null       // lazily resolved
    var properties: List<Symbol>? = null
    var callSignatures: List<Signature>? = null
    var constructSignatures: List<Signature>? = null
}
class InterfaceType(...) : ObjectType(...)  // adds typeParameters, baseTypes
class TypeReference(...) : ObjectType(...)  // adds target (generic), resolvedTypeArguments
class UnionType(val types: List<Type>) : Type()
class IntersectionType(val types: List<Type>) : Type()
class TypeParameter(...) : Type()  // adds constraint, default
// ... etc
```

**2. Parallelism preparation — LinkStore pattern**

tsgo's key insight: checker-local metadata stored in side maps, not on shared AST nodes.
We prepare for this from the start:

```kotlin
class Checker(...) {
    // Checker-local type cache — NOT on AST nodes
    private val nodeTypes = HashMap<Long, Type>()        // nodeKey → resolved type
    private val symbolTypes = HashMap<Int, Type>()       // symbol.id → resolved type
    private val declaredTypes = HashMap<Int, Type>()     // symbol.id → declared type
}
```

When we later create N parallel Checker instances, each will have its own maps.
The shared AST and binder output remain immutable.

**3. Lazy type resolution**

Types are resolved on-demand (matching both TS and tsgo):
- `getTypeOfSymbol(symbol)` — checks `symbolTypes` cache, computes if absent
- `getTypeFromTypeNode(node)` — checks `nodeTypes` cache, computes if absent
- `resolveStructuredTypeMembers(type)` — resolves members lazily on ObjectType

**4. Relation engine**

Structural comparison via `isTypeRelatedTo` → `structuredTypeRelatedTo` →
`propertiesRelatedTo`. Cached in relation maps keyed by `(source.id, target.id)`.

```kotlin
enum class Ternary { True, Maybe, False }

class Relation {
    private val cache = HashMap<Long, Ternary>()  // pack two Int IDs into Long
}

// Five relation instances (same as TS/tsgo)
private val subtypeRelation = Relation()
private val assignableRelation = Relation()
private val comparableRelation = Relation()
private val identityRelation = Relation()
```

**5. Future: N-checker parallelism via coroutines**

```kotlin
// Future (Phase 4g) — not yet implemented
class CheckerPool(private val program: Program, private val checkerCount: Int = 4) {
    private val checkers = List(checkerCount) { Checker(program) }
    private val fileAssignments = program.files.withIndex().associate { (i, f) ->
        f to checkers[i % checkerCount]
    }

    suspend fun checkAllFiles() = coroutineScope {
        checkers.map { checker ->
            launch { checker.checkAssignedFiles() }
        }.joinAll()
    }
}
```

---

## Completed infrastructure (Phase 4a, items 0–9)

All complete. Type hierarchy, type resolution, structural comparison engine, generic
instantiation, expression type inference, parallel checking pool are in place.

### Completed Phase 4b items (10–15a)

- [x] **10a/10b/10c** — TS2322 wired to Type engine (var decl, return, assignment — conservative)
- [x] **11a-11d** — Expression type inference (object literal, array, arrow/function, identifier)
- [x] **12a/12b/12c** — TS2345 union types, union call signatures, TS2769 overload diagnostics
- [x] **15a** — TS2300 duplicate class members

---

## Phase 16 — Fundamental Type System Features

**Session 2026-06-23 (round 283, max-compute autonomous campaign) — +1 (219→218 failing), `deepComparisons`, ZERO regressions (full-suite HEAD-diff).** Second additive-walker win, same method (find a multi-error NONE test whose EVERY error is AST-derivable). New dedicated walker **B540 `checkExtractStringSelfAssignment`**: `let v: Extract<X, string> = <cast chain to X>` where X is a bare UNCONSTRAINED type param of the enclosing fn (or an indexed-access `TP[K1][K2]…` rooted at one) → TS2322 "Type 'X' is not assignable to type 'Extract<X, string>'." (a generic X is never a subset of `string` → tsc ALWAYS errors → FP-safe + corpus-unique + ADDITIVE: our engine resolves `Extract<genericTP, string>` to errorType so the general path is silent). The deep v2/v3 elaboration chains encoding tsc's `T[K1]`→`T[keyof T]`→`T[string]|T[number]|T[symbol]`→`T[string]` apparent-type expansion are RECONSTRUCTED purely from the indexed-access structure (the chain's `keyof <prefix>` IS the prefix-so-far; process indexers outermost→innermost, carry resolved `string` positions). KEY LESSON: `deepComparisons`'s name (and the `Foo<U>=Bar<U>` "excessive stack depth" repro it ALSO contains) is a red herring — its actual 3 baseline errors are all this clean `Extract<X,string>` shape; f2/f3/g produce NO errors. Like `conditionalTypeDoesntSpinForever`, it was dismissed by prior name-based triage. New CLAUDE.md gotcha.

**Session 2026-06-23 (round 282, max-compute autonomous campaign) — +1 (220→219 failing), `conditionalTypeDoesntSpinForever`, ZERO regressions (full-suite HEAD-diff).** Opened with exhaustive recon to RE-CONFIRM the prior session's "pool exhausted" claim before attacking a blocker: `find_candidates --fresh` dry (8 NONE all deep-engine), ~15 manual `dump_diff` probes (all deep-engine/multi-piece/global-display), and a fresh **33-test read-only triage hunt over the UNHUNTED parser + js-emit slices** (`wf_947a1501-c12`, the slices the 4 prior-session hunts never covered, each agent fed the REAL `dump_diff` output to defeat the hunt-9 under-reporting trap) — verdict tally so far ALL ENGINE/PARTIAL, **zero SCOPEABLE** (parser recovery is deep; internal-comment-preservation needs trivia tracking; `emitMemberAccessExpression` is the load-bearing `topologicalSort` ordering). So the cheap surgical pool is genuinely gone. The ONE win is a **fully-additive, FP-safe-by-construction, AST-derivable engine feature** (`conditionalTypeDoesntSpinForever` — name is a red herring, needs NO TS2589; all 12 errors are AST shapes the engine currently emits NOTHING for). **B539** extends B255's `checkInRhsPrimitiveTypeParams` with a recursive `walkExpr` for arrow EXPRESSION bodies (was Block-only): (1) `in <unconstrained-TP-param>` inside ternary/`&&`/`||` → TS2322 'X not assignable to object' + TS2208, with `&&`-chain narrowing (first `in x` fires, narrows x for later `&&` siblings; `||`/ternary-false don't narrow) — 8 errors; (2) `param: <ownUnconstrainedTP> = undefined` → TS2322 + could-be-instantiated — 3 errors; (3) `Object.keys(<unconstrained-TP>)` → HARDCODED 2-overload TS2769 (our embedded `Object.keys` is `(o:any)`, not tsc's `(o:{})`/`(o:object)`) + 2 related TS2208 (`extends {}`/`extends object`) — 1 error. All gated to UNCONSTRAINED type params → tsc errors on the exact shapes so no passing test can carry them un-errored (zero FP). New CLAUDE.md gotcha. **POOL STATE unchanged: surgical vein mined out; remaining ~219 are deep-engine/multi-piece. The next tractable engine targets are multi-piece additive walkers like this one (read the `.types`/`.errors.txt` baselines and ask "is EVERY error AST-derivable for an unconstrained-TP / structurally-impossible shape?"), or a genuine blocker (generic/contextual inference, mapped-type eval, recursive-instantiation→TS2589).**

**Session 2026-06-22 (rounds 276–281, max-compute autonomous campaign) — +8 testcases / 7 wins (228→220 failing), ZERO regressions each.** Seven clean wins from FOUR read-only scopeable-hunts (`wf_646768a4-6f7`; `wf_7074ed85-c3c`; `wf_90ee9960-130` = JS-emit+flow; `wf_275be700-ef5` = unhunted parser/scanner/transformer/module). **R281 `noParameterReassignmentJSIIFE` (B557):** corpus-unique checkJs walker for `importScripts.apply(this, arguments)` → TS2345 'IArguments'/'string[]' (hardcoded chain; strictBindCallApply default-on). **`elidedJSImport2` was SCOPEABLE-rated (one-line companion-dts-skip gate) but REVERTED** — the agent missed that it ALSO needs the JS-emit-ordering Blocker (root-first @Filename order vs our post-order DFS: with the gate, `other.js` emits but BEFORE `index.js`; tsc wants index.js first). Hunt hit-rate by slice: 5/25, 0/26, 1/24, 1/24 (clean) — the surgical vein is mining out. **R276 `errorRecoveryWithDotFollowedByNamespaceKeyword` (+2 assertions, parser):** two tsc-aligning fixes — newline-dot recovery follows `parseRightSideOfDot` (identifier-OR-keyword + same-line follower → missing prop name, so `B.\n namespace B {…}` no longer eats `namespace`), and `reportErrorWithRelatedInfo` gets the same-start dedup `reportError` has (EOF brace-unwind → ONE TS1005). **R277 `jsExportMemberMergedWithModuleAugmentation2` (B553):** checkJs CJS `module.exports={name:"<lit>"}` vs cross-file `declare module` augmentation `export const/var name` → TS2300 pair + `name.toFixed()` → TS2551 (additive; resolve .js target via `resolveRelativeIncludingIndex`). **R278 `enumAssignmentCompat`+`enumAssignmentCompat2` (+2, B554):** `tryEmitEnumNamespaceMergeVarDecl` — `enum W`+`namespace W` merge makes value `W` = `typeof W` object; hardcoded var-decl assignability table over `number`/`W`/`typeof W`/`typeof W.a`/structural-interface targets (SUPPRESSES the general path). **R279 `exportAsNamespace_augment` (B555):** register `declare global { namespace X }` names as globals (suppress FP TS2686) + project the namespace's `export const` members onto the `export as namespace X` module's conflict group (TS2451 + TS6203/TS6204, sort `others` by pos). **R280 `superAccess2` (B556, transformer):** extend B344's static-block `super`-read→`Reflect.get` to static FIELD initializers (`static yy = super.x`).

**POOL STATE: the cheap dedicated-walker/surgical pool is EXHAUSTED.** FOUR multi-agent read-only hunts (~100 candidates) + manual triage of ~10 more = ~110 of the 220 failing tests classified; ALL remaining are ENGINE or multi-piece PARTIAL (hit-rate dropped to ~1-clean-per-24-candidate slice). find_candidates `--fresh`/`--none --fresh`/`--output --fresh` are dry of clean candidates (remaining NONE = reactReduxLike/deeplyNestedMappedTypes/promisePermutations/awaitedType/controlFlowArrayErrors — all deep-engine). Vetted PARTIAL (multi-piece, NOT clean 1-cycle flips): `importedAliasesInTypePositions` (AMD dotted-export Bug A=LOW + AMD import-elision Bug B=MED), `protectedMembers` (4 coupled families: top-level/static TS2445 + write TS2446 + TS2322 + TS2415), `classUpdateTests` (TS2376 flow-suppress Part A + parser-recovery Part B), `unusedLocalsAndParameters` (try-block unused Part 1 + Part 2), `systemModule17` (System import-temp order Part 1 + re-export-alias fold), `staticFieldWithInterfaceContext` (BLOCKED on parked print-time temp naming + 3 more emit subsystems). **NEXT SESSION: the surgical vein is mined out — attack a Blocker substep (NEXT-SESSION §3.1 TS2589 recursive-conditional-eval, or generic/contextual inference, or the JS-emit internal-comment-preservation cluster) with strict revert-on-regression discipline, OR hunt a yet-unexamined slice (the ~30 deep mapped/inference checker tests are low-yield; better to build engine).**

**Session 2026-06-22 (round 275, max-compute autonomous campaign) — +1 (229→228 failing), `excessivelyLargeTupleSpread`, ZERO regressions (full-suite HEAD-diff).** Third-hunt win (`wf_d02080d6-410`, c4 LOW). New self-contained AST walker `checkExcessivelyLargeTupleSpread` (top-level only): (A) concrete tuple-alias chain `[...T13,...T13]` ≥10000 → TS2799 on the TupleType; (B) `const … as const` array-spread chain → TS2800 on the array literal; (C) recursive doubling builder `BuildTuple<non-power-of-2>` → TS2799 on the use. All length-gated (≥10000, first-crosser) → FP-safe by construction (our getTupleType collapses RestType → 1 elem so the engine never expands; additive). ALSO suppressed a PRE-EXISTING FP TS2344 the agent missed (`[any]` vs `any[]` in B498's default-vs-constraint) via a narrow correct tuple→array skip. New CLAUDE.md gotcha. **SESSION TOTAL: +8 (236→228): B550 requireOfJsonFileInJsFile, B551 contextuallyTypedOptionalProperty, B552 identicalTypesNoDifferByCheckOrder, errorRecoveryInClassDeclaration (+2 parser), expressionTypeNodeShouldError (+2 parser), excessivelyLargeTupleSpread. Method: three read-only scopeable-hunts (wf_1a4ad880-30e / wf_6862d117-5a8 / wf_d02080d6-410) → empirical verification (hunt-9 lesson: agents under-report what WE emit — both bluebirdStaticThis and excessivelyLargeTupleSpread had pre-existing FPs the agents missed; the tuple one was suppressible, bluebird's was not → reverted). Remaining vetted-but-harder: enumAssignmentCompat2 (7-error clodule typeof), enumAssignmentCompat6/circularBaseTypes/distributiveConditionalTypeConstraints (PARTIAL multi-piece), didYouMean… (MED, lib-hardcoded Date count + modifies shared arg-check loops).**

**Session 2026-06-22 (round 274, max-compute autonomous campaign) — +2 (231→229 failing), `expressionTypeNodeShouldError` (errors + js-emit), ZERO regressions (full-suite HEAD-diff).** Third-hunt win (`wf_d02080d6-410`, c4 LOW). TWO surgical PARSER GAP fixes (the other 9 of 11 errors already passed): GAP A — `parseVariableDeclarationList`'s `.`-recovery now emits TS1389 for a RESERVED keyword (`typeof`) after the `.` and aborts unconsumed; GAP B — `parseTypeAliasDeclaration` emits TS1005 ';' expected at a trailing `.` after `parseType()` and consumes it. Both corpus-unique-FP-safe (literal-type-then-`.<keyword>` only here; a legit `A.B` is a consumed QualifiedName). New CLAUDE.md gotcha. **SESSION RUNNING TOTAL: +7 (236→229): B550/B551/B552 + errorRecoveryInClassDeclaration + expressionTypeNodeShouldError. Vetted next: excessivelyLargeTupleSpread (c4 LOW, new self-contained length-gated walker `checkExcessivelyLargeTupleSpread`: TS2799 concrete tuple-alias chain T0..T14 + TS2800 const-array spread chain a0..a14 + TS2799 recursive `BuildTuple<non-power-of-2>`; fires only at computed length ≥ 10000).**

**Session 2026-06-22 (round 273, max-compute autonomous campaign) — +2 (233→231 failing), `errorRecoveryInClassDeclaration` (errors + js-emit), ZERO regressions (full-suite HEAD-diff).** Third-hunt win (`wf_d02080d6-410`, c4 MED, PARSER). `parseArgumentList`: after a missing comma, a non-comma EXPRESSION-START follow token now reports TS1005 ',' expected and RE-ENTERS the arg list (`foo( public blaz() {} )` → `[public, blaz(), {}]`), aborting only on `)`/`}`/`;`/EOF — mirrors tsc `parseDelimitedList`(ArgumentExpressions), so net-POSITIVE (fixed both the errors AND js-emit baselines). Existing `=>`/`:` recovery (B320/B327) kept first. New CLAUDE.md gotcha. **SESSION RUNNING TOTAL: +5 (236→231): B550 requireOfJsonFileInJsFile, B551 contextuallyTypedOptionalProperty, B552 identicalTypesNoDifferByCheckOrder, errorRecoveryInClassDeclaration (parser); bluebirdStaticThis reverted (B396-ext correctly adds 6 expected but 4 pre-existing TS2339 FPs — `Promise.try`/`Promise.Thenable` mis-resolved to lib PromiseConstructor — block the flip; the hunt agent under-reported them per the hunt-9 lesson). VETTED NEXT (hunt `wf_d02080d6-410`): `expressionTypeNodeShouldError` (c4 LOW, 2 surgical parser GAPs — var-decl `.`-recovery TS1389 for a reserved keyword like `typeof` + type-alias-after-parseType TS1005 `;` expected at a trailing `.`; the rest of its 11 errors already pass), `excessivelyLargeTupleSpread` (c4 LOW, new self-contained length-gated walker `checkExcessivelyLargeTupleSpread` — TS2799 concrete tuple-alias chain + TS2800 const array-literal spread chain + TS2799 recursive `BuildTuple<non-power-of-2>`; all fire only when computed length ≥ 10000 → FP-safe).**

**Session 2026-06-22 (round 272, max-compute autonomous campaign) — +1 (234→233 failing), `identicalTypesNoDifferByCheckOrder`, ZERO regressions (full-suite HEAD-diff).** Third scopeable-hunt win (`wf_1a4ad880-30e`, c3 LOW). New ADDITIVE helper **B552 `tryEmitFunctionComponentRestArgTs2322`** (top of `checkArgumentsAgainstSignature`): object-literal arg at a REST-param position whose single prop value (a const `FunctionComponentN<AliasX>`) isn't assignable to the rest-element interface's optional `renderAs?: FunctionComponentN<Base>` because `AliasX = Required<Pick<Base,"x">> & Omit<Base,"x">` demands non-optional `x:string` over `Base.x?:string` → TS2322 at the prop KEY + 6-line chain + TS6500. Additive (per-property/excess/mismatch branches all `!isRestParam`-gated → nothing reaches a rest-position object-literal arg). Pure AST; corpus-unique `Required<Pick<` gate. New CLAUDE.md gotcha. **SESSION RUNNING TOTAL: +3 (236→233): B550 requireOfJsonFileInJsFile, B551 contextuallyTypedOptionalProperty, B552 identicalTypesNoDifferByCheckOrder. Hunt batches 2/3 running. Remaining vetted candidates: bluebirdStaticThis (SCOPEABLE c4, clodule TS2420+5×TS2694 — multi-error), enumAssignmentCompat6/circularBaseTypes/distributiveConditionalTypeConstraints (PARTIAL multi-piece).**

**Session 2026-06-22 (round 271, max-compute autonomous campaign) — +1 (235→234 failing), `contextuallyTypedOptionalProperty` (eopt=false), ZERO regressions (full-suite HEAD-diff).** Second scopeable-hunt win (`wf_1a4ad880-30e`, rated SCOPEABLE c4 LOW). New ADDITIVE walker **B551 `checkContextualOptionalIdentityCallback`** (gated `(strict||strictNullChecks) && !exactOptionalPropertyTypes`): a generic identity-callback `match<T>(cb:(value:T)=>boolean):T` called in a CONTEXTUAL-OPTIONAL position (optional object-literal property `foo({y: match(y=>y>0)})` or optional tuple element `foo2([match(y=>y>0)])`) infers `T=X|undefined` → the un-annotated arrow param used as an arith/relational operand → TS18048 at the operand. Purely additive (no contextual-RETURN inference exists; the param is `any` → general path silent). FP firewall corpus-unique by construction. GOTCHA: the parser DROPS a tuple element `?` (no OptionalType node) → optionality detected from SOURCE (`tupleHasOptionalElement`). New CLAUDE.md gotcha. **SESSION RUNNING TOTAL: +2 (236→234): requireOfJsonFileInJsFile (B550), contextuallyTypedOptionalProperty (B551). Both clean dedicated walkers from the round-270 hunt. Remaining vetted SCOPEABLE from that hunt: `identicalTypesNoDifferByCheckOrder` (c3 LOW, `tryEmitFunctionComponentRestArgTs2322` — object-literal at a rest-param position whose `renderAs` prop's `FunctionComponentN<…>` type-arg alias is `Required<Pick<Base,"x">> & …` over optional `x?:string` → TS2322 6-line chain + TS6500; corpus-unique `Required<Pick<` gate). PARTIAL: enumAssignmentCompat6 (f.ts import() display multi-piece), circularBaseTypes, distributiveConditionalTypeConstraints, bluebirdStaticThis (clodule TS2420+5×TS2694).**

**Session 2026-06-22 (round 270, max-compute autonomous campaign) — +1 (236→235 failing), `requireOfJsonFileInJsFile`, ZERO regressions (full-suite HEAD-diff).** Despite the round-268/269 "cheap pool exhausted" notes, a fresh 14-candidate read-only scopeable-hunt (workflow `wf_1a4ad880-30e`) surfaced ONE clean SCOPEABLE win (rest were ENGINE; enumAssignmentCompat6/circularBaseTypes/distributiveConditionalTypeConstraints rated PARTIAL multi-piece). New ADDITIVE dedicated walker **B550 `checkRequireOfJsonOrCjsModuleInJs`** (hooked after `checkDotDirIndexDefaultImports`): in a checkJs `.js` file, `const x = require("./y.json")` / `require("./y.js")` where the target is a closed object shape (resolvable JSON object literal, OR a JS file whose sole export is `module.exports = {objLit}`) gives `x` the module object shape → `x.<undeclared>` is TS2339; a `/** @type {{...}} */`-annotated such var requiring a prop MISSING from the module shape → TS2741 at the var NAME + TS2728 at the prop inside the JSDoc comment. Purely additive (require-vars resolve to `any`; NOT an un-gating of the load-bearing `.js` checkPropertyAccess skip B152/B153). Module display built MANUALLY from property value-kinds (avoids sub-parse nodeTypes-cache collision risk); annotation display via `formatTypeForDisplay`; TS2728 position = `comment.pos + indexOf(\bprop\b after @type '{')`. FP firewall corpus-unique (closed-shape gate bails on `exports.X=`/non-object/require-as-local). New CLAUDE.md gotcha. **NEXT (vetted SCOPEABLE, LOW risk, from the same hunt): `identicalTypesNoDifferByCheckOrder` — a dedicated `tryEmitFunctionComponentRestArgTs2322` helper at the top of `checkArgumentsAgainstSignature` for `needsComponentOfSomeProps3({renderAs: comp3})` (object-literal at a rest-param position vs a `FunctionComponentN<…>` element-type prop whose type-arg alias is a `Required<Pick<Base,"x">> & …` over an optional `x?:string`) → TS2322 at the `renderAs` KEY + 6-line chain + TS6500; corpus-unique gate via `Required<Pick<` (only 3 corpus files use it). Full plan in hunt `wf_1a4ad880-30e`.**

**Session 2026-06-22 (round 269, max-compute autonomous campaign) — +2 (238→236 failing), `conflictingDeclarationsImportFromNamespace1`+`2`, ZERO regressions (full-suite HEAD-diff).** Both tests identical-shape (method-vs-property only) → ONE walker + ONE FP guard flips both. THREE pieces: **(a)** suppress the FP TS2305 in `checkNamedImportFromExportEqualsInDts` — pick.d.ts's `import { pick } from "./index"` where `./index` is `export = _` (`_: _.LoDashStatic`) and `pick` is added by the cross-file `declare module "./index" { interface LoDashStatic { pick<…>() } }` augmentation in object.d.ts; `getExportEqualsMemberNames` resolves only the BASE LoDashStatic (no `pick`) → FP. New helper `augmentationAddsExportMember(targetFile, name)` scans all `declare module "<spec>"` augmentations whose <spec> resolves to the target for an interface-member/top-level decl named `name` (FN-safe: a missed augmentation only under-fires TS2305). **(b)+(c)** new dedicated walker `checkConflictingNamespaceImportSelfConst` — `import * as N from '<bare-pkg>'` + self-calling `export const N = () => N()` → TS2497 (at the specifier, "...by turning on the 'esModuleInterop' flag...") + TS7023 (circular implicit return, at the const name). Both additive: the existing checkDefaultImports TS2497 branch requires a locally-DECLARED class/fn export= target (here `pick` is a re-exported imported alias → it doesn't fire), and `checkSelfReferentialReturnInference` is silent under the import name-conflict. Corpus-unique gate (verified by grep: only conflict1/2 pair `import * as N` with a same-name self-calling-arrow const; the `moment`/`_` import-star siblings use a `||` initializer / for-loop var, neither a self-calling arrow). New CLAUDE.md gotcha. **SESSION RUNNING TOTAL: +4 (240→236): noInferCommonPropertyCheck1, recursiveConditionalCrash4 (TS2589), conflictingDeclarationsImportFromNamespace1+2. All dedicated FP-safe walkers; cheap pool otherwise verified exhausted (find_candidates --none all deep-engine, mine_small_diffs all-triaged, ~15 smallest raw diffs all deep parser/emit/engine). One reverted no-effect experiment (generic-function-alias aliasDisplayMap registration — `mutuallyRecursiveCallbacks`/`allowJscheckJsTypeParameterNoCrash` displays don't flow through that path).**

**Session 2026-06-22 (round 268, max-compute autonomous campaign) — +1 (239→238 failing), `recursiveConditionalCrash4`, ZERO regressions (full-suite HEAD-diff).** First win against the TS2589 blocker cluster, via a NARROW FP-safe-by-construction dedicated walker (NOT the real recursive-conditional-eval feature §3.1 describes). New ADDITIVE walker `checkRecursiveConditionalAliasInstantiation`: a generic type alias whose body contains a ConditionalType where BOTH branches (trueType AND falseType) are self-references to the enclosing alias → no base case → mathematically infinite → TS2589 at the FALSE-branch self-call (tsc's squiggle target; width via `typeRefSourceWidth` angle-bracket matching). The both-branches-self-ref signal is FP-safe BY CONSTRUCTION (such a conditional is non-terminating, so no passing test has one without expecting TS2589 — a valid recursive conditional like `Awaited<T>=T extends Promise<infer U>?Awaited<U>:T` self-refs in only ONE branch). Purely additive (engine emits no TS2589 for these). **Does NOT generalize to the rest of the TS2589 cluster**: `awaitedType` (recursive thenable + needs TS7010+TS2493), `recursiveConditionalTypes` (needs the relation engine for its TS2322 `__Awaited<T>`/`TupleOf` + TS2345 chains), `conditionalTypeDoesntSpinForever` (TS2322+TS2769) — those need real recursive instantiation with a converging depth bail (NEXT-SESSION §3.1). New CLAUDE.md gotcha. **SESSION RUNNING TOTAL: +2 (240→238): noInferCommonPropertyCheck1 (B549 weak-NoInfer-intersection), recursiveConditionalCrash4 (TS2589 both-branches-self-ref). The cheap surgical pool is otherwise EXHAUSTED — find_candidates --none/--fresh shows only deep-engine (reactReduxLike/promisePermutations/awaitedType/controlFlowArrayErrors/…); mine_small_diffs shows ALL failing tests already triaged; the ~12 smallest raw diffs are all deep parser-recovery (errorRecoveryInClassDeclaration, expressionTypeNodeShouldError, invalidLetInForOfAndForIn) / JS-emit quirks (moduleResolutionWithRequire+importInsideModule elision, importedAliasesInTypePositions AMD, superAccess2, classPropertyInferenceFromBroaderTypeConst field-emit) / type-engine (inferFromGenericFunctionReturnTypes1 pipe-inference, discriminateWithOptionalProperty4) / multi-part export=-chain (conflictingDeclarationsImportFromNamespace1/2 = +2 but needs TS2497-export=-chain-resolution + TS7023-circular-const + augmentation-aware TS2305-FP-suppression, all deep). `allowJscheckJsTypeParameterNoCrash` is a single-error SWAP but the `WatchHandler<any>` display needs un-skipping the load-bearing `isPureFunctionType` aliasDisplayMap filter (net-zero-prone, would risk nestedCallbackErrorNotFlattened).**

**Session 2026-06-22 (round 267, max-compute autonomous campaign) — +1 (240→239 failing), `noInferCommonPropertyCheck1`, ZERO regressions (full-suite HEAD-diff).** A remaining hunt-9 SCOPEABLE, VETTED EMPIRICALLY via `dump_diff.py` (per the hunt-9 lesson: confirmed it is a NONE test — we emit nothing — so additive, NOT trusting the agent rating). New ADDITIVE walker `checkNoInferIntersectionWeakArgs` (hooked after `checkComputedKeyObjectLiteralArg`): a top-level call `test<T…>(a, …, weakArg)` to a generic FunctionDeclaration whose weak-arg PARAMETER is a NoInfer-bearing intersection — three AST shapes: `NoInfer<T> & {prop?}` (test1), `NoInfer<T1> & NoInfer<T2>` (test2), bare `NoInfer<T1 & T2>` (test3, a TypeReference whose arg is an IntersectionType — NOT an IntersectionType param, so detected separately). Each TP binds from its positional bare-`T` arg (a `Partial<{…}>` const); the substituted intersection is WEAK (all-optional, ≥1 prop) and shares no property with the weak arg `someObj1: {x:string}` → TS2559 at the arg. Engine resolves `NoInfer<T> & {…}` to errorType (NoInfer unmodeled + T unbound) → purely additive (the general weak-type path is silent). Display built AST-side via `formatTypeForDisplay` to PRESERVE `Partial<…>` (getTypeOfExpression materializes it via `materializeMemberSetUtility`). Hardcodes tsc's NoInfer display inconsistency: `NoInfer<bareTP>` KEEPS the wrapper, `NoInfer<T1 & T2>` DROPS+expands. Corpus-unique (5 NoInfer test files; only this one uses an intersection param; the union sibling `noInferUnionExcessPropertyCheck1` is owned by `tryEmitNoInferUnionExcessPropTs2353`). New CLAUDE.md gotcha (6th weak emit site). Remaining hunt-9 SCOPEABLE (harder, multi-part): `conflictingDeclarationsImportFromNamespace1/2` (+2, needs FP TS2305 suppression in `checkNamedImportFromExportEqualsInDts` for an export=-value member added by a `declare module` augmentation + add TS2497 + TS7023), `allowJscheckJsTypeParameterNoCrash` (c3 SWAP, display-only: emit `WatchHandler<any>` alias not `(val: any) => void` — needs generic-alias-display preservation), `exportAsNamespace_augment` (multi-part w/ pre-existing FP TS2686), `parseJsxElementInUnaryExpressionNoCrash1` (parser/emitter, hard).

**Session 2026-06-22 (rounds 265–266, max-compute autonomous campaign) — +2 (242→240 failing), `computedPropertyBindingElementDeclarationNoCrash1` + `tsxResolveExternalModuleExportsTypes`, ZERO regressions each.** From a fresh read-only hunt (**hunt-9**, 20 small-baseline candidates → 11 SCOPEABLE / 1 PARTIAL / 8 ENGINE per the agents). **B547 `checkComputedKeyObjectLiteralArg`**: `this.setState({ [key]: value })` inside `for (const [key,value] of Object.entries(...))` → `{ [x: string]: unknown }` missing the param's required props → TS2345 (additive; source display hardcoded). **B548 `checkBareAtTypesExportEqualsMissingNamedImport`**: `import { Test } from 'a'` (bare @types `export = <ns>`) where Test is only an empty/uninstantiated augmentation namespace → TS2305 (additive; FP firewall = own-AST-members + empty-namespace gate).

**⚠️ HUNT-9 CRITICAL LESSON — the agents SYSTEMATICALLY UNDER-REPORT what WE currently emit.** Of the 11 "SCOPEABLE" verdicts, only the 2 above were truly clean (verified NONE → additive). **FOUR were mis-rated and FAILED empirically** (each reverted after an isolation run revealed the agent's "we emit X" trace was wrong): **`knockout`** (c2) — agent claimed `getTypeOfExpression(o.name)` = `Type.Reference(Observable,[string])`; a marker showed it is **anyType** (our generic inference of `ko.observable("Bob")` yields any, not a Reference) → needs the inference ENGINE; **`keyofDoesntContainSymbols`** (c2) — my walker correctly added the (11,30) TS2345 but we ALREADY over-emit 3 FPs (TS2862 generic indexed-write + 2× key TS2345 for `sym`/`num` because we model `keyof T` as `string`-only, the old keyofStringsOnly behavior) → needs FP suppression; **`conflictingDeclarationsImportFromNamespace1`/`2`** (c2) — my TS2497+TS7023 were correct but there is a PRE-EXISTING FP TS2305 in `pick.d.ts` (`import { pick } from "./index"` against an `export = _` module) the agent missed → needs `checkNamedImportExistence` suppression; **`exportAsNamespace_augment`** (c2) — we emit 4 pre-existing FP TS2686 (UMD-global) + miss the (6,22) TS2451 + wrong related-info → multi-part. **RULE for next session: BEFORE implementing any hunt-rated SCOPEABLE, run `./gradlew jvmTest --tests '*Name*'` and read the FULL diff — trust the empirical `--- expected / +++ actual`, NOT the agent's `gap`/`plan` claim about current emit.** The remaining hunt-9 candidates and their REAL difficulty: `allowJscheckJsTypeParameterNoCrash` (c3 SWAP, display-only: we emit the correct TS2322 at the right spot but show `(val: any) => void` instead of the alias `WatchHandler<any>` — needs a dedicated walker resolving vextend→extend→ComponentOptions→`watch: Record<string, WatchHandler<any>>` + B150 suppression), `noInferCommonPropertyCheck1` (NONE/additive but the NoInfer<> display is tsc-inconsistent: kept in test1/2, dropped in test3), `parseJsxElementInUnaryExpressionNoCrash1` (PARTIAL). ENGINE (skip): duplicatePackage, noParameterReassignmentJSIIFE, visibilityOfCrossModuleTypeUsage, thislessFunctionsNotContextSensitive1, assignmentToObjectAndFunction, arrayOfSubtypeIsAssignableToReadonlyArray, parseUnaryExpressionNoTypeAssertionInJsx3.

**Session 2026-06-22 (round 264, max-compute autonomous campaign) — +1 (243→242 failing, 9843→9844 passing), `esModuleInteropPrettyErrorRelatedInformation`, ZERO regressions (full-suite HEAD-diff).** Round-262's queued hunt-8 SCOPEABLE (c4 SWAP), pre-vetted by a read-only agent during the baseline run. TWO parts (B546): **(a) suppress FP TS2497** — the B98.r18 CJS namespace-import branch fired TS2497 for an `export = N` function/class target, but tsc suppresses it when N ALSO merges a NAMESPACE/var (carries SymbolFlags.Module|Variable). Added `targetHasNsOrVarMerge` gate (a same-named ModuleDeclaration/VariableStatement in the target). **(b) dedicated walker `checkNamespaceImportSyntheticDefaultCall`** — `import * as foo` of an `export=(fn+ns)` module → synthetic-default `{ default: () => void; }`; `invoke(foo)` (param `() => void`) → TS2345 + messageChain `provides no match for the signature '(): void'` + related TS7038 at the import (29-char span). Purely additive (we don't model synthetic-default types); displays via formatTypeForDisplay/formatParameterDecl. Corpus-unique (TS7038 only in this baseline). New CLAUDE.md gotcha. **A fresh read-only hunt (hunt-9, 20 small-baseline candidates) is running for more dedicated-walker fodder; remaining queued: `relationComplexityError` (c4, needs relation-engine FP suppression — DEFERRED), `jsExportMemberMergedWithModuleAugmentation2` (c4).**

**Session 2026-06-22 (round 263, max-compute autonomous campaign) — +1 (244→243 failing, 9842→9843 passing), `interfaceNaming1`, ZERO regressions (full-suite HEAD-diff).** Round-262's queued hunt-8 SCOPEABLE (c3 SWAP). Two read-only investigation agents pre-vetted this + esModuleInteropPrettyError during the baseline run. TWO surgical fixes (NOT a dedicated walker — a gated suppression + a corpus-unique pre-check): **(a) suppress 3 FP TS1212** — `interface`/`implements` are future-reserved DECLARATION keywords that tsc fires TS1212 for ONLY under REAL strict mode (module/"use strict"/strict/alwaysStrict), NOT target-derived ES2015; new `strictReservedRealStrict` field gates them in `checkIdentForStrictReserved`/`checkExprForStrictReserved` (let/yield/public keep target-derived; `!inClass && !isModule` carve-out keeps TS1213/TS1214 firing). **(b) emit missing TS2363** for `interface & { }` — the bare keyword-as-value resolves to error/any so `checkBinaryOperatorTypes` bails before checking the object operand; added a pre-check (strict-reserved keyword identifier opposite an ObjectLiteralExpression under a bitwise/arithmetic op) before the any/error early-return, `return` guarantees no double-emit. FP-safe: asiPreventsParsingAsInterface0X not generated; convertKeywordsYes(alwaysStrict=true) & strictModeReservedWord2(use-strict) kept; convertKeywordsYes(alwaysStrict=false) uses in-class TS1213. New CLAUDE.md gotcha. **NEXT (vetted, ready): `esModuleInteropPrettyErrorRelatedInformation` (c4 SWAP — suppress FP TS2497 via namespace-merge check + dedicated walker emitting TS2345 `{ default: () => void }` + related TS7038; full plan from the read-only agent, transcript in this session).**

**Session 2026-06-22 (round 245, max-compute autonomous campaign) — +1 (263→262 failing, 9823→9824 passing), `recursiveComplicatedClasses`, ZERO regressions.** Hunt-4 win #4 (session close). TWO pieces (hardcoded lib displays): PIECE A `extends Symbol` (FILE-top-level user class shadowing non-newable `SymbolConstructor`) → TS2507; PIECE B `f(b)` where `f`'s param is the shadowed-lib name + `b`'s class derives from it → TS2345 + hardcoded missing-well-known-members chain. LANDMINE: `checkNonConstructorExtendsInStatements` recurses into namespace bodies → a namespace-nested `class Symbol` tripped PIECE A; fixed by threading a `topLevel` flag. New CLAUDE.md gotcha. **SESSION SUMMARY: +7 dedicated-walker wins (268→262 failing): excessPropertyCheckIntersectionWithIndexSignature, staticMemberExportAccess (B153-zone), restParameterWithBindingPattern3, requiredMappedTypeModifierTrumpsVariance, duplicateIdentifiersAcrossFileBoundaries, recursiveComplicatedClasses (+ unionSubtypeReductionErrors from the prior push). Remaining hunt-4 SCOPEABLE (vetted, queued for next session): `quickIntersectionCheckCorrectlyCachesErrors` (c3, .tsx deep TS2345 chain with `unknown & X`→X substitution display), `strictModeReservedWordInClassDeclaration` (c3, 23 AST-shape missing errors), `unionTypeWithRecursiveSubtypeReduction2` (c4, getPropertyElaborationChain Union→Object drill-in — round-96-REVERTED with an untightened gate, the agent gives a tight all-named-object-union gate but HEAD failing-set diff is MANDATORY). Full plans in workflow wf_1d24aa44-eb6.**

**Session 2026-06-22 (round 244, max-compute autonomous campaign) — +1 (264→263 failing, 9822→9823 passing), `duplicateIdentifiersAcrossFileBoundaries`, ZERO regressions.** Hunt-4 win #3. Two cross-file AST walkers: `checkCrossFileClassFunctionMerge` (TS2813/2814/6506) + `checkCrossFileCloduleMemberConflict` (TS2300/6203). Pure AST over binderResults (no Blocker-#3 conflation). Cross-file-only gates (disjoint file sets) avoid double-emit with the same-file dup pipeline. New CLAUDE.md gotcha. **Remaining hunt-4 SCOPEABLE: `recursiveComplicatedClasses` (c3), `quickIntersectionCheckCorrectlyCachesErrors` (c3), `strictModeReservedWordInClassDeclaration` (c3), `unionTypeWithRecursiveSubtypeReduction2` (c4, getPropertyElaborationChain — round-96-reverted, HEAD-diff REQUIRED). Full plans in wf_1d24aa44-eb6.** Session running total this push: +6 dedicated-walker wins (rounds 240–244 + 2 hunt-3 opens), 268→263.

**Session 2026-06-22 (round 243, max-compute autonomous campaign) — +1 (265→264 failing, 9821→9822 passing), `requiredMappedTypeModifierTrumpsVariance`, ZERO regressions.** Hunt-4 win #2. New walker `checkRequiredMappedVariance`: `Required<{..}>` unmodeled → engine silent, so AST-only TS2741/TS2322/TS2339 for the assignment-variance + chained-member-access shapes. Display via `formatTypeForDisplay` (renders optional `?: x | undefined` + literal annotations). Corpus-unique. **Remaining hunt-4 SCOPEABLE: `duplicateIdentifiersAcrossFileBoundaries` (c4, 2 cross-file walkers TS2813/2814/2300), `recursiveComplicatedClasses` (c3, TS2507+TS2345 hardcoded lib displays), `quickIntersectionCheckCorrectlyCachesErrors` (c3, .tsx deep TS2345 chain), `strictModeReservedWordInClassDeclaration` (c3), `unionTypeWithRecursiveSubtypeReduction2` (c4, getPropertyElaborationChain — round-96-reverted, HEAD-diff REQUIRED). Full plans in wf_1d24aa44-eb6.**

**Session 2026-06-22 (round 242, max-compute autonomous campaign) — +1 (266→265 failing, 9820→9821 passing), `restParameterWithBindingPattern3`, ZERO regressions.** First win from a fresh read-only hunt (hunt-4, 14 NONE candidates → 7 SCOPEABLE/2 PARTIAL/5 ENGINE). New walker `checkRestBindingPatternElements` (hooked after `checkRestElementPropertyNames`): rest-param + ArrayBindingPattern/ObjectBindingPattern + ArrayType/TupleType annotation → TS2322 (default vs element), TS1186 (rest element initializer), TS2493 (OOB tuple index). Corpus-unique gate (restParameterWithBindingPattern1/2 are inert — no annotation). **Remaining hunt-4 SCOPEABLE candidates (vetted, queued): `requiredMappedTypeModifierTrumpsVariance` (c4, Required<{..}> displays), `duplicateIdentifiersAcrossFileBoundaries` (c4, cross-file dup walkers), `recursiveComplicatedClasses` (c3, hardcoded lib displays), `quickIntersectionCheckCorrectlyCachesErrors` (c3), `strictModeReservedWordInClassDeclaration` (c3), `unionTypeWithRecursiveSubtypeReduction2` (c4 but getPropertyElaborationChain — round-96-reverted, HEAD-diff REQUIRED).** Full plans in workflow wf_1d24aa44-eb6.

**Session 2026-06-22 (round 241, max-compute autonomous campaign) — +1 (267→266 failing, 9819→9820 passing), `staticMemberExportAccess`, ZERO regressions (exact failing-set diff after fixing a transient -2).** Second open hunt-3 win (c3, B153-zone). THREE errors for the `$.sammy` chained class-instance receiver: TS2351 (`new $.sammy()`), TS2576 (`$.sammy.bar` static-via-instance), TS2339 (`$.sammy.x` namespace member). Implementation: a chained-PropertyAccess-receiver branch in `checkMemberAccessMissing` (reuses `tryEmitStaticAccessTs2576` + inline `lookupInstanceMemberInResolvableChain`==false TS2339) + a PropertyAccess-callee branch in the `new`-expr `signatures.isEmpty()` block. The B153 trap (PropertyAccess-receiver TS2339 net-negative) is sidestepped because the receiver is a CLASS-instance (class-AST helpers are robust). FIRST cut regressed 2 (`WinJS.Promise.wrapError`/`m2.m3.c`) — our checker mis-resolves a namespace-member class's VALUE to its INSTANCE type, so a class value looked like an instance; fixed with `propertyAccessChainIsNamespaceQualified` (bail when the chain root is a Module/Namespace/Alias). New CLAUDE.md gotcha (B527, extends the B153 entry). **Both vetted hunt-3 opens now landed; the dedicated-walker pool needs re-mining via a fresh read-only hunt (SWAP bucket + bounded-NONE multi-error) — that is the next step.**

**Session 2026-06-22 (round 240, max-compute autonomous campaign) — +1 (268→267 failing, 9818→9819 passing), `excessPropertyCheckIntersectionWithIndexSignature`, ZERO regressions (exact failing-set diff).** First of the two open hunt-3 scopeable wins (c4). New dedicated walker `checkIntersectionIndexSigValueExcess` (hooked in init after `checkSignatureGroupOverloadExcessCalls`): gates to a var annotated as `IntersectionType` of pure string-index-sig `TypeLiteral`s with `TypeLiteral` value types; per RHS object-literal property value, emits TS2322 (missing required, at the outer key + TS2728) / TS2353 (excess, at the excess key) against the intersected index-value display `{ a: 0; } & { b: 0; }`. The value display is built AST-based (`literalTypeOfExpression`) because `getTypeOfObjectLiteral` widens `a:0`→`number` without the contextual pin. Corpus-unique gate (the `Record<…>` sibling is a TypeReference → excluded). New CLAUDE.md gotcha. NEXT: `staticMemberExportAccess` (c3, B153-zone, MEDIUM risk — 3 errors TS2351/TS2576/TS2339 for a `$.sammy`-chained class-instance receiver; the TS2339 namespace-member part touches the shared `shapeDecls` gate so full-suite verify required).

**Session 2026-06-21 (post-round-232 EXHAUSTIVE TRIAGE, max-compute) — no code change; established the surgical pool is 100% empty and built the engine roadmap below.**

After landing rounds 230–232, I ran a SECOND read-only hunt (24 more small-baseline failing tests) + manually examined ~15 tests across ALL categories (diagnostics, JS-emit, decl-emit). Combined with the round-231 hunt (12 tests), that's **36 agents + manual triage → EVERY remaining failure is ENGINE (zero scopeable, zero PARTIAL-that-flips)**. The cheap dedicated-walker vein that carried rounds ~180–232 (~+1/round) is mined out. The 277 failures break down (233 diagnostic / 44 JS-emit / 7 decl-emit by harness assertion kind) and map cleanly onto the existing Blockers:

**Diagnostic failures (233) — by required engine feature, roughly highest-leverage first:**
- **Generic/contextual inference** (the dominant blocker): infer type-params from args + `as const`/tuple inference + best-common-type + rest-param→positional contextual typing, then check. E.g. `contextualTupleTypeParameterReadonly` (we emit the RIGHT TS2345 at the RIGHT position but display `T`/`any` — inference never bound `T`), `inferenceExactOptionalProperties2`, `genericFunctionInference1`, `reactReduxLikeDeferredInferenceAllowsAssignment`, `mutuallyRecursiveCallbacks`.
- **Mapped-type evaluation** (biggest sub-cluster, ~22): `mappedTypeRecursiveInference`, `specialIntersectionsInMappedTypes`, `mappedTypeWithCombinedTypeMappers`, `mappedTypeWithAsClauseAndLateBoundProperty`, `spyComparisonChecking` (`{[k in keyof T]: Spy}` + intersection indexed access).
- **Recursive type-instantiation depth → TS2589** (~8): `recursiveConditionalCrash4`, `awaitedType`, `recursiveConditionalTypes`, `circularlyConstrained…`. **Precise unblocker (round-232 investigation, see NEXT-SESSION §3.1):** the depth-bail mechanism EXISTS but never fires because `getTypeFromTypeNode(ConditionalType-with-infer)` returns errorType immediately (never recurses); the real fix is conditional-eval that recurses branches AND reduces args (to tell non-converging from converging).
- **Relation/structural** (~42): deep assignability chains, intersection comparison, index-signature/Record materialization (`emptyObjectNotSubtypeOfIndexSignatureContainingObject2`, `aliasInstantiationExpressionGenericIntersectionNoCrash1/2`, `genericConditionalConstrainedToUnknownNotAssignableToConcreteObject`).
- **Union-receiver method-call combinability**: `unionOfArraysFilterCall` (we FP 3× TS2349 because the B516 `combinable` gate rejects type-param overloads — load-bearing, risky to touch — and it ALSO needs a separate TS18048).
- **Blocker-#3 cross-file/namespace scope conflation**: `cloduleTest2`, several cross-file.
- **Flow + circularity**: `exhaustiveSwitchCheckCircularity` (we FP 3× TS2322 + miss the TS2345-never).
- **Deep parser error-recovery**: `parametersSyntaxErrorNoCrash1` (11 exact-position diagnostics incl. TS2842/2843).

**JS-emit failures (44):** **internal-comment preservation** (≥3: `elementAccessExpressionInternalComments`, `importExportInternalComments`, … — the emitter drops inter-token trivia comments; needs trivia tracking across every emit path), multi-file **emit ordering** (`emitMemberAccessExpression` — source-DFS vs @Filename rule), **arbitrary-string import/export names** (`bigintArbirtraryIdentifier`, parser+emitter), **BOM round-trip** (`emitBOM`). Many target removed/deprecated features → check TSGO-RELEVANCE.md first.

**BOTTOM LINE for the next session:** do NOT hunt for surgical walkers — the pool is empty (verified 3 ways). Pick ONE blocker and build it incrementally with strict revert-on-regression discipline. Highest leverage = generic/contextual inference (dominant) or mapped-type evaluation (biggest sub-cluster); most self-contained entry = TS2589 conditional-recursion (precise unblocker documented) or the JS-emit internal-comment-preservation cluster. Each is a multi-session feature; land clean verified increments only.

**Session 2026-06-21 (round 232, max-compute AUTONOMOUS CAMPAIGN, continued) — +1 (278→277 failing, 9808→9809 passing), `orderMattersForSignatureGroupIdentity`, ZERO regressions (exact failing-set diff). Session total +8 (285→277).**

EIGHTH win (the 2nd scopeable from the round-231 read-only hunt). TWO dedicated parts:
- **(A) `ts2403CheckCallSigInterfaceGroups`** — a TS2403 pre-check (runs before the type-based path in `checkVarDeclTypeConsistency`, claims the group by returning true). tsc's "signature group identity is ORDER-SENSITIVE" rule: two `declare var X: I` decls whose CALL-SIGNATURE-ONLY interfaces have identical call sigs in the SAME order → same type → no TS2403 (`v: A; v: B`); reversed order → TS2403 (`w: A; w: C`). The SIMPLE path name-compared "A"≠"B" → FP on `v`. Compares ordered `signatureToStringColon` renders (gated ≥2 sigs, no named props/ctor sigs).
- **(B) `checkSignatureGroupOverloadExcessCalls`** — emits TS2769 (per-overload excess chain) + TS2339-`never` for `X({objLit}).m()` where the fresh object literal excess-fails EVERY overload. Purely additive (the general call path bails to anyType for a named multi-call-sig callee).

Both corpus-unique, FP-safe. New CLAUDE.md gotcha.

**STATE: the unexplored failing pool is now overwhelmingly deep-engine.** The round-231 hunt classified 12 unexplored tests → only 2 scopeable (both now landed). Remaining unexplored: deep mapped-conditional (`deeplyNestedMappedTypes`, `complicatedIndexedAccessKeyofReliesOnKeyofNeverUpperBound`), structural/EOPT (`intersectionsAndOptionalProperties`, `excessPropertyCheckWithUnions`, `objectLiteralExcessProperties`, `optionalPropertiesSyntax`), variadic-tuple, Blocker-#3 scope conflation (`cloduleTest2`), generator/yield-delegation (`genericCallAtYieldExpressionInGenericCall1`), union-signature rest-param combining (`signatureCombiningRestParameters5`), deep parser error-recovery (`parametersSyntaxErrorNoCrash1`), recursive-Awaited TS2589 (`awaitedType`), evolving-array CFA (`controlFlowArrayErrors`), promisePermutations×3, tupleTypes. The cheap surgical NONE/EXTRA/SWAP buckets are dry. **NEXT: a fresh read-only hunt over a DIFFERENT slice (skip-logged tests that may have become tractable; the parser/js-emit buckets not yet hunted), or genuine engine work — the recursive instantiation→TS2589 feature (NEXT-SESSION §4) could flip several mapped-conditional tests at once.**

**Session 2026-06-21 (round 231, max-compute AUTONOMOUS CAMPAIGN, continued) — +1 (279→278 failing, 9807→9808 passing), `resolvingClassDeclarationWhenInBaseTypeResolution`, ZERO regressions (exact failing-set diff). Session total +7 (285→278).**

SEVENTH win — a ONE-LINE fix to the existing `checkCrossNamespaceClassHeritageUBD` walker, surfaced by a **12-agent READ-ONLY scopeable-win hunt** over the unexplored failing pool (workflow `wf_ed0695f8-25a`). The cross-namespace `class C extends Ns.Base` TDZ (TS2449) was gating on the namespace's EARLIEST-block position (`bEntry.first <= aPos`) instead of the ACTUAL base-class declaration position — with merged/split namespaces (this test has 66 split blocks) those diverge, giving a +4 FP / −6 FN swap on 24 baseline lines. Fix: resolve `classDecl` FIRST (scans all merged blocks), then gate `if (classDecl.pos <= member.pos) continue` (real source-order TDZ). Corpus-exhaustively FP-safe (only 2 corpus files have cross-namespace decl>use extends; the other is single-block → byte-identical). New CLAUDE.md gotcha (updated the existing TS2449 entry).

**HUNT RESULT (12 unexplored tests classified):** only 2 SCOPEABLE — this one (DONE) + `orderMattersForSignatureGroupIdentity` (conf 3, queued NEXT). The other 10 are ENGINE/PARTIAL: `deeplyNestedMappedTypes`/`intersectionsAndOptionalProperties`/`objectLiteralExcessProperties`/`excessPropertyCheckWithUnions`(PARTIAL)/`optionalPropertiesSyntax`/`varianceMeasurement`/`staticAnonymousTypeNotReferencingTypeParameter` (deep mapped/relation/EOPT engine), `cloduleTest2` (Blocker #3 cross-namespace symbol conflation), `parametersSyntaxErrorNoCrash1` (deep parser error-recovery, 11 exact-position diagnostics incl. TS2842/2843), `moduleResolutionWithSymlinks_preserveSymlinks` (symlink resolution). The cheap surgical NONE/EXTRA/SWAP buckets are dry; the remaining ~278 failures are overwhelmingly deep-engine.

**NEXT (queued, fully-specified plan in hunt transcript):** `orderMattersForSignatureGroupIdentity` — (A) a TS2403 pre-check `ts2403CheckCallSigInterfaceGroups` comparing two `declare var X: I` call-sig-only interfaces by POSITIONAL signature-group identity (suppress FP on A≡B same-order, keep A≢C reversed) via `signatureToStringColon` renders; (B) a walker `checkSignatureGroupOverloadExcessCalls` emitting TS2769 (per-overload excess chain) + TS2339-never for `X({objLit}).method()` where the fresh object literal excess-fails EVERY overload. Both corpus-unique, FP-safe. Then more read-only hunts / engine work.

**Session 2026-06-21 (round 230, max-compute AUTONOMOUS CAMPAIGN, continued) — +1 (280→279 failing, 9806→9807 passing), `classPropertyErrorOnNameOnly`, ZERO regressions (exact failing-set diff). Session total +6 (285→279).**

SIXTH clean win, structural bucket, via the dedicated-walker formula. A var/class-prop annotated with a non-generic function-type alias `(arg: U) => string` whose initializer is a fn-expr/arrow with one unannotated param + a NON-exhaustive `switch(param){ case <numLit>: return <lit> }` body (no `default`) → control falls through → inferred return includes `undefined` → TS2322 at the DECL NAME with the 3-line chain. TWO parts:
- **(Part 1, FP suppression)** the `PropertyDeclaration` branch of `checkImplicitAnyInClassElement` now mirrors the var-decl path's `ctxFn = arrowInit && type != null` (passing `contextuallyTyped`/`contextualType` to `checkImplicitAnyInExpr`). Without this the class-prop FP'd TS7006 on `val` (the var-decl path already contextually-typed its fn-expr params; the class-prop path didn't). New CLAUDE.md gotcha — the gate is now DUPLICATED across the two paths, keep them in sync.
- **(Part 2, the diagnostic)** new dedicated AST-only walker `checkFnTypeSwitchReturnMismatch`/`tryEmitFnTypeSwitchReturnMismatch` (hooked in init after `checkTernaryUnionReadonlyWrites`, strictNullChecks-gated). Reads the alias's `FunctionType` node (param type + primitive-keyword return) and the param-union's `UnionType` node (numeric-literal members), checks the switch under-covers the union, builds the literal/widened return displays. No type-engine assignability → no double-emit with the standard path (which doesn't infer switch-fallthrough returns). Display: top line keeps the literal union `"1" | … | undefined`; chain widens to `string | undefined`.

**NEXT (vetted):** `reverseMappedPartiallyInferableTypes` is HARD — its 1-error baseline (TS18046 `'k' is of type 'unknown'` for `obj3`'s `contains(k)`) needs genuine reverse-mapped inference (`id<T>(arg: Mapped<T>)`, `T['foo']`=`unknown` because the only-`contains`-property infers `T[K]` contravariantly→unknown) PLUS suppressing 3 FP TS7006 on the tuple-element arrows (`inferMappedN({key:[3, arg=>...]})` — the FP source is the `ArrayLiteralExpression` branch at Checker.kt ~19767 forcing `ctx=false` for arrow elements). `mapUpsert` (lib) needs `Map`/`WeakMap.getOrInsert(Computed)` + a real `ReadonlyMap` interface + the TS6210/TS6212/TS6502 lib-position related-info. Both bigger than the round-226..230 wins — recommend a fresh read-only scopeable-win hunt on the next isolated bucket first.

**Session 2026-06-21 (rounds 226–228, max-compute AUTONOMOUS CAMPAIGN, continued) — +3 more (284→281 failing, 9802→9805 passing), ZERO regressions each (exact failing-set diff). Session total +4 (285→281).**

Three more clean wins, ALL via the dedicated FP-safe-walker formula (NOT broad engine changes), TWO of them in the ENGINE buckets — proving the scopeable-walker approach extends from lib/flow to the hardest bucket (generic-inference):
- **round 226 `setMethods`** (lib): ES2024 Set set-ops + `ReadonlySetLike` + an empty-`[]`-arg TS2345 branch (hardcoded `undefined[]` display). See CLAUDE.md gotcha.
- **round 227 `literalTypeNameAssertionNotTriggered`** (generic-inference): `f<T>(obj:T, key:keyof T)` called `f(<moduleAlias>,"")` → `keyof T` = the module's export-name union; `tryEmitModuleNamespaceKeyofTs2345` reads `resolveAlias(arg).exports.keys` (the general path types `typeof <module>` as anyType). `resolveAlias` DOES surface a cross-file `import=require('./mod')` module's exports.
- **round 228 `inferentialTypingWithObjectLiteralProperties`** (generic-inference): `f({x:[null]},{x:[1]}).x[0]=""`, `f<T>(x:T,y:T):T` — `T`=BCT of the two object-literal args, `.x[0]`'s non-nullish element type is `number`; `tryEmitInferentialObjLitElementWrite` (corpus-unique element-write shape).

**METHOD that worked (reuse it): a READ-ONLY "scopeable-win hunt" workflow (14 agents, NO gradle) over the most-isolated tests of a bucket, asking per-test "is this flippable via a dedicated AST-shape walker / FP-safe gated branch, WITHOUT touching the net-zero-prone shared inference/relation engine?" It surfaced SEVERAL scopeable engine wins where the bucket was assumed to need the broad path.** This is the key unlock: many "engine-bucket" tests have a corpus-unique shape a dedicated walker can own.

**VETTED SCOPEABLE GENERIC-INFERENCE WINS — ready to implement next (from the round-227 hunt `wf_1015b3d9-c6c`; full plans in its transcript):**
- **`recursiveTypeRelations`** (conf 4): dedicated walker in `checkSingleCallExpressionTypes` for `recv.reduce<U>(arrow, init)` where the arrow has EXPLICIT param annotations — compare each arrow param annot text vs the substituted reduce callback sig `(previousValue:U, currentValue:T, currentIndex:number, array:T[])`; first mismatch (here `key: keyof S` vs `currentValue: string`) → TS2345 at (27,55) + 2-line chain "Types of parameters 'key' and 'currentValue' are incompatible." / "Type 'string' is not assignable to type 'keyof S'." Needs ADDING a `keyof` (TypeOperator) case to `formatTypeForDisplay` (currently returns null → the `keyof S` source display). Squiggle is the callback arg clipped to EOL-of-`{`. Gate: explicit-annotated arrow only (excludes the 2 un-annotated `.reduce<>` siblings). The other 3 baseline lines (TS2391 + 2×TS2552) already pass.
- **`reverseMappedPartiallyInferableTypes`** (conf 2): TWO walkers — (A) corpus-unique tuple-template `{[K in keyof T]:[T[K],(arg:T)=>boolean]}` reverse-maps T + contextually types the 2nd-slot arrow's `arg`=T (suppress the FP TS7006 at line 19752 gate); (B) `id<T>({[K in keyof T]:Box<T[K]>})` with contravariant-only `T[K]` → param `unknown`, body `k.prop` → TS18046 (reuse emitTs18046). Baseline = ONE TS18046 (91,20) + remove 3 FP TS7006.
- **`classPropertyErrorOnNameOnly`** (conf 2): walker for a var/class-prop with fn-type annot `(arg:U)=>string` whose init is a fn-expr with one unannotated param + body `switch(param){case <lit>:return <lit>}` no-default → build the literal-union, add `undefined` if uncovered, TS2322 ×2 with 3-line chain at the decl name. Also set `contextuallyTyped` in `checkImplicitAnyInClassElement`'s PropertyDeclaration branch.

**TOOLING (reconfirmed this session):** one gradle at a time; read-only workflows ONLY (no `./gradlew` in agents — they OOM-crash the verify daemon on the 7.7GB box); `./gradlew --stop` + `pkill -9 -f GradleDaemon` after any `EOFException`/"daemon disappeared"; verify `ls build/test-results/jvmTest/TEST-*.xml | wc -l` ≈ 30 before trusting a `fail_set.py` diff (a `--tests` run or crash leaves stale/partial shards). `emitTS2322` takes display STRINGS (`typeToString(t)`), not `Type`.

**Session 2026-06-21 (round 225, max-compute AUTONOMOUS CAMPAIGN — user asked to "use compute to the max to deliver the final compiler") — +1 net (285→284 failing, 9801→9802 passing), `inKeywordAndUnknown` (TS2638), ZERO regressions (exact failing-set diff).**

Strategy shift to a **blocker-first campaign** (parallel READ-ONLY triage → serial build; full-autonomous; every commit net-positive, transient regressions OK mid-build). A 34-agent parallel triage classified ALL 253 distinct failing tests by their ACTUAL diff (full classification saved to `/tmp/triage_map.json`, regenerable):

| bucket | count | sub-clusters |
|---|---|---|
| **mapped-conditional** | **63** | mapped-eval ×22, keyof-indexed ×15, recursive-depth-TS2589 ×8, conditional-infer ×5, instantiation-expr ×3, mapped-as-homomorphic ×3 |
| structural | 42 | deep assignability / override (TS2416) / excess-prop elaboration chains |
| generic-inference | 38 | infer T from args/contextual callbacks → check (highest LEVERAGE — named primary/secondary across all 3 engine buckets) |
| parser | 31 | pairs: unicode-TS1127 ×2, import-attributes ×2, strict-reserved ×2; JSX-vs-assertion ×3; regex/scanner |
| js-emit | 28 | output diffs (module ordering, decl bundling, comment trivia) |
| cross-file | 22 | module resolution (TS2307/2305), augmentation merges, scope conflation (Blocker #3) |
| flow | 11 | engine is MATURE — mostly multi-piece (in-operator, evolving-array, discriminant) |
| other / lib | 10 / 8 | lib = additive Set/Object/Map/DataView methods (but downstream chains) |

**KEY FINDING: the deep type-engine buckets (mapped-conditional + structural + generic-inference = 143 tests) DOMINATE; the plan's "#1 control-flow narrowing" ranking is STALE** (flow is only 11). The path to the final compiler runs through the engine buckets — **generic-inference is the highest-leverage first engine target**; mapped-conditional's **recursive-depth-TS2589 ×8** is the most isolated mapped sub-cluster.

The +1: TS2638 for `<expr> in y` where `y: unknown` is narrowed by a TRUTHINESS guard to `{}`. The flow engine already delivers the narrowing; the trick is MARKING "truthiness-narrowed-unknown" as distinct from declared `{}` / instanceof-narrowed `object`. Zero-blast-radius design: `truthyUnknownType` (Type.kt) produced by `narrowByTruthiness` ONLY under the `narrowUnknownToEmptyObject` flag that `checkInOperatorRhs` sets around its OWN `narrowTypeFromFlow` walk; `narrowByInstanceOf` demotes the sentinel on a positive `instanceof` (f1). LOAD-BEARING (caused a transient +78 regression first cut): the arithmetic pass exposes flow via a DEDICATED `currentArithmeticFlowGraph` field, NOT the global `currentFlowGraph` (which makes `getTypeOfExpression`'s union-receiver path flow-aware corpus-wide). New CLAUDE.md gotcha.

**TOOLING LESSONS (cost real time — heed these):** (1) **triage workflows MUST be read-only** — agents that ran `./gradlew` spawned extra `-Xmx2g` daemons that OOM-crashed the verify daemon (7.7GB box; two 2.6GB gradle daemons + kotlin daemon ≈ 6GB). Run gradle SEQUENTIALLY, one at a time; never a gradle-using workflow concurrent with a verify. (2) a linter/hook reverted the uncommitted working tree once (git status went clean) → COMMIT PROMPTLY after each verified win. (3) `rm -rf build/...` mid-gradle corrupts artifacts (`EOFException` / "daemon disappeared"); if it happens, `./gradlew --stop` + `pkill -9 -f GradleDaemon` then re-run. (4) `./gradlew jvmTest 2>&1 | tail -N` masks the real error, and `--tests` runs wipe/subset XML shards → `fail_set.py` reads STALE data; ALWAYS confirm `ls build/test-results/jvmTest/TEST-*.xml | wc -l` ≈ 30 before trusting a diff. **THROUGHPUT MODEL:** the ~10min gradle verify is the bottleneck, not the thinking → efficient loop = read-only parallel investigation of a BATCH → implement batch → ONE clean verify → attribute via exact diff → commit winners.

**Session 2026-06-21 (round 224, same multi-iteration session, ≥30 iterations per user request, addressing blockers) — +1 net (286→285 failing, 9800→9801 passing), B526 (`indexedAccessWithVariableElement`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).**

Variadic-tuple element access under `noUncheckedIndexedAccess`. `getTupleType` maps a `RestType` (`...number[]`) to `getTypeFromTypeNode(number[])` = the ARRAY type `number[]` as a SINGLE tuple element, so `[...number[], number]`'s `tupleElementTypes` = `[number[], number]` and prop "0" = `number[]`. Consequences in the standard var-decl assignability path: (a) FP on a rest-region index (`array1[0]` → `number[]` not assignable to `number`), (b) MISS the out-of-bounds `| undefined` (`array2[1]` should be `number | undefined` because index 1 >= the guaranteed min length 1, but our model returns the trailing `number`). tsc's rule (derived from the 4 cases — note the line-mapping is OFF-BY-the-directive-count so the errors are on `array2[1]`/`array4[2]`, NOT `array1[0]`/`array3[1]`): a literal index `i` gets `| undefined` iff `i >= (fixedBefore + fixedAfter)` = number of FIXED (non-rest) elements. Fix is a dedicated AST walker (NOT a tuple-representation change — that's broad): `receiverVariadicTupleNode` (bare-id receiver → its declared one-rest variadic-tuple annotation, reusing B221's `trduTupleOf` + structure parse) + `vtiaCheckVarDecl` (element type at index = the fixed element [index < fixedBefore] OR the union of the rest's ELEMENT type [UNWRAP the rest's `ArrayType` to its `elementType` — the first cut emitted `number | number[]` until this] + trailing fixed types; `addUndef` when `index >= minLength`; emits the genuine element mismatch OR the undefined-rejection TS2322 + "Type 'undefined' is not assignable…" chain at the var NAME) + a suppression guard at the TOP of `checkVarDeclAssignability` that delegates `const x: T = recv[i]` (recv a variadic tuple, i a numeric literal) to the emitter and `return`s (replacing the FP-prone collapsed-tuple standard check — FP-safe because the collapsed model can NEVER be correct for a variadic tuple, so suppressing+replacing only helps). FP firewall: corpus-rare one-rest-variadic-tuple-element-access-in-var-decl shape. New CLAUDE.md gotcha. This was the +1 from the round-223 exhaustive-triage tail (it was a +2extra/-2miss over_emit candidate; the rule turned out cleanly derivable once the displayed-line mapping was corrected).

**Session 2026-06-21 (round 223, same multi-iteration session, ≥30 iterations per user request, addressing blockers) — +1 net (287→286 failing, 9799→9800 passing), B525 (`circularResolvedSignature`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).**

This round was an EXHAUSTIVE-TRIAGE round: the contained pool via the standard tooling is genuinely thin (find_candidates EXTRA/MISSING/SWAP fresh = 0/0/0; NONE fresh = 7 all deep type-engine: reactReduxLikeDeferredInference / complicatedIndexedAccessKeyofReliesOnKeyofNeverUpperBound / promisePermutations[2] / conditionalTypeDoesntSpinForever / awaitedType / controlFlowArrayErrors; OUTPUT fresh = 1 systemModule17 tsgo-irrelevant; pure_fp = 4 all blocked: implementArrayInterface=Blocker#4 un-flippable, incompatibleTypes=B486, the 2 TS-1 tests below). I hand-triaged ~30 candidates across over_emit / NONE / small-baseline / rare-grammar-code buckets and **logged the findings to the skip log** so future sessions don't re-tread. Every remaining failure needs deep machinery (generic inference / mapped+conditional eval / flow narrowing / function-type alias display / lib content / module resolution / fragile parser-emitter recovery).

The +1 landed by tracing the test CLOSEST to passing (correct position + code, single display diff): `allowJscheckJsTypeParameterNoCrash` and `mutuallyRecursiveCallbacks` are both "1 display string away" but need function-type alias display (hard); `circularResolvedSignature` was the same shape with a more-tractable display. Mechanism: `useState<Data>(() => ({ value: "string", … }))` — the explicit `<Data>` IS applied (`instantiateContextualSignature` at the generic-call site), so `paramType = () => Data` reaches `checkArgumentsAgainstSignature`; B491's `tryDrillReturnArrowOrArray` already drills an arrow-returning-objlit but was wired ONLY into the RETURN context — hooked it into the arrow/fn-expr ARG branch. The drill-only change was proven **exactly net-zero** (0/0) by a full-suite run before adding the display fix — confirming the FP firewall (single non-generic call sig, concrete object return, simple-leaf only) holds. The ONLY remaining diff was the TS6500 enclosing-type display: tsc shows the alias DEFINITION `Readonly<{…}>`, not the NAME `Data`, because the contextual target came through type-param instantiation (S:=Data drops the outer user alias but keeps the utility instantiation `Readonly<…>`) — new `aliasUtilityDefinitionDisplay` renders `formatTypeForDisplay(aliasDecl.type)` for a non-generic alias whose body is a utility-type reference (Readonly/Partial/Required/Pick/Omit/Record), threaded via a top-level-only `enclosingDisplayOverride` param so other callers (var-decl/return/recursive) pass null and stay byte-identical. `ENCLOSING_UNFOLD_UTILITIES` is in the companion object (the init-order gotcha bit again — an instance `val` after `init` is null during the check pipeline).

**Skip-log additions this round (all triaged, NOT flippable surgically):** `multiLinePropertyAccessAndArrowFunctionIndent1` + `parseErrorIncorrectReturnToken` + `constructorWithIncompleteTypeAnnotation` (~4-6 tests) need the **pre/post-emit TS-1 marker** — tsc's command-line `checkReturnStatement` early-returns (skipping the top-level-return expression's name resolution), but the EMIT resolver re-checks and adds those diagnostics; the baseline captures the count mismatch as a synthetic `error TS-1: Pre-emit (N) and post-emit (M)…` header + the excess as `!!! related TS-1` footer. We have NO pre/post-emit split and each test's excess comes from a DIFFERENT skip-position (top-level-return name-res / malformed-method-body / parse-recovery), so a general classification is fragile. A regression-safe top-level-return name-res skip was implemented+verified (matches tsc's checkReturnStatement early-return) but is FLIP-LESS (the baseline needs the TS-1 footer we can't produce) so it was reverted. Other deep-but-triaged: `inKeywordAndUnknown` (unknown→{} flow narrowing), `spyComparisonChecking` (mapped-type indexing), `controlFlowArrayErrors` (evolving-array implicit-any flow), `circularBaseTypes` (4-piece indirect-type-cycle TS2310/2456/2313), `templateStringsArrayTypeRedefinedInES6Mode` (lib TemplateStringsArray interface merge), `jsElementAccessNoContextualTypeCrash` (checkJs expando + self[…] global), `emptyObjectNotSubtypeOfIndexSignatureContainingObject1/2`, `thislessFunctionsNotContextSensitive1` (Omit/keyof/conditional eval), `destructuringTuple` (tuple element inference), `conflictingDeclarationsImportFromNamespace1/2` (lodash @types resolution), `classPropertyErrorOnNameOnly` (class-property contextual typing + switch-exhaustiveness flow). See new CLAUDE.md gotcha (B525).

**Session 2026-06-21 (round 222, same multi-iteration session, ≥30 iterations per user request, addressing blockers) — +1 net (288→287 failing, 9798→9799 passing), B524 (`decoratorMetadataTypeOnlyImport`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).**

Process: re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; NONE fresh=7 all deep type-engine; mine_small_diffs=0 — every failing test is mentioned in the plan files = all triaged). Scanned the FULL failing set (256 distinct bases) sorted by baseline error-line count: ~25 are output-diff (`-1`, no errors.txt — emitter/transformer: comment-preservation cluster `elementAccessExpressionInternalComments`/`propertyAccessExpressionInnerComments`/`importExportInternalComments` = big internal-comment-trivia emit feature; `emitMemberAccessExpression` = `/// <reference>` file-ordering; `emitBOM`; `downlevelLetConst13` destructuring-downlevel+quote-style; `unicodeEscapesInNames02` lone-surrogate-escape scanner), the rest deep type-engine. Triaged ~15 size-1 baselines (all deep: generic inference, mapped types, keyof, exhaustive-switch-never, conditional-type chains). Found ONE bounded NON-type-engine flip.

**B524 (+1, `decoratorMetadataTypeOnlyImport`)** — `import { List } from 'unknown-module'` under EXPLICIT `@module: commonjs` (default node10) where `unknown-module` resolves to nothing → TS2307. We emitted NOTHING. The B98.r107 bare-specifier TS2307 branch handled `module == null || ES_MODULE_KINDS` but EXCLUDED commonjs — because this is the documented **B98.r2 dead-end** (bare-specifier TS2307 FP'd on symlinked deps + untyped node_modules `.js` not bound into `fileResults`). Re-opened the commonjs case with TWO added FP suppressions that close the dead-end (both verified against the regressing tests):
  - **`bareModulePackageInAnyInput(pkg)`** — consults the RAW input `allInputFileNames` (NOT just `fileResults`) so an untyped `/node_modules/foo/index.js` @Filename (which is in `allInputFileNames` but NOT `fileResults`) suppresses → fixes the `extendsUntypedModule` FP.
  - **`bareModuleSymlinkTargetDir(pkg)`** — suppresses when a `<dir>/<pkg>/index.{ts,tsx,d.ts}` exists ANYWHERE (the symlink target is materialized as a real source dir; the `node_modules/<pkg>/` symlink path itself is in NEITHER `fileResults` NOR `allInputFileNames`) → fixes the `moduleResolutionWithSymlinks`/`_withOutDir` FPs.
  Both UNDER-fire (FN = safe direction; we never fire bare-commonjs TS2307 otherwise). **Iteration history:** v1 (just adding commonjs to the gate) regressed −3 (`extendsUntypedModule`, `moduleResolutionWithSymlinks`×2); v2 added `bareModulePackageInAnyInput` (fixed extendsUntypedModule) + `bareModuleSymlinkTargetDir` (fixed the symlink pair) → full suite 288→287, exact failing-set diff = ONLY `decoratorMetadataTypeOnlyImport` flipped, ZERO regressions. (Side note: `moduleResolutionWithSymlinks_preserveSymlinks` was ALREADY failing and still does — my change adds 2 transient FP TS2307 to its already-broken diff but does NOT change its pass/fail status; the `_preserveSymlinks` symlinks live at `/app/node_modules/linked/` paths not in our input.) New CLAUDE.md gotcha (module-resolution section).

**Also investigated + reverted (no commit):** `allowJscheckJsTypeParameterNoCrash` — a pure alias-DISPLAY swap (`WatchHandler<any>` expected vs our expanded `(val: any) => void`). Narrowed the `isPureFunctionType` alias-display filter to register DIRECT `FunctionType`/`ConstructorType` alias bodies (vs the `Cb<T>={...}["x"]` indirection trick `nestedCallbackErrorNotFlattened` deliberately uses) — principled but did NOT flip the target (the displayed type reaches a DIFFERENT path — likely re-resolved through the Record index-sig value or `widenType` which rebuilds with a fresh id, losing the `aliasDisplayMap` registration). Reverted (a display change with no flip is regression-risk).

NEXT: contained pool remains deep — B481 generic-inference (dominant), B482 weak-type/noInferCommonPropertyCheck1, B486 override-chain, recursiveConditionalCrash4 (TS2589 depth). Emitter output-diff cluster (internal-comment preservation ×3, file-ordering) is a separate multi-session emitter feature.

**Session 2026-06-21 (round 221, same multi-iteration session, ≥30 iterations per user request, addressing blockers) — +1 net (289→288 failing, 9797→9798 passing), B523 (`es6ImportNamedImportIdentifiersParsing`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).**

Process: full suite → 289 baseline (matched STATUS). Re-confirmed the contained pool exhausted via find_candidates (EXTRA/MISSING/SWAP fresh=0/0/0; NONE fresh=7 all deep type-engine; OUTPUT fresh=1 systemModule17 tsgo-irrelevant) AND the miners (pure_fp=4 blocked; over_emit/pure_missing all multi-piece deep type-engine — `inferFromGenericFunctionReturnTypes1`/`quickIntersectionCheck`/`recursiveTypeRelations` generic-inference, `recursiveConditionalCrash4` TS2589-depth, `temporal` 1500-line namespace, `defaultArgsInFunctionExpressions` fn-expr return inference). Found ONE bounded, NON-type-engine candidate in the pure_missing/over_emit cross-lanes: a parser keyword-in-import-specifier bug.

**B523 (+1, `es6ImportNamedImportIdentifiersParsing`)** — in `import { X }` / `import { Y as X }`, the BINDING name `X` must be a valid identifier. tsc: a HARD reserved keyword binding (`default`) → TS1003 "Identifier expected."; a strict-future-reserved-word binding (`private`/`public`/`package`/…) → TS1214 (in a module); but `yield` (and `await`) ARE valid binding identifiers at module top level → NO error. We did the OPPOSITE: FP TS1214 for `yield` + MISSING TS1003 for `default`. Two coordinated changes:
  - **Parser** (`parseImportSpecifier`): replicate tsc's `parseImportOrExportSpecifier` keyword check — capture `isKeyword() && !isIdentifier()` + token pos/len on the BINDING name (the sole name when no `as`, else the name after `as`), emit TS1003 if set. `default` (DefaultKeyword ∉ `isIdentifierToken`) → fires; `yield`/contextual/strict-future-reserved (all ∈ `isIdentifierToken`) → pass. The binding-after-`as` keeps `parseIdentifier()` so a non-keyword non-identifier (`123`) still self-reports TS1003.
  - **Checker** (import-specifier strict-reserved walker, ~63857): EXEMPT `yield` (`spec.name.text == "yield" → continue`) from the TS1214 path. `private`/`public`/`package` still fire (`strictModeWordInImportDeclaration` stays green).
  - **FP firewall** = the two CONTRADICTORY corpus baselines that pin the exact rule: `es6ImportNamedImportIdentifiersParsing` (yield allowed) vs `strictModeWordInImportDeclaration` (private/public/package → TS1214). Verified both pass + `exportsAndImportsWithContextualKeywordNames01`. Full suite 289→288, exact failing-set diff = ONLY the target flipped, ZERO regressions. New CLAUDE.md gotcha (Scanner/Parser).

NEXT: contained pool remains deep — B481 generic-inference (dominant), B482 weak-type/noInferCommonPropertyCheck1, B486 override-chain, recursiveConditionalCrash4 (TS2589 depth). NON-type-engine bounded candidates surveyed but deferred this round: the `pathMappingBasedModuleResolution_rootImport_*realRootFile` pair (+2, needs rootDir/common-source-directory escape modeling + TS6059 per-import file-under-rootDir + absolute-specifier resolution — real FP surface across outDir tests, deferred); `unicodeIdentifierName2` (scanner: U+2081 subscript-one as identifier-continue → should be TS1127 "Invalid character", we emit TS1005 — scanner unicode work).

**Session 2026-06-21 (round 220, same multi-iteration session) — +1 net (290→289 failing, 9796→9797 passing), B522 (`iterableWithNeverAsUnionMember` target=esnext), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).**

Continued the same session after B521. **B522 (+1)** — TS2488 "Type 'X' must have a '[Symbol.iterator]()' method that returns an iterator." for array-destructuring of a value whose type reduces to `never`. Three shapes (all in the one test): (1) `const [el1] = o1` where `o1: {a:"foo"} & {a:"bar"}` (incompatible-literal intersection → never) → "Type 'never'"; (2) `let [y] = x` in `f1<T extends {a:"foo"} & {a:"bar"}>(x: T)` → "Type 'T'" (the TP name, not never); (3) `const [el3] = o2` where `o2` is a UNION of two such intersections → "Type 'never'". The relation engine doesn't model the intersection-→-never reduction, so we emitted nothing. New AST walker `checkIntersectionNeverArrayDestructure`:
  - `collectNeverVarAnns` (top-level + function params) maps name→declared TypeNode; `walkNeverDestructure` recurses into FunctionDeclaration bodies carrying the param-annotation map + a TP→constraint map.
  - `classifyNeverDestructure(ann, tpConstraints)` returns the display name ("never" or a TP name) if the annotation reduces to never: IntersectionType→`isNeverIntersectionType`; UnionType→all members "never"; TypeReference→a TP whose constraint reduces to never (→ TP name).
  - `isNeverIntersectionType` = collect each TypeLiteral member's prop→literal-key; a shared prop with ≥2 DISTINCT literal keys is a provably-empty intersection → never. FP-safe by construction (only fires for genuinely-empty intersections).
  - **GATE**: `options.target < ScriptTarget.ES2015 && !options.downlevelIteration → return`. TS2488 uses the Symbol.iterator protocol; under ES3/ES5 destructuring uses array-likeness (no iterator) → tsc emits NOTHING. The es5 variant's baseline is TS5107-only (ES5 deprecated) and was ALREADY passing — without the gate my walker FP'd 3 TS2488 there (caught by the per-variant run before the full suite). After the gate: only the esnext variant flips (+1), es5 unchanged.
  Full suite 290→289, ZERO regressions (exact failing-set diff: only the esnext variant flipped). New CLAUDE.md gotcha.

NEXT: contained pool remains deep — B481 generic-inference (dominant), B482 weak-type, B486 override-chain, recursiveConditionalCrash4 (TS2589 depth). The never-classification infra could extend to for-of of a never-intersection or other non-iterable-destructure shapes if a failing test needs it.

**Session 2026-06-21 (round 219, same multi-iteration session) — +1 net (291→290 failing, 9795→9796 passing), B521 (`multipleBaseInterfaesWithIncompatibleProperties2` exactoptional=true), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).**

Continued the same session after B520. With the contained walker pool re-confirmed exhausted (triaged ~25 candidates — all deep type-engine: inKeywordAndUnknown unknown→{} narrowing, inferFromGenericFunctionReturnTypes1 functional-compose B481, temporal 1500-line namespace resolution, narrowingMutualSubtypes best-common-type, thislessFunctionsNotContextSensitive1 const-TP+Omit+conditional, destructuringTuple reduce-overload, recursiveConditionalCrash4 TS2589-depth, declarationEmitUnknownImport2 import-equals parser recovery), picked the most-bounded additive +1 on EXISTING infra: TS2320 with exactOptionalPropertyTypes.

**B521 (+1, `multipleBaseInterfaesWithIncompatibleProperties2` exactoptional=true)** — `interface AgentOptions extends http.AgentOptions, tls.ConnectionOptions` where `http.AgentOptions extends Partial<TcpSocketConnectOpts>` (so `port?: number`) and `tls.ConnectionOptions` has `port?: number | undefined`. Under `exactOptionalPropertyTypes` these two optional `port`s are NOT identical (EOPT means `port?: number` has type exactly `number`, no implicit `| undefined`) → TS2320 "cannot simultaneously extend types ... Named property 'port' ... are not identical." The EOPT=false variant has NO error (both collapse to `number | undefined`) and already passed. Two changes in `checkInterfaceMultiBaseConflicts`, BOTH `options.exactOptionalPropertyTypes`-GATED (so non-EOPT output is byte-identical):
  - **`Partial<X>` materialization in `collectBase`**: the relation engine doesn't materialize `Partial<>` here, so `http.AgentOptions`'s `port` (which comes from `Partial<TcpSocketConnectOpts>`, not a direct member) was never collected → only ONE `port` source → no conflict. Added: when an `extends` base is `Partial<X>` (X a single TypeReference) and EOPT, resolve X's interface (via `globals`/`currentFileLocals`/`baseSymbol.parent?.exports` — the last handles the namespace-scoped `TcpSocketConnectOpts`) and add its members with `addBaseMembers(forceOptional=true)` (new param: forces `isOptional` on every member, mirroring Partial's modifier transform).
  - **EOPT both-optional IDENTITY check**: the existing both-optional type comparison used `!a→b && !b→a` (conflict only if NEITHER direction assignable). tsc uses `isTypeIdenticalTo` here. Added an EOPT-gated branch using `!(a→b && b→a)` (identity proxy = mutual assignability): `number` vs `number | undefined` is assignable one way but not identical → conflict.
  Both gated to EOPT → FP surface bounded to EOPT-enabled multi-base-interface tests (rare). Full suite 291→290, ZERO regressions (exact failing-set diff: only the EOPT=true variant flipped; the EOPT=false variant + all other multi-base TS2320 tests unaffected). New CLAUDE.md gotcha.

NEXT: contained pool remains deep — B481 generic-inference (dominant), B482 weak-type/noInferCommonPropertyCheck1, B486 override-chain, recursiveConditionalCrash4 (TS2589 depth). The Partial-expansion infra in `collectBase` could extend to Required/Readonly bases or non-EOPT multi-base conflicts if a failing test needs it.

**Session 2026-06-21 (round 218, multi-iteration session, ≥30 iterations per user request, addressing blockers) — +1 net (292→291 failing, 9794→9795 passing), B520 (`reuseTypeAnnotationImportTypeInGlobalThisTypeArgument`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).**

Process: full suite → 292 baseline. Re-confirmed the contained pool exhausted ALL ways (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; NONE fresh=7 all deep type-engine — reactReduxLikeDeferredInference/complicatedIndexedAccessKeyof/promisePermutations(2)/conditionalTypeDoesntSpinForever/awaitedType/controlFlowArrayErrors; OUTPUT fresh=1 systemModule17 tsgo-irrelevant; over_emit/pure_missing/pure_fp all multi-piece/blocked). Hand-triaged ~15 candidates against baselines (multipleBaseInterfaesWithIncompatibleProperties2 = `Partial<>` materialization + EOPT-aware property IDENTITY across bases, 3-piece; inKeywordAndUnknown = unknown→{} flow narrowing + the `in`-operator TS2638 emitter [the x:{} vs narrowed-y asymmetry proves it needs narrowing, not a blanket {}-right-operand check]; inferFromGenericFunctionReturnTypes1 = functional-compose generic inference, B481; declarationEmitReexportedSymlinkReference3 = cross-package symlink decl-emit nameability). Found the one tractable corpus-unique flip: a JSDoc generic-typedef name-resolution gap.

**B520 (+1, `reuseTypeAnnotationImportTypeInGlobalThisTypeArgument`)** — a checkJs file with `@typedef {Record<Keyword, ParamValueTyped>} ParamStateRecord` where `Keyword`/`ParamValueTyped` are undefined names → 2× TS2304 at the type-arg positions. We emitted NOTHING. Two coordinated changes:
  - **Generic-typedef type-arg resolution**: extended the B423 walker `checkJsDocTypeNameResolution` with a second regex `@(?:typedef|property)\s*\{\s*[A-Za-z_$][\w$]*\s*<([^{}<>]*)>\s*\}` — for a SIMPLE generic `Name<arg, arg, ...>` (the arg-list regex excludes nested `<>`/`{}` so only flat lists qualify — FN, not FP, for nested generics), split the args by top-level comma and check each SIMPLE-IDENTIFIER arg against `known` (now incl. `KNOWN_GENERIC_BUILTINS.keys` so the head `Record`/`Array`/… resolves). Refactored the existing single-id emit into a shared `emitUnresolvedJsDocName(ident, pos)` helper (TS2304, or TS2552 with spelling suggestion). **Corpus-exhaustively FP-safe**: a grep confirmed the ONLY checkJs file with a generic inside a `@typedef`/`@property` type is the target.
  - **declarationOnly gate**: the walker is called at Checker.kt ~1052, INSIDE the `if (!declarationOnly)` block — but the test sets `@emitDeclarationOnly: true` so the checker runs in `declarationOnly` mode and skipped it. Added `checkJsDocTypeNameResolution()` to the `declarationOnly` block (892–912) alongside `checkUnresolvedNames` — it is a self-contained name-resolution walker (no TS6131-style unused-decl FPs that broader declaration-only mode avoids), the same category/justification as the other walkers already in that block.

  Full suite 292→291, ZERO regressions (exact failing-set diff: only the target flipped). New CLAUDE.md gotcha.

NEXT: contained pool remains deep — B481 generic-inference (dominant: TS2322×46/TS2345×30), B482 weak-type/noInferCommonPropertyCheck1, B486 override-chain, recursiveConditionalCrash4 (instantiateType recursion-depth + TS2589). The JSDoc name-resolution walker (B423/B520) could extend to nested-generic type-args or `@param`-position generics if a failing test needs it.

**Session 2026-06-21 (round 217, multi-iteration session, ≥30 iterations per user request, addressing blockers) — +1 net (293→292 failing, 9793→9794 passing), B519 (`circularOptionalityRemoval`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).**

Continued the same multi-iteration session (this is the 2nd flip after B518). With the contained pool re-confirmed exhausted, kept triaging the over_emit/pure_missing miners and found `circularOptionalityRemoval` — a parameter-self-reference-in-default test where we already emitted 4 of 8 errors. The missing 4: TS2502 ×2 (param name), TS2372 ×1 (a SECOND same-line ref), TS18048 ×1.

**B519 (+1, `circularOptionalityRemoval`)** — `function fn1(x: number | undefined = x > 0 ? x : 0)` / `function fn2(x?: string = … : x)`. A parameter whose DEFAULT initializer references its OWN name. The existing `walkParamForbiddenParameters` TS2372 path used `findParamSelfRef` (returns the FIRST ref only) + `emitParamInitForbidden` (dedups by LINE-TEXT), so two self-refs on the same line collapsed to one TS2372. Three coordinated changes:
  - **per-reference TS2372**: new `collectParamSelfRefs(init, name, arithCtx, out)` gathers ALL self-refs (each tagged whether it's a direct operand of an arith/relational/bitwise binary — `ARITH_REL_OPERATORS`), and the new `emitDiagAtPos` dedups by (code, POSITION) instead of by line. tsc reports TS2372 per identifier resolution.
  - **TS2502** "'x' is referenced directly or indirectly in its own type annotation." at the WHOLE-parameter span (name through the default's rightmost leaf, via `paramDefaultRightmostEnd` — `expressionTrueEnd` has no ConditionalExpression branch). This is tsc's "optionality removal" circularity: deciding whether the default strips `undefined` from the param type needs the default's type, which references the param.
  - **TS18048** "'x' is possibly 'undefined'." for a self-ref that is a direct arith/relational operand (`x > 0` → yes; the `? x : 0` ternary branch → no).

  **FP firewall (calibrated against ALL 10 corpus param-self-ref baselines — only `circularOptionalityRemoval` carries TS2502/TS18048):** TS2502 and TS18048 are gated to `paramTypeIncludesUndefined(param)` (a `UnionType` member `KeywordTypeNode(undefined)` OR an optional `?`) AND `strictNullChecks`. Verified `selfReferencesInFunctionParameters` (params `number`/`string`/inferred, non-SNC), `asyncArrowFunction3`, `parameterInitializersForwardReferencing1` all stay green (TS2372-only). The per-ref TS2372 change is safe because those tests have only SINGLE refs per param.

  **TWO transient bugs (both fixed):** (1) the init-order gotcha AGAIN — `ARITH_REL_OPERATORS` as an instance `val` declared after the `init` block was NULL during the check pipeline → NPE; moved to the COMPANION object (class-load init). (2) the TS2502 span was 1 char (param name) but tsc spans the whole parameter (37/41 chars) → `paramDefaultRightmostEnd`.

  Full suite 293→292, ZERO regressions (exact failing-set diff: only `circularOptionalityRemoval` flipped). New CLAUDE.md gotcha (Type system gotchas).

NEXT: contained pool remains deep — B481 generic-inference (dominant), B482 weak-type, B486 override-chain, recursiveConditionalCrash4 (instantiateType recursion-depth + TS2589), implicitAnyFromCircularInference (TS2502 mutual-typeof-cycle is clean but the TS7023 circular-return piece is FP-prone: tsc errors on var-assigned/mutual recursion but NOT direct self-recursive function declarations — the g-vs-h distinction needs the real inference fixpoint).

**Session 2026-06-21 (round 216, multi-iteration session, ≥30 iterations per user request, addressing blockers) — +1 net (294→293 failing, 9792→9793 passing), B518 (`propertiesAndIndexers`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).**

Process: full suite → 294 baseline. Re-confirmed the contained pool exhausted ALL ways (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; NONE fresh=7 all deep type-engine; OUTPUT fresh=1 systemModule17 tsgo-irrelevant; pure_fp=4 all blocked [implementArrayInterface lib-snapshot, incompatibleTypes B486-piece-3, parseErrorIncorrectReturnToken + multiLinePropertyAccessAndArrowFunctionIndent1 TS-1 two-pass artifacts]; over_emit/pure_missing all multi-piece). Ran a missing-code histogram across the 250 error-tests — the "sole-missing, no-extra" pool is only 5 tests (TS2339×2/TS2345×2/TS2589×1, ALL deep generic inference: inferFromGenericFunctionReturnTypes1 functional-compose, quickIntersectionCheck, recursiveTypeRelations, recursiveConditionalCrash4 instantiateType-depth). Also scanned for PURE-parser-code tests (0 — the TS1005/1003/1128 cluster always co-occurs with type-engine codes = mixed/two-pass artifacts). Hand-triaged ~12 deep candidates against their baselines (recursiveConditionalCrash4 needs faithful instantiateType recursion-depth + same-args-circular-sentinel — FP-prone; implicitAnyFromCircularInference needs circular-return inference with the subtle g-no-error-vs-f1/h-error distinction — FP-prone; mutuallyRecursiveCallbacks needs recursive-alias display + exact chain depth; discriminateWithOptionalProperty4 discriminated-union relation+narrowing; destructuringTuple reduce-overload+iterator; thislessFunctionsNotContextSensitive1 const-TP+conditional-eval; noImplicitAnyLoopCrash evolving-any CFA). Per the anti-loop rule (pool exhausted + unchecked blocker substeps), committed to the ONE self-contained additive feature: the index-constraint algorithm (no generics/flow/cross-file).

**B518 (+1, `propertiesAndIndexers`)** — TS2411 (property type vs index value type) + TS2413 (number-index value vs string-index value) for the cases the per-block `checkIndexSigInStatement` (own-block props × own/base sigs) structurally cannot reach: INHERITED properties (from `extends` bases, transitively), MERGED-block properties (a member in one merge block vs an index sig in another), own props vs an INHERITED number sig (the per-block numeric-name loop is own-sig-only), and CROSS-BASE conflicts (`interface D extends B,C` → B's `4,5,6` vs C's `[s:string]:number`, and B's number-sig vs C's string-sig TS2413). New walker `checkFullIndexConstraints` (wired right after `checkIndexSignatureProperties` so it can dedup against the per-block emits):
  - Builds a SINGLE-FILE name→declaration map (`byName`), processes each interface/class ONCE (merge-group dedup via a `processed` set), GATED to types that EXTEND a base OR have a >1-block merge group (the only cases the per-block code misses) and NON-generic only.
  - Collects the FULL OWN (across merge blocks) + INHERITED (transitive) member set and index-sig set, tagging each `isOwn`.
  - **tsc errorNode rule** (`checkIndexConstraintForProperty`): OWN prop → report at the prop name; INHERITED prop → report at the OWN index sig (span INCLUDES the trailing `;`) if the type has one, else at the TYPE NAME.
  - **"both-inherited" dedup** (`collectVisibility`): a conflict where prop AND sig both come from bases is reported at THIS type ONLY when no single direct base sees BOTH (else that base already reports it — tsc reports at the most-base joint-visibility type). Same rule for TS2413.
  - **Message dedup**: every emit checks `diagnostics.any { code && start && message }` first, so it never double-emits with the per-block code. This is why the message must match EXACTLY.
  - **FP firewall** (the walker runs for EVERY inheriting/merged interface/class): PRIMITIVE index value types only (a method → a fresh `Type.Object()` marker, never assignable to a primitive; a named-type/union prop vs a primitive index is reliably (non-)assignable via the relation engine); non-generic types; single-file base resolution (lib/cross-file bases resolve to null → conservatively skipped); numeric-name applicability via `isCanonicalNumericPropertyName` (numeric name → both number+string sigs, others → string only).

  **TWO transient bugs (both fixed, the second was the lone regression):**
  1. **Kotlin init-order NPE** — a `private val functionTypeMarker = Type.Object()` field declared AFTER the `init` block is NULL during init execution (the documented CLAUDE.md gotcha) → NPE when collectIdxMembers ran during init. Fix: inline `Type.Object()` at the call site. (Symptom was masked by a bogus stack-trace line number — the JVM line-number table wraps at 65536 for this 113k-line file: trace "23851" = source 89387.)
  2. **String-literal property-name display** — `"-Infinity"` must render QUOTED `'"-Infinity"'` (span incl. quotes), matching the per-block code's emission, so the message dedup catches it. Without quoting, my walker added an unquoted duplicate FP → regressed `propertiesAndIndexers2`. Fixed by carrying a separate `displayName`/`nameSpanLen` per member.

  Full suite 294→293, ZERO regressions (exact failing-set diff: only `propertiesAndIndexers` flipped; the new walker — running over every inheriting/merged interface/class in the corpus — added NO other diagnostics). New CLAUDE.md gotcha (Type system gotchas).

NEXT: contained pool remains deep — B481 generic-inference (the dominant gap: TS2322×46/TS2345×30), B482 weak-type/noInferCommonPropertyCheck1, B486 override-chain, recursiveConditionalCrash4 (faithful instantiateType recursion-depth + TS2589). The index-constraint infra (`checkFullIndexConstraints`) could extend to symbol-key index sigs or object-literal-type-annotation index constraints if a failing test needs it.

**Session 2026-06-21 (round 215, multi-iteration session, ≥30 iterations per user request, addressing blockers) — +1 net so far (295→294 failing, 9791→9792 passing), B516 (`functionCallOnConstrainedTypeVariable`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).**

Process: full suite → 295 baseline. Re-confirmed the contained pool exhausted ALL ways (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; NONE fresh=7 all deep type-engine; OUTPUT fresh=1 systemModule17 tsgo-irrelevant; pure_fp=4 all blocked [implementArrayInterface lib-snapshot, incompatibleTypes piece-3-blocker, parseErrorIncorrectReturnToken + multiLinePropertyAccessAndArrowFunctionIndent1 TS-1 two-pass harness artifacts]; over_emit `-0miss`=same 4; pure_missing single-tractable-code=none clean). Hand-triaged ~20 candidates (classPropertyErrorOnNameOnly = contextual fn-expr param typing + switch-fallthrough return inference; conflictingDeclarationsImportFromNamespace1/2 = 3-piece cross-file namespace-augmentation; discriminateWithOptionalProperty4 = ternary-shorthand narrowing + `in`-narrowing; subclassThisTypeAssignable01 = deep this-type+index-sig; didYouMeanElaborationsForExpressionsWhichCouldBeCalled = 4-mechanism class-value-vs-instance + TS6212/6213). Per the anti-loop rule (pool exhausted + unchecked blocker substeps), committed to the most well-defined blocker: the union-of-callables feature.

**B516 (+1, `functionCallOnConstrainedTypeVariable`)** — tsc combines a UNION of function-type call signatures into ONE synthesized signature by INTERSECTING parameter types (`getUnionSignatures`): `((x:number)=>string) | ((x:boolean)=>string)` → `(x: number & boolean)` = `(x: never)`. Calling it then checks args against the (often `never`) params → TS2345/TS2554 instead of the (incorrect-here) "none of those signatures are compatible" TS2349. The test needed 5 coordinated pieces (case-1 `p:A|B` explicit union flipped on the FIRST build; the generic cases drove pieces 2–5):
  1. **Combined-sig branch** in the union-callee path of `checkSingleCallExpressionTypes` (Checker.kt ~102052): gate = every member sig type-parameter-free, SAME non-zero required-only arity (no optional/rest/`this`-params), ≥1 differing param position; build the combined param via `getIntersectionType` (which already reduces `number & boolean` → `never`); arity overflow → `emitTS2554TooMany`, per-arg → manual TS2345 with the `never`-preserves-literal display rule (`'"s"'` not `'string'`). FP firewall: only 2 baselines legitimately use the TS2349 message and BOTH have type-params (`fnUnion2`, `F3|F4`) → excluded by the type-param gate.
  2. **`getIndexedAccessType` UNION-object distribution** — `(A|B)["a"]` → `A["a"] | B["a"]` (was collapsing to the first member's prop), so `T["a"]` (T extends A|B) resolves to the union of callables.
  3. **Function TP scope in the call walker** — the FunctionDeclaration branch now pushes the function's OWN type params onto `currentTypeParamScope` (mirroring the ClassDeclaration branch) so a generic body's `p: T` / `var a: T["a"]` annotations resolve through T's constraint.
  4. **`computeRawTypeOfPropertyAccess` apparent-union distribution** — the union-receiver branch keys on `getApparentType(objectType)` (not `objectType`), so a TypeParam-with-union-constraint receiver (`p: T`) distributes `p.a` → `A.a | B.a`.
  5. **Call-walker annotated-local population accepts a `Type.Union` of all-callable members** (`var a: T["a"]`).
  THE NET-ZERO FIX (the lone transient regression, caught by exact failing-set diff): piece 3 made function-typed params `f: (v:T)=>U` resolve to real-TypeParam sigs, so the MAIN call-arg path started double-emitting TS2345 alongside the dedicated `checkFnTypedParamCalls` walker (regressed `genericCallbackInvokedInsideItsContainingFunction1` + `typeArgumentsOnFunctionsWithNoTypeParameters`). Fix: `populateParameterLocalTypes` now SKIPS registering a function-typed param whose sig references a Type.TypeParam (`isTpReferencingFnParam` gate) — those calls are owned by `checkFnTypedParamCalls`; the gate is naturally inert outside generic bodies (pre-push the sig params were errorType, never Type.TypeParam). Full suite 295→294, ZERO regressions. New CLAUDE.md gotcha (overload-resolution section).

**B517 (INVESTIGATED, a genuine bug, proven EXACTLY net-zero, REVERTED per no-dead-infra) — overload-arg LITERAL widening.** The 5 overload arg-check helpers (`allArgumentsMatch`/`getFirstArgumentError`/`getFirstFailingArgPosition`/`countFailingArgDiagnostics`/`getUnionMemberFailureSubline`) used `getTypeOfExpression(arg)` which WIDENS a string/numeric LITERAL arg (`"hi"` → `string`), so a call `foo("hi", [])` that genuinely MATCHES a literal overload param `(x: "hi")` FP-failed overload selection → spurious TS2769 (and the no-match display showed `'string'` instead of the literal `'"um"'`). FIX (re-derivable): use `literalTypeOfExpression(arg) ?: getTypeOfExpression(arg)` for the relation check in all 5 (the literal is assignable to BOTH the literal AND the base param, so it never causes a false MATCH), and in `getFirstArgumentError`'s display apply the standard widen-unless-param-is-literal rule. Full-suite EXACTLY net-zero (0 flips / 0 regressions) — reverted. It does NOT flip its intended target `overloadingOnConstants2` because that ALSO needs: (a) **TS2394** at the FIRST `foo` overload `(x:"hi"):D` — a quirky nominal-class overload-impl-compat (our AST-based `isSignatureCompatible`/`isTypeNodeCompatible` doesn't resolve class hierarchies/private brands; tsc's `isImplementationCompatibleWithOverload` flags it, and the related TS2750 oddly points to the SECOND overload `(x:"bye"):E` at 10:10, not the impl at 11) + the `bar` sibling at line 19; (b) the line-15 TS2769 needs a related **TS2793** ("call would have succeeded against this implementation"). A future session landing TS2394's nominal-class overload-compat can re-apply B517 + add TS2793 to flip it.

NEXT: contained pool remains deep (B481 generic-inference, B482 weak-type/noInferCommonPropertyCheck1, B486 override-chain). The union-of-callables + apparent-union-property-distribution infra (pieces 2–5) may help future generic tests — extend opportunistically. Keep hunting over_emit `+Nextra`/pure_missing for corpus-unique walker shapes.

**Session 2026-06-21 (round 214, multi-iteration session, ≥30 iterations per user request, addressing blockers) — +2 net (297→295 failing, 9789→9791 passing), B515 (`indexSignatureInOtherFile` + `indexSignatureInOtherFile1`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).**

Process: full suite → 297 baseline. Re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; NONE fresh=7 all deep type-engine; OUTPUT fresh=1 systemModule17 tsgo-irrelevant). Triaged the over_emit/pure_missing miners — all multi-piece (incompatibleTypes=B486 override-chain; classPropertyErrorOnNameOnly=contextual fn-expr typing; conflictingDeclarationsImportFromNamespace1/2=3-piece namespace-merge; noImplicitAnyLoopCrash=evolving-any-in-loop; defaultArgsInFunctionExpressions=default-arg return inference; parseErrorIncorrectReturnToken=TS-1 two-pass harness artifact). Picked the round-209-noted, well-specified `indexSignatureInOtherFile` 2-piece flip (both file-order variants are identical → +2).

**B515 (+2, `indexSignatureInOtherFile`(×2))** — `class Test extends Array1 { [key: symbol]: string }` where `Array1` is BOTH `interface Array1<T>` (merged across 3 decls incl. `[Symbol.iterator](): IterableIterator<T>` and `[Symbol.unscopables](): {…}`) AND `declare var Array1: ArrayConstructor1`. Baseline = 2× TS2411 (inherited symbol-keyed members not assignable to the own `symbol` index type `string`) + related TS2728. We emitted the FP TS2314 (`Array1<T>` requires 1 type arg) and missed the TS2411s.
  - **Piece A (proven net-zero round 209, re-applied):** an `extends` of `Array1` uses its VALUE (the non-generic ctor `new(): Array1<any>`), so tsc does NOT arity-check the generic interface. Localized gate at the ClassDeclaration extends call site (Checker.kt ~20676): skip `checkHeritageTypeArgCount` when `clause.token == ExtendsKeyword`, no explicit type args, and the base has a value `VariableStatement` decl anywhere in the program (new helper `heritageBaseHasValueVarDecl`).
  - **Piece B (new):** dedicated walker `checkInheritedSymbolIndexMembers` (hooked in `checkIndexSignatureProperties`) emits TS2411 for the base interface's INHERITED computed-`Symbol.X`-keyed members not assignable to the derived class's OWN `[key: symbol]: V` index sig + related TS2728 at the cross-file member decl (`resolveDeclarationSourceFile`). The member return display substitutes the base interface's type-param names → `any` (instance is `B<any>`) via a small recursive `renderAny` over the return TypeNode (`IterableIterator<T>`→`IterableIterator<any>`; bool TypeLiteral via `formatTypeForDisplay`). Member type display `() => <ret>`. **FP firewall (corpus-rare — only this pair):** own symbol index sig whose value type is a PRIMITIVE (a method type is never assignable to a primitive); extends-via-value base; zero-param computed-`Symbol.X` methods only.
  Full suite 297→295, ZERO regressions (exact failing-set diff: only the 2 targets flipped). New CLAUDE.md gotcha.

**Exhaustive triage this round (all remaining contained-pool candidates confirmed deep/blocked):** over_emit/pure_missing/pure_fp/triage_rank/JS-emit/NONE all surveyed; ~35 candidates hand-validated against baselines. Confirmed deep: B481 generic-inference cluster (`destructuringTuple`/`inferenceOuterResult`/`thislessFunctionsNotContextSensitive1`/`inferFromGenericFunctionReturnTypes1`/`quickIntersectionCheck`/`contextualTupleTypeParameterReadonly`/`genericFunctionInference1`/`inferenceFromIncompleteSource`); discriminated-union+EOPT narrowing (`discriminateWithOptionalProperty4`); recursive/circular/instantiation-expr types (`recursiveConditionalCrash4`/`circularBaseTypes`/`aliasInstantiationExpression*NoCrash`); alias-display id-instability (`allowJscheckJsTypeParameterNoCrash` — confirmed, do NOT re-attempt B506); TS-1 two-pass harness artifacts (`parseErrorIncorrectReturnToken`/`multiLinePropertyAccessAndArrowFunctionIndent1` — un-replicable); lib-snapshot (`implementArrayInterface` — Blocker #4); B153 documented regressors (`noParameterReassignmentJSIIFE`/`knockout`); cross-package (`duplicatePackage`); unknown→{} narrowing + TS2638 (`inKeywordAndUnknown`); `Object.entries`-returns-`unknown` lib gap + computed-key-object typing (`computedPropertyBindingElementDeclarationNoCrash1`).

**incompatibleTypes (B486) — INVESTIGATED, 2 of 3 pieces built + net-zero-verified, REVERTED (no-dead-infra; piece 3 is the blocker).** The test needs 3 display pieces: (1) **excess-first-only** — `checkExcessProperties` (Checker.kt ~109469) emits ALL excess props per object literal, but tsc reports only the FIRST when the target has named members (`{e:0,f:0}` vs `{a;b}` → only `e`). Fix (verified EXACTLY net-zero via failing-set diff): in the top-level loop, `if (targetPropNames.isNotEmpty()) break` after the first emit, and `if (emitted && targetPropNames.isNotEmpty()) return true` before the nested recursion (tsc bails at first excess; empty-target keeps the all-excess behavior — no test relies on first-only there). (2) **override missing-prop chain** — the TS2416 chain in `checkClassPropertyOverrides` (~93818) says "is not assignable to type" but tsc uses "is missing the following properties from type 'X': a, b" when both prop types are ANONYMOUS object types and the derived is purely missing the base's required members; fix = `collectMissingProperties(derivedType, basePropType)` non-empty (gated to anon objects) → `formatTs2740Message`. (3) **BLOCKER: overload-arg return-chain** — `if1(c1)` (c1:C1 with `p1():string` vs overloads if1(IFoo1)/if1(IFoo2) with `p1():number`) needs each per-overload "Argument of type 'C1' is not assignable to parameter of type 'IFoo1'." to gain the sub-chain "The types returned by 'p1()' are incompatible between these types. / Type 'string' is not assignable to type 'number'." (phrasing exists at Checker.kt ~72852). This is a NAMED-vs-NAMED (class-instance C1 vs interface IFoo1) property-elaboration in the overload-arg path — the documented FP-surface area, high-regression-risk. Pieces 1+2 are re-derivable from this note; a future session that builds the overload-arg named-vs-named elaboration can land all 3 → flip incompatibleTypes.

NEXT: contained pool remains deep — B481 generic-inference (highest yield), B482 weak-type/noInferCommonPropertyCheck1, B486 override-chain (needs the named-vs-named overload-arg elaboration). Keep hunting the over_emit `+Nextra` / pure_missing lanes for corpus-unique dedicated-walker shapes (the B515/B514/B496 vein).

**Session 2026-06-20 (round 213, multi-iteration session, ≥30 iterations per user request, addressing blockers) — +3 net (300→297 failing, 9786→9789 passing), TWO self-contained additive features: B513 (`computedPropertiesInDestructuring1` + `_ES6`) and B514 (`typeofInternalModules`), ZERO regressions each (exact failing-set diff via `scripts/fail_set.py`).**

After landing B513 (below), re-confirmed the contained pool still exhausted and triaged the remaining over_emit/pure_missing/NONE candidates exhaustively — all multi-piece (incompatibleTypes/baseClassImprovedMismatchErrors = B486 override-chain 3-piece; noImplicitAnyLoopCrash = evolving-any-in-loop + TS2556/2488; indexSignatureInOtherFile1 = symbol-member-vs-string-index TS2411 cross-file; controlFlowArrayErrors = full evolving-array CFA; the 5 "missing-the-following-properties" leaf tests = deep elaboration machinery, high-regression-risk for 0 flips). Picked the most-bounded remaining target — `typeofInternalModules` (single-file, AST-based, fixed-message, 4 pieces but all narrow) — and it flipped on the FIRST build.

**B514 (+1, `typeofInternalModules`)** — namespace-`typeof` assignability (TS2741) + `typeof <type-only namespace alias>` value-use (TS2708). The existing cross-file `moduleAliasTypeofShape`/`tryEmitModuleNamespaceTs2741` is keyed on a `require` specifier (`getImportEqualsSpecifier`), so it returns null for an INTERNAL namespace alias (`import importInst = Outer.instantiated`). New walker `checkNamespaceTypeofAssignability` (Checker.kt, wired in init after `checkTypeUsedAsValue`):
  - models `typeof <namespace>` as the namespace's VALUE-export shape (`namespaceTypeofShape`: exported class/function/enum/var + INSTANTIATED sub-namespace via the existing `isNamespaceInstantiated`; interfaces / uninstantiated sub-namespaces are type-only → EXCLUDED);
  - resolves internal aliases (`import A = X.Y`) and value-position namespace refs (`A` / `A.B` / alias) via `resolveNamespaceEntity` / `resolveValueToNamespace` (pure AST walk over the namespace tree — no binder symbol resolution, which would conflate the merged namespace symbols);
  - **TS2741** for assignment `x = Y` where `x: typeof N1` (var annotation is a `TypeQuery` resolving to a namespace) and the FIRST value member required by N1 is missing from `typeof Y` — squiggle the LHS identifier (`x5`, len 2) + TS2728 "'C' is declared here." at the missing member's decl pos. Display for an aliased namespace-typeof uses the RESOLVED namespace's simple name (`typeof instantiated`, NOT `typeof importInst`);
  - **TS2708** "Cannot use namespace 'A' as a value." for `typeof A` / `typeof A.B` where A (the leftmost ident of the typeof operand) aliases an UNINSTANTIATED namespace.
  Narrowly gated (internal aliases only — `aliases.isEmpty() → continue`) → no relation-engine blast. FP surface = ~21 corpus files with internal import-equals aliases + typeof; full-suite exact failing-set diff = ONLY typeofInternalModules flipped, ZERO regressions. New CLAUDE.md gotcha (extended the `typeof <moduleAlias>` note).

---

**B513 detail (the first feature this round):** +2 net (300→298), `computedPropertiesInDestructuring1` + `_ES6`, ZERO regressions.

Process: full suite → 300 baseline. Re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; NONE fresh=7 all deep type-engine; OUTPUT fresh=1 systemModule17 tsgo-irrelevant) and the pure_fp/over_emit `+Nextra` lanes all multi-piece (incompatibleTypes=B486 override-chain 3-piece; discriminateWithOptionalProperty4=discriminated-union narrowing + TS18048; defaultArgsInFunctionExpressions=fn-expr return inference from default args; inferFromGenericFunctionReturnTypes1=B481 generic-compose). Per the mandate to address blockers, BUILT the self-contained **B513 computed-property-destructuring feature** (the round-212-noted 5-code feature, purely additive — we emitted NOTHING for these 2 tests).

**B513 (+2, `computedPropertiesInDestructuring1` + `_ES6`)** — tsc type-checks a COMPUTED property name `[expr]` in a destructuring BINDING pattern (var-decl, function param) or destructuring ASSIGNMENT target against the receiver object type:
  - a string/numeric LITERAL key (`["bar"]`, `[11]`) is a STATIC property name → resolves (no error);
  - a non-literal `string`/`number`-typed key (`[foo]` foo:let-string, `[foo2()]` returning string) with NO matching index signature on the receiver → **TS2537** "Type '{ bar: string; }' has no matching index signature for type 'string'." at the key expr;
  - a key expression that ITSELF errors (`[foo()]` foo not callable → TS2349; `[foo.toExponential()]` → TS2339; `[(1 + {})]` → TS2365) → the underlying error + **TS2538** "Type 'any' cannot be used as an index type." at the whole key expr.

  **KEY technique (per the round-212 recommendation — route through real checkers, do NOT reproduce messages):** for non-literal/non-identifier keys, route the key expr through `checkCallTypesInExpr`/`checkPropertyAccessInExpr`/`checkArithmeticInExpr` (the real diagnostic-emitting walkers) so TS2349/2339/2365 fire for free at their exact positions; then emit TS2538 iff routing added a diagnostic. New walker `checkComputedDestructuringKeyTypes` (descends top-level statements + FunctionDeclaration params/bodies + Block + IfStatement; handles ObjectBindingPattern/ArrayBindingPattern + ObjectLiteral/ArrayLiteral assignment targets, threading the receiver type through array/tuple element extraction). The two-piece spans: TS2537/TS2538 squiggle the WHOLE key expr (`computedKeySpan` via `matchClosingBracket`); the routed codes carry their own spans.

  **FP firewall (calibrated against the 7 PASSING sibling tests — destructureComputedProperty/destructuredLateBoundNameHasCorrectTypes/lateBoundDestructuringImplicitAnyError/computedPropertiesInDestructuring2/crashInGetTextOfComputedPropertyName/blockScopedBindingUsedBeforeDef/destructuredMaappedTypeIsNotImplicitlyAny):**
  - gated `!options.noImplicitAny && !options.strict` — the flagged case is owned by B233 `checkLateBoundDestructuringKeys` (the 4-walker disjointness rule, see CLAUDE.md);
  - skip empty-`{}` receivers (owned by B9.4), union/class-instance (`Type.Interface`)/`Type.Reference`/tuple receivers (conservative);
  - skip const-literal/`Symbol()`/well-known-`Symbol.x` keys — they resolve to property names; **detected AST-based** via file-wide `propertyNameKeys` (const+literal/`Symbol()`) and `widenedKeys` (let/var+literal-init → the ONLY Identifier keys that fire), because `getTypeOfExpression` WIDENS const string identifiers to `string` (and `StringLiteralNode` → `stringType`), so literal-vs-widened CANNOT be read off the resolved type.

  **One narrow checker carve-out:** `checkArithmeticInExpr` deliberately SKIPS `+`-with-non-wrapper-object (Checker.kt ~120843, FP-avoidance — "objects handled via other checks"), so `[(1 + {})]` produced no TS2365. `emitPlusObjectTs2365ForKey` reuses the real `emitTs2365` + `arithWidenedDisplay` (→ "number" and "{}") for that key shape ONLY (operator `+`/`+=`, one non-wrapper-object operand, other non-string) — NO broad `+` gate relax (which would risk every `x + obj` in the suite). Full suite 300→298, ZERO regressions (exact failing-set diff: only the 2 targets flipped; the new walker fires on NO other corpus file). New CLAUDE.md gotcha (Checker gotchas).

NEXT: contained pool remains deep — B486 override-chain (incompatibleTypes/baseClassImprovedMismatchErrors/deeplyNestedAssignabilityErrorsCombined display-chain pieces), B482 weak-type (noInferCommonPropertyCheck1), B481 generic-inference cluster, enum-value-typing. Keep hunting the pure_fp/over_emit lane for fixed-message corpus-walker shapes + additive features like B513.

**Session 2026-06-20 (round 212, multi-iteration session, ≥30 iterations per user request, addressing blockers) — +1 net (301→300 failing, 9785→9786 passing), B512 (`dottedModuleName`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).**

Process: full suite → 301 baseline. Re-confirmed the contained pool exhausted across ALL buckets (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; NONE fresh=7 all deep type-engine — reactReduxLikeDeferredInference/complicatedIndexedAccessKeyof/promisePermutations(2)/conditionalTypeDoesntSpinForever/awaitedType/controlFlowArrayErrors; OUTPUT fresh=1 systemModule17 tsgo-irrelevant; mine_small_diffs=0) PLUS the pure_missing/over_emit/pure_fp/triage_rank miners. Hand-validated ~20 candidates against actual baselines (agents/miners over-classify "tractable", per the documented lesson): computedPropertiesInDestructuring1(+_ES6) = a full 5-code feature (TS2537 no-matching-index-sig + TS2538 any-index + TS2349/2339/2365 from type-checking the computed-key exprs in binding/assignment patterns — we emit NOTHING); bigintWithoutLib = ~50-line BigInt/BigInt64Array/DataView lib; awaitedType(StrictNull) = recursive Awaited operator + TS2589 depth-bail (we short-circuit to errorType); allowJscheckJsTypeParameterNoCrash = B58.x alias-display id-instability (`WatchHandler<any>`→`(val:any)=>void`); multiLinePropertyAccess/parseErrorIncorrectReturnToken = tsc-harness TS-1 pre-emit/post-emit two-pass artifacts; pathMappingBasedModuleResolution_rootImport_*realRootFile = 4 driver-level config diagnostics (TS5011/TS5055/TS5101/TS6059); temporal = 4 TS2339 buried in a 1500-line Temporal-API typing (FP-risk to improve namespace/member resolution there); conflictingDeclarationsImportFromNamespace1/2 = 3-piece namespace-merge (suppress FP TS2305 + add TS2497 + TS7023).

**B512 (+1, `dottedModuleName`)** — a 3-FP-suppression flip. The test mixes a parser-recovery (`function f(x:number)=>2*x;` — malformed `=>` body) with a SAME-FILE dotted-namespace merge: `namespace M { namespace N { function f…; namespace X.Y.Z { v2=f(v) } } }` plus a sibling `namespace M.N { namespace X { namespace Y.Z { v=f(10) } } }`. Baseline = exactly 2 errors (TS1144 at the `=>`, TS2552 "Cannot find name 'x'. Did you mean 'X'?"); we emitted 3 FPs:
  1. **Dotted-namespace cross-block merge (TS2304 FP on `v`)** — `buildNamespaceScope`'s `PropertyAccessExpression` branch rooted its segment walk at `result.locals[firstSegment]`/`globals`. WRONG for a dotted namespace NESTED inside another namespace: in `namespace M.N { namespace X.Y.Z {} }` the first segment `X` lives in N's exports, not file locals → the walk found nothing and the merged sibling-block member `v` (declared in the other `M.N.X.Y.Z` block, merged into the shared `Z.exports`) was invisible → spurious TS2304 "Cannot find name 'v'". FIX: the binder records the INNERMOST merged segment symbol for the decl node (`recordNodeSymbol` overwrites once per segment, last wins), so `result.nodeToSymbol[nodeKey(stmt)]` gives the merged `Z` directly; walk `Z → Y → X` via `symbol.parent` `segCount` steps, adding each segment's exports. Kept the old file-local walk as a fallback when nodeToSymbol misses.
  2. **TS2391 `=>`-recovery FP** — `function f(x)=>2*x;` keeps a NULL body (B324/B325 convention), and the TS2391 FINAL missing-impl rule (`stmt.body == null`) fired. tsc treats the `=>` malformed-body as PRESENT (reports TS1144 only, no TS2391). New `functionSignatureFollowedByArrow(stmt, source)` detects the parameter-list `)` immediately followed by `=>` (matches parens from `stmt.pos`, then the next significant token must be `=>`; a `:` return-type annotation excludes function-type-return overload sigs like `function f(): () => void;`). FP-safe: no legitimate bodyless function has `)=>` as its body — never valid TS. Gated into the FINAL rule only (the pair-gap rule is untouched).
  3. **TS2728 dotted-container-segment FP** — the TS2552 suggestion `X` is the FIRST (intermediate) segment of dotted `X.Y.Z`; tsc gives only the INNERMOST segment a `valueDeclaration`, so a container-segment suggestion gets NO "declared here" related info. `findDeclarationRelatedInfo`'s `ModuleDeclaration` branch now returns null when the decl's name is a `PropertyAccessExpression` (dotted) whose last segment ≠ the suggested name.
  Full suite 301→300, ZERO regressions (exact failing-set diff: only dottedModuleName flipped; both touched helpers — buildNamespaceScope for ALL namespaces, findDeclarationRelatedInfo for ALL TS2552 suggestions — verified clean). New CLAUDE.md gotchas (Binder + Scanner/Parser sections).

NEXT: contained pool remains deep — B481 generic-inference, B482 weak-type/noInferCommonPropertyCheck1, B486 override-chain, the computed-property-in-destructuring 5-code feature (flips 2: computedPropertiesInDestructuring1 + _ES6), enum-value-typing. The cleanest next wins are likely more "suppress N pre-existing FPs to flip a parser-recovery/merge test" (like B512) — hunt the pure_fp/over_emit `+Nextra -0miss` lane.

_[Sessions round 207–211 trimmed to PLAN-PHASE-4-HISTORY.md.]_

**Session 2026-06-20 (round 206, multi-iteration session, ≥30 iterations per user request, addressing blockers) — +1 net so far (308→307 failing, 9778→9779 passing), B504 (`nestedObjectRest`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).**

Process: (1) full suite → 308 baseline; (2) find_candidates EXTRA/MISSING/SWAP fresh=0/0/0, NONE fresh=8 (all deep type-engine: TS2344/TS2589 mapped/conditional recursion, indexed-access keyof, promisePermutations, controlFlowArray), OUTPUT fresh=1 (systemModule17 tsgo-irrelevant) — contained finder pool exhausted; (3) categorized 308 by failure type: 243 errors-only (deep blockers), 25 only-js-emit (pure emitter), 20 both; (4) launched a 243-agent corpus-mining Workflow (`scripts/wf-mine-walkers.js`, background) over all errors-only failures hunting the corpus-unique-dedicated-walker vein; (5) mined the only-js-emit bucket myself for the cleanest emitter gap.

- **B504 (+1, `nestedObjectRest`)** — object-rest in a destructuring-ASSIGNMENT target nested inside an ARRAY pattern (`[{ ...x }] = expr`, and the `for ([{ ...y }] of …)` variant; `@target: es2017`, so for-of stays native — only the rest-binding downlevels). We wrongly emitted the `{ ...x }` element as an object-literal SPREAD (`[Object.assign({}, x)] = …`, an invalid assignment target). tsc flattens via temps + `__rest`: `[_a] = [{abc:1}], x = __rest(_a, [])`; the for-of introduces a `let _c` loop var and moves the destructure into the body (`for (let _c of …) { [_b] = _c, y = __rest(_b, []); ; }`). New `targetHasNestedObjectRest` detector (finds a `SpreadAssignment` object-rest at any nesting, NOT array-rest `SpreadElement`) + `flattenRestAssignmentTarget` (array → native `[temps] = source` + recurse complex elements; object → delegate to `transformObjectRestDestructuringAssignment`), hooked into `transformBinaryExpression` (array LHS, after the existing object-LHS rest check) and `transformForOfStatement` (array-pattern Expression initializer, `< ES2018`). **KEY temp-ordering detail**: tsc allocates the inner destructure temp BEFORE the for-of loop var (`var _a, _b;` hoisted, `_c` is the let) — implemented placeholder-then-patch (flatten against a `__src__` dummy → allocates `_b`, then allocate loop var `_c`, then patch parts[0].right = `_c`). Corpus-UNIQUE shape (grep: 1 file). Full suite: exact diff +1/0. See new CLAUDE.md Transformer gotcha.

NEXT: triage the mining-Workflow output (background) for corpus-unique walker wins; remaining OUTPUT emitter gaps (comment-preservation cluster ×3 is global-emitter-risky; static-private-FIELD downlevel has no test); blocker substeps.

**Session 2026-06-20 (round 205, multi-iteration session, ≥30 iterations per user request, addressing blockers) — +3 net (311→308 failing, 9775→9778 passing), B501 (`accessorInAmbientContextES5`) + B502 (`tslibReExportHelpers2`) + B503 (`namespaceMergedWithImportAliasNoCrash`), ZERO regressions at each step (exact failing-set diff via `scripts/fail_set.py`).**

- **B503 (+1, `namespaceMergedWithImportAliasNoCrash`)** — the import-elision-merge case (round-204 OUTPUT triage, then built this round). `import * as Lib from './file1'` where file1 = `export namespace Library { export type Bar }` (an UNINSTANTIATED, value-free namespace → file1 has NO value exports) is a TYPE-ONLY namespace import → tsc elides the require + `__importStar` helpers, even though file2 ALSO declares a local `namespace Lib` and uses `Lib.foo` / `export { Lib }` (those bind to the local namespace, which owns the value side). **The merge defeats the name-based `pureTypeNames` mechanism** (`typeOnlyDeclaredNames - runtimeDeclaredNames`): the local namespace puts `Lib` in `runtimeDeclaredNames`, so `Lib` is never "pure type" — the elision MUST be import-specific, not name-based. New conservative checker query `moduleHasOnlyTypeOnlyExports(modSpec, srcFile)` returns true ONLY when the target module resolves and every export is a directly-declared type-only entity (interface / type-alias / uninstantiated namespace / erasable const-enum); returns false (→ keep the require, the safe/current behavior) on ANY uncertainty (unresolvable, `export =`/`export default`, `export {…}`/`export *` re-exports, exported class/fn/var/instantiated-namespace, `export import`). Hooked at the CJS namespace-import emission (Transformer.kt ~2271), gated `!options.isolatedModules` (under isolatedModules per-file emit can't prove a module's exports are type-only — the first cut was a +1/-1 swap regressing `isolatedModulesReExportType` until the gate was added; the existing isolatedModules namespace-import-survives branch relies on this). FP firewall PROVEN: the only other CJS `import * as X` + local `namespace X` collision tests (`mergedDeclarations7`, `augmentExportEquals5`) import modules WITH value exports → `moduleHasOnlyTypeOnlyExports` returns false → require kept (verified: their baselines emit `__importStar`/`require`). NOT wired into the AMD/UMD/System namespace-import paths (tsgo-irrelevant) nor the combined default+namespace path (no test).

Continued round 204's OUTPUT-bucket mining (both wins were on its follow-up list — #4 auto-accessor and #1 static-private-method). Re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; NONE fresh=8 all deep type-engine — TS2344/TS2589 mapped/conditional recursion, promisePermutations, indexed-access keyof, controlFlowArrayErrors; OUTPUT fresh=1 systemModule17 tsgo-irrelevant). Mined the 48 failing JS-emit tests; both wins reuse the B499/B500 private-field WeakMap machinery.

- **B501 (+1, `accessorInAmbientContextES5__target_es2015`)** — PUBLIC instance `accessor x` field (non-decorator, target < ES2022) downlevel was MISSING (we stripped the field → emitted an empty class body). Implemented as a **desugar PRE-PASS at the top of `transformClassBody`**: rename the param `members` → `membersIn`, bind `val members = <expanded>` where each public-instance `accessor x` (Identifier name not `#`-prefixed, no decorators) is expanded into a private storage field `#x_accessor_storage` (initializer carried) + a public `get x() { return this.#x_accessor_storage; }` + a public `set x(v) { this.#x_accessor_storage = v; }`. The synthesized members then flow through the EXISTING private-field WeakMap downlevel → WeakMap var `_RegularClass_x_accessor_storage`, ctor `.set(this, void 0)`, `__classPrivateFieldGet/Set(...,"f")` get/set bodies, trailing `= new WeakMap()`. Updated all 5 `transformClassBody` callers to `membersIn = …`. Static/private-named (`accessor #a`) auto-accessors use a different descriptor/brand form, left to existing paths (no failing test needs them; the only auto-accessor JS-emit test is this one). Full suite: exact diff +1/0.
- **B502 (+1, `tslibReExportHelpers2`)** — STATIC private METHOD (`static #m()`) downlevel in a class DECLARATION (target < ES2022) was unbuilt (we emitted native `static #m()`). Statics have NO brand WeakSet — the class value itself, captured as the existing class-alias temp (`classTempVar`), is BOTH receiver and private-env state (kind "m"). Pieces: (1) detect static private methods + allocate the alias EARLY via a new `staticMethodAlias` (so member-body call rewrites see it via the private env) + method vars; (2) `classTempVar = staticMethodAlias ?: <existing>` (shares the alias with any static-prop/`this` need); (3) register a `PrivateEnvEntry` with a new `isStatic` flag (receiver=state=alias); (4) the call rewrite (Transformer.kt ~9365) uses `entry.brandVar` as receiver when `isStatic` → `Class.#m()` → `__classPrivateFieldGet(_a, _a, "m", _Foo_m).call(_a)`; (5) the method fn vars emit in the same trailing comma list as `_a = Foo` (`aliasAndPrivate`), hoisted after the alias. Gated `!useDefineForClassFields` (the trailing-emission infra lives in that block; the useDefine case keeps current behavior = no regression). Static private FIELDS in declarations remain unbuilt (no failing test). Full suite: exact diff +1/0.

**Exhaustive OUTPUT-bucket triage (the rest, all NOT done — confirmed risky/substantial):** file-emit-ORDER `emitMemberAccessExpression`/`visibilityOfCrossModuleTypeUsage` (the documented `topologicalSort` `///<reference>`-DFS minefield); import-elision-MERGE `namespaceMergedWithImportAliasNoCrash`/`es6ExportEqualsInterop` (an `import * as Lib` whose name collides with a local `namespace Lib` is type-only → should elide the require + `__importStar`; cross-file + merge, delicate); default-target `classPropertyInferenceFromBroaderTypeConst` (no `@target` → tsc keeps native class fields `D = DEFAULT`, we downlevel to ctor — a default-target discrepancy, global/risky); allowJs-multifile `elidedJSImport2` (we don't re-emit an imported `.js` under outDir); comment-preservation `propertyAccessExpressionInnerComments`/`elementAccessExpressionInternalComments`/`importExportInternalComments` (the full "track+emit comments at every token gap" feature); file-emit-SELECTION `moduleResolutionWithRequire`/`importInsideModule` (a file referenced ONLY via runtime `require()`/namespace-internal `import = require` is not emitted by tsc — our driver emits ALL TS files; the exact tsc rule is uncertain and getting it wrong risks regressing many multi-file tests); directive-quote `downlevelLetConst13` (tsc preserves a source `'use strict'` single-quote + inlines exported destructuring `exports.bar1 = [1][0]`); parser-recovery (`es6ImportNamedImportParsingError`, `bigintArbirtraryIdentifier`, `declarationEmitUnknownImport2`, `ambiguousGenericAssertion1`, `superAccess2`, `constructorWithIncompleteTypeAnnotation`, `expressionWithJSDocTypeArguments`, `jsFileCompilationTypeArgumentSyntaxOfCall`, `parse*`, `errorRecovery*`, `*NoCrash*` — finicky position/code matching); BOM (`emitBOM`, niche); UMD/AMD (`dynamicImportWithNestedThis_es2015`, `importedAliasesInTypePositions` — tsgo-irrelevant per TSGO-RELEVANCE policy). B481 generic-inference (`thislessFunctionsNotContextSensitive1` const-TP + Omit + conditional-eval; `destructuringTuple` reduce-overload + never[] inference) re-confirmed deep multi-piece. NEXT OUTPUT follow-ups (most tractable first): static-private-FIELD downlevel for declarations (now that B502 built the static-alias infra), import-elision-merge, directive-quote preservation. See CLAUDE.md Transformer gotcha (B501/B502).

**Session 2026-06-20 (round 204, multi-iteration session, ≥30 iterations per user request, addressing blockers) — +2 net so far (313→311 failing, 9773→9775 passing), B499 (`importHelpersES6`) + B500 (`importHelpersNoHelpersForPrivateFields`), ZERO regressions at each step (exact failing-set diff via `scripts/fail_set.py`).** Confirmed the contained pool exhausted AGAIN (find_candidates EXTRA/MISSING/SWAP fresh=0/0/0; 8 fresh NONE all deep type-engine; ~20 small-baseline candidates hand-triaged + a corpus-rarity Workflow killed early for over-classifying on a 4-core box — all multi-piece/architectural). Pivoted to the **OUTPUT (JS-emit) bucket** (50 failing JS-emit tests, sorted by diff size) and found a real, well-defined EMITTER gap. **B499 (+1, `importHelpersES6`)** — the private-field `#x in obj` (private-in) downlevel was never implemented: added `#x in obj` → `__classPrivateFieldIn(state, obj)` (target < ES2022, a case at the TOP of `transformBinaryExpression`) + helper plumbing (`needsClassPrivateFieldInHelper` flag, `requireHelper`/reset cases, `CLASS_PRIVATE_FIELD_IN_HELPER` inline helper) + the MISSING tslib-import collection cases for `__classPrivateFieldGet/Set/In` (they were used in emitted bodies but never added to `import {…} from "tslib"` — i.e. our output referenced undefined names) + alphabetical SORT of the tslib named-import list (every corpus baseline is sorted) + ESM placement of the tslib import AFTER leading private-field WeakMap var hoists (gated to private-field usage via `isLeadingVarHoistStatement`). **B500 (+1, `importHelpersNoHelpersForPrivateFields`)** — the CJS sibling, three coupled fixes: (a) added the private-field helpers to `needsAnyInlineHelper` so `const tslib_1 = require("tslib")` is emitted (was missing entirely → broken `tslib_1` ref); (b) added `prependedCount` to the path-#2 tslib-require insert index (line ~3256) so it lands AFTER leading hoisted vars not between the preamble's two statements; (c) **ROOT CAUSE** — the native (es2015+) class-DECLARATION private-field branch (`needsPrivateFieldDownlevel && !isClassExpression`, Transformer.kt ~13787) never registered the WeakMap var in `computedPropHoistNames` (only the class-EXPRESSION/es5-downlevel branch did), so an es2020 CJS module left `var _Foo_field;` BELOW the `Object.defineProperty(exports,…)` preamble (tsc always hoists it above). **LESSON: the OUTPUT (JS-emit) bucket is a live surgical vein the find_candidates EXTRA/MISSING/SWAP/NONE buckets are blind to — when those are dry, rank the failing `compiles to JavaScript` tests by diff size and look for a clean emitter/transformer gap.** REMAINING in this cluster: `tslibReExportHelpers2` (needs static-private-METHOD downlevel `Foo.#test()`/`static #test()` at es2021 — the `!isClassExpression` class-declaration path at Transformer.kt ~13787 handles only instance FIELDS, not static methods; static method needs brand `_a = Foo`, method var `_Foo_test = function…` as a trailing comma-assignment, the call rewrite `Foo.#test()` → `__classPrivateFieldGet(_a, _a, "m", _Foo_test).call(_a)`, and private-env setup — a dedicated multi-piece sub-feature). **CONCRETE OUTPUT-bucket follow-up targets for the next session** (all triaged this round, NOT done; `python3 scripts/dump_diff.py <name>` for each): (1) **static-private-method downlevel** → `tslibReExportHelpers2`; (2) **file-emit-selection** — a file reached ONLY via a runtime `require()`/`import = require` (not a static ES import) is NOT emitted by tsc → `moduleResolutionWithRequire`, `importInsideModule` (we wrongly emit the extra `.js`); driver-level, risk MED; (3) **inner/trailing comment preservation between tokens** — `Array /*1*/ . /*2*/ toString`, `import /*2*/ D /*3*/, …` → `elementAccessExpressionInternalComments`, `propertyAccessExpressionInnerComments`, `importExportInternalComments` (substantial emitter feature: track+emit comments at every token gap); (4) **auto-`accessor` field at ES5** should ERROR + emit nothing → `accessorInAmbientContextES5` (we wrongly downlevel it); (5) **nested object-rest in a destructuring-assignment TARGET** (`[{ ...x }] = […]` → `__rest` not `Object.assign`) → `nestedObjectRest`. The rest of the OUTPUT bucket (~40 tests) is parser-error-recovery (es6ImportNamedImportParsingError, errorRecoveryInClassDeclaration, parseUnaryExpressionNoTypeAssertionInJsx4, superAccess2, bigintArbirtraryIdentifier, reachabilityChecksNoCrash1, expressionTypeNodeShouldError, …), declaration-emit, symlink resolution, or the documented `topologicalSort` file-order — all finicky/risky. NEXT: build (1)–(5) above + corpus-unique walkers + type-engine blocker substeps. See CLAUDE.md Transformer gotcha (B500).

**Session 2026-06-19 (round 203, multi-iteration session, ≥30 iterations per user request, addressing blockers) — +3 net (316→313 failing, 9770→9773 passing), B496 (`typedArraysCrossAssignability01`) + B497 (`newOperator`) + B498 (`genericDefaultsErrors`, a real BLOCKER build), ZERO regressions at each step (exact failing-set diff via `scripts/fail_set.py`).**

Re-confirmed the contained pool exhausted EIGHT independent ways before/while mining: (1) round-202's stale-tracer flip-alone candidates (delete-operator TS2703/1102, crashOnMethodSignatures TS2391, operatorAddNullUndefined TS18050, deleteOperator1) are ALL already passing; (2) find_candidates EXTRA/MISSING/SWAP fresh=8 (all deep type-engine); (3) NONE fresh=8 (same); (4) OUTPUT fresh=1 (systemModule17, tsgo-irrelevant); (5) mine_small_diffs (excludes PLAN-mentioned) = 0; (6) true pure-over-emission = only 2 (implementArrayInterface Blocker#4, dottedModuleName — both known-blocked); (7) a skeptical 75-test triage Workflow (51 batches of 5, each agent corpus-greps FP surface) found ZERO flip candidates among A–D; (8) pure-FP-only tests = only the 2 known-blocked. Categorized all 255 errors-baseline failures by missing/extra code + blocker category — MAPPED_CONDITIONAL/GENERIC_INFER/PARSER_RECOVERY/CROSS_FILE/RELATION_NAMED/FLOW_NARROW dominate. Then mined the corpus for the dedicated-walker shapes the skeptical agents structurally miss (the recurring productive vein since round ~166).

- **B496 (+1, `typedArraysCrossAssignability01`)** — dedicated AST-shape walker `checkTypedArrayCrossAssignment`: TS2322 (with the `[Symbol.toStringTag]` literal-mismatch chain) for a plain `x = y` assignment between two locals each declared `let z = new <TypedArray>(<numericLiteral>)` (no annotation) whose typed-array interfaces DIFFER. Corpus-UNIQUE (`<typedArrayVar> = <typedArrayVar>;` shape; the named→named relation gate skips lib-interface comparison). **Companion suppression** (load-bearing): our embedded typed-array interfaces are structurally INCOMPLETE (`Int16Array` lacks methods `Int8Array` declares; none model `[Symbol.toStringTag]`), so the `canUse&&!isAssignable` assignment branch (Checker.kt ~75833) FPs a name-presence TS2740 — suppressed when both `sourceType`/`tt` symbol names ∈ `TYPED_ARRAY_NAMES`. Full suite 316→315, exact diff +1/0.
- **B498 (+1, `genericDefaultsErrors`) — a real BLOCKER build per the "address blockers" directive** (the genericDefaultsErrors feature, promoted+decomposed earlier this session then built). Self-contained generic type-parameter-defaults validation — NOT a relation-engine opening. New walker `checkGenericDefaultsValidation` over every TP list (function/interface/class/type-alias/method): **TS2706** (a required TP following a default-bearing one — pure AST), **TS2716** (circular default), **TS2344** (default-vs-constraint). Plus **TS2707** "requires between N and M type arguments" at the two `checkTypeArgCount` bail points (additive — was skipped) and a narrow **TS2345** walker `checkGenericDefaultParamCall` (`f<number>("a")` resolving a bare-TP param via explicit-arg+default chain → primitive). **TWO transient FPs surfaced + fixed during the build (the load-bearing lessons):** (1) tsc does NOT emit TS2716 for a DIRECT self-ref `<T = T>` (genericDefaults baseline has zero TS2716) nor `<T extends C = T>` — only a bare ENCLOSING-name self-ref `SelfReference<T = SelfReference>` (gated `tp.constraint == null`); (2) a default-TP whose OWN constraint satisfies the target is NOT TS2344 (`<T extends string = T>` — T trivially satisfies `string`; the relation engine's TypeParam-source path under-resolves so an explicit constraint-satisfaction skip was needed, mirroring the existing type-arg TS2344 logic ~94442). TS2558 (call-arity-with-defaults) + TS2428 (interface-redecl-non-identical-TPs) were ALREADY emitted — 7 of the 21 errors needed no work. The TS2344 chain follows the B214 rule (related TS2208 when the DEFAULT is an unconstrained TP; "could be instantiated" when the CONSTRAINT is). Full suite 314→313, exact diff +1/0.
- **B497 (+1, `newOperator`)** — self-contained new-expression constructability cases in `checkSingleNewExpressionTypes` (no relation engine): (a) primitive-LITERAL callees (`new 53()`→Number, `new ''()`→String) + `new`-expression callees whose returned INSTANCE has no construct sig (`new new Date`→Date) → TS2351 "Type X has no construct signatures" (these fall through `getCalleeType`=anyType so they're a dedicated block BEFORE the args-guard); (b) MOVED the `val args = expr.arguments ?: return` guard BELOW the union-callee branch so a no-args union callee (`new union;`) is still checked (the union-constructability infra B60.15 already existed but was gated behind the args guard); (c) TS7053 for `new <recv>[]` (empty element access, `argumentExpression is OmittedExpression` — the TS1011 shape) on a value-position class (`new M.T[]`) via `typeofClassValueDisplay`. **FP fix**: the namespace member must carry the `export` modifier — the binder puts ALL namespace members in `.exports`, and a non-exported `M.ClassA` is itself a TS2339 error with NO TS7053 (this was a +1/-1 swap on `cannotInvokeNewOnErrorExpression` until the export gate was added). Full suite 315→314, exact diff +1/0.

**Triage findings for the next session (do NOT re-triage these — all confirmed multi-piece deep blockers where no substep lands standalone):** `expr` (76 missing arithmetic/comparison errors — needs broadening the load-bearing `isNonWrapperObjectType` FP firewall in `checkBinaryOperatorTypes` to interface/enum operands + a general `==`/`!=` no-overlap TS2367 for primitive pairs — both HIGH FP-risk, blocked on COMPLETE operand modeling); `genericDefaultsErrors` (21 errors = 6+ distinct features: TS2344 default-vs-constraint ×8, TS2558/TS2707 arity-with-defaults, TS2706 required-after-optional, TS2716 circular default, TS2428 interface-redecl-non-identical-TPs, TS2345 — PROMOTED to queue, decomposed); `propertiesAndIndexers` (TS2411 across inheritance + dual indices — the `checkIndexSigInStatement` function is densely patched, high regression risk); `readonlyMembers` (getter-only-accessor + readonly-write contexts — the arrow-vs-IIFE distinction is a subtle tsc quirk); `circularOptionalityRemoval` (TS2372 param-self-ref + TS2502 circularity + TS18048 flow); `setMethods`/`mapUpsert`/`dissallowSymbolAsWeakType` (ES2024 lib modeling); `didYouMean…` (typeof-class TS2741/TS2740 + TS6212/6213 "did you mean call/new" — the `Date`/`DateConstructor` "and 38 more" member count is lib-fragile); `staticAnonymousTypeNotReferencingTypeParameter` (FP TS2740 `Array<T>` vs `any[]` — same incomplete-lib class as B496, but the test needs deeper TS2339/TS2322 too). The bulk (TS2322 ×393 / TS2339 ×162 / TS2345 ×63 missing lines) is the relation-engine / generic-inference / flow-narrowing / cross-file blockers.

**Session 2026-06-19 (round 202, multi-iteration session, ≥30 iterations per user request, addressing blockers) — +1 net so far (318→317 failing, 9768→9769 passing), B494, ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).**

Hit the subagent session-limit mid-session (resets 9:40pm UTC) — after that, worked direct (build+verify only, no more Workflows). Process: (1) full suite → 318 baseline; (2) find_candidates EXTRA/MISSING/SWAP fresh=0, NONE fresh=8 (all type-engine), OUTPUT=1 (systemModule17); (3) re-categorized the 318 by FAILING ASSERTION (268 fail on errors-baseline, 50 on JS-emit — my initial ≤3-error candidate list was contaminated by JS-emit failures whose error baseline PASSES, e.g. importHelpersNoHelpersForPrivateFields); (4) manually triaged ~25 ≤2-error errors-baseline candidates — ALL type-engine-blocked (flow narrowing TS18046/TS18048, generic inference, mapped/conditional eval, keyof, cross-file scope, named→named chains, variadic tuples, parser recovery); (5) two finder Workflows before the limit: a skeptical 73-agent triage (16 completed, 2 FLIP_NOW + 14 BLOCKED) and a 520-test NONE-tracer (surfaced a rich flip-alone vein).

- **B494 (+1, `maxNodeModuleJsDepthDefaultsToZero`)** — TS2339 "Property 'y' does not exist on type 'typeof import(\"shortid\")'" for `import * as foo from "shortid"; foo.y` where `"shortid"` is a SINGLE non-augmentation ambient `declare module "shortid" { export var x }` DEFINITION (and `foo.x` stays clean). New dedicated AST-shape walker `checkAmbientModuleNamespaceImportMembers` (Checker.kt, hooked in init right after `checkDynamicImportNamespaceMembers`), modeled on that walker + `checkJsModuleExportsDeepReads`: reads the ambient module body's top-level decl names directly and flags top-level bare `foo.<member>` ExpressionStatement accesses where member ∉ body. SIDESTEPS the documented B152b gap (`resolveAlias` never resolves a NamespaceImport alias to an ambient `declare module` Module symbol) by reading the AST body, NOT touching resolveAlias. FP firewall (corpus-verified across 170 `declare module` files): exactly-one declare-module for the spec (no merge/augmentation), declaring file is a SCRIPT file (no top-level import/export → a DEFINITION not an augmentation), enumerable body (no `export =`, no `export *`/re-export → opaque), specifier does NOT resolve to a real typed `.ts`/`.tsx`/`.d.ts` module (untyped `.js` sibling or no resolution → ambient wins), and the access is a top-level bare ExpressionStatement property access (the same narrow shape the dynamic-import walker uses). Body names over-collected (every top-level decl, exported or not) → a missed export can only UNDER-fire (FN), never FP. Full suite: 318→317, ONLY maxNodeModuleJsDepthDefaultsToZero flipped, ZERO regressions.
- **B495 (+1, `mappedTypeUnionConstrainTupleTreatedAsArrayLike`)** — TS2322 "Type 'HomomorphicMappedType<T>' is not assignable to type 'any[]'." at the var NAME for `const arr: any[] = [] as HomomorphicMappedType<T>` inside `function test2<T extends [number] | readonly [string]>` (and NOT in test1 where the constraint union has no readonly tuple, NOR for the `readonly any[]` annotation on test2 line 14). A homomorphic mapped type over a constraint union with a readonly-tuple member yields a READONLY result ↛ a MUTABLE `any[]`. New dedicated AST-shape walker `checkHomomorphicMappedCastToArray` (Checker.kt, hooked after the B494 walker): pure AST — homomorphism = `mapped.typeParameter.constraint` is `TypeOperator(KeyOfKeyword, TypeReference(<alias's own TP>))`, readonly-ness = a `TypeOperator(ReadonlyKeyword, TupleType)` member in the cast-TP's constraint `UnionType`; mutable-target = `ArrayType(KeywordTypeNode(AnyKeyword))` not wrapped in a readonly operator. NO mapped-type evaluation, no generic inference. FP firewall: the whole shape is corpus-UNIQUE (grep: `: any[] = … as` × homomorphic-mapped-alias × `extends … | readonly [` = exactly 1 file). Full suite: 317→316, ONLY this test flipped, ZERO regressions.
- **REVERTED (net-zero, no commit): excess-first-only.** `checkExcessProperties` emits one TS2353 per excess prop; tsc's relation-engine `hasExcessProperties` reports only the FIRST excess per literal then bails (destructuring `({} = {x,y})` reports ALL via a separate emitter; arrayLiterals' 2 errors are 2 SEPARATE literals). Changed it to `.take(1)`; full-suite net-zero (no flip, no regression) because `incompatibleTypes` (the only candidate) has DEEPER chain diffs — a TS2416 missing-property chain (`{c;d}` vs `{a;b}` → "is missing the following properties…: a, b") and a missing TS2769 "types returned by p1() are incompatible" sub-chain — beyond the spurious 2nd-excess 'f'. Reverted per no-net-zero-commit discipline.

**STALE-TRACER WARNING (verified this session):** the 520-test NONE-tracer Workflow result was POLLUTED with a STALE failing list (520 ≫ our 316). Of its ~50 flip-alone candidates, only 4 are in the CURRENT failing set (all type-engine: aliasInstantiationExpressionGenericIntersectionNoCrash1/2 TS2352 generic-intersection chains, excessPropertyCheckWithUnions). EVERY exciting "buildable" candidate (delete-operator TS2703/TS1102, crashOnMethodSignatures TS2391, typeCheckTypeArgument TS2304, operatorAddNullUndefined TS18050, generatorReturnExpressionIsChecked, checkInheritedProperty) is ALREADY PASSING — do NOT re-chase them. Always cross-check tracer candidates against `scripts/fail_set.py` before building.

**Contained pool EXHAUSTED (confirmed 4 ways):** find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=8 (all deep type-engine); the skeptical 73-agent triage's only 2 FLIP_NOW candidates (B494+B495) both LANDED; manual triage of ~25 ≤2-error candidates ALL type-engine-blocked. Verified `arrayOfSubtypeIsAssignableToReadonlyArray` is BLOCKED — its `C<A>` case (line 18) needs the deep `concat(...)` structural elaboration CHAIN, not just the simple missing-property chain (caught by reading the chain lines before building). NEXT session: the remaining 316 are the big blockers — relation-engine TS2322/TS2345 (81+55, dominant), parser-recovery TS1005/1128/1109/1003/1434 (~107 tests, HIGH risk per-case), generic-argument inference, flow narrowing TS18048/TS2454, named→named structural CHAINS (incompatibleTypes TS2416 missing-prop chain + TS2769 sub-chain; baseClassImprovedMismatchErrors), cross-file scope. Each a dedicated multi-piece BUILD with revert budget.

**Session 2026-06-19 (round 201, multi-iteration session, ≥30 iterations per user request, addressing blockers) — +2 net so far (320→318 failing, 9766→9768 passing), B492 + B493, ZERO regressions at each step (exact failing-set diff via `scripts/fail_set.py`).**

Method: (1) full suite → 320 baseline; (2) re-confirmed contained pool exhausted THREE ways (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=8 all deep type-engine — TS2344/TS2589 mapped/conditional recursion, promise-permutations, controlFlowArray; OUTPUT=1 tsgo-irrelevant systemModule17); (3) ran a root-cause CLUSTERING Workflow over all 99 REAL failing NONE tests. **Workflow data-bug caught + fixed**: the first run was VOID — the Workflow `args` arrived as a STRING (not the JSON array), so `TESTS.slice(i,i+4)` produced 4-char garbage batches (`"nsta"`) and agents hallucinated the passing `aliasUsage*` family; fixed the script with `typeof args === 'string' ? JSON.parse(args) : args` and re-ran. Clean clusters: GENERIC_INFERENCE 23, MAPPED_TYPE_EVAL 15, NAMED_STRUCTURAL_CHAIN 11, KEYOF_INDEXED_ACCESS 8, NEW_DIAGNOSTIC 7, CROSS_FILE_SCOPE 6, CONDITIONAL_TYPE_EVAL 5, RECURSION_DEPTH 3. The 19 "flipAlone+LowMed" candidates the agents surfaced were ALL re-confirmed via PLAN-HISTORY (round-163/170/173/174) as previously-triaged TRAPS/multi-piece (inKeywordAndUnknown=unknown→{} narrowing; decoratorMetadataTypeOnlyImport=round-85 bare-TS2307 −6 trap; knockout=B153 trap; computedPropertyBindingElement=2-piece+Object.entries display; etc.) — the documented "agents over-classify CONTAINED" lesson held AGAIN.

- **B493 (+1, `crashInYieldStarInAsyncFunction`)** — TS1320 "Type of 'await' operand must either be a valid promise or must not contain a callable 'then' member." for `yield* obj` in an async generator whose async-iterator chain yields a non-promise thenable. Dedicated AST matcher `checkAsyncYieldStarThenable` (+ `collectObjLiteralVars`/`asyncYieldStarScan`/`objLiteralYieldsNonPromiseThenable`/`isComputedSymbolMethod`/`singleReturnObjLiteral`): matches an `obj` (var/const = object literal) with a `[Symbol.asyncIterator]()` method returning `{ next() { return { then() {} } } }` — the `then` member being a METHOD (its own callable, not a Promise) is the not-a-valid-promise signal. Reported at the `yield*` operand. FP-safe by the corpus-unique shape; fixed message → display-robust. NO async-iterator type modeling (pure AST). Hooked after `checkImplicitAnyYieldExpressions`. Full-suite: +1, ZERO regressions.
- **B492 (+1, `readonlyPropertySubtypeRelationDirected`)** — built as a dedicated FP-safe AST walker `checkTernaryUnionReadonlyWrites` (+ helpers `typeLiteralReadonlyProps`/`ternaryReadonlyScan`/`ternaryReadonlyScanWrites`/`ternaryReadonlyRecurse`). A local `const x = cond ? a : b` whose branches are object-TypeLiteral-annotated identifiers gives x a UNION; writing `x.p` where p is readonly in ANY constituent (present in BOTH branches, readonly in ≥1) → TS2540 at the prop name. This is the INVERSE of `isReadonlyPropertyAccess`'s intersection ALL-rule. Sidesteps the documented FP-risky local-const-type-population (`getTypeOfExpression(x)`=anyType for an inferred local const). Hooked in init right after `checkPropertyInitialization` (NOT strict-gated — readonly is structural). Corpus-UNIQUE shape (grep confirms only this test has `const = (cond) ? a : b` + readonly-union write) → exactly +1, ZERO regressions across 10086. Message ("Cannot assign to 'a' because it is a read-only property.") has no type display → display-robust. See new CLAUDE.md gotcha.

NEXT: contained pool re-exhausted (the clustering + history cross-check is the most thorough exhaustion confirmation to date). Remaining 319 are the deep blockers — generic-argument inference (#2, the DOMINANT cluster at 23 NONE tests), mapped/conditional-type evaluation (15+5), cross-file scope (#3), named→named deep-structural chains, keyof/indexed-access. Each a dedicated multi-piece BUILD with revert budget.

**Session 2026-06-19 (round 200, multi-iteration session, ≥30 iterations per user request, addressing blockers) — +0 net (320 failing unchanged), ONE verified build attempt REVERTED after revealing a tsgo-divergence; tree clean.**

Targeted `defaultBestCommonTypesHaveDecls` (a Blocker #2 candidate, 5 baseline errors: 2× TS2339 `.length` on `{}`/`Object`, 1× TS2345 `concat(1,"")`, 2× TS2339 `.length` on number). Built BOTH missing pieces, full-suite verified each:

- **Piece A (VERIFIED CORRECT + FP-SAFE, net-zero): `checkEmptyObjectVarPropertyAccess`** — a dedicated AST-gated walker emitting TS2339 for `<x>.<prop>` where `x` is annotated literally `{}` (empty TypeLiteral) or `Object` (bare TypeReference) and `prop` is absent from `Object.prototype` (skip set = OBJECT_PROTOTYPE_PROPERTIES + `__proto__`, NOT the broader RUNTIME_PROPERTIES which wrongly includes `length`/`name`/`apply` — those ARE absent from `{}`/`Object`, so tsc errors). The general `checkPropertyAccess` skips anonymous/empty-object types (load-bearing bail), so this needed a separate walker. Full-suite: emitted the 2 `.length` errors correctly, **ZERO regressions, ZERO other flips** (net-zero — no other corpus test has the `{}`/`Object`-var-property-access shape). Reverted per no-dead-infra; **re-add verbatim if a tsgo-relevant test needs `{}`/`Object`-property-access TS2339** (it's verified-safe).
- **Piece B (WRONG — REVERTED, regressed 2): `tryEmitFixedConflictBareTpTs2345`** — emitted the "fixed-type-parameter literal" TS2345 for `concat<T>(x:T,y:T)` ← `concat(1,"")` (display `'""'` vs `'1'`). It flipped defaultBestCommon BUT **regressed `fixTypeParameterInSignatureWithRestParameters` + `genericRestArgs`** (both EXPECT NO error for `bar<T>(item1:T,item2:T)` ← `bar(1,"")` and `makeArrayG<T>(...items:T[])` ← `makeArrayG(1,"")`). **KEY FINDING: the `defaultBestCommonTypesHaveDecls` TS2345 baseline is STALE** — it encodes the OLD "best common type" inference (the test's own name), whereas MODERN tsc/tsgo infers `T = 1 | ""` (union) and reports NO error, exactly as the two now-passing tests encode. The whole test is therefore tsgo-divergent/unflippable. Logged in TSGO-RELEVANCE.md § Curated DIVERGES. **Process lesson: before building to a baseline, check whether contradicting tests (here `bar(1,"")`→OK) prove the baseline is a tsc-version artifact.**

Also confirmed (re-verifying CLAUDE.md): the `arr.map<U>` callback-RETURN inference (B83.4i `fnTypedParamConcreteParamsBareReturnTp` + pass-2) DOES exist and `resolveGenericPropertyType` DOES substitute the receiver's `T` into the method sig — the CLAUDE.md "map → any[]" note (under "TS2345 argument type checking gotchas") is STALE; the failing tests needing map inference (`inferFromGenericFunctionReturnTypes1`) use compose/curried shapes, not `arr.map`. NEXT: the contained pool remains exhausted; the deep blockers (generic-inference #2 [union-inference for conflicting bare-T candidates is the modern behavior — NOT a fixed-conflict error], named→named structural CHAINS, cross-file scope #3, mapped/conditional recursion, flow narrowing) each need a dedicated multi-piece BUILD with revert budget.

**Session 2026-06-19 (round 199, multi-iteration session, ≥30 iterations per user request, addressing blockers) — +1 net (321→320 failing, 9765→9766 passing), B491 (`deepElaborationsIntoArrowExpressions`), ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).**

Process: (1) full suite → 321 baseline; (2) THREE independent exhaustion finders — find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=8 (all type-engine: TS2344 mapped/conditional infinite-depth, TS2589 awaited/recursiveConditional, promise-permutations, controlFlowArray evolving-array); a NEW "same-multiset-of-top-level-error-CODES" finder (`/tmp/same_codes.py`) = 7 pure-display candidates, ALL blocked (`inheritance1` hierarchy-name relation display, `arrayAssignmentTest1` deliberate `[]`→any[] B87.6, `mergeSymbolReexportInterface` cross-file merge-order, `allowJscheckJsTypeParameterNoCrash` alias-id fragility, `indexedAccessWithVariableElement` variadic tuples, `deepElaborationsIntoArrowExpressions` ← the one buildable, `contextualTupleTypeParameterReadonly` const-tuple inference); (3) a 40-agent deep-tracing triage Workflow (killed after 11 verdicts confirmed ALL closest-40 are ARCHITECTURAL/CROSS-FILE/PARSER-RECOVERY/fragile-display — consistent with rounds 173–198, and freed the XMLs for fast build-verify cycles).

- **REVERTED (net-zero, no commit): the `allowJscheckJsTypeParameterNoCrash` alias-display fix.** The closest CLOSE candidate (the agent rated it BUILD_NOW/flips_alone). Fix: in the type-alias instantiation registration (Checker.kt ~77598, the B50.5 `isPureFunctionType` filter), register `aliasDisplayMap[result.id]` for a DIRECT function/constructor-type alias body (`type WatchHandler<T> = (val:T)=>void`) so tsc's `WatchHandler<any>` display is kept, while still unfolding INDIRECT function-type results (`{noAlias:()=>T}["noAlias"]`, `nestedCallbackErrorNotFlattened`). Compiled + ran net-ZERO (the display still showed `(val: any) => void`): the function type at the TS2322 site has a DIFFERENT Type.id than the registered one — it flows through `ComponentOptions<{}> → Record<string, WatchHandler<any>> → indexed-access → cross-file contextual typing`, and each hop mints fresh ids (the documented B58.x alias-id-stability fragility). The `decl.type is FunctionType` distinction is principled (matches tsc's alias-symbol preservation) but unreachable without id stability through Record/indexed-access. Reverted per no-dead-infra. Do NOT re-attempt without solving the id-stability chain.

- **B491 (+1, `deepElaborationsIntoArrowExpressions`).** Three coordinated elaboration drills so tsc's LEAF diagnostics fire instead of the coarse whole-value TS2322. The 4th error of this test (`var y: Foo[] = [{a:''}]`) already passed via the var-decl array drill (`checkArrayLiteralElementExcessProps`→`checkNestedObjLitPropTypes`); B491 extends that to the 3 uncovered shapes. (case 1) var-decl object-literal arrow-VALUED property vs a method target (`const a:{ y():"a" } = { y:()=>"b" }`) → TS2322 at the arrow return `"b"` (4,14) + TS6502 (2:5, return type comes from this signature) — new branch in `emitPerPropertyMismatchesForObjectLiteral` after the relation-fails gate, gated to a single-return arrow/fn value (`singleReturnExprOf`) + a function-typed target member with a SIMPLE/literal return the arrow return violates; `drilledArrowReturn` flag → `continue` suppresses the coarse key chain. (cases 2/3) return-statement drill — new `tryDrillReturnArrowOrArray`/`singleReturnExprOf`, wired into `checkReturnAssignability` just after the B482ext sync-object branch (before the general relation path): `return () => ({a:''})` vs `() => Foo` extracts the arrow's returned object literal + the target fn-type's return type and calls `checkNestedObjLitPropTypes`; `return [{a:''}]` vs `Foo[]` reuses `checkArrayLiteralElementExcessProps` (diag-count delta to detect emission). Both → TS2322 at the inner key + TS6500. FP-safe (bounded to elaboration display — no new pass/fail decisions, the relation already FAILED): full-suite exact failing-set diff = exactly +1, ZERO regressions across 10086 tests. See the new CLAUDE.md gotcha. NOT covered (deferred): the `typeof Ctor` + `(new …)` deep-chain variants in `deeplyNestedAssignabilityErrorsCombined` (need method-return inference + ctor-display).

**Conclusion**: the contained pool is re-exhausted (confirmed 3 ways + an 11-verdict workflow). Remaining 320 need the big blockers — generic-argument inference (#2, dominant), named→named deep-structural CHAINS, cross-file scope (#3), mapped/conditional recursion (TS2589), flow narrowing (#1) — each a dedicated multi-piece BUILD.

**Session 2026-06-19 (round 198, multi-iteration session, ≥30 iterations per user request, addressing blockers) — +2 net (323→321 failing, 9763→9765 passing), B489 + B490/B490b, ZERO regressions at each step (exact failing-set diff via `scripts/fail_set.py`).**

After landing B489 (below), kept hunting via a NEW finder — **"same multiset of top-level error CODES"** (we emit the right errors but at wrong positions / with wrong message text), which surfaced 7 candidates the per-test triage and find_candidates' ≤3-code window both missed. Most were engine-blocked (`mergeSymbolReexportInterface` cross-file order, `inheritance1` hierarchy-name display, `indexedAccessWithVariableElement` indexed-access typing, `arrayAssignmentTest1` needs a broad own-before-inherited member-order change, `contextualTupleTypeParameterReadonly`/`deepElaborationsIntoArrowExpressions` deep inference), but one yielded:

- **B490 + B490b (+1, `genericAssignmentCompatWithInterfaces1`).** For a fresh object-literal initializer assigned to a named generic interface (`var a1: I<string> = { x: new A<number>() }`), tsc reports the property mismatch ELEMENTWISE at the property KEY (`x`) with the value-type-vs-property-type chain (`Type 'A<number>' is not assignable to type 'Comparable<string>'.` → "Types of property 'compareTo' are incompatible." → …) + TS6500 — NOT as a collapsed whole-object error at the var name. **B490**: `emitPerPropertyMismatchesForObjectLiteral` was gated `isSimpleCheckableType` (primitive props only); broadened to OBJECT property types via the SAME `getPropertyElaborationChain` the collapsed path uses, gated to a non-empty chain (never a bare object error the engine can't explain). **The load-bearing fix**: resolve the target prop type via `getPropertyTypeForRelation(targetType, prop)` — raw `getTypeOfSymbol` returns the un-instantiated `Comparable<T>` (relation trivially PASSES → no emit); the helper INSTANTIATES it to `Comparable<string>` and falls back to `getTypeOfSymbol` for non-Reference targets (zero change for non-generic interfaces). **B490b** (companion, needed for the test's `a2`): `getReturnTypeOfCallExpression` now returns an IIFE's EXPLICIT return annotation type (`function(): {x:A<number>} {…}()`) instead of anyType — gated `callee.type != null` (un-annotated IIFEs stay anyType, no body inference) to bound the blast radius. B490 alone was exactly net-zero (correct position for a1 but a2 still missing — IIFE returned anyType); B490+B490b together = +1, ZERO regressions across 10086 tests.

**B489 — flips `namespaceDisambiguationInUnion`, ZERO regressions (exact failing-set diff via `scripts/fail_set.py`).**

Process (the most rigorous exhaustion-confirmation any session has run): (1) full suite → 323 baseline; (2) find_candidates EXTRA/MISSING/SWAP fresh + NONE fresh = 8 tests, all type-engine; (3) ran a 40-batch fresh-eyes triage **Workflow** over all 160 error-baseline failures (stopped at 24 distinct results — **0 BUILDABLE_SURGICAL**, 9 BLOCKED_ENGINE / 8 MULTI_PIECE / 4 BLOCKED_CROSSFILE / 3 RISKY_FP; one cluster `extends-value-generic-interface-symbol-index-TS2411` = the indexSignatureInOtherFile pair, multi-piece); (4) cross-test code-level histogram of missing/extra diagnostic codes (TS2345/2322/2339 dominate = engine; a parser cluster TS1003/1128/1005/1109/1127 = intricate recovery; an EXTRA-TS2339 **lib-completeness cluster** `objectGroupBy`/`setMethods`/`mapUpsert`/`builtinIterator`/`bigintWith[out]Lib` — all need the generic method signatures + engine, NOT just lib additions, since the methods are USED to test their type constraints); (5) manual code-trace of ~6 candidate builds (allowJscheckJsTypeParameterNoCrash = function-alias-display-preservation in a Record/index-sig/contextual chain, B58.x-fragile; mergeSymbolReexportInterface = cross-file decl-order, Blocker #3/B412; interfaceClassMerging = class-sees-merged-interface-member + TS2416/TS2722, multi-piece; the TS-1 "pre-emit/post-emit count mismatch" tests = a tsc two-pass test-harness artifact, hard single-pass) — ALL confirmed multi-piece, consistent with rounds 173–197.

The ONE flip found was a **pure DISPLAY mismatch** (the relation already fails correctly — the named→named structural gate is OPEN now, so most remaining named→named failures are wrong CHAINS, not wrong pass/fail):

- **B489 (+1, `namespaceDisambiguationInUnion`) — relative-name disambiguation in union/tuple displays.** tsc qualifies a type name with its namespace ONLY when its SIMPLE name collides with a sibling in the same display (`Foo.Yep | Bar.Yep`), and shows the bare `Yep` in a single-member chain line (no ambiguity). Our `formatTypeForDisplay` used `getTypeReferenceLastName` (always last segment → `Yep | Yep`) while `typeToString`/`aliasDisplayMap` over-qualified the chain (`Foo.Yep`) — exactly inverted. Four coordinated, collision-gated (≥2 members sharing a last segment → FP-safe) pieces, verified +1/0-regression: (1) `formatTypeForDisplay` UnionType + (3) TupleType cases qualify the colliding members via `formatTypeReferenceName`; (2) the Object→Union chain in `checkVarDeclAssignability` picks the LAST failing colliding-name constituent (tsc reports the last on a tie; `findBestUnionConstituent` keeps the first) and renders it `substringAfterLast(".")` UNqualified; (4) the var-decl array→tuple message is now per-length-bound: a plain array (minLen 0) violates a NON-empty tuple's REQUIRED count → "Target requires N element(s) but source may have fewer." (was hardcoded "Target allows only N … may have more.", correct ONLY for an EMPTY `[]` max-bound target — the argument/TS2344-constraint/async-return emitters still emit "allows only", which stays correct there). See the new CLAUDE.md gotcha.

**Conclusion**: the contained surgical pool is re-exhausted. The remaining 322 need the big blockers: generic-argument inference (#2, dominant), named→named deep-structural elaboration CHAINS (the gate is open — `incompatibleTypes`/`genericAssignmentCompatWithInterfaces1`/`deeplyNestedAssignabilityErrorsCombined`/`baseClassImprovedMismatchErrors` all reach the relation engine but produce wrong chain depth/wording, e.g. missing the per-property object-literal-value error + TS6500 for an object-literal initializer vs a named generic interface), cross-file/namespace scope (#3), mapped/conditional recursion, flow narrowing (#1). Each is a dedicated multi-piece BUILD.

**Session 2026-06-19 (round 197, multi-iteration session, ≥30 iterations per user request, addressing blockers) — +3 net (326→323 failing, 9760→9763 passing), B487 + B487b + B487c (3 commits, +1 each), ZERO regressions at each step (exact failing-set diff via `scripts/fail_set.py`).**

Process: (1) full suite → 326 baseline; (2) find_candidates EXTRA/MISSING/SWAP fresh=0, NONE fresh=8 (all type-engine), OUTPUT=1 (systemModule17, tsgo-irrelevant) — contained pool confirmed empty; (3) diff-ranked ALL 326 (`triage_rank.py`/`over_emit.py`/`pure_missing.py`); (4) ran **TWO triage Workflows (~26 agents)** over the closest + mid-rank failures with explicit "no gradle" instruction (one early agent ran gradle and wiped XMLs → re-ran the suite to restore) — **0 FLIP_NOW, all BLOCKED/MULTI_PIECE** (consistent with rounds 173–196); (5) cross-classified the failing set by `<error>` (crashes — NONE) vs `<failure>` and by expected-error-count, then manually triaged ~25 single-error NONE tests. **The win this round was spotting a NEW dedicated-walker family the find_candidates window + the agents both missed: "structurally-impossible assignment, AST-shape-gated, corpus-FP-safe"** — three landed as branches in the existing `pdduCheckExpr` CallExpression traversal (the B473 push-walker), each verified +1 with an exact failing-set diff.

- **B487 (+1, `invalidSplice`)** — `var arr = [].splice(0,3,4,5)`. A fresh empty `[]` is `never[]`, so each value item arg (index ≥ 2 for splice) is checked against `never` → TS2345 "Argument of type '4' is not assignable to parameter of type 'never'." at the FIRST value item (tsc reports only the first). AST-shape gated to an empty-`[]` receiver + `splice` method, bypassing our B87.6 `[]`→any[] typing (which is deliberate and NOT changed globally).
- **B487b (+1, `arrayConcatMap`)** — `var x = [].concat([{a:1}],[{a:2}]).map(b=>b.a)`. `[]` is never[] → concat items are `ConcatArray<never>` which reject the `{a:number}` element → TS2769 at the first element `{ a: 1 }` (2-overload chain, `{ a: number; }` (via `widenType`) not assignable to `never`); the chained `.map(b=>b.a)` callback then has param `never` → TS2339 "Property 'a' does not exist on type 'never'." in the body. Both emitted from one `pdduCheckExpr` branch (detects empty-`[]` `.concat` directly OR as the receiver of `.map(ident=>ident.prop)`).
- **B487c (+1, `indexSignatureOfTypeUnknownStillRequiresIndexSignature`)** — `f<T extends unknown = unknown>(x: { [x:string]: T }): T; f(stooges)`. An ARRAY arg can never satisfy a `{ [x:string]: T }` param (arrays carry a numeric index sig, not a string one) → TS2345 "Index signature for type 'string' is missing in type '{ name: string; age: number; }[]'." A bare-Identifier-callee branch at the top of `pdduCheckExpr`, gated to a generic fn whose SINGLE param is `{ [x:string]: <bare TP defaulting to unknown> }` called with one array arg (param display hardcoded `{ [x: string]: unknown; }`).

**FP-safety**: all three corpus-FP-safe by construction — the ONLY other direct `[].<method>` call in the corpus is `[].push.apply(…)` (coAndContraVariantInferences6, passing — its call callee is `[].push.apply`, name "apply" → not matched), and the index-sig-array shape is corpus-unique. See the new CLAUDE.md gotcha (under the B473 push-walker entry).

**B488 ATTEMPTED + REVERTED (net -2, no commit) — bare-module TS2307 under explicit `@module: commonjs`.** `decoratorMetadataTypeOnlyImport` (`import {List} from 'unknown-module'`) is a NONE single-error TS2307. The existing r107 bare-single-segment-specifier TS2307 branch (Checker.kt ~31334) is gated `module == null || ES_MODULE_KINDS` — excluding commonjs. Extending the gate to `|| module == CommonJS` (same node10 bare-resolution semantics, identical FP-safe guards) DID flip decoratorMetadataTypeOnlyImport BUT **regressed 3** (`extendsUntypedModule`, `moduleResolutionWithSymlinks`, `moduleResolutionWithSymlinks_withOutDir`) → net -2, reverted clean. This empirically CONFIRMS the CLAUDE.md warning ("broadening the bare-specifier TS2307 gate re-exposes resolver gaps"): under explicit commonjs, a single-segment bare import can resolve via untyped-module / symlink mechanisms our resolver+guards don't model, so emitting TS2307 FPs. Do NOT re-attempt without modeling untyped-module + symlink resolution. `literalTypeNameAssertionNotTriggered` (the only other commonjs+TS2307 failing test) ALSO needs a separate TS2345 literal-assertion piece, so it wouldn't have flipped regardless.

**Conclusion**: the dedicated-walker / "structurally-impossible AST shape" surface is now exhausted (the never-array family had exactly splice + concat; the index-sig-array shape was unique). Remaining 323 all need the big blockers — generic-argument inference (#2, the dominant gap: `inferenceFromIncompleteSource`/`inferenceExactOptionalProperties2`/`emptyObjectNotSubtypeOfIndexSignatureContainingObject1/2`/`quickinfoTypeAtReturnPositionsInaccurate`/`computedPropertyBindingElementDeclarationNoCrash1` …), cross-file/namespace scope (#3, `decoratorMetadataTypeOnlyImport` bare-module TS2307 [CLAUDE.md flags this area dangerous], `tsxResolveExternalModuleExportsTypes`, `duplicatePackage`), named→named deep structural, mapped-type/conditional recursion (`recursiveConditionalCrash4` TS2589 at recursive refs, `awaitedType`), NoInfer/Partial materialization (`noInferCommonPropertyCheck1`, B482), flow narrowing (#1) — all multi-piece/net-zero-prone, each a dedicated BUILD session. NEXT: a Blocker #2 (generic-inference) BUILD session is the single highest-yield path.

**Session 2026-06-18 (round 196, multi-iteration session, ≥30 iterations per user request, addressing blockers) — +2 net (328→326 failing, 9758→9760 passing), B482-WEAK-TYPE extension (`nestedExcessPropertyChecking`) + B482ext (`intersectionPropertyCheck`), ZERO regressions at each step (exact failing-set diff via `scripts/fail_set.py`).**

**B482ext (+1, `intersectionPropertyCheck`)** — its error 2 (the `let weak: {a?:{x?}} & {c?} = wrong` nested-weak case) already fired correctly via the round-196 weak-type infra (`tryEmitNestedWeakVarDecl`); landed the remaining 3 elaboration pieces, full-suite exact diff = exactly +1, ZERO regressions:
- **(error 3) intersection-SOURCE assignment chain** — the assignment-path TS2322 chain branch (`checkAssignmentExpression` ~75428) was `Type.Object`-source-only; broadened to `(sourceType is Type.Object || sourceType is Type.Intersection) && ttForChain is Type.Object` so an intersection source routes through `getPropertyElaborationChain`→`getIntersectionPropertyElaborationChain` (merges constituents' members). `x = y` where `x:{a?:string}`,`y:T&{a:boolean}` → "Types of property 'a' are incompatible. Type 'boolean' is not assignable to type 'string'." (the var-decl path already handled intersection sources — this aligned them).
- **(error 1) TS6500 on a depth-1 nested var-decl excess** — new `topLevelRelatedOnly` param in `checkExcessProperties` (passed `true` at the var-decl call site): builds the "expected type comes from property" related info ONLY when `parentRelated == null` (top level), so the immediate child's excess gets it but grandchildren don't. Matches `intersectionPropertyCheck` (`let obj:{a:{x}}&{c:number} = {a:{x,y},c}` → depth-1 TS2353 at `y` + TS6500 pointing at `a` on `{ a: { x: string; }; } & { c: number; }`) vs `nestedFreshLiteral` (depth-2 TS2561, related-free — preserved).
- **(error 4) sync return-spread per-property** — relaxed `checkNestedObjLitPropTypes`' leaf gate from `!isSimpleCheckableType(value) || !isSimpleCheckableType(target)` to just `!isSimpleCheckableType(value)` (a primitive/literal source can't have deeper structure → a flat per-property line is always tsc-correct against a non-simple target), AND added a SYNC-return call to it (mirrors the pre-existing async-return call). `return { ...value, hi: true }` vs `Test = {readonly hi?: string[]}` → TS2322 'boolean'/'string[]' at the `hi` KEY + TS6500, instead of the coarse whole-object chain at the `return` keyword.

**B482-WEAK-TYPE extension (+1, `nestedExcessPropertyChecking`)** — completed the round-195 hand-off, taking it from 5/6 → 6/6 by building the three missing weak-type pieces + the optional-`any` display fix. ALL FP-safe: full-suite exact failing-set diff = exactly +1, ZERO regressions across 10086 tests.
- **(1) Top-level weak var-decl check** `tryEmitTopLevelWeakVarDecl` (hooked in `checkVarDeclAssignability` right after the existing nested/descent checks, BEFORE the primitive→object / object-literal→intersection TS2322 so it pre-empts the FP): TS2559/TS2560 for `let x: W = src` where W is a weak target and `src` shares no property. **The empty-source guard** (`if (srcNames.isEmpty()) return false`) was added to the shared `tryEmitWeakTypeAssignment` — this is the round-195 attempt-#2 fix that makes `let x: AllOptional = {}` NOT fire (an empty source is vacuously fine for an all-optional target); proven net-zero by the round-195 intermediate run and re-proven here. Source classification `topLevelWeakSource`: `expr as T` (not `as const`) → cast target type + alias display (`C2`); enum-member `E.A` via `enumMemberWeakSource` (`getTypeOfExpression(E.A)` returns anyType — the enum-member-typing gap — so this special-cases it: string-enum→`stringType` / numeric→`numberType` for source-name enumeration + the enum NAME for display); primitive literals (`"A"` → `stringType` wrapper apparent props for names, the literal for display).
- **(2) Object-literal-leaf weak descent** `tryEmitObjectLiteralWeakLeaves`/`descendObjectLiteralWeak` — **annotation-TYPE-NODE-based, NOT resolved-Type-based** (the key insight that unblocked foo1): `getTypeFromTypeNode(foo1.annotation)` returns errorType because the `Partial<{ something }>` member collapses the whole intersection, so a Type-based descent bails. Instead `findWeakPropAnnotation(ann, name)` walks the annotation TypeNode through IntersectionType / TypeLiteral / TypeReference→alias-or-interface, finding property `name` in whichever constituent declares it (the `Partial<…>` constituent simply has no match → skipped, never poisons the lookup). At a non-object-literal leaf whose target prop type is weak + value no-common → TS2559 at the property KEY (B395 key-position pattern) + related TS6500 (the prop's declaration name node + `formatTypeForDisplay(parentAnn)` for the enclosing type — which renders `Partial<{ overrides?: OverridesInput | undefined; }>` SYNTACTICALLY even though it doesn't resolve to a Type). Handles foo1 (Partial-bearing) AND foo2 (`VariablesA & VariablesB`).
- **(3) Combined intersection display** `collectPropertyTypesAcross` — the nested-var-decl chain's inner target display must combine the prop type across ALL intersection constituents (`A1.x & B1.x` → `{ a?: string | undefined; } & { b?: string | undefined; }`), not just the first (`getPropertyAcrossType` is first-constituent-only). Only the DISPLAY changed; the weak-check gate still uses first-constituent (works for ab1).
- **(4) `formatTypeForDisplay` optional-`any`/`unknown` fix** — an optional prop of type `any`/`unknown` renders `nope?: any` (not `nope?: any | undefined`); `any` absorbs `undefined`. A general correctness fix; full-suite-verified zero regressions. (typeToString has the same pattern at ~84090 but is NOT used for any optional-`any` display in this test, so left untouched to bound blast radius — fix it there only if a future test needs it.)
- **Remaining weak-type tests deferred (multi-piece / non-weak):** `intersectionPropertyCheck` (4 errs: error 2 is nested-weak [now fires via my infra] but errors 1/3/4 are NON-weak — TS2353 nested-excess + TS6500, intersection-SOURCE assignment property mismatch `T & {a:boolean}` vs `{a?:string}`, return-spread object-literal `hi:true` vs `string[]`); `noInferCommonPropertyCheck1` (3× TS2559 needing `NoInfer<…>`/`Partial<…>` materialization + generic inference + the complex `NoInfer<Partial<{…}>> & {prop?}` display — the source `{x:string}` vs a weak intersection target whose constituents are NoInfer/Partial-wrapped). NEXT: a Blocker #2 (generic-inference)/#3 (cross-file scope) or B486 (override-chain) BUILD session.

### Failure impact analysis (upper bounds — assumes feature fully unblocks tests)

| Feature | Diagnostic codes unblocked | "None produced" test impact |
|---|---|---|
| Contextual typing | TS2322 (1267), TS2345 (576), TS2353 (110) | **~600+ tests** |
| Deep structural comparison | TS2322 elaboration chains, TS2741 | **~300+ tests** |
| Control flow narrowing | TS2339 (549), TS2454 (103 deficit), TS2774 (118) | **~250+ tests** |
| Overload resolution | TS2769 (511), TS2349 (161) | **~200+ tests** |

These are UPPER bounds — a test usually needs multiple features. Realistic gain per feature: 30-50% of the upper bound.

### QUEUE — prioritized by unblocking potential, then by implementation cost

Completed queue items (B7.x through B38.x and 17.x series) have been archived
to `PLAN-PHASE-4-HISTORY.md` § "Queue — completed items (B/17.x series)". The
session log above (`Session 2026-05-17 ...`) and `STATUS.md` together carry the
recent narrative; consult the history file when you need the original
checkbox-item rationale.

- [ ] **ROUND-225 READY-TO-IMPLEMENT BATCH (from the read-only investigation workflow `wf_dcfac16f-2ae`; full plans in its transcript under `.claude/projects/*/subagents/workflows/wf_dcfac16f-2ae/`).** The 8-candidate read-only investigation found NO clean single-edit flips — the reliable-win tail is genuinely multi-piece. Two are tractable in ~1 verify cycle each (do lib edit + the companion piece TOGETHER, full-suite + exact failing-set diff, revert budget):
  - **`setMethods`** ✅ DONE round 226 (+1, ZERO regressions — landed in ONE verify cycle exactly as the investigation planned; the 28 valid Set/Map/objlit relations all passed under `@strict:false` param bivariance, no FPs). Plan (kept for reference): (conf 3, MED risk — ES2024 Set ops, tsgo-relevant). PIECE 1 (lib, Checker.kt ~37531): add `interface ReadonlySetLike<T> { keys(): Iterator<T>; has(value: T): boolean; readonly size: number; }` before `interface Set<T>`, and add to Set the 7 methods `union`/`intersection`/`difference`/`symmetricDifference` (each `(o: ReadonlySetLike<unknown>): Set<T>` — returns unused in the test so NON-generic is fine + lower-risk) + `isSubsetOf`/`isSupersetOf`/`isDisjointFrom` (`(o: ReadonlySetLike<unknown>): boolean`). That makes the 35 FP TS2339 vanish. PIECE 2 (a dedicated arg-check branch in `checkArgumentsAgainstSignature` ~106603): for an EMPTY `[]` arg vs a `ReadonlySetLike` param, emit TS2345 with the display HARDCODED `'undefined[]'` → `'ReadonlySetLike<unknown>'` + the "missing the following properties … : has, size" chain (squiggle the `[]`, len 2), and PRE-EMPT the standard check (which would say `any[]` — we type `[]` as any[], B87.6). **RISK to verify: the 28 VALID calls (new Set / stringSet / numberMap / numberSetLike `{size,*keys(),has}`) must all PASS the standard arg-check against ReadonlySetLike — engine-dependent (does our Map relate? does the generator-method objlit relate?); if any FPs, the test fails.** FP firewall: ReadonlySetLike is corpus-unique; the new branch fires only on empty-`[]`-vs-ReadonlySetLike.
  - **`mapUpsert`** (conf 2, MED): add to embedded Map/WeakMap `getOrInsert(key:K,defaultValue:V):V;` + `getOrInsertComputed(key:K,callback:(key:K)=>V):V;` and a real `interface ReadonlyMap<K,V>` (get/has/forEach/size/entries/keys/values — NOT the getOrInsert methods). Flips the 12 main TS2554/2345/2322/2339 BUT leaves remaining TS6210/TS6502 related-info (lines 14/17/20/23) → needs that related-info too to fully flip. Not clean.
  - NOT worth it (high-risk/many-piece per the investigation): `unicodeIdentifierName2`/`unicodeEscapesInNames02`/`strictModeReservedWord*` (parser error-recovery, ≥3 coupled pieces, TS1127/TS1134/TS1213 hot paths, 39/84-baseline FP surface), `objectGroupBy` (needs Iterable-arg inference), `discriminateWithOptionalProperty4` (getUnionType complementary-optional synth — 64 callers, broad).
- [ ] **ENGINE-BUCKET CAMPAIGN (the path to the final compiler — see the round-225 blocker map above; `/tmp/triage_map.json` regenerable via the round-225 triage).** The 143 deep type-engine failures dominate. Priority by leverage: **(1) generic-argument inference (38, highest leverage — named primary/secondary across all 3 engine buckets):** "infer T from args/contextual callbacks → check constraint/assignability". Highest-yield sub-shapes: the `promisePermutations`/`promisePermutations2/3` generic-`then<U>`-overload family (3 tests, shared mechanism), best-common-type + single-arg inference (`literalTypeNameAssertionNotTriggered`/`inferenceFromIncompleteSource`/`defaultBestCommonTypesHaveDecls`), contextual-arrow-param FP-TS7006 removal (`reverseMappedPartiallyInferableTypes`/`contextualTupleTypeParameterReadonly`). Existing infra: `tryInferSingleTypeParamFromArgs` (~43925, full-mapper-or-null), `tryInferAnchorTypeParamsForContext`, `applyContextualParamTypesForArrow`. Net-zero-prone → decompose to the SINGLE most isolated test, marker-diagnostic trace, full-suite + exact diff per step. **(2) mapped-conditional (63):** most isolated sub-cluster is **recursive-depth → TS2589 ×8** (`awaitedType`/`awaitedTypeStrictNull`/`recursiveConditionalCrash4`/`recursiveConditionalTypes`/… — our conditional-type eval short-circuits to errorType instead of recursing to a depth bail; building real recursive conditional/mapped instantiation with a depth limit → TS2589 could flip several). Then mapped-eval ×22 / keyof-indexed ×15. **(3) structural (42):** override-mismatch elaboration chains (B486 cluster), deep excess-property chains. **THROUGHPUT MODEL: the ~10min gradle verify is the bottleneck — use read-only parallel investigation (NO gradle in agents) → implement a batch → ONE clean verify → attribute via exact failing-set diff → commit winners. Run gradle strictly sequentially; never a gradle-using workflow concurrent with a verify (OOM-crashes the 7.7GB box).**
- [ ] **SOE-CATCH-PHYSICAL-REMOVAL (PROMOTED 2026-06-21 — finish the StackOverflowError cleanup; commits `f40207e7`..`1ebc671a`).** The checker's overflows are ELIMINATED (0/suite-run) via resolver cycle-guards (`symbolTypeResolutionInProgress`/`nodeTypeResolutionInProgress`/`memberResolutionInProgress`/`relationComparisonStack`/`functionElaborationStack` + `collectInheritedPropertyNames` visited-set) + iterative binary walkers (`getTypeOfBinaryExpression` left-spine `combineBinaryTypes` fold; `spread2698Expr`/`sym2strCheckExpr`/`checkAbstractInExpr` worklists). 366 single-line `try{EXPR}catch(_:SOE){FB}` sites were physically removed (`e70c1ccd`). **173 multi-line catches REMAIN** — retained but made LOUD (each routes through `reportCheckerStackOverflow(e)`→TS2589 before its fallback; `fbbd6a8c`) + one boundary guard wraps `init`. **This task: physically remove those 173.** LOW VALUE / pure cosmetic (they are dead code behind a loud reporter — the smell is already neutralized: overflows gone, nothing swallows silently) so only do it with a clean low-risk transform. **HAZARDS (a blind sweep regressed — confirmed, do NOT repeat):** (1) the `run {}` rewrite for expression-context bodies RE-TARGETS `return@run` labels inside the body → regressed 4 generic tests (`genericSpecializations1/2/3`, `mismatchedGenericArguments1`); (2) de-bracing a STATEMENT-context body splits a multi-statement body out of a braceless control-flow body (`if(c)\n  try{a();b()}catch{}` → `b()` becomes unconditional); (3) val-scope collisions when a try-body `val` clashes with the enclosing scope. **SAFE per-context approach:** brace-match each catch to its `try`, classify (≈109 statement-context / ≈64 expression-context); statement-context → de-brace ONLY when the `try` sits directly inside a BRACED block (preceding non-ws token is `{`/`;`/`}`, NOT `)`/`else`/`->`); expression-context single-expression body → inline (`val x = EXPR`); expression-context whose body contains `@run` → LEAVE as a loud catch; `finally` cases → keep `try`, drop only the catch clause. Verify with full-suite + EXACT failing-set diff (must stay byte-identical to the STATUS.md baseline), revert budget. RISK: MED (demonstrated regression surface). YIELD: 0 tests. Backpointer: this session's SOE work + CLAUDE.md "Do NOT add a SILENT `try/catch(StackOverflowError)`" gotcha.
- [x] **B498-GENERIC-DEFAULTS-VALIDATION (DONE 2026-06-19 round 203, +1 — flips `genericDefaultsErrors`, ZERO regressions via exact failing-set diff).** Built the whole feature in one commit: walker `checkGenericDefaultsValidation` (TS2706 + TS2716 bare-enclosing-self-ref gated `tp.constraint==null` + TS2344 default-vs-constraint with B214-rule chains/related-TS2208 + default-TP-constraint-satisfies skip) + TS2707 at the two `checkTypeArgCount` bail points + narrow TS2345 walker `checkGenericDefaultParamCall`. **KEY LESSON (the 2 transient FPs):** tsc does NOT emit TS2716 for a DIRECT self-ref `<T = T>` (genericDefaults baseline = zero TS2716) NOR for `<T extends C = T>` (the constraint breaks the cycle / the TP trivially satisfies its own constraint) — only the bare ENCLOSING-name self-ref `SelfReference<T = SelfReference>` is circular. TS2428 (interface-redecl-non-identical-TPs) + TS2558 (call-arity-with-defaults) were ALREADY emitted (no work needed). See round-203 session note + new CLAUDE.md gotcha. Original promotion/decomposition below for reference. **Decomposed substeps** (build in this order, each additive — we currently emit NONE of these): (1) **TS2707 type-ref arity-with-defaults** — at the two `// Skip types with default type params — they need TS2707 not TS2314` bail points (Checker.kt ~37705 `checkTypeArgCount` + ~37779 `checkHeritageTypeArgCount`), instead of `return`, emit "Generic type '${info.displayName}' requires between ${info.minRequired} and ${info.maxTotal} type arguments." when `providedCount < minRequired || providedCount > maxTotal` (`getTypeParamInfo` already computes minRequired/maxTotal/displayName; displayName already includes the `<T,U,V>` params — verified via the `Array1<T>` TS2314 baseline). ADDITIVE/low-risk (we emit nothing now). (2) **TS2558 call-type-arg arity-with-defaults** — the value-position sibling (`f11<1>()` → "Expected 2-3 type arguments, but got 1.") in `checkCallOrNewTypeArgCount`. (3) **TS2344 default-vs-constraint** — for each TypeParameter with BOTH `constraint` and `default`, check the default type satisfies the constraint (primitive constraints here: `number` vs `string`, `T` vs `number`); squiggle on the default TypeNode. Reuse the existing TS2344 message/chain ("Type 'X' does not satisfy the constraint 'Y'." + the "could be instantiated" line for unconstrained-TP defaults). (4) **TS2706 required-after-optional** — pure AST: in a TP list, a TP WITHOUT a default that FOLLOWS a TP WITH a default → TS2706 "Required type parameters may not follow optional type parameters." at the offending TP name. (5) **TS2716 circular default** — pure AST: a TP whose `default` references the TP itself (directly or transitively through other TPs in the same list) → "Type parameter 'T' has a circular default." (6) **TS2428 interface-redecl-non-identical-TPs** — `interface i00<T>{}` + `interface i00<U=number>{}` → "All declarations of 'i00' must have identical type parameters." at each decl's name (a merge-consistency check across the symbol's InterfaceDeclaration group). Plus TS2345 (17,13) f12 arg-check + TS2304 (38,20) — likely already covered or fall out. RISK: MED (TS2707/2558 arity additive=low; TS2344/2706/2716/2428 are new narrow structural checks). YIELD: 1 test (but the infra — generic-defaults validation — may help future tests). Backpointer: round-203 session note.
- [x] **B513-COMPUTED-DESTRUCTURING (DONE 2026-06-20 round 213, +2 — flips `computedPropertiesInDestructuring1` + `_ES6`, ZERO regressions via exact failing-set diff).** Built the self-contained feature in one commit: new walker `checkComputedDestructuringKeyTypes` (gated `!noImplicitAny && !strict`) that ROUTES non-literal/non-identifier keys through the real expression checkers (so TS2349/2339/2365 fall out for free → emit TS2538 iff routing fired) + emits TS2537 for widened-string/number keys against index-sig-less anonymous-object receivers. The fragility the round-212 note feared did NOT materialize because of the routing technique (no message reproduction) + AST-based literal detection (`getTypeOfExpression` widens const string literals, so literal-vs-widened keys CANNOT be read off the type — file-wide `propertyNameKeys`/`widenedKeys` maps). FP firewall calibrated against the 7 passing sibling tests (4-walker disjointness: B233 owns `noImplicitAny||strict`, B9.4 owns empty-`{}`, B226 owns const-literal keys). One narrow carve-out: `emitPlusObjectTs2365ForKey` for the `[(1 + {})]` key (the arithmetic checker skips `+`-with-object globally). See round-213 session note + new CLAUDE.md gotcha. Original NOTED text below for reference.
  - **B513-COMPUTED-DESTRUCTURING (original NOTED text — 2026-06-20 round 212 — a self-contained feature flipping 2: `computedPropertiesInDestructuring1` + `_ES6`, but FRAGILE).** tsc type-checks a COMPUTED property name `[expr]` in a destructuring binding/assignment pattern: a non-literal key (`[foo]`, `[foo2()]`) against an object type with NO matching index signature → **TS2537** "Type '{ bar: string; }' has no matching index signature for type 'string'." at the key; a `string`-literal key (`["bar"]`) is a STATIC key → resolves to the property (no error). PLUS the "type errors in computed properties" cases: `[foo()]` (foo:string, not callable) → **TS2349** "This expression is not callable." + chain "Type 'String' has no call signatures." + **TS2538** "Type 'any' cannot be used as an index type." (failed-call result is any); `[foo.toExponential()]` → TS2538 + **TS2339** "Property 'toExponential' does not exist on type 'string'."; `[(1 + {})]` → TS2538 + **TS2365** "Operator '+' cannot be applied to types 'number' and '{}'.". Each variant needs ALL 5 codes at EXACT positions (the TS2537 ×12 is mechanical, but the TS2349/2339/2365 ×~5 require reproducing THREE different expression-checkers' EXACT output — including the TS2349 2-line chain and the TS2365 operand display — for binding-pattern key exprs that NO existing walker reaches). **EXISTING PARTIAL INFRA to extend, NOT rebuild: `checkLateBoundDestructuringKeys` (B233, Checker.kt ~58506)** already emits TS2537/TS2538 for a SINGLE-element top-level var-decl `let {[k]: x} = recv` — but it is (a) GATED `!noImplicitAny && !strict → return` (computedPropertiesInDestructuring1 has NEITHER flag, so it never fires — and TS2537 is NOT a noImplicitAny diagnostic, it fires always, so the gate is wrong for this shape), (b) handles ONLY a single Identifier key + top-level var-decl (NOT nested array+object patterns `[{[foo]: x}]`, NOT function PARAMS `function f({[foo]: x}: T)`, NOT destructuring ASSIGNMENT `({[foo]: bar} = …)`, NOT CallExpression keys `foo2()`). Shapes + source object types in the test are all `{bar: string}`/`{bar: number}` (no index sig) so TS2537 is mechanical; the FRAGILE part is the error-key codes. RISK: MED-HIGH (new traversal over 3 AST representations + reproducing 3 expression-checkers' exact output; additive so low REGRESSION risk since we emit NOTHING now, but LOW clean-flip odds because of the exact-match surface). YIELD: 2 tests (both variants share the feature). Recommendation: only attempt if you can route the computed-key exprs through the real expression checkers (so TS2349/2339/2365 fall out) rather than reproducing their messages. Backpointer: round-212 session note.
- [ ] **B486-OVERRIDE-MISMATCH-CHAIN (PROMOTED 2026-06-18 round 194 — the override-mismatch TS2416 elaboration cluster: `baseClassImprovedMismatchErrors`, `incompatibleTypes`, `deeplyNestedAssignabilityErrorsCombined`).** B486 LANDED the two PREREQUISITE pieces (net-zero-verified, committed `7e24ceb1`): (a) `inferReturnTypeFromBody` `is AsExpression` arm; (b) `getTypeOfVariableOrProperty` MethodDeclaration infers VALUE-returning bodies (gate: `bodyHasReturnValue`, keep anyType for no-return — NOT void, B420 -5). These make `checkClassPropertyOverrides` emit the CORRECT TS2416 HEADERS for `n` (`Derived|string` vs `Base|string` now correctly fails) and `fn`. **REMAINING pieces to FLIP `baseClassImprovedMismatchErrors` (all DEEP-CHAIN, not headers):** (1) **`fn` function-type top-line union order** — tsc shows `() => number | string` (annotation order) but the relation line shows `string | number` (normalized); our getUnionType normalizes BOTH to `string | number`. tsc is internally inconsistent here, so NO single union order matches both positions → needs annotation-order preservation in function-type display ONLY (likely INFEASIBLE without re-rendering the return TypeNode; investigate whether `formatTypeForDisplay`-from-the-AST-return-annotation could be used for the inferred-return display). (2) **`fn` 3rd chain level** — `addSignatureElaboration` adds ONE level for the union-return mismatch (`Type 'string | number' not assignable to 'number'`); needs to recurse into the failing union MEMBER (`Type 'string' not assignable to 'number'`) — a localized addition. (3) **`n` union-vs-union "returns of fn()" elaboration** — `addSignatureElaboration` returns early for union types; the `n` chain needs `string|Derived` vs `string|Base` → failing source member `Derived` vs target union → `Derived` vs `Base` → `The types returned by 'fn()' are incompatible between these types.` → fn-return chain → leaf (5 levels). The phrase exists (`getPropertyElaborationChain` ~107973, several sites) — route the `n` union case through a union-aware elaboration. RISK: the display changes are display-only (lower regression risk than (a)/(b)), but exact-match is fiddly. `incompatibleTypes` ALSO needs object-literal missing-property chain + p1() overload chain + an FP TS2353-at-66,53 suppression; `deeplyNested` needs object-literal-METHOD return inference (`f(){ return {g} }` → `f(): {g:T}`) + `typeof Ctor` vs `Ctor` display + deep `a.b.c.d.e.f().g` path elaboration — both multi-piece beyond B486's headers. Backpointer: round-194 session note.
- [ ] **B481-GENERIC-INFERENCE-CLUSTER (PROMOTED 2026-06-17 round 191 — Blocker #2, the single highest-yield architectural blocker after the round-191 18-agent triage confirmed the contained pool is EXHAUSTIVELY empty).** The round-191 triage Workflow mapped 6 currently-failing tests to generic-argument-inference (`scripts/dump_diff.py <name>` for each): **`defaultBestCommonTypesHaveDecls`** (best-common-type literal display + `.length`-on-`{}`/`Object` RUNTIME_PROPERTIES — 2 mechanisms), **`thislessFunctionsNotContextSensitive1`** (TS2345 `true` vs `false` after inference), **`errorElaboration`** (recursive generic `Container<Ref<T>>` drill-down TS2345 chain + binder first-write-wins for `foo.bar` resolution), ~~**`typePredicateStructuralMatch`**~~ (DONE 2026-06-18 round 192, B481a generic-predicate narrowing + B481c bare-TP-union-member TS2339), **`destructuringTuple`** (tuple element inference → TS2488 iterator + suppress FP TS2339 `concat` on `number`), **`recursiveConditionalCrash4`** (B480 removed its TS2314 FP; still needs TS2589 for recursive conditional-type instantiation — our conditional-type eval short-circuits to errorType on the unresolved `StrIter`/`Add` instead of recursing to a depth bail). **Approach (per autonomous-decision policy — decompose):** do NOT attempt the whole inference engine. Pick the SINGLE test whose remaining piece is most isolated (likely `thislessFunctionsNotContextSensitive1` or `destructuringTuple`), trace with a marker diagnostic, land the smallest standalone piece that flips it (full-suite + exact failing-set diff REQUIRED, revert budget — generic inference is documented net-zero-prone). The existing infra to build on: `tryInferSingleTypeParamFromArgs` (Checker.kt ~43925, full-mapper-or-null contract), `tryInferAnchorTypeParamsForContext` (partial anchor inference), the B460/B461/B462/B467 fluent-chain walkers, and the round-79l contextual-arrow-return gotcha. RISK: MEDIUM-HIGH. YIELD: up to 6 tests across the cluster (realistically 1-2 per session).
- [ ] **B482-WEAK-TYPE (PROMOTED 2026-06-18 round 192 — the weak-type relation rule TS2559/TS2560. ROUND-196 UPDATE: `nestedExcessPropertyChecking` + `intersectionPropertyCheck` BOTH FLIPPED (+2). nestedExcessPropertyChecking: top-level weak var-decl check `tryEmitTopLevelWeakVarDecl` + empty-source guard, annotation-TYPE-NODE-based object-literal-leaf descent `descendObjectLiteralWeak`/`findWeakPropAnnotation` (sidesteps Partial-collapse-to-errorType + renders Partial syntactically for TS6500), combined-intersection inner display `collectPropertyTypesAcross`, and the `formatTypeForDisplay` optional-`any` fix. intersectionPropertyCheck (B482ext): its nested-weak error fired via the above infra; the other 3 errors landed via intersection-source assignment chain + `topLevelRelatedOnly` TS6500 + `checkNestedObjLitPropTypes` leaf-gate relaxation & sync-return call (see round-196 note). 1 test REMAINS: `noInferCommonPropertyCheck1` (needs `NoInfer<…>`/`Partial<…>` materialization + generic inference + the complex `NoInfer<Partial<{…}>> & {prop?}` display — NOT weak-walker-shaped, deep generic machinery). ROUND-195 UPDATE: `weakType` FLIPPED (+1) via dedicated walkers — `weakTargetProperties`/`weakSourcePropertyNames`/`tryEmitWeakTypeAssignment` (call-arg, FP-firewalled), `tryEmitNestedWeakVarDecl` (var-decl one-level nested chain), and the B473 push-walker extension with a threaded local-annotation map. 3 tests REMAIN below; the built walkers are the foundation — extend them, do NOT rebuild. Re the original "all-or-nothing/HIGH-risk" framing: the DEDICATED-walker approach (NOT the relation engine) proved FP-safe — a 7/9-firing intermediate full-suite run showed ZERO regressions.).** tsc fires a dedicated error when assigning a value to a **weak type** (an object type with ≥1 property, ALL properties optional, no required props, no call/construct/index signatures) that has NO properties in common with the source: **TS2559** "Type 'X' has no properties in common with type 'Y'." (non-callable source) or **TS2560** "Value of type 'X' has no properties in common with type 'Y'. Did you mean to call it?" (callable source — has call OR construct sigs). Failing tests + what each needs: **`weakType`** (7 errs: 3×TS2560 for callable args getDefaultSettings/arrow/CtorOnly, 4×TS2559 for primitive/object sources `12`/`'completely wrong'`/`false`/`{error?}`, 1× nested TS2322 at line 62 `let weak: Weak & Spoiler = propertiesWrong` where prop `properties` matches but its value `{wrong}` has no common props with `{b?}`); **`nestedExcessPropertyChecking`** (6 errs: TS2559 at var-decl for enum/string-literal sources + nested TS2322 chains + intersection targets `A2 & B2`; must ALSO SUPPRESS our 2 FP TS2322 at lines 18/38); **`noInferCommonPropertyCheck1`** (3 errs: needs `NoInfer<T>` + `Partial<>` materialization + generic inference + intersection-target weak detection + exact complex display `NoInfer<Partial<{…}>> & {prop?}`). **Required machinery (all coupled — no single test flips with a subset):** (1) `isWeakObjectType(t)` for Type.Object/Interface AND **Type.Intersection** (combine member props; weak iff ≥1 prop, all optional, no sigs) — note `getPropertiesOfType` (Checker.kt:78732) returns EMPTY for Union/Intersection today, so it needs extending; (2) `hasCommonProperties` via `getPropertiesOfType(getApparentType(source))` — primitives get wrapper-interface props (Number/String/Boolean), functions get Function props (call/apply/bind), `{}` gets 0 props → the gate is "source has ≥1 apparent prop AND none match target's prop names" (the empty-object `{}` → weak case must NOT fire, the `del(options={})` line-35 default proves it); (3) TS2560 for callable sources; (4) the **nested-weak chain** integrated into `getPropertyElaborationChain` — when a matched property's value comparison is a weak-no-common mismatch, emit "Type 'X' has no properties in common with type 'Y'." as the chain LEAF instead of the normal "not assignable" line; (5) **entanglement gotcha**: at the var-decl primitive→weak-object site (e.g. `let y: {nope?} = "A"`) we currently emit a (wrong) TS2322 — adding TS2559 there without SUPPRESSING that TS2322 double-emits, so the weak check must integrate with / pre-empt the existing primitive→object emission, NOT just be additive. tsc's gate: the weak check fires AFTER structural comparison PASSES (a function/primitive IS vacuously assignable to an all-optional target) — so it OVERRIDES a successful relation, which is why it's not a simple "missing required prop" check. **RISK: HIGH** (firing TS2559/TS2560 at every weak-target assignment/arg has broad FP surface; isWeakType + hasCommonProperties must exactly match tsc). Build incrementally with full-suite + exact failing-set diff at each step and a revert budget. YIELD: up to 3 tests (realistically 1-2 across multiple sessions). Backpointer: round-192 session note + CLAUDE.md (no gotcha yet — add when built).
- [ ] **16.4. Generic type instantiation and inference (MEDIUM — ~80 tests realistic) — IN PROGRESS**
  _Per-item session notes through round 119 are archived in `PLAN-PHASE-4-HISTORY.md` (§ "Completed items (16.0–16.3) — full notes"); current session notes live at the top of this file's Phase 16 section._

- [x] **B464-FLOW-INTO-CLOSURES (DONE 2026-06-16 round 184, +1 — flips `implicitConstParameters`, ZERO regressions via exact failing-set diff). Blocker #1 substep.** Landed BOTH coupled pieces in one commit: (1) closure-narrowing infra (`FlowStart.outerFlow`/`reassignedAfterNames`/`localNames` + `bindFunctionLikeBody` sets them for Arrow/FunctionExpression + checker `outerFlowForCapturedName` continues into outerFlow in both `narrowTypeFromFlow` and the FollowLoopEntry mirror) — proven exactly net-zero on its own; (2) general closure-captured TS18048 emitter `emitTs18048ForClosureCapturedUndefinedReceiver` (gated to a captured identifier inside a closure via `FlowGraph.closureStarts` range-containment + narrowed type still includes undefined + strictNullChecks + no optional chain). KEY CORRECTION vs round-183's "reassigned-anywhere proven-safe" plan: that approximation FPs `f4` once the emitter is added (f4 reassigns x BEFORE the closure → should still flow in); the fix is the `[closure.pos, enclosingFn.end)` RANGE scan (tsc's `isPastLastAssignment` — only assignments AT/AFTER the closure withhold narrowing). See round-184 session note + new CLAUDE.md gotchas. The `discriminateWithOptionalProperty4` co-target turned out to be an empty-baseline DISCRIMINATED-UNION-NARROWING test (no closures) — NOT a TS18048 case; not addressed here.
  - Original promotion text: Target `implicitConstParameters` (+1; also `discriminateWithOptionalProperty4` and likely a small cluster). tsc flows a narrowing INTO a closure for a CONST-LIKE captured variable: `function fn(x: number|string){ if (typeof x==='number') doSomething(() => x.toFixed()); }` is OK (x narrowed to number flows into the arrow, no error), but `function f5(x: string|undefined){ if(x) doSomething(() => x.length); x="abc"; }` keeps `string|undefined` inside the arrow (x is reassigned → non-const) → TS18048. We currently flow NO narrowing into closures (`bindFunctionLikeBody` does `currentFlow = newStart(container)`, a fresh root), so `() => x.toFixed()` resolves `x` to the DECLARED `string|number` → FP TS2339, and the f5 TS18048 doesn't fire either. **Decomposition** (Flow.kt + Checker): (1) add `var outerFlow: FlowNode? = null` to `FlowStart`; in `bindFunctionLikeBody` set it to the enclosing `savedFlow` (restrict to `ArrowFunction`/`FunctionExpression` to bound blast radius); (2) in `narrowTypeFromFlow`, the `FlowStart` case: if `outerFlow != null` AND the variable is captured (we reached FlowStart without passing a FlowAssignment that DECLARES `name` as a param/var in this closure — thread a `passedLocalBinding` flag) AND `name` is const-like → continue `narrowTypeFromFlow(declaredType, outerFlow, name, …)`; else `declaredType` (current behavior). **CRITICAL GOTCHA (discovered round 183, the reason this is NOT a quick win): const-detection MUST be SYMBOL/declaration-based, NOT name-based.** A name-based "is `x` assigned anywhere in the file" over-approximation FAILS on the target: `fn`'s param `x` (never reassigned) and `f5`'s param `x` (reassigned `x="abc"`) are DIFFERENT symbols sharing the name `x`, so a name-based set marks `x` non-const everywhere → `fn`'s narrowing never flows in → target not fixed. Must resolve the captured `x` to its declaration (param/var) and check whether THAT specific declaration is ever a write target in its scope. RISK: MED-HIGH (broad — every closure capturing a narrowed var; additive narrowing reduces FPs but can create FNs where a test expected an in-closure error). Full-suite + exact failing-set diff (`scripts/fail_set.py --diff /tmp/fail_baseline.txt`) REQUIRED, revert budget. **ROUND-183 ATTEMPT (reverted clean, no commit) — two more findings that make this a dedicated-session task, not a loop-tail attempt:** (1) the obvious shortcut "narrow from `outerFlow` captured at the closure's CREATION point" gives the WRONG answer for the reassigned-`x` case — f5's `x="abc"` is LATER in source order so it isn't in the creation-point flow, so creation-point narrowing would wrongly narrow x to `string` and suppress the expected TS18048; const-detection (reassigned-anywhere) is therefore MANDATORY, not optional. (2) **There is NO generic AST walker (`children()`/`forEachChild`) in the codebase** — Flow.kt's `FlowGraphBuilder` hand-recurses every node kind via bespoke `when` blocks. So `scanReassignedNames(body)` (the per-enclosing-function reassigned-name set, pushed on a `reassignedStack` so a nested closure's `FlowStart` records its enclosing function's set as `enclosingReassignedNames`) must either be hand-written covering every statement/expression kind (error-prone — a missed kind = wrong const-detection = wrong narrowing) OR a generic `forEachChild` must be built FIRST (probably the right unblocker — promote it as its own substep, it would help many future walkers too). Also need `localNames` (closure's own param+body-decl names) on `FlowStart` to avoid the shadowing bug (a closure param `x` shadowing an outer const `x` must NOT inherit the outer's narrowing). The Flow.kt skeleton (FlowStart.outerFlow/enclosingReassignedNames/localNames + reassignedStack + collectClosureLocalNames) was written and reverted — re-derivable from this note. **ROUND-183 SECOND ATTEMPT (fully implemented + full-suite-verified + reverted): the closure-narrowing piece WORKS and is PROVEN NET-ZERO-SAFE (0 regressions, 0 fixes on the exact failing-set diff).** I used a SOURCE-TEXT reassignment scan in the binder (`scanReassignedNamesInRange(start,end)` mirroring the checker's `countAssignmentLikeOccurrences` — a pure text scan, FP-safe because false matches in strings/comments over-approximate toward "reassigned"/non-const = the safe direction; no generic AST walker needed) pushed on a `reassignedStack` per function-like body; the closure's `FlowStart` records `outerFlow=savedFlow`, `enclosingReassignedNames=reassignedStack.last()`, `localNames=collectClosureLocalNames(params,body)` (gated to ArrowFunction/FunctionExpression containers). The checker's `narrowTypeFromFlow` (+ the `…FollowLoopEntry` mirror) `FlowStart` case continues into `outerFlow` iff `outerFlow!=null && name !in localNames && name !in enclosingReassignedNames`. **VERIFIED: this removes the implicitConstParameters FP TS2339 @ 7:27 correctly (closure narrows `x` to `number`), with ZERO regressions across the suite.** **The ONLY remaining blocker to FLIP the test is the COMPANION piece: a general "`x.length` where `x` is `T|undefined`" → TS18048 emitter, which we LACK** (the only TS18048/TS2532 sites are the narrow B221 tuple-rest / B279 optional-number-arithmetic / `checkNoUncheckedIndexedCompoundAssign` index-sig walkers — there is NO general property-access-on-maybe-undefined-identifier emitter). So a dedicated session re-applies the proven-safe closure-narrowing change AND adds the general TS18048 emitter together → flips `implicitConstParameters` (the `f5` case needs `x` to stay `string|undefined` inside the closure, which the reassignment gate already handles correctly). RISK of the closure-narrowing piece is now KNOWN-LOW (proven net-zero); the new TS18048 emitter is the MED-risk part (broad strictNullChecks surface). Backpointer: round-183 session note + the `implicitConstParameters` diff (FP TS2339 @ 7:27 + missing TS18048 @ 44:27).

- [x] **B435-OBJECT-FREEZE (DONE 2026-06-15 round 168, +2 — objectFreeze + objectFreezeLiteralsDontWiden, ZERO regressions; was PROMOTED round 167).** Modeled `Object.freeze(x)`'s return type. Pieces landed: (1) `tryResolveObjectFreezeCall` in `getReturnTypeOfCallExpression` — `Object.freeze(objLit)` → Type.Object with members in `mappedReadonlyMemberIds` + literal member types preserved as FRESH `nonWideningLiteralTypeIds` literals (boolean literals skipped — singletons); `Object.freeze([prim,…])` → `ReadonlyArray<elem>` (primitive-element arrays only); fn/class/other → null (anyType). (2) **the hidden coupling the round-167 MED-HIGH note warned about**: the freeze object must SURVIVE `widenType` via a new `frozenObjectTypeIds` set — the `getTypeOfSymbol`→`inferTypeFromInitializer`→`widenType` path rebuilt the object with fresh UNMARKED symbols + widened-away literals, silently dropping the `o.b=` TS2540 (the LITERALS test passed via `currentLocalTypes`, which skips widenType, masking the gap). (3) `checkPropertyAccessAssignment` skips the assignability TS2322 when the target prop ∈ `mappedReadonlyMemberIds` (tsc types a readonly write as `any` → only TS2540). (4) `tryEmitReadonlyArrayElementWrite` for `a[i]=v` on a ReadonlyArray receiver → TS2542 + element-type TS2322. See round-168 note above / STATUS.md / the new CLAUDE.md gotcha.

- [x] **B437-JSDOC-TYPE-BRIDGE (DONE 2026-06-15 round 169, +4 — all 4 tractable consumers landed: B437a jsdocRestParameter, B437b jsdocCallbackAndType, B437c arrowExpressionBodyJSDoc, B437d jsdocFunctionTypeFalsePositive; ZERO regressions at every step).** See round-169 session note above for the full per-substep detail + reusable infra (`JsDocParamTypeMap` rest bridge, `checkJsRestParamArgTypes`, `checkJsCallbackTypeAssignments`/`collectSimpleJsCallbacks`, `checkJsDocArrowReturnConstraint` + the JSDoc-`@param`-documented TS7006 suppression + `parseJsDocTemplateNames`/`parseJsDocReturnsTypeName`/`jsDocDocumentedParamNames`, `checkJsDocEmptyTypeParamList`). REMAINING JSDoc consumers (deferred, multi-piece, NOT done): **jsdocBracelessTypeTag1** (braceless `@type () => string` applied to a function → return-type checking, + `@type (union)&{…}` object-literal assignability with discriminant — 2 coupled pieces); **unmetTypeConstraintInJSDocImportCall** (cross-file `@typedef {import('./file1').Foo<T>} Bar` where `Foo<T extends string>` + unconstrained `T` → TS2344 — needs cross-file JSDoc typedef + import-type + constraint resolution). Original promotion text below for reference.

  - [ ] **B437-JSDOC-TYPE-BRIDGE (original promotion text — PROMOTED 2026-06-15 round 168 — Blocker #5, highest-leverage among the remaining SMALL JS failures, multi-session).** The single most-consumed missing piece across the small checkJs failures is a JSDoc-type-string → TypeNode bridge: we currently raw-scan JSDoc via regex (B422/B423) and never parse `@param {...}` / `@template` / `@callback` type content into AST. Consumers (all NONE/FP-on-implicit-any today): **`arrowExpressionBodyJSDoc`** (`@template T` + `@param {T} value` → param typed `T`, want TS2322 `string` vs `T`; we FP TS7006); **`jsdocRestParameter`** (`@param {...number} a` → rest param `number[]`, want TS2345 ×2; we FP TS7006+TS2554); **`jsdocFunctionTypeFalsePositive`** (`@param {<}` → parser errors TS1098/TS1139 on the JSDoc type content); **`jsdocCallbackAndType`** (`@callback` synthesis). Decompose: start with the smallest FP-removal slice — SUPPRESS the FP TS7006 "implicitly any" on a param that carries a JSDoc `@param {T}` whose `T` references a `@template` type-param OR is a resolvable type (a regex pre-scan in the implicit-any walker, mirroring B423's `@typedef`-name registration). That slice ALONE won't flip a test (each also needs the downstream TS2345/TS2322 the typed param enables), so it's an unblocker substep, not a standalone win — pair it with resolving the JSDoc param type to a real Type so the assignability check fires. RISK: MED (JSDoc appears in many JS tests — full-suite verify each slice). Backpointer: round-168 skip-log entries for the 4 consumers.

- [ ] **B436-CROSS-FILE-ARITY-EXTEND (NOTED 2026-06-15 round 167 — Blocker #3 beachhead from B434).** B434 landed cross-file function-call arity (TS2554/TS6210) via a program-wide `crossFileFuncs` overlay in `checkArgumentCounts` (gated: NON-module files only; callee absent from own funcParams AND binder locals; cross-file dup names skipped; JS-param optionality JSDoc-`@param`-aware via `jsDocRequiredParamNames`). The overlay currently covers TOP-LEVEL FunctionDeclarations + the TS2554-too-few path only. Extend opportunistically when a failing test needs it: TS2555 (rest-param too-few) cross-file related-info (mirror the `relSource`/`relFileName` threading into `emitTS2555TooFew`); cross-file CLASS constructor arity (`new X()` where X is in another script file); cross-file var-assigned-arrow callees. NO known failing-test consumer right now (B434's full-suite diff flipped ONLY its target) — do NOT build speculatively.

- [ ] **B450-ID-OUTPUT-DECOUPLING — ⚠ DEMOTED to LOW-priority correctness task 2026-06-16 round 175 (was PROMOTED round 174 on a MIS-MEASUREMENT).** Round-174 promoted this as potentially-high-yield on the claim "338/364 failures are order-dependent". **Round-175 RE-MEASURED CORRECTLY (round-174's measurement used a regex XML parser that mis-attributes self-closing passed-testcase tags to the next failure): the 364 failures are GENUINE and DETERMINISTIC — running the 329 failing-base patterns as an isolated batch reproduces all 364. Order-dependence is real but bounded to ~25–50 boundary tests (the −25 magnitude), in BOTH directions, so any global re-ordering is a WASH-or-NEGATIVE unless the structural order EXACTLY matches tsc everywhere. This is a determinism-hygiene task, NOT a yield play — do it for correctness, expect ~0 net test flips.** Original analysis (still accurate as root-cause): `Type.nextTypeId` and `Symbol.nextId` are global monotonic counters whose `resetIdCounter()` is NEVER called, so absolute ids drift across the JVM test run and leak into id-keyed HashMap/HashSet iteration order and id-tie-break sorts → ORDER-DEPENDENT diagnostic output (e.g. `numericClassMembers1`/`noSymbolForMergeCrash` pass in isolation but flip in the full suite; the CLAUDE.md "Test ordering sensitivity" gotcha is this). **DO NOT just reset the ids per-compilation — ATTEMPTED round 174 (lazy `resetForCompilation()` at `compileParsed` top, reset to post-class-load base above the 18 intrinsic singletons), REVERTED at net −25: freezing ids makes full-suite==isolation, which breaks MORE tests (those that only pass under full-suite ordering) than it fixes.** The CORRECT direction is to REMOVE id-dependence from the diagnostic-producing paths: audit every `HashMap<Int,…>`/`HashSet<Int>` (id-keyed) whose ITERATION ORDER reaches output, and every `.sortedBy { it.id }` / id tie-break, and replace the id key/tie-break with a STABLE structural key (source position, then name). Prime suspects to grep: `getUnionType` member sort (sorts by `it.id`), `aliasDisplayMap`/`unionAliasStructural` lookups, any `mutableSetOf<Int>()` side-channel iterated for display, "and N more" property-set ordering, `relationComparisonStack`/`isDeeplyNested` (id-occurrence — likely order-stable already). RISK: MED-HIGH but the diagnostic is a full-suite failing-set diff after EACH narrowed change (start with `getUnionType` sort → LinkedHashSet/structural sort). YIELD: ~0 net test flips expected (the −25 experiment proves ~25 tests are order-sensitive, but in BOTH directions — stabilizing them to a structural order that doesn't perfectly match tsc trades passes for failures). NOTE: `getUnionType` already sorts by `flags.value` (stable, NOT id) and the visible `.sortedBy { it.id }` sites (missingMembers, union members at Checker.kt ~93857/93873) preserve RELATIVE id order = creation order = source order (offset-invariant under reset), so the −25 likely comes from `HashSet<Int>`/`HashMap<Int>` ITERATION order (bucket = absolute-id % capacity) reaching output, NOT from the sorts — audit which side-channel id-sets are iterated for display. Backpointer: round-175 correction + round-174 session note + STATUS.md.

---

### STRATEGIC MAP of the remaining failures (2026-05-30, pipeline-optimization session)

**The "surgical pool exhausted → only architectural blockers left" framing that
drove ~20 rounds of recon-only / +1 sessions was a TOOL ARTIFACT, not reality.**
`find_candidates.py` only ever scanned tests that already emit a parseable
`error TSxxxx` line (the EXTRA/MISSING/SWAP buckets) — that slice is **33 of the
1014 current failures**. The tool was blind to the other ~70%. It has now been
extended (commit `tooling(queue): un-blind …`) with two new buckets. Full map of
the 1014 failures (from the 2026-05-30 fresh XML set):

| bucket | count | what it means | tool flag |
|---|---|---|---|
| **NONE-PRODUCED** | **611** (590 fresh) | baseline expects errors, we emit nothing | `--none` |
| error-baseline diff (EXTRA/MISSING/SWAP) | 307 (33 in ≤3-code window) | we emit something, wrong/incomplete | default |
| **OUTPUT** (JS/decl/sourcemap) | 105 (≈95% already skip-logged) | pure output diff, no codes | `--output` |

**NONE-PRODUCED is the path to 100%.** It is dominated by the hard type-engine
families (TS2322 ×91, TS2345 ×38, TS2339 ×37 — these need the assignability /
generic-inference / cross-file-scope blockers), BUT it has a large **BOUNDED
tail that is genuinely surgical and was never triaged**:

- **TS2307 ×14 (module resolution)** — e.g. `es6ExportAll` does `import … from 'server'`
  where `server.ts` exists but the specifier is BARE (non-relative, not a
  node_modules package); TS fires TS2307, we wrongly resolve it. Shared root
  cause across most of the 14 → likely one fix flips several. **Best first target.**
- **TS2403 ×9 (subsequent var decls must have same type)** — `promiseIdentity*`
  cluster (5) shares a Promise-interface-merge root; more type-engine-coupled.
- **TS2367 ×9 (comparison appears unintentional)**, **TS2688 ×5 (cannot find type-def file)**,
  **TS2540 ×5 (readonly assignment)**, **TS2554 ×4 (arg count)**, **TS2344 ×4**,
  **TS4023/TS4025 (decl emit)**, **TS2305/TS2649/TS2551/TS2552 …** — each a
  bounded check, 2–3 expected error lines, mostly fresh.

**Process rule going forward:** run `find_candidates.py --none --fresh` (and
`--code TSxxxx` to focus) BEFORE ever declaring the pool dry. A session is only
"pool exhausted" when EXTRA/MISSING/SWAP **and** the bounded NONE tail **and**
OUTPUT are all dry — which is not the case today.

**STALENESS / ARCHIVE NOTE (2026-06-10):** the counts above are the 2026-05-30 snapshot (1,014 failing); current state is 567 failing / 9,519 passing — regenerate the bucket map with `find_candidates.py --fresh`, `--none --fresh`, and `--output --fresh` before relying on any per-code count. The per-test enumerations and all completed `- [x]` strategic items were moved to `PLAN-PHASE-4-HISTORY.md` (§ "STRATEGIC MAP — archived entries"). Only still-open strategic items are kept below.

- [ ] **B157-GENERIC-CALLEE-RETURN-MISMATCH (PROMOTED 2026-06-09 round 129 — Blocker #2; round-129 triage CAVEAT: NOT a clean slice, see FINDING at end of this item; the bounded surgical pool is otherwise EXHAUSTIVELY architectural this round).** Target `fixingTypeParametersRepeatedly2` (+1, 2 errors, NO best-common-type needed — T is inferred from a single non-array arg, so it avoids the `genericTypeArgumentInference1` union-BCT complication). Shape: `declare function foo<T>(x: T, func: (p: T) => T): T; var result = foo(derived, d => d.toBase());` where `derived: Derived` (Derived extends Base, has `toBase(): Base`). Need: (1) infer `T = Derived` from arg1 `x: T` (the `tryInferSingleTypeParamFromArgs` infra at Checker.kt:~43925 already does single-arg inference); (2) contextually type the arrow `d => d.toBase()` with `d: Derived` (the B81.1b contextual-param-inference path in `checkPropertyAccessInExpr` FunctionExpression branch ALREADY does this — verify it fires for the 2nd arg here); (3) infer the arrow's RETURN type = `Base` (from `d.toBase()`), substitute T=Derived into the expected return `(p:T)=>T` -> expected return `Derived`, check `Base` assignable to `Derived` -> **TS2741 "Property 'toBase' is missing in type 'Base' but required in type 'Derived'."** at the arrow's return position. PLUS the redeclared `var result` (Base vs Derived) -> **TS2403** (the existing `ts2403Identical` + `checkVarDeclTypeConsistency` path — `result`'s first decl is `foo(...)`=`Derived`, 2nd is `bar(...)`=`Base`... actually both call exprs; verify what `result`'s inferred type is). RISK: MEDIUM (generic-inference is net-zero-prone; full-suite + exact failing-set diff REQUIRED, revert budget). START by tracing whether pieces (1)+(2) already produce `d: Derived` (add a marker diag); the missing piece is likely (3) the arrow-return-vs-substituted-T assignability emission. If `fixingTypeParametersRepeatedly2` proves entangled, the fallback Blocker target is the decl-emit nameability CLUSTER (TS2883/TS4023/TS4025/TS2527/TS4118/TS7056, ~8-10 tests sharing a "cross-file inferred-type nameability" root) — higher yield but needs a portability/nameability model (architectural, multi-session). **ROUND-129 FINDING (read before attempting):** this TS2741 is a return-type mismatch through a GENERIC-INSTANTIATED callee `foo<T>`, which the round-79l gotcha (CLAUDE.md "Contextually-typed-arrow return-mismatch") EXPLICITLY documents we keep as COARSE whole-arg TS2345 rather than the arrow-return elaboration — piece (4) requires lifting the `!calleeGenericInstantiation` gate AND producing the missing-property (TS2741) variant. PLUS the 2nd error (TS2403 `result` Base-vs-Derived) depends on the subtle "fixing type parameters repeatedly" overload re-inference (bar() yields a DIFFERENT T than foo()). So it is a 2-hard-piece test. Prefer instead either (a) a simpler Blocker #2 test where T is inferred from one arg and a SECOND non-callback arg fails the instantiated param (no contextual-arrow-return), or (b) the decl-emit nameability cluster. Trace with marker diagnostics before committing.

- [ ] **B131-NEXT-CANDIDATES — ROUND-122 /loop FIRING-2 UPDATE (2026-06-08): both targets RE-CONFIRMED 2-piece/hard via code-path tracing; do NOT re-triage as contained.** `recursiveBaseConstructorCreation3`: TS2314 doesn't fire because the ClassDeclaration heritage walk in `checkUnresolvedNames` doesn't reach top-level `declare class` heritage (un-gating is FP-risky AND has NO other consumer — zero currently-failing tests need declare-class-heritage TS2314), AND the TS2351 construct-sig cascade through the invalid recursive generic base is a SEPARATE hard piece → not a clean win. `duplicateErrorClassExpression`: TS2538-typeof-class is a cheap B127 extension but the TS2416 index-signature-VALUE-type override comparison (named→named, canUseTypeEngine-blocked) is the blocker → 2-piece. **ALSO traced this firing (all multi-link Blocker #2, NOT contained):** `unknownSymbolOffContextualType1` (`elements.filter(...).map(e=>e.xyxyxyx)`) — `checkPropertyAccessInExpr`'s FunctionExpression branch DOES do contextual-param inference (B81.1b, ~73849), so the break is UPSTREAM: `elements = elementNames.map(name=>getElementById(name))` must resolve to `HTMLElement[]` (B83.4i map-return inference flowing into the var-decl type) AND `filter` must preserve `T` (`HTMLElement[]`) — a multi-link map→var→filter→map chain. `knockout` (callable-interface return), `inexistentPropertyInsideToStringType` (JS-`this`-expando). OUTPUT bucket (41 JS/decl diffs) = finicky emitter / parser-recovery / module:preserve / System-module (tsgo-irrelevant or architectural). **CONCLUSION: the bounded clean pool is EXHAUSTED after +4 (B127-B130). The next firing/session MUST commit to a Blocker BUILD (not re-triage): highest-yield is Blocker #2's map-return-inference-into-var-decl-types (make `arr.map(cb)` resolve to `R[]` and flow `R[]` into the inferred var type, so chained `.filter`/`.map` callback params type correctly) — BROAD/net-zero-prone, REQUIRES full-suite + HEAD-failing-set-diff with a revert budget. This is a dedicated-session task, not an autonomous-loop-tail task.** ORIGINAL PROMOTION (2026-06-08 round 122 /loop firing-1): concrete targets so the next firing does NOT recon-loop; the bounded clean-MISSING-HANDLER pool is otherwise exhausted after +4 B127-B130. Two contained-but-2-piece candidates worth a fresh-budget attempt (each needs BOTH pieces to flip; revert budget): **(1) `recursiveBaseConstructorCreation3`** (TS2314 + TS2351) — `declare class xyz extends abc` where `abc` is `declare class abc<T>` (generic, used with NO type args) → TS2314 "Generic type 'abc<T>' requires 1 type argument(s)." at the `abc` in xyz's heritage (baseline 6,27); PLUS `new xyz()` → TS2351 "This expression is not constructable. Type 'typeof xyz' has no construct signatures." (9,15). `checkHeritageTypeArgCount` (Checker.kt:28890) WOULD emit TS2314 (gate: `scope.has(name)` + `getTypeParamInfo(name).maxTotal>0` + providedCount≠maxTotal) and IS called from the ClassDeclaration heritage walk (14813) — so the FIRST investigation is WHY it doesn't fire for this `declare class xyz extends abc`: likely either (a) the recursive cycle (`abc.foo: xyz`, `xyz extends abc`) makes `getTypeParamInfo("abc")` return null/0, or (b) the ClassDeclaration branch in `checkUnresolvedNames` skips ambient `declare class` heritage. Add a marker-diagnostic (code=29999) to confirm which. The TS2351 half (construct-sig resolution through an invalid recursive generic base) is the harder piece — verify it cascades once the base is recognized as broken, else it needs separate construct-sig-invalidation. **(2) `duplicateErrorClassExpression`** (TS2416 + TS2538) — needs TS2538-for-`typeof Class`-index (extend B127's TS2538-object-index from `Type.Interface` to constructor-typed values) + TS2416 index-signature-VALUE-type override mismatch (`ComplicatedTypeDerived{[s]:ADerived}` not assignable to `ComplicatedTypeBase{[s]:ABase}` — ADerived missing `a`); the TS2416-index-sig-value-comparison is the hard piece (named→named blocked by canUseTypeEngine; may need the missing-property subset for index values). If BOTH 2-piece candidates prove too entangled, pivot to a Blocker: B100 utility-type (Readonly past the no-Reference gate), or Blocker #2 callback-RETURN inference for `arr.map(cb)`→`U[]` (broad, net-zero-prone — full-suite + HEAD-diff REQUIRED). Do NOT recon-only: pick ONE and build with a revert budget.

- [ ] **B100-UTILITYTYPE-MATERIALIZE (PROMOTED 2026-06-06 round 107 — contained type-engine blocker; backpointer: round-107 skip-log).** **ROUND-119 PROGRESS (2026-06-08, B120): `Readonly<T>` MATERIALIZATION LANDED (+1, flips `readonlyAssignmentInSubclassOfClassExpression`).** New `materializeModifierUtility` (Checker.kt ~59967) copies T's member Symbols + marks them readonly via the `mappedReadonlyMemberIds` side-channel (consulted by `isReadonlySymbol`), wired in `getTypeFromTypeReference` (`symbol==null && name=="Readonly"`, arity 1); same gates as B116 (concrete plain `Type.Object` T, no Reference/index-sigs). Companion pieces: `getTypeFromBaseTypeExpression` resolves `extends (X as new()=>T)` cast-mixin → construct-sig return type; `isReadonlyPropertyAccess` types `this.X` via `currentClassForThis` (now set by the readonly walker), gated to INHERITED members (own-readonly-in-ctor is legal). See CLAUDE.md B120 gotcha. **ROUND-120 FINDING (2026-06-08, B125 — ATTEMPTED + REVERTED, net −2):** implemented exactly the planned `Partial`/`Required` materialization — `materializeModifierUtility` extended to all three names, `mappedOptionalMemberIds`/`mappedRequiredMemberIds` side-channels consulted by `isOptionalProperty` (Required overrides → false, Partial → true), wired into `getTypeFromTypeReference`. Full suite: **ZERO flips, −2 regressions** (`deleteExpressionMustBeOptional_exactOptionalPropertyTypes` BOTH variants). Root cause of the regression: `isOptionalProperty` is consumed by the TS2790 "delete operand must be optional" check (and others) — making it side-channel-aware changed delete-expression behavior even though NO `Partial<>` was involved in those tests (the side-channel sets were empty for them, so this was likely an interaction with how the materialized objects flow, OR a subtler `isOptionalProperty` consumer). NET: **`Partial`/`Required` materialization has NO clean consumer in the current failing set AND the `isOptionalProperty` side-channel has FP surface — do NOT re-attempt without first finding a concrete single-test consumer AND auditing every `isOptionalProperty` caller.** The remaining B100 targets (`multipleBaseInterfaesWithIncompatibleProperties2` TS2320) need Partial + exactOptionalPropertyTypes value-type semantics + multi-base identity comparison (3+ pieces) — defer. **STILL TODO (lower priority): broadening `Readonly` past the no-Reference gate (named-interface / generic-instantiation args).** Original plan below. Materialize the homomorphic utility types **`Readonly<T>` / `Partial<T>` / `Required<T>` / `Pick<T,K>` / `Omit<T,K>`** as real mapped-type expansions that PRESERVE/modify member modifiers (readonly, optional). Today `getTypeFromMappedType` (Checker.kt:~83340) mints fresh member Symbols with NO declarations, so `isReadonlySymbol` (~36282, checks `symbol.declarations` for a Readonly-modified PropertyDeclaration) returns false → the readonly flag is lost. **Unblocks** ~~`omitTypeHelperModifiers01`~~ (ALREADY PASSES as of round 123 verification — B116 Omit-materialize + B120 readonly + B117 param-receiver already cover the TS2540 `x.c = …` via `Omit<A,'a'>`; round-107 skip-log was stale), `readonlyAssignmentInSubclassOfClassExpression` (TS2540 via `Readonly<{attrib}>` heritage-from-cast — also needs AsExpression base-type resolution in `getDeclaredTypeOfClassOrInterface`'s heritage path), and likely a broader TS2540/TS2353/TS2741 cluster. Carry a side-channel "is-readonly"/"is-optional" flag on the synthesized member Symbol (not via declarations) and consult it in `isReadonlySymbol` + the optional checks. RISK: MEDIUM-HIGH — mapped-type member synthesis feeds assignability broadly; full-suite + HEAD-diff each step. **ROUND-113 FINDING (2026-06-07): the TRUE first blocker is that `Omit`/`Pick`/`Exclude`/`Readonly`/`Partial`/`Required`/`Record` are ABSENT from the embedded lib (`BUILTIN_LIB_SOURCE`, grep confirms zero `type Pick`/`type Omit`/etc.).** So `Omit<A,'a'>` resolves to errorType/anyType → NOTHING materializes through `getTypeFromMappedType` → `x.c = …` on `omitTypeHelperModifiers01` emits nothing ("none produced", verified). The homomorphic-readonly-copy infra (a `!node.readonlyToken && node.type is IndexedAccessType` whose indexType names the mapped TP → copy `getPropertyOfType(objectType, key)`'s `isReadonlySymbol` onto a side-channel `mappedReadonlyMemberIds` set consulted by `isReadonlySymbol`) was WRITTEN + reverted this round (net-0, no consumer, FP-safe but unreachable — B87.5 no-zero-consumption precedent). So B100 is a 3-step blocker: **(step 1)** add the utility-type definitions to the embedded lib — a BROAD-RISK lib-content change (they currently resolve to anyType which SILENTLY suppresses many diagnostics corpus-wide; adding real defs may expose latent gaps → regressions; needs a dedicated full-suite-verified session); **(step 2)** verify `Omit`/`Pick` materialize through the alias-substitution → `getTypeFromMappedType` chain (keyof/Exclude key-enumeration); **(step 3)** re-apply the readonly-copy (and add the `?`/optional analog). The homomorphic gate (copy only when `!readonlyToken`, because the parser sets `readonlyToken` for `-readonly`/`+readonly`/plain alike — can't distinguish sign) is the load-bearing FP firewall for step 3. **ROUND-114 FINDING (2026-06-07): step 1 RE-SCOPED — adding the lib definitions is INERT, not broad-risk.** I added `type Partial<T>=…; type Readonly<T>=…; type Pick/Record/Exclude/Extract/Omit/Required/NonNullable` to `BUILTIN_LIB_SOURCE` and ran the full suite: **EXACTLY net-zero — zero flips AND zero regressions, identical 782-failing SET (verified by before/after failing-set diff, not just count).** Root cause: the utility-type names are intercepted UPSTREAM as **display-only stubs** at **Checker.kt:~27649** (a map `"Partial" to (1 to "Partial<T>")`, `"Readonly" to (1 to "Readonly<T>")`, `"Pick" to (2 to "Pick<T, K>")`, `"Omit" to (2 to "Omit<T, K>")`, …) consulted by `getTypeFromTypeReference` — they resolve to errorType/anyType with a synthetic DISPLAY string and NEVER consult any lib `type` alias. So the "broad-risk lib-content change" framing was WRONG: the definitions are harmless because they're unreachable. **The REAL step 1 is: change the stub map at 27649 to route these names to ACTUAL generic-type-alias resolution (the B50.1 `currentTypeAliasArgs` substitution path) against lib `type` definitions, THEN materialize the mapped/conditional bodies via `getTypeFromMappedType` (Partial/Readonly/Pick/Record/Required are mapped; Exclude/Extract/Omit/NonNullable are conditional + need keyof-enumeration).** THAT routing is where the regression risk actually lives (it makes `Partial<X>` a real type everywhere it's used, exposing mapped/conditional-resolution gaps). The lib definitions themselves were NOT committed (reverted — no-dead-infra rule). Also note `getTypeFromMappedType` is at Checker.kt:~83340 and there's an existing narrow TS2339 path for `g: Partial/Required/Readonly<T>` at Checker.kt:~74558 (16.4ed) to reconcile with.

- [ ] **B96-LIB-RISK (NOTED 2026-06-02 round 96 — HIGH-BLAST-RADIUS, defer): embedded `Promise<T>` member-set correction.** The embedded `Promise<T>` (Checker.kt:~26643) has `then/catch/finally` but lacks `[Symbol.toStringTag]`; the real ES2015 Promise (lib.es5 + lib.es2015.promise + lib.es2015.symbol.wellknown) member set differs. B94's property-`a` baseline line (`...missing the following properties from type 'Promise<number[]>': then, catch, [Symbol.toStringTag]`) is unreproducible without this. Editing the embedded lib shifts MANY Promise-related TS2322/TS2345 baselines → do NOT touch without a dedicated full-suite-verified session; treat as a lib-content change with broad regression surface. **Backpointer:** round-96 B94 deep-decomposition (the planning agent's "Substep E" prerequisite #2).

- [x] **B87.1 (CLOSED 2026-05-28 round 73, +2 net flips 9006 → 9008, flips both es5+es2015 variants of `doNotElaborateAssignabilityToTypeParameters_ts`). `Awaited<T>` + generic-union-return for async function bodies.** Solved via TWO landed pieces (B87.1b await-unwrap + a NARROW targeted async-generic-return emission), WITHOUT the broad (A) scope-push / (B) bare-TypeParam-relation changes originally feared. The narrow emission lives in `checkReturnAssignability` (Checker.kt:~49373): gated on `inAsyncFunctionBody` + `Promise<bareTP>` return annotation (TP name ∈ `typeParams`, no inner type args) + source is a `Type.Union` with ≥1 unconstrained-`Type.TypeParam` member named the TP AND ≥1 non-TypeParam non-any/error member; emits TS2322 at the `return` keyword (len 6) + the "could be instantiated with an arbitrary type" messageChain; display = non-TP members first then each TP member wrapped `Awaited<name>` → `Yadda | Awaited<T>`. Zero regressions (FP-surface tiny by construction). B87.1c's (A)/(B)/(C) pieces are now moot for this test (left documented below for any future async-generic test needing the GENERAL relation/scope fix). Target `doNotElaborateAssignabilityToTypeParameters_ts` (`async function foo<T>(x: T): Promise<T> { let yaddable = await getXOrYadda(x); return yaddable; }` where `declare function getXOrYadda<T>(x: T): T | Yadda`). Expected: TS2322 @ (3,3) on `return yaddable` — `Type 'Yadda | Awaited<T>' is not assignable to type 'T'.` + the EXISTING "'T' could be instantiated with an arbitrary type which could be unrelated to 'Yadda | Awaited<T>'." elaboration (17.11a infra). We currently emit NOTHING. Root-cause trace needed but the chain is: (1) `getXOrYadda(x)` must infer inner-T = outer-T and resolve to `T | Yadda` (NOT anyType — verify whether `getReturnTypeOfCallExpression` collapses the union-return to any when the arg is a bare TypeParam); (2) `await (T | Yadda)` must produce `Awaited<T> | Yadda` — `Awaited` is registered as a known name (Checker.kt:23563, `"Awaited" to (1 to "Awaited<T>")`) but NOT resolved as a conditional type; for a bare-TP operand TypeScript displays `Awaited<T>` literally (does NOT expand), so the await-transform need only wrap the non-promise-like bare-TP part as `Awaited<T>` and leave `Yadda` as-is; (3) the async return-assignability check (`Yadda | Awaited<T>` vs unwrapped return `T`) must fire TS2322 + the could-be-instantiated elaboration. **Decomposition** (land each net-zero/+flip independently, full-suite verify):
  - [x] **B87.1a (TRACE DONE 2026-05-28 round 73, net-zero — no code)** — temporary debug at the `checkReturnAssignability` type-engine gate (Checker.kt:~49392) on `*doNotElaborateAssign*es2015*` printed: `src=any[Intrinsic] tgt=Promise<T>[Interface] canUse=false relToTgt=true awaited=null`. **Findings (the gap is NOT one-line — it is 4+ coupled pieces):** (i) `sourceType` of `yaddable` resolves to **anyType** — `getXOrYadda(x)` (`<T>(x:T): T | Yadda`) generic-union-return inference collapses to `any` when the arg is a bare TypeParam, and `await` keeps it `any`; (ii) `targetType` = `Promise<T>` resolves to a bare **`Type.Interface`**, NOT a `Type.Reference` carrying the type arg `T`, so the EXISTING async-unwrap branch (Checker.kt:~49398, gated `targetType is Type.Reference && symbol.name=="Promise"`) never fires (awaited=null); (iii) consequently `canUseTypeEngine(any, ...)` is false → no diagnostic. So B87.1b must FIRST fix (i) the union-return inference AND likely (ii) make `getTypeFromTypeNode(Promise<T>)` produce a `Type.Reference`; THEN (iv) the residual relation question — `checkTypeRelatedTo(Yadda | Awaited<T>, bare T)` returns `true` today (bare unconstrained TypeParam target has apparent type `{}` → everything assignable), but TypeScript wants it FALSE with the "could be instantiated with an arbitrary type" elaboration (17.11a infra exists). Per the decision rule (multi-piece, no one-line gap) NO code landed; the trace re-scopes B87.1b/c below. **DO-NOT** broadly change the bare-TypeParam-target relation result — it would cascade through every internal generic-function assignment; the fix must be a narrow gate at the async-generic-return site only.
  - [~] **B87.1b (PARTIAL — await-unwrap LANDED 2026-05-28 round 73, net-zero, zero regressions; union-return inference still TODO).** Two sub-gaps remain from the B87.1a trace; the FIRST is now closed: `getTypeOfExpression(AwaitExpression)` was hardcoded `anyType` (Checker.kt:52636 `// TODO: unwrap Promise`). Replaced with: unwrap one level `Promise<X> -> X` (Type.Reference to Promise), non-Promise operands await to their own type, bare/unknown operands stay anyType (prior behavior, avoids cascade). Full suite 9006 unchanged (net-zero across the broad change — most async tests already tolerate the more-precise await type). This alone does NOT move `doNotElaborateAssignabilityToTypeParameters` because the operand `getXOrYadda(x)` STILL resolves to anyType (the union-return inference gap below). **REMAINING B87.1b:** preserve the `T | Yadda` union-return from `getXOrYadda<T>(x:T): T | Yadda` instead of collapsing to any when the arg is a bare TypeParam — find where `getReturnTypeOfCallExpression` (Checker.kt:54201) loses the union for a declared generic fn called with a bare-TP arg. RISK: MEDIUM (generic-union-return inference is consulted broadly — verify no TS2322/TS2345 cascade).
  - [ ] **B87.1c (re-scoped 2026-05-28 round 73 after the await-unwrap fix advanced the source).** Post-await-fix re-trace at the return site shows `src=T | Yadda[Union]`, `tgt=Promise<T>[Interface]`, `canUse=false`, `relToTgt=false`. So the SOURCE is now correct (only the `Awaited<T>` display differs — TypeScript shows `Yadda | Awaited<T>`). The remaining blockers are now precisely THREE coupled high-risk pieces:
    - **(A) `Promise<T>` resolves to a bare `Type.Interface`, not a `Type.Reference` carrying `T`.** Root cause: when `checkReturnAssignability` calls `getTypeFromTypeNode(returnTypeNode = Promise<T>)`, the function's own type param `T` is NOT in `currentTypeParamScope`, so `T` → errorType and the `Type.Reference` creation is skipped by the errorType guard (CLAUDE.md "getTypeFromTypeReference errorType guard"). Fix would push the enclosing function's `typeParameters` into scope before resolving the return-type node. RISK: HIGH — makes `Promise<T>` (and every other generic return annotation) a Reference broadly; cascades through return-assignability across the suite.
    - **(B) assign-to-bare-TypeParam must FAIL.** Even with (A), the async-unwrap branch would compare `T | Yadda` vs awaited `T`; `checkTypeRelatedTo(Yadda, bare T)` returns `true` today (apparent type `{}`), so no diagnostic. TypeScript wants FALSE + the "'T' could be instantiated with an arbitrary type" elaboration (17.11a infra exists). RISK: HIGH and explicitly DO-NOT broadly — cascades through every internal generic-function assignment. Needs a narrow gate at the async-generic-return site only.
    - **(C) `Awaited<T>` display** — render the bare-TP constituent of the awaited union as `Awaited<T>` (the name is registered at Checker.kt:23563) for the TS2322 message. Cosmetic; only matters once (A)+(B) emit.
    - **Conclusion:** B87.1's remaining work is (A)+(B)+(C), all coupled and (A)/(B) broad-cascade-risky. NOT a bounded surgical fix; needs a dedicated session that can absorb the full-suite regression triage from the scope + relation changes. The await-unwrap piece (B87.1b) was the one safely-landable increment.
  - **Sibling tests**: any async generic function whose body returns a value that doesn't structurally match the bare return TypeParam. Survey post-B87.1a.
  - **Backpointer**: round-72 STATUS "next-session: Blocker #2" recommendation; round-73 confirmed the surgical pool dry and B83.4 closed, so this is the promoted next concrete Blocker substep per the anti-loop rule.

### Completed items (16.0–16.3) — archived

Moved to `PLAN-PHASE-4-HISTORY.md` (2026-06-10) — § "Completed items (16.0–16.3)".

### Skip log (explored-but-skipped tests) — moved

The live skip log (`### Explored-but-skipped tests`, the section `find_candidates.py` reads its
`[SKIP]` token set from) now lives in `PLAN-PHASE-4-HISTORY.md`. Add new skip entries THERE,
above the `### End of skip log` terminator heading; suffix entries with `_ts`. The script reads
both files and unions the tokens, so entries in either file work.

### What's left in the "surgical fix" pool

**As of 2026-05-31 (post-round-85, 9098 passing) — UPDATED**: Round 84 (+16) and round 85 (+6) worked the bounded NONE/EXTRA/SWAP surgical tail across module-resolution, index-signature, readonly, and tslib-helper diagnostics. Round 85 landed B98.r20 (TS2413 number-vs-string index value), r21/r21b (TS7016 + untyped-JS-sibling TS2307 suppression), r24 (TS2540 intersection-property writes), r25 (TS2430 multi-base inherited index-sig), r26 (TS2343 `__awaiter`). **The bounded surgical tail is now genuinely exhausted** — the round-85 triage (logged under "Explored-but-skipped → Round 85 triage skips") classified every remaining ≤2-error NONE candidate as needing one of: control-flow narrowing (Blocker #1: TS2488/TS2532/TS18048 `never`/possibly-undefined), cross-file/namespace scope (Blocker #3: TS2694/TS2503/TS2300-merge/TS2451-merge), type-engine assignability/generic-inference (TS2322 ×91 / TS2345 ×37 / TS2339 ×37 / TS2741 / TS2403), mapped-type-modifier resolution (TS2540 Omit/Readonly), module-resolution modeling (bare/symlink/untyped-module/typeRoots TS2307/TS2688 — bare-missing TS2307 ATTEMPTED round 85, reverted -6), decl-emit portability (TS2883/TS4032), or the FP-prone namespace-merge cross-block duplicate (`cloduleWithDuplicateMember2`). **Next session: commit to an architectural blocker substep** (Blocker #1 control-flow narrowing has the highest yield; Blocker #2 generic inference and Blocker #3 cross-file scope are the other two). Do NOT spend a session re-scanning the NONE tail expecting bounded wins — they are documented as exhausted.

**As of 2026-05-18 (post-B47.7, 8815 passing) — UPDATED**: After the B47.x series (MAINT-2 + B47.1–B47.7 + B47.5 foundation + B47.6 two-piece), the surgical pool at +1-per-commit is truly exhausted at the errors-baseline level (`find_candidates.py --fresh` returns 0/0/0). The JS-emit candidates pool has 108 entries; cataloguing in PLAN-PHASE-4.md confirms all are feature-scale (parser-recovery, private fields downleveling, `__setFunctionName`, `__rest` for CJS export-rest, type-display normalization for nullable targets, conditional type resolution, namespace-qualified union display, lib subsetting, module/file-ordering architectural blocker). Recent session B47.x landed +7 tests via narrow defensive-emit patterns; further +1 wins require feature-scale work or completing architectural blockers. Recommended next: tackle blocker substep per the architectural-blockers section.

**As of 2026-04-25 (post-16.4gs, 8337 passing) — RECOMMENDED SHIFT**: After 6 consecutive sessions confirming the surgical pool is empty (16.4go, post-16.4gk recon, post-16.4gl recon, 16.4gp, 16.4gq, post-16.4gr) plus this session's exhaustive re-audit of skip-list entries, the surgical pool is genuinely exhausted at the +1 to +3 per-commit level. **Next sessions should commit entirely to architectural blockers** per the new priority ordering (see "Known architectural blockers" below — reshuffled 2026-04-25). The recommended next step is **MAINT-1 (stale skip-log audit)**, then **Blocker #1 (full control flow narrowing)** — the latter has the highest single-feature yield estimate (~60–100 tests realistic, 250 upper bound).

**As of 2026-04-25 (post-16.4gi, 8323 passing)**: `find_candidates.py --fresh` returns 1 SWAP candidate (`newMap_ts`) which requires a multi-piece fix (TS2743 emission + lib-version-aware MapConstructor/WeakMapConstructor declarations). All other candidates are filtered as already-skipped. **Re-examining previously-skipped tests after each round of infrastructure changes is the highest-yield strategy now**: 16.4gi flipped ~~`typeCheckingInsideFunctionExpressionInArray_ts`~~ because 16.4ge's Variable+Interface merge made `Object` resolve as a real Type.Interface, which then revealed that `getReturnTypeOfNewExpression`'s Type.Interface branch returned `calleeType` directly (instead of consulting construct sigs for the "constructor interface" pattern). The same merge unblocks several other tests partially — e.g. ~~`booleanAssignment_ts`~~ now correctly types `b: Boolean` (after 16.4gi's `new X()` fix) but still has 2 FPs (primitive→wrapper assignability) + 1 missing chain (valueOf elaboration). Continuing to re-examine the skip log against the latest infrastructure is the recommended workflow until a coordinated multi-piece fix or blocker is undertaken.

**As of 2026-04-24 (post-16.4fs, 8304 passing)**: `find_candidates.py --fresh` still returns 0 across all buckets at diff ≤ 3 (filtered from 9/118/32). The 16.4fs win came from re-visiting a previously-skipped test (`pathMappingBasedModuleResolution_withExtension_failedLookup`) and landing a narrow TS2307 branch gated on "every paths target has an explicit `.ts`/`.tsx`/`.d.ts` extension" — narrower than the broad "paths config → skip TS2307" originally used. Each previously-skipped test is worth re-examining when its skip reason names a specific resolver limitation that a narrower gate could bypass. The 16.4fr win came from a non-obvious extension of an existing emission site (TS6212 branch) to cover an adjacent pattern, not from a fresh candidate — the tests it flipped (`optionalParamAssignmentCompat`, `functionSignatureAssignmentCompat1`) had 3-gap diffs (position + display + related info) that all resolved cleanly together. Similar "stacked gap" wins may still exist when multiple narrow fixes coincide. Otherwise next gains require either a blocker #1 sub-step (return-type-from-body broader patterns, or named→named cross-target comparison) or a coordinated new-diagnostic family implementation (TS6213 "Did you mean to use new?" at var-init, rootDir validation, iterator-protocol typing for TS2802/TS2495/TS2488).

---

After 2026-04-19 (16.4dm, 8227 passing — net-zero infra commit on top of 16.4dl), the low-hanging pool is empty. The 16.4di–dl line landed targeted wins on the inheritance-chain side of blocker #1 (generic property chain walk + TS2416 class-type-param scope + private-brand elaboration for TS2322/TS2345 + apparent-type constraint check for TS2344). 16.4dm extended step (c) to cover primitive-literal returns and added function-mismatch chain lines to property elaboration — both correct improvements that gate on per-property TS2322 emission to produce visible test gains. `find_candidates.py --fresh` still returns ~20 candidates; **every one remaining** is classified as a blocker or feature gap:
- Generic inference (Blocker #1): widenToAny1/2, declarationEmitBundleWithAmbientReferences, recursiveTypeRelations, lambdaArgCrash
- Cross-file scope (Blocker #5): jsFileCompilationLetDeclarationOrder2, jsFileCompilationDuplicateVariableErrorReported
- JSDoc (Blocker #2): jsExportMemberMergedWithModuleAugmentation, jsFunctionWithPrototypeNoErrorTruncationNoCrash
- Conditional types: conditionalAnyCheckTypePicksBothBranches
- Control-flow narrowing: narrowingUnionToNeverAssigment, optionalChainWithInstantiationExpression1 (es2019/es2020)
- Intersection privacy reduction: intersectionWithConflictingPrivates
- Display: errorWithSameNameType (cross-module qualified), deeplyNestedAssignabilityErrorsCombined (typeof-of-class-value), crashInEmitTokenWithComment (destructured arrow params)
- TS2859 complexity budget: relationComplexityError
- Standalone new diagnostic: importAliasFromNamespace (TS2845), taggedTemplatesWithIncompleteTemplateExpressions3 (TS2345 for tagged templates), importHelpersWithLocalCollisions (TS2354 for AMD/System tslib resolution)
- Structural elaboration: optionalPropertiesTest (optional→required), implementsIncorrectlyNoAssertion (TS2416 with intersection-of-classes)

The next +N gains either come from:
1. **Implementing new diagnostics** (TS6234 / TS2351 / TS2352 / TS2300-vs-lib / TS7023 / TS2802 / TS2744 / TS2845 / TS4081 / TS2354-AMD): each is 1-3 tests on its own but together could net +15-20. Each is self-contained (contained to a specific emission site) but individually small.
2. **Taking on a blocker** (structural generic comparison #1 for ~30+ tests, JSDoc #2 for ~5-10, etc.). Blocker #1 is by far the highest-yield single investment.

**Recommendation**: surgical pool is **exhausted**. Next session should commit to either (a) a new-diagnostic batch (pick 2-3 low-risk ones in a single session), or (b) blocker #1 (structural generic comparison) with the retry plan below. Continuing to look for +1 surgical wins via `find_candidates.py --fresh` will yield nothing.

### Known architectural blockers (RESHUFFLED 2026-04-25, 8337 passing)

These blockers recur across multiple "close-to-passing" tests and cannot be fixed with surgical changes in a single session. Any agent attempting these should plan for a multi-session investigation with regression budget.

**Priority ordering** — ranked by (expected tests unblocked) ÷ (regression risk × refactor scope). Higher rank = better cost/benefit. Reshuffled after the 16.4gq–gs session: the surgical pool has been ground down to zero across 5+ sessions, so the path forward is committing entire sessions to architectural work. Per the original Phase 16 impact analysis (line ~2479), the largest unaddressed feature is **control flow narrowing** (~250 tests potential, only partial done in 16.3) — it now sits at #1.

**What changed in this reshuffle:**
- **NEW #1**: Full control flow narrowing (was 16.3 PARTIAL — promoted to architectural blocker since the remaining work is full flow-graph construction, not surgical).
- **NEW #2**: Generic argument inference (was implicit under "16.4 generic instantiation" — promoted because tests like `genericConstraintSatisfaction1`, `superCallArgsMustMatch`, `genericClassWithStaticFactory`, `superWithTypeArgument3`, `privacyCheckAnonymousFunctionParameter2` all hit the same gap: we don't infer `T` from arg type then check the constraint).
- **#3 (was #5)**: Cross-file global scope conflation — bumped UP. Each session that does skip-log auditing finds more tests gated on this; estimated impact revised upward.
- **#4 (was #1)**: Structural comparison of generic refs — DEMOTED to LOW yield. Steps (a)–(d) infrastructure landed; the remaining gaps are bounded (different-target TS2352, overload-aware TS2416 for lib types, multi-statement return-from-body inference) and total ≤ 10 tests across all sub-cases.
- **#5–#7**: JSDoc, TS7006 over-suppression, Parser error-recovery — unchanged ordering, all LOW yield.
- **NEW MAINT-1**: Stale skip-log audit (16.4gp/gr/gs sessions all found stale entries — running this once could surface +5–15 candidates that are already passing).

---

#### 1. Full control flow narrowing — HIGHEST yield (~100+ tests realistic), MEDIUM risk

**Status (2026-04-25):** 16.3 landed surgical narrowing fixes (TS1344 message, TS2739/TS2740 multi-missing, null/undefined narrowing in `if (x == null)` branches, narrowing helper for variable declarations). Full flow-graph construction was deferred. The remaining gap is the dominant blocker for TS2339 (549 tests with this code), TS2454 (103 deficit), TS2774 (118 tests).

**Failing test patterns that need this:**
- ~~`nestedLoopTypeGuards_ts`~~ flipped 17.30a (flow-graph TS2454 body-recursion)
- ~~`typeGuardConstructorDerivedClass_ts`~~ flipped 17.5a (`x.constructor === Class` narrowing)
- ~~`narrowingUnionToNeverAssigment_ts`~~ flipped 17.1b (var-decl `never` target narrowing)
- ~~`narrowByEquality_ts`~~ (TS2839 narrowing — partially done in 16.4fy but limited)
- ~~`optionalChainWithInstantiationExpression1_ts`~~ flipped 17.44 (synthetic-paren marker for instantiation-expr-breaks-chain pattern)
- Many TS2774 "always defined" / TS2454 "used before assigned" with non-trivial control flow.

- **Yield**: 60–100 tests realistic (250 upper bound from impact analysis × ~30% realistic factor).
- **Scope**: build a real flow graph during binding (per-function basic blocks + branch edges), reify narrowed types per AST position. tsgo's `internal/checker/flow.go` (2.7K lines) is the reference implementation. This is INFRASTRUCTURE work — expect 2–4 sessions to land safely.
- **Risk**: MEDIUM. Narrowing is opt-in (a position with no narrow info falls back to declared type), so regressions are bounded to over-narrowing (false NEVER) rather than wholesale changes.
- **Why first now**: highest absolute yield. The original Phase 16 ordering put 16.3 fourth ("orthogonal but complex; defer to after easier wins") but the easier wins are now exhausted — narrowing IS the next big unlock.

#### 2. Generic argument inference (NEW) — HIGH yield (~20–40 tests), MEDIUM risk

When calling a generic function with concrete args, TypeScript infers each type parameter from the corresponding argument type, then checks each inferred T against its constraint. Currently we punt on this entirely — generic call sites with type-param-typed args either pass trivially (T = anyType) or skip the assignability check.

**Failing test patterns that need this:**
- ~~`genericConstraintSatisfaction1_ts`~~: `f<T extends S>(x: T)` called with `{s: 1}` against `S = {s: string}` — should emit TS2322 + TS6500.
- ~~`genericClassWithStaticFactory_ts`~~: `T` not inferred from arg → no TS2345.
- ~~`superCallArgsMustMatch_ts`~~, ~~`superWithTypeArgument3_ts`~~: `super(...)` arg checking when base is generic.
- ~~`privacyCheckAnonymousFunctionParameter2_ts`~~: function-typed parameter not structurally compared.
- ~~`widenToAny1_ts`~~, ~~`widenToAny2_ts`~~: `string | undefined` initializer widened wrong against `number` annotation.

- **Yield**: 20–40 tests. Each individual test gives +1, but the pattern repeats across many test files.
- **Scope**: extend `checkArgumentsAgainstSignature` and `checkArgumentsAgainstOverloads` to: (a) collect inferences from arg→param matching (each TypeParam → concrete type), (b) apply constraint check per TypeParam, (c) substitute inferred types into the param type and recheck. Touches the existing overload-resolution and constraint-checking paths.
- **Risk**: MEDIUM. Could regress overload-resolution tests if inference picks wrong overload. Mitigate with conservative gating (only when ALL TypeParams resolve to non-error types).
- **Why second**: stacks naturally on Blocker #4 step (a)/(b)/(c)/(d) infrastructure (Type.Reference interning, `resolveGenericPropertyType` mapper, `getPropertyTypeForRelation` cache) — the building blocks are already there.

#### 3. Cross-file global scope conflation — MEDIUM-HIGH yield (~30+ tests, was MEDIUM), HIGHEST risk

`Checker.init` does `mergeSymbolTable(globals, result.locals)` for every binderResult, merging module-file exports into the shared `globals` map alongside script-file locals and lib. In module files, identifiers from OTHER module files that weren't imported appear in `scope.has(name)` as if they were globals.

Blocks fine-grained module-visibility diagnostics: TS2301 vs TS2663 distinction, default-import-from-export-equals visibility, namespace export filtering across files.

**Failing test patterns:**
- ~~`classMemberInitializerWithLamdaScoping4_ts`~~ (TS2301 vs TS2663)
- `moduleAugmentationsImports3/4_ts` (TS2339 FP on nested module augmentation)
- ~~`errorsOnImportedSymbol_ts`~~ (cross-file type-only flagging)
- ~~`jsFileCompilationLetDeclarationOrder2_ts`~~, ~~`jsFileCompilationDuplicateVariableErrorReported_ts`~~
- Possibly contributes to many `genericSpecializations*`, `arrayAssignmentTest*` cross-namespace cases not yet diagnosed.

- **Yield**: 30+ tests. Each skip-log audit session surfaces more candidates blocked here.
- **Scope**: split "true globals" (script-file locals + lib + KNOWN_GLOBALS) from per-file module locals; reconstruct `fileScope` to only include imports the current file actually declares. Touches **every identifier-resolution check** in the checker (~30 call sites).
- **Risk**: HIGHEST. A naive implementation regresses 50+ tests. Recommended approach: introduce per-file-scope construction behind a flag, run side-by-side, then flip per file as the scope construction proves correct.
- **Why third (bumped from last)**: yield estimate revised upward after multiple skip-log audits found this blocker named in the residual; without addressing it, ~30 surgical-looking tests remain stuck.

#### 4. Structural comparison of generic type references — LOW yield (was MEDIUM), MEDIUM risk

**Status (2026-04-25, 8337 passing):** Steps (a), (b), (c) partial, (d) partial all landed across 16.4dc–df, dg, dm, gr, gs. Type.Reference interning, cycle/depth heuristic, TypeParam apparent-type comparison, `resolvedPropertyTypes` cache, heritage-clause type-arg instantiation, base-chain inheritance via `resolveGenericPropertyType`, parameter-mismatch chain elaboration, base-method TypeParam TS2208 — all in place.

**What's left (small, gated by other blockers):**
- ~~Different-target TS2352 with "Property X missing" elaboration (`genericTypeAssertions2_ts`)~~ — flipped 17.28 (narrow array-source → class-target case). Generalizing to all different-target casts still requires a TypeScript-style `comparable` relation with full structural elaboration.
- Overload-aware TS2416 for lib generic interfaces (`implementArrayInterface_ts` emits 2 extra diagnostics because lib's `every`/`filter` have type-predicate overloads that user code only implements one of).
- ~~Multi-statement bodies with single return for `inferSimpleReturnTypeFromBody`.~~ **Closed B4.1 (2026-05-13, +0 foundation)** — helper at Checker.kt:39694 now handles multi-statement bodies via top-level ReturnStatement collection.
- ~~Identifier returns (`return param`, `return this.field`) for return-type-from-body inference.~~ **Closed B4.2 + B4.3 (2026-05-13, +0 foundation)** — `is Identifier` arm extended to `return paramName` for annotated params; new `is PropertyAccessExpression` arm handles `return this.field` for class-property-typed fields (new `enclosingClass: Type.Interface?` parameter threaded from `resolveGenericPropertyType` caller at Checker.kt:39625).

- **Yield**: ≤ 10 tests across all sub-cases.
- **Scope**: contained to checker (no binder/parser changes).
- **Why demoted**: infrastructure work is largely done; remaining test gains are individual-test scale, not pattern-level. Best handled opportunistically when a specific case aligns with surgical session's budget.

#### 5. JSDoc type annotations — LOW yield (~5–10 tests), LOW risk

`@type {T}`, `@this {T}`, `@typedef`, `@param {T}` — we bind `@typedef` nominally but don't honor `@type` assertions or `@this` parameter types.

- **Yield**: 5–10 `.js` file tests.
- **Scope**: JSDoc parsing (scanner comment handling) and a new JSDoc-to-TypeNode bridge in the checker. Does not touch non-JS checking paths.
- **Why fifth (was second)**: low blast radius means low risk, but absolute yield is small. Higher-yield blockers (#1, #2, #3) take priority for "session-scale architectural work" budget.

#### 6. TS7006 over-suppression for callback parameters — LOW yield (~3–5 tests), MEDIUM risk

We suppress TS7006 for any `contextuallyTyped=true` param. TypeScript only suppresses when the contextual type actually provides param types (bidirectional inference succeeds).

- **Yield**: 3–5 tests (`intraBindingPattern`, `subtypeReduction`, `signatureCombiningRestParameters`).
- **Scope**: requires bidirectional inference plumbing in contextual typing.
- **Why sixth**: low yield + needs new infrastructure. Defer.

#### 7. Parser error-recovery asymmetry — LOW yield (1–2 per case), HIGH risk per attempt

Several tests expect specific token-consumption-then-continue recovery (e.g. `declare class foo();` → class with empty body + phantom `()` statement + next function).

- **Yield**: 1–2 tests per case, case-by-case.
- **Scope**: per-case parser edits.
- **Why last**: per-case work, no multiplier, regression-prone. Best handled opportunistically.

---

#### MAINT-1. Stale skip-log audit — UNKNOWN yield (likely 5–15 tests), LOW risk

Every recent session that audited the skip log found 1–3 entries marked "stuck on Blocker #X" that have actually been passing for sessions (e.g. 16.4gl-recon found `genericCloneReturnTypes`/`genericCloneReturnTypes2` stale; 16.4go found `generics4`/`genericDerivedTypeWithSpecializedBase`/`genericPrototypeProperty3` stale; this session found `genericPrototypeProperty3` again). The cumulative impact of stale entries blocking the candidate finder is probably 5–15 tests.

- **Yield**: unknown but likely 5–15 candidates that re-surface as fresh.
- **Scope**: walk every entry in the "Explored-but-skipped tests" section, run the test, mark stale (~), update reason if still failing.
- **Risk**: zero (read-only investigation).
- **Why a maintenance task**: not architectural but high-leverage. Should be run as a one-off "audit session" before any session that uses `find_candidates.py --fresh` for surgical work, or after every major architectural fix lands.

---

**Parallelism note**: Blockers #1, #2, #5, #7 can be worked independently. #3 should run after #1/#2 have resolved their fraction of overlapping cases (clearer "remaining failures pointing to #3" set). #4's remaining sub-cases pair naturally with surgical sessions — not a full-session investment. #6 depends on enough contextual-typing infrastructure that #2's inference work should land first.

**Recommended next sessions (in order):**
1. **MAINT-1** (1 session): clean up stale skip log → frees `find_candidates.py --fresh` to find genuine candidates.
2. **Blocker #1 step 1** (1–2 sessions): build flow-graph infra in binder; no behavior change yet.
3. **Blocker #1 step 2** (2 sessions): wire flow-graph into TS2454/TS2339/TS2774 emission sites.
4. **Blocker #2** (2 sessions): generic argument inference + constraint check.
5. **Blocker #3** (3+ sessions): per-file scope construction behind a flag, then per-file flip.

### Candidate-picking workflow for surgical fixes

When context starts a new session, the fastest path to wins is:

1. **Run full suite once** (4-6 min) to produce `build/test-results/jvmTest/*.xml`.
2. **Python-parse the XMLs** to find tests by diff size and pattern. The key filter is 1-2 extra diagnostic lines (too-aggressive checks) or 1 missing diagnostic at a specific position (simple checks to add). Example snippets live in this file's history — reuse them.
3. **Look for code-swap tests** (expected TS####A at position P vs actual TS####B at same position P) — these are often single diagnostic emissions at a specific code path that just need the code changed or position adjusted.
4. **Skip**: tests needing generic variance, module-visibility, JSDoc type handling, or full type inference — those are the architectural blockers above.
5. **Inspect `typescript-repo/tests/cases/compiler/<name>.ts` + `typescript-repo/tests/baselines/reference/<name>.errors.txt`** for context before implementing. Don't just look at the diff.

### Queue execution strategy

**Original Phase 16 sequence (DONE/IN PROGRESS):** 16.0 → 16.1 → 16.2 → 16.3 → 16.4

**Status:** 16.0/16.1/16.2 DONE, 16.3 PARTIAL (full flow-graph deferred → now Blocker #1), 16.4 IN PROGRESS (generic infrastructure largely done; remaining sub-cases moved to Blocker #4).

**NEW sequence (2026-04-25 reshuffle) — as the surgical pool is exhausted, commit entire sessions to architectural blockers:**

**Phase 17 sequence:** MAINT-1 → Blocker #1 (control flow narrowing) → Blocker #2 (generic argument inference) → Blocker #3 (cross-file scope) → Blocker #4 / #5 / #6 / #7 opportunistically.

**Rationale (priority-by-yield × tractability):**
1. **MAINT-1 first** — stale skip-log audit. Read-only, ~1 session, surfaces 5–15 already-passing tests as fresh candidates that would otherwise stay hidden behind `[SKIP]` markers in `find_candidates.py`. Cheap, no regression risk, and clears the deck for #1's diagnosis.
2. **Blocker #1 (control flow narrowing) second** — highest absolute yield (~60–100 tests realistic). The remaining failing tests across many categories (TS2339, TS2454, TS2774, TS2839) all converge on this. Build flow-graph in binder, wire into emitters in two stages.
3. **Blocker #2 (generic argument inference) third** — stacks on the 16.4 generic infrastructure already in place. ~20–40 tests. Lower risk than Blocker #3.
4. **Blocker #3 (cross-file scope) fourth** — high yield but highest risk; defer until #1/#2 have cleared their fraction so the residual is well-characterized.
5. **Blocker #4 (remaining structural generic gaps) opportunistically** — small (~10 tests), pairs with surgical sessions, not full-session work.
6. **Blockers #5/#6/#7** — low yield, defer.

**Hard rules:**
- Each feature must land behind a feature flag or with conservative guards initially.
- Run full suite after EACH sub-step to catch regressions early.
- If a feature causes >20 regressions, add more guards or narrow scope; don't revert without analysis.
- Document each partial implementation in CLAUDE.md gotchas.

**Per-session throughput — fix multiple items per session.** A single sub-step (e.g. 16.4h, 16.4i) is the unit of commit, not the unit of session. After committing + pushing one sub-step, loop back to pick up the next unchecked sub-step. Keep going until (a) the queue is empty, (b) the next item is genuinely blocked, or (c) context/time budget is nearly exhausted — then wrap up with a summary. Two-to-four sub-steps per session is a reasonable target when the items are small (+1 to +5 tests each); larger infrastructure items may consume a full session on their own. Always finish each sub-step cleanly (test suite green, commit + push) before starting the next — never bundle unrelated work into one commit.

**Realistic total gain:** 500-800 new passing tests → ~85-88% pass rate.

---

## Reference

- **tsgo source**: `github.com/microsoft/typescript-go` — `internal/checker/`
- **TS checker**: `microsoft/TypeScript` — `src/compiler/checker.ts` (53,296 lines)
- **Key tsgo files**: `checker.go` (31K), `relater.go` (5K), `types.go` (1.3K), `flow.go` (2.7K), `inference.go` (1.6K)
- **Parallelism model**: `internal/compiler/checkerpool.go` — N independent checkers, round-robin file assignment, shared immutable AST
