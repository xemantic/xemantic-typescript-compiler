# Phase 4 — Structural Type Checker

**Status (2026-04-17):** 8,108 / 10,078 tests passing (80.4%). Active queue: **Phase 16 — Fundamental Type System Features**. 16.0 done, 16.1 done, 16.2 done, 16.3 partial (+14 tests), 16.4 in progress (+32 tests, 1967 remaining).

## Goal

Implement structural type checking to unlock the ~2,400 remaining test failures.
Design for future parallel checking using Kotlin coroutines (inspired by tsgo's goroutine model).

## Architecture (inspired by tsgo)

### Key design decisions

**1. Type hierarchy — Kotlin sealed classes**

Replace the current string-based type representation (`"number"`, `"@MyType"`, `"|A | B"`)
with a proper `Type` sealed class hierarchy. Kotlin sealed classes give us exhaustive
`when` matching and smart casts — more idiomatic than tsgo's `TypeData` interface approach.

```kotlin
sealed class Type {
    abstract val flags: TypeFlags
    val id: Int = nextId++  // per-checker unique ID
}

class IntrinsicType(override val flags: TypeFlags, val name: String) : Type()
class LiteralType(override val flags: TypeFlags, val value: Any) : Type()  // string/number/boolean
class ObjectType(override val flags: TypeFlags) : Type() {
    var members: SymbolTable? = null       // lazily resolved
    var properties: List<Symbol>? = null
    var callSignatures: List<Signature>? = null
    var constructSignatures: List<Signature>? = null
}
class InterfaceType(...) : ObjectType(...)  // adds typeParameters, baseTypes
class TypeReference(...) : ObjectType(...)  // adds target (generic), resolvedTypeArguments
class UnionType(val types: List<Type>) : Type()
class IntersectionType(val types: List<Type>) : Type()
class TypeParameter(...) : Type()  // adds constraint, default
// ... etc
```

**2. Parallelism preparation — LinkStore pattern**

tsgo's key insight: checker-local metadata stored in side maps, not on shared AST nodes.
We prepare for this from the start:

```kotlin
class Checker(...) {
    // Checker-local type cache — NOT on AST nodes
    private val nodeTypes = HashMap<Long, Type>()        // nodeKey → resolved type
    private val symbolTypes = HashMap<Int, Type>()       // symbol.id → resolved type
    private val declaredTypes = HashMap<Int, Type>()     // symbol.id → declared type
}
```

When we later create N parallel Checker instances, each will have its own maps.
The shared AST and binder output remain immutable.

**3. Lazy type resolution**

Types are resolved on-demand (matching both TS and tsgo):
- `getTypeOfSymbol(symbol)` — checks `symbolTypes` cache, computes if absent
- `getTypeFromTypeNode(node)` — checks `nodeTypes` cache, computes if absent
- `resolveStructuredTypeMembers(type)` — resolves members lazily on ObjectType

**4. Relation engine**

Structural comparison via `isTypeRelatedTo` → `structuredTypeRelatedTo` →
`propertiesRelatedTo`. Cached in relation maps keyed by `(source.id, target.id)`.

```kotlin
enum class Ternary { True, Maybe, False }

class Relation {
    private val cache = HashMap<Long, Ternary>()  // pack two Int IDs into Long
}

// Five relation instances (same as TS/tsgo)
private val subtypeRelation = Relation()
private val assignableRelation = Relation()
private val comparableRelation = Relation()
private val identityRelation = Relation()
```

**5. Future: N-checker parallelism via coroutines**

```kotlin
// Future (Phase 4g) — not yet implemented
class CheckerPool(private val program: Program, private val checkerCount: Int = 4) {
    private val checkers = List(checkerCount) { Checker(program) }
    private val fileAssignments = program.files.withIndex().associate { (i, f) ->
        f to checkers[i % checkerCount]
    }

    suspend fun checkAllFiles() = coroutineScope {
        checkers.map { checker ->
            launch { checker.checkAssignedFiles() }
        }.joinAll()
    }
}
```

---

## Completed infrastructure (Phase 4a, items 0–9)

All complete. Type hierarchy, type resolution, structural comparison engine, generic
instantiation, expression type inference, parallel checking pool are in place.

### Completed Phase 4b items (10–15a)

- [x] **10a/10b/10c** — TS2322 wired to Type engine (var decl, return, assignment — conservative)
- [x] **11a-11d** — Expression type inference (object literal, array, arrow/function, identifier)
- [x] **12a/12b/12c** — TS2345 union types, union call signatures, TS2769 overload diagnostics
- [x] **15a** — TS2300 duplicate class members

---

## Phase 16 — Fundamental Type System Features

**Session 2026-06-14 (round 155, multi-iteration session, ≥20 iterations per user request, addressing blockers) — CONCLUSION: +5 net (425→420 failing, 9661→9666 passing, ~95.8%), 5 feature commits (B400–B404), ZERO regressions at every COMMITTED step (exact failing-set diffs).** Entry state 9661/425 (post-round-154). Method: independently re-confirmed the bounded surgical pool is exhausted (find_candidates --fresh / --none --fresh / --output --fresh all dry of clean shapes; cluster-sized the 420 failures by distinct-test count: TS2322 ×105, TS2345 ×56, TS2339 ×40 dominate — the assignability/inference core), THEN mined the skip-logged + contained pool for FP-safe narrow-detector shapes the broad triage under-counted. The round-154 "genuinely exhausted, only Blocker BUILDs left" framing was again partly a tool/triage artifact: 5 contained wins existed (the recurring lesson — the micro-walker / contained-type-engine-subset pattern keeps yielding a handful per round even when the finders read empty). All 5 are blocker-adjacent (Blocker #2 generic, Blocker #3 scope, type-engine). Per-fix:
- **B400 (+1, 9662/424) — nonExportedElementsOfMergedModules** (Blocker #3 cross-block scope): TS2339 for a NON-EXPORTED nested namespace member referenced across separate blocks of a merged outer namespace. `namespace One{ namespace B{export var x} } namespace One{ namespace B{export var y} B.x; B.y; }` — non-exported `B` does NOT merge across the two `One` blocks (each keeps its own private copy), so `B.x` in block 2 is TS2339 while `B.y` is fine. Our binder flat-merges all of One's members (Blocker #3); new FP-safe AST walker `checkNonExportedNestedNamespaceCrossBlockRefs` restores per-block scoping. Firewall: script files only; ≥2 merged outer blocks; recv a block-local non-exported nested ns, not exported anywhere, no global binding; member absent locally yet present in a sibling block's copy; only DIRECT expression-statement refs.
- **B401 (+1, 9663/423) — genericImplements**: TS2416 generic-method-override constraint mismatch. base `f<Tb extends A>(): Tb` overridden by `f<Td extends B>(): Td` where base-constraint A not assignable to derived-constraint B → "'T' could be instantiated with an arbitrary type unrelated to 'B'". New detector in checkClassPropertyOverrides (after the predicate-mismatch block): both methods single-tp, both return bare TP, derived constrained, base-constraint (or unknown) not assignable to derived → TS2416 + `<T extends B>() => T` sig display (new genericMethodSig helper) + the 2-line could-be-instantiated chain. Fires regardless of the structural `assignable` (our generic sigs pass trivially). FP-safe (same-constraint Y and unconstrained Z cases don't fire).
- **B402 (+1, 9664/422) — genericWithNoConstraintComparableWithCurlyCurly**: TS2352 for `x as T` where x:`{}` and T's constraint is entirely null/undefined (cannot overlap `{}`). New emitTS2352IfEmptyObjectCastToTypeParam gated behind a new inTypeParamCastPass flag in the AsExpression branch of walkTypeAssertionsInExpr (AsExpression isn't forwarded to the cast callback — CLAUDE.md gotcha). Source `{}` identified via emptyObjectCastLocals tracking (the cast pass doesn't populate local var types → getTypeOfExpression returns any). **HIT the init-order gotcha**: emptyObjectCastLocals (a HashSet mutated from a pass called during Checker init) MUST be declared before init {} — first cut NPE-crashed; moved the declaration up.
- **B403 (+1, 9665/421) — assignmentStricterConstraints**: TS2322 for `g = f` assigning a generic function value with a STRICTER tp constraint to a looser one. `var f=function<T,S extends T>(x:T,y:S){}; var g=function<T,S>(x:T,y:S){}; g=f` → "Type '<T, S extends T>(x: T, y: S) => void' is not assignable to type '<T, S>(x: T, y: S) => void'." + parameter-incompatibility / could-be-instantiated chain + related TS2208 ("might need an `extends T` constraint") on g's S. New standalone walker checkGenericFunctionValueAssignmentConstraints + genericFunctionExprSig (mirrors B401 at the value site). FP-safe (top-level `g=f` of two generic-fn-expr vars, equal counts, some tp index f-constrained-to-sibling-tp / g-unconstrained, names align, a param typed by that tp).
- **B404 (+1, 9666/420) — contextualTypingArrayDestructuringWithDefaults**: TS2322 for an object-destructuring default value incompatible with the destructured property's type from a TUPLE annotation. `let {length = {a:1}}: [number]` → `length` is `1` (tuple literal length, modeled at Checker.kt:101400), `{a:1}` widens to `{ a: number; }`, not assignable → squiggle on `length`. New checkTupleDestructuringDefaultTypes walker (top-level + fn/method/block/module traversal). FP-safe: object-binding + tuple annotation + object-literal default not assignable to the identifier-named property's type.
- **Confirmed-architectural this round (skip-logged below so next session skips re-triage):** emptyObjectNotSubtypeOfIndexSignatureContainingObject1/2 (Blocker #2 callback-return inference + index-sig value compare), circularResolvedSignature (contextual typing through generic callback return — Blocker #2), spyComparisonChecking (mapped-type SpyObj resolution), mappedTypeWithCombinedTypeMappers / mappedTypeUnionConstrainTupleTreatedAsArrayLike / keyRemappingKeyofResult / indexedAccessRelation (mapped/conditional/indexed-access engine), typeParameterFixingWithContextSensitiveArguments2/3 (Blocker #2 inference), assignmentToObjectAndFunction (lib Function member-set, Blocker #4), tsxResolveExternalModuleExportsTypes / checkerInitializationCrash / mergeSymbolReexportedTypeAliasInstantiation (cross-file declare-global/augmentation TS2300/2305, Blocker #3), circularBaseTypes (multi-construct cycle detection), uniqueSymbolJs2 (JSDoc unique-symbol, Blocker #5), objectFreezeLiteralsDontWiden (Object.freeze literal preservation), nestedRecursiveArraysOrObjectsError01 (recursive union + nested excess), arrayFakeFlatNoCrashInferenceDeclarations (TS5088 decl-emit cyclic), redefineArray (lib ArrayConstructor), duplicateLocalVariable1/2 (3 coupled TS2403+2365+2356), controlFlowArrayErrors (evolving-array flow, Blocker #1). **NEXT: the contained narrow-detector pool is harvested again; the next lever is a multi-session Blocker BUILD — the assignability/inference core (TS2322 ×105 / TS2345 ×56) is the dominant remaining mass; Blocker #2 map-return/callback-return inference flowing into var-decl types is the highest-cited single lever (broad/net-zero-prone, needs a dedicated session + revert budget).**

**Session 2026-06-14 (round 154, multi-iteration session, ≥20 iterations per user request, addressing blockers) — CONCLUSION: +4 net (429→425 failing, 9657→9661 passing, ~95.8%), 4 feature commits (B395, B396, B397, B399), ZERO regressions at every COMMITTED step (exact failing-set diffs).** Entry state 9657/429 (post-round-153). Method: a 6-agent parallel triage fleet (Agent tool, no ultracode opt-in) classified ~70 closest-miss (≤4-error-line) tests CONTAINED vs ARCHITECTURAL; then I deep-read ~20 more candidates (fresh medium-diff 5–12-errline, SWAP/MISSING/OUTPUT buckets) myself. **NET FINDING: the bounded surgical pool is GENUINELY exhausted** — the candidate-finder's single-code labels repeatedly MASK multi-piece/JSDoc/generic dependencies (jsDeclarationsInheritedTypes looked like a clean TS2564 but has a hidden spurious-TS2304 `@typedef` cascade; knockout looked like clean TS2339 but its `o.name` receiver resolves to `any`). Every remaining fresh candidate needs a multi-session Blocker BUILD. Per-fix, then the REVERTS + EXHAUSTIVE architectural triage (so the next session skips re-triage):
- **B395 (+1, 9658/428) — deeplyNestedAssignabilityIssue**: a nested object-literal value missing a required property of its target now emits TS2741/2739/2740 at the property KEY (`thing: {}` vs `thing: A` → squiggle `thing`), with TS2728 + TS6500. Edits in checkNestedObjLitPropTypes/emitNestedMissingRequiredProps: arrayPropsOnly gate skips only SIMPLE values now (object-literal values descend); emitNestedMissingRequiredProps gained optional key-position (squiggle KEY not value — tsc reports nested missing-prop at the property NAME in a var-decl context) + enclosing-prop params (TS6500); the coarse `canUse && !isAssignable` whole-init TS2322 is suppressed via a `nestedMissingEmitted` flag (the nested check poisons the relation cache → isAssignable false → coarse would double-report). ZERO regressions.
- **B396 (+1, 9659/427) — exportSpecifierReferencingOuterDeclaration4**: TS2694 for `X.member` in a MODULE file where X is a same-file top-level `declare namespace X` lacking member. NEW walker checkModuleFileLocalNamespaceMemberRefs (Checker.kt ~27692, registered after checkAmbientModuleLocalNamespaceMemberRefs) — the B146 sidestep generalized to module-file top-level Identifier-named declare-namespaces: collect X's members from THIS file's AST (not the cross-file-merged symbol). FP firewall: module-file-only, declare-ns roots, shadow-skip same-name non-ns bindings + bodies with re-export/import-equals. ZERO regressions.
- **B397 (+1, 9660/426) — importTypeAssertionDeprecation**: TS2880 for a deprecated `assert` import-attributes clause `import(spec, { assert: {...} })`. The parser parsed NO second argument in either ImportType (type) or the dynamic-import call (value) — both errored. Now both parse the optional 2nd arg (additive: the comma is inside the import parens, so type-arg lists `Foo<import("x"), Bar>` are unaffected). emitImportAttrAssertDeprecation (Parser.kt ~329) replicates tsc's per-position span quirk (VALUE→`assert` kw len 6; TYPE→assert-clause value inner `{` len 1). 2880 added to GRAMMAR_CLASS_CODES (no suppression trigger). ZERO regressions.
- **B399 (+1, 9661/425) — largeControlFlowGraph (Blocker #1 flow substep)**: TS2563 "containing function or module body is too large for control flow analysis." tsc disables flow analysis past a per-container budget (flowDepth 2000). We approximate per-FILE via `flowGraph.nodeToFlow.size`: an instrumented probe (temp marker @ >600 nodes, full suite) found the ONLY file > 2000 is the 10000-deep chain (~20000 nodes); next-largest valid file ~1372 → a 2000 threshold is FP-safe by a wide margin. Emitted in Checker init (before checkDefiniteAssignment) at the first top-level statements first token. ZERO regressions.
- **knockout — TRIAGED CONTAINED, REVERTED.** Added a checkMemberAccessMissing branch for a PropertyAccess receiver typed as a single-decl base-less no-index-sig callable user interface → TS2339. Marker trace showed `o.name` resolves to `any` (not `Observable<string>`): the object-literal-member + generic-return-inference chain (`var o = {name: ko.observable("Bob")}`) is Blocker #2. Reverted (no dead infra).
- **B398 jsDeclarationsInheritedTypes (TS2564 for JS `@type` class fields) — ATTEMPTED, REVERTED.** Added `hasNonTrivialJsTypeTag` (raw-scan the field's leading JSDoc for a non-trivial `@type {X}`) + un-gated the `member.type==null` skip in checkClassPropertyInit for such JS fields. It did NOT fire (leadingComments not attached to the JS class fields) AND the test's DOMINANT problem is a pre-existing spurious-TS2304 cascade ("Cannot find name 'A'/'B'" from the unparsed `@typedef A`/`@property` at (1,1)/(2,1)) — so it's a 2-piece JSDoc (Blocker #5) test, not contained. Reverted. (Gotcha re-hit: a literal `/*` inside a KDoc opens a NESTED Kotlin block comment — "any/unknown/**\*** and" broke the whole file with a cascade of unresolved-reference errors at line 120; reword to avoid `/*` and `*/`.)
- **EXHAUSTIVE architectural triage (do NOT re-triage these — all need multi-session Blocker BUILDs):** `largeControlFlowGraph` (TS2563) = needs PER-CONTAINER flow-node counting (FlowGraph only exposes per-FILE `nodeToFlow`); tsc's limit is flow-DEPTH 2000 during a single reference walk, NOT node-count, so a node-count heuristic both diverges and is FP-risky — defer. `noSymbolForMergeCrash` (TS2649) = cross-file `type A` + `interface A`+`namespace A` conflict; the tsc TS2649 message ("augment module with value exports… non-module entity") doesn't cleanly match the type-only decls and the existing TS2649 walker is `declare module "X"`-only — subtle, defer (Blocker #3). `instantiationExpressionErrorNoCrash` (TS2344+TS2635) = ReturnType-constraint check + instantiation-expression type-arg-arity (TS2635 is a NEW diagnostic, 0 in codebase) — generic/type-engine. `deeplyNestedAssignabilityErrorsCombined` (SWAP) = class-VALUE typing in object literals (`f: Ctor2` should type `typeof Ctor2`, we show `Ctor2`) — architectural class-value typing. `fixingTypeParametersRepeatedly2` (TS2741+TS2403, top queue blocker) = CONFIRMED 2-hard-pieces by direct read: (1) the round-79l contextually-typed-arrow-return-mismatch through a GENERIC-instantiated callee (we deliberately keep coarse TS2345 there) for the missing-property TS2741 variant; (2) the overload re-inference subtlety (foo=1 sig→result:Derived, bar=2 identical sigs→result:Base→TS2403) — do NOT attempt without first finding a SIMPLER Blocker-#2 repro (round-129 guidance reconfirmed). `narrowByClauseExpressionInSwitchTrue6` = real switch(true)-discriminant POSITIVE narrowing with fall-through (not the negative never/null micro-walker shape). `inferFromGenericFunctionReturnTypes3`, `genericFunctionInference1` = higher-order generic (pipe/compose) inference. `longObjectInstantiationChain3`, `mappedTypeIndexedAccessConstraint`, `contextualTypesNegatedTypeLikeConstraintInGenericMappedType2` = mapped/conditional/Omit modeling. `noImplicitAnyLoopCrash` (TS2556+TS2461) = needs flow-inferred `bar: number|undefined` (we type it `any`). `inKeywordAndUnknown` (TS2638) = unknown→{} truthiness narrowing (flow). **NEXT SESSION: commit to ONE multi-session Blocker BUILD. The highest-yield/most-tractable per this round's evidence: Blocker #2 generic/object-literal-member return inference (root of knockout + the TS2339/TS2345/TS2741 cluster — but net-zero-prone, needs a SIMPLE single-piece repro first) OR Blocker #1 a real per-function flow engine (switch-true positive narrowing, evolving arrays, unknown-narrowing). The micro-walker (B368-370) and contained-type-engine-subset (B388-397) patterns are HARVESTED.**

**Session 2026-06-14 (round 153, multi-iteration session, ≥20 iterations per user request) — CONCLUSION: +3 net (432→429 failing, 9654→9657 passing, ~95.7%), 3 feature commits (B391, B392, B393), ZERO regressions at every COMMITTED step (exact failing-set diffs).** Entry state 9654/432 (post-round-152). Method: parallel Explore-agent triage fleets (4 fleets, ~35 candidates) feeding a sequential implement/verify loop; the round-152 pattern but using the Agent tool directly (no ultracode opt-in this session). B391/B392 were round-152's "remaining batch-2" candidates; **B393 was a genuine Blocker #3-adjacent BUILD landed AFTER confirming the surgical pool dry (the user asked to address blockers).** **B393 (+1, 9657/429) — moduleVisibilityTest3**: namespace-alias visibility through `import X = <local-namespace>`. The file-scoped TS2709 walker (`checkTypeRefForNamespace` uses `currentCheckLocals`=file-locals) and the shared qualified-name resolver (`checkTypeNameResolved` leftSym=globals/file-locals) never reach a namespace-LOCAL import-equals alias, so a NEW dedicated FP-safe walker `checkNamespaceAliasTypeRefs` owns the shape: bare `X` as a type → TS2709; `X.member` where member EXISTS in the target namespace but is NOT exported (via `isNameExportedFromNamespace`) → TS2724 (emitTS2694 + spelling, candidate pool = exported members only). LOAD-BEARING gates discovered during the build: (1) the namespace-only check must be `hasAny(Module OR NamespaceModule OR ValueModule)` NOT bare `Module` — a `namespace` with value members carries `ValueModule`, so `!hasAny(Module)` wrongly excluded it and the first run was net-zero (aliasTargets empty); (2) scan only identifier-named NON-`declare` namespaces — ambient `declare module "X"` routes alias-as-type TS2709 through the existing path, and the `name !is Identifier` gate ALONE didn't suppress the double-emit (the module name parses such that the existing path still fires), so the `ModifierFlag.Declare in modifiers` gate is the real firewall (privacyImportParseErrors deliberately collides the module name with a local alias name). The two wins B391/B392 were the round-152 "remaining batch-2" candidates. **B391 (+1, 9655/431) — switchStatementsWithMultipleDefaults**: an escaped-keyword `default` default clause emits TS1260 "Keywords cannot contain escape characters." as a REAL parse diagnostic, which SUPPRESSES every TS1113 "duplicate default clause" grammar diagnostic in the file (tsc grammarErrorOnNode/hasParseDiagnostics). 3 edits: Scanner `tokenHadUnicodeEscape` flag (set in scanIdentifierWithEscapes, reset in scan(), getter `hasTokenUnicodeEscape()`); parseCaseOrDefaultClause emits TS1260 at the keyword span when the flag is set BEFORE consuming the keyword (both case+default branches, FP-safe — only escaped keywords trigger it; TS1260∉GRAMMAR_CLASS_CODES so it registers in filesWithRealParseDiagnostics); TS1113 added to the grammar-suppression post-filter (single-file gate at TypeScriptCompiler.kt ~913 + multi-file filesWithRealParseDiagnostics ~1379). **B392 (+1, 9656/430) — grammarAmbiguities1**: `f(g < A, B > 7)` parses the call args as two comparisons (`g<A` and `B>7`); tsc emits TS2365 for relational comparison of a non-comparable object/function/typeof-class operand. The arithmetic/comparison checker (Checker.kt ~109660) bailed early for any non-wrapper-Object operand — gated that bail to `!isComparison`; the comparison branch's `!leftComparable||!rightComparable` path now displays object operands via a new `objComparisonOperandDisplay` (typeToString, no literal-text override so `7`→`number`; a bare class-value Identifier renders `typeof X`). The gate change proved 0-regression at scale in the first batch run (only the typeof-display was added after). **REVERTED (one-cycle budget, no-dead-infra): invalidSplice + indexSignatureOfTypeUnknownStillRequiresIndexSignature batch.** Triage flagged both FLIP_NOW: (a) add `...items: T[]` to the embedded-lib `splice` signature; (b) add Array/ReadonlyArray to the missing-index-sig `isNominalSource` gate + drop `Unknown` from the any-skip. NEITHER flipped (invalidSplice: our empty `[]` types as `any[]` not `never[]` so splice T=any, no error; indexSig: the param `{[x:string]:T}` with T inferred `unknown` isn't produced — generic-inference gap) AND the indexSig change REGRESSED `arraySlice` → reverted both via `git checkout Checker.kt`. **EXHAUSTIVE triage findings (so the next session does NOT re-triage these ~35 — all architectural):** rootDir TS6059/TS5011 (`pathMappingBasedModuleResolution_rootImport_*` ×2) needs tsc's dual-root verifyCompilerOptions logic (rootDir='/root' for TS6059 but common-source-dir='/' for TS5011) — high FP surface, defer; interfaceNaming1 = parser-recovery Blocker #7 (we over-emit TS1212 FP on `interface` declaration-keyword + miss TS2363 — the TS1212 suppression couples with TS2693); arrayOfSubtypeIsAssignableToReadonlyArray = emits at right positions but the `C<A>` (subclass-of-Array) line needs the "concat return-type" elaboration chain not the param-contravariance one (per-typeparam variance, B75.2 shortcut fall-through); didYouMeanElaborationsForExpressionsWhichCouldBeCalled = class-value-in-value-position typing (typeof Bar) + TS6212 (architectural); duplicatePackage/visibilityOfCrossModuleTypeUsage = cross-file/package identity (Blocker #3); checkerInitializationCrash/mergeSymbolReexportedTypeAliasInstantiation = cross-file merged-namespace/module TS2300 (Blocker #3); moduleVisibilityTest3 (TS2709+TS2724) = namespace-alias scope-threading + export-visibility through `import X = <local-ns>` across two walkers (3+ coupled pieces, `checkTypeRefForNamespace` is file-scoped + only handles ExternalModuleReference aliases); objectFreezeLiteralsDontWiden = Object.freeze literal-preservation; controlFlowForIndexSignatures = `typeof X` narrowing in type-position; partialDiscriminatedUnionMemberHasGoodError = deep nested-object-vs-union descent through a generic `push()` call; excessivelyLargeTupleSpread (TS2799/TS2800) = tuple-length-through-spread + recursion-limit modeling; templateStringsArrayTypeRedefinedInES6Mode = lib TemplateStringsArray with exact "and 17 more" member-count (Blocker #4); duplicateLocalVariable1 = 3 coupled errors (TS2403+TS2365+TS2356) on a redeclared for-loop var; jsCheckObjectDefineThisNoCrash/jsFileCompilationBindDeepExportsAssignment = JS-expando-`this` (the `.js` skip is load-bearing); knockout/maxNodeModuleJsDepthDefaultsToZero = flow/module-symbol type-flow; instantiationExpressionErrorNoCrash = ReturnType<typeof f<T>> constraint-check at type level; awaitedType = Awaited recursion + lib. **NEXT: the contained pool is genuinely exhausted (confirmed by both the round-152 conclusion AND this round's 35-candidate sweep). Every remaining failure needs a multi-session Blocker BUILD: #1 flow engine (typeof-in-type-position narrowing, evolving arrays), #2 generic/map-return inference (the indexSig + most TS2345 cluster), #3 cross-file/namespace-alias scope (the TS2300 + TS2709/2724 + cross-package cluster), the type-engine assignability core (TS2322 ×24 / TS2345 ×16 / TS2339 ×11), mapped/conditional/recursive/tuple modeling, lib member-sets (Blocker #4), JSDoc, decl-emit nameability. The micro-walker pattern (B368-B370) and contained-type-engine-subset pattern (B388-B392) are mostly harvested — the next lever is real infrastructure.**

**Session 2026-06-14 (round 152, ultracode multi-iteration session, ≥20 iterations per user request) — CONCLUSION: +7 net (445→438 failing, 9641→9648 passing, ~95.7%), 7 feature commits (B379–B385), ZERO regressions at every COMMITTED step (exact failing-set diffs).** Entry state 9641/445 (post-round-151). **Method (the productive ultracode pattern this round): parallel-agent TRIAGE workflows feeding a sequential implement/verify loop.** A 41-agent Workflow classified the ~40 closest-miss DIFF-bucket tests (ranked by actual-diff line count via a custom XML scanner) as CONTAINED vs ARCHITECTURAL, each returning a concrete fix sketch + risk + skip-log-staleness check; 10 came back CONTAINED, and the 7 LOW/MEDIUM-risk full-flip ones were implemented + full-suite-verified + committed one at a time (the gradle suite overlaps each next implementation as a background task). Key finding: several skip-log entries were STALE (e.g. recursiveClassReferenceTest logged "Blocker #1" but the real gap was a contained dotted-namespace resolution miss; missingFunctionImplementation logged "multi-piece" but only 4 TS2393 lines remained). Wins B379–B385 (one TS2345 dotted-namespace; one TS2393 merged-namespace; one JS `@augments` TS2314+TS8026; one TS1194-ambient + TS2300 clodule re-export; one declare-global import-equals TS2667/TS2591/TS2304-suppress; one TypeLiteral overload-merge + TS2367 class-value; one 3-guard namespace/import-alias merge). The ONLY in-session regression: B385's first cut over-broadly gated pieces 1+3 (regressed internalAlias×2 + doYouNeedToChangeYourTargetLibraryES2016Plus) — caught by the post-commit exact failing-set diff, fixed by adding the non-lib gate (piece 1, placed after the TS2550 lib check) and the non-EXPORTED-local-namespace gate (piece 3); re-verified clean before commit. REVERTED (one-cycle budget, no-dead-infra): unicodeIdentifierName2 — the TS1127 parser-recovery fix (consume-through-list-terminator) drops the `"hello"` expression statement from the JS emit; tsc keeps statement-starter tokens via isInSomeParsingContext abort semantics AND suppresses the trailing `;` error, which is too finicky to replicate safely for +1 (blocker #7 parser-recovery, HIGH risk). SKIPPED: assignmentCompat1 — its 2 index-signature relation-engine bugs are the documented 39+-regression hot path (`propertiesRelatedTo` stringIndexInfo satisfy-`continue`); a dedicated relation-engine session, not a tail attempt. Batch-2 triage (32 agents over diff-13..19 DIFF tests) returned ~0 CONTAINED (all architectural: cross-file scope, esModuleInterop-default, index-sig relation, JS expando, generic inference). **NEXT: the contained DIFF pool is harvested; a NONE-bucket triage (29 agents) is the last surgical sweep, then the remaining ~430 failures are the type-engine/flow/cross-file/JSDoc/lib BLOCKERS — the next session should commit to a Blocker BUILD substep (Blocker #1 flow engine or Blocker #2 generic/map-return inference) per the anti-loop rule.** Per-commit notes:
- **B379 (+1, 9642/444) — recursiveClassReferenceTest** (triage1 LOW, fullFlip). `namespace Sample.Thing.Languages.PlainText { class State; class Mode { getInitialState() { return new State(self) } } }` — the dotted name parses as ONE ModuleDeclaration with a left-nested PropertyAccessExpression name; the call-types walker's ModuleDeclaration branch only resolved a bare Identifier, so the inner namespace was never pushed onto `inferenceNamespaceStack`, `new State(self)` resolved its callee to anyType, and constructor-arg TS2345 (Window→IMode) never fired. New `resolveModuleDeclNamespaceSymbol(nameExpr)` (Checker.kt ~90894) flattens the dotted segments (mirroring `bindModuleDeclaration`), resolves the head via inference-namespace/file-locals/globals, walks `.exports` to the innermost segment, and pushes it. The existing named-interface-vs-named-interface arg gate then fires TS2345 with the missing-property chain + TS2728.
- **B380 (+1, 9643/443) — missingFunctionImplementation** (triage1 LOW, fullFlip, additive). TS2393 for a function whose impl BODY appears in 2+ distinct MERGED `namespace N {}` blocks (the N12 case: `namespace N12 { …m(a?){} } namespace N12 { m(a){} }`). `checkMergedNamespaceDuplicates` now also collects same-named FunctionDeclarations per block (with block index); when impl bodies span 2+ distinct blocks, emits TS2393 on EVERY decl of that name (signatures included). The distinct-block gate keeps it disjoint from the single-block overload path (checkDuplicateIdentifiers ~26055, fires only when one block already holds ≥2 bodies) → no double-emit.
- **B381 (+1, 9644/442) — jsExtendsImplicitAny** (triage1 LOW, fullFlip). A JS (@checkJs) `@augments`/`@extends <Base>` JSDoc tag SUPPLIES heritage type args; when the tag's arg count ≠ the base's required type-param count, tsc emits TS2314 at the TAG (the base name, or the whole `Base<...>` span when args present) and SUPPRESSES the TS8026 "provide via @extends tag" on the extends clause. New raw-text scanner `checkJsDocExtendsTags` (mirrors checkJsDocBareGenericTags; balanced-angle-bracket arg count + span) + companion `hasGoverningExtendsTag` (backward-scans to the nearest preceding `/** */` block) suppresses TS8026 in checkHeritageTypeArgCount. checkJs+noImplicitAny gated, JS-only.
- **B382 (+1, 9645/441) — declarationFileNoCrashOnExtraExportModifier** (triage1 LOW, fullFlip). (a) FP fix: `export { x };` (no module specifier) inside an AMBIENT (`declare`) namespace is a legal re-export (tsc inAmbientNamespaceDeclaration guard) — thread an `inAmbient` flag through walkDefaultExportInNamespace and suppress the no-specifier TS1194 when ambient. (b) MISS fix: when a file does `export = X` where X is a clodule (class X + declare namespace X), an `export { Name }` re-export inside the namespace naming a top-level EXPORTED Name makes Name a duplicate module member; new narrow walker `checkExportEqualsCloduleReExport` emits TS2300 at both positions, each with a TS6203 (FP-safe: exact shape, no other fixture).
- **B383 (+1, 9646/440) — importAliasInModuleAugmentation** (triage1 LOW, fullFlip). (a) FP fix: `export import x = A.y;` inside `declare global` registers `x` as a global alias — collectGlobalAugmentationNames now adds ImportEqualsDeclaration names → fileScope suppresses the value- and type-position TS2304 on `x`. (b) MISS fix: `import f = require("fs");` directly inside `declare global` is an import in a module augmentation — walkRequireImportInNamespace threads an `inGlobalAug` flag and, for an ExternalModuleReference there, emits TS2667 at the `import` keyword + routes the specifier through emitTS2307 (node-builtin `fs` → TS2591 with the @types/node hint). `export import x = A.y` (QualifiedName ref) is naturally exempt.
- **B384 (+1, 9647/439) — typeMatch1** (triage1 MEDIUM, fullFlip; the type-engine change verified clean by exact failing-set diff). (1) getTypeFromTypeLiteral named-method branch: a repeated method name is an OVERLOAD (`f(n):string; f(s):number;`) — APPEND the signature to the existing method's callSignatures instead of overwriting (previously only the LAST overload survived → FP TS2322 vs a multi-call-signature target). (2) checkEqualityComparisonNoOverlap: new AST branch tryEmitClassValueNoOverlap — `C == D` of two distinct class VALUES where BOTH carry a private instance member (nominal brand) → `typeof C`/`typeof D` have no overlap → TS2367 (C==C and structurally-overlapping classes excluded).
- **B385 (+1, 9648/438) — namespaceMergedWithImportAliasNoCrash** (triage1 MEDIUM, fullFlip; errors variant — the JS-emit variant is a separate pre-existing failure). 3 FP-safe guards: (1) checkMemberAccessMissing Module branch — a USER type-only/UNINSTANTIATED namespace used in VALUE position (`Library.foo`) is owned by TS2708, suppress the dup TS2339 (gated non-lib, placed AFTER the TS2550 lib-target check so `Intl.PluralRules` keeps its hint); (2) checkImportConflictsWithLocal NamespaceImport branch — `import * as Lib` does NOT merge with a non-declare local `namespace Lib` declared LATER → TS2440; (3) symbolToQualifiedName — a NON-EXPORTED local namespace declared in this file renders unqualified (`Lib`), while an EXPORTED local namespace / cross-module import keeps `"basename".X` (internalAlias* firewall). First cut regressed internalAlias×2 + doYouNeed* via over-broad gates; fixed by the export/lib gates above, re-verified clean.
- **B386 (+2, 9650/436) — exportSpecifierAndLocalMemberDeclaration + bigintIndex** (triage3 NONE-bucket, LOW, fullFlip). Two same-name `declare module "m2"` blocks: Block 1 has a NON-exported local `namespace X` + `export { Y as X }` (a value-rename binding the module's exported X to function Y); Block 2 refs `X.I` with no local namespace X → the exported X is the value alias, not a namespace → TS2503 in Block 2 only. We emitted nothing (the general TS2503 path is post-filtered out of declare-module bodies by B367; the B146 walker skips Block 2's empty nsMembers). New cross-block pre-pass in checkAmbientModuleLocalNamespaceMemberRefs: group same-named ambient blocks; a name is "poisoned" iff non-exported-local-namespace ∧ rename-shadowed (`export { Y as X }`) ∧ never `export namespace`; a poisoned name referenced in a block WITHOUT its own local namespace fires TS2503 at the root. forEachQualifiedTypeRefInStatement's callback now passes the root Identifier node (squiggle position). Flipped bigintIndex too (shared TS2694/ambient-module path).
- **B387 (+1, 9651/435) — externalModuleRefernceResolutionOrderInImportDeclaration** (triage3 NONE-bucket, LOW, fullFlip). A BARE ambient `declare module "X"` whose name coincides with a sibling SOURCE basename (file1.ts `export function foo`) is a STANDALONE ambient module, NOT an augmentation; we wrongly merged its `bar` into file1.ts's locals so `file1.bar()` didn't error. Gate the collectModuleAugmentations target-file-locals merge to skip when the specifier is BARE (non-relative) AND targetFile is a plain .ts/.tsx source (not .d.ts/node_modules) — the B114 import-equals branch then sees exports={foo} and fires TS2339. Tighter than the triage agent's pure-target-type gate (also requires non-relative specifier) so relative `declare module "./foo"` augmentations are untouched.
- **DISCARDED (net-zero, no-dead-infra) — literal-target FP firewall.** A triage subagent left an unverified `typeEngineSettledLiteralTarget` change in the shared tree (workflow agents are NOT isolated worktrees): suppress the widened string-based var-decl/assignment fallback when the type engine confidently PASSES a literal-union target (`let foo: "a"|"b" = "a"`). Saved + verified separately: NET-ZERO (0 flips, 0 regressions) → discarded. PROCESS NOTE: non-isolated workflow agents can mutate the working tree AND wipe full-suite XMLs via their own `gradle --tests '*X*'` runs (per the CLAUDE.md XML-wipe gotcha) — for verification-heavy sessions, stop the triage workflow (TaskStop) before relying on full-suite XMLs, or pass `isolation: 'worktree'` to read-only triage agents.
- **B388 (+1, 9652/434) — typeAliasDeclareKeywordNewlines** (batch-2 triage, LOW, fullFlip; surfaced by an agent, self-verified). Two pieces: (1) Parser TS1142 "Line break not permitted here." — `declare type /*newline*/ T1 = null` has a break between the `type` keyword and the alias NAME; parseTypeAliasDeclaration checks `scanner.hasPrecedingLineBreak()` after `parseExpected(TypeKeyword)` and reports at the name token (only the `declare type` path reaches here — the statement dispatcher's isStartOfTypeAlias already gates line-broken aliases, so no FP). (2) Checker FP firewall — `const t: T = null` where `type T = null`: the type engine resolves T→nullType (assignable) but falls through to the string fallback whose `isAssignableTo("null","@T")` rejects any @-named target as opaque; before emitting TS2322 for a null/undefined source vs an @-named target, re-resolve the annotation and suppress when `checkTypeRelatedTo` confirms assignability.
- **B389 (+1, 9653/433) — declarationEmitExpressionInExtends3** (batch-2 triage, MEDIUM, fullFlip). `class C extends getLocalClass<LocalInterface>(undefined)` — the base is a CallExpression with an EXPLICIT type arg (no inference; the Blocker #2 skip-log citation was stale). checkCallTypesInStatement's ClassDeclaration branch resolved the base symbol + walked member bodies but never walked the heritage base EXPRESSION itself, so the explicit-type-args machinery (instantiate T=LocalInterface, check the `undefined` arg) was never reached. Added a heritage-clause loop calling `checkCallTypesInExpr` on each base expression, inside the active class type-param scope, before the member loop → all 4 TS2345 fire. Same shape as B379.
- **B390 (+1, 9654/432) — didYouMeanStringLiteral** (batch-2 triage, MEDIUM, fullFlip). New AST helper `tryEmitLiteralUnionDidYouMean` (early-return in checkVarDeclAssignability): resolves a var annotation to an ordered string-literal value set — a union / alias-to-union shows the ALIAS name, an intersection of literal-unions shows the REDUCED (set-intersected, first-constituent-ordered) union — and when the source string literal isn't a member, emits TS2820 with a `getSpellingSuggestionFromNames` 'Did you mean' (or plain TS2322 if no close match), preserving the source literal (no widening) and squiggling the var name. Clears all 3 cases (t1→TS2820 'T1'+suggestion; t2→TS2322 reduced union; t3→TS2820 reduced union+suggestion). FP-safe (fires only for a string-literal init absent from the resolved set).
- **REVERTED (one-cycle) — jsdocRestParameter.** The Parser bridge correctly recognized `@param {...number} a` → `a: number[]` rest param (map value carries an isRest flag; applyJSDocParamPrimitiveTypes sets dotDotDotToken) which suppressed the FP TS7006/TS2554, but the checker rest-element TS2345 NEVER FIRED: `.js` call-type checking is SKIPPED (the load-bearing `checkCallExpressionTypes`/`checkPropertyAccess` .js gotcha), so checkArgumentsAgainstSignature is unreachable for JS calls. The triage agent's sketch ("add a check in checkArgumentsAgainstSignature") was wrong about reachability. Proper fix = a NARROW dedicated JS-aware rest-param call walker (like B150's object-literal-method path), NOT the main arg checker — deferred. **Other batch-2 CONTAINED still open** (sketches in the whjoem03x triage result): switchStatementsWithMultipleDefaults (Scanner tokenHasUnicodeEscape tracking + parseCaseOrDefaultClause TS1260, MEDIUM-HIGH 3-edit), grammarAmbiguities1 (TS2365 relational-comparison gate, fullFlip=FALSE), interfaceNaming1 (TS1212 strict-reserved, fullFlip=FALSE), arrayOfSubtypeIsAssignableToReadonlyArray (strictExplicitlyFalse bivariant gate, fullFlip=FALSE), exhaustiveSwitchCheckCircularity (the literal-firewall, fullFlip=FALSE — clears 3 FPs but the test stays red on a missing switch-exhaustiveness TS2345).

**Session 2026-06-13 (round 151, multi-iteration session, ≥20 iterations per user request) — CONCLUSION: +5 net (450→445 failing, 9636→9641 passing, ~95.6%), 3 feature commits (B376–B378), ZERO regressions at every committed step (exact failing-set diffs).** Entry state 9636/450 (post-round-150). This round COMPLETED ALL 3 round-150 next-session candidates (a)/(b)/(c). After the 3 wins, exhaustively re-triaged via custom failing-set scanners (`/tmp/rank.py` by expected-error-count, `/tmp/smalldiff.py` by actual-diff size, `/tmp/overemit.py` for pure over-emit guards, `/tmp/jsdiff.py` for JS-emit) — the contained surgical pool is genuinely EXHAUSTED: error-baseline near-misses, JS-emit (48/50 skip-logged, 2 architectural), over-emit (only `heterogeneousArrayAndOverloads`, a SWAP needing overload-elaboration), the switch-true narrowing family (`narrowByClauseExpressionInSwitchTrue3/6/7` need real fall-through/post-switch flow narrowing), and the bounded NONE tail are all architectural. The next lever is genuinely a Blocker BUILD (full flow engine, generic inference Blocker #2, cross-file/ambient scope Blocker #3) or the bounded-but-multi-piece parser import-attributes / computed-literal-member-name features.
- **B376 (+2, 9638/448) — candidate (c), `missingDiscriminants` + `missingDiscriminants2`.** Suppress the spurious WIDENED TS2322 for a valid object-literal-vs-discriminated-union assignment. `const thing2: Thing = { str:"b", num:1 }` is a valid `Thing` member but the general var-decl path widened the literal to `{ str:string; num:number }` and reported TS2322 against the first union member. `tryEmitUnionDiscriminantExcess` (Checker.kt ~65829) already computes the discriminant-surviving (`kept`) constituents; changed the `excess == null` branch from `return false` to: if the literal is fully assignable to ≥1 kept constituent (all that constituent's declared props present in source AND every declared source prop POSITIVELY `litMatchesNode == true`), `return true` to SUPPRESS the downstream spurious TS2322. The change can ONLY remove a diagnostic for a genuinely-assignable object → no FP surface. The TS2353 excess-property cases (item1/item2) were already correct.
- **B377 (+2, 9640/446) — candidate (a), `publicGetterProtectedSetterFromThisParameter` (es5+es2015).** TWO coupled pieces. (1) `checkPropertyAccessInClassMember` (Checker.kt ~86089) now resolves a method's explicit `this: T` parameter annotation via `getTypeFromTypeNode` and uses T as the `effectiveClassType` for the body walk instead of the lexical class — `this.x` inside `class B { foo(this: A) {…} }` resolves against A, killing the pre-existing TS2339 FP (any/error resolution → no `this.member` check, safe). (2) NEW walker `checkProtectedWriteViaThisParam` (+ `pw*` helpers, Checker.kt ~40288, registered ~1135) emits TS2445 for a WRITE `obj.member = …` to a PROTECTED member from a FREE function whose `this:` param types `this` as class T, when the member's declaring class is not in T's hierarchy (tsc `getEnclosingClassFromThisParameter` rule). Scoped to top-level/namespace free functions (methods use the standard lexical path), WRITE targets only (the setter governs), obj is `this`(→T) or a class-typed param. The reverted-round-150 B375 walker is effectively re-created as the protected-write companion to B374's private walker.
- **B378 (+1, 9641/445) — candidate (b), `typePredicateInLoop`.** Completes the round-150-REVERTED TS1345 (its only consumer had the pre-existing TS2345 FP, now fixed). (1) `resolveUserTypeGuardNarrowing` (Checker.kt ~90873) + the `checkCallTypesInStatement` IfStatement branch narrow X to U inside `if (guard(X)){…}` where `guard`'s return type is a non-asserts `X is U` predicate — so the call-arg checker sees the narrowed type and stops FP'ing TS2345 (`otherFunc(ITEM, arg)` with `arg` narrowed to TypeExt). GOTCHA: the parser stores a predicate's `parameterName` as a **TypeReference wrapping the identifier**, NOT a bare Identifier (traced via marker). FP-safe: narrowing X to the guard's subtype only ever makes MORE arg checks pass. (2) NEW `checkVoidConditionTruthiness` (Checker.kt ~34569, wired into if/while/do/for condition sites in the `checkAlwaysTruthy` pass) emits TS1345 "An expression of type 'void' cannot be tested for truthiness." for a void-returning CALL in a truthiness condition; gated to a CallExpression resolving to exactly the `void` intrinsic (our inference defaults un-analysable returns to `any`, not `void`, so no FP).

**Triaged-not-built this round (all architectural or multi-piece):** `importTypeAssertionDeprecation` (TS2880 — needs PARSER support for `import(spec, {assert:{…}})` attribute syntax in both ImportType and dynamic-import-call positions; we cascade TS1005/TS1128 parse errors). `literalsInComputedProperties1` (needs 3 pieces: extend the Parser TS2452 to numeric-STRING `"3"` and computed `[2]`/`["4"]` names — currently only NumericLiteral/BigInt; MODEL computed-numeric property names as members `{[2]:1}`→member "2" so `x[2]` resolves; SUPPRESS the enum string-index TS7015 on `X["bar"]`). `widenedTypes` (instanceof-grammar + null-position grab-bag, 8 diverse diagnostics). `heterogeneousArrayAndOverloads` (method overload + heterogeneous-array-element inference). `indexSignatureInOtherFile`/`1` (symbol index-sig TS2411 + cross-file interface merge — Blocker #3). `signatureCombiningRestParameters5`, `objectGroupBy`, `instantiationExpressionErrorNoCrash`, `unionOfArraysFilterCall`, `genericCallAtYieldExpressionInGenericCall1` (generic inference / lib members — Blocker #2). `staticAnonymousTypeNotReferencingTypeParameter`, `cloduleTest2` (generic inner statics / clodule merge). **NEXT:** the contained narrow-walker + contained-type-engine pool is again near-exhausted; the remaining ~445 failures are dominated by the three architectural Blockers (full flow engine, generic inference, cross-file/ambient scope) plus the parser import-attributes feature. The reusable infra landed this round (`pwClassDerivesFrom`/`pwFindProtectedWriteDeclaringClass` protected-access helpers; `resolveUserTypeGuardNarrowing` user-type-guard resolver; `checkVoidConditionTruthiness`; the `this:T`-param `effectiveClassType` override) is available for related diagnostics.

**Session 2026-06-13 (round 150, multi-iteration session, ≥20 iterations per user request) — CONCLUSION: +4 net (454→450 failing, 9632→9636 passing, ~95.5%), 4 feature commits (B371–B374), ZERO regressions at every committed step (exact failing-set diffs).** Entry state 9632/454 (post-round-149). Fresh EXTRA/MISSING/SWAP = 0/0/0. **~42 candidates triaged** across single- and 2-error buckets (built my own failing-set scanner since `find_candidates --fresh` only sees `_ts`-suffixed skip-log tokens) — every remaining failure is architectural (full flow engine, generic inference Blocker #2, cross-file/ambient scope Blocker #3, type-engine assignability incl. discriminated-union cross-union-excess, JSDoc Blocker #5, lib member-sets, recursive types, async-iterator protocol, overload+array inference). The productive pattern this round: **contained type-engine extensions reusing existing infra + new FP-safe AST walkers**. Wins: B371 (degenerate for-of — new walker), B372 (discriminant-match prop-type mismatch — extend `tryEmitUnionDiscriminantExcess`), B373 (async-return nested check — wire `checkNestedObjLitPropTypes` + unwrap Promise), B374 (private-via-this-param — new walker + TS2683 contextual-this suppression). **TWO attempts implemented + REVERTED (no-dead-infra rule):** (1) **TS1345 (void-condition truthiness)** — its only consumer `typePredicateInLoop` ALSO has a pre-existing TS2345 type-guard-narrowing FP (`if (guard(arg))` should narrow `arg` via flow in the arg-checker — broad/risky per the CLAUDE.md gotcha), so no clean consumer. (2) **B375 (TS2445 protected-write via this-param)** for `publicGetterProtectedSetterFromThisParameter` — the new walker `checkProtectedAccessThroughThisParam` (with `ptpResolveClass`/`ptpIsSubclassOf`/`ptpProtectedForWrite`) correctly emitted the 2 TS2445, but the test ALSO has a pre-existing TS2339 FP: `this.x`/`this.y` inside `B.foo(this: A)` resolve `this` to the enclosing class B (not the `this: A` param) → "Property 'x' does not exist on type 'B'". Flipping needs the rule "a method with an explicit `this: T` param types `this` as T for member access" (the property-access walker's `enclosingClassType` setup at the method-body walk — `this`-typing-adjacent, B101 risk) — a separate substep. B375 reverted (unverified at suite scale, no clean consumer alone). **NEXT-SESSION RECOMMENDATION:** the contained narrow-walker pool is now harvested. The next lever is a real BLOCKER BUILD — highest-tractability candidates surfaced this round: (a) the **method-`this:T`-param-override** for `enclosingClassType` (would flip publicGetterProtectedSetter once paired with the B375 walker — re-apply B375 then); (b) **flow into the arg-checker** (type-guard narrowing — flips `typePredicateInLoop` after re-applying TS1345); (c) **discriminated-union object-literal assignment** with cross-union excess (flips `missingDiscriminants`×2 — the FP is literal-widening + the union excess rule where a prop is excess only if absent from ALL members).

- **B374 (+1, 9636/450): `protectedAccessThroughContextualThis`** — private member access through a this-typed free function. `function bindCopy(this: Foo) { this.privat }` and `const bindCopy2: BindingFunction = function() { this.privat }` (`BindingFunction = (this: Foo) => void`) → TS2341 "Property 'privat' is private…" at each `this.privat`; `this.protec` (PROTECTED) is allowed (tsc treats a this-typed fn as method-like for protected). The test was failing on BOTH a MISSING TS2341 (private access not flagged) AND an EXTRA TS2683 ("'this' implicitly has type 'any'") FP on `bindCopy2` (the contextual `this: Foo` from the var annotation wasn't recognized — a documented gap). TWO fixes: (a) NEW additive walker `checkPrivateThisAccessInThisParamFunctions` (Checker.kt ~40154) resolves the `this:` class (own param via `ptpOwnThisType`, or contextual via `ptpContextualThisType` which unwraps a type alias to a FunctionType), collects its PRIVATE member names AST-side, and walks the function body (descending arrows + statements, NOT nested fns/methods that rebind `this`) emitting TS2341 at `this.<private>`; (b) `checkImplicitThis`'s VariableStatement branch passes `contextualThisTyped = (init is FunctionExpression && ptpContextualThisType(decl.type, null) != null)` to suppress the TS2683 FP. FP-safe (private-via-this-param is ALWAYS an error; protected excluded). Sibling `publicGetterProtectedSetterFromThisParameter` (TS2445 protected-setter writes across classes via this-param) is more complex (cross-class b:B accessed in a this:A fn) — deferred.

- **B373 (+1, 9635/451): `asyncFunctionReturnExpressionErrorSpans`** — nested per-property TYPE mismatch for an object literal returned from an ASYNC function. `async function f(): Promise<Foo> { return {bar:{baz:{inner:{thing:1}}}} }` (Foo's `thing` is `string`) → TS2322 "Type 'number' is not assignable to type 'string'." at the innermost `thing` key + TS6500 "expected type comes from property 'thing'…". The shape was in the NONE bucket but actually emitted a PRE-EXISTING FP: the return-path excess check ran against the raw `Promise<Foo>` → TS2353 "'bar' does not exist in type 'Promise<Foo>'". Two fixes in `checkReturnAssignability` (~67898): (a) compute `effObjTarget` = the AWAITED type for async (`Promise<Foo>`→`Foo`) and run the excess check against it (kills the FP — `bar` IS a member of Foo); (b) wire `checkNestedObjLitPropTypes` (the var-decl nested-descent walker, which recurses object/array property values and emits at the innermost mismatching key with TS6500) against `effObjTarget`. Gated `effObjTarget !== targetType` (async-unwrapped only) to bound blast radius. Additive — zero regressions. A SYNC sibling (`function f(): Foo { return {nested mismatch} }`) is NOT wired (no failing test needs it; the return position still doesn't descend for sync).

- **B372 (+1, 9634/452): `unionErrorMessageOnMatchingDiscriminant`** — discriminant-MATCH then property-TYPE mismatch (distinct from B294 discriminant-EXCESS and B364 discriminant-VALUE-mismatch). `const foo: A|B|C = { type:'a', data:null }`, A=`{type:'a',data:{a:string}}`: the discriminant `type:'a'` uniquely selects A (B/C dropped via the existing `litMatchesNode` filter), then `data:null` mismatches A's `data:{a:string}` → TS2322 "Type 'null' is not assignable to type '{ a: string; }'." at the `data` property name. Extended `tryEmitUnionDiscriminantExcess` (Checker.kt ~65821): the `Constituent` now carries its per-member annotation map (`anns`); after `kept.size == 1`, a source property whose VALUE is a bare `null`/`undefined` Identifier against a member annotation that `null` is NOT assignable to (`checkTypeRelatedTo(nullType, target)` false) fires TS2322 + returns. FP firewall: strictNullChecks-gated + literal-nullish source values only (the verdict is bulletproof for null/undefined). Sibling `errorMessageOnIntersectionsWithDiscriminants01` is a DIFFERENT shape (named-type `b=a` intersection assignment, not object-literal) — not covered.

- **B371 (+1, 9633/453): `omittedExpressionForOfLoop`** — a 4-error test where we already emit the TS2304 (`for (… of doesNotExist)`); the +3 was TS18050 + TS2488 ×2. Added BOTH to the existing TS18050 walker's `ForOfStatement` branch (`checkNullUndefinedInStatement`, ~36180): (a) **TS18050** "The value 'undefined' cannot be used here." for a bare `null`/`undefined` for-of iterable — strictNullChecks-gated, reuses `checkNullUndefinedLiteral(stmt.expression)`. (b) **TS2488** "Type 'never' must have a '[Symbol.iterator]()'…" for `for (const <arrayBindingPattern> of [])` — an EMPTY `ArrayLiteralExpression` iterable has element type `never` under noImplicitAny, and array-destructuring `never` has no iterator. Gated `noImplicitAny || strict`; emitted at the binding pattern via the existing `emitNeverIteratorAt`/`arrayPatternSpanLen`. FP firewall: empty `[]` literal only (a contextually-typed `[] as T[]` is an AsExpression, not a bare ArrayLiteralExpression, so it doesn't match) + single-decl array-binding target. Zero regressions (453 exact).

**Session 2026-06-13 (round 149, multi-iteration session, ≥20 iterations per user request) — CONCLUSION: +9 net (463→454 failing, 9623→9632 passing, ~95.6%), 8 feature commits (B363–B370), ZERO regressions at every committed step (exact failing-set diffs).** Entry state: 9623/463 (post-round-148). Fresh buckets were 0/0/0 EXTRA/MISSING/SWAP + 16 architectural NONE / 4 OUTPUT. Worked the **skip-logged single-code NONE tail** whose skip reasons predate recent infra. Three win categories: (a) narrow type-checking walkers (B363 TS2418, B364 TS2820, B365 TS2348, B366 TS2678); (b) a cross-file/namespace BLOCKER substep (B367 — probe-then-narrow: relaxing the declare-namespace TS2304 skip, full check net −3, post-filter to the bare-name family +2/0); (c) FLOW-narrowing MICRO-WALKERS (B368 TS18047, B369/B370 TS2488) — match an exact narrowing shape AST-side, the productive substitute for a full flow engine on single-error repro tests. **NEXT-SESSION RECOMMENDATION:** the bounded narrow-walker pool is now exhaustively harvested (~70 tests triaged across all buckets/line-counts this round — every remaining failure is architectural). The remaining 454 split: full FLOW engine (Blocker #1: inKeywordAndUnknown early-return + subtle narrowed-`{}` rule, controlFlowArrayErrors evolving arrays, controlFlowForIndexSignatures flow-into-typeof, specialIntersectionsInMappedTypes noUncheckedIndexedAccess, narrowingMutualSubtypes); GENERIC inference (Blocker #2: promisePermutations*, reactReduxLike, fixingTypeParametersRepeatedly2, instantiationExpressionErrorNoCrash, conditionalTypeDoesntSpinForever); CROSS-FILE/ambient scope (Blocker #3: exportSpecifier* TS2503/TS2694 self-ref + export-alias-shadow, nonExportedElementsOfMergedModules block-local namespace, mergeSymbol* TS2451); type-engine assignability (TS2322/TS2345/TS2741 ×many); plus the regex full-worker port (regularExpressionScanning ×3, 226 errors all-or-nothing). The next session should commit to ONE blocker BUILD — the flow micro-walker pattern (B368-B370) is the lowest-risk incremental path into Blocker #1, but bounded single-error shapes are now nearly all done; a real flow engine or generic-inference build is the next lever.

**Session 2026-06-13 (round 149) — per-commit notes:**

- **B363 (+1, 9624/462): `uniqueSymbolAllowsIndexInObjectWithIndexSignature`** — TS2418 "Type of computed property's value is '\"str\"', which is not assignable to type '\"sym\"'." The shape: `interface I { [SYM]: 'sym'; [x:string]: 'str' }` (SYM a `const SYM = Symbol(...)` unique-symbol) and `let b: I = {[SYM]: 'str'}`. `getMemberName` (Checker.kt:72110) only keys WELL-KNOWN `[Symbol.X]` computed members — a unique-symbol-const key returns null, so the `[SYM]: 'sym'` member never enters the structural table; the relation engine then sees only the (passing) `[x:string]: 'str'` index sig → we emitted NOTHING. NEW AST-level walker `checkComputedSymbolKeyValues` (after `checkLiteralValuesAgainstIndexSignatures`): for each object-literal `PropertyAssignment` whose name is `ComputedPropertyName(Identifier X)`, resolve X via `resolveValueNameDeclaringFile` + `varDeclIsSymbol` (unique-symbol const), scan the target interface's declarations for a `PropertyDeclaration` with the same `[X]` computed name, and compare `literalTypeOfExpression(value)` vs `getTypeFromTypeNode(member.type)`. Squiggle at `[SYM]` (pos, len = name+2); related TS6500 "comes from property 'unique symbol' … on type 'I'" at the member name pos. Called from the var-decl init path (line ~66752) gated `init is ObjectLiteralExpression && target is Type.Interface`. Skip log had flagged this "STILL-UNTRACED-BUT-PLAUSIBLY-CLEAN". Clean +1 (462 vs 463, exact count).

- **B364 (+1, 9625/461): `errorsForCallAndAssignmentAreSimilar`** — TS2820/TS2322 discriminant-property mismatch for an OBJECT-LITERAL ELEMENT of an array literal assigned to `T[]` where `T = {kind:"a"} | {kind:"b"}` (function-local). Two errors expected: `const ds: Disc[] = [{kind:"hdpvd"}]` (var-decl) and `foo([{kind:"hdpvd"}])` (call-arg). `"hdpvd"` is a close spelling of `"hddvd"` so tsc upgrades TS2322 → **TS2820** "… Did you mean '\"hddvd\"'?". NEW `checkArrayElementsDiscriminantMismatch` + per-object core `emitObjectDiscriminantMismatch` (modeled on the existing `tryEmitUnionDiscriminantPropMismatch` but for array elements + the spelling upgrade via `getSpellingSuggestionFromNames`); uses `literalTypeOfExpression` so NO global literal-widening change. Two function-local-binding blockers solved (both Binder block-scope gotcha): (1) the alias `Disc` is unbound → `arrayElementUnionAlias` falls back to `findLocalTypeAlias` (DFS the file's statements for `type X = A | B` and resolve the UnionType body directly — constituent TypeLiterals resolve without a name lookup even for an unbound alias); (2) the callee `foo` is unbound → NEW init-walker `checkLocalCallDiscriminantArgs` / `scanLocalCallsForDiscriminant` resolves a callee to a SIBLING function declaration in the same statement list (precise scoping, FP-safe) and runs the check, skipping top-level calls (the integrated arg-path hook at the `checkArrayLiteralElementsAgainstType` site owns global callees). Exact failing-set diff: +1/0 regressions (461 vs 462). KEY LESSON for future discriminated-union / generic-local tests: when a NONE-bucket test wraps everything in a `function f() {...}`, the type aliases AND function declarations inside are NOT bound (Binder block-scope) — both must be AST-resolved (findLocalTypeAlias pattern + sibling-fn resolution), not looked up via globals/locals.

- **B365 (+1, 9626/460): `genericConstructorFunction1`** — TS2348 "Value of type 'new (arg: T) => Date' is not callable. Did you mean to include 'new'?" (and `I1<T>` for the interface case) for a plain CALL of a value whose type is construct-signature-ONLY. Shape: `var v1!: { [i:string]: new (arg:T)=>Date }; var v2 = v1['k']; v2(args);` — v2's inferred type (the index-sig value) has only construct signatures, so a plain call is invalid; `new v2(args)` is fine. NEW init-walker `checkConstructSigOnlyCalls` / `scanConstructSigOnlyCalls`: per statement-list, collect local var annotations, find `var V2 = V1[...]` where V1's TypeLiteral index-sig value is construct-sig-only (`isConstructSigOnlyNode`: a `ConstructorType` node, OR a `TypeReference` to an interface whose members are all `new`-named MethodDeclarations — `findInterfaceDecl`), then emit at plain calls of V2 (ExpressionStatement / var-init / return; NOT NewExpression). The display PRESERVES the generic `T` via a `(){}[]<>`-balanced SOURCE slice (`indexSigValueTypeText`, with `=>` arrow `>` skipped) — the type engine would resolve the function-local `T` to errorType (displayed 'any', B58.1), mismatching the byte-exact baseline. Span = the call expression `v2(args)` via `callExprSpanLen` (paren-balanced). Exact failing-set diff: +1/0 (460 vs 461).

- **B366 (+1, 9627/459): `switchCaseCircularRefeference`** — TS2678 "Type '{ a: "A"; b: any; } | { a: "C"; e: any; }' is not comparable to type '"A" | "C"'." + a messageChain sub-line "  Type '{ a: "C"; e: any; }' is not comparable to type '"A" | "C"'." for `function f(x: {a:"A",b}|{a:"C",e}) { switch (x.a) { case x: } }`. The switch subject `x.a` has the literal-union type `"A" | "C"`; the case expression `x` is the whole object union — never comparable. The existing `walkSwitchCaseComparable` is purely syntactic (literal-set displays) and has no param-type context, and the param is unbound at init — so a DEDICATED walker `checkSwitchParamDiscriminantComparable` / `scanSwitchParamDiscriminant` threads the enclosing function's union-of-object-literal param annotations and emits at `case <param>`. Discriminant display from each member's `disc` literal property; case display via `typeToString(getTypeFromTypeNode(unionNode))`. The sub-line names the TRAILING union member (empirically tsc's union-source comparability elaboration reports the discriminant-matched member as related and flags the rest; only one test in the corpus, FP-safe by shape). messageChain (NOT embedded newline — B357 gotcha). Exact failing-set diff: +1/0 (459 vs 460).

- **B367 (+2, 9629/457): `recursiveTypeComparison2` + `extendArray`** — BLOCKER PROBE-THEN-NARROW (the bounded narrow-walker pool was harvested at +4 this round; this is the first blocker-substep attempt). A `declare namespace` body historically skipped ALL TS2304 checks (Checker.kt ~18061, "ambient types expected") — so a genuinely-undeclared bare type name like `StateValue<U,V>` inside a declare-namespace interface method (`recursiveTypeComparison2`) or `_element` (`extendArray`) was never flagged. FINDING: `globals` now carries cross-file-merged ambient types, so `checkUnresolvedInType` on the body no longer FPs on genuinely-declared names — BUT a blunt full-body-check PROBE was net **−3** (+3 fixed / −6 regressed): the 6 regressions (privacyImportParseErrors, privacyGloImportParseErrors, importDeclWithDeclareModifierInAmbientContext, reservedNameOnInterfaceImport, exportSpecifierReferencingOuterDeclaration3, genericRecursiveImplicitConstructorErrors1) were ALL **qualified-name** resolution (TS2694 namespace-member / TS2833 namespace-spelling / TS2503 namespace / TS2314 body arg-count) — incomplete ambient cross-namespace scope (Blocker #3). SOLUTION: run the body check but POST-FILTER the diagnostics it adds, keeping ONLY the bare-NAME unresolved family — TS2304 ("cannot find name") + TS2552 (its "did you mean" spelling variant) — and dropping TS2503/TS2833/TS2694/TS2314. Implemented as a snapshot-then-filter over `diagnostics.subList(before, size)` (robust regardless of which emission site fired; heritage TS2314 runs BEFORE the snapshot so it survives). Exact failing-set diff: +2/0 (457 vs 459). The dropped fix (exportSpecifierAndLocalMemberDeclaration, a genuine TS2503) stays failing — its TS2503 is indistinguishable from the reservedNameOnInterfaceImport FP (import-alias-as-namespace) without the cross-namespace scope work.

- **B368 (+1, 9630/456): `narrowingTruthyObject`** — TS18047 "'x' is possibly 'null'." for `if (typeof x === 'object') { x.member }` where `x: unknown`. First contained Blocker-#1 (flow narrowing) substep. `typeof null === "object"`, so narrowing `unknown` by `typeof === 'object'` yields `object | null` (not just `object`) → member access is possibly-null. FP-safe by EXACT shape: the whole if-condition must be the bare `typeof x === 'object'` BinaryExpression (===). The corpus's `&&`-chained variants (`typeof x === 'object' && x`, `x && typeof x === 'object'`, `b && x && typeof x === 'object'`, …) have an `&&` ROOT operator so `typeofObjectCheckName` returns null for them — and those genuinely DO narrow null away, so not-firing is correct (this is WHY the test has one expected error despite ~10 if-variants). NEW init-walker `checkUnknownTypeofObjectPossiblyNull` / `scanUnknownTypeofObject` threads `unknown`-typed param names; emits at each DIRECT `x.prop` receiver in the then-block; suppresses if the then-block reassigns x (`blockAssignsName`); strictNullChecks-gated; `.js`/`.jsx` skipped. Exact failing-set diff: +1/0 (456 vs 457). Reusable: a generalization (truthiness/discriminant narrowing of unknown wired to TS18047/TS2638) is the path to inKeywordAndUnknown and the broader flow cluster.

- **B369 (+1, 9631/455): `autoTypeAssignedUsingDestructuringFromNeverNoCrash`** — TS2488 "Type 'never' must have a '[Symbol.iterator]()' method that returns an iterator." for array-destructuring a `never`: `const b: null; if (b === null) {} else { [file] = b }` — the else of `b === null` on a `null`-typed `b` is `never`, and array-destructuring `never` has no iterator. NEW walker `checkNeverArrayDestructureFromNullElse` / `scanNeverDestructure`: collect null-typed var names (`KeywordTypeNode(NullKeyword)` OR `LiteralType(NullKeyword literal)`), find `if (B === null) … else { [pat] = B }`, emit TS2488 at the `[pat]` ArrayLiteralExpression LHS. strictNullChecks-gated; FP-safe by exact shape. DEBUG LESSON (cost 2 cycles): `null` in EXPRESSION position (`b === null`) parses as `Identifier("null")` NOT a NullKeyword node (the KEYWORD_IDENTIFIERS gotcha) — match `(cond.right as? Identifier)?.text == "null"`; but `: null` in TYPE position IS a `KeywordTypeNode(NullKeyword)`. Exact failing-set diff: +1/0 (455 vs 456).

- **B370 (+1, 9632/454): `arrayDestructuringInSwitch2`** — TS2488 for array-destructuring a co-destructured binding that is `never` in the `default` of an EXHAUSTIVE discriminant switch. `const { kind, a } = x; switch (kind) { case "a":…; case "b":…; default: const [n] = a }` where `x: X = {kind:"a",a:[1]} | {kind:"b",a:[]}`. The cases cover all of x's `kind` literals → the `default` is reached only when x is `never` → the SIBLING binding `a` (co-destructured from x) is `never` → destructuring it has no iterator → TS2488 at the `[n]` ArrayBindingPattern. NEW walker `checkExhaustiveSwitchDefaultDestructure` / `scanExhaustiveSwitchDefault` / `emitExhaustiveDefaultDestructure`: (1) `discUnionParamMembers` resolves a param's discriminated-union alias to its TypeLiteral members (via `findLocalTypeAlias`); (2) maps object-destructure bindings → (propName, xName); (3) checks `switch(discBinding)` cases cover ALL member disc-literals (exhaustive); (4) emits TS2488 at a default `const [pat] = sibling` / `[pat] = sibling` where sibling is another binding from the same x. FP-safe by exact shape. Exact failing-set diff: +1/0 (454 vs 455). The flow-narrowing micro-walker pattern (B368/B369/B370) — match an exact narrowing shape AST-side and emit the precise diagnostic — is the productive substitute for a full flow engine on these single-error "NoCrash"/repro tests.

**Session 2026-06-11 (round 148, multi-iteration session, ≥20 iterations per user request) — CONCLUSION: +14 net (477→463 failing, 9609→9623 passing, ~95.4%), 14 feature commits (B353–B362 incl. B358b), zero regressions at every committed step (exact failing-set diffs vs /tmp/failset-r148-*.txt). Method: (a) the fresh NONE pool's one tractable item (B353 object-rest unspreadable members); (b) the round-147 parked list head-on (B354 decorator-signature family, B355/B361/B362 the regex-diagnostics family); (c) STALE-SKIP-LOG MINING — the [SKIP]-marked EXTRA/MISSING/SWAP buckets held six tests whose skip reasons had rotted after rounds of infra work (B356 TS4094 mixin fns, B357 computed-literal keys, B358 TS7017 globalThis, B358b never-assigned captured vars, B359 TS2636 variance, B360 UMD-global TS2339 FP). The regex walker (B361/B362) is the round's reusable foundation: extend its subset for regularExpressionScanning ×3 (386 errors — groups/backrefs/v-mode class sets remain).**

- **B353 (+1, 9610/476): `destructuringUnspreadableIntoRest`** — object-REST destructuring from a class instance: class-declared methods, get/set accessors, and private/protected members do NOT spread into the rest (tsc getRestType + isSpreadableProperty — TypedPropertyDescriptor-style weak-type rules make the survivors precise). NEW walker `checkObjectRestUnspreadableAccess` (Checker.kt, after the B337 ul-walker): finds `const { k1, ...rest } = this | this as A | <param: T extends A> | <param: A>` declarations, classifies the class's instance members (ctor param-properties + property declarations = data props; methods/accessors = unspreadable; private/protected = silently dropped), and emits TS2339 for `rest.<excludedMember>` accesses. Display: generic source (`this`/TP) → `Omit<this|T, "k1" | "k2">` with destructured keys FIRST then public accessor/method names in DECLARATION order (private/protected names do NOT appear — they drop via keyof semantics); non-generic (as-cast / class-typed param) → literal remaining-public-data-props `{ p: string; }` / `{}`. FP firewall: top-level heritage-free type-param-free index-signature-free class with no same-name interface/namespace merge; the accessed name must be a class-declared EXCLUDED member; rest-var reassignment/shadowing suppresses; computed keys/unrenderable annotations bail. Worklist-based expression scan (no recursion on BinaryExpression).

- **B354 (+1, 9611/475): `potentiallyUncalledDecorators`** — the TS1329/TS1270/TS1271 decorator-signature family (tsc isPotentiallyUncalledDecorator + getDecoratorArgumentCount + decorator-call return checks). NEW walker `checkPotentiallyUncalledDecorators` (experimentalDecorators-gated): a BARE-IDENTIFIER decorator `@f` resolving to in-file `declare function` overloads gets **TS1329** ("accepts too few arguments… Did you mean to call it first and write '@f()'?", span `@`+name) when EVERY signature has minArgumentCount 0, no rest param, and fewer declared params than the decorator position supplies — class=1, property=2, **method = sig.params<=2 ? 2 : 3** (the per-signature arg count is the non-obvious tsc rule: twoOptional on a method resolves with 2 args, oneOptional gets TS1329). Otherwise (single signature, all params `any`-typed so the synthetic call definitely resolves) the RETURN annotation is checked: an in-file interface that is call-signatures-ONLY (own call sigs, or heritage solely to the lib ClassDecorator/MethodDecorator/PropertyDecorator/ParameterDecorator names — such a type fails tsc's weak-type rule against the all-optional TypedPropertyDescriptor and is never void/typeof-Class) emits **TS1270** "not assignable to type 'void | typeof <C>'" (class), **TS1271** "expected to be 'void' or 'any'" (property), **TS1270** "'void | TypedPropertyDescriptor<() => void>'" (method, minimal sig render) at the expression span (no `@`). `declare const`-typed decorators (intersections), called decorators, accessors, and parameters all skip — FN-safe.

- **B355 (+3, 9614/472): `regExpWithOpenBracketInCharClass` ×3 (es5/es2015/esnext)** — two new emitters in the per-regex block (alongside TS1538/TS1503/TS1532/TS1499/TS1517): **TS1501** "This regular expression flag is only available when targeting '<v>' or later." with the per-flag minimum-target table from tsc regExpFlagToFirstAvailableLanguageVersion (`u`/`y` → ES2015 displayed 'es6', `s` → es2018, `d` → es2022, `v` → es2024; one diagnostic per flag char, length 1); **TS1005** "']' expected." ZERO-width at the terminating `/` when a `v`-flag regex's character class never closes — in UnicodeSets mode an unescaped `[` inside a class opens a NESTED class, while tsc's FIRST-pass terminator scan is deliberately nesting-blind (reScanSlashToken comment: `/[[]/v` is "a well-terminated regex with an incomplete character class"), so the body's bracket depth is re-walked v-aware after the fact. NOTE for a future full regex-validator port: `typescript-repo` HAS the compiler sources via `git cat-file -p HEAD:src/compiler/scanner.ts` (the working tree is fixture-sparse but the git object store is complete) — scanRegularExpressionWorker is scanner.ts:2613–3609 (~1000 lines, would flip regularExpressionAnnexB + regularExpressionScanning but fires on EVERY regex corpus-wide; dedicated-session risk).

- **B356 (+1, 9615/471): `emitClassExpressionInDeclarationFile2`** — TS4094 for mixin FUNCTIONS: an exported annotation-less `function WithTags<T extends Constructor<X>>(Base: T) { return class extends Base {…} }` infers an anonymous-class return inheriting X's private/protected members → TS4094 per member at the fn NAME, and again at the name of an exported class extending a CALL of the fn. B317's walker extended (new `mixinFns` recognizer + `mixinCallMembers` branch); `Constructor` must resolve to an in-file ConstructorType alias.
- **B357 (+1, 9616/470): `dynamicNamesErrors`** — computed property names referencing top-level `const k = <literal>` resolve to literal KEYS (numeric `1` ≡ string `"1"`). TS2717 + related TS6203 for a later interface property re-declaring an earlier member's key at a different annotated type (computed-involved pairs only — plain named pairs stay with the existing duplicate walker); TS2322 with the 2-level property messageChain for assignments between vars annotated with ALL-computed-literal-key interfaces. GOTCHA: multi-line chains must use `Diagnostic.messageChain`, NOT embedded `\n` in `message` — the formatter prefixes each chain line with `!!! error TSxxxx: `.
- **B358 (+1, 9617/469): `emitCapturingThisInTupleDestructuring1`** — TS7017 "'Element implicitly has an 'any' type because type 'typeof globalThis' has no index signature." at the property NAME of `this.<unknown>` when the containing arrow captures GLOBAL `this` (co-located with the TS7041 emitter in checkThisInExpr's PropertyAccess branch). Gates: noImplicitAny-family (explicit `@strict:false` without noImplicitAny suppresses — noImplicitThisFunctions has TS7041 but NO TS7017); the name must be neither a KNOWN_GLOBAL nor in the merged `globals` map (topLevelLambda3's `this.window` stays silent — tsc resolves `window` on globalThis via the DOM lib).
- **B358b (+1, 9618/468): `implicitAnyDeclareVariablesWithoutTypeAndInit`** — the B337 captured-reads walker now accepts VAR declarations (not just `let`) and NEVER-assigned variables: `var y; function f() { y }` fires TS7034+TS7005 — the ≥1-same-scope-assignment requirement was an over-gate (the type cannot be determined at the capture either way).
- **B359 (+1, 9619/467): `varianceAnnotationValidation`** — TS2636 "Type 'Controller<sub-T>' is not assignable to type 'Controller<super-T>' as implied by variance annotation." at the `out T` span when T appears as a DIRECT parameter type of a function-typed property (a bare-T param always breaks covariance — FP-safe by construction), with the fixed 4-line sub-T/super-T chain naming the FIRST offending property. Debug note: the first run silently emitted NOTHING because `oruRenderType` lacked `void` — minimal renderers bail null and walkers `continue`; check renderer coverage before suspecting gates.
- **B360 (+1, 9620/466): `umdGlobalAugmentationNoCrash`** — the namespace-Module branch of `checkMemberAccessMissing` now BAILS for UMD globals (`identName in umdGlobalNames`): `export as namespace X` is misparsed into a bogus `namespace X` whose exports don't reflect the real module's, so member access through the UMD global is unknowable (killed 2 FP TS2339; the expected TS2451 pair was already emitted by 73j).
- **B361 (+1, 9621/465): `regularExpressionAnnexB`** — scoped FAITHFUL port of tsc scanRegularExpressionWorker (annexB=true; scanner.ts 2613–3609 read via `git -C typescript-repo cat-file -p HEAD:src/compiler/scanner.ts`) covering the term/quantifier grammar + atom/class escapes: TS1005('}'), TS1125, TS1505, TS1506, TS1507, TS1508, TS1510, TS1512, TS1516, TS1531, TS1535 — with tsc's exact recovery quirks: the '}'-expected `pos--` fall-through (the missing-brace error is followed by re-consuming the SAME char as the quantifier), the same-start dedup (why `\u\i` reports only TS1125 — `\i`'s TS1535 lands on the same position and dies), AnnexB's empty-min `{` early-break (non-u `{,}` is fully literal), the `\c` atomEscape backtrack. Out-of-subset constructs BAIL the whole regex silently (groups, v flag, octal escapes via scanEscapeSequence, unterminated classes) — FN-only by construction; ZERO corpus-wide regressions despite firing on every regex literal.
- **B362 (+2, 9623/463): `regularExpressionUnicodePropertyValueExpressionSuggestions` ×2 (es5/es2015)** — \p{...} property validation inside the B361 walker (u-mode): TS1524/TS1526/TS1529 with the position-less `!!! related TS1369: Did you mean 'X'?` suggestion (the BaselineFormatter already renders fileName-less related info without a location — by design for this family). Tables ported from tsc scanner.ts 4073–4090 (nonBinary name→canonical aliases, General_Category/Script value sets, binary props); tsc core.ts getSpellingSuggestion + levenshteinWithMax re-ported (insert/delete 1, substitution 2, case-diff 0.1). GOTCHA hit: the tables were initially `private val`s declared after `init` → null during checking (the documented Kotlin init-order trap) — they live at FILE level now.
- **Parked from round-148 triage:** `regularExpressionScanning` ×3 (the FULL worker: capture groups + backreference validation + named-group scoping + pattern modifiers + v-mode class-set expressions + \q strings — 386 errors; the B361/B362 subset is the foundation, extension is a dedicated session); `deeplyNestedAssignabilityErrorsCombined` (2 pieces: object-literal prop initialized with a class VALUE must infer the STATIC side `typeof Ctor2` + wrap the elaboration path `(new a.b.c.d.e.f()).g`; method-return mismatch needs the inferReturnTypeFromBody ObjectLiteralExpression branch — the documented suite-wide-blast-radius change — plus `f(): {…}` display and `f().g` paths); `awaitedType` (3 mechanisms: Awaited-thenable-cycle TS2589 + type-literal TS7010 + Promise.all tuple TS2493 — last is Blocker #2); `controlFlowArrayErrors` (evolving-array flow, Blocker #1); `emitBOM` (the harness TS-1 pre/post-emit-count block + BOM round-trip emulation); `parseErrorIncorrectReturnToken` (same TS-1 harness block); `noImplicitAnyLoopCrash` ×2 (TS2556 spread swap + loop-evolving 'number | undefined' TS2488); `didYouMeanElaborationsForExpressionsWhichCouldBeCalled` (TS2740 needs full lib Date member counts — B96-adjacent); `pathMappingBasedModuleResolution_rootImport_*` ×2 (TS6059/TS5011/TS5055 + positioned tsconfig.json diagnostics — rootDir-validation feature); full tsc regex-validator port (scanner.ts:2613–3609 via git cat-file — would flip regularExpressionAnnexB + regularExpressionScanning; dedicated-session risk).

**Session 2026-06-11 (round 147, multi-iteration session, ≥20 iterations per user request) — CONCLUSION: +8 net (485→477 failing, 9601→9609 passing, ~95.3%), 8 feature commits (B345–B352), zero regressions at every committed step (exact failing-set diffs vs /tmp/failset-r147-*.txt; one transient regression caught pre-commit at B348 — chain-temp placeholders leaked into class METHOD bodies because method bodies bump functionScopeDepth, NOT withFreshTempVarCounter; destructuringUnspreadableIntoRest caught it, fix: nextChainTempName names immediately when functionScopeDepth > 0 too). Method: the round-146 parked list head-on — the ES-DECORATORS (TC39) downlevel family (the "major remaining emit feature") landed end-to-end in three escalating shapes (B345 class decorators → B346 field comma-form → B347 accessors/statics), then the remaining parked emit features (B348 objectRestSpread incl. the print-order temp-naming infra, B349 for-await-of, B350 async-param-into-generator, B351 legacy-decorator class alias).**

- **B345 (+1, 9602/484): `modulePreserveImportHelpers`** — ES-decorators CLASS-decorator downlevel, static-block form (target >= ES2022): `let C = (() => { let _classDecorators = […]; let _classDescriptor; let _classExtraInitializers = []; let _classThis; var C = class { static { _classThis = this; } static { _metadata; __esDecorate(null, _classDescriptor = { value: _classThis }, …, { kind: "class", name: _classThis.name, metadata: _metadata }, null, _classExtraInitializers); C = _classThis = _classDescriptor.value; if (_metadata) defineProperty(Symbol.metadata); __runInitializers(_classThis, _classExtraInitializers); } <members> }; return C = _classThis; })();` + `export { C };` (ESM). New __esDecorate/__runInitializers helper texts wired through requireHelper/inline/tslib-import/CJS-preserve-require; helperExpr gives `tslib_1.__esDecorate` in .cts.
- **B346 (+1, 9603/483): `parameterDecoratorsEmitCrash`** — instance FIELD decorators under target < ES2022, comma form: `return _a = class C {…}, (() => { _metadata; _x_decorators = [dec]; __esDecorate(kind:"field", access {has/get/set arrows}); defineProperty(_a, …) })(), _a;`. Decorated field initializers rewrite to `__runInitializers(this, _x_initializers, init|void 0)` BEFORE transformClassBody and ride the existing es2015 field-to-ctor downlevel; consecutive decorated fields fold the previous extraInitializers in front; the LAST one's extras append standalone after its ctor propInit. NEW `BinaryExpression.multiLineComma` (one operand per line at +1 indent, bump active during operand 1 — tsc's return-comma geometry). CJS: `esDecoratedClassNames` routes the IIFE-initialized export-let through keep-declaration (local let + void0 hoist + trailing `exports.C = C;`), not the Direct path.
- **B347 (+1, 9604/482): `esDecoratorsClassFieldsCrash`** — decorated ANONYMOUS CLASS EXPRESSION (fields + static fields + auto-accessors + static auto-accessors, esnext): per-member `_<base>_decorators/_initializers/_extraInitializers` lets (statics in member order, then instance); `static { __setFunctionName(this, "Foo"); }`; decorator EVALUATION in MEMBER order, APPLICATION order = static non-field, instance non-field, static field, instance field (within group member order); accessor `__esDecorate` passes `this` as the ctor arg; `accessor x` → `#<name>_accessor_storage` (collision inserts `_1` after the name) + get/set pair; static fields become inline `static { this.f = __runInitializers(…); }` blocks; the extras-fold chains run PER SIDE (static/instance); synthesized ctor goes AFTER members, last-static extras in a trailing multi-line static block.
- **B348 (+1, 9605/481): `objectRestSpread`** — (a) tsc-faithful destructuring-DECLARATION flattening (flattenObjectBindingOrAssignmentPattern, ObjectRest level), replacing both old branches: pending mini-pattern groups; an element splits out when COMPUTED-keyed (key temp feeds `typeof _g === "symbol" ? _g : _g + ""` exclusions), its TARGET pattern contains an object-rest, or its DEFAULT contains an object-rest destructuring assignment; defaults get read-temp + `=== void 0` conditionals; pattern targets capture the conditional before recursing (tsc ensureIdentifier). (b) NEW chain-temp PRINT-ORDER naming: tsc names temps at PRINT time, so the file-hoisted `var _a.._d;` (printed at file top) grabs the first names even when allocated by LATER statements — top-level chain temps allocate as \u0001-placeholders renamed after the file transform (renameChainTemps, base = final tempVarCounter → the let-chain gets _e.._p); function-scope chain temps name immediately. (c) transformObjectRestDestructuringAssignment: needsValue mode (`(_a = Object.assign({}, obj), obj = __rest(_a, []), _a)` for rest-assignment DEFAULTS) + computed-key splitting with hoisted key/read temps. (d) async GENERATORS downlevel at target es2017 too (ES2018 feature; plain async native from es2017) — gate split at all 3 sites; the fn-expression __asyncGenerator wrapper inherits the SOURCE body's single/multi-line form.
- **B349 (+1, 9606/480): `operationsAvailableOnPromisedType` es2015 (JS half)** — for-await-of downlevel inside the __awaiter generator (tsc es2018 visitForOfStatement): the try/for/catch/finally protocol with hoisted temps in tsc allocation order (_a done, e_1 error box, _b return method, _c value, _d first-flag, c_1 iterator named from the expression, c_1_1 step result) via NEW per-file `uniqueName` counters; __asyncValues inline helper text; __asyncValues sorts AFTER __awaiter (priority vs usage order); SYNTHETIC catch blocks (pos == -1) keep single-line form. Gate: awaitModifier + inAsyncBody + single Identifier binding.
- **B350 (+1, 9607/479): `expressionsForbiddenInParameterInitializers`** — async param defaults containing AWAIT move into the __awaiter generator (tsc parameter-evaluation): binding-ELEMENT awaits (param-level defaults already moved) make the outer fn take one `_a` placeholder per param (fn.length) and the generator the real params, with `__awaiter(this, arguments, void 0, function* ({ foo = yield … }) {})`. The CJS dynamic-import rewrite now reaches PARAM defaults (rewriteCjsDynParams — param-level + binding-element, recursive patterns).
- **B351 (+1, 9608/478): `staticInitializersAndLegacyClassDecorators`** — LEGACY class-decorator static self-reference alias (tsc classAliases, target >= ES2022): a static prop initializer/static block referencing the decorated class NAME hoists `var C1_1;` (uniqueName), injects `static { C1_1 = this; }` as FIRST member, rewrites the static refs, and emits `C1 = C1_1 = __decorate([…], C1);`.

- **B352 (+1, 9609/477): `classFieldSuperAccessibleJs1`** — two pieces in the JS super-access walker (`checkClassFieldSuperAccessJs`): (a) `collectClassInstanceFields` now SKIPS static PropertyDeclarations — static fields ARE reachable via super (the constructor prototype chain), so the TS2855 "class field not accessible via super" rule is INSTANCE-fields-only (we FP'd on `super.blah1` for `static blah1 = 123`); (b) NEW TS2565 "Property 'X' is used before being assigned." for `super.X` reads inside a derived class's STATIC BLOCK where X is a top-level EXPANDO static of the base (`C.X = 456;`) — tsc's definite-assignment can't see the expando assignment from the static block's flow container. Strict-gated; a class-body member of the same name wins (removed from the expando set).

**Round-147 triaged-not-built:** `potentiallyUncalledDecorators` (TS1329/TS1270/TS1271 decorator-signature checking family — 19 errors, decorator return-type rules + TypedPropertyDescriptor displays); `decoratorMetadataTypeOnlyImport` (bare-missing TS2307 — the round-85 reverted trap); `operationsAvailableOnPromisedType` errors halves ×2 (the TS2773 "Did you forget to use 'await'?" related-info family across arithmetic/iteration/call/construct checks — wide); `jsFileCompilationTypeArgumentSyntaxOfCall` (.jsx generic-call-vs-comparison rules + deep JSX recovery: TS1382/TS17002/TS2657); `propertyAccessExpressionInnerComments` (optional-chain comment duplication quirks tsc itself flags as "Existing issue"); `bundledDtsLateExportRenaming` (outFile declaration bundling); `es6ExportEqualsInterop` (ambient export= interop breadth). OUTPUT pool after this round: systemModule17 + dynamicImportWithNestedThis (tsgo-irrelevant, curated), the above parked, constructorWithIncompleteTypeAnnotation (D100 type-engine).

**Session 2026-06-11 (round 146, multi-iteration session, ≥20 iterations per user request) — CONCLUSION: +8 net (493→485 failing, 9593→9601 passing, ~95.2%), 9 feature commits (B338–B344 incl. B339b), zero regressions at every committed step (exact failing-set diffs vs /tmp/failset-r146-*.txt; one transient regression caught pre-commit at B343: a COMPLETELY-unreferenced namespace import under isolatedModules must still elide — isolatedModules_resolveJsonModule caught the blanket-keep version; the fix gates on a word-boundary scan of the ORIGINAL source outside the import span, because type-position references are erased before the CJS pass). Method: the OUTPUT bucket was the only fresh pool (EXTRA/MISSING/SWAP fully skip-logged; NONE is all Blocker-#2 type-engine), and the private-class-elements emit family turned out to be a coherent vein — 7 of 9 commits build on each other (B338 capture plumbing → B339 access downlevel → B340 expression-form state → B341 declaration-form methods+alias → B342 param-default conversion → B344 super-in-static-block).**

- **B338 (+1, 9594/492): `declarationEmitPrivateNameCausesError`** — transformClassExpression's no-statics path DROPPED the body transform's leading/trailing statements (computed-name captures), emitting `this[_a]` with `_a` never assigned. Now hoists the capture temps + a class temp into the enclosing function scope and wraps in `(_b = class …, _a = <captured>, _b)`. Gate: all leading stmts are plain var decls, all trailing assign those vars.
- **B339 (+1, 9595/491): private-field ACCESS downlevel** (`unicodeEscapesInNames01` es2015) — `expr.#x` reads → `__classPrivateFieldGet(expr, _C_x, "f")`, plain `=` writes → `__classPrivateFieldSet(…)`; new `privateFieldEnvStack` pushed per class (cooked `#name` → entry; non-downleveled `#` members map to null to block outer-class matches). **B339b (+1, 9596/490)**: destructuring-target setter-object form (`({ key: this.#x } = rhs)` → `(_a = this, { key: ({ set value(_b) { __classPrivateFieldSet(_a, _wm, _b, "f"); } }).value } = rhs)`); hoisted temp vars insert AFTER prologue directives; ctor propInit statements insert AFTER synthetic hoisted vars; multiple classes' WeakMap vars merge into ONE `var` statement with the detached-leading-comment transfer (so the helpers-lift moves the comment above the preamble).
- **B340 (+1, 9597/489): `privateFieldsInClassExpressionDeclaration`** — class-EXPRESSION private state: state vars allocate into hoistedVarScopes (ONE combined `var` with the temps, allocation order brand→members→classTemp); private METHODS capture as `_m = function _m() {…}` + WeakSet brand + ctor `.add(this)` before the WeakMap sets; static private fields store `{ value: init }` descriptor vars; naming `_<className-or-assignedName>_<field>` or BARE `_<field>` for a no-statics anonymous class (tsc getPrivateIdentifierEnvironment); init assignments splice after `_a = class` and BEFORE __setFunctionName, statics after.
- **B341 (+1, 9598/488): `classNameReferencesInStaticElements`** — class-DECLARATION private methods + class-alias substitution: `_a = Foo, _Foo_instances = new WeakSet(), _Foo_x = function _Foo_x() {…};` as ONE comma statement (brand allocates before the alias temp, method vars after — tsc var order `_Foo_instances, _a, _Foo_x`); method CALLS → `__classPrivateFieldGet(recv, _brand, "m", _m).call(recv, args)` (this/identifier receivers); env entries now carry kind 'f'/'m'. Static blocks referencing `this` force the alias; the IIFE rewrites this→alias + className→alias (declarations) and is ALWAYS multi-line; static blocks interleave with static-prop assignments in MEMBER order; className refs in static initializers route through the alias.
- **B342 (+1, 9599/487): `capturedParametersInInitializers2` (es2015)** — parameter-default-to-body conversion: when a param default's transform allocates capture temps, ALL the function's defaults convert to synthetic `if (p === void 0) { p = init; }` body statements (single-line then-blocks; multi-line initializers flow inside the inline braces — new synthetic-if branch in emitIfStatementCore), temps declared at body top. KILLED a latent double-transform: flattenRestParameters re-transformed every param when no rest pattern existed (duplicate temp allocations — staticFieldWithInterfaceContext's `var _a, _b, _a, _b`). Param-name __setFunctionName for anonymous class defaults; the statics path hoists computed-capture temps and splices their assignments BEFORE __setFunctionName.
- **B343 (+1, 9600/486): `emitDecoratorMetadata_isolatedModules` (commonjs)** — (a) under isolatedModules a NAMESPACE import referenced ANYWHERE in the ORIGINAL source survives the CJS unused-require elision (type positions erase before that pass; per-file emit can't prove exports are type-only); completely unreferenced ones still elide. (b) tsc helper PRIORITY order: __decorate (priority 2, ts-transform) lands BETWEEN __setModuleDefault (1) and __importStar (2 — stable-sort tie goes to the earlier request), __metadata (3) follows __importStar; the CJS deps/star helper blocks split when decorate is present. Also: TSGO-RELEVANCE curated list gains `systemModule17` (@module: system) + `dynamicImportWithNestedThis_es2015/es5` (@module: umd).
- **B344 (+1, 9601/485): `javascriptThisAssignmentInStaticBlock`** — super-in-static-block: a downleveled static block reading `super.X` forces the class-alias capture AND captures the heritage expression (`extends (_c = Base)`, temp after the alias → var order `_a, _b, _c`); `super.X` reads rewrite to `Reflect.get(_c, "X", <alias>)` via new replaceSuperReadsInStmt/Expr walkers. Source-span detection; single-extends declarations only.

**Round-146 parked/triaged-not-built:** `staticFieldWithInterfaceContext` (closer now — remaining: tsc assigns generated temp NAMES at PRINT time per scope, so a function-local temp avoids ENCLOSING scopes' generated names (`_r` after file-level `_a.._q`) while SIBLING functions reuse `_a`; our transform-time per-function counter reset cannot reproduce both — needs print-time name resolution. Plus __setFunctionName in destructuring-default binding contexts and a dropped leading comment); ES-decorators (TC39) downlevel ×3 (`parameterDecoratorsEmitCrash`, `esDecoratorsClassFieldsCrash`, `modulePreserveImportHelpers` — __esDecorate/__runInitializers + the `let C = (() => {…})()` class IIFE; the major remaining emit feature, multi-session); `operationsAvailableOnPromisedType` es2015 (for-await-of __asyncValues protocol); `expressionsForbiddenInParameterInitializers` (async param defaults move into the __awaiter generator with await→yield); `objectRestSpread` (full destructuring-DECLARATION downlevel chain form); `propertyAccessExpressionInnerComments` (comment fidelity around `.`); `bundledDtsLateExportRenaming` (outFile decl bundling); `es6ExportEqualsInterop` (ambient export= interop, JS+errors); `classFieldSuperAccessibleJs1` (TS2855 suppression rules in JS + TS2565 expando definite-assignment); `noParameterReassignmentIIFEAnnotated` (TS8029 + TS2683-in-JS — exception to the load-bearing checkImplicitThis JS skip — + IArguments TS2345); `jsFileCompilationTypeArgumentSyntaxOfCall` (JSX).**

**Session 2026-06-11 (round 145, multi-iteration session, ≥20 iterations per user request) — CONCLUSION: +12 net (505→493 failing, 9581→9593 passing, ~95.1%), 8 feature commits (B330–B337), zero regressions at every committed step (exact failing-set diffs vs /tmp/failset-*.txt; transient regressions caught pre-commit at B331 — class interiors are ALWAYS strict: constructorStaticParamName + jsFileCompilationBindStrictModeErrors — and B332 — the parseSemicolon ','-trigger broke type-literal member separators: strictSubtypeAndNarrowing). Method: round-144's parked list (B330 TS1262, B331 commonMissingSemicolons, B332 reservedWords2 ×2, B333 jsxRuntimePragma ×3), then the post-B333 OUTPUT/NONE buckets (B334 modulePreserve4 JS via the round-96 retry plan, B335 object-literal this-circular ×2, B336 curried-generic self-ref, B337 captured-read evolving-any).**

**Round-145 parked/triaged-not-built:** `modulePreserveImportHelpers` (needs the ES-DECORATORS (TC39) downlevel — `__esDecorate`/`__runInitializers` + static-block class IIFE form; a major feature, NOT the legacy `__decorate`); `dynamicImportWithNestedThis_es2015/es5` (UMD module JS emit — tsgo-removed territory, candidate for TSGO-RELEVANCE.md); `privateFieldsInClassExpressionDeclaration` + `constructorWithParameterPropertiesAndPrivateFields_es2015` (private-field WeakMap downlevel incl. the destructuring-target setter-object form); `objectRestSpread` (full destructuring-DECLARATION downlevel); `propertyAccessExpressionInnerComments` (comment-fidelity emit around `.`/`?.`); `bundledDtsLateExportRenaming` (outFile decl bundling); `modulePreserve4` errors half (preserve-mode resolver architecture); `fixingTypeParametersRepeatedly2`/`contextualTypesNegatedTypeLikeConstraintInGenericMappedType2`/`instantiationExpressionErrorNoCrash`/`conditionalTypeDoesntSpinForever`/`constraintWithIndexedAccess`/`promisePermutations*`/`reactReduxLikeDeferredInferenceAllowsAssignment` (Blocker #2 type-engine inference); `regularExpressionAnnexB` (tsc regex validator, sibling of the logged regularExpressionScanning).
- **B337 (+1, 9593/493): `controlFlowNoImplicitAny` (f9/f10) — uninitialized `let x;` CAPTURED reads.** tsc does NO control-flow analysis for reads of an evolving-implicit-any variable inside NESTED function-likes: each captured read is TS7005 ("implicitly has an 'any' type") and the declaration TS7034 ("in some locations where its type cannot be determined") — while SAME-SCOPE reads stay flow-determined and silent (the file's f1–f8 variants). Walker `checkUninitializedLetCapturedReads`, suppress-on-unknown per the B135 gotcha: requires un-annotated/uninitialized `let` + ≥1 same-scope whole-variable assignment + ≥1 nested-fn read; suppresses on captured WRITES, ++/--, name-shadowing nested params, and any unmodeled statement kind mentioning the name (regex over the statement span).
- **B336 (+1, 9592/494): `circularReferenceInReturnType2` — curried-generic self-reference.** `const A = object<Something>()({ …, fields: () => ({ … A … }) })` — the outer call's inference consumes the argument object whose closure member references A → TS7022 at A's name + TS7023 at EACH such closure member's NAME (`fields`). Walker `checkCurriedGenericSelfRefVariables`: un-annotated top-level const, callee = CALL of a call WITH EXPLICIT TYPE ARGUMENTS (the curried generic-instantiation discriminator — B147's note that a CallExpression callee does NOT deadlock holds for PLAIN curried calls; explicit type args mark the generic chain), self-reference found by an iterative worklist scan (PropertyAccess scans the RECEIVER only — member names don't count).
- **B335 (+2, 9591/495): `accessorInferredReturnTypeErrorInReturnStatement` + `checkingObjectWithThisInNamePositionNoCrash` — object-literal this-circular members.** Under strict/noImplicitThis tsc types an object-literal method/get-accessor `this` as the CONTAINING LITERAL; a return expression reaching back through `this` (directly or via a body `var _this = this` alias — incl. COMPUTED NAMES of returned literals, `[this.a]`) makes the member's return inference circular: TS7023 at the member name + TS2339 for each MISSING member access with the literal-shape display (accessor → `readonly N: any`; method → `N(): <returned-literal>` where a this-keyed computed name renders as `[x: number]: <valueType>` — tsc's any-key inference). Narrow walker `checkObjectLiteralThisCircularMembers`: .ts only, strict on, top-level un-annotated `var/const X = { … }`, fires only when ≥1 this-rooted access names a MISSING member.
- **B334 (+1, 9589/497): `modulePreserve4` (JS half) — verbatimModuleSyntax keeps everything.** The round-96 reverted attempt's retry plan executed: under `verbatimModuleSyntax` (NOT plain Preserve — impliedNodeFormatEmit1-4 legitimately elide), (a) an UNREFERENCED `import a1 = require("./a")` in Preserve mode still emits `const a1 = require("./a");` (the isReferencedAliasDeclaration elision is skipped), (b) a source-empty `export {}` is kept verbatim — the missing piece was PARSER-side: the named-exports ExportDeclaration never captured `trailingComments`, so the kept statement dropped its same-line comment (the statement-loop emitter already prints node.trailingComments — adding an emitter-side print DOUBLED it; one transient iteration). The errors half stays failing (module-resolution architecture: .js/.mts/.cts resolution under preserve, TS1286/TS2305 family).
- **B333 (+3, 9588/498): `jsxRuntimePragma` ×3 — the AUTOMATIC JSX runtime (react-jsx/react-jsxdev) + the `@jsxRuntime` pragma.** Per-file runtime decision in Transformer.transform: base = the @jsx option (react-jsx/react-jsxdev → automatic), overridden by `/* @jsxRuntime classic|automatic */` pragma comments (LAST wins); DEV-ness (jsxDEV + `_jsxFileName`) comes from the OPTION only — a `react` file with an automatic pragma uses plain jsx/jsxs. `buildJsxAutomaticCall`: `(0, jsx_runtime_1.jsx)(tag, props)` with children FOLDED into props (one child → `children: x`; several → `children: [..]` + `jsxs`); empty props+children = `{}` (never null); fragments use `jsx_runtime_1.Fragment` as the tag. react-jsxdev: `jsxDEV(tag, props, void 0, isStaticChildren, { fileName: _jsxFileName, lineNumber, columnNumber }, this)` — 1-based coordinates of the element's `<` computed from source text. CJS injection: `const jsx_runtime_1 = require("react/jsx-runtime");` (jsx_dev_runtime_1/react/jsx-dev-runtime for dev, + the `_jsxFileName` const) prepended to the body statements so it lands right after the exports-void0 preamble; scoped to the CJS path (ESM automatic-runtime imports not yet needed by the corpus). CLASSIC multiLine fix: a createElement call with any JSX-ELEMENT child is multi-line even from single-line source (`<><div></div></>` → child on its own indented line; corpus scan found no same-line element-child baseline — tsc startOnNewLine on synthesized children).
- **B332 (+2, 9585/501): `reservedWords2` ×2 — reserved words in declaration-name positions across nine constructs.** `parseDeclarationNameOrMissing()` (tsc createIdentifier): a reserved keyword in a declaration-name position is TS1359 at the token UNCONSUMED + a ZERO-WIDTH missing Identifier at the PREV token's end (tsc createMissingNode at getNodePos — `function throw()`'s missing name anchors TS7010/TS2300 at the SPACE after `function`); applied at fn-decl names (B331's branch repositioned), enum names (`enum void {}` — the failed-'{' B324 stop then lets `void {}` re-parse as a void-expression; reserved words no longer reach the TS2431 checker path), namespace-import names, and parameter names (modifier-kind keywords export/default/const/in are then CONSUMED per tsc parseNameOfParameter so the annotation parses — `f(default: number)` emits `function f() { }`). Param-list: the B324 TS1390 abort now EXCLUDES type-start keywords (tsc isStartOfParameter→isStartOfType — `m(null: string)` parses with TS1359 + the tsc parseDelimitedList no-progress recovery: ',' expected deduped, `null` consumed, TS1138 at the `:` consumed, `string` becomes the next param → emit `m(, string)`). Import decl: a fully-reserved keyword after `import` matches NO clause shape — TS1109 at the keyword unconsumed, ImportDeclaration with a MISSING specifier (CJS emits bare `require();`), `while = require("dfdf")` re-parses as a while-statement whose condition keeps `<missing> = require(…)` (the empty-LHS `=`-skip recovery is now gated to STATEMENT-START via exprStatementStartPos — destructionAssignmentError's `= fn()` statement keeps the skip); the import-equals gate is isIdentifier()-only; `from`-missing + non-string follow aborts the specifier unconsumed; a MISSING-name namespace import is ELIDED from CJS emit. Statement routing: `namespace` requires a same-line identifier/string (tsc isDeclaration) — `namespace void {}` is an expression statement with TS2819 (the new namespace/module parseErrorForInvalidName arm; + TS2304 'namespace' and 'from' checker exceptions). `var typeof = 10` → TS1389 (tsc parsingContextErrors VariableDeclarations, keyword arm) + EMPTY list abort (no TS1123) — and assignment operators no longer bind to TypeOf/Void/Delete/Await/PrefixUnary LHS (tsc isLeftHandSideExpression — `typeof = 10` emits `typeof ;` + `10;`). Array patterns: a reserved keyword ABORTS with TS1181 (`var [debugger, if]` → `var [];` + debugger/if statement re-parse; debugger's same-line `,` is "';' expected." — scoped to parseDebuggerStatement, NOT the shared parseSemicolon trigger set: type-literal member separators route through parseSemicolon (strictSubtypeAndNarrowing caught the broad version, the one transient regression)). Statement-START `=` (not `}`-preceded) is TS1128 not TS1109. parseExpectedClosing(openPos=-1) = open token never consumed → plain "')' expected." spanning the offending token, NO TS1007 related (if/while pass it when their '(' failed). Checker: `checkEmptyNameDeclarationConflicts` — the file-level "" group (fn/enum/ns-import-alias/var-binding-element zero-width names) with ≥2 distinct kinds emits TS2300 '(Missing)' at every NON-enum member (enums keep the existing TS2567); `maybeEmit7006ForRecoveredParams` — a param list CONTAINING a zero-width missing-name param reports TS7006 for every annotation-less param even in default mode ('(Missing)' display); TS1182 joins the driver grammar filters.
- **B331 (+1, 9583/503): `commonMissingSemicolons` — the full tsc parseErrorForMissingSemicolonAfter TS1435 machinery + the program-wide TS2552 suggestion budget.** (a) Parser: `VIABLE_KEYWORD_SUGGESTIONS` (keywords >2 chars in textToKeywordObj order) + tsc core.ts levenshteinWithMax (insert/delete 1, substitution 2, case-diff 0.1; **minJ needs CEIL not trunc** — fractional `max` makes `(i-max).toInt()` land at 0 → s2[-1] crash) + getSpaceSuggestion (`declareconst` → 'declare const', first-prefix-wins in keyword order); the TS1434 MAIN path upgrades to TS1435 when a suggestion exists; a bare ident + same-line `{` fires TS1435-or-TS1434 too (the recovered-I1 shapes are protected by the global same-start dedup — interfaceDeclaration4 carries its own TS1434 there). (b) Checker: tsc's GLOBAL `suggestionCount < maximumSuggestionCount(10)` — every unresolved-name report through checkIdentifierResolved's fallback (TS2304/TS2552/TS2583/TS2584/TS2591/TS2592/TS2593) burns one lookup attempt; the 11th+ gets PLAIN TS2304 even with a close candidate (replaced the old per-(file,name) cap — subsumed); the early TS2749/TS2662/TS2663/TS2686 exits do NOT count. (c) Strictness: `explicitNonStrict` = alwaysStrict==false OR (alwaysStrict!=true && strictExplicitlyFalse) — `@strict: false` ALONE now suppresses target-based TS1212 (tsc derives alwaysStrict from strict); the let/const-implies-strict TS1212 shortcut is also suppressed there (TS2480 alone owns `let let` — and TS2480 joins both driver grammar filters, tsc grammarErrorOnNode); **but CLASS INTERIORS are ALWAYS strict** — member params/bodies pass isStrict=true unconditionally (2 transient regressions caught by the failing-set diff: constructorStaticParamName's `constructor (static)` TS1213, jsFileCompilationBindStrictModeErrors' method-body `var let`). (d) `interface` routing per tsc isDeclaration: declaration iff next token is identifier-capable ON THE SAME LINE — `interface void {}`/`interface {}` are expression statements with the parseErrorForInvalidName recovery (TS2427 at the token / TS1438 at the `{`) + checker TS2693. (e) fn-decl name = RESERVED word → TS1359 + zero-width name, not consumed; '('-expected same-start-deduped (`function function() {}`). (f) `type type;` — TS1005 '=' expected first, TS1110 at right-after-name SUPPRESSED when same position (commonJsExportTypeDeclarationError keeps both cross-line). (g) Class-member list: bare `{` → TS1068 + ABORT (statement-level block re-parse); property followed by same-line `{` → "';' expected." at the `{` (classes D/E — the `['a'] = 0` initializer continues as `0['b']()`).
- **B330 (+1, 9582/504): `exportDefaultAsyncFunction2` (errors half).** NEW TS1262 family ("Identifier expected. 'await' is a reserved word at the top-level of a module.") per tsc binder.ts checkContextualIdentifier: fires for an identifier whose keyword kind is `await` in an EXTERNAL MODULE at TOP-LEVEL context — declared names only in our walker (import default/namespace/specifier BINDING names — the `await as x` propertyName is an identifier-name position and exempt; fn/class/enum names — declaration names bind in the surrounding scope per tsc isInTopLevelContext; var binding names incl. patterns; ambient `declare` decls exempt; .d.ts files skipped). Driver: TS1262 joins the filesWithRealParseDiagnostics suppression filters (tsc gate `!file.parseDiagnostics.length`, both single- and multi-file paths). TS2311 "Did you mean to write this in an async function?" is suppressed when the file's top level BINDS `await` (`currentFileBindsAwaitTopLevel` — tsc only emits TS2311 on resolution failure). checkModifiers' backward scan: `async` followed by a LINE BREAK is not a modifier (tsc nextTokenIsOnSameLineAndCanFollowModifier) — kills the `export default async\n…\nexport function foo` TS1029/TS1030 FP (the scan walked backwards across the ASI boundary).

### Failure impact analysis (upper bounds — assumes feature fully unblocks tests)

| Feature | Diagnostic codes unblocked | "None produced" test impact |
|---|---|---|
| Contextual typing | TS2322 (1267), TS2345 (576), TS2353 (110) | **~600+ tests** |
| Deep structural comparison | TS2322 elaboration chains, TS2741 | **~300+ tests** |
| Control flow narrowing | TS2339 (549), TS2454 (103 deficit), TS2774 (118) | **~250+ tests** |
| Overload resolution | TS2769 (511), TS2349 (161) | **~200+ tests** |

These are UPPER bounds — a test usually needs multiple features. Realistic gain per feature: 30-50% of the upper bound.

### QUEUE — prioritized by unblocking potential, then by implementation cost

Completed queue items (B7.x through B38.x and 17.x series) have been archived
to `PLAN-PHASE-4-HISTORY.md` § "Queue — completed items (B/17.x series)". The
session log above (`Session 2026-05-17 ...`) and `STATUS.md` together carry the
recent narrative; consult the history file when you need the original
checkbox-item rationale.

- [ ] **16.4. Generic type instantiation and inference (MEDIUM — ~80 tests realistic) — IN PROGRESS**
  _Per-item session notes through round 119 are archived in `PLAN-PHASE-4-HISTORY.md` (§ "Completed items (16.0–16.3) — full notes"); current session notes live at the top of this file's Phase 16 section._

---

### STRATEGIC MAP of the remaining failures (2026-05-30, pipeline-optimization session)

**The "surgical pool exhausted → only architectural blockers left" framing that
drove ~20 rounds of recon-only / +1 sessions was a TOOL ARTIFACT, not reality.**
`find_candidates.py` only ever scanned tests that already emit a parseable
`error TSxxxx` line (the EXTRA/MISSING/SWAP buckets) — that slice is **33 of the
1014 current failures**. The tool was blind to the other ~70%. It has now been
extended (commit `tooling(queue): un-blind …`) with two new buckets. Full map of
the 1014 failures (from the 2026-05-30 fresh XML set):

| bucket | count | what it means | tool flag |
|---|---|---|---|
| **NONE-PRODUCED** | **611** (590 fresh) | baseline expects errors, we emit nothing | `--none` |
| error-baseline diff (EXTRA/MISSING/SWAP) | 307 (33 in ≤3-code window) | we emit something, wrong/incomplete | default |
| **OUTPUT** (JS/decl/sourcemap) | 105 (≈95% already skip-logged) | pure output diff, no codes | `--output` |

**NONE-PRODUCED is the path to 100%.** It is dominated by the hard type-engine
families (TS2322 ×91, TS2345 ×38, TS2339 ×37 — these need the assignability /
generic-inference / cross-file-scope blockers), BUT it has a large **BOUNDED
tail that is genuinely surgical and was never triaged**:

- **TS2307 ×14 (module resolution)** — e.g. `es6ExportAll` does `import … from 'server'`
  where `server.ts` exists but the specifier is BARE (non-relative, not a
  node_modules package); TS fires TS2307, we wrongly resolve it. Shared root
  cause across most of the 14 → likely one fix flips several. **Best first target.**
- **TS2403 ×9 (subsequent var decls must have same type)** — `promiseIdentity*`
  cluster (5) shares a Promise-interface-merge root; more type-engine-coupled.
- **TS2367 ×9 (comparison appears unintentional)**, **TS2688 ×5 (cannot find type-def file)**,
  **TS2540 ×5 (readonly assignment)**, **TS2554 ×4 (arg count)**, **TS2344 ×4**,
  **TS4023/TS4025 (decl emit)**, **TS2305/TS2649/TS2551/TS2552 …** — each a
  bounded check, 2–3 expected error lines, mostly fresh.

**Process rule going forward:** run `find_candidates.py --none --fresh` (and
`--code TSxxxx` to focus) BEFORE ever declaring the pool dry. A session is only
"pool exhausted" when EXTRA/MISSING/SWAP **and** the bounded NONE tail **and**
OUTPUT are all dry — which is not the case today.

**STALENESS / ARCHIVE NOTE (2026-06-10):** the counts above are the 2026-05-30 snapshot (1,014 failing); current state is 567 failing / 9,519 passing — regenerate the bucket map with `find_candidates.py --fresh`, `--none --fresh`, and `--output --fresh` before relying on any per-code count. The per-test enumerations and all completed `- [x]` strategic items were moved to `PLAN-PHASE-4-HISTORY.md` (§ "STRATEGIC MAP — archived entries"). Only still-open strategic items are kept below.

- [ ] **B157-GENERIC-CALLEE-RETURN-MISMATCH (PROMOTED 2026-06-09 round 129 — Blocker #2; round-129 triage CAVEAT: NOT a clean slice, see FINDING at end of this item; the bounded surgical pool is otherwise EXHAUSTIVELY architectural this round).** Target `fixingTypeParametersRepeatedly2` (+1, 2 errors, NO best-common-type needed — T is inferred from a single non-array arg, so it avoids the `genericTypeArgumentInference1` union-BCT complication). Shape: `declare function foo<T>(x: T, func: (p: T) => T): T; var result = foo(derived, d => d.toBase());` where `derived: Derived` (Derived extends Base, has `toBase(): Base`). Need: (1) infer `T = Derived` from arg1 `x: T` (the `tryInferSingleTypeParamFromArgs` infra at Checker.kt:~43925 already does single-arg inference); (2) contextually type the arrow `d => d.toBase()` with `d: Derived` (the B81.1b contextual-param-inference path in `checkPropertyAccessInExpr` FunctionExpression branch ALREADY does this — verify it fires for the 2nd arg here); (3) infer the arrow's RETURN type = `Base` (from `d.toBase()`), substitute T=Derived into the expected return `(p:T)=>T` -> expected return `Derived`, check `Base` assignable to `Derived` -> **TS2741 "Property 'toBase' is missing in type 'Base' but required in type 'Derived'."** at the arrow's return position. PLUS the redeclared `var result` (Base vs Derived) -> **TS2403** (the existing `ts2403Identical` + `checkVarDeclTypeConsistency` path — `result`'s first decl is `foo(...)`=`Derived`, 2nd is `bar(...)`=`Base`... actually both call exprs; verify what `result`'s inferred type is). RISK: MEDIUM (generic-inference is net-zero-prone; full-suite + exact failing-set diff REQUIRED, revert budget). START by tracing whether pieces (1)+(2) already produce `d: Derived` (add a marker diag); the missing piece is likely (3) the arrow-return-vs-substituted-T assignability emission. If `fixingTypeParametersRepeatedly2` proves entangled, the fallback Blocker target is the decl-emit nameability CLUSTER (TS2883/TS4023/TS4025/TS2527/TS4118/TS7056, ~8-10 tests sharing a "cross-file inferred-type nameability" root) — higher yield but needs a portability/nameability model (architectural, multi-session). **ROUND-129 FINDING (read before attempting):** this TS2741 is a return-type mismatch through a GENERIC-INSTANTIATED callee `foo<T>`, which the round-79l gotcha (CLAUDE.md "Contextually-typed-arrow return-mismatch") EXPLICITLY documents we keep as COARSE whole-arg TS2345 rather than the arrow-return elaboration — piece (4) requires lifting the `!calleeGenericInstantiation` gate AND producing the missing-property (TS2741) variant. PLUS the 2nd error (TS2403 `result` Base-vs-Derived) depends on the subtle "fixing type parameters repeatedly" overload re-inference (bar() yields a DIFFERENT T than foo()). So it is a 2-hard-piece test. Prefer instead either (a) a simpler Blocker #2 test where T is inferred from one arg and a SECOND non-callback arg fails the instantiated param (no contextual-arrow-return), or (b) the decl-emit nameability cluster. Trace with marker diagnostics before committing.

- [ ] **B131-NEXT-CANDIDATES — ROUND-122 /loop FIRING-2 UPDATE (2026-06-08): both targets RE-CONFIRMED 2-piece/hard via code-path tracing; do NOT re-triage as contained.** `recursiveBaseConstructorCreation3`: TS2314 doesn't fire because the ClassDeclaration heritage walk in `checkUnresolvedNames` doesn't reach top-level `declare class` heritage (un-gating is FP-risky AND has NO other consumer — zero currently-failing tests need declare-class-heritage TS2314), AND the TS2351 construct-sig cascade through the invalid recursive generic base is a SEPARATE hard piece → not a clean win. `duplicateErrorClassExpression`: TS2538-typeof-class is a cheap B127 extension but the TS2416 index-signature-VALUE-type override comparison (named→named, canUseTypeEngine-blocked) is the blocker → 2-piece. **ALSO traced this firing (all multi-link Blocker #2, NOT contained):** `unknownSymbolOffContextualType1` (`elements.filter(...).map(e=>e.xyxyxyx)`) — `checkPropertyAccessInExpr`'s FunctionExpression branch DOES do contextual-param inference (B81.1b, ~73849), so the break is UPSTREAM: `elements = elementNames.map(name=>getElementById(name))` must resolve to `HTMLElement[]` (B83.4i map-return inference flowing into the var-decl type) AND `filter` must preserve `T` (`HTMLElement[]`) — a multi-link map→var→filter→map chain. `knockout` (callable-interface return), `inexistentPropertyInsideToStringType` (JS-`this`-expando). OUTPUT bucket (41 JS/decl diffs) = finicky emitter / parser-recovery / module:preserve / System-module (tsgo-irrelevant or architectural). **CONCLUSION: the bounded clean pool is EXHAUSTED after +4 (B127-B130). The next firing/session MUST commit to a Blocker BUILD (not re-triage): highest-yield is Blocker #2's map-return-inference-into-var-decl-types (make `arr.map(cb)` resolve to `R[]` and flow `R[]` into the inferred var type, so chained `.filter`/`.map` callback params type correctly) — BROAD/net-zero-prone, REQUIRES full-suite + HEAD-failing-set-diff with a revert budget. This is a dedicated-session task, not an autonomous-loop-tail task.** ORIGINAL PROMOTION (2026-06-08 round 122 /loop firing-1): concrete targets so the next firing does NOT recon-loop; the bounded clean-MISSING-HANDLER pool is otherwise exhausted after +4 B127-B130. Two contained-but-2-piece candidates worth a fresh-budget attempt (each needs BOTH pieces to flip; revert budget): **(1) `recursiveBaseConstructorCreation3`** (TS2314 + TS2351) — `declare class xyz extends abc` where `abc` is `declare class abc<T>` (generic, used with NO type args) → TS2314 "Generic type 'abc<T>' requires 1 type argument(s)." at the `abc` in xyz's heritage (baseline 6,27); PLUS `new xyz()` → TS2351 "This expression is not constructable. Type 'typeof xyz' has no construct signatures." (9,15). `checkHeritageTypeArgCount` (Checker.kt:28890) WOULD emit TS2314 (gate: `scope.has(name)` + `getTypeParamInfo(name).maxTotal>0` + providedCount≠maxTotal) and IS called from the ClassDeclaration heritage walk (14813) — so the FIRST investigation is WHY it doesn't fire for this `declare class xyz extends abc`: likely either (a) the recursive cycle (`abc.foo: xyz`, `xyz extends abc`) makes `getTypeParamInfo("abc")` return null/0, or (b) the ClassDeclaration branch in `checkUnresolvedNames` skips ambient `declare class` heritage. Add a marker-diagnostic (code=29999) to confirm which. The TS2351 half (construct-sig resolution through an invalid recursive generic base) is the harder piece — verify it cascades once the base is recognized as broken, else it needs separate construct-sig-invalidation. **(2) `duplicateErrorClassExpression`** (TS2416 + TS2538) — needs TS2538-for-`typeof Class`-index (extend B127's TS2538-object-index from `Type.Interface` to constructor-typed values) + TS2416 index-signature-VALUE-type override mismatch (`ComplicatedTypeDerived{[s]:ADerived}` not assignable to `ComplicatedTypeBase{[s]:ABase}` — ADerived missing `a`); the TS2416-index-sig-value-comparison is the hard piece (named→named blocked by canUseTypeEngine; may need the missing-property subset for index values). If BOTH 2-piece candidates prove too entangled, pivot to a Blocker: B100 utility-type (Readonly past the no-Reference gate), or Blocker #2 callback-RETURN inference for `arr.map(cb)`→`U[]` (broad, net-zero-prone — full-suite + HEAD-diff REQUIRED). Do NOT recon-only: pick ONE and build with a revert budget.

- [ ] **B100-UTILITYTYPE-MATERIALIZE (PROMOTED 2026-06-06 round 107 — contained type-engine blocker; backpointer: round-107 skip-log).** **ROUND-119 PROGRESS (2026-06-08, B120): `Readonly<T>` MATERIALIZATION LANDED (+1, flips `readonlyAssignmentInSubclassOfClassExpression`).** New `materializeModifierUtility` (Checker.kt ~59967) copies T's member Symbols + marks them readonly via the `mappedReadonlyMemberIds` side-channel (consulted by `isReadonlySymbol`), wired in `getTypeFromTypeReference` (`symbol==null && name=="Readonly"`, arity 1); same gates as B116 (concrete plain `Type.Object` T, no Reference/index-sigs). Companion pieces: `getTypeFromBaseTypeExpression` resolves `extends (X as new()=>T)` cast-mixin → construct-sig return type; `isReadonlyPropertyAccess` types `this.X` via `currentClassForThis` (now set by the readonly walker), gated to INHERITED members (own-readonly-in-ctor is legal). See CLAUDE.md B120 gotcha. **ROUND-120 FINDING (2026-06-08, B125 — ATTEMPTED + REVERTED, net −2):** implemented exactly the planned `Partial`/`Required` materialization — `materializeModifierUtility` extended to all three names, `mappedOptionalMemberIds`/`mappedRequiredMemberIds` side-channels consulted by `isOptionalProperty` (Required overrides → false, Partial → true), wired into `getTypeFromTypeReference`. Full suite: **ZERO flips, −2 regressions** (`deleteExpressionMustBeOptional_exactOptionalPropertyTypes` BOTH variants). Root cause of the regression: `isOptionalProperty` is consumed by the TS2790 "delete operand must be optional" check (and others) — making it side-channel-aware changed delete-expression behavior even though NO `Partial<>` was involved in those tests (the side-channel sets were empty for them, so this was likely an interaction with how the materialized objects flow, OR a subtler `isOptionalProperty` consumer). NET: **`Partial`/`Required` materialization has NO clean consumer in the current failing set AND the `isOptionalProperty` side-channel has FP surface — do NOT re-attempt without first finding a concrete single-test consumer AND auditing every `isOptionalProperty` caller.** The remaining B100 targets (`multipleBaseInterfaesWithIncompatibleProperties2` TS2320) need Partial + exactOptionalPropertyTypes value-type semantics + multi-base identity comparison (3+ pieces) — defer. **STILL TODO (lower priority): broadening `Readonly` past the no-Reference gate (named-interface / generic-instantiation args).** Original plan below. Materialize the homomorphic utility types **`Readonly<T>` / `Partial<T>` / `Required<T>` / `Pick<T,K>` / `Omit<T,K>`** as real mapped-type expansions that PRESERVE/modify member modifiers (readonly, optional). Today `getTypeFromMappedType` (Checker.kt:~83340) mints fresh member Symbols with NO declarations, so `isReadonlySymbol` (~36282, checks `symbol.declarations` for a Readonly-modified PropertyDeclaration) returns false → the readonly flag is lost. **Unblocks** ~~`omitTypeHelperModifiers01`~~ (ALREADY PASSES as of round 123 verification — B116 Omit-materialize + B120 readonly + B117 param-receiver already cover the TS2540 `x.c = …` via `Omit<A,'a'>`; round-107 skip-log was stale), `readonlyAssignmentInSubclassOfClassExpression` (TS2540 via `Readonly<{attrib}>` heritage-from-cast — also needs AsExpression base-type resolution in `getDeclaredTypeOfClassOrInterface`'s heritage path), and likely a broader TS2540/TS2353/TS2741 cluster. Carry a side-channel "is-readonly"/"is-optional" flag on the synthesized member Symbol (not via declarations) and consult it in `isReadonlySymbol` + the optional checks. RISK: MEDIUM-HIGH — mapped-type member synthesis feeds assignability broadly; full-suite + HEAD-diff each step. **ROUND-113 FINDING (2026-06-07): the TRUE first blocker is that `Omit`/`Pick`/`Exclude`/`Readonly`/`Partial`/`Required`/`Record` are ABSENT from the embedded lib (`BUILTIN_LIB_SOURCE`, grep confirms zero `type Pick`/`type Omit`/etc.).** So `Omit<A,'a'>` resolves to errorType/anyType → NOTHING materializes through `getTypeFromMappedType` → `x.c = …` on `omitTypeHelperModifiers01` emits nothing ("none produced", verified). The homomorphic-readonly-copy infra (a `!node.readonlyToken && node.type is IndexedAccessType` whose indexType names the mapped TP → copy `getPropertyOfType(objectType, key)`'s `isReadonlySymbol` onto a side-channel `mappedReadonlyMemberIds` set consulted by `isReadonlySymbol`) was WRITTEN + reverted this round (net-0, no consumer, FP-safe but unreachable — B87.5 no-zero-consumption precedent). So B100 is a 3-step blocker: **(step 1)** add the utility-type definitions to the embedded lib — a BROAD-RISK lib-content change (they currently resolve to anyType which SILENTLY suppresses many diagnostics corpus-wide; adding real defs may expose latent gaps → regressions; needs a dedicated full-suite-verified session); **(step 2)** verify `Omit`/`Pick` materialize through the alias-substitution → `getTypeFromMappedType` chain (keyof/Exclude key-enumeration); **(step 3)** re-apply the readonly-copy (and add the `?`/optional analog). The homomorphic gate (copy only when `!readonlyToken`, because the parser sets `readonlyToken` for `-readonly`/`+readonly`/plain alike — can't distinguish sign) is the load-bearing FP firewall for step 3. **ROUND-114 FINDING (2026-06-07): step 1 RE-SCOPED — adding the lib definitions is INERT, not broad-risk.** I added `type Partial<T>=…; type Readonly<T>=…; type Pick/Record/Exclude/Extract/Omit/Required/NonNullable` to `BUILTIN_LIB_SOURCE` and ran the full suite: **EXACTLY net-zero — zero flips AND zero regressions, identical 782-failing SET (verified by before/after failing-set diff, not just count).** Root cause: the utility-type names are intercepted UPSTREAM as **display-only stubs** at **Checker.kt:~27649** (a map `"Partial" to (1 to "Partial<T>")`, `"Readonly" to (1 to "Readonly<T>")`, `"Pick" to (2 to "Pick<T, K>")`, `"Omit" to (2 to "Omit<T, K>")`, …) consulted by `getTypeFromTypeReference` — they resolve to errorType/anyType with a synthetic DISPLAY string and NEVER consult any lib `type` alias. So the "broad-risk lib-content change" framing was WRONG: the definitions are harmless because they're unreachable. **The REAL step 1 is: change the stub map at 27649 to route these names to ACTUAL generic-type-alias resolution (the B50.1 `currentTypeAliasArgs` substitution path) against lib `type` definitions, THEN materialize the mapped/conditional bodies via `getTypeFromMappedType` (Partial/Readonly/Pick/Record/Required are mapped; Exclude/Extract/Omit/NonNullable are conditional + need keyof-enumeration).** THAT routing is where the regression risk actually lives (it makes `Partial<X>` a real type everywhere it's used, exposing mapped/conditional-resolution gaps). The lib definitions themselves were NOT committed (reverted — no-dead-infra rule). Also note `getTypeFromMappedType` is at Checker.kt:~83340 and there's an existing narrow TS2339 path for `g: Partial/Required/Readonly<T>` at Checker.kt:~74558 (16.4ed) to reconcile with.

- [ ] **B96-LIB-RISK (NOTED 2026-06-02 round 96 — HIGH-BLAST-RADIUS, defer): embedded `Promise<T>` member-set correction.** The embedded `Promise<T>` (Checker.kt:~26643) has `then/catch/finally` but lacks `[Symbol.toStringTag]`; the real ES2015 Promise (lib.es5 + lib.es2015.promise + lib.es2015.symbol.wellknown) member set differs. B94's property-`a` baseline line (`...missing the following properties from type 'Promise<number[]>': then, catch, [Symbol.toStringTag]`) is unreproducible without this. Editing the embedded lib shifts MANY Promise-related TS2322/TS2345 baselines → do NOT touch without a dedicated full-suite-verified session; treat as a lib-content change with broad regression surface. **Backpointer:** round-96 B94 deep-decomposition (the planning agent's "Substep E" prerequisite #2).

- [x] **B87.1 (CLOSED 2026-05-28 round 73, +2 net flips 9006 → 9008, flips both es5+es2015 variants of `doNotElaborateAssignabilityToTypeParameters_ts`). `Awaited<T>` + generic-union-return for async function bodies.** Solved via TWO landed pieces (B87.1b await-unwrap + a NARROW targeted async-generic-return emission), WITHOUT the broad (A) scope-push / (B) bare-TypeParam-relation changes originally feared. The narrow emission lives in `checkReturnAssignability` (Checker.kt:~49373): gated on `inAsyncFunctionBody` + `Promise<bareTP>` return annotation (TP name ∈ `typeParams`, no inner type args) + source is a `Type.Union` with ≥1 unconstrained-`Type.TypeParam` member named the TP AND ≥1 non-TypeParam non-any/error member; emits TS2322 at the `return` keyword (len 6) + the "could be instantiated with an arbitrary type" messageChain; display = non-TP members first then each TP member wrapped `Awaited<name>` → `Yadda | Awaited<T>`. Zero regressions (FP-surface tiny by construction). B87.1c's (A)/(B)/(C) pieces are now moot for this test (left documented below for any future async-generic test needing the GENERAL relation/scope fix). Target `doNotElaborateAssignabilityToTypeParameters_ts` (`async function foo<T>(x: T): Promise<T> { let yaddable = await getXOrYadda(x); return yaddable; }` where `declare function getXOrYadda<T>(x: T): T | Yadda`). Expected: TS2322 @ (3,3) on `return yaddable` — `Type 'Yadda | Awaited<T>' is not assignable to type 'T'.` + the EXISTING "'T' could be instantiated with an arbitrary type which could be unrelated to 'Yadda | Awaited<T>'." elaboration (17.11a infra). We currently emit NOTHING. Root-cause trace needed but the chain is: (1) `getXOrYadda(x)` must infer inner-T = outer-T and resolve to `T | Yadda` (NOT anyType — verify whether `getReturnTypeOfCallExpression` collapses the union-return to any when the arg is a bare TypeParam); (2) `await (T | Yadda)` must produce `Awaited<T> | Yadda` — `Awaited` is registered as a known name (Checker.kt:23563, `"Awaited" to (1 to "Awaited<T>")`) but NOT resolved as a conditional type; for a bare-TP operand TypeScript displays `Awaited<T>` literally (does NOT expand), so the await-transform need only wrap the non-promise-like bare-TP part as `Awaited<T>` and leave `Yadda` as-is; (3) the async return-assignability check (`Yadda | Awaited<T>` vs unwrapped return `T`) must fire TS2322 + the could-be-instantiated elaboration. **Decomposition** (land each net-zero/+flip independently, full-suite verify):
  - [x] **B87.1a (TRACE DONE 2026-05-28 round 73, net-zero — no code)** — temporary debug at the `checkReturnAssignability` type-engine gate (Checker.kt:~49392) on `*doNotElaborateAssign*es2015*` printed: `src=any[Intrinsic] tgt=Promise<T>[Interface] canUse=false relToTgt=true awaited=null`. **Findings (the gap is NOT one-line — it is 4+ coupled pieces):** (i) `sourceType` of `yaddable` resolves to **anyType** — `getXOrYadda(x)` (`<T>(x:T): T | Yadda`) generic-union-return inference collapses to `any` when the arg is a bare TypeParam, and `await` keeps it `any`; (ii) `targetType` = `Promise<T>` resolves to a bare **`Type.Interface`**, NOT a `Type.Reference` carrying the type arg `T`, so the EXISTING async-unwrap branch (Checker.kt:~49398, gated `targetType is Type.Reference && symbol.name=="Promise"`) never fires (awaited=null); (iii) consequently `canUseTypeEngine(any, ...)` is false → no diagnostic. So B87.1b must FIRST fix (i) the union-return inference AND likely (ii) make `getTypeFromTypeNode(Promise<T>)` produce a `Type.Reference`; THEN (iv) the residual relation question — `checkTypeRelatedTo(Yadda | Awaited<T>, bare T)` returns `true` today (bare unconstrained TypeParam target has apparent type `{}` → everything assignable), but TypeScript wants it FALSE with the "could be instantiated with an arbitrary type" elaboration (17.11a infra exists). Per the decision rule (multi-piece, no one-line gap) NO code landed; the trace re-scopes B87.1b/c below. **DO-NOT** broadly change the bare-TypeParam-target relation result — it would cascade through every internal generic-function assignment; the fix must be a narrow gate at the async-generic-return site only.
  - [~] **B87.1b (PARTIAL — await-unwrap LANDED 2026-05-28 round 73, net-zero, zero regressions; union-return inference still TODO).** Two sub-gaps remain from the B87.1a trace; the FIRST is now closed: `getTypeOfExpression(AwaitExpression)` was hardcoded `anyType` (Checker.kt:52636 `// TODO: unwrap Promise`). Replaced with: unwrap one level `Promise<X> -> X` (Type.Reference to Promise), non-Promise operands await to their own type, bare/unknown operands stay anyType (prior behavior, avoids cascade). Full suite 9006 unchanged (net-zero across the broad change — most async tests already tolerate the more-precise await type). This alone does NOT move `doNotElaborateAssignabilityToTypeParameters` because the operand `getXOrYadda(x)` STILL resolves to anyType (the union-return inference gap below). **REMAINING B87.1b:** preserve the `T | Yadda` union-return from `getXOrYadda<T>(x:T): T | Yadda` instead of collapsing to any when the arg is a bare TypeParam — find where `getReturnTypeOfCallExpression` (Checker.kt:54201) loses the union for a declared generic fn called with a bare-TP arg. RISK: MEDIUM (generic-union-return inference is consulted broadly — verify no TS2322/TS2345 cascade).
  - [ ] **B87.1c (re-scoped 2026-05-28 round 73 after the await-unwrap fix advanced the source).** Post-await-fix re-trace at the return site shows `src=T | Yadda[Union]`, `tgt=Promise<T>[Interface]`, `canUse=false`, `relToTgt=false`. So the SOURCE is now correct (only the `Awaited<T>` display differs — TypeScript shows `Yadda | Awaited<T>`). The remaining blockers are now precisely THREE coupled high-risk pieces:
    - **(A) `Promise<T>` resolves to a bare `Type.Interface`, not a `Type.Reference` carrying `T`.** Root cause: when `checkReturnAssignability` calls `getTypeFromTypeNode(returnTypeNode = Promise<T>)`, the function's own type param `T` is NOT in `currentTypeParamScope`, so `T` → errorType and the `Type.Reference` creation is skipped by the errorType guard (CLAUDE.md "getTypeFromTypeReference errorType guard"). Fix would push the enclosing function's `typeParameters` into scope before resolving the return-type node. RISK: HIGH — makes `Promise<T>` (and every other generic return annotation) a Reference broadly; cascades through return-assignability across the suite.
    - **(B) assign-to-bare-TypeParam must FAIL.** Even with (A), the async-unwrap branch would compare `T | Yadda` vs awaited `T`; `checkTypeRelatedTo(Yadda, bare T)` returns `true` today (apparent type `{}`), so no diagnostic. TypeScript wants FALSE + the "'T' could be instantiated with an arbitrary type" elaboration (17.11a infra exists). RISK: HIGH and explicitly DO-NOT broadly — cascades through every internal generic-function assignment. Needs a narrow gate at the async-generic-return site only.
    - **(C) `Awaited<T>` display** — render the bare-TP constituent of the awaited union as `Awaited<T>` (the name is registered at Checker.kt:23563) for the TS2322 message. Cosmetic; only matters once (A)+(B) emit.
    - **Conclusion:** B87.1's remaining work is (A)+(B)+(C), all coupled and (A)/(B) broad-cascade-risky. NOT a bounded surgical fix; needs a dedicated session that can absorb the full-suite regression triage from the scope + relation changes. The await-unwrap piece (B87.1b) was the one safely-landable increment.
  - **Sibling tests**: any async generic function whose body returns a value that doesn't structurally match the bare return TypeParam. Survey post-B87.1a.
  - **Backpointer**: round-72 STATUS "next-session: Blocker #2" recommendation; round-73 confirmed the surgical pool dry and B83.4 closed, so this is the promoted next concrete Blocker substep per the anti-loop rule.

### Completed items (16.0–16.3) — archived

Moved to `PLAN-PHASE-4-HISTORY.md` (2026-06-10) — § "Completed items (16.0–16.3)".

### Skip log (explored-but-skipped tests) — moved

The live skip log (`### Explored-but-skipped tests`, the section `find_candidates.py` reads its
`[SKIP]` token set from) now lives in `PLAN-PHASE-4-HISTORY.md`. Add new skip entries THERE,
above the `### End of skip log` terminator heading; suffix entries with `_ts`. The script reads
both files and unions the tokens, so entries in either file work.

### What's left in the "surgical fix" pool

**As of 2026-05-31 (post-round-85, 9098 passing) — UPDATED**: Round 84 (+16) and round 85 (+6) worked the bounded NONE/EXTRA/SWAP surgical tail across module-resolution, index-signature, readonly, and tslib-helper diagnostics. Round 85 landed B98.r20 (TS2413 number-vs-string index value), r21/r21b (TS7016 + untyped-JS-sibling TS2307 suppression), r24 (TS2540 intersection-property writes), r25 (TS2430 multi-base inherited index-sig), r26 (TS2343 `__awaiter`). **The bounded surgical tail is now genuinely exhausted** — the round-85 triage (logged under "Explored-but-skipped → Round 85 triage skips") classified every remaining ≤2-error NONE candidate as needing one of: control-flow narrowing (Blocker #1: TS2488/TS2532/TS18048 `never`/possibly-undefined), cross-file/namespace scope (Blocker #3: TS2694/TS2503/TS2300-merge/TS2451-merge), type-engine assignability/generic-inference (TS2322 ×91 / TS2345 ×37 / TS2339 ×37 / TS2741 / TS2403), mapped-type-modifier resolution (TS2540 Omit/Readonly), module-resolution modeling (bare/symlink/untyped-module/typeRoots TS2307/TS2688 — bare-missing TS2307 ATTEMPTED round 85, reverted -6), decl-emit portability (TS2883/TS4032), or the FP-prone namespace-merge cross-block duplicate (`cloduleWithDuplicateMember2`). **Next session: commit to an architectural blocker substep** (Blocker #1 control-flow narrowing has the highest yield; Blocker #2 generic inference and Blocker #3 cross-file scope are the other two). Do NOT spend a session re-scanning the NONE tail expecting bounded wins — they are documented as exhausted.

**As of 2026-05-18 (post-B47.7, 8815 passing) — UPDATED**: After the B47.x series (MAINT-2 + B47.1–B47.7 + B47.5 foundation + B47.6 two-piece), the surgical pool at +1-per-commit is truly exhausted at the errors-baseline level (`find_candidates.py --fresh` returns 0/0/0). The JS-emit candidates pool has 108 entries; cataloguing in PLAN-PHASE-4.md confirms all are feature-scale (parser-recovery, private fields downleveling, `__setFunctionName`, `__rest` for CJS export-rest, type-display normalization for nullable targets, conditional type resolution, namespace-qualified union display, lib subsetting, module/file-ordering architectural blocker). Recent session B47.x landed +7 tests via narrow defensive-emit patterns; further +1 wins require feature-scale work or completing architectural blockers. Recommended next: tackle blocker substep per the architectural-blockers section.

**As of 2026-04-25 (post-16.4gs, 8337 passing) — RECOMMENDED SHIFT**: After 6 consecutive sessions confirming the surgical pool is empty (16.4go, post-16.4gk recon, post-16.4gl recon, 16.4gp, 16.4gq, post-16.4gr) plus this session's exhaustive re-audit of skip-list entries, the surgical pool is genuinely exhausted at the +1 to +3 per-commit level. **Next sessions should commit entirely to architectural blockers** per the new priority ordering (see "Known architectural blockers" below — reshuffled 2026-04-25). The recommended next step is **MAINT-1 (stale skip-log audit)**, then **Blocker #1 (full control flow narrowing)** — the latter has the highest single-feature yield estimate (~60–100 tests realistic, 250 upper bound).

**As of 2026-04-25 (post-16.4gi, 8323 passing)**: `find_candidates.py --fresh` returns 1 SWAP candidate (`newMap_ts`) which requires a multi-piece fix (TS2743 emission + lib-version-aware MapConstructor/WeakMapConstructor declarations). All other candidates are filtered as already-skipped. **Re-examining previously-skipped tests after each round of infrastructure changes is the highest-yield strategy now**: 16.4gi flipped ~~`typeCheckingInsideFunctionExpressionInArray_ts`~~ because 16.4ge's Variable+Interface merge made `Object` resolve as a real Type.Interface, which then revealed that `getReturnTypeOfNewExpression`'s Type.Interface branch returned `calleeType` directly (instead of consulting construct sigs for the "constructor interface" pattern). The same merge unblocks several other tests partially — e.g. ~~`booleanAssignment_ts`~~ now correctly types `b: Boolean` (after 16.4gi's `new X()` fix) but still has 2 FPs (primitive→wrapper assignability) + 1 missing chain (valueOf elaboration). Continuing to re-examine the skip log against the latest infrastructure is the recommended workflow until a coordinated multi-piece fix or blocker is undertaken.

**As of 2026-04-24 (post-16.4fs, 8304 passing)**: `find_candidates.py --fresh` still returns 0 across all buckets at diff ≤ 3 (filtered from 9/118/32). The 16.4fs win came from re-visiting a previously-skipped test (`pathMappingBasedModuleResolution_withExtension_failedLookup`) and landing a narrow TS2307 branch gated on "every paths target has an explicit `.ts`/`.tsx`/`.d.ts` extension" — narrower than the broad "paths config → skip TS2307" originally used. Each previously-skipped test is worth re-examining when its skip reason names a specific resolver limitation that a narrower gate could bypass. The 16.4fr win came from a non-obvious extension of an existing emission site (TS6212 branch) to cover an adjacent pattern, not from a fresh candidate — the tests it flipped (`optionalParamAssignmentCompat`, `functionSignatureAssignmentCompat1`) had 3-gap diffs (position + display + related info) that all resolved cleanly together. Similar "stacked gap" wins may still exist when multiple narrow fixes coincide. Otherwise next gains require either a blocker #1 sub-step (return-type-from-body broader patterns, or named→named cross-target comparison) or a coordinated new-diagnostic family implementation (TS6213 "Did you mean to use new?" at var-init, rootDir validation, iterator-protocol typing for TS2802/TS2495/TS2488).

---

After 2026-04-19 (16.4dm, 8227 passing — net-zero infra commit on top of 16.4dl), the low-hanging pool is empty. The 16.4di–dl line landed targeted wins on the inheritance-chain side of blocker #1 (generic property chain walk + TS2416 class-type-param scope + private-brand elaboration for TS2322/TS2345 + apparent-type constraint check for TS2344). 16.4dm extended step (c) to cover primitive-literal returns and added function-mismatch chain lines to property elaboration — both correct improvements that gate on per-property TS2322 emission to produce visible test gains. `find_candidates.py --fresh` still returns ~20 candidates; **every one remaining** is classified as a blocker or feature gap:
- Generic inference (Blocker #1): widenToAny1/2, declarationEmitBundleWithAmbientReferences, recursiveTypeRelations, lambdaArgCrash
- Cross-file scope (Blocker #5): jsFileCompilationLetDeclarationOrder2, jsFileCompilationDuplicateVariableErrorReported
- JSDoc (Blocker #2): jsExportMemberMergedWithModuleAugmentation, jsFunctionWithPrototypeNoErrorTruncationNoCrash
- Conditional types: conditionalAnyCheckTypePicksBothBranches
- Control-flow narrowing: narrowingUnionToNeverAssigment, optionalChainWithInstantiationExpression1 (es2019/es2020)
- Intersection privacy reduction: intersectionWithConflictingPrivates
- Display: errorWithSameNameType (cross-module qualified), deeplyNestedAssignabilityErrorsCombined (typeof-of-class-value), crashInEmitTokenWithComment (destructured arrow params)
- TS2859 complexity budget: relationComplexityError
- Standalone new diagnostic: importAliasFromNamespace (TS2845), taggedTemplatesWithIncompleteTemplateExpressions3 (TS2345 for tagged templates), importHelpersWithLocalCollisions (TS2354 for AMD/System tslib resolution)
- Structural elaboration: optionalPropertiesTest (optional→required), implementsIncorrectlyNoAssertion (TS2416 with intersection-of-classes)

The next +N gains either come from:
1. **Implementing new diagnostics** (TS6234 / TS2351 / TS2352 / TS2300-vs-lib / TS7023 / TS2802 / TS2744 / TS2845 / TS4081 / TS2354-AMD): each is 1-3 tests on its own but together could net +15-20. Each is self-contained (contained to a specific emission site) but individually small.
2. **Taking on a blocker** (structural generic comparison #1 for ~30+ tests, JSDoc #2 for ~5-10, etc.). Blocker #1 is by far the highest-yield single investment.

**Recommendation**: surgical pool is **exhausted**. Next session should commit to either (a) a new-diagnostic batch (pick 2-3 low-risk ones in a single session), or (b) blocker #1 (structural generic comparison) with the retry plan below. Continuing to look for +1 surgical wins via `find_candidates.py --fresh` will yield nothing.

### Known architectural blockers (RESHUFFLED 2026-04-25, 8337 passing)

These blockers recur across multiple "close-to-passing" tests and cannot be fixed with surgical changes in a single session. Any agent attempting these should plan for a multi-session investigation with regression budget.

**Priority ordering** — ranked by (expected tests unblocked) ÷ (regression risk × refactor scope). Higher rank = better cost/benefit. Reshuffled after the 16.4gq–gs session: the surgical pool has been ground down to zero across 5+ sessions, so the path forward is committing entire sessions to architectural work. Per the original Phase 16 impact analysis (line ~2479), the largest unaddressed feature is **control flow narrowing** (~250 tests potential, only partial done in 16.3) — it now sits at #1.

**What changed in this reshuffle:**
- **NEW #1**: Full control flow narrowing (was 16.3 PARTIAL — promoted to architectural blocker since the remaining work is full flow-graph construction, not surgical).
- **NEW #2**: Generic argument inference (was implicit under "16.4 generic instantiation" — promoted because tests like `genericConstraintSatisfaction1`, `superCallArgsMustMatch`, `genericClassWithStaticFactory`, `superWithTypeArgument3`, `privacyCheckAnonymousFunctionParameter2` all hit the same gap: we don't infer `T` from arg type then check the constraint).
- **#3 (was #5)**: Cross-file global scope conflation — bumped UP. Each session that does skip-log auditing finds more tests gated on this; estimated impact revised upward.
- **#4 (was #1)**: Structural comparison of generic refs — DEMOTED to LOW yield. Steps (a)–(d) infrastructure landed; the remaining gaps are bounded (different-target TS2352, overload-aware TS2416 for lib types, multi-statement return-from-body inference) and total ≤ 10 tests across all sub-cases.
- **#5–#7**: JSDoc, TS7006 over-suppression, Parser error-recovery — unchanged ordering, all LOW yield.
- **NEW MAINT-1**: Stale skip-log audit (16.4gp/gr/gs sessions all found stale entries — running this once could surface +5–15 candidates that are already passing).

---

#### 1. Full control flow narrowing — HIGHEST yield (~100+ tests realistic), MEDIUM risk

**Status (2026-04-25):** 16.3 landed surgical narrowing fixes (TS1344 message, TS2739/TS2740 multi-missing, null/undefined narrowing in `if (x == null)` branches, narrowing helper for variable declarations). Full flow-graph construction was deferred. The remaining gap is the dominant blocker for TS2339 (549 tests with this code), TS2454 (103 deficit), TS2774 (118 tests).

**Failing test patterns that need this:**
- ~~`nestedLoopTypeGuards_ts`~~ flipped 17.30a (flow-graph TS2454 body-recursion)
- ~~`typeGuardConstructorDerivedClass_ts`~~ flipped 17.5a (`x.constructor === Class` narrowing)
- ~~`narrowingUnionToNeverAssigment_ts`~~ flipped 17.1b (var-decl `never` target narrowing)
- ~~`narrowByEquality_ts`~~ (TS2839 narrowing — partially done in 16.4fy but limited)
- ~~`optionalChainWithInstantiationExpression1_ts`~~ flipped 17.44 (synthetic-paren marker for instantiation-expr-breaks-chain pattern)
- Many TS2774 "always defined" / TS2454 "used before assigned" with non-trivial control flow.

- **Yield**: 60–100 tests realistic (250 upper bound from impact analysis × ~30% realistic factor).
- **Scope**: build a real flow graph during binding (per-function basic blocks + branch edges), reify narrowed types per AST position. tsgo's `internal/checker/flow.go` (2.7K lines) is the reference implementation. This is INFRASTRUCTURE work — expect 2–4 sessions to land safely.
- **Risk**: MEDIUM. Narrowing is opt-in (a position with no narrow info falls back to declared type), so regressions are bounded to over-narrowing (false NEVER) rather than wholesale changes.
- **Why first now**: highest absolute yield. The original Phase 16 ordering put 16.3 fourth ("orthogonal but complex; defer to after easier wins") but the easier wins are now exhausted — narrowing IS the next big unlock.

#### 2. Generic argument inference (NEW) — HIGH yield (~20–40 tests), MEDIUM risk

When calling a generic function with concrete args, TypeScript infers each type parameter from the corresponding argument type, then checks each inferred T against its constraint. Currently we punt on this entirely — generic call sites with type-param-typed args either pass trivially (T = anyType) or skip the assignability check.

**Failing test patterns that need this:**
- ~~`genericConstraintSatisfaction1_ts`~~: `f<T extends S>(x: T)` called with `{s: 1}` against `S = {s: string}` — should emit TS2322 + TS6500.
- ~~`genericClassWithStaticFactory_ts`~~: `T` not inferred from arg → no TS2345.
- ~~`superCallArgsMustMatch_ts`~~, ~~`superWithTypeArgument3_ts`~~: `super(...)` arg checking when base is generic.
- ~~`privacyCheckAnonymousFunctionParameter2_ts`~~: function-typed parameter not structurally compared.
- ~~`widenToAny1_ts`~~, ~~`widenToAny2_ts`~~: `string | undefined` initializer widened wrong against `number` annotation.

- **Yield**: 20–40 tests. Each individual test gives +1, but the pattern repeats across many test files.
- **Scope**: extend `checkArgumentsAgainstSignature` and `checkArgumentsAgainstOverloads` to: (a) collect inferences from arg→param matching (each TypeParam → concrete type), (b) apply constraint check per TypeParam, (c) substitute inferred types into the param type and recheck. Touches the existing overload-resolution and constraint-checking paths.
- **Risk**: MEDIUM. Could regress overload-resolution tests if inference picks wrong overload. Mitigate with conservative gating (only when ALL TypeParams resolve to non-error types).
- **Why second**: stacks naturally on Blocker #4 step (a)/(b)/(c)/(d) infrastructure (Type.Reference interning, `resolveGenericPropertyType` mapper, `getPropertyTypeForRelation` cache) — the building blocks are already there.

#### 3. Cross-file global scope conflation — MEDIUM-HIGH yield (~30+ tests, was MEDIUM), HIGHEST risk

`Checker.init` does `mergeSymbolTable(globals, result.locals)` for every binderResult, merging module-file exports into the shared `globals` map alongside script-file locals and lib. In module files, identifiers from OTHER module files that weren't imported appear in `scope.has(name)` as if they were globals.

Blocks fine-grained module-visibility diagnostics: TS2301 vs TS2663 distinction, default-import-from-export-equals visibility, namespace export filtering across files.

**Failing test patterns:**
- ~~`classMemberInitializerWithLamdaScoping4_ts`~~ (TS2301 vs TS2663)
- `moduleAugmentationsImports3/4_ts` (TS2339 FP on nested module augmentation)
- ~~`errorsOnImportedSymbol_ts`~~ (cross-file type-only flagging)
- ~~`jsFileCompilationLetDeclarationOrder2_ts`~~, ~~`jsFileCompilationDuplicateVariableErrorReported_ts`~~
- Possibly contributes to many `genericSpecializations*`, `arrayAssignmentTest*` cross-namespace cases not yet diagnosed.

- **Yield**: 30+ tests. Each skip-log audit session surfaces more candidates blocked here.
- **Scope**: split "true globals" (script-file locals + lib + KNOWN_GLOBALS) from per-file module locals; reconstruct `fileScope` to only include imports the current file actually declares. Touches **every identifier-resolution check** in the checker (~30 call sites).
- **Risk**: HIGHEST. A naive implementation regresses 50+ tests. Recommended approach: introduce per-file-scope construction behind a flag, run side-by-side, then flip per file as the scope construction proves correct.
- **Why third (bumped from last)**: yield estimate revised upward after multiple skip-log audits found this blocker named in the residual; without addressing it, ~30 surgical-looking tests remain stuck.

#### 4. Structural comparison of generic type references — LOW yield (was MEDIUM), MEDIUM risk

**Status (2026-04-25, 8337 passing):** Steps (a), (b), (c) partial, (d) partial all landed across 16.4dc–df, dg, dm, gr, gs. Type.Reference interning, cycle/depth heuristic, TypeParam apparent-type comparison, `resolvedPropertyTypes` cache, heritage-clause type-arg instantiation, base-chain inheritance via `resolveGenericPropertyType`, parameter-mismatch chain elaboration, base-method TypeParam TS2208 — all in place.

**What's left (small, gated by other blockers):**
- ~~Different-target TS2352 with "Property X missing" elaboration (`genericTypeAssertions2_ts`)~~ — flipped 17.28 (narrow array-source → class-target case). Generalizing to all different-target casts still requires a TypeScript-style `comparable` relation with full structural elaboration.
- Overload-aware TS2416 for lib generic interfaces (`implementArrayInterface_ts` emits 2 extra diagnostics because lib's `every`/`filter` have type-predicate overloads that user code only implements one of).
- ~~Multi-statement bodies with single return for `inferSimpleReturnTypeFromBody`.~~ **Closed B4.1 (2026-05-13, +0 foundation)** — helper at Checker.kt:39694 now handles multi-statement bodies via top-level ReturnStatement collection.
- ~~Identifier returns (`return param`, `return this.field`) for return-type-from-body inference.~~ **Closed B4.2 + B4.3 (2026-05-13, +0 foundation)** — `is Identifier` arm extended to `return paramName` for annotated params; new `is PropertyAccessExpression` arm handles `return this.field` for class-property-typed fields (new `enclosingClass: Type.Interface?` parameter threaded from `resolveGenericPropertyType` caller at Checker.kt:39625).

- **Yield**: ≤ 10 tests across all sub-cases.
- **Scope**: contained to checker (no binder/parser changes).
- **Why demoted**: infrastructure work is largely done; remaining test gains are individual-test scale, not pattern-level. Best handled opportunistically when a specific case aligns with surgical session's budget.

#### 5. JSDoc type annotations — LOW yield (~5–10 tests), LOW risk

`@type {T}`, `@this {T}`, `@typedef`, `@param {T}` — we bind `@typedef` nominally but don't honor `@type` assertions or `@this` parameter types.

- **Yield**: 5–10 `.js` file tests.
- **Scope**: JSDoc parsing (scanner comment handling) and a new JSDoc-to-TypeNode bridge in the checker. Does not touch non-JS checking paths.
- **Why fifth (was second)**: low blast radius means low risk, but absolute yield is small. Higher-yield blockers (#1, #2, #3) take priority for "session-scale architectural work" budget.

#### 6. TS7006 over-suppression for callback parameters — LOW yield (~3–5 tests), MEDIUM risk

We suppress TS7006 for any `contextuallyTyped=true` param. TypeScript only suppresses when the contextual type actually provides param types (bidirectional inference succeeds).

- **Yield**: 3–5 tests (`intraBindingPattern`, `subtypeReduction`, `signatureCombiningRestParameters`).
- **Scope**: requires bidirectional inference plumbing in contextual typing.
- **Why sixth**: low yield + needs new infrastructure. Defer.

#### 7. Parser error-recovery asymmetry — LOW yield (1–2 per case), HIGH risk per attempt

Several tests expect specific token-consumption-then-continue recovery (e.g. `declare class foo();` → class with empty body + phantom `()` statement + next function).

- **Yield**: 1–2 tests per case, case-by-case.
- **Scope**: per-case parser edits.
- **Why last**: per-case work, no multiplier, regression-prone. Best handled opportunistically.

---

#### MAINT-1. Stale skip-log audit — UNKNOWN yield (likely 5–15 tests), LOW risk

Every recent session that audited the skip log found 1–3 entries marked "stuck on Blocker #X" that have actually been passing for sessions (e.g. 16.4gl-recon found `genericCloneReturnTypes`/`genericCloneReturnTypes2` stale; 16.4go found `generics4`/`genericDerivedTypeWithSpecializedBase`/`genericPrototypeProperty3` stale; this session found `genericPrototypeProperty3` again). The cumulative impact of stale entries blocking the candidate finder is probably 5–15 tests.

- **Yield**: unknown but likely 5–15 candidates that re-surface as fresh.
- **Scope**: walk every entry in the "Explored-but-skipped tests" section, run the test, mark stale (~), update reason if still failing.
- **Risk**: zero (read-only investigation).
- **Why a maintenance task**: not architectural but high-leverage. Should be run as a one-off "audit session" before any session that uses `find_candidates.py --fresh` for surgical work, or after every major architectural fix lands.

---

**Parallelism note**: Blockers #1, #2, #5, #7 can be worked independently. #3 should run after #1/#2 have resolved their fraction of overlapping cases (clearer "remaining failures pointing to #3" set). #4's remaining sub-cases pair naturally with surgical sessions — not a full-session investment. #6 depends on enough contextual-typing infrastructure that #2's inference work should land first.

**Recommended next sessions (in order):**
1. **MAINT-1** (1 session): clean up stale skip log → frees `find_candidates.py --fresh` to find genuine candidates.
2. **Blocker #1 step 1** (1–2 sessions): build flow-graph infra in binder; no behavior change yet.
3. **Blocker #1 step 2** (2 sessions): wire flow-graph into TS2454/TS2339/TS2774 emission sites.
4. **Blocker #2** (2 sessions): generic argument inference + constraint check.
5. **Blocker #3** (3+ sessions): per-file scope construction behind a flag, then per-file flip.

### Candidate-picking workflow for surgical fixes

When context starts a new session, the fastest path to wins is:

1. **Run full suite once** (4-6 min) to produce `build/test-results/jvmTest/*.xml`.
2. **Python-parse the XMLs** to find tests by diff size and pattern. The key filter is 1-2 extra diagnostic lines (too-aggressive checks) or 1 missing diagnostic at a specific position (simple checks to add). Example snippets live in this file's history — reuse them.
3. **Look for code-swap tests** (expected TS####A at position P vs actual TS####B at same position P) — these are often single diagnostic emissions at a specific code path that just need the code changed or position adjusted.
4. **Skip**: tests needing generic variance, module-visibility, JSDoc type handling, or full type inference — those are the architectural blockers above.
5. **Inspect `typescript-repo/tests/cases/compiler/<name>.ts` + `typescript-repo/tests/baselines/reference/<name>.errors.txt`** for context before implementing. Don't just look at the diff.

### Queue execution strategy

**Original Phase 16 sequence (DONE/IN PROGRESS):** 16.0 → 16.1 → 16.2 → 16.3 → 16.4

**Status:** 16.0/16.1/16.2 DONE, 16.3 PARTIAL (full flow-graph deferred → now Blocker #1), 16.4 IN PROGRESS (generic infrastructure largely done; remaining sub-cases moved to Blocker #4).

**NEW sequence (2026-04-25 reshuffle) — as the surgical pool is exhausted, commit entire sessions to architectural blockers:**

**Phase 17 sequence:** MAINT-1 → Blocker #1 (control flow narrowing) → Blocker #2 (generic argument inference) → Blocker #3 (cross-file scope) → Blocker #4 / #5 / #6 / #7 opportunistically.

**Rationale (priority-by-yield × tractability):**
1. **MAINT-1 first** — stale skip-log audit. Read-only, ~1 session, surfaces 5–15 already-passing tests as fresh candidates that would otherwise stay hidden behind `[SKIP]` markers in `find_candidates.py`. Cheap, no regression risk, and clears the deck for #1's diagnosis.
2. **Blocker #1 (control flow narrowing) second** — highest absolute yield (~60–100 tests realistic). The remaining failing tests across many categories (TS2339, TS2454, TS2774, TS2839) all converge on this. Build flow-graph in binder, wire into emitters in two stages.
3. **Blocker #2 (generic argument inference) third** — stacks on the 16.4 generic infrastructure already in place. ~20–40 tests. Lower risk than Blocker #3.
4. **Blocker #3 (cross-file scope) fourth** — high yield but highest risk; defer until #1/#2 have cleared their fraction so the residual is well-characterized.
5. **Blocker #4 (remaining structural generic gaps) opportunistically** — small (~10 tests), pairs with surgical sessions, not full-session work.
6. **Blockers #5/#6/#7** — low yield, defer.

**Hard rules:**
- Each feature must land behind a feature flag or with conservative guards initially.
- Run full suite after EACH sub-step to catch regressions early.
- If a feature causes >20 regressions, add more guards or narrow scope; don't revert without analysis.
- Document each partial implementation in CLAUDE.md gotchas.

**Per-session throughput — fix multiple items per session.** A single sub-step (e.g. 16.4h, 16.4i) is the unit of commit, not the unit of session. After committing + pushing one sub-step, loop back to pick up the next unchecked sub-step. Keep going until (a) the queue is empty, (b) the next item is genuinely blocked, or (c) context/time budget is nearly exhausted — then wrap up with a summary. Two-to-four sub-steps per session is a reasonable target when the items are small (+1 to +5 tests each); larger infrastructure items may consume a full session on their own. Always finish each sub-step cleanly (test suite green, commit + push) before starting the next — never bundle unrelated work into one commit.

**Realistic total gain:** 500-800 new passing tests → ~85-88% pass rate.

---

## Reference

- **tsgo source**: `github.com/microsoft/typescript-go` — `internal/checker/`
- **TS checker**: `microsoft/TypeScript` — `src/compiler/checker.ts` (53,296 lines)
- **Key tsgo files**: `checker.go` (31K), `relater.go` (5K), `types.go` (1.3K), `flow.go` (2.7K), `inference.go` (1.6K)
- **Parallelism model**: `internal/compiler/checkerpool.go` — N independent checkers, round-robin file assignment, shared immutable AST
