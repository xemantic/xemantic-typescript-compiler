# Phase 4 — Structural Type Checker

**Status (2026-04-17):** 8,108 / 10,078 tests passing (80.4%). Active queue: **Phase 16 — Fundamental Type System Features**. 16.0 done, 16.1 done, 16.2 done, 16.3 partial (+14 tests), 16.4 in progress (+32 tests, 1967 remaining).

## Goal

Implement structural type checking to unlock the ~2,400 remaining test failures.
Design for future parallel checking using Kotlin coroutines (inspired by tsgo's goroutine model).

## Architecture (inspired by tsgo)

### Key design decisions

**1. Type hierarchy — Kotlin sealed classes**

Replace the current string-based type representation (`"number"`, `"@MyType"`, `"|A | B"`)
with a proper `Type` sealed class hierarchy. Kotlin sealed classes give us exhaustive
`when` matching and smart casts — more idiomatic than tsgo's `TypeData` interface approach.

```kotlin
sealed class Type {
    abstract val flags: TypeFlags
    val id: Int = nextId++  // per-checker unique ID
}

class IntrinsicType(override val flags: TypeFlags, val name: String) : Type()
class LiteralType(override val flags: TypeFlags, val value: Any) : Type()  // string/number/boolean
class ObjectType(override val flags: TypeFlags) : Type() {
    var members: SymbolTable? = null       // lazily resolved
    var properties: List<Symbol>? = null
    var callSignatures: List<Signature>? = null
    var constructSignatures: List<Signature>? = null
}
class InterfaceType(...) : ObjectType(...)  // adds typeParameters, baseTypes
class TypeReference(...) : ObjectType(...)  // adds target (generic), resolvedTypeArguments
class UnionType(val types: List<Type>) : Type()
class IntersectionType(val types: List<Type>) : Type()
class TypeParameter(...) : Type()  // adds constraint, default
// ... etc
```

**2. Parallelism preparation — LinkStore pattern**

tsgo's key insight: checker-local metadata stored in side maps, not on shared AST nodes.
We prepare for this from the start:

```kotlin
class Checker(...) {
    // Checker-local type cache — NOT on AST nodes
    private val nodeTypes = HashMap<Long, Type>()        // nodeKey → resolved type
    private val symbolTypes = HashMap<Int, Type>()       // symbol.id → resolved type
    private val declaredTypes = HashMap<Int, Type>()     // symbol.id → declared type
}
```

When we later create N parallel Checker instances, each will have its own maps.
The shared AST and binder output remain immutable.

**3. Lazy type resolution**

Types are resolved on-demand (matching both TS and tsgo):
- `getTypeOfSymbol(symbol)` — checks `symbolTypes` cache, computes if absent
- `getTypeFromTypeNode(node)` — checks `nodeTypes` cache, computes if absent
- `resolveStructuredTypeMembers(type)` — resolves members lazily on ObjectType

**4. Relation engine**

Structural comparison via `isTypeRelatedTo` → `structuredTypeRelatedTo` →
`propertiesRelatedTo`. Cached in relation maps keyed by `(source.id, target.id)`.

```kotlin
enum class Ternary { True, Maybe, False }

class Relation {
    private val cache = HashMap<Long, Ternary>()  // pack two Int IDs into Long
}

// Five relation instances (same as TS/tsgo)
private val subtypeRelation = Relation()
private val assignableRelation = Relation()
private val comparableRelation = Relation()
private val identityRelation = Relation()
```

**5. Future: N-checker parallelism via coroutines**

```kotlin
// Future (Phase 4g) — not yet implemented
class CheckerPool(private val program: Program, private val checkerCount: Int = 4) {
    private val checkers = List(checkerCount) { Checker(program) }
    private val fileAssignments = program.files.withIndex().associate { (i, f) ->
        f to checkers[i % checkerCount]
    }

    suspend fun checkAllFiles() = coroutineScope {
        checkers.map { checker ->
            launch { checker.checkAssignedFiles() }
        }.joinAll()
    }
}
```

---

## Completed infrastructure (Phase 4a, items 0–9)

All complete. Type hierarchy, type resolution, structural comparison engine, generic
instantiation, expression type inference, parallel checking pool are in place.

### Completed Phase 4b items (10–15a)

- [x] **10a/10b/10c** — TS2322 wired to Type engine (var decl, return, assignment — conservative)
- [x] **11a-11d** — Expression type inference (object literal, array, arrow/function, identifier)
- [x] **12a/12b/12c** — TS2345 union types, union call signatures, TS2769 overload diagnostics
- [x] **15a** — TS2300 duplicate class members

---

## Phase 16 — Fundamental Type System Features

**Session 2026-06-16 (round 183, multi-iteration session, ≥20 iterations per user request, addressing blockers) — +1 net (347→346 failing, 9739→9740 passing), 1 feature commit (B419b), ZERO regressions (exact failing-set diff via xml.etree vs `/tmp/fail_baseline.txt`).** Method: re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=8 all type-engine; OUTPUT=1 tsgo-irrelevant systemModule17), wrote `scripts/fail_set.py` (xml.etree failing-set extractor + `--diff` for exact regression checking) and a header-diff-size ranker over the 236 "close" (non-NONE) failures, and triaged ~25 candidates directly — all multi-piece (consistent with rounds 173–182): finicky multi-file emit-ordering (moduleResolutionWithRequire/importInsideModule emit an extra `.js` file tsc doesn't — which-files-emit rule, risky); generic-composition (inferFromGenericFunctionReturnTypes1 pipe/map/filter); flow-aware-typeof-in-type-position (controlFlowForIndexSignatures — `typeof c` in a TYPE position must use the narrowed type, but `getTypeFromTypeQuery` deliberately ignores narrowing); circular-type-cycle (circularBaseTypes TS2310/2456/2313 — needs type-LEVEL cycle detection through type-alias+indexed-access+mapped-K, our walker is name-based); Partial+exactOptional (multipleBaseInterfaesWithIncompatibleProperties2 TS2320); bare-specifier TS2307 (decoratorMetadataTypeOnlyImport — FP-risky per CLAUDE.md's relative-only gate); lib-dependent (templateStringsArrayTypeRedefinedInES6Mode needs the lib `TemplateStringsArray` interface merge; noParameterReassignmentJSIIFE needs WorkerGlobalScope `importScripts`); JSON-module-typing (requireOfJsonFileInJsFile). One CONTAINED win the prior triage missed.
- **B419b (+1) — jsFileClassPropertyType2 (JSDoc `@type` on a JS class expando `this.X = …`).** The documented B419 gap (CLAUDE.md: "JSDoc `@type` on the assignment is NOT parsed (so `/** @type {number[]} */ this.p=[]` infers `any[]`, not number[])"). A checkJs class `class C { constructor(){ /** @type {number[]} */ this.p = []; } }` synthesized `p` as `any[]` from the empty-array RHS, so a cross-file `(new C()).p.push("string")` in a sibling `.ts` missed TS2345. Fix: (1) exposed a public `Parser.parseJsDocTypeNodeFromComments(comments)` wrapping the existing private `parsePropertyTypeFromJSDoc` sub-parser; (2) extended `collectConstructorThisAssignments` with an optional `jsdocTypes: MutableMap<String,TypeNode>?` output that, per `this.X=`-statement, parses its leading `@type {T}` JSDoc (first-wins; the name-presence caller `classHasConstructorThisAssignment` passes null so it's unaffected); (3) the B419 synthesis (`resolveInterfaceMembersCore`) now resolves the captured JSDoc TypeNode via `getTypeFromTypeNode` and prefers it over `inferJsExpandoPropType(rhsList)` — but ONLY when it resolves to a concrete non-error Type (else falls back to RHS inference, so the change is a no-op for every other checkJs class). The sub-parser TypeNode's positions point into the JSDoc text (offset 0) but that's harmless here: it's used only to RESOLVE the member type, never to emit a position-bearing diagnostic on the node (the B286 FP-risk applies to name-resolution diagnostics on bare `Array`/`Promise`, not to a concrete `number[]` used as a member type). The push("string")-vs-`number[]` TS2345 already worked once `p` resolved to `number[]`. ZERO regressions (exact failing-set diff: only the target flipped). **Then ATTEMPTED B464 (flow-into-closures, Blocker #1) per the user's "address blockers" directive + revert budget: fully implemented (Flow.kt FlowStart.outerFlow/enclosingReassignedNames/localNames + a source-text `scanReassignedNamesInRange` reassignment scan + checker `narrowTypeFromFlow` FlowStart-closure case) and full-suite-verified — PROVEN NET-ZERO-SAFE (0 regressions, 0 fixes). It correctly removes the `implicitConstParameters` FP TS2339, but the test needs a COMPANION general "`x.length` where x is `T|undefined`"→TS18048 emitter (which we LACK — only narrow B221/B279/noUncheckedIndexedAccess sites exist), so it didn't flip. Reverted per no-dead-infra; the proven-safe closure-narrowing implementation + the missing-companion finding are recorded in the B464 queue note for a dedicated session to land both together.** NEXT: contained pool genuinely exhausted — type-engine Blocker BUILDs (Record/mapped-type materialization, generic-inference engine, B464 flow-into-closures + general TS18048 emitter, circular-type-cycle detection, cross-file scope) + parser-recovery Blocker #7 are the remaining 346.

**Session 2026-06-16 (round 182, multi-iteration session, ≥20 iterations per user request, addressing blockers) — +1 net (348→347 failing, 9738→9739 passing), 1 feature commit (B463), ZERO regressions (exact failing-set diff via xml.etree vs `/tmp/fail_baseline.txt`).** Method: re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=8 all type-engine; OUTPUT=1 tsgo-irrelevant systemModule17), then built a full diff-size ranker over ALL 347 (separating NONE/we-emit-nothing from "close"/we-emit-something-wrong) and triaged ~50 candidates directly. Every remaining failure is coupled-multi-piece (consistent with rounds 173–181): relation-engine elaboration DISPLAY (inheritance1 picks wrong source/target class; optionalParamTypeComparison needs optional-param `| undefined` union-vs-union sub-chain in `getFunctionMismatchElaboration` — MEDIUM-HIGH risk in a broad path, skipped); generic-composition (inferFromGenericFunctionReturnTypes1 compose/map/filter; chainedCalls2 type-param-vs-type-param TS2322 inside a method body — a different mechanism than B462's object-type `collectMissingProperties`); flow-into-closures (implicitConstParameters — const-param narrowing flows into closures, reassigned-param doesn't → the FP `() => x.toFixed()` + missing TS18048); flow-aware typeof-in-type-position (controlFlowForIndexSignatures — `type C = {[k:string]: typeof c}` inside `if (typeof c==='string')`); `unknownEmptyObjectType` narrowing (inKeywordAndUnknown TS2638 — tsc fires only when the `in` RHS is the special narrowed-from-unknown `{}`, NOT a declared `{}`, via `hasEmptyObjectIntersection`); circular-base/constraint (circularBaseTypes TS2310/2456/2313, incorrectRecursiveMappedTypeConstraint TS2313+TS2365); clodule (staticMemberExportAccess TS2351/2576/2339); cross-file (mergeSymbolReexportInterface decl-order Blocker #3); Record/mapped-type materialization (noInferCommonPropertyCheck1, thislessFunctionsNotContextSensitive3). The big type-engine Blockers + parser-recovery (Blocker #7) are the bulk of the 347.
- **B463 (+1) — enumAssignmentCompat7 (nominal enum mismatches in class-override / overload / member-access).** New dedicated `checkEnumNominalClassMismatches` walker (AST + B425 `EnumAsgInfo`, no relation engine), gated to genuinely-incompatible NAMED enums (different simple name OR different member value) — they never relate in tsc, so the walker never double-emits with the general relation paths (which treat numeric enums as `number` and stay silent on this file → it was a NONE test). Three pieces: (A) **TS2339** for a QUALIFIED `<ns>.E.X` value-position member access where X ∉ enum E — gated to a `PropertyAccess` receiver (bare `U8.bit_2` is owned by the standard checkPropertyAccess TS2551 path), and excluding enums merged with a same-name namespace via `encmCollectNsKeys` (their value side gains namespace exports → `memberOrder` incomplete). (B) **TS2416** for a class method override whose single param is a different incompatible enum than the base method's → the contravariant param-mismatch chain (`(param: second.E) => void` not assignable to `(param: first.E) => void` → `first.E` not assignable to `second.E`), rendered with qualified enum names from the TypeReference (not via the relation engine, which would show `number`). (C) **TS2394** for an overload signature whose return enum differs from the impl signature's + related TS2750 at the impl. **The first cut caused 2 regressions** (exactSpellingSuggestion: bare `U8.bit_2` already owned by the TS2551 path → I emitted a redundant TS2339; mergedDeclarations3: `M.Color.Blue` where `Color` is enum-merged-with-namespace and `Blue` is a legit namespace export); both fixed by the qualified-receiver gate + the namespace-merge exclusion. NEXT: the contained pool is genuinely exhausted (50+ triages this round + 9 prior rounds all confirm); the path forward is a dedicated type-engine Blocker BUILD (Record/mapped-type materialization = most consumers; generic-inference engine; flow into-closures/evolving-array; cross-file scope) with full-suite + revert budget.

**Session 2026-06-16 (round 181, multi-iteration session, ≥20 iterations per user request, addressing blockers) — +2 net (350→348 failing, 9736→9738 passing), 2 feature commits (B461, B462), ZERO regressions at every committed step (exact failing-set diffs via xml.etree vs a running `/tmp/fail_baseline.txt`).** Method: re-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=9 all type-engine; OUTPUT=1 tsgo-irrelevant systemModule17), built a smallest-diff ranker AND a rarest-expected-code ranker over all 348 failing tests, and directly triaged ~15 candidates — ALL multi-piece (consistent with rounds 174–180): inferFromGenericFunctionReturnTypes1 (full compose/map/filter generic composition through `reduce` + nested-map contextual typing), the weak-type cluster weakType/noInferCommonPropertyCheck1/nestedExcessPropertyChecking (each needs TS2559/TS2560 weak-type detection + 2-3 more pieces — TS2352 conversion, Weak&Spoiler nested chain, NoInfer<Partial<>> display), objectLiteralExcessProperties (union/generic/intersection excess-check divergence + spelling), inheritance1 (TS2741 elaboration picks the WRONG source/target class — `ImageBase`/`Control` vs `Control`/`SelectableControl`, a broad assignability-chain display issue), computedPropertiesInDestructuring1 (5 codes: needs computed-name EXPRESSION checking in binding patterns + TS2537/TS2538), mappedType* (mapped-type materialization), jsElementAccessNoContextualTypeCrash (JS expando). Both wins EXTEND the B460 generic-inference beachhead.
- **B461 (+1) — fixingTypeParametersRepeatedly2 (single-TP generic-arrow call TS2741 + overload-reinference TS2403).** The documented Blocker #2 / B157 target. Extended `checkGenericCallContextualArrowReturns`/`processGenericArrowCall`: (1) relaxed the bare-TP anchor gate from `< 2` to `< 1` so a single-TP `foo<T>(x:T, cb:(p:T)=>T)` participates (FP firewall unchanged: bail on any non-bare-TP/non-FunctionType value param + every-TP-inferable); (2) new `genericArrowReturnType` helper adds a CallExpression arrow body case (`param.method()` → the method's call-sig `resolvedReturnType`); (3) TS2741 gated to SINGLE-decl callees — for an OVERLOADED callee (`declCounts[name] ≥ 2`) the callback's return TP re-infers to the supertype of {anchor, arrow-return} (`genericArrowSupertype`) so no TS2741 fires and the result type widens (tsc's "fixing type parameters repeatedly" — foo→Derived, overloaded bar→Base); (4) `processGenericArrowCall` now RETURNS the call result type, and `walkGenericArrowCalls` tracks top-level `var x = <genericArrowCall>` result types per statement list (fresh map per list = FP-safe scope handling), emitting TS2403 + TS6203 (`emitGenericArrowRedeclare`) on a redeclaration whose result type displays differently. contextSensitive2/3 (B460, 2-anchor single-decl, non-CallExpression bodies) unaffected.
- **B462 (+1) — chainedCallsWithTypeParameterConstrainedToOtherTypeParameter (fluent generic-method-chain constraint TS2741/TS2739).** New dedicated `checkGenericThenChainConstraints` walker — the documented "method-chain" extension of the beachhead. For `new Chain(arg).then(a=>new B).then(b=>new C)...` where Chain is a generic class with a single self-fluent `then<S extends T>(cb:(x:T)=>S):Chain<S>` method, unwind the `.then` chain to its `new Ctor(arg)` root, compute initial T = widened ctor-arg type, then walk left-to-right: each `.then(arrow)` infers S = the arrow body's `new X` instance type and checks `S extends T` via `collectMissingProperties(S, T)`; on failure → TS2741 (one missing prop; related TS2728 prop-decl + TS6502 cb-FunctionType) / TS2739 (≥2; OWN-before-inherited display order via `missing.reversed()` since our `collectMissingProperties` is base-first; related TS6502) at the arrow body, and the result type arg CLAMPS to the constraint T (so the next link checks `new A` against C, not the unsatisfied B → the second error's target is C). FP firewall `thenChainMethod`: single-type-param class with exactly one `then<S extends T>(cb:(x:T)=>S):ClassName<S>` shape + `new X` arrow bodies + ≥2 chain links (corpus-rare). The TS6502 anchor is the cb FunctionType node (`cbFt.pos`), matching the B460 convention (the baseline col-27 is the FunctionType `(x:T)=>S`, not the param `cb` — the 4-space-vs-baseline-echo-8-space indent fooled the first count). **BUILD GOTCHA (cost ~30 min): the B462 Edit accidentally duplicated the first 2 lines of `resolveTpNode`, leaving a net `+1` unbalanced `{`. The Kotlin compiler reported a MISLEADING "Unresolved reference" cascade (to existing same-file symbols) + a spurious "Missing '}'" far from the edit, AND the low-RAM env (a lingering ~2.4GB java daemon + a concurrent compile) thrashed into GC-overhead/heap OOM that masked it. Diagnosis: `git diff HEAD` brace-net count pinpointed the `+1`; `pkill java` + a single clean compile fixed the OOM.** NEXT: B461/B462 extend the beachhead, but the harder generic-inference variants need real engine work — chainedCalls**2** (method-BODY arrows whose bodies are type-param-typed locals → TS2322 T-vs-S between type params, inside a class method body the walker doesn't recurse into), and compose-style composition through `reduce`. The big type-engine Blockers (Record/mapped-type materialization, flow evolving-array controlFlowArrayErrors, cross-file scope) remain the bulk of the 348.

**Session 2026-06-16 (round 180, multi-iteration session, ≥20 iterations per user request, addressing blockers) — +4 net (354→350 failing, 9732→9736 passing), 3 feature commits (B459, B459b, B460), ZERO regressions at every committed step (exact failing-set diffs via xml.etree vs a running `/tmp/fail_baseline.txt`).** Method: re-confirmed the contained pool exhausted SIX ways (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=9 all type-engine; OUTPUT=1 tsgo-irrelevant; a regenerated structural-only-MISSING filter = 57 candidates all skip-logged/parser-recovery/cross-file; a rarest-missing-code-per-test ranker; pure-missing≤2 = 7 all type-engine; pure-extra/FP-only = 4 all known-hard parser-recovery/lib) PLUS a 10-agent read-only triage Workflow (the recurring lesson held — agents OVER-classify CONTAINED: `widenedTypes` "CONTAINED" actually has a TS2322 `typeof undefined`/array-element mess + 4 FPs; `elidedJSImport1` "CONTAINED" needs cross-file node_modules-namespace-default resolution for the TS2708 the agent claimed "fires naturally" but doesn't; both verified multi-piece independently). Landed 3 dedicated-walker wins.
- **B459 (+1) — overloadsWithComputedNames (TS2389/TS2391/TS2393 for computed METHOD names).** The ONLY gap was `checkMissingImplInClass`'s `else -> continue` skipping all `ComputedPropertyName` method names. New `methodOverloadNameAndDisplay` returns a (grouping-key, display) for a LATE-BINDABLE computed name — string/numeric literal (`["x"]`→`@cstr:x`/`[1]`→`@cnum:1`) or an identifier resolving to a `unique symbol`/`Symbol()` const (`collectUniqueSymbolConstNames`, `@cus:`) — and null for a non-late-bindable name (`[sym]` plain symbol, `[strUnion]` union → skipped, matching tsc which emits nothing). `findMethodImplementation` uses the same keying. emitTS2389/2391/2393 gained a `ComputedPropertyName` span branch (whole `[...]` via `matchClosingBracket` — their `else -> return` silently dropped computed names). Namespaced keys avoid colliding with same-named non-computed methods. INTERFACES untouched (only classes run `checkMissingImplInClass`). The 5 expected errors map exactly (Person impl-name-mismatch, C `["foo"]`/`["bar"]`, C1 `[uniqueSym2]`, C3 `[1]`).
- **B459b (+1) — functionsWithModifiersInBlocks1 (block-level fn modifiers TS1184/TS1029 + dup-impl TS2393).** Three coordinated pieces: (1) broaden the `walkSiblingsForTopLevel` FunctionDeclaration TS1184 from export-only to export/declare/default, emitting ONE at the LEFTMOST modifier (per-modifier `findKeywordPosBackward` + `.minByOrNull`) — namespaces are `atTopLevel` so unaffected; (2) suppress the modifier-ordering TS1029/dup-TS1030 there — `checkDupModInStatement` now threads `atTopLevel` (false for plain `Block`/function-body, true for namespace `ModuleBlock`) and skips `checkModifiers` for a `!atTopLevel` FunctionDeclaration (tsc reports TS1184 and returns); (3) block-NESTED bodied functions sharing a name → TS2393 on each (binder doesn't bind block-scoped fns, B83.5; `checkMissingImplInStatements` gated `atBlockLevel`=true only when recursing into a plain `Block`, so file/namespace stays binder-owned → no double-emit). Was skip-logged as Blocker #7 / B83.5 — landed clean.
- **B460 (+2, both contextSensitive2/3) — typeParameterFixingWithContextSensitiveArguments2/3 (a genuine Blocker #2 generic-inference build).** New dedicated `checkGenericCallContextualArrowReturns` for the shape the broad inference path deliberately leaves uncovered (round-79l "Contextually-typed-arrow return-mismatch"): a bare-IDENTIFIER call to a top-level generic FunctionDeclaration `f<T,U>(a:T, b:U, …, cb:(x:T)=>U)` with ≥2 BARE-TP value-param ANCHORS + ≥1 function-typed-of-TP param + arrow args. Infers TPs from the anchors (`widenType(getTypeOfExpression(arg))`), substitutes into each fn-typed param's `(x:TP)=>TP` (`resolveTpNode`), types the arrow body (simple ident → substituted param type; `param.prop` → that prop's type), checks `collectMissingProperties(arrowRet, substitutedRet)` → single-missing TS2741 + related TS2728 (the missing prop's decl) + TS6502 (the fn-type param node). **FP firewall (the key to landing clean in this net-zero-prone area): bail the WHOLE call if ANY value param is neither a bare-TP ref nor a `(…)=>…` FunctionType, require ≥2 bare-TP anchors AND every TP inferable from a bare-TP arg.** This EXCLUDES the documented round-79l regressors `genericCombinators2` / `contextualTypingOfGenericFunctionTypedArguments1` (which use generic-REFERENCE value params `c:Collection<T>` — a TypeReference WITH typeArguments, → bail — and are METHOD calls, not bare-identifier callees). Do NOT lift the broad `!calleeGenericInstantiation` gate to cover more — that reopens the regressions; instead EXTEND this narrow walker (e.g. NewExpression arrow bodies for `chainedCallsWithTypeParameterConstrainedToOtherTypeParameter`, or the multi-missing TS2739 variant). **NEXT (promote-unblocker for generic-inference #2):** the B460 walker is the beachhead — adjacent shapes to extend it to: NewExpression arrow bodies (`a => new B`, chainedCalls — but that's a METHOD-call chain, needs receiver-type threading), and the `reactReduxLike`/`contextSensitive` variants. The bulk of the remaining 350 is the big type-engine Blockers (Record/mapped-type materialization = most consumers; the broad generic-inference engine; flow evolving-array controlFlowArrayErrors; cross-file scope Blocker #3).

**Session 2026-06-16 (round 179, multi-iteration session, ≥20 iterations per user request, addressing blockers) — +2 net (356→354 failing, 9730→9732 passing), 2 feature commits (B457 flow, B458 const-enum cluster), ZERO regressions at every committed step (exact failing-set diffs vs `/tmp/fail_baseline.txt`, xml.etree).** Method: re-confirmed find_candidates exhausted (EXTRA/MISSING/SWAP fresh=0; NONE fresh=9 type-engine; OUTPUT=1 tsgo-irrelevant), regenerated the round-177/178 purely-structural-missing-codes filter (30 candidates, all tagged `[TRIAGED]`), ran a 12-agent read-only triage Workflow over the small-diff candidates (recursiveTypeRelations, didYouMean…, noImplicitAnyLoopCrash, elidedJSImport1, quickIntersection…, esModuleInterop…, optionalParamTypeComparison, indexSignatureInOtherFile, implicitConstParameters, discriminateWithOptionalProperty4, jsdocBracelessTypeTag1, recursiveComplicatedClasses) — ALL 12 returned ARCHITECTURAL with cited code-path evidence, corroborating that the contained pool is genuinely scraped. Re-verified the most tractable INDEPENDENTLY rather than trusting the triage. Two wins: a CONTAINED one the prior rounds' triage missed (B457), and a genuine blocker BUILD (B458) per the user's "address blockers" directive + the protocol's one-cycle revert budget.
- **B458 (+1) — const-enum diagnostic cluster (constEnumErrors), 6 checks in one new `checkConstEnumDiagnostics` walker, all AST/constant-eval (NO relation engine — the reason it landed clean where the type-engine clusters can't).** TS2474 (member init not a constant — references a NON-existent member `E.X`/`E["X"]`, X ∉ members; distinct from the TS2651 forward-ref-to-an-existing-later-member case, which is a bare Identifier — so the two never collide); TS2477/2478 (init evaluated to ±Infinity / NaN — the existing `evaluateEnumInitializer`/`evaluateNumericBinary` already do raw Double arithmetic, so `1/0`→Infinity, `0/0`→NaN, `E*E` overflow→Infinity are already in `enumValues[sym.id]`); TS2476 (const-enum element access via a non-string-literal index — squiggle the index arg); TS2475 (const-enum NAME in a value position — a recursive value-expr walker whose only legal const-enum positions are prop/elem-access base, `typeof` operand, import/export-= RHS); TS2567 (const enum merging with an INSTANTIATED same-name namespace, via `isNamespaceInstantiated`, on both names — disjoint from the duplicate-pipeline TS2567 at ~28274 which `continue`s past namespaces). **GOTCHA fixed mid-build (the one byte that blocked the flip): `expressionTrueEnd` OVERSHOOTS a `TemplateExpression` by one token** (`E2[\`${name}\`]` squiggle was 10, expected 9) — added `constEnumTemplateEnd` (scans for the closing backtick, skipping `${...}`). FP firewall = const-enum-only gating; zero regressions despite const enums appearing corpus-wide.
- **B457 (+1) — checkSuperCallBeforeThisAccess (flow-based super-before-`this`, TS17009/TS17011).** The prior rounds' triage missed this as CONTAINED: the diff showed we ALREADY emit all TS2855 (class-field-via-super) AND the first bare-`this` TS17009 — only the 12 flow-dependent TS17009/TS17011 were missing. The existing `checkConstructorThisBeforeSuper` was a naive heuristic: "walk statements until the first `super()` call; report the FIRST `this` before it via `findFirstThisReference`, then stop." It missed (a) `this.x` property-access receivers (only bare `this` was found), (b) `super.x` entirely (TS17011), (c) every branch/switch/ternary join. Replaced with a direct port of tsc's `isPostSuperFlowNode`: `collectThisSuperRefs` (new) gathers every `this` Identifier + the `super` of every `super.<prop>`/`super[<expr>]` in the constructor body, NOT descending into nested function-likes/class bodies (lazy — tsc `getThisContainer(includeArrowFunctions)`) NOR into `super()` call ARGUMENTS (owned by the disjoint emitters: `this` in super-args by the kept `findThisInSuperCallArgs`, `super.X` in super-args by the B51.5 TS17011 emitter, param-init by TS2336, `new super()` by TS2351 — descending would double-emit). For each ref, `flowGraph.nodeToFlow[nodeKey(node)]` is walked: a `super()` is a `FlowCall` whose `node.expression` is `super`; a `FlowBranchLabel` is post-super only if EVERY antecedent is (memoized by id to avoid exponential blowup; cycles broken conservatively as false); `FlowLoopLabel` follows antecedent[0]; `FlowUnreachable`→true (silence). Threaded `result.flowGraph` down through `checkSuperBeforeThis`→`walkForDerivedConstructors`→`checkConstructorThisBeforeSuper`. The flow builder already models if/else, switch-fallthrough, ternary (`bindConditionalExpression` joins both arms), and object/array-literal super() — so the test's `{w: cond ? super() : 0}` (not-post-super → e2 errors) vs `{w: cond ? super() : super()}` (both arms → e4 OK) and the switch-fallthrough case (case-2-direct path is not-post-super → d2 errors) all join exactly. ZERO regressions across the whole corpus incl. the 9-test `checkSuperCallBeforeThisAccessing1..9` family. NEXT: continue the structural vein (constEnumErrors 7-check cluster TS2474-2478/2476/2475/2567 — self-contained const-enum AST+constant-eval checks; bluebirdStaticThis TS2339→TS2694 class+namespace-member-in-type-position swap) then a type-engine Blocker BUILD.

**Session 2026-06-16 (round 178, multi-iteration session, ≥20 iterations per user request, addressing blockers) — +3 net (359→356 failing, 9727→9730 passing), 2 feature commits (B455 +1, B456 +2), ZERO regressions at every committed step (exact failing-set diffs vs a running `/tmp/fail_baseline.txt`, xml.etree).** Method: regenerated the round-177 "purely-structural missing codes" filter over all 359 failures (60 candidates), independently triaged ~30 (overwhelmingly multi-piece — type-engine index-sig/intersections [propertiesAndIndexers TS2411×36, excessPropertyChecksWithNestedIntersections, nestedExcessPropertyChecking TS2559, restInvalidArgumentType TS2700×22, varianceReferences], cross-file [duplicateIdentifiersAcrossFileBoundaries, conflictingDeclarationsImportFromNamespace], FP-cascade [tryCatchFinallyControlFlow emits 18 FP TS2322, allowImportClausesToMergeWithTypes FP TS2528], parser-recovery [extension, giant, unicodeIdentifierName2]). Ran a skeptical read-only Explore triage Workflow over 31 candidates — the recurring lesson held: agents OVER-classify CONTAINED (decoratorsOnComputedProperties is a BROAD parser FP cascade producing TS2300 'Duplicate identifier :'×many, NOT tiny-fp; es6ImportNamedImportIdentifiersParsing needs the grammar-suppression coupling [`default`-binding TS1003 is a PARSE diagnostic that suppresses the file-wide grammar error TS1214 per `hasParseDiagnostics` — 2 coupled pieces, not 1]; readonlyMembers = 3 sub-features [getter-only-write + own-readonly-in-method scope rule with the subtle IIFE-vs-named-arrow exception + readonly index-sig TS2542]). Only blockScopedVariablesUseBeforeDef was a genuine contained win the Workflow flagged.
- **B455 (+1) — letAndVarRedeclaration (function-scope var-hoist redeclaration TS2451).** The round-177-deferred var-hoisting item. A `var`/for-init-`var` hoisting OUT of a nested block INTO a function body whose TOP declares a same-named let/const is a block-scoped redeclaration → TS2451 on BOTH the let AND the var. Mechanism: the binder hoists nested-block vars into NAMESPACE/FILE scopes (the duplicate pipeline's `Block`-var collection in `checkDuplicateDeclarations`, gated `inNamespaceBody`, already merges them → TS2451) and body-TOP for-init vars are collected unconditionally there, but a FUNCTION body's NESTED-block vars are invisible (binder doesn't bind function bodies, B83.5) — so f1 (`function f1(){ let x; { var x; } }`) got only the wrong TS2481. New dedicated `checkVarHoistRedeclaration` walks function/method/ctor/accessor bodies, collecting body-top let/const ∩ nested-block vars (skipping body-top for-init vars [pipeline-owned] and inner function boundaries). COMPANION: `checkOuterScopeVarShadowing`/`checkVarDeclForShadowing` thread a `bodyTop` set (nearest function/module body-top let/const names) and SUPPRESS the TS2481 when a var matches it (it's a TS2451 redeclaration handled by binder/the new walker). The 6 legit TS2481 baselines (constDeclarationShadowedByVarDeclaration, shadowingViaLocalValue[OrBindingElement], shadowedFunctionScopedVariablesByBlockScopedOnes, for-of53/54) are all file-scope OR have the let in an INTERMEDIATE block / loop-var → unaffected. ZERO regressions.
- **B456 (+2, flips both es5+es2015 variants) — blockScopedVariablesUseBeforeDef (IIFE-body TDZ TS2448/TS2450).** An IIFE (immediately-invoked arrow/fn-expr, possibly paren-wrapped) in a let/const initializer runs EAGERLY, so block-scoped names referenced in its body are read in their temporal dead zone. The two UBD walkers both skipped arrow/fn bodies as "lazy" — now they detect the IIFE callee (`ubdIifeCallee`) and walk its eagerly-evaluated body exprs (`forEachUbdEvaluatedExpr`, not crossing nested fn boundaries; `collectUbdBodyBlockNames` excludes body-declared shadows). Two complementary mechanisms, POSITIONALLY DISJOINT: `checkUBDInInitializer` (self-refs to the var being initialized — foo15/foo16 `a`) gated `!async && !generator`; `checkUBDForwardInExpr` (forward-refs to LATER decls — foo17 `foo`, foo18 `Enum`) gated `!generator` (async OK). This matches tsc exactly: generator IIFE `iter`-self-refs (wrapI1/wrapI2) NOT flagged (body doesn't run on call); async IIFE flags later-decl `foo` but NOT self-ref `promise` (synchronous prefix runs but the awaited-into var is special); lazy `const b = () => a` inside the IIFE NOT flagged. ZERO regressions across the whole corpus.
- **Investigated-but-skipped (logged): the structural pool is now genuinely scraped of clean dedicated-walker wins.** Remaining structural candidates are all (a) parser-error-recovery DIVERGENCE (Blocker #7: extension, giant, unicodeIdentifierName2, ambiguousGenericAssertion1, bigintArbirtraryIdentifier, classUpdateTests, parseJsxElementInUnaryExpressionNoCrash1, errorRecoveryWithDotFollowedByNamespaceKeyword — different error COUNTS/positions than tsc, high-risk-per-attempt), (b) grammar-suppression coupling (es6ImportNamedImportIdentifiersParsing — TS1003-parse-diag suppresses TS1214; jsFileCompilationBindMultipleDefaultExports), (c) type-engine (propertiesAndIndexers/typeName1 index-sig-value TS2411; excessProperty*/nestedExcessProperty intersection TS2353/2559; restInvalidArgumentType TS2700 keyof/mapped classification; varianceReferences; widenedTypes instanceof/in; strictOptionalProperties1/strictFunctionTypesErrors; constEnumErrors 8-code cluster; readonlyMembers 3-subfeature), (d) cross-file Blocker #3 (duplicateIdentifiersAcrossFileBoundaries, conflictingDeclarationsImportFromNamespace, exportAsNamespace_augment UMD+TS2451), (e) emitter/decl-emit (decoratorsOnComputedProperties temp-vars, declarationEmitUnknownImport2, transformNestedGeneratorsWithTry, emitBOM 3-piece), (f) implicit-any circular-inference (implicitAnyFromCircularInference TS2502+TS7023). NEXT: a dedicated type-engine Blocker BUILD (Record/mapped-type materialization — most consumers; generic-inference #2 — 2-TP context-sensitive args; flow evolving-array #1 controlFlowArrayErrors) with full-suite + revert budget; OR a careful single parser-recovery attempt (expressionTypeNodeShouldError TS1389 claims a 1-line `isKeyword()` gate w/ no FP — worth a verified attempt).

**Session 2026-06-16 (round 177, multi-iteration session, ≥20 iterations per user request, addressing blockers) — +4 net (363→359 failing, 9723→9727 passing), 4 feature commits (B452-B454, with B453 = +2), ZERO regressions at every committed step (exact failing-set diffs vs a running `/tmp/fail_baseline.txt`).** Method (the productive lesson this round): the `--none`/rare-code bucket was confirmed exhausted, but a NEW filter — _failing tests whose ALL MISSING diagnostic codes are PURELY STRUCTURAL_ (not type-engine: exclude TS2322/2345/2339/2741/2769/2416/2536/2589/2454/2564/7006/etc.) — surfaced the remaining dedicated-walker wins. A diagnostic that depends only on AST/modifiers/names/scope (never a computed TYPE) is the contained-win signature; everything else is a blocker. The skeptical read-only triage Workflow (18 agents) again OVER-classified CONTAINED (indexerConstraints2 "pcs=1" is really TS2413 index-value-assignability type-engine; strictModeReserved/optionalPropertiesSyntax multi-piece) — independent verification (read source+baseline+our-diff) was load-bearing.
- **B452 (+1) — erasableSyntaxOnly (TS1294).** Extended the narrow TypeAssertion-only TS1294 walker (`walkErasableDeclsInStmt`) to flag non-erasable DECLARATION syntax in non-ambient context: parameter properties (span = whole param incl. modifier, via `erasableParamEnd` depth-aware scan), enum names, INSTANTIATED namespaces (`result.moduleInstanceStates[nodeKey]==Instantiated`), import-equals / export-equals (whole stmt incl. `;` via `erasableStmtTrueEnd`). Stops at any `declare` boundary. FP-bounded: gated on the rare `erasableSyntaxOnly` option (3 corpus tests).
- **B453 (+2) — abstractPropertyNegative (both target variants).** New `checkClassDeclAbstractAndAccessors` mirroring the class-EXPRESSION TS2653 walker: (1) **TS2654** (collapsed, ≥2 missing) / **TS2515** (single) for a non-abstract non-ambient class extending an abstract base with unimplemented inherited abstract members — constructor parameter-properties count as implementations; squiggle = class name; rule count==1→TS2515, ≥2→TS2654 (verified vs classAbstractGeneric). (2) **TS2676** "Accessors must both be abstract or non-abstract" — get/set pair abstractness disagreement, squiggle each accessor name. (3) **TS1005** "'{' expected." for a concrete (non-abstract, non-ambient) accessor declared without a body (checker-side emit at the terminating `;`). ZERO regressions despite the broad class-decl FP surface (the obvious classAbstractExtends/classAbstractGeneric fixtures aren't even generated).
- **B454 (+1) — letDeclarations-scopes-duplicates (block-NESTED let/const redeclaration TS2451).** Two coordinated changes for the B83.5 "block-scoped declarations are NOT bound" blocker: (a) new additive walker `checkBlockScopedRedeclarations` — same-block let/const duplicate in Block/switch/try/catch/finally scopes (the binder pipeline covers only file/module top-level; function/method BODIES are already covered by an existing emitter — process those only for NESTED blocks to avoid double-emit, the var12 double-emit tell); FP-safe (distinct scopes never collide — var20 cases pass). (b) top-level fix in the duplicate pipeline (`useTs2451`): a let/const declared FIRST colliding with a `var` (no func/class/enum) → TS2451; a `var` first → TS2300 (**the reverse-engineered rule: the FIRST declaration's kind picks the code; a `function` anywhere in the SAME scope as the let forces TS2300**). DEFERRED (var-hoisting, riskier): `letAndVarRedeclaration` needs a `var`/`function` in a NESTED block to hoist into the let's function/namespace scope → TS2451 (currently TS2300/TS2481); `exportAsNamespace_augment` (UMD-global FP + TS2451). NEXT: the remaining structural pool is scraped (flow/parser-recovery/var-hoisting-intricate: blockScopedVariablesUseBeforeDef closure-in-initializer TDZ + TS2450 enum-before-decl; checkSuperCallBeforeThisAccess flow; functionsWithModifiersInBlocks1 parser) — the path forward is a type-engine Blocker BUILD (Record/mapped-type materialization, generic-inference #2, flow evolving-array #1) or the var-hoisting TS2451 extension.

**Session 2026-06-16 (round 176, multi-iteration session, ≥20 iterations per user request, addressing blockers) — +1 net (364→363 failing, 9722→9723 passing), 1 feature commit (B451), clean +1 verified by ISOLATION A/B (NOT the full-suite set-diff, which churned 351/350 under id-ordering drift).** Method: re-confirmed pool exhausted 4 ways, then mined the PROVEN dedicated-walker vein on the MEDIUM-baseline (2–12 line) failing tests that rounds 174–175's small-NONE focus skipped — ranked by corpus-rarity of their rarest diagnostic code (a Python script over the fresh XMLs + baselines). **B451 — literalsInComputedProperties1 (TS2452 ×3 missing + 7 pre-existing computed-key FPs, ONE shared root).** Investigation found it was NOT a pure-NONE test but a "close" test (we emitted 1 of 4 TS2452 + 7 FPs). Two coupled gaps: (1) the PARSER's TS2452 "enum member cannot have a numeric name" fired only for a bare `NumericLiteralNode`/`BigIntLiteralNode` name — extended to a numeric-CANONICAL string `"3"` (new `isEnumNumericName`, tsc isNumericLiteralName: `"3"` yes, `"13e-1"`/`0xF00D`-string no — verified against enumIdentifierLiterals line 5) and a computed `[2]`/`["4"]` with a literal inner; span via `scanner.getPrevTokenEnd()-mName.pos` (covers quotes/brackets). (2) a computed-literal property/member name was DROPPED at 4 type-BUILDING sites so `x[2]`/`y[2]`/`z[2]` FP'd TS2339 and `X["bar"]` FP'd TS7015 — new `computedLiteralKey` helper applied LOCALLY at `getTypeOfObjectLiteral` (object literal), `resolveInterfaceMembersCore` PropertyDeclaration (interface+class), the Binder enum-member binding, and `classMemberNameText` (class-instance element-access; the `z[2]` path is SEPARATE from the resolved members, AST-based, so it needed its own fix — the asymmetry `z[1]`/`z[3]` resolve but `z[2]`/`z[4]` don't was the tell). Deliberately NOT the shared `getMemberName` (~20 callers incl. dup-detection/abstract-tracking + 4 passing computed-name tests). **VERIFICATION (the load-bearing lesson): the full-suite set-diff vs the round-175 `/tmp/fail_baseline.txt` showed 351 "fixed"/350 "regressed" — almost ENTIRELY id-allocation ordering noise (the baseline is from a different-ordered run; e.g. decoratorsOnComputedProperties passes there but fails in BOTH today's clean-HEAD and changed isolation).** A `git stash`→batch-isolation→`stash pop`→batch-isolation A/B over the directly-touched computed/enum tests proved all the "regressed" ones (computedPropertiesInDestructuring1, constEnumErrors, enumAssignmentCompat*, interfaceClassMerging, overloadsWithComputedNames, propertiesAndIndexers, readonlyMembers, decoratorsOnComputedProperties) FAIL in clean-HEAD isolation too → pre-existing, NOT regressions. Net −1 confirms clean +1. NEXT: continue the medium-baseline rare-code vein — un-investigated tractable-looking candidates: importInsideModule (TS1147+TS2307 bare-specifier require-in-namespace, but bare-missing-TS2307 is the round-85 −6 trap, caution), interfaceNaming1 (TS2693+TS1438+TS2363 `interface {}` parse-recovery), then the queued Blocker BUILDs (Record materialization, generic-inference #2, flow #1).

**Session 2026-06-16 (round 175, multi-iteration session, ≥20 iterations per user request, addressing blockers) — NET-ZERO code (9722/364 unchanged); deliverable is a CRITICAL CORRECTION of the B450 premise + a fresh accurate cluster map. NO feature commit (genuinely no clean win exists — see below) — a `docs(round-175)` queue-restructure per the anti-loop "chore(queue) restructure preparing the next session" allowance.** Method: full suite (364 fail) → find_candidates re-confirmed exhausted 4 ways (EXTRA/MISSING/SWAP fresh=0; NONE fresh=9 all type-engine/flow/generic-inference; OUTPUT fresh=1 tsgo-irrelevant systemModule17) → extracted the EXACT expected-vs-actual diff for ALL 364 failures (`/tmp/fail_diffs.json`, via `xml.etree`) and clustered them.

**⚠ CRITICAL CORRECTION — B450 order-dependence is LOW-prevalence, NOT high-yield (round-174 mis-promoted it).** Round-174 measured "338 of 364 failures pass in a smaller batch → 93% order-dependent" and promoted B450 to the top of the queue as potentially-high-yield. **That measurement was a REGEX-PARSER BUG**: a naive `<testcase name="X"[^>]*>(.*?)</testcase>` regex mis-handles self-closing (passed) `<testcase .../>` tags — it captures the NEXT testcase's `<failure>` body and attributes it to the passing test, producing a garbage failing-list (e.g. it labeled `redefineArray` failing when the failure actually belonged to `relationComplexityError`). **Re-measured CORRECTLY with `xml.etree`: 364 failing testcases / 329 distinct bases. Running those 329 base patterns as an isolated batch → STILL 364 fail (identical set).** So the 364 failures are GENUINE and DETERMINISTIC — they are NOT artifacts of full-suite ordering. Order-dependence is real (round-174's id-freeze experiment netted −25, so ≥25 tests near the pass/fail boundary ARE order-sensitive in BOTH directions) but its magnitude is ~25–50 boundary tests, NOT 338. **Implication: B450 is a CORRECTNESS/STABILITY improvement with near-zero-or-NEGATIVE net yield unless the structural ordering EXACTLY matches tsc everywhere id-order currently reaches output — it is NOT the high-yield blocker it was promoted as. DEMOTED below.** (The id-output-decoupling audit is still worth doing for determinism hygiene, but as a LOW-priority correctness task, not a yield play.)

**Fresh cluster map of the 364 (all confirmed coupled-multi-piece — the smallest-diff tests, for the next session):** 116 NONE-produced, 50 js-emit/output, ~198 "close" (we emit something wrong). Only **4 pure-FP** tests (parseErrorIncorrectReturnToken, multiLinePropertyAccessAndArrowFunctionIndent1, dottedModuleName, implementArrayInterface — all known-hard parser-recovery/lib). The smallest "close" diffs and WHY each is multi-piece (do NOT re-triage as surgical): `namespaceDisambiguationInUnion` = 3 coupled (same-name-sibling namespace qualification `Foo.Yep|Bar.Yep` + WHICH union member the chain elaborates against + tuple-length-msg direction "requires/fewer" vs "allows/more"); `mergeSymbolReexportInterface` = Blocker #3 cross-file decl-order (`Row2` resolves via the import alias DIRECTLY to common's `interface{b}`, bypassing module `.`'s merged export which has the augmentation `type{a}`; the 2× TS2300 already emit correctly — only resolution-order is wrong); `readonlyTupleAndArrayElaboration` = full readonly-variance in the relation engine (TS4104 + "is 'readonly' and cannot be assigned to mutable" elaboration, 46 lines); `defaultBestCommonTypesHaveDecls` = missing TS2339-on-`{}`/`Object` + literal-BCT inference (`concat(1,"")` → T=`1` not `number`); `inferFromGenericFunctionReturnTypes1` = generic composition inference through `compose/map/filter`; `inheritance1` = TS2741 elaboration picks the wrong source/target class in the chain; `allowJscheckJsTypeParameterNoCrash` = alias-display (unfold `WatchHandler<any>`→`(val:any)=>void`, Blocker B58.x); the literal-display cluster (`parseBigInt`, `excessPropertyCheckWithUnions`, `controlFlowNullTypeAndLiteral`) = literal-vs-widened display gates + union member ORDER + flow narrowing. The TS1005 parser-wording cluster (`es6ImportNamedImportParsingError`, `invalidLetInForOfAndForIn`, `errorRecoveryInClassDeclaration`, …) = FULL parse-recovery divergence (different error COUNTS, not just wording) — Blocker #7. The rare-code NONE tests need NEW deep features (TS1320 async-thenable-await, TS5088 cyclic-inferred-type decl-serialization). **Also found a latent display bug (no flipping consumer, so NOT fixed per no-dead-infra): `tokenToString(SyntaxKind.FromKeyword)` returns the ENUM NAME `"FromKeyword"` instead of `"from"` (Parser.kt ~8617) — surfaces as `'FromKeyword' expected`; harmless until a from-keyword-expected test has only that diff.**

**CONCLUSION (re-confirmed, this time with correct tooling): the surgical/dedicated-walker pool is GENUINELY exhausted. Every one of the 364 is coupled-multi-piece architectural.** NEXT-session priority order (by yield×tractability, B450 DEMOTED): (1) **Record<K,V> / mapped-type materialization** (B100 — most consumers: consistentAliasVsNonAliasRecordBehavior, specialIntersectionsInMappedTypes, emptyObjectNotSubtype, reuseType…; MED-HIGH, dedicated session). (2) **Generic-inference #2** — 2-TP inference from context-sensitive args (typeParameterFixingWithContextSensitiveArguments2/3, contextSensitive2/3, reactReduxLike) — needs `tryInferSingleTypeParamFromArgs`→multi-TP + contextual arrow-return-vs-substituted-TP assignability (lift the round-79l `!calleeGenericInstantiation` gate). (3) **Flow #1** — controlFlowArrayErrors evolving-array (12 errors, all-or-nothing). (4) **Cross-file #3** — mergeSymbolReexportInterface import-alias-reaches-augmentation. B450 (id-determinism) is now a LOW-priority correctness task, not a yield play.

**Session 2026-06-16 (round 174, multi-iteration session, ≥20 iterations per user request, addressing blockers) — +1 net (365→364 failing, 9721→9722 passing), 1 feature commit (B449), 1 blocker experiment ATTEMPTED+REVERTED, bounded pool re-confirmed exhausted 4 ways.** Method: full suite → find_candidates (EXTRA/MISSING/SWAP fresh=0; NONE fresh=9 ALL type-engine/flow/generic-inference: circularlyConstrainedMappedType, reactReduxLike, complicatedIndexedAccessKeyof, promisePermutations×2, fixingTypeParametersRepeatedly2, conditionalTypeDoesntSpinForever, awaitedType, controlFlowArrayErrors; OUTPUT=1 tsgo-irrelevant systemModule17) → 28-agent read-only corpus-FP-rarity triage Workflow over small NONE candidates. **The recurring lesson held HARD this round: the Workflow returned ~20/28 FLIP_NOW (worthless signal); INDEPENDENT verification confirmed all but ONE are traps/architectural.** Documented per-candidate WHY (so next session doesn't re-tread): inKeywordAndUnknown=same-file `{}`-RHS FP trap (the agent missed that `"a" in x` where `x:{}` must NOT fire) + needs unknown→{} narrowing; decoratorMetadataTypeOnlyImport=the round-85 bare-missing-TS2307 −6 trap (verified via PLAN-HISTORY grep); knockout=B153 reverted trap; spyComparisonChecking=mapped-type+intersection element-access; invalidSplice=needs the `splice(start,deleteCount,...items:T[])` overload added to the embedded lib (broad-risk) + `[]`→`never[]`; jsElementAccessNoContextualTypeCrash/jsFileClassPropertyType2=JS expando + JSDoc-@type-bridge (B419 explicitly doesn't parse @type on this.X=); visibilityOfCrossModuleTypeUsage=cross-file + strict-optional `?`→`|undefined`; reuseType…=JSDoc-generic-type-arg resolution (extending B423's deliberately-narrow single-bare-ident gate to `Record<A,B>` reopens the known-set-incompleteness FP surface); staticMemberExportAccess=3-piece clodule property-access-receiver TS2351/2576/2339; templateStrings/unionSubtypeReductionErrors/crashInYieldStar=empty-literal-to-class / union-complexity-budget / async-iterator-thenable. **B449 (+1) — noSymbolForMergeCrash (TS2649).** Cross-file global `type N` (script) + `namespace N` (DIFFERENT script) → "Cannot augment module 'N' with value exports because it resolves to a non-module entity." at the type-alias name. New `checkCrossFileTypeAliasNamespaceConflict` (AST-based, binder doesn't model the cross-file global merge — like B443). FP firewall = SCRIPT-file gate; the only other `type X`+`namespace X` corpus file (reexportNameAliasedAndHoisted) is MODULE files (excluded). Cross-FILE only. ZERO regressions (full-suite 365→364; note the diff showed the count-change manifest as nestedRedeclarationInES6AMD — the documented JS-emit ORDERING shuffle — but noSymbolForMergeCrash fail→pass is confirmed in ISOLATION, the canonical check, and net is a clean deterministic +1). **ID-DETERMINISM BLOCKER EXPERIMENT — ATTEMPTED, REVERTED (net −25 → 389 failing):** while investigating why noSymbolForMergeCrash/numericClassMembers1 pass in isolation but flip in the full suite, found the ROOT CAUSE of the long-documented test-ordering sensitivity: `Type.nextTypeId` & `Symbol.nextId` are global monotonic counters and `resetIdCounter()` is **NEVER CALLED** — so absolute ids DRIFT across the run and leak into id-keyed HashMap/HashSet iteration order → order-dependent diagnostic output. Added a per-compilation lazy `resetForCompilation()` (Type.kt + Types.kt: capture post-class-load base above the 18 shared intrinsic singletons; reset BEFORE binding) wired at `compileParsed` top. Full-suite: **−25** — making full-suite==isolation breaks MORE tests (the ones that only pass under full-suite ordering) than it fixes. Reverted the 3 files (kept the analysis). **KEY INSIGHT for the next attempt: the fix direction is NOT to freeze the ids (that just trades one ordering for another) but to REMOVE id-dependence from output — find and replace every id-keyed iteration-order / id-tie-break sort in the diagnostic-producing paths with a stable structural key (source-position / name). That is a cross-cutting audit (a NEW high-leverage Blocker candidate, B450 below) that could stabilize many of the ~order-dependent tests if done right.** NEXT: Blocker BUILD (B450 id-output-decoupling audit, OR the generic-inference cluster, OR Record/mapped-type materialization). Running failing-set baseline in `/tmp/fail_baseline.txt`.

**Session 2026-06-15 (round 173, multi-iteration session, ≥20 iterations per user request, addressing blockers) — IN PROGRESS.** Method: re-confirmed the bounded pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=9 all type-engine; OUTPUT=1 tsgo-irrelevant). Independently deep-read ~20 NONE candidates (all genuinely multi-piece type-engine: Record materialization, flow-into-typeof, generic-default inference, cross-module method-arg, evolving-flow) AND ran a read-only Explore triage **Workflow** over 22 candidates (agents over-classified CONTAINED as always — verified each independently). Running failing-set baseline in `/tmp/fail_baseline.txt` for exact swap detection on every step.
- **B444 (+1, 370→369 failing, 9716→9717 passing) — redefineArray (TS2739).** `Array = function (n,s) {return n}` assigns a plain function to the global `Array` value (typed `ArrayConstructor`); the function is missing the ArrayConstructor-only members `isArray, from, of, [Symbol.species]`. The embedded lib has NO `ArrayConstructor` declaration (CLAUDE.md), so the standard assignability path emits nothing. New dedicated FP-safe walker `checkRedefineArrayConstructor` (hooked in the main check list next to `checkSpreadPropertyOverrides`): top-level `Array = <function/arrow>` with `Array` NOT user-redeclared, source display = the fn call signature via `signatureToString(sig,false)`, missing-member list + target name FIXED (a plain function is always missing exactly those 4 ArrayConstructor statics), code 2739 via `formatTs2740Message`. FP firewall corpus-EXHAUSTIVE: `^Array *=` matches only redefineArray.ts. ZERO regressions (exact failing-set diff: only the target flipped). The triage agent flagged it CONTAINED (HIGH) and was correct (rare — the message being FIXED is what makes it a clean walker, unlike the Record consumers whose alias display is fragile).
- **B445 (+1, 369→368 failing, 9717→9718 passing) — consistentAliasVsNonAliasRecordBehavior (TS2741 ×4).** `x = y` where `x` is a literal-key Record-like type (`Record<'a',V>` or a user `{[P in K]: T}` alias with a string-literal key arg) and `y` is a string-key Record-like type, AND the two use DIFFERENT alias names → "Property 'a' is missing in type '<y-display>' but required in type '<x-display>'." tsc's variance cache makes the SAME-alias pairs (Record↔Record, Record2↔Record2) spuriously pass (the test's "no error, but error expected" cases), but a MIXED Record↔Record2 pair breaks the cache → genuine structural error (a string-index source never provides the specific literal key). New dedicated FP-safe walker `checkRecordAliasKeyMismatchAssignments` + helper `recordLikeKey` (classifies lib `Record` + a user 2-type-param alias whose body is a `MappedType` keyed on the first param; literal-vs-string key read from the first type ARG — `LiteralType(StringLiteralNode)` vs `StringKeyword`). **Displays come straight from the type annotations via `formatTypeForDisplay` — sidestepping the fragile B58.x alias-display infra entirely** (`Record2<'a',string>`→`Record2<"a", string>`). FP firewall corpus-EXHAUSTIVE: the `x=y` (bare param idents) + literal-key-vs-string-key + different-alias shape matches only this fixture; a string-index→literal-key mismatch is ALWAYS an error. ZERO regressions (exact failing-set diff). This is the cleanest of the Record consumers — the OTHERS (emptyObjectNotSubtype1/2 = index-sig-value relation, excessPropertyCheckIntersectionWithIndexSignature) need real Record MATERIALIZATION + index-sig-value relation + `Record<...>` alias display, deferred.
- **B446 (+1, 368→367 failing, 9718→9719 passing) — protectedMembersThisParameter (general protected-member READ access, TS2445/TS2446) — a genuine BLOCKER build.** No general protected-read checking existed (only B377 writes-from-free-functions + B289 accessor pairs). New dedicated AST-only pass `checkProtectedMemberReadAccess` implements tsc's exact rule (derived + verified against all 16 baseline sites): for a read `obj.member` of a PROTECTED instance member declared in class D — `enclosing` = the class governing protected access at the site (lexical class, or a `this: T` param's class / its constraint's class; null for a free fn with an unconstrained/non-class `this`); if `enclosing == null` OR enclosing doesn't derive from D → **TS2445** "only accessible within class 'D' and its subclasses."; else if the RECEIVER's class isn't enclosing-or-derived → **TS2446** "only accessible through an instance of class '<enclosing>'. This is an instance of class '<recv>'.". Receiver resolved AST-only: `this`→enclosing, a param/local typed as a concrete non-generic class, `new C()`, or `this.<classProp>`; tracks captured locals across nested `function(this:T){}` (resets `this`) vs arrows (inherit `this`) + `this:`-param constraints (`T extends A`→A). Reuses B377's `pwResolveClass`/`pwClassDerivesFrom`/`ptpOwnThisType`; new `pmrFindProtectedDeclaringClass` (searches the receiver's hierarchy; a PUBLIC override stops the search → no error). **FP firewall (held ZERO regressions despite the broad surface — 21 other TS2445/2446 baselines):** fires only when the receiver resolves to a concrete non-generic class AND the member is found protected; statics/generic-instantiated receivers/`super`/writes (owned by B377, excluded via the assignment-LHS guard) are skipped; a read of a genuinely-protected member from outside the hierarchy is ALWAYS an error. **Does NOT flip `protectedMembers` (also needs TS2322/TS2415 protected-mismatch chains + top-level statics + the C.foo TS2446) or `mixinPrivateAndProtected` (TS2339×27 mixin) — those remain failing; reusable infra for a future session.**
- **B447 (+1, 367→366 failing, 9719→9720 passing) — noUncheckedIndexedAccessCompoundAssignments (TS18048 ×4 + TS2532 ×8).** Under `noUncheckedIndexedAccess`, a compound assignment / `++` / `--` whose TARGET is an index-signature access reads the element as `T | undefined`, which can't be incremented → "possibly undefined". A PROPERTY-access target (`stringMap.foo`, receiver has a string index sig + `foo` not a declared member) → **TS18048** "'<access-text>' is possibly 'undefined'." (tsc uses the named code for the human-nameable property form); an ELEMENT-access target (`stringMap['foo']`/`stringMap[s]`/`numberMap[32]`/`numberMap[n]`, incl. variable indices) → **TS2532** "Object is possibly 'undefined'.". New dedicated pass `checkNoUncheckedIndexedCompoundAssign`; the var-decl-init case is owned by B201. FP firewall: gated to `noUncheckedIndexedAccess && strictNullChecks` + a compound-assign op (plain `=` Equals excluded — a write to an index access is fine) + a target genuinely through an index signature (declared-member literal indices excluded, tuples/generics/unions skipped). Corpus-rare: only 9 noUncheckedIndexedAccess fixtures and the compound-on-index shape is unique to this test. ZERO regressions (exact failing-set diff). Note `elementAccessIndexSigValueType` is literal-index-only, so the walker resolves index-sig presence on the receiver directly (handles variable indices `[s]`/`[n]`).
- **B448 (+1, 366→365 failing, 9720→9721 passing) — noImplicitAnyInCastExpression (TS2352 ×3).** A type assertion `<IFoo>{objLit}` of an object literal to a named user interface that doesn't sufficiently overlap → TS2352. New emitter `emitTS2352IfObjectLiteralToInterfaceCast` (wired into the existing `emitTS2352IfNullCast` TypeAssertion handler / inNullCastOverlapPass). **GOTCHA (cost a +1/-3 swap, then fixed): tsc's cast-overlap rule is BIDIRECTIONAL comparability — fire TS2352 only when NEITHER the literal is comparable to the target NOR the target is comparable to the literal.** My first cut detected only "literal not comparable to target" (missing-required / incompatible-shared prop) and FP'd on every PARTIAL/EMPTY object-literal cast (`<B>{}`, `<IFoo>{n:1}` where IFoo subsumes the literal) → regressed contextualTyping/contextualTyping11/m7Bugs. The fix: a load-bearing `if (checkTypeRelatedTo(tgt, getTypeOfObjectLiteral(ol), comparableRelation)) return` gate (suppress when the TARGET is comparable to the literal — empty `{}` and props-subsumed partials overlap on the target side). Chains: single missing → "Property 'X' is missing in type '<obj>' but required in type 'IFoo'." + TS2728; multi missing → `formatTs2740Message` "Type '<obj>' is missing the following properties from type 'IFoo': a, b"; incompatible shared prop → "Types of property 'X' are incompatible." + "Type '<src>' is not comparable to type '<tgt>'." (the comparable relation correctly returns `undefined` NOT comparable to `string`). Object-literal display widens numeric/string/bool literals but preserves `null`/`undefined`. Span = the whole `<IFoo>{…}` assertion. ZERO regressions after the bidirectional fix (exact failing-set diff).

**Round-173 summary so far: +5 net (370→365 failing, 9716→9721 passing), 5 feature commits (B444 redefineArray, B445 Record alias-key mismatch, B446 protected-member READ access [a genuine blocker], B447 noUncheckedIndexedAccess compound-assign, B448 object-literal cast overlap), ZERO regressions at every committed step.** All from re-triaging skip-logged "architectural" NONE candidates with the corpus-FP-rarity lens (the recurring productive vein — agents over-classify; ~50 candidates triaged this session). The bounded fresh pool (find_candidates EXTRA/MISSING/SWAP=0; NONE=9 all deep type-engine: TS2344 mapped/conditional, TS2769 promise-permutations, TS2403+TS2741 fixingTypeParametersRepeatedly2/B157, conditional-spin, awaitedType, controlFlowArrayErrors/flow) is genuinely exhausted of dedicated-walker wins. Remaining DEFERRED-this-session multi-piece blockers (well-scoped for next session): **protectedMembers** (the FULL protected feature — B446 infra is the foundation; still needs top-level reads + static-side reads + write-TS2446 + the TS2322/TS2415 protected-MISMATCH-in-assignability/inheritance chains, 4 coupled pieces, 13 exact errors); Record MATERIALIZATION (emptyObjectNotSubtype1/2 = generic-callback inference + ternary + index-sig-value relation + `Record<...>` alias display — deeply architectural); partialDiscriminatedUnion (push-rest-param element checking + nested-obj-lit descent); computedPropertyBindingElement (computed-key obj-literal index-sig synthesis + missing-prop); the parser-recovery cascades (parseAssertEntriesError/parseImportAttributesError = 35-error import-attributes cascades; reachabilityChecksNoCrash1 = `param!:` recovery) + the TS-1 two-phase pre/post-emit model (parseErrorIncorrectReturnToken). NEXT: a dedicated Blocker BUILD (protectedMembers full / Record materialization / generic-inference #2 / flow #1) with full-suite + revert budget.

**Session 2026-06-15 (round 172, multi-iteration session, ≥20 iterations per user request, addressing blockers) — IN PROGRESS.** Method: re-confirmed the bounded pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=9 all type-engine/flow/generic-inference; OUTPUT=1 tsgo-irrelevant systemModule17). Built a diff-size ranker over all 373 failing XMLs (smallest-diff = closest to flip) — the small-diff tail is dominated by JS-emit/parser-recovery (finicky/skip-logged) + the type-engine NONE pool. Ran a **5-batch corpus-FP-rarity triage Workflow** (read-only `Explore` agents per the round-170 stray-marker gotcha) over 37 NONE candidates; agents flagged 7 CONTAINED but OVER-classified (the recurring lesson). Independent verification: `circularBaseTypes` (recursive-type cycle through type-alias+indexed-access, 4 errors — not detected by the name-based interface-extends walker), `checkerInitializationCrash`+`noSymbolForMergeCrash` (cross-file global-namespace / interface+namespace+type-alias merge — Blocker #3), `intersectionsOfLargeUnions` (lib `HTMLElementTagNameMap[T][P]` keyof modeling), `keyofIsLiteralContexualType` (literal-array-to-`(keyof T)[]` + `Pick<>` generic inference, 2-piece) — all multi-piece, deferred. **B442 (+2, 373→371 failing, 9713→9715 passing) — duplicateLocalVariable1/2 (TS2403+TS2365+TS2356).** The ONLY genuinely-tractable candidate. A `var X` declared by `for (var X in chars)` (→ `string`, first-decl-wins) then redeclared numeric `for (var X = 0; X < 14; X++)` in the SAME function scope: TS2403 (subsequent decl must be 'string', has 'number') + a related TS6203 at the for-in `var X` + TS2365 (`<` cannot apply to 'string' and 'number', squiggle = whole `X < N`) + TS2356 (string `X++` arithmetic operand). New dedicated walker `checkForInNumericForRedeclare` hooked in `checkFunctionBody` (per-body; `forInNumForProcessed` dedup-set guards the >1×/body invocation); collector recurses control-flow but NOT nested function/class bodies (`var` is function-scoped). The existing TS2403/TS2365/TS2356 paths neither type a for-in `var` as `string` nor apply first-decl-wins across for-loop initializers (collectVarDecls skips initializer var decls) — and TS2356 fires only for type-param operands — so a dedicated walker (not a general var-type-tracking change, which would touch the 32 corpus files with `for (var X in)`) was correct. **FP firewall corpus-EXHAUSTIVE**: the ONLY 2 corpus files with a for-in `var X` AND a later numeric c-for `var X` (same name) are the two targets. ZERO regressions (exact failing-set diff: only the 2 targets flipped). **B443 (+1, 371→370) — checkerInitializationCrash (TS2300 ×2 + TS6203, cross-file Blocker #3 beachhead).** Two different files (`/node_modules/@fullcalendar/{react,core}/index.d.ts`) each declare a `VNode` member inside the SAME `declare global { namespace FullCalendarVDom { … } }` path — `export import VNode = react.ReactNode` (ImportEqualsDeclaration) vs `type VNode = preact.VNode<any>` (TypeAliasDeclaration) — a type-alias never merges, so "Duplicate identifier 'VNode'." at each + a cross-referencing TS6203. New AST-based cross-file walker `checkGlobalNamespaceMemberConflicts` (the cross-file `declare global` namespace merge is NOT modeled by the binder's symbol tables). FP firewall: ≥2 DISTINCT files AND a TypeAliasDeclaration in the mix. Feasibility confirmed first: node_modules `.d.ts` ARE parsed+bound (in binderResults) and their diagnostics flow through (the passing `augmentExportEquals7` reports a node_modules TS2649). **Deep blocker-feasibility analysis (all remaining candidates DEFINITIVELY multi-piece — documented so the next session doesn't re-tread):** the other 5 agent-CONTAINED + ~10 manually-examined are all multi-piece broad-infrastructure: `circularBaseTypes` (4 errors; TS2310/2456/2313 are name-based interface-extends walkers that don't follow `M2 extends M<M3>; M3 = M2[keyof M2]` — needs type-LEVEL cycle detection through type-alias+indexed-access+mapped-K); `noSymbolForMergeCrash` (obscure TS2649 cross-file interface+namespace+type-alias merge — uncertain semantics, FP-risky); `intersectionsOfLargeUnions` (`HTMLElementTagNameMap[T][P]` lib-keyof modeling); `keyofIsLiteralContexualType` (literal-array→`(keyof T)[]` + `Pick<>` generic inference, 2-piece); `iterableWithNeverAsUnionMember` (needs intersection-of-disjoint-literal-props→`never` reduction — BROAD type-engine change — + destructure-iterability TS2488 for 3 shapes); `invalidSplice` (needs the `splice(start,deleteCount,...items:T[])` overload ADDED to the embedded lib + empty-array-receiver→`never[]` + rest-arg-vs-`never`); `emptyObjectNotSubtype…1/2` + `consistentAliasVsNonAliasRecordBehavior` + `specialIntersectionsInMappedTypes` (all need `Record<K,V>` materialization — B100 MED-HIGH, dedicated session); `staticMemberExportAccess` (3-piece property-access-RECEIVER TS2351/2576/2339 — B153-risky); `quickinfoTypeAtReturnPositionsInaccurate` (deep `Entries[EntryId]` type-param indexed-access via constraint). NEXT: the contained pool is genuinely exhausted (this session landed +3 — NOT a recon-only session); the queue's documented multi-session Blocker BUILDs are the only path forward (Record<K,V>/mapped-type materialization has the most consumers; intersection-disjoint→never; generic-inference #2 fixingTypeParametersRepeatedly2; flow #1 controlFlowArrayErrors). Running failing-set baseline in `/tmp/fail_baseline.txt`.

**Session 2026-06-15 (round 171, multi-iteration session, ≥20 iterations per user request, addressing blockers) — IN PROGRESS.** Method: bounded pool re-confirmed exhausted of trivial wins (find_candidates EXTRA/MISSING/SWAP fresh=0; NONE fresh=9 all type-engine; OUTPUT=1 tsgo-irrelevant). Ran a corpus-FP-rarity triage **Workflow** (read-only `Explore` agents per the round-170 stray-marker gotcha) over the 15 un-triaged batch-2/3 candidates, AND independently verified each promising verdict (the round-170 lesson — agents over-classify AND sometimes invert direction). Running failing-set baseline kept in `/tmp/fail_baseline.txt` (full testcase names) for exact swap detection on every step.
- **B441 (+1, 374→373) — ensureNoCrashExportAssignmentDefineProperrtyPotentialMerge (TS2323 ×2).** checkJs CJS: a local `B` bound via `module.exports = B`, with export prop `NS` declared by BOTH `B.NS = …` AND `Object.defineProperty(B, "NS", …)` → "Cannot redeclare exported variable 'NS'." at each site (squiggle = `B.NS` LHS for assign; whole `Object.defineProperty(…)` call for the defineProperty). New dedicated walker `checkJsCjsExportObjectRedeclare` + `matchClosingParen`. FP firewall corpus-EXHAUSTIVE: fires only when a prop has BOTH mechanisms (plain double-assign / lone defineProperty is not a redeclare); the only such checkJs file is the target. Zero regressions.
  Triage of the rest (NOT contained — deferred): readonlyPropertySubtypeRelationDirected (ternary→union local inference + union-readonly, 2 type-engine pieces), declarationEmitReexportedSymlinkReference3 (TS2883 needs @link symlink resolution + exact computed-path string), reuseTypeAnnotationImportTypeInGlobalThisTypeArgument (TS2304 in JSDoc `Record<Keyword,ParamValueTyped>` — B423 extension but 10 checkJs files share the generic-JSDoc-arg shape → real FP surface + cross-file resolution), noSymbolForMergeCrash/checkerInitializationCrash/tsxResolveExternalModuleExportsTypes (cross-file global-merge, Blocker #3), arrayConcatMap/weakType/destructuringTuple/restInvalidArgumentType/varianceReferences/strictFunctionTypesErrors/subclassThisTypeAssignable01/inferentialTyping (type-engine: never[]/concat overloads, weak-type relation, reduce inference, generic-constraint rest, function-type variance, this-types), excessivelyLargeTupleSpread/unionSubtypeReductionErrors (recursive tuple-size/union-complexity limits), duplicateLocalVariable1 (for-in→string + cross-decl), expressionTypeNodeShouldError/interfaceNaming1/constructorWithIncompleteTypeAnnotation (parser-recovery cascades), multiLinePropertyAccessAndArrowFunctionIndent1 (unreproducible TS-1 pre/post-emit-mismatch harness artifact), small checkJs (jsFileClassPropertyType2/allowJscheckJsTypeParameterNoCrash/jsElementAccessNoContextualTypeCrash/noParameterReassignmentJSIIFE = JSDoc-@type-on-assignment + contextual-this + generic inference).
- **B440 (+1, 375→374) — circularlyReferentialInterfaceAccessNoCrash (TS4109 + TS4110 ×2).** Extended the existing B188 TS4109 walker (`checkCircularTypeArgumentSelfRef`) to circular type-args that INDEX BACK into the alias: `Mx<'list', Mxs['p1']>` → TS4109; tuple forms `['list', ArrElem[number][0]][]` / `[['list', TupleElem[0][0]]]` → TS4110 at the innermost offending tuple. AST helpers `selfRefViaIndexedAccess` (unwrap nested IndexedAccessType to base TypeReference, match alias), `selfRefNotCrossingTuple` (attributes to innermost tuple), `collectSelfRefTuples`, `matchClosingBracket` (string-aware `[`…`]`). TS4109 in 2 baselines (sibling ConditionalType shape preserved), TS4110 in 1 → tiny FP surface; full-suite exactly one flip, zero regressions. The triage agents over-classified EVERYTHING as CONTAINED (incl. architectural arrayConcatMap/readonlyPropertySubtypeRelationDirected) — verdicts unreliable both directions; relied on own analysis + corpus rarity of the diagnostic code.
- **B439 (+1, 376→375) — jsDeclarationsInheritedTypes (TS2564 ×3).** `@emitDeclarationOnly:true` → `declarationOnly` checker path, where `checkPropertyInitialization()` (TS2564) never ran (it was only in the `!declarationOnly` block). tsc DOES emit TS2564 in emitDeclarationOnly. The 3 `value;` class properties have JSDoc `@type {A}` → parser-bridged `PropertyDeclaration.type` (non-any) → TS2564. Fix: call `checkPropertyInitialization()` (same strict gate) inside the `declarationOnly` branch; the walker's existing guards make it FP-safe. Zero regressions. NB: the triage agent proposed the OPPOSITE (suppress TS2564) — wrong direction, caught by reading the baseline.

_[Rounds 157–170 session notes trimmed to PLAN-PHASE-4-HISTORY.md (round-180 trim).]_

### Failure impact analysis (upper bounds — assumes feature fully unblocks tests)

| Feature | Diagnostic codes unblocked | "None produced" test impact |
|---|---|---|
| Contextual typing | TS2322 (1267), TS2345 (576), TS2353 (110) | **~600+ tests** |
| Deep structural comparison | TS2322 elaboration chains, TS2741 | **~300+ tests** |
| Control flow narrowing | TS2339 (549), TS2454 (103 deficit), TS2774 (118) | **~250+ tests** |
| Overload resolution | TS2769 (511), TS2349 (161) | **~200+ tests** |

These are UPPER bounds — a test usually needs multiple features. Realistic gain per feature: 30-50% of the upper bound.

### QUEUE — prioritized by unblocking potential, then by implementation cost

Completed queue items (B7.x through B38.x and 17.x series) have been archived
to `PLAN-PHASE-4-HISTORY.md` § "Queue — completed items (B/17.x series)". The
session log above (`Session 2026-05-17 ...`) and `STATUS.md` together carry the
recent narrative; consult the history file when you need the original
checkbox-item rationale.

- [ ] **16.4. Generic type instantiation and inference (MEDIUM — ~80 tests realistic) — IN PROGRESS**
  _Per-item session notes through round 119 are archived in `PLAN-PHASE-4-HISTORY.md` (§ "Completed items (16.0–16.3) — full notes"); current session notes live at the top of this file's Phase 16 section._

- [ ] **B464-FLOW-INTO-CLOSURES (PROMOTED 2026-06-16 round 183 — Blocker #1 substep; reverse-engineered + the key gotcha found, so the next session skips the dead-end).** Target `implicitConstParameters` (+1; also `discriminateWithOptionalProperty4` and likely a small cluster). tsc flows a narrowing INTO a closure for a CONST-LIKE captured variable: `function fn(x: number|string){ if (typeof x==='number') doSomething(() => x.toFixed()); }` is OK (x narrowed to number flows into the arrow, no error), but `function f5(x: string|undefined){ if(x) doSomething(() => x.length); x="abc"; }` keeps `string|undefined` inside the arrow (x is reassigned → non-const) → TS18048. We currently flow NO narrowing into closures (`bindFunctionLikeBody` does `currentFlow = newStart(container)`, a fresh root), so `() => x.toFixed()` resolves `x` to the DECLARED `string|number` → FP TS2339, and the f5 TS18048 doesn't fire either. **Decomposition** (Flow.kt + Checker): (1) add `var outerFlow: FlowNode? = null` to `FlowStart`; in `bindFunctionLikeBody` set it to the enclosing `savedFlow` (restrict to `ArrowFunction`/`FunctionExpression` to bound blast radius); (2) in `narrowTypeFromFlow`, the `FlowStart` case: if `outerFlow != null` AND the variable is captured (we reached FlowStart without passing a FlowAssignment that DECLARES `name` as a param/var in this closure — thread a `passedLocalBinding` flag) AND `name` is const-like → continue `narrowTypeFromFlow(declaredType, outerFlow, name, …)`; else `declaredType` (current behavior). **CRITICAL GOTCHA (discovered round 183, the reason this is NOT a quick win): const-detection MUST be SYMBOL/declaration-based, NOT name-based.** A name-based "is `x` assigned anywhere in the file" over-approximation FAILS on the target: `fn`'s param `x` (never reassigned) and `f5`'s param `x` (reassigned `x="abc"`) are DIFFERENT symbols sharing the name `x`, so a name-based set marks `x` non-const everywhere → `fn`'s narrowing never flows in → target not fixed. Must resolve the captured `x` to its declaration (param/var) and check whether THAT specific declaration is ever a write target in its scope. RISK: MED-HIGH (broad — every closure capturing a narrowed var; additive narrowing reduces FPs but can create FNs where a test expected an in-closure error). Full-suite + exact failing-set diff (`scripts/fail_set.py --diff /tmp/fail_baseline.txt`) REQUIRED, revert budget. **ROUND-183 ATTEMPT (reverted clean, no commit) — two more findings that make this a dedicated-session task, not a loop-tail attempt:** (1) the obvious shortcut "narrow from `outerFlow` captured at the closure's CREATION point" gives the WRONG answer for the reassigned-`x` case — f5's `x="abc"` is LATER in source order so it isn't in the creation-point flow, so creation-point narrowing would wrongly narrow x to `string` and suppress the expected TS18048; const-detection (reassigned-anywhere) is therefore MANDATORY, not optional. (2) **There is NO generic AST walker (`children()`/`forEachChild`) in the codebase** — Flow.kt's `FlowGraphBuilder` hand-recurses every node kind via bespoke `when` blocks. So `scanReassignedNames(body)` (the per-enclosing-function reassigned-name set, pushed on a `reassignedStack` so a nested closure's `FlowStart` records its enclosing function's set as `enclosingReassignedNames`) must either be hand-written covering every statement/expression kind (error-prone — a missed kind = wrong const-detection = wrong narrowing) OR a generic `forEachChild` must be built FIRST (probably the right unblocker — promote it as its own substep, it would help many future walkers too). Also need `localNames` (closure's own param+body-decl names) on `FlowStart` to avoid the shadowing bug (a closure param `x` shadowing an outer const `x` must NOT inherit the outer's narrowing). The Flow.kt skeleton (FlowStart.outerFlow/enclosingReassignedNames/localNames + reassignedStack + collectClosureLocalNames) was written and reverted — re-derivable from this note. **ROUND-183 SECOND ATTEMPT (fully implemented + full-suite-verified + reverted): the closure-narrowing piece WORKS and is PROVEN NET-ZERO-SAFE (0 regressions, 0 fixes on the exact failing-set diff).** I used a SOURCE-TEXT reassignment scan in the binder (`scanReassignedNamesInRange(start,end)` mirroring the checker's `countAssignmentLikeOccurrences` — a pure text scan, FP-safe because false matches in strings/comments over-approximate toward "reassigned"/non-const = the safe direction; no generic AST walker needed) pushed on a `reassignedStack` per function-like body; the closure's `FlowStart` records `outerFlow=savedFlow`, `enclosingReassignedNames=reassignedStack.last()`, `localNames=collectClosureLocalNames(params,body)` (gated to ArrowFunction/FunctionExpression containers). The checker's `narrowTypeFromFlow` (+ the `…FollowLoopEntry` mirror) `FlowStart` case continues into `outerFlow` iff `outerFlow!=null && name !in localNames && name !in enclosingReassignedNames`. **VERIFIED: this removes the implicitConstParameters FP TS2339 @ 7:27 correctly (closure narrows `x` to `number`), with ZERO regressions across the suite.** **The ONLY remaining blocker to FLIP the test is the COMPANION piece: a general "`x.length` where `x` is `T|undefined`" → TS18048 emitter, which we LACK** (the only TS18048/TS2532 sites are the narrow B221 tuple-rest / B279 optional-number-arithmetic / `checkNoUncheckedIndexedCompoundAssign` index-sig walkers — there is NO general property-access-on-maybe-undefined-identifier emitter). So a dedicated session re-applies the proven-safe closure-narrowing change AND adds the general TS18048 emitter together → flips `implicitConstParameters` (the `f5` case needs `x` to stay `string|undefined` inside the closure, which the reassignment gate already handles correctly). RISK of the closure-narrowing piece is now KNOWN-LOW (proven net-zero); the new TS18048 emitter is the MED-risk part (broad strictNullChecks surface). Backpointer: round-183 session note + the `implicitConstParameters` diff (FP TS2339 @ 7:27 + missing TS18048 @ 44:27).

- [x] **B435-OBJECT-FREEZE (DONE 2026-06-15 round 168, +2 — objectFreeze + objectFreezeLiteralsDontWiden, ZERO regressions; was PROMOTED round 167).** Modeled `Object.freeze(x)`'s return type. Pieces landed: (1) `tryResolveObjectFreezeCall` in `getReturnTypeOfCallExpression` — `Object.freeze(objLit)` → Type.Object with members in `mappedReadonlyMemberIds` + literal member types preserved as FRESH `nonWideningLiteralTypeIds` literals (boolean literals skipped — singletons); `Object.freeze([prim,…])` → `ReadonlyArray<elem>` (primitive-element arrays only); fn/class/other → null (anyType). (2) **the hidden coupling the round-167 MED-HIGH note warned about**: the freeze object must SURVIVE `widenType` via a new `frozenObjectTypeIds` set — the `getTypeOfSymbol`→`inferTypeFromInitializer`→`widenType` path rebuilt the object with fresh UNMARKED symbols + widened-away literals, silently dropping the `o.b=` TS2540 (the LITERALS test passed via `currentLocalTypes`, which skips widenType, masking the gap). (3) `checkPropertyAccessAssignment` skips the assignability TS2322 when the target prop ∈ `mappedReadonlyMemberIds` (tsc types a readonly write as `any` → only TS2540). (4) `tryEmitReadonlyArrayElementWrite` for `a[i]=v` on a ReadonlyArray receiver → TS2542 + element-type TS2322. See round-168 note above / STATUS.md / the new CLAUDE.md gotcha.

- [x] **B437-JSDOC-TYPE-BRIDGE (DONE 2026-06-15 round 169, +4 — all 4 tractable consumers landed: B437a jsdocRestParameter, B437b jsdocCallbackAndType, B437c arrowExpressionBodyJSDoc, B437d jsdocFunctionTypeFalsePositive; ZERO regressions at every step).** See round-169 session note above for the full per-substep detail + reusable infra (`JsDocParamTypeMap` rest bridge, `checkJsRestParamArgTypes`, `checkJsCallbackTypeAssignments`/`collectSimpleJsCallbacks`, `checkJsDocArrowReturnConstraint` + the JSDoc-`@param`-documented TS7006 suppression + `parseJsDocTemplateNames`/`parseJsDocReturnsTypeName`/`jsDocDocumentedParamNames`, `checkJsDocEmptyTypeParamList`). REMAINING JSDoc consumers (deferred, multi-piece, NOT done): **jsdocBracelessTypeTag1** (braceless `@type () => string` applied to a function → return-type checking, + `@type (union)&{…}` object-literal assignability with discriminant — 2 coupled pieces); **unmetTypeConstraintInJSDocImportCall** (cross-file `@typedef {import('./file1').Foo<T>} Bar` where `Foo<T extends string>` + unconstrained `T` → TS2344 — needs cross-file JSDoc typedef + import-type + constraint resolution). Original promotion text below for reference.

  - [ ] **B437-JSDOC-TYPE-BRIDGE (original promotion text — PROMOTED 2026-06-15 round 168 — Blocker #5, highest-leverage among the remaining SMALL JS failures, multi-session).** The single most-consumed missing piece across the small checkJs failures is a JSDoc-type-string → TypeNode bridge: we currently raw-scan JSDoc via regex (B422/B423) and never parse `@param {...}` / `@template` / `@callback` type content into AST. Consumers (all NONE/FP-on-implicit-any today): **`arrowExpressionBodyJSDoc`** (`@template T` + `@param {T} value` → param typed `T`, want TS2322 `string` vs `T`; we FP TS7006); **`jsdocRestParameter`** (`@param {...number} a` → rest param `number[]`, want TS2345 ×2; we FP TS7006+TS2554); **`jsdocFunctionTypeFalsePositive`** (`@param {<}` → parser errors TS1098/TS1139 on the JSDoc type content); **`jsdocCallbackAndType`** (`@callback` synthesis). Decompose: start with the smallest FP-removal slice — SUPPRESS the FP TS7006 "implicitly any" on a param that carries a JSDoc `@param {T}` whose `T` references a `@template` type-param OR is a resolvable type (a regex pre-scan in the implicit-any walker, mirroring B423's `@typedef`-name registration). That slice ALONE won't flip a test (each also needs the downstream TS2345/TS2322 the typed param enables), so it's an unblocker substep, not a standalone win — pair it with resolving the JSDoc param type to a real Type so the assignability check fires. RISK: MED (JSDoc appears in many JS tests — full-suite verify each slice). Backpointer: round-168 skip-log entries for the 4 consumers.

- [ ] **B436-CROSS-FILE-ARITY-EXTEND (NOTED 2026-06-15 round 167 — Blocker #3 beachhead from B434).** B434 landed cross-file function-call arity (TS2554/TS6210) via a program-wide `crossFileFuncs` overlay in `checkArgumentCounts` (gated: NON-module files only; callee absent from own funcParams AND binder locals; cross-file dup names skipped; JS-param optionality JSDoc-`@param`-aware via `jsDocRequiredParamNames`). The overlay currently covers TOP-LEVEL FunctionDeclarations + the TS2554-too-few path only. Extend opportunistically when a failing test needs it: TS2555 (rest-param too-few) cross-file related-info (mirror the `relSource`/`relFileName` threading into `emitTS2555TooFew`); cross-file CLASS constructor arity (`new X()` where X is in another script file); cross-file var-assigned-arrow callees. NO known failing-test consumer right now (B434's full-suite diff flipped ONLY its target) — do NOT build speculatively.

- [ ] **B450-ID-OUTPUT-DECOUPLING — ⚠ DEMOTED to LOW-priority correctness task 2026-06-16 round 175 (was PROMOTED round 174 on a MIS-MEASUREMENT).** Round-174 promoted this as potentially-high-yield on the claim "338/364 failures are order-dependent". **Round-175 RE-MEASURED CORRECTLY (round-174's measurement used a regex XML parser that mis-attributes self-closing passed-testcase tags to the next failure): the 364 failures are GENUINE and DETERMINISTIC — running the 329 failing-base patterns as an isolated batch reproduces all 364. Order-dependence is real but bounded to ~25–50 boundary tests (the −25 magnitude), in BOTH directions, so any global re-ordering is a WASH-or-NEGATIVE unless the structural order EXACTLY matches tsc everywhere. This is a determinism-hygiene task, NOT a yield play — do it for correctness, expect ~0 net test flips.** Original analysis (still accurate as root-cause): `Type.nextTypeId` and `Symbol.nextId` are global monotonic counters whose `resetIdCounter()` is NEVER called, so absolute ids drift across the JVM test run and leak into id-keyed HashMap/HashSet iteration order and id-tie-break sorts → ORDER-DEPENDENT diagnostic output (e.g. `numericClassMembers1`/`noSymbolForMergeCrash` pass in isolation but flip in the full suite; the CLAUDE.md "Test ordering sensitivity" gotcha is this). **DO NOT just reset the ids per-compilation — ATTEMPTED round 174 (lazy `resetForCompilation()` at `compileParsed` top, reset to post-class-load base above the 18 intrinsic singletons), REVERTED at net −25: freezing ids makes full-suite==isolation, which breaks MORE tests (those that only pass under full-suite ordering) than it fixes.** The CORRECT direction is to REMOVE id-dependence from the diagnostic-producing paths: audit every `HashMap<Int,…>`/`HashSet<Int>` (id-keyed) whose ITERATION ORDER reaches output, and every `.sortedBy { it.id }` / id tie-break, and replace the id key/tie-break with a STABLE structural key (source position, then name). Prime suspects to grep: `getUnionType` member sort (sorts by `it.id`), `aliasDisplayMap`/`unionAliasStructural` lookups, any `mutableSetOf<Int>()` side-channel iterated for display, "and N more" property-set ordering, `relationComparisonStack`/`isDeeplyNested` (id-occurrence — likely order-stable already). RISK: MED-HIGH but the diagnostic is a full-suite failing-set diff after EACH narrowed change (start with `getUnionType` sort → LinkedHashSet/structural sort). YIELD: ~0 net test flips expected (the −25 experiment proves ~25 tests are order-sensitive, but in BOTH directions — stabilizing them to a structural order that doesn't perfectly match tsc trades passes for failures). NOTE: `getUnionType` already sorts by `flags.value` (stable, NOT id) and the visible `.sortedBy { it.id }` sites (missingMembers, union members at Checker.kt ~93857/93873) preserve RELATIVE id order = creation order = source order (offset-invariant under reset), so the −25 likely comes from `HashSet<Int>`/`HashMap<Int>` ITERATION order (bucket = absolute-id % capacity) reaching output, NOT from the sorts — audit which side-channel id-sets are iterated for display. Backpointer: round-175 correction + round-174 session note + STATUS.md.

---

### STRATEGIC MAP of the remaining failures (2026-05-30, pipeline-optimization session)

**The "surgical pool exhausted → only architectural blockers left" framing that
drove ~20 rounds of recon-only / +1 sessions was a TOOL ARTIFACT, not reality.**
`find_candidates.py` only ever scanned tests that already emit a parseable
`error TSxxxx` line (the EXTRA/MISSING/SWAP buckets) — that slice is **33 of the
1014 current failures**. The tool was blind to the other ~70%. It has now been
extended (commit `tooling(queue): un-blind …`) with two new buckets. Full map of
the 1014 failures (from the 2026-05-30 fresh XML set):

| bucket | count | what it means | tool flag |
|---|---|---|---|
| **NONE-PRODUCED** | **611** (590 fresh) | baseline expects errors, we emit nothing | `--none` |
| error-baseline diff (EXTRA/MISSING/SWAP) | 307 (33 in ≤3-code window) | we emit something, wrong/incomplete | default |
| **OUTPUT** (JS/decl/sourcemap) | 105 (≈95% already skip-logged) | pure output diff, no codes | `--output` |

**NONE-PRODUCED is the path to 100%.** It is dominated by the hard type-engine
families (TS2322 ×91, TS2345 ×38, TS2339 ×37 — these need the assignability /
generic-inference / cross-file-scope blockers), BUT it has a large **BOUNDED
tail that is genuinely surgical and was never triaged**:

- **TS2307 ×14 (module resolution)** — e.g. `es6ExportAll` does `import … from 'server'`
  where `server.ts` exists but the specifier is BARE (non-relative, not a
  node_modules package); TS fires TS2307, we wrongly resolve it. Shared root
  cause across most of the 14 → likely one fix flips several. **Best first target.**
- **TS2403 ×9 (subsequent var decls must have same type)** — `promiseIdentity*`
  cluster (5) shares a Promise-interface-merge root; more type-engine-coupled.
- **TS2367 ×9 (comparison appears unintentional)**, **TS2688 ×5 (cannot find type-def file)**,
  **TS2540 ×5 (readonly assignment)**, **TS2554 ×4 (arg count)**, **TS2344 ×4**,
  **TS4023/TS4025 (decl emit)**, **TS2305/TS2649/TS2551/TS2552 …** — each a
  bounded check, 2–3 expected error lines, mostly fresh.

**Process rule going forward:** run `find_candidates.py --none --fresh` (and
`--code TSxxxx` to focus) BEFORE ever declaring the pool dry. A session is only
"pool exhausted" when EXTRA/MISSING/SWAP **and** the bounded NONE tail **and**
OUTPUT are all dry — which is not the case today.

**STALENESS / ARCHIVE NOTE (2026-06-10):** the counts above are the 2026-05-30 snapshot (1,014 failing); current state is 567 failing / 9,519 passing — regenerate the bucket map with `find_candidates.py --fresh`, `--none --fresh`, and `--output --fresh` before relying on any per-code count. The per-test enumerations and all completed `- [x]` strategic items were moved to `PLAN-PHASE-4-HISTORY.md` (§ "STRATEGIC MAP — archived entries"). Only still-open strategic items are kept below.

- [ ] **B157-GENERIC-CALLEE-RETURN-MISMATCH (PROMOTED 2026-06-09 round 129 — Blocker #2; round-129 triage CAVEAT: NOT a clean slice, see FINDING at end of this item; the bounded surgical pool is otherwise EXHAUSTIVELY architectural this round).** Target `fixingTypeParametersRepeatedly2` (+1, 2 errors, NO best-common-type needed — T is inferred from a single non-array arg, so it avoids the `genericTypeArgumentInference1` union-BCT complication). Shape: `declare function foo<T>(x: T, func: (p: T) => T): T; var result = foo(derived, d => d.toBase());` where `derived: Derived` (Derived extends Base, has `toBase(): Base`). Need: (1) infer `T = Derived` from arg1 `x: T` (the `tryInferSingleTypeParamFromArgs` infra at Checker.kt:~43925 already does single-arg inference); (2) contextually type the arrow `d => d.toBase()` with `d: Derived` (the B81.1b contextual-param-inference path in `checkPropertyAccessInExpr` FunctionExpression branch ALREADY does this — verify it fires for the 2nd arg here); (3) infer the arrow's RETURN type = `Base` (from `d.toBase()`), substitute T=Derived into the expected return `(p:T)=>T` -> expected return `Derived`, check `Base` assignable to `Derived` -> **TS2741 "Property 'toBase' is missing in type 'Base' but required in type 'Derived'."** at the arrow's return position. PLUS the redeclared `var result` (Base vs Derived) -> **TS2403** (the existing `ts2403Identical` + `checkVarDeclTypeConsistency` path — `result`'s first decl is `foo(...)`=`Derived`, 2nd is `bar(...)`=`Base`... actually both call exprs; verify what `result`'s inferred type is). RISK: MEDIUM (generic-inference is net-zero-prone; full-suite + exact failing-set diff REQUIRED, revert budget). START by tracing whether pieces (1)+(2) already produce `d: Derived` (add a marker diag); the missing piece is likely (3) the arrow-return-vs-substituted-T assignability emission. If `fixingTypeParametersRepeatedly2` proves entangled, the fallback Blocker target is the decl-emit nameability CLUSTER (TS2883/TS4023/TS4025/TS2527/TS4118/TS7056, ~8-10 tests sharing a "cross-file inferred-type nameability" root) — higher yield but needs a portability/nameability model (architectural, multi-session). **ROUND-129 FINDING (read before attempting):** this TS2741 is a return-type mismatch through a GENERIC-INSTANTIATED callee `foo<T>`, which the round-79l gotcha (CLAUDE.md "Contextually-typed-arrow return-mismatch") EXPLICITLY documents we keep as COARSE whole-arg TS2345 rather than the arrow-return elaboration — piece (4) requires lifting the `!calleeGenericInstantiation` gate AND producing the missing-property (TS2741) variant. PLUS the 2nd error (TS2403 `result` Base-vs-Derived) depends on the subtle "fixing type parameters repeatedly" overload re-inference (bar() yields a DIFFERENT T than foo()). So it is a 2-hard-piece test. Prefer instead either (a) a simpler Blocker #2 test where T is inferred from one arg and a SECOND non-callback arg fails the instantiated param (no contextual-arrow-return), or (b) the decl-emit nameability cluster. Trace with marker diagnostics before committing.

- [ ] **B131-NEXT-CANDIDATES — ROUND-122 /loop FIRING-2 UPDATE (2026-06-08): both targets RE-CONFIRMED 2-piece/hard via code-path tracing; do NOT re-triage as contained.** `recursiveBaseConstructorCreation3`: TS2314 doesn't fire because the ClassDeclaration heritage walk in `checkUnresolvedNames` doesn't reach top-level `declare class` heritage (un-gating is FP-risky AND has NO other consumer — zero currently-failing tests need declare-class-heritage TS2314), AND the TS2351 construct-sig cascade through the invalid recursive generic base is a SEPARATE hard piece → not a clean win. `duplicateErrorClassExpression`: TS2538-typeof-class is a cheap B127 extension but the TS2416 index-signature-VALUE-type override comparison (named→named, canUseTypeEngine-blocked) is the blocker → 2-piece. **ALSO traced this firing (all multi-link Blocker #2, NOT contained):** `unknownSymbolOffContextualType1` (`elements.filter(...).map(e=>e.xyxyxyx)`) — `checkPropertyAccessInExpr`'s FunctionExpression branch DOES do contextual-param inference (B81.1b, ~73849), so the break is UPSTREAM: `elements = elementNames.map(name=>getElementById(name))` must resolve to `HTMLElement[]` (B83.4i map-return inference flowing into the var-decl type) AND `filter` must preserve `T` (`HTMLElement[]`) — a multi-link map→var→filter→map chain. `knockout` (callable-interface return), `inexistentPropertyInsideToStringType` (JS-`this`-expando). OUTPUT bucket (41 JS/decl diffs) = finicky emitter / parser-recovery / module:preserve / System-module (tsgo-irrelevant or architectural). **CONCLUSION: the bounded clean pool is EXHAUSTED after +4 (B127-B130). The next firing/session MUST commit to a Blocker BUILD (not re-triage): highest-yield is Blocker #2's map-return-inference-into-var-decl-types (make `arr.map(cb)` resolve to `R[]` and flow `R[]` into the inferred var type, so chained `.filter`/`.map` callback params type correctly) — BROAD/net-zero-prone, REQUIRES full-suite + HEAD-failing-set-diff with a revert budget. This is a dedicated-session task, not an autonomous-loop-tail task.** ORIGINAL PROMOTION (2026-06-08 round 122 /loop firing-1): concrete targets so the next firing does NOT recon-loop; the bounded clean-MISSING-HANDLER pool is otherwise exhausted after +4 B127-B130. Two contained-but-2-piece candidates worth a fresh-budget attempt (each needs BOTH pieces to flip; revert budget): **(1) `recursiveBaseConstructorCreation3`** (TS2314 + TS2351) — `declare class xyz extends abc` where `abc` is `declare class abc<T>` (generic, used with NO type args) → TS2314 "Generic type 'abc<T>' requires 1 type argument(s)." at the `abc` in xyz's heritage (baseline 6,27); PLUS `new xyz()` → TS2351 "This expression is not constructable. Type 'typeof xyz' has no construct signatures." (9,15). `checkHeritageTypeArgCount` (Checker.kt:28890) WOULD emit TS2314 (gate: `scope.has(name)` + `getTypeParamInfo(name).maxTotal>0` + providedCount≠maxTotal) and IS called from the ClassDeclaration heritage walk (14813) — so the FIRST investigation is WHY it doesn't fire for this `declare class xyz extends abc`: likely either (a) the recursive cycle (`abc.foo: xyz`, `xyz extends abc`) makes `getTypeParamInfo("abc")` return null/0, or (b) the ClassDeclaration branch in `checkUnresolvedNames` skips ambient `declare class` heritage. Add a marker-diagnostic (code=29999) to confirm which. The TS2351 half (construct-sig resolution through an invalid recursive generic base) is the harder piece — verify it cascades once the base is recognized as broken, else it needs separate construct-sig-invalidation. **(2) `duplicateErrorClassExpression`** (TS2416 + TS2538) — needs TS2538-for-`typeof Class`-index (extend B127's TS2538-object-index from `Type.Interface` to constructor-typed values) + TS2416 index-signature-VALUE-type override mismatch (`ComplicatedTypeDerived{[s]:ADerived}` not assignable to `ComplicatedTypeBase{[s]:ABase}` — ADerived missing `a`); the TS2416-index-sig-value-comparison is the hard piece (named→named blocked by canUseTypeEngine; may need the missing-property subset for index values). If BOTH 2-piece candidates prove too entangled, pivot to a Blocker: B100 utility-type (Readonly past the no-Reference gate), or Blocker #2 callback-RETURN inference for `arr.map(cb)`→`U[]` (broad, net-zero-prone — full-suite + HEAD-diff REQUIRED). Do NOT recon-only: pick ONE and build with a revert budget.

- [ ] **B100-UTILITYTYPE-MATERIALIZE (PROMOTED 2026-06-06 round 107 — contained type-engine blocker; backpointer: round-107 skip-log).** **ROUND-119 PROGRESS (2026-06-08, B120): `Readonly<T>` MATERIALIZATION LANDED (+1, flips `readonlyAssignmentInSubclassOfClassExpression`).** New `materializeModifierUtility` (Checker.kt ~59967) copies T's member Symbols + marks them readonly via the `mappedReadonlyMemberIds` side-channel (consulted by `isReadonlySymbol`), wired in `getTypeFromTypeReference` (`symbol==null && name=="Readonly"`, arity 1); same gates as B116 (concrete plain `Type.Object` T, no Reference/index-sigs). Companion pieces: `getTypeFromBaseTypeExpression` resolves `extends (X as new()=>T)` cast-mixin → construct-sig return type; `isReadonlyPropertyAccess` types `this.X` via `currentClassForThis` (now set by the readonly walker), gated to INHERITED members (own-readonly-in-ctor is legal). See CLAUDE.md B120 gotcha. **ROUND-120 FINDING (2026-06-08, B125 — ATTEMPTED + REVERTED, net −2):** implemented exactly the planned `Partial`/`Required` materialization — `materializeModifierUtility` extended to all three names, `mappedOptionalMemberIds`/`mappedRequiredMemberIds` side-channels consulted by `isOptionalProperty` (Required overrides → false, Partial → true), wired into `getTypeFromTypeReference`. Full suite: **ZERO flips, −2 regressions** (`deleteExpressionMustBeOptional_exactOptionalPropertyTypes` BOTH variants). Root cause of the regression: `isOptionalProperty` is consumed by the TS2790 "delete operand must be optional" check (and others) — making it side-channel-aware changed delete-expression behavior even though NO `Partial<>` was involved in those tests (the side-channel sets were empty for them, so this was likely an interaction with how the materialized objects flow, OR a subtler `isOptionalProperty` consumer). NET: **`Partial`/`Required` materialization has NO clean consumer in the current failing set AND the `isOptionalProperty` side-channel has FP surface — do NOT re-attempt without first finding a concrete single-test consumer AND auditing every `isOptionalProperty` caller.** The remaining B100 targets (`multipleBaseInterfaesWithIncompatibleProperties2` TS2320) need Partial + exactOptionalPropertyTypes value-type semantics + multi-base identity comparison (3+ pieces) — defer. **STILL TODO (lower priority): broadening `Readonly` past the no-Reference gate (named-interface / generic-instantiation args).** Original plan below. Materialize the homomorphic utility types **`Readonly<T>` / `Partial<T>` / `Required<T>` / `Pick<T,K>` / `Omit<T,K>`** as real mapped-type expansions that PRESERVE/modify member modifiers (readonly, optional). Today `getTypeFromMappedType` (Checker.kt:~83340) mints fresh member Symbols with NO declarations, so `isReadonlySymbol` (~36282, checks `symbol.declarations` for a Readonly-modified PropertyDeclaration) returns false → the readonly flag is lost. **Unblocks** ~~`omitTypeHelperModifiers01`~~ (ALREADY PASSES as of round 123 verification — B116 Omit-materialize + B120 readonly + B117 param-receiver already cover the TS2540 `x.c = …` via `Omit<A,'a'>`; round-107 skip-log was stale), `readonlyAssignmentInSubclassOfClassExpression` (TS2540 via `Readonly<{attrib}>` heritage-from-cast — also needs AsExpression base-type resolution in `getDeclaredTypeOfClassOrInterface`'s heritage path), and likely a broader TS2540/TS2353/TS2741 cluster. Carry a side-channel "is-readonly"/"is-optional" flag on the synthesized member Symbol (not via declarations) and consult it in `isReadonlySymbol` + the optional checks. RISK: MEDIUM-HIGH — mapped-type member synthesis feeds assignability broadly; full-suite + HEAD-diff each step. **ROUND-113 FINDING (2026-06-07): the TRUE first blocker is that `Omit`/`Pick`/`Exclude`/`Readonly`/`Partial`/`Required`/`Record` are ABSENT from the embedded lib (`BUILTIN_LIB_SOURCE`, grep confirms zero `type Pick`/`type Omit`/etc.).** So `Omit<A,'a'>` resolves to errorType/anyType → NOTHING materializes through `getTypeFromMappedType` → `x.c = …` on `omitTypeHelperModifiers01` emits nothing ("none produced", verified). The homomorphic-readonly-copy infra (a `!node.readonlyToken && node.type is IndexedAccessType` whose indexType names the mapped TP → copy `getPropertyOfType(objectType, key)`'s `isReadonlySymbol` onto a side-channel `mappedReadonlyMemberIds` set consulted by `isReadonlySymbol`) was WRITTEN + reverted this round (net-0, no consumer, FP-safe but unreachable — B87.5 no-zero-consumption precedent). So B100 is a 3-step blocker: **(step 1)** add the utility-type definitions to the embedded lib — a BROAD-RISK lib-content change (they currently resolve to anyType which SILENTLY suppresses many diagnostics corpus-wide; adding real defs may expose latent gaps → regressions; needs a dedicated full-suite-verified session); **(step 2)** verify `Omit`/`Pick` materialize through the alias-substitution → `getTypeFromMappedType` chain (keyof/Exclude key-enumeration); **(step 3)** re-apply the readonly-copy (and add the `?`/optional analog). The homomorphic gate (copy only when `!readonlyToken`, because the parser sets `readonlyToken` for `-readonly`/`+readonly`/plain alike — can't distinguish sign) is the load-bearing FP firewall for step 3. **ROUND-114 FINDING (2026-06-07): step 1 RE-SCOPED — adding the lib definitions is INERT, not broad-risk.** I added `type Partial<T>=…; type Readonly<T>=…; type Pick/Record/Exclude/Extract/Omit/Required/NonNullable` to `BUILTIN_LIB_SOURCE` and ran the full suite: **EXACTLY net-zero — zero flips AND zero regressions, identical 782-failing SET (verified by before/after failing-set diff, not just count).** Root cause: the utility-type names are intercepted UPSTREAM as **display-only stubs** at **Checker.kt:~27649** (a map `"Partial" to (1 to "Partial<T>")`, `"Readonly" to (1 to "Readonly<T>")`, `"Pick" to (2 to "Pick<T, K>")`, `"Omit" to (2 to "Omit<T, K>")`, …) consulted by `getTypeFromTypeReference` — they resolve to errorType/anyType with a synthetic DISPLAY string and NEVER consult any lib `type` alias. So the "broad-risk lib-content change" framing was WRONG: the definitions are harmless because they're unreachable. **The REAL step 1 is: change the stub map at 27649 to route these names to ACTUAL generic-type-alias resolution (the B50.1 `currentTypeAliasArgs` substitution path) against lib `type` definitions, THEN materialize the mapped/conditional bodies via `getTypeFromMappedType` (Partial/Readonly/Pick/Record/Required are mapped; Exclude/Extract/Omit/NonNullable are conditional + need keyof-enumeration).** THAT routing is where the regression risk actually lives (it makes `Partial<X>` a real type everywhere it's used, exposing mapped/conditional-resolution gaps). The lib definitions themselves were NOT committed (reverted — no-dead-infra rule). Also note `getTypeFromMappedType` is at Checker.kt:~83340 and there's an existing narrow TS2339 path for `g: Partial/Required/Readonly<T>` at Checker.kt:~74558 (16.4ed) to reconcile with.

- [ ] **B96-LIB-RISK (NOTED 2026-06-02 round 96 — HIGH-BLAST-RADIUS, defer): embedded `Promise<T>` member-set correction.** The embedded `Promise<T>` (Checker.kt:~26643) has `then/catch/finally` but lacks `[Symbol.toStringTag]`; the real ES2015 Promise (lib.es5 + lib.es2015.promise + lib.es2015.symbol.wellknown) member set differs. B94's property-`a` baseline line (`...missing the following properties from type 'Promise<number[]>': then, catch, [Symbol.toStringTag]`) is unreproducible without this. Editing the embedded lib shifts MANY Promise-related TS2322/TS2345 baselines → do NOT touch without a dedicated full-suite-verified session; treat as a lib-content change with broad regression surface. **Backpointer:** round-96 B94 deep-decomposition (the planning agent's "Substep E" prerequisite #2).

- [x] **B87.1 (CLOSED 2026-05-28 round 73, +2 net flips 9006 → 9008, flips both es5+es2015 variants of `doNotElaborateAssignabilityToTypeParameters_ts`). `Awaited<T>` + generic-union-return for async function bodies.** Solved via TWO landed pieces (B87.1b await-unwrap + a NARROW targeted async-generic-return emission), WITHOUT the broad (A) scope-push / (B) bare-TypeParam-relation changes originally feared. The narrow emission lives in `checkReturnAssignability` (Checker.kt:~49373): gated on `inAsyncFunctionBody` + `Promise<bareTP>` return annotation (TP name ∈ `typeParams`, no inner type args) + source is a `Type.Union` with ≥1 unconstrained-`Type.TypeParam` member named the TP AND ≥1 non-TypeParam non-any/error member; emits TS2322 at the `return` keyword (len 6) + the "could be instantiated with an arbitrary type" messageChain; display = non-TP members first then each TP member wrapped `Awaited<name>` → `Yadda | Awaited<T>`. Zero regressions (FP-surface tiny by construction). B87.1c's (A)/(B)/(C) pieces are now moot for this test (left documented below for any future async-generic test needing the GENERAL relation/scope fix). Target `doNotElaborateAssignabilityToTypeParameters_ts` (`async function foo<T>(x: T): Promise<T> { let yaddable = await getXOrYadda(x); return yaddable; }` where `declare function getXOrYadda<T>(x: T): T | Yadda`). Expected: TS2322 @ (3,3) on `return yaddable` — `Type 'Yadda | Awaited<T>' is not assignable to type 'T'.` + the EXISTING "'T' could be instantiated with an arbitrary type which could be unrelated to 'Yadda | Awaited<T>'." elaboration (17.11a infra). We currently emit NOTHING. Root-cause trace needed but the chain is: (1) `getXOrYadda(x)` must infer inner-T = outer-T and resolve to `T | Yadda` (NOT anyType — verify whether `getReturnTypeOfCallExpression` collapses the union-return to any when the arg is a bare TypeParam); (2) `await (T | Yadda)` must produce `Awaited<T> | Yadda` — `Awaited` is registered as a known name (Checker.kt:23563, `"Awaited" to (1 to "Awaited<T>")`) but NOT resolved as a conditional type; for a bare-TP operand TypeScript displays `Awaited<T>` literally (does NOT expand), so the await-transform need only wrap the non-promise-like bare-TP part as `Awaited<T>` and leave `Yadda` as-is; (3) the async return-assignability check (`Yadda | Awaited<T>` vs unwrapped return `T`) must fire TS2322 + the could-be-instantiated elaboration. **Decomposition** (land each net-zero/+flip independently, full-suite verify):
  - [x] **B87.1a (TRACE DONE 2026-05-28 round 73, net-zero — no code)** — temporary debug at the `checkReturnAssignability` type-engine gate (Checker.kt:~49392) on `*doNotElaborateAssign*es2015*` printed: `src=any[Intrinsic] tgt=Promise<T>[Interface] canUse=false relToTgt=true awaited=null`. **Findings (the gap is NOT one-line — it is 4+ coupled pieces):** (i) `sourceType` of `yaddable` resolves to **anyType** — `getXOrYadda(x)` (`<T>(x:T): T | Yadda`) generic-union-return inference collapses to `any` when the arg is a bare TypeParam, and `await` keeps it `any`; (ii) `targetType` = `Promise<T>` resolves to a bare **`Type.Interface`**, NOT a `Type.Reference` carrying the type arg `T`, so the EXISTING async-unwrap branch (Checker.kt:~49398, gated `targetType is Type.Reference && symbol.name=="Promise"`) never fires (awaited=null); (iii) consequently `canUseTypeEngine(any, ...)` is false → no diagnostic. So B87.1b must FIRST fix (i) the union-return inference AND likely (ii) make `getTypeFromTypeNode(Promise<T>)` produce a `Type.Reference`; THEN (iv) the residual relation question — `checkTypeRelatedTo(Yadda | Awaited<T>, bare T)` returns `true` today (bare unconstrained TypeParam target has apparent type `{}` → everything assignable), but TypeScript wants it FALSE with the "could be instantiated with an arbitrary type" elaboration (17.11a infra exists). Per the decision rule (multi-piece, no one-line gap) NO code landed; the trace re-scopes B87.1b/c below. **DO-NOT** broadly change the bare-TypeParam-target relation result — it would cascade through every internal generic-function assignment; the fix must be a narrow gate at the async-generic-return site only.
  - [~] **B87.1b (PARTIAL — await-unwrap LANDED 2026-05-28 round 73, net-zero, zero regressions; union-return inference still TODO).** Two sub-gaps remain from the B87.1a trace; the FIRST is now closed: `getTypeOfExpression(AwaitExpression)` was hardcoded `anyType` (Checker.kt:52636 `// TODO: unwrap Promise`). Replaced with: unwrap one level `Promise<X> -> X` (Type.Reference to Promise), non-Promise operands await to their own type, bare/unknown operands stay anyType (prior behavior, avoids cascade). Full suite 9006 unchanged (net-zero across the broad change — most async tests already tolerate the more-precise await type). This alone does NOT move `doNotElaborateAssignabilityToTypeParameters` because the operand `getXOrYadda(x)` STILL resolves to anyType (the union-return inference gap below). **REMAINING B87.1b:** preserve the `T | Yadda` union-return from `getXOrYadda<T>(x:T): T | Yadda` instead of collapsing to any when the arg is a bare TypeParam — find where `getReturnTypeOfCallExpression` (Checker.kt:54201) loses the union for a declared generic fn called with a bare-TP arg. RISK: MEDIUM (generic-union-return inference is consulted broadly — verify no TS2322/TS2345 cascade).
  - [ ] **B87.1c (re-scoped 2026-05-28 round 73 after the await-unwrap fix advanced the source).** Post-await-fix re-trace at the return site shows `src=T | Yadda[Union]`, `tgt=Promise<T>[Interface]`, `canUse=false`, `relToTgt=false`. So the SOURCE is now correct (only the `Awaited<T>` display differs — TypeScript shows `Yadda | Awaited<T>`). The remaining blockers are now precisely THREE coupled high-risk pieces:
    - **(A) `Promise<T>` resolves to a bare `Type.Interface`, not a `Type.Reference` carrying `T`.** Root cause: when `checkReturnAssignability` calls `getTypeFromTypeNode(returnTypeNode = Promise<T>)`, the function's own type param `T` is NOT in `currentTypeParamScope`, so `T` → errorType and the `Type.Reference` creation is skipped by the errorType guard (CLAUDE.md "getTypeFromTypeReference errorType guard"). Fix would push the enclosing function's `typeParameters` into scope before resolving the return-type node. RISK: HIGH — makes `Promise<T>` (and every other generic return annotation) a Reference broadly; cascades through return-assignability across the suite.
    - **(B) assign-to-bare-TypeParam must FAIL.** Even with (A), the async-unwrap branch would compare `T | Yadda` vs awaited `T`; `checkTypeRelatedTo(Yadda, bare T)` returns `true` today (apparent type `{}`), so no diagnostic. TypeScript wants FALSE + the "'T' could be instantiated with an arbitrary type" elaboration (17.11a infra exists). RISK: HIGH and explicitly DO-NOT broadly — cascades through every internal generic-function assignment. Needs a narrow gate at the async-generic-return site only.
    - **(C) `Awaited<T>` display** — render the bare-TP constituent of the awaited union as `Awaited<T>` (the name is registered at Checker.kt:23563) for the TS2322 message. Cosmetic; only matters once (A)+(B) emit.
    - **Conclusion:** B87.1's remaining work is (A)+(B)+(C), all coupled and (A)/(B) broad-cascade-risky. NOT a bounded surgical fix; needs a dedicated session that can absorb the full-suite regression triage from the scope + relation changes. The await-unwrap piece (B87.1b) was the one safely-landable increment.
  - **Sibling tests**: any async generic function whose body returns a value that doesn't structurally match the bare return TypeParam. Survey post-B87.1a.
  - **Backpointer**: round-72 STATUS "next-session: Blocker #2" recommendation; round-73 confirmed the surgical pool dry and B83.4 closed, so this is the promoted next concrete Blocker substep per the anti-loop rule.

### Completed items (16.0–16.3) — archived

Moved to `PLAN-PHASE-4-HISTORY.md` (2026-06-10) — § "Completed items (16.0–16.3)".

### Skip log (explored-but-skipped tests) — moved

The live skip log (`### Explored-but-skipped tests`, the section `find_candidates.py` reads its
`[SKIP]` token set from) now lives in `PLAN-PHASE-4-HISTORY.md`. Add new skip entries THERE,
above the `### End of skip log` terminator heading; suffix entries with `_ts`. The script reads
both files and unions the tokens, so entries in either file work.

### What's left in the "surgical fix" pool

**As of 2026-05-31 (post-round-85, 9098 passing) — UPDATED**: Round 84 (+16) and round 85 (+6) worked the bounded NONE/EXTRA/SWAP surgical tail across module-resolution, index-signature, readonly, and tslib-helper diagnostics. Round 85 landed B98.r20 (TS2413 number-vs-string index value), r21/r21b (TS7016 + untyped-JS-sibling TS2307 suppression), r24 (TS2540 intersection-property writes), r25 (TS2430 multi-base inherited index-sig), r26 (TS2343 `__awaiter`). **The bounded surgical tail is now genuinely exhausted** — the round-85 triage (logged under "Explored-but-skipped → Round 85 triage skips") classified every remaining ≤2-error NONE candidate as needing one of: control-flow narrowing (Blocker #1: TS2488/TS2532/TS18048 `never`/possibly-undefined), cross-file/namespace scope (Blocker #3: TS2694/TS2503/TS2300-merge/TS2451-merge), type-engine assignability/generic-inference (TS2322 ×91 / TS2345 ×37 / TS2339 ×37 / TS2741 / TS2403), mapped-type-modifier resolution (TS2540 Omit/Readonly), module-resolution modeling (bare/symlink/untyped-module/typeRoots TS2307/TS2688 — bare-missing TS2307 ATTEMPTED round 85, reverted -6), decl-emit portability (TS2883/TS4032), or the FP-prone namespace-merge cross-block duplicate (`cloduleWithDuplicateMember2`). **Next session: commit to an architectural blocker substep** (Blocker #1 control-flow narrowing has the highest yield; Blocker #2 generic inference and Blocker #3 cross-file scope are the other two). Do NOT spend a session re-scanning the NONE tail expecting bounded wins — they are documented as exhausted.

**As of 2026-05-18 (post-B47.7, 8815 passing) — UPDATED**: After the B47.x series (MAINT-2 + B47.1–B47.7 + B47.5 foundation + B47.6 two-piece), the surgical pool at +1-per-commit is truly exhausted at the errors-baseline level (`find_candidates.py --fresh` returns 0/0/0). The JS-emit candidates pool has 108 entries; cataloguing in PLAN-PHASE-4.md confirms all are feature-scale (parser-recovery, private fields downleveling, `__setFunctionName`, `__rest` for CJS export-rest, type-display normalization for nullable targets, conditional type resolution, namespace-qualified union display, lib subsetting, module/file-ordering architectural blocker). Recent session B47.x landed +7 tests via narrow defensive-emit patterns; further +1 wins require feature-scale work or completing architectural blockers. Recommended next: tackle blocker substep per the architectural-blockers section.

**As of 2026-04-25 (post-16.4gs, 8337 passing) — RECOMMENDED SHIFT**: After 6 consecutive sessions confirming the surgical pool is empty (16.4go, post-16.4gk recon, post-16.4gl recon, 16.4gp, 16.4gq, post-16.4gr) plus this session's exhaustive re-audit of skip-list entries, the surgical pool is genuinely exhausted at the +1 to +3 per-commit level. **Next sessions should commit entirely to architectural blockers** per the new priority ordering (see "Known architectural blockers" below — reshuffled 2026-04-25). The recommended next step is **MAINT-1 (stale skip-log audit)**, then **Blocker #1 (full control flow narrowing)** — the latter has the highest single-feature yield estimate (~60–100 tests realistic, 250 upper bound).

**As of 2026-04-25 (post-16.4gi, 8323 passing)**: `find_candidates.py --fresh` returns 1 SWAP candidate (`newMap_ts`) which requires a multi-piece fix (TS2743 emission + lib-version-aware MapConstructor/WeakMapConstructor declarations). All other candidates are filtered as already-skipped. **Re-examining previously-skipped tests after each round of infrastructure changes is the highest-yield strategy now**: 16.4gi flipped ~~`typeCheckingInsideFunctionExpressionInArray_ts`~~ because 16.4ge's Variable+Interface merge made `Object` resolve as a real Type.Interface, which then revealed that `getReturnTypeOfNewExpression`'s Type.Interface branch returned `calleeType` directly (instead of consulting construct sigs for the "constructor interface" pattern). The same merge unblocks several other tests partially — e.g. ~~`booleanAssignment_ts`~~ now correctly types `b: Boolean` (after 16.4gi's `new X()` fix) but still has 2 FPs (primitive→wrapper assignability) + 1 missing chain (valueOf elaboration). Continuing to re-examine the skip log against the latest infrastructure is the recommended workflow until a coordinated multi-piece fix or blocker is undertaken.

**As of 2026-04-24 (post-16.4fs, 8304 passing)**: `find_candidates.py --fresh` still returns 0 across all buckets at diff ≤ 3 (filtered from 9/118/32). The 16.4fs win came from re-visiting a previously-skipped test (`pathMappingBasedModuleResolution_withExtension_failedLookup`) and landing a narrow TS2307 branch gated on "every paths target has an explicit `.ts`/`.tsx`/`.d.ts` extension" — narrower than the broad "paths config → skip TS2307" originally used. Each previously-skipped test is worth re-examining when its skip reason names a specific resolver limitation that a narrower gate could bypass. The 16.4fr win came from a non-obvious extension of an existing emission site (TS6212 branch) to cover an adjacent pattern, not from a fresh candidate — the tests it flipped (`optionalParamAssignmentCompat`, `functionSignatureAssignmentCompat1`) had 3-gap diffs (position + display + related info) that all resolved cleanly together. Similar "stacked gap" wins may still exist when multiple narrow fixes coincide. Otherwise next gains require either a blocker #1 sub-step (return-type-from-body broader patterns, or named→named cross-target comparison) or a coordinated new-diagnostic family implementation (TS6213 "Did you mean to use new?" at var-init, rootDir validation, iterator-protocol typing for TS2802/TS2495/TS2488).

---

After 2026-04-19 (16.4dm, 8227 passing — net-zero infra commit on top of 16.4dl), the low-hanging pool is empty. The 16.4di–dl line landed targeted wins on the inheritance-chain side of blocker #1 (generic property chain walk + TS2416 class-type-param scope + private-brand elaboration for TS2322/TS2345 + apparent-type constraint check for TS2344). 16.4dm extended step (c) to cover primitive-literal returns and added function-mismatch chain lines to property elaboration — both correct improvements that gate on per-property TS2322 emission to produce visible test gains. `find_candidates.py --fresh` still returns ~20 candidates; **every one remaining** is classified as a blocker or feature gap:
- Generic inference (Blocker #1): widenToAny1/2, declarationEmitBundleWithAmbientReferences, recursiveTypeRelations, lambdaArgCrash
- Cross-file scope (Blocker #5): jsFileCompilationLetDeclarationOrder2, jsFileCompilationDuplicateVariableErrorReported
- JSDoc (Blocker #2): jsExportMemberMergedWithModuleAugmentation, jsFunctionWithPrototypeNoErrorTruncationNoCrash
- Conditional types: conditionalAnyCheckTypePicksBothBranches
- Control-flow narrowing: narrowingUnionToNeverAssigment, optionalChainWithInstantiationExpression1 (es2019/es2020)
- Intersection privacy reduction: intersectionWithConflictingPrivates
- Display: errorWithSameNameType (cross-module qualified), deeplyNestedAssignabilityErrorsCombined (typeof-of-class-value), crashInEmitTokenWithComment (destructured arrow params)
- TS2859 complexity budget: relationComplexityError
- Standalone new diagnostic: importAliasFromNamespace (TS2845), taggedTemplatesWithIncompleteTemplateExpressions3 (TS2345 for tagged templates), importHelpersWithLocalCollisions (TS2354 for AMD/System tslib resolution)
- Structural elaboration: optionalPropertiesTest (optional→required), implementsIncorrectlyNoAssertion (TS2416 with intersection-of-classes)

The next +N gains either come from:
1. **Implementing new diagnostics** (TS6234 / TS2351 / TS2352 / TS2300-vs-lib / TS7023 / TS2802 / TS2744 / TS2845 / TS4081 / TS2354-AMD): each is 1-3 tests on its own but together could net +15-20. Each is self-contained (contained to a specific emission site) but individually small.
2. **Taking on a blocker** (structural generic comparison #1 for ~30+ tests, JSDoc #2 for ~5-10, etc.). Blocker #1 is by far the highest-yield single investment.

**Recommendation**: surgical pool is **exhausted**. Next session should commit to either (a) a new-diagnostic batch (pick 2-3 low-risk ones in a single session), or (b) blocker #1 (structural generic comparison) with the retry plan below. Continuing to look for +1 surgical wins via `find_candidates.py --fresh` will yield nothing.

### Known architectural blockers (RESHUFFLED 2026-04-25, 8337 passing)

These blockers recur across multiple "close-to-passing" tests and cannot be fixed with surgical changes in a single session. Any agent attempting these should plan for a multi-session investigation with regression budget.

**Priority ordering** — ranked by (expected tests unblocked) ÷ (regression risk × refactor scope). Higher rank = better cost/benefit. Reshuffled after the 16.4gq–gs session: the surgical pool has been ground down to zero across 5+ sessions, so the path forward is committing entire sessions to architectural work. Per the original Phase 16 impact analysis (line ~2479), the largest unaddressed feature is **control flow narrowing** (~250 tests potential, only partial done in 16.3) — it now sits at #1.

**What changed in this reshuffle:**
- **NEW #1**: Full control flow narrowing (was 16.3 PARTIAL — promoted to architectural blocker since the remaining work is full flow-graph construction, not surgical).
- **NEW #2**: Generic argument inference (was implicit under "16.4 generic instantiation" — promoted because tests like `genericConstraintSatisfaction1`, `superCallArgsMustMatch`, `genericClassWithStaticFactory`, `superWithTypeArgument3`, `privacyCheckAnonymousFunctionParameter2` all hit the same gap: we don't infer `T` from arg type then check the constraint).
- **#3 (was #5)**: Cross-file global scope conflation — bumped UP. Each session that does skip-log auditing finds more tests gated on this; estimated impact revised upward.
- **#4 (was #1)**: Structural comparison of generic refs — DEMOTED to LOW yield. Steps (a)–(d) infrastructure landed; the remaining gaps are bounded (different-target TS2352, overload-aware TS2416 for lib types, multi-statement return-from-body inference) and total ≤ 10 tests across all sub-cases.
- **#5–#7**: JSDoc, TS7006 over-suppression, Parser error-recovery — unchanged ordering, all LOW yield.
- **NEW MAINT-1**: Stale skip-log audit (16.4gp/gr/gs sessions all found stale entries — running this once could surface +5–15 candidates that are already passing).

---

#### 1. Full control flow narrowing — HIGHEST yield (~100+ tests realistic), MEDIUM risk

**Status (2026-04-25):** 16.3 landed surgical narrowing fixes (TS1344 message, TS2739/TS2740 multi-missing, null/undefined narrowing in `if (x == null)` branches, narrowing helper for variable declarations). Full flow-graph construction was deferred. The remaining gap is the dominant blocker for TS2339 (549 tests with this code), TS2454 (103 deficit), TS2774 (118 tests).

**Failing test patterns that need this:**
- ~~`nestedLoopTypeGuards_ts`~~ flipped 17.30a (flow-graph TS2454 body-recursion)
- ~~`typeGuardConstructorDerivedClass_ts`~~ flipped 17.5a (`x.constructor === Class` narrowing)
- ~~`narrowingUnionToNeverAssigment_ts`~~ flipped 17.1b (var-decl `never` target narrowing)
- ~~`narrowByEquality_ts`~~ (TS2839 narrowing — partially done in 16.4fy but limited)
- ~~`optionalChainWithInstantiationExpression1_ts`~~ flipped 17.44 (synthetic-paren marker for instantiation-expr-breaks-chain pattern)
- Many TS2774 "always defined" / TS2454 "used before assigned" with non-trivial control flow.

- **Yield**: 60–100 tests realistic (250 upper bound from impact analysis × ~30% realistic factor).
- **Scope**: build a real flow graph during binding (per-function basic blocks + branch edges), reify narrowed types per AST position. tsgo's `internal/checker/flow.go` (2.7K lines) is the reference implementation. This is INFRASTRUCTURE work — expect 2–4 sessions to land safely.
- **Risk**: MEDIUM. Narrowing is opt-in (a position with no narrow info falls back to declared type), so regressions are bounded to over-narrowing (false NEVER) rather than wholesale changes.
- **Why first now**: highest absolute yield. The original Phase 16 ordering put 16.3 fourth ("orthogonal but complex; defer to after easier wins") but the easier wins are now exhausted — narrowing IS the next big unlock.

#### 2. Generic argument inference (NEW) — HIGH yield (~20–40 tests), MEDIUM risk

When calling a generic function with concrete args, TypeScript infers each type parameter from the corresponding argument type, then checks each inferred T against its constraint. Currently we punt on this entirely — generic call sites with type-param-typed args either pass trivially (T = anyType) or skip the assignability check.

**Failing test patterns that need this:**
- ~~`genericConstraintSatisfaction1_ts`~~: `f<T extends S>(x: T)` called with `{s: 1}` against `S = {s: string}` — should emit TS2322 + TS6500.
- ~~`genericClassWithStaticFactory_ts`~~: `T` not inferred from arg → no TS2345.
- ~~`superCallArgsMustMatch_ts`~~, ~~`superWithTypeArgument3_ts`~~: `super(...)` arg checking when base is generic.
- ~~`privacyCheckAnonymousFunctionParameter2_ts`~~: function-typed parameter not structurally compared.
- ~~`widenToAny1_ts`~~, ~~`widenToAny2_ts`~~: `string | undefined` initializer widened wrong against `number` annotation.

- **Yield**: 20–40 tests. Each individual test gives +1, but the pattern repeats across many test files.
- **Scope**: extend `checkArgumentsAgainstSignature` and `checkArgumentsAgainstOverloads` to: (a) collect inferences from arg→param matching (each TypeParam → concrete type), (b) apply constraint check per TypeParam, (c) substitute inferred types into the param type and recheck. Touches the existing overload-resolution and constraint-checking paths.
- **Risk**: MEDIUM. Could regress overload-resolution tests if inference picks wrong overload. Mitigate with conservative gating (only when ALL TypeParams resolve to non-error types).
- **Why second**: stacks naturally on Blocker #4 step (a)/(b)/(c)/(d) infrastructure (Type.Reference interning, `resolveGenericPropertyType` mapper, `getPropertyTypeForRelation` cache) — the building blocks are already there.

#### 3. Cross-file global scope conflation — MEDIUM-HIGH yield (~30+ tests, was MEDIUM), HIGHEST risk

`Checker.init` does `mergeSymbolTable(globals, result.locals)` for every binderResult, merging module-file exports into the shared `globals` map alongside script-file locals and lib. In module files, identifiers from OTHER module files that weren't imported appear in `scope.has(name)` as if they were globals.

Blocks fine-grained module-visibility diagnostics: TS2301 vs TS2663 distinction, default-import-from-export-equals visibility, namespace export filtering across files.

**Failing test patterns:**
- ~~`classMemberInitializerWithLamdaScoping4_ts`~~ (TS2301 vs TS2663)
- `moduleAugmentationsImports3/4_ts` (TS2339 FP on nested module augmentation)
- ~~`errorsOnImportedSymbol_ts`~~ (cross-file type-only flagging)
- ~~`jsFileCompilationLetDeclarationOrder2_ts`~~, ~~`jsFileCompilationDuplicateVariableErrorReported_ts`~~
- Possibly contributes to many `genericSpecializations*`, `arrayAssignmentTest*` cross-namespace cases not yet diagnosed.

- **Yield**: 30+ tests. Each skip-log audit session surfaces more candidates blocked here.
- **Scope**: split "true globals" (script-file locals + lib + KNOWN_GLOBALS) from per-file module locals; reconstruct `fileScope` to only include imports the current file actually declares. Touches **every identifier-resolution check** in the checker (~30 call sites).
- **Risk**: HIGHEST. A naive implementation regresses 50+ tests. Recommended approach: introduce per-file-scope construction behind a flag, run side-by-side, then flip per file as the scope construction proves correct.
- **Why third (bumped from last)**: yield estimate revised upward after multiple skip-log audits found this blocker named in the residual; without addressing it, ~30 surgical-looking tests remain stuck.

#### 4. Structural comparison of generic type references — LOW yield (was MEDIUM), MEDIUM risk

**Status (2026-04-25, 8337 passing):** Steps (a), (b), (c) partial, (d) partial all landed across 16.4dc–df, dg, dm, gr, gs. Type.Reference interning, cycle/depth heuristic, TypeParam apparent-type comparison, `resolvedPropertyTypes` cache, heritage-clause type-arg instantiation, base-chain inheritance via `resolveGenericPropertyType`, parameter-mismatch chain elaboration, base-method TypeParam TS2208 — all in place.

**What's left (small, gated by other blockers):**
- ~~Different-target TS2352 with "Property X missing" elaboration (`genericTypeAssertions2_ts`)~~ — flipped 17.28 (narrow array-source → class-target case). Generalizing to all different-target casts still requires a TypeScript-style `comparable` relation with full structural elaboration.
- Overload-aware TS2416 for lib generic interfaces (`implementArrayInterface_ts` emits 2 extra diagnostics because lib's `every`/`filter` have type-predicate overloads that user code only implements one of).
- ~~Multi-statement bodies with single return for `inferSimpleReturnTypeFromBody`.~~ **Closed B4.1 (2026-05-13, +0 foundation)** — helper at Checker.kt:39694 now handles multi-statement bodies via top-level ReturnStatement collection.
- ~~Identifier returns (`return param`, `return this.field`) for return-type-from-body inference.~~ **Closed B4.2 + B4.3 (2026-05-13, +0 foundation)** — `is Identifier` arm extended to `return paramName` for annotated params; new `is PropertyAccessExpression` arm handles `return this.field` for class-property-typed fields (new `enclosingClass: Type.Interface?` parameter threaded from `resolveGenericPropertyType` caller at Checker.kt:39625).

- **Yield**: ≤ 10 tests across all sub-cases.
- **Scope**: contained to checker (no binder/parser changes).
- **Why demoted**: infrastructure work is largely done; remaining test gains are individual-test scale, not pattern-level. Best handled opportunistically when a specific case aligns with surgical session's budget.

#### 5. JSDoc type annotations — LOW yield (~5–10 tests), LOW risk

`@type {T}`, `@this {T}`, `@typedef`, `@param {T}` — we bind `@typedef` nominally but don't honor `@type` assertions or `@this` parameter types.

- **Yield**: 5–10 `.js` file tests.
- **Scope**: JSDoc parsing (scanner comment handling) and a new JSDoc-to-TypeNode bridge in the checker. Does not touch non-JS checking paths.
- **Why fifth (was second)**: low blast radius means low risk, but absolute yield is small. Higher-yield blockers (#1, #2, #3) take priority for "session-scale architectural work" budget.

#### 6. TS7006 over-suppression for callback parameters — LOW yield (~3–5 tests), MEDIUM risk

We suppress TS7006 for any `contextuallyTyped=true` param. TypeScript only suppresses when the contextual type actually provides param types (bidirectional inference succeeds).

- **Yield**: 3–5 tests (`intraBindingPattern`, `subtypeReduction`, `signatureCombiningRestParameters`).
- **Scope**: requires bidirectional inference plumbing in contextual typing.
- **Why sixth**: low yield + needs new infrastructure. Defer.

#### 7. Parser error-recovery asymmetry — LOW yield (1–2 per case), HIGH risk per attempt

Several tests expect specific token-consumption-then-continue recovery (e.g. `declare class foo();` → class with empty body + phantom `()` statement + next function).

- **Yield**: 1–2 tests per case, case-by-case.
- **Scope**: per-case parser edits.
- **Why last**: per-case work, no multiplier, regression-prone. Best handled opportunistically.

---

#### MAINT-1. Stale skip-log audit — UNKNOWN yield (likely 5–15 tests), LOW risk

Every recent session that audited the skip log found 1–3 entries marked "stuck on Blocker #X" that have actually been passing for sessions (e.g. 16.4gl-recon found `genericCloneReturnTypes`/`genericCloneReturnTypes2` stale; 16.4go found `generics4`/`genericDerivedTypeWithSpecializedBase`/`genericPrototypeProperty3` stale; this session found `genericPrototypeProperty3` again). The cumulative impact of stale entries blocking the candidate finder is probably 5–15 tests.

- **Yield**: unknown but likely 5–15 candidates that re-surface as fresh.
- **Scope**: walk every entry in the "Explored-but-skipped tests" section, run the test, mark stale (~), update reason if still failing.
- **Risk**: zero (read-only investigation).
- **Why a maintenance task**: not architectural but high-leverage. Should be run as a one-off "audit session" before any session that uses `find_candidates.py --fresh` for surgical work, or after every major architectural fix lands.

---

**Parallelism note**: Blockers #1, #2, #5, #7 can be worked independently. #3 should run after #1/#2 have resolved their fraction of overlapping cases (clearer "remaining failures pointing to #3" set). #4's remaining sub-cases pair naturally with surgical sessions — not a full-session investment. #6 depends on enough contextual-typing infrastructure that #2's inference work should land first.

**Recommended next sessions (in order):**
1. **MAINT-1** (1 session): clean up stale skip log → frees `find_candidates.py --fresh` to find genuine candidates.
2. **Blocker #1 step 1** (1–2 sessions): build flow-graph infra in binder; no behavior change yet.
3. **Blocker #1 step 2** (2 sessions): wire flow-graph into TS2454/TS2339/TS2774 emission sites.
4. **Blocker #2** (2 sessions): generic argument inference + constraint check.
5. **Blocker #3** (3+ sessions): per-file scope construction behind a flag, then per-file flip.

### Candidate-picking workflow for surgical fixes

When context starts a new session, the fastest path to wins is:

1. **Run full suite once** (4-6 min) to produce `build/test-results/jvmTest/*.xml`.
2. **Python-parse the XMLs** to find tests by diff size and pattern. The key filter is 1-2 extra diagnostic lines (too-aggressive checks) or 1 missing diagnostic at a specific position (simple checks to add). Example snippets live in this file's history — reuse them.
3. **Look for code-swap tests** (expected TS####A at position P vs actual TS####B at same position P) — these are often single diagnostic emissions at a specific code path that just need the code changed or position adjusted.
4. **Skip**: tests needing generic variance, module-visibility, JSDoc type handling, or full type inference — those are the architectural blockers above.
5. **Inspect `typescript-repo/tests/cases/compiler/<name>.ts` + `typescript-repo/tests/baselines/reference/<name>.errors.txt`** for context before implementing. Don't just look at the diff.

### Queue execution strategy

**Original Phase 16 sequence (DONE/IN PROGRESS):** 16.0 → 16.1 → 16.2 → 16.3 → 16.4

**Status:** 16.0/16.1/16.2 DONE, 16.3 PARTIAL (full flow-graph deferred → now Blocker #1), 16.4 IN PROGRESS (generic infrastructure largely done; remaining sub-cases moved to Blocker #4).

**NEW sequence (2026-04-25 reshuffle) — as the surgical pool is exhausted, commit entire sessions to architectural blockers:**

**Phase 17 sequence:** MAINT-1 → Blocker #1 (control flow narrowing) → Blocker #2 (generic argument inference) → Blocker #3 (cross-file scope) → Blocker #4 / #5 / #6 / #7 opportunistically.

**Rationale (priority-by-yield × tractability):**
1. **MAINT-1 first** — stale skip-log audit. Read-only, ~1 session, surfaces 5–15 already-passing tests as fresh candidates that would otherwise stay hidden behind `[SKIP]` markers in `find_candidates.py`. Cheap, no regression risk, and clears the deck for #1's diagnosis.
2. **Blocker #1 (control flow narrowing) second** — highest absolute yield (~60–100 tests realistic). The remaining failing tests across many categories (TS2339, TS2454, TS2774, TS2839) all converge on this. Build flow-graph in binder, wire into emitters in two stages.
3. **Blocker #2 (generic argument inference) third** — stacks on the 16.4 generic infrastructure already in place. ~20–40 tests. Lower risk than Blocker #3.
4. **Blocker #3 (cross-file scope) fourth** — high yield but highest risk; defer until #1/#2 have cleared their fraction so the residual is well-characterized.
5. **Blocker #4 (remaining structural generic gaps) opportunistically** — small (~10 tests), pairs with surgical sessions, not full-session work.
6. **Blockers #5/#6/#7** — low yield, defer.

**Hard rules:**
- Each feature must land behind a feature flag or with conservative guards initially.
- Run full suite after EACH sub-step to catch regressions early.
- If a feature causes >20 regressions, add more guards or narrow scope; don't revert without analysis.
- Document each partial implementation in CLAUDE.md gotchas.

**Per-session throughput — fix multiple items per session.** A single sub-step (e.g. 16.4h, 16.4i) is the unit of commit, not the unit of session. After committing + pushing one sub-step, loop back to pick up the next unchecked sub-step. Keep going until (a) the queue is empty, (b) the next item is genuinely blocked, or (c) context/time budget is nearly exhausted — then wrap up with a summary. Two-to-four sub-steps per session is a reasonable target when the items are small (+1 to +5 tests each); larger infrastructure items may consume a full session on their own. Always finish each sub-step cleanly (test suite green, commit + push) before starting the next — never bundle unrelated work into one commit.

**Realistic total gain:** 500-800 new passing tests → ~85-88% pass rate.

---

## Reference

- **tsgo source**: `github.com/microsoft/typescript-go` — `internal/checker/`
- **TS checker**: `microsoft/TypeScript` — `src/compiler/checker.ts` (53,296 lines)
- **Key tsgo files**: `checker.go` (31K), `relater.go` (5K), `types.go` (1.3K), `flow.go` (2.7K), `inference.go` (1.6K)
- **Parallelism model**: `internal/compiler/checkerpool.go` — N independent checkers, round-robin file assignment, shared immutable AST
