# Phase 4 — Structural Type Checker

**Status (2026-04-17):** 8,108 / 10,078 tests passing (80.4%). Active queue: **Phase 16 — Fundamental Type System Features**. 16.0 done, 16.1 done, 16.2 done, 16.3 partial (+14 tests), 16.4 in progress (+32 tests, 1967 remaining).

## Goal

Implement structural type checking to unlock the ~2,400 remaining test failures.
Design for future parallel checking using Kotlin coroutines (inspired by tsgo's goroutine model).

## Architecture (inspired by tsgo)

### Key design decisions

**1. Type hierarchy — Kotlin sealed classes**

Replace the current string-based type representation (`"number"`, `"@MyType"`, `"|A | B"`)
with a proper `Type` sealed class hierarchy. Kotlin sealed classes give us exhaustive
`when` matching and smart casts — more idiomatic than tsgo's `TypeData` interface approach.

```kotlin
sealed class Type {
    abstract val flags: TypeFlags
    val id: Int = nextId++  // per-checker unique ID
}

class IntrinsicType(override val flags: TypeFlags, val name: String) : Type()
class LiteralType(override val flags: TypeFlags, val value: Any) : Type()  // string/number/boolean
class ObjectType(override val flags: TypeFlags) : Type() {
    var members: SymbolTable? = null       // lazily resolved
    var properties: List<Symbol>? = null
    var callSignatures: List<Signature>? = null
    var constructSignatures: List<Signature>? = null
}
class InterfaceType(...) : ObjectType(...)  // adds typeParameters, baseTypes
class TypeReference(...) : ObjectType(...)  // adds target (generic), resolvedTypeArguments
class UnionType(val types: List<Type>) : Type()
class IntersectionType(val types: List<Type>) : Type()
class TypeParameter(...) : Type()  // adds constraint, default
// ... etc
```

**2. Parallelism preparation — LinkStore pattern**

tsgo's key insight: checker-local metadata stored in side maps, not on shared AST nodes.
We prepare for this from the start:

```kotlin
class Checker(...) {
    // Checker-local type cache — NOT on AST nodes
    private val nodeTypes = HashMap<Long, Type>()        // nodeKey → resolved type
    private val symbolTypes = HashMap<Int, Type>()       // symbol.id → resolved type
    private val declaredTypes = HashMap<Int, Type>()     // symbol.id → declared type
}
```

When we later create N parallel Checker instances, each will have its own maps.
The shared AST and binder output remain immutable.

**3. Lazy type resolution**

Types are resolved on-demand (matching both TS and tsgo):
- `getTypeOfSymbol(symbol)` — checks `symbolTypes` cache, computes if absent
- `getTypeFromTypeNode(node)` — checks `nodeTypes` cache, computes if absent
- `resolveStructuredTypeMembers(type)` — resolves members lazily on ObjectType

**4. Relation engine**

Structural comparison via `isTypeRelatedTo` → `structuredTypeRelatedTo` →
`propertiesRelatedTo`. Cached in relation maps keyed by `(source.id, target.id)`.

```kotlin
enum class Ternary { True, Maybe, False }

class Relation {
    private val cache = HashMap<Long, Ternary>()  // pack two Int IDs into Long
}

// Five relation instances (same as TS/tsgo)
private val subtypeRelation = Relation()
private val assignableRelation = Relation()
private val comparableRelation = Relation()
private val identityRelation = Relation()
```

**5. Future: N-checker parallelism via coroutines**

```kotlin
// Future (Phase 4g) — not yet implemented
class CheckerPool(private val program: Program, private val checkerCount: Int = 4) {
    private val checkers = List(checkerCount) { Checker(program) }
    private val fileAssignments = program.files.withIndex().associate { (i, f) ->
        f to checkers[i % checkerCount]
    }

    suspend fun checkAllFiles() = coroutineScope {
        checkers.map { checker ->
            launch { checker.checkAssignedFiles() }
        }.joinAll()
    }
}
```

---

## Completed infrastructure (Phase 4a, items 0–9)

All complete. Type hierarchy, type resolution, structural comparison engine, generic
instantiation, expression type inference, parallel checking pool are in place.

### Completed Phase 4b items (10–15a)

- [x] **10a/10b/10c** — TS2322 wired to Type engine (var decl, return, assignment — conservative)
- [x] **11a-11d** — Expression type inference (object literal, array, arrow/function, identifier)
- [x] **12a/12b/12c** — TS2345 union types, union call signatures, TS2769 overload diagnostics
- [x] **15a** — TS2300 duplicate class members

---

## Phase 16 — Fundamental Type System Features

**Session 2026-06-15 (round 166, multi-iteration session, ≥20 iterations per user request, addressing blockers) — +3 net (395→392 failing, 9691→9694 passing), 3 feature commits (B427, B428, B429), ZERO regressions (exact failing-set diffs at every step).** Method: re-confirmed the contained DIFF/SWAP/MISSING pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; mine_small_diffs empty — every error-line-diff test plan-mentioned). Ran a 14-candidate deep code-path-tracing **Workflow** (15 read-only agents, ~2M tokens, 33 min): the agents UNIFORMLY rated everything `architectural` — INCLUDING the 3 tests I then landed via corpus-FP analysis (the recurring over-classification; even the synthesizer's "no genuine surgical win in this batch" was wrong). **The productive technique (now a CLAUDE.md gotcha): for a checkJs TS2339/TS2322 the agent calls architectural, grep the corpus for the exact syntactic shape's RARITY — if 1 file shares it, gate a dedicated walker to it (FP-safe by construction).** **B427 (+1, 9692/394) — jsFileCompilationBindDeepExportsAssignment (TS2339 CommonJS deep-exports-read)**: `exports.a.b.c = 0` READS `exports.a` (receiver of a deeper access) which was never directly declared via `exports.a = …` → TS2339 "...on type 'typeof import(\"a\")'." New `checkJsModuleExportsDeepReads` gated to the deep-read shape (corpus-unique). **B428 (+1, 9693/393) — jsCheckObjectDefineThisNoCrash (TS2339)**: `Object.defineProperty(this,\"X\",…)` in a checkJs class ctor does NOT declare X (per tsc), so a later `this.X` read → TS2339. New `checkJsObjectDefinePropertyThisReads` gated to a ctor containing `Object.defineProperty(this,…)` (corpus-unique). **B429 (+1, 9694/392) — ambientPropertyDeclarationInJs (TS2322 + TS2339)**: a checkJs class `declare <prop>: T` (illegal — TS8009/TS8010) STILL types `prop` as T, so `this.prop = {}` (TS2322) and `this.prop.foo` (TS2339 via getApparentType/getPropertyOfType) are checked against T. New `checkJsAmbientDeclaredClassProperties` gated to a class with a `declare`-annotated property (corpus-unique). **ATTEMPTED + REVERTED (skip-logged):** optionalParamTypeComparison (fn-vs-fn optional-`|undefined` widening — TS `getBestMatchingType` asymmetry, line-4 fixes/line-5 intractable); dottedModuleName (TS2391 `=>`-recovery suppression is correct + FP-safe, but a 2nd FP TS2304 from cross-block dotted-namespace merge blocks the flip — Blocker #3; the triage agent flagged it surgical-1piece but MISSED the 2nd FP); B430/jsFileClassPropertyType2 (inline `@type {number[]}` on a JS-class-ctor `this.X=[]` — honoring it regresses thisInFunctionCallJs's same-file array-method contextual-`this` TS2683, a latent array-method-contextual-typing gap, Blocker #2-adjacent). NEXT: contained pool genuinely exhausted (re-confirmed by my own analysis AND a 15-agent workflow); remaining is multi-session Blocker BUILDs — array-method-return/contextual-`this` inference (#2, highest yield, net-zero-prone), JS-expando-function modeling (#5, 13-file FP surface), cross-file dotted-namespace merge (#3), utility-type variance/materialization.

**Session 2026-06-15 (round 165, multi-iteration session, ≥20 iterations per user request, addressing blockers) — +3 net (398→395 failing, 9688→9691 passing), 3 feature commits (B424, B425, B426), ZERO regressions (exact failing-set diffs at every step).** Method: independently re-confirmed the contained pool exhausted — find_candidates EXTRA/MISSING/SWAP fresh=0; the fresh NONE pool (13 tests) all JS-modeling (maxNodeModuleJsDepth/knockout/jsCheckObjectDefineThis/jsFileCompilationBindDeepExports) or large mapped/conditional/generic-inference; an inline message-length + diff-size ranker over all 396 failing XMLs surfaced the closest DIFF tests the ≤3-code window misses. Ran a 5-agent READ-ONLY triage fleet over 40 small NONE candidates (flow/JS, mapped/indexed, inference/rest-param/optional, crash/literal, module/decl) + ~15 manual deep-reads of DIFF tests — ALL confirmed multi-piece/architectural except the 2 wins. (Agents again systematically over-classify CONTAINED; only inexistentPropertyInsideToStringType survived as genuinely surgical, with a corpus-exhaustive FP measurement.) **B424 (+1, 9689/397) — inexistentPropertyInsideToStringType (TS2339).** A top-level constructor-style `function NAME(){…}` in a checkJs `.js` file has `this` typed as the function NAME; its members are the `this.X=` writes in its own body. Reading `this.Y` where Y is neither written nor a runtime property → TS2339 "Property 'Y' does not exist on type 'NAME'." (`function toString(){ this.yadda; this.someValue="" }` → TS2339 on `yadda`). New dedicated walker `checkJsConstructorThisReads` (Checker.kt) — NOT an un-gating of the load-bearing `.js` checkPropertyAccess skip (B152/B153). FP firewall: fires only when the fn has ≥1 `this.X=` write AND its body is EXCLUSIVELY top-level `this.<id>` bare-read + `this.<id>=…` write expr-statements (BAILS on any other statement/expression shape — nested fns rebinding `this`, control flow, calls); reuses `collectConstructorThisAssignments`. Corpus-exhaustive: exactly 4 checkJs files have a top-level fn using `this`; the other 3 (expandoFunctionContextualTypesJs, controlFlowInstanceof's uglify.js AtTop, jsFunctionWithPrototypeNoErrorTruncationNoCrash's Color) bail or have only writes. **B425 (+1, 9690/396) — enumAssignmentCompat3 (enum-to-enum nominal TS2322).** Assigning one enum-typed variable to another, INCOMPATIBLE, enum-typed variable (`abc=secondAbcd`, `abc:First.E`, `secondAbcd:Abcd.E`). DERIVED TypeScript's nominal enum relation directly from the baseline: two enums relate iff they share a SIMPLE name AND every source member is present in the target with an EQUAL value; a const enum relates only to itself; different simple names → no chain. The chain reports the FIRST failing source member: missing → "Property 'X' is missing in type 'TGT'."; differing value → "Each declaration of 'E.X' differs in its value, where 'V_tgt' was expected but 'V_src' was given." Type names are qualified (`Namespace.E`) ONLY when src and tgt share a simple name (TypeScript's relative-name disambiguation; `Nope` vs `E` → simple). New `checkEnumToEnumAssignments` (additive, nominal — NO relation engine; reuses `evalEnumConstExpr`, new `collectEnumsForAsg` merging namespace-nested + multi-block enums). String/un-evaluable enums → values=null → conservatively assignable (FN, no FP). Squiggle = LHS identifier. Disjoint from checkEnumLiteralAssignments (literal RHS) + checkNamespaceEnumUnionAssignments (union annotations). The remaining enum tests are each broad multi-piece (compat/compat2 = `typeof Enum` value-position object typing [enum-value currently typed as the union, not the static object — broad change]; compat6 = string-enums + function-param vars + IIFE-nested-scope enum shadowing + the `import("f")` cross-file qualified display [4-5 sub-features]; compat7 = enum-relation wired into TS2416 method-override + TS2394 overload-impl + namespace-qualified-enum-member TS2339 [3 walkers]) — deferred. **B426 (+1, 9691/395) — spreadUnionPropOverride (object-literal spread support + TS2783/TS2785).** A genuine blocker BUILD (per the user's "address blockers" directive). TWO coupled pieces, both landed: (1) `getTypeOfObjectLiteral` is now spread-aware — the old `// spread not yet supported` branch ignored spreads, FALSE-positive'ing TS2345 "Property 'label' is missing" at `fun({id, ...find()??{label}})`. New `spreadGuaranteedProps(t)` returns a spread's GUARANTEED (required, non-optional) props: a single object → its required props; a UNION → props required in EVERY NON-NULLISH constituent (spreading undefined/null is a no-op, so nullish members are filtered — this is the KEY insight that avoided needing a `??` nullish-removal change: `(undefined|Thing)|{label}` → filter undefined → intersect {Thing,{label}} → guaranteed={label}). The merge is additive (only adds props not already explicit). (2) New full-tree walker `checkSpreadPropertyOverrides` emits TS2783 + TS2785-related for an explicit object-literal property appearing BEFORE a spread whose type GUARANTEES it (`{x:1, ...({x,y}|{x,z})}` → TS2783 on x). FP firewall: the spread must GUARANTEE the prop — `{y}|{y,x}` (x only sometimes) and optional `{a,b?}` do NOT fire. Squiggle = whole property assignment (`x: 1`, via expressionTrueEnd); TS2785 related at the spread `...` (`expression.pos - 3`). The walker recursion is conservative (statement + common-expression positions, BinaryExpression iterative) — FN-safe (missing a position drops a diagnostic, never adds a wrong one). ZERO regressions. **Confirmed-architectural this round (skip-logged below):** allowJscheckJsTypeParameterNoCrash allowJscheckJsTypeParameterNoCrash (generic-fn-alias display `WatchHandler<any>` vs unfolded — alias-display Blocker B58.x); emitBOM (@emitBOM output-feature + re-parsed-BOM baseline); esModuleInteropPrettyErrorRelatedInformation (export= namespace-import + TS7038 not-callable); interfaceAssignmentCompat (fn-arg structural + enum-static-member); duplicateLocalVariable1/2 (for-in-var string typing + cross-loop var-redeclaration + operator checks); protectedMembers (TS2415/2445/2446 multi-code); noImplicitAnyLoopCrash (spread-arg TS2556/TS2488 + evolving-array flow); dottedModuleName (=>-body parser-recovery TS2391 + cross-namespace-merge TS2304); conflictingDeclarationsImportFromNamespace1 (lodash export= re-export chain, Blocker #3); indexSignatureInOtherFile (value-position-extends false-TS2314 + cross-file merged-member TS2411); allowImportClausesToMergeWithTypes (type-default+value-default merge + TS2749). NEXT: contained pool genuinely exhausted (re-confirmed independently); remaining is multi-session Blocker BUILDs — JS-modeling/JSDoc #5, control-flow #1, generic-inference #2, cross-file scope #3, utility-type materialization (Record/Partial/generic args), and enum `typeof`-object modeling.

**Session 2026-06-15 (round 164, multi-iteration session, ≥20 iterations per user request, addressing blockers) — +2 net so far (400→398 failing, 9686→9688 passing), 1 feature commit (B423), ZERO regressions (exact failing-set diff: pre=400, post=398, only the 2 targets flipped).** Method: independently RE-confirmed the contained pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; mine_small_diffs → every error-line-diff test already plan-mentioned) by directly deep-reading ~30 candidates — ALL multi-piece: utility-type materialization (`Record<string,V>` for emptyObjectNotSubtype…, `ReturnType<T[M]>` for genericConditional…); deep conditional/indexed-access/Parameters resolution (signatureCombiningRestParameters3/4, recursiveTupleTypeInference); instantiation-expression types + the `comparable` relation chain (aliasInstantiationExpression…NoCrash1/2 TS2352); flow narrowing wired into the `in`-check + FP surface (inKeywordAndUnknown TS2638 — the agent's "1-piece" was the forbidden `getTypeOfIdentifier` broadcast); 3-piece ambient class+function merge (constructorOverloads4); JS-modeling/JSDoc Blocker #5 (maxNodeModuleJsDepth…, jsCheckObjectDefineThisNoCrash, jsFileCompilationBindDeepExportsAssignment, redefineArray=ArrayConstructor lib, uniqueSymbolJs2=JSDoc unique-symbol); cross-file Blocker #3 (tsxResolveExternalModuleExportsTypes, noSymbolForMergeCrash); `Object.freeze` literal-preservation (objectFreezeLiteralsDontWiden); the TS-1 two-phase pre-emit/post-emit diagnostic model we lack (multiLinePropertyAccessAndArrowFunctionIndent1, parseErrorIncorrectReturnToken). **Re-learned (recurring): the candidate-finder/closeness ranking conflates the errors-baseline line count with the actually-failing test VARIANT — many "1-line" tests fail on their JS-EMIT variant (importInsideModule, importHelpersES6, symbolLinkDeclarationEmitModuleNames), not the errors variant.** Two read-only agent fleets RE-confirmed the over-classify-CONTAINED lesson (agents focus on emission MECHANICS — "just forward AsExpression to the TS2352 emitter", "add a @callback synthesizer" — and miss that the diagnostic CONTENT needs deep type resolution / a JSDoc-type-to-TypeNode bridge that doesn't exist). **B423 (+2, 9688/398) — jsdocResolveNameFailureInTypedef (TS2304) + jsdocPropertyTagInvalid (TS2552).** An UNRESOLVABLE single-identifier type name in a JSDoc `@typedef {Name}` / `@property {Name}` tag (checkJs `.js` file) emits TS2304 (`@typedef {CantResolveThis}`) or TS2552 ("Did you mean 'string'?" for `@property {sting}`). New `checkJsDocTypeNameResolution` (Checker.kt ~16136) raw-scans `@(?:typedef|property)\s*\{\s*(IDENT)\s*\}` (single bare identifier ONLY → auto-skips `{a|b}`/`{import(…)}`/`{{…}}`/`{A<T>}`) and resolves IDENT against `JSDOC_TYPE_PRIMITIVE_NAMES` ∪ KNOWN_GLOBALS ∪ file binder-locals ∪ sibling `@typedef`/`@template` names; emits TS2552 (with `getSpellingSuggestionFromNames`) else TS2304. **FP firewall — corpus-EXHAUSTIVE**: a grep over ALL fixtures showed the ONLY unresolvable single-identifier `@typedef`/`@property` types are these 2 targets (every other is `Object`/`number`/`string`/`boolean`/`object`/`function`, all in the known set). **The clean +2 (not a SWAP) required a COMPANION fix**: both tests were ALREADY emitting FP TS2304 on `@param {Ty}`/`@param {MyType}` because the binder does NOT bind `@typedef` names, so the EXISTING JSDoc-param type-name resolver (`checkUnresolvedInType` via `checkUnresolvedNames`) wrongly flagged a valid typedef reference. Fixed by registering each file's `@typedef NAME` via `fileScope.addType()` in `checkUnresolvedNames`' `.js`/`.jsx` branch (Checker.kt ~17660). The DEBUG marker (code 29999) trace was load-bearing here — it PROVED my new scan matched only `sting`/`CantResolveThis` and that `Ty`/`MyType` were in `known`, isolating the FP to the separate pre-existing @param path. Reusable infra: `checkJsDocTypeNameResolution`, `JSDOC_TYPE_PRIMITIVE_NAMES`, and the `@typedef`-name `fileScope.addType` registration (the FIRST place `@typedef` names resolve as types — extend for `@param`/`@returns`/`@type` name-resolution or constrained `@template` instantiation when a consumer appears). NEXT (queued for this session): more JSDoc slices (expressionWithJSDocTypeArguments = TS8020/TS17019/TS17020 parser-syntactic JSDoc-in-.ts, candidate; jsdocCallbackAndType = @callback synthesis, medium) + noSymbolForMergeCrash (TS2649 cross-file type-alias-vs-namespace, agent rates medium).

**Session 2026-06-14 (round 163, multi-iteration session, ≥20 iterations per user request, addressing blockers) — +1 net so far (402→401 failing, 9684→9685 passing), 1 feature commit (B421), ZERO regressions (exact failing-set diff).** Method: independently re-confirmed the contained DIFF/SWAP/MISSING pool exhausted (find_candidates EXTRA/MISSING/SWAP fresh=0; mine_small_diffs.py → every error-line-diff test already plan-mentioned). Investigated the TS5107 ×14 cluster — DEAD END (the deprecation diagnostic itself works; the single-TS5107 failures `declarationEmitAmdModuleNameDirective`/`downlevelLetConst13`/`importedAliasesInTypePositions`/`dynamicImportWithNestedThis_es2015`/`systemModule17` all fail on AMD/UMD/System **JS-emit** output, tsgo-irrelevant). Traced ~20 NONE candidates incl. all 5 TS18048-only tests (discriminateWithOptionalProperty4 = forbidden getTypeOfIdentifier-narrowing per round 162; implicitConstParameters = closure-effectively-const flow + no bare-id-receiver TS18048 path = 2-piece; unionOfArraysFilterCall = union-of-tuples callback typing; specialIntersectionsInMappedTypes = Record materialize + noUncheckedIndexedAccess; contextuallyTypedOptionalProperty = contextual generic inference) — ALL multi-piece. Ran a 14-candidate adversarial-triage **Workflow** (read-only agents, stopped early after the investigation stage produced concrete sketches; refutations didn't finish): 4 CONTAINED claims surfaced (noInferCommonPropertyCheck1, strictOptionalProperties3, overloadOnConstInheritance2, unionSubtypeReductionErrors) — I independently judged 3 over-classified (noInfer = needs Blocker #2 generic inference of `T=Partial<{a,b}>` from arg1 + NoInfer/Partial materialization which resolve to errorType today; unionSubtypeReductionErrors = TS2590 complexity-budget needs the exact element-position 5006,9 threshold = fragile; strictOptionalProperties3 = TS2375 needs JSDoc @typedef raw-source parsing, Blocker #5). The recurring agent-fleet lesson holds (over-classify CONTAINED). **B421 (+1, 9685/401) — overloadOnConstInheritance2 (TS2430 overloaded-base interface-extends mismatch).** A derived interface re-declaring an OVERLOADED base method with a single incompatible signature incorrectly extends the base. `interface Base { addEventListener(x:string):any; addEventListener(x:'foo'):string }` + `interface Deriver extends Base { addEventListener(x:'bar'):string }` → derived `(x: "bar") => string` not assignable to base overload set `{ (x: string): any; (x: "foo"): string; }`. The existing `checkInterfaceExtendsClauses` method branch fired ONLY for single-base-decl arity mismatches (`baseProp.declarations.count{MethodDeclaration}==1`); new `else if (size>=2)` branch + `tryEmitTS2430OverloadSetMismatch` (same-arity, bivariant param check, finds the first base overload with a genuinely-unrelated param, renders the 4-line chain `Types of property '…' are incompatible. / Type '<derivedSig>' is not assignable to type '<baseSet>'. / Types of parameters 'x' and 'x' are incompatible. / Type '"foo"' is not assignable to type '"bar"'.`) + `methodSigDisplayColon` (colon-form `(x: string): any`). **FP firewall (load-bearing): base has ≥2 method decls AND derived has EXACTLY ONE decl of the name.** A derived re-declaring the FULL overload set (overloadOnConstInheritance1, derived count 2) is compatible → skipped; single-base-decl (inheritance3) and class-implements (inheritance4) take other paths; generic/type-param methods skipped. ZERO regressions (exact diff: only overloadOnConstInheritance2 flipped). Reusable infra: `tryEmitTS2430OverloadSetMismatch` / `methodSigDisplayColon` (extend for arity-differing overloads or return-type-driven mismatches if a consumer appears). **B422 (+1, 9686/400) — strictOptionalProperties3 (TS2375 JS @typedef exactOptionalPropertyTypes).** `/** @type {A} */ const a = { value: undefined }` in a `.js` file where the `@typedef` declares `value` OPTIONAL with a non-undefined type → TS2375 under exactOptionalPropertyTypes (you can't assign `undefined` to an optional-but-non-undefined property). The `@typedef` is NOT bound anywhere (raw-source only — there is no JSDocTypedef AST node), so new `resolveJsTypedefOptionalProps` scans the JSDoc comments for BOTH forms: `@typedef {object} A` + sibling `@property {number} [value]` tags (`[..]` = optional), and inline `@typedef {{ value?: number }} B` (`?` = optional). `tryEmitJsTypedefExactOptional` checks each object-literal `prop: undefined` against the typedef's optional non-undefined prop, emits TS2375 + the 2-line chain (`Types of property 'value' are incompatible. / Type 'undefined' is not assignable to type 'number'.`) at the var NAME (squiggle len = name). Added as a `run {}` block in `checkVarDeclAssignability` right after the B286 `@type {Object}` block. FP firewall (nil surface): exactOptional+strictNull + JS file + `decl.typeFromJSDoc` + UNBOUND `@type {Name}` TypeReference (globals/currentFileLocals null) + object-literal init + a `prop: undefined` value matching an OPTIONAL non-undefined typedef prop; corpus grep found NO other JS+@type+exactOptional test. strictOptionalProperties4 (sibling, empty baseline) uses `({})` with no undefined prop → no fire. ZERO regressions. Reusable infra: `resolveJsTypedefOptionalProps` (the only JSDoc `@typedef`-shape resolver in the codebase — extend for required-prop / nested-typedef / `@param`-shape checks when a consumer appears).

**[Round 163 candidates triaged + skip-logged — do NOT re-investigate]:** Beyond the 2 wins, these were traced this round and confirmed multi-session-blocker: the 5 TS18048-only tests (implicitConstParameters = closure-effectively-const flow + missing bare-id-receiver TS18048 path = 2-piece; unionOfArraysFilterCall = union-of-tuples callback element typing; specialIntersectionsInMappedTypes = Record materialize + noUncheckedIndexedAccess; contextuallyTypedOptionalProperty = contextual generic inference; discriminateWithOptionalProperty4 = forbidden getTypeOfIdentifier narrowing); the JS-expando TS2339 cluster (expandoFunctionNestedAssigments = function-expando member collection at valid-vs-nested-function positions; jsFileCompilationBindDeepExportsAssignment = CJS `exports.a.b.c` module.exports modeling; inexistentPropertyInsideToStringType = this-expando in plain JS function — all Blocker #5, touch the load-bearing `.js` skips); `noInferCommonPropertyCheck1` (TS2559 needs Blocker #2 inference of `T=Partial<{a,b}>` from arg1 + NoInfer/Partial materialization which resolve to errorType today); `unionSubtypeReductionErrors` (TS2590 complexity budget — needs the exact element-position 5006,9 threshold, fragile); `asyncIteratorExtraParameters` (5-piece: computed-symbol members + iterability validation + AsyncIterable real lib signature + async-generator-return inference); `multipleBaseInterfaesWithIncompatibleProperties2`/`intersectionsOfLargeUnions` (utility-type/large-union type-engine); the TS5107 ×14 cluster (AMD/UMD/System JS-EMIT output diffs, tsgo-irrelevant; the TS5107 deprecation diagnostic itself works). The contained pool remains genuinely exhausted; the surgical wins this round came from two narrow FP-safe detectors (overload-set TS2430, JS-typedef TS2375), the recurring productive pattern.

**Session 2026-06-14 (round 162, multi-iteration session, ≥20 iterations per user request, addressing blockers) — +1 net so far (403→402 failing, 9683→9684 passing), 1 feature commit (B420), ZERO regressions (exact failing-set diffs).** Method: confirmed EXTRA/MISSING/SWAP fresh = 0; the line-level diff miner (`mine_small_diffs.py`) shows the closest DIFF tests are ALL already plan-mentioned (2-piece/architectural). Two read-only research-agent fleets (14 candidates) + direct deep-reads: every closest-miss is 2-piece, flow-blocked, shared-display-path-risky, or tsgo-irrelevant. **Lessons on the agent fleet (recurring): they systematically over-classify CONTAINED, miss tsgo-relevance (`keyofDoesntContainSymbols` = `@keyofStringsOnly`, a removed option), miss CLAUDE.md gotchas (one proposed wiring narrowing into `getTypeOfIdentifier` — the EXACT high-risk change CLAUDE.md forbids), and over-simplify multi-diff tests (`namespaceDisambiguationInUnion` looked like a 1-line display fix but has 4 entangled diffs: union-member display + sub-chain constituent choice + tuple display + tuple-length message wording).** Per-candidate verdicts this round (do NOT re-triage): `arrayOfSubtypeIsAssignableToReadonlyArray` = bivariant-param elaboration under `@strict:false`, CONTAINED but touches the SHARED `getFunctionMismatchElaboration` path (regression risk across all strict:false fn-mismatch tests); `classPropertyErrorOnNameOnly` = needs class-property-initializer contextual-typing + assignability (broad new surface); `typePredicateStructuralMatch` = generic-predicate narrowing (Blocker #2, 2-piece); `inheritance1` = 4100-line cascade (NOT a display swap); `didYouMeanElaborations*`/`abstractPropertyNegative` = multi-piece; `discriminateWithOptionalProperty4` = ternary-condition narrowing FP (Blocker #1 — the agent's `getTypeOfIdentifier` fix is the forbidden broadcast); `mergeSymbolReexportInterface` = cross-file merge ORDER (Blocker #3 deferred by B412); `contextualTupleTypeParameterReadonly`/`inferFromGenericFunctionReturnTypes1` = generic inference (Blocker #2). **B420 (+1, 9684/402) — returnInConstructor1**: a class constructor returning an OBJECT LITERAL or a NEW expression whose type is not assignable to the instance type now emits TS2322 + TS2409 (the primitive/type-param/null return cases were already covered). Object-literal: per-property TS2322 at the property KEY + TS6500 'expected type comes from property X', plus one TS2409 at the `return` keyword. New-expression: whole-type TS2322 + a LOCALLY-built 'Types of property X are incompatible' chain (`buildCtorNewReturnChain`, gated to a single-property mismatch — bounds the new-expression FP surface) + TS2409. **Two load-bearing lessons (now in CLAUDE.md):** (1) the local chain builder renders a no-return-body method as `() => void` because the global `getTypeOfSymbol` defaults such methods to `() => any` and broadening THAT now regresses 5 accessor/override tests (the old "net-zero" CLAUDE.md note was stale) → void inference is DISPLAY-ONLY; (2) eagerly resolving every class's declared type in the walker poisons type caches by first-touch ordering (regressed getAndSetNotIdenticalType2/3 + inheritanceMemberAccessorOverriding*) → resolve the class type ONLY when a ctor actually returns an obj-lit/new-expr. Reusable infra: `checkConstructorReturnObjectOrNew` / `buildCtorNewReturnChain` / `renderSymbolTypeWithVoidInference`.

**Session 2026-06-14 (round 161, multi-iteration session, ≥20 iterations per user request, addressing blockers) — +4 net (407→403 failing, 9679→9683 passing, ~96.0%), 3 feature commits (B418a, B418b, B419), ZERO regressions at every step (exact failing-set diffs).** Method: a line-level closeness analyzer over all 407 failing XMLs (counts every differing diff line, not just `error TS` summary lines) ranked the genuinely-closest DIFF tests that the ≤3-code `find_candidates` window misses. Parallel READ-ONLY research agents traced candidates and proposed FP-safe fixes; I made the go/no-go and verified. **LESSON (re-learned the hard way): a research/triage agent must NOT run `./gradlew --tests` — it wipes the full-suite XMLs (CLAUDE.md gotcha) and collides with the background full-suite run, corrupting the failing-set diff.** The non-JS/non-parser contained pool is otherwise exhausted (every remaining DIFF/NONE candidate triaged to JS-modeling/JSDoc Blocker #5, parser-recovery Blocker #7, cross-file merge-order Blocker #3, generic-inference Blocker #2, or flow Blocker #1).

**B418a (+1, 407→406) — heterogeneousArrayAndOverloads (TS2769 best-matching-overload collapse).** When ALL overloads of a call fail AND ≥1 overload fails on MULTIPLE array-literal elements, tsc reports ONLY the overload with the FEWEST per-element diagnostics (the single best candidate) and anchors the squiggle at that overload's failing element — `chooseOverload`'s `max>1 ? allDiagnostics[minIndex] : flatten`. We were emitting every failing overload + anchoring at the method name (B280 path). New `countFailingArgDiagnostics` counts failing array-literal elements per overload; `checkArgumentsAgainstOverloads` collapses to the min-count overload (`reported`) when any overload has ≥2 element mismatches, routing the chain, position, and related-info through `reported`. Position drilling to the element already existed (`findInnerMismatchPosition`). FP firewall: collapse triggers ONLY when an array-literal arg fails ≥2 elements (every other multi-overload case keeps `maxCount==1` → the flatten path, unchanged); corpus-verified sole consumer of the collapsed form.

**B418b (+1, 406→405) — assignmentCompat1 (index-signature assignability).** Two FP-safe relation-engine fixes. (1) `propertiesRelatedTo`: a source INDEX SIGNATURE does NOT satisfy a target's required NAMED property — removed a spurious shortcut that treated a source string-index-sig as providing every named prop (so `{[k:string]:any}`→`{one:number}` wrongly passed; the number-index-sig source already correctly failed). tsc looks up the source prop via getPropertyOfType which never synthesizes a named prop from a source index sig → both now TS2741. (2) `structuredTypeRelatedTo`: a primitive `string` IS assignable to a target whose only index sig is NUMERIC (`{[n:number]:any}` — "string has numeric indexer", via String's apparent-type numeric index sig) but NOT a string-index target (String has no string indexer); `boolean`/`number` reject both. New tightly-gated branch (anonymous index-sig target + primitive source) requires the source apparent type to supply a matching index sig for EACH index sig the target needs (a string-index source also covers a numeric-index target). Both only ADD correct results / report genuine misses. **NOTE: my first cut of (2) delegated to `checkTypeRelatedTo(apparent, target)` and OVERSHOT** (made `string→{[s:string]:any}` and `boolean→{[n:number]:any}` wrongly pass — objectTypeRelatedTo's index-sig check doesn't enforce source index-sig PRESENCE); the precise presence-checking branch is the fix.

**B419 (+2, 405→403) — jsFileClassPropertyType + jsFileClassPropertyType3 (JS class expando instance properties, Blocker #5).** A class in a `.js`/checkJs file declares instance properties via `this.X = <expr>` in its constructor; tsc synthesizes those as real members (typed from the RHS) so external `(new C()).X` from a .ts file sees the property → TS2322 on a bad write, not TS2339. `resolveInterfaceMembersCore` now synthesizes these for JS-file classes: scan the constructor (recursing into if/else + blocks) for `this.<id> = expr`, infer the type from the RHS value(s) (widened; multiple assignments drop nullish members when a non-nullish one exists, e.g. `this.p=null`/`this.p=0` → number), add a Property symbol with the inferred type cached in `symbolTypes` (which `getTypeOfSymbol` reads first). Explicit/inherited members win. **GOTCHA: the NewExpression-receiver TS2339 branch in `checkMemberAccessMissing` uses AST-based `lookupInstanceMemberInResolvableChain` (NOT the resolved Type.Interface members), so it needed a companion guard `classHasConstructorThisAssignment`.** Gated to JS-file classes (`jsFileClassDecls`, an identity Set built from JS-like binderResults — `resolveDeclarationSourceFile` is unreliable in multi-file since positions are per-file) + the simple `this.<id>=expr` shape. JSDoc `@type` on the assignment is NOT parsed → the `@type {number[]}` sibling jsFileClassPropertyType2 stays failing (infers `any[]`). Reusable infra: `jsFileClassDecls`, `collectConstructorThisAssignments`, `inferJsExpandoPropType`, `classHasConstructorThisAssignment` — extend for prototype-assignment / JSDoc-typed expando when a consumer appears.

**Session 2026-06-14 (round 160, multi-iteration session, ≥20 iterations per user request, addressing blockers) — +3 net (410→407 failing, 9676→9679 passing), 3 feature commits (B415 additive excess; B416 + B417 Blocker #1 control-flow narrowing — FULLY healed the switch-true cluster), ZERO regressions at every step (exact failing-set diffs).** Method: confirmed the contained pool architectural (~40 candidate deep-reads), then committed to the Blocker #1 switch-true cluster per the user directive, harvesting it to completion. The cluster (`narrowByClauseExpressionInSwitchTrue3/6/7`) is now 0-failing (test7=B411 prior round, test6=B416, test3=B417 this round).

**B417 (+1, 9679/407) — narrowByClauseExpressionInSwitchTrue3 (Blocker #1 BUILD).** test3 narrows the scrutinee PAST a switch: `switch (true) { case shape.kind === "circle": return … }` then `if (shape.kind === "circle")` — reaching past the switch means circle didn't match, so shape is `{square}`, and the if-condition is a no-overlap comparison. Baseline = 3 errors (2× TS2367 on the if/else-if conditions + 1 TS2339 'radius does not exist on never' in the first if body). Two pieces: **(1) binder post-switch exhaustiveness narrowing** (`Flow.kt bindSwitchStatement`): when a switch has NO default, the no-match path reaching `postSwitch` did so with EVERY case condition false → narrow the no-match flow by each case-expr-false (chained `newCondition(false, …)` over `preSwitch`) instead of joining bare `preSwitch`. NET-ZERO standalone (full-suite verified) and it ALONE made test3's TS2339-on-never fire (the if body narrows `{square}` by circle → never → `shape.radius` TS2339). For a non-discriminant scrutinee each FlowCondition narrows nothing (harmless). **(2) `checkFlowNoOverlapCondition` + `emitFlowNoOverlap` + `propTypeLiteralSet`** (hooked into BOTH if-statement assignability dispatchers at 64893 + 66084, which carry param + flow context via checkFunctionBody → currentLocalTypes): flow-aware TS2367 for a discriminant comparison `recv.prop === literal` (or `!==`/`==`/`!=`, symmetric, recursing `&&`/`||`) in an if/else-if condition where flow narrowing has reduced `recv` so `recv.prop` can no longer be the literal. Gets the declared recv type via `getTypeOfExpression`, narrows via `getNarrowedTypeForReference` (needs `getFlowAt` → `currentFlowGraph`, set by the assignability driver). **FP firewall** (load-bearing): fires only when the DECLARED prop's literal set INCLUDED the compared literal but the NARROWED set does NOT (a genuine flow-INDUCED no-overlap — statically-impossible comparisons stay owned by the existing const-literal / typeof TS2367 sites), AND the narrowed prop is an all-literal set, AND `narrowed !== declared` (narrowing actually changed something). Displays the narrowed prop literal(s) vs the compared literal (`"square"` and `"circle"`). The switch's own 2nd case (orig line 13) correctly emits NOTHING (the baseline doesn't want it — only the post-switch if/else-if). **Reusable infra**: the post-switch-exhaustiveness binder narrowing (correct for ANY no-default switch); `checkFlowNoOverlapCondition`/`propTypeLiteralSet` (flow-aware discriminant no-overlap — extendable to while/ternary conditions and other comparison positions if a consumer appears).

**B416 (+1, 9678/408) — narrowByClauseExpressionInSwitchTrue6 (Blocker #1 BUILD).** After confirming the contained pool architectural, ground-truth-traced the switch-true siblings B411 left failing: test6 emitted a ~13-error CASCADE (TS2322 'C not assignable to never' at the default `const never:never=x` + TS2339 'kind on never' at case EXPRESSIONS + 'aProps on A|C' from the default falling through as C). Its baseline expects EXACTLY 2 TS2339s (lines 70/73, the fallthrough switch). Flipped it with THREE coupled pieces, each verified individually net-zero/zero-regression before combining: **(1) Binder** (`Flow.kt bindSwitchStatement`): a switch's case EXPRESSIONS all evaluate at the switch head before any body runs → bind `clause.expression` at `preSwitch`, not at `currentFlow` (the prior clause body's fallthrough flow, which after a return/break is unreachable `never` → FP TS2339-on-never on the case expr). NET-ZERO standalone. **(2) `narrowByDiscriminantProperty`**: extended to narrow a SINGLE discriminated member (non-Union) to `never` when its own literal discriminant contradicts the comparison — lets sequential negative narrowing reach the exhaustive `never` once a union collapses to one member (fixes default→never; the A|C fallthrough then becomes A|never=A). FP-safely gated to a member that GENUINELY carries the discriminant literal (else returns null → no narrowing, no short-circuit of narrowByEquality's direct-literal path). NET-ZERO standalone (was reverted earlier this round for exactly that reason; re-applied here once the binder gap + display gave it a consumer). **(3) `unionAliasStructural`**: the switch fallthrough rebuilds the FULL `MyType=A|B|C` union via `getUnionType`, which does NOT intern (line 103160 mints `Type.Union(sorted)`), so the result has a fresh `Type.id` and the id-keyed `aliasDisplayMap` misses → we displayed `A | B | C` where tsc shows `MyType`. New member-id-set→name map (registered for NON-generic union aliases whose members are ALL named object types), consulted at the top of `typeToString`'s Union case. A strict SUBSET (`A | B`, line 70) has a different member set → no match → unfolds (matches tsc — line 70 IS `A | B` in the baseline). FP firewall (the `allNamed` gate): primitive/literal unions (`string | number`) share singleton ids corpus-wide and would over-match inline unions. **test3 remains** (case-expr needs prior-cases-FALSE negative narrowing so `case shape.kind==="circle"` after the first case sees `{square}` → TS2367 no-overlap; ALSO the TS2367 check must consult flow narrowing) — multi-piece, deferred. **Reusable infra**: the per-clause case-expr-at-preSwitch binder fix (correct for ALL switches); the single-member→never discriminant narrowing (useful for any discriminated-union exhaustiveness check); `unionAliasStructural` (full-union-reconstruction alias display — a partial answer to the documented B58.3/B58.4 union-alias-display problem, scoped to exact-member-set matches of named-type unions).

**B415 (+1, 9677/409) — nestedRecursiveArraysOrObjectsError01.** Entry 9676/410. Method: fresh full suite → `find_candidates.py --fresh` (EXTRA/MISSING/SWAP=0; NONE=13 across 9 sigs, all architectural on inspection: TS2339×4 JS-expando, TS2344×2/TS2322 mapped-conditional-keyof type-engine, TS2769/TS2345 huge-overload promisePermutations, TS2403+TS2741 fixingTypeParametersRepeatedly2 the documented 2-hard-piece blocker, TS2493+TS2589+TS7010 awaitedType 3-mechanism, controlFlowArrayErrors evolving-array flow; OUTPUT=1 systemModule17 System-module tsgo-irrelevant). Read the smallest NONE candidates (redefineArray=lib ArrayConstructor member-set; uniqueSymbolJs2=JS unique-symbol; mappedTypeWithAsClauseAndLateBoundProperty=full Array-member display; mergeSymbolReexportInterface=cross-file merge-ORDER which-decl-wins, deep Blocker #3) — all multi-piece. **GROUND-TRUTH traced the switch-true siblings** (B411 left them): test6 emits a CASCADE of ~13 wrong errors (TS2322 'C not assignable to never' at the default `const never:never=x` because sequential negative narrowing collapses the union to a single member then `narrowByDiscriminantProperty` bails on non-unions; + TS2339 'kind on never' at case EXPRESSIONS because the binder binds them with the previous clause-body fallthrough flow — the documented binder gap #1) and test3 emits 1 wrong TS2339-on-never + misses all 3 expected (TS2367 no-overlap must consult flow narrowing). Both are multi-piece binder+flow, deferred. **ATTEMPTED + REVERTED (net-zero, no consumer):** extended `narrowByDiscriminantProperty` to narrow a SINGLE discriminated member to `never` on a contradicting comparison (correct tsc behavior, FP-safely gated to a member genuinely carrying the discriminant literal) — full suite +0/−0; it fixes test6's default-never symptom but the test still has ~12 other wrong errors from the binder gap, so no flip and no other consumer → reverted per the no-dead-infra rule (re-apply when the binder case-expression-flow gap lands). **LANDED — B415 (+1, 9677/409) — nestedRecursiveArraysOrObjectsError01** (additive, FP-safe): array-literal excess-property check (TS2353 'jj' "does not exist in type 'Style'") through a recursive `Style = StyleBase | StyleArray` / `interface StyleArray extends Array<Style>` target. `checkArrayLiteralElementExcessProps` previously bailed unless the target was a direct `Type.Reference` to Array. Extended with three helpers: `arrayElementTypeForExcess` resolves the element type of an array-LIKE target (Reference-to-Array | interface-extends-Array — recovering the `Array<X>` arg from the heritage AST via the element SYMBOL, NOT `getTypeFromTypeNode`, because `baseTypes` drops the instantiation to the raw `Array<T>` interface AND the node-level cache holds the errorType-sentinel for the self-recursive alias → `getTypeFromTypeNode(Style)` returns 'any'; `getDeclaredTypeOfSymbol(globals["Style"])` reads the now-complete `declaredTypes` | union-with-exactly-one-array-member); `objectMemberForExcess` returns a union's single non-array object member; and a self-contained Union branch recurses array-literal elements through the array member and excess-checks leaf object literals against the object member, threading the annotation alias name 'Style' (a bare union would unfold to 'StyleBase | StyleArray'). FP-safe (excess is conservative — genuine excess NAMES only; the union descent requires exactly one object + one array member); existing Reference-to-Array/primitive paths untouched. ZERO regressions (exact failing-set diff: +1/−0). **Reusable infra**: `arrayElementTypeForExcess` / `objectMemberForExcess` / `arrayBaseElementTypeFromHeritage` (heritage `extends Array<X>` arg recovery via element symbol — useful for any check needing a named array-interface's element type when `baseTypes` lost the instantiation).

**Session 2026-06-14 (round 159, multi-iteration session, ≥20 iterations per user request, addressing blockers) — +2 net so far (412→410 failing, 9674→9676 passing), 2 feature commits (B413, B414), ZERO regressions (exact failing-set diffs).** Entry 9674/412. Method: re-confirmed fresh EXTRA/MISSING/SWAP=0; a line-level diff analyzer (count ALL differing content lines, not just `error TS` summary lines) surfaced the genuinely-closest tests the ≤3-code `find_candidates` window misses. Findings: the small-diff fresh tests are all 2-piece (`indexSignatureInOtherFile`/`1` = a TS2314-value-position FALSE-POSITIVE [`class Test extends Array1` where `Array1` is both a generic interface AND a `declare var` value → extending the value needs no type args; clean fix but flips nothing alone] + a missing TS2411 symbol-index-inherited-member that needs cross-file merged-member enumeration; `importWithTrailingSlash` = `.`/`..` package-index resolution [resolveModuleSpecifier returns null for these per B412] + cross-file default-import member modeling; `recursiveConditionalCrash4` = the same TS2314-value FP + 2× TS2589 recursive-conditional depth). A 5-agent triage fleet over the closure-narrowing / parser-FP / small-swap / discriminant / recursive clusters confirmed all ARCHITECTURAL except one: the **ADDITIVE elaboration-chain vein** — tests that emit the right top-level TS2322 but MISS chain sub-lines (FP-safe by construction: the new branches fire only on type-pair shapes that previously produced no chain). Landed 2: **B413 (+1, 9675/411) — errorMessageOnIntersectionsWithDiscriminants01**: discriminated intersection→intersection elaboration (`A = Common & {foo:1}` vs `B = Common & {bar:1}`, Common a union). New `getDiscriminatedIntersectionElaboration` distributes the source intersection-of-union into constituents, reports the first failing constituent against the target alias, drills into the discriminant-matched target constituent, names the missing required prop's sub-object; dispatched from `getPropertyElaborationChain` + wired into the assignment-expression chain path (which only built chains for Object→Object / Union sources). **B414 (+1, 9676/410) — unionTypeErrorMessageTypeRefs01**: generic-ref/alias → union elaboration (`A<Foo>` vs `A<Bar> | B<Baz> | C<Kwah>`, both interface-Reference and `type X<T>` alias-Object halves). New assignment-path branch picks the best union constituent and, when it is the SAME generic as the source (same interface target OR same alias name, via `genericIdentityAndArgs`), compares the type ARGS directly and elaborates the first failing pair. ADDITIVE-chain vein now dry (analyzer: remaining candidates are swaps `optionalParamTypeComparison` / deep-recursion `unionTypeWithRecursiveSubtypeReduction2` / architectural). DEFERRED (assessed, FP-safe but net-zero — no clean single-test consumer): TS2314 value-position-extends suppression (3 tests, all 2-piece); optional-TARGET-prop `| undefined` widening in `getPropertyElaborationChain` leaf (`intersectionsAndOptionalProperties` has 4 entangled issues incl. a `let x: To` shadowing bug). Closure-capture narrowing (`implicitConstParameters` — narrow never-reassigned captured vars inside nested arrows + TS18048 for reassigned ones) is the next Blocker #1 piece but needs FlowStart-outer-flow infra (multi-piece, regression-prone) — deferred.

**Session 2026-06-14 (round 158, multi-iteration session, ≥20 iterations per user request, addressing blockers) — +3 net (415→412 failing, 9671→9674 passing), 2 feature commits (B411 Blocker #1, B412 Blocker #3), ZERO regressions (exact failing-set diffs).** Entry state 9671/415 (post-round-157). Method: re-confirmed fresh EXTRA/MISSING/SWAP=0; swept EVERY remaining code cluster by extracting actual-vs-expected straight from the failing XMLs (no per-test gradle re-runs — avoids XML-wipe + daemon contention), confirming the contained pool is genuinely exhausted (parser-recovery=Blocker #7 HIGH-risk bespoke + round-152 revert precedent; `mergeSymbol*` cluster=Blocker #3 blocked on `.`-package-index resolution which `resolveModuleSpecifier` doesn't do; crash tests `checkerInitializationCrash`/`aliasInstantiationExpressionGenericIntersectionNoCrash1/2`=cross-file global-namespace-merge / instantiation-expression-types + intersection-comparability; `constructorOverloads4`=3-piece ambient class+function merge FP + rest-param TS2769 + TS2349; TS5107=AMD/UMD tsgo-irrelevant; `objectFreezeLiteralsDontWiden`/mapped/conditional/`circularResolvedSignature`/`objectGroupBy`=type-engine; `controlFlowForIndexSignatures`/`controlFlowArrayErrors`/switch-true/TS18048=flow). Per the user directive + the standing ~10-session recommendation, committed to **Blocker #1 (control-flow narrowing) BUILD**. **B411 (+1, 9672/414) — narrowByClauseExpressionInSwitchTrue7**: `switch (true) { case <cond>: }` narrowing. New `narrowBySwitchOnTrue`, dispatched from `narrowBySwitchClause` when the switch expression is the literal `true` Identifier. For a `FlowSwitchClause` representing a DIRECT match of clause N: narrow the scrutinee POSITIVELY by clause N's case expression (via the existing `applyConditionNarrowing` — reuses `===`/`!==` discriminant, `instanceof`, `in`, truthiness, `&&`/`||`), and NEGATIVELY by every CASE clause appearing before N (they tested false to reach N); a `default` clause narrows negatively by EVERY case clause (it matches iff none did). Fallthrough between clause bodies is ALREADY modeled by the FlowGraphBuilder's BranchLabel merges (the body entry unions the direct-match `FlowSwitchClause` with the prior body's fallthrough flow), so only the per-clause direct-match narrowing was needed — the `A | B` / `MyType` fallthrough unions fall out for free. This suppresses the FP TS2339/TS2322 we previously emitted on the un-narrowed union across all three switch-true tests. test7 flips cleanly: its sole residual is the exhaustiveness `const never: never = someDerived` where someDerived narrows to `Derived2` (our var-decl assignability path already consults flow narrowing — confirmed empirically, despite the CLAUDE.md `getTypeOfIdentifier`-doesn't-narrow note: a different var-decl code path picks it up). test3 (case-EXPRESSION flow: the binder binds `case shape.kind===...` expressions with the PREVIOUS clause-body fallthrough flow, which after a `return` body is `never`, so `shape.kind` FPs TS2339-on-never; ALSO needs post-switch "no-match" narrowing + TS2367 no-overlap to consult narrowing) and test6 (sequential discriminant narrowing collapses a union to a SINGLE member before the last `!cond`, and `narrowByDiscriminantProperty` bails on non-unions so the single member never reaches `never`; ALSO needs union→alias `MyType` display reconstruction which the Union case of `typeToString` does NOT do) remain — multi-piece, deferred. The only failing tests using `switch (true)` are test3+test6, both infeasible, so the default-exhaustiveness intersection fix would be no-dead-infra. ZERO regressions (exact failing-set diff: +1/−0, only test7).

**B412 (+2, 9674/412) — Blocker #3 (cross-file scope): mergeSymbol cluster.** A name declared in a `declare module "X"` AUGMENTATION that is ALSO re-exported into module X via `export {N} from './other'` → both contribute N to X's shape → duplicate. tsc reports at BOTH the augmentation declaration and the re-export specifier, each with a cross-referencing TS6203 "was also declared here". CODE by augmentation-decl kind: a TYPE-ALIAS aug decl → TS2300 (type aliases never declaration-merge); a block-scoped const/let → TS2451. New `checkModuleAugmentationReexportDuplicates` walker (registered 73h2, gated `binderResults.size > 1`): for each `declare module "spec"` augmentation, resolve `spec` → target file (via new `resolveAugmentationTargetFile`, which handles the `.`/`./` package-index specifier — NOT resolved by `resolveModuleSpecifier` — LOCALLY as the sibling `index.{ts,tsx,d.ts}` in the augmentation file's directory, avoiding a risky global-resolver change), collect the target's `export {N} from './x'` re-export names, and for each augmentation TypeAlias/const-let decl whose name is re-exported, emit the symmetric pair via `emitAugReexportDup`. Augmentation decls are considered only if they MERGE into the module (body with export statements → exported decls only; no module syntax → all decls). The whole `.d.ts` duplicate-identifier pipeline is skipped and the B92d augmentation walker (`checkCrossFileModuleAugmentationDuplicates`) only covers the target's OWN exports with EXPORTED aug decls, so this shape (non-exported aug decl vs a re-export) was uncovered. FP-safe by construction (a re-exported name + a non-mergeable aug decl is always a genuine duplicate). Flips `mergeSymbolRexportFunction` (TS2451 const Row vs re-exported function Row) + `mergeSymbolReexportedTypeAliasInstantiation` (TS2300 type Row2 vs re-exported interface Row2). The 3rd cluster member `mergeSymbolReexportInterface` now correctly gets its 2 TS2300 but STILL fails on a SEPARATE TS2741: `const x: Row2 = {}` should report missing `a` (the augmentation's first-declared `type Row2 = {a}`) but we resolve Row2 to common's re-exported `interface Row2 {b}` and report missing `b` — cross-file merge ORDER (which declaration wins for type resolution), a deeper Blocker #3 issue, deferred. ZERO regressions (exact failing-set diff: +2/−0).

**Session 2026-06-14 (round 157, multi-iteration session, ≥20 iterations per user request, addressing blockers) — CONCLUSION: +2 net (417→415 failing, 9669→9671 passing, ~95.9%), 2 feature commits (B408, B409), ZERO regressions at every committed step (exact failing-set diffs). 1 attempt reverted (B410, no-dead-infra).** Both wins are Blocker #3-adjacent (namespace-local scope) and clear the ENTIRE type-as-namespace / value-as-type / enum-in-type-position family (after this round NO failing test has a TS2702/TS2713/TS2749 baseline). **REVERTED — B410 (allowImportClausesToMergeWithTypes, TS2749 for a value-only default-import used as a type)**: built `aliasResolvesToValueOnly` + `resolveDefaultImportExprTarget` (resolve `import x from "./b"` where b is `export default <identifier>` → the value-only target) and un-gated the `isValueOnlyTypeRef` Alias-suppression. It did NOT fire (the cross-file default-import-of-ExportAssignment resolution returned uncertain) AND the test ALSO needs a SEPARATE TS2528 suppression (a.ts has a TYPE default `export default interface zzz` + a VALUE default `export { zzz as default }` — different spaces, tsc allows both, we wrongly emit "multiple default exports"). 2-piece cross-file test; reverted to avoid FP risk on the broad TS2749 gate with no payoff. **EXHAUSTION RE-CONFIRMED across every vein** (do NOT re-triage): fresh EXTRA/MISSING/SWAP=0; 39-test deep triage (13 agents) all ARCHITECTURAL; the AST+symbol lens (the B409 lesson) checked against the type-as-value family (newOperator/defaultArgsInFunctionExpressions/controlFlowFunctionLikeCircular1 — all multi-error type-engine cascades, NONE-bucket), tslib (importHelpersES6 — helper-completeness + AMD/System resolution), JSDoc (jsdocResolveNameFailureInTypedef/jsdocPropertyTagInvalid — Blocker #5 JSDoc-type-parser), esModuleInterop (esModuleInteropPrettyError — namespace-shape + fn-vs-object assignability), flow (narrowByClauseExpressionInSwitchTrue7/inKeywordAndUnknown/implicitConstParameters — real switch-true/unknown-narrowing/effectively-const flow), generic/never (invalidSplice/exhaustiveSwitchCheckCircularity — need the broad `[]`→`never[]` base-type change or exhaustiveness-to-never, both net-zero-prone), indexed-access (errorElaboration — generic-Container comparison cascade). **NEXT SESSION: the contained pool (incl. the AST+symbol micro-detector vein) is genuinely harvested — commit to a multi-session Blocker BUILD with a revert budget: Blocker #1 a real per-function flow engine (switch-true positive narrowing + unknown→{} + evolving arrays), Blocker #2 generic argument inference (un-gate the arg-check when `tryInferSingleTypeParamFromArgs` returns a FULL mapper — needs a clean `f<T>(x:T,y:T)` consumer + full-suite revert budget), Blocker #3 cross-file default-import/re-export value-vs-type-space resolution (would land B410 once paired with the TS2528 type-vs-value-default-space fix), or the enum-to-enum assignability model (enumAssignmentCompat ×5, 9-32 errors each).** Reusable infra landed this round: `checkTypeUsedAsNamespaceRefs` (the type-as-namespace walker), `resolveTypeRefToEnum` (follows `type X = Enum` but not `typeof Enum`), `lookupTypeAsNsName` (ns-chain → globals/file-locals name resolution). The B410 helpers (`aliasResolvesToValueOnly`, `resolveDefaultImportExprTarget`) were reverted but the approach is documented for a future cross-file session. Entry state 9669/417 (post-round-156). Method: re-confirmed fresh EXTRA/MISSING/SWAP=0; then ran TWO triage fleets (8 + 5 agents = 39 candidates) over the NONE bucket + skip-logged diffs with FRESH eyes and the new B406/B407 infra in mind. **ALL 39 came back ARCHITECTURAL** (per-test verdicts the agents returned: iterator-extra-params=generator-return display infeasible; protected*/overloadOnConst=multi-mechanism + sig-vs-overload-set elaboration; merge/redeclare cluster=Blocker #3 cross-file scope / JS-expando; enum-compat6/7=enum-to-enum relation model; objectFreeze=`Object.freeze` returns `any` not `Readonly<T>`/`readonly T[]`; crash/cast/inference tests=awaited-type engine / generic-inference Blocker #2 / mapped-type eval; uniqueSymbolJs2/inexistentProperty/pushType=JS-expando + `.js` skip Blocker #5; redefineArray=lib ArrayConstructor member-set; noImplicitAnyInCast=cast-mode comparability + TS2728-on-member; controlFlowForIndexSig=flow-into-typeof; inferenceFromIncompleteSource/keyofIsLiteral/indexSignatureOfTypeUnknown=Blocker #2 generic arg inference). The contained micro-detector pool is therefore GENUINELY harvested — DO NOT re-triage these 39 (logged). Per the user's directive to address blockers, the win is a **Blocker #3-adjacent BUILD** (the one semi-contained lead surfaced by triage, completed cleanly):
- **B408 (+1, 9670/416) — errorForUsingPropertyOfTypeAsType01** (Blocker #3 namespace-local scope): a namespace-LOCAL TYPE used as a NAMESPACE qualifier in TYPE position → TS2702/TS2713. The existing `checkTypeNameResolved` walker emits TS2702 only for TOP-LEVEL leftmost types and never distinguishes TS2713 (its leftmost lookup is `globals`/`currentFileLocals` only — a namespace-local `Foo` resolves to null → nothing fires). NEW dedicated walker `checkTypeUsedAsNamespaceRefs` (registered after `checkNamespaceAliasTypeRefs`) walks namespaces tracking the enclosing-ns symbol (via `nodeToSymbol`), resolves each qualified segment through the ns-chain exports, and applies tsc's `checkAndReportErrorForUsingTypeAsNamespace` rule (verified against all 5 namespaces of the test): **LEFTMOST segment is the type** (`Foo.bar`) → `getDeclaredTypeOfSymbol` + `getPropertyOfType(declType, member)` (union = common-property) present → TS2713 (squiggle the whole `Foo.bar`), else TS2702 (squiggle `Foo`); **NON-leftmost segment is the type** (`N.Foo.bar`, reached via namespace `N`) → ALWAYS TS2713 at the member. The leftmost emission is gated to namespace-local leftmosts (`fromNsChain`) so it never double-emits with the existing top-level TS2702 path; `isPureTypeSymbol` excludes Module/NamespaceModule/ValueModule/Enum so clodules + enums stay legal qualifiers (the test's `Color.Red`/enum cases correctly bail). Companion **parser fix**: `parseQualifiedName` now emits TS1003 "Identifier expected." (overrideLength 0) for a missing right side after `.` (`import lol = Test5.Foo.` → TS1003 at len-0 + my TS2713 at the same position). FP-safe by construction (a `Type.member` ref in type position is ALWAYS a tsc error — valid `A.B` has A a namespace). ZERO regressions (exact failing-set diff: +1/−0, only errorForUsingPropertyOfTypeAsType01 flipped). - **B409 (+1, 9671/415) — errorForUsingPropertyOfTypeAsType03** (sibling completion): the B408 walker already produced 4 of 03's 7 errors (c1/c2 TS2702, d1/d2 TS2713). The remaining 3 turned out to be CONTAINED narrow AST shapes (agent F's "architectural" verdict was over-cautious — they need NO full enum value-type modeling, just shape detection + `enum.exports` membership): (1) `Enum.Member.X` (3+ segment QualifiedName, seg0 a real EnumDeclaration, seg1 ∈ enum.exports) → **TS2749** "'Enum.Member.X' refers to a value, but is being used as a type here. Did you mean 'typeof Enum.Member.X'?" (squiggle the full dotted name); (2) `Enum["Member"]` / `AliasToEnum["Member"]` (IndexedAccessType, objectType resolves to an enum directly or via a `type C = Enum` alias, index a string-literal ∈ enum.exports) → **TS2339** "Property 'Member' does not exist on type 'Enum'." (squiggle the `"Member"` index incl. quotes, display the resolved enum name). Plus a fix to the leftmost member-presence check: `type C = typeof Enum` was resolving via `getTypeFromTypeQuery` → anyType (the typeof resolver only checks `globals`, missing the namespace-local enum) → getPropertyOfType missed the members → wrong TS2702; now special-cased to read `enum.exports` directly → TS2713 (d1/d2). `resolveTypeRefToEnum` follows `type X = Enum` (TypeReference body) but NOT `type X = typeof Enum` (TypeQuery body keeps the members, so `C2["Red"]` correctly emits nothing). All FP-safe by construction (an enum-member-name index, or `Enum.Member.X` in type position, is ALWAYS a tsc error). ZERO regressions (exact diff: +1/−0). **LESSON: triage agents over-classify enum/indexed-access-in-type-position as "needs the type engine" — several are pure AST + symbol-table-membership shapes once you notice the member is in `enum.exports`.**

_[Round 156 session note trimmed to PLAN-PHASE-4-HISTORY.md.]_

### Failure impact analysis (upper bounds — assumes feature fully unblocks tests)

| Feature | Diagnostic codes unblocked | "None produced" test impact |
|---|---|---|
| Contextual typing | TS2322 (1267), TS2345 (576), TS2353 (110) | **~600+ tests** |
| Deep structural comparison | TS2322 elaboration chains, TS2741 | **~300+ tests** |
| Control flow narrowing | TS2339 (549), TS2454 (103 deficit), TS2774 (118) | **~250+ tests** |
| Overload resolution | TS2769 (511), TS2349 (161) | **~200+ tests** |

These are UPPER bounds — a test usually needs multiple features. Realistic gain per feature: 30-50% of the upper bound.

### QUEUE — prioritized by unblocking potential, then by implementation cost

Completed queue items (B7.x through B38.x and 17.x series) have been archived
to `PLAN-PHASE-4-HISTORY.md` § "Queue — completed items (B/17.x series)". The
session log above (`Session 2026-05-17 ...`) and `STATUS.md` together carry the
recent narrative; consult the history file when you need the original
checkbox-item rationale.

- [ ] **16.4. Generic type instantiation and inference (MEDIUM — ~80 tests realistic) — IN PROGRESS**
  _Per-item session notes through round 119 are archived in `PLAN-PHASE-4-HISTORY.md` (§ "Completed items (16.0–16.3) — full notes"); current session notes live at the top of this file's Phase 16 section._

- [ ] **B435-OBJECT-FREEZE (PROMOTED 2026-06-15 round 167 — bounded type-engine blocker, 2 tests).** Model `Object.freeze(x)`'s return type so the readonly + literal-preservation diagnostics fire. Targets: **`objectFreeze`** (3 errors: TS2540 `o.b=` on a frozen object property; TS2542 + TS2322 on `a[0]=` of a frozen array) and **`objectFreezeLiteralsDontWiden`** (1 error: TS2322 `'latest'` vs `'1011831'` — frozen object preserves literal prop types and `let x = frozen.prop` does NOT widen the non-fresh literal). **Pieces (each verified-needed this round):** (1) `tryResolveObjectFreezeCall` in `getReturnTypeOfCallExpression` (~76875, next to `tryResolveArrayFromCall`): `Object.freeze(objLit)` → the obj-literal `Type.Object` with members in `mappedReadonlyMemberIds` (reuse B120 infra); `Object.freeze(arrLit)` → `ReadonlyArray<elem>` (use `globalReadonlyArrayType`); `Object.freeze(fn/class)` → arg type unchanged (the `f`/`c` cases expect NO error). (2) object-readonly write `o.b=` → existing `isReadonlyPropertyAccess`+TS2540 ALREADY works IF `o`'s recorded type is the readonly Type.Object — verify `currentLocalTypes` records the freeze return. (3) **NEW (the hard/broad piece):** an element-access WRITE to a `ReadonlyArray` receiver → TS2542 "Index signature in type 'readonly number[]' only permits reading." — TS2542 currently fires ONLY in the delete-operator context (Checker.kt ~45788/45874), NOT for `a[0]=`; adding a general element-access-write readonly check is BROAD (element writes are everywhere) → gate tightly to a ReadonlyArray receiver + full-suite-verify. (4) element-type TS2322 for `a[0] = a[2].toString()` (string vs number) on the readonly array. objectFreeze needs ALL of 1–4; objectFreezeLiteralsDontWiden needs 1 + non-widening of `let x = frozen.prop` + the literal-assignment TS2322. RISK: MED-HIGH (piece 3 is broad). Backpointer: round-167 STATUS note; the round-167 workflow rated both FLIP_NOW but under-counted the coupling.

- [ ] **B436-CROSS-FILE-ARITY-EXTEND (NOTED 2026-06-15 round 167 — Blocker #3 beachhead from B434).** B434 landed cross-file function-call arity (TS2554/TS6210) via a program-wide `crossFileFuncs` overlay in `checkArgumentCounts` (gated: NON-module files only; callee absent from own funcParams AND binder locals; cross-file dup names skipped; JS-param optionality JSDoc-`@param`-aware via `jsDocRequiredParamNames`). The overlay currently covers TOP-LEVEL FunctionDeclarations + the TS2554-too-few path only. Extend opportunistically when a failing test needs it: TS2555 (rest-param too-few) cross-file related-info (mirror the `relSource`/`relFileName` threading into `emitTS2555TooFew`); cross-file CLASS constructor arity (`new X()` where X is in another script file); cross-file var-assigned-arrow callees. NO known failing-test consumer right now (B434's full-suite diff flipped ONLY its target) — do NOT build speculatively.

---

### STRATEGIC MAP of the remaining failures (2026-05-30, pipeline-optimization session)

**The "surgical pool exhausted → only architectural blockers left" framing that
drove ~20 rounds of recon-only / +1 sessions was a TOOL ARTIFACT, not reality.**
`find_candidates.py` only ever scanned tests that already emit a parseable
`error TSxxxx` line (the EXTRA/MISSING/SWAP buckets) — that slice is **33 of the
1014 current failures**. The tool was blind to the other ~70%. It has now been
extended (commit `tooling(queue): un-blind …`) with two new buckets. Full map of
the 1014 failures (from the 2026-05-30 fresh XML set):

| bucket | count | what it means | tool flag |
|---|---|---|---|
| **NONE-PRODUCED** | **611** (590 fresh) | baseline expects errors, we emit nothing | `--none` |
| error-baseline diff (EXTRA/MISSING/SWAP) | 307 (33 in ≤3-code window) | we emit something, wrong/incomplete | default |
| **OUTPUT** (JS/decl/sourcemap) | 105 (≈95% already skip-logged) | pure output diff, no codes | `--output` |

**NONE-PRODUCED is the path to 100%.** It is dominated by the hard type-engine
families (TS2322 ×91, TS2345 ×38, TS2339 ×37 — these need the assignability /
generic-inference / cross-file-scope blockers), BUT it has a large **BOUNDED
tail that is genuinely surgical and was never triaged**:

- **TS2307 ×14 (module resolution)** — e.g. `es6ExportAll` does `import … from 'server'`
  where `server.ts` exists but the specifier is BARE (non-relative, not a
  node_modules package); TS fires TS2307, we wrongly resolve it. Shared root
  cause across most of the 14 → likely one fix flips several. **Best first target.**
- **TS2403 ×9 (subsequent var decls must have same type)** — `promiseIdentity*`
  cluster (5) shares a Promise-interface-merge root; more type-engine-coupled.
- **TS2367 ×9 (comparison appears unintentional)**, **TS2688 ×5 (cannot find type-def file)**,
  **TS2540 ×5 (readonly assignment)**, **TS2554 ×4 (arg count)**, **TS2344 ×4**,
  **TS4023/TS4025 (decl emit)**, **TS2305/TS2649/TS2551/TS2552 …** — each a
  bounded check, 2–3 expected error lines, mostly fresh.

**Process rule going forward:** run `find_candidates.py --none --fresh` (and
`--code TSxxxx` to focus) BEFORE ever declaring the pool dry. A session is only
"pool exhausted" when EXTRA/MISSING/SWAP **and** the bounded NONE tail **and**
OUTPUT are all dry — which is not the case today.

**STALENESS / ARCHIVE NOTE (2026-06-10):** the counts above are the 2026-05-30 snapshot (1,014 failing); current state is 567 failing / 9,519 passing — regenerate the bucket map with `find_candidates.py --fresh`, `--none --fresh`, and `--output --fresh` before relying on any per-code count. The per-test enumerations and all completed `- [x]` strategic items were moved to `PLAN-PHASE-4-HISTORY.md` (§ "STRATEGIC MAP — archived entries"). Only still-open strategic items are kept below.

- [ ] **B157-GENERIC-CALLEE-RETURN-MISMATCH (PROMOTED 2026-06-09 round 129 — Blocker #2; round-129 triage CAVEAT: NOT a clean slice, see FINDING at end of this item; the bounded surgical pool is otherwise EXHAUSTIVELY architectural this round).** Target `fixingTypeParametersRepeatedly2` (+1, 2 errors, NO best-common-type needed — T is inferred from a single non-array arg, so it avoids the `genericTypeArgumentInference1` union-BCT complication). Shape: `declare function foo<T>(x: T, func: (p: T) => T): T; var result = foo(derived, d => d.toBase());` where `derived: Derived` (Derived extends Base, has `toBase(): Base`). Need: (1) infer `T = Derived` from arg1 `x: T` (the `tryInferSingleTypeParamFromArgs` infra at Checker.kt:~43925 already does single-arg inference); (2) contextually type the arrow `d => d.toBase()` with `d: Derived` (the B81.1b contextual-param-inference path in `checkPropertyAccessInExpr` FunctionExpression branch ALREADY does this — verify it fires for the 2nd arg here); (3) infer the arrow's RETURN type = `Base` (from `d.toBase()`), substitute T=Derived into the expected return `(p:T)=>T` -> expected return `Derived`, check `Base` assignable to `Derived` -> **TS2741 "Property 'toBase' is missing in type 'Base' but required in type 'Derived'."** at the arrow's return position. PLUS the redeclared `var result` (Base vs Derived) -> **TS2403** (the existing `ts2403Identical` + `checkVarDeclTypeConsistency` path — `result`'s first decl is `foo(...)`=`Derived`, 2nd is `bar(...)`=`Base`... actually both call exprs; verify what `result`'s inferred type is). RISK: MEDIUM (generic-inference is net-zero-prone; full-suite + exact failing-set diff REQUIRED, revert budget). START by tracing whether pieces (1)+(2) already produce `d: Derived` (add a marker diag); the missing piece is likely (3) the arrow-return-vs-substituted-T assignability emission. If `fixingTypeParametersRepeatedly2` proves entangled, the fallback Blocker target is the decl-emit nameability CLUSTER (TS2883/TS4023/TS4025/TS2527/TS4118/TS7056, ~8-10 tests sharing a "cross-file inferred-type nameability" root) — higher yield but needs a portability/nameability model (architectural, multi-session). **ROUND-129 FINDING (read before attempting):** this TS2741 is a return-type mismatch through a GENERIC-INSTANTIATED callee `foo<T>`, which the round-79l gotcha (CLAUDE.md "Contextually-typed-arrow return-mismatch") EXPLICITLY documents we keep as COARSE whole-arg TS2345 rather than the arrow-return elaboration — piece (4) requires lifting the `!calleeGenericInstantiation` gate AND producing the missing-property (TS2741) variant. PLUS the 2nd error (TS2403 `result` Base-vs-Derived) depends on the subtle "fixing type parameters repeatedly" overload re-inference (bar() yields a DIFFERENT T than foo()). So it is a 2-hard-piece test. Prefer instead either (a) a simpler Blocker #2 test where T is inferred from one arg and a SECOND non-callback arg fails the instantiated param (no contextual-arrow-return), or (b) the decl-emit nameability cluster. Trace with marker diagnostics before committing.

- [ ] **B131-NEXT-CANDIDATES — ROUND-122 /loop FIRING-2 UPDATE (2026-06-08): both targets RE-CONFIRMED 2-piece/hard via code-path tracing; do NOT re-triage as contained.** `recursiveBaseConstructorCreation3`: TS2314 doesn't fire because the ClassDeclaration heritage walk in `checkUnresolvedNames` doesn't reach top-level `declare class` heritage (un-gating is FP-risky AND has NO other consumer — zero currently-failing tests need declare-class-heritage TS2314), AND the TS2351 construct-sig cascade through the invalid recursive generic base is a SEPARATE hard piece → not a clean win. `duplicateErrorClassExpression`: TS2538-typeof-class is a cheap B127 extension but the TS2416 index-signature-VALUE-type override comparison (named→named, canUseTypeEngine-blocked) is the blocker → 2-piece. **ALSO traced this firing (all multi-link Blocker #2, NOT contained):** `unknownSymbolOffContextualType1` (`elements.filter(...).map(e=>e.xyxyxyx)`) — `checkPropertyAccessInExpr`'s FunctionExpression branch DOES do contextual-param inference (B81.1b, ~73849), so the break is UPSTREAM: `elements = elementNames.map(name=>getElementById(name))` must resolve to `HTMLElement[]` (B83.4i map-return inference flowing into the var-decl type) AND `filter` must preserve `T` (`HTMLElement[]`) — a multi-link map→var→filter→map chain. `knockout` (callable-interface return), `inexistentPropertyInsideToStringType` (JS-`this`-expando). OUTPUT bucket (41 JS/decl diffs) = finicky emitter / parser-recovery / module:preserve / System-module (tsgo-irrelevant or architectural). **CONCLUSION: the bounded clean pool is EXHAUSTED after +4 (B127-B130). The next firing/session MUST commit to a Blocker BUILD (not re-triage): highest-yield is Blocker #2's map-return-inference-into-var-decl-types (make `arr.map(cb)` resolve to `R[]` and flow `R[]` into the inferred var type, so chained `.filter`/`.map` callback params type correctly) — BROAD/net-zero-prone, REQUIRES full-suite + HEAD-failing-set-diff with a revert budget. This is a dedicated-session task, not an autonomous-loop-tail task.** ORIGINAL PROMOTION (2026-06-08 round 122 /loop firing-1): concrete targets so the next firing does NOT recon-loop; the bounded clean-MISSING-HANDLER pool is otherwise exhausted after +4 B127-B130. Two contained-but-2-piece candidates worth a fresh-budget attempt (each needs BOTH pieces to flip; revert budget): **(1) `recursiveBaseConstructorCreation3`** (TS2314 + TS2351) — `declare class xyz extends abc` where `abc` is `declare class abc<T>` (generic, used with NO type args) → TS2314 "Generic type 'abc<T>' requires 1 type argument(s)." at the `abc` in xyz's heritage (baseline 6,27); PLUS `new xyz()` → TS2351 "This expression is not constructable. Type 'typeof xyz' has no construct signatures." (9,15). `checkHeritageTypeArgCount` (Checker.kt:28890) WOULD emit TS2314 (gate: `scope.has(name)` + `getTypeParamInfo(name).maxTotal>0` + providedCount≠maxTotal) and IS called from the ClassDeclaration heritage walk (14813) — so the FIRST investigation is WHY it doesn't fire for this `declare class xyz extends abc`: likely either (a) the recursive cycle (`abc.foo: xyz`, `xyz extends abc`) makes `getTypeParamInfo("abc")` return null/0, or (b) the ClassDeclaration branch in `checkUnresolvedNames` skips ambient `declare class` heritage. Add a marker-diagnostic (code=29999) to confirm which. The TS2351 half (construct-sig resolution through an invalid recursive generic base) is the harder piece — verify it cascades once the base is recognized as broken, else it needs separate construct-sig-invalidation. **(2) `duplicateErrorClassExpression`** (TS2416 + TS2538) — needs TS2538-for-`typeof Class`-index (extend B127's TS2538-object-index from `Type.Interface` to constructor-typed values) + TS2416 index-signature-VALUE-type override mismatch (`ComplicatedTypeDerived{[s]:ADerived}` not assignable to `ComplicatedTypeBase{[s]:ABase}` — ADerived missing `a`); the TS2416-index-sig-value-comparison is the hard piece (named→named blocked by canUseTypeEngine; may need the missing-property subset for index values). If BOTH 2-piece candidates prove too entangled, pivot to a Blocker: B100 utility-type (Readonly past the no-Reference gate), or Blocker #2 callback-RETURN inference for `arr.map(cb)`→`U[]` (broad, net-zero-prone — full-suite + HEAD-diff REQUIRED). Do NOT recon-only: pick ONE and build with a revert budget.

- [ ] **B100-UTILITYTYPE-MATERIALIZE (PROMOTED 2026-06-06 round 107 — contained type-engine blocker; backpointer: round-107 skip-log).** **ROUND-119 PROGRESS (2026-06-08, B120): `Readonly<T>` MATERIALIZATION LANDED (+1, flips `readonlyAssignmentInSubclassOfClassExpression`).** New `materializeModifierUtility` (Checker.kt ~59967) copies T's member Symbols + marks them readonly via the `mappedReadonlyMemberIds` side-channel (consulted by `isReadonlySymbol`), wired in `getTypeFromTypeReference` (`symbol==null && name=="Readonly"`, arity 1); same gates as B116 (concrete plain `Type.Object` T, no Reference/index-sigs). Companion pieces: `getTypeFromBaseTypeExpression` resolves `extends (X as new()=>T)` cast-mixin → construct-sig return type; `isReadonlyPropertyAccess` types `this.X` via `currentClassForThis` (now set by the readonly walker), gated to INHERITED members (own-readonly-in-ctor is legal). See CLAUDE.md B120 gotcha. **ROUND-120 FINDING (2026-06-08, B125 — ATTEMPTED + REVERTED, net −2):** implemented exactly the planned `Partial`/`Required` materialization — `materializeModifierUtility` extended to all three names, `mappedOptionalMemberIds`/`mappedRequiredMemberIds` side-channels consulted by `isOptionalProperty` (Required overrides → false, Partial → true), wired into `getTypeFromTypeReference`. Full suite: **ZERO flips, −2 regressions** (`deleteExpressionMustBeOptional_exactOptionalPropertyTypes` BOTH variants). Root cause of the regression: `isOptionalProperty` is consumed by the TS2790 "delete operand must be optional" check (and others) — making it side-channel-aware changed delete-expression behavior even though NO `Partial<>` was involved in those tests (the side-channel sets were empty for them, so this was likely an interaction with how the materialized objects flow, OR a subtler `isOptionalProperty` consumer). NET: **`Partial`/`Required` materialization has NO clean consumer in the current failing set AND the `isOptionalProperty` side-channel has FP surface — do NOT re-attempt without first finding a concrete single-test consumer AND auditing every `isOptionalProperty` caller.** The remaining B100 targets (`multipleBaseInterfaesWithIncompatibleProperties2` TS2320) need Partial + exactOptionalPropertyTypes value-type semantics + multi-base identity comparison (3+ pieces) — defer. **STILL TODO (lower priority): broadening `Readonly` past the no-Reference gate (named-interface / generic-instantiation args).** Original plan below. Materialize the homomorphic utility types **`Readonly<T>` / `Partial<T>` / `Required<T>` / `Pick<T,K>` / `Omit<T,K>`** as real mapped-type expansions that PRESERVE/modify member modifiers (readonly, optional). Today `getTypeFromMappedType` (Checker.kt:~83340) mints fresh member Symbols with NO declarations, so `isReadonlySymbol` (~36282, checks `symbol.declarations` for a Readonly-modified PropertyDeclaration) returns false → the readonly flag is lost. **Unblocks** ~~`omitTypeHelperModifiers01`~~ (ALREADY PASSES as of round 123 verification — B116 Omit-materialize + B120 readonly + B117 param-receiver already cover the TS2540 `x.c = …` via `Omit<A,'a'>`; round-107 skip-log was stale), `readonlyAssignmentInSubclassOfClassExpression` (TS2540 via `Readonly<{attrib}>` heritage-from-cast — also needs AsExpression base-type resolution in `getDeclaredTypeOfClassOrInterface`'s heritage path), and likely a broader TS2540/TS2353/TS2741 cluster. Carry a side-channel "is-readonly"/"is-optional" flag on the synthesized member Symbol (not via declarations) and consult it in `isReadonlySymbol` + the optional checks. RISK: MEDIUM-HIGH — mapped-type member synthesis feeds assignability broadly; full-suite + HEAD-diff each step. **ROUND-113 FINDING (2026-06-07): the TRUE first blocker is that `Omit`/`Pick`/`Exclude`/`Readonly`/`Partial`/`Required`/`Record` are ABSENT from the embedded lib (`BUILTIN_LIB_SOURCE`, grep confirms zero `type Pick`/`type Omit`/etc.).** So `Omit<A,'a'>` resolves to errorType/anyType → NOTHING materializes through `getTypeFromMappedType` → `x.c = …` on `omitTypeHelperModifiers01` emits nothing ("none produced", verified). The homomorphic-readonly-copy infra (a `!node.readonlyToken && node.type is IndexedAccessType` whose indexType names the mapped TP → copy `getPropertyOfType(objectType, key)`'s `isReadonlySymbol` onto a side-channel `mappedReadonlyMemberIds` set consulted by `isReadonlySymbol`) was WRITTEN + reverted this round (net-0, no consumer, FP-safe but unreachable — B87.5 no-zero-consumption precedent). So B100 is a 3-step blocker: **(step 1)** add the utility-type definitions to the embedded lib — a BROAD-RISK lib-content change (they currently resolve to anyType which SILENTLY suppresses many diagnostics corpus-wide; adding real defs may expose latent gaps → regressions; needs a dedicated full-suite-verified session); **(step 2)** verify `Omit`/`Pick` materialize through the alias-substitution → `getTypeFromMappedType` chain (keyof/Exclude key-enumeration); **(step 3)** re-apply the readonly-copy (and add the `?`/optional analog). The homomorphic gate (copy only when `!readonlyToken`, because the parser sets `readonlyToken` for `-readonly`/`+readonly`/plain alike — can't distinguish sign) is the load-bearing FP firewall for step 3. **ROUND-114 FINDING (2026-06-07): step 1 RE-SCOPED — adding the lib definitions is INERT, not broad-risk.** I added `type Partial<T>=…; type Readonly<T>=…; type Pick/Record/Exclude/Extract/Omit/Required/NonNullable` to `BUILTIN_LIB_SOURCE` and ran the full suite: **EXACTLY net-zero — zero flips AND zero regressions, identical 782-failing SET (verified by before/after failing-set diff, not just count).** Root cause: the utility-type names are intercepted UPSTREAM as **display-only stubs** at **Checker.kt:~27649** (a map `"Partial" to (1 to "Partial<T>")`, `"Readonly" to (1 to "Readonly<T>")`, `"Pick" to (2 to "Pick<T, K>")`, `"Omit" to (2 to "Omit<T, K>")`, …) consulted by `getTypeFromTypeReference` — they resolve to errorType/anyType with a synthetic DISPLAY string and NEVER consult any lib `type` alias. So the "broad-risk lib-content change" framing was WRONG: the definitions are harmless because they're unreachable. **The REAL step 1 is: change the stub map at 27649 to route these names to ACTUAL generic-type-alias resolution (the B50.1 `currentTypeAliasArgs` substitution path) against lib `type` definitions, THEN materialize the mapped/conditional bodies via `getTypeFromMappedType` (Partial/Readonly/Pick/Record/Required are mapped; Exclude/Extract/Omit/NonNullable are conditional + need keyof-enumeration).** THAT routing is where the regression risk actually lives (it makes `Partial<X>` a real type everywhere it's used, exposing mapped/conditional-resolution gaps). The lib definitions themselves were NOT committed (reverted — no-dead-infra rule). Also note `getTypeFromMappedType` is at Checker.kt:~83340 and there's an existing narrow TS2339 path for `g: Partial/Required/Readonly<T>` at Checker.kt:~74558 (16.4ed) to reconcile with.

- [ ] **B96-LIB-RISK (NOTED 2026-06-02 round 96 — HIGH-BLAST-RADIUS, defer): embedded `Promise<T>` member-set correction.** The embedded `Promise<T>` (Checker.kt:~26643) has `then/catch/finally` but lacks `[Symbol.toStringTag]`; the real ES2015 Promise (lib.es5 + lib.es2015.promise + lib.es2015.symbol.wellknown) member set differs. B94's property-`a` baseline line (`...missing the following properties from type 'Promise<number[]>': then, catch, [Symbol.toStringTag]`) is unreproducible without this. Editing the embedded lib shifts MANY Promise-related TS2322/TS2345 baselines → do NOT touch without a dedicated full-suite-verified session; treat as a lib-content change with broad regression surface. **Backpointer:** round-96 B94 deep-decomposition (the planning agent's "Substep E" prerequisite #2).

- [x] **B87.1 (CLOSED 2026-05-28 round 73, +2 net flips 9006 → 9008, flips both es5+es2015 variants of `doNotElaborateAssignabilityToTypeParameters_ts`). `Awaited<T>` + generic-union-return for async function bodies.** Solved via TWO landed pieces (B87.1b await-unwrap + a NARROW targeted async-generic-return emission), WITHOUT the broad (A) scope-push / (B) bare-TypeParam-relation changes originally feared. The narrow emission lives in `checkReturnAssignability` (Checker.kt:~49373): gated on `inAsyncFunctionBody` + `Promise<bareTP>` return annotation (TP name ∈ `typeParams`, no inner type args) + source is a `Type.Union` with ≥1 unconstrained-`Type.TypeParam` member named the TP AND ≥1 non-TypeParam non-any/error member; emits TS2322 at the `return` keyword (len 6) + the "could be instantiated with an arbitrary type" messageChain; display = non-TP members first then each TP member wrapped `Awaited<name>` → `Yadda | Awaited<T>`. Zero regressions (FP-surface tiny by construction). B87.1c's (A)/(B)/(C) pieces are now moot for this test (left documented below for any future async-generic test needing the GENERAL relation/scope fix). Target `doNotElaborateAssignabilityToTypeParameters_ts` (`async function foo<T>(x: T): Promise<T> { let yaddable = await getXOrYadda(x); return yaddable; }` where `declare function getXOrYadda<T>(x: T): T | Yadda`). Expected: TS2322 @ (3,3) on `return yaddable` — `Type 'Yadda | Awaited<T>' is not assignable to type 'T'.` + the EXISTING "'T' could be instantiated with an arbitrary type which could be unrelated to 'Yadda | Awaited<T>'." elaboration (17.11a infra). We currently emit NOTHING. Root-cause trace needed but the chain is: (1) `getXOrYadda(x)` must infer inner-T = outer-T and resolve to `T | Yadda` (NOT anyType — verify whether `getReturnTypeOfCallExpression` collapses the union-return to any when the arg is a bare TypeParam); (2) `await (T | Yadda)` must produce `Awaited<T> | Yadda` — `Awaited` is registered as a known name (Checker.kt:23563, `"Awaited" to (1 to "Awaited<T>")`) but NOT resolved as a conditional type; for a bare-TP operand TypeScript displays `Awaited<T>` literally (does NOT expand), so the await-transform need only wrap the non-promise-like bare-TP part as `Awaited<T>` and leave `Yadda` as-is; (3) the async return-assignability check (`Yadda | Awaited<T>` vs unwrapped return `T`) must fire TS2322 + the could-be-instantiated elaboration. **Decomposition** (land each net-zero/+flip independently, full-suite verify):
  - [x] **B87.1a (TRACE DONE 2026-05-28 round 73, net-zero — no code)** — temporary debug at the `checkReturnAssignability` type-engine gate (Checker.kt:~49392) on `*doNotElaborateAssign*es2015*` printed: `src=any[Intrinsic] tgt=Promise<T>[Interface] canUse=false relToTgt=true awaited=null`. **Findings (the gap is NOT one-line — it is 4+ coupled pieces):** (i) `sourceType` of `yaddable` resolves to **anyType** — `getXOrYadda(x)` (`<T>(x:T): T | Yadda`) generic-union-return inference collapses to `any` when the arg is a bare TypeParam, and `await` keeps it `any`; (ii) `targetType` = `Promise<T>` resolves to a bare **`Type.Interface`**, NOT a `Type.Reference` carrying the type arg `T`, so the EXISTING async-unwrap branch (Checker.kt:~49398, gated `targetType is Type.Reference && symbol.name=="Promise"`) never fires (awaited=null); (iii) consequently `canUseTypeEngine(any, ...)` is false → no diagnostic. So B87.1b must FIRST fix (i) the union-return inference AND likely (ii) make `getTypeFromTypeNode(Promise<T>)` produce a `Type.Reference`; THEN (iv) the residual relation question — `checkTypeRelatedTo(Yadda | Awaited<T>, bare T)` returns `true` today (bare unconstrained TypeParam target has apparent type `{}` → everything assignable), but TypeScript wants it FALSE with the "could be instantiated with an arbitrary type" elaboration (17.11a infra exists). Per the decision rule (multi-piece, no one-line gap) NO code landed; the trace re-scopes B87.1b/c below. **DO-NOT** broadly change the bare-TypeParam-target relation result — it would cascade through every internal generic-function assignment; the fix must be a narrow gate at the async-generic-return site only.
  - [~] **B87.1b (PARTIAL — await-unwrap LANDED 2026-05-28 round 73, net-zero, zero regressions; union-return inference still TODO).** Two sub-gaps remain from the B87.1a trace; the FIRST is now closed: `getTypeOfExpression(AwaitExpression)` was hardcoded `anyType` (Checker.kt:52636 `// TODO: unwrap Promise`). Replaced with: unwrap one level `Promise<X> -> X` (Type.Reference to Promise), non-Promise operands await to their own type, bare/unknown operands stay anyType (prior behavior, avoids cascade). Full suite 9006 unchanged (net-zero across the broad change — most async tests already tolerate the more-precise await type). This alone does NOT move `doNotElaborateAssignabilityToTypeParameters` because the operand `getXOrYadda(x)` STILL resolves to anyType (the union-return inference gap below). **REMAINING B87.1b:** preserve the `T | Yadda` union-return from `getXOrYadda<T>(x:T): T | Yadda` instead of collapsing to any when the arg is a bare TypeParam — find where `getReturnTypeOfCallExpression` (Checker.kt:54201) loses the union for a declared generic fn called with a bare-TP arg. RISK: MEDIUM (generic-union-return inference is consulted broadly — verify no TS2322/TS2345 cascade).
  - [ ] **B87.1c (re-scoped 2026-05-28 round 73 after the await-unwrap fix advanced the source).** Post-await-fix re-trace at the return site shows `src=T | Yadda[Union]`, `tgt=Promise<T>[Interface]`, `canUse=false`, `relToTgt=false`. So the SOURCE is now correct (only the `Awaited<T>` display differs — TypeScript shows `Yadda | Awaited<T>`). The remaining blockers are now precisely THREE coupled high-risk pieces:
    - **(A) `Promise<T>` resolves to a bare `Type.Interface`, not a `Type.Reference` carrying `T`.** Root cause: when `checkReturnAssignability` calls `getTypeFromTypeNode(returnTypeNode = Promise<T>)`, the function's own type param `T` is NOT in `currentTypeParamScope`, so `T` → errorType and the `Type.Reference` creation is skipped by the errorType guard (CLAUDE.md "getTypeFromTypeReference errorType guard"). Fix would push the enclosing function's `typeParameters` into scope before resolving the return-type node. RISK: HIGH — makes `Promise<T>` (and every other generic return annotation) a Reference broadly; cascades through return-assignability across the suite.
    - **(B) assign-to-bare-TypeParam must FAIL.** Even with (A), the async-unwrap branch would compare `T | Yadda` vs awaited `T`; `checkTypeRelatedTo(Yadda, bare T)` returns `true` today (apparent type `{}`), so no diagnostic. TypeScript wants FALSE + the "'T' could be instantiated with an arbitrary type" elaboration (17.11a infra exists). RISK: HIGH and explicitly DO-NOT broadly — cascades through every internal generic-function assignment. Needs a narrow gate at the async-generic-return site only.
    - **(C) `Awaited<T>` display** — render the bare-TP constituent of the awaited union as `Awaited<T>` (the name is registered at Checker.kt:23563) for the TS2322 message. Cosmetic; only matters once (A)+(B) emit.
    - **Conclusion:** B87.1's remaining work is (A)+(B)+(C), all coupled and (A)/(B) broad-cascade-risky. NOT a bounded surgical fix; needs a dedicated session that can absorb the full-suite regression triage from the scope + relation changes. The await-unwrap piece (B87.1b) was the one safely-landable increment.
  - **Sibling tests**: any async generic function whose body returns a value that doesn't structurally match the bare return TypeParam. Survey post-B87.1a.
  - **Backpointer**: round-72 STATUS "next-session: Blocker #2" recommendation; round-73 confirmed the surgical pool dry and B83.4 closed, so this is the promoted next concrete Blocker substep per the anti-loop rule.

### Completed items (16.0–16.3) — archived

Moved to `PLAN-PHASE-4-HISTORY.md` (2026-06-10) — § "Completed items (16.0–16.3)".

### Skip log (explored-but-skipped tests) — moved

The live skip log (`### Explored-but-skipped tests`, the section `find_candidates.py` reads its
`[SKIP]` token set from) now lives in `PLAN-PHASE-4-HISTORY.md`. Add new skip entries THERE,
above the `### End of skip log` terminator heading; suffix entries with `_ts`. The script reads
both files and unions the tokens, so entries in either file work.

### What's left in the "surgical fix" pool

**As of 2026-05-31 (post-round-85, 9098 passing) — UPDATED**: Round 84 (+16) and round 85 (+6) worked the bounded NONE/EXTRA/SWAP surgical tail across module-resolution, index-signature, readonly, and tslib-helper diagnostics. Round 85 landed B98.r20 (TS2413 number-vs-string index value), r21/r21b (TS7016 + untyped-JS-sibling TS2307 suppression), r24 (TS2540 intersection-property writes), r25 (TS2430 multi-base inherited index-sig), r26 (TS2343 `__awaiter`). **The bounded surgical tail is now genuinely exhausted** — the round-85 triage (logged under "Explored-but-skipped → Round 85 triage skips") classified every remaining ≤2-error NONE candidate as needing one of: control-flow narrowing (Blocker #1: TS2488/TS2532/TS18048 `never`/possibly-undefined), cross-file/namespace scope (Blocker #3: TS2694/TS2503/TS2300-merge/TS2451-merge), type-engine assignability/generic-inference (TS2322 ×91 / TS2345 ×37 / TS2339 ×37 / TS2741 / TS2403), mapped-type-modifier resolution (TS2540 Omit/Readonly), module-resolution modeling (bare/symlink/untyped-module/typeRoots TS2307/TS2688 — bare-missing TS2307 ATTEMPTED round 85, reverted -6), decl-emit portability (TS2883/TS4032), or the FP-prone namespace-merge cross-block duplicate (`cloduleWithDuplicateMember2`). **Next session: commit to an architectural blocker substep** (Blocker #1 control-flow narrowing has the highest yield; Blocker #2 generic inference and Blocker #3 cross-file scope are the other two). Do NOT spend a session re-scanning the NONE tail expecting bounded wins — they are documented as exhausted.

**As of 2026-05-18 (post-B47.7, 8815 passing) — UPDATED**: After the B47.x series (MAINT-2 + B47.1–B47.7 + B47.5 foundation + B47.6 two-piece), the surgical pool at +1-per-commit is truly exhausted at the errors-baseline level (`find_candidates.py --fresh` returns 0/0/0). The JS-emit candidates pool has 108 entries; cataloguing in PLAN-PHASE-4.md confirms all are feature-scale (parser-recovery, private fields downleveling, `__setFunctionName`, `__rest` for CJS export-rest, type-display normalization for nullable targets, conditional type resolution, namespace-qualified union display, lib subsetting, module/file-ordering architectural blocker). Recent session B47.x landed +7 tests via narrow defensive-emit patterns; further +1 wins require feature-scale work or completing architectural blockers. Recommended next: tackle blocker substep per the architectural-blockers section.

**As of 2026-04-25 (post-16.4gs, 8337 passing) — RECOMMENDED SHIFT**: After 6 consecutive sessions confirming the surgical pool is empty (16.4go, post-16.4gk recon, post-16.4gl recon, 16.4gp, 16.4gq, post-16.4gr) plus this session's exhaustive re-audit of skip-list entries, the surgical pool is genuinely exhausted at the +1 to +3 per-commit level. **Next sessions should commit entirely to architectural blockers** per the new priority ordering (see "Known architectural blockers" below — reshuffled 2026-04-25). The recommended next step is **MAINT-1 (stale skip-log audit)**, then **Blocker #1 (full control flow narrowing)** — the latter has the highest single-feature yield estimate (~60–100 tests realistic, 250 upper bound).

**As of 2026-04-25 (post-16.4gi, 8323 passing)**: `find_candidates.py --fresh` returns 1 SWAP candidate (`newMap_ts`) which requires a multi-piece fix (TS2743 emission + lib-version-aware MapConstructor/WeakMapConstructor declarations). All other candidates are filtered as already-skipped. **Re-examining previously-skipped tests after each round of infrastructure changes is the highest-yield strategy now**: 16.4gi flipped ~~`typeCheckingInsideFunctionExpressionInArray_ts`~~ because 16.4ge's Variable+Interface merge made `Object` resolve as a real Type.Interface, which then revealed that `getReturnTypeOfNewExpression`'s Type.Interface branch returned `calleeType` directly (instead of consulting construct sigs for the "constructor interface" pattern). The same merge unblocks several other tests partially — e.g. ~~`booleanAssignment_ts`~~ now correctly types `b: Boolean` (after 16.4gi's `new X()` fix) but still has 2 FPs (primitive→wrapper assignability) + 1 missing chain (valueOf elaboration). Continuing to re-examine the skip log against the latest infrastructure is the recommended workflow until a coordinated multi-piece fix or blocker is undertaken.

**As of 2026-04-24 (post-16.4fs, 8304 passing)**: `find_candidates.py --fresh` still returns 0 across all buckets at diff ≤ 3 (filtered from 9/118/32). The 16.4fs win came from re-visiting a previously-skipped test (`pathMappingBasedModuleResolution_withExtension_failedLookup`) and landing a narrow TS2307 branch gated on "every paths target has an explicit `.ts`/`.tsx`/`.d.ts` extension" — narrower than the broad "paths config → skip TS2307" originally used. Each previously-skipped test is worth re-examining when its skip reason names a specific resolver limitation that a narrower gate could bypass. The 16.4fr win came from a non-obvious extension of an existing emission site (TS6212 branch) to cover an adjacent pattern, not from a fresh candidate — the tests it flipped (`optionalParamAssignmentCompat`, `functionSignatureAssignmentCompat1`) had 3-gap diffs (position + display + related info) that all resolved cleanly together. Similar "stacked gap" wins may still exist when multiple narrow fixes coincide. Otherwise next gains require either a blocker #1 sub-step (return-type-from-body broader patterns, or named→named cross-target comparison) or a coordinated new-diagnostic family implementation (TS6213 "Did you mean to use new?" at var-init, rootDir validation, iterator-protocol typing for TS2802/TS2495/TS2488).

---

After 2026-04-19 (16.4dm, 8227 passing — net-zero infra commit on top of 16.4dl), the low-hanging pool is empty. The 16.4di–dl line landed targeted wins on the inheritance-chain side of blocker #1 (generic property chain walk + TS2416 class-type-param scope + private-brand elaboration for TS2322/TS2345 + apparent-type constraint check for TS2344). 16.4dm extended step (c) to cover primitive-literal returns and added function-mismatch chain lines to property elaboration — both correct improvements that gate on per-property TS2322 emission to produce visible test gains. `find_candidates.py --fresh` still returns ~20 candidates; **every one remaining** is classified as a blocker or feature gap:
- Generic inference (Blocker #1): widenToAny1/2, declarationEmitBundleWithAmbientReferences, recursiveTypeRelations, lambdaArgCrash
- Cross-file scope (Blocker #5): jsFileCompilationLetDeclarationOrder2, jsFileCompilationDuplicateVariableErrorReported
- JSDoc (Blocker #2): jsExportMemberMergedWithModuleAugmentation, jsFunctionWithPrototypeNoErrorTruncationNoCrash
- Conditional types: conditionalAnyCheckTypePicksBothBranches
- Control-flow narrowing: narrowingUnionToNeverAssigment, optionalChainWithInstantiationExpression1 (es2019/es2020)
- Intersection privacy reduction: intersectionWithConflictingPrivates
- Display: errorWithSameNameType (cross-module qualified), deeplyNestedAssignabilityErrorsCombined (typeof-of-class-value), crashInEmitTokenWithComment (destructured arrow params)
- TS2859 complexity budget: relationComplexityError
- Standalone new diagnostic: importAliasFromNamespace (TS2845), taggedTemplatesWithIncompleteTemplateExpressions3 (TS2345 for tagged templates), importHelpersWithLocalCollisions (TS2354 for AMD/System tslib resolution)
- Structural elaboration: optionalPropertiesTest (optional→required), implementsIncorrectlyNoAssertion (TS2416 with intersection-of-classes)

The next +N gains either come from:
1. **Implementing new diagnostics** (TS6234 / TS2351 / TS2352 / TS2300-vs-lib / TS7023 / TS2802 / TS2744 / TS2845 / TS4081 / TS2354-AMD): each is 1-3 tests on its own but together could net +15-20. Each is self-contained (contained to a specific emission site) but individually small.
2. **Taking on a blocker** (structural generic comparison #1 for ~30+ tests, JSDoc #2 for ~5-10, etc.). Blocker #1 is by far the highest-yield single investment.

**Recommendation**: surgical pool is **exhausted**. Next session should commit to either (a) a new-diagnostic batch (pick 2-3 low-risk ones in a single session), or (b) blocker #1 (structural generic comparison) with the retry plan below. Continuing to look for +1 surgical wins via `find_candidates.py --fresh` will yield nothing.

### Known architectural blockers (RESHUFFLED 2026-04-25, 8337 passing)

These blockers recur across multiple "close-to-passing" tests and cannot be fixed with surgical changes in a single session. Any agent attempting these should plan for a multi-session investigation with regression budget.

**Priority ordering** — ranked by (expected tests unblocked) ÷ (regression risk × refactor scope). Higher rank = better cost/benefit. Reshuffled after the 16.4gq–gs session: the surgical pool has been ground down to zero across 5+ sessions, so the path forward is committing entire sessions to architectural work. Per the original Phase 16 impact analysis (line ~2479), the largest unaddressed feature is **control flow narrowing** (~250 tests potential, only partial done in 16.3) — it now sits at #1.

**What changed in this reshuffle:**
- **NEW #1**: Full control flow narrowing (was 16.3 PARTIAL — promoted to architectural blocker since the remaining work is full flow-graph construction, not surgical).
- **NEW #2**: Generic argument inference (was implicit under "16.4 generic instantiation" — promoted because tests like `genericConstraintSatisfaction1`, `superCallArgsMustMatch`, `genericClassWithStaticFactory`, `superWithTypeArgument3`, `privacyCheckAnonymousFunctionParameter2` all hit the same gap: we don't infer `T` from arg type then check the constraint).
- **#3 (was #5)**: Cross-file global scope conflation — bumped UP. Each session that does skip-log auditing finds more tests gated on this; estimated impact revised upward.
- **#4 (was #1)**: Structural comparison of generic refs — DEMOTED to LOW yield. Steps (a)–(d) infrastructure landed; the remaining gaps are bounded (different-target TS2352, overload-aware TS2416 for lib types, multi-statement return-from-body inference) and total ≤ 10 tests across all sub-cases.
- **#5–#7**: JSDoc, TS7006 over-suppression, Parser error-recovery — unchanged ordering, all LOW yield.
- **NEW MAINT-1**: Stale skip-log audit (16.4gp/gr/gs sessions all found stale entries — running this once could surface +5–15 candidates that are already passing).

---

#### 1. Full control flow narrowing — HIGHEST yield (~100+ tests realistic), MEDIUM risk

**Status (2026-04-25):** 16.3 landed surgical narrowing fixes (TS1344 message, TS2739/TS2740 multi-missing, null/undefined narrowing in `if (x == null)` branches, narrowing helper for variable declarations). Full flow-graph construction was deferred. The remaining gap is the dominant blocker for TS2339 (549 tests with this code), TS2454 (103 deficit), TS2774 (118 tests).

**Failing test patterns that need this:**
- ~~`nestedLoopTypeGuards_ts`~~ flipped 17.30a (flow-graph TS2454 body-recursion)
- ~~`typeGuardConstructorDerivedClass_ts`~~ flipped 17.5a (`x.constructor === Class` narrowing)
- ~~`narrowingUnionToNeverAssigment_ts`~~ flipped 17.1b (var-decl `never` target narrowing)
- ~~`narrowByEquality_ts`~~ (TS2839 narrowing — partially done in 16.4fy but limited)
- ~~`optionalChainWithInstantiationExpression1_ts`~~ flipped 17.44 (synthetic-paren marker for instantiation-expr-breaks-chain pattern)
- Many TS2774 "always defined" / TS2454 "used before assigned" with non-trivial control flow.

- **Yield**: 60–100 tests realistic (250 upper bound from impact analysis × ~30% realistic factor).
- **Scope**: build a real flow graph during binding (per-function basic blocks + branch edges), reify narrowed types per AST position. tsgo's `internal/checker/flow.go` (2.7K lines) is the reference implementation. This is INFRASTRUCTURE work — expect 2–4 sessions to land safely.
- **Risk**: MEDIUM. Narrowing is opt-in (a position with no narrow info falls back to declared type), so regressions are bounded to over-narrowing (false NEVER) rather than wholesale changes.
- **Why first now**: highest absolute yield. The original Phase 16 ordering put 16.3 fourth ("orthogonal but complex; defer to after easier wins") but the easier wins are now exhausted — narrowing IS the next big unlock.

#### 2. Generic argument inference (NEW) — HIGH yield (~20–40 tests), MEDIUM risk

When calling a generic function with concrete args, TypeScript infers each type parameter from the corresponding argument type, then checks each inferred T against its constraint. Currently we punt on this entirely — generic call sites with type-param-typed args either pass trivially (T = anyType) or skip the assignability check.

**Failing test patterns that need this:**
- ~~`genericConstraintSatisfaction1_ts`~~: `f<T extends S>(x: T)` called with `{s: 1}` against `S = {s: string}` — should emit TS2322 + TS6500.
- ~~`genericClassWithStaticFactory_ts`~~: `T` not inferred from arg → no TS2345.
- ~~`superCallArgsMustMatch_ts`~~, ~~`superWithTypeArgument3_ts`~~: `super(...)` arg checking when base is generic.
- ~~`privacyCheckAnonymousFunctionParameter2_ts`~~: function-typed parameter not structurally compared.
- ~~`widenToAny1_ts`~~, ~~`widenToAny2_ts`~~: `string | undefined` initializer widened wrong against `number` annotation.

- **Yield**: 20–40 tests. Each individual test gives +1, but the pattern repeats across many test files.
- **Scope**: extend `checkArgumentsAgainstSignature` and `checkArgumentsAgainstOverloads` to: (a) collect inferences from arg→param matching (each TypeParam → concrete type), (b) apply constraint check per TypeParam, (c) substitute inferred types into the param type and recheck. Touches the existing overload-resolution and constraint-checking paths.
- **Risk**: MEDIUM. Could regress overload-resolution tests if inference picks wrong overload. Mitigate with conservative gating (only when ALL TypeParams resolve to non-error types).
- **Why second**: stacks naturally on Blocker #4 step (a)/(b)/(c)/(d) infrastructure (Type.Reference interning, `resolveGenericPropertyType` mapper, `getPropertyTypeForRelation` cache) — the building blocks are already there.

#### 3. Cross-file global scope conflation — MEDIUM-HIGH yield (~30+ tests, was MEDIUM), HIGHEST risk

`Checker.init` does `mergeSymbolTable(globals, result.locals)` for every binderResult, merging module-file exports into the shared `globals` map alongside script-file locals and lib. In module files, identifiers from OTHER module files that weren't imported appear in `scope.has(name)` as if they were globals.

Blocks fine-grained module-visibility diagnostics: TS2301 vs TS2663 distinction, default-import-from-export-equals visibility, namespace export filtering across files.

**Failing test patterns:**
- ~~`classMemberInitializerWithLamdaScoping4_ts`~~ (TS2301 vs TS2663)
- `moduleAugmentationsImports3/4_ts` (TS2339 FP on nested module augmentation)
- ~~`errorsOnImportedSymbol_ts`~~ (cross-file type-only flagging)
- ~~`jsFileCompilationLetDeclarationOrder2_ts`~~, ~~`jsFileCompilationDuplicateVariableErrorReported_ts`~~
- Possibly contributes to many `genericSpecializations*`, `arrayAssignmentTest*` cross-namespace cases not yet diagnosed.

- **Yield**: 30+ tests. Each skip-log audit session surfaces more candidates blocked here.
- **Scope**: split "true globals" (script-file locals + lib + KNOWN_GLOBALS) from per-file module locals; reconstruct `fileScope` to only include imports the current file actually declares. Touches **every identifier-resolution check** in the checker (~30 call sites).
- **Risk**: HIGHEST. A naive implementation regresses 50+ tests. Recommended approach: introduce per-file-scope construction behind a flag, run side-by-side, then flip per file as the scope construction proves correct.
- **Why third (bumped from last)**: yield estimate revised upward after multiple skip-log audits found this blocker named in the residual; without addressing it, ~30 surgical-looking tests remain stuck.

#### 4. Structural comparison of generic type references — LOW yield (was MEDIUM), MEDIUM risk

**Status (2026-04-25, 8337 passing):** Steps (a), (b), (c) partial, (d) partial all landed across 16.4dc–df, dg, dm, gr, gs. Type.Reference interning, cycle/depth heuristic, TypeParam apparent-type comparison, `resolvedPropertyTypes` cache, heritage-clause type-arg instantiation, base-chain inheritance via `resolveGenericPropertyType`, parameter-mismatch chain elaboration, base-method TypeParam TS2208 — all in place.

**What's left (small, gated by other blockers):**
- ~~Different-target TS2352 with "Property X missing" elaboration (`genericTypeAssertions2_ts`)~~ — flipped 17.28 (narrow array-source → class-target case). Generalizing to all different-target casts still requires a TypeScript-style `comparable` relation with full structural elaboration.
- Overload-aware TS2416 for lib generic interfaces (`implementArrayInterface_ts` emits 2 extra diagnostics because lib's `every`/`filter` have type-predicate overloads that user code only implements one of).
- ~~Multi-statement bodies with single return for `inferSimpleReturnTypeFromBody`.~~ **Closed B4.1 (2026-05-13, +0 foundation)** — helper at Checker.kt:39694 now handles multi-statement bodies via top-level ReturnStatement collection.
- ~~Identifier returns (`return param`, `return this.field`) for return-type-from-body inference.~~ **Closed B4.2 + B4.3 (2026-05-13, +0 foundation)** — `is Identifier` arm extended to `return paramName` for annotated params; new `is PropertyAccessExpression` arm handles `return this.field` for class-property-typed fields (new `enclosingClass: Type.Interface?` parameter threaded from `resolveGenericPropertyType` caller at Checker.kt:39625).

- **Yield**: ≤ 10 tests across all sub-cases.
- **Scope**: contained to checker (no binder/parser changes).
- **Why demoted**: infrastructure work is largely done; remaining test gains are individual-test scale, not pattern-level. Best handled opportunistically when a specific case aligns with surgical session's budget.

#### 5. JSDoc type annotations — LOW yield (~5–10 tests), LOW risk

`@type {T}`, `@this {T}`, `@typedef`, `@param {T}` — we bind `@typedef` nominally but don't honor `@type` assertions or `@this` parameter types.

- **Yield**: 5–10 `.js` file tests.
- **Scope**: JSDoc parsing (scanner comment handling) and a new JSDoc-to-TypeNode bridge in the checker. Does not touch non-JS checking paths.
- **Why fifth (was second)**: low blast radius means low risk, but absolute yield is small. Higher-yield blockers (#1, #2, #3) take priority for "session-scale architectural work" budget.

#### 6. TS7006 over-suppression for callback parameters — LOW yield (~3–5 tests), MEDIUM risk

We suppress TS7006 for any `contextuallyTyped=true` param. TypeScript only suppresses when the contextual type actually provides param types (bidirectional inference succeeds).

- **Yield**: 3–5 tests (`intraBindingPattern`, `subtypeReduction`, `signatureCombiningRestParameters`).
- **Scope**: requires bidirectional inference plumbing in contextual typing.
- **Why sixth**: low yield + needs new infrastructure. Defer.

#### 7. Parser error-recovery asymmetry — LOW yield (1–2 per case), HIGH risk per attempt

Several tests expect specific token-consumption-then-continue recovery (e.g. `declare class foo();` → class with empty body + phantom `()` statement + next function).

- **Yield**: 1–2 tests per case, case-by-case.
- **Scope**: per-case parser edits.
- **Why last**: per-case work, no multiplier, regression-prone. Best handled opportunistically.

---

#### MAINT-1. Stale skip-log audit — UNKNOWN yield (likely 5–15 tests), LOW risk

Every recent session that audited the skip log found 1–3 entries marked "stuck on Blocker #X" that have actually been passing for sessions (e.g. 16.4gl-recon found `genericCloneReturnTypes`/`genericCloneReturnTypes2` stale; 16.4go found `generics4`/`genericDerivedTypeWithSpecializedBase`/`genericPrototypeProperty3` stale; this session found `genericPrototypeProperty3` again). The cumulative impact of stale entries blocking the candidate finder is probably 5–15 tests.

- **Yield**: unknown but likely 5–15 candidates that re-surface as fresh.
- **Scope**: walk every entry in the "Explored-but-skipped tests" section, run the test, mark stale (~), update reason if still failing.
- **Risk**: zero (read-only investigation).
- **Why a maintenance task**: not architectural but high-leverage. Should be run as a one-off "audit session" before any session that uses `find_candidates.py --fresh` for surgical work, or after every major architectural fix lands.

---

**Parallelism note**: Blockers #1, #2, #5, #7 can be worked independently. #3 should run after #1/#2 have resolved their fraction of overlapping cases (clearer "remaining failures pointing to #3" set). #4's remaining sub-cases pair naturally with surgical sessions — not a full-session investment. #6 depends on enough contextual-typing infrastructure that #2's inference work should land first.

**Recommended next sessions (in order):**
1. **MAINT-1** (1 session): clean up stale skip log → frees `find_candidates.py --fresh` to find genuine candidates.
2. **Blocker #1 step 1** (1–2 sessions): build flow-graph infra in binder; no behavior change yet.
3. **Blocker #1 step 2** (2 sessions): wire flow-graph into TS2454/TS2339/TS2774 emission sites.
4. **Blocker #2** (2 sessions): generic argument inference + constraint check.
5. **Blocker #3** (3+ sessions): per-file scope construction behind a flag, then per-file flip.

### Candidate-picking workflow for surgical fixes

When context starts a new session, the fastest path to wins is:

1. **Run full suite once** (4-6 min) to produce `build/test-results/jvmTest/*.xml`.
2. **Python-parse the XMLs** to find tests by diff size and pattern. The key filter is 1-2 extra diagnostic lines (too-aggressive checks) or 1 missing diagnostic at a specific position (simple checks to add). Example snippets live in this file's history — reuse them.
3. **Look for code-swap tests** (expected TS####A at position P vs actual TS####B at same position P) — these are often single diagnostic emissions at a specific code path that just need the code changed or position adjusted.
4. **Skip**: tests needing generic variance, module-visibility, JSDoc type handling, or full type inference — those are the architectural blockers above.
5. **Inspect `typescript-repo/tests/cases/compiler/<name>.ts` + `typescript-repo/tests/baselines/reference/<name>.errors.txt`** for context before implementing. Don't just look at the diff.

### Queue execution strategy

**Original Phase 16 sequence (DONE/IN PROGRESS):** 16.0 → 16.1 → 16.2 → 16.3 → 16.4

**Status:** 16.0/16.1/16.2 DONE, 16.3 PARTIAL (full flow-graph deferred → now Blocker #1), 16.4 IN PROGRESS (generic infrastructure largely done; remaining sub-cases moved to Blocker #4).

**NEW sequence (2026-04-25 reshuffle) — as the surgical pool is exhausted, commit entire sessions to architectural blockers:**

**Phase 17 sequence:** MAINT-1 → Blocker #1 (control flow narrowing) → Blocker #2 (generic argument inference) → Blocker #3 (cross-file scope) → Blocker #4 / #5 / #6 / #7 opportunistically.

**Rationale (priority-by-yield × tractability):**
1. **MAINT-1 first** — stale skip-log audit. Read-only, ~1 session, surfaces 5–15 already-passing tests as fresh candidates that would otherwise stay hidden behind `[SKIP]` markers in `find_candidates.py`. Cheap, no regression risk, and clears the deck for #1's diagnosis.
2. **Blocker #1 (control flow narrowing) second** — highest absolute yield (~60–100 tests realistic). The remaining failing tests across many categories (TS2339, TS2454, TS2774, TS2839) all converge on this. Build flow-graph in binder, wire into emitters in two stages.
3. **Blocker #2 (generic argument inference) third** — stacks on the 16.4 generic infrastructure already in place. ~20–40 tests. Lower risk than Blocker #3.
4. **Blocker #3 (cross-file scope) fourth** — high yield but highest risk; defer until #1/#2 have cleared their fraction so the residual is well-characterized.
5. **Blocker #4 (remaining structural generic gaps) opportunistically** — small (~10 tests), pairs with surgical sessions, not full-session work.
6. **Blockers #5/#6/#7** — low yield, defer.

**Hard rules:**
- Each feature must land behind a feature flag or with conservative guards initially.
- Run full suite after EACH sub-step to catch regressions early.
- If a feature causes >20 regressions, add more guards or narrow scope; don't revert without analysis.
- Document each partial implementation in CLAUDE.md gotchas.

**Per-session throughput — fix multiple items per session.** A single sub-step (e.g. 16.4h, 16.4i) is the unit of commit, not the unit of session. After committing + pushing one sub-step, loop back to pick up the next unchecked sub-step. Keep going until (a) the queue is empty, (b) the next item is genuinely blocked, or (c) context/time budget is nearly exhausted — then wrap up with a summary. Two-to-four sub-steps per session is a reasonable target when the items are small (+1 to +5 tests each); larger infrastructure items may consume a full session on their own. Always finish each sub-step cleanly (test suite green, commit + push) before starting the next — never bundle unrelated work into one commit.

**Realistic total gain:** 500-800 new passing tests → ~85-88% pass rate.

---

## Reference

- **tsgo source**: `github.com/microsoft/typescript-go` — `internal/checker/`
- **TS checker**: `microsoft/TypeScript` — `src/compiler/checker.ts` (53,296 lines)
- **Key tsgo files**: `checker.go` (31K), `relater.go` (5K), `types.go` (1.3K), `flow.go` (2.7K), `inference.go` (1.6K)
- **Parallelism model**: `internal/compiler/checkerpool.go` — N independent checkers, round-robin file assignment, shared immutable AST
